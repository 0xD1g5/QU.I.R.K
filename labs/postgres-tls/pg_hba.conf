# postgres-tls chaos-lab pg_hba.conf
# Allow SSL connections from anywhere with trust auth (lab-only fixture)
# TYPE  DATABASE  USER  ADDRESS      METHOD
local   all       all                trust
hostssl all       all   0.0.0.0/0    trust
hostssl all       all   ::/0         trust
