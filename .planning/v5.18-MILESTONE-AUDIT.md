---
milestone: v5.18
name: Migration Execution
status: passed
audited: 2026-09-03
phases: 5
requirements: 16
blockers: 0
---

# v5.18 Migration Execution — Milestone Audit

## Verdict: PASSED — 0 blockers

All 5 phases independently verified `passed`. All 16 requirements closed. Cross-phase integration
traced end to end with real code reads; the chain genuinely connects.

## Phase Verifications

| Phase | Name | Verification | Score |
|---|---|---|---|
| 177 | Release Toolchain Repair | passed | 4/4 |
| 178 | Finding Identity Repair | passed | 12/12 |
| 179 | Remediation Item Model | passed | 15/15 |
| 180 | Closure Verification | passed | 12/12 |
| 181 | Surfacing | passed | 15/15 |

Two verifications initially returned `gaps_found` and were resolved before close:
- **Phase 180** — the close-out ran three UAT gates but missed `test_uat_series_format.py`; its own
  commit had wrapped a `**Result:**` line, so its stated one-failure baseline was wrong. Verifier
  measured `2 failed`. Fixed, re-verified, raised to `passed` 12/12.
- **Phase 181** — a SUMMARY narrative error (claimed 34 collected cases; actual 16). Corrected;
  did not affect capability or the pass determination.

## Requirements Coverage — 16/16

| ID | Phase | Status |
|---|---|---|
| RELEASE-01/02/03 | 177 | Complete |
| IDENT-01/02/03 | 178 | Complete |
| REMED-01/02/03 | 179 | Complete |
| CLOSE-01/02/03 | 180 | Complete |
| SURF-01/02/03 | 181 | Complete |
| ADVISORY-01 | standing 177-181 | Complete — closed in 181-09 on re-verified guard evidence |

## Cross-Phase Integration — 8 links, all WIRED, 0 orphaned, 0 broken

1. **178 → 179 fingerprint identity** — `remediation_persist.py:254` calls
   `TicketingChannel.compute_fingerprint()`, the same shared staticmethod, not a re-implementation.
   The join key cannot drift because only one function produces it.
2. **179 → 180 closure join** — `compute_closure` never recomputes a fingerprint; it reads the
   persisted `finding_fingerprint` column for both scans.
3. **179 → 180 comparability** — `SCOPE_SIGNATURE_VERSION = "2.0.0"` defined once, imported by
   `closure.py`. A missing signature returns `(False, "missing_signature")` — never
   comparable-by-default.
4. **180 → 181 threading** — `closure_counters` passed at `run_scan.py:3727`; `writer.py` has zero
   call-sites for `compute_closure`/`scans_are_comparable` (names appear only in comments).
   `_SCAN_LEVEL_REFUSAL_KEYS` mirrors `COMPARABILITY_REASONS` with a parametrized no-drift test.
5. **180 → 181 CBOM VEX** — `_load_remediation_items` scoped to the current `scan_run_id`, returns
   `[]` on scan-level refusal; `_VEX_STATE_MAP` maps `not_observed→IN_TRIAGE`,
   `resurfaced→EXPLOITABLE` (distinct from `open`), `closed→RESOLVED`; unmapped → no entry.
6. **181 report parity** — `BURNDOWN_ADVISORY_CAPTION` byte-identical across all three renderers.
7. **181 dashboard** — no new endpoint; `roadmap.tsx` labels `not_observed` as
   **"Not verified this scan"**, never "Clean".
8. **ADVISORY-01 firewall** — two guards, AST + call-node level, `SCORE_WEIGHTS` asserted free of
   any closure/burndown/remediation-derived key.

Pipeline order confirmed in source: `db_persist → remediation_persist → scope_signature →
closure_verify → reporting`.

## Test Evidence

- Full suite: `1 failed, 4014 passed, 42 skipped, 73 xfailed, 4 xpassed` — sole failure is the
  carried `DEFER-172-01` (`tests/test_skip_registry.py::test_no_unregistered_skips`), v5.17 scope.
- Integration-relevant subset: 179 passed, 0 failed across 12 files; render sections 16 passed.
- All 6 UAT gates green; all 9 staleness catalogs green (including the new PQC deadline catalog).

## Accepted Limitations — bounded, documented, not blockers

| Limitation | Where recorded |
|---|---|
| Sensor-origin findings excluded from closure (`_ingest_envelope` sets no `scan_run_id`) | `docs/operators-guide.md` §15 + ROADMAP `## Backlog` |
| CNSA 2.0 deadline dates omitted (primary source HTTP 403) | `quirk/scanner/pqc_deadlines.py` module docstring |
| D-178-A wording divergence / D-178-B detection-coverage gap | `docs/reviews/178-derivation-path-divergence.md` |
| CDXA `declarations` deferred (no model module in cyclonedx-python-lib 11.7.0) | REQUIREMENTS.md SURF-01 |
| `DEFER-172-01` skip-registry line drift | carried v5.17; sole expected suite failure |

## Tech Debt Identified

- **Stale duplicate `writer.py` copies** under `.claude/worktrees/agent-*/` and `build/lib/`.
  Not an integration defect (canonical `quirk/reports/writer.py` verified), but worth a cleanup pass.
- **`gsd-sdk` `state.*` / `roadmap.*` verbs corrupt STATE.md** — nine occurrences across Phases
  179-181 (global substring replace; also stale-value regression). Executors adapted by hand-editing.
  Recorded in project memory.
- **`a11y-baseline-environment-mismatch`** — 33 a11y baselines generated on macOS, enforced on Linux
  CI; 31 never checked against the runner. Follow-up logged in Phase 177.

## Definition of Done

v5.18 set out to close the loop: track remediation items to completion across re-scans, showing
movement against the roadmap rather than only against the score. It did — and shipped the two
previously-unreleased milestones (v5.16, v5.17) as **v5.18.0**, the first published release since
5.12.0.

The standing constraint held throughout: **closure state is advisory-only and never feeds the
quantum-readiness score**, now machine-enforced across intelligence, report, CBOM, and dashboard
surfaces.
