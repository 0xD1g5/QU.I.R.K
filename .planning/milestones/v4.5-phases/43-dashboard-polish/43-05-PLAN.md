---
phase: 43-dashboard-polish
plan: 05
type: execute
wave: 5
depends_on: []
files_modified:
  - src/dashboard/tests/a11y/run-a11y.mjs
  - src/dashboard/src/pages/findings.tsx
  - src/dashboard/src/pages/identity.tsx
autonomous: true
gap_closure: true
requirements: [DASH-07, DASH-08]

must_haves:
  truths:
    - "npm run a11y:check exits 0 and every route's summary line shows PASS when there are no new violations, even if baseline violations exist"
    - "Findings page shows no pagination controls when the dataset fits on a single page"
    - "Identity page shows no pagination controls when the dataset fits on a single page"
  artifacts:
    - path: src/dashboard/tests/a11y/run-a11y.mjs
      provides: "Corrected summary using newViolations count instead of raw violations count"
      contains: "newViolationsCount"
    - path: src/dashboard/src/pages/findings.tsx
      provides: "Pagination bar guarded by table.getPageCount() > 1"
      contains: "getPageCount() > 1"
    - path: src/dashboard/src/pages/identity.tsx
      provides: "Pagination bar guarded by table.getPageCount() > 1"
      contains: "getPageCount() > 1"
  key_links:
    - from: "run-a11y.mjs line 206"
      to: "summary array"
      via: "newViolationsCount local"
      pattern: "newViolationsCount"
    - from: "findings.tsx pagination div"
      to: "table.getPageCount()"
      via: "JSX conditional"
      pattern: "getPageCount\\(\\) > 1"
---

<objective>
Close two UAT gaps from Phase 43 testing: (1) the a11y harness summary block incorrectly labels routes as FAIL based on raw axe violation count rather than baseline-delta count, and (2) pagination controls on Findings and Identity pages render as visually-absent disabled buttons on single-page datasets.

Purpose: Restore accurate a11y reporting and eliminate confusing phantom UI controls.
Output: Three files patched; a11y harness exits 0 with all-PASS summary; pagination bar absent on single-page datasets.
</objective>

<execution_context>
@/Users/digs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/digs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-dashboard-polish/43-UAT.md

<interfaces>
<!-- From src/dashboard/tests/a11y/run-a11y.mjs lines 178-206 -->
<!-- newViolations is computed at line 178 as a filter over results.violations.
     It is in scope for the per-route block but NOT stored for the summary push at line 206.
     summary.push currently uses results.violations.length (raw total).
     Fix: capture newViolations.length in a local before page.close(), push that value. -->

<!-- From src/dashboard/src/pages/findings.tsx lines 179-186 -->
<!-- Pagination bar:
     <div className="flex items-center justify-between text-sm text-muted-foreground mt-2">
       <span>Page {table.getState().pagination.pageIndex + 1} of {table.getPageCount()}</span>
       <div className="flex gap-2">
         <Button ... disabled={!table.getCanPreviousPage()}>Previous</Button>
         <Button ... disabled={!table.getCanNextPage()}>Next</Button>
       </div>
     </div>
     Fix: wrap entire div in {table.getPageCount() > 1 && ( ... )} -->

<!-- From src/dashboard/src/pages/identity.tsx lines 188-195 -->
<!-- Same pagination bar pattern as findings.tsx. Same fix applies. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix a11y summary to use baseline-delta count</name>
  <files>src/dashboard/tests/a11y/run-a11y.mjs</files>
  <action>
At line 206 the summary push uses `results.violations.length` (raw axe total). `newViolations` is computed at line 178 but falls out of scope before the push. Fix:

1. After the `newViolations` computation block (around line 182) and before the `unallowlisted` block, add a local:
   ```js
   const newViolationsCount = newViolations.length
   ```
   This must be inside the per-route block (`UPDATE_BASELINES` branch does NOT compute `newViolations`, so guard this line with a check: place it only in the `else` path that computes `newViolations`, OR initialize it as `0` before the baseline block and assign inside the `else` block).

   The cleanest approach: declare `let newViolationsCount = 0` immediately before the `existsSync(baselinePath)` block at line 165, then assign `newViolationsCount = newViolations.length` in the `else` block at line 191 (after `console.log` for the no-new-violations case), so `UPDATE_BASELINES` mode naturally leaves it as 0.

2. Change line 206:
   - Before: `summary.push({ slug, violations: results.violations.length, console: unallowlisted.length })`
   - After:  `summary.push({ slug, violations: newViolationsCount, console: unallowlisted.length })`

3. The summary print loop at line 215 checks `violations === 0` for PASS — this is now correct because `violations` in the summary object is the new-violations count, not the raw total.

Do NOT change the per-route console output at line 184/192 — those already use `newViolations.length` correctly. Only the summary push at line 206 changes.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK/src/dashboard && grep -n "newViolationsCount" tests/a11y/run-a11y.mjs</automated>
  </verify>
  <done>
`grep` returns at least two lines: the `let newViolationsCount = 0` declaration and the `summary.push` line referencing `newViolationsCount`. `results.violations.length` no longer appears in the `summary.push` call.
  </done>
</task>

<task type="auto">
  <name>Task 2: Guard pagination bar on single-page datasets (findings + identity)</name>
  <files>src/dashboard/src/pages/findings.tsx, src/dashboard/src/pages/identity.tsx</files>
  <action>
In both files, the pagination `<div>` renders unconditionally. With `pageSize=25` and fewer than 25 rows, `table.getPageCount()` returns 1 and both buttons render in a disabled state that looks absent but is technically present, causing confusion.

**findings.tsx (lines ~179-186):**
Wrap the outer pagination `<div>` in a conditional:
```tsx
{table.getPageCount() > 1 && (
  <div className="flex items-center justify-between text-sm text-muted-foreground mt-2">
    <span>Page {table.getState().pagination.pageIndex + 1} of {table.getPageCount()}</span>
    <div className="flex gap-2">
      <Button variant="outline" size="sm" onClick={() => table.previousPage()} disabled={!table.getCanPreviousPage()}>Previous</Button>
      <Button variant="outline" size="sm" onClick={() => table.nextPage()} disabled={!table.getCanNextPage()}>Next</Button>
    </div>
  </div>
)}
```

**identity.tsx (lines ~188-195):**
Apply the identical wrap. The existing outer guard `{identityFindings.length > 0 && ...}` is unrelated — this pagination guard is a separate inner change within that block.

No other changes to either file. Do not modify column definitions, table setup, or any other JSX.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK/src/dashboard && grep -c "getPageCount() > 1" src/pages/findings.tsx src/pages/identity.tsx</automated>
  </verify>
  <done>
Each file reports count >= 1 for the `getPageCount() > 1` pattern. TypeScript build passes: `npm run build` from `src/dashboard/` exits 0 with no type errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → axe-core output | Harness reads axe results; no user data crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-05-01 | Tampering | run-a11y.mjs summary logic | accept | Test harness internal — no external input; fix is purely cosmetic (count source change) |
| T-43-05-02 | Denial of Service | Pagination conditional | accept | Client-side React render guard; no server surface |
</threat_model>

<verification>
1. `cd src/dashboard && grep -n "newViolationsCount" tests/a11y/run-a11y.mjs` — shows declaration and usage
2. `grep "results.violations.length" tests/a11y/run-a11y.mjs` — must NOT appear in the summary.push line
3. `grep -c "getPageCount() > 1" src/pages/findings.tsx src/pages/identity.tsx` — each returns 1
4. `npm run build` from `src/dashboard/` exits 0
</verification>

<success_criteria>
- a11y harness: summary block emits PASS for any route with 0 new violations, regardless of how many baseline violations exist
- Findings page: pagination bar is absent (not rendered) when dataset has 25 or fewer rows
- Identity page: pagination bar is absent (not rendered) when dataset has 25 or fewer rows
- TypeScript build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/43-dashboard-polish/43-05-SUMMARY.md`
</output>
