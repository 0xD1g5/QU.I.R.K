---
phase: 43-dashboard-polish
plan: 03
type: execute
wave: 2
depends_on: [43-01]
files_modified:
  - src/dashboard/src/components/sidebar.tsx
  - src/dashboard/src/index.css
autonomous: true
requirements: [DASH-03]
tags: [react, a11y, focus, contrast, css-tokens, sidebar]

must_haves:
  truths:
    - "Sidebar Link elements receive a visible focus ring (Tailwind focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2) when focused via keyboard Tab"
    - "No new hardcoded hsl() literals are introduced — any contrast adjustment is made via CSS variables in index.css per Phase 39 D-13"
    - "Color tokens used by severity badges (CRITICAL, HIGH, MEDIUM, LOW, INFO) pass WCAG AA contrast against both light and dark theme backgrounds — verified by axe color-contrast rule"
  artifacts:
    - path: "src/dashboard/src/components/sidebar.tsx"
      provides: "Sidebar with focus-visible ring on Link primitives (router Links lack default rings)"
      contains: "focus-visible:ring-2"
    - path: "src/dashboard/src/index.css"
      provides: "CSS-variable token system; any contrast token adjustments live here, never inline"
  key_links:
    - from: "src/dashboard/src/components/sidebar.tsx"
      to: "Tailwind focus-visible utility classes"
      via: "className concatenation on Link"
      pattern: "focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2"
---

<objective>
Close the focus-indicator and color-contrast portions of DASH-03 without touching page files (so this plan can run in parallel with Plan 02 in Wave 2).

1. Add Tailwind `focus-visible:` ring utilities to the sidebar `<Link>` primitives, since React Router's Link does not ship the shadcn focus-ring convention by default.
2. Run a baseline contrast audit on the existing CSS token palette and adjust the lowest-contrast severity tokens (most likely `LOW` per Risk Register row 3) ONLY IF axe `color-contrast` flags them — adjustments live in `index.css` CSS variables, never as inline `hsl()` literals (D-18).

Purpose: Closes DASH-03 keyboard-focus and contrast SCs in isolated files (sidebar + index.css) so it does not collide with Plan 02's page sweep.

Output: Sidebar Links have visible focus rings on Tab; index.css token map is updated only if needed for contrast compliance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/43-dashboard-polish/43-CONTEXT.md
@.planning/phases/43-dashboard-polish/43-PATTERNS.md
@.planning/phases/43-dashboard-polish/43-01-SUMMARY.md

@src/dashboard/src/components/sidebar.tsx
@src/dashboard/src/index.css
@src/dashboard/src/pages/motion.tsx
@src/dashboard/src/pages/findings.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add focus-visible ring utilities to sidebar Link primitives</name>
  <files>src/dashboard/src/components/sidebar.tsx</files>
  <read_first>
    src/dashboard/src/components/sidebar.tsx,
    .planning/phases/43-dashboard-polish/43-PATTERNS.md
  </read_first>
  <action>
    Open `src/dashboard/src/components/sidebar.tsx`. Find the `<Link>` block (currently at lines 63-76 per PATTERNS.md) — the className uses `cn(...)` to compose existing utilities.

    Append three Tailwind `focus-visible:` utilities to the className argument list, AFTER the existing `transition-colors` and `min-h-[44px]` entries but as a new string in the `cn(...)` call. The shadcn convention is:
    ```
    "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2"
    ```

    Concrete edit — change from:
    ```tsx
    <Link
      to={path}
      aria-label={label}
      className={cn(
        "flex items-center gap-3 px-2 py-2 rounded-md text-sm transition-colors",
        "min-h-[44px]",
        isActive
          ? "text-foreground border-b-2 lg:border-b-0 lg:border-l-2 border-accent bg-accent/10"
          : "text-muted-foreground hover:text-foreground hover:bg-accent/5",
      )}
    >
    ```
    to:
    ```tsx
    <Link
      to={path}
      aria-label={label}
      className={cn(
        "flex items-center gap-3 px-2 py-2 rounded-md text-sm transition-colors",
        "min-h-[44px]",
        "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2",
        isActive
          ? "text-foreground border-b-2 lg:border-b-0 lg:border-l-2 border-accent bg-accent/10"
          : "text-muted-foreground hover:text-foreground hover:bg-accent/5",
      )}
    >
    ```

    DO NOT change any other element in the file. The shadcn primitives elsewhere (Button, Input, Tabs from radix, etc.) already include focus-visible rings; sidebar Link is a router-router primitive that needs explicit utilities (per CONTEXT D-15 and Risk Register row 6).

    If there are additional non-shadcn interactive elements in sidebar.tsx (e.g., a collapse-toggle button outside the radix system), apply the same focus-visible string to those className arguments. Do NOT add focus styles to elements that already inherit them from shadcn.

    Decisions implemented: D-15 (focus-visible utilities on custom elements only), D-16 (sweep verified by axe focus rules — this task adds the styles; verification is in Plan 04's a11y:check run).
  </action>
  <verify>
    <automated>cd src/dashboard && grep -c "focus-visible:ring-2" src/components/sidebar.tsx && grep -c "focus-visible:ring-ring" src/components/sidebar.tsx && grep -c "focus-visible:ring-offset-2" src/components/sidebar.tsx && grep -c "focus-visible:outline-none" src/components/sidebar.tsx && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "focus-visible:ring-2" src/dashboard/src/components/sidebar.tsx` returns at least 1
    - `grep -c "focus-visible:ring-ring" src/dashboard/src/components/sidebar.tsx` returns at least 1
    - `grep -c "focus-visible:ring-offset-2" src/dashboard/src/components/sidebar.tsx` returns at least 1
    - `grep -c "focus-visible:outline-none" src/dashboard/src/components/sidebar.tsx` returns at least 1
    - `grep -c "<Link" src/dashboard/src/components/sidebar.tsx` returns the same count as before this change (no Links accidentally removed)
    - `cd src/dashboard && npm run lint` exits 0
    - `cd src/dashboard && npm run build` exits 0
    - Manual: load preview, Tab through sidebar, each Link shows a visible ring outline (verified in Plan 04's full sweep).
  </acceptance_criteria>
  <done>
    Sidebar Link className includes the four focus-visible utilities (outline-none, ring-2, ring-ring, ring-offset-2); no other elements modified; lint + build clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Baseline severity-badge contrast audit; adjust CSS variable tokens only if axe flags violations</name>
  <files>src/dashboard/src/index.css</files>
  <read_first>
    src/dashboard/src/index.css,
    src/dashboard/src/pages/motion.tsx,
    src/dashboard/src/pages/findings.tsx,
    src/dashboard/src/pages/data-at-rest.tsx,
    .planning/phases/43-dashboard-polish/43-PATTERNS.md,
    .planning/phases/43-dashboard-polish/43-RESEARCH.md
  </read_first>
  <action>
    1. **Run a contrast probe before changing anything.** With Plan 01's harness in place, execute:
       ```bash
       cd src/dashboard && npm run build && VITE_A11Y_FIXTURE=1 npm run a11y:check 2>&1 | tee /tmp/contrast-probe.log
       ```
       Search the output for `color-contrast` violations on `/findings`, `/motion`, `/data-at-rest`. Capture each offending selector and the computed foreground/background pair.

    2. **If axe reports zero `color-contrast` violations:** make no changes to index.css. The existing palette already passes WCAG AA. Document this finding in the SUMMARY and exit successfully — file `src/dashboard/src/index.css` is in `files_modified` only because we may need to touch it; if no violations exist, leave it untouched.

    3. **If axe reports `color-contrast` violations:** for each violation, identify the corresponding CSS variable in `index.css` (e.g., `--severity-low`, `--muted-foreground`) and adjust its lightness so the computed contrast ratio against the theme background is at least 4.5:1 for normal text and 3:1 for large text. Use the WebAIM contrast checker formula or axe's reported "expected contrast ratio" delta.

    Concrete examples:
    - `LOW` severity (Risk Register row 3 calls out `hsl(213_94%_68%)` on dark bg). If axe flags this, adjust the corresponding CSS variable. The current palette uses inline `bg-[hsl(213_94%_68%)] text-black` in motion.tsx:11-17 and findings.tsx:23-29 — these inline values are the source of truth and were chosen so foreground (black) meets contrast on a 68%-lightness blue. If axe flags the badge, the fix is in the CSS variable / Tailwind token, NOT in these inline strings (D-18 forbids new inline `hsl()` adjustments).

    4. **Hard rule (D-18 / Phase 39 D-13):** any contrast fix MUST be made via a CSS variable adjustment in `index.css`. Do NOT add new `hsl(...)` literals in tsx files. Do NOT change the existing inline `hsl()` strings in motion.tsx / findings.tsx / data-at-rest.tsx — those are intentional CSS-variable shorthands that read from Tailwind's theme.

    5. Write a brief audit note to `.planning/phases/43-dashboard-polish/43-03-CONTRAST-AUDIT.md` (one paragraph): list which axe violations were found and what was changed (or "no violations — no changes needed"). This file is informational; not committed to ROADMAP/SUMMARY tracking but useful for Plan 04 baseline review.

    Decisions implemented: D-18 (CSS-variable token system, no hardcoded hsl in tsx), D-16 (contrast verified by axe color-contrast rule), Phase 39 D-13 (no new hsl literals).
  </action>
  <verify>
    <automated>cd src/dashboard && test -f src/index.css && ! git diff src/index.css 2>/dev/null | grep -E '^\+.*hsl\([0-9]' || true; ! grep -rE 'bg-\[hsl\(' src/dashboard/src/pages/ 2>/dev/null | grep -v 'motion.tsx\|findings.tsx\|data-at-rest.tsx' && test -f .planning/phases/43-dashboard-polish/43-03-CONTRAST-AUDIT.md && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Either: (a) `git diff src/dashboard/src/index.css` shows no changes (no axe color-contrast violations were found), OR (b) `git diff src/dashboard/src/index.css` shows only CSS variable value adjustments (lines matching `^[+-].*--[a-z-]+:.*` patterns), no new `@apply` rules, no hardcoded `hsl()` literals introduced as `bg-[hsl(...)]` strings in tsx files
    - `grep -rE 'bg-\[hsl\(' src/dashboard/src/pages/ | grep -vE 'motion\.tsx|findings\.tsx|data-at-rest\.tsx'` returns no results (no NEW pages added inline hsl strings — only the three existing pre-Phase-43 pages keep theirs)
    - `test -f .planning/phases/43-dashboard-polish/43-03-CONTRAST-AUDIT.md` exits 0 — audit note exists
    - `cd src/dashboard && npm run build` exits 0
    - Re-running `cd src/dashboard && VITE_A11Y_FIXTURE=1 npm run a11y:check 2>&1 | grep -c 'color-contrast'` returns 0 (no remaining color-contrast violations) — this is the binding criterion for DASH-03 contrast SC.
  </acceptance_criteria>
  <done>
    Color-contrast is verified against the populated fixture: either no violations were found (no index.css change) or violations were closed by adjusting CSS variable values (no new inline hsl() literals). A short audit note records the outcome. Build is clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tailwind class strings -> compiled CSS | Build-time only; no runtime injection. |
| CSS variable values -> rendered colors | Static values in index.css; no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-08 | Tampering | Sidebar Link className | accept | className is a static literal in source; no template injection surface. |
| T-43-09 | Information Disclosure | Color tokens | accept | Tokens are public visual-design values, not secrets. |
</threat_model>

<verification>
After both tasks:
- `cd src/dashboard && npm run lint` exits 0
- `cd src/dashboard && npm run build` exits 0
- `cd src/dashboard && VITE_A11Y_FIXTURE=1 npm run a11y:check 2>&1 | grep -c 'color-contrast'` returns 0
- Manual: Tab through sidebar, each Link shows visible focus ring.
</verification>

<success_criteria>
- Sidebar Link primitives show a visible focus ring on keyboard focus
- axe color-contrast rule returns zero violations against the seeded fixture across all 9 routes
- No new inline `hsl()` literals introduced in tsx files
</success_criteria>

<output>
After completion, create `.planning/phases/43-dashboard-polish/43-03-SUMMARY.md`
</output>
