---
phase: 40-chaos-lab-parity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - quantum-chaos-enterprise-lab/expected_results_v4.md
  - quantum-chaos-enterprise-lab/expected_results_v3.md
autonomous: true
requirements: [LAB-03]
must_haves:
  truths:
    - "expected_results_v4.md exists and contains a `## Profile: <name>` H2 section for each network-listener profile (core, phaseA, cloud, identity, pki, jwt, registry, source, ssh-weak, ldaps, dnssec, saml, kerberos)"
    - "Each listener profile section uses the v3-proven schema: Port | Service | Expected protocol | Expected condition / tag | Notes (D-05)"
    - "expected_results_v3.md is annotated at the top as superseded by v4 (kept for historical reference)"
    - "SAML port drift fixed: row uses port 8080 (compose source of truth), not the v3 oracle's 8880"
    - "Profile-name drift fixed: `dnssec` (not `bind9`), `saml` (not `simpla-samlphp`), `kerberos` (not `samba-dc`)"
    - "pki profile section is added (was absent in v3 oracle) — port 17443, MTLS_STEPCA tag"
  artifacts:
    - path: quantum-chaos-enterprise-lab/expected_results_v4.md
      provides: v4 oracle covering listener profiles (v4.0/4.1/4.2 carryovers)
      contains: "## Profile: phaseA"
    - path: quantum-chaos-enterprise-lab/expected_results_v3.md
      provides: superseded notice
      contains: "Superseded by"
  key_links:
    - from: quantum-chaos-enterprise-lab/expected_results_v4.md
      to: quantum-chaos-enterprise-lab/docker-compose.yml
      via: "port + profile-name alignment per RESEARCH §Docker Compose Profile Inventory"
      pattern: "## Profile:"
---

<objective>
Create the new `expected_results_v4.md` oracle and populate it with **listener-profile sections** (v4.0 baseline core, v4.0 phaseA/cloud/identity/pki, v4.1 jwt/registry/source/storage/ssh-weak/ldaps, v4.2 dnssec/saml/kerberos). DAR / config-introspection profiles (database, storage-s3, vault, storage hybrid, email, broker) are handled by plan 40-03 in parallel. Also archive `expected_results_v3.md` with a top-of-file "superseded by v4" note (D-01).

Purpose: LAB-03 (oracle exists for v4.3+v4.4 profiles — half delivered here, half in 40-03; together they form the complete v4 oracle).

Output: `expected_results_v4.md` with 14 listener-profile sections + `expected_results_v3.md` archive header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-chaos-lab-parity/40-CONTEXT.md
@.planning/phases/40-chaos-lab-parity/40-RESEARCH.md
@quantum-chaos-enterprise-lab/expected_results_v3.md
@quantum-chaos-enterprise-lab/docker-compose.yml
@docs/chaos-lab.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create expected_results_v4.md scaffold + listener-profile sections (core through pki)</name>
  <files>quantum-chaos-enterprise-lab/expected_results_v4.md</files>
  <read_first>
    - quantum-chaos-enterprise-lab/expected_results_v3.md (entire file — schema reference; copy listener rows verbatim per RESEARCH §"Starting-State Summary" mapping table)
    - quantum-chaos-enterprise-lab/docker-compose.yml (verify ports for each listener profile against the inventory)
    - .planning/phases/40-chaos-lab-parity/40-RESEARCH.md (§"Docker Compose Profile Inventory" rows for each profile + §"Scanner Finding Tags" Style A table)
    - .planning/phases/40-chaos-lab-parity/40-CONTEXT.md (D-04, D-05, D-07 — schema rules)
    - docs/chaos-lab.md (lines 339-386 — Section 5 Complete Port Reference, source for the missing pki section)
  </read_first>
  <action>
    Create `quantum-chaos-enterprise-lab/expected_results_v4.md` with this structure:

    **Top-of-file header:**
    ```markdown
    # Expected Scanner Results — v4 Oracle

    **Scope:** Every Docker Compose profile shipped through QU.I.R.K. v4.4 (v4.0 baseline + v4.1/4.2 expansions + v4.3 DAR + v4.4 messaging).
    **Status:** Authoritative. Supersedes `expected_results_v3.md`.
    **Schema:** Per-profile H2 sections (`## Profile: <name>`). Network-listener profiles use `Port | Service | Expected protocol | Expected condition / tag | Notes`. DAR / config-introspection profiles use category-tuned schemas (see plan 40-03 for database / storage-s3 / vault / storage / email / broker).

    Use the matching `## Profile: <name>` anchor as the cross-reference target from `README.md` (D-11).
    ```

    **Then add these listener-profile sections in this order**, each with: H2 anchor `## Profile: <name>`, one-line "what this profile exercises" intro, the `./lab.sh` invocation example (`PROFILE_ARGS="--profile <name>" ./lab.sh up`), and the table per D-05 schema. Copy rows verbatim from v3 oracle line ranges given in RESEARCH §"Starting-State Summary":

    1. **`## Profile: core`** — Per RESEARCH Open Question #2 (recommendation: include). Copy v3 oracle lines 8-22. Intro: "*The \"core\" baseline — always-on TLS / HTTP / SSH chaos matrix; present in every `./lab.sh up` regardless of profile selection.*"
    2. **`## Profile: phaseA`** — Copy v3 oracle lines 25-70 (Phase A1 + A2 + A3 sub-sections). Intro: "Service-inventory expansion + TLS chain scenarios + SNI ingress."
    3. **`## Profile: cloud`** — Copy v3 oracle lines 96-110. Intro: "LocalStack S3/STS/IAM + Azurite (Blob/Queue/Table) behind TLS terminators on 21000-21002 and 24566."
    4. **`## Profile: identity`** — Copy v3 oracle lines 74-88. Intro: "Keycloak IdP + step-ca + OpenLDAP + mTLS gateway."
    5. **`## Profile: pki`** — NEW (v3 oracle has no pki section). Single row:
       `| 17443 | mtls-stepca-gateway | TLS 1.2/1.3 | MTLS_STEPCA | step-ca-issued mTLS gateway; depends on identity profile services (whoami, step-ca). |`
       Intro: "step-ca-issued mTLS gateway. Requires `identity` profile to be up first (shared services)."

    Do NOT include jwt/registry/source/storage/ssh-weak/ldaps/dnssec/saml/kerberos in this task — those go in Task 2 to keep the diff manageable.

    Per D-07: every section must include the H2 anchor, intro line, lab.sh invocation block, and condition/tag column populated.
  </action>
  <verify>
    <automated>
      test -f quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q '^# Expected Scanner Results — v4 Oracle' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q '^## Profile: core' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q '^## Profile: phaseA' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q '^## Profile: cloud' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q '^## Profile: identity' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q '^## Profile: pki' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q 'MTLS_STEPCA' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q '17443' quantum-chaos-enterprise-lab/expected_results_v4.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `expected_results_v4.md` exists with H1 title `# Expected Scanner Results — v4 Oracle`
    - File contains "Supersedes `expected_results_v3.md`" sentence in the header
    - Five `## Profile: ...` H2 sections present: `core`, `phaseA`, `cloud`, `identity`, `pki`
    - `pki` section is new content with port 17443 and `MTLS_STEPCA` tag
    - Each section has a `PROFILE_ARGS="--profile ..." ./lab.sh up` invocation example (not required for core)
    - Each table uses the D-05 schema columns: `Port | Service | Expected protocol | Expected condition / tag | Notes`
  </acceptance_criteria>
  <done>5 listener profile sections (core through pki) populated in expected_results_v4.md.</done>
</task>

<task type="auto">
  <name>Task 2: Add remaining listener-profile sections (jwt through kerberos) + archive v3 oracle</name>
  <files>quantum-chaos-enterprise-lab/expected_results_v4.md, quantum-chaos-enterprise-lab/expected_results_v3.md</files>
  <read_first>
    - quantum-chaos-enterprise-lab/expected_results_v3.md (lines 114-266 — jwt/registry/source/ssh-weak/ldaps/dnssec/saml/kerberos sections)
    - quantum-chaos-enterprise-lab/expected_results_v4.md (file created in Task 1 — append to it)
    - .planning/phases/40-chaos-lab-parity/40-RESEARCH.md (§"Docker Compose Profile Inventory" — port and service-name truth; §"Starting-State Summary" — the v3-oracle line ranges and the explicit drift fixups)
    - .planning/phases/40-chaos-lab-parity/40-CONTEXT.md (D-01 — v3 archive note wording authority)
  </read_first>
  <action>
    Note: v3 oracle keeps drift names verbatim (historical reference per D-01); ONLY v4 oracle gets the SAML-port and profile-name fixes.

    **A) Append to `expected_results_v4.md`** these listener profile sections, in order. Copy rows verbatim from the v3 oracle line ranges below, then apply the listed drift fixes. Each section gets an H2 anchor, intro line, lab.sh invocation example, and D-05 schema table.

    1. **`## Profile: jwt`** — copy v3 lines 114-127. Intro: "4 JWT microservices with weak alg configs (LAB-01 / SCAN-03)."
    2. **`## Profile: registry`** — copy v3 lines 131-146. Intro: "Docker Registry v2 + 3 seeded images with old crypto libs."
    3. **`## Profile: source`** — copy v3 lines 150-165. Intro: "Gitea + seeded repos with crypto anti-patterns (semgrep target)."
    4. **`## Profile: ssh-weak`** — copy v3 lines 189-204. Intro: "OpenSSH 7.6p1 with deliberately weak KEX/hostkey/MAC algorithms on port 20022."
    5. **`## Profile: ldaps`** — copy v3 lines 208-220. Intro: "OpenLDAP over LDAPS on standard port 636 with self-signed cert."
    6. **`## Profile: dnssec`** — copy v3 lines 224-237. **Drift fix:** v3 names this profile `bind9`; rename to `dnssec` (compose is source of truth). Intro: "BIND9 with weak DNSSEC zones (RSASHA1) on UDP/TCP 15353."
    7. **`## Profile: saml`** — copy v3 lines 241-252. **Drift fixes (per RESEARCH §"Docker Compose Profile Inventory" + §"Starting-State Summary"):** (a) profile name `simpla-samlphp` → `saml`, (b) port `8880` → `8080`. Intro: "simpleSAMLphp IdP with weak signing cert (RSA-1024 / SHA-1) on port 8080. Note: shares port 8080 with Keycloak's container-internal port; no host conflict because Keycloak uses `expose:` not `ports:` for 8080."
    8. **`## Profile: kerberos`** — copy v3 lines 256-266. **Drift fix:** profile name `samba-dc` → `kerberos`. Intro: "Samba AD-DC for Kerberos etype enumeration on privileged ports 88 + 389. ⚠ These ports collide with system DNS/LDAP if anything else is listening locally."

    **B) Annotate `expected_results_v3.md`** by inserting (immediately after the existing H1 line) this block verbatim:

    ```markdown

    > **Superseded by `expected_results_v4.md`** (Phase 40, v4.5 milestone, 2026-04-29). This file is retained for historical reference only — it predates the v4.3 DAR profiles (`database`, `storage-s3`, `vault`) and the v4.4 messaging profiles (`email`, `broker`), and contains drift in the SAML port (8880 vs compose 8080) and several profile names (`bind9` vs `dnssec`, `simpla-samlphp` vs `saml`, `samba-dc` vs `kerberos`). Use `expected_results_v4.md` for all UAT and consultant reference work.

    ```

    Do not delete or otherwise modify v3 oracle content — only prepend this notice.

    Per D-01: v3 oracle is no longer authoritative but stays in repo. Per D-03: v4 filename is roadmap-locked, do not version-strip.
  </action>
  <verify>
    <automated>
      test "$(grep -c '^## Profile:' quantum-chaos-enterprise-lab/expected_results_v4.md)" -eq 13 &amp;&amp; \
      grep -q '^## Profile: jwt' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q '^## Profile: kerberos' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q '^## Profile: dnssec' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q '^## Profile: saml' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      ! grep -E 'Profile: (bind9|simpla-samlphp|samba-dc)' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -A2 '^## Profile: saml' quantum-chaos-enterprise-lab/expected_results_v4.md | grep -q '8080' &amp;&amp; \
      grep -q 'Superseded by' quantum-chaos-enterprise-lab/expected_results_v3.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Profile:' expected_results_v4.md` returns 13 (5 from Task 1 + 8 from Task 2)
    - All 8 new sections present: `jwt`, `registry`, `source`, `ssh-weak`, `ldaps`, `dnssec`, `saml`, `kerberos`
    - No occurrences of pre-drift profile names `bind9`, `simpla-samlphp`, `samba-dc` in v4 oracle
    - SAML section references port `8080` (not `8880`)
    - `expected_results_v3.md` contains the "Superseded by" notice in its first 10 lines
  </acceptance_criteria>
  <done>13 listener-profile sections complete in v4 oracle; v3 oracle archived with notice. (DAR sections delivered by plan 40-03, bringing total to 18 profiles.)</done>
</task>

</tasks>

<threat_model>
No runtime attack surface — documentation only. Markdown content is read by humans and (later) by README cross-link navigation; nothing is executed.
</threat_model>

<verification>
- 13 listener-profile H2 sections present in v4 oracle (this plan's contribution)
- All drift fixes applied (port 8080 for SAML; profile names align with compose)
- pki section newly added
- v3 oracle archived with explicit "Superseded by" notice
</verification>

<success_criteria>
- LAB-03 listener-profile half complete (DAR half delivered by plan 40-03)
- Anchors are stable, lowercase, single-word GitHub-flavored (e.g., `#profile-core`, `#profile-storage-s3`) so plan 40-04 README cross-links work. All headings strip parens/slashes/colons; descriptors live in the intro line under the heading.
</success_criteria>

<output>
Create `.planning/phases/40-chaos-lab-parity/40-02-SUMMARY.md` listing the 13 sections added, the drift fixes applied, and the v3 oracle archive note.
</output>
