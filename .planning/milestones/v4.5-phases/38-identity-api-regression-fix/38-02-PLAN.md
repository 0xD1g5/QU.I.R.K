---
phase: 38-identity-api-regression-fix
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - .planning/phases/36-dashboard-motion-tab/36-VALIDATION.md
autonomous: true
requirements: [GAP-03]
must_haves:
  truths:
    - ".planning/phases/36-dashboard-motion-tab/36-VALIDATION.md exists on disk"
    - "Frontmatter contains nyquist_compliant: true AND wave_0_complete: true"
    - "A gap-closure note in the file references Phase 38 GAP-01 as the predicate that justified the flip"
  artifacts:
    - path: ".planning/phases/36-dashboard-motion-tab/36-VALIDATION.md"
      provides: "Restored Phase 36 validation matrix with wave_0_complete flipped"
      contains: "wave_0_complete: true"
  key_links:
    - from: ".planning/phases/36-dashboard-motion-tab/36-VALIDATION.md frontmatter"
      to: "Phase 38 GAP-01 closure (PLAN 38-01)"
      via: "gap_closed comment + body note"
      pattern: "Phase 38"
---

<objective>
Restore `.planning/phases/36-dashboard-motion-tab/36-VALIDATION.md` from git commit `99f48d2` (last-known-good) and flip `wave_0_complete: false` to `wave_0_complete: true`, closing GAP-03 / DEF-v4.4-01. The flip is now justified because PLAN 38-01 closed the SAML scan-window regression that the original deferral was waiting on.

Purpose: Closes GAP-03 — the v4.4 close-out gap that has been pinned open since 2026-04-28 pending the GAP-01 fix.

Output: Restored 36-VALIDATION.md file with `nyquist_compliant: true, wave_0_complete: true` and a one-line `gap_closed` note linking the flip to Phase 38.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-identity-api-regression-fix/38-CONTEXT.md
@.planning/phases/38-identity-api-regression-fix/38-RESEARCH.md
@.planning/phases/38-identity-api-regression-fix/38-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore 36-VALIDATION.md verbatim from commit 99f48d2 and flip the wave_0_complete flag with gap-closure annotation</name>
  <files>.planning/phases/36-dashboard-motion-tab/36-VALIDATION.md</files>
  <read_first>
    - Run `git show 99f48d2:.planning/phases/36-dashboard-motion-tab/36-VALIDATION.md` to read the canonical historical content (do NOT pipe directly to a write — read it first to confirm it is non-empty and well-formed)
    - .planning/phases/38-identity-api-regression-fix/38-RESEARCH.md "Pattern 2 — Restore 36-VALIDATION.md" for the exact diff to apply
    - .planning/phases/38-identity-api-regression-fix/38-01-SUMMARY.md (must exist — proves PLAN 38-01 closed GAP-01 / GAP-02 and the flip is now justified)
  </read_first>
  <action>
Per D-04 in CONTEXT.md and Pattern 2 in RESEARCH.md:

1. Confirm the target directory exists; create it if missing:
   ```
   mkdir -p .planning/phases/36-dashboard-motion-tab
   ```

2. Recover the historical content into the target path (verbatim from commit `99f48d2`):
   ```
   git show 99f48d2:.planning/phases/36-dashboard-motion-tab/36-VALIDATION.md > .planning/phases/36-dashboard-motion-tab/36-VALIDATION.md
   ```

3. Open the restored file with the `Edit` tool and apply ONLY these frontmatter changes (the rest of the file body is unchanged):
   - Replace `wave_0_complete: false` with `wave_0_complete: true`.
   - Add a new frontmatter line directly below `approved: 2026-04-28`:
     ```
     gap_closed: 2026-04-29 (Phase 38, GAP-03 — predicate GAP-01/GAP-02 closed in PLAN 38-01)
     ```

4. Add a closing note block at the very end of the file body (after any existing trailing content):
   ```markdown

   ---

   ## Wave 0 Closure (Phase 38, 2026-04-29)

   `wave_0_complete: true` — flipped after Phase 38 PLAN 38-01 closed the SAML
   scan-window regression (GAP-01 / GAP-02). The 5-minute backward bracket in
   `quirk/dashboard/api/routes/scan.py` (`SESSION_BRACKET`) restores SAML/OIDC
   visibility in `/api/scan/latest` `identity_findings[]`, which was the only
   remaining predicate gap. See `.planning/phases/38-identity-api-regression-fix/38-01-SUMMARY.md`.
   ```

Do NOT edit any other line. Minimal-diff per CLAUDE.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/36-dashboard-motion-tab/36-VALIDATION.md && python -c "import re,pathlib;p=pathlib.Path('.planning/phases/36-dashboard-motion-tab/36-VALIDATION.md');c=p.read_text();assert re.search(r'^nyquist_compliant\s*:\s*true', c, re.M), 'nyquist_compliant != true';assert re.search(r'^wave_0_complete\s*:\s*true', c, re.M), 'wave_0_complete != true';assert 'Phase 38' in c, 'no Phase 38 reference';print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/36-dashboard-motion-tab/36-VALIDATION.md` exists.
    - `grep -c "^nyquist_compliant: true$" .planning/phases/36-dashboard-motion-tab/36-VALIDATION.md` returns `1`.
    - `grep -c "^wave_0_complete: true$" .planning/phases/36-dashboard-motion-tab/36-VALIDATION.md` returns `1`.
    - `grep -c "^wave_0_complete: false$" .planning/phases/36-dashboard-motion-tab/36-VALIDATION.md` returns `0` (the old value is gone, not duplicated).
    - `grep -c "^gap_closed:" .planning/phases/36-dashboard-motion-tab/36-VALIDATION.md` returns `1`.
    - `grep -c "PLAN 38-01" .planning/phases/36-dashboard-motion-tab/36-VALIDATION.md` returns at least `1` (the closure-note reference).
    - The file's body content (everything below the frontmatter and above the new closure note) byte-matches `git show 99f48d2:.planning/phases/36-dashboard-motion-tab/36-VALIDATION.md`'s body — verifiable by diffing the body region; minor whitespace tolerance is acceptable.
  </acceptance_criteria>
  <done>36-VALIDATION.md restored, frontmatter flag flipped, gap_closed annotation present, closure note tied to PLAN 38-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure documentation change; no runtime trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-04 | Repudiation | Phase 36 wave_0_complete flag history | mitigate | `gap_closed:` frontmatter line + Wave 0 Closure body note record the date, predicate (GAP-01/02), and the responsible plan (PLAN 38-01) — auditable provenance for the flip |
</threat_model>

<verification>
- File exists at the expected path
- Frontmatter shows both `nyquist_compliant: true` and `wave_0_complete: true`
- Closure note references PLAN 38-01
- No duplicate or stale `wave_0_complete: false` lines remain
</verification>

<success_criteria>
- GAP-03 closed
- DEF-v4.4-01 ready to mark closed in PLAN 38-04 (STATE.md update)
- Phase 36 validation matrix complete and dated
</success_criteria>

<output>
After completion, create `.planning/phases/38-identity-api-regression-fix/38-02-SUMMARY.md` documenting:
- The exact `git show 99f48d2:` command output verifying the source content
- The applied frontmatter diff (4 lines changed/added)
- The body-appended closure note (verbatim)
- Confirmation that the file body matches the historical source modulo the closure-note append
</output>
