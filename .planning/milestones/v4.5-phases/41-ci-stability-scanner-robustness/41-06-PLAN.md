---
phase: 41
plan: 06
type: execute
wave: 4
depends_on: [41-02, 41-03]
files_modified:
  - docs/configuration.md
  - docs/timeout-retry-audit.md
  - quantum-chaos-enterprise-lab/lab.sh
autonomous: true
requirements: [ROBUST-04, ROBUST-02]

must_haves:
  truths:
    - "docs/configuration.md documents the new [scan.timeouts] and [scan.retry] sub-tables with every per-scanner slot"
    - "docs/configuration.md contains the documented overall scan upper-bound formula (D-10): sum(per_scanner_timeout × max_targets_for_phase) + 10s safety_margin"
    - "docs/timeout-retry-audit.md is a markdown table listing every scanner's effective timeout/retry values + canonical source citation (ROBUST-04)"
    - "lab.sh down arm uses `compose --profile \"*\" down --remove-orphans` (D-18)"
    - "lab.sh reset arm uses `compose --profile \"*\" down -v --remove-orphans` (D-18 extension per RESEARCH Open Question 4)"
  artifacts:
    - path: "docs/configuration.md"
      provides: "[scan.timeouts] / [scan.retry] documentation + upper-bound formula"
      contains: "scan.timeouts"
    - path: "docs/timeout-retry-audit.md"
      provides: "ROBUST-04 audit table — per-scanner timeout/retry values"
      contains: "scan.timeouts"
    - path: "quantum-chaos-enterprise-lab/lab.sh"
      provides: "Profile-sweep on down and reset arms"
      contains: "compose --profile"
  key_links:
    - from: "docs/configuration.md"
      to: "quirk/config.py TimeoutsCfg / RetryCfg dataclass"
      via: "documented field names match dataclass attributes"
      pattern: "tls_seconds"
    - from: "lab.sh down arm"
      to: "compose --profile sweep"
      via: "explicit --profile \"*\" flag"
      pattern: "compose --profile \"\\*\""
---

<objective>
Land the consultant-facing documentation that makes the Phase 41 changes useful (D-10 upper-bound
formula, ROBUST-04 audit doc) and fix the Phase 40 carry-over `lab.sh` profile-sweep gap on both
the `down` and `reset` arms (D-18 + extension).

Purpose: Without docs, consultants can't quote timeout policy to clients (ROBUST-04). Without the
lab.sh fix, profile-tagged services survive `./lab.sh down` and contaminate subsequent runs.

Output: docs/configuration.md updated, docs/timeout-retry-audit.md created, lab.sh fixed on both
arms in a single commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-ci-stability-scanner-robustness/41-CONTEXT.md
@.planning/phases/41-ci-stability-scanner-robustness/41-RESEARCH.md
@.planning/phases/41-ci-stability-scanner-robustness/41-PATTERNS.md
@.planning/phases/41-02-SUMMARY.md
@.planning/phases/41-03-SUMMARY.md

<interfaces>
TimeoutsCfg slots (Plan 02): default_seconds=5, fingerprint_seconds=4, tls_seconds=6, ssh_seconds=6,
jwt_seconds=10, container_seconds=120, source_seconds=300, dnssec_seconds=10, saml_seconds=10,
kerberos_seconds=10, vault_seconds=10, db_connect_seconds=5, broker_seconds=10, email_seconds=10.

RetryCfg slots (Plan 02): retry_count=0, backoff_base_seconds=1.0, backoff_max_seconds=5.0.

Upper-bound formula (D-10, RESEARCH lines 348-358):
  scan_upper_bound = (
    fingerprint_timeout * N_targets
    + tls_timeout * N_tls_candidates
    + ssh_timeout * N_ssh_candidates
    + max(jwt_timeout, container_timeout, source_timeout, ...) * N_connector_targets
  ) + 10s safety_margin

Conservative single-host estimate: 4 + 6 + 6 + 10 + 10 = 36s + 10s safety ≈ 46s.

lab.sh current (lines 97-108, RESEARCH §"lab.sh Down-Arm Diff"):
- down arm: `compose down`
- reset arm: `compose down -v` followed by `compose up -d`

Replacement (D-18 + RESEARCH Open Question 4 — both arms):
- down arm: `compose --profile "*" down --remove-orphans`
- reset arm: `compose --profile "*" down -v --remove-orphans` then `compose up -d`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/configuration.md + create ROBUST-04 audit doc</name>
  <files>docs/configuration.md, docs/timeout-retry-audit.md</files>
  <read_first>
    - docs/configuration.md (full file — find the existing scan/timeout config section to extend)
    - quirk/config.py post-Plan-02 (confirm TimeoutsCfg / RetryCfg field names match docs)
    - .planning/phases/41-ci-stability-scanner-robustness/41-RESEARCH.md §"Proposed [scan.timeouts] / [scan.retry] Shape" §"Upper-bound formula"
    - .planning/phases/41-ci-stability-scanner-robustness/41-CONTEXT.md D-09, D-10
  </read_first>
  <action>
    1. **docs/configuration.md** — Add a new section "## Timeout & Retry Policy (v4.5+)". Include:
       - Description of `[scan.timeouts]` sub-table with every slot (default_seconds, fingerprint_seconds, tls_seconds, ssh_seconds, jwt_seconds, container_seconds, source_seconds, dnssec_seconds, saml_seconds, kerberos_seconds, vault_seconds, db_connect_seconds, broker_seconds, email_seconds) and its default value.
       - Description of `[scan.retry]` sub-table (retry_count=0, backoff_base_seconds=1.0, backoff_max_seconds=5.0).
       - Explicit deprecation notice: legacy flat fields `timeout_seconds`, `tls_timeout_seconds`, `ssh_timeout_seconds`, `fingerprint_timeout_seconds` still readable but emit DeprecationWarning; users should migrate to the sub-table.
       - **Upper-bound formula (D-10)** verbatim as a fenced code block:
         ```
         scan_upper_bound = (
           fingerprint_timeout * N_targets
           + tls_timeout       * N_tls_candidates
           + ssh_timeout       * N_ssh_candidates
           + max(jwt_timeout, container_timeout, source_timeout, ...) * N_connector_targets
         ) + 10s safety_margin
         ```
         followed by a worked example for a single-host scan: ~46s.
       - Example TOML snippet showing both sub-tables in use.

    2. **docs/timeout-retry-audit.md** (new) — Single-page markdown audit doc per ROBUST-04 / D-09. Structure:
       - Frontmatter or heading: "Phase 41 Timeout & Retry Audit"
       - Markdown table with columns: `Scanner | Timeout slot | Default (s) | Retry count | Backoff base (s) | Backoff max (s) | Canonical source`
       - One row per scanner (fingerprint, tls, ssh, jwt, container, source, dnssec, saml, kerberos, vault, db_pg, db_mysql, broker_kafka, broker_rabbitmq, broker_redis, email)
       - Canonical source column: `cfg.scan.timeouts.<slot>` for every row
       - Closing paragraph: every scanner reads from the canonical TimeoutsCfg/RetryCfg; legacy hardcoded literals removed in Phase 41 Plan 03.

    3. Run `python -c "open('docs/configuration.md').read(); open('docs/timeout-retry-audit.md').read()"` to confirm files are readable.
  </action>
  <verify>
    <automated>test -f docs/configuration.md &amp;&amp; test -f docs/timeout-retry-audit.md &amp;&amp; grep -q "scan.timeouts" docs/configuration.md &amp;&amp; grep -q "scan.retry" docs/configuration.md &amp;&amp; grep -q "safety_margin" docs/configuration.md &amp;&amp; grep -q "DeprecationWarning" docs/configuration.md &amp;&amp; grep -q "tls_seconds" docs/timeout-retry-audit.md &amp;&amp; grep -q "broker_seconds" docs/timeout-retry-audit.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "scan.timeouts" docs/configuration.md` exit 0
    - `grep -q "scan.retry" docs/configuration.md` exit 0
    - `grep -q "scan_upper_bound" docs/configuration.md` exit 0
    - `grep -q "safety_margin" docs/configuration.md` exit 0
    - `grep -q "DeprecationWarning" docs/configuration.md` exit 0
    - `grep -c "cfg.scan.timeouts" docs/timeout-retry-audit.md` returns at least 14 (one per slot)
    - All 14+ scanner rows present (grep `-c "^|" docs/timeout-retry-audit.md` returns at least 16: header, separator, 14+ scanner rows)
    - Both files exist and are non-empty
  </acceptance_criteria>
  <done>docs/configuration.md documents the new sub-tables and upper-bound formula; docs/timeout-retry-audit.md is the ROBUST-04 audit doc.</done>
</task>

<task type="auto">
  <name>Task 2: lab.sh down + reset arm profile-sweep fix (D-18)</name>
  <files>quantum-chaos-enterprise-lab/lab.sh</files>
  <read_first>
    - quantum-chaos-enterprise-lab/lab.sh lines 90-115 (down arm and reset arm)
    - .planning/phases/41-ci-stability-scanner-robustness/41-RESEARCH.md §"lab.sh Down-Arm Diff"
    - .planning/phases/41-ci-stability-scanner-robustness/41-CONTEXT.md D-18
    - CLAUDE.md "Chaos Lab Maintenance" section
  </read_first>
  <action>
    1. In quantum-chaos-enterprise-lab/lab.sh, locate the `down)` case arm (around line 97-101).
       Replace `compose down` with `compose --profile "*" down --remove-orphans`. Keep the
       surrounding echo lines unchanged.

    2. Locate the `reset)` case arm (around line 104-108). Replace `compose down -v` with
       `compose --profile "*" down -v --remove-orphans`. Leave `compose up -d` unchanged. (This
       extends D-18 per RESEARCH Open Question 4 — same intent applies to both arms; flagged in
       the SUMMARY as "D-18 extension, trivially safe.")

    3. Verify the script is still bash-valid: `bash -n quantum-chaos-enterprise-lab/lab.sh`.

    4. Smoke check: `./quantum-chaos-enterprise-lab/lab.sh status` should still execute without
       errors (does not bring up containers — just reports profile/status). If `lab.sh status`
       requires a running compose, skip this smoke and rely on bash -n.
  </action>
  <verify>
    <automated>bash -n quantum-chaos-enterprise-lab/lab.sh &amp;&amp; grep -q 'compose --profile "\*" down --remove-orphans' quantum-chaos-enterprise-lab/lab.sh &amp;&amp; grep -q 'compose --profile "\*" down -v --remove-orphans' quantum-chaos-enterprise-lab/lab.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n quantum-chaos-enterprise-lab/lab.sh` exits 0
    - `grep -c 'compose --profile "\*" down --remove-orphans' quantum-chaos-enterprise-lab/lab.sh` returns at least 1 (down arm)
    - `grep -c 'compose --profile "\*" down -v --remove-orphans' quantum-chaos-enterprise-lab/lab.sh` returns at least 1 (reset arm)
    - `grep -c '^    compose down$' quantum-chaos-enterprise-lab/lab.sh` returns 0 (old down arm gone)
    - `grep -c '^    compose down -v$' quantum-chaos-enterprise-lab/lab.sh` returns 0 (old reset arm gone)
  </acceptance_criteria>
  <done>lab.sh down and reset arms both use `--profile "*" ... --remove-orphans` to sweep profile-tagged services.</done>
</task>

</tasks>

<verification>
- `bash -n quantum-chaos-enterprise-lab/lab.sh` exits 0
- `grep -q "scan_upper_bound" docs/configuration.md` exit 0
- `grep -q "cfg.scan.timeouts" docs/timeout-retry-audit.md` exit 0
- Manual UAT-41-03: `./lab.sh up && ./lab.sh down && docker ps -a | grep quirk-lab` returns no rows
</verification>

<success_criteria>
- docs/configuration.md documents [scan.timeouts] / [scan.retry] sub-tables, deprecation aliases, and the D-10 upper-bound formula
- docs/timeout-retry-audit.md is the ROBUST-04 audit table — every scanner with its timeout/retry slot + canonical source
- lab.sh down arm sweeps profile-tagged services
- lab.sh reset arm sweeps profile-tagged services (D-18 extension)
</success_criteria>

<output>
After completion, create `.planning/phases/41-ci-stability-scanner-robustness/41-06-SUMMARY.md`.
</output>
