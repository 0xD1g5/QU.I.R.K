---
phase: 41
plan: 03
type: execute
wave: 2
depends_on: [41-01, 41-02]
files_modified:
  - run_scan.py
  - quirk/scanner/db_connector.py
  - quirk/scanner/vault_connector.py
  - tests/test_scan_robustness.py
autonomous: true
requirements: [ROBUST-02, ROBUST-04]

must_haves:
  truths:
    - "run_scan.py does NOT mutate cfg.scan.* attributes anywhere (BACK-45 dissolved per D-08)"
    - "TLS and SSH phases pass timeout/concurrency as explicit kwargs sourced from cfg.scan.timeouts.<name>_seconds"
    - "run_scan.py:743 broker_scanner call uses scan_profile (local var), NOT cfg.scan.profile (which does not exist)"
    - "Hardcoded timeouts in db_connector.py and vault_connector.py read from cfg.scan.timeouts.<name>_seconds"
  artifacts:
    - path: "run_scan.py"
      provides: "Mutation-free TLS/SSH phase setup, explicit timeout kwargs, profile bug fix"
    - path: "quirk/scanner/db_connector.py"
      provides: "connect_timeout sourced from cfg.scan.timeouts.db_connect_seconds"
    - path: "quirk/scanner/vault_connector.py"
      provides: "timeout sourced from cfg.scan.timeouts.vault_seconds"
  key_links:
    - from: "run_scan.py TLS/SSH phases"
      to: "cfg.scan.timeouts.tls_seconds / .ssh_seconds"
      via: "explicit kwarg passed to scan_tls_targets / scan_ssh_targets"
      pattern: "timeouts\\.(tls|ssh)_seconds"
    - from: "run_scan.py:743 broker call"
      to: "scan_profile local variable"
      via: "profile=scan_profile kwarg"
      pattern: "profile=scan_profile"
---

<objective>
Eliminate the BACK-45 cfg.scan mutation pattern (D-08), fix the run_scan.py:743 cfg.scan.profile
AttributeError bug, and switch every hardcoded/divergent timeout source to read read-only from
cfg.scan.timeouts.<name>_seconds.

Purpose: Without this plan, ROBUST-02 cannot be verified — the timeouts dataclass from Plan 02
exists but nothing reads from it yet. The cfg.scan.profile bug also currently crashes any
broker-enabled scan; folding the fix here keeps the diff in the same file the wave already
touches.

Output: All scanner timeout reads sourced from the canonical sub-table; no mutation of cfg.scan
remains; broker scan no longer crashes; ROBUST-02 timeout tests in test_scan_robustness.py pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-ci-stability-scanner-robustness/41-CONTEXT.md
@.planning/phases/41-ci-stability-scanner-robustness/41-RESEARCH.md
@.planning/phases/41-ci-stability-scanner-robustness/41-PATTERNS.md
@.planning/phases/41-01-SUMMARY.md
@.planning/phases/41-02-SUMMARY.md

<interfaces>
From RESEARCH §"cfg.scan Mutation Map" — exact mutation sites to remove:
- run_scan.py:414-417  TLS phase setup (mutates cfg.scan.timeout_seconds, cfg.scan.concurrency)
- run_scan.py:433-434  TLS phase teardown in finally (restores)
- run_scan.py:444-445  SSH phase setup
- run_scan.py:458-459  SSH phase teardown in finally

From RESEARCH §"Scanner Inventory" — timeout sources to convert:
- run_scan.py:312 (fingerprint)  cfg.scan.fingerprint_timeout_seconds → cfg.scan.timeouts.fingerprint_seconds
- run_scan.py:417 (TLS — was mutated) → cfg.scan.timeouts.tls_seconds
- run_scan.py:444-445 (SSH — was mutated) → cfg.scan.timeouts.ssh_seconds
- run_scan.py:469 (jwt) cfg.scan.timeout_seconds → cfg.scan.timeouts.jwt_seconds
- run_scan.py:481 (container) → cfg.scan.timeouts.container_seconds
- run_scan.py:493 (source) → cfg.scan.timeouts.source_seconds
- run_scan.py:727 (email) → cfg.scan.timeouts.email_seconds
- run_scan.py:743/752/757 (broker kafka/rabbitmq/redis) → cfg.scan.timeouts.broker_seconds
- db_connector.py:87,206 hardcoded connect_timeout=5 → cfg.scan.timeouts.db_connect_seconds
- vault_connector.py:413 hardcoded timeout=10 → cfg.scan.timeouts.vault_seconds

Bug at run_scan.py:743 (RESEARCH line 145):
`scan_kafka_targets(..., profile=cfg.scan.profile, ...)`  — ScanCfg has NO profile attribute.
Fix: pass `profile=scan_profile` (the argparse-derived local variable already in scope at line 224).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove cfg.scan mutation in run_scan.py + fix profile bug + convert timeout reads</name>
  <files>run_scan.py</files>
  <read_first>
    - run_scan.py lines 200-260 (find scan_profile local var)
    - run_scan.py lines 300-330 (fingerprint timeout site)
    - run_scan.py lines 410-470 (TLS + SSH mutation pattern — BACK-45)
    - run_scan.py lines 470-510 (jwt, container, source)
    - run_scan.py lines 720-770 (email + broker calls — including the bug at line 743)
    - .planning/phases/41-ci-stability-scanner-robustness/41-PATTERNS.md §"run_scan.py" §"D-08"
    - .planning/phases/41-ci-stability-scanner-robustness/41-RESEARCH.md §"cfg.scan Mutation Map" §"Scanner Inventory"
  </read_first>
  <action>
    Make ALL the following edits in run_scan.py — keep diff minimal:

    1. **Delete TLS mutation pair** (lines ~414-417 set; ~433-434 finally restore):
       - Remove `base_timeout = cfg.scan.timeout_seconds` and `base_conc = cfg.scan.concurrency` setup lines
       - Remove the two assignment lines `cfg.scan.timeout_seconds = ...; cfg.scan.concurrency = ...`
       - Remove the `try:` / `finally:` block restoring `cfg.scan.*`; keep only the inner `with _phase_timer(...)` block
       - Pass tls timeout/concurrency to `scan_tls_targets(...)` as explicit kwargs:
         `timeout=cfg.scan.timeouts.tls_seconds, concurrency=cfg.scan.tls_concurrency` (note: tls_concurrency stays a flat field on ScanCfg — only the four timeout fields became aliases in Plan 02)
       - Verify scan_tls_targets() signature accepts `timeout=` / `concurrency=` kwargs; if it currently reads from cfg, leave that read in the scanner unchanged for now (D-08 says scanners read READ-ONLY; passing kwargs is the cleanest, but if signature doesn't accept them, scanners must read cfg.scan.timeouts.tls_seconds directly. Read scanner/tls_scanner.py to decide.)

    2. **Delete SSH mutation pair** (same pattern, lines ~444-459) — same approach as TLS, using
       `cfg.scan.timeouts.ssh_seconds` and `cfg.scan.ssh_concurrency`.

    3. **Convert remaining flat timeout reads to sub-table reads:**
       - Line ~312 fingerprint: `cfg.scan.fingerprint_timeout_seconds` → `cfg.scan.timeouts.fingerprint_seconds`
       - Line ~469 jwt: `cfg.scan.timeout_seconds` → `cfg.scan.timeouts.jwt_seconds`
       - Line ~481 container: `cfg.scan.timeout_seconds` → `cfg.scan.timeouts.container_seconds`
       - Line ~493 source: `cfg.scan.timeout_seconds` → `cfg.scan.timeouts.source_seconds`
       - Line ~727 email: `cfg.scan.timeout_seconds` → `cfg.scan.timeouts.email_seconds`
       - Lines ~743, 752, 757 broker (kafka/rabbitmq/redis): `cfg.scan.timeout_seconds` → `cfg.scan.timeouts.broker_seconds`

    4. **Bug fix run_scan.py:743**: Change `profile=cfg.scan.profile` to `profile=scan_profile`.
       (The variable `scan_profile` is the argparse-derived string and is already in scope from
       line ~224. Verify by reading lines 220-230.)

    5. **Do NOT modify** `quirk/engine/profiles.py` `apply_profile()` — that is intentional pre-scan
       initialisation and is OUT OF D-08 scope (see RESEARCH "Also note" line 171).

    6. Run `python -m compileall run_scan.py` and `python -m compileall quirk/scanner/tls_scanner.py quirk/scanner/ssh_scanner.py` — must be clean.

    7. After edits, smoke test with a no-op scan invocation that doesn't hit network:
       `python -c "import run_scan; print('ok')"` exits 0.
  </action>
  <verify>
    <automated>python -m compileall run_scan.py -q && grep -c "cfg.scan.timeout_seconds = " run_scan.py | grep -q "^0$" && grep -c "cfg.scan.concurrency = " run_scan.py | grep -q "^0$" && grep -c "cfg\\.scan\\.\\(tls\\|ssh\\|fingerprint\\)_timeout_seconds" run_scan.py | grep -q "^0$" && grep -q "profile=scan_profile" run_scan.py && ! grep -q "profile=cfg.scan.profile" run_scan.py && grep -q "cfg.scan.timeouts.tls_seconds" run_scan.py && grep -q "cfg.scan.timeouts.ssh_seconds" run_scan.py && grep -q "cfg.scan.timeouts.broker_seconds" run_scan.py</automated>
  </verify>
  <acceptance_criteria>
    - Filter comments first: `grep -v '^[[:space:]]*#' run_scan.py | grep -c "cfg.scan.timeout_seconds = "` returns 0 (no assignments)
    - `grep -v '^[[:space:]]*#' run_scan.py | grep -c "cfg.scan.concurrency = "` returns 0
    - `grep -E "cfg\\.scan\\.(tls|ssh|fingerprint)_timeout_seconds" run_scan.py` produces no matches (legacy flat reads gone)
    - `grep -q "profile=scan_profile" run_scan.py` → exit 0
    - `grep -q "profile=cfg.scan.profile" run_scan.py` returns no match (bug fixed)
    - `grep -q "cfg.scan.timeouts.tls_seconds" run_scan.py` → exit 0
    - `grep -q "cfg.scan.timeouts.ssh_seconds" run_scan.py` → exit 0
    - `grep -q "cfg.scan.timeouts.fingerprint_seconds" run_scan.py` → exit 0
    - `grep -q "cfg.scan.timeouts.jwt_seconds" run_scan.py` → exit 0
    - `grep -q "cfg.scan.timeouts.broker_seconds" run_scan.py` → exit 0
    - `python -m compileall run_scan.py -q` exits 0
    - `python -c "import run_scan"` exits 0
  </acceptance_criteria>
  <done>BACK-45 mutation pattern dissolved; broker AttributeError bug fixed; all flat timeout reads in run_scan.py route through cfg.scan.timeouts.</done>
</task>

<task type="auto">
  <name>Task 2: Convert hardcoded scanner timeouts (db, vault) + ROBUST-02 test green</name>
  <files>quirk/scanner/db_connector.py, quirk/scanner/vault_connector.py, tests/test_scan_robustness.py</files>
  <read_first>
    - quirk/scanner/db_connector.py lines 80-100 and 200-220 (hardcoded connect_timeout=5)
    - quirk/scanner/vault_connector.py lines 405-420 (hardcoded timeout=10)
    - tests/test_scan_robustness.py current xfail stub for ROBUST-02
    - .planning/phases/41-ci-stability-scanner-robustness/41-RESEARCH.md §"Scanner Inventory" rows 10, 12
  </read_first>
  <action>
    1. **db_connector.py**: Locate the two `connect_timeout=5` literals (RESEARCH says lines 87 and 206 — verify exact lines). Each scanner entrypoint receives a `cfg` (or `cfg.scan`) object. Replace each `connect_timeout=5` literal with `connect_timeout=getattr(cfg.scan.timeouts, "db_connect_seconds", 5)` — reading from the canonical TimeoutsCfg, falling back to literal 5 if the cfg shape is older (defense-in-depth for tests that pass mocked cfg).

    2. **vault_connector.py**: Same pattern for the line 413 `timeout=10` literal — replace with `timeout=getattr(cfg.scan.timeouts, "vault_seconds", 10)`.

    3. Run `python -m compileall quirk/scanner/db_connector.py quirk/scanner/vault_connector.py -q`.

    4. **Convert ROBUST-02 timeout-respected test to green** in tests/test_scan_robustness.py:
       Replace the `test_per_scanner_timeout_respected_tls` xfail stub with a real assertion:

       ```python
       def test_per_scanner_timeout_respected_tls():
           """ROBUST-02: TLS scanner reads cfg.scan.timeouts.tls_seconds."""
           import inspect
           import run_scan
           src = inspect.getsource(run_scan)
           # The TLS phase must reference the new sub-table read
           assert "cfg.scan.timeouts.tls_seconds" in src, "TLS timeout not sourced from canonical sub-table"
           # And NOT the legacy mutation pattern
           assert "cfg.scan.timeout_seconds = " not in src, "BACK-45 mutation still present"
           assert "cfg.scan.concurrency = " not in src, "BACK-45 concurrency mutation still present"
       ```

       Remove the `@pytest.mark.xfail` decorator on this test.
  </action>
  <verify>
    <automated>python -m compileall quirk/scanner/db_connector.py quirk/scanner/vault_connector.py -q && grep -q "cfg.scan.timeouts.db_connect_seconds" quirk/scanner/db_connector.py && grep -q "cfg.scan.timeouts.vault_seconds" quirk/scanner/vault_connector.py && pytest tests/test_scan_robustness.py::test_per_scanner_timeout_respected_tls -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "connect_timeout=5" quirk/scanner/db_connector.py` returns 0 (literal replaced)
    - `grep -q "cfg.scan.timeouts.db_connect_seconds" quirk/scanner/db_connector.py` → exit 0
    - `grep -q "cfg.scan.timeouts.vault_seconds" quirk/scanner/vault_connector.py` → exit 0
    - `pytest tests/test_scan_robustness.py::test_per_scanner_timeout_respected_tls -x` exits 0 (PASSED)
    - `python -m compileall quirk/ -q` exits 0
  </acceptance_criteria>
  <done>db_connector and vault_connector read from canonical TimeoutsCfg; ROBUST-02 TLS timeout test green.</done>
</task>

</tasks>

<verification>
- `pytest -m 'not slow' -x tests/test_scan_robustness.py::test_per_scanner_timeout_respected_tls tests/test_timeouts_config.py` all green
- `python -c "import run_scan"` exits 0
- `grep -E "cfg\\.scan\\.(timeout_seconds|concurrency) =" run_scan.py` produces no non-comment hits
- `grep -q "profile=scan_profile" run_scan.py && ! grep -q "profile=cfg.scan.profile" run_scan.py`
</verification>

<success_criteria>
- BACK-45 mutation pattern fully removed from run_scan.py
- run_scan.py:743 cfg.scan.profile bug fixed (uses scan_profile local var)
- All run_scan.py timeout reads route through cfg.scan.timeouts.<name>_seconds
- db_connector.py and vault_connector.py read from canonical TimeoutsCfg with safe fallback
- ROBUST-02 TLS timeout test green
</success_criteria>

<output>
After completion, create `.planning/phases/41-ci-stability-scanner-robustness/41-03-SUMMARY.md`.
</output>
