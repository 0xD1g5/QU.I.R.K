---
phase: 39
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_dar_dashboard.py
autonomous: true
requirements: [GAP-04]
threat_refs: []

must_haves:
  truths:
    - "tests/test_dar_dashboard.py exists and contains 8 failing tests covering all DAR projection cases"
    - "Test fixture factory _ep() supports DAR-relevant fields (service_detail, dat_scan_json, scan_error, severity)"
    - "Tests fail with ImportError or assertion before any implementation exists (RED state)"
  artifacts:
    - path: "tests/test_dar_dashboard.py"
      provides: "Wave 0 RED scaffold for DAR projection + API contract"
      contains: "def test_derive_dar"
  key_links:
    - from: "tests/test_dar_dashboard.py"
      to: "quirk.dashboard.api.routes.scan._derive_dar_findings"
      via: "import inside each test function"
      pattern: "from quirk.dashboard.api.routes.scan import _derive_dar_findings"
---

<objective>
Wave 0 — create the failing test scaffold for Phase 39 DAR projection. All tests MUST fail (RED) until Plan 02 lands the schema + projection.

Purpose: Establish Nyquist-compliant feedback contract per VALIDATION.md before any implementation lands.

Output: tests/test_dar_dashboard.py with 8 unit/integration tests + extended _ep() fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-data-at-rest-dashboard-tab/39-CONTEXT.md
@.planning/phases/39-data-at-rest-dashboard-tab/39-RESEARCH.md
@.planning/phases/39-data-at-rest-dashboard-tab/39-PATTERNS.md
@.planning/phases/39-data-at-rest-dashboard-tab/39-VALIDATION.md
@CLAUDE.md
@tests/test_dashboard_api.py
</context>

<interfaces>
<!-- Fixture pattern lifted from tests/test_dashboard_api.py:97-135 -->

```python
from types import SimpleNamespace

def _ep(**kw):
    defaults = dict(
        host="example.com", port=0, protocol="",
        tls_version=None, cipher_suite=None, cert_not_after=None,
        # DAR-specific extensions:
        service_detail=None, dat_scan_json=None, scan_error=None,
        severity="INFO",
    )
    defaults.update(kw)
    return SimpleNamespace(**defaults)
```

Existing dashboard_client fixture available in tests/conftest.py (Phase 36 precedent).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing DAR test scaffold</name>
  <files>tests/test_dar_dashboard.py</files>
  <read_first>
    - tests/test_dashboard_api.py (full file — copy _ep factory + dashboard_client fixture pattern)
    - .planning/phases/39-data-at-rest-dashboard-tab/39-VALIDATION.md (test list)
    - .planning/phases/39-data-at-rest-dashboard-tab/39-RESEARCH.md §Validation Architecture (8 tests)
    - .planning/phases/39-data-at-rest-dashboard-tab/39-RESEARCH.md §Projection Logic (per-protocol expectations for assertions)
    - .planning/phases/39-data-at-rest-dashboard-tab/39-PATTERNS.md §tests/test_dar_dashboard.py
  </read_first>
  <behavior>
    All 8 tests defined in VALIDATION.md must exist and fail BEFORE Plan 02 implementation.
    - test_derive_dar_findings_db: POSTGRESQL endpoint with service_detail="PostgreSQL/ssl-enforced" → DarFinding(category="database")
    - test_derive_dar_db_postgresql: service_detail="PostgreSQL/ssl-off" → encryption_at_rest=False, tls_in_transit=False, severity="HIGH"
    - test_derive_dar_s3: dat_scan_json='{"service_detail":"S3/sse-s3"}' on protocol="S3" → encryption_mode="SSE-S3", encryption_at_rest=True, category="object_storage"
    - test_derive_dar_k8s_dispatch: two endpoints (one with "namespace" in dat, one with "provider"="EKS" in dat) → both produce DarFinding(category="kubernetes") with different field sets (namespace populated vs encryption_provider populated)
    - test_derive_dar_vault_dispatch: three endpoints with "key_name" / "mount_point" / "auth_path" in dat respectively → mount_type ∈ {"transit","pki","auth"}; seal_type and auto_unseal None for all
    - test_derive_dar_scan_error_excluded: endpoint with scan_error="boom" excluded from results
    - test_api_dar_findings_key: dashboard_client.get("/api/scan/latest") response contains "dar_findings" key
    - test_api_dar_findings_empty: when no DAR endpoints exist, dar_findings == [] (not absent)
  </behavior>
  <action>
    Create tests/test_dar_dashboard.py.

    1. Module docstring: '"""Phase 39 GAP-04: Data at Rest dashboard projection + API contract tests."""'

    2. Imports:
       ```python
       import json
       import pytest
       from types import SimpleNamespace
       ```

    3. Define `_ep(**kw)` factory at module top — copy exactly from tests/test_dashboard_api.py:100-104, EXTEND defaults with: `service_detail=None, dat_scan_json=None, scan_error=None, severity="INFO"`.

    4. Implement each of the 8 tests above as a top-level `def test_*` function. Each unit test does:
       ```python
       from quirk.dashboard.api.routes.scan import _derive_dar_findings
       out = _derive_dar_findings([_ep(...)])
       assert ...
       ```

    5. Integration tests use the existing `dashboard_client` fixture (already in tests/conftest.py from Phase 36 — import not needed, pytest auto-discovers):
       ```python
       def test_api_dar_findings_key(dashboard_client):
           resp = dashboard_client.get("/api/scan/latest")
           assert resp.status_code in (200, 404)
           if resp.status_code == 200:
               data = resp.json()
               assert "dar_findings" in data
               assert isinstance(data["dar_findings"], list)
       ```

    6. For test_derive_dar_db_postgresql, use:
       `_ep(host="db.test", port=5432, protocol="POSTGRESQL", service_detail="PostgreSQL/ssl-off", severity="HIGH")`
       and assert: `out[0].category == "database"`, `out[0].encryption_at_rest is False`, `out[0].tls_in_transit is False`, `out[0].severity == "HIGH"`.

    7. For test_derive_dar_s3, use:
       `_ep(host="bucket.s3", port=443, protocol="S3", dat_scan_json=json.dumps({"service_detail":"S3/sse-s3"}), severity="INFO")`
       and assert: `out[0].category == "object_storage"`, `out[0].encryption_mode == "SSE-S3"`, `out[0].encryption_at_rest is True`.

    8. For test_derive_dar_k8s_dispatch, build TWO endpoints in one list — one with `dat_scan_json=json.dumps({"namespace":"default","secret_type_counts":{"Opaque":3}})`, one with `dat_scan_json=json.dumps({"cluster":"prod","provider":"EKS","encryptionConfig":[{"resources":["secrets"]}]})`. Assert: 2 results, both category="kubernetes", first has `namespace=="default"`, second has `encryption_provider` populated and `namespace is None`.

    9. For test_derive_dar_vault_dispatch, build three endpoints with protocol="VAULT" and dat_scan_json containing key_name / mount_point / auth_path respectively. Assert all 3 results have `category=="vault"`, `mount_type` values are `"transit"`, `"pki"`, `"auth"`, and `seal_type is None and auto_unseal is None` for all.

    10. For test_derive_dar_scan_error_excluded, pass `[_ep(protocol="POSTGRESQL", scan_error="permission denied")]` — expect `out == []`.

    Per D-04: tests reference `_derive_dar_findings` from `quirk.dashboard.api.routes.scan` — implementation does not exist yet, so all tests MUST raise ImportError or AttributeError now (this is the RED state).

    Per CLAUDE.md: PEP 8, minimal diff. No new test infra — reuse pytest + existing dashboard_client fixture.
  </action>
  <verify>
    <automated>pytest tests/test_dar_dashboard.py --collect-only -q 2>&amp;1 | grep -c "test_" | awk '$1 >= 8 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/test_dar_dashboard.py` exits 0
    - `grep -c "^def test_" tests/test_dar_dashboard.py` returns >= 8
    - `grep -q "from quirk.dashboard.api.routes.scan import _derive_dar_findings" tests/test_dar_dashboard.py`
    - `grep -q "service_detail=None" tests/test_dar_dashboard.py` (extended fixture)
    - `grep -q "dat_scan_json=None" tests/test_dar_dashboard.py`
    - `grep -q "scan_error=None" tests/test_dar_dashboard.py`
    - `grep -q "test_derive_dar_findings_db" tests/test_dar_dashboard.py`
    - `grep -q "test_derive_dar_db_postgresql" tests/test_dar_dashboard.py`
    - `grep -q "test_derive_dar_s3" tests/test_dar_dashboard.py`
    - `grep -q "test_derive_dar_k8s_dispatch" tests/test_dar_dashboard.py`
    - `grep -q "test_derive_dar_vault_dispatch" tests/test_dar_dashboard.py`
    - `grep -q "test_derive_dar_scan_error_excluded" tests/test_dar_dashboard.py`
    - `grep -q "test_api_dar_findings_key" tests/test_dar_dashboard.py`
    - `grep -q "test_api_dar_findings_empty" tests/test_dar_dashboard.py`
    - `pytest tests/test_dar_dashboard.py -x -q` exits non-zero (RED — implementation missing)
    - `python -m compileall tests/test_dar_dashboard.py` exits 0
  </acceptance_criteria>
  <done>
    Test file exists with 8 tests + extended _ep fixture; all unit tests fail with ImportError on `_derive_dar_findings` import; integration tests fail until schema lands.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test code only — no runtime trust boundaries crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-00 | (n/a) | tests/test_dar_dashboard.py | accept | Test scaffold has no runtime exposure; no PII; no network calls |
</threat_model>

<verification>
- All 8 test names present (grep gates above)
- pytest collects ≥ 8 tests
- Tests RED on collection due to missing `_derive_dar_findings` symbol
</verification>

<success_criteria>
Wave 0 RED scaffold complete; Plan 02 has a feedback contract to GREEN against.
</success_criteria>

<output>
After completion, create `.planning/phases/39-data-at-rest-dashboard-tab/39-01-SUMMARY.md`
</output>
