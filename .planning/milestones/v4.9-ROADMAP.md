# Milestone v4.9: Audit Depth

**Status:** ✅ SHIPPED 2026-05-15
**Phases:** 69–77 (+ inserted 69.1)
**Total Plans:** 38

## Overview

**Milestone Goal:** Systematically close all 169 findings from the 2026-05-08 audit ledger (13 deferred BLOCKERs + 92 WARNINGs + 29 INFOs + 35 already-closed) and lock the invariant via a CI gate. AUDIT-TASKS.md reaches zero bare-open rows with every finding carrying an explicit `[x] closed`, `[ ] deferred-*`, or `[ ] wont-fix` disposition with inline rationale.

**Outcome:** 169 audit findings dispositioned across 9 phases (69–77); zero bare-open invariant locked via `tests/test_audit_ledger_zero_open.py` CI gate that fails any future regression.

## Phases

### Phase 69: Deferred BLOCKERs — Scanner + Cloud
**Goal**: The six highest-priority deferred BLOCKERs in the scanner and cloud subsystems are fixed — resource leaks eliminated, cloud data written to correct fields, and rate-limiting primitives safe under all inputs. Closes audit findings scanners-protocol/CR-07, CR-08 and scanners-cloud/CR-02, CR-03, CR-06, CR-07, CR-08, CR-09, CR-10.
**Depends on**: Phase 68 (v4.8 complete)
**Requirements**: BLOCK-01, BLOCK-02, BLOCK-03, BLOCK-04, BLOCK-05, BLOCK-06
**Plans**: 6 plans
  - 69-01 (Wave B): BLOCK-01 — sslyze try/finally cleanup + fingerprint socket close on all exception paths
  - 69-02 (Wave A): BLOCK-02 — GCP Cloud SQL severity → severity column; rewrite 3 existing tests
  - 69-03 (Wave B): BLOCK-03 — K8s empty-aks_clusters short-circuit (returns [] per D-09)
  - 69-04 (Wave A): BLOCK-04 — Azure Blob 3-way branch (BLOB-CMK / BLOB-PLATFORM / BLOB-UNKNOWN) via service_detail
  - 69-05 (Wave A): BLOCK-05 — cache.py ttl_hours<=0 returns None
  - 69-06 (Wave A): BLOCK-06 — TokenBucket capacity guard + threading.Condition + rate=0 fast path

### Phase 69.1: K8s Test Fixture Hardening (INSERTED)
**Goal**: Autouse `*_AVAILABLE` fixture added to `tests/test_k8s_connector.py` so the canonical `.venv` (Python 3.14) runs the full k8s_connector suite green regardless of optional cloud-SDK installation. Unblocks Phase 69-03 test + 4 pre-existing failures.
**Depends on**: Phase 69
**Plans**: 1 plan

### Phase 70: Deferred BLOCKERs — API + QRAMM Model
**Goal**: The two deferred BLOCKERs in the API/QRAMM subsystem are resolved — the `QRAMMProfile` table has a real DB-level FK constraint with safe session deletion, and the classifier no longer uses a bare `except` or interpolates unvalidated strings into DDL. Closes audit findings api-cli-core/CR-04, CR-05, CR-06, CR-07.
**Depends on**: Phase 69
**Requirements**: BLOCK-07, BLOCK-08
**Plans**: 3 plans
  - 70-01: FK retrofit on qramm_profiles.session_id + per-connection PRAGMA foreign_keys=ON + delete_session reorder
  - 70-02: Narrow _qs_for_alg except + module logger in scan.py
  - 70-03: _SAFE_COL_TYPE_RE allowlist in 4 _ensure_* helpers + AUDIT-TASKS row flips

### Phase 71: Protocol Scanner WARNINGs
**Goal**: All five WARNING clusters in the protocol scanner subsystem are resolved — coverage percentages are bounded, severity comparisons are case-insensitive, subprocess errors are logged not swallowed, nmap inputs are validated and parsed safely, identity scanner inputs are bounded, extras/ThreadPool/dedup issues fixed. Closes scanners-protocol/WR-01 through WR-14.
**Depends on**: Phase 69
**Requirements**: PROTO-01, PROTO-02, PROTO-03, PROTO-04, PROTO-05
**Plans**: 5 plans
  - 71-01: calculate_coverage clamp + case-insensitive severity in quantum_readiness_score
  - 71-02: Narrow WR-03 subprocess except + module logger in fingerprint scanner
  - 71-03: nmap default port CSV + extra_args allowlist + defusedxml parser
  - 71-04: DNSSEC bound + Kerberos decode-log + secrets nonce + SAML JSON byte cap
  - 71-05: Unified extras messaging + ScanCfg.motion_concurrency + delete tls_scanner dup + target_expander cap/dedup/normalize

### Phase 72: Cloud Scanner WARNINGs
**Goal**: All five WARNING clusters in the cloud scanner subsystem are resolved — AWS/Azure/GCP data correctness, Cache and scope_hash robustness, profiles.py mutation guards, and Vault/DB connector hardening. Closes scanners-cloud/WR-01 through WR-24.
**Depends on**: Phase 69
**Requirements**: CLOUD-01, CLOUD-02, CLOUD-03, CLOUD-04, CLOUD-05
**Plans**: 5 plans (24/24 WR rows closed, 119/119 targeted tests green)
  - 72-01: AWS connector hardening (ACM, KMS, S3, EKS)
  - 72-02: Azure + K8s data-correctness (KeyVault, K8s Counter/cluster_name)
  - 72-03: GCP connector correctness (KMS pagination cap, Cloud SQL service_detail)
  - 72-04: Cache + scope_hash + profiles.py integrity
  - 72-05: Misc cloud hardening (risk_engine→findings_evaluator rename, profiles mutation guard, vault PKI PEM, dedupe stable sort)

### Phase 73: CBOM + Intelligence + Reports WARNINGs
**Goal**: All three WARNING clusters in the CBOM/intelligence/reports subsystem are resolved — PDF resources are cleaned up, weak-crypto predicates are consistent, and score weights, roadmap output, and cipher labels are corrected. Closes cbom-intel-reports/WR-01 through WR-14.
**Depends on**: Phase 69
**Requirements**: INTEL-01, INTEL-02, INTEL-03
**Plans**: 3 plans
  - 73-01: INTEL-01 PDF render hardening (typed except + Playwright finally)
  - 73-02: Weak-crypto predicate unification (`quirk/util/weak_crypto.py`)
  - 73-03: SCORE_WEIGHTS normalization / roadmap / executive / TLS KEX / confidence

### Phase 74: QRAMM + Compliance WARNINGs
**Goal**: All three WARNING clusters in the QRAMM/compliance subsystem are resolved — practice scores reject out-of-range inputs, the evidence bridge is TZ-safe and idempotent, and migration advisor precision, coverage disambiguation, and stale comments are fixed. Closes qramm-compliance/WR-01 through WR-13.
**Depends on**: Phase 70
**Requirements**: QWARN-01, QWARN-02, QWARN-03
**Plans**: 3 plans
  - 74-01: QRAMM practice scoring correctness (range validation, endpoint count in Practice 1.1)
  - 74-02: Evidence bridge correctness (TZ-safe date compare, idempotent synchronize_session)
  - 74-03: Migration advisor + compliance map + model_meta helper (is_qramm_model_stale)

### Phase 75: API + CLI + Core WARNINGs
**Goal**: All four WARNING clusters in the API/CLI/core subsystem are resolved — doctor checks return meaningful data, scan-id time-window is microsecond-safe, list_scans grouping is correct, and QRAMM/interactive/validate/route input hardening is complete. Closes api-cli-core/WR-01 through WR-17.
**Depends on**: Phase 70
**Requirements**: APCL-01, APCL-02, APCL-03, APCL-04
**Plans**: 4 plans
  - 75-01: APCL-01 doctor actionability + DB path determinism
  - 75-02: APCL-02 API correctness (scan-id microsecond window, list_scans grouping)
  - 75-03: APCL-03 QRAMM/DAR API hardening
  - 75-04: APCL-04 interactive + validate + routes hardening

### Phase 76: React Frontend WARNINGs
**Goal**: All three WARNING clusters in the React frontend are resolved — API error surfacing, localStorage/PDF/ComplianceMapTab correctness, and cert regex/CBOM typing/scorecard math are fixed. Closes react-frontend/WR-02, WR-04 through WR-13.
**Depends on**: Phase 75
**Requirements**: REACT-01, REACT-02, REACT-03
**Plans**: 3 plans (11/11 WR rows closed)
  - 76-01: REACT-01 API error surfacing (useScanList, executive body.detail, QRAMM submitError)
  - 76-02: REACT-02 localStorage allowlist, PDF cleanup, ComplianceMapTab dep narrowing
  - 76-03: REACT-03 cert regex / Cytoscape typing / Scorecard math

### Phase 77: INFO/Code Quality + Audit Ledger Closure
**Goal**: All four INFO/code-quality requirement groups are addressed across the four subsystems (protocol scanner, CBOM/intelligence, API/CLI, React frontend), and AUDIT-TASKS.md is brought to zero bare-open rows — every one of the 169 findings carries an explicit closed, deferred, or wont-fix disposition. Closes scanners-protocol/IN-01..06, cbom-intel-reports/IN-01..09, api-cli-core/IN-01..07, react-frontend/IN-01..07, and LEDGER-01.
**Depends on**: Phase 71, Phase 72, Phase 73, Phase 74, Phase 75, Phase 76
**Requirements**: INFO-01, INFO-02, INFO-03, INFO-04, LEDGER-01
**Plans**: 5 plans (29 INFO rows closed, LEDGER-01 zero-bare-open invariant + CI gate)
  - 77-01: INFO-01 protocol scanner INFOs (6 rows)
  - 77-02: INFO-02 CBOM/intelligence/reports INFOs (9 rows)
  - 77-03: INFO-03 API/CLI INFOs (7 rows)
  - 77-04: INFO-04 React frontend INFOs (7 rows)
  - 77-05: LEDGER-01 audit ledger closure + D-31 CI gate

---

## Milestone Summary

**Decimal Phases:**

- Phase 69.1: K8s Test Fixture Hardening (inserted after Phase 69 to unblock test gating before Phase 70)

**Key Decisions:**

- v4.9-D-01: Deferred BLOCKERs split into two phases by code locality — scanner/cloud resource correctness (Phase 69) vs API/QRAMM model integrity (Phase 70). Both highest-priority and unblock WARNING fixes in the same subsystems.
- v4.9-D-02: WARNING phases strictly subsystem-scoped (protocol scanner, cloud scanner, CBOM/intel/reports, QRAMM/compliance, API/CLI/core, React frontend) to eliminate cross-phase merge conflicts.
- v4.9-D-03: LEDGER-01 (audit closure) folded into Phase 77 as the final step — all finding rows dispositioned as phases 69–76 execute.
- v4.9-D-04: INFO/code-quality items (INFO-01..04) batched into Phase 77 alongside LEDGER-01 since they are low-risk, non-interdependent, and a natural final-cleanup sweep.
- Phase 77 D-05: Weak-crypto predicates consolidated into `quirk/util/weak_crypto.py` with `Phase 77 D-05` annotations at all 4 consumer call-sites (evidence.py, broker_scanner.py, email_scanner.py, tls_scanner.py).
- Phase 77 D-31: CI gate `tests/test_audit_ledger_zero_open.py` installs two invariants — zero `[ ] open` bare rows AND every deferred/wont-fix row has `— <rationale>` inline. Lock forward.

**Issues Resolved:**

- 169 audit findings (from 2026-05-08 audit) fully dispositioned: 166 `[x] closed`, 2 `[ ] deferred-*` with rationale, 4 `[ ] wont-fix` with rationale.
- All 13 deferred BLOCKERs (BLOCK-01..08 + cluster CR rows) closed across Phases 69 + 70.
- All 92 WARNINGs closed across Phases 71–76 by subsystem.
- All 29 INFOs closed in Phase 77.
- 4 cross-phase wires verified end-to-end via integration audit (`.planning/v4.9-MILESTONE-AUDIT.md`): weak_crypto consolidation, migration_advisor `_matches`, AUDIT-TASKS invariant, CI gate.

**Issues Deferred:**

- Items in REQUIREMENTS.md Future Requirements section: HTML/PDF injection hardening (separate attack surface shape; v4.8 D-06), migration_planner.py deletion (wont-fix per scanners-cloud/CR-01; v5.2 tech-debt sweep), CBOM FIPS 140-3 CMVP attestation (requires attestation data feed), S/MIME content scanning, Windows AD CS live connector.

**Technical Debt Incurred:**

- `.planning/REQUIREMENTS.md` traceability table retained stale `Pending` status column at milestone close (documentation hygiene — phase VERIFICATION.md files are authoritative). Noted in audit as WARNING-class.
- Phase 999.83 (Chaos Lab Service Config Drift) remains open as a backlog/standalone phase outside v4.9 scope.

**Audit Verdict (`.planning/v4.9-MILESTONE-AUDIT.md`):**

- Audit ledger invariant: PASSED (0 bare-open, 166 closed, 2 deferred, 4 wont-fix; CI gate green).
- Requirements coverage: PASSED (all 35 v4.9 requirements have a closing phase with `status: passed`).
- Cross-phase integration: PASSED (4 key wires verified).

---

**Timeline:** 2026-05-14 → 2026-05-15 (~24 hours wall-clock).
**Git range:** `5e20a23` (test 69-01 RED) → `9415f38` (docs(77): phase wrap + v4.9 milestone closure docs).
**Diff stat:** 339 files changed, 37,658 insertions(+), 2,359 deletions(-) across 210 commits.

_For current project status, see .planning/ROADMAP.md_
