---
phase: 79-smime-ldap-discovery-scanner
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - tests/test_smime_scanner.py
  - tests/test_smime_no_envelope_leak.py
  - tests/test_smime_ast_gate.py
  - tests/fixtures/smime/alice.der
  - tests/fixtures/smime/bob.der
  - tests/fixtures/smime/carol.der
  - docs/UAT-SERIES.md
autonomous: true
requirements:
  - SMIME-04
  - SMIME-08
must_haves:
  truths:
    - "tests/test_smime_scanner.py: scan_smime_targets returns the expected severity for each of three fixture certs (alice HIGH, bob HIGH, carol no finding) when ldap3 is mocked to return the fixture DER bytes"
    - "tests/test_smime_no_envelope_leak.py: when scan_smime_targets is given a target carrying sentinel To/From/Subject/Message-ID fields, none of those sentinel strings appears anywhere in the returned endpoints or smime_scan_json (SMIME-04 privacy invariant)"
    - "tests/test_smime_ast_gate.py: ast.parse of quirk/scanner/smime_scanner.py finds zero imports of imaplib / poplib / smtplib / email or email.* submodules (SMIME-08)"
    - "pytest tests/test_smime_*.py -x passes (3 new test files all green)"
    - "pytest as a whole has exactly one expected red: tests/test_score_weights_invariant.py (Phase 83 will close)"
    - "docs/UAT-SERIES.md gains SMIME test cases and is synced to the Obsidian vault"
    - "Obsidian phase note exists at /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-79-SMIME-LDAP-Discovery-Scanner.md"
  artifacts:
    - path: "tests/test_smime_scanner.py"
      provides: "Unit tests against mocked ldap3 returning alice/bob/carol fixtures"
    - path: "tests/test_smime_no_envelope_leak.py"
      provides: "Privacy invariant: no IMAP envelope fields leak into smime_scan_json"
    - path: "tests/test_smime_ast_gate.py"
      provides: "AST gate forbidding imaplib/poplib/smtplib/email.* imports in smime_scanner.py"
    - path: "tests/fixtures/smime/alice.der"
      provides: "RSA-1024 SHA-1 DER cert (HIGH)"
    - path: "tests/fixtures/smime/bob.der"
      provides: "RSA-1024 SHA-256 DER cert (HIGH key only)"
    - path: "tests/fixtures/smime/carol.der"
      provides: "RSA-2048 SHA-256 DER cert (SAFE)"
    - path: "docs/UAT-SERIES.md"
      provides: "SMIME test-case rows in the relevant series"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-79-SMIME-LDAP-Discovery-Scanner.md"
      provides: "Obsidian phase note (status: complete) with Goal/Requirements/Success Criteria/What Was Built"
  key_links:
    - from: "tests/test_smime_ast_gate.py"
      to: "quirk/scanner/smime_scanner.py"
      via: "ast.parse + ast.walk"
      pattern: 'TARGET = .*smime_scanner\.py'
    - from: "tests/test_smime_no_envelope_leak.py"
      to: "scan_smime_targets output"
      via: "sentinel-string absence assertion"
      pattern: 'SENTINEL_'
---

<objective>
Lock down Phase 79 with three test files plus phase closure (UAT-SERIES.md
update, Obsidian phase note, conventional commits).

Purpose:
- Prove SMIME-01 / SMIME-02 / SMIME-05 / SMIME-06 with a unit test exercising
  the three fixture certs.
- Prove SMIME-04 (privacy invariant) with a content-absence test.
- Prove SMIME-08 (AST CI gate) with a static-analysis test that fails if any
  IMAP/email import sneaks into `smime_scanner.py` in the future.
- Close the phase with documentation (UAT series + Obsidian note) per CLAUDE.md
  mandatory phase completion steps.

Output:
- Three new pytest files under `tests/`.
- Three DER cert fixtures under `tests/fixtures/smime/` (separate from chaos
  lab fixtures — keeps the test suite hermetic and copyable).
- `docs/UAT-SERIES.md` updated and synced to the Obsidian vault.
- Obsidian phase note written directly to the vault filesystem.
- Two atomic commits per CLAUDE.md / phase precedent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-smime-ldap-discovery-scanner/79-CONTEXT.md
@.planning/phases/79-smime-ldap-discovery-scanner/79-RESEARCH.md
@.planning/phases/79-smime-ldap-discovery-scanner/79-PATTERNS.md
@.planning/phases/79-smime-ldap-discovery-scanner/79-01-SUMMARY.md
@.planning/phases/79-smime-ldap-discovery-scanner/79-02-SUMMARY.md
@.planning/phases/79-smime-ldap-discovery-scanner/79-03-SUMMARY.md
@tests/test_scan_error_gate.py
@docs/UAT-SERIES.md
@CLAUDE.md
</context>

<interfaces>
<!-- Anchors verified 2026-05-16. -->

`tests/test_scan_error_gate.py` is the cleanest AST-walker template in the suite
(PATTERNS.md authoritative analog for SMIME-08).

The three DER fixture files in `tests/fixtures/smime/` should be either:
(a) byte-identical copies of `quantum-chaos-enterprise-lab/smime/certs/*.der`
    from Plan 79-01 (preferred — single regen source), OR
(b) regenerated via the same `regen.sh` script targeting the test fixtures dir.
Choose (a) — copy the bytes verbatim so the chaos lab and the test suite
share the same cryptographic surface.

CLAUDE.md mandatory phase completion steps (verbatim summary):
1. Create Obsidian phase note via direct filesystem Write (NOT `obsidian CLI content=` — files too large).
   Path: `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-79-SMIME-LDAP-Discovery-Scanner.md`
   Frontmatter: `status: complete`, `type: phase`, `source: .planning/phases/79-smime-ldap-discovery-scanner/`, `updated: 2026-05-16`
   Sections: Goal | Requirements Covered (SMIME-01…08) | Success Criteria
   (from ROADMAP) | What Was Built (one subsection per plan, sourced from
   each plan's SUMMARY.md) | `[[Roadmap]]` link.
2. Update `docs/UAT-SERIES.md` with new SMIME test cases.
3. Sync UAT-SERIES.md to vault via direct filesystem write (per CLAUDE.md
   pattern with `/tmp/uat_vault.md` staging).
4. Commit `docs/UAT-SERIES.md`.

Forbidden imports for the AST gate (D-79-R1 / SMIME-08 / CONTEXT cross-cutting):
- Modules: `imaplib`, `poplib`, `smtplib`, `email`
- ImportFrom prefixes: `email.` (catches `email.message`, `email.header`, etc.)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create unit test + DER fixtures + privacy invariant test</name>
  <files>
    tests/test_smime_scanner.py,
    tests/test_smime_no_envelope_leak.py,
    tests/fixtures/smime/alice.der,
    tests/fixtures/smime/bob.der,
    tests/fixtures/smime/carol.der
  </files>
  <action>
    Step A — Copy the three DER fixtures from
    `quantum-chaos-enterprise-lab/smime/certs/{alice,bob,carol}.der` into
    `tests/fixtures/smime/`. Byte-identical — same regen source ensures the
    chaos lab oracle and the unit suite agree.

    Step B — Create `tests/test_smime_scanner.py`. Use `unittest.mock.patch`
    on `ldap3.Server` and `ldap3.Connection`. Make `Connection.bind()` return
    True. Make `Connection.extend.standard.paged_search` return an iterable
    of dict entries, one per fixture user. Each entry's `raw_attributes`
    contains a `userSMIMECertificate` key with a `list[bytes]` value
    (the DER bytes loaded from `tests/fixtures/smime/<user>.der`).

    Assertions per cert:
    - alice (RSA-1024 SHA-1) → exactly one `CryptoEndpoint` with
      `protocol="SMIME"`, `severity="HIGH"`, `cert_pubkey_alg="RSA"`,
      `cert_pubkey_size=1024`, `smime_scan_json` non-empty, reasons include
      both `weak-signing-alg` and `weak-rsa-key`.
    - bob (RSA-1024 SHA-256) → exactly one endpoint, `severity="HIGH"`,
      reasons include `weak-rsa-key` and NOT `weak-signing-alg`.
    - carol (RSA-2048 SHA-256) → ZERO endpoints emitted (SAFE — no finding).

    Step C — Create `tests/test_smime_no_envelope_leak.py` (SMIME-04 privacy
    invariant). Single test function (PATTERNS.md template):
    ```python
    def test_no_imap_envelope_fields_in_smime_output():
        target = SimpleNamespace(
            host="ldap.example.com",
            realm="QUIRK.LAB",
            to="SENTINEL_TO_FIELD",
            from_="SENTINEL_FROM_FIELD",
            subject="SENTINEL_SUBJECT_FIELD",
            message_id="SENTINEL_MESSAGEID_FIELD",
        )
        with mocked_ldap3_returning(fixture_dir / "carol.der"):
            endpoints = scan_smime_targets([target])
        blob = json.dumps([ep.model_dump() if hasattr(ep, "model_dump") else ep.__dict__ for ep in endpoints])
        for sentinel in ("SENTINEL_TO_FIELD", "SENTINEL_FROM_FIELD",
                         "SENTINEL_SUBJECT_FIELD", "SENTINEL_MESSAGEID_FIELD"):
            assert sentinel not in blob, f"SMIME-04 envelope leak: {sentinel}"
    ```
    Use `carol.der` so the test still flows through the scanner end-to-end even
    though carol produces no finding — the assertion is content-absence across
    `endpoints` (which may be empty) AND any internal state the scanner might
    leak. Optionally also test with `alice.der` so the assertion runs against
    a non-empty endpoint list.

    Implements SMIME-04 privacy leg.
  </action>
  <verify>
    <automated>pytest tests/test_smime_scanner.py tests/test_smime_no_envelope_leak.py -x</automated>
  </verify>
  <done>
    Both new test files pass. Three DER fixtures committed under
    `tests/fixtures/smime/`. The scanner's contract (alice HIGH+two-reason,
    bob HIGH+one-reason, carol no-finding) is locked into pytest.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SMIME-08 AST CI gate</name>
  <files>tests/test_smime_ast_gate.py</files>
  <action>
    Create `tests/test_smime_ast_gate.py` by cloning the structural shape of
    `tests/test_scan_error_gate.py` (the PATTERNS.md cleanest analog).
    Body per PATTERNS.md template:
    ```python
    import ast, pathlib
    PROJECT_ROOT = pathlib.Path(__file__).resolve().parent.parent
    TARGET = PROJECT_ROOT / "quirk" / "scanner" / "smime_scanner.py"

    FORBIDDEN_MODULES = {"imaplib", "poplib", "smtplib", "email"}
    FORBIDDEN_FROM_PREFIXES = ("email.",)

    def test_smime_scanner_no_imap_or_envelope_imports():
        tree = ast.parse(TARGET.read_text())
        violations = []
        for node in ast.walk(tree):
            if isinstance(node, ast.Import):
                for alias in node.names:
                    if alias.name in FORBIDDEN_MODULES \
                       or any(alias.name.startswith(p) for p in FORBIDDEN_FROM_PREFIXES):
                        violations.append(f"import {alias.name}")
            elif isinstance(node, ast.ImportFrom):
                mod = node.module or ""
                if mod in FORBIDDEN_MODULES \
                   or any(mod.startswith(p) for p in FORBIDDEN_FROM_PREFIXES):
                    violations.append(f"from {mod} import ...")
        assert not violations, f"SMIME-08 violation — IMAP/envelope imports: {violations}"
    ```

    Implements SMIME-08.
  </action>
  <verify>
    <automated>pytest tests/test_smime_ast_gate.py -x</automated>
  </verify>
  <done>
    `tests/test_smime_ast_gate.py` passes against the current
    `quirk/scanner/smime_scanner.py` (which contains zero forbidden imports
    per Plan 79-02 Task 1).
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full phase test sweep + first atomic commit</name>
  <files>(no file edits — verification + commit)</files>
  <action>
    Run the full test sweep for Phase 79 code and verify the expected red
    state:
    ```
    pytest tests/test_smime_scanner.py tests/test_smime_no_envelope_leak.py tests/test_smime_ast_gate.py -x
    pytest tests/ -x --ignore=tests/test_score_weights_invariant.py
    ```
    The first command must be all green. The second command (full suite minus
    the deliberately-red invariant test) must also be green. If anything else
    fails, halt and fix before committing.

    Then `python -m compileall quirk/` returns 0.

    Atomic commit #1 — code + tests:
    ```
    feat(79-04): smime scanner tests + AST gate

    - tests/test_smime_scanner.py: unit tests against mocked ldap3 + 3 DER fixtures
    - tests/test_smime_no_envelope_leak.py: SMIME-04 privacy invariant
    - tests/test_smime_ast_gate.py: SMIME-08 AST CI gate

    Note: tests/test_score_weights_invariant.py is intentionally red
    (sum drifted 261.0 -> 267.0); Phase 83 CLEAN-01 owns the
    consolidated bump after Phase 80 lands its weight.
    ```
    Explicit file paths only — NEVER `git add -A`.
  </action>
  <verify>
    <automated>pytest tests/test_smime_scanner.py tests/test_smime_no_envelope_leak.py tests/test_smime_ast_gate.py -x &amp;&amp; pytest tests/ --ignore=tests/test_score_weights_invariant.py -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    All three SMIME test files pass. Full suite (minus the one expected red
    test) passes. Commit landed with the explicit file list documented above.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update docs/UAT-SERIES.md + sync to vault + commit</name>
  <files>docs/UAT-SERIES.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md</files>
  <action>
    Per CLAUDE.md mandatory phase completion steps #2-4:

    Step A — Read `docs/UAT-SERIES.md` to find the most appropriate series for
    SMIME (likely the Identity / Scanner-Coverage series). Add test rows:
    - UAT case: "Run smime chaos lab profile end-to-end"
      - Setup: `PROFILE_ARGS="--profile smime" ./lab.sh up`
      - Action: `quirk scan --smime-target ldap://localhost:38900 --smime-base dc=quirk,dc=lab`
      - Pass criteria: scan output contains 2 HIGH SMIME findings (alice, bob);
        no SMIME finding for carol; `smime_scan_json` populated in
        `crypto_endpoints` row; Identity tab in the React dashboard renders
        the findings.
    - UAT case: "Verify SMIME-04 privacy invariant"
      - Action: `pytest tests/test_smime_no_envelope_leak.py -x`
      - Pass criteria: test passes; manual grep of `smime_scan_json` for
        `To:|From:|Subject:|Message-ID:` returns zero matches.
    - UAT case: "Verify SMIME-08 AST gate"
      - Action: `pytest tests/test_smime_ast_gate.py -x`
      - Pass criteria: test passes; adding any `import imaplib` line to
        `quirk/scanner/smime_scanner.py` and re-running the test must produce
        a non-zero exit.
    Update `**Last Updated:**` to today's date (2026-05-16).

    Step B — Sync to Obsidian vault using the CLAUDE.md staging pattern
    (UAT-SERIES.md is too large for `obsidian CLI content=`):
    ```
    printf '%s\n' '---' 'project: QU.I.R.K.' 'type: reference' \
      'status: active' 'source: docs/UAT-SERIES.md' \
      'updated: 2026-05-16' '---' '' > /tmp/uat_vault.md
    cat docs/UAT-SERIES.md >> /tmp/uat_vault.md
    cp /tmp/uat_vault.md /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md
    ```

    Step C — Atomic commit #2 — docs only:
    ```
    docs(phase-79): update UAT-SERIES.md
    ```
    Files: `docs/UAT-SERIES.md` only (the vault file is outside the repo).
    NEVER `git add -A`.
  </action>
  <verify>
    <automated>grep -c -i 'smime' docs/UAT-SERIES.md &amp;&amp; ls -la /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md</automated>
  </verify>
  <done>
    `docs/UAT-SERIES.md` contains at least 3 new SMIME-related rows and the
    Last Updated date is 2026-05-16. The vault copy at
    `20_Dev-Work/QUIRK/UAT-Series.md` matches (with vault frontmatter prepended).
    Second commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 5: Write Obsidian phase note (direct filesystem) + final verification</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-79-SMIME-LDAP-Discovery-Scanner.md</files>
  <action>
    Per CLAUDE.md mandatory phase completion step #1, write the phase note
    directly to the vault filesystem with the Write tool (NOT `obsidian CLI
    content=` — files too large for shell expansion).

    Source the body from the four plan SUMMARY.md files
    (`79-01-SUMMARY.md` through `79-04-SUMMARY.md`).

    Frontmatter:
    ```
    ---
    project: QU.I.R.K.
    type: phase
    status: complete
    source: .planning/phases/79-smime-ldap-discovery-scanner/
    updated: 2026-05-16
    ---
    ```

    Sections required (mirror the existing
    `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-78-*.md` shape):
    1. `# Phase 79: S/MIME LDAP Discovery Scanner`
    2. `## Goal` — verbatim from ROADMAP.md Phase 79 entry (lines 1528-1538).
    3. `## Requirements Covered` — bullet list of SMIME-01 through SMIME-08.
    4. `## Success Criteria` — 5 truths from ROADMAP.md verbatim.
    5. `## What Was Built` — one subsection per plan:
       - `### Plan 79-01: Foundation (ORM column + chaos lab)`
       - `### Plan 79-02: Scanner module + CBOM wiring`
       - `### Plan 79-03: Scoring + evidence counters`
       - `### Plan 79-04: Tests + AST gate + phase closure`
       Each subsection is a 2-4 line summary drawn from the corresponding
       SUMMARY.md.
    6. `## Notes` — call out the deliberate red state of
       `tests/test_score_weights_invariant.py` (Phase 83 owns the
       consolidated bump per D-79-R3) and the LDAPS deferral (D-79-R9 →
       Phase 82 or follow-up).
    7. `## Related` — `[[Roadmap]]`, `[[Requirements]]`, `[[Phase-78-HTML-PDF-Injection-Hardening]]`,
       `[[Phase-80-Windows-AD-CS-Scanner]]` (the latter may not exist yet —
       that's fine, leave the wikilink).

    This task does NOT produce a git commit — vault files are outside the
    repo.

    Final phase verification (gate before declaring complete):
    - `python -m compileall quirk/` exits 0
    - `pytest tests/test_smime_*.py -x` all green
    - `pytest tests/` shows exactly ONE failure: `test_score_weights_invariant`
      (intentional per D-79-R3)
    - Both phase commits landed (`feat(79-04): ...` and `docs(phase-79): ...`)
    - Vault phase note exists and renders without errors
    - All four plan SUMMARY.md files exist
  </action>
  <verify>
    <automated>ls -la /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-79-SMIME-LDAP-Discovery-Scanner.md &amp;&amp; pytest tests/test_smime_scanner.py tests/test_smime_no_envelope_leak.py tests/test_smime_ast_gate.py -x</automated>
  </verify>
  <done>
    Obsidian phase note exists at the vault path. SMIME unit + privacy + AST
    tests all pass. The phase is complete; only `test_score_weights_invariant`
    remains intentionally red (Phase 83 will close it).
  </done>
</task>

</tasks>

<verification>
- `python -m compileall quirk/` exits 0
- `pytest tests/test_smime_scanner.py tests/test_smime_no_envelope_leak.py tests/test_smime_ast_gate.py -x` all green
- Full `pytest tests/` shows exactly one expected failure
  (`test_score_weights_invariant`) — intentional per D-79-R3.
- `docs/UAT-SERIES.md` contains SMIME rows and **Last Updated:** 2026-05-16.
- Obsidian vault note `Phase-79-SMIME-LDAP-Discovery-Scanner.md` exists with
  status: complete.
- Two atomic commits landed:
  1. `feat(79-04): smime scanner tests + AST gate`
  2. `docs(phase-79): update UAT-SERIES.md`
  NEVER `git add -A`.
</verification>

<success_criteria>
- SMIME-04 satisfied (privacy invariant test green).
- SMIME-08 satisfied (AST CI gate green and will fail on any future
  IMAP/email import drift).
- All SMIME requirements (SMIME-01 through SMIME-08) covered across the
  four plans; verification mapping documented in the orchestrator brief.
- Phase 79 fully closed per CLAUDE.md mandatory phase completion steps:
  UAT-SERIES.md updated + synced to Obsidian, phase note written directly
  to the vault filesystem.
- Known red: `tests/test_score_weights_invariant.py` — Phase 83 CLEAN-01
  owns the consolidated bump.
</success_criteria>

<output>
After completion, create
`.planning/phases/79-smime-ldap-discovery-scanner/79-04-SUMMARY.md`
summarizing: test files created, fixtures path, AST gate forbidden-import set,
UAT-SERIES.md row count, Obsidian vault note path, and the deliberate red
state of `test_score_weights_invariant.py` for Phase 83.
</output>
