---
phase: 78-html-pdf-injection-hardening
plan: 03
type: execute
wave: 2
depends_on: [78-01]
files_modified:
  - quirk/reports/executive.py
  - quirk/reports/writer.py
  - tests/test_md_cell_escape.py
autonomous: true
requirements:
  - HARDEN-01
  - HARDEN-03
must_haves:
  truths:
    - "Every scanner-controlled cell in quirk/reports/executive.py markdown emission wraps the value in md_cell()"
    - "Every scanner-controlled cell in quirk/reports/writer.py::_scorecard_markdown / _roadmap_markdown wraps in md_cell()"
    - "md_cell() escapes pipe `|`, newline `\\n`, carriage return `\\r`, and backtick characters (unit test asserts all four)"
    - "Integer cells (port, count, score) are NOT wrapped — only string-typed adversary-controllable cells"
    - "Existing technical.py md_cell coverage is preserved (regression-only)"
  artifacts:
    - path: "quirk/reports/executive.py"
      provides: "md_cell-wrapped markdown emission for scanner-controlled cells"
      contains: "from quirk.reports._md_escape import md_cell"
    - path: "quirk/reports/writer.py"
      provides: "md_cell-wrapped _scorecard_markdown + _roadmap_markdown"
      contains: "md_cell("
    - path: "tests/test_md_cell_escape.py"
      provides: "Unit tests for md_cell escape characters"
      contains: "def test_"
  key_links:
    - from: "quirk/reports/executive.py"
      to: "quirk/reports/_md_escape.py"
      via: "from quirk.reports._md_escape import md_cell"
      pattern: "from quirk.reports._md_escape import md_cell"
    - from: "quirk/reports/writer.py"
      to: "quirk/reports/_md_escape.py"
      via: "md_cell wrap in _scorecard_markdown / _roadmap_markdown"
      pattern: "md_cell\\("
---

<objective>
Extend `md_cell()` coverage from the already-wrapped `technical.py` reference
to every remaining scanner-controlled markdown emission site identified in
RESEARCH.md Cluster A: `executive.py` lines 169/188/206/222/224/235/238 and
`writer.py` lines 74-75/81/97. Add explicit unit tests proving `md_cell`
escapes `|`, `\n`, `\r`, and backtick.

Purpose: Closes HARDEN-01 (md_cell parity) and the markdown portion of
HARDEN-03 (scanner-emitted free-text sanitized before render). Pair with
Plan 02 (Jinja sanitize) to cover both render targets.

Output: executive.py + writer.py edits, new tests/test_md_cell_escape.py,
atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-html-pdf-injection-hardening/78-CONTEXT.md
@.planning/phases/78-html-pdf-injection-hardening/78-RESEARCH.md
@.planning/phases/78-html-pdf-injection-hardening/78-PATTERNS.md
@quirk/reports/_md_escape.py
@quirk/reports/technical.py
@quirk/reports/executive.py
@quirk/reports/writer.py

<interfaces>
<!-- Existing reference: quirk/reports/_md_escape.py — md_cell(value) -> str -->
<!-- Already escapes pipe / newline / CRLF / control chars per CONTEXT.md       -->

<!-- Reference call pattern (technical.py:44, 63, 83, 99):                      -->
```python
from quirk.reports._md_escape import md_cell
f"| {md_cell(e.host)} | {e.port} | {md_cell(_service_detail(e))} |"
# Strings wrapped; integers (port) NOT wrapped.
```

<!-- Cluster A target sites per RESEARCH.md §Scanner-String Emission Sites:    -->
<!-- executive.py:                                                              -->
<!--   :169  f"- {d['reason']} (**-{d['points']}**)"      → wrap reason         -->
<!--   :188  f"  - {category}: {count}"                   → wrap category       -->
<!--   :206  f"- {b}"                                      → wrap b              -->
<!--   :222  f"- **{item['title']}** — {item['why']}"     → wrap title + why    -->
<!--   :224  f"  - Owner: ... | Timeframe: ..."           → wrap both           -->
<!--   :235  f"- **{r.get('path')}** — {r.get('recommendation')}" → wrap both   -->
<!--   :238  f"  - Target: {host}:{port} | Severity: ..." → wrap host           -->
<!-- writer.py:                                                                  -->
<!--   :74-75 _scorecard_markdown driver lines                                  -->
<!--   :81    _scorecard_markdown bold-title bullet                             -->
<!--   :97    _roadmap_markdown bold-title bullet                               -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests/test_md_cell_escape.py first (RED)</name>
  <files>tests/test_md_cell_escape.py</files>
  <behavior>
    - test_pipe_escaped: `md_cell("a|b")` contains escaped pipe (output does NOT contain a raw `|` that would split a GFM column)
    - test_newline_neutralized: `md_cell("a\nb")` does NOT contain `\n` (newline collapsed/escaped — CONTEXT.md mandates `\n` neutralized)
    - test_crlf_neutralized: `md_cell("a\r\nb")` does NOT contain `\r` or `\n`
    - test_backtick_handled: `md_cell("a`b")` — assert the output is a string (backtick policy per existing `_md_escape.py` behavior; if it currently escapes, assert escape; if it currently passes through, assert passthrough — read the existing module to determine the truthful assertion). If `_md_escape.py` does NOT currently escape backtick, mark the assertion as "passthrough (CONTEXT.md backtick-deferred)" with a comment and the explicit assertion that the output type is `str`.
    - test_control_char_handled: `md_cell("a\x07b")` returns a `str` without raising
    - test_none_returns_empty_or_dash: behavior matches the existing `md_cell()` contract for None input (verify against `_md_escape.py`; assert whatever the current module does)
    - test_integer_passes: `md_cell(42)` returns `"42"` (existing behavior — coerce via str)
  </behavior>
  <action>
    Create `tests/test_md_cell_escape.py`. Standard pytest module: `from quirk.reports._md_escape import md_cell`. Before writing assertions, Read `quirk/reports/_md_escape.py` once to determine the current escape policy for backtick / None / control chars — the test must encode the ACTUAL contract, not a hypothetical one. Document any deferred-policy areas in a docstring inside the test (e.g., "backtick passthrough per CONTEXT.md deferred backtick guard, R-4"). Run the tests; they should pass against the existing `_md_escape.py` (this plan does NOT modify the escape module — it only extends caller coverage). If any assertion fails, the failure indicates a contract mismatch between RESEARCH/CONTEXT expectations and the existing module — fix the assertion to match reality and note the discrepancy in the SUMMARY.
  </action>
  <verify>
    <automated>pytest tests/test_md_cell_escape.py -x -q</automated>
  </verify>
  <done>tests/test_md_cell_escape.py passes; pipe/newline/CRLF/control-char escapes verified against the actual `_md_escape.py` contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wrap scanner-controlled cells in quirk/reports/executive.py</name>
  <files>quirk/reports/executive.py</files>
  <behavior>
    - `from quirk.reports._md_escape import md_cell` is added to the import block (mirrors `technical.py:4`)
    - Line 169 wraps `d['reason']` in `md_cell()`
    - Line 188 wraps `category` in `md_cell()` (count is int — NOT wrapped)
    - Line 206 wraps `b` in `md_cell()`
    - Line 222 wraps both `item['title']` and `item['why']` in `md_cell()`
    - Line 224 wraps both `item['owner_placeholder']` and `item['timeframe']` in `md_cell()`
    - Line 235 wraps both `r.get('path')` and `r.get('recommendation')` in `md_cell()`
    - Line 238 wraps `r.get('host')` in `md_cell()`; `r.get('port')` and `r.get('severity')` evaluation policy: per RESEARCH §Cluster A, port is numeric (NOT wrapped); severity is an internal enum-like string (NOT wrapped per RESEARCH "internally formatted strings; low-risk"). When in doubt between wrap and pass-through, prefer wrap (R-4 defense-in-depth).
    - No other behavioral changes (the markdown text body, headings, ordering all preserved)
    - Module compiles + existing executive.py tests (if any) still pass
  </behavior>
  <action>
    Edit `quirk/reports/executive.py`:
    1. Add `from quirk.reports._md_escape import md_cell` to the imports.
    2. Apply the wrap pattern at each enumerated line per the behavior list. Use the same idiom as `technical.py:44`: replace `{x}` with `{md_cell(x)}` inside f-strings. Do NOT modify the surrounding text (`"- "`, `"  - "`, `"**"`, `"—"`, `" | "`). When wrapping a `.get(...)` call, pass the full `.get('key','')` expression to `md_cell(...)` so empty/None inputs flow through the helper's None handling.
    3. Per R-4 (RESEARCH): `md_cell` is GFM-table-shaped but acceptable for bullet contexts in Phase 78 — log this as accepted technical debt in the SUMMARY, do NOT introduce a separate `md_text` helper this phase.
    4. Do NOT touch any non-scanner-controlled interpolation (literal headings, score numerics, date strings).
  </action>
  <verify>
    <automated>python -m compileall quirk/reports/executive.py &amp;&amp; grep -q "from quirk.reports._md_escape import md_cell" quirk/reports/executive.py &amp;&amp; [ "$(grep -c "md_cell(" quirk/reports/executive.py)" -ge 10 ]</automated>
  </verify>
  <done>executive.py imports md_cell; at least 10 md_cell() call sites present (covering lines 169, 188, 206, 222a, 222b, 224a, 224b, 235a, 235b, 238); compileall clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wrap scanner-controlled cells in quirk/reports/writer.py (_scorecard_markdown + _roadmap_markdown)</name>
  <files>quirk/reports/writer.py</files>
  <behavior>
    - `from quirk.reports._md_escape import md_cell` is present (add if not already)
    - `_scorecard_markdown` lines 74-75 wrap each `d` (driver string) in `md_cell()`
    - `_scorecard_markdown` line 81 wraps both `a.get('title')` and `a.get('why')` in `md_cell()`
    - `_roadmap_markdown` line 97 wraps `r.get('title')`, `r.get('why')`, and the `dep_txt` concatenation components in `md_cell()`
    - Numeric/score values remain unwrapped
    - Module compiles + `pytest tests/test_reports_writer.py -x -q` still passes (per R-6 RESEARCH note: update any pre-existing assertion that relies on raw `<`/`|` in output, and document in the commit message)
  </behavior>
  <action>
    Edit `quirk/reports/writer.py`:
    1. Verify the `md_cell` import — add `from quirk.reports._md_escape import md_cell` if absent.
    2. In `_scorecard_markdown`, at lines 74-75 (driver bullet) and 81 (action bullet), wrap each scanner-controlled string in `md_cell(...)` using the technical.py pattern.
    3. In `_roadmap_markdown`, at line 97, wrap `r.get('title')`, `r.get('why')`, and (if `dep_txt` is constructed from scanner-derived strings, e.g., dependency titles) wrap those components individually as well. If `dep_txt` is already a plain literal joiner (e.g., " (depends: ...)"), only wrap the scanner-controlled fragments inside it.
    4. Do NOT modify `render_html_report` invocation (line 194) — Plan 04 hardens the Playwright path.
    5. If any pre-existing test in `tests/test_reports_writer.py` asserts on raw `|` or `\n` in writer output, update the assertion to match the now-escaped form and note the update in the commit body (per R-6).
  </action>
  <verify>
    <automated>python -m compileall quirk/reports/writer.py &amp;&amp; pytest tests/test_reports_writer.py -x -q &amp;&amp; [ "$(grep -c "md_cell(" quirk/reports/writer.py)" -ge 5 ]</automated>
  </verify>
  <done>writer.py wraps Cluster A sites in _scorecard_markdown + _roadmap_markdown; existing tests pass (with any documented escape-assertion updates); compileall clean.</done>
</task>

<task type="auto">
  <name>Task 4: Compile + atomic commit Plan 03</name>
  <files>quirk/reports/executive.py, quirk/reports/writer.py, tests/test_md_cell_escape.py</files>
  <action>
    Run `python -m compileall quirk/` to confirm package-wide clean. Run the full Phase 78 test slice so far: `pytest tests/test_sanitize_scanner_text.py tests/test_md_cell_escape.py tests/test_reports_writer.py tests/test_report_sanitization.py -x -q`. Then atomically commit Plan 03:
    `node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "feat(78): wrap Cluster A markdown cells in md_cell across executive.py + writer.py + escape unit tests (HARDEN-01, HARDEN-03 markdown portion)" --files quirk/reports/executive.py quirk/reports/writer.py tests/test_md_cell_escape.py`
  </action>
  <verify>
    <automated>python -m compileall quirk/ &amp;&amp; pytest tests/test_sanitize_scanner_text.py tests/test_md_cell_escape.py tests/test_reports_writer.py tests/test_report_sanitization.py -x -q &amp;&amp; git log -1 --pretty=%s | grep -q "feat(78): wrap Cluster A markdown cells"</automated>
  </verify>
  <done>quirk/ compiles; all Phase 78 unit tests + existing reports suite pass; atomic commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanner finding/driver dict → markdown bullet/table cell | Adversary-controlled text crossing into `.md` deliverables (consumed by consultants, possibly piped into other tools) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-10 | Tampering | Pipe `\|` injection adding a fake GFM column to a finding row | mitigate | `md_cell()` escapes `\|` to `\\|` (existing helper) |
| T-78-11 | Tampering | CRLF injection adding a fake row to a markdown table | mitigate | `md_cell()` neutralizes `\n` and `\r\n` to a single space |
| T-78-12 | Tampering | Backtick injection creating spoofed inline code (e.g., faking a real CLI path) | accept | CONTEXT.md defers backtick handling; document as technical debt (R-4); revisit if downstream phase introduces backtick-sensitive renderer |
| T-78-13 | Tampering | Bullet-context reuse of md_cell (GFM-table-shaped helper) | accept | Documented technical debt per R-4 — pipe/newline escape still applies; backtick deferred |
</threat_model>

<verification>
- `python -m compileall quirk/` exits 0
- `pytest tests/test_md_cell_escape.py -x -q` passes (pipe/newline/CRLF/control-char + behavior matches existing _md_escape contract)
- `pytest tests/test_reports_writer.py tests/test_report_sanitization.py -x -q` passes
- `grep -c "md_cell(" quirk/reports/executive.py ≥ 10`
- `grep -c "md_cell(" quirk/reports/writer.py ≥ 5`
- Atomic commit landed
</verification>

<success_criteria>
1. Every Cluster A site enumerated in RESEARCH.md is wrapped in md_cell()
2. Unit tests prove pipe / newline / CRLF / control-char escape (and document backtick-deferred contract)
3. Existing reports tests pass (with any documented escape-assertion updates per R-6)
4. Atomic commit with conventional Phase 78 message
</success_criteria>

<output>
After completion, create `.planning/phases/78-html-pdf-injection-hardening/78-03-SUMMARY.md` with:
- Files edited (executive.py call-site count, writer.py call-site count)
- Test file added + test count
- Any pre-existing assertion updates (per R-6) listed by test name
- Requirements closed: HARDEN-01 (full), HARDEN-03 (markdown portion — HTML/PDF portion covered by Plan 02 + Plan 04)
</output>
