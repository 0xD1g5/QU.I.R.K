---
phase: 80-windows-adcs-scanner
plan: 02
type: execute
wave: 2
depends_on: [01]
parallel_safe_with: [80-03]
parallel_safe_note: >
  This plan touches scanner module + cbom/builder.py + run_scan.py + models.py.
  Plan 80-03 touches scoring.py + evidence.py. Zero file overlap → safe to
  execute in parallel within Wave 2. Both depend only on Plan 80-01.
files_modified:
  - quirk/scanner/adcs_scanner.py
  - quirk/models.py
  - quirk/cbom/builder.py
  - run_scan.py
autonomous: true
requirements:
  - ADCS-01
  - ADCS-02
  - ADCS-05
  - ADCS-06
  - ADCS-09
must_haves:
  truths:
    - "`scan_adcs_targets(targets, ...)` returns a list of CryptoEndpoint with `protocol='ADCS'` when given a seeded LDAP target"
    - "SIMPLE bind is attempted when adcs_user/adcs_password are configured; anonymous bind otherwise; bind failure emits an ADCS-UNREACH LOW finding instead of raising (D-80-R3 / ADCS-04 SC#2)"
    - "ESC1, ESC2, ESC3, ESC6 emit real misconfig findings (HIGH/MEDIUM per RESEARCH Pattern 2)"
    - "ESC4, ESC5, ESC7, ESC8 emit ADCS-COVERAGE-GAP LOW findings — one per non-LDAP-observable ESC class per target (D-80-R8)"
    - "CA signing cert weakness uses quirk/util/weak_crypto.py::is_weak_cipher"
    - "Module header contains the ADCS-09 read-only invariant statement verbatim"
    - "CBOM Pass-1 emits algorithm components for ADCS endpoints; Pass-2 and Pass-3 inline skip-tuples both include 'ADCS' (lines 538 and 622 per D-80-R1)"
    - "run_scan.py dispatches _run_adcs_phase after _run_smime_phase, resume-aware, data_at_rest stage"
  artifacts:
    - path: "quirk/scanner/adcs_scanner.py"
      provides: "scan_adcs_targets entry point + bind/search scaffolding + ESC classifier + CA cert parser + ADCS-09 module header"
      exports: ["scan_adcs_targets"]
      min_lines: 150
    - path: "quirk/cbom/builder.py"
      provides: "Pass-1 ADCS emit branch (after SMIME elif) + Pass-2/3 skip-list 'ADCS' literals at lines 538 + 622"
      contains: 'ep.protocol == "ADCS"'
    - path: "run_scan.py"
      provides: "_run_adcs_phase dispatcher + _dar_protocols append + adcs_endpoints resume bucket"
      contains: 'scan_adcs_targets'
  key_links:
    - from: "quirk/scanner/adcs_scanner.py"
      to: "quirk/util/weak_crypto.py::is_weak_cipher"
      via: "shared classifier (D-80 cross-cutting)"
      pattern: 'from quirk\.util\.weak_crypto import'
    - from: "quirk/scanner/adcs_scanner.py"
      to: "ldap3.Server + Connection + extend.standard.paged_search"
      via: "SIMPLE-or-ANONYMOUS bind + paged_search of CN=Configuration partition"
      pattern: 'paged_search'
    - from: "quirk/cbom/builder.py Pass-1"
      to: "_register_algorithm"
      via: 'elif ep.protocol == "ADCS"'
      pattern: 'ep\.protocol == "ADCS"'
    - from: "run_scan.py::_run_adcs_phase"
      to: "scan_adcs_targets"
      via: "config-driven ADCS branch (mirrors _run_smime_phase)"
      pattern: 'scan_adcs_targets'
---

<objective>
Build the AD CS scanner module, wire it into the CBOM builder (Pass-1 emit +
Pass-2/3 skip-list at the post-Phase-79 line numbers), and dispatch it from
`run_scan.py` after `_run_smime_phase`.

Purpose: This is the heart of Phase 80 — it produces the IdentityFinding
entries with `protocol="ADCS"` that the rest of the pipeline (scoring,
evidence, CBOM, dashboard) consumes. Implements the bulk of ADCS-01, ADCS-02,
ADCS-05, ADCS-06, and the runtime arm of ADCS-09 (module header invariant).

Output:
- New `quirk/scanner/adcs_scanner.py` (~150-200 lines, singular path per D-80-R9)
  with the ADCS-09 module header invariant statement verbatim.
- Pass-1 emit branch in `quirk/cbom/builder.py` immediately after the SMIME
  elif (after line 462 per D-80-R1).
- Two inline tuple appends — `"ADCS"` added at the post-Phase-79 line numbers
  **538** (Pass-2) and **622** (Pass-3) per D-80-R1 (NOT 528/611 as CONTEXT
  cites — PATTERNS.md drift table confirms 538/622 post-Phase-79).
- `_run_adcs_phase` dispatcher wired into `run_scan.py` after
  `_run_smime_phase`, with resume awareness and the data_at_rest stage label.
- `quirk/models.py::CryptoEndpoint` gains `adcs_scan_json: str | None = None`
  if not already present (Pydantic surface counterpart to the DB column).

This plan does NOT touch `quirk/intelligence/scoring.py` or
`quirk/intelligence/evidence.py` — Plan 80-03 owns those in parallel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/80-windows-adcs-scanner/80-CONTEXT.md
@.planning/phases/80-windows-adcs-scanner/80-RESEARCH.md
@.planning/phases/80-windows-adcs-scanner/80-PATTERNS.md
@.planning/phases/80-windows-adcs-scanner/80-01-SUMMARY.md
@quirk/scanner/smime_scanner.py
@quirk/scanner/saml_scanner.py
@quirk/scanner/kerberos_scanner.py
@quirk/util/weak_crypto.py
@quirk/cbom/builder.py
@quirk/models.py
@run_scan.py
@CLAUDE.md
</context>

<interfaces>
<!-- Anchors verified 2026-05-16 (post-Phase-79). Line numbers per D-80-R1/R2. -->

ldap3 bind+search pattern for the AD CS Configuration partition (RESEARCH
Pattern 1; D-80-R3 — SIMPLE bind preferred, ANONYMOUS fallback for chaos lab):
```
server = ldap3.Server(host, port=port, get_info=ldap3.ALL, connect_timeout=timeout)
if user and password:
    conn = ldap3.Connection(server, user=user, password=password,
                            authentication=ldap3.SIMPLE, receive_timeout=timeout)
else:
    conn = ldap3.Connection(server, authentication=ldap3.ANONYMOUS,
                            receive_timeout=timeout)
if not conn.bind():
    raise ldap3.core.exceptions.LDAPBindError(conn.last_error or "bind-rejected")
config_base = (server.info.other or {}).get("configurationNamingContext", [None])[0] \
              or f"CN=Configuration,{base_dn}"
```

CA enumeration search base:
`CN=Enrollment Services,CN=Public Key Services,CN=Services,{config_base}`
filter `(objectClass=pKIEnrollmentService)`, attributes
`["cn", "cACertificate", "certificateTemplates", "dNSHostName"]`.

Template enumeration search base:
`CN=Certificate Templates,CN=Public Key Services,CN=Services,{config_base}`
filter `(objectClass=pKICertificateTemplate)`, attributes per RESEARCH
Pattern 1 (msPKI-Certificate-Name-Flag, msPKI-Enrollment-Flag,
msPKI-Certificate-Application-Policy, pKIExtendedKeyUsage, msPKI-RA-Signature,
nTSecurityDescriptor, pKIKeyUsage, msPKI-Minimal-Key-Size).

ESC bitmask classifier — copy `_classify_template_escs(entry)` body from
RESEARCH.md Pattern 2 verbatim. Returns `list[(esc_id, severity, reasons)]`.
The classifier emits ESC1/ESC2/ESC3/ESC6 only. ESC4/ESC5/ESC7/ESC8 are
emitted as COVERAGE-GAP findings by the caller (D-80-R8).

CA cert parsing — mirror smime `_parse_smime_cert` shape but DER-only (RESEARCH
Pattern 3 — cACertificate is always DER per [MS-CRTD] §2.21). Use
`cryptography.x509.load_der_x509_certificate`. Returns
`{key_alg, key_bits, sig_hash, serial, not_after, expired}` or None on failure.

ADCS-UNREACH coverage gap (D-80-R3 + ADCS-04 SC#2 — RESEARCH Pattern 4):
emit `CryptoEndpoint(host=..., port=..., protocol="ADCS",
service_detail=f"adcs-unreachable|base={config_base}", severity="LOW",
scan_error=safe_str(exc), scan_error_category="exception")` and `continue` —
NEVER let the exception propagate to the scan session error log.

cbom/builder.py anchors (VERIFIED 2026-05-16 post-Phase-79 per D-80-R1):
- Pass-1 SMIME branch ends ~line 462; insert ADCS branch immediately after.
- Pass-2 skip tuple — line 538: append `"ADCS"`.
- Pass-3 skip tuple — line 622: append `"ADCS"`.
(CONTEXT cites 528/611 — STALE. PATTERNS.md drift table confirms 538/622.)

run_scan.py anchors (per RESEARCH "Hooking into cfg.connectors" + PATTERNS):
- `_run_smime_phase` at lines 1396-1415 — analog; insert `_run_adcs_phase`
  immediately after.
- `_dar_eps` aggregate at line ~1453 — include `adcs_endpoints`.
- Final concat at line ~1619 — include `adcs_endpoints`.
- `_dar_protocols` tuple at ~line 1210 — append `"ADCS"`.
- Resume bucket near line 1219 —
  `adcs_endpoints = [e for e in _resumed_endpoints if getattr(e, "protocol", "") == "ADCS"]`.
- Resume-count log at ~1226 — include adcs count.
Phase 79 mutated this file; analog lines may have shifted slightly — grep
`_run_smime_phase` to confirm exact offsets before editing.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create quirk/scanner/adcs_scanner.py + extend CryptoEndpoint</name>
  <files>quirk/scanner/adcs_scanner.py, quirk/models.py</files>
  <action>
    Create `quirk/scanner/adcs_scanner.py` (singular path — D-80-R9). Target
    150-200 lines. Mirror `quirk/scanner/smime_scanner.py` structurally.

    Required structure:

    1. Module docstring containing the ADCS-09 invariant statement verbatim
       (per CONTEXT D Module-header-invariant, locked):
       "Active Directory Certificate Services (AD CS) enumeration via
       authenticated LDAP. This scanner performs read-only LDAP enumeration
       of AD CS configuration. No certificate enrollment, no template
       creation, no CSR generation, no write operations under any code path.
       Enforced by tests/test_adcs_ast_gate.py (ADCS-09) and
       tests/test_adcs_no_writes.py.

       Phase 80 — ADCS-01 ... ADCS-08. Enumerates CA configurations under
       CN=Public Key Services,CN=Services,CN=Configuration,<root-DN> and
       certificate templates under CN=Certificate Templates,...; classifies
       msPKI-* attributes for ESC1-ESC8 observable misconfigurations."

    2. Imports — explicitly DO NOT import:
       - `certipy_ad`, `certipy` (D-80-R4 / v4.10-D-02 — pinned cryptography incompat)
       - `cryptography.x509.CertificateSigningRequestBuilder` (name-level check)
       - `impacket.*` LDAP modify modules
       Allowed: `ldap3` (under try/except gate with `LDAP3_AVAILABLE` flag per
       smime_scanner.py:17-21), `cryptography.x509` (load_der_x509_certificate
       ONLY — never CSR builder), standard library `logging`, `json`,
       `datetime`, plus `quirk.util.weak_crypto.is_weak_cipher` and
       `quirk.util.safe_str.safe_str` (Phase 59 LEAK-03).

    3. Module-level `logger = logging.getLogger(__name__)`.

    4. ESC bitmask constants — copy from RESEARCH.md Pattern 2 verbatim
       (CT_FLAG_ENROLLEE_SUPPLIES_SUBJECT=0x00000001,
       CT_FLAG_PEND_ALL_REQUESTS=0x00000002, EKU_CLIENT_AUTH,
       EKU_PKINIT_CLIENT_AUTH, EKU_SMART_CARD_LOGON, EKU_CERT_REQUEST_AGENT,
       EKU_ANY_PURPOSE, EKU_SUBORDINATE_CA, etc.).

    5. `_realm_to_base_dn(realm: str) -> str` helper — mirror the smime
       helper: `QUIRK.LAB` -> `DC=quirk,DC=lab`; empty input → `""`.

    6. `_parse_ca_cert(der_bytes: bytes) -> dict | None` — DER parse via
       `cryptography.x509.load_der_x509_certificate` (no PEM fallback — RESEARCH
       Pattern 3, [MS-CRTD] §2.21 cACertificate is always DER). Returns
       `{key_alg, key_bits, sig_hash, serial, not_after, expired}`. On parse
       failure log via `safe_str(exc)` and return None.

    7. `_classify_template_escs(entry: dict) -> list[tuple[str, str, list[str]]]`
       — copy body from RESEARCH Pattern 2 verbatim. Emits ESC1/ESC2/ESC3/ESC6
       findings only. Handles RESEARCH Pitfall 3 (`int(... or 0)` everywhere)
       and Pitfall 7 (bytes→str decode for OID list attrs).

    8. `_bind_and_query(host, port, timeout, *, user=None, password=None) ->
       tuple[Connection, str]` — duplicate scaffolding per CONTEXT D-Area-2
       (no shared helper extraction). Build server with `get_info=ldap3.ALL`
       so rootDSE `configurationNamingContext` is populated. SIMPLE bind if
       creds, ANONYMOUS otherwise. Raise `LDAPBindError` on bind failure so
       the caller converts to ADCS-UNREACH.

    9. Top-level `scan_adcs_targets(targets, timeout=10, logger=None,
       session_start=None, *, search_base=None, user=None, password=None) ->
       list[CryptoEndpoint]`:
       - ldap3 availability guard — if False, log WARNING, return `[]`.
       - For each target:
         a. Derive `base_dn = search_base or _realm_to_base_dn(target.realm)`.
         b. `try: conn, config_base = _bind_and_query(...)` —
            `except (LDAPException, OSError) as exc:` → emit ADCS-UNREACH
            CryptoEndpoint(protocol="ADCS", severity="LOW",
            service_detail=f"adcs-unreachable|base={base_dn}",
            scan_error=safe_str(exc), scan_error_category="exception",
            scanned_at=now) and `continue` (D-80-R3 / ADCS-04 SC#2).
         c. Phase A — CA enumeration. paged_search at the CA base
            (Interfaces). Filter referral rows per RESEARCH Pitfall 2
            (`if entry.get("type") != "searchResEntry": continue`). For
            each multi-valued `cACertificate` value (RESEARCH Pitfall 4),
            parse DER and classify sig_hash via `is_weak_cipher`. Also
            check `key_alg == "RSA"` and `key_bits < 2048`. Weak →
            CryptoEndpoint(protocol="ADCS", cert_pubkey_alg=...,
            cert_pubkey_size=..., cert_sig_alg=...,
            service_detail=f"ca|cn={cn}|reason=weak-signing-alg",
            severity="HIGH", adcs_scan_json=json.dumps({...}),
            scanned_at=now).
         d. Phase B — Template enumeration. paged_search at the template
            base. For each entry call `_classify_template_escs(entry)` →
            list of (esc_id, severity, reasons). Emit one CryptoEndpoint
            per ESC finding with
            `service_detail=f"template|cn={cn}|{esc_id.lower()}-{primary-reason}"`
            and `severity=...`. Classifier handles only ESC1/ESC2/ESC3/ESC6.
         e. Phase C — Coverage-gap emission (D-80-R8). For each ESC class
            in {"ESC4", "ESC5", "ESC7", "ESC8"}, emit exactly ONE
            CryptoEndpoint per target: severity="LOW",
            service_detail=f"coverage-gap|{esc_class}|reason=not-ldap-observable",
            protocol="ADCS", adcs_scan_json=json.dumps({"esc": esc_class,
            "reason": "not-ldap-observable"}). Four COVERAGE-GAP endpoints
            total per target.
       - Wrap every exception write to `scan_error=` in `safe_str(exc)`
         (Phase 59 LEAK-03; covered by tests/test_scan_error_gate.py).
       - Return the consolidated list.

    Edit B — `quirk/models.py::CryptoEndpoint`. Grep for `smime_scan_json` to
    confirm the field landed in Phase 79. Add
    `adcs_scan_json: str | None = None` immediately after, in the same
    additive style. This is the Pydantic surface counterpart to the DB column
    added in Plan 80-01.

    Implements ADCS-01 (ldap3 scanner; no certipy-ad), ADCS-02 (ESC1/2/3/6
    misconfig + ESC4/5/7/8 coverage-gap), ADCS-05 (protocol="ADCS"), and
    ADCS-09 (module header invariant — the runtime AST + no-writes gates
    land in Plan 80-04).
  </action>
  <verify>
    <automated>python -m compileall quirk/scanner/adcs_scanner.py quirk/models.py &amp;&amp; python -c "from quirk.scanner.adcs_scanner import scan_adcs_targets, _realm_to_base_dn, _classify_template_escs; assert _realm_to_base_dn('QUIRK.LAB') == 'DC=quirk,DC=lab'; assert _realm_to_base_dn('') == ''; assert callable(scan_adcs_targets) and callable(_classify_template_escs)" &amp;&amp; grep -q 'read-only LDAP enumeration' quirk/scanner/adcs_scanner.py &amp;&amp; ! grep -qE 'import +certipy|from +certipy|CertificateSigningRequestBuilder' quirk/scanner/adcs_scanner.py</automated>
  </verify>
  <done>
    `quirk/scanner/adcs_scanner.py` exists, compiles, exports
    `scan_adcs_targets`. Module header contains the ADCS-09 invariant string.
    No forbidden imports (`certipy`, `CertificateSigningRequestBuilder`).
    `_realm_to_base_dn` round-trips. `CryptoEndpoint` Pydantic surface includes
    `adcs_scan_json`.
  </done>
</task>

<task type="auto">
  <name>Task 2: CBOM Pass-1 emit + Pass-2/3 skip-list (two-site append at lines 538/622)</name>
  <files>quirk/cbom/builder.py</files>
  <action>
    Three surgical edits in `quirk/cbom/builder.py`. Per D-80-R1, the CONTEXT
    line numbers (528/611) are STALE; the verified post-Phase-79 anchors are
    538 and 622. Grep `"SMIME"` first to confirm the exact tuple locations
    before editing — they should be on or near 538 and 622.

    Edit A — Pass-1 emit branch. Find the SMIME elif branch (around line
    454-462, ending immediately before the Pass-1 closing dedent). Insert the
    ADCS elif immediately after the SMIME branch:
    ```
    elif ep.protocol == "ADCS":
        # ADCS: cert_pubkey_alg holds the CA signing algorithm or template
        # key algorithm. Pass-1 only — Pass-2/3 skip the ADCS literal
        # (see skip tuples below). Phase 80 ADCS-06.
        if ep.cert_pubkey_alg:
            _register_algorithm(
                ep.cert_pubkey_alg, algo_registry, key_size=ep.cert_pubkey_size
            )
    ```
    Confirm the exact `_register_algorithm` signature against the SMIME analog
    immediately above before writing — that analog is authoritative.

    Edit B — Pass-2 inline skip-tuple at line 538 (D-80-R1, site 1). Current
    state (verified):
    ```
    "SSH", "CONTAINER", "SOURCE", "KERBEROS", "SAML", "DNSSEC", "SMIME",
    ```
    Append `"ADCS"` to this tuple (either inline at end of the same line or on
    a new line; match local style).

    Edit C — Pass-3 inline skip-tuple at line 622 (D-80-R1, site 2). Current
    state (verified):
    ```
    "DNSSEC", "SAML", "KERBEROS", "SMIME",
    ```
    Append `"ADCS"`.

    Per D-80-R1 / CONTEXT, do NOT introduce a named `IDENTITY_SKIP_PROTOCOLS`
    constant — that refactor is out of Phase 80 scope (deferred).

    Implements ADCS-06.
  </action>
  <verify>
    <automated>python -m compileall quirk/cbom/builder.py &amp;&amp; grep -n 'ep.protocol == "ADCS"' quirk/cbom/builder.py | head -2 &amp;&amp; ADCS_HITS=$(grep -v '^[[:space:]]*#' quirk/cbom/builder.py | grep -cE '"ADCS"') &amp;&amp; test "$ADCS_HITS" -ge 3</automated>
  </verify>
  <done>
    Pass-1 contains an `elif ep.protocol == "ADCS":` branch calling
    `_register_algorithm`. Both Pass-2 (line ~538) and Pass-3 (line ~622)
    inline tuples contain `"ADCS"`. Non-comment `"ADCS"` occurrences ≥ 3
    (Pass-1 + Pass-2 + Pass-3). `python -m compileall` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire scan_adcs_targets into run_scan.py (resume-aware, data_at_rest)</name>
  <files>run_scan.py</files>
  <action>
    Five surgical edits in `run_scan.py`, mirroring the `_run_smime_phase`
    pattern that landed in Phase 79. Grep `_run_smime_phase` first to anchor
    every offset (Phase 79 mutated this file; analog line numbers may have
    drifted from the RESEARCH-quoted values).

    Edit A — `_dar_protocols` tuple at ~line 1210. Append `"ADCS"`.

    Edit B — Resume bucket near ~line 1219. Add immediately after the SMIME
    resume bucket line:
    ```
    adcs_endpoints = [e for e in _resumed_endpoints if getattr(e, "protocol", "") == "ADCS"]
    ```

    Edit C — Resume-count log at ~line 1226. Extend the log message to
    include the adcs count (mirror the smime addition).

    Edit D — Insert `_run_adcs_phase` function and dispatch IMMEDIATELY after
    the `_run_smime_phase` block (~line 1396-1415 analog). Per RESEARCH
    "Hooking into cfg.connectors":
    ```
    def _run_adcs_phase():
        if not (getattr(cfg.connectors, "enable_adcs", False)
                and getattr(cfg.connectors, "adcs_targets", None)):
            return []
        from quirk.scanner.adcs_scanner import scan_adcs_targets
        eps = scan_adcs_targets(
            targets=cfg.connectors.adcs_targets,
            timeout=getattr(cfg.connectors, "adcs_timeout", 10),
            logger=logger,
            session_start=session_start,
            search_base=getattr(cfg.connectors, "adcs_search_base", None),
            user=getattr(cfg.connectors, "adcs_user", None),
            password=getattr(cfg.connectors, "adcs_password", None),
        )
        logger.info("ADCS scan: %d endpoints from %d targets",
                    len(eps), len(cfg.connectors.adcs_targets))
        return eps

    adcs_endpoints = _wrapped_phase(
        run_stats, "adcs_scanning", "adcs_scanner",
        _run_adcs_phase, error_endpoints, logger,
    ) or []
    ```
    Use `or []` to preserve any resume-time `adcs_endpoints` populated by
    Edit B when the phase is skipped (same pattern as smime).

    Edit E — `_dar_eps` aggregate at ~line 1453 AND the final endpoint
    concat at ~line 1619 — include `adcs_endpoints` in both sums.
    Grep `smime_endpoints` to confirm both addition sites; the ADCS addition
    must touch both.

    Implements ADCS-01 (orchestrator wiring leg). Together with Plan 80-04's
    CLI test fixtures, this closes ADCS-01.
  </action>
  <verify>
    <automated>python -m compileall run_scan.py &amp;&amp; grep -n 'scan_adcs_targets\|_run_adcs_phase\|adcs_endpoints' run_scan.py | head -10</automated>
  </verify>
  <done>
    `run_scan.py` imports `scan_adcs_targets` via `_run_adcs_phase`, dispatches
    after `_run_smime_phase`, propagates results into `_dar_eps` and the final
    concat, and resumes `adcs_endpoints` correctly. `python -m compileall`
    exits 0.
  </done>
</task>

</tasks>

<verification>
- `python -m compileall quirk/ run_scan.py` exits 0
- `grep -c '"ADCS"' quirk/cbom/builder.py` (non-comment) ≥ 3
- `grep -c 'adcs_endpoints' run_scan.py` ≥ 4 (definition, resume bucket,
  _dar_eps sum, final concat)
- No `certipy` or `CertificateSigningRequestBuilder` references in
  `quirk/scanner/adcs_scanner.py` (Plan 80-04 enforces the gate; this is a
  pre-flight sanity check)
- Manual smoke (deferred to Plan 80-04 once unit tests exist):
  `PROFILE_ARGS="--profile adcs" ./lab.sh up` then a scan against
  `ldap://localhost:38910` produces 1 weak-signing HIGH (CA cert), 1 ESC1
  HIGH (BadTemplate-ESC1), 0 from SafeTemplate, and 4 COVERAGE-GAP LOW
  (ESC4/5/7/8).
- Atomic commit with EXPLICIT file paths (NEVER `git add -A` — Phase 78
  Wave 2 race; Plan 80-03 runs in parallel and MUST commit only its own files):
  `feat(80-02): adcs scanner module + cbom pass-1 emit + skip-list extensions`
  Files: `quirk/scanner/adcs_scanner.py`, `quirk/models.py`,
  `quirk/cbom/builder.py`, `run_scan.py`.
</verification>

<success_criteria>
- ADCS-01 satisfied: `scan_adcs_targets` exists in `quirk/scanner/adcs_scanner.py`
  using ldap3 only (no certipy-ad) and queries the AD CS Configuration
  partition (CA enumeration + template enumeration).
- ADCS-02 satisfied: ESC1/ESC2/ESC3/ESC6 emit deterministic LDAP-attribute-driven
  findings; ESC4/ESC5/ESC7/ESC8 emit ADCS-COVERAGE-GAP findings (D-80-R8).
  ESC9-ESC16 NOT touched.
- ADCS-05 satisfied: Endpoints emit with `protocol="ADCS"`; existing
  protocol-agnostic `/api/scan/latest` and Identity tab pick them up.
- ADCS-06 satisfied: CBOM Pass-1 emits algorithm components; Pass-2 + Pass-3
  inline tuples both include `"ADCS"` at lines 538 and 622.
- ADCS-09 satisfied (runtime arm): module header carries the read-only
  invariant statement verbatim. Static-analysis enforcement lands in
  Plan 80-04 (AST gate + no-writes test).
- ADCS-04 (partial): the ADCS-UNREACH coverage-gap pattern is in place — no
  exception propagates. The counters that make ADCS-04 fully complete are
  added in Plan 80-03 (parallel).
- Plan 80-03 (parallel) and Plan 80-04 (tests + closure) unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/80-windows-adcs-scanner/80-02-SUMMARY.md`
summarizing: scanner module final line count, ESC classes emitted vs.
coverage-gapped, CBOM edit sites (Pass-1 + verified line numbers for Pass-2/3),
run_scan.py wiring location, any deviations from PATTERNS.md sketch (especially
if line numbers drifted from the cited 538/622).
</output>
