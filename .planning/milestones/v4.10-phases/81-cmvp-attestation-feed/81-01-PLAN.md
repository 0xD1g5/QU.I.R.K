---
phase: 81-cmvp-attestation-feed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/fetch_cmvp_curation.py
  - quirk/compliance/cmvp_curated.csv
  - quirk/compliance/cmvp_cache.json
  - pyproject.toml
  - quirk/errors.py
autonomous: true
requirements:
  - CMVP-01
  - CMVP-04
must_haves:
  truths:
    - "A one-off scraping script lives under scripts/ and, given a CSV of intended modules, produces real CMVP certificate numbers + per-cert detail rows from the live NIST page"
    - "quirk/compliance/cmvp_curated.csv exists with ~50 rows, each with a real (non-placeholder 4523) certificate_number, sourced by running the script against the live NIST page"
    - "quirk/compliance/cmvp_cache.json exists with last_verified=2026-05-16, source_url=NIST CSRC search URL, schema_version='1.0', and ~50 module entries each with certificate_number, vendor, name, module_version, fips_level, algorithms[]"
    - "The 10 known-good cert numbers from RESEARCH.md (4282, 4339, 4523, 4719, 4790, 4793, 4794, 4811, 4905, 4985) appear in the curated CSV and the cache"
    - "pyproject.toml [project] dependencies contains 'beautifulsoup4>=4.13.0' (grouped near lxml at line 28)"
    - "quirk/errors.py registers CMVP-REFRESH-NETWORK, CMVP-REFRESH-PARSE, CMVP-REFRESH-NO-CHANGES, CMVP-STALE error entries with cause+fix prose"
    - "Repository remains scan-capable offline: cmvp_cache.json is the only fallback the runtime requires"
  artifacts:
    - path: "scripts/fetch_cmvp_curation.py"
      provides: "One-off operator script that resolves vendor/name strings against the live NIST search page and emits the curated CSV"
      contains: "searchResultsTable"
    - path: "quirk/compliance/cmvp_curated.csv"
      provides: "Curated 50-module selection (certificate_number,vendor,name,rationale) committed for reproducibility"
      contains: "certificate_number,vendor,name,rationale"
    - path: "quirk/compliance/cmvp_cache.json"
      provides: "Bundled offline CMVP snapshot consumed by the runtime"
      contains: '"last_verified"'
    - path: "pyproject.toml"
      provides: "beautifulsoup4>=4.13.0 declared in [project] dependencies"
      contains: "beautifulsoup4>=4.13.0"
    - path: "quirk/errors.py"
      provides: "CMVP-* error code registry block (network/parse/no-changes/stale)"
      contains: "CMVP-REFRESH-NETWORK"
  key_links:
    - from: "scripts/fetch_cmvp_curation.py"
      to: "https://csrc.nist.gov/projects/cryptographic-module-validation-program/validated-modules/search"
      via: "httpx GET + BeautifulSoup(lxml) parse of table#searchResultsTable"
      pattern: "searchResultsTable"
    - from: "quirk/compliance/cmvp_curated.csv"
      to: "quirk/compliance/cmvp_cache.json"
      via: "script reads CSV → fetches per-cert detail page → writes JSON"
      pattern: "certificate_number"
    - from: "pyproject.toml"
      to: "scripts/fetch_cmvp_curation.py + quirk/compliance/cmvp.py (Plan 02)"
      via: "beautifulsoup4 dependency floor"
      pattern: "beautifulsoup4>=4.13.0"
---

<objective>
Foundation pass for Phase 81: materialize real CMVP data and dependency wiring
BEFORE any runtime code reads the cache. Closes the 40-of-50 cert-number gap
flagged in RESEARCH.md (A1 / [ASSUMED] block at line 420) by running a one-off
operator script against the live NIST CSRC validated-modules page, producing
the curated CSV with real certificate numbers, and seeding the offline cache
from per-cert detail-page fetches. Also registers the bs4 dependency and the
CMVP-* error codes the refresh CLI (Plan 02) will reference.

Purpose: Every downstream task in Phase 81 reads or writes cmvp_cache.json,
the curated CSV, or quirk/errors.py. Landing them first as a single atomic
commit keeps Plans 02–04 deterministic.
Output: scripts/fetch_cmvp_curation.py + quirk/compliance/cmvp_curated.csv +
quirk/compliance/cmvp_cache.json + pyproject.toml dep + quirk/errors.py block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-cmvp-attestation-feed/81-CONTEXT.md
@.planning/phases/81-cmvp-attestation-feed/81-RESEARCH.md
@.planning/phases/81-cmvp-attestation-feed/81-PATTERNS.md
@quirk/errors.py
@pyproject.toml
@quirk/qramm/model_meta.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the one-off scraping script + produce the curated CSV</name>
  <files>scripts/fetch_cmvp_curation.py, quirk/compliance/cmvp_curated.csv</files>
  <action>
Create scripts/fetch_cmvp_curation.py — an operator-run, one-shot Python script
(NOT imported by any runtime code) that resolves real CMVP certificate numbers.
Mirror the parser sketch in RESEARCH.md §BeautifulSoup4 Parser Sketch lines
229–355: use httpx.Client (sync), User-Agent 'QU.I.R.K. CMVP refresh
(https://github.com/&lt;org&gt;/quirk)', timeout httpx.Timeout(15.0, connect=5.0),
and BeautifulSoup with the 'lxml' parser. GET the NIST CSRC search URL with
params {SearchMode: Basic, Standard: 140-3, CertificateStatus: Active,
displayall: 1}, parse table#searchResultsTable, then for each desired
(vendor, module_name) tuple match against the parsed rows to extract the real
certificate number. Seed the desired-tuple list from the rationale strings in
RESEARCH.md §Curated 50-Module Seed List lines 366–417 (the 10 known-good
cert numbers from RESEARCH already anchor the script — 4282, 4339, 4523,
4719, 4790, 4793, 4794, 4811, 4905, 4985 — and the script confirms / fills
the remaining 40 against live NIST data).

The script writes quirk/compliance/cmvp_curated.csv with columns
'certificate_number,vendor,name,rationale'. Sort rows by vendor then name.
Include a 100ms time.sleep between requests for politeness (RESEARCH §Robots).

Then RUN the script once against live NIST and commit the resulting
cmvp_curated.csv. Every row's certificate_number column MUST be a numeric
string with NO placeholder '4523' duplication (the RESEARCH [ASSUMED] flag at
line 420 is what this task closes). If a desired (vendor, name) tuple has no
match on live NIST, drop it from the CSV with a stderr warning — do not invent
a number. Target a final row count between 45 and 55 inclusive.
  </action>
  <verify>
    <automated>test -f scripts/fetch_cmvp_curation.py &amp;&amp; test -f quirk/compliance/cmvp_curated.csv &amp;&amp; head -1 quirk/compliance/cmvp_curated.csv | grep -q 'certificate_number,vendor,name,rationale' &amp;&amp; ROWS=$(tail -n +2 quirk/compliance/cmvp_curated.csv | wc -l | tr -d ' ') &amp;&amp; [ "$ROWS" -ge 45 ] &amp;&amp; [ "$ROWS" -le 55 ] &amp;&amp; PLACEHOLDERS=$(tail -n +2 quirk/compliance/cmvp_curated.csv | awk -F, '$1=="4523"' | wc -l | tr -d ' ') &amp;&amp; [ "$PLACEHOLDERS" -le 1 ] &amp;&amp; for N in 4282 4339 4719 4790 4793 4794 4811 4905 4985; do grep -q "^$N," quirk/compliance/cmvp_curated.csv || { echo "missing anchor cert $N"; exit 1; }; done</automated>
  </verify>
  <done>scripts/fetch_cmvp_curation.py exists and is executable as `python scripts/fetch_cmvp_curation.py`; quirk/compliance/cmvp_curated.csv exists with 45–55 rows, header row 'certificate_number,vendor,name,rationale', at most one row using '4523' (NOT 40+ as in the RESEARCH placeholder), and contains all 9 verifiable anchor cert numbers from RESEARCH (4282, 4339, 4719, 4790, 4793, 4794, 4811, 4905, 4985).</done>
</task>

<task type="auto">
  <name>Task 2: Seed quirk/compliance/cmvp_cache.json + register bs4 dep + CMVP-* error codes</name>
  <files>quirk/compliance/cmvp_cache.json, pyproject.toml, quirk/errors.py</files>
  <action>
Extend scripts/fetch_cmvp_curation.py with a `--emit-cache` flag (or write a
second helper script in the same file) that reads cmvp_curated.csv, fetches
each certificate detail page (URL pattern from RESEARCH §NIST CMVP Page
Structure lines 178–214 — '/projects/cryptographic-module-validation-program/
certificate/{N}'), and parses module_version, fips_level, overall_level, and
the CAVP algorithm family list from table#fips-algo-table per RESEARCH lines
285–331. Emit quirk/compliance/cmvp_cache.json with the schema locked in
CONTEXT §Cross-cutting + RESEARCH §Cache Schema lines 428–445:

  {
    "schema_version": "1.0",
    "last_verified": "2026-05-16",
    "source_url": "https://csrc.nist.gov/projects/cryptographic-module-validation-program/validated-modules/search",
    "modules": [ { "certificate_number": str, "vendor": str, "name": str,
                   "module_version": str, "fips_level": "140-3"|"140-2"|"unknown",
                   "overall_level": str, "algorithms": [str, ...] }, ... ]
  }

Algorithm lists per module come straight from CAVP family names on the detail
page (AES, ECDSA, RSA, SHS, HMAC, DRBG, KAS, KBKDF, ...) — sorted, deduplicated.
Per CONTEXT §v4.10-D-01 the cache schema MUST NOT contain any 'certified' key.

In pyproject.toml [project] dependencies, add `"beautifulsoup4&gt;=4.13.0",` on
its own line grouped near `"lxml&gt;=6.0",` (currently around line 28). Mirror
the single-line shape from PATTERNS.md §pyproject.toml — Phase 78 nh3 analog.

In quirk/errors.py, append a CMVP domain block AFTER the CBOM-001 block
(around line 168 per PATTERNS.md). Add four ErrorEntry rows verbatim per
PATTERNS.md §quirk/errors.py — CMVP-* entries lines 151–172: CMVP-REFRESH-NETWORK
(network failure cause + connectivity remediation), CMVP-REFRESH-PARSE (selector
miss cause + file-issue + pin-to-bundled-cache remediation), CMVP-REFRESH-NO-CHANGES
(info-level, no-action-needed prose), CMVP-STALE (90-day cause + `quirk compliance
cmvp refresh` remediation pointing at the runbook commit-message format from
CONTEXT §Area 4).
  </action>
  <verify>
    <automated>python -m json.tool quirk/compliance/cmvp_cache.json &gt; /dev/null &amp;&amp; python -c "import json,sys; d=json.load(open('quirk/compliance/cmvp_cache.json')); assert d['schema_version']=='1.0', d.get('schema_version'); assert d['last_verified']=='2026-05-16', d['last_verified']; assert 'csrc.nist.gov' in d['source_url']; mods=d['modules']; assert 45 &lt;= len(mods) &lt;= 55, len(mods); import re; assert all(re.match(r'^\\d+$', m['certificate_number']) for m in mods); assert all(set(['certificate_number','vendor','name','module_version','fips_level','algorithms']) &lt;= set(m) for m in mods); assert all(isinstance(m['algorithms'], list) for m in mods); assert 'certified' not in json.dumps(d), 'v4.10-D-01 violation: certified key present in cmvp_cache.json'; print('OK')" &amp;&amp; grep -q '"beautifulsoup4&gt;=4.13.0"' pyproject.toml &amp;&amp; python -c "from quirk.errors import ERROR_REGISTRY; assert 'CMVP-REFRESH-NETWORK' in ERROR_REGISTRY; assert 'CMVP-REFRESH-PARSE' in ERROR_REGISTRY; assert 'CMVP-REFRESH-NO-CHANGES' in ERROR_REGISTRY; assert 'CMVP-STALE' in ERROR_REGISTRY; print('errors OK')"</automated>
  </verify>
  <done>cmvp_cache.json is valid JSON conforming to the locked schema; 45–55 module rows, every certificate_number numeric, no 'certified' key anywhere; beautifulsoup4&gt;=4.13.0 in pyproject.toml; four CMVP-* error codes registered in quirk/errors.py and importable.</done>
</task>

<task type="auto">
  <name>Task 3: Commit foundation atomically with explicit file paths</name>
  <files>(commit only — no new file modifications)</files>
  <action>
Stage ONLY the files this plan owns (NEVER use `git add -A` per CLAUDE.md hygiene mandate):

  git add scripts/fetch_cmvp_curation.py \
          quirk/compliance/cmvp_curated.csv \
          quirk/compliance/cmvp_cache.json \
          pyproject.toml \
          quirk/errors.py

Commit with message: `feat(81-01): cmvp_cache.json seed + curated modules + beautifulsoup4 dep + error codes` (matches the Phase 81 commit-cadence guidance at the top of this plan set). Use the gsd-tools commit helper:

  node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit \
    "feat(81-01): cmvp_cache.json seed + curated modules + beautifulsoup4 dep + error codes" \
    --files scripts/fetch_cmvp_curation.py quirk/compliance/cmvp_curated.csv quirk/compliance/cmvp_cache.json pyproject.toml quirk/errors.py
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q '^feat(81-01):' &amp;&amp; git show --stat HEAD | grep -q 'cmvp_cache.json' &amp;&amp; git show --stat HEAD | grep -q 'cmvp_curated.csv' &amp;&amp; git show --stat HEAD | grep -q 'pyproject.toml' &amp;&amp; git show --stat HEAD | grep -q 'quirk/errors.py' &amp;&amp; git status --porcelain | grep -E '(cmvp_cache|cmvp_curated|errors.py|pyproject.toml)' | wc -l | tr -d ' ' | grep -q '^0$'</automated>
  </verify>
  <done>HEAD commit subject starts with `feat(81-01):`; the 5 owned files appear in `git show --stat HEAD`; working tree has no remaining unstaged changes to those files.</done>
</task>

</tasks>

<verification>
- `python -m json.tool quirk/compliance/cmvp_cache.json` parses cleanly
- `grep -c '^[0-9]' quirk/compliance/cmvp_curated.csv` returns a count between 45 and 55
- `python -c "from quirk.errors import ERROR_REGISTRY; print(sorted(k for k in ERROR_REGISTRY if k.startswith('CMVP')))"` prints all four CMVP codes
- No file outside `files_modified` is touched
</verification>

<success_criteria>
- Real cert numbers in cmvp_curated.csv (RESEARCH A1 placeholder gap closed)
- cmvp_cache.json schema-valid, 45–55 modules, NO `certified` key (v4.10-D-01)
- beautifulsoup4>=4.13.0 in pyproject.toml dependencies
- Four CMVP-* error codes registered and importable
- Atomic feat(81-01) commit on HEAD
</success_criteria>

<output>
After completion, create `.planning/phases/81-cmvp-attestation-feed/81-01-SUMMARY.md`
</output>
