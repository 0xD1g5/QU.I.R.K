---
phase: 81-cmvp-attestation-feed
plan: 03
type: execute
wave: 2
depends_on:
  - 81-01
files_modified:
  - quirk/reports/templates/report.html.j2
  - quirk/reports/executive.py
  - quirk/reports/technical.py
  - quirk/reports/html_renderer.py
autonomous: true
requirements:
  - CMVP-06
must_haves:
  truths:
    - "report.html.j2 contains a new `<h2>Algorithm Inventory (FIPS 140-3 Coverage)</h2>` section with a 4-column table (Algorithm | NIST Level | FIPS Status | CMVP Coverage)"
    - "Every scanner-controlled cell in the new Algorithm Inventory table wraps its value through `| sanitize` (Phase 78 chokepoint contract)"
    - "For an algorithm with no coverage match, the CMVP Coverage cell renders the static literal string `Not in CMVP catalog` (NEVER `Not certified`)"
    - "Markdown executive + technical reports each gain a `CMVP Coverage` column in their algorithm tables, populated via quirk.compliance.cmvp.coverage_for_algorithm()"
    - "html_renderer.py (or the active HTML writer) passes a new `algorithms` template context built from the CBOM algo registry; each row carries name, nist_level, fips_status, cmvp_coverage"
  artifacts:
    - path: "quirk/reports/templates/report.html.j2"
      provides: "Algorithm Inventory section with CMVP Coverage column; every scanner cell sanitized"
      contains: "Algorithm Inventory"
    - path: "quirk/reports/executive.py"
      provides: "CMVP Coverage column in the markdown algorithm table"
      contains: "CMVP Coverage"
    - path: "quirk/reports/html_renderer.py"
      provides: "algorithms template context wired from the CBOM algo registry"
      contains: "algorithms"
  key_links:
    - from: "quirk/reports/html_renderer.py"
      to: "quirk/compliance/cmvp.py::coverage_for_algorithm"
      via: "import + per-row lookup when building the `algorithms` template context"
      pattern: "coverage_for_algorithm"
    - from: "quirk/reports/templates/report.html.j2 Algorithm Inventory cells"
      to: "quirk/reports/templates/report.html.j2 sanitize filter"
      via: "Jinja `| sanitize` chokepoint per Phase 78 HARDEN-02/03"
      pattern: "| sanitize"
    - from: "quirk/reports/executive.py + technical.py"
      to: "quirk/compliance/cmvp.py::coverage_for_algorithm"
      via: "per-algorithm query when emitting the markdown algorithm rows"
      pattern: "coverage_for_algorithm"
---

<objective>
Surface CMVP coverage in the consultant-facing deliverables. Per D-81-R5, no
Algorithm Inventory table exists in report.html.j2 today — Phase 81 INTRODUCES
one by cloning the Endpoint Inventory pattern at lines 304-323. Every scanner-
controlled cell pipes through `| sanitize` (Phase 78 chokepoint). Markdown
executive + technical reports gain a `CMVP Coverage` column populated from
quirk/compliance/cmvp.coverage_for_algorithm(). Missing matches render
"Not in CMVP catalog" (NEVER "Not certified", per locked CONTEXT decision).

Purpose: Closes CMVP-06 (HTML/PDF report column) and the consultant UX side of
CMVP-05 (coverage visible to the operator).
Output: New Algorithm Inventory section in report.html.j2 + CMVP Coverage column
in executive.py + technical.py + algorithms template context in html_renderer.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-cmvp-attestation-feed/81-CONTEXT.md
@.planning/phases/81-cmvp-attestation-feed/81-RESEARCH.md
@.planning/phases/81-cmvp-attestation-feed/81-PATTERNS.md
@.planning/phases/81-cmvp-attestation-feed/81-01-SUMMARY.md
@quirk/reports/templates/report.html.j2
@quirk/reports/executive.py
@quirk/reports/technical.py
@quirk/reports/html_renderer.py

<interfaces>
<!-- Phase 78 sanitize chokepoint contract — every scanner-controlled cell wraps with | sanitize. -->
<!-- See report.html.j2:176 comment and the Endpoint Inventory table at lines 304-323 for the analog. -->

Existing Endpoint Inventory pattern (clone shape for the new Algorithm Inventory):
  <h2>Endpoint Inventory</h2>
  <table>
    <thead><tr><th>...</th></tr></thead>
    <tbody>
    {% for e in endpoints %}
    <tr>
      <td>{{ e.host | sanitize }}</td>
      ...
    </tr>
    {% endfor %}
    </tbody>
  </table>

Target shape for the new Algorithm Inventory (per PATTERNS.md §Report CMVP Coverage column, lines 211-227):
  <h2>Algorithm Inventory (FIPS 140-3 Coverage)</h2>
  {% if algorithms %}
  <table>
    <thead><tr><th>Algorithm</th><th>NIST Level</th><th>FIPS Status</th><th>CMVP Coverage</th></tr></thead>
    <tbody>
    {% for a in algorithms %}
    <tr>
      <td>{{ a.name | sanitize }}</td>
      <td>{{ a.nist_level }}</td>
      <td>{{ a.fips_status | sanitize }}</td>
      <td>{{ a.cmvp_coverage | sanitize if a.cmvp_coverage else 'Not in CMVP catalog' }}</td>
    </tr>
    {% endfor %}
    </tbody>
  </table>
  {% endif %}

`algorithms` template context shape (built in html_renderer.py):
  [
    {"name": str, "nist_level": int | str, "fips_status": str,
     "cmvp_coverage": str | None  # comma-joined module names, or None for empty matches
    },
    ...
  ]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Algorithm Inventory section to report.html.j2 with sanitize-wrapped cells</name>
  <files>quirk/reports/templates/report.html.j2</files>
  <action>
Per D-81-R5: no Algorithm Inventory table exists today. INTRODUCE one by cloning
the Endpoint Inventory section structure currently at lines 304-323 (4-column
table inside `{% if endpoints %}`). Insert the new section either immediately
after the Endpoint Inventory block or in the existing FIPS-140-3 compliance
section — choose whichever placement keeps the consultant deliverable reading
"Endpoints → Algorithms → Findings". Look for the existing compliance section
heading and insert there if present; otherwise place after Endpoint Inventory.

Use exactly the Jinja snippet from PATTERNS.md §Report CMVP Coverage column
(see <interfaces> above). Header cells are template-static and do NOT need
sanitize. Every cell with scanner-controlled content MUST be wrapped in
`| sanitize`:

- `{{ a.name | sanitize }}` — algorithm name
- `{{ a.nist_level }}` — integer/coded value (no sanitize required per existing
  pattern for numeric/coded cells; if it is a string in this codebase, sanitize it)
- `{{ a.fips_status | sanitize }}` — derived from _fips_status string
- `{{ a.cmvp_coverage | sanitize if a.cmvp_coverage else 'Not in CMVP catalog' }}`

The literal `Not in CMVP catalog` is template-static and not sanitized
(PATTERNS.md line 230). NEVER use the phrase `Not certified` (CONTEXT Area 3 lock).

Wrap the new section in the `{% if algorithms %}` guard so renderers that do
not yet pass the new context don't render an empty heading.

After editing, confirm the existing Phase 78 sanitize chokepoint comment at
report.html.j2:176 still applies (it does — same template).
  </action>
  <verify>
    <automated>grep -q 'Algorithm Inventory' quirk/reports/templates/report.html.j2 &amp;&amp; grep -q 'CMVP Coverage' quirk/reports/templates/report.html.j2 &amp;&amp; grep -q 'Not in CMVP catalog' quirk/reports/templates/report.html.j2 &amp;&amp; ! grep -q 'Not certified' quirk/reports/templates/report.html.j2 &amp;&amp; python -c "
import re, pathlib
tpl = pathlib.Path('quirk/reports/templates/report.html.j2').read_text()
# Extract the Algorithm Inventory table block — between the heading and the closing /table after it
m = re.search(r'Algorithm Inventory.*?&lt;/table&gt;', tpl, re.DOTALL)
assert m, 'Algorithm Inventory table block not found'
block = m.group(0)
# Every {{ a.X }} cell MUST be piped through | sanitize OR use the safe-static fallback
import re as _re
cells = _re.findall(r'\{\{\s*a\.([a-zA-Z_]+)([^}]*)\}\}', block)
for field, expr in cells:
    if field in ('name', 'fips_status', 'cmvp_coverage'):
        assert 'sanitize' in expr, f'a.{field} cell missing | sanitize: {expr!r}'
print('sanitize chokepoint enforced on Algorithm Inventory cells')
"</automated>
  </verify>
  <done>report.html.j2 contains the Algorithm Inventory section with 4 columns; every scanner-controlled cell wraps with `| sanitize`; the literal `Not in CMVP catalog` appears as the empty-match fallback; the forbidden phrase `Not certified` does NOT appear.</done>
</task>

<task type="auto">
  <name>Task 2: Wire algorithms template context in html_renderer.py + add CMVP Coverage column to markdown reports</name>
  <files>quirk/reports/html_renderer.py, quirk/reports/executive.py, quirk/reports/technical.py</files>
  <action>
In quirk/reports/html_renderer.py, locate the function that renders
report.html.j2 (search for `report.html.j2` or the Jinja `Environment.get_template`
call). Build a new `algorithms` template context list before rendering. Source
the algorithm names from the CBOM algo registry / Pass-1 algorithm components
(whatever shape the renderer already passes to other parts of the template).
For each algorithm:

  from quirk.compliance.cmvp import coverage_for_algorithm
  ...
  coverage = coverage_for_algorithm(alg_name)
  cmvp_coverage = ", ".join(m["name"] for m in coverage) if coverage else None
  algorithms.append({
      "name": alg_name,
      "nist_level": <existing field>,
      "fips_status": <existing string>,
      "cmvp_coverage": cmvp_coverage,
  })

Pass `algorithms=algorithms` into the template render context alongside the
existing context dict.

In quirk/reports/executive.py: locate the existing algorithm summary table
(grep for headings like "Algorithm" or for an existing markdown table that
includes algorithm names). Add a new column header `| CMVP Coverage |` and
populate per-row by calling `coverage_for_algorithm(row.name)` — comma-join
module names, or render the literal `Not in CMVP catalog` for empty matches.
If executive.py has no existing algorithm table, ADD a minimal one mirroring
the HTML shape (4 columns: Algorithm, NIST Level, FIPS Status, CMVP Coverage).

In quirk/reports/technical.py: do the same — add a `CMVP Coverage` column to
its algorithm table; if it has none, add one.

Markdown empty-match wording MUST be `Not in CMVP catalog` verbatim — never
`Not certified` (CONTEXT Area 3 + v4.10-D-01).

Import the coverage helper at the top of each modified file:
`from quirk.compliance.cmvp import coverage_for_algorithm`
  </action>
  <verify>
    <automated>grep -q 'coverage_for_algorithm' quirk/reports/html_renderer.py &amp;&amp; grep -q 'algorithms' quirk/reports/html_renderer.py &amp;&amp; grep -q 'CMVP Coverage' quirk/reports/executive.py &amp;&amp; grep -q 'CMVP Coverage' quirk/reports/technical.py &amp;&amp; grep -q 'coverage_for_algorithm' quirk/reports/executive.py &amp;&amp; grep -q 'coverage_for_algorithm' quirk/reports/technical.py &amp;&amp; ! grep -q 'Not certified' quirk/reports/executive.py &amp;&amp; ! grep -q 'Not certified' quirk/reports/technical.py &amp;&amp; ! grep -q 'Not certified' quirk/reports/html_renderer.py &amp;&amp; python -m py_compile quirk/reports/html_renderer.py quirk/reports/executive.py quirk/reports/technical.py</automated>
  </verify>
  <done>html_renderer.py builds the `algorithms` context and passes it to the template; executive.py and technical.py both contain a `CMVP Coverage` column wired via coverage_for_algorithm; the forbidden phrase `Not certified` appears nowhere; all three files compile.</done>
</task>

<task type="auto">
  <name>Task 3: Commit report-column changes atomically</name>
  <files>(commit only)</files>
  <action>
Stage only the 4 owned files (NEVER `git add -A`):

  node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit \
    "feat(81-03): cmvp coverage column in algorithm inventory table" \
    --files quirk/reports/templates/report.html.j2 quirk/reports/html_renderer.py quirk/reports/executive.py quirk/reports/technical.py
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q '^feat(81-03):' &amp;&amp; git show --stat HEAD | grep -q 'report.html.j2' &amp;&amp; git show --stat HEAD | grep -q 'html_renderer.py' &amp;&amp; git show --stat HEAD | grep -q 'executive.py' &amp;&amp; git show --stat HEAD | grep -q 'technical.py'</automated>
  </verify>
  <done>HEAD subject starts with `feat(81-03):`; 4 owned files appear in the commit; no other files modified.</done>
</task>

</tasks>

<verification>
- `grep -c 'Algorithm Inventory' quirk/reports/templates/report.html.j2` ≥ 1
- `grep -c 'CMVP Coverage' quirk/reports/{executive,technical}.py quirk/reports/templates/report.html.j2` ≥ 3
- `grep -rE 'Not certified' quirk/reports/` returns 0 matches (regression guard for CONTEXT Area 3 lock)
- Jinja renders without unbound-variable errors when `algorithms=[]` is passed (the `{% if algorithms %}` guard handles empty-context cases)
</verification>

<success_criteria>
- New Algorithm Inventory section in report.html.j2 with 4 columns
- Every scanner-controlled cell in that table piped through `| sanitize`
- Empty matches render `Not in CMVP catalog`; the phrase `Not certified` appears nowhere
- Markdown executive + technical reports each gain a CMVP Coverage column
- html_renderer.py builds and passes the `algorithms` template context
- Atomic feat(81-03) commit
</success_criteria>

<output>
After completion, create `.planning/phases/81-cmvp-attestation-feed/81-03-SUMMARY.md`
</output>
