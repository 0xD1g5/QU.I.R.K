---
phase: 71-protocol-scanner-warnings
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/discovery/nmap_provider.py
  - quirk/discovery/nmap_parser.py
  - tests/test_nmap_hardening.py
  - .planning/audit-2026-05-08/AUDIT-TASKS.md
autonomous: true
requirements: [PROTO-03]
must_haves:
  truths:
    - "nmap_provider.run_nmap_discovery rejects extra_args tokens containing shell-metacharacters via a regex allowlist"
    - "nmap_parser parses nmap XML output via defusedxml, eliminating XXE and billion-laughs exposure"
    - "nmap_provider default port CSV is the broadest consulting-grade union composed from cfg.scan.ports_tls plus the fixed protocol port set in D-03"
    - "Audit rows scanners-protocol/WR-04, WR-05, WR-06 are flipped to [x] closed under Phase 71"
  artifacts:
    - path: "quirk/discovery/nmap_provider.py"
      provides: "Default port CSV + extra_args allowlist guard"
      contains: "_SAFE_NMAP_ARG_RE"
    - path: "quirk/discovery/nmap_parser.py"
      provides: "defusedxml-based XML parsing"
      contains: "defusedxml"
    - path: "tests/test_nmap_hardening.py"
      provides: "Allowlist + defusedxml + port CSV tests"
      contains: "def test_"
  key_links:
    - from: "quirk/discovery/nmap_provider.py::run_nmap_discovery"
      to: "extra_args validation"
      via: "_SAFE_NMAP_ARG_RE.match(token) check before subprocess"
      pattern: "_SAFE_NMAP_ARG_RE"
    - from: "quirk/discovery/nmap_parser.py"
      to: "defusedxml.ElementTree"
      via: "import switch from xml.etree to defusedxml.ElementTree"
      pattern: "import defusedxml\\.ElementTree"
---

<objective>
Close PROTO-03 (WR-04, WR-05, WR-06) by hardening the nmap subsystem:

1. **WR-04** — Default port CSV in `nmap_provider.py:54` becomes the documented consulting union (cfg.scan.ports_tls + fixed protocol set) per D-03.
2. **WR-05** — `run_nmap_discovery` validates each `extra_args` token against the regex `^[A-Za-z0-9._:/=,-]+$` per D-04; unsafe tokens raise `ValueError(f"Unsafe nmap extra arg: {token!r}")`. Mirrors `_SAFE_COL_TYPE_RE` defense-in-depth pattern from Phase 70.
3. **WR-06** — `nmap_parser.py` switches from `xml.etree.ElementTree` to `defusedxml.ElementTree` per D-05. defusedxml is already a core dep (`pyproject.toml:29`).

Purpose: Eliminate command-injection (WR-05) and XXE/billion-laughs (WR-06) attack surface; ensure the nmap-discovery default scan profile covers the consulting-grade port set (WR-04).
Output: Patched provider + parser, new test module, three audit rows flipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/71-protocol-scanner-warnings/71-CONTEXT.md
@.planning/audit-2026-05-08/AUDIT-TASKS.md

<interfaces>
Defense-in-depth allowlist precedent (Phase 70, quirk/db.py:34):
```python
import re
_SAFE_COL_TYPE_RE = re.compile(r"^[A-Z][A-Z0-9_ ()]*$")

if not _SAFE_COL_TYPE_RE.match(col_type):
    raise ValueError(f"Unsafe column type: {col_type!r}")
```
Mirror this shape for `_SAFE_NMAP_ARG_RE`.

defusedxml drop-in:
```python
# Before:  import xml.etree.ElementTree as ET
# After:
import defusedxml.ElementTree as ET
```
defusedxml.ElementTree has the same public surface as stdlib ET for parse/fromstring; no other code changes needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: nmap_provider — default port CSV + extra_args allowlist (WR-04, WR-05)</name>
  <files>quirk/discovery/nmap_provider.py</files>
  <read_first>
    - quirk/discovery/nmap_provider.py (entire file — pay attention to line 54 default port CSV and the `run_nmap_discovery` signature/extra_args handling)
    - quirk/config.py (find `ScanCfg.ports_tls` — confirm canonical TLS port list)
    - quirk/db.py:34 (`_SAFE_COL_TYPE_RE` precedent)
    - .planning/phases/71-protocol-scanner-warnings/71-CONTEXT.md (D-03, D-04, D-04a)
  </read_first>
  <action>
    **Part A — Default port CSV (D-03, WR-04):**
    At line 54, replace the existing default port CSV. Compose as:
      - Dynamic half: `cfg.scan.ports_tls` (currently `[443, 8443, 9443, 10443, 5001]`) so it tracks future TLS port changes.
      - Fixed half: `[22, 25, 80, 88, 389, 465, 587, 636, 993, 995, 3389, 5671, 8080, 9092]`.
    Union, sort numerically, dedupe, join with `,`. Match the existing `_default_nmap_args` style (single sorted CSV or list-then-join — pick what fits per D-03a). Example implementation:
    ```python
    _FIXED_NMAP_PORTS = (22, 25, 80, 88, 389, 465, 587, 636, 993, 995, 3389, 5671, 8080, 9092)
    def _default_nmap_ports(cfg):
        ports = sorted(set(tuple(cfg.scan.ports_tls) + _FIXED_NMAP_PORTS))
        return ",".join(str(p) for p in ports)
    ```
    Use this in the default-args construction at line 54 (or wherever the CSV currently lives).

    **Part B — extra_args allowlist (D-04, WR-05):**
    Add at module scope:
    ```python
    import re
    _SAFE_NMAP_ARG_RE = re.compile(r"^[A-Za-z0-9._:/=,-]+$")
    ```
    In `run_nmap_discovery`, BEFORE the subprocess invocation, validate each `extra_args` token:
    ```python
    if extra_args:
        # extra_args may be List[str] or a single shell string per D-04a; normalize:
        if isinstance(extra_args, str):
            tokens = extra_args.split()
        else:
            tokens = list(extra_args)
        for token in tokens:
            if not _SAFE_NMAP_ARG_RE.match(token):
                raise ValueError(f"Unsafe nmap extra arg: {token!r}")
    ```
    Researcher: inspect the existing extra_args type at the function signature; if it's already `List[str]`, drop the `isinstance` branch and just iterate. The point is to fail loud before subprocess.

    No quoting/escaping — reject loudly. Per D-04.

    PEP 8, minimal diff. Do NOT touch other code paths in this file.
  </action>
  <verify>
    <automated>python -m compileall quirk/discovery/nmap_provider.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "_SAFE_NMAP_ARG_RE\\s*=\\s*re\\.compile" quirk/discovery/nmap_provider.py` returns one hit
    - `grep -n "Unsafe nmap extra arg" quirk/discovery/nmap_provider.py` returns one hit
    - `grep -n "5001\\|8443\\|9092\\|5671" quirk/discovery/nmap_provider.py` returns hits proving the new port set is wired (TLS ports + Kafka/AMQPS in the fixed set)
    - `python -m compileall quirk/discovery/nmap_provider.py` exits 0
    - `python -c "from quirk.discovery.nmap_provider import _SAFE_NMAP_ARG_RE; assert _SAFE_NMAP_ARG_RE.match('-sV'); assert not _SAFE_NMAP_ARG_RE.match('; rm -rf /')"` exits 0
  </acceptance_criteria>
  <done>
    Default port CSV composed per D-03; extra_args allowlist guard in place; ValueError on malicious tokens.
  </done>
</task>

<task type="auto">
  <name>Task 2: nmap_parser — switch to defusedxml (WR-06)</name>
  <files>quirk/discovery/nmap_parser.py</files>
  <read_first>
    - quirk/discovery/nmap_parser.py (entire file — confirm it uses `xml.etree.ElementTree as ET` and that no defused-incompatible APIs are used)
    - pyproject.toml line ~29 (confirm `defusedxml>=0.7.1` is present)
  </read_first>
  <action>
    Per D-05: replace the import line `import xml.etree.ElementTree as ET` (or equivalent) with `import defusedxml.ElementTree as ET`. defusedxml.ElementTree exposes `parse`, `fromstring`, and `iterparse` with the same signatures as stdlib ET, so no call-site changes should be required.

    If the file uses `ET.Element` for constructing new elements (not parsing), defusedxml does NOT shadow the Element class — you may need `from xml.etree.ElementTree import Element` alongside the defused parse import. Verify by grep: `grep -n "ET\\.Element\\|ET\\.SubElement" quirk/discovery/nmap_parser.py`. If hits exist, import `Element`/`SubElement` from stdlib `xml.etree.ElementTree` separately while keeping all parse calls on defusedxml.

    Add an inline comment at the import: `# defusedxml per audit WR-06 — defuses XXE/billion-laughs on nmap XML output`.

    Minimal diff. PEP 8.
  </action>
  <verify>
    <automated>python -m compileall quirk/discovery/nmap_parser.py &amp;&amp; python -c "import quirk.discovery.nmap_parser; assert 'defusedxml' in str(quirk.discovery.nmap_parser.ET) or hasattr(quirk.discovery.nmap_parser, 'ET')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^import defusedxml\\.ElementTree" quirk/discovery/nmap_parser.py` >= 1
    - `grep -cE "^import xml\\.etree\\.ElementTree as ET" quirk/discovery/nmap_parser.py` == 0 (stdlib ET-as-ET import removed; bare `xml.etree.ElementTree` imports for Element class are OK if needed)
    - `python -c "from quirk.discovery import nmap_parser"` exits 0
  </acceptance_criteria>
  <done>
    nmap_parser parses via defusedxml; XXE/billion-laughs surface defused.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests + flip WR-04/WR-05/WR-06 audit rows</name>
  <files>tests/test_nmap_hardening.py, .planning/audit-2026-05-08/AUDIT-TASKS.md</files>
  <read_first>
    - quirk/discovery/nmap_provider.py (post-Task 1)
    - quirk/discovery/nmap_parser.py (post-Task 2)
    - .planning/audit-2026-05-08/AUDIT-TASKS.md (WR-04, WR-05, WR-06 rows)
  </read_first>
  <action>
    Create `tests/test_nmap_hardening.py`:
    - `test_safe_nmap_arg_re_accepts_legitimate_args` — parametrize over `["-sV", "-p", "443,8443", "--script=ssl-enum-ciphers", "-Pn", "-T4", "192.168.1.0/24"]`; each must match.
    - `test_safe_nmap_arg_re_rejects_injection` — parametrize over `["; rm -rf /", "$(whoami)", "`id`", "&&", "|", "<file", ">out", "$IFS", "\\n", "arg with space"]`; each must NOT match. (Note: whitespace not in allowlist; `;`, `&`, `|`, `$`, backtick, redirects all blocked.)
    - `test_run_nmap_discovery_rejects_unsafe_extra_args(monkeypatch)` — call `run_nmap_discovery` with `extra_args=["; rm -rf /"]`; assert `ValueError` raised with message containing `"Unsafe nmap extra arg"`. Monkeypatch `subprocess.run` to raise if reached, to prove validation happens before subprocess.
    - `test_default_port_csv_includes_consulting_set(cfg_fixture)` — build a minimal ScanCfg with `ports_tls=[443, 8443, 9443, 10443, 5001]`; call the default-args builder; assert resulting port CSV contains `443`, `8443`, `5671` (AMQPS), `9092` (Kafka), `88` (Kerberos), `22` (SSH), `3389` (RDP).
    - `test_nmap_parser_uses_defusedxml` — `import quirk.discovery.nmap_parser as np; assert np.ET.__name__.startswith('defusedxml')` (defusedxml.ElementTree exposes `__name__` as `defusedxml.ElementTree`).
    - `test_nmap_parser_blocks_xxe` — feed a minimal XXE payload through `np.ET.fromstring(...)`; defusedxml raises `defusedxml.common.EntitiesForbidden` (or similar). Catch and assert.

    Then flip audit ledger rows WR-04, WR-05, WR-06 (lines ~59-61): each `| — | [ ] open |` → `| Phase 71 | [x] closed |`.
  </action>
  <verify>
    <automated>pytest tests/test_nmap_hardening.py -x &amp;&amp; grep -cE "scanners-protocol/WR-0[456].*Phase 71.*\\[x\\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_nmap_hardening.py exists; all tests pass (≥ 6 tests)
    - `grep -cE "scanners-protocol/WR-0[456].*Phase 71.*\\[x\\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 3
    - No other audit rows changed
  </acceptance_criteria>
  <done>
    Allowlist + defusedxml + port-CSV behaviors proven; WR-04/05/06 closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config / CLI → `run_nmap_discovery` extra_args | Free-form strings flow to subprocess; shell injection surface if unvalidated |
| nmap binary stdout → `nmap_parser` | nmap returns attacker-influenced XML (scan target controls service banners); XXE / billion-laughs / external-DTD surface in stdlib ET |
| operator config → default port CSV | A malformed port set causes vacuous scans (silent miss) — D-03 ensures consulting-grade coverage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-05 | Tampering / Elevation | nmap_provider.run_nmap_discovery extra_args | mitigate | Regex allowlist `^[A-Za-z0-9._:/=,-]+$` rejects shell metacharacters; raises ValueError before subprocess. Defense-in-depth — current callers build extra_args from validated cfg, but guard catches future code paths and direct API use. ASVS L1 §5.3.4 (command injection). |
| T-71-06 | Denial of Service / Information Disclosure | nmap_parser XML parse | mitigate | defusedxml.ElementTree blocks external DTD references (XXE), recursive entity expansion (billion-laughs), and external entity loading. ASVS L1 §5.5.2 (deserialization safety for XML). |
| T-71-07 | Information Disclosure (coverage gap) | nmap_provider default port CSV | mitigate | D-03 union ensures the default scan covers TLS, SSH, HTTP, Kerberos, LDAP/S, RDP, AMQPS, Kafka, and email-motion ports — closing the silent-miss gap WR-04 flagged. |
</threat_model>

<verification>
- `python -m compileall quirk/discovery/nmap_provider.py quirk/discovery/nmap_parser.py` clean
- `pytest tests/test_nmap_hardening.py -x` passes
- AUDIT-TASKS.md WR-04, WR-05, WR-06 rows show `Phase 71 | [x] closed`
- No other files in `quirk/discovery/` touched beyond `nmap_provider.py` and `nmap_parser.py`
</verification>

<success_criteria>
- Default port CSV composed per D-03 (TLS dynamic half + fixed consulting set)
- extra_args allowlist regex enforced; malicious tokens raise ValueError
- defusedxml replaces stdlib ET in nmap_parser
- 6+ tests pass
- WR-04, WR-05, WR-06 closed in audit ledger
</success_criteria>

<output>
Create `.planning/phases/71-protocol-scanner-warnings/71-03-SUMMARY.md` documenting the three fixes, the final port CSV, the allowlist regex, the defusedxml import diff, and audit row closures.
</output>
