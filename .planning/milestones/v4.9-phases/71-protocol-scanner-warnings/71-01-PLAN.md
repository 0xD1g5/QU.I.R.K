---
phase: 71-protocol-scanner-warnings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/discovery/coverage.py
  - tests/test_coverage_bounds.py
  - .planning/audit-2026-05-08/AUDIT-TASKS.md
autonomous: true
requirements: [PROTO-01]
must_haves:
  truths:
    - "calculate_coverage returns a value in [0.0, 1.0] regardless of numerator/denominator inputs"
    - "quantum_readiness_score classifies severity case-insensitively (e.g. 'High', 'HIGH', 'high' all match)"
    - "Audit rows scanners-protocol/WR-01 and WR-02 are flipped to [x] closed with Phase 71 attribution"
  artifacts:
    - path: "quirk/discovery/coverage.py"
      provides: "Clamped calculate_coverage and case-insensitive severity comparison"
      contains: "max(0.0, min(1.0,"
    - path: "tests/test_coverage_bounds.py"
      provides: "RED-then-GREEN tests for clamp + severity normalization"
      contains: "def test_"
  key_links:
    - from: "quirk/discovery/coverage.py::calculate_coverage"
      to: "return statement"
      via: "max(0.0, min(1.0, value)) wrapper"
      pattern: "max\\(0\\.0, min\\(1\\.0"
    - from: "quirk/discovery/coverage.py::quantum_readiness_score"
      to: "severity comparison"
      via: "str(severity).upper() normalization before comparison"
      pattern: "\\.upper\\(\\)"
---

<objective>
Close PROTO-01 by clamping `calculate_coverage` to `[0.0, 1.0]` and normalizing severity comparisons in `quantum_readiness_score` to be case-insensitive. Both fixes live in `quirk/discovery/coverage.py`. Flip audit ledger rows WR-01 and WR-02 to closed with Phase 71 attribution.

Purpose: Prevent >100% coverage reports and prevent missed severities when input data uses mixed casing.
Output: Patched `coverage.py`, new test module, two audit rows flipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/71-protocol-scanner-warnings/71-CONTEXT.md
@.planning/audit-2026-05-08/AUDIT-TASKS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clamp calculate_coverage return and normalize severity comparison</name>
  <files>quirk/discovery/coverage.py</files>
  <read_first>
    - quirk/discovery/coverage.py (entire file — small module)
    - .planning/phases/71-protocol-scanner-warnings/71-CONTEXT.md (D-06, D-07)
  </read_first>
  <behavior>
    - calculate_coverage(0, 0, 0) returns 0.0 (no division-by-zero, no negative)
    - calculate_coverage where tls_endpoints exceeds target_count returns 1.0 (clamped, not >1.0)
    - calculate_coverage(negative target_count, ...) returns 0.0 (clamped low bound)
    - quantum_readiness_score classifies "high", "High", "HIGH" identically (uppercase normalization per D-07)
    - quantum_readiness_score classifies "critical", "Critical", "CRITICAL" identically
  </behavior>
  <action>
    Per D-06: at the final `return` of `calculate_coverage`, wrap the value as `return max(0.0, min(1.0, coverage))`. Add an inline comment `# Clamp per audit WR-01 (Phase 71)`. Do NOT change the numerator/denominator math — that belongs to a future INTEL-03 phase per D-15.

    Per D-07: in `quantum_readiness_score`, locate the severity comparison branch (likely `if severity in ("CRITICAL", "HIGH"):` or similar). Replace with `if str(severity).upper() in ("CRITICAL", "HIGH"):` (or the existing severity tuple, uppercased). Use `.upper()` — NOT `.casefold()` per D-07. If multiple severity branches exist (CRITICAL/HIGH/MEDIUM/LOW), apply the same `.upper()` normalization to each comparison consistently.

    Minimal diff. PEP 8. No refactor beyond the two lines required.
  </action>
  <verify>
    <automated>python -m compileall quirk/discovery/coverage.py &amp;&amp; pytest tests/test_coverage_bounds.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "max(0.0, min(1.0" quirk/discovery/coverage.py` returns at least one hit inside `calculate_coverage`
    - `grep -n "\\.upper()" quirk/discovery/coverage.py` returns at least one hit inside `quantum_readiness_score`
    - `python -c "from quirk.discovery.coverage import calculate_coverage; assert calculate_coverage(10, 5, 20) == 1.0"` exits 0 (tls_endpoints exceeds target_count → clamped to 1.0)
    - `python -c "from quirk.discovery.coverage import calculate_coverage; assert calculate_coverage(-5, 10, 5) == 0.0"` exits 0 (negative target_count → clamped to 0.0)
    - `python -c "from quirk.discovery.coverage import calculate_coverage; r = calculate_coverage(10, 5, 5); assert 0.0 <= r <= 1.0"` exits 0 (mid-range result inside bounds)
    - tests/test_coverage_bounds.py passes
  </acceptance_criteria>
  <done>
    coverage.py has the clamp and the uppercase severity normalization; all new tests pass; existing tests pass; `python -m compileall` is clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tests + flip WR-01/WR-02 audit rows</name>
  <files>tests/test_coverage_bounds.py, .planning/audit-2026-05-08/AUDIT-TASKS.md</files>
  <read_first>
    - quirk/discovery/coverage.py (after Task 1 edits)
    - .planning/audit-2026-05-08/AUDIT-TASKS.md lines 56-57 (WR-01, WR-02 rows)
    - tests/ (skim a neighbor test module for fixture style, e.g. tests/test_db_migrations.py)
  </read_first>
  <action>
    Create `tests/test_coverage_bounds.py`:
    - `test_calculate_coverage_clamps_above_one` — feed numerator > denominator, assert return ≤ 1.0
    - `test_calculate_coverage_clamps_below_zero` — feed negative numerator, assert return ≥ 0.0
    - `test_calculate_coverage_zero_denominator` — feed (0, 0), assert no exception, return in [0.0, 1.0]
    - `test_calculate_coverage_normal_range` — feed (5, 10), assert 0.5
    - `test_quantum_readiness_score_severity_case_insensitive` — parametrized over `["high", "High", "HIGH", "critical", "Critical", "CRITICAL"]`; assert each is treated equivalently to its uppercase canonical form (build a minimal findings list with that severity and assert the score reflects the high/critical weighting). Researcher: inspect `quantum_readiness_score` signature to construct the minimal valid input shape.

    Update `.planning/audit-2026-05-08/AUDIT-TASKS.md`:
    - Line 56: change WR-01 row's last two columns from `| — | [ ] open |` to `| Phase 71 | [x] closed |`
    - Line 57: change WR-02 row's last two columns from `| — | [ ] open |` to `| Phase 71 | [x] closed |`
  </action>
  <verify>
    <automated>pytest tests/test_coverage_bounds.py -x &amp;&amp; grep -c "scanners-protocol/WR-0[12].*Phase 71.*\\[x\\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_coverage_bounds.py exists with ≥ 5 test functions; all pass
    - `grep -c "scanners-protocol/WR-01.*Phase 71.*\\[x\\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 1
    - `grep -c "scanners-protocol/WR-02.*Phase 71.*\\[x\\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 1
    - No other audit rows changed (`git diff --stat .planning/audit-2026-05-08/AUDIT-TASKS.md` shows only the WR-01/WR-02 lines modified)
  </acceptance_criteria>
  <done>
    Test module green; audit ledger reflects WR-01 and WR-02 closed under Phase 71.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scan finding input → score calculation | Severity strings from heterogeneous scanners may arrive in mixed case; coverage numerator/denominator may be malformed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01 | Information Disclosure | calculate_coverage return | mitigate | Clamp to [0.0, 1.0] so report consumers cannot be misled by >100% coverage values that would falsely inflate executive summaries |
| T-71-02 | Tampering | quantum_readiness_score severity classification | mitigate | Normalize severity via `.upper()` so a scanner emitting `"High"` instead of `"HIGH"` cannot silently dodge inclusion in the high/critical bucket |
</threat_model>

<verification>
- `python -m compileall quirk/discovery/coverage.py` clean
- `pytest tests/test_coverage_bounds.py -x` passes
- Existing scoring tests still pass (`pytest tests/ -k "score or coverage" -x` if such tests exist)
- AUDIT-TASKS.md WR-01 and WR-02 rows show `Phase 71 | [x] closed`
</verification>

<success_criteria>
- calculate_coverage clamped to [0.0, 1.0]
- quantum_readiness_score severity comparison is case-insensitive via `.upper()`
- 5+ new tests prove the behavior
- WR-01 and WR-02 audit rows closed
- No collateral changes to formula math (per D-15)
</success_criteria>

<output>
Create `.planning/phases/71-protocol-scanner-warnings/71-01-SUMMARY.md` documenting: files changed, audit rows flipped (WR-01, WR-02), test count, and rationale linking to D-06/D-07.
</output>
