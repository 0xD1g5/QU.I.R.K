---
phase: 76-react-frontend-warnings
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dashboard/src/components/theme-provider.tsx
  - src/dashboard/src/pages/executive.tsx
  - src/dashboard/src/components/qramm/ComplianceMapTab.tsx
  - src/dashboard/src/components/__tests__/theme-provider.test.tsx
  - src/dashboard/src/pages/__tests__/executive-pdf-cleanup.test.tsx
  - src/dashboard/src/components/qramm/__tests__/compliance-map-tab.test.tsx
  - .planning/audit-2026-05-08/AUDIT-TASKS.md
autonomous: true
requirements: [REACT-02]
must_haves:
  truths:
    - "`components/theme-provider.tsx` validates `localStorage.getItem(storageKey)` against `VALID_THEMES = ['light', 'dark', 'system'] as const` allowlist; raw or invalid values silently fall back to `'system'` (no console.warn) per D-05 (WR-04)"
    - "`pages/executive.tsx` PDF download stores the revoke timer ID in `revokeTimerRef = useRef<number | null>(null)` and the blob URL in `blobUrlRef = useRef<string | null>(null)`; a component-scope `useEffect` cleanup function calls `clearTimeout(revokeTimerRef.current)` and `URL.revokeObjectURL(blobUrlRef.current)` on unmount per D-06 (WR-05)"
    - "`components/qramm/ComplianceMapTab.tsx` useEffect dependency array (line 136) narrowed from `[ctx.sessionId, ctx.scoreResult]` to `[ctx.sessionId]` — fetch no longer re-fires on every scoreResult mutation per D-07 (WR-13); RESEARCH C-4 confirms `ctx.sessionId` is the stable field (`scoreResult.session_id` does not exist on that type)"
    - "`cd src/dashboard && npm run build` exits 0 after edits"
    - "Audit rows react-frontend/WR-04, WR-05, WR-13 flipped to `Phase 76 | [x] closed`"
  artifacts:
    - path: "src/dashboard/src/components/theme-provider.tsx"
      provides: "VALID_THEMES allowlist + type-narrowing helper (D-05)"
      contains: "VALID_THEMES"
    - path: "src/dashboard/src/pages/executive.tsx"
      provides: "useRef-tracked timer + blob URL cleanup (D-06)"
      contains: "revokeTimerRef"
    - path: "src/dashboard/src/components/qramm/ComplianceMapTab.tsx"
      provides: "Narrowed dependency array — sessionId-only (D-07)"
      contains: "[ctx.sessionId]"
    - path: "src/dashboard/src/components/__tests__/theme-provider.test.tsx"
      provides: "Parametrized RED-then-GREEN coverage of D-05 allowlist"
      contains: "VALID_THEMES"
    - path: "src/dashboard/src/pages/__tests__/executive-pdf-cleanup.test.tsx"
      provides: "RED-then-GREEN coverage of D-06 unmount cleanup"
      contains: "revokeObjectURL"
    - path: "src/dashboard/src/components/qramm/__tests__/compliance-map-tab.test.tsx"
      provides: "RED-then-GREEN coverage of D-07 narrowed deps"
      contains: "scoreResult"
  key_links:
    - from: "components/theme-provider.tsx"
      to: "VALID_THEMES allowlist"
      via: "as const tuple + includes-narrowed cast"
      pattern: "VALID_THEMES"
    - from: "pages/executive.tsx handleExportPdf"
      to: "useRef timer + blob URL cleanup"
      via: "revokeTimerRef.current = window.setTimeout(...); cleanup clears it"
      pattern: "revokeTimerRef"
    - from: "components/qramm/ComplianceMapTab.tsx fetch effect"
      to: "narrowed sessionId-only dependency"
      via: "}, [ctx.sessionId])"
      pattern: "\\[ctx\\.sessionId\\]"
---

<objective>
Close REACT-02 (WR-04, WR-05, WR-13) — localStorage validation, PDF resource cleanup, and ComplianceMapTab refetch narrowing.

1. **WR-04 (D-05)** — `components/theme-provider.tsx:17-19` raw-casts `localStorage.getItem(storageKey) as Theme`. Replace with a `VALID_THEMES = ['light', 'dark', 'system'] as const` allowlist + `getStoredTheme()` helper that returns `defaultTheme` when the raw value is unrecognized. Silent fallback (no console.warn) — theme is QoL, not security.

2. **WR-05 (D-06)** — `pages/executive.tsx:115` uses `setTimeout(() => URL.revokeObjectURL(url), 100)` with no cleanup. If the user navigates away mid-download the timer fires against an unmounted component AND the blob URL may leak. Fix: store the timer ID in `revokeTimerRef = useRef<number | null>(null)` and the blob URL in `blobUrlRef = useRef<string | null>(null)`; a component-scope `useEffect(() => () => { clearTimeout(...); URL.revokeObjectURL(...) }, [])` cleanup handles both on unmount. This mirrors RESEARCH Pattern 2 / Phase 62 HOOK precedent.

3. **WR-13 (D-07)** — `components/qramm/ComplianceMapTab.tsx:136` useEffect depends on `[ctx.sessionId, ctx.scoreResult]`. RESEARCH C-4 confirms: `ctx.scoreResult` is not the stable identity (and `scoreResult.session_id` does not exist on that type); narrow to `[ctx.sessionId]`. Compliance rows are framework-question mapping, independent of scored values — auto-refetch on score change is spurious. Phase 55 Calculate Score button + Recharts static-children preserved untouched (D-12).

Then `cd src/dashboard && npm run build` (exit 0 required) and flip audit rows WR-04, WR-05, WR-13 to `Phase 76 | [x] closed`.

Purpose: Tampered/corrupted localStorage values no longer crash theme-provider; executive PDF flow no longer leaks timers or blob URLs across unmount; ComplianceMapTab stops re-fetching on every score recompute.
Output: 3 source edits, 3 new test modules, 3 audit rows flipped, 1 successful `npm run build`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/76-react-frontend-warnings/76-CONTEXT.md
@.planning/phases/76-react-frontend-warnings/76-RESEARCH.md
@.planning/audit-2026-05-08/AUDIT-TASKS.md

<interfaces>
Existing surfaces (verified at HEAD cf2417a):

src/dashboard/src/components/theme-provider.tsx (lines 1-61):
- Line 17-19: `(localStorage.getItem(storageKey) as Theme) || defaultTheme` — raw cast (WR-04 site)

src/dashboard/src/pages/executive.tsx (lines 99-126):
- Line 115: `setTimeout(() => URL.revokeObjectURL(url), 100)` — WR-05 site
- WR-06 / coerceErrorDetail (76-01-PLAN) lives at line 118 — DO NOT TOUCH that block here

src/dashboard/src/components/qramm/ComplianceMapTab.tsx (lines 100-160):
- Line 121: fetch URL built from `ctx.sessionId`
- Line 136: useEffect dep array `[ctx.sessionId, ctx.scoreResult]` (WR-13 site — narrow to `[ctx.sessionId]`)

Target VALID_THEMES (D-05):
```
const VALID_THEMES = ['light', 'dark', 'system'] as const
type Theme = typeof VALID_THEMES[number]
function getStoredTheme(storageKey: string, defaultTheme: Theme): Theme {
    const raw = localStorage.getItem(storageKey)
    return (VALID_THEMES as readonly string[]).includes(raw ?? '') ? (raw as Theme) : defaultTheme
}
```

Target cleanup ref pattern (D-06):
```
const revokeTimerRef = useRef<number | null>(null)
const blobUrlRef = useRef<string | null>(null)

useEffect(() => {
    return () => {
        if (revokeTimerRef.current !== null) clearTimeout(revokeTimerRef.current)
        if (blobUrlRef.current) URL.revokeObjectURL(blobUrlRef.current)
    }
}, [])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing tests for D-05, D-06, D-07 (3 new test modules)</name>
  <files>src/dashboard/src/components/__tests__/theme-provider.test.tsx, src/dashboard/src/pages/__tests__/executive-pdf-cleanup.test.tsx, src/dashboard/src/components/qramm/__tests__/compliance-map-tab.test.tsx</files>
  <read_first>
    - src/dashboard/src/components/theme-provider.tsx (full file)
    - src/dashboard/src/pages/executive.tsx (lines 99-130 — WR-05 site at 115)
    - src/dashboard/src/components/qramm/ComplianceMapTab.tsx (lines 100-160)
    - src/dashboard/src/hooks/__tests__/useScanData.test.tsx (Vitest idiom reference)
    - .planning/phases/76-react-frontend-warnings/76-CONTEXT.md (D-05..D-07)
    - .planning/phases/76-react-frontend-warnings/76-RESEARCH.md (Pattern 2, C-4)
  </read_first>
  <behavior>
    - D-05 (WR-04) — Parametrized over inputs `'light'`, `'dark'`, `'system'`, `'banana'`, `''`, `null` × default `'system'`:
        - `'light'` → `'light'`; `'dark'` → `'dark'`; `'system'` → `'system'`
        - `'banana'` → `'system'` (fallback)
        - `''` → `'system'`
        - `null` (key absent) → `'system'`
    - D-05 — No `console.warn` called during fallback (spy on `console.warn`)
    - D-06 (WR-05) — Using `vi.useFakeTimers()`, mount `<ExecutivePage />`, simulate the PDF download path (mock fetch + `URL.createObjectURL` + spy `URL.revokeObjectURL`), unmount before the 100ms timer fires. Assert: `URL.revokeObjectURL` was called exactly once (from cleanup, not from the scheduled timer) AND no error thrown.
    - D-06 — Same flow but DO NOT unmount; advance timers by 100ms; assert `URL.revokeObjectURL` called once (timer path). Then unmount; assert no additional revoke (idempotent — `blobUrlRef.current` already cleared).
    - D-07 (WR-13) — Render `<ComplianceMapTab />` with a fixture `ctx = { sessionId: 'sess-1', scoreResult: {dimensions: []} }`. Mock fetch; assert `fetch` called once. Update `ctx.scoreResult` to a different object reference (same `sessionId`); rerender; assert `fetch` NOT called again (call count still 1).
    - D-07 — Update `ctx.sessionId` to `'sess-2'`; rerender; assert `fetch` called twice.
  </behavior>
  <action>
    Create three Vitest modules.

    **`src/dashboard/src/components/__tests__/theme-provider.test.tsx`** — parametrized table:
    - `it.each([['light', 'light'], ['dark', 'dark'], ['system', 'system'], ['banana', 'system'], ['', 'system']])('returns %s for stored value', ...)`
    - One `it('returns default when key absent')` setting `localStorage.removeItem('theme')`.
    - One `it('does not console.warn on invalid value')` with `vi.spyOn(console, 'warn')`.
    - Either (a) import and test the exported `getStoredTheme` helper directly (clean unit) — preferred, requires Task 2 to export it; OR (b) mount `<ThemeProvider defaultTheme="system">` and inspect the resolved theme via a test child consuming `useTheme()`. Prefer (a).
    - Total >= 6 `it()` blocks (5 parametrized + console.warn).

    **`src/dashboard/src/pages/__tests__/executive-pdf-cleanup.test.tsx`** — two `it()` blocks:
    - `it('cleans up timer and blob URL on unmount before revoke fires')` — `vi.useFakeTimers()`, `vi.stubGlobal('URL', { createObjectURL: vi.fn(() => 'blob:fake'), revokeObjectURL: vi.fn() })`. Mock fetch to return a Blob. Render `<ExecutivePage />`, trigger the PDF download button via `userEvent`, await the createObjectURL call, unmount BEFORE `vi.advanceTimersByTime(100)`. Assert `URL.revokeObjectURL` called with `'blob:fake'`. Run `vi.advanceTimersByTime(100)`; assert revoke NOT called again.
    - `it('revokes via scheduled timer when component remains mounted')` — same setup, advance timers 100ms without unmount, assert revoke called once; then unmount, assert no additional revoke.

    **`src/dashboard/src/components/qramm/__tests__/compliance-map-tab.test.tsx`** — three `it()` blocks:
    - `it('fetches once on initial mount with sessionId')`
    - `it('does not refetch when scoreResult mutates but sessionId is stable')` — rerender with new `scoreResult` object; expect fetch call count unchanged.
    - `it('refetches when sessionId changes')` — rerender with different `sessionId`; expect fetch call count incremented.
    - Use a context provider stub (the actual `QrammContext` or whatever provider the component consumes); read_first confirms shape. Mock fetch globally.

    All three RED tests MUST fail because:
    - `theme-provider.tsx` does not yet export `getStoredTheme` and the raw-cast still permits arbitrary values.
    - `executive.tsx` does not yet use `useRef` cleanup — `URL.revokeObjectURL` is not called on unmount.
    - `ComplianceMapTab.tsx` still depends on `ctx.scoreResult`, so fetch fires on scoreResult mutation.

    Run `cd src/dashboard && npm test -- theme-provider executive-pdf-cleanup compliance-map-tab 2>&1 | tail -30` and confirm failure.

    Commit message: `test(76-02): add failing tests for REACT-02 (D-05 theme allowlist, D-06 PDF cleanup, D-07 deps narrow)`.
  </action>
  <verify>
    <automated>cd src/dashboard && npm test -- theme-provider executive-pdf-cleanup compliance-map-tab 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All 3 test files exist
    - `grep -cE "it\\(|it\\.each" src/dashboard/src/components/__tests__/theme-provider.test.tsx` >= 5
    - `grep -cE "it\\(" src/dashboard/src/pages/__tests__/executive-pdf-cleanup.test.tsx` >= 2
    - `grep -cE "it\\(" src/dashboard/src/components/qramm/__tests__/compliance-map-tab.test.tsx` >= 3
    - `grep -cE "VALID_THEMES" src/dashboard/src/components/__tests__/theme-provider.test.tsx` >= 1
    - `grep -cE "revokeObjectURL" src/dashboard/src/pages/__tests__/executive-pdf-cleanup.test.tsx` >= 1
    - `grep -cE "scoreResult" src/dashboard/src/components/qramm/__tests__/compliance-map-tab.test.tsx` >= 1
    - `cd src/dashboard && npm test -- theme-provider executive-pdf-cleanup compliance-map-tab` exits NON-ZERO (RED state)
  </acceptance_criteria>
  <done>
    Three failing test modules committed; vitest exits non-zero with assertion errors tied to D-05..D-07.
  </done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement D-05, D-06, D-07 in theme-provider, executive, ComplianceMapTab</name>
  <files>src/dashboard/src/components/theme-provider.tsx, src/dashboard/src/pages/executive.tsx, src/dashboard/src/components/qramm/ComplianceMapTab.tsx</files>
  <read_first>
    - src/dashboard/src/components/theme-provider.tsx (full file)
    - src/dashboard/src/pages/executive.tsx (lines 99-130 — WR-05 site at 115)
    - src/dashboard/src/components/qramm/ComplianceMapTab.tsx (lines 100-160 — WR-13 site at 136)
    - src/dashboard/src/components/__tests__/theme-provider.test.tsx (Task 1 — confirm `getStoredTheme` import shape)
    - src/dashboard/src/pages/__tests__/executive-pdf-cleanup.test.tsx
    - src/dashboard/src/components/qramm/__tests__/compliance-map-tab.test.tsx
  </read_first>
  <action>
    Apply three surgical edits per `<interfaces>` target shapes.

    **theme-provider.tsx (D-05, WR-04):**
    1. Add at module scope (near existing `Theme` type):
       ```
       export const VALID_THEMES = ['light', 'dark', 'system'] as const
       export type Theme = typeof VALID_THEMES[number]
       export function getStoredTheme(storageKey: string, defaultTheme: Theme): Theme {
           const raw = typeof window === 'undefined' ? null : localStorage.getItem(storageKey)
           return (VALID_THEMES as readonly string[]).includes(raw ?? '') ? (raw as Theme) : defaultTheme
       }
       ```
       (SSR-safe `typeof window === 'undefined'` guard is harmless; matches existing dashboard conventions if any.)
    2. Replace the raw cast at line 17-19. The `useState` initializer becomes:
       ```
       const [theme, setTheme] = useState<Theme>(() => getStoredTheme(storageKey, defaultTheme))
       ```
       Remove the now-unused inline cast. Keep all other behavior (system-resolver effect, setter that writes back to localStorage) untouched.

    **executive.tsx (D-06, WR-05):**
    3. Inside the `ExecutivePage` component body, near other `useRef` declarations (or near the top of the function body):
       ```
       const revokeTimerRef = useRef<number | null>(null)
       const blobUrlRef = useRef<string | null>(null)

       useEffect(() => {
           return () => {
               if (revokeTimerRef.current !== null) {
                   clearTimeout(revokeTimerRef.current)
                   revokeTimerRef.current = null
               }
               if (blobUrlRef.current) {
                   URL.revokeObjectURL(blobUrlRef.current)
                   blobUrlRef.current = null
               }
           }
       }, [])
       ```
    4. In `handleExportPdf`, replace the bare `setTimeout` (line 115) with:
       ```
       blobUrlRef.current = url
       revokeTimerRef.current = window.setTimeout(() => {
           URL.revokeObjectURL(url)
           blobUrlRef.current = null
           revokeTimerRef.current = null
       }, 100)
       ```
       Do NOT touch the WR-06 / `coerceErrorDetail` block at line 118 (that landed in 76-01-PLAN).
    5. If `useRef` / `useEffect` are not already imported from `'react'`, add them.

    **ComplianceMapTab.tsx (D-07, WR-13):**
    6. Locate the useEffect at line 136. Replace its dependency array `[ctx.sessionId, ctx.scoreResult]` with `[ctx.sessionId]`. Add an inline comment: `// D-07: scoreResult intentionally omitted — compliance rows are framework-question mapping, independent of scored values`.
    7. Do NOT touch the fetch URL build (line 121) or any Recharts components — D-12 preserves them.

    Per D-12: no component-structure changes, no new shadcn primitives, no Recharts touches, no Phase-62 hook touches, no Phase 55 Calculate Score button changes.

    Run `cd src/dashboard && npm test -- theme-provider executive-pdf-cleanup compliance-map-tab` — all green.

    Commit message: `feat(76-02): implement REACT-02 fixes (D-05 theme allowlist, D-06 PDF cleanup, D-07 deps narrow)`.
  </action>
  <verify>
    <automated>cd src/dashboard && npm test -- theme-provider executive-pdf-cleanup compliance-map-tab 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "VALID_THEMES = \\['light', 'dark', 'system'\\] as const" src/dashboard/src/components/theme-provider.tsx` == 1
    - `grep -cE "export function getStoredTheme" src/dashboard/src/components/theme-provider.tsx` == 1
    - `grep -v '^[[:space:]]*//' src/dashboard/src/components/theme-provider.tsx | grep -cE "as Theme\\) \\|\\| defaultTheme" ` == 0 (old raw cast removed)
    - `grep -cE "revokeTimerRef" src/dashboard/src/pages/executive.tsx` >= 3
    - `grep -cE "blobUrlRef" src/dashboard/src/pages/executive.tsx` >= 3
    - `grep -cE "URL\\.revokeObjectURL\\(blobUrlRef\\.current\\)" src/dashboard/src/pages/executive.tsx` >= 1
    - `grep -cE "\\[ctx\\.sessionId\\]" src/dashboard/src/components/qramm/ComplianceMapTab.tsx` >= 1
    - `grep -v '^[[:space:]]*//' src/dashboard/src/components/qramm/ComplianceMapTab.tsx | grep -cE "\\[ctx\\.sessionId, ctx\\.scoreResult\\]"` == 0 (old broad dep removed)
    - `cd src/dashboard && npm test -- theme-provider executive-pdf-cleanup compliance-map-tab` exits 0 (GREEN)
  </acceptance_criteria>
  <done>
    All three Task 1 test modules pass; Phase 55 Recharts/Calculate-Score code unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build gate + flip WR-04, WR-05, WR-13 audit rows</name>
  <files>.planning/audit-2026-05-08/AUDIT-TASKS.md</files>
  <read_first>
    - .planning/audit-2026-05-08/AUDIT-TASKS.md lines 222-233 (react-frontend WR rows)
  </read_first>
  <action>
    Step 1 — Build gate. Run `cd src/dashboard && npm run build` and confirm exit code 0. If non-zero, do NOT flip audit rows; fix the build first.

    Step 2 — Update `.planning/audit-2026-05-08/AUDIT-TASKS.md`, flipping these three rows from `| — | [ ] open |` to `| Phase 76 | [x] closed |`:
    - Line 224 (WR-04): theme-provider casts localStorage value to Theme without validation
    - Line 225 (WR-05): executive PDF download setTimeout revoke leaks across unmount
    - Line 233 (WR-13): ComplianceMapTab re-fetches on every ctx.scoreResult change

    No other rows touched. Single docs commit.

    Commit message: `docs(76-02): close react-frontend WR-04/WR-05/WR-13 in audit ledger`.
  </action>
  <verify>
    <automated>cd src/dashboard && npm run build 2>&1 | tail -5; cd ../.. && grep -cE "react-frontend/WR-(04|05|13).*Phase 76.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/dashboard && npm run build` exits 0
    - `grep -cE "react-frontend/WR-(04|05|13).*Phase 76.*\\[x\\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 3
  </acceptance_criteria>
  <done>
    `npm run build` exits 0; AUDIT-TASKS.md shows WR-04, WR-05, WR-13 closed under Phase 76.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `localStorage` → theme-provider | Untrusted browser-storage value crosses into typed state |
| Component unmount → setTimeout/blob URL | Async resource lifetime crosses React component lifetime |
| QRAMM context mutation → fetch effect | Object identity changes trigger network calls; broad deps create spurious traffic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-05 | Tampering | localStorage value tampered to non-Theme string (or null) crashing or producing invalid UI state | mitigate | D-05 `VALID_THEMES` allowlist + `getStoredTheme` fallback to default |
| T-76-06 | DoS (resource leak) | executive PDF setTimeout firing after unmount + blob URL never revoked | mitigate | D-06 useRef + useEffect cleanup releases both resources on unmount |
| T-76-07 | DoS (spurious load) | ComplianceMapTab fetching on every scoreResult mutation flooding API | mitigate | D-07 narrow dep array to sessionId only |
</threat_model>

<verification>
- `cd src/dashboard && npm test -- theme-provider executive-pdf-cleanup compliance-map-tab` passes
- `cd src/dashboard && npm run build` exits 0
- AUDIT-TASKS.md WR-04, WR-05, WR-13 rows show `Phase 76 | [x] closed`
- D-12 do-not-touch list honored (no Phase 55 Calculate Score button changes, no Recharts touches)
</verification>

<success_criteria>
- `getStoredTheme` exported helper exists and is consumed by `<ThemeProvider>` useState initializer
- `revokeTimerRef` and `blobUrlRef` exist in `executive.tsx` and are released by a cleanup useEffect
- `ComplianceMapTab.tsx` useEffect depends only on `[ctx.sessionId]`
- 3 new Vitest modules with full RED→GREEN history
- `npm run build` exits 0
- WR-04, WR-05, WR-13 closed in AUDIT-TASKS.md
</success_criteria>

<output>
Create `.planning/phases/76-react-frontend-warnings/76-02-SUMMARY.md` documenting: D-05 VALID_THEMES allowlist + exported `getStoredTheme`, D-06 useRef cleanup pattern landed (Phase 62 precedent reused), C-4 confirmed (`ctx.sessionId` is the stable field — `ctx.scoreResult?.session_id` does not exist on the type), three audit rows flipped (WR-04, WR-05, WR-13), test count, RED→GREEN commit cadence, `npm run build` confirmed green.
</output>
