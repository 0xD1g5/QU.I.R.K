---
phase: 73-cbom-intel-reports-warnings
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/util/weak_crypto.py
  - quirk/intelligence/evidence.py
  - tests/test_weak_crypto_helper.py
  - tests/test_intelligence_evidence.py
  - .planning/audit-2026-05-08/AUDIT-TASKS.md
autonomous: true
requirements: [INTEL-02]
must_haves:
  truths:
    - "`quirk/util/weak_crypto.py` exists with module-private `_WEAK_CIPHER_TOKENS` frozenset and public `is_weak_cipher(cipher_or_label) -> bool` + `is_legacy_tls_version(tls_version) -> bool` helpers (D-02, D-10)"
    - "`_WEAK_CIPHER_TOKENS` contains DES, 3DES, RC4, MD5, NULL, EXPORT, ANON, DES-CBC, IDEA, SHA1, SHA-1, CBC3 (CBC3 added per RESEARCH C-8 for parity with tls_capabilities.py:103)"
    - "`is_weak_cipher` uppercases input once then checks `any(t in upper for t in _WEAK_CIPHER_TOKENS)`; returns False on None/empty"
    - "`is_legacy_tls_version` returns True for {TLSV1, TLSV1.0, TLSV1.1, SSLV3} (case-insensitive)"
    - "evidence.py `motion_broker_weak_tls_count` predicate routes through `is_weak_cipher` + `is_legacy_tls_version` (D-10, WR-03)"
    - "evidence.py `motion_email_weak_cipher_count` predicate routes through `is_weak_cipher` — email and broker now produce identical output for identical input (WR-11, D-02)"
    - "evidence.py ECDSA detection at lines 127-131 uses `key_alg.startswith((\"EC\", \"ECDSA\"))` aligning EC ↔ ECDSA emitter conventions (D-03, WR-04)"
    - "evidence.py SAML detection at lines 155-161 uses `is_weak_cipher(_saml_alg)` instead of strict `_saml_alg == \"SHA1\"` (D-02, WR-10)"
    - "Audit rows cbom-intel-reports/WR-03, WR-04, WR-10, WR-11 are flipped to `Phase 73 | [x] closed`"
  artifacts:
    - path: "quirk/util/weak_crypto.py"
      provides: "Centralized weak-cipher and legacy-TLS predicates"
      contains: "_WEAK_CIPHER_TOKENS"
    - path: "quirk/intelligence/evidence.py"
      provides: "Unified weak-crypto predicates + aligned ECDSA detection"
      contains: "is_weak_cipher"
    - path: "tests/test_weak_crypto_helper.py"
      provides: "Parametrized cipher-string table + email/broker parity assertion"
      contains: "def test_"
  key_links:
    - from: "quirk/intelligence/evidence.py::motion_broker_weak_tls_count site"
      to: "quirk.util.weak_crypto.is_weak_cipher + is_legacy_tls_version"
      via: "from quirk.util.weak_crypto import is_weak_cipher, is_legacy_tls_version"
      pattern: "from quirk\\.util\\.weak_crypto import"
    - from: "quirk/intelligence/evidence.py::motion_email_weak_cipher_count site"
      to: "quirk.util.weak_crypto.is_weak_cipher"
      via: "if is_weak_cipher(cipher): ..."
      pattern: "is_weak_cipher\\(cipher"
    - from: "quirk/intelligence/evidence.py ECDSA branch line 130"
      to: "EC ↔ ECDSA tuple startswith"
      via: "key_alg.startswith((\"EC\", \"ECDSA\"))"
      pattern: 'startswith\(\("EC", "ECDSA"\)\)'
    - from: "quirk/intelligence/evidence.py SAML SHA-1 branch line 158"
      to: "is_weak_cipher(_saml_alg)"
      via: "if is_weak_cipher(_saml_alg):"
      pattern: "is_weak_cipher\\(_saml_alg"
---

<objective>
Close INTEL-02 by unifying weak-crypto predicates across the intelligence/evidence subsystem:

1. **D-02 (WR-11)** — Create new module `quirk/util/weak_crypto.py` mirroring `quirk/util/safe_exc.py` shape (Phase 59 precedent). Exposes:
   - Module-private `_WEAK_CIPHER_TOKENS: Final[frozenset[str]]` containing the locked D-02 token set PLUS `"CBC3"` for parity with `quirk/util/tls_capabilities.py:103` (per RESEARCH C-8 / open question 3).
   - Public `is_weak_cipher(cipher_or_label: str | None) -> bool` — uppercases input once, then `any(t in upper for t in _WEAK_CIPHER_TOKENS)`; returns False on None/empty.
   - Public `is_legacy_tls_version(tls_version: str | None) -> bool` — uppercases input once, then membership check against `{"TLSV1", "TLSV1.0", "TLSV1.1", "SSLV3"}`; returns False on None/empty. Per D-10 this lives in the same module (deferred separate `tls_versions.py` per CONTEXT Deferred Ideas).

2. **D-03 (WR-04)** — In `quirk/intelligence/evidence.py` at lines 127-131, change the ECDSA detection from `elif key_alg.startswith("ECDSA"):` to `elif key_alg.startswith(("EC", "ECDSA")):`. This aligns the consumer to both TLS scanner (`EC`) and cloud KMS normalizer (`ECDSA`) emitter conventions (CONTEXT: D-14 forbids touching emitter side).

3. **D-02 / WR-10** — In `evidence.py` at lines 155-161, replace `if _saml_alg == "SHA1":` with `if is_weak_cipher(_saml_alg):`. Handles mixed-case (`"SHA-1"`, `"sha1"`, `"#rsa-sha1"`) robustly.

4. **D-10 / WR-03** — In `evidence.py` at lines 240-253 (`motion_broker_weak_tls_count` branch), replace the inline `tls_v in {"TLSV1", "TLSV1.0", "TLSV1.1", "SSLV3"}` with `is_legacy_tls_version(tls_v)`. Replace the inline cipher-token list inside `motion_broker_weak_cipher_count` with `is_weak_cipher(cipher)` for the TOKEN portion; preserve the special-case static-RSA check (`cipher.startswith("TLS_RSA_WITH_")` and the `AES128-SHA / AES256-SHA without ECDHE/DHE` clause) as inline logic — those are structural predicates, not single-token presence checks, and CONTEXT D-10 only unifies the token portion.

5. **D-02 / WR-11** — In `evidence.py` at lines 263-269 (`motion_email_weak_cipher_count`), replace the inline `any(s in cipher for s in ("3DES", "RC4"))` token check with `is_weak_cipher(cipher)`. After this change, the email predicate covers the same token set as the broker predicate — parity verified by the new test `test_email_broker_parity`. Preserve the `cipher.startswith("TLS_RSA_WITH_")` structural check inline (matches broker's special-case shape).

Then flip audit rows WR-03, WR-04, WR-10, WR-11 (lines 151, 152, 158, 159 of `AUDIT-TASKS.md`) to `Phase 73 | [x] closed`.

Purpose: Single source of truth for weak-cipher detection across email/broker/SAML evidence paths; ECDSA consumer aligned to both emitter conventions without touching the emitters.
Output: New `quirk/util/weak_crypto.py`, edits to `quirk/intelligence/evidence.py` at four sites, new `tests/test_weak_crypto_helper.py`, extensions to `tests/test_intelligence_evidence.py`, four audit rows flipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/73-cbom-intel-reports-warnings/73-CONTEXT.md
@.planning/phases/73-cbom-intel-reports-warnings/73-RESEARCH.md
@.planning/audit-2026-05-08/AUDIT-TASKS.md

<interfaces>
Phase 59 module-shape precedent (verified at quirk/util/safe_exc.py:1-53):
```
"""Module purpose docstring.

Decision enforcement:
  - <DEC-ID>: ...
"""
from __future__ import annotations
from typing import Final

_PRIVATE_CONSTANT: Final[...] = (...)

def public_helper(arg: str | None) -> bool:
    """Docstring."""
    if arg is None:
        return False
    upper = arg.upper()
    return any(t in upper for t in _PRIVATE_CONSTANT)
```

Target weak_crypto.py token set (D-02 base set + CBC3 from RESEARCH C-8):
```
_WEAK_CIPHER_TOKENS: Final[frozenset[str]] = frozenset({
    "DES", "3DES", "CBC3", "RC4", "MD5", "NULL", "EXPORT", "ANON",
    "DES-CBC", "IDEA",
    "SHA1", "SHA-1",
})
```

Legacy TLS version set (D-10, sourced from current motion_broker inline check at evidence.py:243):
`{"TLSV1", "TLSV1.0", "TLSV1.1", "SSLV3"}`

Current evidence.py fix sites (verified at HEAD):
- Lines 127-131: ECDSA branch — `elif key_alg.startswith("ECDSA"):` → `elif key_alg.startswith(("EC", "ECDSA")):`
- Lines 155-161: SAML branch — `if _saml_alg == "SHA1":` → `if is_weak_cipher(_saml_alg):`
- Lines 240-253: motion_broker — inline tls_v set → `is_legacy_tls_version(tls_v)`; inline cipher-token set → `is_weak_cipher(cipher)` (token portion only; structural TLS_RSA_WITH_ and ECDHE-less-AES-SHA stay inline)
- Lines 263-269: motion_email — inline `any(s in cipher for s in ("3DES", "RC4"))` → `is_weak_cipher(cipher)`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create quirk/util/weak_crypto.py with _WEAK_CIPHER_TOKENS + is_weak_cipher + is_legacy_tls_version (D-02, D-10)</name>
  <files>quirk/util/weak_crypto.py, tests/test_weak_crypto_helper.py</files>
  <read_first>
    - quirk/util/safe_exc.py (module-shape precedent; full file, ~53 lines)
    - quirk/scanner/tls_capabilities.py lines 95-115 (existing weak_markers for CBC3 parity reference per RESEARCH C-8)
    - .planning/phases/73-cbom-intel-reports-warnings/73-CONTEXT.md (D-02, D-02a, D-10)
    - .planning/phases/73-cbom-intel-reports-warnings/73-RESEARCH.md (Pattern 1, open question 3, C-8)
  </read_first>
  <behavior>
    - `is_weak_cipher(None)` returns False.
    - `is_weak_cipher("")` returns False.
    - `is_weak_cipher("DES-CBC-SHA")` returns True (DES token).
    - `is_weak_cipher("RC4-MD5")` returns True (RC4 + MD5).
    - `is_weak_cipher("NULL-SHA")` returns True (NULL).
    - `is_weak_cipher("EXPORT40-RC2-CBC-MD5")` returns True (EXPORT).
    - `is_weak_cipher("ADH-AES128-SHA")` returns True (ANON).
    - `is_weak_cipher("DES-CBC3-SHA")` returns True (CBC3 token added per RESEARCH C-8; also matches 3DES if input contained that spelling).
    - `is_weak_cipher("IDEA-CBC-SHA")` returns True (IDEA).
    - `is_weak_cipher("SHA1")` returns True (SHA1 token; SAML-flavored input).
    - `is_weak_cipher("sha-1")` returns True (lowercase normalized via upper(); matches SHA-1 token).
    - `is_weak_cipher("AES128-GCM-SHA256")` returns False (no weak token).
    - `is_weak_cipher("ECDHE-RSA-AES256-GCM-SHA384")` returns False.
    - `is_weak_cipher("CHACHA20-POLY1305-SHA256")` returns False.
    - `is_legacy_tls_version(None)` / `("")` returns False.
    - `is_legacy_tls_version("TLSV1")` / `("TLSv1.0")` / `("tlsv1.1")` / `("SSLV3")` all return True.
    - `is_legacy_tls_version("TLSV1.2")` / `("TLSv1.3")` returns False.
  </behavior>
  <action>
    Create `quirk/util/weak_crypto.py` mirroring the `safe_exc.py` shape (per RESEARCH Pattern 1):

    1. Module docstring naming D-02 / D-10 as the enforcing decisions and INTEL-02 as the requirement.
    2. `from __future__ import annotations` and `from typing import Final`.
    3. `_WEAK_CIPHER_TOKENS: Final[frozenset[str]] = frozenset({...})` per the `<interfaces>` token set. Use D-02a discretion: set is **private** (underscore prefix), helpers are **public**. This discourages bypass per CONTEXT D-02a default recommendation.
    4. `_LEGACY_TLS_VERSIONS: Final[frozenset[str]] = frozenset({"TLSV1", "TLSV1.0", "TLSV1.1", "SSLV3"})`.
    5. `def is_weak_cipher(cipher_or_label: str | None) -> bool:` — guard None/empty → False; uppercase once; `return any(t in upper for t in _WEAK_CIPHER_TOKENS)`.
    6. `def is_legacy_tls_version(tls_version: str | None) -> bool:` — guard None/empty → False; uppercase once; `return upper in _LEGACY_TLS_VERSIONS`.

    Both functions include short docstrings naming the locked decision (D-02 / D-10) for traceability.

    Then create `tests/test_weak_crypto_helper.py` with a parametrized test table covering the cases in `<behavior>` (use `pytest.mark.parametrize` with at least 15 (cipher, expected) tuples for `is_weak_cipher` and 8 (version, expected) tuples for `is_legacy_tls_version`). Include `test_email_broker_parity` reading: "for each cipher in the parametrized weak set, `is_weak_cipher(cipher)` returns the same value regardless of repeated call" — placeholder for the cross-predicate parity check finished in Task 2 (this test in Task 1 only asserts symmetry of the helper itself; Task 2 adds the evidence.py-level parity test).

    Per D-14: do NOT modify `quirk/util/safe_exc.py`; do NOT modify `quirk/scanner/tls_capabilities.py` (its existing `_is_weak_cipher` is out of audit scope for this WR cluster); do NOT introduce a separate `quirk/util/tls_versions.py` (deferred per CONTEXT Deferred Ideas).

    PEP 8. New module ≤ 60 lines.
  </action>
  <verify>
    <automated>python -m compileall quirk/util/weak_crypto.py && pytest tests/test_weak_crypto_helper.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `quirk/util/weak_crypto.py` exists
    - `grep -nE "_WEAK_CIPHER_TOKENS" quirk/util/weak_crypto.py` returns >= 2 hits (definition + use)
    - `grep -nE '"CBC3"' quirk/util/weak_crypto.py` returns 1 hit (RESEARCH C-8 parity token)
    - `grep -nE "^def is_weak_cipher" quirk/util/weak_crypto.py` returns 1 hit
    - `grep -nE "^def is_legacy_tls_version" quirk/util/weak_crypto.py` returns 1 hit
    - `grep -nE "_LEGACY_TLS_VERSIONS" quirk/util/weak_crypto.py` returns >= 2 hits
    - `python -m compileall quirk/util/weak_crypto.py` exits 0
    - `pytest tests/test_weak_crypto_helper.py -x` exits 0
    - `grep -cE "@pytest\.mark\.parametrize" tests/test_weak_crypto_helper.py` >= 2
  </acceptance_criteria>
  <done>
    `quirk/util/weak_crypto.py` exports `is_weak_cipher` and `is_legacy_tls_version` with `_WEAK_CIPHER_TOKENS` (incl. CBC3) private; parametrized tests cover the table from `<behavior>`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route evidence.py predicates through weak_crypto; align ECDSA detection (D-02, D-03, D-10; WR-03/04/10/11)</name>
  <files>quirk/intelligence/evidence.py, tests/test_intelligence_evidence.py, tests/test_weak_crypto_helper.py</files>
  <read_first>
    - quirk/intelligence/evidence.py lines 120-275 (full ECDSA branch + SAML branch + motion_broker block + motion_email block)
    - quirk/util/weak_crypto.py (post-Task 1)
    - tests/test_intelligence_evidence.py (current shape — for extension)
    - .planning/phases/73-cbom-intel-reports-warnings/73-CONTEXT.md (D-02, D-03, D-10)
    - .planning/phases/73-cbom-intel-reports-warnings/73-RESEARCH.md (INTEL-02 fix sites + Code Examples section)
  </read_first>
  <behavior>
    - WR-04 (D-03): For an endpoint with `cert_pubkey_alg="EC"`, the ECDSA counter increments. For `cert_pubkey_alg="ECDSA"`, the ECDSA counter increments. For `cert_pubkey_alg="ED25519"` or unknown, ECDSA counter does NOT increment.
    - WR-10 (D-02): For a SAML endpoint with `cert_pubkey_alg="SHA-1"` (with hyphen), `saml_weak_signing_count` increments. Same for `"sha1"`, `"#rsa-sha1"`. Pre-fix code missed all three.
    - WR-03 (D-10): For a KAFKA-TLS endpoint with `tls_version="TLSv1.1"`, `motion_broker_weak_tls_count` increments via `is_legacy_tls_version`. Behavior unchanged from pre-fix for already-uppercase inputs.
    - WR-11 (D-02): For an email endpoint with `cipher_suite="DES-CBC-SHA"` (a weak token NOT in the pre-fix email check), `motion_email_weak_cipher_count` increments — proves the email predicate has been broadened to match the broker token set.
    - Parity assertion: For each cipher in the weak-set, applying the broker predicate and the email predicate produces identical truth values when given identical input. Same for non-weak ciphers.
  </behavior>
  <action>
    **Part A — Imports.** At the top of `quirk/intelligence/evidence.py`, add:
    ```
    from quirk.util.weak_crypto import is_weak_cipher, is_legacy_tls_version
    ```
    Place alongside existing `quirk.util` imports if any; otherwise insert in the import block by PEP 8 ordering.

    **Part B — D-03 ECDSA alignment (WR-04, lines 127-131).**
    Replace `elif key_alg.startswith("ECDSA"):` with `elif key_alg.startswith(("EC", "ECDSA")):`. No other change to this branch. Per RESEARCH "ECDSA detection — current state", `key_alg` is already `.upper()`'d at line 127, so the tuple is applied to already-uppercased input.

    **Part C — D-02 SAML SHA-1 (WR-10, lines 155-161).**
    Replace `if _saml_alg == "SHA1":` with `if is_weak_cipher(_saml_alg):`. The `elif _saml_size ...` branch is unchanged.

    **Part D — D-10 motion_broker (WR-03, lines 240-253).**
    Replace `if tls_v in {"TLSV1", "TLSV1.0", "TLSV1.1", "SSLV3"}:` with `if is_legacy_tls_version(tls_v):`. (`tls_v` is already `.upper()`'d at line 242, so the helper's internal upper() is idempotent.)

    For the cipher-token portion (the `any(s in cipher for s in ("3DES", "RC4", "DES-CBC"))` clause), replace with `is_weak_cipher(cipher)`. Per the locked D-10 boundary, **preserve** the structural clauses inline:
    - `cipher.startswith("TLS_RSA_WITH_")` — static-RSA structural check
    - `(any(s in cipher for s in ("AES128-SHA", "AES256-SHA")) and "ECDHE" not in cipher and "DHE" not in cipher)` — ECDHE-less-AES-SHA structural check

    Final shape:
    ```
    if cipher and (
        cipher.startswith("TLS_RSA_WITH_")
        or is_weak_cipher(cipher)
        or (any(s in cipher for s in ("AES128-SHA", "AES256-SHA"))
            and "ECDHE" not in cipher and "DHE" not in cipher)
    ):
        motion_broker_weak_cipher_count += 1
    ```

    **Part E — D-02 motion_email (WR-11, lines 263-269).**
    Replace `any(s in cipher for s in ("3DES", "RC4"))` with `is_weak_cipher(cipher)`. Preserve `cipher.startswith("TLS_RSA_WITH_")` inline (matches broker's special-case shape). Final shape:
    ```
    if cipher and (
        cipher.startswith("TLS_RSA_WITH_")
        or is_weak_cipher(cipher)
    ):
        motion_email_weak_cipher_count += 1
    ```

    **Part F — Tests.** Extend `tests/test_intelligence_evidence.py` with:
    - `test_ecdsa_alias_ec` and `test_ecdsa_alias_ecdsa` — assert both `cert_pubkey_alg` values increment the ECDSA counter (WR-04 / D-03 RED-then-GREEN).
    - `test_ecdsa_negative_ed25519` — assert ED25519 input does NOT increment the ECDSA counter (proves we didn't over-match).
    - `test_saml_sha1_mixed_case` — assert `_saml_alg` in `("SHA-1", "sha1", "#rsa-sha1")` all increment `saml_weak_signing_count` (WR-10 / D-02 RED-then-GREEN).
    - `test_motion_broker_legacy_tls` — `tls_version="TLSv1.1"` increments `motion_broker_weak_tls_count`.
    - `test_motion_email_des_cbc_now_detected` — `cipher_suite="DES-CBC-SHA"` increments `motion_email_weak_cipher_count` (WR-11 RED-then-GREEN — pre-fix code missed DES-CBC).

    Append to `tests/test_weak_crypto_helper.py`:
    - `test_email_broker_parity` — parametrize over a list of (cipher, expected_weak) tuples; build the smallest evidence-layer harness that exercises both `motion_email_weak_cipher_count` and `motion_broker_weak_tls_count` accumulators with that cipher and asserts parity for the token-only set (excluding the broker's structural-only `TLS_RSA_WITH_` and `AESxxx-SHA without ECDHE` cases — those are structural, not token-based, and remain broker-only). The simplest harness: directly call the helpers and assert `is_weak_cipher(cipher)` is the single source of truth for the email branch.

    Per D-14: do NOT touch any code path outside the four labelled fix sites; do NOT touch `cbom/builder.py`, `trends.py`, `technical.py`, or the TLS scanner emitter side; do NOT alter the structural inline checks listed above.

    PEP 8. Minimal diff.
  </action>
  <verify>
    <automated>python -m compileall quirk/intelligence/evidence.py && pytest tests/test_intelligence_evidence.py tests/test_weak_crypto_helper.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "from quirk\.util\.weak_crypto import is_weak_cipher, is_legacy_tls_version" quirk/intelligence/evidence.py` returns 1 hit
    - `grep -nE 'startswith\(\("EC", "ECDSA"\)\)' quirk/intelligence/evidence.py` returns 1 hit
    - `grep -nE "is_weak_cipher\(_saml_alg\)" quirk/intelligence/evidence.py` returns 1 hit
    - `grep -nE "is_legacy_tls_version\(tls_v\)" quirk/intelligence/evidence.py` returns 1 hit
    - `grep -cE "is_weak_cipher\(cipher\)" quirk/intelligence/evidence.py` >= 2 (broker + email)
    - `grep -v '^#' quirk/intelligence/evidence.py | grep -cE '_saml_alg == "SHA1"'` == 0 (strict equality removed)
    - `grep -v '^#' quirk/intelligence/evidence.py | grep -cE 'startswith\("ECDSA"\)'` == 0 (single-string ECDSA startswith removed)
    - `python -m compileall quirk/intelligence/evidence.py` exits 0
    - `pytest tests/test_intelligence_evidence.py tests/test_weak_crypto_helper.py -x` exits 0
    - `grep -cE "def test_(ecdsa_alias|saml_sha1_mixed|motion_broker_legacy_tls|motion_email_des_cbc|email_broker_parity)" tests/test_intelligence_evidence.py tests/test_weak_crypto_helper.py` >= 5
  </acceptance_criteria>
  <done>
    evidence.py routes through `quirk.util.weak_crypto`; ECDSA detection aliases EC/ECDSA; SAML SHA-1 mixed-case caught; email/broker token sets unified; RED-then-GREEN tests landed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Flip WR-03, WR-04, WR-10, WR-11 audit rows</name>
  <files>.planning/audit-2026-05-08/AUDIT-TASKS.md</files>
  <read_first>
    - .planning/audit-2026-05-08/AUDIT-TASKS.md lines 151, 152, 158, 159
  </read_first>
  <action>
    Update `.planning/audit-2026-05-08/AUDIT-TASKS.md`:
    - Line 151 (WR-03): `| — | [ ] open |` → `| Phase 73 | [x] closed |`
    - Line 152 (WR-04): same flip
    - Line 158 (WR-10): same flip
    - Line 159 (WR-11): same flip

    Per-row evidence summaries land in the SUMMARY.md, not the audit row body. Four rows, single docs commit at end. No other rows touched.
  </action>
  <verify>
    <automated>grep -cE "cbom-intel-reports/WR-(03|04|10|11).*Phase 73.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "cbom-intel-reports/WR-(03|04|10|11).*Phase 73.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 4
    - `git diff --stat .planning/audit-2026-05-08/AUDIT-TASKS.md` shows only four row lines modified
  </acceptance_criteria>
  <done>
    AUDIT-TASKS.md shows WR-03, WR-04, WR-10, WR-11 closed under Phase 73.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TLS scanner / cloud KMS emitter → evidence consumer | Two emitters use different alphabets for the same algorithm (EC vs ECDSA); consumer must accept both without forcing the emitters to converge (D-14) |
| SAML metadata → evidence consumer | SAML signing-algorithm strings appear in multiple forms (`SHA1`, `SHA-1`, `#rsa-sha1`); strict equality misses real weak signatures |
| Email/broker scanner → evidence consumer | Two scanners feed weak-cipher predicates from divergent token lists; drift produces silently-different findings for the same cipher |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-05 | Information Disclosure (missed weak signatures in SAML / email) | Inline-per-site weak-token checks | mitigate | Route through `quirk.util.weak_crypto.is_weak_cipher` (D-02) — single source of truth |
| T-73-06 | Information Disclosure (false-negative on ECDSA-emitted-as-EC) | `evidence.py` ECDSA branch strict startswith | mitigate | `startswith(("EC", "ECDSA"))` aligns both emitter conventions (D-03) without touching emitters (D-14) |
| T-73-07 | Tampering (predicate drift between email and broker) | Divergent inline token lists | mitigate | Unify via `is_weak_cipher` + parity test (D-02, WR-11) |
</threat_model>

<verification>
- `python -m compileall quirk/util/weak_crypto.py quirk/intelligence/evidence.py` clean
- `pytest tests/test_weak_crypto_helper.py tests/test_intelligence_evidence.py -x` passes
- AUDIT-TASKS.md WR-03, WR-04, WR-10, WR-11 rows show `Phase 73 | [x] closed`
- No collateral changes outside the four locked sites in `evidence.py` (per D-14)
- `_WEAK_CIPHER_TOKENS` includes `CBC3` per RESEARCH C-8 adjudication
</verification>

<success_criteria>
- `quirk/util/weak_crypto.py` exports `is_weak_cipher` + `is_legacy_tls_version`
- `_WEAK_CIPHER_TOKENS` private; helpers public (D-02a)
- `evidence.py` ECDSA detection aligned (D-03)
- `evidence.py` SAML SHA-1 routed through helper (D-02 / WR-10)
- `evidence.py` motion_broker uses `is_weak_cipher` + `is_legacy_tls_version` (D-10)
- `evidence.py` motion_email matches broker token set (D-02 / WR-11)
- 15+ parametrized cipher cases + 8+ TLS version cases pass
- WR-03, WR-04, WR-10, WR-11 closed
</success_criteria>

<output>
Create `.planning/phases/73-cbom-intel-reports-warnings/73-02-SUMMARY.md` documenting: new `quirk/util/weak_crypto.py` module (D-02 / D-10), CBC3 added per RESEARCH C-8, four evidence.py sites unified, four audit rows flipped (WR-03/04/10/11), test count, and rationale linking to D-02, D-02a, D-03, D-10.
</output>
</content>
