---
phase: 73-cbom-intel-reports-warnings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/reports/html_renderer.py
  - tests/test_pdf_render_hardening.py
  - tests/test_reports_writer.py
  - .planning/audit-2026-05-08/AUDIT-TASKS.md
autonomous: true
requirements: [INTEL-01]
must_haves:
  truths:
    - "render_pdf_report narrows the inner `except` to (playwright.sync_api.Error, playwright.sync_api.TimeoutError, OSError, RuntimeError) — no blanket `except Exception`"
    - "render_pdf_report wraps Playwright lifecycle in try/finally; finally calls `if browser is not None: browser.close()` defensively (inner try/except Exception: pass)"
    - "On the narrowed exception path, render_pdf_report prints `f\"PDF generation failed: {safe_str(e)}; scan complete, HTML report at {html_path}\"` to sys.stderr"
    - "ImportError fallback path (Playwright missing) is preserved unchanged — D-01a"
    - "safe_str routing satisfies tests/test_safe_exc_gate.py"
    - "Audit rows cbom-intel-reports/WR-01, WR-02, WR-14 are flipped to `Phase 73 | [x] closed`"
  artifacts:
    - path: "quirk/reports/html_renderer.py"
      provides: "Narrowed PDF render except + try/finally cleanup + stderr advisory"
      contains: "playwright.sync_api"
    - path: "tests/test_pdf_render_hardening.py"
      provides: "RED-then-GREEN coverage of D-01 narrowed except + finally cleanup"
      contains: "def test_"
  key_links:
    - from: "quirk/reports/html_renderer.py::render_pdf_report"
      to: "playwright.sync_api.Error / TimeoutError"
      via: "from playwright.sync_api import Error as PlaywrightError, TimeoutError as PlaywrightTimeoutError"
      pattern: "playwright\\.sync_api"
    - from: "quirk/reports/html_renderer.py::render_pdf_report finally"
      to: "browser.close() defensive cleanup"
      via: "if browser is not None: try: browser.close() except Exception: pass"
      pattern: "if browser is not None"
    - from: "quirk/reports/html_renderer.py except branch"
      to: "sys.stderr advisory"
      via: "print(f\"PDF generation failed: {safe_str(e)}; ...\", file=sys.stderr)"
      pattern: "PDF generation failed"
---

<objective>
Close INTEL-01 by hardening the PDF render path in `quirk/reports/html_renderer.py::render_pdf_report` (lines 105-128 at HEAD):

1. **WR-01 (D-01)** — Replace blanket `except Exception:` with narrowed tuple `(PlaywrightError, PlaywrightTimeoutError, OSError, RuntimeError)`. Per RESEARCH C-2, the current Playwright code is the **sync API** (`playwright.sync_api.sync_playwright` at line 111), so D-01's `asyncio.TimeoutError` is unreachable — drop it. The imports become `from playwright.sync_api import Error as PlaywrightError, TimeoutError as PlaywrightTimeoutError`.

2. **WR-02 (D-01)** — Wrap the Playwright lifecycle so `browser` is initialized as `None` before the inner `try`, then `finally: if browser is not None: try: browser.close() except Exception: pass`. Per RESEARCH C-2, there is no explicit `context` object in the sync API (implicit via `new_page()`), so the cleanup target is `browser` only.

3. **WR-14 (D-01)** — In the narrowed `except` block, emit `print(f"PDF generation failed: {safe_str(e)}; scan complete, HTML report at {html_path}", file=sys.stderr)`. Per RESEARCH C-3, emit from the callee (`html_renderer.py`) since `html_path` is already in scope and the user-visible advisory is required by WR-14. `safe_str` import per D-01 satisfies the `tests/test_safe_exc_gate.py` AST gate.

4. **D-01a (discretion)** — The existing ImportError guard at lines 110-113 (`try: from playwright.sync_api import sync_playwright; except ImportError: return False`) is preserved verbatim. Per RESEARCH C-7, Playwright lives in the `[dashboard]` extra (not `[reports]`); the existing guard already satisfies the pattern. No change.

Then flip audit rows WR-01, WR-02, WR-14 (lines 149, 150, 162 of `AUDIT-TASKS.md`) to `Phase 73 | [x] closed` with per-row evidence.

Purpose: PDF render surfaces programmer bugs explicitly, never leaks the Playwright chromium subprocess on inner-raise, and produces a user-visible warning when generation fails.
Output: Patched `html_renderer.py`, new `tests/test_pdf_render_hardening.py`, extended `tests/test_reports_writer.py` (stderr advisory assertion), three audit rows flipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/73-cbom-intel-reports-warnings/73-CONTEXT.md
@.planning/phases/73-cbom-intel-reports-warnings/73-RESEARCH.md
@.planning/audit-2026-05-08/AUDIT-TASKS.md

<interfaces>
Playwright sync API surface (verified at quirk/reports/html_renderer.py:110-113):
- `from playwright.sync_api import sync_playwright` — already imported under ImportError guard
- `from playwright.sync_api import Error as PlaywrightError, TimeoutError as PlaywrightTimeoutError` — NEW imports for D-01 narrowed except (both classes exist since Playwright 1.16; project pin is >=1.58.0)

safe_str helper (verified at quirk/util/safe_exc.py:36):
- `from quirk.util.safe_exc import safe_str`
- `safe_str(exc: BaseException) -> str` — used in WR-14 advisory; AST-gated by tests/test_safe_exc_gate.py

Stderr advisory precedent (verified at quirk/util/optional_extra.py:225):
- `print(..., file=sys.stderr)` — same idiom; WR-14 uses inline string per D-01 (NOT a registered error code, per RESEARCH C-3 path (a)).

Target shape inside render_pdf_report (translated from CONTEXT D-01 async → sync per RESEARCH C-2):
```
browser = None
try:
    with sync_playwright() as p:
        browser = p.chromium.launch()
        page = browser.new_page()
        page.goto(f"file://{os.path.abspath(html_path)}")
        page.pdf(path=pdf_path, format="A4", margin={...}, print_background=True)
    return True
except (PlaywrightError, PlaywrightTimeoutError, OSError, RuntimeError) as e:
    print(f"PDF generation failed: {safe_str(e)}; scan complete, HTML report at {html_path}", file=sys.stderr)
    return False
finally:
    if browser is not None:
        try:
            browser.close()
        except Exception:
            pass
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: html_renderer narrowed except + finally cleanup + stderr advisory (WR-01, WR-02, WR-14 per D-01)</name>
  <files>quirk/reports/html_renderer.py</files>
  <read_first>
    - quirk/reports/html_renderer.py (full file — small, ~150 lines)
    - quirk/util/safe_exc.py (confirm safe_str signature)
    - quirk/util/optional_extra.py lines 220-230 (stderr advisory idiom)
    - .planning/phases/73-cbom-intel-reports-warnings/73-CONTEXT.md (D-01, D-01a)
    - .planning/phases/73-cbom-intel-reports-warnings/73-RESEARCH.md (C-2, C-3, C-7)
  </read_first>
  <action>
    Edit `quirk/reports/html_renderer.py::render_pdf_report` (lines 105-128). Preserve the outer ImportError guard at lines 110-113 verbatim (D-01a per C-7). At the top of the file, ensure `import sys` and `from quirk.util.safe_exc import safe_str` are present (add if missing — minimal diff, alongside existing imports).

    Inside the function body, after the existing `try: from playwright.sync_api import sync_playwright; except ImportError: return False` guard, restructure to match the target shape in `<interfaces>`:

    1. Add second import line immediately after the existing sync_playwright import, inside the same try-block scope (or in a sibling import statement immediately after):
       `from playwright.sync_api import Error as PlaywrightError, TimeoutError as PlaywrightTimeoutError`
       Place both imports under the same `except ImportError: return False` guard.

    2. Initialize `browser = None` immediately before the inner `try:` block.

    3. Replace the existing inner `try ... except Exception: return False` block with the narrowed except shape from `<interfaces>`. Preserve every existing line inside the `with sync_playwright() as p:` block exactly as it is at HEAD — only the `except` and the new `finally` clause change behavior.

    4. The narrowed except block emits the stderr advisory (per D-01 verbatim format string) and returns False. Use `safe_str(e)` for the exception text — required by `tests/test_safe_exc_gate.py` (RESEARCH dependency-confirmation section).

    5. The `finally` clause closes the browser defensively. Per RESEARCH C-2, sync API has no explicit `context` object — close `browser` only, wrapped in inner `try/except Exception: pass` so a close-time error never masks the original exception.

    Per D-14: do NOT touch the `[dashboard]` extra in `pyproject.toml`. Do NOT swap PDF engines. Do NOT change `writer.py:184` beyond what is already there (the `pdf_path = None` consumption pattern stays). Do NOT introduce an INSTALL-NNN registered error code — D-01 locks the inline format string per RESEARCH C-3.

    PEP 8. Minimal diff.
  </action>
  <verify>
    <automated>python -m compileall quirk/reports/html_renderer.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "from playwright\.sync_api import Error as PlaywrightError" quirk/reports/html_renderer.py` returns at least one hit
    - `grep -nE "from playwright\.sync_api import .*TimeoutError as PlaywrightTimeoutError" quirk/reports/html_renderer.py` returns at least one hit
    - `grep -cE "except \(PlaywrightError, PlaywrightTimeoutError, OSError, RuntimeError\)" quirk/reports/html_renderer.py` >= 1
    - `grep -v '^#' quirk/reports/html_renderer.py | grep -cE "except Exception" ` reflects no NEW blanket except (the only `except Exception` permitted is the inner defensive close-time guard inside `finally`)
    - `grep -nE "browser = None" quirk/reports/html_renderer.py` returns at least one hit
    - `grep -nE "if browser is not None" quirk/reports/html_renderer.py` returns at least one hit
    - `grep -nE "PDF generation failed: " quirk/reports/html_renderer.py` returns at least one hit
    - `grep -nE "safe_str\(e\)" quirk/reports/html_renderer.py` returns at least one hit
    - `grep -nE "from quirk\.util\.safe_exc import safe_str" quirk/reports/html_renderer.py` returns at least one hit
    - `grep -nE "except ImportError" quirk/reports/html_renderer.py` returns at least one hit (D-01a preserved)
    - `python -m compileall quirk/reports/html_renderer.py` exits 0
    - `pytest tests/test_safe_exc_gate.py -x` passes (AST gate satisfied)
  </acceptance_criteria>
  <done>
    `render_pdf_report` narrows its except, closes the browser in `finally`, prints the stderr advisory via `safe_str`, and preserves the ImportError fallback.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests for PDF render hardening (WR-01, WR-02, WR-14)</name>
  <files>tests/test_pdf_render_hardening.py, tests/test_reports_writer.py</files>
  <read_first>
    - quirk/reports/html_renderer.py (post-Task 1)
    - tests/test_pdf_export.py (mock idiom reference — sync_playwright patching)
    - tests/test_reports_writer.py (current shape — for the WR-14 stderr assertion extension)
  </read_first>
  <behavior>
    - WR-01: When `page.pdf(...)` raises a `RuntimeError`, `render_pdf_report` returns False (does NOT raise) and stderr contains "PDF generation failed:" — the narrowed except catches the expected type.
    - WR-01 (negative): When `page.pdf(...)` raises a `KeyError` (a programmer bug — NOT in the narrowed tuple), `render_pdf_report` propagates the `KeyError` (does NOT swallow it) — proving the blanket-except is gone.
    - WR-02: When `page.pdf(...)` raises a `PlaywrightError`, `browser.close()` is still invoked by the `finally` block — assert via MagicMock that `browser.close` was called at least once.
    - WR-02 (close-time error): When the inner-raise path triggers `finally: browser.close()` and `browser.close` itself raises, the original exception is NOT masked (function still returns False, stderr still contains the advisory) — defensive close-time `try/except Exception: pass` validated.
    - WR-14: stderr advisory format matches the D-01 string verbatim: contains `PDF generation failed:`, contains the exception text via `safe_str`, contains `scan complete, HTML report at {html_path}`.
    - ImportError branch (D-01a): When `sync_playwright` import fails (patch `builtins.__import__` or the attribute), `render_pdf_report` returns False with no stderr advisory — pre-existing behavior preserved.
  </behavior>
  <action>
    Create `tests/test_pdf_render_hardening.py` mirroring the mock idiom from `tests/test_pdf_export.py`. Use `unittest.mock.patch` on `quirk.reports.html_renderer.sync_playwright` (or whichever symbol the function resolves at call time — confirm by reading the import shape post-Task 1). Use `capsys` (pytest fixture) to capture stderr.

    Test functions (one commit, all in the same module):

    - `test_render_pdf_returns_false_on_playwright_error(tmp_path, capsys)` — patch `sync_playwright` so `page.pdf` raises `PlaywrightError("simulated")`. Assert `render_pdf_report(html, pdf)` returns False; assert `"PDF generation failed:" in capsys.readouterr().err`; assert `"scan complete, HTML report at" in capsys.readouterr().err` is satisfied via the same captured output (capture once).
    - `test_render_pdf_returns_false_on_runtime_error(tmp_path, capsys)` — same shape with `RuntimeError`. Asserts the RuntimeError is in the narrowed tuple.
    - `test_render_pdf_returns_false_on_os_error(tmp_path, capsys)` — same shape with `OSError`. Asserts OSError is in the narrowed tuple.
    - `test_render_pdf_propagates_unexpected_exception(tmp_path)` — patch so `page.pdf` raises `KeyError("programmer bug")`. Assert `pytest.raises(KeyError)` when calling `render_pdf_report`. **Proves the blanket-except is gone (WR-01 RED-then-GREEN).**
    - `test_render_pdf_closes_browser_in_finally(tmp_path)` — patch `sync_playwright` with a mock whose `chromium.launch()` returns a MagicMock browser; `page.pdf` raises `PlaywrightError`. Assert `browser.close.called is True` (WR-02 GREEN).
    - `test_render_pdf_close_failure_does_not_mask(tmp_path, capsys)` — same as above, but `browser.close.side_effect = OSError("close failed")`. Assert function returns False; assert advisory in stderr (no propagated close-time error).
    - `test_render_pdf_import_error_returns_false(tmp_path, capsys, monkeypatch)` — simulate Playwright missing via monkeypatching `sys.modules['playwright.sync_api']` to raise on attribute access (or use `monkeypatch.setattr` to make `sync_playwright` raise `ImportError` at import resolution). Assert returns False; assert stderr is empty (the ImportError path does NOT emit the advisory — only the inner narrowed except does).

    Extend `tests/test_reports_writer.py` with **one** new test `test_pdf_failure_advisory_propagates_via_writer(tmp_path, capsys)`. Drives the writer entry-point with a forced PDF failure (patch `render_pdf_report` to return False), asserts the writer assigns `pdf_path = None` AND stderr contains `PDF generation failed:` (because the callee emits the advisory per RESEARCH C-3). Reuse the existing fixture shape in that file.

    Per RED-then-GREEN: commit the test additions FIRST as a failing-test commit IF they fail against pre-Task-1 main; in practice Task 1 has landed, so a single GREEN commit is acceptable (the orchestrator atomizes commits per the wrap section).

    PEP 8. Minimal diff in `test_reports_writer.py` (one new test only).
  </action>
  <verify>
    <automated>pytest tests/test_pdf_render_hardening.py tests/test_reports_writer.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_pdf_render_hardening.py` exists with >= 7 test functions; all pass
    - `pytest tests/test_pdf_render_hardening.py -x` exits 0
    - `pytest tests/test_reports_writer.py::test_pdf_failure_advisory_propagates_via_writer -x` exits 0
    - `grep -cE "def test_render_pdf" tests/test_pdf_render_hardening.py` >= 7
    - `grep -cE "PDF generation failed" tests/test_pdf_render_hardening.py` >= 1
    - `grep -cE "browser\.close" tests/test_pdf_render_hardening.py` >= 1
  </acceptance_criteria>
  <done>
    PDF render hardening covered by RED-then-GREEN tests including the negative case that proves the blanket-except is removed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Flip WR-01, WR-02, WR-14 audit rows</name>
  <files>.planning/audit-2026-05-08/AUDIT-TASKS.md</files>
  <read_first>
    - .planning/audit-2026-05-08/AUDIT-TASKS.md lines 149, 150, 162 (WR-01, WR-02, WR-14 rows)
  </read_first>
  <action>
    Update `.planning/audit-2026-05-08/AUDIT-TASKS.md`:
    - Line 149 (WR-01): `| — | [ ] open |` → `| Phase 73 | [x] closed |`
    - Line 150 (WR-02): same flip
    - Line 162 (WR-14): same flip

    Per-row evidence summaries land in the SUMMARY.md (Task wrap), not the audit row body — the row format only carries the closing phase + status. Three rows, one line per row, single docs commit at end. No other rows touched.
  </action>
  <verify>
    <automated>grep -cE "cbom-intel-reports/WR-(01|02|14).*Phase 73.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "cbom-intel-reports/WR-(01|02|14).*Phase 73.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 3
    - `git diff --stat .planning/audit-2026-05-08/AUDIT-TASKS.md` shows only three row lines modified
    - `grep -cE "cbom-intel-reports/WR-(03|04|06|07|08|09|10|11|12|13).*\[ \] open" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 10 (other INTEL WR rows untouched by this plan)
  </acceptance_criteria>
  <done>
    AUDIT-TASKS.md shows WR-01, WR-02, WR-14 closed under Phase 73.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright chromium subprocess → host OS | Long-lived browser process leaks if `browser.close()` is bypassed by inner-raise |
| Exception text → stderr → user terminal | Raw exception strings may contain file paths, env-var values, or other sensitive context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Tampering (silent failure) | render_pdf_report blanket `except Exception` | mitigate | Narrow to `(PlaywrightError, PlaywrightTimeoutError, OSError, RuntimeError)` per D-01; programmer bugs now propagate |
| T-73-02 | Denial of Service (resource leak) | Playwright chromium subprocess on inner-raise | mitigate | `try/finally` with defensive `if browser is not None: browser.close()` per D-01 (WR-02) |
| T-73-03 | Information Disclosure (raw exception text in stderr advisory) | WR-14 advisory message | mitigate | Route exception text through `quirk.util.safe_exc.safe_str` (AST-gated by tests/test_safe_exc_gate.py) per D-01 |
| T-73-04 | Denial of Service (close-time error masks original) | finally: browser.close() raising | mitigate | Inner `try/except Exception: pass` around the close call so the narrowed except keeps its return-False semantics |
</threat_model>

<verification>
- `python -m compileall quirk/reports/html_renderer.py` clean
- `pytest tests/test_pdf_render_hardening.py tests/test_reports_writer.py tests/test_safe_exc_gate.py -x` passes
- AUDIT-TASKS.md WR-01, WR-02, WR-14 rows show `Phase 73 | [x] closed`
- No collateral changes to `html_renderer.py` beyond the locked D-01 surgical edits (per D-14)
</verification>

<success_criteria>
- `render_pdf_report` narrowed `except` to the four-type tuple
- Playwright lifecycle wrapped in `try/finally` with defensive `browser.close()`
- Stderr advisory printed via `safe_str(e)` matching D-01 format
- ImportError fallback (D-01a) unchanged
- 7+ tests pass in `tests/test_pdf_render_hardening.py`
- 1 new test passes in `tests/test_reports_writer.py`
- WR-01, WR-02, WR-14 closed in AUDIT-TASKS.md
</success_criteria>

<output>
Create `.planning/phases/73-cbom-intel-reports-warnings/73-01-SUMMARY.md` documenting: D-01 sync-API translation (per RESEARCH C-2), stderr advisory from callee (per RESEARCH C-3), three audit rows flipped (WR-01, WR-02, WR-14), test count, and rationale linking to D-01 + D-01a (no `[reports]` extra per C-7 — D-01a satisfied by existing `[dashboard]`-gated ImportError).
</output>
</content>
