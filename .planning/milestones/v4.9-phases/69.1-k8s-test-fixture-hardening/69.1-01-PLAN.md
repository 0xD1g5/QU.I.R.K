---
phase: 69.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_k8s_connector.py
autonomous: true
requirements: [TEST-69.1-01]
must_haves:
  truths:
    - "tests/test_k8s_connector.py module-level autouse fixture patches K8S_AVAILABLE/AKS_AVAILABLE/GKE_AVAILABLE to True"
    - "All 5 previously-failing k8s_connector tests pass in .venv (Python 3.14)"
    - "No production source code changes (k8s_connector.py untouched)"
    - "pytest tests/test_k8s_connector.py -q reports 18+ passed, 0 failed"
  artifacts:
    - path: tests/test_k8s_connector.py
      provides: "Autouse fixture _k8s_available_flags at module top"
---

<objective>
Close TEST-69.1-01: tests/test_k8s_connector.py works under .venv (Python 3.14) regardless of optional cloud-SDK installation by patching *_AVAILABLE flags at fixture scope rather than per-test.

Purpose: Unblock the Phase 69-03 RED-GREEN test (`test_aks_empty_cluster_list_returns_empty`) and 4 pre-existing failures (`test_gke_encrypted_no_severity`, `test_gke_unencrypted_produces_high`, `test_secret_rbac_403_produces_insufficient_privileges`, `test_aks_credential_failure_emits_inaccessible_per_cluster`) so the canonical project environment runs the full k8s_connector test suite green.

Output: Module-level `@pytest.fixture(autouse=True)` in `tests/test_k8s_connector.py` per CONTEXT D-01.
</objective>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add autouse *_AVAILABLE fixture to tests/test_k8s_connector.py</name>
  <files>tests/test_k8s_connector.py</files>
  <read_first>
    - tests/test_k8s_connector.py (current imports block + first test fixture)
    - .planning/phases/69.1-k8s-test-fixture-hardening/69.1-CONTEXT.md §Specific Implementation
  </read_first>
  <action>
    1. Find the imports block at the top of tests/test_k8s_connector.py.
    2. Ensure `pytest` and `unittest.mock.patch` are imported (they should already be — verify).
    3. Add a module-level `@pytest.fixture(autouse=True)` named `_k8s_available_flags` that yields inside three nested `patch(...)` context managers for K8S_AVAILABLE / AKS_AVAILABLE / GKE_AVAILABLE (verbatim shape from CONTEXT D-01).
    4. Optional cleanup: leave the existing per-test `with patch("...AKS_AVAILABLE", True):` blocks in place (no-op when outer patch already set value to True; removing them is cosmetic and risks introducing diff noise).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_k8s_connector.py -q exits 0 AND output shows 18+ passed, 0 failed</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_k8s_connector.py -q` exits 0
    - Output line includes "passed" with count ≥ 18 (current: 13 passed; +5 fixed)
    - Output line shows 0 failed
    - `grep -c '_k8s_available_flags' tests/test_k8s_connector.py` returns ≥ 1
    - `git diff quirk/scanner/k8s_connector.py` returns empty (no production changes)
  </acceptance_criteria>
  <done>Fixture in place; all 5 previously-failing tests green; no production source changes.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest tests/test_k8s_connector.py -q` exits 0 with 18+ passed, 0 failed
- `.venv/bin/python -m pytest tests/ -q` (full suite) does not regress — compare before/after pass counts
- Phase 69-03 test now green in canonical env (`test_aks_empty_cluster_list_returns_empty`)
- Production source untouched: `git diff quirk/` returns empty
</verification>

<success_criteria>
test_k8s_connector.py runs cleanly in .venv; the Phase 69-03 test failure documented in deferred-items.md is closed; 4 pre-existing failures resolved by the same fix.
</success_criteria>

<output>
Create `.planning/phases/69.1-k8s-test-fixture-hardening/69.1-01-SUMMARY.md`.
</output>
