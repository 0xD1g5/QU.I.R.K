---
phase: 70
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/db.py
  - quirk/models.py
  - quirk/dashboard/api/routes/qramm.py
  - tests/test_qramm_delete_session_fk.py
  - tests/test_qramm_models.py
autonomous: true
requirements:
  - BLOCK-07
must_haves:
  truths:
    - "qramm_profiles table has a FK from session_id REFERENCES qramm_sessions(id) ON DELETE SET NULL on both fresh DBs and pre-existing FK-less DBs"
    - "PRAGMA foreign_keys reads 1 on every newly opened SQLAlchemy connection"
    - "DELETE /api/qramm/sessions/{id} on a session with a linked QRAMMProfile completes with HTTP 204, raises no IntegrityError, and leaves zero qramm_profiles rows for that session_id"
    - "init_db() is idempotent — running it twice on the same on-disk DB does not raise and does not duplicate the qramm_profiles table"
  artifacts:
    - path: "quirk/db.py"
      provides: "Module-level @event.listens_for(Engine, 'connect') hook _sqlite_fk_pragma issuing PRAGMA foreign_keys=ON; new _ensure_qramm_profiles_fk(engine) migration; call to _ensure_qramm_profiles_fk(engine) inside init_db() after _ensure_phase54_qramm_columns"
      contains: "_ensure_qramm_profiles_fk"
    - path: "quirk/models.py"
      provides: "QRAMMProfile.session_id declared as Column(Integer, ForeignKey('qramm_sessions.id', ondelete='SET NULL'), nullable=True); ForeignKey added to the sqlalchemy import line"
      contains: "ForeignKey(\"qramm_sessions.id\", ondelete=\"SET NULL\")"
    - path: "quirk/dashboard/api/routes/qramm.py"
      provides: "delete_session body re-ordered per D-04 — null profile_id, flush, delete QRAMMProfile rows, delete QRAMMAnswer rows, delete session, commit"
      contains: "session.profile_id = None"
    - path: "tests/test_qramm_delete_session_fk.py"
      provides: "Integration tests covering FK-safe delete_session behavior under PRAGMA foreign_keys=ON"
      min_lines: 40
    - path: "tests/test_qramm_models.py"
      provides: "test_qramm_profiles_has_db_level_fk (PRAGMA foreign_key_list assertion) and test_connect_event_enables_fk_pragma (PRAGMA foreign_keys=1 assertion)"
      contains: "PRAGMA foreign_key_list"
  key_links:
    - from: "quirk/dashboard/api/routes/qramm.py::delete_session"
      to: "quirk/models.py::QRAMMProfile.session_id ForeignKey"
      via: "session.profile_id = None + db.flush() before profile delete"
      pattern: "session\\.profile_id = None"
    - from: "quirk/db.py::init_db"
      to: "quirk/db.py::_ensure_qramm_profiles_fk"
      via: "function call after _ensure_phase54_qramm_columns(engine)"
      pattern: "_ensure_qramm_profiles_fk\\(engine\\)"
    - from: "quirk/db.py module scope"
      to: "sqlalchemy.engine.Engine"
      via: "@event.listens_for(Engine, 'connect')"
      pattern: "event\\.listens_for\\(Engine,\\s*[\"']connect[\"']\\)"
---

<objective>
Retrofit a real DB-level foreign key on `qramm_profiles.session_id`, enable per-connection SQLite FK enforcement via a SQLAlchemy `connect` event listener, and re-order `delete_session` so the reverse pointer and linked profile rows are cleared before the session is deleted.

Purpose: Closes audit BLOCKERs `api-cli-core/CR-04` and `CR-05`. Promotes the FK from documentation-only to enforced; eliminates a class of IntegrityError + dangling-row failure modes in QRAMM session deletion.

Output: FK constraint visible via `PRAGMA foreign_key_list('qramm_profiles')`; per-connection `PRAGMA foreign_keys=ON`; ordered `delete_session` body; new tests asserting both schema-level and behavioral guarantees.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-deferred-blockers-api-qramm-model/70-CONTEXT.md
@.planning/phases/70-deferred-blockers-api-qramm-model/70-RESEARCH.md
@.planning/phases/70-deferred-blockers-api-qramm-model/70-PATTERNS.md
@.planning/phases/70-deferred-blockers-api-qramm-model/70-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Existing contracts the executor needs. Do NOT re-discover these. -->

From quirk/models.py L137-155 (current QRAMMProfile shape — target of edit at L148):

  class QRAMMProfile(Base):
      __tablename__ = "qramm_profiles"
      id = Column(Integer, primary_key=True, autoincrement=True)
      session_id = Column(Integer, nullable=True)   # ← REPLACE
      industry = Column(String(64), nullable=True)
      org_size = Column(String(32), nullable=True)
      data_sensitivity = Column(String(32), nullable=True)
      regulatory_obligations = Column(Text, nullable=True)
      geographic_scope = Column(String(32), nullable=True)
      multiplier = Column(Float, nullable=True)
      created_at = Column(DateTime, nullable=True)

From quirk/db.py L1-13 (imports + existing allowlist; add `event` here):

  import os, re
  from sqlalchemy import create_engine, inspect as sa_inspect, text
  from sqlalchemy.engine import Engine
  from sqlalchemy.orm import sessionmaker
  from quirk.models import Base
  _SAFE_COL_RE = re.compile(r"^[a-z][a-z0-9_]*$")

From quirk/dashboard/api/routes/qramm.py L32 (already imports QRAMMProfile, QRAMMAnswer, QRAMMSession — no new imports needed):

  from quirk.models import QRAMMAnswer, QRAMMProfile, QRAMMSession

From quirk/dashboard/api/routes/qramm.py L414-421 (current delete_session body — target of replacement):

  @router.delete("/qramm/sessions/{session_id}", status_code=204)
  def delete_session(session_id: int, db: Session = Depends(get_db)) -> None:
      session = _get_session_or_404(db, session_id)
      db.query(QRAMMAnswer).filter(QRAMMAnswer.session_id == session_id).delete()
      db.delete(session)
      db.commit()
      return None

From tests/test_qramm_router.py::_make_qramm_client L20-43 — UUID-named shared-cache in-memory SQLite TestClient fixture; calls Base.metadata.create_all(engine) (NOT init_db). Per RESEARCH §"Test-engine subtlety" the module-level connect event listener attaches to the Engine class and fires for this engine too, and D-03 makes the FK present at create_all time. Reuse as-is.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing tests for FK presence, PRAGMA enforcement, and FK-safe delete_session</name>
  <files>tests/test_qramm_delete_session_fk.py, tests/test_qramm_models.py</files>
  <read_first>
    - tests/test_qramm_router.py L1-80 and L200-280 (fixture `_make_qramm_client` and existing `test_delete_session_cascades_answers` shape — PATTERNS.md §"tests/test_qramm_delete_session_fk.py")
    - tests/test_qramm_models.py L1-160 (existing TestQRAMMProfileColumns at L132 — extend this file)
    - tests/test_init_db_idempotent.py L20-58 (tmp_path + init_db + inspect/PRAGMA pattern — PATTERNS.md §"tests/test_qramm_models.py")
    - .planning/phases/70-deferred-blockers-api-qramm-model/70-RESEARCH.md §"Validation Architecture" (full test list)
    - .planning/phases/70-deferred-blockers-api-qramm-model/70-PATTERNS.md (analog references)
  </read_first>
  <behavior>
    - test_qramm_profiles_has_db_level_fk (NEW in tests/test_qramm_models.py): init_db(tmp_path / "fk.db") then `PRAGMA foreign_key_list('qramm_profiles')` returns ≥1 row whose referenced-table column (index 2) equals "qramm_sessions"; on_delete column equals "SET NULL"
    - test_connect_event_enables_fk_pragma (NEW in tests/test_qramm_models.py): init_db(tmp_path / "pragma.db") then `PRAGMA foreign_keys` scalar returns 1
    - test_qramm_profiles_fk_retrofit_idempotent (NEW in tests/test_qramm_models.py): init_db on a tmp path twice — both calls succeed; second call does NOT rebuild (assert table row count preserved across the second call when a row was inserted between calls)
    - test_delete_session_with_profile_clears_fk (NEW in tests/test_qramm_delete_session_fk.py): using `_make_qramm_client` style fixture, create a QRAMM session, attach a QRAMMProfile row (session.profile_id = profile.id), DELETE /api/qramm/sessions/{id} returns 204, no IntegrityError raised, post-delete query of QRAMMProfile filtered by session_id returns 0 rows
    - test_delete_session_with_profile_and_answers (NEW in tests/test_qramm_delete_session_fk.py): same as above plus pre-existing QRAMMAnswer rows; assert both profile rows and answer rows are 0 after delete; assert the session row is gone
  </behavior>
  <action>
    Create tests/test_qramm_delete_session_fk.py importing `pytest`, `uuid`, `create_engine`, `sessionmaker`, `TestClient`, and re-using the `_make_qramm_client` shape from tests/test_qramm_router.py L20-43 (copy-locally or import — match whichever the existing tests do). Both new tests use the TestClient + TestingSession pattern; for `session.profile_id` linking, import `QRAMMProfile` and `QRAMMSession` from `quirk.models`, instantiate a profile within a TestingSession, set session.profile_id = profile.id, commit, then issue the DELETE. Assertion text per `<behavior>`. Extend tests/test_qramm_models.py with three new top-level test functions (test_qramm_profiles_has_db_level_fk, test_connect_event_enables_fk_pragma, test_qramm_profiles_fk_retrofit_idempotent) using `tmp_path` and the existing `from quirk.db import init_db` import. Use `from sqlalchemy import text` and `engine.connect()` for PRAGMA queries. Tests MUST FAIL on the current main branch (RED) — verify before Task 2.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && pytest tests/test_qramm_delete_session_fk.py tests/test_qramm_models.py::test_qramm_profiles_has_db_level_fk tests/test_qramm_models.py::test_connect_event_enables_fk_pragma tests/test_qramm_models.py::test_qramm_profiles_fk_retrofit_idempotent -x 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_qramm_delete_session_fk.py exists and contains exactly two test functions (test_delete_session_with_profile_clears_fk, test_delete_session_with_profile_and_answers)
    - tests/test_qramm_models.py contains three new top-level functions named test_qramm_profiles_has_db_level_fk, test_connect_event_enables_fk_pragma, test_qramm_profiles_fk_retrofit_idempotent (grep verifiable)
    - Running the pytest command above on the current code yields ≥4 failures (RED state) — failures indicate either AssertionError (FK not present, PRAGMA off) or IntegrityError on the delete path
    - Tests reference `init_db` from `quirk.db`, not direct `create_engine` calls (so that the migration path is exercised)
  </acceptance_criteria>
  <done>Wave 0 tests committed and failing for the documented reason (FK absent / PRAGMA off / delete unordered).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Land FK + PRAGMA + delete_session reorder so Wave 0 tests pass GREEN</name>
  <files>quirk/db.py, quirk/models.py, quirk/dashboard/api/routes/qramm.py</files>
  <read_first>
    - quirk/db.py full file (L1-290) — confirm import block, allowlist location (L13), `_ensure_*` helpers (L93-211), and init_db (L256-285) call ordering
    - quirk/models.py L1-160 — confirm current `from sqlalchemy import ...` line at top of file and current QRAMMProfile body (L137-155)
    - quirk/dashboard/api/routes/qramm.py L1-60 and L400-460 — confirm imports and current delete_session body
    - .planning/phases/70-deferred-blockers-api-qramm-model/70-CONTEXT.md decisions D-01 through D-04 (locked)
    - .planning/phases/70-deferred-blockers-api-qramm-model/70-RESEARCH.md §"Code Examples" and §"Common Pitfalls" 1, 2, 3, 4
    - .planning/phases/70-deferred-blockers-api-qramm-model/70-PATTERNS.md §"quirk/db.py", §"quirk/models.py", §"quirk/dashboard/api/routes/qramm.py"
  </read_first>
  <action>
    Per D-01: add `_ensure_qramm_profiles_fk(engine)` to quirk/db.py implementing the SQLite 12-step rebuild. Idempotency guard uses `PRAGMA foreign_key_list('qramm_profiles')` — return early if any returned row has referenced-table (index 2) == "qramm_sessions". Issue `PRAGMA foreign_keys=OFF` BEFORE `conn.begin()` (Pitfall 3 — PRAGMA is a no-op inside a transaction). New table schema mirrors the columns currently declared on QRAMMProfile in quirk/models.py L137-155 (id INTEGER PRIMARY KEY AUTOINCREMENT, session_id INTEGER REFERENCES qramm_sessions(id) ON DELETE SET NULL, industry VARCHAR(64), org_size VARCHAR(32), data_sensitivity VARCHAR(32), regulatory_obligations TEXT, geographic_scope VARCHAR(32), multiplier FLOAT, created_at DATETIME). Use explicit column-name `INSERT INTO qramm_profiles_new (cols...) SELECT cols... FROM qramm_profiles` (Pitfall 1). Wrap copy/drop/rename in `trans = conn.begin()` with try/except trans.rollback(); reraise. Re-enable `PRAGMA foreign_keys=ON` in a `finally` after trans completes. Call `_ensure_qramm_profiles_fk(engine)` from `init_db()` immediately after `_ensure_phase54_qramm_columns(engine)` (PATTERNS.md §"init_db ordering pattern" insert point). Per D-02: add `from sqlalchemy import event` to the existing sqlalchemy import line in quirk/db.py and add module-level `_sqlite_fk_pragma` decorated with `@event.listens_for(Engine, "connect")` — issues `PRAGMA foreign_keys=ON` via the raw DBAPI cursor with try/finally cursor.close(). Place the listener immediately AFTER imports and BEFORE `_SAFE_COL_RE`. Per D-03: update quirk/models.py — add `ForeignKey` to the existing `from sqlalchemy import ...` line; replace L148 `session_id = Column(Integer, nullable=True)` with the three-line form `session_id = Column(Integer, ForeignKey("qramm_sessions.id", ondelete="SET NULL"), nullable=True)`. Per D-04: replace the body of `delete_session` in quirk/dashboard/api/routes/qramm.py with the six-step sequence — set session.profile_id = None; db.flush(); delete QRAMMProfile rows where session_id == session_id (use synchronize_session=False if existing answer-delete uses it, otherwise default); delete QRAMMAnswer rows where session_id == session_id; db.delete(session); db.commit(); return None. Preserve the `_get_session_or_404` lookup at the top of the function. Add a one-line `# Phase 70 BLOCK-07/D-04: FK-safe ordering.` comment above the new sequence. Do NOT inline implementations elsewhere.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m compileall quirk/db.py quirk/models.py quirk/dashboard/api/routes/qramm.py -q && pytest tests/test_qramm_delete_session_fk.py tests/test_qramm_models.py tests/test_qramm_router.py tests/test_init_db_idempotent.py -x -q 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `python -m compileall` on the three modified modules returns exit code 0
    - `grep -n "_ensure_qramm_profiles_fk" quirk/db.py` shows BOTH a definition (`def _ensure_qramm_profiles_fk`) and a call from inside `init_db` (two matches minimum)
    - `grep -nE '@event\.listens_for\(Engine,\s*["'"'"']connect["'"'"']\)' quirk/db.py` returns exactly one match
    - `grep -n 'ForeignKey("qramm_sessions.id", ondelete="SET NULL")' quirk/models.py` returns exactly one match
    - `grep -n 'session\.profile_id = None' quirk/dashboard/api/routes/qramm.py` returns exactly one match inside the delete_session function
    - All four Wave 0 tests from Task 1 now pass GREEN (no failures, no errors)
    - Existing tests/test_qramm_router.py (full file), tests/test_qramm_models.py (full file), and tests/test_init_db_idempotent.py (full file) all pass — no FK-OFF regression
    - PRAGMA foreign_keys=OFF appears in quirk/db.py BEFORE `conn.begin()` and PRAGMA foreign_keys=ON appears AFTER the transaction completes (verify by reading the new function — Pitfall 3 hygiene)
  </acceptance_criteria>
  <done>FK retrofit, per-connection PRAGMA hook, model ForeignKey declaration, and re-ordered delete_session body all in place; full QRAMM + init_db test surface green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → FastAPI DELETE /api/qramm/sessions/{id} | Untrusted session_id path parameter (already an int via FastAPI validation) |
| Python process → on-disk SQLite file | Schema integrity boundary; FK enforcement crosses here only when PRAGMA is set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Tampering | `qramm_profiles` row referential integrity | mitigate | DB-level FK `REFERENCES qramm_sessions(id) ON DELETE SET NULL` (D-03) + per-connection `PRAGMA foreign_keys=ON` (D-02) — declared in model and retrofitted into existing DBs via `_ensure_qramm_profiles_fk` (D-01) |
| T-70-02 | Tampering | `delete_session` route | mitigate | Explicit ordered cleanup in `delete_session` (D-04): null reverse pointer + flush before profile delete; FK is safety-net, app code is cleanup driver |
| T-70-03 | Denial of Service | `_ensure_qramm_profiles_fk` migration runtime | accept | 12-step rebuild copies all rows once; `qramm_profiles` is bounded (one row per QRAMM session) — negligible runtime even at thousands of rows |
</threat_model>

<verification>
- All four Wave 0 tests pass (FK presence, PRAGMA on, retrofit idempotent, delete_session FK-safe)
- Pre-existing QRAMM and init_db tests still green
- `python -m compileall` clean on all three edited modules
- `grep -n "ForeignKey" quirk/models.py` shows the new FK declaration on QRAMMProfile.session_id
- `grep -nE "PRAGMA foreign_keys=ON" quirk/db.py` matches the connect event listener AND the migration tail
</verification>

<success_criteria>
1. `qramm_profiles` table has a FK from `session_id` to `qramm_sessions.id` with `ON DELETE SET NULL`, visible via `PRAGMA foreign_key_list('qramm_profiles')`, both on fresh DBs (via model declarative `ForeignKey`) and on pre-existing DBs (via `_ensure_qramm_profiles_fk` migration)
2. Per-connection `PRAGMA foreign_keys` reads `1` on every SQLAlchemy connection
3. `DELETE /api/qramm/sessions/{id}` with an attached `QRAMMProfile` row completes with HTTP 204, raises no `IntegrityError`, and leaves zero `qramm_profiles` rows for that `session_id`
4. `init_db()` is idempotent — running twice on the same on-disk DB does not raise and does not duplicate or drop the `qramm_profiles` table
</success_criteria>

<output>
After completion, create `.planning/phases/70-deferred-blockers-api-qramm-model/70-01-SUMMARY.md` documenting: the three locked decisions implemented (D-01, D-02, D-03, D-04), files modified with line ranges, test names added, and the SQLite Pitfall 3 ordering verified.
</output>
