---
phase: 70
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/dashboard/api/routes/scan.py
  - tests/test_cbom_scan_route.py
autonomous: true
requirements:
  - BLOCK-08
must_haves:
  truths:
    - "_qs_for_alg returns the string 'Unknown' AND emits a WARNING log when classify_algorithm raises KeyError, TypeError, or AttributeError"
    - "_qs_for_alg propagates exceptions of any other type (e.g. RuntimeError, ValueError) — does not silently swallow real bugs"
    - "scan.py defines a module-level logger via logging.getLogger(__name__)"
  artifacts:
    - path: "quirk/dashboard/api/routes/scan.py"
      provides: "Module-level `logger = logging.getLogger(__name__)`; `_qs_for_alg` body uses `except (KeyError, TypeError, AttributeError) as e` and calls `logger.warning('classifier failed for alg=%r: %s', alg, e)`"
      contains: "except (KeyError, TypeError, AttributeError)"
    - path: "tests/test_cbom_scan_route.py"
      provides: "Parametrized tests for the three narrowed exception types + propagation test for unrelated types + warning-log assertion"
      min_lines: 30
  key_links:
    - from: "quirk/dashboard/api/routes/scan.py::_qs_for_alg"
      to: "quirk/cbom/classifier.classify_algorithm"
      via: "narrow `except (KeyError, TypeError, AttributeError) as e`"
      pattern: "except \\(KeyError, TypeError, AttributeError\\)"
    - from: "quirk/dashboard/api/routes/scan.py::_qs_for_alg"
      to: "logging module logger"
      via: "logger.warning('classifier failed for alg=%r: %s', alg, e)"
      pattern: "logger\\.warning\\([\"']classifier failed"
---

<objective>
Replace the bare `except Exception` at `quirk/dashboard/api/routes/scan.py::_qs_for_alg` with a specific exception tuple `(KeyError, TypeError, AttributeError)`, add a logged warning, and let any other exception type propagate to the route handler.

Purpose: Closes audit BLOCKER `api-cli-core/CR-06` (part of BLOCK-08). Eliminates silent finding loss when the classifier raises an unexpected exception type; ensures real bugs surface instead of being relabeled `"Unknown"`.

Output: Narrowed `except`, module-level logger, and a new dedicated test file exercising both the swallowed-and-logged path and the propagation path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-deferred-blockers-api-qramm-model/70-CONTEXT.md
@.planning/phases/70-deferred-blockers-api-qramm-model/70-RESEARCH.md
@.planning/phases/70-deferred-blockers-api-qramm-model/70-PATTERNS.md
@.planning/phases/70-deferred-blockers-api-qramm-model/70-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Existing contracts the executor needs. -->

From quirk/dashboard/api/routes/scan.py L632-642 (current `_qs_for_alg` body — currently a closure inside `_derive_cbom`):

  def _derive_cbom(endpoints: list[CryptoEndpoint]) -> list[CbomComponent]:
      from quirk.cbom.classifier import classify_algorithm, quantum_safety_label

      def _qs_for_alg(alg: str) -> str:
          try:
              _, nist_level, _ = classify_algorithm(alg)
              raw = quantum_safety_label(nist_level)
          except Exception:                       # ← REPLACE per D-05
              raw = "unknown"
          return _QS_DISPLAY.get(raw, "Unknown")

From quirk/dashboard/api/routes/qramm.py L17-19, 49 (logger idiom — copy this shape):

  import logging
  # ...
  logger = logging.getLogger(__name__)

From quirk/cbom/classifier.py L204-249 (referenced contract): `classify_algorithm(alg: str | None)` is total — never raises directly. Exceptions in the `_qs_for_alg` call chain originate downstream in `quantum_safety_label` or from non-string input passed into `classify_algorithm`. Expected failure modes (per CONTEXT D-05): `KeyError`, `TypeError`, `AttributeError`.

Per RESEARCH Open Question 2 (Claude's Discretion): lifting `_qs_for_alg` to module scope for direct unit-testability is RECOMMENDED. Cost: one indent change + relocate the `from quirk.cbom.classifier import ...` line to module scope; benefit: directly importable and monkeypatchable in tests. This plan elects to lift it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing tests for narrow-except, logged warning, and propagation of unrelated exceptions</name>
  <files>tests/test_cbom_scan_route.py</files>
  <read_first>
    - quirk/dashboard/api/routes/scan.py L620-660 (current `_qs_for_alg` and `_QS_DISPLAY` mapping — confirm return value `"Unknown"` for the `"unknown"` fallback key)
    - quirk/cbom/classifier.py L204-249 (confirm `classify_algorithm` return shape `(family, nist_level, ...)`)
    - tests/test_qramm_router.py L1-50 (general pytest + monkeypatch import patterns used in this codebase)
    - .planning/phases/70-deferred-blockers-api-qramm-model/70-PATTERNS.md §"tests/test_cbom_scan_route.py" (parametrize + caplog + monkeypatch shape)
    - .planning/phases/70-deferred-blockers-api-qramm-model/70-RESEARCH.md §"Common Pitfalls" Pitfall 5
    - .planning/phases/70-deferred-blockers-api-qramm-model/70-CONTEXT.md D-05
  </read_first>
  <behavior>
    - test_qs_for_alg_returns_unknown_on_keyerror: when `quirk.cbom.classifier.classify_algorithm` is monkeypatched to raise KeyError, `_qs_for_alg("RSA-2048")` returns `"Unknown"` AND caplog at WARNING level contains the substring "classifier failed"
    - test_qs_for_alg_returns_unknown_on_typeerror: same shape, TypeError → `"Unknown"` + warning log
    - test_qs_for_alg_returns_unknown_on_attributeerror: same shape, AttributeError → `"Unknown"` + warning log
    - test_qs_for_alg_propagates_runtime_error: when classify_algorithm is monkeypatched to raise RuntimeError, `_qs_for_alg("RSA-2048")` raises RuntimeError (does NOT return any string, does NOT log a warning)
    - test_qs_for_alg_propagates_value_error: same shape with ValueError → propagates
    - test_qs_for_alg_happy_path: with classify_algorithm unmocked and a real alg like "RSA-2048", returns a non-empty string and emits no WARNING (regression guard)
  </behavior>
  <action>
    Create tests/test_cbom_scan_route.py. Imports: `import pytest`, `from quirk.dashboard.api.routes.scan import _qs_for_alg` (this import requires Task 2 to lift the function to module scope — Wave 0 tests are EXPECTED to fail with ImportError or by hitting the un-narrowed except, both are valid RED states). For the three narrow-except cases, use `pytest.mark.parametrize("exc_cls", [KeyError, TypeError, AttributeError])` and a fixture function `_raises(*a, **kw): raise exc_cls("boom")` injected via `monkeypatch.setattr("quirk.cbom.classifier.classify_algorithm", _raises)`. Assert `result == "Unknown"` and use `caplog.at_level("WARNING")` + `assert "classifier failed" in caplog.text`. For propagation cases, parametrize over `(RuntimeError, ValueError)` and use `with pytest.raises(<cls>): _qs_for_alg("RSA-2048")`. Happy-path test uses no monkeypatch. Tests MUST fail on current code (RED) — verify before Task 2.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && pytest tests/test_cbom_scan_route.py -x 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_cbom_scan_route.py exists
    - File contains test functions or parametrized cases covering all six behaviors named in `<behavior>`
    - On the current (pre-Task-2) code, pytest reports FAILURES (either ImportError because `_qs_for_alg` is not module-scope, OR the propagation tests fail because the bare `except Exception` swallows RuntimeError/ValueError)
    - Tests use `monkeypatch.setattr` against the dotted path `"quirk.cbom.classifier.classify_algorithm"` (grep verifiable)
    - Tests use `caplog.at_level("WARNING")` for the warning-log assertion
  </acceptance_criteria>
  <done>RED state confirmed: failing tests exist for all six behaviors; failure mode documented in commit message.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Narrow the except, add module logger, lift _qs_for_alg to module scope</name>
  <files>quirk/dashboard/api/routes/scan.py</files>
  <read_first>
    - quirk/dashboard/api/routes/scan.py L1-40 (current import block — confirm no existing `import logging` or module logger)
    - quirk/dashboard/api/routes/scan.py L620-660 (current `_derive_cbom` body containing `_qs_for_alg` closure)
    - quirk/dashboard/api/routes/qramm.py L17-19 and L49 (logger pattern — `import logging` and `logger = logging.getLogger(__name__)` to mirror)
    - .planning/phases/70-deferred-blockers-api-qramm-model/70-CONTEXT.md D-05 and Claude's Discretion #3
    - .planning/phases/70-deferred-blockers-api-qramm-model/70-RESEARCH.md §"Code Examples" `_qs_for_alg` narrowing, §"Open Questions" item 2
    - .planning/phases/70-deferred-blockers-api-qramm-model/70-PATTERNS.md §"quirk/dashboard/api/routes/scan.py"
  </read_first>
  <action>
    Add `import logging` to the import block at the top of quirk/dashboard/api/routes/scan.py (alphabetize with existing stdlib imports). Add module-scope `logger = logging.getLogger(__name__)` after imports and before the first function definition, mirroring quirk/dashboard/api/routes/qramm.py L19/L49. Lift `_qs_for_alg` from inside `_derive_cbom` to module scope (per RESEARCH Open Question 2 — minimal-diff cost, enables direct unit testability). Place it at module scope above `_derive_cbom`. Move the `from quirk.cbom.classifier import classify_algorithm, quantum_safety_label` import that currently lives inside `_derive_cbom` to module scope as well (or keep it lazy inside `_qs_for_alg` if it must remain lazy to avoid circular import — verify by trying both; if a top-level import works, prefer it). In `_qs_for_alg`, replace `except Exception:` with `except (KeyError, TypeError, AttributeError) as e:` and INSERT `logger.warning("classifier failed for alg=%r: %s", alg, e)` BEFORE the `raw = "unknown"` line. Inside `_derive_cbom`, replace the previous local closure body with a call to the module-scope `_qs_for_alg`. Do NOT change any other code in `_derive_cbom` or in `scan.py` — diff must be tight per D-07 minimal-diff philosophy. Do NOT widen the exception tuple beyond `(KeyError, TypeError, AttributeError)` — Pitfall 5.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m compileall quirk/dashboard/api/routes/scan.py -q && pytest tests/test_cbom_scan_route.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `python -m compileall quirk/dashboard/api/routes/scan.py` exits 0
    - `grep -nE "^import logging$" quirk/dashboard/api/routes/scan.py` returns exactly one match
    - `grep -nE "^logger = logging\.getLogger\(__name__\)$" quirk/dashboard/api/routes/scan.py` returns exactly one match at module scope (not indented)
    - `grep -nE "^def _qs_for_alg\(" quirk/dashboard/api/routes/scan.py` returns exactly one match (proves the function is module-scope, not a closure)
    - `grep -nE "except \(KeyError, TypeError, AttributeError\)" quirk/dashboard/api/routes/scan.py` returns exactly one match inside `_qs_for_alg`
    - `grep -nE 'logger\.warning\("classifier failed' quirk/dashboard/api/routes/scan.py` returns exactly one match
    - `grep -cnE 'except Exception:' quirk/dashboard/api/routes/scan.py` does NOT include the line previously at L640 (the original bare except is gone — confirm by reading the new file)
    - All six tests in tests/test_cbom_scan_route.py pass GREEN
    - Existing scan.py-related tests (any test that imports from `quirk.dashboard.api.routes.scan` or hits a `/api/scan*` route) continue to pass: `pytest tests/ -k "scan or cbom" -x -q` returns no new failures attributable to this change
  </acceptance_criteria>
  <done>Narrow except + logged warning + module-scope lift in place; Wave 0 tests green; no regressions in scan/cbom test surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Classifier internals → `_qs_for_alg` consumer | Exceptions cross this boundary; narrow vs broad except controls what surfaces vs. silently degrades |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-04 | Information Disclosure (negative — silent suppression) / Repudiation | `_qs_for_alg` bare `except` | mitigate | Narrow to `(KeyError, TypeError, AttributeError)` per D-05 + `logger.warning` so suppressed failures leave an audit trail. Other exception classes propagate so real bugs surface. |
| T-70-05 | Tampering | classifier algorithm input string `alg` | accept | `alg` is internally sourced from CryptoEndpoint records (not user free-text); narrow except already protects against malformed-input crashes |
</threat_model>

<verification>
- All six Wave 0 tests in tests/test_cbom_scan_route.py pass
- `grep -n "except Exception" quirk/dashboard/api/routes/scan.py` does NOT match the prior `_qs_for_alg` site (broader scan.py except clauses unrelated to this plan may remain — those are Phase 75)
- No new failures in `pytest tests/ -k "scan or cbom" -x -q`
- `python -m compileall quirk/dashboard/api/routes/scan.py` clean
</verification>

<success_criteria>
1. `_qs_for_alg` catches exactly `(KeyError, TypeError, AttributeError)`; logs a structured `logger.warning` message; returns `"Unknown"` only for those three types
2. Any other exception type raised by `classify_algorithm` or `quantum_safety_label` propagates out of `_qs_for_alg`
3. `_qs_for_alg` is importable as `from quirk.dashboard.api.routes.scan import _qs_for_alg` (module-scope, directly testable)
4. Existing CBOM/scan tests continue to pass — no regression
</success_criteria>

<output>
After completion, create `.planning/phases/70-deferred-blockers-api-qramm-model/70-02-SUMMARY.md` documenting: D-05 implemented (with the three-type tuple), lift-to-module-scope decision rationale (RESEARCH Open Question 2 closed), test names added, and confirmation that no other broad `except Exception` clauses in scan.py were touched (those belong to Phase 75).
</output>
