---
phase: 70
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/db.py
  - tests/test_db_migrations.py
  - .planning/audit-2026-05-08/AUDIT-TASKS.md
autonomous: true
requirements:
  - BLOCK-08
must_haves:
  truths:
    - "quirk/db.py defines a module-level `_SAFE_COL_TYPE_RE` regex allowlist accepting only TEXT, INTEGER, REAL, BOOLEAN, DATETIME, and VARCHAR(\\d{1,4})"
    - "Each of _ensure_v43_columns, _ensure_phase41_columns, _ensure_phase46_columns, _ensure_phase54_qramm_columns raises ValueError when its DDL dict is poisoned with a col_type that fails the allowlist"
    - "All four helpers continue to succeed with their existing real col_type values (TEXT, VARCHAR(16), VARCHAR(32), BOOLEAN)"
    - "AUDIT-TASKS.md rows api-cli-core/CR-04, CR-05, CR-06, CR-07 are flipped from `[ ] deferred-v4.9` to `[x] closed` with Phase 70 evidence appended"
  artifacts:
    - path: "quirk/db.py"
      provides: "Module-level `_SAFE_COL_TYPE_RE = re.compile(r\"^(TEXT|INTEGER|REAL|BOOLEAN|DATETIME|VARCHAR\\(\\d{1,4}\\))$\")` adjacent to `_SAFE_COL_RE`; ValueError guard inserted in 4 `_ensure_*_columns` helpers immediately after the existing _SAFE_COL_RE check"
      contains: "_SAFE_COL_TYPE_RE"
    - path: "tests/test_db_migrations.py"
      provides: "Parametrized accept/reject matrix for _SAFE_COL_TYPE_RE plus poisoned-dict tests against all four guarded helpers"
      min_lines: 50
    - path: ".planning/audit-2026-05-08/AUDIT-TASKS.md"
      provides: "Rows api-cli-core/CR-04, CR-05, CR-06, CR-07 marked `[x] closed` with Phase 70 column and per-row evidence summary"
      contains: "[x] closed"
  key_links:
    - from: "quirk/db.py::_ensure_v43_columns / _ensure_phase41_columns / _ensure_phase46_columns / _ensure_phase54_qramm_columns"
      to: "quirk/db.py::_SAFE_COL_TYPE_RE"
      via: "if not _SAFE_COL_TYPE_RE.match(col_type): raise ValueError(...)"
      pattern: "_SAFE_COL_TYPE_RE\\.match\\(col_type\\)"
    - from: ".planning/audit-2026-05-08/AUDIT-TASKS.md row CR-04/05"
      to: "70-01-PLAN.md"
      via: "evidence column referencing tests landed in plan 70-01"
      pattern: "Phase 70"
    - from: ".planning/audit-2026-05-08/AUDIT-TASKS.md row CR-06"
      to: "70-02-PLAN.md"
      via: "evidence column referencing tests landed in plan 70-02"
      pattern: "Phase 70"
    - from: ".planning/audit-2026-05-08/AUDIT-TASKS.md row CR-07"
      to: "70-03-PLAN.md"
      via: "evidence column referencing tests landed in this plan"
      pattern: "Phase 70"
---

<objective>
Add the `_SAFE_COL_TYPE_RE` regex allowlist for DDL type fragments to `quirk/db.py`, insert the `ValueError` guard into the four migration helpers that interpolate `col_type` from a dict, and flip the four audit rows (CR-04/05/06/07) to `[x] closed` with per-row evidence.

Purpose: Closes audit BLOCKER `api-cli-core/CR-07` (defense-in-depth for SQL injection on DDL type fragments — completes BLOCK-08 alongside Plan 70-02) and lands the AUDIT-TASKS row flips that document Phase 70 closure for all four CR rows (CR-04/05 from Plan 70-01, CR-06 from Plan 70-02, CR-07 from this plan).

Output: New regex constant in `quirk/db.py`, four parallel guard insertions, new test file exercising the accept/reject matrix, and four flipped audit rows with evidence pointing at the tests landed by Plans 70-01, 70-02, and 70-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-deferred-blockers-api-qramm-model/70-CONTEXT.md
@.planning/phases/70-deferred-blockers-api-qramm-model/70-RESEARCH.md
@.planning/phases/70-deferred-blockers-api-qramm-model/70-PATTERNS.md
@.planning/phases/70-deferred-blockers-api-qramm-model/70-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Existing contracts the executor needs. -->

From quirk/db.py L12-13 (allowlist constant location — `_SAFE_COL_TYPE_RE` goes immediately below):

  # Allowlist pattern for migration column names — prevents SQL injection via column interpolation.
  _SAFE_COL_RE = re.compile(r"^[a-z][a-z0-9_]*$")

From quirk/db.py L93-211 — four `_ensure_*_columns` helpers that read `col_type` from a dict (D-06 scope):

  _ensure_v43_columns       — iterates _V43_COLUMN_DDLS (values: "TEXT", "VARCHAR(16)")
  _ensure_phase41_columns   — iterates _PHASE41_COLUMN_DDLS (values: "VARCHAR(32)")
  _ensure_phase46_columns   — iterates _PHASE46_COLUMN_DDLS (values: "BOOLEAN")
  _ensure_phase54_qramm_columns — iterates _PHASE54_QRAMM_ANSWER_DDLS (values: "TEXT")

The existing per-helper shape (template for guard insertion — example from `_ensure_v43_columns`):

    for col, col_type in _V43_COLUMN_DDLS.items():
        if not _SAFE_COL_RE.match(col):
            raise ValueError(f"Unsafe column name in migration: {col!r}")
        # ← INSERT _SAFE_COL_TYPE_RE guard here per D-06
        if col not in existing:
            conn.execute(text(f"ALTER TABLE crypto_endpoints ADD COLUMN {col} {col_type}"))

Per D-07 — the following helpers do NOT bind `col_type` from a variable (they interpolate literal "TEXT") and MUST NOT be touched:
  _ensure_identity_columns (L49)
  _ensure_gcp_columns (L71)
  _ensure_email_columns (L112)
  _ensure_broker_columns (L132)

From .planning/audit-2026-05-08/AUDIT-TASKS.md L180-183 — target rows (currently `[ ] deferred-v4.9`):

  | api-cli-core/CR-04 | BLOCKER | QRAMMProfile.session_id is nullable and has no DB-level FK | — | [ ] deferred-v4.9 |
  | api-cli-core/CR-05 | BLOCKER | delete_session does not clear qramm_sessions.profile_id link | — | [ ] deferred-v4.9 |
  | api-cli-core/CR-06 | BLOCKER | Bare except: pass in classifier call drops findings silently | — | [ ] deferred-v4.9 |
  | api-cli-core/CR-07 | BLOCKER | SQL injection guard on column names lacks col_type DDL fragment | — | [ ] deferred-v4.9 |

From .planning/audit-2026-05-08/AUDIT-TASKS.md L84-87 — Phase 69 closure shape (template for the flip):

  | scanners-cloud/CR-06 | BLOCKER | ... | Phase 69 (BLOCK-05) | [x] closed — closed by Phase 69 (BLOCK-05): <one-line resolution>. <Test ref>. |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing tests for _SAFE_COL_TYPE_RE allowlist and the four guarded helpers</name>
  <files>tests/test_db_migrations.py</files>
  <read_first>
    - quirk/db.py L1-211 (read the full migration helper region; confirm exact dict names `_V43_COLUMN_DDLS`, `_PHASE41_COLUMN_DDLS`, `_PHASE46_COLUMN_DDLS`, `_PHASE54_QRAMM_ANSWER_DDLS`)
    - tests/test_init_db_idempotent.py L1-60 (PATTERNS analog: iterate `_ensure_*` family + tmp_path + init_db usage)
    - .planning/phases/70-deferred-blockers-api-qramm-model/70-CONTEXT.md D-06 and D-07
    - .planning/phases/70-deferred-blockers-api-qramm-model/70-RESEARCH.md §"Common Pitfalls" Pitfall 6
    - .planning/phases/70-deferred-blockers-api-qramm-model/70-PATTERNS.md §"tests/test_db_migrations.py"
  </read_first>
  <behavior>
    - test_safe_col_type_re_accepts_real_values: parametrized over ["TEXT", "INTEGER", "REAL", "BOOLEAN", "DATETIME", "VARCHAR(16)", "VARCHAR(32)", "VARCHAR(9999)"] — each value matches `_SAFE_COL_TYPE_RE`
    - test_safe_col_type_re_rejects_unsafe_values: parametrized over ["TEXT; DROP TABLE x", "VARCHAR(99999)", "varchar(16)", "", "TEXT NOT NULL", "INTEGER PRIMARY KEY", "BLOB", "VARCHAR()"] — each value does NOT match
    - test_v43_columns_rejects_poisoned_col_type: init_db on tmp_path; monkeypatch.setitem on quirk.db._V43_COLUMN_DDLS injecting `("evil_col", "TEXT; DROP TABLE x")`; calling `_ensure_v43_columns(engine)` raises ValueError matching "Unsafe column type"
    - test_phase41_columns_rejects_poisoned_col_type: same pattern, dict `_PHASE41_COLUMN_DDLS`, helper `_ensure_phase41_columns`
    - test_phase46_columns_rejects_poisoned_col_type: same pattern, dict `_PHASE46_COLUMN_DDLS`, helper `_ensure_phase46_columns`
    - test_phase54_qramm_columns_rejects_poisoned_col_type: same pattern, dict `_PHASE54_QRAMM_ANSWER_DDLS`, helper `_ensure_phase54_qramm_columns`
    - test_all_guarded_helpers_accept_real_values: init_db on tmp_path completes without raising — regression that real values still pass the new guard
  </behavior>
  <action>
    Create tests/test_db_migrations.py. Imports: `import pytest`, `from pathlib import Path`, `import quirk.db as quirk_db`, `from quirk.db import init_db`. For the regex parametrized tests, import `_SAFE_COL_TYPE_RE` from quirk.db and assert `_SAFE_COL_TYPE_RE.match(value) is not None` (or `is None` for reject cases). For the four poisoned-dict tests, follow the PATTERNS.md L309-321 pattern — call `init_db(str(tmp_path / "poison.db"))` to get an engine, then `monkeypatch.setitem(quirk_db._V43_COLUMN_DDLS, "evil_col", "TEXT; DROP TABLE x")` (substitute the dict + helper for each variant), then `with pytest.raises(ValueError, match="Unsafe column type"):` call the helper. Each of the four poisoned tests is its own function (not parametrized — different dicts + helpers). Imports MUST resolve — `_SAFE_COL_TYPE_RE` does not exist yet, so this Wave 0 file is expected to RED with ImportError. Tests MUST fail on the current code.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && pytest tests/test_db_migrations.py -x 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_db_migrations.py exists
    - File contains test functions covering all seven behaviors named in `<behavior>`
    - File imports `_SAFE_COL_TYPE_RE` from `quirk.db` (grep verifiable)
    - On the current (pre-Task-2) code, pytest RED state — either ImportError on `_SAFE_COL_TYPE_RE` or AssertionError on the poisoned-dict tests (because no ValueError is raised yet)
    - Parametrized accept cases include both `VARCHAR(16)` and `VARCHAR(9999)` (boundary check on the `\\d{1,4}` bound)
    - Parametrized reject cases include `VARCHAR(99999)` (5-digit overrun — must fail) and `"TEXT; DROP TABLE x"` (injection canary)
  </acceptance_criteria>
  <done>RED state confirmed: failing tests exist for the regex matrix and all four guarded-helper poison cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add _SAFE_COL_TYPE_RE + ValueError guard to four _ensure_* helpers</name>
  <files>quirk/db.py</files>
  <read_first>
    - quirk/db.py full file (L1-290) — exact line numbers for the four target helpers and the existing `_SAFE_COL_RE` definition at L13
    - .planning/phases/70-deferred-blockers-api-qramm-model/70-CONTEXT.md D-06 (verbatim regex string) and D-07 (do-not-touch list)
    - .planning/phases/70-deferred-blockers-api-qramm-model/70-RESEARCH.md §"Common Pitfalls" Pitfall 6 (regex coverage verification table)
    - .planning/phases/70-deferred-blockers-api-qramm-model/70-PATTERNS.md §"quirk/db.py" — "Allowlist constant pattern" and "Migration helper guard then DDL pattern"
  </read_first>
  <action>
    Add `_SAFE_COL_TYPE_RE = re.compile(r"^(TEXT|INTEGER|REAL|BOOLEAN|DATETIME|VARCHAR\(\d{1,4}\))$")` to quirk/db.py on the line immediately following the existing `_SAFE_COL_RE` declaration (L13). In each of the four target helpers (`_ensure_v43_columns`, `_ensure_phase41_columns`, `_ensure_phase46_columns`, `_ensure_phase54_qramm_columns`), insert the two-line guard immediately AFTER the existing `_SAFE_COL_RE` check and BEFORE the `if col not in existing:` / `conn.execute(text(...))` block. The inserted lines: `if not _SAFE_COL_TYPE_RE.match(col_type):` then `    raise ValueError(f"Unsafe column type in migration: {col_type!r}")`. Critically — the regex check must run on EVERY iteration of the dict loop (so a poisoned col_type fails fast even if the column already exists per `existing`), which is naturally the case if you insert the guard before the `if col not in existing:` branch. Do NOT modify `_ensure_identity_columns`, `_ensure_gcp_columns`, `_ensure_email_columns`, `_ensure_broker_columns` (D-07 — literal `TEXT` only, no col_type variable). Do NOT modify the new function `_ensure_qramm_profiles_fk` from Plan 70-01 (it does not iterate a col_type dict). Diff target: 1 new constant line + 4 × 2-line guard insertions = ~9 new lines total. Confirm the regex coverage table from RESEARCH Pitfall 6: TEXT, VARCHAR(16), VARCHAR(32), BOOLEAN all match.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m compileall quirk/db.py -q && pytest tests/test_db_migrations.py tests/test_init_db_idempotent.py tests/test_qramm_models.py -x -q 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `python -m compileall quirk/db.py` exits 0
    - `grep -nE "^_SAFE_COL_TYPE_RE = re\.compile" quirk/db.py` returns exactly one match adjacent to `_SAFE_COL_RE`
    - `grep -cnE "_SAFE_COL_TYPE_RE\.match\(col_type\)" quirk/db.py` returns at least 4 (one per guarded helper)
    - `grep -cnE 'raise ValueError\(f"Unsafe column type in migration' quirk/db.py` returns at least 4
    - The four prohibited helpers (`_ensure_identity_columns`, `_ensure_gcp_columns`, `_ensure_email_columns`, `_ensure_broker_columns`) are UNCHANGED — verify by `git diff quirk/db.py` showing edits ONLY in the four target helpers + the new constant line
    - All Wave 0 tests in tests/test_db_migrations.py pass GREEN
    - tests/test_init_db_idempotent.py continues to pass (real col_type values still flow through the new guard)
    - tests/test_qramm_models.py continues to pass (no regression in FK / PRAGMA tests from Plan 70-01)
  </acceptance_criteria>
  <done>_SAFE_COL_TYPE_RE constant + 4 guard insertions in place; Wave 0 tests green; D-07 do-not-touch list respected.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Flip AUDIT-TASKS rows CR-04/05/06/07 to [x] closed with Phase 70 evidence</name>
  <files>.planning/audit-2026-05-08/AUDIT-TASKS.md</files>
  <read_first>
    - .planning/audit-2026-05-08/AUDIT-TASKS.md L180-183 (target rows in their current `[ ] deferred-v4.9` state)
    - .planning/audit-2026-05-08/AUDIT-TASKS.md L84-87 (Phase 69 closure shape — template for the flip pattern)
    - .planning/audit-2026-05-08/AUDIT-TASKS.md L396-432 (per-row detail blocks for CR-04/05/06/07 — append closing paragraph here per Phase 69 precedent)
    - .planning/phases/70-deferred-blockers-api-qramm-model/70-CONTEXT.md §"AUDIT-TASKS Row-Flip Pattern" (target text shape)
    - .planning/phases/70-deferred-blockers-api-qramm-model/70-RESEARCH.md §"AUDIT-TASKS Row-Flip Pattern (from Phase 64.1 / Phase 69 precedent)"
    - .planning/phases/70-deferred-blockers-api-qramm-model/70-PATTERNS.md §".planning/audit-2026-05-08/AUDIT-TASKS.md"
  </read_first>
  <action>
    In .planning/audit-2026-05-08/AUDIT-TASKS.md, edit the four rows at L180-183. For each row, change column 4 from `—` to `Phase 70` and change column 5 from `[ ] deferred-v4.9` to `[x] closed — closed by Phase 70 (BLOCK-NN): <one-line resolution>. <test refs>` using the per-row resolution text below:
      • CR-04 (BLOCK-07): "ForeignKey(qramm_sessions.id, ondelete=SET NULL) on QRAMMProfile.session_id + _ensure_qramm_profiles_fk 12-step rebuild + per-connection PRAGMA foreign_keys=ON via connect event. Tests: tests/test_qramm_models.py::test_qramm_profiles_has_db_level_fk, ::test_connect_event_enables_fk_pragma"
      • CR-05 (BLOCK-07): "delete_session re-ordered to null session.profile_id + flush before profile/answer deletes (D-04). Tests: tests/test_qramm_delete_session_fk.py::test_delete_session_with_profile_clears_fk, ::test_delete_session_with_profile_and_answers"
      • CR-06 (BLOCK-08): "_qs_for_alg narrowed to except (KeyError, TypeError, AttributeError) with logger.warning; unrelated exceptions propagate. Tests: tests/test_cbom_scan_route.py (full file — 6 tests covering swallow + propagation)"
      • CR-07 (BLOCK-08): "_SAFE_COL_TYPE_RE allowlist + ValueError guard in _ensure_v43/_phase41/_phase46/_phase54_qramm_columns. Tests: tests/test_db_migrations.py (full file — regex matrix + 4 poisoned-dict tests)"
    Then locate the per-row detail blocks for CR-04, CR-05, CR-06, CR-07 in the L396-432 region (block headers may use `### api-cli-core/CR-04` or similar — confirm exact heading shape by reading the surrounding context). Append the four-line closing paragraph block to each per Phase 69 precedent:
      > **closed by Phase 70** — <70-NN>
      > - Resolution: <one-line summary>
      > - Evidence: <test file references>
      > - Commit: <leave as `(pending)` — will be replaced when phase commits>
    Match each row to its owning plan: CR-04 → 70-01, CR-05 → 70-01, CR-06 → 70-02, CR-07 → 70-03. Do NOT modify any other rows in AUDIT-TASKS.md.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && grep -cE "^\| api-cli-core/CR-0[4567] \| BLOCKER \|.*\| Phase 70 \| \[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md</automated>
  </verify>
  <acceptance_criteria>
    - The grep command in `<verify>` returns exactly `4`
    - `grep -cE "\[ \] deferred-v4\.9" .planning/audit-2026-05-08/AUDIT-TASKS.md` returns 4 fewer hits than before this task (no other rows flipped)
    - Each of the four flipped rows mentions specific test files (`tests/test_qramm_models.py`, `tests/test_qramm_delete_session_fk.py`, `tests/test_cbom_scan_route.py`, `tests/test_db_migrations.py`) — `grep -E "Phase 70 \| \[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md` shows all four lines contain `tests/test_`
    - Per-row detail blocks for CR-04/05/06/07 each have a `> **closed by Phase 70** —` closing paragraph appended (4 grep hits)
    - No other table row in AUDIT-TASKS.md is modified — `git diff .planning/audit-2026-05-08/AUDIT-TASKS.md` shows edits ONLY at the four target rows and their four detail blocks
  </acceptance_criteria>
  <done>Four audit rows flipped to [x] closed with Phase 70 evidence; per-row closure paragraphs appended; LEDGER-01 progress recorded for Phase 77 closeout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python source (`_*_COLUMN_DDLS` dict values) → SQLite DDL via `text(f"ALTER TABLE ... {col_type}")` | Defense-in-depth: today's values are hard-coded literals, but the f-string interpolation surface remains a future-contributor risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-06 | Tampering (SQL injection via DDL fragment) | `_ensure_*_columns` helpers reading `col_type` from a dict | mitigate | `_SAFE_COL_TYPE_RE` allowlist regex + fail-fast `ValueError` on every iteration (D-06); mirrors the existing `_SAFE_COL_RE` precedent at quirk/db.py:13 |
| T-70-07 | Tampering | Helpers excluded by D-07 (`_ensure_identity/gcp/email/broker_columns`) | accept | These helpers interpolate a literal `"TEXT"` and bind no `col_type` from a variable — no current injection surface. Defense-in-depth here would expand the diff beyond audit-closing scope (deferred per D-07). |
</threat_model>

<verification>
- `_SAFE_COL_TYPE_RE` constant present in quirk/db.py adjacent to `_SAFE_COL_RE`
- 4 guard insertions confirmed via grep (`_SAFE_COL_TYPE_RE.match(col_type)` × ≥4)
- All Wave 0 tests in tests/test_db_migrations.py pass
- tests/test_init_db_idempotent.py + tests/test_qramm_models.py + tests/test_qramm_delete_session_fk.py + tests/test_cbom_scan_route.py all still green (no cross-plan regression)
- AUDIT-TASKS.md rows CR-04/05/06/07 flipped with Phase 70 evidence
- `python -m compileall quirk/` clean across the whole package
- Phase completion mandatory steps (per CLAUDE.md): Obsidian phase note at `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-70-Deferred-BLOCKERs-API-QRAMM-Model.md`; `docs/UAT-SERIES.md` updated and committed; UAT-SERIES.md synced to vault
</verification>

<success_criteria>
1. `_SAFE_COL_TYPE_RE` exists at quirk/db.py module scope; matches all current real DDL values; rejects injection canaries
2. Four `_ensure_*_columns` helpers raise `ValueError` when handed a poisoned `col_type`; continue to function on real values
3. AUDIT-TASKS.md rows api-cli-core/CR-04, CR-05, CR-06, CR-07 are `[x] closed` with Phase 70 evidence referencing the test files added by Plans 70-01, 70-02, and 70-03
4. D-07 do-not-touch list (`_ensure_identity_columns`, `_ensure_gcp_columns`, `_ensure_email_columns`, `_ensure_broker_columns`) is respected — no edits to those four helpers
</success_criteria>

<output>
After completion, create `.planning/phases/70-deferred-blockers-api-qramm-model/70-03-SUMMARY.md` documenting: D-06 implemented across four helpers (with the verbatim regex), D-07 do-not-touch list confirmed, test names added, and the AUDIT-TASKS row-flip closure for all four CR rows. This SUMMARY is the natural phase-closeout artifact — also serves as the source-of-truth for the Phase 70 Obsidian note that follows per CLAUDE.md mandatory completion steps.
</output>
