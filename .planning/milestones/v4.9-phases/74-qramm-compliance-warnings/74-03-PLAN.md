---
phase: 74-qramm-compliance-warnings
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/assessment/migration_advisor.py
  - quirk/qramm/evidence_bridge.py
  - quirk/qramm/compliance_map.py
  - quirk/qramm/model_meta.py
  - quirk/compliance/__init__.py
  - quirk/dashboard/api/routes/qramm.py
  - tests/test_migration_advisor_precision.py
  - tests/test_compliance_coverage_status.py
  - tests/test_qramm_model_stale.py
  - .planning/audit-2026-05-08/AUDIT-TASKS.md
autonomous: true
requirements: [QWARN-03]
must_haves:
  truths:
    - "`quirk/assessment/migration_advisor.py` defines module-level `CANONICAL_ALG_SYNONYMS: Final[dict[str, frozenset[str]]]` (DES, 3DES, RC4, MD5, SHA1 — minimum) and `_matches(canonical: str, text: str) -> bool` using `r\"\\b(...)\\b\"` regex with `re.IGNORECASE`; existing title-driven substring checks (`\"legacy tls\" in title`, `\"ssh\" in title`, etc.) replaced with `_matches`-style word-boundary regex on title tokens. Per RESEARCH C-4 path (a) + user input override: advisor stays title-driven; synonyms map is the D-09 consumer's shared source of truth (D-08, WR-09)"
    - "`quirk/qramm/evidence_bridge.py::_walk_json_for_alg_strings` (lines 165-204) adds a third dict-branch arm: `elif isinstance(value, str) and value: if any(_matches(canon, value) for canon in CANONICAL_ALG_SYNONYMS): out.append(value)` — extending coverage to non-`_ALG_KEYS` string values while preserving the existing keyed + list behavior (D-09, WR-10)"
    - "`quirk/qramm/compliance_map.py` adds NEW parallel dict `SCANNER_COVERAGE_STATUS: Dict[str, Literal['covered','partial','pending','n/a']]` mapping the same keys as `SCANNER_COVERAGE` (CVI → 'covered'; SGRM, DPE, ITR → 'pending'). The existing `SCANNER_COVERAGE` numeric dict and `QRAMM_COMPLIANCE_WEIGHTS` are UNCHANGED per RESEARCH C-5 + user input override targeting `SCANNER_COVERAGE` (NOT `QRAMM_COMPLIANCE_WEIGHTS`) (D-10, WR-11)"
    - "`quirk/dashboard/api/routes/qramm.py` rollup consumer (lines 598-663) reads `SCANNER_COVERAGE_STATUS[dimension]` alongside `SCANNER_COVERAGE[dimension]`; surfaces `coverage_status` on the `ComplianceMapRow` response shape (new optional field); 'pending' and 'n/a' statuses are EXCLUDED from the rollup; 'partial' contributes at half-weight per D-10a default"
    - "`tests/test_compliance_coverage_status.py` exists and asserts: `set(SCANNER_COVERAGE_STATUS.keys()) == set(SCANNER_COVERAGE.keys())` AND every value ∈ `{'covered','partial','pending','n/a'}`"
    - "`quirk/qramm/model_meta.py` adds public `def is_qramm_model_stale(today: datetime.date | None = None) -> bool` using nested `QRAMM_MODEL[\"last_verified\"]` access (NOT a top-level `LAST_VERIFIED` constant per RESEARCH C-6 + user input override); compares `(reference - last_verified).days > STALENESS_THRESHOLD_DAYS` (D-11, WR-12)"
    - "`tests/test_qramm_model_stale.py` exists with parametrized cases: `today=date(2026,12,31)` returns True (stale); `today=date(2026,5,15)` returns False (fresh); default `today=None` works"
    - "`quirk/compliance/__init__.py` line 3 docstring no longer contains `# TODO Phase 50` — the suffix is stripped; the rest of the line and surrounding docstring are preserved verbatim. No deferred-items.md note created (target doc `docs/operators-guide.md:321` exists per RESEARCH C-9 + user input override + Pitfall 8) (D-12, WR-13)"
    - "Audit rows qramm-compliance/WR-09, WR-10, WR-11, WR-12, WR-13 are flipped to `Phase 74 | [x] closed`"
  artifacts:
    - path: "quirk/assessment/migration_advisor.py"
      provides: "CANONICAL_ALG_SYNONYMS dict + _matches word-boundary regex; word-boundary title checks"
      contains: "CANONICAL_ALG_SYNONYMS"
    - path: "quirk/qramm/evidence_bridge.py"
      provides: "_walk_json_for_alg_strings non-keyed string-value scan via _matches"
      contains: "_matches"
    - path: "quirk/qramm/compliance_map.py"
      provides: "SCANNER_COVERAGE_STATUS parallel dict"
      contains: "SCANNER_COVERAGE_STATUS"
    - path: "quirk/dashboard/api/routes/qramm.py"
      provides: "coverage_status surfaced on ComplianceMapRow; pending/n_a excluded from rollup"
      contains: "SCANNER_COVERAGE_STATUS"
    - path: "quirk/qramm/model_meta.py"
      provides: "is_qramm_model_stale(today=None) public helper"
      contains: "def is_qramm_model_stale"
    - path: "quirk/compliance/__init__.py"
      provides: "Stale TODO Phase 50 suffix removed from line 3"
      contains: "Compliance Map Maintenance"
    - path: "tests/test_compliance_coverage_status.py"
      provides: "CI gate asserting every SCANNER_COVERAGE_STATUS entry valid"
      contains: "SCANNER_COVERAGE_STATUS"
    - path: "tests/test_qramm_model_stale.py"
      provides: "is_qramm_model_stale parametrized stale/fresh coverage"
      contains: "is_qramm_model_stale"
    - path: "tests/test_migration_advisor_precision.py"
      provides: "Word-boundary regression: DESede, AES-128, libdes3.so, TripleDES_v2"
      contains: "DESede"
  key_links:
    - from: "quirk/assessment/migration_advisor.py::_matches"
      to: "quirk/qramm/evidence_bridge.py::_walk_json_for_alg_strings"
      via: "from quirk.assessment.migration_advisor import CANONICAL_ALG_SYNONYMS, _matches"
      pattern: "from quirk\\.assessment\\.migration_advisor import"
    - from: "quirk/qramm/compliance_map.py SCANNER_COVERAGE_STATUS"
      to: "quirk/dashboard/api/routes/qramm.py rollup consumer"
      via: "SCANNER_COVERAGE_STATUS[dimension] read alongside SCANNER_COVERAGE[dimension]"
      pattern: "SCANNER_COVERAGE_STATUS"
    - from: "quirk/qramm/model_meta.py::is_qramm_model_stale"
      to: "QRAMM_MODEL[\"last_verified\"]"
      via: "datetime.date.fromisoformat(QRAMM_MODEL[\"last_verified\"])"
      pattern: "QRAMM_MODEL\\[\"last_verified\"\\]"
    - from: "quirk/compliance/__init__.py line 3 docstring"
      to: "docs/operators-guide.md §7 Compliance Map Maintenance"
      via: "see docs/operators-guide.md §Compliance Map Maintenance (TODO Phase 50 suffix removed)"
      pattern: "Compliance Map Maintenance"
---

<objective>
Close QWARN-03 with five surgical fixes across migration advisor, evidence bridge, compliance map, model_meta, and the stale TODO — plus the dashboard rollup consumer wiring for D-10. Mirrors Phase 73's per-WR-row hardening shape with one extra cross-module link (D-08 helper → D-09 consumer).

1. **WR-09 (D-08)** — `quirk/assessment/migration_advisor.py`: per RESEARCH C-4 path (a) + user input override, introduce module-level `CANONICAL_ALG_SYNONYMS: Final[dict[str, frozenset[str]]]` and `_matches(canonical, text) -> bool` helper (`r"\b(...)\b"` regex, `re.IGNORECASE`). The advisor stays title-driven (per C-8 deferral); replace each existing title substring check (`"legacy tls" in title`, `"plaintext http" in title`, `"ssh" in title`, etc. at lines 14-76) with `_matches`-style word-boundary regex on the title token. Synonyms map seeded with `DES`, `3DES`, `RC4`, `MD5`, `SHA1` minimum — extend per the parametrized test cases (`DES-EDE`, `DES-CBC`, `TripleDES`, `DES-EDE3`, `ARCFOUR`, `SHA-1`).

2. **WR-10 (D-09)** — `quirk/qramm/evidence_bridge.py:165-204` `_walk_json_for_alg_strings`: per RESEARCH Pitfall 6, add a third dict-branch arm. After the existing `if key in _ALG_KEYS and isinstance(value, str) and value: out.append(value)` and the recursive container arm, add `elif isinstance(value, str) and value: if any(_matches(canon, value) for canon in CANONICAL_ALG_SYNONYMS): out.append(value)`. Import `from quirk.assessment.migration_advisor import CANONICAL_ALG_SYNONYMS, _matches`. Preserves existing keyed-and-list behavior; only adds the missing non-keyed-string scan.

3. **WR-11 (D-10)** — `quirk/qramm/compliance_map.py`: per RESEARCH C-5 + user input override targeting `SCANNER_COVERAGE` (NOT `QRAMM_COMPLIANCE_WEIGHTS` — which has no zero-weight entries; min weight is 0.4). Add NEW parallel dict:
   ```python
   from typing import Literal, Final
   CoverageStatus = Literal['covered', 'partial', 'pending', 'n/a']
   SCANNER_COVERAGE_STATUS: Final[Dict[str, CoverageStatus]] = {
       "CVI":  "covered",
       "SGRM": "pending",
       "DPE":  "pending",
       "ITR":  "pending",
   }
   ```
   Defaults per user input override: zero-weight SGRM/DPE/ITR → `'pending'`; the rest (CVI) → `'covered'`. Existing `SCANNER_COVERAGE` numeric dict and `QRAMM_COMPLIANCE_WEIGHTS` are UNCHANGED (D-14 + minimal-diff invariant).

   Update consumer at `quirk/dashboard/api/routes/qramm.py:598-663`: import `SCANNER_COVERAGE_STATUS`; surface `coverage_status` on the `ComplianceMapRow` response (new optional field — frontend reads but does not break if absent); exclude `'pending'` and `'n/a'` from the rollup; `'partial'` contributes at half-weight per D-10a default. The HTML/PDF report compliance table and React `ComplianceMapTab` will pick this up automatically via the response shape (no Phase 74 React work — that lands in Phase 76).

   Add CI gate test `tests/test_compliance_coverage_status.py` asserting every entry's status ∈ `{'covered','partial','pending','n/a'}` AND keys match `SCANNER_COVERAGE.keys()`.

4. **WR-12 (D-11)** — `quirk/qramm/model_meta.py`: per RESEARCH C-6 + user input override, add public helper using nested `QRAMM_MODEL["last_verified"]` access (NO module-level `LAST_VERIFIED` constant):
   ```python
   import datetime
   def is_qramm_model_stale(today: datetime.date | None = None) -> bool:
       reference = today or datetime.date.today()
       last_verified = datetime.date.fromisoformat(QRAMM_MODEL["last_verified"])
       age = (reference - last_verified).days
       return age > STALENESS_THRESHOLD_DAYS
   ```
   Phase 75 will wire this into `quirk doctor`. The existing `.github/workflows/python-staleness.yml` CI workflow continues to import existing test invariants — no workflow change in this phase.

5. **WR-13 (D-12)** — `quirk/compliance/__init__.py:3`: per RESEARCH C-9 + user input override + Pitfall 8, target doc `docs/operators-guide.md:321 ## 7. Compliance Map Maintenance` EXISTS; the TODO is dead. Strip the trailing ` # TODO Phase 50` from line 3 of the docstring. Preserve the rest of the line and the surrounding docstring verbatim. NO deferred-items.md note required (target work landed).

Then flip audit rows WR-09 (line 132), WR-10 (133), WR-11 (134), WR-12 (135), WR-13 (136) of `AUDIT-TASKS.md` to `Phase 74 | [x] closed`.

Purpose: Migration advisor is precision-matched (no `'DES' in 'DESede'` family of false positives); evidence walker captures non-keyed algorithm tokens; compliance map distinguishes "not yet covered" from "in scope"; staleness math is centralized for Phase 75 + CI consumption; stale TODO is removed.
Output: Edits in 6 source files, 3 new test modules, 5 audit rows flipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/74-qramm-compliance-warnings/74-CONTEXT.md
@.planning/phases/74-qramm-compliance-warnings/74-RESEARCH.md
@.planning/audit-2026-05-08/AUDIT-TASKS.md

<interfaces>
Existing surfaces (verified):

quirk/assessment/migration_advisor.py (lines 14-76):
- `def recommend_migration_paths(findings)` — iterates findings; for each, lowercases `title` and runs substring checks (`"legacy tls"`, `"plaintext http"`, `"ssh"`, etc.). NO algorithm-string matching in current code (per RESEARCH C-4).
- NEW: `CANONICAL_ALG_SYNONYMS: Final[dict[str, frozenset[str]]]` at module level
- NEW: `_matches(canonical: str, text: str) -> bool` helper

quirk/qramm/evidence_bridge.py:
- `_ALG_KEYS = {"algorithm", "alg", "name", "keyAlgorithm", "key_algorithm"}` at line 32
- `_walk_json_for_alg_strings(obj)` at lines 165-204 — dict-iteration arm at 188-196 (D-09 fix site)

quirk/qramm/compliance_map.py:
- `SCANNER_COVERAGE: Dict[str, float] = {"CVI":1.0, "SGRM":0.0, "DPE":0.0, "ITR":0.0}` at lines 35-40 (D-10 target — keep unchanged)
- `QRAMM_COMPLIANCE_WEIGHTS: Dict[str, Dict[str, float]]` at line 78+ (min weight 0.4 — UNCHANGED per RESEARCH C-5)

quirk/dashboard/api/routes/qramm.py (lines 598-663):
- Rollup consumer reads `SCANNER_COVERAGE[dimension]` and `QRAMM_COMPLIANCE_WEIGHTS[practice_area][framework]` to build `ComplianceMapRow` response

quirk/qramm/model_meta.py:
- `STALENESS_THRESHOLD_DAYS: int = 90` (module constant — keep)
- `QRAMM_MODEL = {..., "last_verified": "2026-05-05", ...}` (D-11 source — nested access)
- NEW: `is_qramm_model_stale(today=None) -> bool` public function

quirk/compliance/__init__.py:
- Line 3 docstring: `Maintenance cadence: see docs/operators-guide.md §"Compliance Map Maintenance".  # TODO Phase 50`
- POST-EDIT line 3: `Maintenance cadence: see docs/operators-guide.md §"Compliance Map Maintenance".`

Target helper module shape (D-08):
```python
# top of quirk/assessment/migration_advisor.py
from __future__ import annotations
import re
from typing import Final

CANONICAL_ALG_SYNONYMS: Final[dict[str, frozenset[str]]] = {
    "DES":  frozenset({"DES", "DES-EDE", "DES-CBC"}),
    "3DES": frozenset({"3DES", "TripleDES", "DES-EDE3"}),
    "RC4":  frozenset({"RC4", "ARCFOUR"}),
    "MD5":  frozenset({"MD5"}),
    "SHA1": frozenset({"SHA1", "SHA-1"}),
}

def _matches(canonical: str, text: str) -> bool:
    variants = CANONICAL_ALG_SYNONYMS.get(canonical, frozenset({canonical}))
    pattern = r"\b(" + "|".join(re.escape(v) for v in variants) + r")\b"
    return bool(re.search(pattern, text, re.IGNORECASE))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing tests for D-08, D-09, D-10, D-11 across three test modules</name>
  <files>tests/test_migration_advisor_precision.py, tests/test_compliance_coverage_status.py, tests/test_qramm_model_stale.py</files>
  <read_first>
    - quirk/assessment/migration_advisor.py (full file)
    - quirk/qramm/evidence_bridge.py lines 32, 165-204
    - quirk/qramm/compliance_map.py (full file)
    - quirk/qramm/model_meta.py (full file)
    - tests/test_qramm_staleness.py (parametrize idiom reference)
    - .planning/phases/74-qramm-compliance-warnings/74-CONTEXT.md (D-08..D-12)
    - .planning/phases/74-qramm-compliance-warnings/74-RESEARCH.md (Pitfalls 5, 6, 7, 9 + C-4..C-6)
  </read_first>
  <behavior>
    **tests/test_migration_advisor_precision.py (D-08, D-09):**
    - `_matches("DES", "DESede")` returns False (canonical false-positive guarded)
    - `_matches("DES", "DES-CBC")` returns True (variant)
    - `_matches("DES", "DES-EDE")` returns True (variant)
    - `_matches("DES", "AES-128")` returns False (unrelated algorithm)
    - `_matches("DES", "libdes3.so")` returns False (substring inside identifier)
    - `_matches("3DES", "TripleDES_v2")` returns True (variant + suffix; regex word boundary at `_`)
       - NOTE: if `\b` treats `_` as non-boundary (Python regex `\b` treats `_` as word char), the test may need to assert False instead — researcher confirms expected semantics during read_first. Default expectation: True if `\b` accepts the `_v2` boundary, False if not. Document chosen expectation in the test.
    - `_matches("SHA1", "SHA-1")` returns True
    - `_matches("MD5", "MD5withRSA")` — researcher picks expected (default: False — `MD5withRSA` is not word-boundary-isolated from `withRSA`)
    - parametrize via `pytest.mark.parametrize` for clarity
    - D-09 integration: `_walk_json_for_alg_strings({"foo": "RC4-MD5", "cipher_list": ["AES-128-GCM", "RC4"]})` returns a list that INCLUDES `"RC4-MD5"` (non-keyed string scanned via `_matches`) AND `"RC4"` (from the list arm)
    - D-09: `_walk_json_for_alg_strings({"foo": "hello world"})` returns `[]` (non-keyed non-matching string skipped)

    **tests/test_compliance_coverage_status.py (D-10):**
    - `set(SCANNER_COVERAGE_STATUS.keys()) == set(SCANNER_COVERAGE.keys())` (key parity invariant)
    - Every value in `SCANNER_COVERAGE_STATUS` ∈ `{'covered','partial','pending','n/a'}`
    - `SCANNER_COVERAGE_STATUS["CVI"] == "covered"` AND `SCANNER_COVERAGE_STATUS["SGRM"] == "pending"` (default migration per user override)

    **tests/test_qramm_model_stale.py (D-11):**
    - `is_qramm_model_stale(today=datetime.date(2026, 12, 31))` returns True (>90 days since 2026-05-05 last_verified)
    - `is_qramm_model_stale(today=datetime.date(2026, 5, 15))` returns False (~10 days, well within threshold)
    - `is_qramm_model_stale()` (default `today=None`) does not raise; returns a bool
    - boundary: `is_qramm_model_stale(today=datetime.date(2026, 8, 3))` — exactly 90 days from 2026-05-05 — returns False (boundary `> STALENESS_THRESHOLD_DAYS`, not `>=`)
    - boundary: `is_qramm_model_stale(today=datetime.date(2026, 8, 4))` — 91 days — returns True
  </behavior>
  <action>
    Create three test files. Each is a single committed RED state — imports the as-yet-nonexistent symbols (`_matches`, `CANONICAL_ALG_SYNONYMS`, `SCANNER_COVERAGE_STATUS`, `is_qramm_model_stale`) and the imports themselves will fail at collection time — that IS the RED state.

    **tests/test_migration_advisor_precision.py:**
    ```python
    import pytest
    from quirk.assessment.migration_advisor import _matches, CANONICAL_ALG_SYNONYMS
    from quirk.qramm.evidence_bridge import _walk_json_for_alg_strings
    # ...parametrized cases as described in behavior
    ```

    **tests/test_compliance_coverage_status.py:**
    ```python
    from quirk.qramm.compliance_map import SCANNER_COVERAGE, SCANNER_COVERAGE_STATUS
    # ...key parity + value validity asserts
    ```

    **tests/test_qramm_model_stale.py:**
    ```python
    import datetime
    import pytest
    from quirk.qramm.model_meta import is_qramm_model_stale
    # ...parametrized stale/fresh + boundary
    ```

    Commit message: `test(74-03): add failing tests for QWARN-03 (D-08..D-11)`. Run `pytest tests/test_migration_advisor_precision.py tests/test_compliance_coverage_status.py tests/test_qramm_model_stale.py -x`; confirm failure (ImportError / NameError on the missing symbols).

    PEP 8.
  </action>
  <verify>
    <automated>pytest tests/test_migration_advisor_precision.py tests/test_compliance_coverage_status.py tests/test_qramm_model_stale.py -x 2>&1 | grep -E "FAILED|ERROR|ImportError" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - All three test files exist
    - `grep -cE "^def test_" tests/test_migration_advisor_precision.py` >= 7
    - `grep -cE "DESede" tests/test_migration_advisor_precision.py` >= 1
    - `grep -cE "libdes3" tests/test_migration_advisor_precision.py` >= 1
    - `grep -cE "TripleDES" tests/test_migration_advisor_precision.py` >= 1
    - `grep -cE "_walk_json_for_alg_strings" tests/test_migration_advisor_precision.py` >= 1
    - `grep -cE "SCANNER_COVERAGE_STATUS" tests/test_compliance_coverage_status.py` >= 1
    - `grep -cE "^def test_" tests/test_compliance_coverage_status.py` >= 1
    - `grep -cE "is_qramm_model_stale" tests/test_qramm_model_stale.py` >= 1
    - `grep -cE "^def test_" tests/test_qramm_model_stale.py` >= 4
    - `pytest tests/test_migration_advisor_precision.py tests/test_compliance_coverage_status.py tests/test_qramm_model_stale.py -x` exits NON-ZERO (RED state)
  </acceptance_criteria>
  <done>
    Three failing test modules committed; pytest exits non-zero on ImportError for the missing symbols.
  </done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement D-08, D-09, D-10, D-11 across migration_advisor, evidence_bridge, compliance_map, qramm route, model_meta</name>
  <files>quirk/assessment/migration_advisor.py, quirk/qramm/evidence_bridge.py, quirk/qramm/compliance_map.py, quirk/dashboard/api/routes/qramm.py, quirk/qramm/model_meta.py</files>
  <read_first>
    - quirk/assessment/migration_advisor.py (full file)
    - quirk/qramm/evidence_bridge.py lines 1-50, 160-210
    - quirk/qramm/compliance_map.py (full file)
    - quirk/dashboard/api/routes/qramm.py lines 590-670
    - quirk/qramm/model_meta.py (full file)
    - quirk/util/weak_crypto.py (Phase 73 shape reference for D-08)
    - tests/test_migration_advisor_precision.py, tests/test_compliance_coverage_status.py, tests/test_qramm_model_stale.py (post-Task 1)
  </read_first>
  <action>
    Apply five surgical edits per `<interfaces>` target shapes. Each is its own logical commit (commit boundaries: D-08, D-09, D-10+consumer, D-11). The executor may collapse D-08+D-09 into one commit if the import chain demands atomic landing.

    **1. (D-08) `quirk/assessment/migration_advisor.py`:**
    - Add `from __future__ import annotations`, `import re`, `from typing import Final` at top (preserve existing imports).
    - Add module-level `CANONICAL_ALG_SYNONYMS` per `<interfaces>` target shape (DES, 3DES, RC4, MD5, SHA1 minimum — extend if test cases reference additional variants).
    - Add `_matches(canonical: str, text: str) -> bool` per target shape.
    - Replace each title substring check in `recommend_migration_paths` (lines 14-76):
      - `if "legacy tls" in title:` → `if re.search(r"\blegacy tls\b", title):`
      - `if "plaintext http" in title:` → analogous
      - `if "ssh" in title:` → `if re.search(r"\bssh\b", title):` (closes the `sshfp` false-positive RESEARCH cites)
      - Apply the same word-boundary transform to every remaining `... in title:` check in the function.
    - Per RESEARCH C-8: do NOT add cipher_suite / cert_pubkey_alg inspection (deferred to Phase 75+).

    Commit: `feat(74-03): add CANONICAL_ALG_SYNONYMS + _matches; word-boundary advisor titles (D-08, WR-09)`.

    **2. (D-09) `quirk/qramm/evidence_bridge.py:165-204`:**
    - Add at top of imports: `from quirk.assessment.migration_advisor import CANONICAL_ALG_SYNONYMS, _matches`.
    - In `_walk_json_for_alg_strings`, modify the dict-iteration arm. After the existing `if key in _ALG_KEYS ...` branch and the recursive container branch, add the non-keyed string scan:
      ```python
      elif isinstance(value, str) and value:
          # D-09 (WR-10): scan non-_ALG_KEYS string values for canonical algorithm tokens
          if any(_matches(canon, value) for canon in CANONICAL_ALG_SYNONYMS):
              out.append(value)
      ```
    - Preserve the existing list-iteration arm verbatim (it already appends bare strings — keep that behavior).

    Commit: `feat(74-03): extend _walk_json_for_alg_strings non-keyed string scan via _matches (D-09, WR-10)`.

    **3. (D-10) `quirk/qramm/compliance_map.py`:**
    - Add `from typing import Literal, Final` to imports (Final if not present).
    - Add `CoverageStatus = Literal['covered', 'partial', 'pending', 'n/a']` near top of module.
    - Below the existing `SCANNER_COVERAGE` block (lines 35-40), add the new parallel dict:
      ```python
      SCANNER_COVERAGE_STATUS: Final[Dict[str, CoverageStatus]] = {
          "CVI":  "covered",
          "SGRM": "pending",
          "DPE":  "pending",
          "ITR":  "pending",
      }
      ```
    - Do NOT modify existing `SCANNER_COVERAGE` numeric dict or `QRAMM_COMPLIANCE_WEIGHTS` (D-14 invariants).

    **3b. (D-10 consumer) `quirk/dashboard/api/routes/qramm.py:598-663`:**
    - Import `SCANNER_COVERAGE_STATUS` from `quirk.qramm.compliance_map`.
    - In the rollup builder, read `SCANNER_COVERAGE_STATUS[dimension]` alongside `SCANNER_COVERAGE[dimension]`.
    - Skip dimensions where status ∈ `{'pending', 'n/a'}` (do NOT contribute to rollup).
    - For status `'partial'`, multiply that dimension's ceiling contribution by 0.5 (D-10a default).
    - For status `'covered'`, use full ceiling unchanged.
    - Surface `coverage_status` on the response shape (add an optional `coverage_status: Optional[str] = None` field to the relevant Pydantic model OR include in the dict response — researcher matches the existing `ComplianceMapRow` style).

    Commit: `feat(74-03): add SCANNER_COVERAGE_STATUS + dashboard rollup consumer (D-10, WR-11)`.

    **4. (D-11) `quirk/qramm/model_meta.py`:**
    - Add `import datetime` if not present (verify via read_first).
    - Add the `is_qramm_model_stale` function per `<interfaces>` target shape, using `QRAMM_MODEL["last_verified"]` nested access (NO new `LAST_VERIFIED` module constant per RESEARCH C-6 + user override).
    - The function MUST handle `today=None` by defaulting to `datetime.date.today()`.
    - Boundary: comparison is `age > STALENESS_THRESHOLD_DAYS` (strict greater-than), matching the existing Phase 49/56 staleness conventions.

    Commit: `feat(74-03): add is_qramm_model_stale public helper (D-11, WR-12)`.

    Per D-14: do NOT modify `questions.py`, the 5-band scale, `tests/test_compliance_freshness.py`, or BLOCKER-closed evidence-bridge paths.

    PEP 8. Minimal diff per edit.
  </action>
  <verify>
    <automated>python -m compileall quirk/assessment/migration_advisor.py quirk/qramm/evidence_bridge.py quirk/qramm/compliance_map.py quirk/dashboard/api/routes/qramm.py quirk/qramm/model_meta.py && pytest tests/test_migration_advisor_precision.py tests/test_compliance_coverage_status.py tests/test_qramm_model_stale.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -m compileall` clean on all 5 modified source files
    - `grep -cE "CANONICAL_ALG_SYNONYMS" quirk/assessment/migration_advisor.py` >= 1
    - `grep -cE "def _matches" quirk/assessment/migration_advisor.py` >= 1
    - `grep -cE "\\\\b" quirk/assessment/migration_advisor.py` >= 3 (word-boundary regex applied to multiple title checks)
    - `grep -cE "from quirk\.assessment\.migration_advisor import .*_matches" quirk/qramm/evidence_bridge.py` >= 1
    - `grep -cE "_matches\(canon" quirk/qramm/evidence_bridge.py` >= 1
    - `grep -cE "SCANNER_COVERAGE_STATUS" quirk/qramm/compliance_map.py` >= 1
    - `grep -cE "Literal\['covered'" quirk/qramm/compliance_map.py` >= 1
    - `grep -cE "SCANNER_COVERAGE_STATUS" quirk/dashboard/api/routes/qramm.py` >= 1
    - `grep -cE "def is_qramm_model_stale" quirk/qramm/model_meta.py` >= 1
    - `grep -cE "QRAMM_MODEL\[\"last_verified\"\]" quirk/qramm/model_meta.py` >= 1
    - `grep -v '^#' quirk/qramm/model_meta.py | grep -cE "^LAST_VERIFIED" ` == 0 (no top-level constant per C-6)
    - `pytest tests/test_migration_advisor_precision.py tests/test_compliance_coverage_status.py tests/test_qramm_model_stale.py -x` exits 0 (GREEN)
    - `pytest tests/test_qramm_staleness.py tests/test_compliance_freshness.py -x` exits 0 (no collateral regression)
  </acceptance_criteria>
  <done>
    All Task 1 tests pass; pre-existing tests unaffected.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove stale TODO Phase 50 comment (D-12, WR-13)</name>
  <files>quirk/compliance/__init__.py</files>
  <read_first>
    - quirk/compliance/__init__.py (first 10 lines — verify line 3 still contains `# TODO Phase 50`)
    - docs/operators-guide.md lines 318-330 (verify §7 Compliance Map Maintenance exists per RESEARCH C-9 + Pitfall 8)
  </read_first>
  <action>
    On line 3 of `quirk/compliance/__init__.py`, strip the trailing ` # TODO Phase 50` suffix. The pre-edit line is:
    ```
    Maintenance cadence: see docs/operators-guide.md §"Compliance Map Maintenance".  # TODO Phase 50
    ```
    The post-edit line is:
    ```
    Maintenance cadence: see docs/operators-guide.md §"Compliance Map Maintenance".
    ```
    The rest of the docstring and surrounding code are unchanged. No deferred-items.md note required per RESEARCH C-9 (target doc verified to exist at `docs/operators-guide.md:321`).

    PEP 8. Single-line edit.

    Commit message: `chore(74-03): remove stale TODO Phase 50 comment (D-12, WR-13)`.
  </action>
  <verify>
    <automated>grep -cE "TODO Phase 50" quirk/compliance/__init__.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "TODO Phase 50" quirk/compliance/__init__.py` == 0
    - `grep -cE "Compliance Map Maintenance" quirk/compliance/__init__.py` >= 1 (rest of line preserved)
    - `grep -cE "TODO Phase 50" docs/` (recursive) returns no production code matches — only verbatim references in `.planning/` audit notes are acceptable
    - `python -m compileall quirk/compliance/__init__.py` exits 0
    - No deferred-items.md created (per RESEARCH C-9)
  </acceptance_criteria>
  <done>
    Stale TODO suffix removed; target doc reference preserved.
  </done>
</task>

<task type="auto">
  <name>Task 4: Flip WR-09, WR-10, WR-11, WR-12, WR-13 audit rows</name>
  <files>.planning/audit-2026-05-08/AUDIT-TASKS.md</files>
  <read_first>
    - .planning/audit-2026-05-08/AUDIT-TASKS.md lines 124-136 (qramm-compliance WR rows)
  </read_first>
  <action>
    Update `.planning/audit-2026-05-08/AUDIT-TASKS.md`, flipping these five rows from `| — | [ ] open |` to `| Phase 74 | [x] closed |`:
    - Line 132 (WR-09): migration_advisor substring matching produces false positives
    - Line 133 (WR-10): _walk_json_for_alg_strings skips non-_ALG_KEYS strings
    - Line 134 (WR-11): compliance_map.py weight 0.0 ambiguous vs not-yet-covered
    - Line 135 (WR-12): model_meta.py lacks is_qramm_model_stale helper
    - Line 136 (WR-13): TODO Phase 50 comment left in production module header

    Per-row evidence summaries land in the Plan SUMMARY. Single docs commit.

    Commit message: `docs(74-03): close qramm-compliance WR-09..WR-13 in audit ledger`.
  </action>
  <verify>
    <automated>grep -cE "qramm-compliance/WR-(09|10|11|12|13).*Phase 74.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "qramm-compliance/WR-(09|10|11|12|13).*Phase 74.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 5
    - `grep -cE "qramm-compliance/WR-.*\[ \] open" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 0 (combined with 74-01 + 74-02 closures, all 13 qramm-compliance WR rows are now closed)
    - `grep -cE "qramm-compliance/WR-.*Phase 74.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 13 (full phase closure verified post-execute)
  </acceptance_criteria>
  <done>
    AUDIT-TASKS.md shows WR-09..WR-13 closed under Phase 74; all 13 qramm-compliance WR rows closed by phase end.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Finding `title` (string) → migration advisor | Untrusted upstream scanner output crosses into recommendation engine; substring false-positives mis-route migration advice |
| Arbitrary JSON blob → `_walk_json_for_alg_strings` | Schema-drift in scanner output may store algorithm tokens under non-`_ALG_KEYS` keys |
| User-supplied `today` → `is_qramm_model_stale` | Test-only injection; production callers pass `today=None` (real clock) |
| `compliance_map.py` schema → dashboard rollup → operator-facing report | Weight=0.0 vs not-yet-covered ambiguity poisons compliance-evidence artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-10 | Tampering (recommendation integrity) | Substring matching `'DES' in 'DESede'` mis-routes migration advice | mitigate | Word-boundary regex via `_matches` + `CANONICAL_ALG_SYNONYMS` (D-08) |
| T-74-11 | Information Disclosure (missing evidence) | Algorithm tokens under non-`_ALG_KEYS` JSON keys silently dropped | mitigate | Non-keyed string scan via `_matches` (D-09); preserves existing keyed + list behavior |
| T-74-12 | Tampering (compliance artifact misrepresentation) | `weight=0.0` reads as "covered" in rollup; operator screenshots false coverage | mitigate | Explicit `coverage_status` field — `'pending'` / `'n/a'` EXCLUDED from rollup; rendered as new column in HTML/PDF (D-10) |
| T-74-13 | Repudiation (silent catalog drift) | QRAMM catalog `last_verified` math duplicated across modules; staleness goes unreported | mitigate | Centralize via `is_qramm_model_stale()` (D-11); Phase 75 wires into `quirk doctor` |
| T-74-14 | Information Disclosure (stale TODO misleads contributors) | `# TODO Phase 50` suggests work pending when target doc landed | mitigate | Delete suffix per D-12; target doc verified |
</threat_model>

<verification>
- `python -m compileall` clean on all modified source files
- `pytest tests/test_migration_advisor_precision.py tests/test_compliance_coverage_status.py tests/test_qramm_model_stale.py -x` passes
- `pytest tests/test_qramm_staleness.py tests/test_compliance_freshness.py -x` passes (no collateral regression)
- AUDIT-TASKS.md WR-09..WR-13 rows show `Phase 74 | [x] closed`; combined with 74-01/74-02, all 13 qramm-compliance WR rows closed
- `grep -rE "# TODO Phase 50" quirk/` returns no matches
- No collateral changes to `questions.py`, the 5-band scale, `tests/test_compliance_freshness.py`, or `quirk/engine/migration_planner.py` per D-14
</verification>

<success_criteria>
- `_matches("DES", "DESede") == False` AND `_matches("DES", "DES-CBC") == True`
- `_walk_json_for_alg_strings` extracts non-keyed algorithm tokens via `_matches` (D-09)
- `SCANNER_COVERAGE_STATUS` exists with valid Literal values; dashboard consumer reads it; `'pending'` and `'n/a'` excluded from rollup; `'partial'` at half-weight
- `is_qramm_model_stale(today=date(2026,12,31)) == True`; `is_qramm_model_stale(today=date(2026,5,15)) == False`
- Stale `# TODO Phase 50` removed from `quirk/compliance/__init__.py`
- 12+ tests pass across the three new test modules
- WR-09, WR-10, WR-11, WR-12, WR-13 closed in AUDIT-TASKS.md
- All 13 qramm-compliance WR rows are closed by phase end (combined with 74-01 + 74-02)
</success_criteria>

<output>
Create `.planning/phases/74-qramm-compliance-warnings/74-03-SUMMARY.md` documenting: D-08 path (a) — `CANONICAL_ALG_SYNONYMS` + `_matches` introduced with title-driven advisor staying as-is per RESEARCH C-4 + C-8; D-09 third dict-arm pattern per Pitfall 6; D-10 `SCANNER_COVERAGE_STATUS` parallel-dict shape per C-5 + user override (targets `SCANNER_COVERAGE` not `QRAMM_COMPLIANCE_WEIGHTS`); D-11 nested `QRAMM_MODEL["last_verified"]` access per C-6 + user override (no module-level `LAST_VERIFIED` constant); D-12 stale TODO removed; no deferred-items.md note required per C-9; five audit rows flipped (WR-09..WR-13); combined phase total: 13/13 qramm-compliance WR rows closed; RED→GREEN commit cadence per task.
</output>
