---
phase: 69
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - quirk/scanner/k8s_connector.py
  - tests/test_k8s_connector.py
  - .planning/audit-2026-05-08/AUDIT-TASKS.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-69-Deferred-BLOCKERs-Scanner-Cloud.md
autonomous: true
requirements: [BLOCK-03]
must_haves:
  truths:
    - "scan_k8s_targets with valid azure_cred AND empty aks_clusters list returns [] without raising (per locked decision D-09 — K8S-03 invariant applies at per-provider level, NOT for empty cluster list)"
    - "scan_k8s_targets with azure_cred=None continues to emit inaccessible finding (Phase 29 path preserved)"
    - "_scan_aks_encryption is NEVER called with credential=None or empty cluster_configs (defensive short-circuit prevents AttributeError per CR-09)"
    - "AUDIT-TASKS.md ledger flips all six BLOCKER rows to closed"
    - "Obsidian phase note exists at the QUIRK Phases path"
    - "python -m compileall quirk/ exits 0 and full pytest suite green"
  artifacts:
    - path: quirk/scanner/k8s_connector.py
      provides: "Explicit empty-aks_clusters short-circuit BEFORE _scan_aks_encryption — returns [] (CR-09 closure per D-09)"
    - path: tests/test_k8s_connector.py
      provides: "test_aks_empty_cluster_list_returns_empty covering CR-09 path"
    - path: /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-69-Deferred-BLOCKERs-Scanner-Cloud.md
      provides: "Obsidian phase note per CLAUDE.md mandatory step"
  key_links:
    - from: quirk/scanner/k8s_connector.py
      to: quirk/scanner/k8s_connector.py:_scan_aks_encryption
      via: "guarded call — only invoked when aks_clusters is non-empty"
      pattern: "if not (aks_clusters or []): continue/return [] for that path"
---

<objective>
Close BLOCK-03 (CR-09 only — CR-03 was closed in Phase 29): when `scan_k8s_targets` has a valid `azure_cred` BUT `aks_clusters` is empty (or None), it currently calls `_scan_aks_encryption(cluster_configs=[])` which can raise AttributeError on the empty path (per CR-09 audit). Per locked decision **D-09**, add an explicit empty-list short-circuit that **returns `[]` without raising** — D-09 explicitly states the K8S-03 "at least one finding" invariant applies at the per-provider level, NOT for an empty cluster list. Do NOT emit an inaccessible finding for the empty-aks_clusters case (that path is reserved for `azure_cred=None`, owned by Phase 29).

THIS PLAN ALSO HOSTS the CLAUDE.md mandatory phase-completion tasks (Obsidian phase note, AUDIT-TASKS.md ledger flip, full compileall + pytest). It runs LAST in Wave B so all earlier plans' fixes are present when the full suite is run.

Purpose: Restores K8S-03 invariant for the empty-cluster-list edge case missed by Phase 29 AND seals Phase 69 per project standards.
Output: Guard in `scan_k8s_targets`; new pytest case; Obsidian note; ledger update; green full suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-deferred-blockers-scanner-cloud/69-CONTEXT.md
@.planning/phases/69-deferred-blockers-scanner-cloud/69-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CR-09 empty-aks_clusters short-circuit (returns [] per D-09)</name>
  <files>quirk/scanner/k8s_connector.py, tests/test_k8s_connector.py</files>
  <read_first>
    - quirk/scanner/k8s_connector.py (focus on scan_k8s_targets ~lines 452-499 for the existing CR-02 None-cred path; lines 327-352 for _emit_inaccessible_finding helper signature; line 527 for the K8S-03 absolute-empty safety net)
    - .planning/phases/69-deferred-blockers-scanner-cloud/69-CONTEXT.md §"D-09" — LOCKED DECISION: empty aks_clusters returns [] without raising; do NOT emit an inaccessible finding for this path
    - .planning/phases/69-deferred-blockers-scanner-cloud/69-RESEARCH.md §"K8s None-credential guard (BLOCK-03)" — note: RESEARCH code example shows emit-inaccessible pattern, but D-09 SUPERSEDES; follow D-09
    - tests/test_k8s_connector.py (focus on test_aks_credential_failure_* — these PASS already; new test must not duplicate them)
  </read_first>
  <behavior>
    - test_aks_empty_cluster_list_returns_empty: build inputs where azure_cred is a non-None mock AND aks_clusters=[] (or None); call scan_k8s_targets; assert no AttributeError raised; assert no CryptoEndpoint with scan_error=="encryption-config-inaccessible" was added by THIS path (the only inaccessible-finding emission must come from the credential=None branch, not the empty-cluster branch); assert _scan_aks_encryption was NOT called (monkeypatch it to raise to confirm short-circuit)
    - DO NOT modify test_aks_credential_failure_emits_inaccessible_per_cluster — that path is Phase 29 work and still passes
  </behavior>
  <action>
    1. In `scan_k8s_targets`, AFTER the existing credential-success branch is entered (where azure_cred is not None), and BEFORE the `_scan_aks_encryption(...)` call, add a short-circuit guard:
       - `if not (aks_clusters or []):` → `pass` / `continue` / fall through such that no call to `_scan_aks_encryption` is made and no finding is appended for this empty-list path. Per D-09, the empty-list case returns [] without raising and without emitting.
       - `else: results.extend(_scan_aks_encryption(...))` (preserve existing call exactly).
    2. DO NOT call `_emit_inaccessible_finding` for the empty-aks_clusters branch — that helper is reserved for the `azure_cred is None` branch (Phase 29 work, do not touch).
    3. DO NOT modify the existing `azure_cred is None` branch — Phase 29 closed CR-03.
    4. DO NOT change `_scan_aks_encryption` body or signature.
    5. In `tests/test_k8s_connector.py`, append `test_aks_empty_cluster_list_returns_empty` per <behavior>. Reuse existing fixture style. Monkeypatch `quirk.scanner.k8s_connector._scan_aks_encryption` to raise AssertionError("should not be called") so the test fails loudly if the guard is missing.
    6. Minimal diff per CLAUDE.md.
  </action>
  <verify>
    <automated>pytest tests/test_k8s_connector.py -x -q exits 0 AND python -m compileall quirk/scanner/k8s_connector.py exits 0</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_k8s_connector.py::test_aks_empty_cluster_list_returns_empty -x -q` exits 0
    - `pytest tests/test_k8s_connector.py -k aks -x -q` exits 0 (no regression in existing AKS tests)
    - `grep -n 'aks_clusters or \[\]' quirk/scanner/k8s_connector.py` returns the new guard line
    - `grep -A2 'aks_clusters or \[\]' quirk/scanner/k8s_connector.py` does NOT contain `_emit_inaccessible_finding` on the empty-list branch
    - `python -m compileall quirk/scanner/k8s_connector.py` exits 0
  </acceptance_criteria>
  <done>scan_k8s_targets short-circuits to [] for empty-aks_clusters with valid credential (no raise, no inaccessible finding); existing CR-03 None-cred path untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-69 mandatory closing — compileall, full pytest, AUDIT-TASKS ledger flip</name>
  <files>.planning/audit-2026-05-08/AUDIT-TASKS.md</files>
  <read_first>
    - CLAUDE.md §"Mandatory Phase Completion Steps"
    - .planning/audit-2026-05-08/AUDIT-TASKS.md (locate the rows for `scanners-protocol/CR-07`, `scanners-protocol/CR-08`, `scanners-cloud/CR-02`, `scanners-cloud/CR-03`, `scanners-cloud/CR-06`, `scanners-cloud/CR-07`, `scanners-cloud/CR-08`, `scanners-cloud/CR-09`, `scanners-cloud/CR-10`)
  </read_first>
  <action>
    1. Run `python -m compileall quirk/` — must exit 0.
    2. Run `pytest tests/ -x -q` — full suite must pass green. If any test fails, STOP and surface the failure; do not flip the ledger.
    3. In `.planning/audit-2026-05-08/AUDIT-TASKS.md`, flip the eight audit rows listed in <read_first> from `[ ] deferred-v4.9` (or `[ ] open`) to `[x] closed — closed by Phase 69 (BLOCK-XX)` mapping:
       - scanners-protocol/CR-07 → BLOCK-01 / BLOCK-06 (sslyze leak)
       - scanners-protocol/CR-08 → BLOCK-01 (fingerprint socket)
       - scanners-cloud/CR-02 → BLOCK-02
       - scanners-cloud/CR-03 → BLOCK-03 (already closed Phase 29; note "Phase 29 + Phase 69 verified")
       - scanners-cloud/CR-06 → BLOCK-05
       - scanners-cloud/CR-07 → BLOCK-06
       - scanners-cloud/CR-08 → BLOCK-06
       - scanners-cloud/CR-09 → BLOCK-03
       - scanners-cloud/CR-10 → BLOCK-04
    4. Minimal diff: only flip the row checkbox + append closure note. Do not restructure the file.
  </action>
  <verify>
    <automated>python -m compileall quirk/ exits 0 AND pytest tests/ -x -q exits 0 AND grep -c 'closed by Phase 69' .planning/audit-2026-05-08/AUDIT-TASKS.md returns at least 8</automated>
  </verify>
  <acceptance_criteria>
    - `python -m compileall quirk/` exits 0
    - `pytest tests/ -x -q` exits 0 (full suite green)
    - `grep -c 'closed by Phase 69' .planning/audit-2026-05-08/AUDIT-TASKS.md` returns >= 8
    - No `[ ] deferred-v4.9` rows remain among the eight audit rows in scope
  </acceptance_criteria>
  <done>Full suite green; ledger reflects all six BLOCKERs closed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Create Obsidian Phase 69 note (vault filesystem write)</name>
  <what-built>
    The first two tasks closed BLOCK-03 (CR-09) and ran the mandatory compileall + full pytest + AUDIT-TASKS ledger flip. Per CLAUDE.md, Phase 69 also requires an Obsidian phase note. The executor will write it directly to the vault filesystem (NOT via obsidian CLI — file is too large for shell expansion).
  </what-built>
  <how-to-verify>
    1. Executor writes `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-69-Deferred-BLOCKERs-Scanner-Cloud.md` with frontmatter (`project: QU.I.R.K.`, `type: phase`, `status: complete`, `source: .planning/phases/69-deferred-blockers-scanner-cloud/`, `updated: 2026-05-14`) + Goal + Requirements Covered (BLOCK-01..06) + Success Criteria + What Was Built (one subsection per BLOCKER, sourced from the per-plan SUMMARY.md files) + a `[[Roadmap]]` wikilink at the end. Use existing notes in `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/` as the template.
    2. Verify the file exists and is readable.
    3. Confirm `docs/UAT-SERIES.md` was already updated (BLOCK-05 plan task) — if not, surface the gap.
    4. Sync UAT-SERIES.md to Obsidian per CLAUDE.md step 3 (write to vault filesystem).
    5. Commit `docs/UAT-SERIES.md` per CLAUDE.md step 4.
  </how-to-verify>
  <resume-signal>Type "approved" once the Obsidian note exists and UAT-SERIES sync + commit are done; or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- `pytest tests/test_k8s_connector.py -x -q` green
- `python -m compileall quirk/` green
- `pytest tests/ -x -q` full suite green
- `grep -c 'closed by Phase 69' .planning/audit-2026-05-08/AUDIT-TASKS.md` >= 8
- Obsidian phase note exists at the QUIRK Phases path
</verification>

<success_criteria>
K8S-03 invariant holds for the empty-cluster-list edge case; full pytest suite green; AUDIT-TASKS.md ledger reflects all six BLOCKERs closed; Obsidian phase note created.
</success_criteria>

<output>
Create `.planning/phases/69-deferred-blockers-scanner-cloud/69-03-SUMMARY.md` covering both BLOCK-03 fix and phase-completion artifacts.
</output>
