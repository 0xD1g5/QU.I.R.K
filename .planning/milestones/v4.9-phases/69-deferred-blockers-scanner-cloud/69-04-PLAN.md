---
phase: 69
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/scanner/azure_connector.py
  - tests/test_azure_blob.py
autonomous: true
requirements: [BLOCK-04]
must_haves:
  truths:
    - "Azure Blob containers with key_source=microsoft.storage emit service_detail=BLOB/platform-managed and dat_scan_json.finding_id=BLOB-PLATFORM"
    - "Azure Blob containers with absent/null key_source emit service_detail=BLOB/unknown and dat_scan_json.finding_id=BLOB-UNKNOWN"
    - "microsoft.keyvault path remains BLOB/cmk / BLOB-CMK with no severity"
  artifacts:
    - path: quirk/scanner/azure_connector.py
      provides: "_scan_blob_encryption three-way branch on key_source"
    - path: tests/test_azure_blob.py
      provides: "Tests asserting BLOB-UNKNOWN finding for absent key_source and finding_id in dat_scan_json"
  key_links:
    - from: quirk/scanner/azure_connector.py
      to: quirk/intelligence/evidence.py
      via: 'service_detail substring "BLOB/platform-managed"'
      pattern: "BLOB/(cmk|platform-managed|unknown)"
---

<objective>
Close BLOCK-04 (CR-10): Azure Blob `_scan_blob_encryption` currently treats absent/null `key_source` identically to `microsoft.storage` (platform-managed). Per D-04, distinguish these as semantically distinct findings using `service_detail` (existing convention) and `dat_scan_json["finding_id"]` (new programmatic key). NO new schema column.

Purpose: Lets downstream CBOM intelligence and scoring distinguish "we know it's platform-managed" from "we couldn't determine the key source".
Output: Three-way branch in `_scan_blob_encryption`; new+modified tests in `tests/test_azure_blob.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-deferred-blockers-scanner-cloud/69-CONTEXT.md
@.planning/phases/69-deferred-blockers-scanner-cloud/69-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Three-way branch on key_source in _scan_blob_encryption</name>
  <files>quirk/scanner/azure_connector.py, tests/test_azure_blob.py</files>
  <read_first>
    - quirk/scanner/azure_connector.py (focus on _scan_blob_encryption, ~lines 120-226; current two-way branch ~line 173)
    - tests/test_azure_blob.py (focus on _make_account fixture lines 9-24 and existing absent-key-source test)
    - quirk/intelligence/evidence.py around line 204 (verify "BLOB/platform-managed" substring match — confirm "BLOB/unknown" does not collide)
    - .planning/phases/69-deferred-blockers-scanner-cloud/69-RESEARCH.md §"Azure Blob three-way branch (BLOCK-04)" code example
  </read_first>
  <behavior>
    - Account with key_source="microsoft.keyvault" → service_detail="BLOB/cmk", dat_scan_json["finding_id"]=="BLOB-CMK", severity is None
    - Account with key_source="microsoft.storage" → service_detail="BLOB/platform-managed", dat_scan_json["finding_id"]=="BLOB-PLATFORM", severity=="MEDIUM"
    - Account with encryption=None or encryption.key_source=None → service_detail="BLOB/unknown", dat_scan_json["finding_id"]=="BLOB-UNKNOWN", severity=="MEDIUM"
    - dat_scan_json also includes "description" and "key_source" (raw value or "absent")
  </behavior>
  <action>
    1. In `_scan_blob_encryption`, replace the current two-way `key_source` branch with a three-way branch per D-04 and the RESEARCH code example:
       - "microsoft.keyvault" → service_detail="BLOB/cmk", finding_id="BLOB-CMK", description="Customer-managed key (Azure Key Vault)", severity=None
       - "microsoft.storage" → service_detail="BLOB/platform-managed", finding_id="BLOB-PLATFORM", description="Platform-managed AES-256 (Microsoft.Storage) — not customer-managed", severity="MEDIUM"
       - else (absent/null/empty/unrecognized) → service_detail="BLOB/unknown", finding_id="BLOB-UNKNOWN", description="Encryption key source unavailable — could not determine key management type", severity="MEDIUM"
    2. Encode `finding_id` and `description` into `dat_scan_json` payload alongside existing fields. Set `dat_scan_json["key_source"]=key_source_raw or "absent"`.
    3. Do NOT pass `finding_id=` as a CryptoEndpoint kwarg — column does not exist (Pitfall 6).
    4. In tests/test_azure_blob.py: rename or add `test_azure_blob_absent_key_source_unknown` asserting service_detail=="BLOB/unknown" and dat_scan_json finding_id=="BLOB-UNKNOWN" (use `_make_account(name, key_source=None)`). Add `test_azure_blob_finding_id_in_dat_scan_json` parameterized over CMK/PLATFORM/UNKNOWN. Preserve any existing platform-managed tests (string is unchanged).
  </action>
  <verify>
    <automated>pytest tests/test_azure_blob.py -x -q exits 0 AND python -m compileall quirk/scanner/azure_connector.py exits 0</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_azure_blob.py::test_azure_blob_absent_key_source_unknown -x -q` exits 0
    - `pytest tests/test_azure_blob.py::test_azure_blob_finding_id_in_dat_scan_json -x -q` exits 0
    - `grep -n 'finding_id=' quirk/scanner/azure_connector.py` returns no CryptoEndpoint kwarg usage
    - `grep -n '"BLOB/unknown"' quirk/scanner/azure_connector.py` returns the new branch
    - `python -m compileall quirk/scanner/azure_connector.py` exits 0
  </acceptance_criteria>
  <done>Azure Blob emits three semantically distinct findings; dat_scan_json carries finding_id; no schema change.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_azure_blob.py -x -q` green
- `pytest tests/test_dar_storage_scoring.py -x -q` green (regression — string "BLOB/platform-managed" unchanged)
- `python -m compileall quirk/scanner/azure_connector.py` green
</verification>

<success_criteria>
Three Azure Blob states emit semantically distinct findings via service_detail + dat_scan_json.finding_id; tests cover all three; no CryptoEndpoint schema change.
</success_criteria>

<output>
Create `.planning/phases/69-deferred-blockers-scanner-cloud/69-04-SUMMARY.md`.
</output>
