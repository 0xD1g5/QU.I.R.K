---
phase: 72-cloud-scanner-warnings
plan: 05
type: execute
wave: 2
depends_on: [72-04]
files_modified:
  - quirk/config.py
  - quirk/engine/profiles.py
  - quirk/engine/risk_engine.py
  - quirk/engine/findings_evaluator.py
  - quirk/scanner/db_connector.py
  - quirk/scanner/vault_connector.py
  - run_scan.py
  - tests/test_risk_engine.py
  - tests/test_email_findings.py
  - tests/test_risk_engine_coverage_gap.py
  - tests/test_risk_engine_cert_defects.py
  - tests/test_broker_run_integration.py
  - tests/test_profiles.py
  - tests/test_findings_evaluator_dedupe.py
  - tests/test_db_connector.py
  - tests/test_vault_connector.py
  - tests/snapshots/
  - .planning/audit-2026-05-08/AUDIT-TASKS.md
autonomous: true
requirements: [CLOUD-05]
must_haves:
  truths:
    - "quirk/engine/risk_engine.py is renamed (git mv) to quirk/engine/findings_evaluator.py; a 2-line shim recreated at risk_engine.py re-exports * from findings_evaluator (no DeprecationWarning at import)"
    - "All 6 internal callers of `quirk.engine.risk_engine` (run_scan.py + 5 test modules) migrate to `quirk.engine.findings_evaluator` in the same commit (D-05a default yes)"
    - "ConnectorsCfg dataclass has _user_set_fields: frozenset sidecar (repr=False, compare=False); quirk/config.py populates it from the raw YAML connectors block at load time"
    - "profiles.py enable_email/enable_broker mutations check `if 'enable_email' not in cfg.connectors._user_set_fields` before mutating; same for enable_broker"
    - "profiles.py standard branch suppresses _set_if_default calls whose value equals the dataclass field default"
    - "_dedupe_findings sort key is (severity_rank, title, host, port); recommendation is dropped; _SEVERITY_RANK module-private dict {CRITICAL:0, HIGH:1, MEDIUM:2, LOW:3, INFO:4}"
    - "_postprocess_findings iterates tuple(findings) and applies adds/removes after the iteration (D-24 defensive snapshot)"
    - "db_connector.py omits the password kwarg entirely when password is None (lets libpq read from .pgpass/PGPASSWORD); explicit empty-string passwords pass through with INFO log"
    - "db_connector.py mysql/rds exception handlers route through quirk.util.safe_exc.safe_str (postgres path already covered in Phase 59)"
    - "vault_connector.py raises ValueError when token=None — no implicit os.environ['VAULT_TOKEN'] fallback in the connector; run_scan.py reads the env explicitly and passes through"
    - "vault_connector._scan_pki_mounts uses cryptography.x509.load_pem_x509_certificates (plural) instead of naive split"
    - "Golden-file snapshots for _dedupe_findings sort change are regenerated in a separate commit per CONTEXT D-04 test strategy"
    - "Audit rows scanners-cloud/WR-07, WR-08, WR-09, WR-10, WR-11, WR-12, WR-18, WR-23, WR-24 are flipped to [x] closed under Phase 72"
  artifacts:
    - path: "quirk/engine/findings_evaluator.py"
      provides: "Renamed module; canonical home for post-scan finding evaluation"
      contains: "_SEVERITY_RANK"
    - path: "quirk/engine/risk_engine.py"
      provides: "2-line deprecation shim re-exporting from findings_evaluator"
      contains: "from quirk.engine.findings_evaluator import"
    - path: "quirk/config.py"
      provides: "ConnectorsCfg._user_set_fields sidecar populated at config load"
      contains: "_user_set_fields"
    - path: "quirk/engine/profiles.py"
      provides: "user-explicit enable_email/enable_broker guard + standard re-apply suppression"
      contains: "_user_set_fields"
    - path: "tests/test_findings_evaluator_dedupe.py"
      provides: "Golden-file test for D-04 sort stability across recommendation-only diffs"
      contains: "def test_"
  key_links:
    - from: "quirk/engine/risk_engine.py shim"
      to: "quirk/engine/findings_evaluator.py"
      via: "from quirk.engine.findings_evaluator import *"
      pattern: "from quirk\\.engine\\.findings_evaluator import"
    - from: "quirk/config.py conn_raw build site"
      to: "ConnectorsCfg._user_set_fields"
      via: "frozenset(conn_raw.keys())"
      pattern: "frozenset\\(conn_raw\\.keys"
    - from: "quirk/engine/profiles.py enable_email mutation"
      to: "user-explicit guard"
      via: "'enable_email' not in cfg.connectors._user_set_fields"
      pattern: "not in cfg\\.connectors\\._user_set_fields"
    - from: "quirk/engine/findings_evaluator.py::_dedupe_findings"
      to: "_SEVERITY_RANK"
      via: "(severity_rank, title, host, port) sort key"
      pattern: "_SEVERITY_RANK"
    - from: "quirk/scanner/vault_connector.py::scan_vault_targets"
      to: "ValueError"
      via: "raise ValueError when token is None"
      pattern: "vault_connector requires explicit token"
---

<objective>
Close CLOUD-05 by sweeping the 9 remaining miscellaneous cloud WARNINGs (the largest cluster, but each fix is surgically localized):

1. **WR-07 (D-20)** — `quirk/scanner/db_connector.py:88-95, 213-220`: psycopg2 connect — when `password is None`, omit the `password` kwarg entirely (libpq reads `.pgpass`/`PGPASSWORD`). When `password == ""` explicitly, pass through with INFO log.
2. **WR-08 (D-21)** — `quirk/scanner/db_connector.py:158-167` (and any mysql/rds analog per RESEARCH Pitfall 3): apply `quirk.util.safe_exc.safe_str(exc)` before storing exception text in `scan_error`. Postgres path is already covered in Phase 59 — focus on mysql/rds branches (per RESEARCH C-2 narrowed scope).
3. **WR-09 (D-22)** — `quirk/scanner/vault_connector.py:396-430`: when caller passes `token=None`, raise `ValueError("vault_connector requires explicit token; pass os.environ.get('VAULT_TOKEN') if env fallback intended")`. Migrate the caller (`run_scan.py`) to read the env var explicitly and pass it through.
4. **WR-10 (D-05 + D-05a)** — `git mv quirk/engine/risk_engine.py quirk/engine/findings_evaluator.py`; recreate `risk_engine.py` as a 2-line shim: `"""Deprecated alias for quirk.engine.findings_evaluator. Removed in v5.0."""` + `from quirk.engine.findings_evaluator import *  # noqa: F401, F403`. Add module docstring to `findings_evaluator.py` clarifying "NOT the score engine". Per D-05a default yes: migrate all 6 internal callers (`run_scan.py:36`, 5 test files) in the SAME commit.
5. **WR-11 (D-02)** — `quirk/config.py:~377`: track raw-YAML connector keys via `cfg.connectors._user_set_fields = frozenset(conn_raw.keys())`. Add `_user_set_fields: frozenset = field(default_factory=frozenset, repr=False, compare=False)` to `ConnectorsCfg` dataclass. In `quirk/engine/profiles.py:110-117, 134-141`, guard each `enable_email`/`enable_broker` mutation: `if 'enable_email' not in cfg.connectors._user_set_fields: if not cfg.connectors.enable_email: cfg.connectors.enable_email = True`. Use sidecar name `_user_set_fields` (D-02a default).
6. **WR-12 (D-03)** — `quirk/engine/profiles.py:121-122` (and any similar sites): audit each `_set_if_default(...)` call in the `standard` branch; suppress (delete) calls where the supplied value equals the field's dataclass default. Researcher inventories the full set — do NOT blanket-strip.
7. **WR-18 (D-23)** — `quirk/scanner/vault_connector.py:277-282`: replace `.split("-----BEGIN CERTIFICATE-----")` heuristic with `cryptography.x509.load_pem_x509_certificates(pem_bytes)` (plural; available in `cryptography>=36`; project pins `>=44.0` per pyproject.toml:13).
8. **WR-23 (D-24)** — `quirk/engine/findings_evaluator.py::_postprocess_findings` (post-rename; was risk_engine.py:335-371): iterate `tuple(findings)` for the read pass; accumulate adds/removes in separate lists; apply mutations after iteration. Per RESEARCH C-2 the current risk is hypothetical but the defensive snapshot is the locked decision.
9. **WR-24 (D-04 + C-4)** — `quirk/engine/findings_evaluator.py::_dedupe_findings` (post-rename; was risk_engine.py:312-319): replace the current 4-tuple sort key with `(severity_rank, title, host, port)`. Per RESEARCH C-4 / Pitfall 6: the locked `finding_id` field has no current analog; map to `title` (the existing identity-defining column). Add module-private `_SEVERITY_RANK = {"CRITICAL": 0, "HIGH": 1, "MEDIUM": 2, "LOW": 3, "INFO": 4}` (D-04a default private).

Then flip 9 audit ledger rows and regenerate golden-file snapshots in a separate snapshot-only commit per CONTEXT test strategy.

**Wave note:** PLAN 05 is wave 2 (depends on PLAN 04) because D-19 in PLAN 04 references `cfg.connectors._user_set_fields` (added here in D-02). PLAN 04 defensively pops the sidecar by key name so the dependency is loose, but the explicit wave ordering documents the relationship and avoids interleaved-commit confusion if executed in parallel.

Purpose: Resolve the largest cluster of cloud WARNINGs — credential safety, structural rename, profile-mutation guards, library-grade PEM parsing, mutation-safe iteration, stable dedup ordering.
Output: 1 renamed file + shim, 4 patched files, 8+ test files (5 migrated, 3+ new/extended), snapshot regen, 9 audit rows flipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/72-cloud-scanner-warnings/72-CONTEXT.md
@.planning/phases/72-cloud-scanner-warnings/72-RESEARCH.md
@.planning/audit-2026-05-08/AUDIT-TASKS.md

<interfaces>
risk_engine.py shim shape (D-05):
```
"""Deprecated alias for quirk.engine.findings_evaluator. Removed in v5.0."""
from quirk.engine.findings_evaluator import *  # noqa: F401, F403
```

findings_evaluator.py module docstring (D-05):
```
"""Post-scan findings evaluator — NOT the score engine.

Quantum-readiness scoring lives in `quirk/intelligence/`. This module
handles per-finding evaluation and deduplication only.
"""
```

ConnectorsCfg sidecar (D-02):
```
@dataclass
class ConnectorsCfg:
    # ... existing fields ...
    _user_set_fields: frozenset = field(default_factory=frozenset, repr=False, compare=False)
```

profiles.py mutation guard (D-02):
```
if 'enable_email' not in cfg.connectors._user_set_fields:
    if not cfg.connectors.enable_email:
        cfg.connectors.enable_email = True
# Same shape for enable_broker
```

_dedupe_findings new sort (D-04 + C-4 title-for-finding_id):
```
_SEVERITY_RANK = {"CRITICAL": 0, "HIGH": 1, "MEDIUM": 2, "LOW": 3, "INFO": 4}

return [
    deduped[k]
    for k in sorted(
        deduped.keys(),
        key=lambda x: (
            _SEVERITY_RANK.get(str(deduped[x].get("severity", "INFO")).upper(), 4),
            x[2],  # title — per C-4 / Pitfall 6 mapping for D-04's "finding_id"
            x[0],  # host
            x[1],  # port
        ),
    )
]
```

D-09-style as_completed pattern is already in PLAN 01 (S3); not needed here.

Vault PKI plural parser (D-23):
```
from cryptography import x509
certs = x509.load_pem_x509_certificates(chain_pem.encode("utf-8"))
for cert in certs:
    single_pem = cert.public_bytes(serialization.Encoding.PEM).decode("ascii")
    # ... existing _classify_pki_cert + emit ...
```

run_scan.py:36 callers to migrate (from research):
- run_scan.py:36
- tests/test_risk_engine.py:13
- tests/test_email_findings.py: lines 58, 75, 89, 105, 121, 137
- tests/test_risk_engine_coverage_gap.py:11
- tests/test_risk_engine_cert_defects.py:18
- tests/test_broker_run_integration.py:22
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv risk_engine.py → findings_evaluator.py + shim + migrate 6 internal callers (WR-10 / D-05 / D-05a)</name>
  <files>quirk/engine/risk_engine.py, quirk/engine/findings_evaluator.py, run_scan.py, tests/test_risk_engine.py, tests/test_email_findings.py, tests/test_risk_engine_coverage_gap.py, tests/test_risk_engine_cert_defects.py, tests/test_broker_run_integration.py</files>
  <read_first>
    - quirk/engine/risk_engine.py (entire file)
    - run_scan.py line 36 (existing import)
    - The 5 test files listed above — confirm exact import shapes
    - .planning/phases/72-cloud-scanner-warnings/72-CONTEXT.md (D-05, D-05a)
  </read_first>
  <action>
    1. **Rename via git:**
       ```
       git mv quirk/engine/risk_engine.py quirk/engine/findings_evaluator.py
       ```

    2. **Add module docstring to findings_evaluator.py** (top of file, before existing imports):
       ```
       """Post-scan findings evaluator — NOT the score engine.

       Quantum-readiness scoring lives in `quirk/intelligence/`. This module
       handles per-finding evaluation and deduplication only.
       """
       ```

    3. **Recreate quirk/engine/risk_engine.py as a 2-line shim:**
       ```
       """Deprecated alias for quirk.engine.findings_evaluator. Removed in v5.0."""
       from quirk.engine.findings_evaluator import *  # noqa: F401, F403
       ```
       No `DeprecationWarning` at import time per D-05.

    4. **Migrate all 6 internal callers** to import from `quirk.engine.findings_evaluator` instead of `quirk.engine.risk_engine`. Per D-05a default yes (atomic single commit):
       - `run_scan.py:36` — `from quirk.engine.risk_engine import evaluate_endpoints, evaluate_email_endpoints, evaluate_broker_endpoints` → `from quirk.engine.findings_evaluator import evaluate_endpoints, evaluate_email_endpoints, evaluate_broker_endpoints`
       - `tests/test_risk_engine.py:13` — same migration (preserve all imported names)
       - `tests/test_email_findings.py` lines 58, 75, 89, 105, 121, 137 — 6 sites for `evaluate_email_endpoints`; migrate each
       - `tests/test_risk_engine_coverage_gap.py:11`
       - `tests/test_risk_engine_cert_defects.py:18`
       - `tests/test_broker_run_integration.py:22`

       Search-and-replace `from quirk.engine.risk_engine import` → `from quirk.engine.findings_evaluator import` across the project. Verify no other importers exist:
       ```
       grep -rn "from quirk.engine.risk_engine\|import quirk.engine.risk_engine" quirk/ tests/ scripts/ run_scan.py 2>/dev/null
       ```
       If hits remain after the targeted edits, migrate them too in this same commit.

    Per D-25: do NOT change any logic inside the renamed file. The rename is purely structural.

    PEP 8. Single atomic commit suggested: `refactor(72-rename): risk_engine.py → findings_evaluator.py + migrate 6 callers (WR-10)`.
  </action>
  <verify>
    <automated>python -m compileall quirk/engine/findings_evaluator.py quirk/engine/risk_engine.py run_scan.py &amp;&amp; python -c "from quirk.engine.findings_evaluator import evaluate_endpoints; from quirk.engine.risk_engine import evaluate_endpoints as shim_e; assert evaluate_endpoints is shim_e"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f quirk/engine/findings_evaluator.py` exits 0
    - `test -f quirk/engine/risk_engine.py` exits 0 (shim still exists)
    - `wc -l quirk/engine/risk_engine.py` returns ≤ 5 (2-line shim + docstring + maybe blank lines)
    - `grep -nE "from quirk\\.engine\\.findings_evaluator import" quirk/engine/risk_engine.py` returns exactly one hit
    - `grep -rn "from quirk.engine.risk_engine\\|import quirk.engine.risk_engine" quirk/ tests/ run_scan.py 2>/dev/null | grep -v "engine/risk_engine.py:"` returns zero hits (no caller still imports the old name; the shim itself doesn't count)
    - `python -c "from quirk.engine.findings_evaluator import evaluate_endpoints, evaluate_email_endpoints, evaluate_broker_endpoints, _dedupe_findings, _postprocess_findings"` exits 0
    - All 6 migrated files compile: `python -m compileall run_scan.py tests/test_risk_engine.py tests/test_email_findings.py tests/test_risk_engine_coverage_gap.py tests/test_risk_engine_cert_defects.py tests/test_broker_run_integration.py` exits 0
  </acceptance_criteria>
  <done>
    Rename complete; shim in place; 6 internal callers migrated in same commit; both names import the same symbols.
  </done>
</task>

<task type="auto">
  <name>Task 2: findings_evaluator.py — _SEVERITY_RANK + new _dedupe_findings sort + tuple-snapshot _postprocess_findings (WR-23 / WR-24 / D-04 / D-24)</name>
  <files>quirk/engine/findings_evaluator.py</files>
  <read_first>
    - quirk/engine/findings_evaluator.py (post-Task 1 rename — focus lines 288-371)
    - .planning/phases/72-cloud-scanner-warnings/72-CONTEXT.md (D-04, D-04a, D-24)
    - .planning/phases/72-cloud-scanner-warnings/72-RESEARCH.md (Pitfall 6, C-4, C-2)
  </read_first>
  <action>
    **Part A — D-04 (WR-24): _SEVERITY_RANK + new sort key.**

    1. At module scope (near other module-level constants), add the private dict:
       ```
       _SEVERITY_RANK = {"CRITICAL": 0, "HIGH": 1, "MEDIUM": 2, "LOW": 3, "INFO": 4}
       ```
       Per D-04a default private; do NOT export.

    2. In `_dedupe_findings` (lines 312-319), replace the existing 4-tuple sort key. Per RESEARCH C-4 / Pitfall 6, the locked `finding_id` field has no current analog — map it to the existing `title` column (the identity-defining field in the current dedup tuple `(host, port, title, recommendation)`).

       Replace the existing sort with:
       ```
       return [
           deduped[k]
           for k in sorted(
               deduped.keys(),
               key=lambda x: (
                   _SEVERITY_RANK.get(str(deduped[x].get("severity", "INFO")).upper(), 4),
                   x[2],  # title — per Phase 72 C-4 mapping for D-04's "finding_id"
                   x[0],  # host
                   x[1],  # port
               ),
           )
       ]
       ```
       Researcher: confirm `x[2]` is the title position in the current `deduped` dict's key tuple. The existing dedup-tuple structure is `(host, port, title, recommendation)`; the new sort drops `recommendation` entirely and reorders by severity-then-title-then-host-then-port.

       Add an inline comment:
       ```
       # Phase 72 D-04 / WR-24: stable sort key — severity_rank first so high-severity findings cluster.
       # 'recommendation' dropped from key: remediation-text edits no longer reshuffle golden output.
       # C-4 adjudication: "finding_id" in D-04 maps to `title` (no finding_id column exists).
       ```

    **Part B — D-24 (WR-23): _postprocess_findings mutation-safe iteration.**

    In `_postprocess_findings` (lines 335-371), apply the defensive snapshot pattern. Per RESEARCH C-2 the current code mutates fields in-place (not list extends/removes), but D-24 mandates the snapshot regardless.

    Replace the existing iteration shape:
    ```
    for f in findings:
        # ... existing read-and-mutate-fields logic ...
    ```
    With:
    ```
    # Phase 72 D-24 / WR-23: defensive snapshot iteration.
    # Current body mutates fields of existing finding dicts in-place; the snapshot
    # protects against future maintainers adding append/remove without re-checking iteration safety.
    adds, removes = [], []
    for f in tuple(findings):
        # ... existing read-only logic that decides to mutate f ...
        # If a finding should be appended, append to `adds`; if removed, append to `removes`.
    for f in removes:
        findings.remove(f)
    findings.extend(adds)
    ```
    If the existing body only mutates fields in-place (no list adds/removes), the `adds`/`removes` lists will be empty at end-of-loop and the post-iteration mutation steps are no-ops — that's correct and matches RESEARCH C-2.

    Researcher: read the existing `_postprocess_findings` body carefully. Preserve all in-place field mutations verbatim. Only the iteration variable (`for f in findings` → `for f in tuple(findings)`) and the optional `adds`/`removes` accumulators are added.

    Per D-25: do NOT change other sort logic in `findings_evaluator.py`. Do NOT change algorithm/severity-classification rules.

    PEP 8. Minimal diff.
  </action>
  <verify>
    <automated>python -m compileall quirk/engine/findings_evaluator.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^_SEVERITY_RANK = " quirk/engine/findings_evaluator.py` returns exactly one hit at module scope
    - `grep -nE "_SEVERITY_RANK\\.get" quirk/engine/findings_evaluator.py` returns at least one hit inside `_dedupe_findings`
    - `grep -nE "for .* in tuple\\(findings\\)" quirk/engine/findings_evaluator.py` returns at least one hit inside `_postprocess_findings`
    - `grep -nE "recommendation" quirk/engine/findings_evaluator.py | grep -v "#" | grep -i "sort\\|key=lambda"` returns zero hits (recommendation no longer in sort key; commented references and unrelated reads are allowed)
    - `python -m compileall quirk/engine/findings_evaluator.py` exits 0
  </acceptance_criteria>
  <done>
    findings_evaluator.py has _SEVERITY_RANK, new sort key drops recommendation, _postprocess_findings iterates tuple snapshot.
  </done>
</task>

<task type="auto">
  <name>Task 3: ConnectorsCfg._user_set_fields + profiles.py mutation guard + standard re-apply suppression (WR-11 / WR-12 / D-02 / D-03)</name>
  <files>quirk/config.py, quirk/engine/profiles.py</files>
  <read_first>
    - quirk/config.py — ConnectorsCfg dataclass definition (research cites lines 183-256) + conn_raw construction (~line 377-380)
    - quirk/engine/profiles.py (entire file — 153 lines; focus 110-117 deep branch, 134-141 standard branch, 121-122 standard re-apply site)
    - .planning/phases/72-cloud-scanner-warnings/72-CONTEXT.md (D-02, D-02a, D-03)
    - .planning/phases/72-cloud-scanner-warnings/72-RESEARCH.md (D-02 example)
  </read_first>
  <action>
    **Part A — D-02 (WR-11): ConnectorsCfg sidecar + config load population.**

    1. In `quirk/config.py`, locate the `ConnectorsCfg` dataclass definition. Add the sidecar field at the end of the field list (after all real fields):
       ```
       from dataclasses import dataclass, field  # ensure `field` is imported

       @dataclass
       class ConnectorsCfg:
           # ... all existing fields unchanged ...
           # Phase 72 D-02: tracks which keys appeared in the raw YAML connectors block.
           # Used by quirk.engine.profiles to suppress mutation of user-explicit values.
           _user_set_fields: frozenset = field(default_factory=frozenset, repr=False, compare=False)
       ```
       Per D-02a default: name is `_user_set_fields`. Researcher: grep first for any pre-existing `_user_set` / `_explicit` precedent — if present, match the existing naming; otherwise use `_user_set_fields`.

    2. In `quirk/config.py` after `conn_raw` is built and `ConnectorsCfg` is constructed (~line 377-380 per research):
       ```
       connectors._user_set_fields = frozenset(conn_raw.keys())
       ```
       Use the actual variable names in scope (the `connectors` object and the `conn_raw` dict).

    **Part B — D-02 (WR-11): profiles.py enable_email / enable_broker guard.**

    In `quirk/engine/profiles.py`, locate the two mutation sites:
    - Lines 110-117 (`deep` branch) — `enable_email` and `enable_broker` mutations
    - Lines 134-141 (`standard` branch) — same shape

    At each site, wrap the mutation with the user-explicit guard:
    ```
    if 'enable_email' not in cfg.connectors._user_set_fields:
        if not cfg.connectors.enable_email:
            cfg.connectors.enable_email = True
    if 'enable_broker' not in cfg.connectors._user_set_fields:
        if not cfg.connectors.enable_broker:
            cfg.connectors.enable_broker = True
    ```
    A user who wrote `enable_email: false` explicitly in YAML is now respected — the profile won't auto-flip it back to True.

    **Part C — D-03 (WR-12): standard branch re-apply suppression.**

    In `quirk/engine/profiles.py`, the `standard` branch (lines ~118-153). At line 121-122 (per audit row), there are `_set_if_default(name, value, default=X)` calls where the supplied `value` equals the field's dataclass default — those are no-ops and obscure intent.

    Researcher: inventory each `_set_if_default(...)` call in the `standard` branch. For each call, compare `value` to the field's dataclass default:
    ```
    # use dataclasses.fields(ScanCfg) (or whatever cfg subclass) to read the default
    field_default = next(f for f in dataclasses.fields(ScanCfg) if f.name == name).default
    if value == field_default:
        # this _set_if_default call is a no-op — delete it
    ```
    Do NOT blanket-strip — only suppress where `value == field_default`. If `value` is a different sentinel (e.g., a tuned-for-standard value that differs from the dataclass default), keep the call.

    Add an inline comment at each deletion site:
    ```
    # Phase 72 D-03 / WR-12: removed no-op _set_if_default(<name>, <value>) — value matched dataclass default.
    ```

    Per D-25: do NOT change the deep branch's _set_if_default calls. Do NOT introduce new helpers. PEP 8. Minimal diff.
  </action>
  <verify>
    <automated>python -m compileall quirk/config.py quirk/engine/profiles.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "_user_set_fields:.*frozenset" quirk/config.py` returns at least one hit (dataclass field definition)
    - `grep -nE "frozenset\\(conn_raw\\.keys" quirk/config.py` returns at least one hit (population site)
    - `grep -nE "'enable_email' not in cfg\\.connectors\\._user_set_fields" quirk/engine/profiles.py` returns at least one hit (deep branch)
    - `grep -nE "'enable_broker' not in cfg\\.connectors\\._user_set_fields" quirk/engine/profiles.py` returns at least one hit
    - `grep -cE "'enable_email' not in cfg\\.connectors\\._user_set_fields" quirk/engine/profiles.py` >= 2 if both deep + standard branches have the guard (D-02 says both)
    - `grep -nE "D-03|WR-12" quirk/engine/profiles.py` returns at least one hit (comment marking suppressed call site)
    - `python -m compileall quirk/config.py quirk/engine/profiles.py` exits 0
  </acceptance_criteria>
  <done>
    ConnectorsCfg has sidecar; config.py populates it; profiles.py guards email/broker mutations; standard re-apply no-ops removed.
  </done>
</task>

<task type="auto">
  <name>Task 4: db_connector.py password None handling + mysql/rds safe_str (WR-07 / WR-08 / D-20 / D-21)</name>
  <files>quirk/scanner/db_connector.py</files>
  <read_first>
    - quirk/scanner/db_connector.py lines 80-100 (postgres connect site for D-20)
    - quirk/scanner/db_connector.py lines 150-180 (postgres exception — already covered in Phase 59, just verify safe_str present)
    - quirk/scanner/db_connector.py lines 205-225 (mysql/rds connect site + exception handler)
    - quirk/util/safe_exc.py (confirm safe_str signature)
    - .planning/phases/72-cloud-scanner-warnings/72-CONTEXT.md (D-20, D-21)
    - .planning/phases/72-cloud-scanner-warnings/72-RESEARCH.md (C-2 narrowed scope, Pitfall 3)
  </read_first>
  <action>
    **Part A — D-20 (WR-07): password None handling.**

    At both connect sites (lines ~88-95 postgres, ~213-220 mysql/rds):

    Currently:
    ```
    psycopg2.connect(host=host, port=port, user=user, password=password or '', ...)
    ```

    Change to:
    ```
    kwargs = dict(host=host, port=port, user=user, ...)
    if password is None:
        # Phase 72 D-20 / WR-07: omit password kwarg entirely; libpq reads .pgpass / PGPASSWORD
        pass
    elif password == "":
        # Phase 72 D-20: explicit empty-password attempt — pass through with INFO log
        if logger:
            logger.info("DB connect with explicit empty password (user=%s)", user)
        kwargs["password"] = ""
    else:
        kwargs["password"] = password
    psycopg2.connect(**kwargs)
    ```
    Apply analogously to the mysql/rds branch (same `if/elif/else` shape, using the appropriate connector library's `connect()`).

    **Part B — D-21 (WR-08): safe_str on mysql/rds exception handlers (per RESEARCH C-2 narrowed scope).**

    Per RESEARCH Pitfall 3 + C-2: the postgres branch (line ~166) already routes through `safe_str(exc)` from Phase 59 LEAK-01. Focus the D-21 edit on the mysql/rds branches where `safe_str` may not yet be applied.

    Locate every `scan_error=f"... {exc}"` (or `scan_error = str(exc)`) site in the mysql/rds branches of `db_connector.py`. Wrap each exception text with `safe_str`:
    ```
    from quirk.util.safe_exc import safe_str  # ensure module-level import

    except (pymysql.OperationalError, ...) as exc:
        scan_error = safe_str(exc)
        # ... rest unchanged ...
    ```
    Do NOT touch the postgres exception path — Phase 59 already covers it.

    Researcher: grep `safe_str` and `scan_error` across db_connector.py to inventory exact sites. There may be 1-3 mysql/rds exception handlers requiring the wrap. The AST-gate test in `tests/test_safe_exc_gate.py` will validate exhaustive coverage post-edit.

    Per D-25: do NOT touch postgres path. Do NOT introduce new exception types. PEP 8. Minimal diff.
  </action>
  <verify>
    <automated>python -m compileall quirk/scanner/db_connector.py &amp;&amp; pytest tests/test_safe_exc_gate.py -x 2>/dev/null || python -m compileall quirk/scanner/db_connector.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "password=password or ''" quirk/scanner/db_connector.py` returns zero hits (the silent empty-string default is removed)
    - `grep -nE "password is None" quirk/scanner/db_connector.py` returns at least two hits (postgres + mysql branches)
    - `grep -nE "safe_str\\(exc\\)" quirk/scanner/db_connector.py` returns at least two hits total (postgres pre-existing + at least one new mysql/rds)
    - `grep -nE "from quirk\\.util\\.safe_exc import" quirk/scanner/db_connector.py` returns at least one hit
    - `python -m compileall quirk/scanner/db_connector.py` exits 0
    - If `tests/test_safe_exc_gate.py` exists, it passes (AST gate proves all exception paths route through safe_str)
  </acceptance_criteria>
  <done>
    db_connector.py omits password when None, logs explicit empty-string, mysql/rds exception text sanitized via safe_str.
  </done>
</task>

<task type="auto">
  <name>Task 5: vault_connector.py — explicit token + run_scan.py env migration + PEM plural parser (WR-09 / WR-18 / D-22 / D-23)</name>
  <files>quirk/scanner/vault_connector.py, run_scan.py</files>
  <read_first>
    - quirk/scanner/vault_connector.py lines 270-290 (D-23 PEM split site at line 277-282)
    - quirk/scanner/vault_connector.py lines 390-430 (D-22 token handling site at line 396)
    - run_scan.py (entire file — find `scan_vault_targets` callers; how vault token is currently sourced)
    - quirk/util/safe_exc.py (for reference — already imported by db_connector)
    - .planning/phases/72-cloud-scanner-warnings/72-CONTEXT.md (D-22, D-23)
    - .planning/phases/72-cloud-scanner-warnings/72-RESEARCH.md (D-22 caller migration note, Q3)
  </read_first>
  <action>
    **Part A — D-22 (WR-09): vault_connector explicit token requirement.**

    At `vault_connector.py:396-406` (the existing env-fallback code path):

    Current (paraphrased):
    ```
    if token is None:
        token = os.environ.get("VAULT_TOKEN")
    ```

    Replace with:
    ```
    if token is None:
        raise ValueError(
            "vault_connector requires explicit token; "
            "pass os.environ.get('VAULT_TOKEN') if env fallback intended"
        )
    ```
    Remove the implicit `os.environ` read entirely from the connector. The caller is now responsible.

    **Part B — D-22 (WR-09): run_scan.py caller migration.**

    Grep `run_scan.py` (and any other call site) for `scan_vault_targets(` and pass-throughs of the vault token. At each site, ensure the token is read from env explicitly at the caller boundary:
    ```
    vault_token = os.environ.get("VAULT_TOKEN") or cfg.connectors.vault_token
    if vault_token is None:
        logger.warning("VAULT_TOKEN not set and no cfg.connectors.vault_token; skipping Vault scan")
    else:
        endpoints = scan_vault_targets(..., token=vault_token, ...)
    ```
    Researcher: read the actual `run_scan.py` call site for the existing variable names. If `cfg.connectors.vault_token` is the canonical source, prefer that; the env var is the operator-facing override.

    Per RESEARCH Q3: also check `tests/test_vault_connector.py` — any test that called `scan_vault_targets(token=None)` expecting env fallback now needs to explicitly pass a token. Update those tests in Task 6.

    **Part C — D-23 (WR-18): PEM plural parser.**

    At `vault_connector.py:277-282`:

    Current (paraphrased):
    ```
    chunks = chain_pem.split("-----BEGIN CERTIFICATE-----")
    for chunk in chunks:
        if not chunk.strip():
            continue
        single_pem = "-----BEGIN CERTIFICATE-----" + chunk
        # ... _classify_pki_cert(single_pem) + emit ...
    ```

    Replace with:
    ```
    from cryptography import x509
    from cryptography.hazmat.primitives import serialization  # if not already imported

    try:
        certs = x509.load_pem_x509_certificates(chain_pem.encode("utf-8"))
    except AttributeError:
        if logger:
            logger.warning(
                "cryptography lib too old for load_pem_x509_certificates "
                "(need >= 36); falling back to naive split — bump cryptography"
            )
        certs = []  # or fall back to existing naive split path
    except ValueError as e:
        if logger:
            logger.warning("PEM chain parse failed: %s", e)
        certs = []

    for cert in certs:
        single_pem = cert.public_bytes(serialization.Encoding.PEM).decode("ascii")
        # ... existing _classify_pki_cert(single_pem) + CryptoEndpoint emission ...
    ```

    The plural form is available since `cryptography>=36`; the project pins `>=44.0` (pyproject.toml:13 per research), so the `AttributeError` branch is defensive only.

    Researcher: ensure the imports `from cryptography import x509` and `from cryptography.hazmat.primitives import serialization` are at module top. Reuse existing imports if already present.

    Per D-25: do NOT change `_classify_pki_cert` itself. Do NOT change `CryptoEndpoint` emission shape. Only the PEM-split → load step changes.

    PEP 8. Minimal diff.
  </action>
  <verify>
    <automated>python -m compileall quirk/scanner/vault_connector.py run_scan.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "vault_connector requires explicit token" quirk/scanner/vault_connector.py` returns at least one hit
    - `grep -nE "os\\.environ.*VAULT_TOKEN" quirk/scanner/vault_connector.py` returns zero hits (env read removed from the connector module)
    - `grep -nE "os\\.environ.*VAULT_TOKEN\\|VAULT_TOKEN.*os\\.environ" run_scan.py` returns at least one hit (env read migrated to caller)
    - `grep -nE "load_pem_x509_certificates" quirk/scanner/vault_connector.py` returns at least one hit (plural form)
    - `grep -nE "split\\(\"-----BEGIN CERTIFICATE-----\"\\)" quirk/scanner/vault_connector.py` returns zero hits in the live code path (naive split removed; if retained as fallback inside an except, keep it but the primary path is the plural parser)
    - `python -m compileall quirk/scanner/vault_connector.py run_scan.py` exits 0
  </acceptance_criteria>
  <done>
    vault_connector raises on token=None; run_scan.py reads VAULT_TOKEN explicitly; PEM parsing uses the cryptography library's plural form.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Tests for all CLOUD-05 fixes (WR-07/08/09/10/11/12/18/23/24)</name>
  <files>tests/test_profiles.py, tests/test_findings_evaluator_dedupe.py, tests/test_db_connector.py, tests/test_vault_connector.py</files>
  <read_first>
    - All five files modified in Tasks 1-5 (post-edit)
    - tests/test_risk_engine.py (existing — covers some _dedupe/_postprocess behavior already)
    - tests/test_db_connector.py and tests/test_vault_connector.py (existing — extend, don't replace)
    - .planning/phases/72-cloud-scanner-warnings/72-CONTEXT.md (D-02/03/04/20/21/22/23/24 test expectations)
  </read_first>
  <behavior>
    - WR-07 (D-20): db_connector connect with password=None omits the kwarg; password="" passes through with INFO log; password="x" passes through normally
    - WR-08 (D-21): db_connector mysql/rds exception handler stores scan_error via safe_str (host/credentials stripped)
    - WR-09 (D-22): vault_connector raises ValueError when token=None; explicit token succeeds
    - WR-10 (D-05): `from quirk.engine.risk_engine import evaluate_endpoints` still works (shim); `from quirk.engine.findings_evaluator import evaluate_endpoints` also works; both reference the same function object
    - WR-11 (D-02): if YAML has `connectors: { enable_email: false }`, the standard/deep profile does NOT flip enable_email back to True; if YAML omits enable_email, the profile DOES flip it to True per profile rules
    - WR-12 (D-03): the `standard` branch profiles.py code has fewer `_set_if_default` calls than before (proves no-op removal)
    - WR-18 (D-23): vault_connector parses a multi-cert chain PEM into N cert endpoints (where N is the number of certs in the chain) using `load_pem_x509_certificates`
    - WR-23 (D-24): _postprocess_findings still works correctly when iterated tuple snapshot is in effect (no behavior change — defensive only)
    - WR-24 (D-04): two findings differing ONLY in `recommendation` text produce identical dedup output ordering (golden-file regression-safe under remediation edits)
    - WR-24 (D-04 / golden regen): the new sort key produces a deterministic ordering that the regenerated snapshots reflect
  </behavior>
  <action>
    **Create `tests/test_profiles.py`** (new file):
    - `test_profiles_respects_user_explicit_enable_email_false` — build a `Config` where `cfg.connectors.enable_email = False` AND `cfg.connectors._user_set_fields = frozenset({'enable_email'})`. Apply standard profile. Assert `cfg.connectors.enable_email is False` (NOT flipped to True).
    - `test_profiles_flips_enable_email_when_not_user_set` — same but `_user_set_fields = frozenset()`. Apply standard profile. Assert `cfg.connectors.enable_email is True` (default mutation works).
    - `test_profiles_respects_user_explicit_enable_broker_false` — analogous for broker.
    - `test_profiles_flips_enable_broker_when_not_user_set` — analogous.
    - `test_profiles_standard_branch_no_op_calls_removed` — inspect the source of `quirk.engine.profiles` (via `inspect.getsource`). Count `_set_if_default(` occurrences in the `standard` branch. Assert the count is LESS than a pre-Phase-72 baseline (researcher reads the file at HEAD before edit to record the baseline; assert post-edit count < baseline).

    **Create `tests/test_findings_evaluator_dedupe.py`** (new file):
    - `test_dedupe_sort_stable_under_recommendation_diff` — build two findings with identical (host, port, title, severity) but different `recommendation` text. Dedup. Assert the resulting order matches a fixed list (the recommendation diff does not reshuffle order).
    - `test_dedupe_sort_severity_priority` — build 4 findings with severities LOW, HIGH, INFO, CRITICAL (all other fields distinct enough to avoid dedup-collapse). Dedup. Assert output order is CRITICAL, HIGH, LOW, INFO (by _SEVERITY_RANK).
    - `test_severity_rank_module_private` — `import quirk.engine.findings_evaluator as m; assert "_SEVERITY_RANK" in m.__dict__` (existence) AND assert it is not re-exported via `__all__` if `__all__` is defined.
    - `test_dedupe_via_risk_engine_shim_works` — `from quirk.engine.risk_engine import _dedupe_findings; from quirk.engine.findings_evaluator import _dedupe_findings as canonical; assert _dedupe_findings is canonical`.

    **Extend `tests/test_db_connector.py`** (do NOT replace existing):
    - `test_connect_password_none_omits_kwarg` — patch `psycopg2.connect` (or whichever underlying connect call) to capture kwargs. Call the connect helper with `password=None`. Assert `'password' not in captured_kwargs`.
    - `test_connect_password_empty_string_passes_through_with_info_log` — same patch, call with `password=""`. Assert `captured_kwargs['password'] == ""` AND `caplog.text` contains a substring about explicit empty password.
    - `test_connect_password_nonempty_passes_through` — call with `password="secret"`. Assert `captured_kwargs['password'] == "secret"`.
    - `test_mysql_exception_uses_safe_str` — patch the mysql connect to raise an exception whose message contains a hostname/credential. Call the mysql scan path. Assert `scan_error` does NOT contain the credential substring (safe_str strips it).

    **Extend `tests/test_vault_connector.py`** (do NOT replace existing):
    - `test_scan_vault_targets_raises_on_none_token` — call `scan_vault_targets(..., token=None, ...)`. Assert `pytest.raises(ValueError, match="explicit token")`.
    - `test_scan_vault_targets_accepts_explicit_token` — call with `token="hvs.xxxx"`. Patch the underlying Vault client. Assert no exception.
    - `test_scan_pki_mounts_parses_multi_cert_chain` — build a fake PEM string containing two `-----BEGIN CERTIFICATE-----` blocks (use `cryptography.hazmat.primitives` to generate two self-signed test certs and concatenate their PEM forms). Call `_scan_pki_mounts` (or whichever wrapper exposes it). Assert 2 `CryptoEndpoint` objects are emitted.
    - `test_scan_pki_mounts_uses_load_pem_x509_certificates` — `inspect.getsource(quirk.scanner.vault_connector)` contains the substring `"load_pem_x509_certificates"`.

    Researcher: tests must use existing project fixture style (look at neighbors).
  </behavior>
  <verify>
    <automated>pytest tests/test_profiles.py tests/test_findings_evaluator_dedupe.py tests/test_db_connector.py tests/test_vault_connector.py tests/test_risk_engine.py tests/test_email_findings.py -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_profiles.py exists with ≥ 5 test functions; all pass
    - tests/test_findings_evaluator_dedupe.py exists with ≥ 4 test functions; all pass
    - tests/test_db_connector.py has ≥ 4 new test functions; existing + new tests pass
    - tests/test_vault_connector.py has ≥ 4 new test functions; existing + new tests pass
    - The migrated test files (test_risk_engine.py, test_email_findings.py, etc.) still pass after the import migration in Task 1
  </acceptance_criteria>
  <done>
    All 9 CLOUD-05 fixes covered by tests; existing test suite still green after rename + import migration.
  </done>
</task>

<task type="auto">
  <name>Task 7: Regenerate golden-file snapshots for _dedupe_findings sort change (D-04)</name>
  <files>tests/snapshots/, .planning/phases/72-cloud-scanner-warnings/72-05-SUMMARY.md</files>
  <read_first>
    - tests/ — identify which snapshot files reference `_dedupe_findings` output ordering (likely under `tests/snapshots/` or `tests/golden/` — researcher locates)
    - .planning/phases/72-cloud-scanner-warnings/72-CONTEXT.md (test_strategy section — separate-commit requirement for golden regen)
  </read_first>
  <action>
    Per CONTEXT test strategy: "Snapshot regeneration commit — D-04 changes golden-file ordering across the project. Plan must include a dedicated commit `chore(72-snapshots): regen goldens for _dedupe_findings sort key change (D-04)` separate from the code-change commit, so the diff is auditable."

    1. Run the full test suite once to identify which snapshot files fail due to the new sort order:
       ```
       pytest -x 2>&1 | grep -E "snapshot|golden|fixture" | head -50
       ```

    2. For each failing snapshot, regenerate via the project's snapshot-update mechanism. Common options:
       - `pytest --snapshot-update` (if syrupy or similar is used)
       - Manual regeneration: run the test with a `WRITE_GOLDENS=1` env var if the suite uses that pattern
       - Researcher: grep for `golden` / `snapshot` / `--snapshot-update` to identify the project's convention

    3. **Important:** This regeneration is a SEPARATE commit from the code changes in Tasks 1-5. Suggested commit message:
       ```
       chore(72-snapshots): regen goldens for _dedupe_findings sort key change (D-04)
       
       Sort key changed from (host, port, title, recommendation) to
       (severity_rank, title, host, port) per Phase 72 D-04.
       Snapshot diff reflects reordering only — no finding data changes.
       ```

    4. Visual review: spot-check 2-3 regenerated snapshots. Confirm the only differences are reordering (same set of findings, different sequence). If finding *content* changed unexpectedly, STOP and investigate — that signals a bug in the new sort logic, not a benign regen.

    5. If no snapshots reference dedup output ordering (the project may not use snapshots for this), document that in the SUMMARY: `"No golden files reference _dedupe_findings output ordering; D-04 sort change has no snapshot impact."`

    Per D-25: do NOT regenerate snapshots unrelated to dedup ordering. If `pytest -x` shows failures in unrelated modules, those are bugs introduced by Tasks 1-5 and must be debugged, not snapshot-overwritten.
  </action>
  <verify>
    <automated>pytest -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -x` exits 0 (full suite green after rename + sort change + snapshot regen)
    - Snapshot regeneration is in a separate git commit (researcher confirms via `git log --oneline -3` showing the `chore(72-snapshots)` commit AFTER the code-change commit)
    - SUMMARY documents which snapshots were regenerated AND the rationale (reordering only, no content drift)
  </acceptance_criteria>
  <done>
    Full test suite green; golden snapshots regenerated in a dedicated commit; reordering verified.
  </done>
</task>

<task type="auto">
  <name>Task 8: Flip WR-07/08/09/10/11/12/18/23/24 audit ledger rows</name>
  <files>.planning/audit-2026-05-08/AUDIT-TASKS.md</files>
  <read_first>
    - .planning/audit-2026-05-08/AUDIT-TASKS.md lines 97-114 (the 9 rows)
  </read_first>
  <action>
    Flip each of the following audit ledger rows from `| — | [ ] open |` to `| Phase 72 | [x] closed |`. One line per row, single docs commit at the end of the plan:

    - Line 97 (WR-07): DB connector psycopg2.connect password defaults to empty string
    - Line 98 (WR-08): DB connector exception message does not strip target host
    - Line 99 (WR-09): vault_connector reads VAULT_TOKEN from env after token=None
    - Line 100 (WR-10): risk_engine.py is misnamed / actual scorer not in this review
    - Line 101 (WR-11): profiles.py mutates enable_email/enable_broker w/o user override
    - Line 102 (WR-12): profiles.py standard profile re-applies defaults equal to baseline
    - Line 108 (WR-18): Vault _scan_pki_mounts PEM split heuristic fragile
    - Line 113 (WR-23): evaluate_endpoints _postprocess_findings mutates during iteration
    - Line 114 (WR-24): _dedupe_findings 4-tuple ordering causes unstable golden outputs

    No other rows touched. Commit message: `docs(72-audit): close 9 CLOUD-05 WR rows (WR-07/08/09/10/11/12/18/23/24)`.

    SUMMARY notes per-row evidence:
    - WR-07: `password kwarg omitted when None; explicit "" pass-through with INFO log; tested at tests/test_db_connector.py::test_connect_password_*`
    - WR-08: `mysql/rds exception text via safe_str (postgres pre-covered Phase 59); tested at tests/test_db_connector.py::test_mysql_exception_uses_safe_str`
    - WR-09: `ValueError on token=None; run_scan.py reads VAULT_TOKEN explicitly; tested at tests/test_vault_connector.py::test_scan_vault_targets_raises_on_none_token`
    - WR-10: `git mv risk_engine.py → findings_evaluator.py; shim re-exports; 6 internal callers migrated atomically; verified at tests/test_findings_evaluator_dedupe.py::test_dedupe_via_risk_engine_shim_works`
    - WR-11: `ConnectorsCfg._user_set_fields sidecar; profiles.py guards both branches; tested at tests/test_profiles.py::test_profiles_respects_user_explicit_*`
    - WR-12: `standard branch no-op _set_if_default calls removed (count baseline vs post-edit verified); tested at tests/test_profiles.py::test_profiles_standard_branch_no_op_calls_removed`
    - WR-18: `cryptography.x509.load_pem_x509_certificates plural form; tested at tests/test_vault_connector.py::test_scan_pki_mounts_parses_multi_cert_chain`
    - WR-23: `tuple(findings) snapshot iteration in _postprocess_findings; defensive per C-2; no behavior change`
    - WR-24: `_SEVERITY_RANK + (severity_rank, title, host, port) sort; recommendation dropped from key; golden snapshots regenerated separately; C-4 adjudication: title is the finding_id surrogate`
  </action>
  <verify>
    <automated>grep -cE "scanners-cloud/WR-(07|08|09|10|11|12|18|23|24).*Phase 72.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "scanners-cloud/WR-(07|08|09|10|11|12|18|23|24).*Phase 72.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 9
    - `git diff --stat .planning/audit-2026-05-08/AUDIT-TASKS.md` shows only the 9 WR row lines modified
    - No other audit rows touched
  </acceptance_criteria>
  <done>
    All 9 CLOUD-05 audit rows closed under Phase 72 in a single docs commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator YAML connectors block → runtime cfg.connectors | User-explicit values silently overwritten by profile-application mutation |
| Vault token caller → connector | Implicit env-var fallback masks misconfiguration; explicit raise surfaces the contract |
| psycopg2 password kwarg | Empty-string default silently submits credentials; libpq's .pgpass fallback is the correct path |
| Multi-cert PEM chain string | Naive `.split("-----BEGIN ...")` fails on edge cases (mixed line endings, embedded comments) |
| _dedupe_findings sort key | Including remediation text in the key causes golden-output churn on benign text edits |
| _postprocess_findings iteration | Hypothetical future list-mutation during iteration causes silent data loss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-15 | Tampering (config override) | profiles.py enable_email/broker mutation | mitigate | Track raw-YAML keys via _user_set_fields sidecar; guard each mutation (D-02) |
| T-72-16 | Spoofing (intent confusion) | profiles.py standard re-apply of dataclass defaults | mitigate | Remove no-op _set_if_default calls; profile intent now matches code (D-03) |
| T-72-17 | Information Disclosure (cred leak) | psycopg2 password='' default | mitigate | Omit password kwarg when None; libpq handles .pgpass (D-20) |
| T-72-18 | Information Disclosure (cred leak in exc) | db_connector mysql/rds scan_error | mitigate | safe_str(exc) per Phase 59 LEAK-01 pattern (D-21) |
| T-72-19 | Spoofing (implicit env fallback) | vault_connector token=None → os.environ['VAULT_TOKEN'] | mitigate | Raise ValueError; caller passes env explicitly (D-22) |
| T-72-20 | Tampering (PEM parse edge cases) | vault _scan_pki_mounts naive split | mitigate | cryptography.x509.load_pem_x509_certificates (D-23) |
| T-72-21 | Tampering (silent mutation-during-iter) | _postprocess_findings | mitigate | tuple(findings) snapshot defensive pattern (D-24) |
| T-72-22 | Tampering (output drift) | _dedupe_findings sort key includes recommendation | mitigate | Drop recommendation; sort by (severity_rank, title, host, port) (D-04) |
| T-72-23 | Repudiation (module-name confusion) | risk_engine.py misnamed as scorer | mitigate | Rename to findings_evaluator.py + 2-line shim (D-05) |
</threat_model>

<verification>
- `python -m compileall quirk/config.py quirk/engine/profiles.py quirk/engine/risk_engine.py quirk/engine/findings_evaluator.py quirk/scanner/db_connector.py quirk/scanner/vault_connector.py run_scan.py` clean
- `pytest tests/test_profiles.py tests/test_findings_evaluator_dedupe.py tests/test_db_connector.py tests/test_vault_connector.py tests/test_risk_engine.py tests/test_email_findings.py -x` passes
- Full `pytest -x` passes (golden snapshots regenerated)
- AUDIT-TASKS.md WR-07/08/09/10/11/12/18/23/24 rows show `Phase 72 | [x] closed`
- `git log --oneline -5` shows the snapshot regen commit is SEPARATE from the code-change commit (CONTEXT test_strategy requirement)
- No collateral changes beyond the 9 locked D-NN sites (D-25 compliance)
</verification>

<success_criteria>
- risk_engine.py renamed to findings_evaluator.py + 2-line shim + 6 internal callers migrated atomically
- _SEVERITY_RANK + new sort key drops recommendation
- _postprocess_findings iterates tuple snapshot
- ConnectorsCfg._user_set_fields sidecar + profiles.py user-explicit guards
- Standard branch no-op _set_if_default calls removed
- db_connector password=None omits kwarg; mysql/rds exception via safe_str
- vault_connector raises on token=None; run_scan.py reads env; PEM via plural cryptography parser
- 17+ new tests pass + 6 migrated test files still pass
- Snapshot regeneration committed separately
- WR-07, WR-08, WR-09, WR-10, WR-11, WR-12, WR-18, WR-23, WR-24 closed
</success_criteria>

<output>
Create `.planning/phases/72-cloud-scanner-warnings/72-05-SUMMARY.md` documenting:
- The rename (D-05) with the list of 6 migrated importers and the shim shape
- The _SEVERITY_RANK + new sort key (D-04) and the C-4 / Pitfall-6 adjudication ("finding_id" → "title")
- The ConnectorsCfg sidecar (D-02) and profiles.py mutation guards
- The standard branch no-op removal inventory (D-03) — list each suppressed _set_if_default call
- db_connector password handling (D-20) and the mysql/rds safe_str coverage (D-21 narrowed per C-2)
- vault_connector explicit-token requirement (D-22) and run_scan.py env migration
- Vault PKI plural parser (D-23)
- _postprocess_findings tuple snapshot (D-24) with C-2 note
- Snapshot regen commit_sha + per-row audit evidence
- Test count (17+ new) and verification commands
</output>
</content>
</invoke>