---
phase: 72-cloud-scanner-warnings
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/scanner/gcp_connector.py
  - tests/test_gcp_connector.py
  - .planning/audit-2026-05-08/AUDIT-TASKS.md
autonomous: true
requirements: [CLOUD-03]
must_haves:
  truths:
    - "gcp_connector.py defines MAX_KMS_PAGES = 1000 at module scope and each while-pagination loop in _scan_kms raises ValueError after 1000 iterations"
    - "GCP _scan_kms skips both CRYPTO_KEY_VERSION_ALGORITHM_UNSPECIFIED and UNKNOWN keys consistently with an INFO log naming the algorithm"
    - "GCP _scan_cloud_sql verifies the existing service_detail routing covers the description token (per C-3 verification-first adjudication)"
    - "Audit rows scanners-cloud/WR-04, WR-05, WR-22 are flipped to [x] closed under Phase 72"
  artifacts:
    - path: "quirk/scanner/gcp_connector.py"
      provides: "MAX_KMS_PAGES cap, UNSPECIFIED+UNKNOWN skip set, Cloud SQL service_detail verified/extended"
      contains: "MAX_KMS_PAGES"
    - path: "tests/test_gcp_connector.py"
      provides: "Parametrized 1001-page pagination test, UNSPECIFIED/UNKNOWN skip tests, Cloud SQL service_detail assertion"
      contains: "MAX_KMS_PAGES"
  key_links:
    - from: "quirk/scanner/gcp_connector.py module scope"
      to: "MAX_KMS_PAGES constant"
      via: "MAX_KMS_PAGES = 1000"
      pattern: "MAX_KMS_PAGES = 1000"
    - from: "quirk/scanner/gcp_connector.py::_scan_kms"
      to: "ValueError raise"
      via: "page_count > MAX_KMS_PAGES check"
      pattern: "page_count > MAX_KMS_PAGES"
    - from: "quirk/scanner/gcp_connector.py::_scan_kms algorithm check"
      to: "skip set"
      via: "{'CRYPTO_KEY_VERSION_ALGORITHM_UNSPECIFIED', 'UNKNOWN'}"
      pattern: "CRYPTO_KEY_VERSION_ALGORITHM_UNSPECIFIED"
---

<objective>
Close CLOUD-03 by hardening GCP connector pagination, algorithm-skip consistency, and Cloud SQL description surfacing:

1. **WR-04 (D-01)** — `quirk/scanner/gcp_connector.py::_scan_kms` (lines 131-224): each `while page_token:` loop (project-rings → key-rings → keys, triple-nested) maintains a `page_count` counter. If `page_count > MAX_KMS_PAGES` (constant `1000` at module scope), raise `ValueError`. Per D-01a default: per-loop counter (each iteration domain bounded separately).
2. **WR-05 (D-16)** — `quirk/scanner/gcp_connector.py:174-180`: per RESEARCH Pitfall 2, current code coerces unknown algorithms via `GCP_KMS_ALGORITHM_MAP.get(algorithm, ("UNKNOWN", None))` and then checks `alg_name == "UNKNOWN"`. D-16 requires explicitly checking the **raw algorithm string** against the skip set `{"CRYPTO_KEY_VERSION_ALGORITHM_UNSPECIFIED", "UNKNOWN"}` BEFORE the map lookup, plus an INFO log that names the algorithm.
3. **WR-22 (D-17 + C-3)** — `quirk/scanner/gcp_connector.py::_scan_cloud_sql` (lines 262-272): per RESEARCH C-3 the current code already builds `service_detail=f"CLOUD_SQL/{description.replace(' ', '-')}"`, so the description IS surfaced via a slash-suffix encoding. **First task verifies this**; if the description is already in `service_detail`, the row is closeable with evidence "no-op, already surfaced via slash-suffix; verified <commit_sha>" plus an assertion test. If the audit's strict reading (description in a separate field) is required, surface to the user — but the CryptoEndpoint schema has only one `service_detail` field, so the slash-suffix is the only structurally valid option.

Then flip audit ledger rows WR-04, WR-05, WR-22 to `Phase 72 | [x] closed`.

Purpose: GCP connector cannot run away on a pathological KMS deployment, treats UNSPECIFIED/UNKNOWN keys uniformly with explicit logging, and the Cloud SQL description is provably reachable from the finding output.
Output: Patched `gcp_connector.py`, new `tests/test_gcp_connector.py`, three audit rows flipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/72-cloud-scanner-warnings/72-CONTEXT.md
@.planning/phases/72-cloud-scanner-warnings/72-RESEARCH.md
@.planning/audit-2026-05-08/AUDIT-TASKS.md

<interfaces>
Phase 71 PROTO-05 D-01 fail-loud bound pattern (precedent for D-01 here):
```
if num_addresses > MAX:
    raise ValueError(f"... exceeded {MAX}; aborting to prevent runaway scan")
```

GCP pagination shape (verified in research):
```
loc_request = client.locations().list(...)
while loc_request is not None:
    loc_response = loc_request.execute()
    # ... process ...
    loc_request = client.locations().list_next(previous_request=loc_request, previous_response=loc_response)
```

Skip set (D-16, raw algorithm strings):
```
_GCP_KMS_SKIP_ALGORITHMS = frozenset({
    "CRYPTO_KEY_VERSION_ALGORITHM_UNSPECIFIED",
    "UNKNOWN",
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GCP KMS pagination cap + UNSPECIFIED/UNKNOWN skip set (WR-04 / WR-05)</name>
  <files>quirk/scanner/gcp_connector.py</files>
  <read_first>
    - quirk/scanner/gcp_connector.py lines 1-50 (imports + module constants)
    - quirk/scanner/gcp_connector.py lines 131-224 (_scan_kms — triple-nested pagination)
    - quirk/scanner/gcp_connector.py lines 170-185 (algorithm skip site for D-16)
    - .planning/phases/72-cloud-scanner-warnings/72-CONTEXT.md (D-01, D-01a, D-16)
    - .planning/phases/72-cloud-scanner-warnings/72-RESEARCH.md (Pitfall 2)
  </read_first>
  <action>
    **Part A — D-01 (WR-04): per-loop MAX_KMS_PAGES cap.**

    1. At module scope (with other constants near top of file), add:
       ```
       MAX_KMS_PAGES = 1000  # Phase 72 D-01: per-loop cap; ~1M items at default page size
       ```

    2. In `_scan_kms`, locate each of the three `while ... is not None:` pagination loops (project-rings, key-rings, keys). For EACH loop, per D-01a (per-loop counter, default):
       ```
       page_count = 0
       while loc_request is not None:
           page_count += 1
           if page_count > MAX_KMS_PAGES:
               raise ValueError(
                   f"GCP KMS pagination exceeded {MAX_KMS_PAGES} pages for "
                   f"{<resource_identifier_in_scope>}; aborting to prevent runaway scan"
               )
           # ... existing body unchanged ...
       ```
       The `<resource_identifier_in_scope>` substitutes the resource name available at that loop level (e.g., `project_resource` for the outer, `location` for the middle, `key_ring` for the inner). Use whatever variable is already in scope at each level so the error message identifies WHICH loop tripped.

    **Part B — D-16 (WR-05): explicit raw-algorithm skip + INFO log.**

    1. At module scope (near `GCP_KMS_ALGORITHM_MAP`), add:
       ```
       _GCP_KMS_SKIP_ALGORITHMS = frozenset({
           "CRYPTO_KEY_VERSION_ALGORITHM_UNSPECIFIED",
           "UNKNOWN",
       })
       ```

    2. In `_scan_kms` at lines 174-180, BEFORE the `GCP_KMS_ALGORITHM_MAP.get(...)` lookup, insert:
       ```
       if algorithm in _GCP_KMS_SKIP_ALGORITHMS:
           if logger:
               logger.info("GCP key %s skipped (algorithm=%s)", key_resource_name, algorithm)
           continue
       ```
       This is distinct from the existing `alg_name == "UNKNOWN"` check (which keys off the mapped output). Per RESEARCH Pitfall 2, the explicit raw-string check + INFO log is what D-16 mandates.

       The existing `alg_name == "UNKNOWN"` branch may now be dead code for the two strings in the skip set — but DO NOT remove it; other algorithms may also map to "UNKNOWN" by default, and removing the post-map branch is incidental cleanup forbidden by D-25.

    Use the actual variable names already in scope (`algorithm`, `key_resource_name` or similar; read the function to confirm). PEP 8. Minimal diff.
  </action>
  <verify>
    <automated>python -m compileall quirk/scanner/gcp_connector.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^MAX_KMS_PAGES = 1000" quirk/scanner/gcp_connector.py` returns exactly one hit at module scope
    - `grep -cE "page_count > MAX_KMS_PAGES" quirk/scanner/gcp_connector.py` >= 3 (one per pagination loop, per D-01a per-loop default)
    - `grep -cE "page_count = 0" quirk/scanner/gcp_connector.py` >= 3 (one initialization per loop)
    - `grep -nE "GCP KMS pagination exceeded" quirk/scanner/gcp_connector.py` returns at least one hit
    - `grep -nE "_GCP_KMS_SKIP_ALGORITHMS" quirk/scanner/gcp_connector.py` returns at least two hits (definition + use)
    - `grep -nE "skipped \\(algorithm=" quirk/scanner/gcp_connector.py` returns at least one hit
    - `python -m compileall quirk/scanner/gcp_connector.py` exits 0
  </acceptance_criteria>
  <done>
    gcp_connector.py has the pagination cap (per-loop) and the explicit algorithm skip set; file compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify GCP Cloud SQL service_detail surfaces description (WR-22 / D-17 / C-3)</name>
  <files>quirk/scanner/gcp_connector.py</files>
  <read_first>
    - quirk/scanner/gcp_connector.py lines 260-280 (_scan_cloud_sql current implementation)
    - .planning/phases/72-cloud-scanner-warnings/72-CONTEXT.md (D-17)
    - .planning/phases/72-cloud-scanner-warnings/72-RESEARCH.md (C-3 — verification-first adjudication)
  </read_first>
  <action>
    Per RESEARCH C-3, the current `_scan_cloud_sql` at line 268 already builds `service_detail=f"CLOUD_SQL/{description.replace(' ', '-')}"`. The description IS already surfaced via `service_detail` (encoded in a slash-separated suffix).

    **Step 1 — Verify current state:**
    Read `_scan_cloud_sql` (lines 262-272) and confirm the `service_detail` field includes the `description` token. If confirmed, this is the C-3 adjudication path: WR-22 closes as verification-only.

    **Step 2 — If verified (expected path):**
    Make NO code change. The Task 3 audit-row flip records evidence: `"no-op, already surfaced via slash-suffix at gcp_connector.py:268; verified <commit_sha>"`. The associated assertion test in Task 3 (`test_cloud_sql_service_detail_contains_description`) provides defense-in-depth proof.

    **Step 3 — If NOT verified (description missing from service_detail):**
    Add the description to `service_detail` using the slash-suffix encoding pattern already established by Phase 69 BLOCK-02:
    ```
    service_detail = f"CLOUD_SQL/{(description or 'no-description').replace(' ', '-')}"
    ```
    Use the exact variable name in scope for the description field (likely `description`). Mirror the existing Phase 69 routing — do NOT introduce a new field on `CryptoEndpoint`.

    Per D-25: do NOT modify other fields in `_scan_cloud_sql`. Do NOT introduce new schema fields. The `service_detail` field is the canonical surface.
  </action>
  <verify>
    <automated>python -m compileall quirk/scanner/gcp_connector.py &amp;&amp; grep -cE "CLOUD_SQL/" quirk/scanner/gcp_connector.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "service_detail.*CLOUD_SQL.*description|CLOUD_SQL/\\{" quirk/scanner/gcp_connector.py` returns at least one hit (the description routing exists)
    - `python -m compileall quirk/scanner/gcp_connector.py` exits 0
    - Either: no diff to `_scan_cloud_sql` (verification-only path, C-3 expected) OR a minimal one-line diff adding description to `service_detail` (corrective path)
  </acceptance_criteria>
  <done>
    `_scan_cloud_sql` provably routes description through `service_detail` (either pre-existing or now-corrected).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests + flip WR-04/05/22 audit rows</name>
  <files>tests/test_gcp_connector.py, .planning/audit-2026-05-08/AUDIT-TASKS.md</files>
  <read_first>
    - quirk/scanner/gcp_connector.py (post-Tasks 1-2)
    - tests/test_k8s_connector.py (skim for mock fixture style)
    - .planning/audit-2026-05-08/AUDIT-TASKS.md lines 94, 95, 112 (WR-04/05/22)
  </read_first>
  <behavior>
    - WR-04: _scan_kms invoked with a mock client that returns 1001 successive non-None paginated requests raises ValueError containing "exceeded 1000 pages"
    - WR-04: _scan_kms invoked with 5 paginated requests completes normally (no false positive)
    - WR-05: _scan_kms invoked with a key whose algorithm string is "CRYPTO_KEY_VERSION_ALGORITHM_UNSPECIFIED" emits no finding for that key and logs at INFO level
    - WR-05: _scan_kms invoked with a key whose algorithm string is "UNKNOWN" emits no finding for that key and logs at INFO
    - WR-22: _scan_cloud_sql invoked with a fake instance having description="Production primary DB" produces a finding whose service_detail contains the substring "Production-primary-DB" (slash-suffix encoding)
  </behavior>
  <action>
    Create `tests/test_gcp_connector.py`:

    **WR-04 (pagination cap):**
    - `test_kms_pagination_cap_raises_after_1000_pages` — parametrize: build a mock GCP KMS client whose `list_next` returns a non-None request 1001 times. Call `_scan_kms`. Assert `pytest.raises(ValueError, match="exceeded 1000 pages")`.
    - `test_kms_pagination_under_cap_completes` — mock returns non-None for 5 iterations then None. Assert no exception, function returns normally.

    **WR-05 (UNSPECIFIED/UNKNOWN skip):**
    - `test_kms_skips_unspecified_algorithm` — fake key with `algorithm="CRYPTO_KEY_VERSION_ALGORITHM_UNSPECIFIED"`. Assert no `CryptoEndpoint` is emitted for that key AND `caplog.text` contains `"algorithm=CRYPTO_KEY_VERSION_ALGORITHM_UNSPECIFIED"`.
    - `test_kms_skips_unknown_algorithm` — same with `algorithm="UNKNOWN"`.
    - `test_kms_emits_for_real_algorithm` — `algorithm="GOOGLE_SYMMETRIC_ENCRYPTION"` (a real GCP KMS algorithm string). Assert a finding IS emitted (negative-of-skip, prevents over-eager skipping).

    **WR-22 (Cloud SQL service_detail):**
    - `test_cloud_sql_service_detail_contains_description` — fake Cloud SQL instance with `description="Production primary DB"`. Call `_scan_cloud_sql`. Assert the resulting `CryptoEndpoint.service_detail` contains the substring `"Production-primary-DB"` (per the slash-suffix slug encoding at gcp_connector.py:268).

    Use `unittest.mock.MagicMock` to build the fake GCP client objects. Researcher: look at how the existing test suite mocks GCP — if there's no precedent, use `MagicMock` directly.

    **Flip audit ledger rows:**
    - Line 94 (WR-04): `| — | [ ] open |` → `| Phase 72 | [x] closed |`
    - Line 95 (WR-05): same
    - Line 112 (WR-22): same — evidence line in the summary references the C-3 verification adjudication

    Three rows, single docs commit.
  </behavior>
  <verify>
    <automated>pytest tests/test_gcp_connector.py -x &amp;&amp; grep -cE "scanners-cloud/WR-(04|05|22).*Phase 72.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_gcp_connector.py exists with ≥ 6 test functions; all pass
    - `grep -cE "scanners-cloud/WR-(04|05|22).*Phase 72.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 3
    - `git diff --stat .planning/audit-2026-05-08/AUDIT-TASKS.md` shows only the three WR row lines modified
  </acceptance_criteria>
  <done>
    Pagination cap, algorithm skip, and Cloud SQL service_detail all proven by tests; WR-04/05/22 closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GCP API pagination → connector | A pathological / hostile KMS deployment with millions of keys can DoS the scanner via unbounded `list_next` loops |
| GCP algorithm enum → finding emission | UNSPECIFIED / UNKNOWN values inconsistently treated produce non-deterministic findings |
| Cloud SQL description field → report consumer | Description-only-in-cloud_scan_json strands the data from report renderers that read service_detail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-09 | Denial of Service (self-inflicted) | GCP _scan_kms unbounded triple-nested pagination | mitigate | MAX_KMS_PAGES = 1000 per-loop cap; ValueError on overflow (D-01) |
| T-72-10 | Information Disclosure (inconsistent classification) | GCP _scan_kms UNSPECIFIED vs UNKNOWN handling | mitigate | Explicit raw-string skip set {UNSPECIFIED, UNKNOWN} with INFO log (D-16) |
| T-72-11 | Information Disclosure (data stranding) | GCP Cloud SQL description not in service_detail | mitigate | Verified routing via slash-suffix encoding in service_detail (D-17, C-3) |
</threat_model>

<verification>
- `python -m compileall quirk/scanner/gcp_connector.py` clean
- `pytest tests/test_gcp_connector.py -x` passes
- AUDIT-TASKS.md WR-04/05/22 rows show `Phase 72 | [x] closed`
- No collateral changes beyond D-01/D-16/D-17 sites (D-25 compliance)
</verification>

<success_criteria>
- MAX_KMS_PAGES = 1000 module constant + per-loop counter + ValueError raise (D-01a default)
- UNSPECIFIED + UNKNOWN raw-algorithm skip set with INFO log
- Cloud SQL description verifiably surfaced in service_detail (C-3 verification-first path)
- 6+ tests pass
- WR-04, WR-05, WR-22 closed
</success_criteria>

<output>
Create `.planning/phases/72-cloud-scanner-warnings/72-03-SUMMARY.md` documenting: pagination cap implementation (per-loop counter rationale), algorithm skip-set, C-3 verification adjudication for WR-22 (cite the verifying commit_sha), audit rows flipped, test counts, and rationale linking to D-01/D-16/D-17.
</output>
</content>
</invoke>