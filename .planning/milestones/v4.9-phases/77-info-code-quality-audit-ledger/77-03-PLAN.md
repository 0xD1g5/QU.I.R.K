---
phase: 77-info-code-quality-audit-ledger
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/dashboard/api/routes/qramm.py
  - quirk/cli/banner.py
  - quirk/db.py
  - tests/test_qramm_routes_typed_response.py
  - tests/test_banner_comment_corrected.py
  - tests/test_qramm_multiplier_constants.py
  - tests/test_db_ensure_columns_generic.py
autonomous: true
requirements: [INFO-03]
must_haves:
  truths:
    - "`quirk/dashboard/api/routes/qramm.py` public score endpoint declares an explicit Pydantic `BaseModel` response model (e.g. `QrammScoreResponse`) and uses `response_model=QrammScoreResponse` on `@router.post(...)`; the return type annotation is the model, not `Dict[str, Any]` (Discretion D-16: narrow to public score response per RESEARCH recommendation; full TypedDict migration deferred to v5.0) per D-16"
    - "`quirk/cli/banner.py:64` comment is corrected to state that `_FACES` is a raw string `r\"...\"` and the literal `\\-` is preserved exactly (RESEARCH C-5 / Pitfall 4: the audit + CONTEXT both misread; only the comment is wrong, not the code) per D-17"
    - "**D-18 (IN-03)** disposition: RESEARCH C-11 / A4 / Q2 — interactive TZ fallback site NOT located at HEAD. Researcher recommends Task 1 Wave-0 grep sweep; if no site found, IN-03 closes as audit-flip-only with 'site not present at HEAD — likely fixed by earlier phase or audit row stale.' Final disposition recorded in 77-03-SUMMARY.md and consumed by PLAN 77-05"
    - "`quirk/dashboard/api/routes/qramm.py` defines module-level constants `MULTIPLIER_MIN = 0.8`, `MULTIPLIER_MAX = 1.5`, `MULTIPLIER_LOW_STEP = 0.10`, `MULTIPLIER_HIGH_STEP = 0.20`; the 4 magic-number literals at lines 172-176, 192-193, 363-368 are replaced by the named constants (RESEARCH C-2 / Pitfall 2: in routes/qramm.py NOT scoring.py — CONTEXT D-19 directive corrected) per D-19"
    - "**D-20 (IN-05)** disposition: RESEARCH C-6 / Pitfall 5 / A6 — `quirk/dashboard/api/app.py:124-127::_make_handler` is ALREADY the canonical factory pattern per audit-row prose ('No-op'). No code change; closes as audit-flip-only via PLAN 77-05"
    - "`quirk/db.py` introduces a single generic `_ensure_columns(engine, table, expected_cols)` helper (RESEARCH Pattern 3). The 8 column-adding helpers (`_ensure_identity_columns`, `_ensure_gcp_columns`, `_ensure_v43_columns`, `_ensure_email_columns`, `_ensure_broker_columns`, `_ensure_phase41_columns`, `_ensure_phase46_columns`, `_ensure_phase54_qramm_columns`) are removed; their column lists become module-level constants consumed by `_ensure_columns`. The 5 table-creating helpers (`_ensure_qramm_profiles_fk`, `_ensure_qramm_tables`, `_ensure_scheduled_tables`, `_ensure_scan_jobs_table`, `_ensure_scan_checkpoints_table`) are UNTOUCHED per RESEARCH inventory recommendation per D-21"
    - "**D-22 (IN-07)** disposition: RESEARCH C-3 / Pitfall 3 / A2 / Q1 — `quirk/util/targets.py:215-240::projected_probe_count` carries an explicit Phase 65 Risks #4 comment (lines 218-221) requiring `.hosts()` to avoid IPv4 /24 off-by-2. CONTEXT D-22 directs the opposite. Disposition: close IN-07 as `[ ] wont-fix — Phase 65 Risks #4 documented decision`. No code change; audit-row flips to wont-fix-with-rationale via PLAN 77-05"
    - "`python -m compileall quirk/dashboard quirk/cli quirk/db.py` exits 0; all 4 INFO-03 implementation tests pass; existing `quirk/db.py` regression band passes"
  artifacts:
    - path: "quirk/dashboard/api/routes/qramm.py"
      provides: "QrammScoreResponse Pydantic model + MULTIPLIER_* constants (D-16, D-19); D-19 location adjudicated per RESEARCH C-2"
      contains: "MULTIPLIER_MIN"
    - path: "quirk/cli/banner.py"
      provides: "Corrected comment about raw-string _FACES (D-17 per RESEARCH C-5)"
      contains: "raw string"
    - path: "quirk/db.py"
      provides: "Generic _ensure_columns helper (D-21); 8 column-adding helpers consolidated"
      contains: "def _ensure_columns"
    - path: "tests/test_qramm_multiplier_constants.py"
      provides: "AST gate confirming literal 0.8/1.5/0.10/0.20 replaced by MULTIPLIER_* constants (D-19)"
      contains: "MULTIPLIER_MIN"
    - path: "tests/test_db_ensure_columns_generic.py"
      provides: "Generic helper coverage + regression that all migrated tables still receive expected columns (D-21)"
      contains: "_ensure_columns"
  key_links:
    - from: "quirk/dashboard/api/routes/qramm.py"
      to: "Pydantic BaseModel response_model"
      via: "@router.post('/score', response_model=QrammScoreResponse)"
      pattern: "response_model=QrammScoreResponse"
    - from: "quirk/dashboard/api/routes/qramm.py constants"
      to: "factor/clamp call sites at lines 172-176, 192-193, 363-368"
      via: "MULTIPLIER_MIN, MULTIPLIER_MAX, MULTIPLIER_LOW_STEP, MULTIPLIER_HIGH_STEP module-level constants"
      pattern: "MULTIPLIER_(MIN|MAX|LOW_STEP|HIGH_STEP)"
    - from: "quirk/db.py::init_db"
      to: "_ensure_columns(engine, table, COLUMN_TUPLE)"
      via: "8 call sites pass per-feature constants to the single generic helper"
      pattern: "_ensure_columns\\("
---

<objective>
Close INFO-03 — all 7 API/CLI INFO findings (api-cli-core/IN-01..07). Per-row adjudications applied from RESEARCH (3 rows are audit-flip-only; 4 rows take real code edits):

1. **D-16 (IN-01)** — `quirk/dashboard/api/routes/qramm.py` has 8+ `Dict[str, Any]` return sites (lines 76, 280, 358, 416, 424, 573, 577, 652 per RESEARCH primary). Per Discretion D-16 + Deferred Ideas: narrow to the public **scoring response** (`compute_qramm_score` endpoint, line 96+). Define `QrammScoreResponse(BaseModel)` with the actual fields; use `response_model=QrammScoreResponse`; update the function's return type annotation. The remaining `Dict[str, Any]` sites are deferred to v5.0.

2. **D-17 (IN-02)** — RESEARCH C-5 / Pitfall 4: `quirk/cli/banner.py:65` is `_FACES = (r"..."` — raw string. The `\-` is correctly preserved as a literal backslash-dash. The COMMENT at line 64 is misleading. Fix: rewrite line 64 to `# _FACES uses raw string r"..."; backslash-dash is literal text — closes api-cli-core/IN-02 per RESEARCH C-5`. No code change to `_FACES` itself.

3. **D-18 (IN-03)** — RESEARCH C-11 / Pitfall 6 / Q2: site NOT located at HEAD. Task 1 runs a Wave-0 grep sweep across `quirk/cli/`, `quirk/dashboard/api/`, `quirk/qramm/` for any TZ-string fallback. Three possible dispositions:
   - (a) Site found: apply 1-line IANA fix (`"UTC"` instead of legacy abbreviation)
   - (b) No site found: close as `[ ] wont-fix — site not present at HEAD; audit row stale or pre-fixed by earlier phase`
   - (c) Site found but already IANA-correct: close as audit-flip-only
   Disposition is recorded in 77-03-SUMMARY.md and consumed by PLAN 77-05.

4. **D-19 (IN-04)** — RESEARCH C-2 / Pitfall 2: the 4 magic numbers `0.8 / 1.5 / 0.10 / 0.20` live in `quirk/dashboard/api/routes/qramm.py:172-176, 192-193, 363-368` (NOT `quirk/qramm/scoring.py` as CONTEXT D-19 claimed). Extract into module-level constants per RESEARCH Pattern 5:
   ```python
   MULTIPLIER_MIN = 0.8
   MULTIPLIER_MAX = 1.5
   MULTIPLIER_LOW_STEP = 0.10
   MULTIPLIER_HIGH_STEP = 0.20
   ```
   Add a module docstring tying these to Phase 54 / Phase 75 D-06 lineage. Replace all 4 literal-number occurrences at the 3 line ranges.

5. **D-20 (IN-05)** — RESEARCH C-6 / Pitfall 5 / A6: audit row text says `"_make_handler(fp, mt) is the canonical factory pattern — implementation is correct. No-op."` `quirk/dashboard/api/app.py:124-127` is ALREADY correct. No code change. Closes as audit-flip-only via PLAN 77-05.

6. **D-21 (IN-06)** — `quirk/db.py` has 11 `_ensure_*_columns`-family helpers per RESEARCH inventory. Per researcher recommendation, consolidate ONLY the **8 column-adding helpers**; leave the 5 table-creating / FK helpers alone (different pattern). Apply RESEARCH Pattern 3:
   ```python
   def _ensure_columns(engine, table: str, expected: tuple[tuple[str, str], ...]) -> None: ...

   _IDENTITY_COLUMNS = ((...), ...)
   _GCP_COLUMNS = (...)
   # ... 8 module-level constants
   ```

7. **D-22 (IN-07)** — RESEARCH C-3 / Pitfall 3 / A2 / Q1: `quirk/util/targets.py:215-240` carries an explicit Phase 65 Risks #4 comment requiring `.hosts()` over `.num_addresses` to avoid IPv4 /24 off-by-2. CONTEXT D-22 reverses this Phase 65 decision. Disposition: **wont-fix** with rationale citing Phase 65 Risks #4. No code change. PLAN 77-05 flips the audit row.

Per D-32: no new pip deps, no schema migrations. Audit-row flips for IN-01..07 deferred to PLAN 77-05.

Purpose: Tighten the public QRAMM score response shape (D-16); fix the misleading raw-string comment (D-17); finalize the elusive TZ-fallback disposition (D-18); document QRAMM multiplier constants in their actual home (D-19); preserve the deliberate Phase 65 CIDR choice (D-22); consolidate the 8 column-adding helpers into a generic pattern (D-21).
Output: 3 production source edits (qramm.py, banner.py, db.py), 4 new test modules, 0 audit row flips (deferred to 77-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/77-info-code-quality-audit-ledger/77-CONTEXT.md
@.planning/phases/77-info-code-quality-audit-ledger/77-RESEARCH.md

<interfaces>
Existing surfaces (verified at HEAD cf2417a per RESEARCH primary sources):

quirk/dashboard/api/routes/qramm.py:
- Line 96+: `QrammScoreRequest(BaseModel)` already defined (Pydantic in use)
- Line 76, 280, 358, 416, 424, 573, 577, 652: `Dict[str, Any]` return-type sites
- Lines 172-176: `_low_factor = 1.0 - 0.10 * ...` etc. (factor maps using 0.10, 0.20)
- Lines 192-193: clamp to `[0.8, 1.5]`
- Lines 363-368: validation against `[0.8, 1.5]`

quirk/cli/banner.py:
- Line 64: misleading comment about `\-` escape
- Line 65: `_FACES = (r"     @__   ..." ...)` (raw string — already correct)

quirk/db.py — 13 helpers total per RESEARCH; 8 column-adding (consolidate):
| Line | Helper                          |
|------|---------------------------------|
| 70   | _ensure_identity_columns        |
| 92   | _ensure_gcp_columns             |
| 114  | _ensure_v43_columns             |
| 135  | _ensure_email_columns           |
| 155  | _ensure_broker_columns          |
| 176  | _ensure_phase41_columns         |
| 201  | _ensure_phase46_columns         |
| 225  | _ensure_phase54_qramm_columns   |

5 table-creating / FK helpers — DO NOT TOUCH per RESEARCH:
- _ensure_qramm_profiles_fk (243)
- _ensure_qramm_tables (322)
- _ensure_scheduled_tables (336)
- _ensure_scan_jobs_table (346)
- _ensure_scan_checkpoints_table (355)

Pattern 3 from RESEARCH:
```python
def _ensure_columns(engine, table: str, expected: tuple[tuple[str, str], ...]) -> None:
    """Generic SQLite ALTER-TABLE-IF-MISSING. Mirrors prior _ensure_<feature>_columns shape."""
    insp = inspect(engine)
    existing = {c["name"] for c in insp.get_columns(table)}
    with engine.begin() as conn:
        for col, ddl in expected:
            if col not in existing:
                conn.execute(text(f"ALTER TABLE {table} ADD COLUMN {col} {ddl}"))
```

quirk/dashboard/api/app.py:124-127 — `_make_handler(fp, mt)` factory pattern (D-20 no-op).
quirk/util/targets.py:215-240 — `projected_probe_count` with Risks #4 comment (D-22 no-op).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing tests for D-16, D-19, D-21 + Wave-0 disposition checks for D-17/D-18/D-20/D-22</name>
  <files>tests/test_qramm_routes_typed_response.py, tests/test_banner_comment_corrected.py, tests/test_qramm_multiplier_constants.py, tests/test_db_ensure_columns_generic.py</files>
  <read_first>
    - quirk/dashboard/api/routes/qramm.py (lines 60-200, 270-380, 410-440, 560-660)
    - quirk/cli/banner.py (lines 55-80)
    - quirk/db.py (lines 60-270 — all 8 column-adding helpers; lines 240-360 — table-creating helpers DO NOT TOUCH)
    - quirk/dashboard/api/app.py (lines 110-140 — D-20 verification: factory pattern confirmation)
    - quirk/util/targets.py (lines 210-245 — D-22 verification: Risks #4 comment confirmation)
    - .planning/audit-2026-05-08/api-cli-core/REVIEW.md (D-20 audit-row prose for the "No-op" citation)
    - .planning/phases/77-info-code-quality-audit-ledger/77-RESEARCH.md (C-2, C-3, C-5, C-6, C-11, Pitfalls 2/3/4/5/6, Q2)
    - .planning/phases/77-info-code-quality-audit-ledger/77-CONTEXT.md (D-16..D-22)
  </read_first>
  <behavior>
    - D-16 (IN-01): `from quirk.dashboard.api.routes.qramm import QrammScoreResponse; assert issubclass(QrammScoreResponse, BaseModel)`. AST gate: `from fastapi.testclient import TestClient; ...; resp = client.post("/qramm/score", json={...valid input...}); assert resp.status_code == 200; resp.json()` — fields match QrammScoreResponse declared schema. RED because QrammScoreResponse does not yet exist.
    - D-17 (IN-02): `src = pathlib.Path("quirk/cli/banner.py").read_text(); assert "raw string" in src` (comment-correction gate). RED until the comment is rewritten.
    - D-19 (IN-04): `from quirk.dashboard.api.routes.qramm import MULTIPLIER_MIN, MULTIPLIER_MAX, MULTIPLIER_LOW_STEP, MULTIPLIER_HIGH_STEP; assert MULTIPLIER_MIN == 0.8; assert MULTIPLIER_MAX == 1.5; assert MULTIPLIER_LOW_STEP == 0.10; assert MULTIPLIER_HIGH_STEP == 0.20`. AST gate: `import ast; tree = ast.parse(open("quirk/dashboard/api/routes/qramm.py").read())` — count `Constant` nodes with value in {0.8, 1.5, 0.10, 0.20} excluding the 4 module-level constant definitions; assert count == 0 (no remaining literals).
    - D-21 (IN-06): `from quirk.db import _ensure_columns, _IDENTITY_COLUMNS, _GCP_COLUMNS`; `assert callable(_ensure_columns)`. Spy test: create an in-memory SQLite engine, call `init_db`, then `inspect(engine).get_columns("crypto_endpoint")` must include all expected columns from the 8 migrated tuples. AST gate: `import ast; tree = ast.parse(open("quirk/db.py").read())` — count `FunctionDef` nodes whose name matches `_ensure_(identity|gcp|v43|email|broker|phase41|phase46|phase54_qramm)_columns`; assert count == 0 (all 8 removed).

    **Wave-0 disposition tasks (no test files — record in commit body for PLAN 77-05 consumer):**
    - D-18 / Q2: run `grep -rn -E '(tzlocal|UTC.*fallback|datetime\\.tzinfo|pytz)' quirk/cli/ quirk/dashboard/api/ quirk/qramm/ 2>&1 | tee /tmp/77-03-d18-sweep.txt`. If sweep finds a candidate site, document it; if empty, document "no site found" disposition.
    - D-20: `grep -n "_make_handler" quirk/dashboard/api/app.py` — confirm factory pattern present (already correct per audit-row "No-op").
    - D-22: `grep -n "Risks #4" quirk/util/targets.py` — confirm Phase 65 rationale comment is present (basis for wont-fix disposition).
  </behavior>
  <action>
    Create 4 pytest modules per shapes above:

    - `tests/test_qramm_routes_typed_response.py`: import QrammScoreResponse, exercise via FastAPI TestClient (RED state).
    - `tests/test_banner_comment_corrected.py`: string assert (RED state).
    - `tests/test_qramm_multiplier_constants.py`: constants import + AST gate (RED state).
    - `tests/test_db_ensure_columns_generic.py`: generic helper + integration smoke + AST gate (RED state). Use `sqlalchemy.create_engine("sqlite:///:memory:")` for the integration sub-test.

    Run the Wave-0 disposition sweeps and write the results into the Task 1 commit body:
    ```
    D-18 sweep: <grep output> → disposition: <found|wont-fix|already-IANA>
    D-20: _make_handler factory pattern confirmed at app.py:124 → audit-flip-only
    D-22: Risks #4 comment confirmed at targets.py:218 → wont-fix
    ```

    Run `pytest tests/test_qramm_routes_typed_response.py tests/test_banner_comment_corrected.py tests/test_qramm_multiplier_constants.py tests/test_db_ensure_columns_generic.py 2>&1 | tail -25` — confirm all 4 FAIL (RED).

    Commit message: `test(77-03): add failing tests for INFO-03 (D-16, D-17, D-19, D-21) + record D-18/D-20/D-22 dispositions per RESEARCH C-2/C-3/C-5/C-6/C-11`.
  </action>
  <verify>
    <automated>pytest tests/test_qramm_routes_typed_response.py tests/test_banner_comment_corrected.py tests/test_qramm_multiplier_constants.py tests/test_db_ensure_columns_generic.py 2>&1 | tail -25; grep -n "Risks #4" quirk/util/targets.py; grep -n "_make_handler" quirk/dashboard/api/app.py</automated>
  </verify>
  <acceptance_criteria>
    - All 4 test files exist
    - `grep -c "def test_" tests/test_qramm_routes_typed_response.py` >= 1
    - `grep -c "def test_" tests/test_banner_comment_corrected.py` >= 1
    - `grep -c "def test_" tests/test_qramm_multiplier_constants.py` >= 2
    - `grep -c "def test_" tests/test_db_ensure_columns_generic.py` >= 2
    - `pytest tests/test_qramm_routes_typed_response.py tests/test_banner_comment_corrected.py tests/test_qramm_multiplier_constants.py tests/test_db_ensure_columns_generic.py` exits NON-ZERO (RED)
    - `grep -c "Risks #4" quirk/util/targets.py` >= 1 (confirms D-22 wont-fix basis exists)
    - `grep -c "_make_handler" quirk/dashboard/api/app.py` >= 1 (confirms D-20 factory pattern exists)
    - D-18 sweep output written to commit body
  </acceptance_criteria>
  <done>
    Four failing test modules committed; D-18 disposition recorded in commit; D-20/D-22 audit-flip basis verified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement D-16, D-19, D-21 (qramm.py + db.py); D-17 comment fix; D-18 fix-if-applicable</name>
  <files>quirk/dashboard/api/routes/qramm.py, quirk/cli/banner.py, quirk/db.py</files>
  <read_first>
    - All 3 source files at line ranges in `<interfaces>`
    - Task 1 commit body (D-18 disposition)
    - Test files from Task 1 (confirm import names match)
  </read_first>
  <action>
    Apply edits per shapes:

    **D-16 (qramm.py):**
    1. Near the existing `QrammScoreRequest` Pydantic model (line 96+), define:
       ```python
       class QrammScoreResponse(BaseModel):
           """Public QRAMM scoring response (Phase 77 D-16 — narrows from Dict[str, Any]; closes api-cli-core/IN-01)."""
           score: float
           band: str
           multiplier: float
           # ... include the fields actually returned by compute_qramm_score per read_first
       ```
       (Inspect the existing endpoint return-dict keys via read_first; mirror them as model fields.)
    2. At the `@router.post("/score", ...)` decorator, add `response_model=QrammScoreResponse`.
    3. Change the function's return type annotation from `Dict[str, Any]` to `QrammScoreResponse`.
    4. Construct the response as `QrammScoreResponse(**existing_dict)` at the return site.
    5. Leave the 7 other `Dict[str, Any]` sites untouched (deferred to v5.0 per CONTEXT Deferred Ideas).

    **D-17 (banner.py):**
    - Line 64: replace the misleading comment with:
      ```
      # _FACES uses raw string r"..."; backslash-dash is literal text — closes api-cli-core/IN-02 per RESEARCH C-5.
      ```
    - Line 65: `_FACES = (r"..."` UNCHANGED.

    **D-18 (qramm.py or CLI — site-dependent):**
    - If Task 1 sweep found a TZ-string fallback site needing the IANA fix: apply the 1-line change there. Cite `# closes api-cli-core/IN-03`.
    - If no site found: no edit; disposition recorded for PLAN 77-05.

    **D-19 (qramm.py):**
    1. Add module-level constants near the top of the file (after imports, before the first router definition):
       ```python
       # QRAMM multiplier clamp constants — Phase 77 D-19 / RESEARCH C-2 (location adjudicated:
       # constants live here in routes/qramm.py, NOT quirk/qramm/scoring.py).
       # Lineage: Phase 54 introduced the multiplier; Phase 75 D-06 confirmed the [0.8, 1.5] band.
       MULTIPLIER_MIN: float = 0.8
       MULTIPLIER_MAX: float = 1.5
       MULTIPLIER_LOW_STEP: float = 0.10
       MULTIPLIER_HIGH_STEP: float = 0.20
       ```
    2. Replace literals at lines 172-176 (factor maps), 192-193 (clamp), 363-368 (validation) with the named constants. Use grep after edit to confirm zero remaining `0.8`, `1.5`, `0.10`, `0.20` numeric literals outside the constant definitions (excluding docstrings and comments).

    **D-21 (db.py):**
    1. Define the generic helper per RESEARCH Pattern 3:
       ```python
       def _ensure_columns(engine, table: str, expected: tuple[tuple[str, str], ...]) -> None:
           """Generic SQLite ALTER-TABLE-IF-MISSING. Consolidates 8 column-adding helpers in Phase 77 D-21.
           Closes api-cli-core/IN-06.
           """
           insp = inspect(engine)
           existing = {c["name"] for c in insp.get_columns(table)}
           with engine.begin() as conn:
               for col, ddl in expected:
                   if col not in existing:
                       conn.execute(text(f"ALTER TABLE {table} ADD COLUMN {col} {ddl}"))
       ```
    2. For each of the 8 helpers being consolidated:
       - Extract the per-helper column list into a module-level constant: `_IDENTITY_COLUMNS = ((col, ddl), ...)`, etc.
       - Delete the helper function definition
       - Update the call site in `init_db` (or wherever the helpers are called) to `_ensure_columns(engine, table_name, _IDENTITY_COLUMNS)`, etc.
    3. **DO NOT TOUCH** the 5 table-creating / FK helpers (lines 243, 322, 336, 346, 355) — different pattern per RESEARCH.

    Run `python -m compileall quirk/dashboard quirk/cli quirk/db.py` then the 4 targeted pytests. All green.

    Commit message: `feat(77-03): close INFO-03 (D-16, D-17, D-19, D-21) — qramm typed response, banner comment, db generic helper; D-18 disposition recorded`.
  </action>
  <verify>
    <automated>python -m compileall quirk/dashboard quirk/cli quirk/db.py 2>&1 | tail -5; pytest tests/test_qramm_routes_typed_response.py tests/test_banner_comment_corrected.py tests/test_qramm_multiplier_constants.py tests/test_db_ensure_columns_generic.py 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class QrammScoreResponse" quirk/dashboard/api/routes/qramm.py` >= 1
    - `grep -c "response_model=QrammScoreResponse" quirk/dashboard/api/routes/qramm.py` >= 1
    - `grep -c "raw string" quirk/cli/banner.py` >= 1
    - `grep -cE "^MULTIPLIER_MIN" quirk/dashboard/api/routes/qramm.py` == 1
    - `grep -cE "^MULTIPLIER_MAX" quirk/dashboard/api/routes/qramm.py` == 1
    - `grep -cE "^MULTIPLIER_LOW_STEP" quirk/dashboard/api/routes/qramm.py` == 1
    - `grep -cE "^MULTIPLIER_HIGH_STEP" quirk/dashboard/api/routes/qramm.py` == 1
    - `grep -c "def _ensure_columns" quirk/db.py` >= 1
    - `grep -cE "def _ensure_(identity|gcp|v43|email|broker|phase41|phase46|phase54_qramm)_columns" quirk/db.py` == 0
    - `grep -cE "def _ensure_(qramm_profiles_fk|qramm_tables|scheduled_tables|scan_jobs_table|scan_checkpoints_table)" quirk/db.py` == 5 (untouched)
    - `python -m compileall quirk/dashboard quirk/cli quirk/db.py` exits 0
    - `pytest tests/test_qramm_routes_typed_response.py tests/test_banner_comment_corrected.py tests/test_qramm_multiplier_constants.py tests/test_db_ensure_columns_generic.py` exits 0 (GREEN)
  </acceptance_criteria>
  <done>
    All 4 INFO-03 tests pass; 8 db column helpers consolidated; 5 table-creating helpers untouched; QRAMM constants extracted; banner comment corrected.
  </done>
</task>

<task type="auto">
  <name>Task 3: Regression sweep — db + qramm routes + CLI test bands</name>
  <files>tests/test_db_ensure_columns_generic.py</files>
  <read_first>
    - tests/test_db.py (if exists)
    - tests/test_qramm_routes.py (if exists)
  </read_first>
  <action>
    Run the regression band:

    ```
    pytest tests/test_db.py tests/test_qramm_routes.py tests/test_qramm_scoring.py tests/test_banner.py -x
    ```

    (Skip any test file that does not exist — confirm via `ls tests/`.)

    If any failure:
    - D-21 consolidation may break a test that called a deleted helper by name — update the test to use the new shape (`_ensure_columns(engine, table, _IDENTITY_COLUMNS)`).
    - D-19 constant extraction may break a test asserting on literal `0.8` in source — update to `MULTIPLIER_MIN`.
    - D-16 typed response may break a test asserting JSON shape — verify the model fields match the previous dict keys exactly (no field rename).

    Commit message (only if fixes applied): `fix(77-03): adjust regression tests for INFO-03 semantic shifts`. If clean: no commit.
  </action>
  <verify>
    <automated>ls tests/ | grep -E "test_(db|qramm_routes|qramm_scoring|banner)\\.py" 2>&1; pytest $(ls tests/test_db.py tests/test_qramm_routes.py tests/test_qramm_scoring.py tests/test_banner.py 2>/dev/null) -x 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - All extant regression tests pass under `pytest -x`
    - No new pip dependency added
    - `quirk/dashboard/api/app.py` UNCHANGED (D-20 audit-flip-only)
    - `quirk/util/targets.py` UNCHANGED (D-22 wont-fix)
    - `quirk/qramm/scoring.py` UNCHANGED (D-19 fix is in routes/qramm.py per C-2)
  </acceptance_criteria>
  <done>
    DB + QRAMM + CLI regression band green; D-20/D-22 source files confirmed untouched; D-19 confirmed in correct location per C-2.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → QRAMM scoring endpoint | Untrusted request body crosses into Pydantic validation; response shape exposed to consumers |
| Migration ordering → SQLite ALTER TABLE | Existing column inspection drives DDL; misordered execution could double-ALTER on a re-run |
| QRAMM multiplier inputs → clamp constants | Validation against MULTIPLIER_MIN/MAX governs whether a request is accepted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-03-01 | Tampering | QRAMM score response untyped Dict[str, Any] permits silent field drift across versions | mitigate | D-16 — QrammScoreResponse Pydantic model + response_model declaration |
| T-77-03-02 | Tampering | QRAMM multiplier clamp drift (e.g., wrong factor map) — magic numbers hard to audit | mitigate | D-19 — named constants traceable to Phase 54 / Phase 75 D-06 |
| T-77-03-03 | Repudiation | 8 _ensure_*_columns helpers with subtle behavioral drift make migration audit harder | mitigate | D-21 — generic helper + module constants surface the column lists |
| T-77-03-04 | Tampering | Reversing Phase 65 Risks #4 (D-22 as written in CONTEXT) reintroduces IPv4 /24 off-by-2 in probe count, leading to under-probe or over-probe DoS risk | accept | D-22 closes wont-fix preserving Phase 65 decision |
| T-77-03-05 | Information Disclosure | QrammScoreResponse exposes fields previously hidden in untyped dict — surface review needed | accept | Existing fields are operator-facing; no new sensitive fields added |
</threat_model>

<verification>
- 4 INFO-03 targeted tests pass
- Regression band (`pytest tests/test_db.py tests/test_qramm_routes.py tests/test_qramm_scoring.py tests/test_banner.py -x`) passes
- `python -m compileall quirk/dashboard quirk/cli quirk/db.py` exits 0
- `quirk/dashboard/api/app.py` UNCHANGED (D-20)
- `quirk/util/targets.py` UNCHANGED (D-22)
- `quirk/qramm/scoring.py` UNCHANGED (D-19 → correct file is routes/qramm.py per C-2)
- 5 table-creating helpers in `quirk/db.py` UNCHANGED
- D-18 disposition recorded in 77-03-SUMMARY.md and Task 1 commit body
- 0 audit row flips (consolidated by 77-05)
</verification>

<success_criteria>
- 4 new test modules with RED→GREEN history
- 3 production source files edited (qramm.py, banner.py, db.py); optionally a 4th if D-18 site found
- INFO-03 (IN-01..07) dispositions finalized; audit-row flips deferred to PLAN 77-05
- RESEARCH C-2 (D-19 location), C-3 (D-22 wont-fix), C-5 (D-17 comment-only), C-6 (D-20 no-op), C-11 (D-18 site search) adjudications applied
- 8 _ensure_*_columns helpers consolidated; 5 table-creating untouched
- No new pip dependency
</success_criteria>

<output>
Create `.planning/phases/77-info-code-quality-audit-ledger/77-03-SUMMARY.md` documenting: D-16 narrowed to public score response (Discretion + Deferred); D-17 comment fix per C-5; D-18 disposition (site found / wont-fix / already-IANA — record actual result); D-19 constants in routes/qramm.py per C-2 (NOT scoring.py); D-20 audit-flip-only per C-6; D-21 8-helper consolidation, 5-helper preservation; D-22 wont-fix per C-3 / Phase 65 Risks #4; audit-row flips deferred to PLAN 77-05 with explicit disposition for each of IN-01..07.
</output>
