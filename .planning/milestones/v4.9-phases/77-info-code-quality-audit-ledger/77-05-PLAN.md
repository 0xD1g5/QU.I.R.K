---
phase: 77-info-code-quality-audit-ledger
plan: 05
type: execute
wave: 2
depends_on: ["01", "02", "03", "04"]
files_modified:
  - .planning/audit-2026-05-08/AUDIT-TASKS.md
  - tests/test_audit_ledger_zero_open.py
autonomous: true
requirements: [LEDGER-01]
must_haves:
  truths:
    - "All 29 INFO-severity audit rows in `.planning/audit-2026-05-08/AUDIT-TASKS.md` are flipped from `| — | [ ] open |` to their final Phase 77 disposition: 27 rows `| Phase 77 | [x] closed |` and 2 rows with adjudicated alternative dispositions (api-cli-core/IN-05 audit-flip-only no-op per RESEARCH C-6; api-cli-core/IN-07 `[ ] wont-fix` with Phase 65 Risks #4 rationale per RESEARCH C-3). D-18 disposition consumed from 77-03-SUMMARY.md (may be closed/wont-fix/already-fixed)"
    - "The 2 pre-existing bare-disposition rows (scanners-cloud/CR-01 wont-fix at line 81, scanners-cloud/CR-03 deferred-v4.9 at line 83) gain inline rationale matching the existing `[x] closed`-row format per D-30 + RESEARCH C-4 / Q5 (NOT 4 rows as CONTEXT claimed — 2 verified at HEAD)"
    - "A new pytest module `tests/test_audit_ledger_zero_open.py` exists per D-31 + RESEARCH code example; it greps for `^\\| .* \\[ \\] open\\s*\\|` in AUDIT-TASKS.md and asserts the match count is exactly 0 (the v4.9 milestone-completion invariant)"
    - "`pytest tests/test_audit_ledger_zero_open.py -x` passes (zero bare-open rows remain)"
    - "Final invariant: `grep -cE \"^\\| .* \\[ \\] open\\s*\\|\" .planning/audit-2026-05-08/AUDIT-TASKS.md` returns `0`"
    - "Full Python suite `pytest -x` passes (regression gate)"
    - "Full Vitest suite `cd src/dashboard && npm test` passes (regression gate)"
    - "`cd src/dashboard && npm run build` exits 0 (build gate carried from 77-04)"
  artifacts:
    - path: ".planning/audit-2026-05-08/AUDIT-TASKS.md"
      provides: "29 INFO row flips + 2 bare-row rationale additions; final state has zero bare-open rows"
      contains: "Phase 77"
    - path: "tests/test_audit_ledger_zero_open.py"
      provides: "CI gate enforcing zero bare-open rows in perpetuity (D-31 milestone invariant)"
      contains: "bare-open"
  key_links:
    - from: "tests/test_audit_ledger_zero_open.py"
      to: ".planning/audit-2026-05-08/AUDIT-TASKS.md"
      via: "regex r'^\\| .* \\[ \\] open\\s*\\|' applied via re.MULTILINE"
      pattern: "AUDIT-TASKS\\.md"
    - from: "Phase 77 plans 01-04"
      to: "AUDIT-TASKS.md row flips"
      via: "consolidated row dispositions from 77-01-SUMMARY..77-04-SUMMARY.md"
      pattern: "Phase 77 \\| \\[x\\] closed"
---

<objective>
Close LEDGER-01 — the **v4.9 milestone-completion gate**. Three deliverables:

1. **D-30 (bare-row rationale):** RESEARCH C-4 / A7 / Q5 — CONTEXT D-30 claimed 4 bare-disposition rows; HEAD has exactly **2** (verified):
   - `scanners-cloud/CR-01` (line 81 — `[ ] wont-fix`) — migration_planner.py stub
   - `scanners-cloud/CR-03` (line 83 — `[ ] deferred-v4.9`) — K8s scan_k8s_targets None-cred path

   Both already carry prose rationale in following lines (84-91 per RESEARCH primary). Per RESEARCH code-example block (D-30 inline-rationale format), append inline rationale into the row's disposition cell so the cell mirrors the `[x] closed`-row format. Template text in `<interfaces>` below.

2. **29 INFO row flips:** Consolidate audit-row dispositions for all 29 INFO rows (scanners-protocol/IN-01..06, cbom-intel-reports/IN-01..09, api-cli-core/IN-01..07, react-frontend/IN-01..07) based on the implementation results recorded in the four upstream SUMMARYs (77-01..77-04). Final mapping:

   | Audit rows | Disposition | Source SUMMARY |
   |-----------|-------------|--------|
   | scanners-protocol/IN-01..06 (6 rows) | `Phase 77 \| [x] closed` | 77-01-SUMMARY |
   | cbom-intel-reports/IN-01..06, IN-08, IN-09 (8 rows) | `Phase 77 \| [x] closed` | 77-02-SUMMARY |
   | cbom-intel-reports/IN-07 (D-13) | `Phase 77 \| [x] closed` (audit-flip-only with C-7 mutation evidence OR with real dead-branch removal — consume final form from 77-02-SUMMARY) | 77-02-SUMMARY (RESEARCH C-7) |
   | api-cli-core/IN-01 (D-16) | `Phase 77 \| [x] closed` | 77-03-SUMMARY |
   | api-cli-core/IN-02 (D-17) | `Phase 77 \| [x] closed` (comment-fix-only per C-5) | 77-03-SUMMARY |
   | api-cli-core/IN-03 (D-18) | Consume from 77-03-SUMMARY: closed if site found+fixed, OR `[ ] wont-fix — site not present at HEAD` per C-11/Q2 | 77-03-SUMMARY |
   | api-cli-core/IN-04 (D-19) | `Phase 77 \| [x] closed` (constants in routes/qramm.py per C-2) | 77-03-SUMMARY |
   | api-cli-core/IN-05 (D-20) | `Phase 77 \| [x] closed` with audit-flip-only "No-op" rationale per C-6 | 77-03-SUMMARY |
   | api-cli-core/IN-06 (D-21) | `Phase 77 \| [x] closed` | 77-03-SUMMARY |
   | api-cli-core/IN-07 (D-22) | `[ ] wont-fix — Phase 65 Risks #4 documented decision` per C-3 / Q1 | 77-03-SUMMARY |
   | react-frontend/IN-01..07 (7 rows) | `Phase 77 \| [x] closed` | 77-04-SUMMARY |

   **Self-check before final commit:** every IN-NN row appears EXACTLY ONCE in the final mapping. The CI gate test catches any miss.

3. **D-31 CI gate:** `tests/test_audit_ledger_zero_open.py` per RESEARCH code example. Imports `re`, reads AUDIT-TASKS.md via `pathlib.Path`, applies regex `r"^\\|\\s.*\\[ \\] open\\s*\\|"` with `re.MULTILINE`, asserts the match list is empty. v4.9 milestone gate + forward regression protection.

Per D-32: no schema migrations, no feature work, no new pip deps.

**Plan dependency:** wave 2, depends on `["01", "02", "03", "04"]` because row flips reference the SUMMARYs of those plans.

Purpose: Close the v4.9 milestone gate (zero bare-open rows in AUDIT-TASKS.md) + install a CI gate that prevents future regression. Land the 2 bare-row inline rationales per D-30. Carry D-18, D-20, D-22 final dispositions from the four upstream plan summaries.
Output: 1 .md update (~31 row updates: 29 flips + 2 rationale additions), 1 new pytest module, 1 invariant green, 0 new pip deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/77-info-code-quality-audit-ledger/77-CONTEXT.md
@.planning/phases/77-info-code-quality-audit-ledger/77-RESEARCH.md
@.planning/phases/77-info-code-quality-audit-ledger/77-01-SUMMARY.md
@.planning/phases/77-info-code-quality-audit-ledger/77-02-SUMMARY.md
@.planning/phases/77-info-code-quality-audit-ledger/77-03-SUMMARY.md
@.planning/phases/77-info-code-quality-audit-ledger/77-04-SUMMARY.md
@.planning/audit-2026-05-08/AUDIT-TASKS.md

<interfaces>
AUDIT-TASKS.md row format (existing `[x] closed` example):
```
| <audit-id> | <severity> | <description> | Phase NN | [x] closed |
```

Lines to flip (verified at HEAD per RESEARCH primary; line numbers may shift after edits):
- 70-75: scanners-protocol/IN-01..06 (6 rows)
- 81: scanners-cloud/CR-01 (bare wont-fix — D-30 rationale append)
- 83: scanners-cloud/CR-03 (bare deferred-v4.9 — D-30 rationale append)
- 163-171: cbom-intel-reports/IN-01..09 (9 rows)
- 203-209: api-cli-core/IN-01..07 (7 rows)
- 235-241: react-frontend/IN-01..07 (7 rows)

D-31 CI gate code (from RESEARCH code-example block):
```python
# tests/test_audit_ledger_zero_open.py
"""Phase 77 LEDGER-01: AUDIT-TASKS.md must have zero bare-open rows at milestone close."""
from pathlib import Path
import re

LEDGER = Path(__file__).resolve().parent.parent / ".planning" / "audit-2026-05-08" / "AUDIT-TASKS.md"
_OPEN_RE = re.compile(r"^\|\s.*\[ \] open\s*\|", re.MULTILINE)


def test_audit_ledger_has_zero_bare_open_rows() -> None:
    text = LEDGER.read_text(encoding="utf-8")
    matches = _OPEN_RE.findall(text)
    assert not matches, (
        f"Audit ledger has {len(matches)} bare-open row(s); v4.9 milestone gate requires zero. "
        f"Each finding must be [x] closed, [ ] deferred-vX.Y with rationale, or [ ] wont-fix with rationale."
    )
```

D-30 inline-rationale templates (from RESEARCH code-example block):

CR-01 (wont-fix):
> `[ ] wont-fix — \`quirk/cbom/migration_planner.py\` is a 16-line stub; v4.x decision is that migration-planning intelligence is the consumer's responsibility (out-of-scope for cryptographic inventory scope). Re-evaluate in v5.0 if a typed migration pipeline is needed.`

CR-03 (deferred-v4.9):
> `[ ] deferred-v4.9 — Phase 29 documented the credentialed AKS path; the None-cred fallback is exercised in production only when running unauthenticated against an AKS cluster; the existing graceful-degradation path returns an empty list (correct). v4.9 will add an explicit \`K8S-04\` log for the None-cred branch.`

api-cli-core/IN-07 (D-22 per C-3 / Q1):
> `[ ] wont-fix — Phase 65 Risks #4 documented decision; \`.num_addresses\` would re-introduce IPv4 /24 off-by-2 (see comment at \`quirk/util/targets.py:218-221\`). Re-evaluate only if Phase 65 Risks #4 is itself reversed.`

api-cli-core/IN-05 (D-20 per C-6) — audit-flip-only:
> `Phase 77 | [x] closed — audit-flip-only per audit-row prose ("_make_handler(fp, mt) is the canonical factory pattern — implementation is correct. No-op.") confirmed at quirk/dashboard/api/app.py:124-127 per RESEARCH C-6.`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — create D-31 CI gate test (failing pre-flip)</name>
  <files>tests/test_audit_ledger_zero_open.py</files>
  <read_first>
    - .planning/audit-2026-05-08/AUDIT-TASKS.md (confirm 29 bare-open rows exist at this point in time, before Task 2)
    - .planning/phases/77-info-code-quality-audit-ledger/77-RESEARCH.md (D-31 code example)
  </read_first>
  <action>
    Create `tests/test_audit_ledger_zero_open.py` exactly per the RESEARCH code-example block. This test MUST currently fail because 29 bare-open rows still exist at this point in the wave-2 sequence (Plans 01-04 deferred their row flips here by design).

    Run `pytest tests/test_audit_ledger_zero_open.py -x 2>&1 | tail -10` and confirm:
    - Test FAILS with assertion message stating "29 bare-open row(s)" (RED state).
    - The regex correctly counts the rows and gives Task 2 a concrete target.

    Baseline grep:
    ```
    grep -cE "^\\| .* \\[ \\] open\\s*\\|" .planning/audit-2026-05-08/AUDIT-TASKS.md
    ```
    Expect 29.

    Commit message: `test(77-05): add LEDGER-01 zero-bare-open CI gate (D-31) — currently RED with 29 open rows`.
  </action>
  <verify>
    <automated>pytest tests/test_audit_ledger_zero_open.py -x 2>&1 | tail -10; grep -cE "^\\| .* \\[ \\] open\\s*\\|" .planning/audit-2026-05-08/AUDIT-TASKS.md</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_audit_ledger_zero_open.py` exists
    - `grep -c "def test_audit_ledger_has_zero_bare_open_rows" tests/test_audit_ledger_zero_open.py` == 1
    - `grep -c "re.MULTILINE" tests/test_audit_ledger_zero_open.py` >= 1
    - `pytest tests/test_audit_ledger_zero_open.py -x` exits NON-ZERO (RED) with message mentioning bare-open row count
    - `grep -cE "^\\| .* \\[ \\] open\\s*\\|" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 29 (baseline confirmed before Task 2)
  </acceptance_criteria>
  <done>
    CI gate test committed in RED state; 29 bare-open rows confirmed as the work target for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Consolidate 29 INFO row flips + 2 bare-row rationale additions (LEDGER-01 core)</name>
  <files>.planning/audit-2026-05-08/AUDIT-TASKS.md</files>
  <read_first>
    - .planning/audit-2026-05-08/AUDIT-TASKS.md (lines 60-90 protocol+cloud; 155-175 cbom-intel; 195-215 api-cli; 230-245 react-frontend)
    - .planning/phases/77-info-code-quality-audit-ledger/77-01-SUMMARY.md (consume IN-01..06 dispositions)
    - .planning/phases/77-info-code-quality-audit-ledger/77-02-SUMMARY.md (consume IN-01..09 dispositions, including D-13 C-7 outcome)
    - .planning/phases/77-info-code-quality-audit-ledger/77-03-SUMMARY.md (consume IN-01..07 dispositions, including D-18 Wave 0 disposition)
    - .planning/phases/77-info-code-quality-audit-ledger/77-04-SUMMARY.md (consume IN-01..07 dispositions)
  </read_first>
  <action>
    Apply row flips per the disposition table in `<objective>`. Implementation steps:

    **Step 1 — 29 INFO row flips.**

    For each of the 29 rows, change the disposition cell from `| — | [ ] open |` to:

    - **Default (most rows):** `| Phase 77 | [x] closed |`

    - **Adjudicated exceptions** (consult upstream SUMMARYs first):
      - **api-cli-core/IN-03 (D-18):** flip per 77-03-SUMMARY.md. If "site found and fixed": `| Phase 77 | [x] closed |`. If "site not found": `| Phase 77 | [ ] wont-fix — site not present at HEAD; audit row stale or pre-fixed by earlier phase. Forward-protect via test_audit_ledger_zero_open.py invariant. |`.
      - **api-cli-core/IN-05 (D-20):** `| Phase 77 | [x] closed — audit-flip-only per audit-row prose ("_make_handler(fp, mt) is the canonical factory pattern — implementation is correct. No-op.") confirmed at quirk/dashboard/api/app.py:124-127 per RESEARCH C-6. |`
      - **api-cli-core/IN-07 (D-22):** `| Phase 77 | [ ] wont-fix — Phase 65 Risks #4 documented decision; .num_addresses would re-introduce IPv4 /24 off-by-2 (see comment at quirk/util/targets.py:218-221). Re-evaluate only if Phase 65 Risks #4 is itself reversed. |`
      - **cbom-intel-reports/IN-07 (D-13):** flip per 77-02-SUMMARY.md. If C-7 confirmed both branches reachable: `| Phase 77 | [x] closed — audit-flip-only; mutation test in tests/test_html_renderer_roadmap_section.py proves both timeframe == tf and phase == tf branches reachable per RESEARCH C-7. |`. If a real dead branch was removed: `| Phase 77 | [x] closed |`.

    **Step 2 — 2 bare-row rationale additions (D-30).**

    - **Line 81 (`scanners-cloud/CR-01`):** Replace `| — | [ ] wont-fix |` with the RESEARCH-template wont-fix rationale (full text in `<interfaces>` block). Keep the Phase column blank (`—`) — CR-01 is NOT closed by Phase 77; only the bare-disposition cell gains inline rationale.
    - **Line 83 (`scanners-cloud/CR-03`):** Replace `| — | [ ] deferred-v4.9 |` with the RESEARCH-template deferred-v4.9 rationale. Keep the Phase column blank (`—`).

    (Per RESEARCH A7: the test regex `^\\| .* \\[ \\] open\\s*\\|` matches only `[ ] open` — `[ ] wont-fix` and `[ ] deferred-v4.9` rows do NOT count toward the bare-open invariant. The work here is the inline-rationale upgrade per D-30.)

    **Step 3 — Self-checks before commit.**

    Run all invariant greps:
    ```
    grep -cE "^\\| .* \\[ \\] open\\s*\\|" .planning/audit-2026-05-08/AUDIT-TASKS.md          # MUST be 0
    grep -cE "scanners-protocol/IN-0[1-6].*Phase 77.*\\[x\\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md  # MUST be 6
    grep -cE "cbom-intel-reports/IN-0[1-9].*Phase 77" .planning/audit-2026-05-08/AUDIT-TASKS.md                 # MUST be 9
    grep -cE "api-cli-core/IN-0[1-7].*Phase 77" .planning/audit-2026-05-08/AUDIT-TASKS.md                       # MUST be 7
    grep -cE "react-frontend/IN-0[1-7].*Phase 77" .planning/audit-2026-05-08/AUDIT-TASKS.md                     # MUST be 7
    grep -cE "scanners-cloud/CR-01.*16-line stub" .planning/audit-2026-05-08/AUDIT-TASKS.md                     # MUST be 1
    grep -cE "scanners-cloud/CR-03.*K8S-04" .planning/audit-2026-05-08/AUDIT-TASKS.md                           # MUST be 1
    grep -cE "api-cli-core/IN-07.*Phase 65 Risks #4" .planning/audit-2026-05-08/AUDIT-TASKS.md                  # MUST be 1
    ```
    If any count is off, fix the row(s) before commit. Do NOT proceed to Task 3 with mismatched counts.

    **Step 4 — Run the CI gate.** It MUST now PASS (GREEN):
    ```
    pytest tests/test_audit_ledger_zero_open.py -x
    ```

    Commit message: `docs(77-05): close LEDGER-01 — flip 29 INFO rows + add D-30 inline rationale to CR-01/CR-03 (zero bare-open invariant achieved)`.
  </action>
  <verify>
    <automated>grep -cE "^\| .* \[ \] open\s*\|" .planning/audit-2026-05-08/AUDIT-TASKS.md; grep -cE "scanners-protocol/IN-0[1-6].*Phase 77" .planning/audit-2026-05-08/AUDIT-TASKS.md; grep -cE "cbom-intel-reports/IN-0[1-9].*Phase 77" .planning/audit-2026-05-08/AUDIT-TASKS.md; grep -cE "api-cli-core/IN-0[1-7].*Phase 77" .planning/audit-2026-05-08/AUDIT-TASKS.md; grep -cE "react-frontend/IN-0[1-7].*Phase 77" .planning/audit-2026-05-08/AUDIT-TASKS.md; grep -cE "scanners-cloud/CR-01.*16-line stub" .planning/audit-2026-05-08/AUDIT-TASKS.md; grep -cE "scanners-cloud/CR-03.*K8S-04" .planning/audit-2026-05-08/AUDIT-TASKS.md; pytest tests/test_audit_ledger_zero_open.py -x 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\\| .* \\[ \\] open\\s*\\|" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 0 (the v4.9 milestone-gate invariant)
    - `grep -cE "scanners-protocol/IN-0[1-6].*Phase 77" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 6
    - `grep -cE "cbom-intel-reports/IN-0[1-9].*Phase 77" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 9
    - `grep -cE "api-cli-core/IN-0[1-7].*Phase 77" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 7
    - `grep -cE "react-frontend/IN-0[1-7].*Phase 77" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 7
    - `grep -cE "scanners-cloud/CR-01.*16-line stub" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 1
    - `grep -cE "scanners-cloud/CR-03.*K8S-04" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 1
    - `grep -cE "api-cli-core/IN-07.*Phase 65 Risks #4" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 1
    - `pytest tests/test_audit_ledger_zero_open.py -x` exits 0 (GREEN — the test that was RED in Task 1 now passes)
  </acceptance_criteria>
  <done>
    AUDIT-TASKS.md has zero bare-open rows; 29 INFO rows flipped per upstream SUMMARYs; 2 bare-disposition rows carry inline rationale; CI gate test passes; v4.9 milestone gate satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 3: Phase-gate regression sweep — full Python suite + full Vitest + npm build</name>
  <files>tests/test_audit_ledger_zero_open.py</files>
  <read_first>
    - Output of `pytest -x 2>&1 | tail -40`
    - Output of `cd src/dashboard && npm test 2>&1 | tail -40`
    - Output of `cd src/dashboard && npm run build 2>&1 | tail -10`
  </read_first>
  <action>
    This is the **v4.9 phase gate**. All three regression suites must be green together:

    1. **Full Python suite:**
       ```
       pytest -x
       ```
       Includes every test added by Plans 01-04 (INFO-01..04) and the new CI gate from Task 1. Any failure here blocks the phase wrap.

    2. **Full Vitest suite:**
       ```
       cd src/dashboard && npm test
       ```
       Includes Phase 76 + Phase 77 frontend tests.

    3. **npm build:**
       ```
       cd src/dashboard && npm run build
       ```
       Already validated in 77-04 Task 3; re-run as the phase-gate confirmation.

    4. **Invariant re-grep** (defense in depth):
       ```
       grep -cE "^\\| .* \\[ \\] open\\s*\\|" .planning/audit-2026-05-08/AUDIT-TASKS.md
       ```
       MUST return `0`.

    5. **CI gate re-run:**
       ```
       pytest tests/test_audit_ledger_zero_open.py -x
       ```
       MUST exit 0.

    No source files are expected to change in this task — it is a verification gate. If any suite fails:
    - Diagnose the failure
    - Trace it back to which plan (01-04) introduced the breaking change
    - Fix in this task's commit (do NOT roll back upstream plans)
    - Re-run the full gate before committing

    Commit message (only if fixes applied): `fix(77-05): close phase-gate regressions surfaced during LEDGER-01 wrap`. If all gates green on first try: no commit (verification only).
  </action>
  <verify>
    <automated>pytest -x 2>&1 | tail -20; cd src/dashboard && npm test 2>&1 | tail -20; cd src/dashboard && npm run build 2>&1 | tail -10; grep -cE "^\| .* \[ \] open\s*\|" /Volumes/Digs-1TB/Development/quantum-apps/QUIRK/.planning/audit-2026-05-08/AUDIT-TASKS.md; pytest /Volumes/Digs-1TB/Development/quantum-apps/QUIRK/tests/test_audit_ledger_zero_open.py -x 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -x` exits 0 (full Python suite green)
    - `cd src/dashboard && npm test` exits 0 (full Vitest suite green)
    - `cd src/dashboard && npm run build` exits 0
    - `grep -cE "^\\| .* \\[ \\] open\\s*\\|" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 0
    - `pytest tests/test_audit_ledger_zero_open.py -x` exits 0
    - No new pip / npm dependencies (D-32)
    - v4.9 milestone gate satisfied: all 5 phase-77 SCs from ROADMAP.md verified
  </acceptance_criteria>
  <done>
    Full regression sweep green; v4.9 milestone-completion gate achieved (SC-5 zero-bare-open invariant locked in); ready for phase wrap.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Future planner / developer → AUDIT-TASKS.md | A future commit could re-introduce a bare-open row (intentional or accidental); CI gate forward-protects |
| Test fixture path → AUDIT-TASKS.md location | Test resolves the ledger via `__file__` parent.parent; project-relocation would break it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-05-01 | Tampering | A future audit row added with bare `[ ] open` would bypass milestone hygiene | mitigate | D-31 CI gate test catches it on every pytest run (regression guard in perpetuity) |
| T-77-05-02 | Repudiation | Bare `[ ] wont-fix` / `[ ] deferred` rows carry no inline rationale → auditor cannot reconstruct decision later | mitigate | D-30 inline-rationale upgrade for CR-01 + CR-03 + IN-07 |
| T-77-05-03 | Information Disclosure | Wont-fix / deferred rationale text may leak implementation detail | accept | All rationale text references public file paths / phase numbers — no secrets |
| T-77-05-04 | Tampering | Audit-row mass edit could silently overwrite a previously-closed row | mitigate | Task 2 Step 3 self-check greps verify per-subsystem counts BEFORE commit; CI gate verifies AFTER |
</threat_model>

<verification>
- `tests/test_audit_ledger_zero_open.py` exists and passes
- `grep -cE "^\| .* \[ \] open\s*\|" .planning/audit-2026-05-08/AUDIT-TASKS.md` returns `0`
- All 29 INFO rows show a Phase 77 disposition (closed OR wont-fix with rationale)
- 2 bare-disposition rows (CR-01, CR-03) carry inline rationale per D-30
- `pytest -x` passes (full Python regression)
- `cd src/dashboard && npm test` passes (full Vitest regression)
- `cd src/dashboard && npm run build` exits 0
- D-32 honored: no new pip / npm deps
- v4.9 milestone-completion gate (SC-5 ROADMAP) satisfied
</verification>

<success_criteria>
- 1 new pytest module with RED→GREEN history (the gate test itself transitions states across Task 1 → Task 2)
- AUDIT-TASKS.md: 29 INFO row flips + 2 bare-row rationale additions
- D-31 CI gate locked in for the project lifetime
- All 5 Phase 77 ROADMAP success criteria verifiable
- No new pip / npm dependencies
</success_criteria>

<output>
Create `.planning/phases/77-info-code-quality-audit-ledger/77-05-SUMMARY.md` documenting:
- Final disposition table for all 29 INFO rows (with adjudication source: 77-01..77-04 SUMMARYs)
- 2 D-30 bare-row rationales landed (CR-01, CR-03 per RESEARCH C-4 / Q5 — count was 2 not 4 as CONTEXT claimed)
- D-31 CI gate installed and proven green
- D-18 final disposition (consumed from 77-03-SUMMARY)
- D-20 / D-22 wont-fix / no-op rationales (per RESEARCH C-3 / C-6)
- Full Python + Vitest + npm-build regression sweep all green
- v4.9 milestone-completion gate (zero bare-open rows) ACHIEVED
- Forward-protection mechanism (CI gate runs on every pytest invocation)

Then proceed to the CLAUDE.md mandatory phase completion steps (Obsidian phase note, UAT-SERIES.md update, Obsidian sync, commit).
</output>
