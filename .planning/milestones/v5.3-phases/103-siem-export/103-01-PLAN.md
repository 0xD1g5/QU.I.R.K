---
phase: 103-siem-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/siem/__init__.py
  - quirk/siem/formatter.py
  - tests/test_siem_cef.py
  - tests/test_siem_payload_whitelist.py
autonomous: true
requirements: [SIEM-02]

must_haves:
  truths:
    - "Every finding maps to a CEF:0 line with exactly 8 pipe-delimited header fields"
    - "Severity maps CRITICAL->10, HIGH->8, MEDIUM->5, LOW->3"
    - "Header escapes backslash and pipe; extension escapes backslash, equals, and newlines; backslash is escaped first"
    - "cert PEM, SANs, and compliance list never appear in the CEF payload; host and port do appear"
    - "signature falls back to a slugified title when category/id is absent"
  artifacts:
    - path: "quirk/siem/formatter.py"
      provides: "CEF escaping, severity mapping, to_cef_finding whitelist, build_cef_event"
      min_lines: 60
      contains: "def build_cef_event"
    - path: "quirk/siem/__init__.py"
      provides: "siem package marker"
    - path: "tests/test_siem_cef.py"
      provides: "CEF format/escaping/severity/fallback tests"
    - path: "tests/test_siem_payload_whitelist.py"
      provides: "cert/compliance exclusion + host/port inclusion tests"
  key_links:
    - from: "quirk/siem/formatter.py"
      to: "build_cef_event consumes to_cef_finding output"
      via: "explicit named .get() field extraction (ISEC-03)"
      pattern: "to_cef_finding"
---

<objective>
Build the pure CEF transformation layer: the per-finding whitelist (`to_cef_finding`), the two CEF escaping functions, the severity map, and `build_cef_event`. No I/O — pure string transformation, fully unit-testable.

Purpose: Correct, injection-safe CEF formatting is the heart of SIEM-02 and the most error-prone surface (escaping order, severity, payload exfiltration). Isolating it lets Plan 03's dispatcher consume a stable contract.
Output: `quirk/siem/__init__.py`, `quirk/siem/formatter.py`, and two test files (Wave 0 scaffolds for this plan's behavior).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-siem-export/103-CONTEXT.md
@.planning/phases/103-siem-export/103-RESEARCH.md
@.planning/phases/103-siem-export/103-PATTERNS.md

<interfaces>
<!-- Contract this plan publishes for Plan 03 to consume -->
quirk/siem/formatter.py exports:
  to_cef_finding(finding: dict) -> dict      # ISEC-03 explicit whitelist
  build_cef_event(finding: dict, version: str) -> str   # full CEF:0 line (no <PRI> prefix)
  _CEF_SEVERITY: dict[str,int]               # CRITICAL=10, HIGH=8, MEDIUM=5, LOW=3

Confirmed findings dict keys (from output/findings-20260522-011356.json):
  severity, host, port, title, description, recommendation, compliance
  NO `category` key — signature must fall back to slugified title.
  cert_pem/cert_sans/cert_subject/cert_issuer are NOT in findings json (live on CryptoEndpoint DB row).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing CEF formatter + whitelist tests (Wave 0)</name>
  <files>tests/test_siem_cef.py, tests/test_siem_payload_whitelist.py</files>
  <read_first>
    - .planning/phases/103-siem-export/103-PATTERNS.md (Test Pattern Assignments: tests/test_siem_formatter.py + tests/test_siem_payload_whitelist.py, lines ~418-441)
    - tests/test_notify_payload_whitelist.py (analog structure: ALLOWED/FORBIDDEN frozenset assertions)
    - .planning/phases/103-siem-export/103-RESEARCH.md (Pattern 1 CEF structure ~L156-224; Pattern 4 whitelist ~L340-390)
  </read_first>
  <behavior>
    test_siem_cef.py:
    - test_cef_header_field_count: build_cef_event(finding, "1.0.0").split("|") yields exactly 8 parts
    - test_severity_mapping: CRITICAL->10, HIGH->8, MEDIUM->5, LOW->3; unknown severity -> 3
    - test_header_escaping: a title containing "|" becomes "\|" and "\" becomes "\\"; "=" in a header field is NOT escaped
    - test_extension_escaping: an extension value with "=" becomes "\=", "\" becomes "\\", "\n"/"\r\n"/"\r" become literal "\n"
    - test_escaping_backslash_first: input "\|" yields "\\|" (not "\\\|") in a header field
    - test_category_fallback_when_absent: finding with no category/id key -> signature derived from slugified title (lowercase, non-alnum -> hyphen)
    test_siem_payload_whitelist.py:
    - test_allowed_fields_present: to_cef_finding output contains severity, host, port, title, category, description fields
    - test_host_port_present: host and port survive the whitelist
    - test_cert_pem_excluded: a finding dict salted with cert_pem/cert_sans/cert_subject/private_key -> none appear in to_cef_finding output OR in build_cef_event(...) string
    - test_compliance_excluded: a finding with a compliance list -> "compliance" key absent from to_cef_finding output and the substring not in the CEF line
  </behavior>
  <action>
    Create both test files. Define module-level ALLOWED_FIELDS and FORBIDDEN_FIELDS frozensets in the whitelist test mirroring tests/test_notify_payload_whitelist.py. Import from quirk.siem.formatter. Use the socketserver/DB-free pure-function style — no network, no DB. Tests MUST fail at this point (formatter.py does not yet exist). Assert against the literal escaped strings, not via re-parsing.
  </action>
  <verify>
    <automated>python -m pytest tests/test_siem_cef.py tests/test_siem_payload_whitelist.py -q 2>&1 | grep -E "error|ModuleNotFoundError|failed" </automated>
  </verify>
  <done>Both test files exist and fail with ModuleNotFoundError / collection error because quirk.siem.formatter is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement formatter.py to pass the tests (GREEN)</name>
  <files>quirk/siem/__init__.py, quirk/siem/formatter.py</files>
  <read_first>
    - .planning/phases/103-siem-export/103-RESEARCH.md (Pattern 1 build_cef_event ~L206-222; Complete CEF Escaping ~L525-541; to_cef_finding ~L555-567)
    - .planning/phases/103-siem-export/103-PATTERNS.md (formatter.py assignment ~L89-142; Critical Gotchas 1-3, 6)
    - quirk/notify/payload.py (to_integration_payload explicit-extraction discipline, ISEC-03 comment)
  </read_first>
  <action>
    Create empty quirk/siem/__init__.py. Create quirk/siem/formatter.py starting with `from __future__ import annotations`. Implement:
    - `_cef_escape_header(value: str) -> str`: replace "\\" with "\\\\" FIRST, then "|" with "\\|".
    - `_cef_escape_extension(value: str) -> str`: replace "\\" with "\\\\" FIRST, then "=" with "\\=", then "\r\n"/"\r"/"\n" -> "\\n".
    - `_slugify(title: str) -> str`: lowercase, non-alphanumeric runs -> single hyphen, strip leading/trailing hyphens; empty -> "unknown".
    - `_CEF_SEVERITY = {"CRITICAL": 10, "HIGH": 8, "MEDIUM": 5, "LOW": 3}`.
    - `to_cef_finding(finding: dict) -> dict`: EXPLICIT named .get() extraction ONLY (no dict comprehension / exclusion list). Return severity (upper), host, port, title, category (= finding.get("category") or finding.get("id") or _slugify(title)), description[:256], recommendation[:256]. NEVER pass through cert/compliance/key fields.
    - `build_cef_event(finding: dict, version: str) -> str`: call to_cef_finding first; map severity via _CEF_SEVERITY (default 3); header = `CEF:0|QUIRK|scanner|{escaped version}|{escaped signature}|{escaped name}|{sev}`; extension `dhost={ext host} dpt={port} cs1={ext category} cs1Label=Category cs2={ext desc} cs2Label=EvidenceSummary msg={ext recommendation or desc}`; return `f"{header}|{ext}"`. NO syslog <PRI> prefix here — that is transport's job. No fenced inline implementations beyond these identifiers.
  </action>
  <verify>
    <automated>python -m pytest tests/test_siem_cef.py tests/test_siem_payload_whitelist.py -q && python -m compileall quirk/siem/formatter.py -q</automated>
  </verify>
  <done>All tests in both files pass; build_cef_event returns a valid 8-field CEF line; to_cef_finding provably excludes cert/compliance/key material; compileall clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| finding dict -> CEF string | Untrusted scanner-derived content (titles, descriptions) crosses into a CEF line consumed by an external SIEM parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01 | Tampering | build_cef_event / _cef_escape_* | mitigate | Two-function escaping (header: `\`,`|`; extension: `\`,`=`,newline); backslash escaped first. Tested by test_header_escaping, test_extension_escaping, test_escaping_backslash_first — prevents CEF injection / event forgery / log injection |
| T-103-02 | Information Disclosure | to_cef_finding | mitigate | Explicit named .get() whitelist (no exclusion-list comprehension). cert PEM/SANs/private-key/compliance never extracted. Tested by test_cert_pem_excluded, test_compliance_excluded |
| T-103-03 | Denial of Service | build_cef_event | accept | description/recommendation truncated to 256 chars to bound datagram size; oversized findings cannot inflate the payload |
</threat_model>

<verification>
- `python -m pytest tests/test_siem_cef.py tests/test_siem_payload_whitelist.py -q` all green
- Manual grep: `python -c "from quirk.siem.formatter import build_cef_event; print(build_cef_event({'severity':'CRITICAL','title':'x|y','host':'h','port':443,'description':'d','recommendation':'r','cert_pem':'SECRET'}, '1.0.0'))"` shows escaped pipe, no "SECRET"
</verification>

<success_criteria>
- formatter.py implements both escape functions (backslash-first), severity map, slugify fallback, explicit whitelist, build_cef_event
- No cert/compliance/key material can reach a CEF line
- All Task-1 tests pass
</success_criteria>

<output>
Create `.planning/phases/103-siem-export/103-01-SUMMARY.md` when done.
</output>
