---
phase: 103-siem-export
plan: 04
type: execute
wave: 3
depends_on: ["103-03"]
files_modified:
  - docs/configuration.md
  - docs/sample-config.yaml
  - docs/UAT-SERIES.md
autonomous: true
requirements: [SIEM-01, SIEM-02]

must_haves:
  truths:
    - "Operators can read how to configure [siem] (host/port/protocol/export_after_scan/timeout_seconds) and run `quirk export --siem`"
    - "docs/sample-config.yaml contains a commented [siem] block"
    - "docs/UAT-SERIES.md has SIEM export test cases and an updated Last Updated date"
    - "An Obsidian phase note for Phase 103 exists and UAT-Series.md is synced"
  artifacts:
    - path: "docs/configuration.md"
      provides: "SIEM export configuration + CLI usage section"
      contains: "siem"
    - path: "docs/UAT-SERIES.md"
      provides: "Phase 103 SIEM UAT cases"
  key_links:
    - from: "docs/UAT-SERIES.md"
      to: "Obsidian vault UAT-Series.md"
      via: "vault filesystem copy per CLAUDE.md step 3"
      pattern: "siem"
---

<objective>
Document the SIEM export feature for operators and record UAT, then run the CLAUDE.md mandatory phase-completion docs steps (Obsidian phase note, UAT-SERIES update + sync + commit).

Purpose: Per-phase docs + Obsidian sync are mandatory (CLAUDE.md). This plan is intentionally last (Wave 3) so docs describe shipped behavior from Plan 03's SUMMARY.
Output: configuration docs + sample config + UAT cases + Obsidian phase note + synced UAT-Series.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-siem-export/103-CONTEXT.md
@.planning/phases/103-siem-export/103-RESEARCH.md
@.planning/phases/103-siem-export/103-01-SUMMARY.md
@.planning/phases/103-siem-export/103-02-SUMMARY.md
@.planning/phases/103-siem-export/103-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document SIEM export config + CLI</name>
  <files>docs/configuration.md, docs/sample-config.yaml</files>
  <read_first>
    - docs/configuration.md (existing structure + the [notifications] section as the analog block to mirror)
    - docs/sample-config.yaml (existing commented blocks; mirror the notifications block layout)
    - .planning/phases/103-siem-export/103-RESEARCH.md (YAML config snippet ~L330-338; TCP NUL framing note ~L289)
  </read_first>
  <action>
    Add a "SIEM Export (syslog/CEF)" section to docs/configuration.md describing: the `[siem]` config keys (host, port default 514, protocol udp|tcp, export_after_scan, timeout_seconds), the QUIRK_CONFIG_PATH load discipline, `quirk export --siem [--input PATH] [--output-dir DIR]` usage, one-CEF-event-per-finding semantics, the CEF field mapping (severity 10/8/5/3, dhost/dpt/cs1/cs2/msg), the payload-safety note (no cert PEM/SANs/compliance), and the after-scan flag. Note that HEC + Elastic-native + TLS-syslog are deferred. Add a commented `siem:` block to docs/sample-config.yaml mirroring the research snippet, with the raw-TCP framing caveat as a comment. Keep diffs minimal; do not restructure existing sections.
  </action>
  <verify>
    <automated>grep -q "siem" docs/sample-config.yaml && grep -iq "export --siem" docs/configuration.md && grep -iq "export_after_scan" docs/configuration.md</automated>
  </verify>
  <done>configuration.md has a SIEM export section covering config keys + CLI + field mapping + safety; sample-config.yaml has a commented [siem] block.</done>
</task>

<task type="auto">
  <name>Task 2: Add SIEM UAT cases + update + sync + commit UAT-SERIES</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (header `**Last Updated:**` line; an existing `### UAT-N-NN` case as the format template; the highest existing series number to pick the next)
    - CLAUDE.md (Mandatory Phase Completion Steps 2-4: UAT update, vault sync, commit)
  </read_first>
  <action>
    Add new UAT cases for SIEM export in the appropriate series (new series block, e.g. SIEM, with cases for: SIEM-01 syslog UDP/TCP delivery of CEF events to a receiver; SIEM-02 correct CEF field mapping + no cert PEM/PKI topology in payload; failure isolation — unreachable endpoint produces clear error + audit row and does NOT corrupt the scan record; after-scan export when export_after_scan=true). Each case follows the existing `### UAT-...` template (Steps, Expected, Pass Criteria), referencing `quirk export --siem` and the live-SIEM manual-verification note from 103-VALIDATION.md. Update the `**Last Updated:**` line to today's date with a Phase 103 wrap summary entry.
    Then sync to Obsidian per CLAUDE.md step 3: build /tmp/uat_vault.md with frontmatter + the file body and copy to "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md".
    Then commit per CLAUDE.md step 4: `node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-103): update UAT-SERIES.md" --files docs/UAT-SERIES.md`.
  </action>
  <verify>
    <automated>grep -iq "siem" docs/UAT-SERIES.md && grep -q "2026-05-24" docs/UAT-SERIES.md && test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"</automated>
  </verify>
  <done>UAT-SERIES.md has SIEM export cases, updated Last Updated date; vault UAT-Series.md synced; UAT-SERIES.md committed.</done>
</task>

<task type="auto">
  <name>Task 3: Create Obsidian Phase 103 note</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-103-SIEM-Export.md</files>
  <read_first>
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/ (an existing recent phase note as the template: frontmatter + Goal + Requirements Covered + Success Criteria + What Was Built per-plan + [[Roadmap]] link)
    - .planning/phases/103-siem-export/103-01-SUMMARY.md, 103-02-SUMMARY.md, 103-03-SUMMARY.md
    - CLAUDE.md (Mandatory Phase Completion Step 1)
  </read_first>
  <action>
    Write the phase note directly to the vault filesystem at /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-103-SIEM-Export.md (do NOT use obsidian CLI content= — too large for shell). Frontmatter: project QU.I.R.K., type phase, status complete, source .planning/phases/103-siem-export/, updated 2026-05-24. Body: Goal (from ROADMAP), Requirements Covered (SIEM-01, SIEM-02), Success Criteria (3 from ROADMAP), What Was Built (one subsection per plan sourced from the SUMMARY files: formatter/whitelist; config/transport; dispatcher/CLI/hooks; docs), and a `[[Roadmap]]` wikilink.
  </action>
  <verify>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-103-SIEM-Export.md" && grep -q "SIEM-01" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-103-SIEM-Export.md"</automated>
  </verify>
  <done>Phase-103 Obsidian note exists with frontmatter, goal, requirements, success criteria, per-plan What Was Built, and Roadmap link.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> operator config | Documentation guides operators to configure a plaintext syslog target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-10 | Information Disclosure | docs/configuration.md SIEM section | mitigate | Documentation explicitly states the payload excludes cert PEM / PKI topology and that syslog is plaintext (TLS deferred) so operators place collectors on trusted internal networks |
| T-103-11 | Tampering | docs only | accept | No code or dependency changes in this plan; documentation + vault sync only |
</threat_model>

<verification>
- `grep -i siem docs/configuration.md docs/sample-config.yaml docs/UAT-SERIES.md`
- Obsidian phase note and synced UAT-Series.md exist
- UAT-SERIES.md committed via gsd-tools
</verification>

<success_criteria>
- Operators can configure and run SIEM export from the docs
- UAT cases recorded, synced, committed
- Phase 103 Obsidian note created
</success_criteria>

<output>
Create `.planning/phases/103-siem-export/103-04-SUMMARY.md` when done.
</output>
