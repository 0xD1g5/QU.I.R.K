---
phase: 105-servicenow-ticketing
plan: 02
type: execute
wave: 2
depends_on: ["105-01"]
files_modified:
  - quirk/cli/ticket_cmd.py
  - tests/test_ticket_cmd.py
autonomous: true
requirements: [TICKET-02]
must_haves:
  truths:
    - "quirk ticket create --backend servicenow dispatches findings through ServiceNowChannel"
    - "quirk ticket create with no --backend defaults to jira (backward compatible)"
    - "Missing [ticketing.servicenow] block with --backend servicenow exits with a clear error"
    - "The missing-[tickets]-extra advisory message is backend-neutral (no longer Jira-specific)"
  artifacts:
    - path: "quirk/cli/ticket_cmd.py"
      provides: "--backend {jira,servicenow} flag + conditional dispatch + neutral extra-gate message"
      contains: "--backend"
    - path: "tests/test_ticket_cmd.py"
      provides: "test_backend_servicenow dispatch test + default-jira regression test"
      contains: "def test_backend_servicenow"
  key_links:
    - from: "quirk/cli/ticket_cmd.py"
      to: "quirk/ticketing/servicenow.py::ServiceNowChannel"
      via: "lazy import in the --backend servicenow branch"
      pattern: "from quirk.ticketing.servicenow import ServiceNowChannel"
---

<objective>
Add `--backend {jira,servicenow}` dispatch to the shared `quirk ticket create` CLI (default jira), and make the missing-extra advisory backend-neutral. ServiceNow becomes selectable from the CLI without disturbing the existing Jira path.

Purpose: Completes the user-facing surface for TICKET-02 — the command a ServiceNow team actually runs.
Output: quirk/cli/ticket_cmd.py (extended), tests/test_ticket_cmd.py (new dispatch tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/105-servicenow-ticketing/105-RESEARCH.md
@.planning/phases/105-servicenow-ticketing/105-PATTERNS.md
@.planning/phases/105-servicenow-ticketing/105-01-SUMMARY.md

<interfaces>
From quirk/cli/ticket_cmd.py (current state, to be extended):
- run_ticket(argv): builds argparse parser (add_argument calls ~L66-77), `args = parser.parse_args(argv)` ~L84
- extra gate ~L87-89: `if not is_extra_available("tickets"): print("ERROR: Jira ticketing skipped — run \`pip install quirk[tickets]\` to enable.", file=sys.stderr)`
- dispatch ~L124-139: `if cfg is None or cfg.jira is None:` ... `from quirk.ticketing.jira import JiraChannel` / `channel = JiraChannel(cfg.jira)`

From quirk/ticketing/servicenow.py (Plan 105-01):
- class ServiceNowChannel(TicketingChannel) — constructed with cfg.servicenow

From quirk/ticketing/config.py (Plan 105-01):
- TicketingCfg.servicenow: Optional[ServiceNowTicketingCfg]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add --backend flag, neutral extra-gate message, and conditional dispatch</name>
  <files>quirk/cli/ticket_cmd.py</files>
  <read_first>
    - quirk/cli/ticket_cmd.py — existing run_ticket: the add_argument block (~L66-77), parse_args (~L84), extra gate (~L87-89), cfg load + jira dispatch (~L124-139)
    - 105-PATTERNS.md "quirk/cli/ticket_cmd.py — MODIFY" section (lines 263-343) — add_argument, neutral message, backend-conditional dispatch block
    - 105-RESEARCH.md "CLI Backend Dispatch and Extra Gate" (lines 383-398) and Open Question 1 (lines 602-605) — keep gate at top, neutralize message
  </read_first>
  <behavior>
    - parser accepts --backend with choices jira/servicenow, default jira
    - extra-gate advisory reads "ERROR: Ticketing skipped — run `pip install quirk[tickets]` to enable." (backend-neutral)
    - args.backend == "servicenow" with cfg.servicenow is None → exit code 2 with a servicenow-specific config error
    - args.backend default → existing JiraChannel path unchanged
  </behavior>
  <action>
    In run_ticket, add a parser.add_argument("--backend", choices=["jira","servicenow"], default="jira", help="Ticketing backend to use (default: jira)") alongside the existing arguments (before parse_args). Update the extra-gate message string (~L89) from "ERROR: Jira ticketing skipped — ..." to backend-neutral "ERROR: Ticketing skipped — run `pip install quirk[tickets]` to enable." — keep the gate at the top per RESEARCH recommendation (CLI is shared; jira extra is the trigger). Replace the hard-coded `if cfg is None or cfg.jira is None:` jira-only dispatch (~L124-139) with: first guard `if cfg is None:` (config-not-found error → exit 2); then branch on args.backend: if "servicenow", guard `if cfg.servicenow is None:` print "ERROR: [ticketing.servicenow] block not configured. Add a servicenow sub-block to QUIRK_CONFIG_PATH." → exit 2, else lazy `from quirk.ticketing.servicenow import ServiceNowChannel` and `channel = ServiceNowChannel(cfg.servicenow)`; else (default jira) guard `if cfg.jira is None:` print "ERROR: [ticketing.jira] block not configured. Add a jira sub-block to QUIRK_CONFIG_PATH." → exit 2, else lazy import JiraChannel and `channel = JiraChannel(cfg.jira)`. Preserve the existing dispatch loop (`for finding in findings: channel.dispatch_finding(...)`), the safe_str exception wrapper, and exit codes. Minimal diff — do not refactor unrelated argparse or scan-loading code. PEP 8.
  </action>
  <acceptance_criteria>
    - source: quirk/cli/ticket_cmd.py contains add_argument("--backend", ...) with choices jira/servicenow default jira; the extra-gate message no longer contains the word "Jira"; a `from quirk.ticketing.servicenow import ServiceNowChannel` lazy import inside the servicenow branch
    - behavior: --backend servicenow with a configured servicenow block constructs ServiceNowChannel; default invocation still constructs JiraChannel
    - test-command: `python -m pytest tests/test_ticket_cmd.py -q` passes (see Task 2)
  </acceptance_criteria>
  <verify>
    <automated>python -m compileall quirk/cli/ticket_cmd.py >/dev/null && grep -q 'choices=\["jira", "servicenow"\]\|choices=\[.jira., .servicenow.\]' quirk/cli/ticket_cmd.py && ! grep -q 'Jira ticketing skipped' quirk/cli/ticket_cmd.py && echo OK</automated>
  </verify>
  <done>--backend flag present with jira/servicenow choices; extra-gate message neutralized; backend-conditional dispatch wired; module compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CLI dispatch tests (servicenow + default-jira regression)</name>
  <files>tests/test_ticket_cmd.py</files>
  <read_first>
    - tests/test_ticket_cmd.py — existing run_ticket test structure: how findings JSON + config are set up, how QUIRK_CONFIG_PATH / QUIRK_DB_PATH env are monkeypatched, how the channel is patched/asserted
    - 105-RESEARCH.md "Phase Requirements to Test Map" row TICKET-02 CLI dispatch (line 649)
    - quirk/cli/ticket_cmd.py (post-Task-1) — the servicenow/jira branches to assert against
  </read_first>
  <behavior>
    - test_backend_servicenow: run_ticket(["--backend","servicenow", ...]) with a servicenow config block patches/asserts ServiceNowChannel is constructed and dispatch_finding is called per finding
    - default-jira regression: existing jira invocation path still selects JiraChannel
    - --backend servicenow with no servicenow config block → SystemExit code 2
  </behavior>
  <action>
    Add to tests/test_ticket_cmd.py, mirroring the existing run_ticket test setup. test_backend_servicenow: write a findings JSON + a config with a valid https servicenow block (instance_url, user_env, password_env) to tmp_path, monkeypatch QUIRK_CONFIG_PATH and QUIRK_DB_PATH and is_extra_available→True; patch quirk.ticketing.servicenow.ServiceNowChannel (or patch its dispatch_finding) to a MagicMock and assert it was constructed with cfg.servicenow and dispatch_finding called once per finding. Add a regression test (or extend an existing one) asserting that default backend (no --backend flag) still routes to JiraChannel. Add test_backend_servicenow_missing_config asserting SystemExit with code 2 when --backend servicenow but no servicenow block configured. Use the existing helpers/fixtures in the file; do not introduce a live HTTP call (the channel is mocked).
  </action>
  <acceptance_criteria>
    - source: tests/test_ticket_cmd.py defines test_backend_servicenow and a missing-config exit-2 test; the default-jira path is asserted
    - behavior: tests pass against the Task-1 dispatch logic without any network call
    - test-command: `python -m pytest tests/test_ticket_cmd.py -q` all green
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_ticket_cmd.py -q</automated>
  </verify>
  <done>CLI dispatch tests green: servicenow routes to ServiceNowChannel, default routes to JiraChannel, missing servicenow config exits 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → backend selection | --backend value is a closed choices set (jira/servicenow); no free-form injection |
| CLI → config file | QUIRK_CONFIG_PATH-supplied YAML parsed into TicketingCfg; creds are env-var names only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-07 | Spoofing | --backend value | mitigate | argparse choices=["jira","servicenow"] rejects any other value before dispatch |
| T-105-08 | Information Disclosure | exception text from dispatch loop | mitigate | existing safe_str(exc) wrapper in run_ticket preserved unchanged |
| T-105-09 | Denial of Service | missing extra silently proceeds | mitigate | is_extra_available("tickets") gate retained at top; neutral advisory message |
| T-105-SC | Tampering | npm/pip/cargo installs | accept | zero new pip deps; no install task in this plan |
</threat_model>

<verification>
- `python -m pytest tests/test_ticket_cmd.py -q` — all green
- `python -m compileall quirk/cli/ticket_cmd.py` — succeeds
- `grep -q 'Jira ticketing skipped' quirk/cli/ticket_cmd.py` — no match (message neutralized)
</verification>

<success_criteria>
- `quirk ticket create --backend servicenow` routes to ServiceNowChannel
- Default (no flag) routes to JiraChannel — backward compatible
- Missing servicenow config block exits 2 with a clear message
- Missing-extra advisory is backend-neutral
</success_criteria>

<output>
Create `.planning/phases/105-servicenow-ticketing/105-02-SUMMARY.md` when done
</output>
