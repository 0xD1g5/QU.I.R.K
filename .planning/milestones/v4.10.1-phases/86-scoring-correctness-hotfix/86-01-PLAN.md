---
phase: 86-scoring-correctness-hotfix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/intelligence/scoring.py
  - tests/test_score_weights_invariant.py
  - tests/test_scoring_normalization.py
autonomous: true
requirements:
  - SCORE-FIX-01
  - SCORE-FIX-02
  - SCORE-FIX-03
must_haves:
  truths:
    - "compute_readiness_score returns int(round(sum_of_subscores / 1.5)) instead of a clamp at 100."
    - "Calling compute_readiness_score with all six subscores at 25 returns exactly 100 (rating EXCELLENT)."
    - "Calling compute_readiness_score with all six subscores at 0 returns exactly 0 (rating POOR)."
    - "Canonical sum 25+25+23+3+25+19=120 yields overall=80 (rating GOOD), not 100."
    - "The module docstring at scoring.py:1-17 names the 0-25 subscore scale and the int(round(sum/1.5)) formula, with no Phase 60 SCORE-04 clamp-to-[0,100]-is-intentional wording."
    - "SCORE_WEIGHTS sum=275.0 / count=36 invariant still holds (weights are independent of aggregation)."
  artifacts:
    - path: "quirk/intelligence/scoring.py"
      provides: "Normalized overall readiness aggregator + corrected module docstring"
      contains: "int(round("
    - path: "tests/test_scoring_normalization.py"
      provides: "Boundary + canonical-example asserts for compute_readiness_score"
      exports: ["test_overall_score_max_when_all_subscores_at_25", "test_overall_score_zero_when_all_subscores_zero", "test_overall_score_canonical_example_120_to_80"]
    - path: "tests/test_score_weights_invariant.py"
      provides: "Weight-sum / count invariants — preserved and audited against new formula"
      contains: "275.0"
  key_links:
    - from: "quirk/intelligence/scoring.py compute_readiness_score"
      to: "_apply_weighted_impacts(score_cap=25.0)"
      via: "six per-category subscores summed then normalized"
      pattern: "int\\(round\\(.*/\\s*1\\.5\\)\\)"
    - from: "tests/test_scoring_normalization.py"
      to: "quirk.intelligence.scoring.compute_readiness_score"
      via: "direct import + synthetic evidence fixtures"
      pattern: "from quirk\\.intelligence\\.scoring import compute_readiness_score"
---

<objective>
Replace the broken clamp-at-100 aggregation in `compute_readiness_score` with the locked normalization formula `int(round(sum / 1.5))`, rewrite the misleading module docstring to describe the actual contract, and lock the new math behind a boundary test (0/100 endpoints + canonical 120→80 example). Backend math becomes correct in isolation — the CLI's `quirk export-json` already reflects the fix without any frontend changes.

Purpose: Fix the root cause of the marquee `100/EXCELLENT` bug. Once this lands, every consumer of `compute_readiness_score` (dashboard, CLI JSON, future renderers) sees the correct overall score; the dashboard's separate "subscore renders red because 25/100 < 50%" rendering bug is addressed in Plan 02.

Output:
- `quirk/intelligence/scoring.py` lines 253-257 replaced with normalized formula (per D-01)
- `quirk/intelligence/scoring.py` lines 1-17 docstring rewritten (per D-11)
- `tests/test_scoring_normalization.py` new file with boundary + canonical tests (per D-09)
- `tests/test_score_weights_invariant.py` audited; 275.0/36 invariants preserved (per D-08)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/86-scoring-correctness-hotfix/CONTEXT.md
@quirk/intelligence/scoring.py
@tests/test_score_weights_invariant.py

<interfaces>
<!-- Existing scoring.py contract — DO NOT change signatures. -->

From quirk/intelligence/scoring.py:
- `_clamp(v: float, lo: float, hi: float) -> float` — kept (still used inside `_apply_weighted_impacts` at line 105-106).
- `_rating(score: int) -> str` — kept unchanged per D-03. EXCELLENT ≥ 85, GOOD ≥ 70, MODERATE ≥ 55, FAIR ≥ 35, POOR < 35.
- `_apply_weighted_impacts(impacts: List[Tuple[str, float]], score_cap: float = 25.0) -> Tuple[int, List[Tuple[str, int]]]` — kept unchanged per D-02 (preserves the 0-25 per-category budget).
- `compute_readiness_score(evidence, *, ...) -> Dict` — returns `{"score": int, "rating": str, "subscores": {hygiene, modern_tls, identity_trust, agility_signals, data_at_rest, data_in_motion}, "drivers": [...]}`. Only the `total_score` computation at lines 253-257 changes.
- `SCORE_WEIGHTS: Dict[str, float]` — sum 275.0, count 36 (Phase 83 rebalance). NO change.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add boundary + canonical normalization tests (RED)</name>
  <files>tests/test_scoring_normalization.py</files>
  <behavior>
    - Test `test_overall_score_max_when_all_subscores_at_25`: build evidence such that all six categories yield subscore=25 (no negative impacts). Assert `compute_readiness_score(evidence)["score"] == 100` and `["rating"] == "EXCELLENT"`. The simplest such evidence is an empty mapping with safe defaults — verify by reading scoring.py how empty evidence flows through `_ratio` and `_apply_weighted_impacts` (denominator-zero or zero impacts ⇒ subscore stays at score_cap=25).
    - Test `test_overall_score_zero_when_all_subscores_zero`: build evidence whose ratios drive every subscore to 0 (heavy negative impacts in every category). Assert `score == 0` and `rating == "POOR"`. If a synthetic evidence dict that drives all six subscores to 0 is awkward, monkeypatch the six `_apply_weighted_impacts` call sites by calling the public function with crafted counters from the evidence schema (host_count + per-finding counters used by lines 112-251).
    - Test `test_overall_score_canonical_example_120_to_80`: this test MUST actually call `compute_readiness_score(...)` with synthetic evidence that yields the exact canonical subscore tuple (25, 25, 23, 3, 25, 19) and assert `result["score"] == 80` and `result["rating"] == "GOOD"`. Do NOT shortcut to a pure arithmetic identity (`int(round(120/1.5)) == 80`) — that would pass trivially and would not catch a broken `compute_readiness_score`. The test MUST exercise the real aggregator path. If synthesizing the exact canonical subscores via evidence is impractical, monkeypatch the six `_apply_weighted_impacts` call sites inside `compute_readiness_score` (using pytest's `monkeypatch.setattr`) to return the canonical tuple, then call `compute_readiness_score(...)` and assert on its returned `score` and `rating`. Cite D-09 in the test docstring.
    - Tests MUST initially fail against the unchanged scoring.py (the canonical case currently returns 100 because of the clamp). This is the RED step. The `test_overall_score_canonical_example_120_to_80` test specifically MUST appear in pytest's FAILED list when run against unmodified scoring.py.
  </behavior>
  <action>Create `tests/test_scoring_normalization.py` per D-09 with the three tests above. Use `from quirk.intelligence.scoring import compute_readiness_score, _rating`. Read `quirk/intelligence/scoring.py` lines 112-251 to determine the minimal evidence shape that drives each subscore to its extremes — prefer the simplest fixture (empty dict for the max case; a dict that sets host_count > 0 and every per-finding counter equal to host_count for the zero case). For the canonical 120→80 case, if direct evidence synthesis is impractical, use `monkeypatch.setattr` on the `_apply_weighted_impacts` symbol to return the canonical (subscore, drivers) tuple per call so the real `compute_readiness_score` runs end-to-end against the canonical 25+25+23+3+25+19 tuple. Do NOT modify scoring.py in this task. Run pytest and confirm the canonical-example test fails (current output 100, expected 80). Commit as `test(86-01): add scoring normalization boundary tests (RED)`.</action>
  <verify>
    <automated>pytest tests/test_scoring_normalization.py -v 2>&1 | tee /tmp/86-01-red.log | grep -E "test_overall_score_canonical_example_120_to_80 FAILED" && grep -E "test_overall_score_canonical_example_120_to_80 FAILED" /tmp/86-01-red.log | grep -v "^#"</automated>
  </verify>
  <done>File `tests/test_scoring_normalization.py` exists with three named tests. Running `pytest tests/test_scoring_normalization.py -v` produces a non-empty match for the literal string `test_overall_score_canonical_example_120_to_80 FAILED` (proving the canonical test actually invoked `compute_readiness_score` and got 100 instead of 80, not a trivially-passing arithmetic identity). The boundary tests likely also fail at this stage. RED step committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply normalization formula + rewrite docstring (GREEN)</name>
  <files>quirk/intelligence/scoring.py, tests/test_score_weights_invariant.py</files>
  <behavior>
    - After the change, `compute_readiness_score` returns `int(round((hygiene + modern_tls + identity_trust + agility + dar + motion) / 1.5))` for the overall score (per D-01).
    - All six subscore call sites and `_apply_weighted_impacts(score_cap=25.0)` are unchanged (per D-02).
    - `_rating` thresholds unchanged (per D-03).
    - Module docstring at lines 1-17 names the 0-25 subscore scale, the `int(round(sum / 1.5))` aggregation, and removes the misleading "Phase 60 SCORE-04 / CR-06 closure" / "clamp [0,100] is intentional" wording (per D-11). A short v4.10.1 anchor sentence replaces it.
    - `tests/test_score_weights_invariant.py` still passes — neither test (sum=275.0, count=36) depends on the clamp; verify by reading both assertions. If either depends on the broken clamp behavior, update only the broken assertion (none should — they target SCORE_WEIGHTS only).
  </behavior>
  <action>Edit `quirk/intelligence/scoring.py` lines 253-257 to read `total_score = int(round((hygiene_score + modern_tls_score + identity_trust_score + agility_score + dar_score + motion_score) / 1.5))` per D-01. Then rewrite lines 1-17 (the SCORE_WEIGHTS preamble) per D-11 — preserve the "ABSOLUTE per-ratio coefficients, sum = 275.0 by design" framing (since that invariant is real), but delete the "total_score arithmetic below already clamps to [0, 100]" and "Phase 60 SCORE-04 / CR-06 closure" sentences. Replace with a short v4.10.1 paragraph stating: six subscores each on 0-25 scale (via `_apply_weighted_impacts(score_cap=25.0)`), overall readiness = `int(round(sum / 1.5))` mapped through `_rating()`. Keep the "contributor must update test_score_weights_invariant.py" guardrail line — that part is still accurate. Audit `tests/test_score_weights_invariant.py` (lines 5-30): both tests target SCORE_WEIGHTS sum/count and are independent of the aggregator, so no edits expected — confirm by re-running and only edit if an assertion is observed to fail. Commit as `fix(86-01): normalize overall readiness (sum/1.5) + rewrite scoring docstring (GREEN)`.</action>
  <verify>
    <automated>pytest tests/test_scoring_normalization.py tests/test_score_weights_invariant.py -v 2>&1 | tail -25</automated>
  </verify>
  <done>All three tests in `tests/test_scoring_normalization.py` PASS. `test_score_weights_sum_invariant` and `test_score_weights_count_invariant` PASS. `grep -n "clamp.*\[0,\s*100\].*intentional\|Phase 60 SCORE-04" quirk/intelligence/scoring.py` returns no matches in lines 1-17. `grep -n "int(round" quirk/intelligence/scoring.py` shows the new normalization at the former line-253 location.</done>
</task>

<task type="auto">
  <name>Task 3: Regression sweep — full scoring + intelligence test suite</name>
  <files>(no edits expected; this task is a verification gate)</files>
  <action>Run the broader scoring / intelligence test suite to catch any incidental assertion that relied on the clamp behavior (e.g., a downstream test that hard-coded overall=100 from a previously-clamped fixture). If a legitimately-broken assertion surfaces, fix only that assertion's expected value (do not change the formula or _rating thresholds — those are locked by D-01/D-03). Document any such fix in the commit message. If everything passes clean, commit only if there are uncommitted edits; otherwise skip the commit.</action>
  <verify>
    <automated>pytest tests/ -x -k "scor or scoring or readiness or weights" 2>&1 | tail -15</automated>
  </verify>
  <done>The scoring-related test selection passes with zero failures. Any incidental assertion drift has been corrected (or confirmed absent). `python -m compileall quirk/intelligence/scoring.py tests/test_scoring_normalization.py` exits 0.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_scoring_normalization.py -v` — all three tests pass.
- `pytest tests/test_score_weights_invariant.py -v` — both invariants hold (275.0 / 36).
- `pytest tests/ -k "scor or readiness or weights"` — zero failures.
- `python -c "from quirk.intelligence.scoring import compute_readiness_score, _rating; print(_rating(80))"` prints `GOOD`.
- `grep -n "Phase 60 SCORE-04\|clamp.*intentional" quirk/intelligence/scoring.py | grep -v '^#'` returns no matches.
- CLI smoke (optional, no automation needed at this stage): `quirk export-json` on an existing scan now reports overall score = `int(round(sum_of_subscores / 1.5))`, not the clamped value.
</verification>

<success_criteria>
1. `compute_readiness_score` aggregation matches `int(round(sum / 1.5))` (D-01, SCORE-FIX-01).
2. Module docstring honestly describes the 0-25 subscore + normalize-by-1.5 contract; no clamp-is-intentional wording remains (D-11, SCORE-FIX-02).
3. Boundary + canonical-example tests committed and green (D-09, SCORE-FIX-03).
4. SCORE_WEIGHTS sum=275.0 / count=36 invariant preserved (D-08).
5. No edits to `_apply_weighted_impacts`, `_rating`, or the six subscore call sites.
</success_criteria>

<output>
After completion, create `.planning/phases/86-scoring-correctness-hotfix/86-01-SUMMARY.md` summarizing: lines changed in scoring.py, docstring rewrite excerpt, the three new tests, and a sample `compute_readiness_score` call demonstrating the canonical 120→80 result.
</output>
