---
phase: 86-scoring-correctness-hotfix
plan: 02
type: execute
wave: 2
depends_on:
  - 86-01
files_modified:
  - src/dashboard/src/components/gauges/ScoreGauge.tsx
  - src/dashboard/src/pages/executive.tsx
  - src/dashboard/src/pages/data-at-rest.tsx
  - src/dashboard/src/components/gauges/__tests__/ScoreGauge.test.tsx
autonomous: true
requirements:
  - GAUGE-01
  - GAUGE-02
  - GAUGE-03
must_haves:
  truths:
    - "ScoreGauge accepts a maxValue prop with default 100; existing callers that omit it are unaffected."
    - "Arc fill length and the displayed numeric label are computed against score / maxValue (not a hard-coded /100)."
    - "_gaugeColor operates on a normalized fraction (0.0-1.0): red < 0.5, amber 0.5-0.79, green >= 0.8."
    - "executive.tsx subscore gauges (Hygiene, Modern TLS, Identity, Agility, Data at Rest, Data in Motion) all pass maxValue={25}."
    - "executive.tsx overall-readiness gauge (line 244-250 region) does NOT pass maxValue (defaults to 100, unchanged behavior)."
    - "data-at-rest.tsx:301 standalone Data at Rest gauge passes maxValue={25} (per D-07 — darScore is a 0-25 subscore)."
    - "A subscore value of 25 with maxValue=25 renders green; 3 with maxValue=25 renders red; an overall of 80 with default maxValue=100 renders amber/green per the >=0.8 boundary."
  artifacts:
    - path: "src/dashboard/src/components/gauges/ScoreGauge.tsx"
      provides: "Maxvalue-aware gauge with normalized color thresholds"
      contains: "maxValue"
    - path: "src/dashboard/src/pages/executive.tsx"
      provides: "Six subscore gauges wired with maxValue={25}; overall gauge unchanged"
      contains: "maxValue={25}"
    - path: "src/dashboard/src/pages/data-at-rest.tsx"
      provides: "Standalone Data at Rest tab gauge wired with maxValue={25}"
      contains: "maxValue={25}"
    - path: "src/dashboard/src/components/gauges/__tests__/ScoreGauge.test.tsx"
      provides: "Vitest+RTL coverage for the three D-10 color/scale cases"
      exports: ["describe('ScoreGauge')"]
  key_links:
    - from: "src/dashboard/src/components/gauges/ScoreGauge.tsx _gaugeColor"
      to: "ScoreGauge arc fill calculation"
      via: "shared normalized fraction = score / (maxValue ?? 100)"
      pattern: "score\\s*/\\s*(maxValue|max)"
    - from: "src/dashboard/src/pages/executive.tsx subscore row"
      to: "ScoreGauge maxValue prop"
      via: "explicit maxValue={25} on each of six subscore gauges"
      pattern: "maxValue=\\{25\\}"
    - from: "src/dashboard/src/pages/data-at-rest.tsx:301"
      to: "ScoreGauge maxValue prop"
      via: "explicit maxValue={25} on the standalone Data at Rest tab gauge"
      pattern: "maxValue=\\{25\\}"
---

<objective>
Wire the dashboard so the (now-correct) backend numbers from Plan 01 render with the correct color and arc fill on every gauge. Add a `maxValue?: number` prop to `ScoreGauge` (default 100), rewrite `_gaugeColor` to operate on a normalized fraction, update `executive.tsx` so every subscore gauge passes `maxValue={25}` while the overall-readiness gauge keeps the default, and update the standalone Data at Rest tab gauge in `data-at-rest.tsx` so it passes `maxValue={25}` (the seventh real caller, per revised D-07). Add vitest+RTL coverage for the D-10 cases. After this plan lands, the dashboard exec summary and the Data at Rest tab match reality end-to-end.

Purpose: Without this plan, Plan 01 ships a backend fix that visibly *worsens* the dashboard — subscores of 5/25 (now correctly red in the math) would still render red against a 0-100 scale, and the overall would change number but every subscore radial (executive page + Data at Rest tab) would still look wrong-colored. The two plans are coupled by design.

Output:
- `ScoreGauge.tsx` extended with optional `maxValue` prop (per D-04) and normalized `_gaugeColor` (per D-05)
- `executive.tsx` lines 262-267 updated with `maxValue={25}` on all six subscore gauges (per D-06)
- `data-at-rest.tsx:301` updated with `maxValue={25}` on the standalone Data at Rest tab gauge (per revised D-07)
- New vitest test file under `src/dashboard/src/components/gauges/__tests__/` covering the D-10 assertions
- `npm run build` succeeds in `src/dashboard/` — pre-built statics ready for `quirk serve` (per project memory: dashboard requires explicit build step)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-scoring-correctness-hotfix/CONTEXT.md
@.planning/phases/86-scoring-correctness-hotfix/86-01-PLAN.md
@src/dashboard/src/components/gauges/ScoreGauge.tsx
@src/dashboard/src/pages/executive.tsx
@src/dashboard/src/pages/data-at-rest.tsx

<interfaces>
<!-- ScoreGauge current contract — extend, do not break. -->

From src/dashboard/src/components/gauges/ScoreGauge.tsx:
```typescript
interface ScoreGaugeProps {
  score: number          // currently documented "0-100" — will become "0..maxValue"
  label: string
  size?: number
  strokeColor?: string
  isOverall?: boolean
}
// New optional prop (this plan): maxValue?: number — default 100.
```

Two internal call sites currently use the literal `100`:
- Line 21: `const fillLength = (score / 100) * circumference` (marked unused; can be deleted or migrated)
- Line 30: `const fillEndAngle = Math.PI - (score / 100) * Math.PI` (THIS is the active arc calculation — must migrate to `(score / maxValue)`)
- `_gaugeColor(score)` thresholds at lines 9-13 must become `_gaugeColor(fraction)` with thresholds `>= 0.8` (green), `>= 0.5` (amber), else red.

From src/dashboard/src/pages/executive.tsx (lines 244-267):
- Overall gauge at lines 245-250 — DO NOT pass maxValue (D-06 keeps default 100).
- Six subscore gauges at lines 262-267 — each must add `maxValue={25}`.

From src/dashboard/src/pages/data-at-rest.tsx (line 301):
- Standalone Data at Rest tab gauge: `<ScoreGauge score={darScore} label="Data at Rest" size={120} />` where `darScore = data?.score?.subscores?.data_at_rest ?? 0` (a 0-25 subscore).
- MUST add `maxValue={25}` per revised D-07 — same reasoning as the executive-page subscore gauges.

Complete enumerated ScoreGauge call sites (revised D-07, verified 2026-05-22):
- executive.tsx ~line 245 (overall, default maxValue) — IN SCOPE (keeps default).
- executive.tsx ~lines 262-267 (six subscores) — IN SCOPE (add maxValue={25}).
- data-at-rest.tsx:301 (Data at Rest tab) — IN SCOPE (add maxValue={25}).
- print.tsx — NOT a caller; file does not import or render ScoreGauge. No edit, no guard needed.
- sidebar.tsx:2 — comment-only reference (token migration history); NOT a caller.

Available test infra (verified): vitest + @testing-library/react + @testing-library/jest-dom + jsdom. `npm test` runs `vitest run`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add maxValue prop + normalized color thresholds to ScoreGauge</name>
  <files>src/dashboard/src/components/gauges/ScoreGauge.tsx</files>
  <behavior>
    - `ScoreGaugeProps` gains optional `maxValue?: number` (default 100).
    - Inside the component, derive `const fraction = Math.max(0, Math.min(1, score / (maxValue ?? 100)))` once, and use it everywhere the old code referenced `score / 100` — specifically the `fillEndAngle` computation on the previously-line-30 line.
    - `_gaugeColor` is rewritten to accept a fraction (0.0-1.0): `>= 0.8` returns the quantum-safe (green) token; `>= 0.5` returns the at-risk (amber) token; else returns the vulnerable (red) token. Same HSL CSS-var tokens as before.
    - The displayed numeric label (`{score}` inside the SVG `<text>`) keeps showing the raw score value, NOT a percentage — only the arc fill and color are normalized. (Subscore "25 / 25" still reads as "25" in the gauge text.)
    - The `isOverall` accent-color path (line 40: `strokeColor ?? (isOverall ? "hsl(var(--accent))" : _gaugeColor(...))`) keeps using the accent for overall gauges; only the non-overall branch is affected by the `_gaugeColor` signature change.
    - Default behavior for any existing caller that omits `maxValue` is identical to today (since `100/100` = the prior `score/100`).
    - The unused `fillLength` constant on line 21 may be deleted or migrated to use `maxValue`; either is acceptable. The `void fillLength` marker line (line 36) should be deleted if `fillLength` is removed.
  </behavior>
  <action>Edit `src/dashboard/src/components/gauges/ScoreGauge.tsx`. Add `maxValue?: number` to `ScoreGaugeProps` per D-04. Destructure `maxValue = 100` in the function signature on line 15. Compute `const fraction = Math.max(0, Math.min(1, score / maxValue))` near the top of the function body. Change `_gaugeColor(score: number)` to `_gaugeColor(fraction: number)` with thresholds `>= 0.8` / `>= 0.5` / else (per D-05). Update the `fillEndAngle` line to use `fraction` instead of `score / 100`. Update the `color = ... _gaugeColor(score)` call on line 40 to pass `fraction` instead of `score`. Either delete the unused `fillLength` block or migrate it to `fraction * circumference` (deletion is preferred — it was already marked unused via `void fillLength`). Do NOT change the displayed `{score}` text node. Do NOT change `isOverall` accent behavior. Run `npm run lint` and `npm test` in `src/dashboard/` after the edit. Commit as `feat(86-02): ScoreGauge supports maxValue prop + normalized color thresholds`.</action>
  <verify>
    <automated>cd src/dashboard && npm run lint -- --max-warnings=0 2>&1 | tail -10 && grep -nE "maxValue|fraction" src/components/gauges/ScoreGauge.tsx | head -10</automated>
  </verify>
  <done>`ScoreGauge.tsx` exports the same component name with `maxValue?: number` prop documented in the interface. `grep -n "score / 100" src/dashboard/src/components/gauges/ScoreGauge.tsx` returns no live-code matches (any remaining occurrence is in a comment only). Lint passes with zero warnings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire executive.tsx + data-at-rest.tsx subscore gauges with maxValue={25}</name>
  <files>src/dashboard/src/pages/executive.tsx, src/dashboard/src/pages/data-at-rest.tsx</files>
  <behavior>
    - All six subscore `<ScoreGauge>` elements in `executive.tsx` at lines 262-267 pass `maxValue={25}` (per D-06).
    - The overall-readiness `<ScoreGauge>` in `executive.tsx` at lines 245-250 does NOT add `maxValue` — it inherits the default 100 (per D-06).
    - The standalone Data at Rest tab `<ScoreGauge>` in `data-at-rest.tsx` at line 301 passes `maxValue={25}` (per revised D-07 — `darScore` is a 0-25 subscore).
    - No other props on those eight elements change. No layout / className changes. No other files touched (print.tsx is NOT a caller and remains untouched).
  </behavior>
  <action>
    1. Edit `src/dashboard/src/pages/executive.tsx` lines 262-267 only. For each of the six subscore lines, insert `maxValue={25}` after the `score=` prop (before `label`). Confirm the overall gauge at lines 245-250 is unchanged.
    2. Edit `src/dashboard/src/pages/data-at-rest.tsx` line 301 only. Change `<ScoreGauge score={darScore} label="Data at Rest" size={120} />` to `<ScoreGauge score={darScore} label="Data at Rest" size={120} maxValue={25} />`. No other line in this file changes.
    3. Run `npm run lint` and `npm run build` in `src/dashboard/` (per project memory: dashboard requires explicit build step before Vite statics reflect .tsx edits). Commit as `feat(86-02): executive.tsx + data-at-rest.tsx subscore gauges use maxValue={25}`.
  </action>
  <verify>
    <automated>grep -c "maxValue={25}" src/dashboard/src/pages/executive.tsx && grep -c "maxValue={25}" src/dashboard/src/pages/data-at-rest.tsx && grep -nE "maxValue|^\\s*<ScoreGauge" src/dashboard/src/pages/executive.tsx | head -20 && cd src/dashboard && npm run build 2>&1 | tail -8</automated>
  </verify>
  <done>`grep -c "maxValue={25}" src/dashboard/src/pages/executive.tsx` returns exactly 6. `grep -c "maxValue={25}" src/dashboard/src/pages/data-at-rest.tsx` returns exactly 1. The overall-readiness gauge block in executive.tsx (lines around 244-250) contains zero occurrences of `maxValue`. `npm run build` exits 0 and `src/dashboard/dist/` has fresh statics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add ScoreGauge vitest+RTL coverage for D-10 cases</name>
  <files>src/dashboard/src/components/gauges/__tests__/ScoreGauge.test.tsx</files>
  <behavior>
    - Test "renders green when subscore equals its category max (25/25)": render `<ScoreGauge score={25} maxValue={25} label="Hygiene" />`, query the colored fill `<path>` (the second `<path>` inside the SVG, or the one whose `stroke` attribute is not the border token), assert its `stroke` attribute resolves to the quantum-safe CSS variable (`hsl(var(--quantum-safe))`). Practical approach: assert `path[stroke="hsl(var(--quantum-safe))"]` exists in the rendered output. The displayed text node renders "25" — assert `screen.getByText("25")` is present.
    - Test "renders red when subscore is low (3/25)": render with `score={3}` `maxValue={25}`. Assert the fill path's stroke is `hsl(var(--quantum-vulnerable))`. Text "3" is present.
    - Test "renders amber for overall=79 with default maxValue, green at 80 boundary": render with `score={79}` `label="Overall Readiness"` (no maxValue, no isOverall). Assert fill stroke is `hsl(var(--quantum-at-risk))`. Then render with `score={80}` and assert fill stroke is `hsl(var(--quantum-safe))`. This locks the `>= 0.8` threshold from D-05.
    - Additional sanity test: `<ScoreGauge score={25} label="..." />` with NO maxValue prop renders red (because 25/100 = 0.25 < 0.5) — proves default behavior is unchanged for legacy callers.
  </behavior>
  <action>Create `src/dashboard/src/components/gauges/__tests__/ScoreGauge.test.tsx` per D-10. Use `import { render, screen } from "@testing-library/react"`, `import { describe, it, expect } from "vitest"`, and `import { ScoreGauge } from "../ScoreGauge"`. For each case: render the component, then query the colored fill via `container.querySelector('path[stroke^="hsl(var(--quantum"]')` and assert `.getAttribute("stroke")` matches the expected CSS-var token string. Add the legacy-default sanity test described above. Run `npm test` in `src/dashboard/`. Commit as `test(86-02): ScoreGauge color + scale coverage (D-10)`.</action>
  <verify>
    <automated>cd src/dashboard && npm test -- src/components/gauges/__tests__/ScoreGauge.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <done>The new test file exists with at least four `it(...)` cases covering green-at-max, red-at-low, amber-at-79 + green-at-80 boundary, and legacy-default-red. `npm test` reports all ScoreGauge tests passing. No other test file regresses.</done>
</task>

</tasks>

<verification>
- `cd src/dashboard && npm run lint -- --max-warnings=0` — passes.
- `cd src/dashboard && npm test` — all tests including the new ScoreGauge file pass.
- `cd src/dashboard && npm run build` — exits 0; `dist/` is regenerated.
- `grep -c "maxValue={25}" src/dashboard/src/pages/executive.tsx` returns exactly 6.
- `grep -c "maxValue={25}" src/dashboard/src/pages/data-at-rest.tsx` returns exactly 1.
- Manual sanity check (covered by Plan 03's UAT): subscore radial at value 25 renders green; subscore at value 3 renders red; overall at value 80 renders green (boundary); Data at Rest tab gauge color matches its fraction of 25.
</verification>

<success_criteria>
1. `ScoreGauge` accepts `maxValue?: number` with default 100; existing callers unaffected (D-04, GAUGE-01).
2. `_gaugeColor` evaluates a fraction (0-1) using the locked thresholds 0.5 / 0.8 (D-05, GAUGE-02).
3. All six subscore gauges in `executive.tsx` pass `maxValue={25}`; overall gauge keeps default (D-06, GAUGE-03).
4. The standalone Data at Rest tab gauge in `data-at-rest.tsx:301` passes `maxValue={25}` (revised D-07).
5. Vitest covers the green-at-max, red-at-low, amber/green-boundary, and legacy-default cases (D-10).
6. Production bundle rebuilt — `quirk serve` will serve the corrected statics in Plan 03's UAT.
</success_criteria>

<output>
After completion, create `.planning/phases/86-scoring-correctness-hotfix/86-02-SUMMARY.md` listing the prop change, the `executive.tsx` diff (six insertions), the `data-at-rest.tsx` diff (one insertion), the vitest cases, and confirmation that print.tsx was not a caller and was not touched (revised D-07).
</output>
