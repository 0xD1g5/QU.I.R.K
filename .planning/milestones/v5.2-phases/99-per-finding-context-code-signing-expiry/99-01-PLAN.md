---
phase: 99-per-finding-context-code-signing-expiry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/reports/content_model.py
  - tests/test_content_model_phase99.py
  - tests/test_exec_content_model.py
autonomous: true
requirements: [CTX-01, CTX-02]
must_haves:
  truths:
    - "ALGO_IMPACT_MAP entries are 3-tuples (risk_label, impact_sentence, quantum_risk_sentence)"
    - "REMEDIATION_CATALOG provides a weakness-specific remediation string per crypto class"
    - "_build_top_risks still unpacks ALGO_IMPACT_MAP correctly after the tuple extension"
    - "CODESIGN_EXPIRY and CODESIGN_APPROACHING_EXPIRY keys resolve in both maps and via _classify_finding (check_id route)"
  artifacts:
    - path: "quirk/reports/content_model.py"
      provides: "3-tuple ALGO_IMPACT_MAP, REMEDIATION_CATALOG, extended _ALGO_KEYWORDS, public exports"
      contains: "REMEDIATION_CATALOG"
    - path: "tests/test_content_model_phase99.py"
      provides: "unit coverage for catalog parity + quantum_risk sentence presence"
  key_links:
    - from: "quirk/reports/content_model.py"
      to: "ALGO_IMPACT_MAP / REMEDIATION_CATALOG"
      via: "matching key sets keyed by crypto class"
      pattern: "REMEDIATION_CATALOG"
---

<objective>
Establish the content-model foundation Phase 99 builds on: extend `ALGO_IMPACT_MAP` from a
2-tuple to a 3-tuple (adding the per-finding `quantum_risk_sentence`), add the new
`REMEDIATION_CATALOG`, register the two new code-signing crypto-class keys, and fix every
existing 2-value unpack that the tuple extension breaks.

Purpose: This is the single source of truth (D-01, D-04) that Plan 02's `_build_finding`
enrichment and `evaluate_codesign_endpoints` consume. Nothing downstream can read
`quantum_risk` or catalog remediation until these maps exist.
Output: Updated `content_model.py` + new unit test file + updated existing test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-per-finding-context-code-signing-expiry/99-CONTEXT.md
@.planning/phases/99-per-finding-context-code-signing-expiry/99-RESEARCH.md
@.planning/phases/99-per-finding-context-code-signing-expiry/99-PATTERNS.md
@.planning/phases/99-per-finding-context-code-signing-expiry/99-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ALGO_IMPACT_MAP to 3-tuple + add REMEDIATION_CATALOG + new keys</name>
  <files>quirk/reports/content_model.py</files>
  <read_first>
    - quirk/reports/content_model.py (ALGO_IMPACT_MAP lines ~95-155, _ALGO_KEYWORDS ~162-177, _classify_finding, __all__ ~531-546, _build_top_risks ~360)
    - .planning/phases/99-per-finding-context-code-signing-expiry/99-UI-SPEC.md §Copywriting Contract (verbatim quantum_risk sentences) + §Per-Finding Remediation Catalog (verbatim remediation strings) + §Field Name Contract (fallback string)
    - .planning/phases/99-per-finding-context-code-signing-expiry/99-PATTERNS.md §content_model.py section
  </read_first>
  <behavior>
    - ALGO_IMPACT_MAP["RSA"] is a 3-tuple; index [0]/[1] unchanged from current values, index [2] equals the UI-SPEC RSA quantum_risk sentence verbatim
    - Every existing key (RSA, ECC, ECDSA, DH/DHE, DSA, WEAK_HASH/MD5, SHA1, WEAK_KEY_EXCHANGE/DHE_EXPORT, RC4, 3DES/DES) gains its UI-SPEC quantum_risk sentence at index [2]
    - New keys CODESIGN_EXPIRY and CODESIGN_APPROACHING_EXPIRY exist in ALGO_IMPACT_MAP as 3-tuples
    - REMEDIATION_CATALOG has the SAME key set as ALGO_IMPACT_MAP; each value is the UI-SPEC verbatim remediation string for that key
    - _classify_finding({"check_id": "CODESIGN_EXPIRY", ...}) returns "CODESIGN_EXPIRY"
  </behavior>
  <action>
    Per D-01: change the `ALGO_IMPACT_MAP` type annotation to `Dict[str, tuple[str, str, str]]`
    and append a third tuple element `quantum_risk_sentence` to every existing entry, using the
    verbatim strings from 99-UI-SPEC.md §Copywriting Contract. Add two NEW keys
    `CODESIGN_EXPIRY` and `CODESIGN_APPROACHING_EXPIRY` (their three values from the UI-SPEC
    expiry rows / Code-Signing Expiry Findings quantum_risk column).

    Per D-04: add a module-level `REMEDIATION_CATALOG: Dict[str, str]` immediately after
    ALGO_IMPACT_MAP with the identical key set, populated verbatim from 99-UI-SPEC.md
    §Per-Finding Remediation Catalog.

    Add `CODESIGN_EXPIRY` and `CODESIGN_APPROACHING_EXPIRY` to `_ALGO_KEYWORDS` so
    `_classify_finding` matches codesign-expiry findings via their `check_id` field (research
    Assumption A1 / Open Question 1 — check_id route).

    Add `REMEDIATION_CATALOG` and `_classify_finding` to the module `__all__` export list.

    Define a module-level fallback constant for the default quantum_risk sentence using the
    UI-SPEC §Field Name Contract default-fallback string (name it e.g.
    `FALLBACK_QUANTUM_RISK`) and export it. Do NOT touch `_build_finding` (Plan 02 owns that).
    Keep diffs minimal per CLAUDE.md.
  </action>
  <verify>
    <automated>python -m compileall quirk/reports/content_model.py && python -c "from quirk.reports.content_model import ALGO_IMPACT_MAP, REMEDIATION_CATALOG; assert all(len(v)==3 for v in ALGO_IMPACT_MAP.values()); assert set(REMEDIATION_CATALOG) == set(ALGO_IMPACT_MAP); assert 'CODESIGN_EXPIRY' in REMEDIATION_CATALOG and 'CODESIGN_APPROACHING_EXPIRY' in REMEDIATION_CATALOG"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c` assertion above exits 0 (all 3-tuples; key sets equal; codesign keys present)
    - `grep -v '^#' quirk/reports/content_model.py | grep -c 'REMEDIATION_CATALOG'` >= 1
    - ALGO_IMPACT_MAP[2] strings match 99-UI-SPEC.md §Copywriting Contract verbatim (manual cross-check during execution)
  </acceptance_criteria>
  <done>ALGO_IMPACT_MAP is a 3-tuple map with codesign keys; REMEDIATION_CATALOG exists with matching keys; _ALGO_KEYWORDS and __all__ updated; compileall passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix breaking unpacks + add Phase 99 content-model unit tests</name>
  <files>tests/test_content_model_phase99.py, tests/test_exec_content_model.py, quirk/reports/content_model.py</files>
  <read_first>
    - quirk/reports/content_model.py:_build_top_risks (~line 360, `risk_label, impact_sentence = ALGO_IMPACT_MAP[crypto_class]`)
    - tests/test_exec_content_model.py (~line 115, `_, expected_sentence = ALGO_IMPACT_MAP["RSA"]`)
    - tests/test_exec_content_model.py (imports + fixture style ~lines 1-76, test_top_risks_populated ~84-119)
    - .planning/phases/99-per-finding-context-code-signing-expiry/99-RESEARCH.md §Validation Architecture (test names) + §Common Pitfalls 1
  </read_first>
  <behavior>
    - test_quantum_risk_field_populated: ALGO_IMPACT_MAP["RSA"][2] is a non-empty str
    - test_remediation_catalog_key_parity: set(REMEDIATION_CATALOG) == set(ALGO_IMPACT_MAP)
    - test_codesign_keys_present: both CODESIGN_EXPIRY and CODESIGN_APPROACHING_EXPIRY resolve in both maps
    - test_classify_finding_matches_codesign_via_check_id: _classify_finding({"check_id": "CODESIGN_EXPIRY"}) == "CODESIGN_EXPIRY"
    - test_build_top_risks_unpacks_three_tuple: _build_top_risks runs without ValueError on the 3-tuple
  </behavior>
  <action>
    Fix the two known breaking 2-value unpacks (Pitfall 1): in `content_model.py:_build_top_risks`
    change `risk_label, impact_sentence = ALGO_IMPACT_MAP[crypto_class]` to a 3-value unpack
    `risk_label, impact_sentence, _ = ...`; in `tests/test_exec_content_model.py` change
    `_, expected_sentence = ALGO_IMPACT_MAP["RSA"]` to `_, expected_sentence, _ = ...`. Grep for
    any other `= ALGO_IMPACT_MAP[` callers and fix each.

    Create `tests/test_content_model_phase99.py` (analog tests/test_exec_content_model.py — same
    import + inline-dict-fixture style) covering the five behaviors above. Each assertion carries
    an inline rationale string naming the requirement ID (CTX-01 / CTX-02).
  </action>
  <verify>
    <automated>python -m pytest tests/test_content_model_phase99.py tests/test_exec_content_model.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_content_model_phase99.py tests/test_exec_content_model.py -x -q` exits 0
    - `grep -rn '= ALGO_IMPACT_MAP\[' quirk/ tests/ | grep -v ', _ =' | grep -v '\[crypto_class\]\[' ` returns no 2-value unpack survivors (manual confirm)
  </acceptance_criteria>
  <done>All ALGO_IMPACT_MAP unpacks updated to 3 values; new Phase 99 content-model tests pass; the pre-existing exec content-model test passes against the 3-tuple.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author-locked catalog → finding dict | Static UI-SPEC copy enters report content model; no runtime/external input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01 | Tampering | ALGO_IMPACT_MAP / REMEDIATION_CATALOG static strings | accept | Author-locked verbatim copy from 99-UI-SPEC.md; no scanner/user input flows into these maps; values are constants compiled into the module |
| T-99-02 | Information Disclosure | quantum_risk / remediation prose | accept | Strings are generic crypto guidance; contain no secrets, hosts, or PII |
| T-99-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (enrichment only — RESEARCH confirms zero new deps); slopcheck N/A |
</threat_model>

<verification>
- `python -m compileall quirk/reports/content_model.py` clean
- `python -m pytest tests/test_content_model_phase99.py tests/test_exec_content_model.py -x -q` green
- ALGO_IMPACT_MAP and REMEDIATION_CATALOG key sets identical; both contain the two codesign keys
</verification>

<success_criteria>
ALGO_IMPACT_MAP is a fully-populated 3-tuple map (existing + 2 codesign keys), REMEDIATION_CATALOG
exists with matching keys and verbatim UI-SPEC copy, all tuple-unpack call sites fixed, and Phase 99
content-model unit tests pass.
</success_criteria>

<output>
Create `.planning/phases/99-per-finding-context-code-signing-expiry/99-01-SUMMARY.md` when done.
</output>
