---
phase: 98-executive-narrative-score-transparency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/reports/content_model.py
  - tests/test_exec_content_model.py
  - tests/test_congruence_guard.py
autonomous: true
requirements: [EXEC-02, EXEC-03, TRANS-01, TRANS-03]
nyquist_compliant: true

must_haves:
  truths:
    - "build_exec_content() returns an ExecContent carrying narrative, top_risks, roadmap_items (with effort/impact + within-bucket priority ordering), subscores (all six pillars), and sev_counts"
    - "_check_congruence raises ReportCongruenceError when an EXCELLENT/GOOD/MODERATE band coexists with any CRITICAL finding, and does not raise for FAIR/POOR"
    - "Top-risks sentences are derived from the static crypto-class -> impact-band map (D-02), not per-finding 'so what' prose"
    - "Roadmap items within each NOW/NEXT/LATER bucket are ordered high-impact/low-effort first (D-04/D-05)"
  artifacts:
    - path: "quirk/reports/content_model.py"
      provides: "ExecContent/RiskItem/RoadmapItem dataclasses, ALGO_IMPACT_MAP, EFFORT_IMPACT_MAP, build_exec_content(), _check_congruence(), ReportCongruenceError"
      contains: "class ExecContent"
    - path: "tests/test_exec_content_model.py"
      provides: "Unit coverage for build_exec_content shape, top-risks, roadmap ordering, subscores pass-through"
    - path: "tests/test_congruence_guard.py"
      provides: "Unit coverage for _check_congruence band/severity cases + message format"
  key_links:
    - from: "build_exec_content"
      to: "_check_congruence"
      via: "called internally before returning ExecContent (raises before any I/O)"
      pattern: "_check_congruence\\("
    - from: "build_exec_content"
      to: "score_raw['score']"
      via: "canonical scoring-engine key (NOT compat 'total')"
      pattern: "score_raw\\[.score.\\]|score_raw\\.get\\(.score."
---

<objective>
Build the shared executive content model that both report renderers will consume (D-03). This is the single seam between scoring/findings data and the CLI/HTML surfaces: a typed `ExecContent` object, the D-02 algorithm-class -> impact-band map, the D-05 effort/impact map, the within-bucket priority ordering (D-04), and the D-06 congruence guard that fails the build before any file I/O.

Purpose: Eliminate CLI/HTML content drift at the source rather than only testing for it; make the headline-band-vs-severity contradiction structurally impossible to emit.
Output: `quirk/reports/content_model.py` plus its two unit test files. No renderer or writer wiring in this plan (that is 98-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-executive-narrative-score-transparency/98-CONTEXT.md
@.planning/phases/98-executive-narrative-score-transparency/98-RESEARCH.md
@.planning/phases/98-executive-narrative-score-transparency/98-PATTERNS.md
@.planning/phases/98-executive-narrative-score-transparency/98-UI-SPEC.md

<interfaces>
<!-- Canonical scoring-engine output shape (from compute_readiness_score) — build_exec_content RECEIVES this, NOT the writer compat wrapper.
     score_raw = {"score": int, "rating": "EXCELLENT|GOOD|MODERATE|FAIR|POOR", "subscores": {hygiene, modern_tls, identity_trust, agility_signals, data_at_rest, data_in_motion}, "drivers": [str, ...]}
     Roadmap items from build_phased_roadmap: dicts with keys phase ("NOW|NEXT|LATER"), title, why, owner_placeholder, dependencies, timeframe, _priority.
     Findings: list[dict] with keys including title, description, severity ("CRITICAL|HIGH|MEDIUM|LOW|INFO"), optionally category/check_id.
     Analog dataclass module: quirk/intelligence/schema.py. Custom-exception-subclasses-ValueError + registry-dict pattern: quirk/errors.py.
     Band->narrative-lead collapse (Claude discretion, per RESEARCH Pattern 4): EXCELLENT/GOOD -> GOOD lead; MODERATE -> FAIR lead; FAIR -> POOR lead; POOR -> CRITICAL lead.
     Copy strings (narrative leads, risk sentences, congruence error) are FIXED in UI-SPEC Copywriting Contract. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ExecContent model, static maps, builder, congruence guard</name>
  <files>quirk/reports/content_model.py</files>
  <read_first>
    - .planning/phases/98-executive-narrative-score-transparency/98-RESEARCH.md (Patterns 1, 2, 4, 5, 6; Pitfall 1 score-key mismatch)
    - .planning/phases/98-executive-narrative-score-transparency/98-PATTERNS.md (content_model.py section + Shared Patterns)
    - .planning/phases/98-executive-narrative-score-transparency/98-CONTEXT.md (D-01 through D-07)
    - .planning/phases/98-executive-narrative-score-transparency/98-UI-SPEC.md (Copywriting Contract — exact narrative-lead, risk, rollup, effort/impact, and congruence-guard strings)
    - quirk/intelligence/schema.py (dataclass module analog)
    - quirk/errors.py (custom-exception + registry-dict pattern + __all__)
  </read_first>
  <behavior>
    - build_exec_content(score_raw, findings, roadmap_items) returns an ExecContent with: narrative_lead (band-keyed, collapsed 5->4 per RESEARCH Pattern 4), narrative_drivers (from score_raw["drivers"]), top_risks (list[RiskItem]), roadmap_items (list[RoadmapItem], priority-ordered within each NOW/NEXT/LATER bucket), score_total (from score_raw["score"]), score_band (from score_raw["rating"]), subscores (all six pillar keys), raw_sum (sum of subscores), sev_counts (counted once from findings).
    - top_risks are selected from ALGO_IMPACT_MAP keyed on crypto class + severity (D-02); executive-tier framing only, NO per-finding "so what".
    - Each RoadmapItem carries effort/impact bands from EFFORT_IMPACT_MAP (D-05; default MEDIUM/MEDIUM when title not mapped) and a priority_score; within-bucket sort is high-impact/low-effort first, tie-broken by original _priority.
    - _check_congruence("GOOD", {"CRITICAL": 3}) raises ReportCongruenceError; ("EXCELLENT", {"CRITICAL": 1}) and ("MODERATE", {"CRITICAL": 1}) raise; ("FAIR", {"CRITICAL": 5}) and ("POOR", {"CRITICAL": 9}) do not.
    - ReportCongruenceError message matches the UI-SPEC Copywriting Contract congruence string exactly (band + n).
    - build_exec_content calls _check_congruence on (score_band, sev_counts) before returning — raises before any caller does I/O.
    - subscores empty-dict edge case: raw_sum = 0 without error.
  </behavior>
  <action>
    Create `quirk/reports/content_model.py` with `from __future__ import annotations`. Define plain `@dataclass` (NOT frozen/slots — these are mutable build-time objects, per PATTERNS) `RiskItem` (risk_label, impact_sentence, severity), `RoadmapItem` (phase, title, why, owner_placeholder, timeframe, effort, impact, priority_score), and `ExecContent` (narrative_lead, narrative_drivers, top_risks, roadmap_items, score_total, score_band, subscores, raw_sum, sev_counts). Define module-level dict literals `ALGO_IMPACT_MAP` (crypto_class -> (risk_label, impact_sentence)) and `EFFORT_IMPACT_MAP` (finding-type/title-keyword -> (effort, impact)) seeded from the UI-SPEC copy; `EFFORT_RANK`/`IMPACT_RANK` ordering dicts. Define `ReportCongruenceError(ValueError)`, `_check_congruence(band, sev_counts)` using a `_BAND_CRITICAL_THRESHOLD` map (EXCELLENT/GOOD/MODERATE -> 0, FAIR/POOR -> None=no restriction), and `build_exec_content(score_raw, findings, roadmap_items)`. Source score_total/subscores/band from canonical `score_raw["score"]`/`["subscores"]`/`["rating"]` (per Pitfall 1 — NOT "total"). Map the 5 scoring bands to 4 narrative leads per RESEARCH Pattern 4. Count sev_counts once from findings. Tag every definition with `# D-NN / REQ-ID:` comments. Export public names via `__all__`. No fenced implementation here — read the analogs.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m compileall quirk/reports/content_model.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -m compileall quirk/reports/content_model.py` exits 0.
    - `grep -n "class ExecContent" quirk/reports/content_model.py` matches; `grep -n "class ReportCongruenceError" quirk/reports/content_model.py` matches.
    - `grep -n "ALGO_IMPACT_MAP" quirk/reports/content_model.py` and `grep -n "EFFORT_IMPACT_MAP" quirk/reports/content_model.py` both match.
    - `python -c "import quirk.reports.content_model as m; print(m.build_exec_content, m._check_congruence)"` runs without error.
    - No top-level `import playwright` or `import pypdf` (`grep -vE '^\s*#' quirk/reports/content_model.py | grep -cE '^(import|from) (playwright|pypdf)'` returns 0).
  </acceptance_criteria>
  <done>content_model.py exists with the full shared model, both static maps, builder, and congruence guard; module imports cleanly and exposes the public API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for content model + congruence guard</name>
  <files>tests/test_exec_content_model.py, tests/test_congruence_guard.py</files>
  <read_first>
    - quirk/reports/content_model.py (the module under test — from Task 1)
    - tests/test_score_transparency.py (fixture-factory + assertion-message analog)
    - tests/test_executive_score_guard.py (error-path unit-test analog)
    - .planning/phases/98-executive-narrative-score-transparency/98-VALIDATION.md (exact pytest node IDs)
    - .planning/phases/98-executive-narrative-score-transparency/98-UI-SPEC.md (congruence error string to assert against)
  </read_first>
  <behavior>
    - test_exec_content_model.py defines node IDs: test_top_risks_populated, test_roadmap_priority_ordering, test_subscores_all_keys_present. Fixtures use the CANONICAL score_raw key shape ("score", "rating", "subscores", "drivers"), NOT "total".
    - test_top_risks_populated: build_exec_content with findings containing an RSA/ECC + CRITICAL finding yields >=1 RiskItem whose impact_sentence comes from ALGO_IMPACT_MAP.
    - test_roadmap_priority_ordering: given roadmap items in a single bucket with mixed effort/impact, the returned roadmap_items for that bucket are ordered high-impact/low-effort first.
    - test_subscores_all_keys_present: ExecContent.subscores contains all six pillar keys.
    - test_congruence_guard.py defines node IDs: test_good_band_with_critical_raises (pytest.raises ReportCongruenceError), test_fair_band_with_critical_ok (no raise). Assert the raised message matches the UI-SPEC congruence string for band+n.
  </behavior>
  <action>
    Create both test files with `from __future__ import annotations`. Import the public API from `quirk.reports.content_model`. Build canonical-shape score_raw fixtures (key "score", not "total"). Use descriptive assertion messages naming the requirement ID (per test_score_transparency.py style). Cover the exact node IDs listed in 98-VALIDATION.md. Do NOT add watch-mode flags. Do not write the integration `test_guard_blocks_report_generation` here — that belongs in 98-02 where write_reports is wired.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m pytest tests/test_exec_content_model.py tests/test_congruence_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_congruence_guard.py::test_good_band_with_critical_raises -x` exits 0.
    - `pytest tests/test_congruence_guard.py::test_fair_band_with_critical_ok -x` exits 0.
    - `pytest tests/test_exec_content_model.py::test_top_risks_populated -x` exits 0.
    - `pytest tests/test_exec_content_model.py::test_roadmap_priority_ordering -x` exits 0.
    - `pytest tests/test_exec_content_model.py::test_subscores_all_keys_present -x` exits 0.
  </acceptance_criteria>
  <done>Both test files pass against content_model.py; the exact VALIDATION.md node IDs exist and are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanner findings -> content model | Finding-derived text (titles, descriptions) enters ExecContent fields; untrusted scanner-controlled input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Tampering | top_risks impact sentences in content_model.py | mitigate | Top-risks sentences come from the static ALGO_IMPACT_MAP (template strings), not from raw finding text; any finding-derived substring placed into an ExecContent field that downstream renders into a markdown cell is escaped at the renderer in 98-02 via md_cell (HARDEN-01). content_model.py itself emits no markdown/HTML. |
| T-98-02 | Tampering | sev_counts integrity | mitigate | sev_counts computed once in build_exec_content from findings; single source feeds both the congruence guard and (in 98-02) both renderers — prevents divergent recount. |
| T-98-SC | Tampering | npm/pip/cargo installs | accept | Phase 98 adds zero new packages (RESEARCH: "No New Dependencies"); no install tasks, no Package Legitimacy Audit needed. |
</threat_model>

<verification>
- `python -m compileall quirk/reports/content_model.py` clean.
- `python -m pytest tests/test_exec_content_model.py tests/test_congruence_guard.py -x -q` green.
</verification>

<success_criteria>
- ExecContent shared model exists with narrative, top_risks, priority-ordered roadmap_items, six-pillar subscores, sev_counts.
- Congruence guard raises for incompatible band/severity, passes for FAIR/POOR, message matches UI-SPEC copy.
- Builder sources the canonical `score_raw["score"]` key (Pitfall 1 avoided).
- All VALIDATION.md node IDs for this plan are green.
</success_criteria>

<output>
Create `.planning/phases/98-executive-narrative-score-transparency/98-01-SUMMARY.md` when done.
</output>
