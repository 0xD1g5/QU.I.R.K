---
phase: 98-executive-narrative-score-transparency
plan: 03
type: execute
wave: 3
depends_on: [98-02]
files_modified:
  - tests/test_cross_surface_parity.py
  - docs/UAT-SERIES.md
autonomous: true
requirements: [EXEC-04]
nyquist_compliant: true

must_haves:
  truths:
    - "A single ExecContent instance passed to both build_exec_markdown() and render_html_report() produces identical narrative-lead text and identical top-risks count/labels in both surfaces"
    - "docs/UAT-SERIES.md reflects the new executive narrative / top-risks / roadmap-priority / rollup-formula sections and is synced to the Obsidian vault"
  artifacts:
    - path: "tests/test_cross_surface_parity.py"
      provides: "Cross-surface content identity gate (EXEC-04 corroboration, D-03a)"
      contains: "test_narrative_content_parity"
    - path: "docs/UAT-SERIES.md"
      provides: "Updated UAT cases covering Phase 98 report sections"
  key_links:
    - from: "tests/test_cross_surface_parity.py"
      to: "build_exec_content"
      via: "one instance shared to both renderers, asserted identical"
      pattern: "build_exec_content"
---

<objective>
Prove cross-surface content parity (EXEC-04) with a belt-and-suspenders test on top of the structural D-03 guarantee, and complete the project's mandatory per-phase documentation obligations: update docs/UAT-SERIES.md for the new report sections and sync it to the Obsidian vault.

Purpose: Close EXEC-04 with an automated corroboration gate (D-03a) and satisfy the project's documentation/UAT/Obsidian-sync convention required every phase.
Output: Cross-surface parity test + updated/synced UAT-SERIES.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-executive-narrative-score-transparency/98-CONTEXT.md
@.planning/phases/98-executive-narrative-score-transparency/98-PATTERNS.md
@.planning/phases/98-executive-narrative-score-transparency/98-VALIDATION.md
@.planning/phases/98-executive-narrative-score-transparency/98-02-SUMMARY.md
@quirk/reports/content_model.py
@CLAUDE.md

<interfaces>
<!-- Parity test analog: tests/test_score_render_parity.py (two-surface identity gate).
     Plan: call build_exec_content with score_raw, findings, roadmap_items ONCE; pass the same exec_content to build_exec_markdown (CLI) and render_html_report (HTML); assert exec_content.narrative_lead appears in BOTH outputs and top_risks count matches the rendered risks-list item count.
     CLAUDE.md mandatory per-phase docs steps: UAT-SERIES.md update + Obsidian vault sync (write directly to vault filesystem, not via obsidian CLI content= for large files). The UAT-SERIES.md git commit and the Obsidian phase note are execute-phase completion obligations handled by the orchestrator, not this plan. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-surface content parity test</name>
  <files>tests/test_cross_surface_parity.py</files>
  <read_first>
    - tests/test_score_render_parity.py (two-surface identity-gate analog)
    - tests/test_html_report.py (SimpleNamespace cfg factory + render-to-file-then-read)
    - quirk/reports/content_model.py (ExecContent API)
    - quirk/reports/executive.py (build_exec_markdown exec_content kwarg)
    - quirk/reports/html_renderer.py (render_html_report exec_content kwarg)
    - .planning/phases/98-executive-narrative-score-transparency/98-VALIDATION.md (node IDs test_narrative_content_parity, test_top_risks_parity)
  </read_first>
  <behavior>
    - test_narrative_content_parity: build one ExecContent; render CLI markdown and HTML from it; assert exec_content.narrative_lead substring appears in BOTH outputs.
    - test_top_risks_parity: assert the number of top_risks in ExecContent equals the count of rendered risk items in the HTML risks-list AND the count of risk bullets in the CLI markdown; labels match.
  </behavior>
  <action>
    Create tests/test_cross_surface_parity.py with a future-annotations import. Build a canonical-shape score_raw fixture (key "score") and a minimal findings list that yields at least one top-risk. Call build_exec_content once; pass the same instance to build_exec_markdown(cfg, endpoints, findings, exec_content=exec_content) and render_html_report(..., exec_content=exec_content) (write HTML to a tmp dir per the test_html_report.py pattern). Assert the VALIDATION.md node IDs test_narrative_content_parity and test_top_risks_parity. Use descriptive assertion messages naming EXEC-04. No watch-mode flags.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m pytest tests/test_cross_surface_parity.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - pytest tests/test_cross_surface_parity.py::test_narrative_content_parity -x exits 0.
    - pytest tests/test_cross_surface_parity.py::test_top_risks_parity -x exits 0.
    - Full quick suite green: pytest of test_exec_content_model.py, test_congruence_guard.py, test_exec_narrative_ordering.py, test_cross_surface_parity.py exits 0.
  </acceptance_criteria>
  <done>Cross-surface parity tests green; the same ExecContent yields identical narrative + top-risks across CLI and HTML.</done>
</task>

<task type="auto">
  <name>Task 2: Update docs/UAT-SERIES.md and sync to Obsidian vault</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - CLAUDE.md (Mandatory Phase Completion Steps 2 and 3 — UAT-SERIES.md update + vault sync procedure)
    - docs/UAT-SERIES.md (current structure, version strings, Last Updated header)
    - .planning/phases/98-executive-narrative-score-transparency/98-RESEARCH.md (Manual-Only Verification: PDF visual parity)
  </read_first>
  <action>
    Update docs/UAT-SERIES.md per CLAUDE.md Step 2: add or extend the relevant test series with cases for the Phase 98 deliverables — the Readiness Assessment narrative appearing before findings, the Priority Business Risks section, effort/impact-labelled roadmap items, the rollup-formula "How this score was computed" block, and the congruence-guard abort behavior. Include the manual PDF visual-parity check from RESEARCH.md (run quirk report on a fixture scan, open HTML, export PDF, confirm identical narrative/risks/roadmap/score-decomposition sections). Update any version strings if bumped and the Last Updated date. Then sync to the Obsidian vault by writing the framed file directly to the vault filesystem path /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md (prepend the frontmatter block per CLAUDE.md Step 3; do NOT use the obsidian CLI content= parameter — the file is too large for shell expansion). Do not git-commit here — the UAT-SERIES.md commit and the Obsidian phase note are execute-phase completion obligations the orchestrator handles.
  </action>
  <verify>
    <automated>grep -qiE "readiness assessment|priority business risk|how this score was computed" docs/UAT-SERIES.md; test -f /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/UAT-SERIES.md contains case text for the Readiness Assessment narrative, Priority Business Risks, and the rollup-formula block (grep above matches).
    - docs/UAT-SERIES.md Last Updated header reflects today's date.
    - The vault file /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md exists with the frontmatter block prepended.
  </acceptance_criteria>
  <done>UAT-SERIES.md covers the Phase 98 report sections and the manual PDF parity check; the vault copy is synced. Git commit deferred to the orchestrator's phase-completion steps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanner findings -> rendered surfaces under test | Parity test renders both surfaces from fixture findings; no new production trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-07 | Information Disclosure | cross-surface content divergence (CLI vs HTML) | mitigate | Structural single-source guarantee (D-03, 98-01/02) plus this parity test as corroboration (D-03a) — divergence is caught automatically. |
| T-98-SC | Tampering | npm/pip/cargo installs | accept | Phase 98 adds zero new packages; no install tasks. |
</threat_model>

<verification>
- `python -m pytest tests/test_cross_surface_parity.py -x -q` green.
- Full quick suite green across the four new test files.
- Full suite green before verify-work: `python -m pytest tests/ -q`.
</verification>

<success_criteria>
- Cross-surface parity gate green (EXEC-04 corroborated).
- docs/UAT-SERIES.md updated for Phase 98 sections and synced to the Obsidian vault.
</success_criteria>

<output>
Create `.planning/phases/98-executive-narrative-score-transparency/98-03-SUMMARY.md` when done.
</output>
