---
phase: 97-v5-1-tech-debt-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_credential_leakage.py
  - docs/UAT-SERIES.md
autonomous: true
requirements: [TD-01]
must_haves:
  truths:
    - "At least one sentinel leak test injects the SENTINEL through the REAL scanner/scrub path (a CryptoEndpoint whose scan_error is set by the actual exception handler) rather than asserting on data the test pre-scrubbed via safe_str"
    - "The PDF-export leak test is explicitly marked as a documented coverage gap, not coverage"
    - "docs/UAT-SERIES.md and the Obsidian vault reflect the Phase 97 changes"
  artifacts:
    - path: "tests/test_credential_leakage.py"
      provides: "≥1 real-path sentinel surface + PDF coverage-gap annotation (D-04)"
    - path: "docs/UAT-SERIES.md"
      provides: "Updated UAT series + last-updated date"
  key_links:
    - from: "real scanner exception handler"
      to: "CryptoEndpoint.scan_error → json.dumps assertion"
      via: "safe_str applied by the real path, not the test body"
      pattern: "scan_error"
---

<objective>
Close the test-honesty item D-04 (WR-05): the sentinel leak suite currently
re-asserts data the test itself already scrubbed via safe_str, so it cannot catch
a regression in the real PDF renderer or an unscrubbed write path. Route at least
ONE surface through the actual scanner/scrub path, and mark the PDF assertion as
a documented coverage gap. Then perform the mandatory CLAUDE.md phase-completion
documentation sync (UAT-SERIES.md + Obsidian phase note + UAT vault sync).

Purpose: The leak suite's "11 surfaces" claim must not overstate what is
mechanically verified — at least one surface must exercise the real scrub path
so a regression in it is actually caught.
Output: A real-path sentinel test, a PDF coverage-gap annotation, updated
UAT-SERIES.md, and synced Obsidian notes.

Scope guard: per CONTEXT.md, only ≥1 surface is routed through the real path;
a fuller leak-suite rebuild is OUT OF SCOPE. Pick the highest-value surface
(scan_error JSON or PDF render) per Claude's Discretion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-v5-1-tech-debt-cleanup/97-CONTEXT.md
@.planning/phases/97-v5-1-tech-debt-cleanup/97-PATTERNS.md

<interfaces>
<!-- tests/test_credential_leakage.py: -->
<!--   SENTINEL = "QUIRK_SENTINEL_CRED_d41d8cd9"  (line 38) -->
<!--   test_sentinel_not_in_scan_error_json  (lines 145-158) — pre-scrubs via safe_str in test body -->
<!--   test_sentinel_not_in_db_row  (lines 161-226) — BEST real-path analog: real from_cli + safe_str + DB write/read -->
<!--   test_sentinel_not_in_pdf_export_surface  (lines 303-356) — to be annotated as a documented coverage gap -->
<!-- The real path: CryptoEndpoint.scan_error is written via safe_str in the scanner's _wrapped_phase / exception handler. -->
<!-- Goal: build a CryptoEndpoint whose scan_error is set by calling the REAL exception handler with a SENTINEL-bearing input. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route ≥1 sentinel surface through the real scrub path; annotate PDF as coverage gap (D-04 / WR-05)</name>
  <files>tests/test_credential_leakage.py</files>
  <read_first>
    - tests/test_credential_leakage.py (the file being modified — read SENTINEL at line 38, test_sentinel_not_in_scan_error_json at 145-158, the test_sentinel_not_in_db_row real-path analog at 161-226, and test_sentinel_not_in_pdf_export_surface at 303-356)
    - quirk/util/safe_exc.py (the safe_str helper exercised by the real path — confirm what it scrubs so the test asserts the genuine scrub behavior, not a test-body pre-scrub)
    - .planning/phases/97-v5-1-tech-debt-cleanup/97-PATTERNS.md (§test_credential_leakage.py D-04 — the real-path template and which surface to route)
    - .planning/milestones/v5.1-phases/93-credential-infrastructure/93-REVIEW.md §WR-05 (lines 253-267 — the assert-on-pre-scrubbed-data finding and the real-write-path fix the user chose)
  </read_first>
  <behavior>
    - Test 1 (real-path scrub): a CryptoEndpoint whose scan_error is populated by the ACTUAL scanner exception handler (or _wrapped_phase equivalent) given a SENTINEL-bearing exception input — NOT by calling safe_str in the test body — has its scan_error asserted free of SENTINEL via json.dumps. If safe_str were removed from the real path, this test would FAIL (the distinguishing property vs. the pre-scrubbed test).
    - Test 2 (negative control, optional but recommended): asserting the same path WITHOUT scrubbing would leak SENTINEL — documents why the real-path test has teeth.
  </behavior>
  <action>
    Modify ONE existing surface (prefer test_sentinel_not_in_scan_error_json at 145-158,
    the highest-value JSON surface, per Claude's Discretion) so the SENTINEL reaches
    scan_error through the REAL scanner exception path: invoke the actual exception
    handler / _wrapped_phase path with a SENTINEL-bearing input so the safe_str scrub is
    applied by production code, then assert SENTINEL is absent from
    json.dumps({"scan_error": ep.scan_error}). Model the structure on
    test_sentinel_not_in_db_row (161-226) which already drives the real from_cli +
    safe_str path. Do NOT call safe_str in the test body for this surface — that is the
    whole point. Separately, add a docstring/comment to
    test_sentinel_not_in_pdf_export_surface (303-356) explicitly stating it is a
    DOCUMENTED COVERAGE GAP (no live PDF render performed — requires running server +
    Playwright Chromium) and therefore is not mechanical coverage of the PDF renderer.
    Leave the remaining sentinel tests as-is (fuller rebuild is out of scope). Do NOT
    weaken or delete the existing pre-scrubbed assertions — only add the real-path one
    and annotate the PDF gap.
  </action>
  <verify>
    <automated>python -m pytest tests/test_credential_leakage.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: at least one test in the file populates scan_error via the real scanner exception path (no `safe_str(` call in that test's body before constructing the CryptoEndpoint).
    - Source assertion: test_sentinel_not_in_pdf_export_surface carries an explicit "coverage gap" / "documented coverage gap" annotation in its docstring or a comment.
    - Behavior assertion (Test 1): the real-path test passes (SENTINEL absent from json.dumps of scan_error).
    - Behavior assertion: the real-path test would FAIL if safe_str were removed from the production path (it asserts genuine scrub behavior, not a test-body pre-scrub). Demonstrate with the negative-control reasoning or a temporary local check; do not commit a broken production change.
    - `python -m pytest tests/test_credential_leakage.py -q` passes.
  </acceptance_criteria>
  <done>≥1 sentinel surface is routed through the real scanner/scrub path (would fail if scrubbing regressed), the PDF test is annotated as a documented coverage gap, and the remaining tests are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Mandatory phase-completion docs sync — UAT-SERIES.md, Obsidian phase note, UAT vault sync</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (the file being updated — read the header `**Last Updated:**` line and the credential / scheduler / fuzzer-related series to find where Phase 97 cleanup changes belong)
    - ./CLAUDE.md (§Mandatory Phase Completion Steps — the exact required steps: Obsidian phase note path, UAT-SERIES update rules, UAT vault sync printf/cat/cp pattern, and the commit step)
    - .planning/phases/97-v5-1-tech-debt-cleanup/97-01-SUMMARY.md, 97-02-SUMMARY.md, 97-03-SUMMARY.md (peer-plan summaries to source "What Was Built" — only those that exist at execution time; this plan runs in the same wave, so read whichever are present)
  </read_first>
  <action>
    Per CLAUDE.md Mandatory Phase Completion Steps:
    (1) Update docs/UAT-SERIES.md — reflect the Phase 97 cleanup: note the corrected
    credential env-var docstring (D-01), the scheduler parse-based fail-closed
    auth-reject (D-05, a behavior change a UAT step may exercise), the JWT
    pre-existing-param reject (D-03), and the fuzzer cascade back-off now tripping on
    connection exceptions (D-06). Add or amend test cases in the relevant series; bump
    the `**Last Updated:**` date at the top to today (2026-05-23). No version string
    bump is expected (cleanup phase, no release).
    (2) Create the Obsidian phase note by WRITING DIRECTLY to the vault filesystem (do
    NOT use the obsidian CLI content= parameter — files are too large for shell
    expansion) at
    /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-97-v5.1-Tech-Debt-Cleanup.md
    with frontmatter (project: QU.I.R.K., type: phase, status: complete,
    source: .planning/phases/97-v5-1-tech-debt-cleanup/, updated: 2026-05-23) + Goal +
    Requirements Covered (TD-01, TD-02) + Success Criteria + What Was Built (one
    subsection per plan, sourced from the 97-0N-SUMMARY.md files) + a [[Roadmap]] link.
    Follow the existing Phase note template at /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/.
    (3) Sync UAT-SERIES.md to the vault by writing directly to the vault filesystem
    using the printf-frontmatter + cat-append + cp pattern documented in CLAUDE.md,
    targeting /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md.
    (4) Commit docs/UAT-SERIES.md via the gsd-tools commit helper as documented in
    CLAUDE.md.
  </action>
  <verify>
    <automated>grep -q "2026-05-23" docs/UAT-SERIES.md && test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-97-v5.1-Tech-Debt-Cleanup.md" && test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: docs/UAT-SERIES.md `**Last Updated:**` line shows 2026-05-23 and references the Phase 97 cleanup changes (at minimum the scheduler fail-closed and fuzzer cascade behavior changes).
    - Artifact assertion: the Obsidian phase note exists at /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-97-v5.1-Tech-Debt-Cleanup.md with status: complete frontmatter and a "What Was Built" subsection per plan.
    - Artifact assertion: /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md exists and matches the updated docs/UAT-SERIES.md content (plus vault frontmatter).
    - docs/UAT-SERIES.md committed via the gsd-tools commit helper.
  </acceptance_criteria>
  <done>UAT-SERIES.md updated and committed, the Obsidian phase note created with per-plan What-Was-Built sections, and the UAT series synced to the vault — all four CLAUDE.md mandatory completion steps satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanner exception → scan_error stored/rendered surface | A credential-bearing exception must be scrubbed (safe_str) before it lands in any persisted/rendered surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-11 | Information Disclosure | sentinel leak test honesty (D-04) | mitigate | This change STRENGTHENS verification of an existing leak control: routing ≥1 surface through the real scrub path means a future regression that removes safe_str from the production scan_error path is actually caught by CI, instead of being masked by a test that pre-scrubs its own input. The PDF coverage-gap annotation prevents overstating coverage. No control is weakened. |
| T-97-12 | Information Disclosure | docs/vault notes referencing credential fixes | accept | The phase note and UAT series describe behavior (fail-closed reject, cascade back-off, docstring correction) — no secrets, no SENTINEL value, no credential material is written into docs or the vault. |
| T-97-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. N/A — no new dependencies. |

**Control-weakening check:** D-04 only increases the rigor of leak-suite verification
(real-path test would fail on a scrub regression) and honestly labels the PDF gap. No
runtime control is altered. The docs sync writes behavior descriptions only, no secrets.
</threat_model>

<verification>
- `python -m pytest tests/test_credential_leakage.py -q` passes
- ≥1 sentinel surface routed through the real scrub path (no test-body safe_str for that surface)
- PDF test annotated as a documented coverage gap
- docs/UAT-SERIES.md updated (2026-05-23) and committed
- Obsidian phase note + UAT vault sync files exist
</verification>

<success_criteria>
At least one sentinel leak test exercises the real scanner/scrub path (and would
fail on a scrub regression), the PDF test is honestly labeled a coverage gap, and
all four CLAUDE.md mandatory phase-completion documentation steps are done.
Contributes to Phase 97 Success Criterion 1 (safe credential handling, honestly verified).
</success_criteria>

<output>
Create `.planning/phases/97-v5-1-tech-debt-cleanup/97-04-SUMMARY.md` when done
</output>
