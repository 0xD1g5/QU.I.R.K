---
phase: 97-v5-1-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/auth/credentials.py
autonomous: true
requirements: [TD-01]
must_haves:
  truths:
    - "A developer reading from_cli sees an env-var contract docstring that matches the actual `ref in os.environ` behavior (no all-caps claim)"
    - "as_headers/query_param each carry a comment documenting the accepted per-call str-copy proliferation under D-05 and its once-per-endpoint-per-scan call bound"
  artifacts:
    - path: "quirk/auth/credentials.py"
      provides: "Corrected from_cli docstring (D-01) + D-02 proliferation comments"
      contains: "any name present in the environment"
  key_links: []
---

<objective>
Close the two zero/near-zero-behavior-change credential documentation items from
93-REVIEW (WR-02, WR-03) per locked decisions D-01 and D-02. This plan changes
docstrings and comments ONLY — no runtime behavior changes, no signature changes.

Purpose: The codebase entering the v5.2 report milestone must present
unambiguous, accurate credential-handling documentation. WR-02 is a
docstring/behavior mismatch (the "all-caps" contract is documented but
unenforced); WR-03 is an accepted-but-undocumented str-copy proliferation that
v5.1 D-05 already owns.
Output: quirk/auth/credentials.py with a corrected from_cli env-var docstring
and explicit D-02 proliferation comments on as_headers and query_param.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-v5-1-tech-debt-cleanup/97-CONTEXT.md
@.planning/phases/97-v5-1-tech-debt-cleanup/97-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct from_cli env-var docstring (D-01 / WR-02)</name>
  <files>quirk/auth/credentials.py</files>
  <read_first>
    - quirk/auth/credentials.py (the file being modified — read from_cli at lines 115-169 and _resolve_reference env-var block at lines ~206-222 to confirm the actual `ref in os.environ` behavior the docstring must match)
    - .planning/phases/97-v5-1-tech-debt-cleanup/97-PATTERNS.md (§credentials.py D-01 — the env-var analog block and the decision-tag comment style)
    - .planning/milestones/v5.1-phases/93-credential-infrastructure/93-REVIEW.md §WR-02 (lines 209-222 — the docstring/behavior mismatch and the document-it fix option the user chose)
  </read_first>
  <action>
    In from_cli's docstring (the bulleted env-var line currently reading
    "Looks like an env-var name (all-caps, set in environment): read and delete
    the env var ..." at credentials.py:~129), replace the "all-caps" wording with
    a statement matching the actual implementation: any name present in the
    environment is read and deleted (no all-caps requirement). Keep the
    parenthetical rationale about preventing subprocess inheritance. Do NOT add an
    isupper() check anywhere — this is a docstring-only correction (D-01, zero
    behavior change). Reference the decision tag in the line if it aids
    traceability, matching the existing `(D-NN)` comment style in this module.
  </action>
  <verify>
    <automated>grep -q "any name present in the environment" quirk/auth/credentials.py && ! grep -qi "all-caps" quirk/auth/credentials.py && python -m compileall quirk/auth/credentials.py && python -m pytest tests/test_credential_context.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: from_cli docstring contains the substring "any name present in the environment".
    - Source assertion: the string "all-caps" no longer appears anywhere in credentials.py.
    - Behavior assertion: no `isupper()` or case-validation guard added — `_resolve_reference` env-var branch remains `if ref in os.environ` unchanged.
    - `python -m compileall quirk/auth/credentials.py` exits 0.
    - `python -m pytest tests/test_credential_context.py -q` passes (no behavior regression).
  </acceptance_criteria>
  <done>The from_cli env-var docstring matches the actual `ref in os.environ` behavior; "all-caps" is gone; no behavior changed.</done>
</task>

<task type="auto">
  <name>Task 2: Document accepted str-copy proliferation on as_headers / query_param (D-02 / WR-03)</name>
  <files>quirk/auth/credentials.py</files>
  <read_first>
    - quirk/auth/credentials.py (as_headers at lines 53-66 and query_param at lines 68-77 — the two `self._secret_buf.decode("utf-8")` sites that materialize a new immortal str per call)
    - .planning/phases/97-v5-1-tech-debt-cleanup/97-PATTERNS.md (§credentials.py D-02 — the str-only-at-injection comment style and the line-65 `(D-NN)` inline-comment precedent)
    - .planning/milestones/v5.1-phases/93-credential-infrastructure/93-REVIEW.md §WR-03 (lines 224-238 — the proliferation finding and the "document + bound under D-05" option the user chose)
  </read_first>
  <action>
    Add an inline comment directly above the `secret = self._secret_buf.decode("utf-8")`
    line in BOTH as_headers (line ~58) and query_param (line ~76). Each comment must
    state, in directive prose: (1) this decode materializes a new immortal str the GC
    controls and close() cannot zero; (2) this str-copy proliferation is explicitly
    accepted under v5.1 D-05 (best-effort zeroization); (3) the call is bounded —
    as_headers is invoked once per scan_jwt_endpoint and query_param once per _get,
    i.e. once per endpoint per scan. Reference the decision tags (D-05, and D-04 where
    the existing docstring already cites it) using the existing `(D-NN)` comment style.
    Each comment MUST contain the literal token `D-05` so the per-site gate can confirm
    annotation at both functions. Do NOT refactor to materialize-once, do NOT thread a
    pre-built dict through call sites, do NOT change either signature or return type
    (D-02 — document, not refactor).
  </action>
  <verify>
    <automated>grep -A5 'def as_headers' quirk/auth/credentials.py | grep -q 'D-05' && grep -A8 'def query_param' quirk/auth/credentials.py | grep -q 'D-05' && python -m compileall quirk/auth/credentials.py && python -m pytest tests/test_credential_context.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: a comment containing the token `D-05` and mentioning the accepted str-copy proliferation appears immediately above the decode in BOTH as_headers and query_param (two distinct comment sites — confirmed independently by the per-function grep).
    - Source assertion: each comment notes the once-per-endpoint-per-scan call bound.
    - Behavior assertion: as_headers and query_param signatures and return types are unchanged; both still call `self._secret_buf.decode("utf-8")` exactly as before (no materialize-once refactor).
    - `python -m compileall quirk/auth/credentials.py` exits 0.
    - `python -m pytest tests/test_credential_context.py -q` passes.
  </acceptance_criteria>
  <done>Both decode sites carry a D-02 comment documenting the accepted proliferation and call bound; no runtime behavior changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arg → CredentialContext | Operator-supplied credential reference resolved to an in-memory secret |
| in-memory secret → log/header/URL | Secret materialized as str at injection boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | Information Disclosure | from_cli docstring correction (D-01) | accept | Docstring-only change; no code path altered, so no new disclosure surface. The unenforced "all-caps" claim was documentation, not a control — correcting it removes a misleading contract without weakening any guard. |
| T-97-02 | Information Disclosure | as_headers/query_param comments (D-02) | accept | Comment-only change; the str-copy proliferation it documents already exists and is owned by v5.1 D-05. No new secret materialization is introduced; the once-per-endpoint-per-scan bound is unchanged. No control is weakened. |
| T-97-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; N/A — no new dependencies added. |

**Control-weakening check (per phase threat-model directive):** Neither task touches a
runtime control. D-01 corrects a docstring whose "all-caps" claim was never enforced;
removing it cannot weaken a guard that never existed. D-02 documents already-shipped
behavior accepted under D-05. No fail-open/fail-closed semantics change.
</threat_model>

<verification>
- `grep -q "any name present in the environment" quirk/auth/credentials.py` (D-01)
- `! grep -qi "all-caps" quirk/auth/credentials.py` (D-01)
- D-02 comments present at both decode sites referencing D-05 and the call bound (confirmed per-function: `grep -A5 'def as_headers' ... | grep -q 'D-05'` and `grep -A8 'def query_param' ... | grep -q 'D-05'`)
- `python -m compileall quirk/auth/credentials.py` exits 0
- `python -m pytest tests/test_credential_context.py tests/test_credential_leakage.py -q` passes (no regression)
</verification>

<success_criteria>
The from_cli env-var docstring matches actual behavior (no all-caps claim), both
decode sites document the accepted D-05 proliferation and call bound, and no
runtime behavior changed (credential tests still pass). Maps to Phase 97 Success
Criterion 1 (unambiguous, safe credential handling visible in the code).
</success_criteria>

<output>
Create `.planning/phases/97-v5-1-tech-debt-cleanup/97-01-SUMMARY.md` when done
</output>
