---
phase: 66
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_dashboard_scan_history.py
autonomous: true
requirements:
  - UI-HIST-01
  - UI-HIST-02
must_haves:
  truths:
    - "A failing pytest file exists exercising every UI-HIST-01 and UI-HIST-02 behavior listed in VALIDATION.md"
    - "Subsequent plans can run `pytest tests/test_dashboard_scan_history.py -x -q` and see RED tests, not collection errors"
  artifacts:
    - path: "tests/test_dashboard_scan_history.py"
      provides: "Wave 0 test scaffold for scan history and compare endpoints"
      contains: "test_list_scans_schema, test_list_scans_no_limit, test_clone_data_recovery, test_clone_reconstruction, test_compare_schema, test_compare_self, test_compare_score_delta, test_compare_finding_diff, test_compare_endpoint_diff"
  key_links:
    - from: "tests/test_dashboard_scan_history.py"
      to: "quirk.dashboard.api.app.create_app"
      via: "TestClient + dependency_overrides[get_db]"
      pattern: "TestClient\\(app\\)"
---

<objective>
Create the Wave 0 pytest scaffold required by VALIDATION.md so that Plans 02 and 03 have a real failing test target from their first commit. The file MUST collect successfully (no import errors), instantiate a TestClient bound to an in-memory shared-cache SQLite DB, and contain stubbed test functions for every row in VALIDATION.md §Per-Task Verification Map.

Purpose: Enforce Nyquist compliance — every executor task has an automated verify command pointing at a real test function.
Output: One new file `tests/test_dashboard_scan_history.py` collecting 9 RED tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-dashboard-scan-history-clone-compare/66-CONTEXT.md
@.planning/phases/66-dashboard-scan-history-clone-compare/66-RESEARCH.md
@.planning/phases/66-dashboard-scan-history-clone-compare/66-PATTERNS.md
@.planning/phases/66-dashboard-scan-history-clone-compare/66-VALIDATION.md

<interfaces>
From quirk/dashboard/api/schemas.py:
- class ScanSession(BaseModel): scan_id: str; scanned_at: datetime; total_endpoints: int
- class FindingCounts(BaseModel): high: int = 0; medium: int = 0; low: int = 0
- class SubScores(BaseModel): hygiene, modern_tls, identity_trust, agility_signals, data_at_rest, data_in_motion

From quirk/models.py:
- class CryptoEndpoint(Base): id, host, port, protocol, severity, scanned_at, scan_run_id, scan_error, tls_version, cipher_suite, cert_pubkey_alg
- class ScanJob(Base): scan_run_id (String, nullable=True), target, profile, calibration

From quirk/dashboard/api/app.py:
- create_app() -> FastAPI
- get_db dependency from quirk.dashboard.api.deps

Existing analog: tests/test_dashboard_trends.py (shared-cache SQLite fixture pattern)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create failing test scaffold tests/test_dashboard_scan_history.py</name>
  <files>tests/test_dashboard_scan_history.py</files>
  <read_first>
    - tests/test_dashboard_trends.py (copy the `_make_client_and_session()` shared-cache SQLite fixture verbatim — same pattern, different module name)
    - quirk/dashboard/api/schemas.py (confirm ScanSession, FindingCounts, SubScores field shapes)
    - quirk/models.py (confirm CryptoEndpoint and ScanJob columns: host, port, protocol, severity, scanned_at, scan_run_id, scan_error, tls_version, cipher_suite, cert_pubkey_alg; ScanJob.scan_run_id, target, profile, calibration)
    - .planning/phases/66-dashboard-scan-history-clone-compare/66-VALIDATION.md (the 9 test names below come from §Per-Task Verification Map)
    - .planning/phases/66-dashboard-scan-history-clone-compare/66-PATTERNS.md (§"Test Fixture Pattern (seeded DB)" lines 738–770)
  </read_first>
  <action>
Create `tests/test_dashboard_scan_history.py` with the structure below. The file must collect (no syntax / import errors), the fixture must work, and each test body must contain at least one `assert` that will currently FAIL because the production code does not yet support the new shape. This is intentional — we want RED, not skips.

Required imports and fixture (copy-adapt from `tests/test_dashboard_trends.py::_make_uat31_client_and_session`):

```python
from __future__ import annotations

import uuid
from datetime import datetime, timedelta

import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker

from quirk.dashboard.api.app import create_app
from quirk.dashboard.api.deps import get_db
from quirk.models import Base, CryptoEndpoint, ScanJob


def _make_client_and_session():
    db_name = f"test_scan_history_{uuid.uuid4().hex}"
    engine = create_engine(
        f"sqlite:///file:{db_name}?mode=memory&cache=shared&uri=true",
        connect_args={"check_same_thread": False},
    )
    Base.metadata.create_all(engine)
    TestingSession = sessionmaker(bind=engine, autoflush=False, autocommit=False)

    def _override_get_db():
        db = TestingSession()
        try:
            yield db
        finally:
            db.close()

    app = create_app()
    app.dependency_overrides[get_db] = _override_get_db
    return TestClient(app), TestingSession
```

Then add EXACTLY these 9 test functions (names are load-bearing — VALIDATION.md references each by name):

1. `test_list_scans_schema` — seed 1 session of 2 CryptoEndpoint rows (host="10.0.0.1", port=443, protocol="tls", severity="HIGH", scanned_at=datetime.utcnow()). GET `/api/scans`. Assert response is 200 and the first item contains keys: `scan_id, scanned_at, total_endpoints, score, profile, calibration, target, finding_counts` AND `finding_counts` has keys `high, medium, low`.

2. `test_list_scans_no_limit` — seed 12 distinct sessions (loop, scanned_at = utcnow - timedelta(minutes=i) for i in range(12)). GET `/api/scans`. Assert `len(response.json()) == 12` (the LIMIT 10 cap is gone).

3. `test_clone_data_recovery` — seed 1 CryptoEndpoint session AND a matching `ScanJob(scan_run_id=<ts_str of session>, target="dashboard.example.com", profile="full", calibration="thorough")`. GET `/api/scans`. Assert the response item has `target == "dashboard.example.com"`, `profile == "full"`, `calibration == "thorough"`.

4. `test_clone_reconstruction` — seed 1 session with 2 CryptoEndpoint rows (hosts "cli-a.example.com" and "cli-b.example.com"), NO ScanJob row. GET `/api/scans`. Assert item has `profile is None`, `calibration is None`, AND `target` contains both `"cli-a.example.com"` and `"cli-b.example.com"` (use `assert "cli-a.example.com" in item["target"]`).

5. `test_compare_schema` — seed 2 sessions A (newer) and B (older), each with 1 CryptoEndpoint (different hosts, severity="HIGH"). GET `/api/compare?a=<ts_a>&b=<ts_b>`. Assert 200 and response keys: `scan_a, scan_b, score_delta, subscore_deltas, added_findings, removed_findings, endpoints_only_in_a, endpoints_only_in_b, changed_endpoints`. Assert `subscore_deltas` contains all 6 pillar keys: `hygiene, modern_tls, identity_trust, agility_signals, data_at_rest, data_in_motion`.

6. `test_compare_self` — seed 1 session. GET `/api/compare?a=<ts>&b=<ts>`. Assert `response.status_code == 400` AND `response.json()["detail"] == "Cannot compare a scan to itself."`.

7. `test_compare_score_delta` — seed session A with all-good TLS (tls_version="TLSv1.3", cipher_suite="TLS_AES_256_GCM_SHA384", severity="INFO") and session B with weak TLS (tls_version="TLSv1.0", severity="HIGH"). GET `/api/compare?a=<ts_a>&b=<ts_b>`. Assert `response.json()["score_delta"] > 0` (A scores higher than B).

8. `test_compare_finding_diff` — seed A with CryptoEndpoint(host="x.example.com", protocol="tls", severity="HIGH"), seed B with CryptoEndpoint(host="y.example.com", protocol="tls", severity="HIGH"). GET compare. Assert `len(response.json()["added_findings"]) >= 1` (x is in A not B) AND `len(response.json()["removed_findings"]) >= 1` (y is in B not A).

9. `test_compare_endpoint_diff` — seed A with hosts {"shared.example.com", "only-a.example.com"} and B with hosts {"shared.example.com", "only-b.example.com"}. Have shared.example.com differ between A and B on `tls_version` (TLSv1.3 vs TLSv1.0). GET compare. Assert `"only-a.example.com" in response.json()["endpoints_only_in_a"]`, `"only-b.example.com" in response.json()["endpoints_only_in_b"]`, AND that `changed_endpoints` contains an entry with `host == "shared.example.com"`.

Helper utility (inline in the file): a `_seed_session(TestingSession, scanned_at, endpoints: list[dict])` helper that creates CryptoEndpoint rows with the given `scanned_at` datetime and fields. Use `scan_run_id = scanned_at.isoformat()` so ScanJob joins work.

All tests use the same shared-cache pattern: open a `TestingSession()` for seeding, commit, then call `client.get(...)`.

These tests MUST fail on first run because:
- `/api/scans` does not yet return `score, profile, calibration, target, finding_counts` (Plan 02 adds them) and still has LIMIT 10
- `/api/compare` does not exist yet (Plan 02 adds it)

DO NOT use `pytest.skip` or `pytest.xfail` — let them fail RED.
  </action>
  <verify>
    <automated>python -m pytest tests/test_dashboard_scan_history.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f tests/test_dashboard_scan_history.py`
    - `grep -c "def test_list_scans_schema\|def test_list_scans_no_limit\|def test_clone_data_recovery\|def test_clone_reconstruction\|def test_compare_schema\|def test_compare_self\|def test_compare_score_delta\|def test_compare_finding_diff\|def test_compare_endpoint_diff" tests/test_dashboard_scan_history.py` returns `9`
    - `grep -c "def _make_client_and_session" tests/test_dashboard_scan_history.py` returns `1`
    - `grep -q "mode=memory&cache=shared" tests/test_dashboard_scan_history.py` succeeds
    - `python -m pytest tests/test_dashboard_scan_history.py --collect-only -q` exits 0 and lists 9 tests
    - `python -m pytest tests/test_dashboard_scan_history.py -x -q` exits NON-ZERO (RED — production code does not yet support new shape; this is expected and proves the tests assert real behavior)
    - `python -m compileall tests/test_dashboard_scan_history.py` exits 0
  </acceptance_criteria>
  <done>The scaffold file collects 9 tests and they fail for the right reason (assertion failure or HTTP 404/422 from missing endpoint), not for collection / import / syntax reasons. Plans 02 and 03 will turn each test GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → in-memory SQLite | tests run in-process; no external IO |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-W0-01 | Information Disclosure | test fixture DB | accept | shared-cache in-memory SQLite, per-process unique name (uuid4 suffix) — no persistence, no PII |

(T-66-01 and T-66-02 from RESEARCH.md are about production endpoints, not the test scaffold — they are addressed in Plans 02/03.)
</threat_model>

<verification>
- `python -m pytest tests/test_dashboard_scan_history.py --collect-only -q` lists exactly 9 tests
- `python -m compileall tests/` exits 0
- Running the tests fails on assertions / 404, NOT on imports
</verification>

<success_criteria>
- File `tests/test_dashboard_scan_history.py` exists
- Exactly 9 test functions, names match VALIDATION.md verbatim
- Each test seeds DB via shared-cache SQLite and exercises `/api/scans` or `/api/compare`
- Collection succeeds; execution fails (RED) — this is the desired Wave 0 state
</success_criteria>

<output>
After completion, create `.planning/phases/66-dashboard-scan-history-clone-compare/66-01-SUMMARY.md`
</output>
