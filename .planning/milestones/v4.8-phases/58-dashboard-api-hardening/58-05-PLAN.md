---
phase: 58-dashboard-api-hardening
plan: 05
type: tdd
wave: 2
depends_on:
  - 58-03
files_modified:
  - tests/test_cli_init.py
  - tests/test_targets_parser.py
autonomous: true
requirements:
  - HARDEN-API-04
  - HARDEN-API-06

must_haves:
  truths:
    - "50+ traversal patterns are tested against quirk init --output and each is rejected"
    - "TargetFileError is raised for @file paths outside CWD"
    - "TargetFileError is raised for @file paths under /etc /proc /sys /dev"
    - "TargetFileError reason codes match exactly: path_traversal, path_not_allowed_prefix, target_file_too_large, target_file_too_many_lines"
    - "Size cap test: file > 1 MB raises TargetFileError with reason target_file_too_large"
    - "Line cap test: file > 10000 lines raises TargetFileError with reason target_file_too_many_lines"
    - "pytest test_cli_init.py and test_targets_parser.py exit 0"
  artifacts:
    - path: "tests/test_cli_init.py"
      provides: "path-traversal fuzz corpus (50+ patterns) for quirk init --output"
      contains: "test_init_rejects_traversal_paths"
    - path: "tests/test_targets_parser.py"
      provides: "@file guard tests for TargetFileError and all 4 reason codes"
      contains: "TargetFileError"
  key_links:
    - from: "tests/test_cli_init.py"
      to: "quirk.cli.init_cmd.run_init"
      via: "direct call with bad_path; assert rejection message in stdout/stderr"
      pattern: "run_init"
    - from: "tests/test_targets_parser.py"
      to: "quirk.util.targets.parse_target_tokens"
      via: "pytest.raises(TargetFileError)"
      pattern: "TargetFileError"
---

<objective>
Write TDD tests for the CLI path-traversal guard and the @file target guards implemented in Plan 03. The fuzz corpus for quirk init (50+ traversal patterns) and the TargetFileError reason-code tests are the permanent regression gates for CR-01 and CR-09.

Purpose: These tests are the acceptance proof for HARDEN-API-04 and HARDEN-API-06. A future refactor of init_cmd.py or targets.py that accidentally removes the guard will immediately fail CI.

Output: Extended tests/test_cli_init.py with parametrized fuzz corpus; extended tests/test_targets_parser.py with TargetFileError guard tests.
</objective>

<execution_context>
@/Users/digs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/digs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/58-dashboard-api-hardening/58-CONTEXT.md
@.planning/phases/58-dashboard-api-hardening/58-PATTERNS.md

<interfaces>
<!-- Contracts from Plan 03 that tests must verify. -->

From quirk/cli/init_cmd.py (Plan 03 guard):
- run_init(output_path) warns and returns (does not raise) on traversal paths
- Warning message contains "resolves outside the current working" or "path-traversal segments"
- Check capsys.readouterr() — output on stdout or stderr contains warning keyword

From quirk/util/targets.py (Plan 03 guard):
```python
class TargetFileError(ValueError):
    path: str
    reason: str  # one of RC_* constants

RC_PATH_TRAVERSAL = "path_traversal"
RC_PATH_NOT_ALLOWED_PREFIX = "path_not_allowed_prefix"
RC_TARGET_FILE_TOO_LARGE = "target_file_too_large"
RC_TARGET_FILE_TOO_MANY_LINES = "target_file_too_many_lines"

# parse_target_tokens raises TargetFileError before load_targets_file()
```

From tests/util/test_subprocess_input.py (existing pattern to mirror):
- @pytest.mark.parametrize with ids=lambda pattern for readable test IDs
- Each test: single assertion on the reject outcome
</interfaces>
</context>

<feature>
  <name>CLI path-traversal fuzz corpus and @file TargetFileError tests</name>
  <files>tests/test_cli_init.py, tests/test_targets_parser.py</files>
  <behavior>
    RED phase — both test files updated with new test functions that initially fail
    (before Plan 03 guards are in place, traversal paths would succeed):

    test_cli_init.py additions:
    - test_init_rejects_traversal_paths(bad_path, capsys, tmp_path):
      Parametrized over 50+ patterns. For each bad_path, call run_init(bad_path) from
      within a tmp_path (to prevent any accidental writes). Assert that stdout+stderr
      contains at least one of: "outside", "traversal", "not allowed", "warning".
      Patterns include: ../evil.yaml, ../../etc/passwd, subdir/../../evil.yaml,
      /tmp/evil.yaml, /etc/passwd, /var/log/evil, file\x00.yaml, ..\\evil.yaml,
      a/b/c/../../../../../../../etc/passwd, plus 40+ more covering unicode, null bytes,
      URL encoding, Windows-style separators, deep nesting, and absolute paths.

    test_targets_parser.py additions:
    - test_at_file_outside_cwd_raises_path_traversal(tmp_path):
      Create a file at tmp_path/evil.txt. Call parse_target_tokens("@../evil.txt") from
      a subprocess-CWD context. Assert TargetFileError is raised with reason = "path_traversal".
    - test_at_file_blocked_prefix_raises_path_not_allowed(tmp_path):
      Mock os.path.realpath for a path starting with /etc. Assert reason = "path_not_allowed_prefix".
    - test_at_file_too_large_raises(tmp_path):
      Create a file > 1 MB. Call parse_target_tokens("@large.txt") from tmp_path.
      Assert TargetFileError with reason = "target_file_too_large".
    - test_at_file_too_many_lines_raises(tmp_path):
      Create a file with 10001 lines. Assert TargetFileError with reason = "target_file_too_many_lines".
    - test_at_file_reason_codes_are_correct_strings():
      Assert RC_PATH_TRAVERSAL == "path_traversal", etc. — locks exact reason-code strings.

    GREEN phase — Plan 03 guards are already implemented; tests should pass immediately.
    If tests fail in RED (they should — the guards exist, but the test file is new),
    investigate whether the test is correctly structured.
  </behavior>
  <implementation>
    Read the existing test_cli_init.py and test_targets_parser.py in full before adding tests.
    Append new test functions — do not delete existing tests.

    For the parametrize corpus in test_cli_init.py, include AT LEAST these 50 patterns:
    "../evil.yaml", "../../etc/passwd", "subdir/../../evil.yaml",
    "../../../tmp/evil", "/tmp/evil.yaml", "/etc/passwd", "/var/log/evil.yaml",
    "/root/.ssh/authorized_keys", "/home/user/.bashrc", "/.secret",
    "file\x00.yaml", "fi\x01le.yaml", "fil\x1ble.yaml",
    "..\\/evil.yaml", "..\\evil.yaml", "..%2Fevil.yaml", "..%5Cevil.yaml",
    "%2e%2e/evil.yaml", "%2e%2e%2fevil.yaml",
    "a/b/c/../../../../../../../etc/passwd",
    "a/../../etc/shadow", "x/y/../../../../../../tmp/evil",
    "....//evil", ".../evil", "..../evil",
    "normal/../evil.yaml", "normal/../../evil.yaml",
    "/proc/self/environ", "/proc/self/cmdline", "/sys/kernel/debug",
    "/dev/sda", "/dev/null/../etc/passwd",
    "~/.ssh/authorized_keys", "~/../../etc/passwd",
    "COM1", "PRN", "AUX",  # Windows device names (defense-in-depth)
    "safe/../../evil", "./good/../../../evil",
    "a" * 4096 + "/../evil",  # very long path with traversal
    "normal/" + "../" * 20 + "evil",
    "/absolute/path/outside/cwd",
    "/usr/bin/evil", "/opt/evil",
    "subdir/./../..",

    For test_targets_parser.py, use pytest's tmp_path fixture and monkeypatch or
    os.chdir to set CWD to tmp_path so that relative-path tests work correctly.
    Do NOT use os.chdir without cleanup — use monkeypatch.chdir(tmp_path) if available,
    or wrap in a context manager that restores CWD.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test inputs → init_cmd.py | Attacker-supplied path strings; tests simulate adversarial corpus |
| Test CWD → file system | Tests must not accidentally write to real filesystem paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-05-T | Tampering | Test corpus incompleteness | mitigate | 50+ patterns covering dotdot, absolute, null-byte, URL-encoded, Windows-style, deep nesting; ids=lambda for readable CI output |
| T-58-05-D | Denial of Service | Large file test creates >1 MB temp file | accept | pytest tmp_path cleanup removes test artifacts; no persistent disk usage |
</threat_model>

<verification>
```bash
python -m pytest tests/test_cli_init.py -v -k "traversal" --tb=short
python -m pytest tests/test_targets_parser.py -v -k "at_file or reason_code" --tb=short
python -m pytest tests/test_cli_init.py tests/test_targets_parser.py -v --tb=short
```
All commands exit 0.
</verification>

<success_criteria>
- RED: test functions added; pytest exits non-zero (new tests fail before guards OR passes immediately because guards from Plan 03 are already present — either outcome acceptable)
- GREEN: all new tests pass — guards in place from Plan 03
- test_cli_init.py has >= 50 parametrized traversal patterns
- test_targets_parser.py covers all 4 TargetFileError reason codes
- python -m pytest tests/test_cli_init.py tests/test_targets_parser.py exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/58-dashboard-api-hardening/58-05-SUMMARY.md`
</output>
