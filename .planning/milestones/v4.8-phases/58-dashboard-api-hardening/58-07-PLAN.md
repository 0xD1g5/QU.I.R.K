---
phase: 58-dashboard-api-hardening
plan: 07
type: execute
wave: 3
depends_on:
  - 58-04
  - 58-05
  - 58-06
files_modified:
  - .planning/audit-2026-05-08/AUDIT-TASKS.md
  - docs/UAT-SERIES.md
autonomous: false
requirements:
  - HARDEN-API-01
  - HARDEN-API-02
  - HARDEN-API-03
  - HARDEN-API-04
  - HARDEN-API-05
  - HARDEN-API-06

must_haves:
  truths:
    - "AUDIT-TASKS.md rows for api-cli-core/CR-01, CR-02, CR-03, CR-09 are marked [x] closed"
    - "docs/UAT-SERIES.md reflects Phase 58 security hardening additions"
    - "Full pytest suite passes — no regressions introduced"
    - "Phase 58 Obsidian note exists at /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-58-Dashboard-Api-Hardening.md"
    - "docs/UAT-SERIES.md synced to Obsidian vault and committed"
  artifacts:
    - path: ".planning/audit-2026-05-08/AUDIT-TASKS.md"
      provides: "4 audit findings flipped to [x] closed"
      contains: "[x] closed"
    - path: "docs/UAT-SERIES.md"
      provides: "Phase 58 UAT test cases for auth, CSRF, rate limit, path guard, PDF SSRF"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-58-Dashboard-Api-Hardening.md"
      provides: "Obsidian phase note with goal, requirements, success criteria, and what was built"
  key_links:
    - from: ".planning/audit-2026-05-08/AUDIT-TASKS.md"
      to: "api-cli-core/CR-01, CR-02, CR-03, CR-09"
      via: "status column flipped from [ ] to [x]"
      pattern: "\\[x\\] closed"
---

<objective>
Close the audit ledger (flip CR-01, CR-02, CR-03, CR-09 to [x] closed), update UAT-SERIES.md with Phase 58 security test cases, run the full test suite to confirm no regressions, and complete mandatory phase completion steps (Obsidian phase note, UAT vault sync, UAT commit).

Purpose: D-15 requires explicit audit ledger closure at phase completion. The UAT additions document what operators should test when validating the security hardening in a live environment. CLAUDE.md mandates Obsidian phase note creation and UAT-SERIES.md sync after every phase.

Output: AUDIT-TASKS.md with 4 rows closed; UAT-SERIES.md with Phase 58 section; all pytest tests green; Obsidian phase note written; UAT synced to vault and committed.
</objective>

<execution_context>
@/Users/digs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/digs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/58-dashboard-api-hardening/58-CONTEXT.md
@.planning/audit-2026-05-08/AUDIT-TASKS.md

<interfaces>
<!-- AUDIT-TASKS.md ledger row format. -->

Current row format for api-cli-core findings (to update):
```
| api-cli-core/CR-01 | BLOCKER | Path traversal in `quirk init --output` | Phase 58 (HARDEN-API-04) | [ ] mapped |
| api-cli-core/CR-02 | BLOCKER | SSRF / port binding in `routes/pdf.py` | Phase 58 (HARDEN-API-05) | [ ] mapped |
| api-cli-core/CR-03 | BLOCKER | Missing authentication on every dashboard route | Phase 58 (HARDEN-API-01..03) | [ ] mapped |
| api-cli-core/CR-09 | BLOCKER | `parse_target_tokens` reflective DoS via @file | Phase 58 (HARDEN-API-06) | [ ] mapped |
```

Target row format after closure (change `[ ] mapped` to `[x] closed`):
```
| api-cli-core/CR-01 | BLOCKER | Path traversal in `quirk init --output` | Phase 58 (HARDEN-API-04) | [x] closed |
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run full test suite and confirm no regressions</name>
  <what-built>
Run the full Python test suite to confirm all Phase 58 tests pass and no regressions exist:

```bash
python -m pytest tests/ -v --tb=short 2>&1 | tail -40
```

If any Phase 58 tests fail (test_api_auth.py, test_cli_init.py, test_targets_parser.py), diagnose and fix the failure before proceeding. The test run is a pre-condition for ledger closure.

If pre-existing tests unrelated to Phase 58 fail (e.g., chaos-lab integration tests that require Docker), note them as pre-existing failures in the SUMMARY.md but do not block closure.
  </what-built>
  <how-to-verify>
Run and confirm results:
1. `python -m pytest tests/test_api_auth.py -v --tb=short` — must exit 0
2. `python -m pytest tests/test_cli_init.py -v -k "traversal" --tb=short` — must exit 0
3. `python -m pytest tests/test_targets_parser.py -v -k "at_file or reason_code" --tb=short` — must exit 0

All Phase 58 test files must pass. Note any pre-existing unrelated failures for SUMMARY documentation.
  </how-to-verify>
  <resume-signal>Type "tests-pass" when all Phase 58 tests are green, or describe any failures</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Flip audit ledger rows and update UAT-SERIES.md (per D-15)</name>
  <read_first>
    - .planning/audit-2026-05-08/AUDIT-TASKS.md (find the api-cli-core section; identify the 4 CR rows to flip)
    - docs/UAT-SERIES.md (find the right UAT series to update — look for dashboard/API series)
  </read_first>
  <files>
    .planning/audit-2026-05-08/AUDIT-TASKS.md,
    docs/UAT-SERIES.md
  </files>
  <action>
STEP 1 — AUDIT-TASKS.md:
Read the file and find the `### Dashboard API + CLI + Core Platform` section (or similar heading
for api-cli-core findings). Find rows for CR-01, CR-02, CR-03, and CR-09. Change each row's
status column from `[ ] mapped` to `[x] closed`.

The exact four rows to change:
- `api-cli-core/CR-01` — path traversal in quirk init — Phase 58 (HARDEN-API-04)
- `api-cli-core/CR-02` — SSRF / port binding in routes/pdf.py — Phase 58 (HARDEN-API-05)
- `api-cli-core/CR-03` — Missing auth on dashboard routes — Phase 58 (HARDEN-API-01..03)
- `api-cli-core/CR-09` — parse_target_tokens reflective DoS — Phase 58 (HARDEN-API-06)

Also update the Summary table at the top: reduce BLOCKER Mapped count by 4, increase
Closed count by 4 (move 4 entries from Mapped to Closed in the totals row).

STEP 2 — docs/UAT-SERIES.md:
Read the file and find the correct UAT series for dashboard/API functionality. Add a new
Phase 58 UAT block with the following test cases:

```markdown
### Phase 58 — Dashboard API Hardening

**UAT-58-01: Bearer token auth (HARDEN-API-01)**
- Set QUIRK_API_TOKEN=test-token, start the dashboard
- Make a GET request to /api/qramm/sessions without Authorization header → expect 401
- Make a GET request with Authorization: Bearer test-token → expect 200
- With no QUIRK_API_TOKEN set → all requests pass through (auth disabled)
Pass criteria: 401 body = {"detail": "Authentication required"}

**UAT-58-02: CSRF header enforcement (HARDEN-API-01)**
- Set QUIRK_API_TOKEN=test-token, start the dashboard
- POST /api/qramm/sessions with valid Bearer token but without X-Quirk-Request header → expect 403
- POST with both Bearer token and X-Quirk-Request: 1 → expect non-403 response
Pass criteria: 403 body contains "Missing CSRF header: X-Quirk-Request"

**UAT-58-03: CORS allowlist (HARDEN-API-02)**
- Send OPTIONS preflight with Origin: http://evil.example.com → preflight returns no CORS headers (blocked)
- Send OPTIONS preflight with Origin: http://127.0.0.1:8512 → preflight returns CORS headers (allowed)
Pass criteria: Non-allowlisted origin receives no Access-Control-Allow-Origin header

**UAT-58-04: Rate limiting (HARDEN-API-03)**
- Send 61 POST requests to /api/qramm/sessions within 60 seconds (with valid auth + CSRF)
- 61st request → expect 429 with Retry-After header
- GET /api/health — send 100 GET requests → none return 429
Pass criteria: 429 with Retry-After; health always 200

**UAT-58-05: quirk init path-traversal guard (HARDEN-API-04)**
- Run: quirk init --output ../evil.yaml → expect warning message, no file written
- Run: quirk init --output /tmp/evil.yaml → expect warning message, no file written
- Run: quirk init --output quirk-output.yaml → expect normal config creation
Pass criteria: traversal paths print warning containing "outside" or "traversal"; no file created at malicious path

**UAT-58-06: PDF SSRF port guard (HARDEN-API-05)**
- Set QUIRK_SERVE_PORT=80 and call POST /api/export/pdf → expect 500 with {"detail": "QUIRK_SERVE_PORT is out of allowed range (1024–65535)."}
- Set QUIRK_SERVE_PORT=0 → same 500 response
- Normal operation (QUIRK_SERVE_PORT=8512) → PDF export succeeds

**UAT-58-07: @file target guard (HARDEN-API-06)**
- Run: quirk scan --targets @/etc/passwd → expect TargetFileError with reason path_not_allowed_prefix
- Create a file > 1 MB and run: quirk scan --targets @bigfile.txt → expect reason target_file_too_large
- Create a file with 10001 lines → expect reason target_file_too_many_lines
Pass criteria: each violation surfaces a clear error message; no silent failures
```

Update the `**Last Updated:**` date at the top of UAT-SERIES.md to 2026-05-09.
  </action>
  <verify>
    <automated>grep -c '\[x\] closed' .planning/audit-2026-05-08/AUDIT-TASKS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "api-cli-core/CR-01" .planning/audit-2026-05-08/AUDIT-TASKS.md` shows `[x] closed`
    - `grep "api-cli-core/CR-02" .planning/audit-2026-05-08/AUDIT-TASKS.md` shows `[x] closed`
    - `grep "api-cli-core/CR-03" .planning/audit-2026-05-08/AUDIT-TASKS.md` shows `[x] closed`
    - `grep "api-cli-core/CR-09" .planning/audit-2026-05-08/AUDIT-TASKS.md` shows `[x] closed`
    - `grep -c "UAT-58-" docs/UAT-SERIES.md` >= 7
    - `grep "Last Updated" docs/UAT-SERIES.md` shows 2026-05-09
  </acceptance_criteria>
  <done>Four audit blocker rows flipped to [x] closed per D-15; UAT-SERIES.md updated with 7 Phase 58 test cases; AUDIT-TASKS.md summary counts updated.</done>
</task>

<task type="auto">
  <name>Task 3: Write Phase 58 Obsidian phase note (per CLAUDE.md Step 1)</name>
  <read_first>
    - .planning/phases/58-dashboard-api-hardening/58-01-SUMMARY.md (what was built in Plan 01)
    - .planning/phases/58-dashboard-api-hardening/58-02-SUMMARY.md (what was built in Plan 02)
    - .planning/phases/58-dashboard-api-hardening/58-03-SUMMARY.md (what was built in Plan 03)
    - .planning/phases/58-dashboard-api-hardening/58-04-SUMMARY.md (what was built in Plan 04)
    - .planning/phases/58-dashboard-api-hardening/58-05-SUMMARY.md (what was built in Plan 05)
    - .planning/phases/58-dashboard-api-hardening/58-06-SUMMARY.md (what was built in Plan 06)
  </read_first>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-58-Dashboard-Api-Hardening.md</files>
  <action>
Using the Write tool (NOT the obsidian CLI — file is populated from SUMMARY.md content which
may be large), write the Phase 58 note directly to the vault filesystem at:
  /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-58-Dashboard-Api-Hardening.md

Structure the note as follows (source content from SUMMARY.md files read above):

```markdown
---
project: QU.I.R.K.
type: phase
status: complete
source: .planning/phases/58-dashboard-api-hardening/
updated: 2026-05-09
---

# Phase 58 — Dashboard API Hardening

## Goal

Close audit blockers CR-01, CR-02, CR-03, and CR-09 by hardening the dashboard API
with bearer token authentication, CSRF enforcement, CORS allowlisting, per-IP rate
limiting, a path-traversal guard on `quirk init --output`, SSRF port-range clamping
in `routes/pdf.py`, and `@file` target loading guards in `parse_target_tokens()`.

## Requirements Covered

- HARDEN-API-01: Bearer token auth + CSRF on all /api/* (except GET /api/health)
- HARDEN-API-02: CORS allowlist — loopback origins only; foreign origins blocked
- HARDEN-API-03: Per-IP rate limiting — 60 mutating req/min; 429 + Retry-After
- HARDEN-API-04: Path-traversal guard on `quirk init --output`
- HARDEN-API-05: PDF SSRF port-range clamp (1024–65535) + Playwright redirect guard
- HARDEN-API-06: `@file` target allowlist, 1 MB size cap, 10,000 line cap

## Success Criteria

- All pytest tests in test_api_auth.py, test_cli_init.py, test_targets_parser.py pass
- AUDIT-TASKS.md: CR-01, CR-02, CR-03, CR-09 all show [x] closed
- CORS blocks non-loopback origins at preflight
- 61st POST from same IP within 60s returns 429 with Retry-After header
- quirk init --output ../evil.yaml prints warning; no file written outside CWD
- POST /api/export/pdf with QUIRK_SERVE_PORT=80 returns 500 with range error
- @file token pointing to /etc/passwd raises TargetFileError(path_not_allowed_prefix)

## What Was Built

{Populate this section from the SUMMARY.md files for Plans 01–06 — one subsection per plan,
summarizing the key files created/modified and what each plan delivered.}

### Plan 01 — Auth + CSRF middleware
{From 58-01-SUMMARY.md}

### Plan 02 — Rate-limit + CORS middleware
{From 58-02-SUMMARY.md}

### Plan 03 — Path-traversal + SSRF guards
{From 58-03-SUMMARY.md}

### Plan 04 — TDD auth test suite + router wiring
{From 58-04-SUMMARY.md}

### Plan 05 — @file target guards
{From 58-05-SUMMARY.md}

### Plan 06 — fetchApi() migration
{From 58-06-SUMMARY.md}

## Links

[[Roadmap]]
```

Fill in the "What Was Built" subsections from the SUMMARY.md files you read above.
If a SUMMARY.md does not yet exist (executor may be running plans in sequence), leave
the subsection as a stub with "See {plan}-SUMMARY.md when available."
  </action>
  <verify>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-58-Dashboard-Api-Hardening.md" && echo "note exists"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-58-Dashboard-Api-Hardening.md`
    - File contains frontmatter with `type: phase` and `status: complete`
    - File contains `## Goal` section
    - File contains `## Requirements Covered` with all 6 HARDEN-API requirements
    - File contains `[[Roadmap]]` link
    - File contains `## What Was Built` with subsections for each plan
  </acceptance_criteria>
  <done>Obsidian phase note written to vault filesystem with frontmatter, goal, requirements, success criteria, what-was-built from SUMMARY.md files, and [[Roadmap]] link — per CLAUDE.md Step 1.</done>
</task>

<task type="auto">
  <name>Task 4: Sync UAT-SERIES.md to Obsidian vault and commit (per CLAUDE.md Steps 3+4)</name>
  <files>docs/UAT-SERIES.md</files>
  <action>
STEP 1 — Sync UAT-SERIES.md to the Obsidian vault (per CLAUDE.md Step 3):

```bash
printf "---\nproject: QU.I.R.K.\ntype: reference\nstatus: active\nsource: docs/UAT-SERIES.md\nupdated: 2026-05-09\n---\n\n" > /tmp/uat_vault.md
cat /Volumes/Digs-1TB/Development/quantum-apps/QUIRK/docs/UAT-SERIES.md >> /tmp/uat_vault.md
cp /tmp/uat_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
```

STEP 2 — Commit docs/UAT-SERIES.md to git (per CLAUDE.md Step 4):

```bash
node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-58): update UAT-SERIES.md" --files docs/UAT-SERIES.md
```
  </action>
  <verify>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" && grep -c "UAT-58-" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"</automated>
  </verify>
  <acceptance_criteria>
    - `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` exists with Phase 58 UAT entries
    - Vault file contains frontmatter with `source: docs/UAT-SERIES.md` and `updated: 2026-05-09`
    - `node gsd-tools.cjs commit` exits 0 (docs/UAT-SERIES.md committed)
  </acceptance_criteria>
  <done>UAT-SERIES.md synced to Obsidian vault via printf/cat/cp pattern; docs/UAT-SERIES.md committed via gsd-tools.cjs — per CLAUDE.md Steps 3 and 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → full suite | No new trust boundaries — this is a verification + documentation task |
| Executor → vault filesystem | Write access to local Obsidian vault directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-07-I | Information Disclosure | UAT-SERIES.md documents attack vectors publicly | accept | UAT-SERIES.md is internal planning documentation; not a public-facing file; documents test inputs not production secrets |
</threat_model>

<verification>
```bash
# All Phase 58 tests pass
python -m pytest tests/test_api_auth.py tests/test_cli_init.py tests/test_targets_parser.py -v --tb=short

# Audit rows closed
grep "api-cli-core/CR-0[123]" .planning/audit-2026-05-08/AUDIT-TASKS.md | grep -c "\[x\] closed"
# Must be 3

grep "api-cli-core/CR-09" .planning/audit-2026-05-08/AUDIT-TASKS.md | grep -c "\[x\] closed"
# Must be 1

# UAT entries added
grep -c "UAT-58-" docs/UAT-SERIES.md
# Must be >= 7

# Obsidian phase note exists
test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-58-Dashboard-Api-Hardening.md"
# Must exit 0

# UAT synced to vault
test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
# Must exit 0
```
</verification>

<success_criteria>
- All Phase 58 pytest tests pass (test_api_auth.py, test_cli_init.py, test_targets_parser.py)
- AUDIT-TASKS.md: CR-01, CR-02, CR-03, CR-09 all show [x] closed
- AUDIT-TASKS.md summary counts updated (Mapped -4, Closed +4)
- UAT-SERIES.md updated with 7 Phase 58 UAT test cases
- UAT-SERIES.md Last Updated date = 2026-05-09
- Obsidian phase note written to /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-58-Dashboard-Api-Hardening.md
- UAT-Series.md synced to Obsidian vault at /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md
- docs/UAT-SERIES.md committed via gsd-tools.cjs
</success_criteria>

<output>
After completion, create `.planning/phases/58-dashboard-api-hardening/58-07-SUMMARY.md`
</output>
