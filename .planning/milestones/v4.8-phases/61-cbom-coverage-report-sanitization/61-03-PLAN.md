---
phase: 61
plan: 03
type: execute
wave: 2
depends_on: [61-01, 61-02]
files_modified:
  - .planning/audit-2026-05-08/AUDIT-TASKS.md
  - docs/UAT-SERIES.md
autonomous: true
requirements:
  - CBOM-COVER-01
  - CBOM-COVER-02
  - REPORT-SAN-01
  - REPORT-SAN-02

must_haves:
  truths:
    - "AUDIT-TASKS.md rows for cbom-intel-reports/CR-01, CR-02, CR-07 are flipped from `[ ] open` to `[x] closed`"
    - "docs/UAT-SERIES.md is updated to reflect new sanitization expectations + CBOM coverage assertions and synced to the Obsidian vault"
    - "An Obsidian phase note `Phase-61-CBOM-Coverage-Report-Sanitization.md` is created at `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/` summarizing what was built"
    - "The full phase test surface (`pytest tests/ -k 'cbom or report' -q`) passes end-to-end after Wave 1"
  artifacts:
    - path: ".planning/audit-2026-05-08/AUDIT-TASKS.md"
      provides: "Audit ledger with CR-01/CR-02/CR-07 flipped to closed"
      contains: "[x] closed"
    - path: "docs/UAT-SERIES.md"
      provides: "User-facing UAT series updated with Phase 61 assertions"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-61-CBOM-Coverage-Report-Sanitization.md"
      provides: "Obsidian phase note (synced from SUMMARYs)"
  key_links:
    - from: "AUDIT-TASKS.md CR-01/CR-02/CR-07 rows"
      to: ".planning/phases/61-cbom-coverage-report-sanitization/"
      via: "phase reference (Phase 61) in the closure note"
      pattern: "Phase 61"
    - from: "Obsidian phase note"
      to: "Roadmap.md Phase 61 entry"
      via: "[[Roadmap]] wikilink"
      pattern: "\\[\\[Roadmap\\]\\]"
---

<objective>
Close out Phase 61 by (a) flipping the three audit ledger rows that this
phase resolves to `[x] closed`, (b) updating `docs/UAT-SERIES.md` with the
new CBOM coverage and sanitization expectations and syncing it to the
Obsidian vault, and (c) creating the Obsidian phase note per CLAUDE.md
mandatory phase completion steps.

Purpose: Without this plan, the audit ledger drifts out of sync with reality
(blockers stay "open" even though they're fixed), the user-facing UAT-SERIES
documentation lags shipped behavior, and the Obsidian vault loses
phase-by-phase traceability — all three are explicit project requirements
per CLAUDE.md.
Output: 1 modified audit ledger, 1 modified UAT-SERIES + 1 vault sync, 1 new
Obsidian phase note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-cbom-coverage-report-sanitization/61-CONTEXT.md
@CLAUDE.md
@.planning/phases/61-cbom-coverage-report-sanitization/61-01-SUMMARY.md
@.planning/phases/61-cbom-coverage-report-sanitization/61-02-SUMMARY.md
@.planning/audit-2026-05-08/AUDIT-TASKS.md
@docs/UAT-SERIES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip audit ledger rows CR-01, CR-02, CR-07 to closed (D-14)</name>
  <files>.planning/audit-2026-05-08/AUDIT-TASKS.md</files>
  <read_first>
    - .planning/audit-2026-05-08/AUDIT-TASKS.md (FULL relevant region — find the cbom-intel-reports/CR-01, CR-02, CR-07 rows; per CONTEXT.md these are around lines 141–147)
    - .planning/phases/61-cbom-coverage-report-sanitization/61-CONTEXT.md (D-14 — exact rows to flip)
    - .planning/phases/61-cbom-coverage-report-sanitization/61-01-SUMMARY.md (Wave 1 outputs — CBOM Pass-1 + coverage tests)
    - .planning/phases/61-cbom-coverage-report-sanitization/61-02-SUMMARY.md (Wave 1 outputs — md_cell + sanitization tests)
  </read_first>
  <behavior>
    - The row tagged `cbom-intel-reports/CR-01` flips from `[ ] open` (or whatever its current open marker is) to `[x] closed` with a Phase 61 reference appended
    - The row tagged `cbom-intel-reports/CR-02` flips to `[x] closed` with a Phase 61 reference appended
    - The row tagged `cbom-intel-reports/CR-07` flips to `[x] closed` with a Phase 61 reference appended
    - All other rows in AUDIT-TASKS.md are unchanged
    - The file remains valid markdown (table structure preserved)
  </behavior>
  <action>
1. Open `.planning/audit-2026-05-08/AUDIT-TASKS.md` and locate the three rows by their IDs:
   - `cbom-intel-reports/CR-01` (CBOM Pass-1 zero algo)
   - `cbom-intel-reports/CR-02` (VAULT TLS branch fallthrough)
   - `cbom-intel-reports/CR-07` (markdown injection)

2. For each row, change the open marker (`[ ] open` or `[ ]`) to `[x] closed` and append a closure note in the same row's notes/disposition column:

   Example transform:
   ```
   BEFORE: | CR-01 | cbom-intel-reports | ... | [ ] open | <existing notes> |
   AFTER:  | CR-01 | cbom-intel-reports | ... | [x] closed | <existing notes> — closed by Phase 61 (CBOM-COVER-01, builder.py Pass-1 branches + tests/test_cbom_coverage.py) |
   ```

   Adjust the exact column layout to match what's actually in AUDIT-TASKS.md — read the file first, mirror its column count and separator placement.

3. Closure notes per row:
   - CR-01 → `closed by Phase 61 (CBOM-COVER-01, see tests/test_cbom_coverage.py per-family parametrize)`
   - CR-02 → `closed by Phase 61 (CBOM-COVER-02, dedicated VAULT Pass-1 branch + tests/test_cbom_vault_consistency.py golden snapshot)`
   - CR-07 → `closed by Phase 61 (REPORT-SAN-01/02, quirk/reports/_md_escape.md_cell + tests/test_report_sanitization.py adversarial corpus)`

4. Do NOT modify any other row in the file. Use Edit (not Write) so unrelated rows are untouched.
  </action>
  <verify>
    <automated>grep -A0 'cbom-intel-reports/CR-01\|cbom-intel-reports/CR-02\|cbom-intel-reports/CR-07' .planning/audit-2026-05-08/AUDIT-TASKS.md | grep -c '\[x\] closed'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'cbom-intel-reports/CR-01.*\[x\] closed' .planning/audit-2026-05-08/AUDIT-TASKS.md` returns 1
    - `grep -c 'cbom-intel-reports/CR-02.*\[x\] closed' .planning/audit-2026-05-08/AUDIT-TASKS.md` returns 1
    - `grep -c 'cbom-intel-reports/CR-07.*\[x\] closed' .planning/audit-2026-05-08/AUDIT-TASKS.md` returns 1
    - `grep -c 'Phase 61' .planning/audit-2026-05-08/AUDIT-TASKS.md` returns ≥ 3 (closure notes for each row)
    - Total `[x] closed` count in file increased by exactly 3 vs pre-change baseline (use `git diff --stat .planning/audit-2026-05-08/AUDIT-TASKS.md` to confirm only this file changed and only the 3 expected rows differ)
    - File still parses as valid markdown (visual sanity check via `head -20` and `grep -c '^|' .planning/audit-2026-05-08/AUDIT-TASKS.md` returns the same row count as before — no rows lost)
  </acceptance_criteria>
  <done>
    Three audit ledger rows flipped to closed with Phase 61 attribution. All other rows untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update docs/UAT-SERIES.md and sync to Obsidian (CLAUDE.md mandatory step)</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (FULL file — find which series is most relevant to Phase 61: CBOM coverage and report sanitization. Look for sections covering CBOM viewer, technical report, or scan-output verification)
    - .planning/phases/61-cbom-coverage-report-sanitization/61-01-SUMMARY.md
    - .planning/phases/61-cbom-coverage-report-sanitization/61-02-SUMMARY.md
    - CLAUDE.md (mandatory phase completion steps 2 and 3 — sync UAT-SERIES.md to vault filesystem at `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md`)
  </read_first>
  <behavior>
    - The relevant UAT series in `docs/UAT-SERIES.md` gains a new test case (or updates an existing one) covering: "CBOM contains ≥1 algorithm component for every protocol family scanned" and "Technical report renders cleanly when scanner returns banner / cert / finding text containing pipes or newlines"
    - The `**Last Updated:**` date at the top of `docs/UAT-SERIES.md` is bumped to today (2026-05-10)
    - The vault file `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` is created/overwritten with frontmatter + full content via the printf+cat+cp pipeline documented in CLAUDE.md
  </behavior>
  <action>
**Step A — Decide the target series.** Read `docs/UAT-SERIES.md` and find the series most aligned with CBOM output verification and technical report rendering. Likely candidates: a "CBOM Viewer" series, a "Report Generation" series, or a generic "Scan Output" series. If multiple are relevant, update each.

**Step B — Add or update test cases.** For each relevant series, add a test case (or update an existing one) along these lines:

```markdown
### UAT-X-NN: CBOM emits algorithm components for all scanned protocol families

**Steps:**
1. Run `quirk scan` against the chaos lab profile that exercises database, container, source, ssh-weak, storage-s3, vault, and broker subfamilies.
2. Inspect the generated CBOM JSON (`<output>/cbom.json`).
3. For each protocol family in the scan inventory, confirm at least one component with `type: cryptographic-asset` and `cryptoProperties.assetType: algorithm`.

**Pass criteria:**
- No protocol family present in the scan inventory has zero algorithm components in the CBOM.
- VAULT endpoints have algorithm components named `rsa-*`, `aes256-gcm96`, or `ed25519` (matching the transit key types).

### UAT-X-MM: Technical report survives adversarial scanner banners

**Steps:**
1. Run `quirk scan` against a target whose service banner / cert subject / finding text contains `|`, `\n`, or control characters.
2. Open the generated `report.md`.
3. Open the same file in any GFM-aware preview (GitHub, VS Code, Obsidian).

**Pass criteria:**
- All tables render with consistent column counts.
- No row is broken by an embedded `|` or `\n` in scanner-derived data.
- All adversary-derived strings appear in their cell with `|` shown as `\|`.
```

Tailor the exact wording, series ID, and case numbers to match the file's existing convention.

**Step C — Bump the timestamp.** At the top of the file, update `**Last Updated:** 2026-05-10` (or whatever format the file uses).

**Step D — Sync to Obsidian.** Execute the documented pipeline from CLAUDE.md:

```bash
printf "---\nproject: QU.I.R.K.\ntype: reference\nstatus: active\nsource: docs/UAT-SERIES.md\nupdated: 2026-05-10\n---\n\n" > /tmp/uat_vault.md
cat docs/UAT-SERIES.md >> /tmp/uat_vault.md
cp /tmp/uat_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
```

(Use absolute paths everywhere; do not `cd`.)
  </action>
  <verify>
    <automated>grep -c '2026-05-10' docs/UAT-SERIES.md ; grep -c 'algorithm component\|adversarial\|md_cell\|sanitiz\|GFM' docs/UAT-SERIES.md ; test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" && echo "vault file present" ; head -8 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '2026-05-10' docs/UAT-SERIES.md` returns ≥ 1
    - `grep -ci 'algorithm component\|adversarial\|sanitiz\|GFM' docs/UAT-SERIES.md` returns ≥ 2 (new content references CBOM coverage AND sanitization)
    - File `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` exists and is non-empty
    - First line of vault file is `---` (frontmatter) and `head -8 /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` shows `project: QU.I.R.K.`, `type: reference`, `status: active`, `source: docs/UAT-SERIES.md`, `updated: 2026-05-10`
    - Vault file content after the frontmatter matches the (current) repo `docs/UAT-SERIES.md` content (`tail -n +8 /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` head/tail compares cleanly with `docs/UAT-SERIES.md`)
  </acceptance_criteria>
  <done>
    docs/UAT-SERIES.md updated with Phase 61 assertions + bumped date; vault file synced via the documented pipeline.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Obsidian phase note (CLAUDE.md mandatory step 1)</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-61-CBOM-Coverage-Report-Sanitization.md</files>
  <read_first>
    - .planning/phases/61-cbom-coverage-report-sanitization/61-01-SUMMARY.md
    - .planning/phases/61-cbom-coverage-report-sanitization/61-02-SUMMARY.md
    - CLAUDE.md (mandatory phase completion step 1 — exact frontmatter and section structure)
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/ (list directory; use the most recent existing phase note as a template — e.g. Phase-60-* for layout consistency)
  </read_first>
  <behavior>
    - File `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-61-CBOM-Coverage-Report-Sanitization.md` exists
    - Frontmatter present: `project: QU.I.R.K.`, `type: phase`, `status: complete`, `source: .planning/phases/61-cbom-coverage-report-sanitization/`, `updated: 2026-05-10`
    - Body contains: Goal (from ROADMAP §Phase 61), Requirements Covered (CBOM-COVER-01/02, REPORT-SAN-01/02), Success Criteria (the 4 from ROADMAP), What Was Built (one subsection per plan, sourced from the plan SUMMARYs), and a `[[Roadmap]]` wikilink
    - File is written directly to vault filesystem (NOT via `obsidian CLI content=` per CLAUDE.md note about file size)
  </behavior>
  <action>
1. List `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/` and read the most recent phase note (e.g., `Phase-60-*.md`) as a template.

2. Write directly to `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-61-CBOM-Coverage-Report-Sanitization.md` using the Write tool, with this structure:

```markdown
---
project: QU.I.R.K.
type: phase
status: complete
source: .planning/phases/61-cbom-coverage-report-sanitization/
updated: 2026-05-10
---

# Phase 61: CBOM Coverage + Report Sanitization

## Goal

CBOM Pass-1 emits at least one algorithm component for every protocol family
the scanner produces evidence for, VAULT is classified consistently across
all three CBOM passes, and markdown reports cannot be broken or injected by
adversary-controllable strings (host, cipher, cert subject, banner, finding
text). Closes audit blockers 13, 14 (`cbom-intel-reports/CR-01`,
`cbom-intel-reports/CR-02`, `cbom-intel-reports/CR-07`).

## Requirements Covered

- **CBOM-COVER-01** — Per-family CBOM Pass-1 algorithm component coverage
- **CBOM-COVER-02** — VAULT consistent classification across Pass-1/2/3
- **REPORT-SAN-01** — Markdown table cell escaping for adversary-controllable fields
- **REPORT-SAN-02** — Adversarial-corpus pytest fixture for technical report

## Success Criteria

1. Per-profile CBOM emission test asserts ≥ 1 algorithm component for each
   of the 12+ previously-zero-algo protocol families; fails loudly on
   regression.
2. VAULT routed through a vault-specific Pass-1 branch (not the TLS branch);
   Pass-2 / Pass-3 emit consistent evidence claims; golden snapshot fixture
   is byte-identical across runs.
3. Adversarial markdown corpus (pipes, newlines, backticks, HTML entities,
   control characters) renders as valid GFM tables with no row-break or
   injection escape.
4. CycloneDX 1.6 schema validation continues to pass for every chaos-lab
   profile post-fix.

## What Was Built

### Plan 01 — CBOM Pass-1 Coverage + VAULT Snapshot

<paste 4–8 line summary from 61-01-SUMMARY.md>

### Plan 02 — Markdown Sanitization

<paste 4–8 line summary from 61-02-SUMMARY.md>

### Plan 03 — Audit Ledger + Documentation Sync

Flipped `cbom-intel-reports/CR-01`, `CR-02`, `CR-07` to `[x] closed` in
`.planning/audit-2026-05-08/AUDIT-TASKS.md`. Updated `docs/UAT-SERIES.md`
with new CBOM coverage + sanitization assertions and synced to the
Obsidian vault. Created this phase note.

## Links

- [[Roadmap]]
- [[Requirements]]
```

3. Use the Write tool with the absolute vault path. Do NOT use `obsidian CLI content=` (file is too large for shell expansion per CLAUDE.md).

4. After writing, confirm the file exists and frontmatter is intact.
  </action>
  <verify>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-61-CBOM-Coverage-Report-Sanitization.md" && head -10 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-61-CBOM-Coverage-Report-Sanitization.md" ; grep -c '\[\[Roadmap\]\]' "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-61-CBOM-Coverage-Report-Sanitization.md"</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-61-CBOM-Coverage-Report-Sanitization.md` exists
    - First line is `---`
    - `grep -c 'type: phase' "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-61-CBOM-Coverage-Report-Sanitization.md"` returns 1
    - `grep -c 'status: complete' "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-61-CBOM-Coverage-Report-Sanitization.md"` returns 1
    - `grep -c 'updated: 2026-05-10' "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-61-CBOM-Coverage-Report-Sanitization.md"` returns 1
    - `grep -c 'CBOM-COVER-01\|CBOM-COVER-02\|REPORT-SAN-01\|REPORT-SAN-02' "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-61-CBOM-Coverage-Report-Sanitization.md"` returns ≥ 4
    - `grep -c '\[\[Roadmap\]\]' "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-61-CBOM-Coverage-Report-Sanitization.md"` returns 1
    - File contains a "What Was Built" section with subsections referencing Plan 01, Plan 02, and Plan 03
  </acceptance_criteria>
  <done>
    Obsidian phase note exists with valid frontmatter, all 4 requirements named, summaries from both plan SUMMARYs, and a `[[Roadmap]]` link. Per CLAUDE.md mandatory phase completion step 1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → vault filesystem | UAT-SERIES.md and phase note content are written to vault path; vault is local-disk only (no remote sync target managed by this code) |
| project planning artifacts → audit ledger | AUDIT-TASKS.md transitions are administrative; tampering would mislead the audit-driven roadmap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-11 | Tampering | AUDIT-TASKS.md row flip | mitigate | Edit (not Write) used so only the targeted 3 rows change; closure notes name Phase 61 + the SUMMARY artifacts so the flip is auditable |
| T-61-12 | Repudiation | phase note creation | accept | Phase note frontmatter records `source` and `updated` fields linking back to `.planning/phases/61-…/` artifacts; git history of the planning dir provides the canonical record |
| T-61-13 | Information Disclosure | vault sync | accept | Vault is local-disk; UAT-SERIES.md is already a public-facing doc in the repo; no secrets cross the boundary |
</threat_model>

<verification>
After all 3 tasks complete:

```bash
# Audit ledger
grep -A0 'cbom-intel-reports/CR-01\|cbom-intel-reports/CR-02\|cbom-intel-reports/CR-07' .planning/audit-2026-05-08/AUDIT-TASKS.md

# UAT-SERIES sync
diff <(tail -n +8 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md") docs/UAT-SERIES.md | head -5  # should be empty or trivial

# Phase note
ls -la "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-61-CBOM-Coverage-Report-Sanitization.md"

# End-to-end test surface (re-run to confirm Plan 01 + Plan 02 still pass)
pytest tests/ -k "cbom or report" -q 2>&1 | tail -10
```

All four ROADMAP §Phase 61 success criteria are now closed across Plans 01, 02, 03.
</verification>

<success_criteria>
- [ ] AUDIT-TASKS.md: CR-01, CR-02, CR-07 flipped to `[x] closed` with Phase 61 attribution
- [ ] docs/UAT-SERIES.md updated with Phase 61 CBOM coverage + sanitization assertions and date bumped
- [ ] Vault file `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` synced
- [ ] Vault file `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-61-CBOM-Coverage-Report-Sanitization.md` created
- [ ] `pytest tests/ -k "cbom or report"` still passes end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/61-cbom-coverage-report-sanitization/61-03-SUMMARY.md`
</output>
