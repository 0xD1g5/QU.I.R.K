---
milestone: v5.17
milestone_name: Defect Drain
audited: 2026-09-01T00:00:00Z
status: passed
status_original: gaps_found
resolved: 2026-09-01T21:10:00Z
scores:
  requirements: 16/16
  requirements_at_audit: 14/16
  phases: 5/5
  phases_at_audit: 4/5
  integration: 12/12
  flows: 8/8
gaps:
  requirements:
    - id: "LABRUN-01"
      status: "partial"
      phase: "Phase 176 — Chaos-Lab Re-Run"
      claimed_by_plans: ["176-03-PLAN.md", "176-04-PLAN.md", "176-05-PLAN.md", "176-06-PLAN.md"]
      completed_by_plans: ["176-08-SUMMARY.md"]
      verification_status: "missing"
      evidence: >
        Substantively delivered and evidenced (10 PASS / 3 FAIL / 0 GAP across all 13 cases,
        ledger-applied, TRIAGE-176-01/02 backlogged, TRIAGE-176-03 fixed with a regression test),
        but Phase 176 has NO 176-VERIFICATION.md. No independent verifier has re-derived these
        claims. Per audit-milestone step 2, an unverified phase is a blocker regardless of
        self-reported evidence quality.
    - id: "LABRUN-02"
      status: "partial"
      phase: "Phase 176 — Chaos-Lab Re-Run"
      claimed_by_plans: ["176-01-PLAN.md", "176-02-PLAN.md"]
      completed_by_plans: ["176-02-SUMMARY.md", "176-06-SUMMARY.md"]
      verification_status: "missing"
      evidence: >
        uat_runner.py:154 stale '4.2.0' literal + broken `'quirk' in ver.lower()` substring check
        root-caused and fixed under D-01's narrow lift (git diff --numstat = 1 1), guarded by
        tests/test_uat_runner_version_check.py (6 nodes, demonstrated RED then GREEN), UAT-1-02
        re-dispositioned PASS through the ledger. Same verification gap as LABRUN-01 — the phase
        was never verified.
  integration: []
  flows: []
warnings:
  - id: "DRIFT-01"
    severity: "MINOR"
    requirement: "LABRUN-01"
    location: ".planning/ROADMAP.md line ~310 (176-07 addendum bullet) and the Progress table"
    detail: >
      ROADMAP.md contradicts itself. The 176-07 bullet still reads "Tally unchanged (9/13 PASS,
      2 BACKLOG, 2 GAP)" and the Progress table's Phase 176 row still reads "6/6 | Plans Complete
      — pending /gsd-verify-phase 176 | 2026-08-30" — neither knows plan 176-08 (2026-08-31)
      happened. The same file's own Backlog section (~line 654) and REQUIREMENTS.md both carry the
      correct final tally. Ground truth: 10 PASS / 3 FAIL / 0 GAP, corroborated by
      176-08-SUMMARY.md and the ledger (UAT-5-11 GAP→PASS, UAT-6-08 GAP→FAIL).
      Bookkeeping only — no code or traceability impact.
  - id: "RECORD-01"
    severity: "INFO"
    requirement: "SCOPE-01"
    detail: >
      SCOPE-01 is `[ ]` in REQUIREMENTS.md by deliberate decision, NOT by oversight. Do not flip
      this checkbox. The requirement's own text asserts behavior the product intentionally does
      not have; the literal fix was built, shipped, live-verified, then reverted the same day once
      shown to fire on every real CLI config. Recorded as an accepted override in
      173-VERIFICATION.md with user sign-off (2026-08-29). Counted here as satisfied-by-override.
nyquist:
  compliant_phases: [172, 173, 174, 175, 176]
  partial_phases: []
  missing_phases: []
  overall: COMPLIANT
tech_debt:
  - phase: 172-fuzzing-disclosure-safety
    items:
      - "DEFER-172-01: tests/skip_registry.py line-number drift across 5 files (caused by Phases 166/170, not 172). Still the sole full-suite failure — test_skip_registry::test_no_unregistered_skips. Carried across all 5 phases of this milestone."
      - "DEFER-172-02: reproducible macOS SIGSEGV in forked children of tests/test_install_errors.py (Network.framework fork-safety, not a QUIRK bug). Non-failing — pytest summary unaffected."
  - phase: 174-dashboard-api-correctness
    items:
      - "DASH-06 D-01: score-profile persistence for CLI-run scans deliberately deferred — needs a schema migration with no reliable backfill key. Disclosed in ROADMAP.md and REQUIREMENTS.md, not silently dropped."
      - "DASH-07 D-02: the GET /api/scan/latest 404 on empty DB remains visible in DevTools. Cannot be suppressed from frontend code; silencing it would change a documented error contract (rejected by user decision)."
  - phase: 176-chaos-lab-re-run
    items:
      - "TRIAGE-176-01 (BACKLOGGED, unfixed): chaos lab certs/keycloak.crt is byte-identical to certs/modern.crt (CN=modern.chaos.local), so UAT-5-13's cert-subject criterion is not actually verifiable. Needs its own plan and test."
      - "TRIAGE-176-02 (BACKLOGGED, unfixed): no distinct PLAINTEXT_HTTP/HTTP_EXPOSURE finding type exists; ports inside and outside ports_tls collapse to the same finding title regardless of intent. Root-caused to _postprocess_findings in quirk/engine/findings_evaluator.py. Genuine product classification gap."
      - "TRIAGE-176-03 (CLOSED in 176-08) — retained as debt because the fix UNBLOCKS never-verified downstream paths: the CBOM SSH algorithm components, qramm/evidence_bridge.py, scanner/hardware_scanner.py, and the dashboard SSH panel have all been receiving empty data since the ssh-audit integration shipped. None of their SSH paths have ever run against real ssh-audit output."
      - "UAT-6-08 criterion 1 (case-text correction carried forward): demands ED25519 be 'not quantum-vulnerable' — cryptographically false. QUIRK correctly returns nist_quantum_security_level=0."
      - "UAT-6-08 criterion 4 (case-text correction carried forward): expects per-algorithm NIST levels in the findings JSON; they exist only in the CBOM, which has no CLI emission path. Either retarget the criterion or file the missing surface as its own product gap."
      - "UAT-5-11 pass criteria (cosmetic): use the word CRITICAL, which ssh-audit never emits (its vocabulary is fail/warn/info). No disposition impact."
  - phase: milestone-wide
    items:
      - "pyproject.toml still reads 5.15.0. v5.16 shipped deliberately untagged; the version bump is blocked behind a broken local editable install (stale __editable__.quirk-4.0.0.pth) that makes tests/test_version.py fail. v5.17 is a planning label, not a release."
---

# Milestone v5.17 "Defect Drain" — Audit Report

**Audited:** 2026-09-01
**Status:** `gaps_found` — one unverified phase
**Phases:** 172–176 (5 phases, 29 plans)
**Requirements:** 16

## Executive Summary

v5.17 set out to fix the defects the v5.16 drain surfaced and convert its misleading records into
true ones. On substance it did that, and did it unusually honestly: three of its sixteen
requirements were resolved as *"the specification is wrong, not the code"* — each with the rejected
alternative built and demonstrated to fail rather than merely argued against.

**The milestone is one artifact away from passing.** Phase 176 has no `176-VERIFICATION.md`. Its
evidence is dense and self-consistent, but no independent verifier has re-derived it — and 176 is
precisely the phase that changed **product code** (`quirk/scanner/ssh_scanner.py`, TRIAGE-176-03)
after its plans were declared complete, at explicit user direction, as a deliberate departure from
its own no-product-change rule. That is the phase least safe to close on self-report.

Everything else is clean. The integration checker found **zero blockers, zero orphaned
requirements, and zero broken flows** across all twelve wired requirements.

## Requirements Coverage (3-source cross-reference)

| REQ-ID | Phase | REQUIREMENTS.md | VERIFICATION.md | SUMMARY frontmatter | Final |
|--------|-------|-----------------|-----------------|---------------------|-------|
| SAFE-01 | 172 | `[x]` | passed | 172-01/04/05/06 | **satisfied** |
| SAFE-02 | 172 | `[x]` | passed | 172-01/02/04/05/06 | **satisfied** |
| SAFE-03 | 172 | `[x]` | passed | 172-03/04/05/06 | **satisfied** |
| SCOPE-01 | 173 | `[ ]` *(deliberate)* | passed **(override, user-accepted)** | 173-01 | **satisfied (override)** |
| SCOPE-02 | 173 | `[x]` | passed | 173-02/04 | **satisfied** |
| SCOPE-03 | 173 | `[x]` | passed | 173-03 | **satisfied** |
| DASH-06 | 174 | `[x]` | passed | 174-01/04/05 | **satisfied** *(CLI persistence deferred, disclosed)* |
| DASH-07 | 174 | `[x]` | passed | 174-02/04/05 | **satisfied** *(re-interpreted D-02, zero code)* |
| DASH-08 | 174 | `[x]` | passed | 174-03/04/05 | **satisfied** *(docs corrected, UI untouched)* |
| CASEFIX-01 | 175 | `[x]` | passed | 175-01/02/06/07 | **satisfied** |
| CASEFIX-02 | 175 | `[x]` | passed | 175-01/02/06/07 | **satisfied** |
| CASEFIX-03 | 175 | `[x]` | passed | 175-01/02/06/07 | **satisfied** |
| CASEFIX-04 | 175 | `[x]` | passed | 175-01/03/04/05/06/07 | **satisfied** |
| CASEFIX-05 | 175 | `[x]` | passed | 175-01/03/06/07 | **satisfied** |
| LABRUN-01 | 176 | `[x]` | **MISSING** | 176-08 | **partial** |
| LABRUN-02 | 176 | `[x]` | **MISSING** | 176-02/06 | **partial** |

**Score: 14/16 satisfied, 2 partial, 0 unsatisfied, 0 orphaned.**

Orphan detection: every REQ-ID in the traceability table appears in a phase VERIFICATION.md
requirements table except LABRUN-01/02, which are absent only because the file does not exist —
not because they were assigned and forgotten.

## Phase Verification Status

| Phase | VERIFICATION.md | Status | Score | Overrides |
|-------|-----------------|--------|-------|-----------|
| 172 Fuzzing & Disclosure Safety | ✅ | passed | 3/3 | 0 |
| 173 Scanner Scope & Config Correctness | ✅ | passed | 3/3 | **1 (SCOPE-01, user-accepted)** |
| 174 Dashboard & API Correctness | ✅ | passed | 7/7 | 0 |
| 175 Case & Documentation Defect Correction | ✅ | passed | 12/12 | 0 |
| 176 Chaos-Lab Re-Run | ❌ **absent** | — | — | — |

Anti-patterns (`TODO`/`FIXME`/`XXX`/`HACK`/`PLACEHOLDER`/stubs) across all four verified phases:
**zero matches**, each verifier having grepped the phase's own modified-file set.

## Nyquist Compliance

| Phase | VALIDATION.md | `nyquist_compliant` | `wave_0_complete` | Status |
|-------|---------------|---------------------|-------------------|--------|
| 172 | ✅ | true | true | COMPLIANT |
| 173 | ✅ | true | true | COMPLIANT |
| 174 | ✅ | true | true | COMPLIANT |
| 175 | ✅ | true | true | COMPLIANT |
| 176 | ✅ (audited + reaudited 2026-09-01) | true | true | COMPLIANT |

**Overall: COMPLIANT.** No `/gsd-validate-phase` runs needed — note that 176 *is* Nyquist-validated
even though it is not goal-verified; these are different gates measuring different things.

## Cross-Phase Integration

**No blockers. No orphans. No broken flows.**

| Flow | Path | Status |
|------|------|--------|
| Fuzz safety | argparse → `FUZZ-001`/`FUZZ-002` → `sys.exit(2)` | WIRED |
| URL redaction | `openapi_scanner.py` → `url_allowlist._redact_url_preview` → `SpecParsingError` | WIRED |
| Phase-timing omission | `_PHASE_SKIPPED` sentinel → `run_stats["timings_sec"]` (~25 call sites) | WIRED |
| Missing-extra signal | `_broker/_smime/_adcs_missing_extra` → `optional_extra.py` → `scan_error_category="missing_extra"` → dashboard schemas / trends / sensor_cmd | WIRED |
| Dashboard scoring | `routes/scan.py:list_scans` → `compute_readiness_score(profile=calibration)` → `useScanList.ts` | WIRED |
| Empty-DB state | `/api/scan/latest` → documented `DASHBOARD-005/006` 404 contract | WIRED |
| Sidebar order | `174-SIDEBAR-ORDER.md` ↔ `sidebar.tsx` NAV_ITEMS (14) ↔ `test_sidebar_nav_order.py` | WIRED |
| UAT corpus triangle | `UAT-SERIES.md` headings == result blocks; `uat_disposition_apply.py verify` → **378 rows agree, exit 0**; four guard suites **48 passed** | WIRED |

**SCOPE-01 revert verified clean.** Grep for `scope_narrowed` / `port_scope_origin` /
`ScanCfg._user_set_fields` returns zero residue — the reverted suppression mechanism left no dead
code behind.

**Carry-forwards all landed.** `UAT-94-05` (172→175), `UAT-36-05` (173→175), `UAT-8-07` (174→175)
each confirmed addressed in Phase 175 commits `c53c1e8f`, `8185ce88`, `c5bd8282`. `UAT-8-07`
correctly remains DEFERRED rather than being silently dropped or manufactured into a PASS.

**Phase 175's "zero product code changed" claim independently verified** via `git show --stat`
across all ten Phase-175 commits: only `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`,
`.planning/STATE.md`, `docs/UAT-SERIES.md`, `docs/uat-disposition-ledger.jsonl` were touched. The
claim is true, not merely asserted.

## Gaps

### BLOCKER — Phase 176 is unverified

`.planning/phases/176-chaos-lab-re-run/` contains PLANs, SUMMARYs, VALIDATION, SECURITY, UAT,
DEFECT-TRIAGE, LABRUN-EVIDENCE and HARNESS-LIFT — but **no `176-VERIFICATION.md`**. `ROADMAP.md`
leaves the phase checkbox `[ ]` and its Progress row explicitly says *"pending
`/gsd-verify-phase 176`"*; `STATE.md` closes with *"Next step: `/gsd-verify-phase 176`"*. This is
a known, deliberately-parked step, not a discovery — but it is still the gate this audit cannot
waive.

It matters more here than it would for a documentation phase: Phase 176 shipped a **product code
fix** (`_run_ssh_audit`'s argv) outside its own declared scope, at user direction, mid-phase. The
defect it fixed had survived the full test suite for its entire life because
`tests/test_ssh_scanner.py` patched `subprocess.run` and asserted only on `return_value`, never
`call_args`. A phase that finds a class of bug the suite is structurally blind to is exactly the
phase that warrants an independent verifier.

### MINOR — ROADMAP.md contradicts itself on the LABRUN-01 tally

`ROADMAP.md`'s 176-07 bullet still reads *"Tally unchanged (9/13 PASS, 2 BACKLOG, 2 GAP)"* and its
Progress table still stops at `6/6 | 2026-08-30`. Both predate plan 176-08 (2026-08-31). The same
file's Backlog section and `REQUIREMENTS.md` both carry the correct **10 PASS / 3 FAIL / 0 GAP**,
corroborated by `176-08-SUMMARY.md` and the ledger. Bookkeeping drift with no traceability impact —
`REQUIREMENTS.md` is authoritative and already right.

### INFO — SCOPE-01's unchecked box is intentional

Do not "fix" it. See `warnings.RECORD-01` in the frontmatter.

## Tech Debt Carried Out of v5.17

**7 items across 3 phases plus 1 milestone-wide.** Two are genuine unfixed product/lab defects
already promoted verbatim to the ROADMAP backlog (TRIAGE-176-01, TRIAGE-176-02); three are UAT
case-text corrections carried forward in the same class as this milestone's own
`UAT-94-05`/`UAT-36-05`/`UAT-8-07` precedent; the rest are disclosed deferrals. See the
`tech_debt` block in the frontmatter for the itemized list with root causes.

The one that deserves a second look during v5.18 planning is **TRIAGE-176-03's downstream tail**:
the fix is closed, but the CBOM SSH components, the QRAMM evidence bridge, the hardware scanner,
and the dashboard SSH panel have *never* run against real `ssh-audit` output. That is a body of
code with no verified behavior, not a resolved bug.

## Suite Baseline

`pytest -q -m ""` → **1 failed, 3802 passed** (per `STATE.md`, post-176-08). The single failure is
the pre-existing `tests/test_skip_registry.py::test_no_unregistered_skips` (`DEFER-172-01`),
identical node across all five phases of this milestone. **Zero new failing nodes introduced by
v5.17.**

---

_Audited by: Claude (gsd-audit-milestone orchestrator) with gsd-integration-checker_


---

## Resolution Addendum — 2026-09-01

The audit above was written on 2026-09-01 before Phase 176 had been verified. Its findings are
preserved verbatim; this section records how each was closed. Nothing in the body was edited.

### Gaps — both CLOSED

Both `LABRUN-01` and `LABRUN-02` were marked `partial` for a single shared reason:
`verification_status: "missing"` — Phase 176 had no `176-VERIFICATION.md`, so no independent
verifier had re-derived its self-reported claims. Neither gap disputed the substance of the work.

`176-VERIFICATION.md` was created on 2026-09-01 by a `gsd-verifier` phase-goal pass:
`status: passed`, **15/15 must-haves verified, 0 overrides applied**. The verifier re-derived the
claims against the codebase rather than reading the SUMMARYs:

- **SC3 root cause independently reproduced.** `git show 72c06529^:uat_runner.py` line 154 read
  `status = 'PASS' if code == 0 and ('4.2.0' in ver or 'quirk' in ver.lower()) else 'FAIL'`. Both
  disjuncts are unsatisfiable against `QU.I.R.K. v5.15.0` (`'quirk' in 'qu.i.r.k. v5.15.0'` is
  False — the dots). Note this **falsifies the ROADMAP's own success-criterion premise** that "no
  such literal exists there"; the phase corrected the premise with evidence rather than
  inheriting it.
- **SC1 evidenced by transcript, not narration.** `176-LABRUN-EVIDENCE.md` carries
  `LAB STATUS: UP`, a 33-container `docker compose -p chaoslab ps -a` dump, 13 `OUTCOME:` blocks
  each with a literal command and verbatim output, and `TEARDOWN: CONFIRMED`.
- **SC2 triage reaches a durable home.** TRIAGE-176-01/02 present verbatim in the ROADMAP Backlog;
  TRIAGE-176-03 fixed in-phase with an argv-asserting regression test.
- **Executed, not read:** 6 + 18 + 6 + 29 test nodes green; `uat_disposition_apply.py verify`
  exit 0 across 378 rows; ledger↔corpus outcome match confirmed case-by-case for all 14 ids;
  `quantum-chaos-enterprise-lab/` byte-unchanged across every phase commit.

`gsd-sdk query audit-open` independently corroborates: `verification_gaps: 0`.

`scripts/verify_phase_gates.py` Phase 176 Close Gate: **clean** — VERIFICATION.md present,
VALIDATION.md current, UAT-SERIES.md coverage satisfied.

### Warnings

- **DRIFT-01 (MINOR) — FIXED 2026-09-01.** The ROADMAP's 176-07 bullet still read "Tally unchanged
  (9/13 PASS, 2 BACKLOG, 2 GAP)" and **the file had no 176-08 bullet at all** — it never learned
  the GAP-closure re-run happened. The 176-07 bullet now reads as superseded, a 176-08 bullet was
  added carrying the correct final tally (**10 PASS / 3 FAIL / 0 GAP**) and TRIAGE-176-03, and the
  Progress row was updated from `Plans Complete — pending /gsd-verify-phase 176 | 2026-08-30` to
  `Complete — verified 15/15 | 2026-09-01`.
- **RECORD-01 (INFO) — HONORED.** `SCOPE-01` remains `[ ]` in REQUIREMENTS.md and is archived as
  **satisfied-by-override**, not as a gap, per this warning's explicit instruction and the user
  sign-off recorded in `173-VERIFICATION.md` (2026-08-29). The checkbox was not flipped.

### Note on tooling

The audit's DRIFT-01 text quotes `/gsd-verify-phase 176` because that string was in ROADMAP.md and
STATE.md at audit time. **No such GSD command exists.** `NN-VERIFICATION.md` is produced by the
`gsd-verifier` subagent that `/gsd-execute-phase` spawns at its `verify_phase_goal` step; it can be
dispatched directly to backfill a phase. All such references were corrected on 2026-09-01 (commit
`05d96fb0`). The quotes in the body above are left as-is because they are a faithful record of what
those files said at audit time.
