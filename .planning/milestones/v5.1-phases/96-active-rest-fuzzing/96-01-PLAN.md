---
phase: 96-active-rest-fuzzing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/test_install_all_excludes_schemathesis.py
  - tests/test_rest_fuzzer_gate.py
  - quirk/scanner/rest_fuzzer.py
autonomous: true
requirements: [FUZZ-02, FUZZ-03]
must_haves:
  truths:
    - "Running quirk scan --fuzz with non-TTY stdin hard-aborts before any request is sent"
    - "TTY confirm gate proceeds only on the literal string CONFIRM; any other input aborts"
    - "A fuzz budget request above 500 raises ValueError and cannot be bypassed via config"
    - "schemathesis is resolvable in quirk[api] but absent from quirk[all]"
  artifacts:
    - path: "quirk/scanner/rest_fuzzer.py"
      provides: "confirm_fuzz_gate, _resolve_budget, MAX_FUZZ_BUDGET, DEFAULT_FUZZ_BUDGET, FUZZ_RATE_DEFAULT"
      contains: "def confirm_fuzz_gate"
    - path: "tests/test_rest_fuzzer_gate.py"
      provides: "Gate + budget unit tests; zero-requests-before-confirm assertion"
      contains: "call_count == 0"
    - path: "pyproject.toml"
      provides: "schemathesis in [api] extras"
      contains: "schemathesis"
  key_links:
    - from: "tests/test_rest_fuzzer_gate.py"
      to: "quirk/scanner/rest_fuzzer.py"
      via: "import confirm_fuzz_gate, _resolve_budget, MAX_FUZZ_BUDGET"
      pattern: "from quirk.scanner.rest_fuzzer import"
---

<objective>
Stand up the Phase 96 Wave 0 infrastructure and the most safety-critical code in the
milestone: the REST-fuzz CONFIRM gate and the hard non-TTY abort, built test-first.

Purpose: Per CONTEXT.md specifics, the gate and non-TTY hard-abort MUST be proven by tests
that assert ZERO network requests are dispatched before/without CONFIRM, BEFORE any
request-dispatch code exists. Everything downstream (Plan 02 dispatch loop) builds on a
proven gate. Also install schemathesis into [api] extras and extend the CI guard.
Output: pyproject.toml [api] += schemathesis; extended schemathesis CI guard; gate test
suite; rest_fuzzer.py module with confirm_fuzz_gate + budget ceiling + module stub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-active-rest-fuzzing/96-RESEARCH.md
@.planning/phases/96-active-rest-fuzzing/96-PATTERNS.md
@.planning/phases/96-active-rest-fuzzing/96-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install schemathesis in [api] extras and extend the CI guard</name>
  <files>pyproject.toml, tests/test_install_all_excludes_schemathesis.py</files>
  <read_first>
    - pyproject.toml [api] block (lines 62-64) — the file being modified
    - pyproject.toml [all] meta-extra (lines 87-95) — MUST NOT change
    - tests/test_install_all_excludes_schemathesis.py (lines 119-134) — existing [all]-exclusion guard, the analog and the file being extended
    - 96-PATTERNS.md "pyproject.toml" + "test_install_all_excludes_schemathesis.py" sections
  </read_first>
  <action>
    Add `schemathesis>=4.4.4` to the `[api]` optional-dependencies group in pyproject.toml,
    after the existing `openapi-spec-validator>=0.9.0` entry, with an inline comment noting
    [api]-only / NOT [all] (v5.1-D-05). Do NOT modify the `[all]` meta-extra — schemathesis
    stays excluded from `[all]`. Then run `.venv/bin/pip install -e ".[api]"` to install
    schemathesis + transitive hypothesis into the venv.

    Extend tests/test_install_all_excludes_schemathesis.py by ADDING a new test function
    `test_install_api_includes_schemathesis` that parses pyproject.toml and asserts
    schemathesis IS present in the [api] extras list. Do NOT modify the existing
    `test_install_all_excludes_schemathesis` test body — it remains the [all]-exclusion
    guard (PKG-01). Use the same tomllib/tomli parse approach the existing test uses.
  </action>
  <acceptance_criteria>
    - pyproject.toml [api] group contains a `schemathesis>=4.4.4` requirement string
    - pyproject.toml [all] group does NOT contain the substring `schemathesis`
    - New test function named exactly `test_install_api_includes_schemathesis` exists
    - Existing `test_install_all_excludes_schemathesis` function body unchanged (git diff shows only additions)
    - `.venv/bin/python -c "import schemathesis; from schemathesis.core.result import Ok"` exits 0
    - `.venv/bin/python -m pytest tests/test_install_all_excludes_schemathesis.py -x -q` passes
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_install_all_excludes_schemathesis.py -x -q</automated>
  </verify>
  <done>schemathesis resolvable in [api], absent from [all]; both guard tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test-first CONFIRM gate, non-TTY hard abort, and budget hard ceiling</name>
  <files>tests/test_rest_fuzzer_gate.py, quirk/scanner/rest_fuzzer.py</files>
  <behavior>
    - test_non_tty_hard_abort_zero_requests: confirm_fuzz_gate(is_tty=False) returns False, prints to injected stderr_print_fn, and a mocked session.request is never called (call_count == 0)
    - test_confirm_required_exact_string: is_tty=True returns False for each of "y","yes","confirm",""," ","CONFIRM " and returns True only for exactly "CONFIRM"
    - test_budget_hard_ceiling: _resolve_budget(501) raises ValueError matching "hard maximum"; _resolve_budget(500)==500; _resolve_budget(None)==50
    - test_default_budget_resolves: _resolve_budget(50)==50
  </behavior>
  <read_first>
    - quirk/util/targets.py maybe_confirm_probe_budget (lines 243-294) — injectable prompt_fn/stderr_print_fn analog (note: it AUTO-PROCEEDS in non-TTY; the fuzz gate must HARD-ABORT instead)
    - 96-RESEARCH.md Pattern 2 (confirm_fuzz_gate), Pattern 3 (budget ceiling), Anti-Patterns
    - 96-PATTERNS.md "confirm_fuzz_gate pattern", "Hard budget constants pattern", "test_rest_fuzzer_gate.py" sections
    - run_scan.py line 960 (sys.stdin.isatty() is the correct TTY check, NOT stdout)
  </read_first>
  <action>
    Write tests/test_rest_fuzzer_gate.py FIRST (RED), covering the four behaviors above.
    Use the injectable-fn pattern: pass prompt_fn=lambda _: <input> and a list-appending
    stderr_print_fn; pass is_tty explicitly (never touch real sys.stdin in tests). For the
    zero-requests assertion, create a MagicMock session and assert session.request.call_count
    == 0 after a non-TTY gate call.

    Then create quirk/scanner/rest_fuzzer.py implementing only the gate-layer surface needed
    to pass: module imports per 96-PATTERNS.md (with the schemathesis try/except setting
    SCHEMATHESIS_AVAILABLE); module constants MAX_FUZZ_BUDGET: Final[int] = 500,
    DEFAULT_FUZZ_BUDGET = 50, FUZZ_RATE_DEFAULT = 5.0; `_resolve_budget(requested)` raising
    ValueError when effective > MAX_FUZZ_BUDGET; and `confirm_fuzz_gate(budget, target_count,
    is_tty=None, prompt_fn=input, stderr_print_fn=None)` that auto-detects is_tty via
    sys.stdin.isatty() when None, HARD-ABORTS (returns False, prints error) in non-TTY, and in
    TTY requires the prompt answer to equal exactly the literal string "CONFIRM" after .strip().
    Do NOT implement run_fuzz_scan or any probe/dispatch code in this plan — Plan 02 owns those.
    Run python -m compileall on the new module per CLAUDE.md.
  </action>
  <acceptance_criteria>
    - quirk/scanner/rest_fuzzer.py defines MAX_FUZZ_BUDGET == 500 as a module constant
    - DEFAULT_FUZZ_BUDGET == 50 and FUZZ_RATE_DEFAULT == 5.0 defined
    - confirm_fuzz_gate returns False when is_tty=False and invokes stderr_print_fn
    - confirm_fuzz_gate returns True only when the TTY answer strips to exactly "CONFIRM"
    - _resolve_budget raises ValueError on 501; returns 500 on 500; returns 50 on None
    - tests/test_rest_fuzzer_gate.py asserts a mocked session.request.call_count == 0 for the non-TTY path
    - No call to requests.Session().request or socket.create_connection exists anywhere in rest_fuzzer.py yet (grep -v '^#' shows zero dispatch sites)
    - `.venv/bin/python -m compileall quirk/scanner/rest_fuzzer.py` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_rest_fuzzer_gate.py -x -q</automated>
  </verify>
  <done>Gate + budget ceiling proven by tests asserting zero requests before CONFIRM; no dispatch code present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → fuzz CLI | The CONFIRM gate is the authorization boundary; no active traffic crosses without explicit operator intent |
| CI / headless → fuzz CLI | Non-TTY stdin must be refused before any socket opens |
| package manager → runtime | schemathesis install must be legitimate and [api]-scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01 | Tampering | confirm_fuzz_gate non-TTY path | mitigate | Hard-abort returns False before any I/O; test asserts mocked session.request.call_count == 0 (test-first, this plan) |
| T-96-02 | Elevation of Privilege | _resolve_budget config bypass | mitigate | MAX_FUZZ_BUDGET=500 enforced inside the module via ValueError; not reliant on CLI arg validation |
| T-96-03 | Spoofing | TTY CONFIRM literal match | mitigate | Exact-string "CONFIRM" required; "y"/"yes"/"confirm"/whitespace all abort (tested) |
| T-96-SC | Tampering | pip install schemathesis | mitigate | RESEARCH Package Legitimacy Audit [OK] (schemathesis ~7yr, 300k+/mo, slopcheck OK); not [ASSUMED]/[SUS] so no blocking checkpoint required |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_rest_fuzzer_gate.py tests/test_install_all_excludes_schemathesis.py -x -q` green
- `grep -v '^#' quirk/scanner/rest_fuzzer.py | grep -c "session.request\|socket.create_connection"` returns 0 (no dispatch code yet)
</verification>

<success_criteria>
- schemathesis installed in [api], absent from [all], both guard tests green
- confirm_fuzz_gate hard-aborts in non-TTY (zero requests proven by mock), requires literal CONFIRM in TTY
- Budget hard ceiling (500) unbypassable via _resolve_budget
</success_criteria>

<output>
Create `.planning/phases/96-active-rest-fuzzing/96-01-SUMMARY.md` when done
</output>
