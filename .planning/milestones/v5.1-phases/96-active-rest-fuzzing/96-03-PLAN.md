---
phase: 96-active-rest-fuzzing
plan: 03
type: execute
wave: 3
depends_on: [96-02]
files_modified:
  - run_scan.py
  - quirk/cbom/builder.py
  - quirk/intelligence/evidence.py
  - quirk/intelligence/scoring.py
  - tests/test_score_weights_invariant.py
  - tests/test_cbom_rest_fuzz_no_tls_component.py
autonomous: true
requirements: [FUZZ-01, FUZZ-02, FUZZ-03, SCORE-01]
must_haves:
  truths:
    - "quirk scan --fuzz invokes the CONFIRM gate before the fuzz scan phase and aborts cleanly on non-CONFIRM or non-TTY"
    - "--fuzz-budget defaults to 50 and is rejected above 500"
    - "--fuzz-jwt-alg-confusion triggers the alg-confusion probe"
    - "REST_FUZZ CRITICAL/HIGH findings increment fuzz_finding_count and feed agility_signals"
    - "SCORE_WEIGHTS sum is 303.0 and count is 41"
  artifacts:
    - path: "run_scan.py"
      provides: "--fuzz / --fuzz-jwt-alg-confusion / --fuzz-budget flags + gated fuzz scan phase"
      contains: "--fuzz"
    - path: "quirk/intelligence/evidence.py"
      provides: "REST_FUZZ protocol key + fuzz_finding_count + agility_fuzz_crypto_posture_ratio"
      contains: "REST_FUZZ"
    - path: "quirk/intelligence/scoring.py"
      provides: "agility_fuzz_crypto_posture_ratio weight 4.0"
      contains: "agility_fuzz_crypto_posture_ratio"
    - path: "tests/test_score_weights_invariant.py"
      provides: "sum 303.0 + count 41 invariants"
      contains: "303.0"
  key_links:
    - from: "run_scan.py"
      to: "quirk.scanner.rest_fuzzer.run_fuzz_scan"
      via: "_wrapped_phase fuzz scan phase"
      pattern: "run_fuzz_scan"
    - from: "quirk/intelligence/scoring.py"
      to: "evidence fuzz_finding_count"
      via: "agility_impacts append"
      pattern: "agility_fuzz_crypto_posture_ratio"
---

<objective>
Wire the fuzzer into the CLI and the scoring pipeline: run_scan.py gains --fuzz /
--fuzz-jwt-alg-confusion / --fuzz-budget flags, the CONFIRM gate at the CLI boundary, and a
_wrapped_phase fuzz scan phase; evidence.py gains the REST_FUZZ protocol key + finding
counter; scoring.py adds the agility weight; and the SCORE_WEIGHTS invariant advances to
303.0 / 41.

Purpose: Make the fuzzer reachable from the CLI and make its findings count toward the
readiness score — the final SCORE_WEIGHTS step of v5.1 (299.0→303.0). Sum AND count
invariants are updated together (recurring lesson).
Output: CLI flags + gated phase; REST_FUZZ evidence counter; +4.0 agility weight; updated
invariant test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-active-rest-fuzzing/96-RESEARCH.md
@.planning/phases/96-active-rest-fuzzing/96-PATTERNS.md
@.planning/phases/96-active-rest-fuzzing/96-CONTEXT.md
@.planning/phases/96-active-rest-fuzzing/96-02-SUMMARY.md

<interfaces>
From quirk/scanner/rest_fuzzer.py (Plans 01-02):
  def run_fuzz_scan(spec_dict, base_url, cfg, cred_ctx=None, budget=50, prompt_fn=input, run_alg_confusion=False) -> List[CryptoEndpoint]
  def confirm_fuzz_gate(budget, target_count, is_tty=None, prompt_fn=input, stderr_print_fn=None) -> bool

Current scoring state (RESEARCH, VERIFIED): SCORE_WEIGHTS sum=299.0, count=40.
Target: add one key agility_fuzz_crypto_posture_ratio: 4.0 → sum 303.0, count 41.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLI flags + gated fuzz scan phase in run_scan.py</name>
  <files>run_scan.py</files>
  <read_first>
    - run_scan.py nmap gate block (lines 942-963), openapi scan phase + _wrapped_phase (lines 1370-1398), missing-extra advisory (lines 147-161), --openapi-spec flag block (~line 729), line 960 (sys.stdin.isatty())
    - 96-PATTERNS.md "run_scan.py — --fuzz / --fuzz-jwt-alg-confusion flags + scan phase" section
    - quirk/scanner/rest_fuzzer.py — confirm_fuzz_gate + run_fuzz_scan signatures
  </read_first>
  <action>
    Add three argparse flags after the --openapi-spec block: --fuzz (store_true; help references
    FUZZ-01 + requires --openapi-spec + interactive confirmation), --fuzz-jwt-alg-confusion
    (store_true; help references FUZZ-04 + requires --fuzz), and --fuzz-budget (type=int,
    default=50, metavar N, help noting default 50 / hard max 500). Emit the missing_extra advisory
    via the is_extra_available("schemathesis")/_emit_missing_extra_advisory pattern when args.fuzz
    is set but schemathesis is absent.

    Invoke confirm_fuzz_gate at the CLI boundary (analogous to the nmap gate block) BEFORE the
    fuzz scan phase, passing is_tty=sys.stdin.isatty() and target_count=len(openapi_endpoints);
    on False, log "Aborted by user — REST fuzz gate rejected." and skip the fuzz phase (clean
    abort, no findings). Add a _run_fuzz_phase() that returns [] unless args.fuzz and discovered
    openapi_endpoints exist, then calls run_fuzz_scan with the retained spec_dict, base_url from
    cfg.targets, cfg, cred_ctx, budget=args.fuzz_budget, run_alg_confusion=args.fuzz_jwt_alg_confusion;
    wrap it via _wrapped_phase(run_stats, "fuzz_scanning", "rest_fuzzer", ...). DOUBLE-PROMPT
    RESOLUTION (single call point): the CLI does NOT call confirm_fuzz_gate itself — run_fuzz_scan
    owns the single gate call internally (it already calls confirm_fuzz_gate with prompt_fn). The
    CLI passes its real prompt_fn (default input) through to run_fuzz_scan and relies on that one
    internal gate, so the user is prompted EXACTLY ONCE. Do not add a redundant external gate call.
    Run python -m compileall run_scan.py.
  </action>
  <acceptance_criteria>
    - `python -c "import run_scan"` (via .venv) imports without error
    - `.venv/bin/python run_scan.py --help` lists --fuzz, --fuzz-jwt-alg-confusion, --fuzz-budget
    - --fuzz-budget default is 50
    - With args.fuzz and non-TTY stdin, the run aborts before the fuzz phase (no run_fuzz_scan dispatch)
    - User is prompted for CONFIRM exactly once (no double prompt) — documented in the SUMMARY
    - missing_extra advisory emitted when --fuzz set and schemathesis absent
    - `.venv/bin/python -m compileall run_scan.py` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python run_scan.py --help | grep -E -- "--fuzz-jwt-alg-confusion|--fuzz-budget|--fuzz"</automated>
  </verify>
  <done>--fuzz / --fuzz-jwt-alg-confusion / --fuzz-budget flags wired; CONFIRM gate at CLI boundary; gated _wrapped_phase fuzz phase; single prompt.</done>
</task>

<task type="auto">
  <name>Task 2: REST_FUZZ CBOM skip + evidence counter + agility weight + invariant 303.0/41</name>
  <files>quirk/cbom/builder.py, quirk/intelligence/evidence.py, quirk/intelligence/scoring.py, tests/test_score_weights_invariant.py, tests/test_cbom_rest_fuzz_no_tls_component.py</files>
  <read_first>
    - quirk/cbom/builder.py Pass-2 skip tuple (~line 680, the "CODE_SIGNING" entry) AND Pass-3 `elif ep.protocol in (...)` skip tuple (~line 876) — REST_FUZZ must be added to BOTH so REST_FUZZ endpoints do NOT fall through to the TLS else clause and emit phantom protocol:tls components
    - quirk/intelligence/evidence.py _PROTOCOL_KEYS (lines 11-19), CODE_SIGNING counter + elif (lines 128-338), output dict entry (~lines 491-493)
    - quirk/intelligence/scoring.py SCORE_WEIGHTS Phase 95 entry (line 61), agility_impacts Phase 95 append (lines 236-241)
    - tests/test_score_weights_invariant.py sum invariant (lines 27-34), count invariant (lines 47-50)
    - 96-PATTERNS.md "evidence.py", "scoring.py", "test_score_weights_invariant.py" sections (Patterns 9 + 10)
  </read_first>
  <action>
    In quirk/cbom/builder.py: add the literal "REST_FUZZ" to BOTH the Pass-2 skip tuple
    (~line 680, alongside "CODE_SIGNING") AND the Pass-3 `elif ep.protocol in (...)` skip tuple
    (~line 876). This prevents REST_FUZZ endpoints from falling through to the TLS `else` clause
    and emitting spurious `protocol:tls:{host}:{port}` CBOM components (REST_FUZZ findings carry no
    TLS metadata). Write tests/test_cbom_rest_fuzz_no_tls_component.py: build_cbom([CryptoEndpoint(
    protocol="REST_FUZZ", host="api.x", port=443, severity="HIGH")]) emits ZERO components whose
    bom_ref starts with "crypto/protocol/tls/".

    In evidence.py: append "REST_FUZZ" to the _PROTOCOL_KEYS tuple (UPPERCASE, exact) after
    "CODE_SIGNING"; add a `fuzz_finding_count = 0` counter near codesign_weak_algo_count; add an
    `elif proto == "REST_FUZZ":` branch (after the CODE_SIGNING elif) that increments
    fuzz_finding_count when the endpoint severity (uppercased) is in ("CRITICAL","HIGH") — INFO
    probe_skipped rows do NOT count; add output-dict entries `"fuzz_finding_count": fuzz_finding_count`
    and `"agility_fuzz_crypto_posture_ratio": round(fuzz_finding_count/total_endpoints, 4) if
    total_endpoints else 0.0`.

    In scoring.py: add `"agility_fuzz_crypto_posture_ratio": 4.0` to SCORE_WEIGHTS after the Phase
    95 entry; append the agility impact tuple ("Active REST fuzz crypto-posture findings",
    -_ratio(fuzz_findings, denom) * w["agility_fuzz_crypto_posture_ratio"]) following the Phase 95
    codesign append, reading fuzz_findings = max(0, _as_int(evidence.get("fuzz_finding_count", 0))).

    In tests/test_score_weights_invariant.py: update the SUM assertion 299.0 → 303.0 AND the COUNT
    assertion 40 → 41 in the SAME change (recurring lesson — never update only one). Add docstring
    notes recording the +4.0 / +1 delta for agility_fuzz_crypto_posture_ratio.
    Run python -m compileall on both modules.
  </action>
  <acceptance_criteria>
    - `grep -c '"REST_FUZZ"' quirk/intelligence/evidence.py` returns >= 1 and it is in _PROTOCOL_KEYS
    - evidence.py REST_FUZZ branch increments only for severity in {CRITICAL, HIGH}
    - evidence output dict contains fuzz_finding_count and agility_fuzz_crypto_posture_ratio
    - scoring.py SCORE_WEIGHTS contains agility_fuzz_crypto_posture_ratio == 4.0
    - sum(SCORE_WEIGHTS.values()) == 303.0 (within 1e-9) and len(SCORE_WEIGHTS) == 41
    - test_score_weights_invariant.py asserts BOTH 303.0 and 41
    - "REST_FUZZ" present in BOTH the Pass-2 and Pass-3 skip tuples in quirk/cbom/builder.py
    - test_cbom_rest_fuzz_no_tls_component.py asserts a REST_FUZZ endpoint produces zero crypto/protocol/tls/* components
    - `.venv/bin/python -m compileall quirk/cbom/builder.py quirk/intelligence/evidence.py quirk/intelligence/scoring.py` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_score_weights_invariant.py -x -q</automated>
  </verify>
  <done>REST_FUZZ findings feed agility_signals; SCORE_WEIGHTS at 303.0/41 with both invariants green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → fuzz phase | --fuzz must pass the CONFIRM gate before any fuzz dispatch |
| fuzz findings → score | REST_FUZZ severity drives the agility signal; INFO must not inflate findings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-09 | Tampering | CLI --fuzz bypassing the gate | mitigate | confirm_fuzz_gate invoked at the CLI boundary before _run_fuzz_phase; run_fuzz_scan also gates internally (defense in depth, single prompt) |
| T-96-10 | Repudiation | score drift from miscount | mitigate | INFO probe_skipped rows excluded from fuzz_finding_count; SCORE_WEIGHTS sum+count dual invariant tested |
| T-96-SC | Tampering | schemathesis runtime import | mitigate | missing_extra advisory when --fuzz set without [api]; no silent partial run |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_score_weights_invariant.py -x -q` green
- `.venv/bin/python run_scan.py --help` lists all three fuzz flags
- `.venv/bin/python -m compileall run_scan.py quirk/intelligence/evidence.py quirk/intelligence/scoring.py` exits 0
</verification>

<success_criteria>
- --fuzz / --fuzz-jwt-alg-confusion / --fuzz-budget wired with single CONFIRM prompt and non-TTY abort
- REST_FUZZ CRITICAL/HIGH findings feed agility_signals; INFO excluded
- SCORE_WEIGHTS sum 303.0 / count 41, both invariants green
</success_criteria>

<output>
Create `.planning/phases/96-active-rest-fuzzing/96-03-SUMMARY.md` when done
</output>
