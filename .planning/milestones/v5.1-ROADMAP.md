# v5.1 Milestone Archive — Authenticated Scanning + API Surface Depth

**Shipped:** 2026-05-23 · **Phases:** 93–96 (4) · **Plans:** 16 · **Requirements:** 21/21 · **Audit:** PASSED · **Tag:** v5.1.0 (local)

**SCORE_WEIGHTS:** 283.0/37 → 303.0/41 (PQC agility ceiling + 4 v5.1 agility signals)

## Key Accomplishments

1. **Ephemeral credential model** (AUTH-01..05): Bearer/OAuth2 + API-key (header/query) + HTTP Basic via CLI flag/env/prompt; never persisted to SQLite/CBOM/logs/dashboard; committed 11-surface security-review gate; QRK-SCHED-AUTH-001 blocks authenticated scheduled scans.
2. **OpenAPI + bearer-token analysis** (SPEC-01..03, TOKEN-01..03): `analyze-token` JWT classifier (alg:none CRITICAL), OpenAPI spec scanner hardened against $ref SSRF (pre-validate raw-ref reject) + 10MB DoS gate + scope-gated URL fetch; CBOM bearer classification `declared_algorithm (unverified)`.
3. **Code-signing certificate inventory** (CSIGN-01..03): LDAP `userCertificate` (CodeSigning EKU) + in-process TLS-EKU check; RSA<2048/EC<256/SHA-1 → HIGH; SHA-256/surrogate-key cross-source CBOM dedup (TLS-derived component wins).
4. **Gated active REST fuzzing** (FUZZ-01..04): schemathesis-driven crypto-posture probes (TLS downgrade, cipher, HSTS, HTTP-cred) + RS256→HS256 alg-confusion; literal `CONFIRM` gate, hard non-TTY abort, six guardrails, unbypassable budget ceiling (50/500) bounding ALL traffic.
5. **`[api]` extras group** (PKG-01): openapi-spec-validator + schemathesis, excluded from `[all]` with CI guard (impacket-exclusion pattern).
6. **Chaos lab coverage** (LAB-01): ldaps code-signing fixture + new `fuzz-target` profile (port 20100) with expected_results + README.

---

## 🚧 v5.1 Authenticated Scanning + API Surface Depth

**Milestone Goal:** Add an optional, ephemeral credential model that unlocks deeper crypto findings across the API surface — without QU.I.R.K. ever becoming a secret store.

**Key constraints:**
- Credentials are ephemeral / in-memory only — never persisted to SQLite, CBOM, logs, or dashboard API responses
- Authenticated scheduled scans are architecturally prohibited while credentials are ephemeral-only
- `schemathesis` excluded from `[all]` extras (mirrors impacket exclusion pattern); enforced by CI
- SCORE_WEIGHTS sum advances 283.0 → 293.0 → 299.0 → 303.0 across three phases; no 7th subscore pillar
- Per CLAUDE.md: every phase includes documentation-update + Obsidian-sync + UAT-SERIES.md tasks
- Per CLAUDE.md: chaos lab changes must update lab.sh + README + expected_results_*.md in the same change

## Phases

- [x] **Phase 93: Credential Infrastructure** - Ephemeral credential model (Bearer/OAuth2, API key, HTTP Basic) with committed security-review gate deliverable (completed 2026-05-23)
- [x] **Phase 94: OpenAPI & Bearer Token Analysis** - Passive spec parsing + bearer token classify/decode; `[api]` extras group introduced (completed 2026-05-23)
- [x] **Phase 95: Code-Signing Certificate Inventory** - LDAP `userCertificate` + TLS EKU code-signing discovery; zero new core deps (completed 2026-05-23)
- [x] **Phase 96: Active REST Fuzzing** - Gated crypto-posture fuzzer with six mandatory guardrails + alg-confusion sub-flag; ships last (completed 2026-05-23)

## Phase Details

### Phase 93: Credential Infrastructure
**Goal**: Users can supply ephemeral Bearer/OAuth2, API-key, or HTTP Basic credentials for a single authenticated scan run, with a committed security-review gate ensuring credentials never leak into stored artifacts
**Depends on**: Phase 92 (v5.0 close-out)
**Requirements**: AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05
**Success Criteria** (what must be TRUE):
  1. User can pass credentials via `--auth-bearer`, `--auth-api-key`, `--auth-basic`, `QUIRK_AUTH_*` env vars, or interactive prompt; an authenticated scan run completes without error
  2. A completed scan's SQLite row, CBOM output, dashboard API response, PDF export, and log files contain no raw credential value — scrubbing verified by automated test
  3. Running `quirk schedule add` with `enable_authenticated_mode: true` exits with error code QRK-SCHED-AUTH-001 and a clear human-readable message
  4. A committed security-review gate deliverable documents all 11 credential-leakage surfaces audited, the `safe_str()` extension covering API-key header patterns, and the best-effort (non-provable) nature of in-memory zeroization in Python
  5. The AST CI gate deny-list is extended to flag `bearer`, `api_key`, `authorization`, `token` in `json.dumps()` / `model_dump()` calls, and CI is green
**Plans**: 4 plans
- [x] 93-01-PLAN.md — CredentialContext core (bytearray secret, from_cli reference model, enable_authenticated_mode flag) [AUTH-01]
- [x] 93-02-PLAN.md — Scrubbing + regression guards (safe_str ext, AST/schema gates, scheduler QRK-SCHED-AUTH-001) [AUTH-03, AUTH-05]
- [x] 93-03-PLAN.md — Wire JWT/API consumer + sentinel leak-detection suite + committed security-review gate [AUTH-01, AUTH-02, AUTH-04]
- [x] 93-04-PLAN.md — Docs + UAT-SERIES + Obsidian sync (CLAUDE.md mandatory per-phase steps) [AUTH-01..04]

### Phase 94: OpenAPI & Bearer Token Analysis
**Goal**: Users can decode and classify bearer/JWT tokens and analyze OpenAPI/Swagger specs for API crypto posture, both passively and with no active traffic to any target
**Depends on**: Phase 93
**Requirements**: SPEC-01, SPEC-02, SPEC-03, TOKEN-01, TOKEN-02, TOKEN-03, SCORE-01 (partial — SCORE_WEIGHTS +10.0 → 293.0), PKG-01
**Success Criteria** (what must be TRUE):
  1. User can run `quirk analyze-token <jwt>` and receive a report showing the algorithm, key size, expiry, quantum-safety classification, and a CRITICAL flag for any `alg: none` token (case-insensitive: none, None, NONE, NonE)
  2. User can point QUIRK at a local OpenAPI spec file and receive findings for plaintext `servers`, unauthenticated endpoints, and weak or absent security schemes; findings appear in the standard findings table
  3. A spec URL is only fetched when that URL is within the configured scan-target scope; an out-of-scope URL is rejected with a clear error before any network request is made
  4. A spec containing an internal-network `$ref` URL (e.g., `http://169.254.169.254/`) causes a `SpecParsingError` rather than an outbound network request — SSRF prevention verified by a CI fixture test
  5. Bearer tokens captured during an authenticated scan appear in the CBOM with a `declared_algorithm (unverified)` label; the raw token value is absent from all stored artifacts (SQLite, CBOM, logs)
  6. `pip install quirk[api]` pulls in `openapi-spec-validator`; `pip install quirk[all]` does not pull in `schemathesis` — enforced by a new CI test mirroring `test_install_all_excludes_impacket.py`
**Plans**: 3 plans
- [x] 94-01-PLAN.md — analyze-token command (TOKEN-01/03) + CBOM bearer classification (TOKEN-02) + SCORE_WEIGHTS +10.0 (SCORE-01) [TOKEN-01, TOKEN-02, TOKEN-03, SCORE-01]
- [x] 94-02-PLAN.md — OpenAPI scanner with SSRF/DoS/scope hardening (SPEC-01/02/03) + [api] extras & schemathesis CI guard (PKG-01) [SPEC-01, SPEC-02, SPEC-03, PKG-01]
- [x] 94-03-PLAN.md — docs + UAT-SERIES.md + Obsidian sync (CLAUDE.md mandatory) [all]

### Phase 95: Code-Signing Certificate Inventory
**Goal**: Users can inventory code-signing certificates discovered from LDAP and from existing TLS cert captures, with weak-algorithm findings and fingerprint-based CBOM de-duplication
**Depends on**: Phase 93
**Requirements**: CSIGN-01, CSIGN-02, CSIGN-03, SCORE-01 (partial — SCORE_WEIGHTS +6.0 → 299.0), LAB-01 (partial)
**Success Criteria** (what must be TRUE):
  1. User can run a scan with `--inventory-code-signing` and receive a list of discovered code-signing certs — sourced from LDAP `userCertificate` (CodeSigning EKU) and from EKU checks on TLS-captured certs — in the findings output
  2. A code-signing certificate using RSA < 2048 bits, EC < 256 bits, or a SHA-1 signature hash raises a HIGH severity finding with category `CODE-SIGN/weak-algorithm`
  3. Running the inventory against a target that already has TLS certs captured does not produce duplicate CBOM components — de-duplication by SHA-256 fingerprint is confirmed by an automated test asserting stable component count
  4. Code-signing signals contribute to the `agility_signals` subscore (SCORE_WEIGHTS sum 299.0); existing scans without code-signing data are unaffected by the new weights
**Plans**: 4 plans
- [x] 95-01-PLAN.md — codesign_scanner.py (LDAP userCertificate + TLS EKU) + ConnectorsCfg + weak-algo classification [CSIGN-01, CSIGN-02]
- [x] 95-02-PLAN.md — CBOM CODE_SIGNING branch + SHA-256 fingerprint dedup + evidence/scoring +6.0 (299.0/40) [CSIGN-03, SCORE-01]
- [x] 95-03-PLAN.md — run_scan.py --inventory-code-signing wiring + ldaps chaos fixture triple-update [CSIGN-01, LAB-01]
- [x] 95-04-PLAN.md — docs + UAT-SERIES.md + Obsidian sync (CLAUDE.md mandatory) [CSIGN-01..03, SCORE-01, LAB-01]

### Phase 96: Active REST Fuzzing
**Goal**: Users can opt in to active REST crypto-posture fuzzing — TLS downgrade, cipher acceptance, HSTS, and JWT alg-confusion probes — gated behind an explicit `CONFIRM` prompt, bounded request budget, and hard non-TTY abort
**Depends on**: Phase 93, Phase 94
**Requirements**: FUZZ-01, FUZZ-02, FUZZ-03, FUZZ-04, SCORE-01 (final — SCORE_WEIGHTS +4.0 → 303.0), LAB-01 (final)
**Success Criteria** (what must be TRUE):
  1. Running `quirk scan --fuzz` in a TTY presents a budget summary and requires the user to type the literal word `CONFIRM`; any other input (including Enter alone) aborts the run cleanly with no requests sent
  2. Running `quirk scan --fuzz` with non-TTY stdin (piped input, CI environment) hard-aborts before sending any requests and prints a clear non-interactive-mode error
  3. The fuzzer pauses and reports a warning after three consecutive 5xx responses; the total request count never exceeds the configured budget (default 50, hard max 500)
  4. Running `quirk scan --fuzz --fuzz-jwt-alg-confusion` sends the RS256→HS256 confusion probe and produces a finding when the target server accepts the forged token
  5. SCORE_WEIGHTS sum is 303.0 and `tests/test_score_weights_invariant.py` is green; `schemathesis` is absent from `pip install quirk[all]` and `tests/test_install_all_excludes_schemathesis.py` enforces this in CI
**Plans**: 5 plans
- [x] 96-01-PLAN.md — Wave-0 setup + test-first CONFIRM gate / non-TTY hard-abort / budget ceiling ([api] schemathesis) [FUZZ-02, FUZZ-03]
- [x] 96-02-PLAN.md — Fuzzer core: GET-only dispatch + 5 guardrails + TLS/cipher/HSTS/http-cred probes + RS256→HS256 alg-confusion [FUZZ-01, FUZZ-02, FUZZ-04]
- [x] 96-03-PLAN.md — run_scan.py --fuzz flags + gated phase + REST_FUZZ evidence/scoring (SCORE_WEIGHTS 303.0/41) [FUZZ-01, FUZZ-02, FUZZ-03, SCORE-01]
- [x] 96-04-PLAN.md — fuzz-target chaos profile + compose + expected_results + README triple-update [LAB-01]
- [x] 96-05-PLAN.md — docs + UAT-SERIES.md + Obsidian sync (CLAUDE.md mandatory) [FUZZ-01..04, SCORE-01, LAB-01]

## Progress

| Phase | Milestone | Plans Complete | Status | Completed |
|-------|-----------|----------------|--------|-----------|
| 93. Credential Infrastructure | v5.1 | 4/4 | Complete   | 2026-05-23 |
| 94. OpenAPI & Bearer Token Analysis | v5.1 | 3/3 | Complete   | 2026-05-23 |
| 95. Code-Signing Certificate Inventory | v5.1 | 4/4 | Complete   | 2026-05-23 |
| 96. Active REST Fuzzing | v5.1 | 5/5 | Complete   | 2026-05-23 |
