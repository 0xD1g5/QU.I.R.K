---
phase: 145-liveness-pre-pass
plan: 02
type: execute
wave: 2
depends_on: ["145-01"]
files_modified:
  - run_scan.py
  - quirk/models.py
  - tests/test_liveness_prepass.py
autonomous: true
requirements: [DISC-03]

must_haves:
  truths:
    - "Each discovery batch runs a TCP-SYN/ACK liveness probe before its full port sweep"
    - "Hosts the pre-pass finds non-responsive are skipped from the sweep but recorded as CryptoEndpoint rows, never silently dropped"
    - "A privilege downgrade of the SYN probe is detected once per scan and disclosed as a logger message plus a CryptoEndpoint advisory row"
    - "Liveness-skip rows do not flip the discovery ScanCheckpoint to partial-failure status"
    - "A batch whose hosts are all non-responsive spawns no sweep subprocess"
  artifacts:
    - path: "run_scan.py"
      provides: "_is_privileged(), _emit_liveness_fallback_advisory(), batch-loop pre-pass step"
      contains: "def _is_privileged"
    - path: "quirk/models.py"
      provides: "scan_error_category comment documenting liveness_skip and privilege_fallback"
      contains: "liveness_skip"
    - path: "tests/test_liveness_prepass.py"
      provides: "pre-pass filter, skip-row, privilege-advisory, and checkpoint-isolation coverage"
  key_links:
    - from: "run_scan.py"
      to: "quirk.discovery.nmap_provider.run_nmap_liveness_check"
      via: "import + per-batch call before run_nmap_discovery"
      pattern: "run_nmap_liveness_check"
    - from: "run_scan.py batch loop"
      to: "error_endpoints"
      via: "CryptoEndpoint(scan_error_category='liveness_skip') per non-responsive host"
      pattern: "liveness_skip"
    - from: "run_scan.py"
      to: "os.geteuid"
      via: "_is_privileged() getattr guard, evaluated once before the batch loop"
      pattern: "getattr\\(os, \"geteuid\", None\\)"
---

<objective>
Wire the Plan 01 primitives into the Phase 144 sequential batch loop in `run_scan.py`: determine
privilege once per scan, disclose a SYN→connect downgrade, run the liveness pre-pass per batch,
skip the expensive `-sT` sweep for non-responsive hosts, and record every skipped host as a
`CryptoEndpoint` row so Phase 146 has a queryable source for undetermined-host disclosure.

Purpose: this is the DISC-03 behavior change itself — everything in Plan 01 is inert until it runs
inside the batch loop.
Output: modified `run_scan.py` discovery block, an extended `scan_error_category` comment in
`quirk/models.py`, and `tests/test_liveness_prepass.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/145-liveness-pre-pass/145-CONTEXT.md
@.planning/phases/145-liveness-pre-pass/145-RESEARCH.md
@.planning/phases/145-liveness-pre-pass/145-PATTERNS.md
@.planning/phases/145-liveness-pre-pass/145-01-SUMMARY.md

<interfaces>
<!-- Contracts produced by Plan 01 and existing run_scan.py helpers. -->

quirk/discovery/nmap_provider.py (Plan 01):
  def run_nmap_liveness_check(targets: List[str], ports: List[int], output_dir: str,
                              logger=None, nmap_path: str = "nmap",
                              timeout_seconds: int = 600,
                              port_spec_override: Optional[str] = None) -> List[NmapHostStatus]
  # raises RuntimeError on nmap-missing / timeout / non-zero exit; ValueError on unsafe port spec

quirk/discovery/nmap_parser.py (Plan 01):
  @dataclass NmapHostStatus: host: str, up: bool, reason: str

run_scan.py (existing):
  def _emit_missing_extra_advisory(scanner_name: str, extra_group: str, error_endpoints) -> None
  def _collect_stage_partial_failures(run_stats, stage, error_endpoints, previous_error_count) -> list
  # ^ derives partial-failure entries from EVERY error_endpoint appended during the stage
  Discovery block locals in scope at the batch loop: error_endpoints, logger, cfg, args,
  ports_for_nmap, port_spec_override, extra_args, batch_num, _MAX_HOSTS_PER_CIDR,
  _expand_and_dedup_hosts, _chunked, run_stats, scan_run_id, _err_before_discovery

quirk/models.py (existing, line 35):
  scan_error_category = Column(String(32), nullable=True)  # Phase 41 D-11 + Phase 57 D-06: missing_extra|timeout|exception|config|invalid_input
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Privilege detection + fallback-advisory helper + models.py category comment</name>
  <files>run_scan.py, quirk/models.py, tests/test_liveness_prepass.py</files>
  <read_first>
    - run_scan.py lines 130-200 (the `_scanner_guard` exception wrapper and `_emit_missing_extra_advisory` — the exact advisory-row shape D-01 mirrors)
    - run_scan.py lines 280-310 (`_collect_stage_partial_failures` — how newly appended error_endpoints become partial-failure entries)
    - quirk/models.py lines 25-45 (CryptoEndpoint columns, especially `scan_error_category` at line 35 and its pipe-delimited comment convention)
    - tests/test_nmap_provider.py (module docstring + plain-assert test conventions to mirror in the new test file)
    - .planning/phases/145-liveness-pre-pass/145-CONTEXT.md (D-01, D-02)
  </read_first>
  <behavior>
    - `_is_privileged()` returns `True` when `os.geteuid()` returns 0, `False` when it returns non-zero, and `None` when `os.geteuid` does not exist on the platform (Windows).
    - `_emit_liveness_fallback_advisory(error_endpoints, logger)` appends exactly one `CryptoEndpoint` with `host="liveness-prepass"`, `port=0`, `protocol="ADVISORY"`, `scan_error_category="privilege_fallback"`, and calls `logger.info` once.
    - Calling `_emit_liveness_fallback_advisory` twice appends two rows — the caller, not the helper, is responsible for once-per-scan gating (matches `_emit_missing_extra_advisory`'s contract).
  </behavior>
  <action>
    In `quirk/models.py`, extend the inline comment on the `scan_error_category` column (line 35) by
    appending the two new category values to the existing pipe-delimited list, producing
    `...missing_extra|timeout|exception|config|invalid_input|liveness_skip|privilege_fallback`, and
    add `+ Phase 145 D-05` to the phase-provenance prefix, matching how Phase 57 D-06 was appended.
    This is a comment-only edit: `scan_error_category` is `Column(String(32), nullable=True)`, an
    untyped string, so NO schema change and NO migration is required.

    In `run_scan.py`, add two module-level helpers immediately after the existing
    `_emit_missing_extra_advisory` definition (around line 183):

    1. `_is_privileged() -> Optional[bool]` — implements D-02. Use
       `geteuid = getattr(os, "geteuid", None)`; return `None` when it is `None`, otherwise
       `geteuid() == 0`. The `getattr` guard is mandatory: `os.geteuid` does not exist on Windows
       and this code path runs on the Windows sensor build. Docstring must state that `None` means
       "privilege could not be determined" and that callers treat that as NOT privileged (never
       silently claim privileged when unverifiable).

    2. `_emit_liveness_fallback_advisory(error_endpoints, logger) -> None` — implements D-01,
       following `_emit_missing_extra_advisory`'s exact shape (logger message + one CryptoEndpoint
       advisory row, so the condition is visible in the scan artifact and not just on the console).
       Log via `logger.info` with a message stating the SYN pre-pass may have silently degraded to
       a TCP connect probe because QUIRK is not running with raw-socket privileges, and that
       results remain valid but the pre-pass is slower than intended. Append
       `CryptoEndpoint(host="liveness-prepass", port=0, protocol="ADVISORY",
       scan_error="liveness pre-pass SYN probe may have degraded to TCP connect (no raw-socket privileges)",
       scan_error_category="privilege_fallback")`. Use `logger.info` rather than
       `print(..., file=sys.stderr)` — this is an informational degradation, not an install error
       needing the QRK-INSTALL-001 error-code channel.

    Create `tests/test_liveness_prepass.py` with a module docstring naming Phase 145 / Plan 02 /
    DISC-03 / D-01,D-02,D-04,D-05. Add tests for every behavior bullet above:
    `test_is_privileged_true_when_euid_zero`, `test_is_privileged_false_when_euid_nonzero`
    (both via `unittest.mock.patch("os.geteuid", ...)`),
    `test_is_privileged_none_when_geteuid_missing` (patch with
    `unittest.mock.patch.object(os, "geteuid", None)` deleted via `monkeypatch.delattr(os, "geteuid", raising=False)`),
    and `test_fallback_advisory_row_shape`. Import `_is_privileged` /
    `_emit_liveness_fallback_advisory` from `run_scan` inside the test bodies, matching the
    `from run_scan import _collect_stage_partial_failures` pattern already used in
    `tests/test_nmap_provider.py`. Use a small stub logger object exposing `info`/`stamp`/`error`
    rather than constructing a real `Logger`.
  </action>
  <acceptance_criteria>
    - `grep -c "def _is_privileged" run_scan.py` returns 1
    - `grep -c "def _emit_liveness_fallback_advisory" run_scan.py` returns 1
    - `grep -n 'getattr(os, "geteuid", None)' run_scan.py` returns a match
    - `grep -n "liveness_skip|privilege_fallback" quirk/models.py` returns a match on the `scan_error_category` line
    - `git diff --stat quirk/models.py` shows exactly 1 changed line (comment-only, no schema change)
    - `pytest tests/test_liveness_prepass.py -x` passes with at least 4 tests collected
    - `python -m compileall run_scan.py quirk/models.py` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_liveness_prepass.py -x -q</automated>
  </verify>
  <done>`_is_privileged()` handles POSIX and Windows-missing-geteuid, the fallback advisory produces a logger message plus one `privilege_fallback` CryptoEndpoint row, and `scan_error_category`'s documented value set includes both new categories.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Insert the liveness pre-pass into the Phase 144 batch loop</name>
  <files>run_scan.py, tests/test_liveness_prepass.py</files>
  <read_first>
    - run_scan.py lines 1260-1350 (the whole discovery block: cache branch, nmap-unavailable branch, the `else:` nmap batch-loop branch at 1289-1330, the `_discovery_batch_loop_ran` ScanCheckpoint block at 1332-1344, and the `if not targets:` early return at 1346)
    - run_scan.py lines 1220-1250 (where `nmap_port_scope`, `port_spec_override`, `ports_for_nmap`, and `extra_args` are assigned)
    - run_scan.py lines 280-310 (`_collect_stage_partial_failures`)
    - run_scan.py lines 167-200 (advisory + helper conventions, as extended by Task 1)
    - quirk/discovery/nmap_provider.py (Plan 01 — `run_nmap_liveness_check` signature and its RuntimeError contract)
    - tests/test_nmap_provider.py lines 26-115 (the `_run_batched_discovery` loop-mirroring helper convention this task's tests extend)
    - .planning/phases/145-liveness-pre-pass/145-RESEARCH.md (Pitfall 4: skip the sweep entirely when a batch has zero survivors; Assumption A1: hosts absent from the XML)
  </read_first>
  <behavior>
    - Given a batch of 3 hosts where the pre-pass reports host 2 down, only hosts 1 and 3 are passed to the sweep call.
    - Host 2 produces exactly one `CryptoEndpoint` with `host="10.0.0.2"` (its real address, not a synthetic batch label), `port=0`, `protocol="ADVISORY"`, `scan_error_category="liveness_skip"`.
    - A batch where every host is reported down performs zero sweep calls and still produces one `liveness_skip` row per host.
    - A host present in the batch but absent from the pre-pass results is treated as responsive (fail-open) and IS passed to the sweep — it is never silently dropped.
    - When the pre-pass itself raises `RuntimeError`, the entire batch is passed to the sweep unfiltered, no `liveness_skip` rows are produced, and a warning is logged — the batch is never lost.
    - `liveness_skip` and `privilege_fallback` rows appended during discovery do NOT appear in the list returned by `_collect_stage_partial_failures("discovery", ...)`, so the discovery `ScanCheckpoint` stays `completed` rather than flipping to `partial`.
  </behavior>
  <action>
    Modify the `else:` (nmap batch-loop) branch of the discovery block in `run_scan.py`,
    lines ~1289-1330. Import `run_nmap_liveness_check` alongside the existing `run_nmap_discovery`
    import.

    Before the `for batch in _chunked(...)` loop (adjacent to the existing
    `_discovery_batch_loop_ran = True` / `all_open_ports = []` / `batch_num = 0` initialisers), add:
    - `liveness_endpoints: List[CryptoEndpoint] = []` — a SEPARATE accumulator, not `error_endpoints`.
      This is load-bearing: `_collect_stage_partial_failures` (line 285) turns EVERY endpoint
      appended to `error_endpoints` during the stage into a partial-failure entry, which would make
      the discovery ScanCheckpoint report `status="partial"` for any scan containing a single dead
      IP. Liveness skips are normal outcomes, not failures.
    - `_liveness_privileged = _is_privileged()` — evaluated exactly once here, per D-02, inside this
      branch only (so the advisory never fires on the cache-hit or nmap-absent branches where no
      pre-pass runs).
    - `if _liveness_privileged is not True: _emit_liveness_fallback_advisory(liveness_endpoints, logger)`
      — the `is not True` form makes the `None` (undeterminable, e.g. Windows) case disclose the
      degradation too, per Task 1's contract. Fires once per scan, not per batch.

    Inside the `for batch in ...:` body, immediately after `batch_num += 1` and BEFORE the existing
    `try: batch_open_ports = run_nmap_discovery(...)` block, insert the pre-pass step:
    - Call `run_nmap_liveness_check(targets=batch, ports=ports_for_nmap if port_spec_override is None else [],
      output_dir=cfg.output.directory, logger=logger, nmap_path=args.nmap_path,
      timeout_seconds=args.nmap_timeout, port_spec_override=port_spec_override)` inside its own
      `try` / `except RuntimeError as exc` block. On `RuntimeError`: log
      `logger.info(f"liveness pre-pass for batch {batch_num} failed ({exc!r}) — sweeping all hosts in batch")`
      and set `sweep_targets = batch` (fail-open — a pre-pass failure must never cost coverage), then
      proceed to the sweep without appending any liveness rows.
    - On success: build `down_hosts = {s.host for s in statuses if not s.up}`, then
      `sweep_targets = [h for h in batch if h not in down_hosts]`. Deriving the survivor list by
      EXCLUDING known-down hosts (rather than including known-up hosts) is deliberate: it makes a
      host that nmap omitted entirely from the XML default to being swept (RESEARCH.md assumption
      A1) instead of vanishing.
    - For each `h in batch if h in down_hosts`, append
      `CryptoEndpoint(host=h, port=0, protocol="ADVISORY",
      scan_error="liveness pre-pass: no response", scan_error_category="liveness_skip")` to
      `liveness_endpoints` (D-04/D-05: real per-host identity, distinct category, not a bare counter).
    - `logger.stamp(f"liveness pre-pass batch {batch_num}: {len(sweep_targets)} responsive, {len(down_hosts)} skipped")`.
    - `if not sweep_targets: continue` — short-circuit before the sweep so a fully-dead batch spawns
      no nmap subprocess at all (RESEARCH.md Pitfall 4).

    Change the existing `run_nmap_discovery(...)` call's `targets=batch` argument to
    `targets=sweep_targets`. Leave every other argument, the `except RuntimeError` batch-failure
    handler, and the `scan_error_category="exception"` row shape untouched.

    After the existing `if _discovery_batch_loop_ran:` ScanCheckpoint block (ends ~line 1344) and
    before the `if not targets:` early return at ~line 1346, add
    `error_endpoints.extend(liveness_endpoints)` with a comment explaining the ordering: the rows
    still reach the DB and the report, but they are merged in after the partial-failure snapshot so
    normal liveness skips never mark discovery as partially failed. Guard nothing — `extend` on an
    empty list is a no-op, and `liveness_endpoints` is only defined on this branch, so place the
    extend inside the same `if _discovery_batch_loop_ran:` block, after `write_scan_checkpoint`.

    Extend `tests/test_liveness_prepass.py` with a `_run_batched_prepass(host_tokens, liveness_fn,
    sweep_fn, chunk_size)` helper that mirrors the new loop shape exactly, in the same spirit and
    with the same "Mirrors run_scan.py's ... exactly" docstring convention as
    `tests/test_nmap_provider.py::_run_batched_discovery`. Return
    `(all_results, liveness_endpoints, sweep_calls, batch_count)`. Add one focused test per
    behavior bullet, named: `test_liveness_prepass_filters_batch_before_sweep`,
    `test_liveness_skip_appends_liveness_skip_category`,
    `test_all_dead_batch_skips_sweep_call`,
    `test_host_absent_from_liveness_results_is_swept`,
    `test_liveness_failure_falls_back_to_full_batch_sweep`, and
    `test_liveness_rows_excluded_from_discovery_partial_failures` (this last one imports
    `_collect_stage_partial_failures` from `run_scan` and asserts it returns an empty list when only
    `liveness_endpoints` were populated during the stage window).
  </action>
  <acceptance_criteria>
    - `grep -n "run_nmap_liveness_check" run_scan.py` shows both an import and a call site inside the batch loop
    - `grep -n "targets=sweep_targets" run_scan.py` returns exactly 1 match
    - `grep -c "liveness_endpoints" run_scan.py` returns at least 5 (declaration, advisory call, skip append, extend, plus loop usage)
    - `grep -n "_liveness_privileged = _is_privileged()" run_scan.py` returns exactly 1 match, positioned BEFORE the `for batch in _chunked` line (confirm with `grep -n` line numbers)
    - `grep -n "error_endpoints.extend(liveness_endpoints)" run_scan.py` returns a line number GREATER than the `write_scan_checkpoint` call in the discovery-checkpoint block
    - `pytest tests/test_liveness_prepass.py -x` passes with at least 10 tests collected
    - `pytest tests/test_nmap_provider.py -x` still passes (Phase 144 batch-loop tests unregressed)
    - `python -m compileall run_scan.py` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_liveness_prepass.py tests/test_nmap_provider.py tests/test_nmap_parser.py -x -q</automated>
  </verify>
  <done>Each batch runs the liveness pre-pass first, sweeps only survivors, records every skipped host as a `liveness_skip` CryptoEndpoint row, short-circuits fully-dead batches, fails open on pre-pass errors, and leaves the discovery ScanCheckpoint's partial-failure semantics unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator scan config → nmap subprocess argv | Port scope / target list reach the new per-batch liveness subprocess |
| nmap subprocess → XML parser | Local nmap output parsed by lxml on every batch |
| Scan orchestration → SQLite `crypto_endpoints` | Liveness-skip and advisory rows persisted as scan artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-01 | Tampering | Port-spec string reaching `-PS<ports>` argv from `port_spec_override` | mitigate | run_scan.py forwards `port_spec_override` unmodified to `run_nmap_liveness_check`, which allowlist-validates the assembled token with `_SAFE_NMAP_ARG_RE.fullmatch()` before `subprocess.run` (implemented in Plan 01, T-145-01); no second, weaker validation path is added here |
| T-145-02 | Tampering / Info Disclosure | Per-batch liveness XML parsing | mitigate | Parsing occurs only via `parse_nmap_host_status()`, which uses the `quirk.util.xml_safe.make_safe_parser()` WR-06 chokepoint (Plan 01); run_scan.py never parses nmap XML directly |
| T-145-04 | Information Disclosure | `privilege_fallback` advisory row content | mitigate | Advisory text names only the degradation condition — no euid value, username, path, or host identity is written into `scan_error`; row `host` is the fixed literal `"liveness-prepass"` |
| T-145-05 | Repudiation | Silent SYN→connect downgrade hidden from the audit trail | mitigate | D-01: the downgrade is recorded as a persisted `CryptoEndpoint` advisory row (survives into the DB and report artifact), not console-only logging; the `None`/undeterminable privilege case also discloses rather than assuming best-case |
| T-145-06 | Denial of Service | Additional nmap subprocess per batch | accept | Pre-pass is bounded by the existing `--host-timeout 10s` / `--max-retries 1` caps and short-circuits entirely for fully-dead batches; net effect is a reduction in total probe work, and the upstream probe-budget guard and `security.trusted_targets` allowlist are unchanged |
| T-145-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this phase (RESEARCH.md "Package Legitimacy Audit: Not applicable"); no install task to gate |
</threat_model>

<verification>
- `pytest tests/test_liveness_prepass.py tests/test_nmap_provider.py tests/test_nmap_parser.py tests/test_nmap_scope_args.py tests/test_jobs_nmap_scope_cap.py -x -q` green
- `pytest` (full suite, `not slow` default) shows no new failures versus the pre-phase baseline
- `python -m compileall run_scan.py quirk/models.py quirk/discovery/` exits 0
</verification>

<success_criteria>
- ROADMAP success criterion 1: each batch runs `-sn -PS<port-list>` ahead of its full port sweep
- ROADMAP success criterion 2: non-responsive hosts are skipped from the sweep and still counted, as per-host `liveness_skip` CryptoEndpoint rows
- ROADMAP success criterion 3: a privilege fallback is explicitly detected once per scan and disclosed via logger plus a persisted advisory row
- Discovery ScanCheckpoint status semantics from Phase 144 are unchanged by normal liveness skips
</success_criteria>

<output>
Create `.planning/phases/145-liveness-pre-pass/145-02-SUMMARY.md` when done
</output>
