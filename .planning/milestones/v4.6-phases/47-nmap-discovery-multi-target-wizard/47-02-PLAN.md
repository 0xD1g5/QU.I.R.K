---
phase: 47-nmap-discovery-multi-target-wizard
plan: 02
type: execute
wave: 2
depends_on: [47-01]
files_modified:
  - quirk/discovery/nmap_provider.py
  - quirk/util/optional_extra.py
  - quirk/interactive.py
  - run_scan.py
  - tests/test_nmap_provider.py
  - tests/test_optional_extra.py
  - tests/test_run_scan_budget_guard.py
autonomous: true
requirements:
  - DISCOVER-01
  - DISCOVER-02
  - DISCOVER-03
  - DISCOVER-04
must_haves:
  truths:
    - "User running the wizard sees one global y/N nmap prompt; answer applies to all targets."
    - "When `nmap` binary is absent, scan does NOT crash — it falls back to CONSULTING_TLS_PORTS and emits one coverage_gap INFO finding."
    - "`_default_nmap_args` always includes `--max-parallelism 100`."
    - "When `targets × ports > 10,000`, TTY users see a y/N confirm before nmap fires; non-TTY users see a stderr warning and auto-proceed."
    - "Non-interactive (`--config`) runs honor `--discovery {builtin,nmap}` (no wizard prompt fired); D-09 satisfied without a new config sub-table."
  artifacts:
    - path: "quirk/discovery/nmap_provider.py"
      provides: "_default_nmap_args appends --max-parallelism 100"
      contains: "--max-parallelism"
    - path: "quirk/util/optional_extra.py"
      provides: "OptionalExtra extended with optional `binary` field; new `nmap` registry entry"
      contains: "binary"
    - path: "tests/test_run_scan_budget_guard.py"
      provides: "TTY confirm + non-TTY auto-proceed coverage for the >10,000 probe guard"
  key_links:
    - from: "run_scan.py:332"
      to: "probe-budget guard"
      via: "maybe_confirm_probe_budget(..., threshold=10_000, ...) — D-12 literal pinned at call site"
      pattern: "threshold=10_000"
    - from: "quirk/util/optional_extra.py"
      to: "shutil.which"
      via: "binary-availability probe when entry.binary is set"
      pattern: "shutil.which"
    - from: "quirk/interactive.py"
      to: "wizard nmap y/N prompt"
      via: "single global prompt; result reflected onto cfg via the existing --discovery code path"
      pattern: "enable nmap"
---

<objective>
Wire nmap discovery into the wizard with a global y/N toggle, hard-code `--max-parallelism 100` in the nmap default args, add a TTY-aware probe-budget guard at >10,000 probes, and emit a Phase 45-style coverage_gap INFO finding when the `nmap` binary is missing (falling back to `CONSULTING_TLS_PORTS`). This plan satisfies DISCOVER-01..04 and depends on Plan 47-01's parser populating `cfg.targets`.

Purpose: turns the parser's resolved target list into a real nmap-driven enterprise scan workflow with operator-safe defaults.

Output: extended `OptionalExtra` (optional `binary` field), new `nmap` registry entry, one-line nmap arg patch, wizard prompt, budget guard, three new test files / extensions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-nmap-discovery-multi-target-wizard/47-CONTEXT.md
@.planning/phases/47-nmap-discovery-multi-target-wizard/47-RESEARCH.md
@.planning/phases/45-install-day-ux/45-CONTEXT.md
@quirk/util/optional_extra.py
@quirk/discovery/nmap_provider.py
@quirk/interactive.py
@run_scan.py
@CLAUDE.md

**Decisions implemented (CONTEXT.md):**
- D-06 — One global y/N nmap prompt in the wizard (NOT per-target).
- D-07 — Hard-code `--max-parallelism 100` in `nmap_provider._default_nmap_args` (not configurable in this phase).
- D-08 — Missing `nmap` binary → coverage_gap INFO finding via the optional-extra registry pattern + fall back to `CONSULTING_TLS_PORTS`.
- D-09 — Non-interactive (`--config` / non-TTY stdin) skips the wizard prompt; existing `--discovery {builtin,nmap}` CLI flag is the source of truth (Risks #1 — no new `DiscoveryCfg` sub-table this phase).
- D-10 — TTY-aware probe-budget guard at >10,000 probes; uses `sys.stdout.isatty()`.
- D-11 — Probe math uses post-config resolved port list (`ports_for_nmap`) and post-CIDR-expansion target count.
- D-12 — Threshold locked at 10,000 (not configurable).

**Research anchors (RESEARCH.md):**
- F4 Option A — extend `OptionalExtra` with `binary: Optional[str] = None`; check `shutil.which(binary)` when set; backward-compatible since the field is keyword-only with a default.
- F8 — Insert guard just before the `with _phase_timer(...)` block at `run_scan.py:332`, before `run_nmap_discovery` at L337.
- F9 — Current default args at `nmap_provider.py:21-29` lack `--max-parallelism` (verified absent).
- F10 — `sys.stdout.isatty()` for the budget guard; `sys.stdin.isatty()` is implicit in the wizard via the existing `--config`-vs-wizard routing at `run_scan.py:284` (no new TTY check needed).
- Risks #4 — Probe-budget projection uses `ipaddress.ip_network(t).hosts()` count (excludes net/bcast) NOT `.num_addresses`, matching the existing `target_expander.py:16` semantics.
- Risks #6 — `binary` field MUST be a keyword-only optional with default `None`; existing 4 registry entries use kwargs so this is non-breaking.

<interfaces>
<!-- Existing surface this plan extends. -->

From quirk/util/optional_extra.py (current — Phase 45):
```python
@dataclass(frozen=True)
class OptionalExtra:
    extra: str
    modules: Tuple[str, ...]
    scanner_label: str
    install_hint: str
    enabled_attrs: Tuple[str, ...]
```

From quirk/discovery/nmap_provider.py:21-29 (current default args):
```python
return ["-sT", "-n", "-Pn", "--open", "-p", ports_csv,
        "--max-retries", "1", "--host-timeout", "10s"]
```

From quirk/interactive.py: `CONSULTING_TLS_PORTS` (17-port fallback list), `_prompt`, `_prompt_int` — existing helpers.

From run_scan.py:
- L221  — argparse construction
- L228  — existing `--discovery {builtin,nmap}` arg (REUSE, do not duplicate per Risks #1)
- L332  — pre-`run_nmap_discovery` insertion site for budget guard
- L337  — `run_nmap_discovery` call (do not move)
- L386  — existing `probe_missing_extras(cfg, error_endpoints)` call (binary probe rides this)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append --max-parallelism 100 to _default_nmap_args + extend OptionalExtra with optional binary field + register `nmap` (TDD)</name>
  <files>quirk/discovery/nmap_provider.py, quirk/util/optional_extra.py, tests/test_nmap_provider.py, tests/test_optional_extra.py</files>
  <behavior>
    RED first:
      - tests/test_nmap_provider.py — `test_default_args_includes_max_parallelism`: assert `_default_nmap_args("443,8443")` returns a list containing the consecutive elements `"--max-parallelism", "100"`. (Create file if absent; confirm pytest discovers it.)
      - tests/test_optional_extra.py — extend with:
        - `test_binary_field_default_is_none`: `OptionalExtra(extra="x", modules=("y",), scanner_label="z", install_hint="hint with `pip install quirk[x]`", enabled_attrs=()).binary is None` (proves backward compat).
        - `test_nmap_registry_entry_present`: there is exactly one entry in REGISTRY whose `extra == "nmap"`, with `binary == "nmap"`, `modules == ()`, `enabled_attrs == ("enable_nmap",)`, install_hint contains the literal substring "install nmap".
        - `test_nmap_binary_missing_emits_advisory` (DISCOVER-02): with `cfg.connectors.enable_nmap = True` and `shutil.which("nmap")` patched to return None, `probe_missing_extras(cfg, error_endpoints)` appends one CryptoEndpoint with `protocol == "ADVISORY"`, `scan_error_category == "missing_extra"`, `host == "nmap_discovery"`.
        - `test_nmap_binary_present_no_advisory`: same setup with `shutil.which("nmap")` returning `/usr/bin/nmap` → no advisory appended.
        - `test_nmap_disabled_silent`: `cfg.connectors.enable_nmap = False` AND binary missing → no advisory (D-08 silent-when-disabled invariant carries over from Phase 45).
      Confirm RED.

    GREEN:
      (a) `quirk/discovery/nmap_provider.py:21-29` — append `"--max-parallelism", "100"` to the returned list. Inline comment: `# D-07: hard-coded; not configurable in Phase 47.`
      (b) `quirk/util/optional_extra.py`:
          - Add `binary: Optional[str] = None` field to `OptionalExtra` (keyword-only via dataclass field default; import `Optional`). Risks #6: KEEP the existing 4 entries unchanged — they already use kwargs.
          - Update module docstring to mention the `binary` field and the binary-probe semantics.
          - Modify `is_extra_available(extra)`: if entry.binary is set, additionally require `shutil.which(entry.binary) is not None`. Import `shutil` at top.
          - Modify `probe_missing_extras(cfg, error_endpoints)`: in the availability check loop, treat the entry as "available" only if all modules are findable AND (entry.binary is None OR `shutil.which(entry.binary) is not None`). Reuse the same advisory-row shape (no new code path) so trends.py exclusion keeps working.
          - Add the new entry to REGISTRY (place AFTER the dashboard entry; preserve existing ordering):
            ```python
            OptionalExtra(
                extra="nmap",
                modules=(),
                binary="nmap",
                scanner_label="nmap_discovery",
                install_hint=(
                    "Nmap discovery unavailable — install nmap "
                    "(https://nmap.org/) and ensure it is in PATH; "
                    "falling back to consulting-tls port list"
                ),
                enabled_attrs=("enable_nmap",),
            ),
            ```
          - The `enable_nmap` attr lives on `cfg.connectors`. If `cfg.connectors.enable_nmap` does not exist on the dataclass, USE `getattr(connectors, "enable_nmap", False)` — `probe_missing_extras` already does this defensive lookup at line 143. No `quirk/config.py` schema change required this phase (Risks #1 / D-09 — the existing `--discovery {builtin,nmap}` CLI flag drives this; reflect it onto `cfg.connectors.enable_nmap` in Task 2 below).
      Confirm GREEN.
      `python -m compileall quirk/discovery/nmap_provider.py quirk/util/optional_extra.py` clean.
  </behavior>
  <action>
    Implement per behavior block. Inline `# D-07` and `# D-08` comments where the decisions are encoded. PEP 8. Minimal diff — do NOT reorder existing REGISTRY entries; do NOT alter the dashboard / identity / db / cloud entries.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m compileall quirk/discovery/nmap_provider.py quirk/util/optional_extra.py && pytest tests/test_nmap_provider.py tests/test_optional_extra.py -x</automated>
  </verify>
  <done>
    - `_default_nmap_args` includes `--max-parallelism 100` (DISCOVER-03).
    - `OptionalExtra` has optional `binary` field, default None (backward compat: existing 4 entries unchanged).
    - REGISTRY has a 5th entry `extra="nmap"`, `binary="nmap"`, `enabled_attrs=("enable_nmap",)`.
    - `probe_missing_extras` correctly emits one advisory when `enable_nmap=True` and binary missing; silent when disabled or binary present (DISCOVER-02 + D-08 silent-when-disabled).
    - All new + extended tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wizard nmap y/N prompt + reflect onto cfg.connectors.enable_nmap + nmap-binary fallback to CONSULTING_TLS_PORTS (TDD)</name>
  <files>quirk/interactive.py, run_scan.py, tests/test_optional_extra.py (extend), tests/test_run_scan_budget_guard.py (create scaffold)</files>
  <behavior>
    RED first:
      - In tests/test_optional_extra.py add `test_nmap_fallback_uses_consulting_tls_ports`: assert that the helper used by `run_scan.py` to choose `ports_for_nmap` falls back to `CONSULTING_TLS_PORTS` when nmap is unavailable. Implementation note: extract a small pure helper `def select_nmap_port_list(cfg, nmap_available: bool) -> list[int]` into `quirk/util/optional_extra.py` (or a new tiny module if naming feels off — planner discretion) so this is unit-testable without booting `run_scan.py`. Returns `cfg.scan.ports` when available, `CONSULTING_TLS_PORTS` when not. Single-source — both `run_scan.py` and `test_optional_extra.py` import from the same place.
      - Stub `tests/test_run_scan_budget_guard.py` with a placeholder skip (Task 3 fills in the assertions). This file scaffolding lets Task 3 stay focused on guard logic.
      Confirm RED.

    GREEN — modifications:
      (a) `quirk/interactive.py`:
          - After the new single target prompt added by Plan 47-01, add the global nmap y/N prompt:
            ```python
            enable_nmap = _prompt_bool(
                "Run nmap port discovery first? (recommended for >10 hosts)",
                default=False,
            )  # D-06: single global toggle, NOT per-target
            ```
            If `_prompt_bool` does not exist, use the existing `_prompt` returning a string and parse `y/yes` case-insensitively.
          - Reflect into the cfg shape the wizard already returns (e.g. `cfg.connectors.enable_nmap = enable_nmap`). If `connectors` lacks this field, set via `setattr(cfg.connectors, "enable_nmap", enable_nmap)` — defensive; we are NOT introducing a `DiscoveryCfg` sub-table this phase (Risks #1).
      (b) `run_scan.py`:
          - In the existing `--discovery {builtin,nmap}` handling block (search for the arg, read its current consumer), ALSO write its boolean form onto `cfg.connectors.enable_nmap` so the registry probe at L386 sees a consistent flag in BOTH modes (wizard and `--config`). One liner:
            ```python
            setattr(cfg.connectors, "enable_nmap", args.discovery == "nmap")  # D-09
            ```
            Place this immediately after argparse + cfg load resolution and BEFORE `probe_missing_extras` is called. Comment with D-09 reference.
          - At the nmap-call site (currently `run_scan.py:337` block), wrap selection of `ports_for_nmap` with the new `select_nmap_port_list(cfg, nmap_available)` helper. `nmap_available` = `is_extra_available("nmap")`. When False, use `CONSULTING_TLS_PORTS` and SKIP the `run_nmap_discovery` call (the registry advisory emitted at L386 already informs the user — D-08).
      (c) Extend the existing test added in Task 1 (`test_nmap_binary_missing_emits_advisory`) — verify a single `coverage_gap` / `missing_extra` advisory is emitted (no double-emit).

    Confirm GREEN. `python -m compileall quirk/ run_scan.py` clean.
  </behavior>
  <action>Implement per behavior block; inline `# D-06`, `# D-08`, `# D-09` comments. Keep diffs minimal. PEP 8.</action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m compileall quirk/ run_scan.py && pytest tests/test_optional_extra.py tests/test_nmap_provider.py -x</automated>
  </verify>
  <done>
    - Wizard asks ONE global nmap y/N (D-06); answer set on `cfg.connectors.enable_nmap`.
    - `--config` non-interactive runs reflect `--discovery nmap` onto `cfg.connectors.enable_nmap` (D-09); no new config sub-table.
    - When binary missing AND `enable_nmap=True`: ONE coverage_gap INFO advisory; `select_nmap_port_list` returns `CONSULTING_TLS_PORTS`; `run_nmap_discovery` is NOT called (no crash) (DISCOVER-02 + D-08).
    - All Task 1 + Task 2 tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TTY-aware probe-budget guard at >10,000 probes (TDD)</name>
  <files>run_scan.py, tests/test_run_scan_budget_guard.py</files>
  <behavior>
    RED — fill in tests/test_run_scan_budget_guard.py with three cases. To keep tests independent of `run_scan.py` import side-effects, EXTRACT the guard into a small pure function before testing — proposed location: `quirk/util/targets.py` (parser is already imported there) or a sibling helper module. Function signature:
    ```python
    def projected_probe_count(targets: list[str], ports: list[int]) -> int:
        """Count hosts (1 per FQDN/IP, .hosts() count per CIDR) × len(ports). Risks #4: uses .hosts() not .num_addresses."""

    def maybe_confirm_probe_budget(
        targets: list[str],
        ports: list[int],
        threshold: int = 10_000,
        is_tty: bool | None = None,
        prompt_fn=input,
        stderr_print_fn=None,
    ) -> bool:
        """Returns True if scan should proceed, False if user aborted.

        D-10: TTY → require y/N confirm. Non-TTY → print stderr warning and return True.
        D-12: threshold hard-coded at 10_000 (the kwarg exists for testability only — run_scan.py MUST pass the literal 10_000).
        """
    ```

    Tests:
      - `test_projected_probe_count_includes_cidr_hosts_excludes_net_bcast`: `projected_probe_count(["10.0.0.0/30"], [443])` returns 2 (`.hosts()` for /30 = 2 hosts). Risks #4.
      - `test_projected_probe_count_mix`: `projected_probe_count(["a.com", "b.com", "10.0.0.0/24"], [443, 8443])` returns `(2 + 254) * 2 == 512`.
      - `test_under_threshold_no_prompt`: `maybe_confirm_probe_budget(["a.com"], [443], is_tty=True, prompt_fn=lambda *_: pytest.fail("should not prompt"))` returns True without invoking prompt.
      - `test_over_threshold_tty_user_confirms_yes`: 200 hosts × 51 ports = 10,200; `is_tty=True`, `prompt_fn=lambda _: "y"` → returns True.
      - `test_over_threshold_tty_user_aborts_no`: same hosts/ports; `prompt_fn=lambda _: "n"` → returns False.
      - `test_over_threshold_non_tty_prints_stderr_and_proceeds`: `is_tty=False`, capture `stderr_print_fn` calls; returns True; the captured message contains "10,200" (or formatted equivalent).
      Confirm RED.

    GREEN:
      (a) Implement the two helpers in `quirk/util/targets.py` (it already houses target-related logic). Use `ipaddress.ip_network(t, strict=False).hosts()` count for any token containing '/'; count 1 otherwise.
      (b) `run_scan.py`: just before the `with _phase_timer(...)` block at L332 (before `run_nmap_discovery` at L337) insert:
          ```python
          # D-10/D-11/D-12: TTY-aware probe-budget guard
          if not maybe_confirm_probe_budget(
              targets=nmap_targets,
              ports=ports_for_nmap,
              threshold=10_000,  # D-12: threshold locked to 10,000 by roadmap success criterion #5; not configurable
              is_tty=sys.stdout.isatty(),
          ):
              logger.info("Aborted by user — projected probe count exceeded threshold.")
              return
          ```
          Add `import sys` at the top of `run_scan.py` if not already present (likely is — verify).
      Confirm GREEN.
      `python -m compileall quirk/ run_scan.py` clean.
  </behavior>
  <action>
    Implement helpers + insert guard. Inline `# D-10` / `# D-11` / `# D-12` comments. Minimal diff. PEP 8. Do NOT touch the surrounding `_phase_timer`/`run_nmap_discovery` code other than the insertion immediately before.

    Note: `nmap_targets` is the post-`_build_nmap_target_list` list (raw CIDR strings + hosts) — `projected_probe_count` MUST handle CIDR string tokens. `ports_for_nmap` is the post-config resolved port list, satisfying D-11.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m compileall quirk/ run_scan.py && pytest tests/test_run_scan_budget_guard.py tests/test_targets_parser.py -x</automated>
  </verify>
  <done>
    - `projected_probe_count` returns the exact host × port count using `.hosts()` for CIDRs (Risks #4).
    - `maybe_confirm_probe_budget` returns True under threshold without prompting; over threshold + TTY → prompts; over threshold + non-TTY → stderr warn + True.
    - `run_scan.py:332` invokes the guard before `run_nmap_discovery`; if user answers no, scan returns cleanly without invoking nmap (DISCOVER-04).
    - All 6 budget-guard tests green; no Task 1/2 regression.
  </done>
</task>

<task type="auto">
  <name>Task 4: Atomic commits + full-suite regression sweep</name>
  <files>(none — git operations only)</files>
  <action>
    Three atomic commits per project convention (no `--no-verify`):

    1. `feat(47-02): hard-code --max-parallelism 100 + add nmap to optional-extra registry (DISCOVER-02, DISCOVER-03)` — files: quirk/discovery/nmap_provider.py, quirk/util/optional_extra.py, tests/test_nmap_provider.py, tests/test_optional_extra.py
    2. `feat(47-02): wizard nmap y/N prompt + CONSULTING_TLS_PORTS fallback (DISCOVER-01, D-06, D-08, D-09)` — files: quirk/interactive.py, run_scan.py
    3. `feat(47-02): TTY-aware probe-budget guard at 10,000 probes (DISCOVER-04, D-10..D-12)` — files: quirk/util/targets.py, run_scan.py, tests/test_run_scan_budget_guard.py

    Then full regression: `pytest tests/ -x`. Fix-forward any failures.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && git log --oneline -10 | grep -c "47-02" | grep -v '^0$' && pytest tests/ -x</automated>
  </verify>
  <done>Three (or four, if a fix-forward was needed) commits with `47-02` scope; full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `nmap_targets` (parsed in Plan 47-01) → `subprocess.run(["nmap", ...])` | List-form args; never `shell=True` (verified at `nmap_provider.py:55`) |
| `cfg.connectors.enable_nmap` → registry probe loop | Defensive `getattr(..., False)` so missing attribute is silent (Phase 45 invariant) |
| Probe-budget projection → user-facing confirm | Calculation uses `ipaddress` stdlib only; no eval/exec |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-05 | Denial of Service | nmap subprocess at /16+ scope | mitigate | `--max-parallelism 100` cap (D-07) prevents macOS socket exhaustion; probe-budget TTY confirm at >10,000 probes requires explicit operator approval (D-10) |
| T-47-06 | Tampering | subprocess args | mitigate | Existing list-form `subprocess.run(args)` at `nmap_provider.py:55`; no shell. Targets validated upstream by Plan 47-01 parser (T-47-01) |
| T-47-07 | Repudiation | silent missing-binary failure | mitigate | Coverage_gap INFO advisory emitted via registry (D-08); fallback to `CONSULTING_TLS_PORTS` documented in the same finding's install_hint |
| T-47-08 | Denial of Service | non-TTY automated runs with massive CIDR | accept | D-10 mandates auto-proceed in non-TTY mode (CI / scripted runs); operator owns the upstream `--targets-file` content and the stderr warning is captured in run logs |
| T-47-09 | Tampering | binary substitution (PATH attack on `shutil.which("nmap")`) | accept | CLI-trusted boundary; operator's own machine. Standard host-security hygiene assumed. |
</threat_model>

<verification>
- `pytest tests/test_nmap_provider.py tests/test_optional_extra.py tests/test_run_scan_budget_guard.py -x` green
- `python -m compileall quirk/ run_scan.py` clean
- Manual: `python -c "from quirk.discovery.nmap_provider import _default_nmap_args; assert '--max-parallelism' in _default_nmap_args('443')"` exits 0
- Manual: temporarily patch PATH to hide nmap, run `quirk --discovery nmap --targets-file /tmp/h.txt --config /tmp/c.yaml`; observe ADVISORY row in output, no crash, scan proceeds with CONSULTING_TLS_PORTS
- Full regression: `pytest tests/ -x` green
</verification>

<success_criteria>
Roadmap success criterion 4 and the probe-budget half of 5 are observably satisfied:
4. **nmap toggle in wizard; `--max-parallelism 100`; missing-binary → clear warning + fallback to default ports (no crash)** —
   - Wizard asks one global y/N (D-06).
   - `_default_nmap_args` always includes `--max-parallelism 100` (D-07).
   - Missing nmap → coverage_gap INFO finding via Phase 45 registry pattern + fallback to `CONSULTING_TLS_PORTS` (D-08).
5. **`len(targets) × len(ports) > 10,000` warning before nmap** — TTY users confirm y/N; non-TTY users get stderr warning + auto-proceed (D-10/D-11/D-12).

Non-interactive D-09 invariant: `--discovery nmap` reflects onto `cfg.connectors.enable_nmap` so the registry advisory and the wizard prompt converge on the same boolean — no new `DiscoveryCfg` schema this phase (Risks #1).
</success_criteria>

<output>
After completion, create `.planning/phases/47-nmap-discovery-multi-target-wizard/47-02-SUMMARY.md` documenting:
- The optional-extra `binary` field extension (backward-compat note for Phase 45 callers)
- `select_nmap_port_list` helper rationale and location
- Probe-budget guard helper signature + the `.hosts()` vs `.num_addresses` choice (Risks #4)
- Why no `DiscoveryCfg` sub-table was added (Risks #1 / D-09)
- Test count and DISCOVER-XX coverage matrix
</output>

