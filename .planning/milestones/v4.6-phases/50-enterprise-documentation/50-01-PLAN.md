---
phase: 50-enterprise-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_phase50_docs_presence.py
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-04]
tags: [docs, phase-50, gate-test, tdd-red]
must_haves:
  truths:
    - "A pytest gate fails the suite when docs/architecture.md is missing or lacks required sections"
    - "A pytest gate fails the suite when docs/operators-guide.md is missing or lacks required sections, source URLs, or compliance-tooling citations"
    - "Test FAILS at end of Wave 1 (RED) — docs do not yet exist"
  artifacts:
    - path: "tests/test_phase50_docs_presence.py"
      provides: "File-presence + section-substring gate for both new docs"
      contains: "test_required_docs_resolve"
  key_links:
    - from: "tests/test_phase50_docs_presence.py"
      to: "docs/architecture.md, docs/operators-guide.md"
      via: "open(...).read().lower() + substring assertions"
      pattern: "_REQUIRED_SECTIONS"
---

<objective>
Create the Wave-0 RED gate test that locks in the section-content invariants for the
two new docs that Plans 50-02 and 50-03 will write. Mirrors the canonical project
pattern from `tests/test_pqc_terminology_gate.py` (Phase 48): read repo file from disk,
lowercased substring assertions, no fixtures.

Purpose: Prevent silent doc drift. Once Plans 50-02 and 50-03 land, this test goes
GREEN; any future PR that removes a required section, source URL, or compliance
citation breaks CI.

Output: One new test file. After this plan ships, `pytest tests/test_phase50_docs_presence.py`
MUST FAIL with a clear "Required Phase 50 doc missing" message — that is the RED
condition by design.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-enterprise-documentation/50-CONTEXT.md
@.planning/phases/50-enterprise-documentation/50-RESEARCH.md
@.planning/phases/50-enterprise-documentation/50-PATTERNS.md
@tests/test_pqc_terminology_gate.py
@CLAUDE.md

<interfaces>
<!-- Pattern analog from tests/test_pqc_terminology_gate.py (Phase 48) -->
<!-- Convention: _REPO_ROOT resolves repo root from __file__; files read with utf-8 + lowercased before substring check. -->

```python
import os

_REPO_ROOT = os.path.normpath(os.path.join(os.path.dirname(__file__), ".."))

def _read(rel: str) -> str:
    return open(os.path.join(_REPO_ROOT, rel), encoding="utf-8").read().lower()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author tests/test_phase50_docs_presence.py</name>
  <files>tests/test_phase50_docs_presence.py</files>
  <behavior>
    - test_required_docs_resolve: asserts both docs/architecture.md and docs/operators-guide.md exist on disk; FAILS until Plans 50-02/03 ship.
    - test_required_sections_present: for each doc, asserts the lowercased file contents contain every required substring; FAILS with the (file, missing_substring) tuple list when any are absent.
    - architecture.md required substrings: "data flow", "trust boundar" (matches boundary/boundaries), "```mermaid", "credential" (covers connector credential-storage matrix per scope addition #1).
    - operators-guide.md required substrings: "troubleshoot", "compliance map maintenance", "quirk compliance status", "staleness_threshold_days", "tests/test_compliance_freshness.py", "https://www.pcisecuritystandards.org" (PCI source URL), "https://www.ecfr.gov" (HIPAA canonical regulation text), "hhs.gov" (HIPAA publisher; case-insensitive — note: HHS publishes via ECFR per RESEARCH.md "Source URLs" table; both substrings required), "https://csrc.nist.gov" (NIST source URL), "quirk init" (covers scope addition #2).
  </behavior>
  <action>
    Copy the structure of `tests/test_pqc_terminology_gate.py` verbatim. Module docstring should read:

    """Phase 50 docs presence gate: enforce architecture.md + operators-guide.md ship with the required sections so docs cannot silently regress.

    Pattern modelled on tests/test_pqc_terminology_gate.py — read source file from disk, substring-check the contents.
    """

    Define module constants:
    - `_REPO_ROOT = os.path.normpath(os.path.join(os.path.dirname(__file__), ".."))`
    - `_REQUIRED_DOCS = ["docs/architecture.md", "docs/operators-guide.md"]`
    - `_REQUIRED_SECTIONS: dict[str, tuple[str, ...]]` keyed by relative path. Architecture.md tuple includes: "data flow", "trust boundar", "```mermaid", "credential". Operators-guide.md tuple includes: "troubleshoot", "compliance map maintenance", "quirk compliance status", "staleness_threshold_days", "tests/test_compliance_freshness.py", "https://www.pcisecuritystandards.org", "https://www.ecfr.gov", "hhs.gov", "https://csrc.nist.gov", "quirk init".

    Define helper `_read(rel: str) -> str` returning lowercased file contents.

    Two test functions:
    - `def test_required_docs_resolve()` — for each rel in _REQUIRED_DOCS, assert os.path.isfile(os.path.join(_REPO_ROOT, rel)) with message f"Required Phase 50 doc missing: {rel}".
    - `def test_required_sections_present()` — iterate _REQUIRED_SECTIONS, accumulate (rel, needle) pairs where needle.lower() not in text; final `assert not missing, f"Phase 50 docs missing required sections: {missing}"`.

    PEP 8: 4-space indent, double quotes for docstrings, module-level constants UPPERCASE with leading underscore. No trailing whitespace. Use `from __future__ import annotations` only if needed (the analog does not, so omit).

    DO NOT create or modify docs/architecture.md or docs/operators-guide.md in this plan — they are deliberately absent until Plans 50-02 / 50-03. Per D-RED gate convention, the test MUST fail at end of Wave 1.

    Implements scope additions #1 (credential-storage matrix → "credential" substring), #2 ("quirk init" subsection → "quirk init" substring), #3 (the test file itself).
  </action>
  <verify>
    <automated>python -m compileall tests/test_phase50_docs_presence.py 2>&1 | grep -v '^Listing' || true; python -m pytest tests/test_phase50_docs_presence.py -x 2>&1 | grep -E '(FAILED|Required Phase 50 doc missing)'</automated>
  </verify>
  <done>
    File compiles cleanly under `python -m compileall`. `pytest tests/test_phase50_docs_presence.py` FAILS at `test_required_docs_resolve` with "Required Phase 50 doc missing: docs/architecture.md" — confirming RED state. No other tests in the existing suite are affected.
  </done>
</task>

<task type="auto">
  <name>Task 2: Leave gate test uncommitted for atomic landing in 50-02</name>
  <files>tests/test_phase50_docs_presence.py</files>
  <action>
    **DO NOT COMMIT this file in Plan 50-01.** Per W6 fix: committing a deliberately-failing test in isolation would turn QUIRK-v4 CI red between waves. Plan 50-02 will commit `tests/test_phase50_docs_presence.py` atomically alongside `docs/architecture.md` in a single commit — at which point the architecture half goes GREEN immediately and only the operators-guide.md substrings remain RED until 50-03 lands.

    Concretely: leave the file on disk (created by Task 1) either staged or unstaged. Do NOT invoke `gsd-tools.cjs commit` for this path in 50-01. Plan 50-02's commit task will stage and commit it.

    No git operation in this task.
  </action>
  <verify>
    <automated>test -f tests/test_phase50_docs_presence.py && (git diff --cached --name-only | grep -q "^tests/test_phase50_docs_presence.py$" || git status --porcelain | grep -q "tests/test_phase50_docs_presence.py") && ! git log --oneline -- tests/test_phase50_docs_presence.py | grep -q .</automated>
  </verify>
  <done>Test file present on disk (staged or unstaged), NOT yet committed. Plan 50-02 will commit it atomically with docs/architecture.md.</done>
</task>

</tasks>

<verification>
- `python -m compileall tests/test_phase50_docs_presence.py` succeeds (PEP 8 / syntax clean).
- `pytest tests/test_phase50_docs_presence.py` FAILS — required RED state until Plans 50-02 and 50-03 land. Failure message names the missing doc paths.
- `pytest -m 'not slow' tests/ -x --ignore=tests/test_phase50_docs_presence.py` still green (rest of fleet unaffected).
- `git log` shows ZERO new commits in this plan — the test file is intentionally left uncommitted for atomic landing in Plan 50-02 (W6 fix).
</verification>

<success_criteria>
- `tests/test_phase50_docs_presence.py` exists, imports cleanly, defines the two test functions enumerated under `<behavior>`.
- Test FAILS in current state (no docs yet) with a clear, actionable message — proves the gate is wired.
- Required substring set covers DOCS-01 (architecture sections), DOCS-02 (operators-guide sections + quirk init), DOCS-04 (compliance maintenance citations + PCI/ECFR/HHS/NIST source URLs), and scope addition #1 (credential-storage matrix via "credential" substring).
- NO commit recorded in this plan — test file is staged/unstaged for atomic landing in Plan 50-02 (W6 fix avoids RED CI between waves).
</success_criteria>

<output>
After completion, create `.planning/phases/50-enterprise-documentation/50-01-SUMMARY.md` documenting:
- Final substring set (verbatim from the file).
- The deliberate RED state and which downstream plans turn it GREEN (50-02 → architecture.md; 50-03 → operators-guide.md).
- The Phase 48 PQC-terminology-gate analog cited at the top of the test docstring.
</output>
