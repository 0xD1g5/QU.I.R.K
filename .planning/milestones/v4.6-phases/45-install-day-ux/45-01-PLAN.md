---
phase: 45-install-day-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/test_install_all_excludes_impacket.py
  - docs/installation.md
autonomous: true
requirements: [INSTALL-03]
tags: [packaging, extras, regression-test]
must_haves:
  truths:
    - "`pip install quirk[all]` resolves successfully with all of cloud + db + motion + redis + dashboard"
    - "`pip install quirk[all]` does NOT pull impacket (transitive pyOpenSSL/cryptography downgrade guard)"
    - "Users see in installation.md that `[all]` excludes `[identity]` and pulls Playwright browser binaries"
  artifacts:
    - path: "pyproject.toml"
      provides: "[all] meta-extra"
      contains: "all = ["
    - path: "tests/test_install_all_excludes_impacket.py"
      provides: "Regression test asserting impacket is absent from quirk[all]"
      contains: "test_install_all_excludes_impacket"
    - path: "docs/installation.md"
      provides: "User-facing documentation of [all] composition + Playwright footnote"
  key_links:
    - from: "pyproject.toml [all]"
      to: "self-referential extras (cloud, db, motion, redis, dashboard)"
      pattern: "quirk\\[(cloud|db|motion|redis|dashboard)\\]"
    - from: "tests/test_install_all_excludes_impacket.py"
      to: "pip --dry-run --report JSON"
      pattern: "impacket.*not in installed"
---

<objective>
Add the `[all]` meta-extra to `pyproject.toml` (D-01: cloud + db + motion + redis + dashboard;
impacket explicitly excluded), wire a CI-grade regression test that asserts `pip install quirk[all]`
does not transitively pull `impacket`, and document the `[all]` composition (with Playwright
size note) in `docs/installation.md`.

Purpose: SC #3 of Phase 45 — `pip install quirk[all]` installs all scanner extras; impacket NOT
in `[all]` (stays in `[identity]` only). This is the structural guard that prevents the
pyOpenSSL/cryptography downgrade from breaking the TLS scanner.

Output: Updated `pyproject.toml` with the `[all]` group, a slow-marked regression test, and
user-facing install documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/45-install-day-ux/45-CONTEXT.md
@.planning/phases/45-install-day-ux/45-RESEARCH.md
@pyproject.toml
@docs/installation.md
@CLAUDE.md

<interfaces>
<!-- Existing pyproject.toml extras (lines 33-61) — self-referential pattern already in use -->

```toml
[project.optional-dependencies]
dashboard = ["fastapi>=0.128.8", "uvicorn[standard]>=0.39.0", "python-multipart>=0.0.20", "playwright>=1.58.0"]
identity = ["impacket>=0.13.0,<0.14", "ldap3>=2.9.1"]
cloud    = ["google-api-python-client>=2.0.0", "google-auth>=2.36.0", "azure-mgmt-storage>=21.0.0",
            "kubernetes>=35.0.0", "google-cloud-container>=2.0.0",
            "azure-mgmt-containerservice>=35.0.0", "hvac>=2.4.0"]
db       = ["psycopg2-binary>=2.9.0", "PyMySQL>=1.1.0"]
motion   = ["quirk[email]", "quirk[broker]", "quirk[kafka]"]
email    = []
broker   = ["redis>=5.0"]
kafka    = ["kafka-python>=2.0"]
redis    = ["redis>=5.0"]
```

<!-- Pytest markers already declared (pyproject.toml:76-79) — no change needed -->
markers = ["slow", "live_infra"]
addopts = "-m 'not slow'"  # default run skips slow tests
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the regression test (RED)</name>
  <files>tests/test_install_all_excludes_impacket.py</files>
  <behavior>
    - Test 1 (test_install_all_excludes_impacket): pip dry-run resolution of `<repo_root>[all]`
      MUST succeed (returncode 0) AND the resulting JSON report MUST NOT list `impacket` in the
      `install` array. Test is marked `@pytest.mark.slow` so default `pytest` runs skip it; CI
      runs `pytest -m slow` separately. Asserts the literal string "impacket" is absent
      (case-insensitive on the package name) from the resolved set.
    - Test fails BEFORE implementation because the `[all]` extra does not exist yet.
  </behavior>
  <action>
    Create `tests/test_install_all_excludes_impacket.py` per RESEARCH.md §7 option (a).
    The test invokes `python -m pip install --dry-run --ignore-installed --quiet --report
    <tmp>/report.json -e <repo_root>[all]`, parses the JSON, lowercases each
    `item["metadata"]["name"]` from `report["install"]`, and asserts `"impacket" not in installed`.
    Decorate with `@pytest.mark.slow`. Set `subprocess.run` timeout to 180s. Failure message
    must reference Phase 45 / D-01 and the pyOpenSSL/cryptography downgrade rationale.
    Run `pytest -m slow tests/test_install_all_excludes_impacket.py -x` and confirm the test
    FAILS or ERRORS at this stage (no `[all]` extra exists yet — pip will error with
    "WARNING: ... does not provide the extra 'all'", so capture that as the RED state).
    Commit: `test(45): add regression test asserting quirk[all] excludes impacket`.
  </action>
  <verify>
    <automated>pytest -m slow tests/test_install_all_excludes_impacket.py -x 2>&1 | grep -E "FAILED|ERROR|does not provide the extra"</automated>
  </verify>
  <done>
    Test file exists, decorated `@pytest.mark.slow`, references D-01 in failure message, and
    confirmed RED before Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the `[all]` meta-extra to pyproject.toml (GREEN)</name>
  <files>pyproject.toml</files>
  <behavior>
    - After applying this change, `pytest -m slow tests/test_install_all_excludes_impacket.py -x`
      passes (impacket absent from resolved `[all]`).
    - `pip install --dry-run -e .[all]` succeeds and lists at minimum the union of cloud + db +
      motion + redis + dashboard packages.
    - `pip install --dry-run -e .[identity]` still resolves and DOES include impacket (regression
      protection — `[identity]` is unchanged).
  </behavior>
  <action>
    Edit `pyproject.toml` `[project.optional-dependencies]`. Append a new `all` entry after the
    existing extras (per D-01 and RESEARCH.md §6):

    ```toml
    all = [
        "quirk[cloud]",
        "quirk[db]",
        "quirk[motion]",
        "quirk[redis]",
        "quirk[dashboard]",
    ]
    # NOTE: quirk[identity] is INTENTIONALLY EXCLUDED from [all] — impacket
    # transitively pulls pyOpenSSL which downgrades cryptography and breaks the
    # TLS scanner. See Phase 45 / D-01. tests/test_install_all_excludes_impacket.py
    # guards this; do NOT add quirk[identity] to the list above.
    ```

    Use self-referential extras (already proven by the existing
    `motion = ["quirk[email]", "quirk[broker]", "quirk[kafka]"]` pattern at line 57). Do NOT
    inline the package lists — the per-extra lists remain the single source of truth.

    Run `pip install --dry-run -e '.[all]'` locally to confirm resolution succeeds, then run
    `pytest -m slow tests/test_install_all_excludes_impacket.py -x` and confirm GREEN.
    Run `python -m compileall quirk run_scan.py` per CLAUDE.md Code Standards.
    Commit: `feat(45): add [all] meta-extra to pyproject.toml (D-01 — excludes impacket)`.
  </action>
  <verify>
    <automated>pytest -m slow tests/test_install_all_excludes_impacket.py -x</automated>
  </verify>
  <done>
    `[all]` extra present in `pyproject.toml`; regression test GREEN; impacket comment present;
    `python -m compileall` clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Document `[all]` composition in installation.md</name>
  <files>docs/installation.md</files>
  <action>
    Edit `docs/installation.md` per RESEARCH.md §10 (D-02 documentation requirement):

    1. In the "System Requirements" section (around line 7), add a bullet: "pip ≥ 21.3
       (required for self-referential extras resolution; pip 22.2+ recommended for the
       `--report` JSON test in CI)."

    2. In the "Optional Dependencies" table (around line 116), add a new row at the top:
       `| All optional scanners (recommended for consultants) | \`pip install quirk[all]\` —
       installs cloud + db + motion + redis + dashboard. **Excludes \`[identity]\`** because
       impacket transitively downgrades the cryptography library and breaks the TLS scanner.
       Includes Playwright browser binaries via \`[dashboard]\` (~250 MB). |`

    3. Add a new subsection after the Optional Dependencies table titled "Why `[all]` excludes
       `[identity]`": one paragraph explaining the impacket → pyOpenSSL → cryptography
       downgrade chain (per RESEARCH.md §8). Tell consultants who need Kerberos to install
       `[identity]` in a separate venv: `pip install quirk[all]` then in a fresh venv
       `pip install quirk[identity]`.

    4. Add a "Coverage advisories" callout after the install verification section: "If you
       enable a scanner whose optional extra is missing, QUIRK will emit a single INFO
       advisory finding per skipped scanner instead of crashing. Each advisory names the
       exact `pip install quirk[<extra>]` command to run."

    Commit: `docs(45): document [all] composition, identity exclusion, Playwright cost`.
  </action>
  <verify>
    <automated>grep -E "pip install quirk\[all\]|excludes.*identity|Playwright" docs/installation.md | grep -v '^#' | grep -c .</automated>
  </verify>
  <done>
    Three substantive additions present in `docs/installation.md`: pip-version bullet, `[all]`
    table row, identity-exclusion subsection, and coverage-advisory callout. Verify command
    returns ≥ 3.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → PyPI resolver | The `pip install` invocation crosses into PyPI's resolution graph; transitive deps are out of QUIRK's direct control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | pyproject.toml `[all]` extra | mitigate | Self-referential extras keep per-extra lists as SOT; `tests/test_install_all_excludes_impacket.py` guards against accidental impacket inclusion (D-01). |
| T-45-02 | Tampering | Transitive crypto-library downgrade via impacket → pyOpenSSL | mitigate | Regression test asserts impacket absent from `[all]`; CI gate. |
| T-45-03 | Information Disclosure | docs/installation.md examples | accept | No secrets; documentation only. |
</threat_model>

<verification>
- `pip install --dry-run -e '.[all]'` resolves successfully.
- `pytest -m slow tests/test_install_all_excludes_impacket.py -x` is GREEN.
- `pip install --dry-run -e '.[identity]'` still includes impacket (unchanged).
- `python -m compileall quirk run_scan.py` returns zero errors.
- `docs/installation.md` contains the literal strings `pip install quirk[all]`,
  `excludes [identity]`, and a Playwright size mention.
</verification>

<success_criteria>
- Phase 45 SC #3: `pip install quirk[all]` installs all scanner extras; impacket NOT in `[all]`.
- D-01 honored: regression test in CI prevents future drift.
- D-02 honored: Playwright browser binary cost documented in user-facing install docs.
</success_criteria>

<out_of_scope>
- Migrating per-scanner `*_AVAILABLE` flags (D-11 hard constraint — Plan 02 also respects).
- Centralized probe / advisory emission (Plan 02).
- Report rendering / dashboard DTO changes (Plan 03).
- Chaos lab updates (this phase does not touch the lab).
</out_of_scope>

<output>
After completion, create `.planning/phases/45-install-day-ux/45-01-SUMMARY.md` per the standard
SUMMARY template.
</output>
