---
phase: 111-console-dashboard-awareness
plan: 03
type: execute
wave: 3
depends_on: ["111-01", "111-02"]
files_modified:
  - docs/UAT-SERIES.md
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03]
must_haves:
  truths:
    - "docs/UAT-SERIES.md has a Series 111 covering DASH-01/02/03 with concrete pass criteria"
    - "UAT-SERIES.md is synced to the Obsidian vault and a Phase 111 vault note exists"
    - "The full backend + frontend suite is green and a real built dashboard is opened for human visual confirmation"
  artifacts:
    - path: "docs/UAT-SERIES.md"
      provides: "Series 111 test cases"
      contains: "Series 111"
  key_links:
    - from: "docs/UAT-SERIES.md"
      to: "Obsidian vault UAT-Series.md + Phase-111 note"
      via: "vault filesystem write"
      pattern: "Series 111"
---

<objective>
Close out Phase 111 documentation: add a Series 111 to docs/UAT-SERIES.md covering the sensor registry, segment filter, per-segment gauges, and coverage banner; sync UAT-SERIES.md + a Phase 111 note to the Obsidian vault; run the full suite; and present the live built dashboard for human visual confirmation (visual fidelity is human-UAT gated, not asserted by presence tests).

Purpose: Satisfy the mandatory per-phase docs/Obsidian/UAT steps in CLAUDE.md and gate visual fidelity on human UAT.
Output: Updated + committed UAT-SERIES.md, Obsidian vault notes, full-suite green, and a human verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/111-console-dashboard-awareness/111-CONTEXT.md
@.planning/phases/111-console-dashboard-awareness/111-UI-SPEC.md
@.planning/phases/111-console-dashboard-awareness/111-VALIDATION.md
@.planning/phases/111-console-dashboard-awareness/111-01-SUMMARY.md
@.planning/phases/111-console-dashboard-awareness/111-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Series 111 to docs/UAT-SERIES.md + sync to Obsidian + commit</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (the "Series 110" block ~L12775 onward as the structural template; the document header "Last Updated" line)
    - .planning/phases/111-console-dashboard-awareness/111-VALIDATION.md (per-task verification map -> pass criteria)
    - .planning/phases/111-console-dashboard-awareness/111-01-SUMMARY.md and 111-02-SUMMARY.md (exact endpoints/fields/components shipped)
    - CLAUDE.md (Mandatory Phase Completion Steps §2/§3/§4 — UAT-SERIES update + vault sync + commit command)
  </read_first>
  <action>
    Append a "## Series 111: Console Dashboard Awareness (Phase 111 — v5.4)" section to docs/UAT-SERIES.md, mirroring the Series 110 block structure. Add concrete pass criteria for: UAT-111-01 sensor registry (GET /api/sensor/registry + Sensors page table with current/stale/unknown badges); UAT-111-02 segment filter (?segment= on /api/scan/latest + "All segments" dropdown on Findings/CBOM; NULL-segment single-host scan unaffected); UAT-111-03 per-segment gauges + coverage banner (GET /api/merge/latest per-segment scores; amber non-dismissible role=alert banner above gauges; graceful no-merge -> org-wide gauge only). Update the document header `**Last Updated:**` to 2026-05-25.
    Sync to Obsidian by writing UAT-SERIES.md to the vault filesystem (do NOT use obsidian CLI content= — file too large): prepend the project frontmatter block and copy to `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` per the CLAUDE.md §3 printf+cat+cp procedure.
    Commit UAT-SERIES.md with the gsd-tools commit command from CLAUDE.md §4: message `docs(phase-111): update UAT-SERIES.md`.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && grep -q "Series 111" docs/UAT-SERIES.md && test -f /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md && grep -q "Series 111" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Series 111" docs/UAT-SERIES.md` and the block has UAT-111-01/02/03 pass criteria covering DASH-01/02/03.
    - Document header `**Last Updated:**` is 2026-05-25.
    - `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` exists and contains "Series 111".
    - UAT-SERIES.md committed with the phase-111 message.
  </acceptance_criteria>
  <done>Series 111 added + synced to vault + committed.</done>
</task>

<task type="auto">
  <name>Task 2: Create the Phase 111 Obsidian phase note + run full suite</name>
  <files>(/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-111-Console-Dashboard-Awareness.md — vault filesystem)</files>
  <read_first>
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-99-Per-Finding-Context-Code-Signing-Expiry.md (phase-note template: frontmatter + Goal + Requirements Covered + Success Criteria + What Was Built per plan + [[Roadmap]] link)
    - .planning/phases/111-console-dashboard-awareness/111-01-SUMMARY.md and 111-02-SUMMARY.md (What Was Built source)
    - .planning/ROADMAP.md (Phase 111 Goal + Success Criteria + DASH requirements)
    - CLAUDE.md (Mandatory Phase Completion Steps §1 — write the note directly to the vault filesystem, NOT via obsidian CLI content=)
  </read_first>
  <action>
    Write `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-111-Console-Dashboard-Awareness.md` directly to the vault filesystem (CLAUDE.md §1 — do not use obsidian CLI content=). Frontmatter: project QU.I.R.K., type phase, status complete, source .planning/phases/111-console-dashboard-awareness/, updated 2026-05-25. Body: Goal (from ROADMAP), Requirements Covered (DASH-01/02/03), Success Criteria (the three ROADMAP criteria), What Was Built (one subsection per plan sourced from the SUMMARY files — backend endpoints/schema fields in Plan 01; Sensors page/filters/gauges/banner in Plan 02), and a `[[Roadmap]]` link.
    Then run the full validation suite (see verify) to confirm backend + build + frontend are green before the human-UAT checkpoint.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m pytest tests/ -k "registry or merge_latest or segment_filter or finding_segment or dashboard_api or route_coverage" -q && python -m compileall quirk run_scan.py && test -f /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-111-Console-Dashboard-Awareness.md && (cd src/dashboard && npm run build && npx vitest run)</automated>
  </verify>
  <acceptance_criteria>
    - The Phase-111 vault note exists with frontmatter status: complete and a `[[Roadmap]]` link.
    - Phase 111 backend tests + dashboard_api + route_coverage all pass.
    - `python -m compileall quirk run_scan.py` clean.
    - `npm run build` exits 0 and `npx vitest run` passes.
  </acceptance_criteria>
  <done>Phase 111 vault note written; full backend+frontend suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the dashboard awareness UI against 111-UI-SPEC.md</name>
  <action>Human visual verification only — no code changes. Confirm the built dashboard matches the UI-SPEC per the steps below.</action>
  <what-built>The complete Phase 111 dashboard awareness UI: a Sensors registry page with status badges, "All segments" filters on Findings and CBOM, per-segment score gauges on the Executive page, and the amber non-dismissible coverage_warning banner. Built statics are current (npm run build ran in Task 2).</what-built>
  <how-to-verify>
    1. Start the dashboard server and open it in a browser (e.g. `python run_scan.py` per the project's dashboard-serve path, then visit the dashboard URL).
    2. Click the new "Sensors" nav item (radio icon, between Scan History and Schedules). Confirm the registry table shows Sensor ID (monospace) / Segment / Version / Last Seen / Status, with status badges showing a TEXT label (Current/Stale/Unknown) plus color — not color alone. If no sensors are enrolled, confirm the empty-state enroll-command message.
    3. On Findings and CBOM pages, confirm an "All segments" dropdown appears in the filter bar; selecting a segment filters the rows; "All segments" restores all (including any single-host NULL-segment rows).
    4. On the Executive page, confirm per-segment gauges render alongside the org-wide gauge when merge data exists; if a merge ran with missing sensors, confirm the amber coverage banner appears ABOVE the gauges with factual copy and no dismiss button. With no merge, confirm only the org-wide gauge shows and no banner.
    5. Compare all of the above against 111-UI-SPEC.md (colors, badge styling, banner tone, layout).
  </how-to-verify>
  <verify><human-check>Human confirms the dashboard matches 111-UI-SPEC.md</human-check></verify>
  <done>Human approves the Sensors page, segment filters, per-segment gauges, and coverage banner against the UI-SPEC.</done>
  <resume-signal>Type "approved" or describe any visual/interaction discrepancies vs. the UI-SPEC</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/vault writes | Local filesystem writes only — no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-09 | Repudiation | coverage banner human verification | mitigate | Human-UAT checkpoint explicitly confirms the coverage banner fires and is non-dismissible — UX-integrity control verified by a person, not only presence tests |
| T-111-SC | Tampering | npm/pip installs | accept | No new packages — docs/vault/verify only |
</threat_model>

<verification>
- `grep -q "Series 111" docs/UAT-SERIES.md` and vault UAT-Series.md
- Phase-111 vault note exists
- Full backend + build + vitest suite green
- Human confirms the dashboard against 111-UI-SPEC.md
</verification>

<success_criteria>
- Series 111 in UAT-SERIES.md + synced to vault; Phase 111 vault note created
- Full suite green
- Human-verified visual fidelity against the UI-SPEC
</success_criteria>

<output>
Create `.planning/phases/111-console-dashboard-awareness/111-03-SUMMARY.md` when done
</output>
