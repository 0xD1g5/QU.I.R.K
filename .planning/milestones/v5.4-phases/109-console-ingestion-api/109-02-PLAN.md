---
phase: 109-console-ingestion-api
plan: 02
type: execute
wave: 2
depends_on: [109-01]
files_modified:
  - quirk/dashboard/api/routes/sensor.py
  - quirk/dashboard/api/app.py
  - quirk/cli/console_cmd.py
autonomous: true
requirements: [CONSOLE-01, CONSOLE-02, CONSOLE-03, CONSOLE-04, CONSOLE-05]
must_haves:
  truths:
    - "POST /api/sensor/push exists on the quirk serve FastAPI app and returns 200 on a valid authenticated push"
    - "An unauthenticated POST /api/sensor/push returns 401 (router-level require_auth, no bypass)"
    - "A body over 10 MB returns 413; a duplicate payload_id returns 409; a push outside ±15 min returns 422 with console_utc in the body"
    - "An unknown sensor_id returns 4xx and is audited"
    - "Every push attempt (success and every failure branch) writes one IntegrationDelivery row with destination='sensor_push' and safe_str-scrubbed error text"
    - "A valid push persists a sensor_pushes dedup row, updates sensors.last_push_at, and writes CryptoEndpoint rows tagged with sensor_id and segment"
    - "Push and air-gap import share one ingest path (_ingest_envelope); version skew is warn-only (extra='ignore')"
  artifacts:
    - path: "quirk/dashboard/api/routes/sensor.py"
      provides: "APIRouter(dependencies=[Depends(require_auth)]) with POST /sensor/push, PushEnvelope Pydantic model, failure-mode handling + audit"
      contains: "router = APIRouter(dependencies=[Depends(require_auth)])"
    - path: "quirk/dashboard/api/app.py"
      provides: "sensor.router mounted at /api"
      contains: "sensor.router"
    - path: "quirk/cli/console_cmd.py"
      provides: "_ingest_envelope real logic (dedup/last_push_at/CryptoEndpoint persist) with db=None param"
      contains: "def _ingest_envelope"
  key_links:
    - from: "quirk/dashboard/api/app.py"
      to: "quirk/dashboard/api/routes/sensor.py::router"
      via: "application.include_router(sensor.router, prefix='/api')"
      pattern: "include_router\\(sensor\\.router"
    - from: "quirk/dashboard/api/routes/sensor.py::sensor_push"
      to: "quirk/cli/console_cmd.py::_ingest_envelope"
      via: "shared ingest call with injected db session"
      pattern: "_ingest_envelope\\("
    - from: "quirk/dashboard/api/routes/sensor.py"
      to: "quirk.models.IntegrationDelivery"
      via: "audit row per attempt, error_summary=safe_str(exc)"
      pattern: "IntegrationDelivery\\("
---

<objective>
Add `POST /api/sensor/push` (new `quirk/dashboard/api/routes/sensor.py`) with router-level
`require_auth` (M2M, NO require_csrf), the `PushEnvelope` Pydantic model (`extra='ignore'`),
the locked failure-mode ladder (401 → 413 → parse/version-skew graceful → 422 replay →
409 dedup → 200), `IntegrationDelivery` audit rows on EVERY attempt, and replace the
Phase 108 `_ingest_envelope` stub with real dedup + `sensors.last_push_at` update +
`CryptoEndpoint` persistence — so HTTPS push and air-gap import share ONE ingest path.

Purpose: The core ingestion endpoint with the full locked §6 security contract.
Output: New sensor route + app.py mount + real _ingest_envelope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-console-ingestion-api/109-CONTEXT.md
@.planning/phases/109-console-ingestion-api/109-RESEARCH.md
@.planning/phases/109-console-ingestion-api/109-PATTERNS.md
@.planning/phases/109-console-ingestion-api/109-01-SUMMARY.md
@docs/architecture-distributed.md

<interfaces>
<!-- Extracted from codebase — executor needs no further exploration. -->

quirk/dashboard/api/middleware/auth.py::require_auth(request, credentials) -> None
  Router-level dep; passthrough when QUIRK_API_TOKEN empty (D-02); 401 on wrong/missing token.
quirk/dashboard/api/deps.py::get_db() -> Generator[Session]  (per-request session)
quirk/dashboard/api/deps.py::_default_db_path() -> str
quirk/util/safe_exc.py::safe_str(exc) -> str  (NEVER str(exc)/repr(exc))

quirk/models.py:
  IntegrationDelivery: scan_id (String(64) NOT NULL, index), finding_hash (nullable),
    destination (String(64) NOT NULL), status (String(16) NOT NULL "ok"|"failed"),
    attempted_at (DateTime NOT NULL), error_summary (Text nullable — safe_str only)
  Sensor: sensor_id (PK String(36)), segment, last_push_at (nullable), ...
  SensorPush: payload_id (String(64) NOT NULL, UNIQUE → 409 seam), sensor_id (FK), received_at (DateTime)
  CryptoEndpoint: host (NOT NULL), port (NOT NULL), protocol (nullable), scanned_at (nullable),
    sensor_id (String(255) nullable, NO FK — D-03), segment (String(255) nullable), + TLS/cert cols

quirk/cli/console_cmd.py — current _ingest_envelope signature (MUST keep first 4 params):
  def _ingest_envelope(envelope: dict, config_path: str,
                       skip_replay_window: bool = False, qpush_sig: str | None = None) -> None
  _cmd_import_results calls it with skip_replay_window=True (air-gap D-15).

quirk/dashboard/api/app.py — create_app() include_router block (lines 108-115), prefix="/api";
  routes imported at line 25: `from quirk.dashboard.api.routes import health, jobs, pdf, qramm, scan, schedules, trends`

quirk/cli/sensor_cmd.py::_build_envelope — pushed_at format "%Y-%m-%dT%H:%M:%SZ"; finding dict
  fields: host, port, protocol, scanned_at, tls_version, cipher_suite, cert_subject, cert_issuer,
  cert_sans, cert_sig_alg, cert_pubkey_alg, cert_pubkey_size, cert_not_before, cert_not_after,
  sensor_id, segment.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace _ingest_envelope stub with real dedup + persist logic (shared path)</name>
  <files>quirk/cli/console_cmd.py</files>
  <read_first>
    - quirk/cli/console_cmd.py (_ingest_envelope stub, lines 183-229; _cmd_import_results call site, line 178)
    - 109-RESEARCH.md (Pattern 7 db=None extension, lines 461-509; Pattern 6 IntegrityError dedup, 437-459; Pattern 8 CryptoEndpoint persist, 511-528)
    - 109-PATTERNS.md (DB session creation for CLI, lines 159-171)
  </read_first>
  <behavior>
    - Given db=None (CLI air-gap path): the function opens its own session via init_db(_default_db_path())
      + sessionmaker, runs the ingest, and closes the session in a finally.
    - Given an injected db Session (HTTPS path): uses that session, does not open or close its own.
    - On a fresh payload_id: inserts a SensorPush row (payload_id, sensor_id, received_at=now),
      updates the matching sensors.last_push_at = now, and inserts one CryptoEndpoint row per
      finding tagged with envelope-top-level sensor_id and segment.
    - On a duplicate payload_id: db.flush() raises IntegrityError → db.rollback() → raises a
      typed/sentinel error the route maps to 409 (e.g. raise a module-level DuplicatePayloadError);
      the CLI air-gap path surfaces it as a fixed-string operator message.
    - skip_replay_window is honored as-is (replay-window enforcement lives in the route, not here).
    - DateTime strings (scanned_at, cert_not_before/after) parsed via
      datetime.fromisoformat(v.replace("Z","+00:00")).replace(tzinfo=None); malformed → None, no crash.
  </behavior>
  <action>
    Extend `_ingest_envelope` signature to add a trailing `db=None` param (keep the existing four
    params and their order — air-gap caller at line 178 is unchanged). Add `_own_session = db is None`;
    when own, build engine via `init_db(_default_db_path())` + sessionmaker and open a session,
    closing it in a `finally`. Implement: insert `SensorPush(payload_id, sensor_id, received_at=now)`
    then `db.flush()` inside try/except `IntegrityError`; on IntegrityError `db.rollback()` and
    `raise DuplicatePayloadError` (define a small module-level exception class). Query the Sensor by
    `envelope["sensor_id"]`; if found, set `sensor.last_push_at = now`. For each finding dict in
    `envelope.get("findings", [])`, construct a `CryptoEndpoint` using host/port/protocol/scanned_at
    + TLS/cert fields present, overriding sensor_id and segment with the envelope top-level values
    (authoritative). `db.commit()` when own-session; when injected, leave commit to the route's audit
    flow OR flush + let the route commit — choose flush-only when injected so the route owns the final
    commit/rollback boundary. Use `now = datetime.now(timezone.utc).replace(tzinfo=None)`. NEVER use
    str(exc)/repr(exc) anywhere (use safe_str if logging). Remove the Phase 108 print-summary stub body.
  </action>
  <verify>
    <automated>pytest tests/ -k "ingest or sensor_push" -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m compileall quirk/cli/console_cmd.py` succeeds.
    - With db=None the function self-manages its session (grep: `_own_session` and a `finally:` close).
    - A fresh payload writes one SensorPush row, sets sensors.last_push_at, and writes len(findings)
      CryptoEndpoint rows with the envelope's sensor_id/segment on each.
    - A duplicate payload_id raises DuplicatePayloadError after db.rollback() (no second SensorPush row).
    - The air-gap call site `_ingest_envelope(envelope, config_path, skip_replay_window=True, qpush_sig=qpush_sig)`
      still type-checks and runs (param order preserved).
    - No `str(exc)`/`repr(exc)` in the function body (AST gate Plan 03).
  </acceptance_criteria>
  <done>One shared ingest path performs dedup + last_push_at + CryptoEndpoint persist for push and air-gap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create sensor.py route — PushEnvelope model, failure-mode ladder, audit-per-attempt</name>
  <files>quirk/dashboard/api/routes/sensor.py</files>
  <read_first>
    - quirk/dashboard/api/routes/scan.py (router-level require_auth, no CSRF — line 81)
    - quirk/dashboard/api/routes/schedules.py (get_db, IntegrityError→fixed 409, _utcnow_naive — lines 69-71, 124, 151-162)
    - quirk/ticketing/base.py (IntegrationDelivery write + safe_str — lines 104-151)
    - 109-RESEARCH.md (Patterns 1-6 + Pitfalls 1-7; route skeleton lines 622-659)
    - 109-PATTERNS.md (sensor.py assignments, lines 25-104; audit BEFORE raising pre-ingest HTTPExceptions, line 242)
  </read_first>
  <behavior>
    - Unauthenticated POST /api/sensor/push → 401 (router-level require_auth; verified by existing
      test_all_mutating_routes_have_auth_dependency once mounted).
    - Content-Length > 10MB OR actual body length > 10MB → 413; audit row (status=failed) written first.
    - Body decompressed (zstd, reuse _MAX_DECOMPRESS_BYTES guard from console_cmd) then parsed into
      PushEnvelope(extra='ignore'); schema_version/sensor_version mismatch → warn-only log, never 422/500.
    - pushed_at outside ±15 min of received_at → 422 with body {"error":"replay_window_exceeded",
      "console_utc": received_at as "%Y-%m-%dT%H:%M:%SZ"}; audit row first.
    - Unknown sensor_id (no Sensor row) → 4xx (e.g. 404/422), audited with fixed error_summary.
    - DuplicatePayloadError from _ingest_envelope → 409 "Duplicate payload_id" (fixed string); audited.
    - Valid push → _ingest_envelope(..., db=db) persists, audit row status="ok", returns 200.
    - EVERY branch (413/parse-fail/422/unknown-sensor/409/200) writes exactly one IntegrationDelivery
      row; error_summary always safe_str(exc) or a fixed string, never raw exception.
  </behavior>
  <action>
    Create `quirk/dashboard/api/routes/sensor.py`. Imports per <interfaces> + PushEnvelope from
    pydantic (BaseModel, ConfigDict(extra="ignore"); fields payload_id, pushed_at, schema_version,
    sensor_version, sensor_id, segment, findings: list = []). Define
    `router = APIRouter(dependencies=[Depends(require_auth)])` (NO require_csrf — Pitfall 1).
    Module constants `_BODY_LIMIT = 10*1024*1024`, `_REPLAY_WINDOW = timedelta(minutes=15)`.
    Add a private `_audit(db, scan_id, status, error_summary)` helper that builds an
    IntegrationDelivery(destination="sensor_push", finding_hash=None, attempted_at=now,
    scan_id=scan_id, status=status, error_summary=error_summary), db.add + commit wrapped so an
    audit-write failure is logged via safe_str and does not mask the original error (commit OUTSIDE
    the ingest try — base.py L149 pattern). Implement `async def sensor_push(request, db=Depends(get_db))`:
    set received_at; check Content-Length then `await request.body()` length → 413 (audit-then-raise);
    decompress with the zstd cap; on decompress/JSON/Pydantic parse failure → audit + raise (graceful,
    NOT 500); validate replay window (skip never here — HTTPS always enforces; pushed_at parsed with
    Z→+00:00) → 422 + console_utc (audit-then-raise); look up Sensor by envelope.sensor_id → 4xx if
    None (audit-then-raise with fixed "unknown_sensor_id"); call
    `_ingest_envelope(envelope_dict, config_path="config.yaml", skip_replay_window=False, qpush_sig=<X-Sensor-Signature or None>, db=db)`;
    catch DuplicatePayloadError → audit + 409 fixed string. Record + structurally validate the
    X-Sensor-Signature header (present + matches `hmac-sha256=<hex>`); carry the value into the
    qpush_sig param — do NOT cryptographically verify (deferred v5.5). On success: db.commit(),
    audit status="ok", return {"status":"accepted", ...} 200. scan_id = envelope.pushed_at or
    received_at fallback. Use safe_str(exc) for every error_summary derived from an exception.
  </action>
  <verify>
    <automated>pytest tests/ -k "sensor_push" -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m compileall quirk/dashboard/api/routes/sensor.py` succeeds.
    - `router = APIRouter(dependencies=[Depends(require_auth)])` present; require_csrf absent (grep).
    - PushEnvelope has `model_config = ConfigDict(extra="ignore")`.
    - Each failure branch writes an IntegrationDelivery row before raising HTTPException
      (grep: `_audit(` appears in 413, 422, unknown-sensor, 409, and success paths).
    - X-Sensor-Signature is read and forwarded as qpush_sig; no hmac verify call present.
    - No `str(exc)`/`repr(exc)` in the file (AST gate Plan 03).
  </acceptance_criteria>
  <done>POST /api/sensor/push enforces the full locked failure-mode ladder with audit-per-attempt.</done>
</task>

<task type="auto">
  <name>Task 3: Mount sensor.router in app.py</name>
  <files>quirk/dashboard/api/app.py</files>
  <read_first>
    - quirk/dashboard/api/app.py (route import line 25; include_router block lines 108-115)
    - 109-PATTERNS.md (app.py mount, lines 108-121)
  </read_first>
  <action>
    Add `sensor` to the routes import at line 25 (`from quirk.dashboard.api.routes import
    health, jobs, pdf, qramm, scan, schedules, sensor, trends`). Inside `create_app`, add
    `application.include_router(sensor.router, prefix="/api")` alongside the other include_router
    calls (lines 108-115), before any static-file mounts. This yields the path POST /api/sensor/push (CONSOLE-01).
  </action>
  <verify>
    <automated>python -c "from quirk.dashboard.api.app import create_app; app=create_app(); assert any(getattr(r,'path','')=='/api/sensor/push' for r in app.routes), 'route missing'"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m compileall quirk/dashboard/api/app.py` succeeds.
    - The created app exposes a route with path `/api/sensor/push`.
    - The existing `test_all_mutating_routes_have_auth_dependency` gate still passes (router-level auth present).
  </acceptance_criteria>
  <done>POST /api/sensor/push is live on the quirk serve FastAPI app.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sensor → POST /api/sensor/push | Untrusted compressed payload + bearer token + X-Sensor-Signature crosses here |
| route handler → console DB | Validated envelope persisted as SensorPush/CryptoEndpoint; audit rows on every path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-04 | Elevation of Privilege | sensor router auth | mitigate | Router-level APIRouter(dependencies=[Depends(require_auth)]); 401 gating test (Plan 03); test_all_mutating_routes_have_auth_dependency gate |
| T-109-05 | Spoofing | replay / stale push | mitigate | ±15-min window on pushed_at vs received_at → 422+console_utc; payload_id UNIQUE → 409 dedup |
| T-109-06 | Denial of Service | oversized / decompression-bomb body | mitigate | Content-Length guard + body-length check → 413; zstd cap _MAX_DECOMPRESS_BYTES before parse |
| T-109-07 | Information Disclosure | exception text in response/audit | mitigate | safe_str(exc) on all error paths; fixed strings for 409/unknown-sensor; AST gate (Plan 03) |
| T-109-08 | Repudiation | missing audit on failure branches | mitigate | IntegrationDelivery row written on EVERY attempt incl. pre-ingest failures, commit outside ingest try |
| T-109-09 | Spoofing | forged unknown-sensor payload | mitigate | Sensor lookup by sensor_id → 4xx before any persistence; audited |
| T-109-10 | Tampering | concurrent same payload_id race | mitigate | UNIQUE constraint + IntegrityError → rollback-first → 409 fixed string (never stringify exc) |
| T-109-11 | Spoofing | HMAC signature not verified (v5.4) | accept | hmac_key column absent (Phase 107); record + structurally validate X-Sensor-Signature, carry to qpush_sig; full verify deferred v5.5 |
| T-109-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages; N/A per 109-RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- `python -m compileall quirk run_scan.py` clean.
- `pytest tests/test_api_auth.py -k "auth_dependency" -q` green (new route inherits router-level auth).
- `pytest tests/ -k "sensor_push or ingest" -q` green (full coverage authored in Plan 03).
</verification>

<success_criteria>
- POST /api/sensor/push live with 401/413/422/409/4xx/200 ladder and audit-per-attempt.
- Push and air-gap import share one _ingest_envelope path; version skew warn-only.
- CONSOLE-01..05 implemented.
</success_criteria>

<output>
Create `.planning/phases/109-console-ingestion-api/109-02-SUMMARY.md` when done.
</output>
