---
phase: 108-sensor-push-cli-windows-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/util/no_redirect.py
  - quirk/notify/channels/webhook.py
  - quirk/ticketing/servicenow.py
  - quirk/cli/scheduler_cmd.py
  - pyproject.toml
  - tests/test_no_redirect_extraction.py
  - tests/test_scheduler_posix_fixes.py
autonomous: true
requirements: [STAB-02, SENSOR-05]
must_haves:
  truths:
    - "_NoRedirectHandler is importable from quirk.util.no_redirect"
    - "webhook.py and servicenow.py no longer define _NoRedirectHandler (they import it)"
    - "platformdirs, tenacity, zstandard are declared in pyproject.toml core dependencies"
    - "scheduler_cmd output dir is anchored to cfg.output.directory (not a CWD-relative path)"
    - "scheduler_cmd SIGTERM registration is guarded by sys.platform != 'win32'"
  artifacts:
    - path: "quirk/util/no_redirect.py"
      provides: "Single-source _NoRedirectHandler SSRF redirect guard"
      contains: "class _NoRedirectHandler"
    - path: "tests/test_no_redirect_extraction.py"
      provides: "STAB-02 import + no-duplicate-definition guard"
    - path: "tests/test_scheduler_posix_fixes.py"
      provides: "SENSOR-05 POSIX-ism regression tests"
  key_links:
    - from: "quirk/notify/channels/webhook.py"
      to: "quirk/util/no_redirect.py"
      via: "from quirk.util.no_redirect import _NoRedirectHandler"
      pattern: "from quirk.util.no_redirect import _NoRedirectHandler"
    - from: "quirk/ticketing/servicenow.py"
      to: "quirk/util/no_redirect.py"
      via: "from quirk.util.no_redirect import _NoRedirectHandler"
      pattern: "from quirk.util.no_redirect import _NoRedirectHandler"
---

<objective>
Land the cross-OS prerequisites for the sensor runtime: extract the duplicated _NoRedirectHandler SSRF guard to a single module (STAB-02), declare the three new core dependencies (platformdirs, tenacity, zstandard), and fix the two confirmed POSIX-isms in scheduler_cmd.py (SENSOR-05).

Purpose: These are foundational changes that Plans 02/03 (sensor runtime) depend on. STAB-02 is a locked sensor-phase prerequisite (v5.4-D-05). The dependency declarations must land before any sensor code that imports them can run. The POSIX-ism fixes let the scheduler run identically on Windows and POSIX.

Output: quirk/util/no_redirect.py, updated importers, three new pyproject deps, two scheduler fixes, two regression test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-sensor-push-cli-windows-ci/108-CONTEXT.md
@.planning/phases/108-sensor-push-cli-windows-ci/108-PATTERNS.md

<interfaces>
_NoRedirectHandler is IDENTICAL in both source files (verified). Pure extract.

From quirk/notify/channels/webhook.py:29-41 (and servicenow.py:34-47, identical):
- class _NoRedirectHandler(urllib.request.HTTPRedirectHandler) with redirect_request() raising urllib.error.HTTPError("Redirect blocked (SSRF guard)")
- webhook.py uses it at L89: urllib.request.build_opener(_NoRedirectHandler)
- servicenow.py uses it at L115, L161, L210: same build_opener call

From quirk/cli/scheduler_cmd.py (verified line locations):
- _dispatch_schedule(schedule, db, config_path) — config_path IS in scope; load_config(config_path) yields cfg
- output_dir uses Path("output/scheduled") / safe_name / now.strftime(...) — the CWD-relative bug
- run_scheduler(): signal.signal(signal.SIGINT, _handle_signal) then signal.signal(signal.SIGTERM, _handle_signal) — the unconditional SIGTERM
- top imports: argparse, os, re, signal, subprocess, sys (L4-9). load_config is NOT currently imported.

From quirk/config.py:292-293: class OutputCfg has directory: str; load_config(path) at L491 returns AppConfig with cfg.output.directory.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract _NoRedirectHandler to quirk/util/no_redirect.py (STAB-02)</name>
  <files>quirk/util/no_redirect.py, quirk/notify/channels/webhook.py, quirk/ticketing/servicenow.py, tests/test_no_redirect_extraction.py</files>
  <read_first>
    - quirk/notify/channels/webhook.py (lines 1-95: class definition at 29-41, usage at 89)
    - quirk/ticketing/servicenow.py (lines 1-50 and usage at 115/161/210)
    - .planning/phases/108-sensor-push-cli-windows-ci/108-PATTERNS.md (quirk/util/no_redirect.py section)
    - tests/scanner/test_phase57_invariants.py (static-analysis test shape)
  </read_first>
  <action>
    Create quirk/util/no_redirect.py containing the _NoRedirectHandler(urllib.request.HTTPRedirectHandler) class verbatim from webhook.py:29-41 (preserve the docstring describing the post-validation SSRF redirect-bypass threat model). Module docstring notes the STAB-02 extraction and that all callers import from here.
    In quirk/notify/channels/webhook.py: remove the local class _NoRedirectHandler definition (lines 29-41) and add the import from quirk.util.no_redirect. Leave the L89 build_opener usage unchanged.
    In quirk/ticketing/servicenow.py: remove the local class _NoRedirectHandler definition (lines 34-47) and add the import from quirk.util.no_redirect. Leave all three build_opener usages (L115/L161/L210) unchanged.
    Create tests/test_no_redirect_extraction.py: test_no_redirect_importable (imports from quirk.util.no_redirect, asserts issubclass of urllib.request.HTTPRedirectHandler) and test_no_duplicate_definitions (reads webhook.py and servicenow.py source, asserts the class definition string is absent in each). No logic changes to the handler.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m pytest tests/test_no_redirect_extraction.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - from quirk.util.no_redirect import _NoRedirectHandler succeeds and the class subclasses urllib.request.HTTPRedirectHandler
    - grep -c "class _NoRedirectHandler" quirk/notify/channels/webhook.py returns 0
    - grep -c "class _NoRedirectHandler" quirk/ticketing/servicenow.py returns 0
    - both former definers contain the line: from quirk.util.no_redirect import _NoRedirectHandler
    - tests/test_no_redirect_extraction.py passes
  </acceptance_criteria>
  <done>_NoRedirectHandler lives in one module; both former definers import it; redirect-blocking behavior unchanged; guarded by a regression test.</done>
</task>

<task type="auto">
  <name>Task 2: Declare platformdirs, tenacity, zstandard in pyproject.toml core deps</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (lines 5-34: [project] dependencies list and structure)
    - .planning/phases/108-sensor-push-cli-windows-ci/108-PATTERNS.md (pyproject.toml new dependencies section)
  </read_first>
  <action>
    Append three entries to the [project] dependencies list (NOT to [project.optional-dependencies] — these are required for the core sensor runtime): platformdirs>=4.3.0, tenacity>=8.2.0, zstandard>=0.22.0. Use the existing package>=floor.version formatting and add a trailing inline comment on each citing the requirement (SENSOR-05 for platformdirs; SENSOR-02 for tenacity; SENSOR-02/03/04 for zstandard). All three are NET-NEW (verified absent) and none are on the v5.4-D-04 forbidden list. zstandard is net-new despite the arch-doc claim it was already in the codebase (RESEARCH Pitfall 1).
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && pip install -e . -q && python -c "import platformdirs, tenacity, zstandard; print('imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "platformdirs" pyproject.toml returns at least 1 and the entry is inside the [project] dependencies array
    - grep -c "tenacity" pyproject.toml returns at least 1
    - grep -c "zstandard" pyproject.toml returns at least 1
    - none of the three appear under [project.optional-dependencies]
    - python -c "import platformdirs, tenacity, zstandard" succeeds after pip install -e .
  </acceptance_criteria>
  <done>All three deps are declared as core dependencies and import cleanly on a fresh editable install.</done>
</task>

<task type="auto">
  <name>Task 3: Fix scheduler_cmd.py POSIX-isms — output path anchor + SIGTERM guard (SENSOR-05)</name>
  <files>quirk/cli/scheduler_cmd.py, tests/test_scheduler_posix_fixes.py</files>
  <read_first>
    - quirk/cli/scheduler_cmd.py (lines 1-14 imports; 110-165 _dispatch_schedule; 248-262 run_scheduler signal block)
    - quirk/config.py (lines 292-293 OutputCfg.directory; 491 load_config)
    - .planning/phases/108-sensor-push-cli-windows-ci/108-PATTERNS.md (scheduler_cmd.py POSIX-ism fixes section)
  </read_first>
  <action>
    Fix 1 (path anchor): In _dispatch_schedule, replace the CWD-relative output_dir assignment Path("output/scheduled") / safe_name / now.strftime(...) with an anchor to cfg.output.directory: load cfg via load_config(config_path) (config_path is the function's third parameter), then build Path(cfg.output.directory) / "scheduled" / safe_name / now.strftime("%Y%m%d-%H%M%S"). Add from quirk.config import load_config to the module imports only if not already present (it is NOT currently imported). Do not change the mkdir(parents=True, exist_ok=True) call or the subprocess invocation. Preserve the WR-02 safe_name sanitization above it.
    Fix 2 (SIGTERM guard): In run_scheduler, wrap the signal.signal(signal.SIGTERM, _handle_signal) line in if sys.platform != "win32":. Leave the SIGINT registration unconditional. sys is already imported at L9 — no new import.
    Create tests/test_scheduler_posix_fixes.py with two tests sourced statically (these are guards, not live scheduler runs): test_output_dir_anchored asserts the scheduler_cmd.py source no longer contains the literal Path("output/scheduled") and DOES contain cfg.output.directory; test_sigterm_guard asserts the source contains the if sys.platform != "win32": guard immediately governing the SIGTERM registration (assert both 'sys.platform != "win32"' and signal.SIGTERM appear, and that the substring between the platform check and SIGTERM registration is short — i.e. they are adjacent). Both tests use a tokenize-comment-strip read consistent with tests/scanner/test_phase57_invariants.py so comments cannot satisfy the assertion.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m pytest tests/test_scheduler_posix_fixes.py -x -q && python -m compileall quirk/cli/scheduler_cmd.py</automated>
  </verify>
  <acceptance_criteria>
    - scheduler_cmd.py source (comments stripped) does NOT contain Path("output/scheduled")
    - scheduler_cmd.py source contains cfg.output.directory
    - scheduler_cmd.py source contains both sys.platform != "win32" and signal.SIGTERM with the platform guard governing the SIGTERM registration
    - from quirk.config import load_config appears exactly once
    - existing scheduler tests still pass: python -m pytest tests/ -k scheduler -q
    - tests/test_scheduler_posix_fixes.py passes
  </acceptance_criteria>
  <done>Scheduler output dir is config-anchored and absolute; SIGTERM registration is Windows-guarded; both fixes locked by comment-stripped static regression tests; existing scheduler behavior unchanged on POSIX.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| outbound HTTP from notify/ticketing → external endpoint | _NoRedirectHandler blocks post-validation 3xx redirect to internal targets (SSRF) |
| scheduler process → OS signal layer | SIGTERM handling differs on Windows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-01 | Spoofing | _NoRedirectHandler extraction (webhook/servicenow) | mitigate | Extraction is byte-identical copy; redirect_request still raises HTTPError on any 3xx; test_no_redirect_importable asserts subclass + behavior preserved |
| T-108-02 | Tampering | scheduler output path | mitigate | Anchor to cfg.output.directory (absolute); WR-02 safe_name sanitization preserved so schedule.name cannot traverse outside the output tree |
| T-108-SC | Tampering | pip installs (platformdirs, tenacity, zstandard) | accept | RESEARCH Package Legitimacy Audit: all three [OK] on PyPI (5–9 yrs old), none [SLOP]/[SUS], none on v5.4-D-04 forbidden list — no blocking human checkpoint required |
</threat_model>

<verification>
- python -m pytest tests/test_no_redirect_extraction.py tests/test_scheduler_posix_fixes.py -q is green
- python -m pytest tests/ -k "scheduler or webhook or servicenow or no_redirect" -q does not regress
- pip install -e . on a clean venv imports platformdirs, tenacity, zstandard
- python -m compileall quirk run_scan.py exits 0
</verification>

<success_criteria>
- STAB-02: _NoRedirectHandler in one module; both former definers import it; no duplicate class definitions
- SENSOR-05 (partial): scheduler output path anchored to cfg.output.directory; SIGTERM platform-guarded
- Three new core deps declared and importable; none under optional-dependencies; none forbidden
</success_criteria>

<output>
Create .planning/phases/108-sensor-push-cli-windows-ci/108-01-SUMMARY.md when done
</output>
