---
phase: 89-chaos-lab-profiles
plan: 03
type: execute
wave: 2
depends_on: ["89-01"]
files_modified:
  - labs/grpc-tls/Dockerfile
  - labs/grpc-tls/main.go
  - labs/grpc-tls/go.mod
  - labs/grpc-tls/go.sum
  - labs/grpc-tls/Makefile
  - labs/grpc-tls/README.md
  - quantum-chaos-enterprise-lab/docker-compose.yml
  - quantum-chaos-enterprise-lab/README.md
  - quantum-chaos-enterprise-lab/expected_results_v4.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [LAB-03, LAB-05]
must_haves:
  truths:
    - "An empirical sslyze-vs-ALPN-h2 confirmation runs BEFORE the grpc-tls probe approach is finalized — a blocking gate, not a deferred check (D-03)"
    - "docker compose --profile grpc-tls up starts a minimal Go gRPC TLS server advertising ALPN h2 on 39443 (D-03)"
    - "LAB-03 is closed as already-covered: the existing email profile's STARTTLS on 30587 is documented in expected_results_v4.md + UAT, no standalone smtp-starttls service added (D-01)"
  artifacts:
    - path: "labs/grpc-tls/main.go"
      provides: "Minimal grpc.NewServer with TLS credentials (auto ALPN h2)"
      contains: "credentials.NewServerTLSFromFile"
    - path: "labs/grpc-tls/Dockerfile"
      provides: "Multi-stage Go build, FROM golang:1.23-alpine pinned (CHAOS-05)"
      contains: "golang:1.23-alpine"
    - path: "quantum-chaos-enterprise-lab/expected_results_v4.md"
      provides: "## Profile: grpc-tls section + LAB-03 STARTTLS note under ## Profile: email"
      contains: "## Profile: grpc-tls"
  key_links:
    - from: "labs/grpc-tls/Dockerfile"
      to: "labs/grpc-tls/main.go"
      via: "multi-stage build copies grpc-server binary"
      pattern: "go build"
    - from: "quantum-chaos-enterprise-lab/docker-compose.yml"
      to: "labs/grpc-tls/Dockerfile"
      via: "build context for grpc-tls service"
      pattern: "labs/grpc-tls"
---

<objective>
Two deliverables that both touch the shared lab-sync files (so sequenced after Plan 01):

1. LAB-05 grpc-tls profile — custom minimal Go gRPC-TLS image with ALPN h2 (D-03). The FIRST
   task empirically confirms sslyze negotiates against the h2-only endpoint BEFORE the probe
   approach is finalized; if it cannot, that surfaces as an in-flight blocker (no silent fallback).
2. LAB-03 already-covered closure (D-01) — document that the existing `email` profile's STARTTLS
   on port 30587 satisfies smtp-starttls; add an expected_results note + UAT step + mark closed in
   REQUIREMENTS.md. NO standalone smtp-starttls service.

Purpose: Close LAB-05 (with the ALPN risk resolved empirically) and LAB-03 (as documented coverage).
Output: labs/grpc-tls/ Go image + profile, email STARTTLS oracle note, REQUIREMENTS.md closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-chaos-lab-profiles/89-CONTEXT.md
@.planning/phases/89-chaos-lab-profiles/89-RESEARCH.md
@.planning/phases/89-chaos-lab-profiles/89-01-SUMMARY.md

<interfaces>
<!-- grpc-tls (RESEARCH L349-408): port 39443:443; image built from labs/grpc-tls/Dockerfile
     (FROM golang:1.23-alpine pinned per CHAOS-05; the build-FROM satisfies the pin gate for
     build-only services). main.go: grpc.NewServer(grpc.Creds(credentials.NewServerTLSFromFile))
     — grpc-go auto-sets NextProtos:["h2"]. RSA-2048 self-signed cert (quantum-vulnerable MEDIUM). -->
<!-- D-03 ALPN risk (RESEARCH L392-403, Pitfall 3, Open Q1): sslyze has no ALPN ScanCommand.
     Empirical question = does sslyze's TLS negotiation succeed + emit cipher/cert findings against
     an h2-only grpc-go server. Fallback IF sslyze fails: openssl s_client -alpn h2 -connect
     localhost:39443 as evidence source, documented as a limitation in expected_results_v4.md. -->
<!-- LAB-03 (D-01, RESEARCH L467-489): docker-compose.yml postfix-email has 30587:587 (L1005).
     email_scanner.py uses sslyze ProtocolWithOpportunisticTlsEnum.SMTP for 587. expected_results_v4.md
     L438 already shows the 30587 SMTP-STARTTLS HIGH finding. Deliverable = explicit LAB-03 coverage
     note under "## Profile: email" (L426) + UAT step + REQUIREMENTS.md closure. NO new service. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (D-03 BLOCKING GATE): build grpc-tls image + empirically confirm sslyze ALPN-h2</name>
  <files>labs/grpc-tls/Dockerfile, labs/grpc-tls/main.go, labs/grpc-tls/go.mod, labs/grpc-tls/go.sum, labs/grpc-tls/Makefile, labs/grpc-tls/README.md, quantum-chaos-enterprise-lab/docker-compose.yml</files>
  <read_first>
    - .planning/phases/89-chaos-lab-profiles/89-RESEARCH.md L349-408 (Dockerfile skeleton, main.go skeleton, ALPN risk)
    - labs/broker/Makefile (RSA-2048 cert-gen pattern; CN=grpc-tls.chaos.local)
    - quantum-chaos-enterprise-lab/docker-compose.yml (an existing `build:`-context service block shape, if any, for the grpc-tls build service)
  </read_first>
  <action>Create labs/grpc-tls/ with a multi-stage Dockerfile (FROM golang:1.23-alpine builder → minimal alpine runtime; build-FROM tag satisfies CHAOS-05 for build-only services), main.go implementing a minimal grpc.NewServer(grpc.Creds(credentials.NewServerTLSFromFile("/tls/server.crt","/tls/server.key"))) listening on :443 (grpc-go auto-negotiates ALPN h2), go.mod/go.sum pinning google.golang.org/grpc, and a Makefile with `certs` (RSA-2048, CN=grpc-tls.chaos.local) + `build` targets. Add a grpc-tls service to docker-compose.yml: build context ../labs/grpc-tls, profiles:["grpc-tls"], ports "39443:443", TLS healthcheck. THEN — before finalizing the probe approach — bring the service up and EMPIRICALLY run sslyze against localhost:39443 (CERTIFICATE_INFO + TLS_1_2/1_3 cipher suites). Record in the SUMMARY whether sslyze negotiates and emits cipher/cert findings. If sslyze FAILS (ServerScanStatus ERROR / connection rejected per Pitfall 3): do NOT silently fall back — record the failure as a surfaced deviation, capture `openssl s_client -alpn h2 -connect localhost:39443` output as the alternative evidence source, and note the limitation. This empirical result drives Task 2's expected_results wording (D-03).</action>
  <verify>
    <automated>cd quantum-chaos-enterprise-lab && docker compose config -q && python -m pytest ../tests/test_chaos_lab_image_pinning.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config -q` validates the grpc-tls build service.
    - CHAOS-05 gate passes (Dockerfile FROM golang:1.23-alpine pinned; build-only service path).
    - `./lab.sh profiles` lists `grpc-tls`.
    - LIVE (Docker-gated, BLOCKING per D-03): grpc-tls builds + starts; sslyze run against 39443 recorded as SUCCESS (cipher/cert findings emitted) or FAILURE (with openssl s_client evidence + documented limitation). The empirical result is captured in the SUMMARY before Task 2 writes the oracle.
  </acceptance_criteria>
  <done>grpc-tls image builds + runs ALPN h2 on 39443; sslyze ALPN-h2 behavior empirically confirmed and recorded (no silent fallback).</done>
</task>

<task type="auto">
  <name>Task 2: grpc-tls lab-sync + LAB-03 email STARTTLS closure</name>
  <files>quantum-chaos-enterprise-lab/README.md, quantum-chaos-enterprise-lab/expected_results_v4.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - quantum-chaos-enterprise-lab/expected_results_v4.md "## Profile: email" (L426) — existing 30587 SMTP-STARTTLS finding (L438)
    - quantum-chaos-enterprise-lab/README.md Profile Summary table (L25-43)
    - .planning/REQUIREMENTS.md L28-33 + L83-88 (LAB-0X rows to flip)
  </read_first>
  <action>Finish the grpc-tls four-file lab-sync: add a README Profile Summary row (grpc-tls | grpc-tls | 39443 | link | v5.0 Phase 89) and a "## Profile: grpc-tls" section to expected_results_v4.md listing port 39443, the grpc-tls service, ALPN h2, and the expected findings — worded to MATCH the Task 1 empirical result (sslyze RSA-2048 MEDIUM + cipher info if sslyze succeeded, OR the openssl-s_client-evidenced finding + documented sslyze limitation if it failed). For LAB-03 (D-01): add an explicit coverage note under "## Profile: email" stating that port 30587 satisfies the smtp-starttls requirement (LAB-03) — scanner emits protocol=SMTP-STARTTLS, service_detail=SMTP-STARTTLS:587 (HIGH, EMAIL-09); no standalone smtp-starttls service exists by decision D-01. In REQUIREMENTS.md, mark LAB-03 closed with the documented "covered by email profile (D-01)" note and update the LAB-03/LAB-05 status rows. Add UAT coverage in docs/UAT-SERIES.md (per CLAUDE.md mandatory step) for both the grpc-tls profile and the email-30587 STARTTLS assertion.</action>
  <verify>
    <automated>QUIRK_DB_PATH=./quirk.db python -m pytest tests/test_phase89_lab_expected_results.py -q</automated>
  </verify>
  <acceptance_criteria>
    - expected_results_v4.md has "## Profile: grpc-tls" + an explicit LAB-03 STARTTLS coverage note under "## Profile: email".
    - README Profile Summary lists grpc-tls.
    - REQUIREMENTS.md LAB-03 marked closed (covered-by-email, D-01) and LAB-05 status updated.
    - LIVE (Docker-gated): `PROFILE_ARGS="--profile email" ./lab.sh up` + scan emits the 30587 SMTP-STARTTLS HIGH finding (LAB-03 proof).
    - No standalone smtp-starttls service added (D-01).
  </acceptance_criteria>
  <done>grpc-tls lab-sync complete (compose+lab.sh+README+expected_results); LAB-03 documented as email-covered + REQUIREMENTS closure; UAT updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanner host → grpc-tls TLS port 39443 | sslyze/openssl probes the h2-only TLS endpoint |
| Go module registry → grpc-tls build | go.mod pulls google.golang.org/grpc at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-06 | Information Disclosure | grpc-tls weak (RSA-2048) cert exposed | accept | Lab-only fixture by design (D-02/D-03); self-signed CN=grpc-tls.chaos.local, gitignored key, host 39443 on a local Docker host. |
| T-89-07 | Tampering | grpc-tls build base image + Go modules | mitigate | FROM golang:1.23-alpine pinned (official library image, RESEARCH Audit Approved); CHAOS-05 gate enforces the FROM tag; go.sum pins module hashes. |
| T-89-08 | Information Disclosure | expected_results_v4.md oracle wording | mitigate | Findings + ALPN behavior only; no secrets. LAB-03 note references the existing email finding, no new credentials. |
</threat_model>

<verification>
- `cd quantum-chaos-enterprise-lab && docker compose config -q` validates.
- `python -m pytest tests/test_chaos_lab_image_pinning.py tests/test_phase89_lab_expected_results.py -q` green.
- `./lab.sh profiles` lists grpc-tls.
- Live (Docker-gated): grpc-tls ALPN-h2 sslyze result recorded; email-30587 STARTTLS finding emitted.
- REQUIREMENTS.md LAB-03 + LAB-05 closure rows updated.
- No new failures vs the 39-failure baseline.
</verification>

<success_criteria>
- LAB-05: grpc-tls profile up, sslyze ALPN-h2 empirically resolved (D-03), four-file lab-sync complete.
- LAB-03: closed as email-covered (D-01) with oracle note + UAT + REQUIREMENTS closure; no standalone service.
</success_criteria>

<output>
Create `.planning/phases/89-chaos-lab-profiles/89-03-SUMMARY.md` when done.
</output>
