---
phase: 91-code-cleanup-bookkeeping
plan: 02
type: execute
wave: 2
depends_on: ["91-01"]
files_modified:
  - quirk/reports/writer.py
  - tests/test_cert_pubkey_fix.py
  - quirk/intelligence/schema.py
  - quirk/intelligence/__init__.py
  - tests/test_intelligence_schema.py
  - docs/dead-code-candidates.md
autonomous: false
requirements: [CLEAN-02]
must_haves:
  truths:
    - "_extract_cert_key_type and its unused RichText import are removed from writer.py with no production caller broken"
    - "The five intelligence schema dataclasses are removed (or the Phase 77 D-15 conflict is resolved) without breaking import quirk.intelligence"
    - "Removed symbols' test files are deleted; the suite collects without referencing them"
    - "A clean-venv smoke test (pip install -e . && quirk --version && quirk doctor) passes after the deletion batch"
    - "docs/dead-code-candidates.md catalogues the repo-wide vulture pass without any of it being deleted"
  artifacts:
    - path: "docs/dead-code-candidates.md"
      provides: "Reviewed dead-code backlog (D-02b), no deletions"
      contains: "vulture"
    - path: "quirk/intelligence/__init__.py"
      provides: "Package re-exports consistent with whatever schema state ships"
      min_lines: 10
  key_links:
    - from: "quirk/intelligence/__init__.py"
      to: "quirk/intelligence/schema.py"
      via: "re-export imports must match the file's actual contents (Pitfall 1)"
      pattern: "from quirk.intelligence.schema import"
    - from: "quirk/reports/writer.py"
      to: "quirk/cbom/builder.py"
      via: "builder reads ep.cert_pubkey_alg directly; does NOT call _extract_cert_key_type"
      pattern: "cert_pubkey_alg"
---

<objective>
Close CLEAN-02 (Tier-B function/module deletions). Remove vulture-confirmed dead code: `_extract_cert_key_type` in writer.py and the unused intelligence schema dataclasses, plus their test files; then produce the D-02b repo-wide vulture catalogue WITHOUT deleting anything else. Ships AFTER Plan 91-01 (Tier-A) per v5.0-D-06.

Purpose: Remove genuinely dead code with static-analysis confidence (vulture/AST, never grep) and a clean-venv safety net, while capturing the remaining dead-code signal as a reviewed backlog rather than acting on it blindly.
Output: Smaller, dead-code-free intelligence/reports surface; a vulture catalogue at docs/dead-code-candidates.md; clean-venv smoke passing.

⚠ BLOCKING CONFLICT (must resolve at Task 2): A Phase 77 D-15 CI gate (tests/test_intelligence_public_api.py) asserts `IntelligenceReport` MUST remain in the public API and MUST NOT be deleted — directly conflicting with D-02/BACK-52 which lists it for removal. See the decision checkpoint (Task 1) below.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-code-cleanup-bookkeeping/91-CONTEXT.md
@.planning/phases/91-code-cleanup-bookkeeping/91-RESEARCH.md
@.planning/phases/91-code-cleanup-bookkeeping/91-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Confirmed by planner from the codebase. -->

quirk/reports/writer.py:
    line 9:  unused `RichText` from rich.text (vulture 90%) — delete
    line 73: `_extract_cert_key_type()` (vulture 60%, defined never called in prod) — delete (~lines 73-88)
    line 59: `SCHEMA_VERSION = 2`  ← writer's OWN constant, UNRELATED to intelligence/schema.py. Do NOT touch.
  CBOM builder reads ep.cert_pubkey_alg directly; only tests/test_cert_pubkey_fix.py imports the helper.

quirk/intelligence/schema.py defines 5 frozen dataclasses:
    ScoreInputs, ScoreResult, ConfidenceResult, RoadmapItem, IntelligenceReport
  IntelligenceReport's fields are typed as the other four (score_inputs: ScoreInputs, etc.) — coupled unit.

quirk/intelligence/__init__.py re-exports all 5 + SCHEMA_VERSION (import block lines 5-12, __all__ lines 19-23).
  Deleting schema.py WITHOUT updating __init__.py → ImportError on `import quirk.intelligence` (Pitfall 1).

CONFLICT — existing importers of the schema dataclasses (research missed test_intelligence_public_api.py):
    tests/test_intelligence_schema.py        — constructs IntelligenceReport(...) and exercises all fields
    tests/test_intelligence_public_api.py    — Phase 77 D-15 pivot CI GATE: asserts IntelligenceReport
        MUST stay exported from quirk.intelligence AND in __all__, MUST keep its "typed return shape"
        docstring, AND that test_intelligence_schema.py constructs it. This gate FAILS if schema.py /
        test_intelligence_schema.py are deleted.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Resolve the Phase 77 D-15 vs BACK-52 deletion conflict</name>
  <action>STOP and present this decision to the user. Do not delete any intelligence schema files until the user selects an option. Surface the conflict between BACK-52/D-02 (delete the 5 dataclasses) and the Phase 77 D-15 CI gate (tests/test_intelligence_public_api.py, which forbids deleting IntelligenceReport). Record the chosen option in the SUMMARY and carry it into Task 2.</action>
  <decision>BACK-52/D-02 lists the intelligence schema dataclasses (including IntelligenceReport) for deletion, but a Phase 77 D-15 locked CI gate (tests/test_intelligence_public_api.py) explicitly asserts IntelligenceReport must NEVER be deleted and must remain in the public API. These cannot both hold. How should Tier-B proceed?</decision>
  <context>tests/test_intelligence_public_api.py was added in commit 9416c37 (Phase 77 D-15 pivot) precisely to "fail first and force the importer cascade to be re-examined" if a future refactor tries to delete the dataclass. IntelligenceReport's fields are typed as the other four dataclasses, so they are a coupled unit — you cannot delete the four and keep IntelligenceReport compiling. The user's D-02 decision in 91-CONTEXT.md is "delete only BACK-49/50/51/52/54" and predates surfacing this conflict.</context>
  <options>
    <option id="option-a">
      <name>Honor Phase 77 D-15 — keep the schema dataclasses, drop them from Tier-B scope</name>
      <pros>No regression to the existing D-15 CI gate; respects the documented "typed return shape" intent; lowest risk.</pros>
      <cons>BACK-52 schema.py portion remains nominally open — record it as superseded-by-D-15 in REQUIREMENTS traceability and CONCERNS, not as incomplete.</cons>
    </option>
    <option id="option-b">
      <name>Honor BACK-52 — delete all 5 dataclasses + both test files + the D-15 gate</name>
      <pros>Fully satisfies the literal BACK-52/D-02 deletion list.</pros>
      <cons>Deletes a deliberately-placed Phase 77 guardrail and its public-API contract; reverses a prior locked decision; higher blast radius (__init__.py, two test files, one CI gate).</cons>
    </option>
  </options>
  <resume-signal>Select: option-a (recommended — keep IntelligenceReport, proceed with only the writer.py deletions in Task 2) or option-b (delete everything, including the D-15 gate)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Vulture-confirm + delete Tier-B dead code (CLEAN-02, BACK-50/52)</name>
  <files>quirk/reports/writer.py, tests/test_cert_pubkey_fix.py, quirk/intelligence/schema.py, quirk/intelligence/__init__.py, tests/test_intelligence_schema.py</files>
  <read_first>91-RESEARCH.md BACK-50 + BACK-52 sections, the <interfaces> block above, the Task 1 decision outcome</read_first>
  <action>Install vulture (pip install vulture==2.16 — PyPI [OK], approved in 91-RESEARCH Package Legitimacy Audit; temporary dev tool, do NOT add to pyproject.toml). Before each deletion, confirm reachability with vulture AND a grep of run_scan.py / __init__.py re-exports (NOT grep alone — Pitfall 5 for scanner entry points; this target set is not a scanner entry point but follow the confirm-then-delete discipline).
  ALWAYS (independent of Task 1): In quirk/reports/writer.py delete `_extract_cert_key_type()` (~lines 73-88) and the unused `RichText` import (line 9). Do NOT touch writer.py:59 `SCHEMA_VERSION = 2` (writer's own constant). Delete tests/test_cert_pubkey_fix.py (tests the now-deleted helper).
  CONDITIONAL on Task 1:
    - If option-a: STOP here. Do NOT delete schema.py / test_intelligence_schema.py / touch __init__.py re-exports. The IntelligenceReport public-API gate stays green.
    - If option-b: Delete quirk/intelligence/schema.py; remove the schema imports (lines 5-12) and __all__ entries (lines 19-23) for the 5 dataclasses from quirk/intelligence/__init__.py while KEEPING the non-schema re-exports intact (Pitfall 1); delete tests/test_intelligence_schema.py AND tests/test_intelligence_public_api.py (the D-15 gate). Update REQUIREMENTS/CONCERNS traceability to note the D-15 reversal.
  Minimal diffs, PEP 8. Run `python -m compileall quirk tests` after edits.</action>
  <verify>
    <automated>python -c "import quirk.intelligence; import quirk.reports.writer" && grep -c '_extract_cert_key_type' quirk/reports/writer.py | grep -qx 0 && test ! -f tests/test_cert_pubkey_fix.py && python -m compileall -q quirk tests && echo PASS</automated>
  </verify>
  <done>`import quirk.intelligence` and `import quirk.reports.writer` both succeed; `_extract_cert_key_type` and the RichText import are gone from writer.py; test_cert_pubkey_fix.py is deleted; schema deletions match the Task 1 decision (and if option-b, __init__.py has no dangling schema re-exports); compileall clean.</done>
</task>

<task type="auto">
  <name>Task 3: Clean-venv smoke test + D-02b vulture catalogue (CLEAN-02 / D-02b)</name>
  <files>docs/dead-code-candidates.md</files>
  <read_first>91-RESEARCH.md "Vulture D-02b Report Preview" + "Common Pitfalls" (scanner entry-point false positives)</read_first>
  <action>Clean-venv smoke test after the deletion batch (D-02 locked): run `pip install -e . && quirk --version && quirk doctor` and confirm no import errors. D-02b (report-the-rest): run a repo-wide pass `vulture quirk/ --min-confidence 60` and write docs/dead-code-candidates.md cataloguing every finding WITHOUT deleting any of it. The doc MUST: (1) state it is a reviewed backlog for a future per-item review, NOT an action list; (2) explicitly flag that the bulk of 60% findings are scanner entry points (scan_*_targets) reachable via run_scan.py dynamic dispatch and are NOT dead (Pitfall 5); (3) separate the high-signal 80%+ findings (unused imports in azure/gcp/k8s connectors' try/except blocks, html_renderer.py:110 unreachable else, scheduler_cmd.py:29 signal-handler params) from the noisy 60% tail. Do NOT delete anything reported here (D-02b is explicitly report-only — optional-extra/dynamic-import paths hide reachability).</action>
  <verify>
    <automated>test -f docs/dead-code-candidates.md && grep -qi 'vulture' docs/dead-code-candidates.md && grep -qi 'not.*delet\|reviewed backlog\|not an action list' docs/dead-code-candidates.md && echo PASS</automated>
  </verify>
  <done>Clean-venv smoke (pip install -e . && quirk --version && quirk doctor) passes with no import errors; docs/dead-code-candidates.md exists, references the vulture pass, separates 80%+ from 60% findings, flags scanner entry-point false positives, and declares itself report-only with zero deletions taken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deletion → reachable code | Removing a symbol that is actually reachable (dynamic import / __init__ re-export / optional-extra) would break runtime |
| dev install → pip (vulture) | Temporary dev tool install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-03 | Denial of Service (broken import) | quirk/intelligence/__init__.py re-exports | mitigate | vulture/AST + grep of __init__ re-exports confirm reachability before deletion (NOT grep alone); `import quirk.intelligence` verify command; clean-venv smoke (pip install -e . && quirk --version && quirk doctor) after the batch (Pitfall 1). |
| T-91-04 | Tampering (false-positive deletion) | scanner entry points in D-02b report | accept | D-02b is report-only — nothing flagged is deleted; doc explicitly warns scan_*_targets are reachable via run_scan.py dynamic dispatch (Pitfall 5). |
| T-91-SC | Tampering | pip install vulture==2.16 | mitigate | vulture verified on PyPI ([OK], slopcheck pass, 91-RESEARCH Package Legitimacy Audit); pure-Python, no postinstall scripts; installed as a transient dev tool, not added to pyproject.toml. |
</threat_model>

<verification>
- CLEAN-02 deletions: `import quirk.intelligence` + `import quirk.reports.writer` succeed; `grep -c _extract_cert_key_type quirk/reports/writer.py` == 0; deleted test files absent from `python -m pytest tests/ --collect-only -q`.
- CLEAN-02 D-02b: `docs/dead-code-candidates.md` exists, references vulture, declares report-only.
- Clean-venv smoke: `pip install -e . && quirk --version && quirk doctor` no import errors.
- Regression gate: `QUIRK_DB_PATH=/tmp/quirk91.db python -m pytest tests/ -q` no NEW failures vs baseline; `python -m compileall quirk tests` clean.
</verification>

<success_criteria>
- Phase 77 D-15 conflict explicitly resolved (Task 1 decision recorded).
- _extract_cert_key_type + RichText import removed; test_cert_pubkey_fix.py deleted.
- Schema deletions consistent with the Task 1 decision; quirk.intelligence still imports.
- docs/dead-code-candidates.md catalogues the vulture pass, report-only, scanner false positives flagged.
- Clean-venv smoke passes; no new test failures vs baseline.
</success_criteria>

<output>
Create `.planning/phases/91-code-cleanup-bookkeeping/91-02-SUMMARY.md` when done.
</output>
