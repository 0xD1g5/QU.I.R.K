---
phase: 150-test-suite-green-baseline-ci-gate
plan: 06
type: execute
wave: 2
depends_on: [150-04]
files_modified:
  - tests/test_bacnet_scanner.py
  - tests/test_modbus_scanner.py
  - tests/test_snmp_scanner_contract.py
  - tests/test_identity_surface.py
  - tests/test_rest_fuzzer_cascade.py
  - tests/test_rest_fuzzer_dedup.py
  - tests/test_rest_fuzzer_pinned_session.py
  - tests/test_rest_fuzzer_probes.py
  - tests/test_openapi_scanner.py
  - tests/scanner/test_phase57_invariants.py
  - tests/test_audit_ledger_zero_open.py
  - tests/test_extras_concurrency_expander.py
  - tests/skip_registry.py
autonomous: true
requirements: [SUITE-02, SUITE-03]

must_haves:
  truths:
    - "Every test that depends on the identity, hw or api extras takes a documented skip path instead of hard-crashing when those extras are absent, matching the install surface `.[all]` actually provides"
    - "Tests that read `.planning/audit-2026-05-08/AUDIT-TASKS.md` skip cleanly on a public clone where that path is gitignored, instead of raising FileNotFoundError"
    - "Every new skip is registered in tests/skip_registry.py under a category that identifies it as Phase 150 CI-parity work, distinguishable from pre-existing optional_extra skips"
    - "The tests that specifically exercise the extra-absent graceful-fallback path still execute — no module-level importorskip was introduced"
  artifacts:
    - path: "tests/skip_registry.py"
      provides: "ci_extras_gap and gitignored_planning_dir categories covering all new Phase 150 skips"
      contains: "ci_extras_gap"
  key_links:
    - from: "tests/skip_registry.py"
      to: "tests/test_skip_registry.py::test_no_unregistered_skips"
      via: "AST walk matching (filename, lineno) with +/-2 tolerance"
      pattern: "ALLOWED_SKIPS"
---

<objective>
Close Categories A, B, C, D and F — 35 of the 38 real-CI failures — by giving each affected test a
per-test guard that skips when its precondition is genuinely unavailable, and registering every one
of those skips (D-09, D-10, D-11, D-15).

Purpose: `.[all]` deliberately excludes `identity` (impacket), `hw` (pysnmp/pymodbus/bacpypes3) and
`api` (schemathesis/openapi-spec-validator), and `.planning/` is gitignored on the public repo. These
tests currently assume those things are present and raise AttributeError / ModuleNotFoundError /
FileNotFoundError instead of skipping — so the very install boundary the project deliberately
maintains makes the suite red.
Output: 35 guarded tests, two new registry categories, a green skip-registry meta-gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/150-test-suite-green-baseline-ci-gate/150-CONTEXT.md
@.planning/phases/150-test-suite-green-baseline-ci-gate/150-03-SUMMARY.md
@.planning/phases/150-test-suite-green-baseline-ci-gate/150-04-SUMMARY.md
@tests/skip_registry.py

<interfaces>
<!-- Verified facts. Do not re-derive. -->

The guard idiom to match (D-09 cites `tests/test_aws_connector.py:172`):

    def test_scan_s3_propagates_build_endpoint_exception():
        """..."""
        if not aws_connector.BOTO3_AVAILABLE:
            pytest.skip("boto3 not installed")

That is a per-test, first-statement-after-docstring availability check against the scanner module's
own module-level flag. Use that exact shape wherever the scanner module already exposes a flag; use
`try: import <pkg> / except ImportError: pytest.skip("<pkg> not installed")` only where no flag
exists. Both are per-test. Module-level `pytest.importorskip()` is explicitly rejected by D-09.

Availability flags that already exist (use these, do not create new ones):
  quirk/scanner/bacnet_scanner.py:59-61   `_PYBACNET_AVAILABLE`   (bacpypes3, [hw])
  quirk/scanner/modbus_scanner.py:46-48   `_PYMODBUS_AVAILABLE`   (pymodbus, [hw])
  quirk/scanner/snmp_scanner.py:63-65     `_PYSNMP_AVAILABLE`     (pysnmp, [hw])
  quirk/scanner/rest_fuzzer.py:64-66      `SCHEMATHESIS_AVAILABLE` (schemathesis, [api])
  quirk/scanner/openapi_scanner.py:36-39  `OPENAPI_AVAILABLE`     (openapi-spec-validator, [api])
No flag exists for impacket — `tests/test_identity_surface.py` uses the try/import/except form.

The skip-registry meta-gate (`tests/test_skip_registry.py`): AST-walks every `tests/**/*.py` except
`skip_registry.py` and `test_skip_registry.py`, flags `pytest.skip(...)`, `pytest.importorskip(...)`
calls and `@pytest.mark.{skip,skipif,xfail}` decorators, and matches `(py_file.name, node.lineno)`
against `ALLOWED_SKIPS` with a +/-2 LINE_TOLERANCE. The registry key is the BASE FILENAME only, so
`tests/scanner/test_phase57_invariants.py` registers as `test_phase57_invariants.py`.

Existing registry entries in files this plan edits — these line numbers shift when guards are
inserted above them and MUST be re-synced:
  test_snmp_scanner_contract.py → 380, 599, 632, 674 (target guard goes at line ~705, after all four)
  test_openapi_scanner.py       → 231
No other file in `files_modified` currently has a registry entry (verify, do not assume).

File-shape facts that ruled out module-level importorskip (D-09): `test_snmp_scanner_contract.py`
has 22 tests of which 1 fails; `test_rest_fuzzer_probes.py` has 21 tests of which 9 fail. The
majority in both files validate the "extra absent, graceful fallback" behavior itself and must keep
executing.

Category A's four failing tests and their runtime reads of the gitignored path:
  tests/scanner/test_phase57_invariants.py:95   `(REPO_ROOT / ".planning" / "audit-2026-05-08" / "AUDIT-TASKS.md").read_text()`
  tests/test_audit_ledger_zero_open.py:26       module-level path construction feeding
                                                `test_audit_ledger_has_zero_bare_open_rows` and
                                                `test_deferred_and_wontfix_rows_have_rationale`
  tests/test_extras_concurrency_expander.py:256 `(root / ".planning/audit-2026-05-08/AUDIT-TASKS.md").read_text(...)`
The scope is exactly these 3 files / 4 tests — the other 3 test files that mention `.planning/` do so
only in comments (verified during discussion).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard the hw- and identity-gated tests (D-09, D-10, D-11 — Categories B and F)</name>
  <files>tests/test_bacnet_scanner.py, tests/test_modbus_scanner.py, tests/test_snmp_scanner_contract.py, tests/test_identity_surface.py, tests/skip_registry.py</files>
  <read_first>
    - tests/test_aws_connector.py lines 165-175 (the canonical idiom D-09 requires)
    - tests/skip_registry.py lines 1-15 (module docstring listing the category set — it must gain the new names) and lines 46-73 (the Phase 149 D-04 entry block, the row shape to copy)
    - tests/test_bacnet_scanner.py (whole file, 134 lines — see how `test_parse_device_object` at line 73 and `test_single_inflight_no_writes_unicast` at line 100 reach the scanner module)
    - tests/test_modbus_scanner.py (whole file, 149 lines — `test_parse_device_id` at 59, `test_parse_device_id_decodes_bytes` at 88, `test_single_inflight_no_writes` at 125)
    - tests/test_snmp_scanner_contract.py lines 370-390, 590-690 and 700-728 (the four existing registered skips and `test_arp_walk_import_guard_returns_empty_with_zero_network_calls` at line 704)
    - tests/test_identity_surface.py lines 460-500 (`Issue3ScanWindowRegressionTest::test_issue3_scan_window_returns_all_identity_protocols` at line 477)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-CONTEXT.md (D-09, D-10, D-11)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-04-SUMMARY.md (the parity-venv failure inventory — the authoritative list of which tests actually fail)
  </read_first>
  <action>
Work in the CI-parity venv from plan 150-04 (`$HOME/.cache/quirk-ci-parity-venv/bin/python`). Before
editing, run each target file there with `-m ""` and record the exact set of failing test IDs. Guard
exactly those tests — the counts in `150-03-SUMMARY.md` (2 bacnet, 3 modbus, 1 snmp, 1 identity) are
the expectation, but the parity-venv run is the authority. If the sets differ, note the delta in the
SUMMARY and guard what actually fails.

For each failing test, insert a guard as the first statement after the test function's docstring
(for the class-based `Issue3ScanWindowRegressionTest`, insert it at the top of the test method
itself, not in `setUp`, unless the class has other tests that would need the same guard — in that
case use `setUp` and register that single line instead). Use the scanner module's existing
availability flag where one exists: `_PYBACNET_AVAILABLE` for the two bacnet tests,
`_PYMODBUS_AVAILABLE` for the three modbus tests, `_PYSNMP_AVAILABLE` for
`test_arp_walk_import_guard_returns_empty_with_zero_network_calls`. Import the scanner module inside
the guard if the test file does not already have it in scope. Skip reasons must name the missing
package exactly as the existing registry rows do: "bacpypes3 not installed", "pymodbus not
installed", "pysnmp not installed".

For `tests/test_identity_surface.py::Issue3ScanWindowRegressionTest::test_issue3_scan_window_returns_all_identity_protocols`
use the try/import/except form against `impacket` with the reason "impacket not installed". D-11 is
explicit that this test gets the same treatment as the other 30 despite being a positive assertion
about a real KERBEROS finding rather than a guard-path test — do not special-case it, do not weaken
the assertion, and do not delete it.

Do NOT add a module-level `pytest.importorskip` to any of these files, and do NOT add
`@pytest.mark.skipif` decorators — D-09 requires the per-test call form uniformly, including in
`test_bacnet_scanner.py` and `test_modbus_scanner.py` where every failing test happens to be in the
same file.

Then update `tests/skip_registry.py`:
  - extend the module docstring's category enumeration (line 4) to include `"ci_extras_gap"`;
  - add a clearly commented block, headed as Phase 150 D-09/D-10 CI-parity gap closure, containing
    one row per new skip with the base filename, the guard's `pytest.skip(...)` call line number,
    the category string `"ci_extras_gap"`, and a reason naming the missing extras group and the
    package (for example: hw extra absent from .[all]; bacpypes3 not installed);
  - re-sync the four existing `test_snmp_scanner_contract.py` line numbers (380, 599, 632, 674) if
    any guard insertion shifted them beyond the +/-2 tolerance. Insertions at line ~705 are after
    all four, so no shift is expected — verify with the meta-gate rather than assuming.

Keep `ci_extras_gap` strictly separate from the pre-existing `optional_extra` category: D-10's whole
point is that Phase 150's CI-parity skips must be greppable independently of unrelated dev-sandbox
skips. Do not retro-categorize any existing row.
  </action>
  <verify>
    <automated>$HOME/.cache/quirk-ci-parity-venv/bin/python -m pytest tests/test_bacnet_scanner.py tests/test_modbus_scanner.py tests/test_snmp_scanner_contract.py tests/test_identity_surface.py -q -m ""</automated>
    <automated>$HOME/.cache/quirk-ci-parity-venv/bin/python -m pytest tests/test_skip_registry.py -q -m ""</automated>
    <automated>grep -v '^ *#' tests/skip_registry.py | grep -c "ci_extras_gap"</automated>
    <automated>grep -c "importorskip" tests/test_bacnet_scanner.py tests/test_modbus_scanner.py tests/test_snmp_scanner_contract.py tests/test_identity_surface.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_bacnet_scanner.py tests/test_modbus_scanner.py tests/test_snmp_scanner_contract.py tests/test_identity_surface.py -q -m ""` in the parity venv reports `0 failed`
    - The same command in the repo's broad `.venv` (which has hw + identity installed) still reports `0 failed` and shows the guarded tests as PASSED, not skipped — proving the guards gate on availability, not unconditionally
    - `pytest tests/test_skip_registry.py -q -m ""` reports `1 passed`
    - `grep -c "importorskip" ` over the four edited test files returns `0` for each
    - `grep -c "@pytest.mark.skipif" ` over the four edited test files shows no net new decorators versus `git diff`
    - `grep -v '^ *#' tests/skip_registry.py | grep -c "ci_extras_gap"` equals the number of guards added in this task
    - `tests/skip_registry.py` line 4's category enumeration includes `ci_extras_gap`
    - `git diff tests/test_identity_surface.py` shows only an added guard — no assertion text changed
  </acceptance_criteria>
  <done>All Category B and Category F failures skip cleanly on a `.[all]`-only install, still run where the extras exist, and every skip is registered under ci_extras_gap.</done>
</task>

<task type="auto">
  <name>Task 2: Guard the api-extras-gated tests (D-09, D-10 — Categories C and D)</name>
  <files>tests/test_rest_fuzzer_cascade.py, tests/test_rest_fuzzer_dedup.py, tests/test_rest_fuzzer_pinned_session.py, tests/test_rest_fuzzer_probes.py, tests/test_openapi_scanner.py, tests/skip_registry.py</files>
  <read_first>
    - tests/test_aws_connector.py lines 165-175 (the idiom)
    - tests/skip_registry.py (the block added by Task 1 — new rows go in the same block, same shape)
    - tests/test_rest_fuzzer_cascade.py, tests/test_rest_fuzzer_dedup.py, tests/test_rest_fuzzer_pinned_session.py (all three files in full — they are 135-260 lines each)
    - tests/test_rest_fuzzer_probes.py (822 lines — read the class/function headers and the failing tests' bodies; do not read the whole file line by line if a targeted read of the failing classes suffices)
    - tests/test_openapi_scanner.py (261 lines, including the existing registered skip at line 231)
    - quirk/scanner/rest_fuzzer.py lines 55-75 and 480-495 (the `SCHEMATHESIS_AVAILABLE` flag and its guard usage)
    - quirk/scanner/openapi_scanner.py lines 20-45 and 200-215 and 360-370 (the `OPENAPI_AVAILABLE` flag and its two guard usages)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-CONTEXT.md (D-09 — per-test only; the file-shape evidence that rules out module-level importorskip)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-04-SUMMARY.md (parity-venv failure inventory)
  </read_first>
  <action>
Run each of the five files in the CI-parity venv with `-m ""` and capture the exact failing test IDs.
`150-03-SUMMARY.md` expects 18 failures across the four rest_fuzzer files (3 cascade, 3 dedup, 1
pinned_session, 9 probes) and 6 in `test_openapi_scanner.py`; the parity-venv run is authoritative
if the sets differ.

Guard each failing test with a per-test check on the corresponding module flag —
`SCHEMATHESIS_AVAILABLE` from `quirk/scanner/rest_fuzzer.py` for the four rest_fuzzer files,
`OPENAPI_AVAILABLE` from `quirk/scanner/openapi_scanner.py` for `test_openapi_scanner.py` — placed
as the first statement after the test's docstring, with skip reasons "schemathesis not installed"
and "openapi-spec-validator not installed" respectively. For class-based tests (the dedup file and
several probe classes), put the guard at the top of each failing test method; only use `setUp` /
`setup_method` when every test in that class fails for the same reason, and in that case register
the single `setUp` line.

Critical constraint from D-09: do NOT add a module-level `pytest.importorskip` to any of these
files. `test_rest_fuzzer_probes.py` has 21 tests of which only 9 fail, and the remaining 12
specifically exercise the SSRF / scope-gate / budget-cap / rate-limiter behavior that must keep
running without schemathesis installed. A module-level skip would silently stop testing the
fallback path — the exact class of blind spot this phase exists to eliminate. Verify after editing
that the non-failing tests in each file still execute (they must appear as passed, not skipped, in
the parity venv).

Add one `ci_extras_gap` row per new guard to `tests/skip_registry.py`, in the same Phase 150 block
Task 1 created, with reasons naming the api extras group and the package. Re-sync the existing
`test_openapi_scanner.py` entry at line 231 if any guard inserted above it shifted it beyond the
+/-2 tolerance — check its current line number after editing and update the row.
  </action>
  <verify>
    <automated>$HOME/.cache/quirk-ci-parity-venv/bin/python -m pytest tests/test_rest_fuzzer_cascade.py tests/test_rest_fuzzer_dedup.py tests/test_rest_fuzzer_pinned_session.py tests/test_rest_fuzzer_probes.py tests/test_openapi_scanner.py -q -m "" -rs</automated>
    <automated>$HOME/.cache/quirk-ci-parity-venv/bin/python -m pytest tests/test_skip_registry.py -q -m ""</automated>
    <automated>grep -c "importorskip" tests/test_rest_fuzzer_cascade.py tests/test_rest_fuzzer_dedup.py tests/test_rest_fuzzer_pinned_session.py tests/test_rest_fuzzer_probes.py tests/test_openapi_scanner.py</automated>
  </verify>
  <acceptance_criteria>
    - The five-file run in the parity venv reports `0 failed`
    - In that same run, `test_rest_fuzzer_probes.py` reports at least 12 passed (the fallback-path tests still execute) and `test_snmp_scanner_contract.py`-style wholesale skipping did not occur
    - `grep -c "importorskip"` returns `0` for each of the five files
    - The same five-file run in the repo's broad `.venv` (schemathesis + openapi-spec-validator present) reports `0 failed` with the guarded tests PASSED rather than skipped
    - `pytest tests/test_skip_registry.py -q -m ""` reports `1 passed`
    - The `test_openapi_scanner.py` row in `tests/skip_registry.py` points at the current line number of its pre-existing skip (within +/-2)
    - Total `ci_extras_gap` rows after this task equals the total number of guards added in Tasks 1 and 2 combined
  </acceptance_criteria>
  <done>All Category C and D failures skip cleanly under `.[all]`, the graceful-fallback tests still run, and every skip is registered.</done>
</task>

<task type="auto">
  <name>Task 3: Guard the gitignored-planning-dir fixture reads and finalize the registry (D-15)</name>
  <files>tests/scanner/test_phase57_invariants.py, tests/test_audit_ledger_zero_open.py, tests/test_extras_concurrency_expander.py, tests/skip_registry.py</files>
  <read_first>
    - tests/scanner/test_phase57_invariants.py (125 lines — `test_audit_tasks_six_blockers_closed` and its `.read_text()` at line 95)
    - tests/test_audit_ledger_zero_open.py (74 lines — the module-level path construction at lines ~20-30 and both failing tests)
    - tests/test_extras_concurrency_expander.py lines 240-259 (`test_audit_rows_flipped_to_phase_71` and its `.read_text()` at line 256)
    - tests/skip_registry.py (the Phase 150 block from Tasks 1 and 2)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-CONTEXT.md (D-15 — existence-check guard, new category `gitignored_planning_dir`, scope verified complete at 4 tests / 3 files)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-04-SUMMARY.md (parity-venv failure inventory)
  </read_first>
  <action>
`.planning/audit-2026-05-08/AUDIT-TASKS.md` is gitignored on this public repo (Phase 120
PUBREPO-01), so it will never exist in a public clone or a CI checkout. All four tests read it
directly with no existence check.

Add a per-test existence guard — resolve the path, then `if not <path>.exists(): pytest.skip(...)`
as the first statement after the docstring, before any `.read_text()`. Use a skip reason naming the
path and the reason it is absent, for example that `.planning/audit-2026-05-08/AUDIT-TASKS.md` is
gitignored on the public repo per PUBREPO-01. Where the path is currently built at module scope (as
in `tests/test_audit_ledger_zero_open.py`), keep the module-scope constant and put the guard inside
each of the two test functions — do not convert this to a module-level skip, and do not weaken or
delete any of the assertions that run when the file IS present (these gates still protect the
private working copy).

Register all four skips in `tests/skip_registry.py` under the new category
`"gitignored_planning_dir"`, keyed on the BASE filename only —
`test_phase57_invariants.py` (not the `scanner/` prefix), `test_audit_ledger_zero_open.py`,
`test_extras_concurrency_expander.py`. Extend the module docstring's category enumeration at line 4
so it lists both `ci_extras_gap` and `gitignored_planning_dir`.

Do not widen scope: the other three test files that mention `.planning/`
(`test_coverage_bounds.py`, `test_dashboard_api.py`, `test_dashboard_trends.py`) reference it only
in comments, never at runtime — confirm this with a grep of runtime statements before concluding,
then leave them untouched.

Finally, do the registry hygiene pass for the whole plan: re-run the meta-gate, and if it reports
any violation, fix the offending line number rather than widening `LINE_TOLERANCE` or exempting a
file. Then run the complete set of files touched by Tasks 1-3 in the parity venv with `-rs` and
confirm every reported skip reason matches a registered row.
  </action>
  <verify>
    <automated>$HOME/.cache/quirk-ci-parity-venv/bin/python -m pytest tests/scanner/test_phase57_invariants.py tests/test_audit_ledger_zero_open.py tests/test_extras_concurrency_expander.py -q -m "" -rs</automated>
    <automated>$HOME/.cache/quirk-ci-parity-venv/bin/python -m pytest tests/test_skip_registry.py -q -m ""</automated>
    <automated>grep -v '^ *#' tests/skip_registry.py | grep -c "gitignored_planning_dir"</automated>
    <automated>$HOME/.cache/quirk-ci-parity-venv/bin/python -m compileall -q tests/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^ *#' tests/skip_registry.py | grep -c "gitignored_planning_dir"` returns exactly `4`
    - `tests/skip_registry.py` line 4's category enumeration lists both `ci_extras_gap` and `gitignored_planning_dir`
    - With `.planning/audit-2026-05-08/AUDIT-TASKS.md` temporarily renamed away, the three-file run reports `0 failed` and 4 skipped; with it restored, the same run reports `0 failed` and 0 skipped
    - `pytest tests/test_skip_registry.py -q -m ""` reports `1 passed`
    - `grep -rn "AUDIT-TASKS.md" tests/ | grep -v "skip_registry" | wc -l` shows reads only in the three guarded files
    - `LINE_TOLERANCE` in `tests/test_skip_registry.py` is still `2` and `EXEMPT_FILES` is unchanged
    - `python -m compileall -q tests/` exits 0
  </acceptance_criteria>
  <done>Category A skips cleanly on a public checkout, the assertions still fire when the ledger is present, and the skip registry is internally consistent for all 35 new skips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Optional extras surface → test suite | Tests decide behavior based on whether third-party packages are importable |
| Private `.planning/` working copy → public CI checkout | Test fixtures read a path that exists locally but never in CI |
| Skip registry → CI gate | The registry is the sole allowlist deciding which skips are legitimate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-21 | Tampering | Over-broad skipping silently disabling real coverage | mitigate | D-09 forbids module-level importorskip; Task 2 asserts `test_rest_fuzzer_probes.py` still reports at least 12 passed, and every task asserts the same files are green-with-tests-PASSED in the broad `.venv` where the extras exist |
| T-150-22 | Repudiation | A skip added without a traceable reason | mitigate | Every new skip gets an `ALLOWED_SKIPS` row with a category and a reason naming the missing package or path; the AST meta-gate fails the build on any unregistered marker |
| T-150-23 | Information Disclosure | Skip reason strings leaking private planning paths into public CI logs | accept | The path `.planning/audit-2026-05-08/AUDIT-TASKS.md` is already documented in the public `.gitignore` and in `docs/test-triage-149.md`; the filename alone discloses nothing sensitive |
| T-150-24 | Elevation of Privilege | Weakening the meta-gate (raising LINE_TOLERANCE or exempting files) to make the registry pass | mitigate | Task 3 explicitly forbids it and asserts `LINE_TOLERANCE == 2` with `EXEMPT_FILES` unchanged |
| T-150-SC | Tampering | pip/npm/cargo installs | mitigate | This plan installs nothing — it only adds guards against packages already declared in `pyproject.toml` |
</threat_model>

<verification>
1. All 12 edited test files report `0 failed` in the CI-parity venv with `-m ""`.
2. The same files report `0 failed` with the guarded tests PASSED in the repo's broad `.venv`.
3. `tests/test_skip_registry.py::test_no_unregistered_skips` passes.
4. `ci_extras_gap` row count equals the number of extras guards added; `gitignored_planning_dir` row count is exactly 4.
5. No module-level `importorskip` was added to any of the 6 extras-gated files.
6. `LINE_TOLERANCE` and `EXEMPT_FILES` in the meta-gate are unchanged.
</verification>

<success_criteria>
- 35 of the 38 real-CI failures are resolved by honoring the declared install boundary, not by disabling coverage.
- Phase 150's CI-parity skips are greppable as their own category, separate from pre-existing skips.
</success_criteria>

<output>
Create `.planning/phases/150-test-suite-green-baseline-ci-gate/150-06-SUMMARY.md` when done.
</output>
