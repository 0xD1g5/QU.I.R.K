---
phase: 150-test-suite-green-baseline-ci-gate
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_v41_gap_closure.py
  - tests/test_sensor_ingest.py
  - tests/skip_registry.py
  - .planning/phases/150-test-suite-green-baseline-ci-gate/150-D17-INVESTIGATION.md
autonomous: true
requirements: [SUITE-02, SUITE-03]

must_haves:
  truths:
    - "A CI-parity virtualenv exists outside the repo tree, provisioned with exactly `pip install -e \".[all]\"` + pytest, and its failure list is recorded so local runs stop lying about CI"
    - "The dead Phase-16 package-version assertion no longer exists in the suite (D-16)"
    - "The /api/sensor/push CI-only failure is root-caused with written evidence, not guessed at (D-17)"
  artifacts:
    - path: ".planning/phases/150-test-suite-green-baseline-ci-gate/150-D17-INVESTIGATION.md"
      provides: "Root-cause record and disposition for tests/test_sensor_ingest.py::test_push_endpoint_exists"
      contains: "sensor/push"
  key_links:
    - from: "$HOME/.cache/quirk-ci-parity-venv"
      to: "pyproject.toml [project.optional-dependencies] all"
      via: "pip install -e \".[all]\" (no identity/hw/api extras)"
      pattern: "pip install -e"
---

<objective>
Stand up the CI-parity environment that every later plan in this phase verifies against, then close
the two singleton failures from the real CI run: delete the dead Phase-16 package-version scaffold
(D-16) and root-cause the `/api/sensor/push` route-missing failure (D-17).

Purpose: `.continue-here.md`'s single blocking constraint is "local-baseline-as-CI-proxy" — every
local "0 failed" claim in Phase 149 and Plan 150-02 was made in a sandbox with `identity`/`hw`/`api`
extras installed that CI does not have. No further claim about this suite is trustworthy until a
venv provisioned with exactly CI's install command exists.
Output: a reusable CI-parity venv, a recorded failure inventory, one deleted dead test, and a
written root-cause for the one unexplained failure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/150-test-suite-green-baseline-ci-gate/150-CONTEXT.md
@.planning/phases/150-test-suite-green-baseline-ci-gate/150-03-SUMMARY.md
@.planning/phases/150-test-suite-green-baseline-ci-gate/.continue-here.md
@CONTRIBUTING.md

<interfaces>
<!-- Environment facts. Do not re-derive. -->

CI install (`.github/workflows/python-ci.yml`, job key `linux-full-suite`, display name
`Linux Full Suite`, lines 398-421):
  runs-on: ubuntu-latest, actions/setup-python 3.11
  pip install -e ".[all]"
  pip install pytest
  pytest -q -m ""

`.[all]` (pyproject.toml lines 117-128) resolves to: cloud, cbom, db, motion, redis, dashboard,
adcs, docx, notify, tickets. It does NOT include `identity` (impacket), `hw` (pysnmp, pymodbus,
bacpypes3) or `api` (openapi-spec-validator, schemathesis). Those exclusions are deliberate and
guard-tested (`tests/test_install_all_excludes_impacket.py`,
`tests/test_install_all_excludes_pysnmp.py`, `tests/test_install_all_excludes_schemathesis.py`).
Never add them to the parity venv — that would recreate the exact blindness this plan exists to fix.

Distribution name is `quirk-scanner` (pyproject.toml `name`), version `5.11.0`. There has been no
distribution named `quirk` since the v4.10 PyPI rename.

The repo's own `.venv/` is the broad dev sandbox ([all]+[identity]+[hw]+[api]) and must be left
untouched. Do not create the parity venv inside the repo tree — `.gitignore` only covers `.venv/`,
`.venv2/`, `venv/`, and several tests walk the repo tree.

Route registration facts already verified during discussion (do not re-verify from scratch):
`quirk/dashboard/api/app.py::create_app` lines 122-123 call
`application.include_router(sensor.router, prefix="/api")` and
`application.include_router(sensor.sensor_push_router, prefix="/api")` unconditionally — no
try/except, no extras gating. `quirk/dashboard/api/routes/sensor.py:64` creates
`sensor_push_router` unconditionally and line 368 declares `@sensor_push_router.post("/sensor/push")`.
`zstandard` (imported by sensor.py) is a base dependency, not an extra.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Provision the CI-parity venv and record its authoritative failure inventory</name>
  <files>(no repo files modified — creates $HOME/.cache/quirk-ci-parity-venv and a scratchpad log)</files>
  <read_first>
    - .planning/phases/150-test-suite-green-baseline-ci-gate/.continue-here.md (the BLOCKING CONSTRAINT block — local-baseline-as-CI-proxy; acknowledge it explicitly in the SUMMARY)
    - .github/workflows/python-ci.yml lines 398-421 (the exact install + invocation this venv must mirror)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-03-SUMMARY.md (the 38-failure, 8-category list this run should reproduce)
    - pyproject.toml lines 38-132 and 151-159 (the `all` extras group and the `addopts = "-m 'not slow'"` default that `-m ""` overrides)
  </read_first>
  <action>
Create a fresh virtualenv at `$HOME/.cache/quirk-ci-parity-venv` using the highest available
CPython 3.11 interpreter if one is present on the machine; if 3.11 is unavailable, use the closest
available interpreter and record the exact `python -V` in the SUMMARY as a known parity gap. Do not
create it inside the repository working tree, and do not reuse or modify the repo's existing
`.venv/`.

Provision it with exactly two commands, in this order, always invoked as
`$HOME/.cache/quirk-ci-parity-venv/bin/python -m pip ...` so the interpreter/pip mismatch footgun
recorded in `150-02-SUMMARY.md` cannot reproduce: `pip install -e ".[all]"` from the repo root, then
`pip install pytest`. Install nothing else. Explicitly do NOT install `identity`, `hw`, or `api`
extras, and do not install `impacket`, `pysnmp`, `pymodbus`, `bacpypes3`, `schemathesis`, or
`openapi-spec-validator` directly.

Assert the parity boundary before running anything: `pip list` in that venv must not contain
impacket, pysnmp, pymodbus, bacpypes3, schemathesis, or openapi-spec-validator. If any is present,
the venv is contaminated — delete it and rebuild.

Then run the full suite exactly as CI does, from the repo root, using the parity venv's interpreter:
`pytest -q -m ""`. Capture the entire output to a scratchpad log file. Docker may be running
locally; if the chaos-lab profile tests attempt to start containers, let them — that is what CI
does too, and Task 1 of plan 150-08 depends on knowing whether they pass here.

From the log, extract every `^FAILED` line into an inventory and diff it against
`150-03-SUMMARY.md`'s 8 categories. Record in the SUMMARY: the total counts line, the failure
inventory, which of the 38 CI failures reproduced, which did not, and any failure present locally
that was NOT in the CI run (that would be a local-only artifact and must be called out rather than
folded into this phase's remediation).

Do not fix any failure in this task. This task establishes ground truth only.
  </action>
  <verify>
    <automated>$HOME/.cache/quirk-ci-parity-venv/bin/python -m pip list 2>/dev/null | grep -Ei "^(impacket|pysnmp|pymodbus|bacpypes3|schemathesis|openapi-spec-validator) " | wc -l</automated>
    <automated>$HOME/.cache/quirk-ci-parity-venv/bin/python -c "import importlib.metadata as m; print(m.version('quirk-scanner'))"</automated>
  </verify>
  <acceptance_criteria>
    - `$HOME/.cache/quirk-ci-parity-venv/bin/python` exists and is not the repo's `.venv` interpreter
    - The pip-list grep for impacket/pysnmp/pymodbus/bacpypes3/schemathesis/openapi-spec-validator returns `0`
    - `importlib.metadata.version('quirk-scanner')` resolves in the parity venv (proves the editable install produced discoverable dist metadata)
    - A full-suite log exists in the session scratchpad containing a pytest summary line with `failed`
    - The SUMMARY contains a `FAILED` inventory and an explicit per-category reproduce/not-reproduce mapping against 150-03-SUMMARY.md's Categories A-H
    - `git status --porcelain` shows no new tracked-file changes from the install (any build artifacts left by the editable install are removed or confirmed gitignored)
  </acceptance_criteria>
  <done>A CI-parity venv exists at a stable path outside the repo, provably free of the three excluded extras groups, and its full-suite failure inventory is recorded and reconciled against the real CI run.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the dead Phase-16 package-version scaffold (D-16)</name>
  <files>tests/test_v41_gap_closure.py</files>
  <read_first>
    - tests/test_v41_gap_closure.py (all 60 lines — the module docstring names CLI-04 and SCORE-04; only the CLI-04 half is being removed)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-CONTEXT.md (D-16 — delete, do not fix in place)
    - pyproject.toml lines 1-20 (confirm `name = "quirk-scanner"` and the current version, so the SUMMARY can state why `version("quirk")` can never resolve)
  </read_first>
  <action>
Delete the method `test_package_manifest_version_is_4_1_0` from
`tests/test_v41_gap_closure.py::TestV41GapClosure` in full, including its docstring. Per D-16 this
is a dead Phase-16-era RED scaffold: it calls `importlib.metadata.version("quirk")` and asserts the
result equals the string `4.4.0`, but the distribution was renamed to `quirk-scanner` at the v4.10
PyPI rename and `pyproject.toml` now declares version `5.11.0`. The queried distribution name has
not existed for 15+ releases; it only ever passed in dev sandboxes carrying pre-rename editable
metadata. Do not attempt to fix it in place by renaming the queried distribution or updating the
version literal — a hardcoded version assertion would go stale again on the next release and there
is no live requirement pinning the version through `importlib.metadata`.

Keep the other two methods (`test_interactive_output_dir_default_is_quirk_output` and
`test_interactive_db_path_default_is_quirk_output`) untouched — they assert live `quirk/interactive.py`
defaults. Remove the now-dangling `import importlib.metadata` if no remaining code in the file uses
it, and update the module docstring so it no longer describes the removed CLI-04 assertion, leaving
the SCORE-04 description intact.

Confirm nothing else in the repo references the deleted method name and that no
`tests/skip_registry.py` entry exists for `test_v41_gap_closure.py` (there is none today — verify,
do not assume).
  </action>
  <verify>
    <automated>grep -c "test_package_manifest_version_is_4_1_0" tests/test_v41_gap_closure.py; grep -rn "test_package_manifest_version_is_4_1_0" tests/ docs/ | wc -l</automated>
    <automated>$HOME/.cache/quirk-ci-parity-venv/bin/python -m pytest tests/test_v41_gap_closure.py -q -m ""</automated>
    <automated>$HOME/.cache/quirk-ci-parity-venv/bin/python -m compileall -q tests/test_v41_gap_closure.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "test_package_manifest_version_is_4_1_0" tests/ docs/ | wc -l` returns `0`
    - `grep -c 'importlib.metadata.version("quirk")' tests/test_v41_gap_closure.py` returns `0`
    - `pytest tests/test_v41_gap_closure.py -q -m ""` in the parity venv reports `2 passed`
    - `grep -c "test_v41_gap_closure" tests/skip_registry.py` returns `0`
    - `pytest tests/test_skip_registry.py -q -m ""` in the parity venv reports `1 passed`
  </acceptance_criteria>
  <done>The stale `version("quirk")` assertion is gone from the suite, the remaining two assertions in the file still pass, and the skip-registry meta-gate is unaffected.</done>
</task>

<task type="auto">
  <name>Task 3: Root-cause the /api/sensor/push CI-only failure and dispose of it (D-17)</name>
  <files>tests/test_sensor_ingest.py, tests/skip_registry.py, .planning/phases/150-test-suite-green-baseline-ci-gate/150-D17-INVESTIGATION.md</files>
  <read_first>
    - tests/test_sensor_ingest.py lines 1-100 and 172-200 (the `_app_with_db()` helper and `test_push_endpoint_exists` itself)
    - quirk/dashboard/api/app.py lines 95-125 (unconditional `include_router(sensor.sensor_push_router, prefix="/api")` — already verified, start here rather than re-deriving)
    - quirk/dashboard/api/routes/sensor.py lines 31-70 and 360-375 (`sensor_push_router = APIRouter(dependencies=[Depends(require_sensor_auth)])` and the `@sensor_push_router.post("/sensor/push")` declaration)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-CONTEXT.md (D-17 — not root-caused, not to be guessed at; no speculative skip)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-03-SUMMARY.md Category G
  </read_first>
  <action>
D-17 is explicitly a scoped investigation, not a pre-decided fix. An unexplained 404 could be a real
regression, so the "don't paper over failures" precedent from D-07/D-09 applies: do not register a
skip until the cause is known and written down.

Run the investigation in this order in the parity venv from Task 1, recording each observation:

1. Standalone: run `tests/test_sensor_ingest.py::test_push_endpoint_exists` alone with `-m ""`.
2. Whole file: run `tests/test_sensor_ingest.py` with `-m ""`.
3. Full suite ordering: if steps 1 and 2 pass, the failure is order- or state-dependent. Re-run the
   whole suite with `-m ""` and confirm the failure reproduces there; then bisect by running
   `tests/` up to and including `test_sensor_ingest.py` in collection order to find the earliest
   prefix that reproduces it.
4. If it reproduces standalone, print the actual `route_paths` list the assertion produced and
   compare it against a direct `create_app()` call in a bare Python REPL in the same venv, and
   inspect whether `quirk/dashboard/api/routes/sensor.py` imported successfully.
5. Search the suite for state that could remove or replace the router:
   `grep -rn "sys.modules" tests/ | grep -Ei "sensor|dashboard|app"`,
   `grep -rn "importlib.reload" tests/`, and
   `grep -rn "create_app" tests/ | grep -v test_sensor_ingest`.

Then dispose of the finding in exactly one of three ways, and write which one and why into
`.planning/phases/150-test-suite-green-baseline-ci-gate/150-D17-INVESTIGATION.md`:

- **Real product defect** — fix the production code in `quirk/dashboard/api/` and leave the test
  asserting the same contract. Do not weaken the assertion.
- **Test-construction / cross-test-pollution defect** — fix the test (for example by isolating app
  construction) so it asserts the same contract robustly. No skip registration.
- **Genuinely environment-dependent** — only if steps 1-5 produce a written, specific mechanism, add
  a guard using the same per-test idiom as `tests/test_aws_connector.py:172` and register it in
  `tests/skip_registry.py` under the new category `ci_extras_gap` with a reason string that names
  the mechanism and points at `150-D17-INVESTIGATION.md`. A reason of the form "environment
  dependent" without a named mechanism is not acceptable.

If the failure does not reproduce in the parity venv at all, that is itself the finding: record it,
state that the disposition is deferred to plan 150-08's real CI run, and add no skip. In that case
150-08 Task 1 must treat a recurrence as an in-scope investigation, not a stop condition.

The investigation file must record: the exact commands run, the observed result of each of steps
1-5, the identified mechanism (or an explicit "not reproduced locally" statement), the chosen
disposition, and the reasoning for rejecting the other two dispositions.
  </action>
  <verify>
    <automated>test -f .planning/phases/150-test-suite-green-baseline-ci-gate/150-D17-INVESTIGATION.md &amp;&amp; grep -c "sensor/push" .planning/phases/150-test-suite-green-baseline-ci-gate/150-D17-INVESTIGATION.md</automated>
    <automated>$HOME/.cache/quirk-ci-parity-venv/bin/python -m pytest tests/test_sensor_ingest.py -q -m ""</automated>
    <automated>$HOME/.cache/quirk-ci-parity-venv/bin/python -m pytest tests/test_skip_registry.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - `150-D17-INVESTIGATION.md` exists and contains a section per investigation step 1-5 with the literal commands run and their observed outcomes
    - The file states exactly one disposition among: production fix, test fix, registered environment skip, or explicit "not reproduced locally — deferred to 150-08"
    - `pytest tests/test_sensor_ingest.py -q -m ""` in the parity venv reports `0 failed`
    - `pytest tests/test_skip_registry.py -q -m ""` in the parity venv reports `1 passed`
    - If and only if a skip was registered, `grep -c "ci_extras_gap" tests/skip_registry.py` is at least 1 and the reason string names a concrete mechanism (not the bare phrase "environment dependent")
    - No assertion in `test_push_endpoint_exists` was weakened: `grep -c '"/api/sensor/push" in route_paths' tests/test_sensor_ingest.py` returns `1` unless the whole test was replaced by a documented production fix path
  </acceptance_criteria>
  <done>The Category G failure has a written, evidence-backed root cause and exactly one justified disposition; no speculative skip was added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI index → parity venv | `pip install -e ".[all]"` resolves and executes third-party package code during install |
| Test suite → local Docker daemon | The `-m ""` full-suite run starts chaos-lab containers on the developer machine |
| Investigation artifacts → public repo | `.planning/` is gitignored on this public repo; findings stay on disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-14 | Tampering | Parity venv contaminated with excluded extras, re-creating the blind spot | mitigate | Task 1 asserts the pip-list grep for impacket/pysnmp/pymodbus/bacpypes3/schemathesis/openapi-spec-validator returns 0 and rebuilds the venv if not |
| T-150-15 | Repudiation | Claiming a root cause for the /api/sensor/push failure without evidence | mitigate | D-17 forbids speculative skips; Task 3 requires a per-step command/outcome record in 150-D17-INVESTIGATION.md before any disposition |
| T-150-16 | Denial of Service | Full-suite `-m ""` run leaves chaos-lab containers running on the dev machine | accept | `tests/test_chaos_lab_idempotency.py` tears down via `./lab.sh down` in its own `finally`; residual containers are a local-only nuisance |
| T-150-SC | Tampering | pip installs | mitigate | No new packages are introduced by this plan — only the already-declared `.[all]` set from `pyproject.toml` plus `pytest`, identical to the CI job |
</threat_model>

<verification>
1. Parity venv exists at `$HOME/.cache/quirk-ci-parity-venv` with zero excluded-extras packages.
2. Full-suite failure inventory recorded and reconciled against 150-03-SUMMARY.md Categories A-H.
3. `test_package_manifest_version_is_4_1_0` deleted; `tests/test_v41_gap_closure.py` reports 2 passed.
4. `150-D17-INVESTIGATION.md` exists with a per-step evidence trail and one stated disposition.
5. `tests/test_skip_registry.py` still passes.
</verification>

<success_criteria>
- Every later plan in this phase can verify against a venv that actually matches CI's install.
- Categories G and H from the real CI run are resolved or evidenced, not guessed.
</success_criteria>

<output>
Create `.planning/phases/150-test-suite-green-baseline-ci-gate/150-04-SUMMARY.md` when done.
</output>
