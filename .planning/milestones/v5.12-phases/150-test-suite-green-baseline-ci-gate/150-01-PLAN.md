---
phase: 150-test-suite-green-baseline-ci-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/scanner/kerberos_scanner.py
  - tests/test_identity_scanner_hardening.py
  - tests/skip_registry.py
  - docs/test-triage-149.md
autonomous: true
requirements: [SUITE-02]

must_haves:
  truths:
    - "quirk's Kerberos scanner builds a valid AS-REQ on the currently-pinned impacket >=0.13.0,<0.14 instead of raising pyasn1 KeyError('Bad BitString initializer type')"
    - "tests/test_identity_scanner_hardening.py::test_kdc_udp_decode_failure_logs and ::test_build_as_req_nonce_uses_secrets pass as real (non-xfail) tests in an impacket-installed environment"
    - "The skip-registry meta-gate (tests/test_skip_registry.py::test_no_unregistered_skips) stays green after the marker and registry deletions"
    - "docs/test-triage-149.md no longer claims those two tests are quarantined, and records the Phase 150 D-05 fix without rewriting Phase 149 history"
  artifacts:
    - path: "quirk/scanner/kerberos_scanner.py"
      provides: "impacket-version-tolerant kdc-options construction in _build_as_req"
      contains: "Phase 150 D-05"
    - path: "tests/test_identity_scanner_hardening.py"
      provides: "Two un-quarantined Kerberos tests (no xfail decorators)"
    - path: "tests/skip_registry.py"
      provides: "ALLOWED_SKIPS with the 2 pre_existing_triage_149 KDCOptions rows removed"
    - path: "docs/test-triage-149.md"
      provides: "Ledger rows + reconciliation addendum reflecting the Phase 150 D-05 fix"
  key_links:
    - from: "quirk/scanner/kerberos_scanner.py::_build_as_req"
      to: "impacket.krb5.constants"
      via: "encodeFlags / enum-shape-tolerant flag construction"
      pattern: "encodeFlags"
---

<objective>
Fix the impacket 0.13.0 `KDCOptions` class-to-enum incompatibility in the Kerberos scanner (D-05),
then un-quarantine the two tests Phase 149 xfail-marked because of it, remove their skip-registry
rows, and update the Phase 149 disposition ledger so it stops claiming they are quarantined.

Purpose: this is a genuine currently-shipping defect — on the current pin, every Kerberos AS-REQ
build raises inside `_build_as_req`, so Kerberos etype enumeration is broken for real operators.
Leaving `xfail(strict=False)` in place after a genuine fix would let a future regression in this
exact code path go uncaught, defeating this phase's gate.
Output: fixed scanner, 2 permanently CI-enforced tests, cleaned registry, corrected ledger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/150-test-suite-green-baseline-ci-gate/150-CONTEXT.md
@.planning/phases/150-test-suite-green-baseline-ci-gate/150-PATTERNS.md
@quirk/scanner/kerberos_scanner.py
@tests/test_identity_scanner_hardening.py
@tests/skip_registry.py

<interfaces>
<!-- Contracts the executor needs. Do not go exploring for these. -->

quirk/scanner/kerberos_scanner.py (current state, lines 85-111):
  def _build_as_req(client_name, server_name, realm: str)
    - line 96-98 is the defect: `req_body['kdc-options'] = constants.KDCOptions(constants.KDCOptions.forwardable)`
    - called from `_probe_kdc` (line 126) and `_probe_kdc_udp` (line 172)
    - module-level `IMPACKET_AVAILABLE` bool guards all impacket use (lines 3-22)
    - lines 6-13 hold the sibling already-applied fix for the same defect class
      (`METHOD_DATA as MethodData` nested try/except) — this is the house pattern to mirror

tests/skip_registry.py:
  ALLOWED_SKIPS: list[tuple[str, int, str, str]]  # (file_rel_to_tests, lineno, category, reason)
  categories in {"optional_extra", "live_infra", "pre_existing_triage_149"}

tests/test_skip_registry.py:
  test_no_unregistered_skips()  # AST walker, +/-2 line tolerance, marker names {skipif, skip, xfail}
</interfaces>

**Environment note:** impacket is NOT installed in this repo's working environment (`quirk[all]`
intentionally excludes the `identity` extra — pyproject.toml lines 129-132, Phase 45 D-01). Do NOT
install impacket into the primary dev environment; it pulls pyOpenSSL and downgrades cryptography,
breaking the TLS scanner. Verify the fix in a throwaway venv only (Task 1).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make _build_as_req tolerant of impacket 0.13.0's KDCOptions enum</name>
  <files>quirk/scanner/kerberos_scanner.py</files>
  <read_first>
    - quirk/scanner/kerberos_scanner.py (lines 1-115 — the import guard at 3-22 is the pattern to mirror; the defect is at 96-98)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-PATTERNS.md (section "quirk/scanner/kerberos_scanner.py::_build_as_req (D-05 bug fix)" — root cause + house fix pattern)
    - pyproject.toml lines 129-132 (why impacket is excluded from [all])
  </read_first>
  <action>
Replace the `req_body['kdc-options']` assignment at lines 96-98 of `_build_as_req` with an
impacket-shape-tolerant construction, keeping the same semantic intent (advertise the
`forwardable` KDC option only).

Before writing the fix, introspect the pinned impacket in a throwaway venv to pick the correct
branch. Create the venv under the session scratchpad directory (NOT in the repo, NOT the active
dev env):
  - `python3 -m venv <scratchpad>/impacket-venv`
  - `<scratchpad>/impacket-venv/bin/pip install -e .` then
    `<scratchpad>/impacket-venv/bin/pip install "impacket>=0.13.0,<0.14" pytest`
  - if `tests/conftest.py` fails to import in that venv for missing optional deps, re-run the
    install as `pip install -e ".[all]"` in the same venv and continue.
  - introspect with `python -c` printing: `hasattr(constants, "encodeFlags")`,
    `type(constants.KDCOptions)`, `isinstance(constants.KDCOptions, type) and issubclass(constants.KDCOptions, enum.Enum)`,
    and `constants.KDCOptions.forwardable` / `.value` — import path is `from impacket.krb5 import constants`.

Target implementation shape:
  - Preferred path when `constants.encodeFlags` exists: build the bit string via
    `constants.encodeFlags([constants.KDCOptions.forwardable.value])` — this is impacket's own
    idiom for turning KDCOption enum members into the pyasn1 BitString that `kdc-options` expects.
  - Legacy fallback for impacket <0.13 (where `KDCOptions` is still a bit-flag helper class):
    keep the original `constants.KDCOptions(constants.KDCOptions.forwardable)` construction,
    selected by capability detection (presence of `encodeFlags` / enum-ness of `KDCOptions`), not
    by version-string parsing.
  - If the introspection shows `encodeFlags` is absent on the pinned version, fall back to
    constructing the flag value from `constants.KDCOptions.forwardable.value` directly against the
    pyasn1 BitString API — the non-negotiable requirement is that `_build_as_req` returns a valid
    AS_REQ on `impacket>=0.13.0,<0.14` without raising.

Add an inline comment immediately above the construction, in the exact citation style of the
existing lines 6-13 comment block, beginning with `# Phase 150 D-05:` and naming the root cause
(impacket 0.13.0 changed `constants.KDCOptions` from a bit-flag helper class to a plain
`enum.Enum`, so calling it as a constructor raises pyasn1 `KeyError('Bad BitString initializer
type')`).

Do NOT change `_probe_kdc`, `_probe_kdc_udp`, `ALL_ETYPES`, the nonce line (109), or the module
import guard. Do NOT add impacket to any extras group in pyproject.toml. Delete the throwaway venv
when done.
  </action>
  <verify>
    <automated>&lt;scratchpad&gt;/impacket-venv/bin/python -m pytest tests/test_identity_scanner_hardening.py -q -m "" -p no:cacheprovider</automated>
    <automated>python -m compileall quirk/scanner/kerberos_scanner.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Phase 150 D-05" quirk/scanner/kerberos_scanner.py` returns at least one line
    - `grep -v '^\s*#' quirk/scanner/kerberos_scanner.py | grep -c "constants.KDCOptions(constants.KDCOptions.forwardable)"` returns 0 OR that call appears only inside the legacy `<0.13` fallback branch (verified by reading the surrounding branch condition)
    - In the throwaway impacket venv: `python -c "from quirk.scanner import kerberos_scanner as k; from impacket.krb5 import constants; from impacket.krb5.types import Principal; p=Principal('nobody', type=constants.PrincipalNameType.NT_PRINCIPAL.value); s=Principal('krbtgt/EXAMPLE.COM', type=constants.PrincipalNameType.NT_SRV_INST.value); print(k._build_as_req(p,s,'EXAMPLE.COM') is not None)"` prints `True` and exits 0
    - In the same venv, `pytest tests/test_identity_scanner_hardening.py -q -m ""` reports 0 failed and 0 errors, and `test_build_as_req_nonce_uses_secrets` is reported as xpassed or passed (not failed/xfailed)
    - `python -m compileall quirk` exits 0 in the primary dev environment
    - `git diff --name-only` shows only `quirk/scanner/kerberos_scanner.py` changed by this task
  </acceptance_criteria>
  <done>`_build_as_req` returns a valid AS_REQ under impacket 0.13.0 with no pyasn1 KeyError, and the legacy impacket path is preserved behind capability detection.</done>
</task>

<task type="auto">
  <name>Task 2: Un-quarantine the two Kerberos tests and delete their skip-registry rows</name>
  <files>tests/test_identity_scanner_hardening.py, tests/skip_registry.py</files>
  <read_first>
    - tests/test_identity_scanner_hardening.py (lines 78-150 — the two `@pytest.mark.xfail(...)` decorator blocks at 85-95 and 114-119, plus the `_kerb_mod` fixture at ~78-83)
    - tests/skip_registry.py (module docstring lines 1-13 for the deletion convention; rows at lines 55, 202, 203)
    - tests/test_skip_registry.py (the AST walker `test_no_unregistered_skips`, marker set and +/-2 line tolerance)
  </read_first>
  <action>
Delete both `@pytest.mark.xfail(strict=False, reason="Phase 149-11: ...")` decorator blocks in
`tests/test_identity_scanner_hardening.py` in their entirety — the block above
`def test_kdc_udp_decode_failure_logs(_kerb_mod, caplog)` and the block above
`def test_build_as_req_nonce_uses_secrets(_kerb_mod)`. The `def` lines, docstrings and test bodies
stay byte-identical. Keep the top-level `import pytest` — it is still needed by the `_kerb_mod`
fixture's `pytest.importorskip("impacket")` and other markers in the file.

In `tests/skip_registry.py`, delete exactly two tuple rows from `ALLOWED_SKIPS` (currently lines
202 and 203), both with category `pre_existing_triage_149` and file
`"test_identity_scanner_hardening.py"` at line numbers 85 and 114. Delete the whole rows — the
registry convention (docstring lines 6-8) is that un-registering means removing the tuple, never
changing its category string.

Do NOT touch the row at line 55:
`("test_identity_scanner_hardening.py", 80, "optional_extra", "impacket not installed")` — that
registers the `pytest.importorskip("impacket")` fixture skip, which remains correct because
`quirk[all]` still excludes the `identity` extra (D-01), so these tests still take the skip path in
CI. Deleting the decorator blocks shifts line numbers only BELOW line 85, so the line-80 entry is
unaffected; re-verify after the edit that no other `ALLOWED_SKIPS` row referencing
`test_identity_scanner_hardening.py` now points at a shifted marker line.
  </action>
  <verify>
    <automated>python -m pytest tests/test_skip_registry.py -q -m ""</automated>
    <automated>python -m pytest tests/test_identity_scanner_hardening.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pre_existing_triage_149" tests/skip_registry.py` returns exactly 2 fewer than before the edit (record both counts in the SUMMARY)
    - `grep -n "test_identity_scanner_hardening.py" tests/skip_registry.py` returns exactly one row, and it is the `("test_identity_scanner_hardening.py", 80, "optional_extra", "impacket not installed")` row
    - `grep -n "xfail" tests/test_identity_scanner_hardening.py` returns no lines
    - `pytest tests/test_skip_registry.py -q -m ""` reports `1 passed`
    - `pytest tests/test_identity_scanner_hardening.py -q -m ""` reports 0 failed in the primary dev env (the 2 Kerberos tests skip there via `importorskip`, which is expected and registered)
    - Re-running the Task 1 throwaway-venv command `pytest tests/test_identity_scanner_hardening.py -q -m ""` reports 0 failed and 0 xfailed
  </acceptance_criteria>
  <done>Both xfail decorators and both `pre_existing_triage_149` registry rows are gone; the skip-registry meta-gate is green; the two tests are permanently enforced wherever impacket is installed.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the Phase 149 disposition ledger for the fixed tests</name>
  <files>docs/test-triage-149.md</files>
  <read_first>
    - docs/test-triage-149.md (the Cluster-9 disposition table rows for `test_kdc_udp_decode_failure_logs` and `test_build_as_req_nonce_uses_secrets`, around lines 163-164; the `### What changed` reconciliation narrative around lines 449-464; the `### Ledger integrity checks` section near line 496)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-CONTEXT.md (D-05, including the note that these tests still skip in the new CI job because `[all]` excludes `identity`)
  </read_first>
  <action>
Update the two ledger rows for
`tests/test_identity_scanner_hardening.py::test_kdc_udp_decode_failure_logs` and
`::test_build_as_req_nonce_uses_secrets`:
  - Disposition column: change `quarantined-xfail` / `superseded by Plan 11 — ... residual
    quarantined-xfail` wording to state the tests are **fixed in Phase 150 (D-05)**.
  - Final column ("Registered in skip_registry.py?"): change from `yes (tests/skip_registry.py,
    ...)` to a `no` statement noting the fix landed and the xfail + registry entries were removed.
  - Keep the Sub-reason / evidence prose intact — the rows are provenance and must not be deleted
    or historically rewritten.

Append (do not overwrite) a short new subsection near the Reconciliation section titled
`### Phase 150 follow-up: KDCOptions fixed` stating: the impacket 0.13.0 `KDCOptions` enum
incompatibility in `quirk/scanner/kerberos_scanner.py::_build_as_req` was fixed under Phase 150
D-05; both tests' `xfail` markers and both `pre_existing_triage_149` `ALLOWED_SKIPS` rows were
removed; the tests are now permanently enforced; and — importantly for future readers — they still
take the `pytest.importorskip("impacket")` skip path in the new Linux CI job itself, because
`quirk[all]` intentionally excludes the `identity` extra (pyproject.toml lines 129-132, Phase 45
D-01, Phase 150 D-01), so the CI gate does not exercise them.

Do not restate stale run counts (3088 passed / 42 skipped / 81 xfailed) as current truth anywhere
in the new text; those numbers belong to the Phase 149 historical narrative only. Do not modify the
`### Ledger integrity checks` row-count assertions — the row count is unchanged (rows are edited,
not removed).
  </action>
  <verify>
    <automated>grep -c "Phase 150" docs/test-triage-149.md</automated>
    <automated>python -m pytest tests/ -q -m "" -k "docs_presence"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Phase 150 follow-up: KDCOptions fixed" docs/test-triage-149.md` returns exactly one line
    - `grep -n "test_build_as_req_nonce_uses_secrets" docs/test-triage-149.md` still returns its original ledger row (row not deleted) and that row no longer contains the string `quarantined-xfail` as its disposition
    - The updated rows contain no `yes (tests/skip_registry.py, \`test_identity_scanner_hardening.py:85\`)` or `:114` claim
    - The new subsection explicitly mentions both `identity` extra exclusion and that the CI job does not exercise these two tests
    - Ledger table row count is unchanged: the total distinct Test ID count stated in `### Ledger integrity checks` still matches the tables
  </acceptance_criteria>
  <done>The ledger accurately reports the two tests as fixed-and-unquarantined, with the CI-does-not-exercise-them caveat recorded, and Phase 149 history preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KDC network response → `_probe_kdc` / `_probe_kdc_udp` | Untrusted ASN.1 bytes from a scanned host cross into pyasn1 decoding |
| Third-party package (impacket) → scanner import surface | Version-shape drift changes runtime behavior of a security scanner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-01 | Denial of Service | `quirk/scanner/kerberos_scanner.py::_build_as_req` | mitigate | The current defect makes every Kerberos probe raise; the Task 1 fix restores function and is capability-detected so neither impacket branch can raise at build time. Verified by the throwaway-venv AS-REQ construction assertion. |
| T-150-02 | Tampering | impacket version-shape drift silently flipping `IMPACKET_AVAILABLE` | mitigate | Task 2 removes `xfail(strict=False)` so a future regression in this exact path fails loudly instead of being absorbed; the pin `impacket>=0.13.0,<0.14` in pyproject.toml is unchanged by this plan. |
| T-150-03 | Elevation of Privilege | throwaway venv installing `identity` extras into the dev environment | mitigate | Install impacket ONLY into a scratchpad venv, never the primary env (pyOpenSSL→cryptography downgrade chain, Phase 45 D-01); venv deleted after Task 1; `tests/test_install_all_excludes_impacket.py` guards the extras boundary. |
| T-150-04 | Information Disclosure | ledger/doc edits leaking host or credential detail | accept | `docs/test-triage-149.md` edits are disposition metadata only; no scan output, hostnames, or secrets are introduced. |
| T-150-SC | Tampering | pip installs (`impacket>=0.13.0,<0.14`) | mitigate | impacket is already a declared, pinned dependency of the existing `identity` extra in pyproject.toml — no new package is introduced by this plan, so no new legitimacy checkpoint is required. Install occurs in an isolated, deleted-after-use venv. |
</threat_model>

<verification>
1. `python -m compileall quirk tests` exits 0.
2. `pytest tests/test_skip_registry.py -q -m ""` → 1 passed.
3. `pytest tests/test_identity_scanner_hardening.py -q -m ""` → 0 failed in the primary env.
4. Throwaway impacket venv: `pytest tests/test_identity_scanner_hardening.py -q -m ""` → 0 failed, 0 xfailed.
5. `pytest tests/test_install_all_excludes_impacket.py tests/test_install_all_excludes_pysnmp.py -q -m ""` → passed (extras boundary intact).
6. `grep -n "test_identity_scanner_hardening.py" tests/skip_registry.py` → exactly the one `optional_extra` row.
</verification>

<success_criteria>
- `_build_as_req` produces a valid AS_REQ under the pinned impacket with no pyasn1 KeyError.
- Zero `xfail` markers remain in `tests/test_identity_scanner_hardening.py`.
- Zero `pre_existing_triage_149` rows for that file remain in `tests/skip_registry.py`.
- `docs/test-triage-149.md` records the fix and no longer claims those tests are quarantined.
- No pyproject.toml extras change; impacket never installed into the primary dev environment.
</success_criteria>

<output>
Create `.planning/phases/150-test-suite-green-baseline-ci-gate/150-01-SUMMARY.md` when done.
</output>
