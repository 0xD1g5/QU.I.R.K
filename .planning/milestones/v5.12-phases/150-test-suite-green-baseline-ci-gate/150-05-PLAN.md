---
phase: 150-test-suite-green-baseline-ci-gate
plan: 05
type: execute
wave: 2
depends_on: [150-04]
files_modified:
  - quantum-chaos-enterprise-lab/lab.sh
  - tests/test_lab_profile_certs.py
  - quantum-chaos-enterprise-lab/README.md
  - quantum-chaos-enterprise-lab/expected_results_v4.md
  - labs/email/README.md
  - labs/email/expected_results.md
  - labs/grpc-tls/README.md
  - docs/chaos-lab.md
  - CONTRIBUTING.md
autonomous: true
requirements: [SUITE-02, SUITE-03]

must_haves:
  truths:
    - "A fresh clone with no certs can run the email and grpc-tls chaos-lab profiles without a manual `make certs` step"
    - "`tests/test_chaos_lab_idempotency.py::test_profile_re_up_is_idempotent[email]` no longer fails on a Docker-enabled runner because of a missing bind-mount source"
    - "lab.sh remains a faithful reflection of the lab's current state per CLAUDE.md's Chaos Lab Maintenance rule — README, expected-results oracles and docs/chaos-lab.md all describe the new automatic cert generation"
    - "A contributor reproducing CI locally with `pytest -q -m \"\"` is warned that Docker containers will start"
  artifacts:
    - path: "quantum-chaos-enterprise-lab/lab.sh"
      provides: "Idempotent per-profile cert generation for labs/email and labs/grpc-tls, invoked from up/all/reset and exposed as `./lab.sh certs`"
      contains: "ensure_profile_certs"
    - path: "tests/test_lab_profile_certs.py"
      provides: "Automated regression proving the generator materializes all six cert files idempotently"
      contains: "dovecot"
  key_links:
    - from: "quantum-chaos-enterprise-lab/lab.sh"
      to: "labs/email/certs/dovecot.key"
      via: "openssl generation in ensure_profile_certs, called before compose up"
      pattern: "labs/email/certs"
    - from: "quantum-chaos-enterprise-lab/docker-compose.yml"
      to: "labs/email/certs/dovecot.key"
      via: "dovecot-email read-only bind mount"
      pattern: "labs/email/certs/dovecot"
---

<objective>
Close Category E: the `email` chaos-lab profile fails to start in CI because
`labs/email/certs/dovecot.{key,crt}` are gitignored and nothing generates them automatically, and
`labs/grpc-tls/` has the identical latent gap (D-12, D-13). Extend `lab.sh` so both profiles'
certs are materialized on demand, prove it with a test, and bring every chaos-lab doc surface into
line with CLAUDE.md's Chaos Lab Maintenance rule. Also add D-14's Docker warning to CONTRIBUTING.md.

Purpose: `ubuntu-latest` runners have Docker preinstalled, so D-04's `-m ""` override genuinely
spins up chaos-lab profiles. Docker creates an empty directory for a missing bind-mount source and
then fails to mount that directory onto a file destination, producing the "not a directory" runc
error seen in the real run.
Output: a first-run-safe lab, a regression test, and synchronized docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/150-test-suite-green-baseline-ci-gate/150-CONTEXT.md
@.planning/phases/150-test-suite-green-baseline-ci-gate/150-03-SUMMARY.md
@CLAUDE.md
@CONTRIBUTING.md

<interfaces>
<!-- Verified facts. Do not re-derive. -->

`quantum-chaos-enterprise-lab/lab.sh` structure:
  line 76  `ensure_lab_certs()` — generates ONLY the top-level `quantum-chaos-enterprise-lab/certs/`
           pair (ca.key/ca.crt, client.key/client.crt) via openssl, idempotently, guarded by
           `if [[ ! -f ... ]]`. It does not touch any `labs/*/certs/` path.
  line 173 `up)` arm calls `ensure_lab_certs` after `_validate_pinned_tags`
  line 184 `all)` arm calls `ensure_lab_certs`
  line 230 `reset)` arm runs `compose --profile "*" down -v` then `compose up -d` and calls NO cert
           function at all — it hits the same missing-mount failure.
  Usage text listing commands lives near lines 20-55.

Bind mounts that need the files (`quantum-chaos-enterprise-lab/docker-compose.yml`):
  line 1032-1033  ../labs/email/certs/postfix.crt, ../labs/email/certs/postfix.key
  line 1057-1058  ../labs/email/certs/dovecot.crt, ../labs/email/certs/dovecot.key
  line 1287-1288  ../labs/grpc-tls/certs/grpc-tls.crt, ../labs/grpc-tls/certs/grpc-tls.key

Existing per-lab Makefile `certs` targets already define the canonical openssl invocations — reuse
them verbatim rather than inventing new parameters:
  labs/email/Makefile      → `openssl req -x509 -newkey rsa:2048 -keyout certs/postfix.key -out
                              certs/postfix.crt -days 3650 -nodes -subj "/CN=postfix.chaos.local"`
                              and the same for dovecot with `/CN=dovecot.chaos.local`;
                              then `chmod 644 certs/*.crt` and `chmod 600 certs/*.key`
  labs/grpc-tls/Makefile   → `openssl req -x509 -newkey rsa:2048 -keyout certs/grpc-tls.key -out
                              certs/grpc-tls.crt -days 3650 -nodes -subj "/CN=grpc-tls.chaos.local"`;
                              then `chmod 644 certs/grpc-tls.crt` and `chmod 640 certs/grpc-tls.key`

`labs/email/.gitignore` and `labs/grpc-tls/.gitignore` both exclude `certs/*.crt` and `certs/*.key`,
so generated files stay untracked. The lab-wide gitignore audit is complete: only these two profiles
have gitignored certs without an automatic generator (D-12/D-13 scope is exactly 2 profiles).

`tests/test_chaos_lab_idempotency.py` runs `./lab.sh up` with `PROFILE_ARGS="--profile <name>"`
from `quantum-chaos-enterprise-lab/` as cwd, once per discovered profile, twice each.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add idempotent per-profile cert generation to lab.sh (D-12, D-13)</name>
  <files>quantum-chaos-enterprise-lab/lab.sh, tests/test_lab_profile_certs.py</files>
  <behavior>
    - Running `./lab.sh certs` from `quantum-chaos-enterprise-lab/` on a tree with no profile certs creates all six files: labs/email/certs/{postfix,dovecot}.{key,crt} and labs/grpc-tls/certs/grpc-tls.{key,crt}
    - Each generated `.crt` is a parseable X.509 certificate whose subject CN is postfix.chaos.local, dovecot.chaos.local, or grpc-tls.chaos.local respectively
    - Running `./lab.sh certs` a second time is a no-op: the six files' contents are byte-identical to the first run (idempotent, matching ensure_lab_certs' contract)
    - `./lab.sh certs` succeeds without Docker running — it must not call `compose` or `_validate_pinned_tags`
    - `lab.sh` passes `bash -n` syntax checking
  </behavior>
  <read_first>
    - quantum-chaos-enterprise-lab/lab.sh lines 1-110 (header, usage text, `compose()` helper, and the full `ensure_lab_certs()` body — the style, comment voice, and idempotency guard to mirror)
    - quantum-chaos-enterprise-lab/lab.sh lines 164-240 (the `case` dispatch: `up`, `all`, `down`, `reset` arms and where cert calls belong)
    - labs/email/Makefile and labs/grpc-tls/Makefile (the canonical openssl invocations, CNs, and chmod modes to reuse verbatim)
    - quantum-chaos-enterprise-lab/docker-compose.yml lines 1015-1060 and 1273-1290 (the exact bind-mount source paths that must exist)
    - tests/test_chaos_lab_idempotency.py lines 55-75 (how `_up()` invokes `./lab.sh up` with cwd and PROFILE_ARGS)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-CONTEXT.md (D-12, D-13)
  </read_first>
  <action>
Write `tests/test_lab_profile_certs.py` FIRST, encoding the five behaviors above, then implement
until it passes.

Test file constraints: it must contain no `pytest.skip`, `pytest.importorskip`,
`@pytest.mark.skip`, `@pytest.mark.skipif` or `@pytest.mark.xfail` anywhere — any of those would
require a `tests/skip_registry.py` entry and this file must run unconditionally on both macOS and
`ubuntu-latest` (openssl and bash are present on both). Do not mark it `slow`; it must be collected
under both the default `-m 'not slow'` invocation and CI's `-m ""`. It must not require Docker.
Invoke the script as `subprocess.run(["./lab.sh", "certs"], cwd=<lab dir>)` and assert on
`returncode == 0`. Parse each generated certificate with
`openssl x509 -noout -subject -in <path>` (or `cryptography.x509.load_pem_x509_certificate`, which
is a base dependency) to assert the CN. For idempotency, hash the six files after run one and after
run two and assert equality. Do not delete a developer's pre-existing certs as a test side effect:
if the files already exist when the test starts, run the command twice and assert only idempotency
plus CN correctness; only assert the create-from-nothing behavior when generating into a temp copy
of the labs tree or when the files were absent at test start.

Then implement in `quantum-chaos-enterprise-lab/lab.sh`:

Add a new function `ensure_profile_certs()` immediately after `ensure_lab_certs()`, written in the
same voice: a comment block explaining that `labs/email/certs/*` and `labs/grpc-tls/certs/*` are
gitignored (`labs/*/.gitignore`), that the `dovecot-email`, `postfix-email` and `grpc-tls` compose
services bind-mount them read-only, and that Docker turns a missing bind-mount source into an empty
directory and then fails the mount — which is exactly the CI failure this closes. Resolve the labs
root relative to the script as `"$(dirname "$0")/../labs"`. For each of the three cert pairs, guard
with `if [[ ! -f <key> || ! -f <crt> ]]`, `mkdir -p` the certs directory, echo a one-line
first-run-regen message matching `ensure_lab_certs`' existing emoji/message style, then run the
exact openssl invocation from that profile's Makefile with the same CN, key size, `-days 3650`,
`-nodes`, and the same chmod modes (644 for crt; 600 for the email keys, 640 for the grpc-tls key).
Suppress openssl chatter with `>/dev/null 2>&1` as `ensure_lab_certs` does. The function must be
re-entrant and produce no output when all six files already exist.

Wire it into the dispatch:
  - call `ensure_profile_certs` immediately after each existing `ensure_lab_certs` call in the `up`
    and `all` arms;
  - add both `ensure_lab_certs` and `ensure_profile_certs` to the `reset` arm before its
    `compose up -d`, since reset currently calls neither and would reproduce the same mount failure;
  - add a new `certs)` arm that calls `ensure_lab_certs` then `ensure_profile_certs`, prints a
    confirmation line, and exits without touching Docker;
  - add the `certs` command to the usage/help text alongside the existing commands.

Do not change `_validate_pinned_tags`, the `compose()` helper, `down`, `status`, `logs`, or the
`ALL_PROFILES` derivation. Do not commit any generated cert file — confirm they land under the
existing `labs/*/.gitignore` patterns.
  </action>
  <verify>
    <automated>bash -n quantum-chaos-enterprise-lab/lab.sh</automated>
    <automated>$HOME/.cache/quirk-ci-parity-venv/bin/python -m pytest tests/test_lab_profile_certs.py -q -m ""</automated>
    <automated>$HOME/.cache/quirk-ci-parity-venv/bin/python -m pytest tests/test_skip_registry.py -q -m ""</automated>
    <automated>git status --porcelain labs/ | grep -c "certs/"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n quantum-chaos-enterprise-lab/lab.sh` exits 0
    - `grep -c "ensure_profile_certs" quantum-chaos-enterprise-lab/lab.sh` returns at least `5` (definition + up + all + reset + certs arm)
    - `grep -v '^#' quantum-chaos-enterprise-lab/lab.sh | grep -c "labs/email/certs"` is at least 1 and the same for `labs/grpc-tls/certs`
    - Running `./lab.sh certs` from `quantum-chaos-enterprise-lab/` exits 0 and all six files exist: labs/email/certs/postfix.key, postfix.crt, dovecot.key, dovecot.crt, labs/grpc-tls/certs/grpc-tls.key, grpc-tls.crt
    - `openssl x509 -noout -subject -in labs/email/certs/dovecot.crt` prints a subject containing `dovecot.chaos.local`
    - `pytest tests/test_lab_profile_certs.py -q -m ""` reports `0 failed` in the parity venv
    - `pytest tests/test_skip_registry.py -q -m ""` reports `1 passed` (the new test file introduced no unregistered skip markers)
    - `git status --porcelain labs/ | grep -c "certs/"` returns `0` (generated certs remain untracked)
    - `grep -c "certs" quantum-chaos-enterprise-lab/lab.sh` shows the new command listed in the usage text block
  </acceptance_criteria>
  <done>A fresh clone can start the email and grpc-tls profiles with no manual step, the generator is idempotent and Docker-free, and a non-skipping regression test proves it.</done>
</task>

<task type="auto">
  <name>Task 2: Sync every chaos-lab doc surface to the new cert behavior (CLAUDE.md Chaos Lab Maintenance)</name>
  <files>quantum-chaos-enterprise-lab/README.md, quantum-chaos-enterprise-lab/expected_results_v4.md, labs/email/README.md, labs/email/expected_results.md, labs/grpc-tls/README.md, docs/chaos-lab.md</files>
  <read_first>
    - CLAUDE.md (the "Chaos Lab Maintenance" rule and the "Per-Phase Documentation Checklist" row for chaos-lab changes, plus the LIVE-03 Obsidian source→vault mapping table)
    - quantum-chaos-enterprise-lab/README.md around line 59 (email row) and line 67 (grpc-tls row) in the profile table
    - docs/chaos-lab.md §2 "Quick Start" (line ~25) and §3.18 "email Profile" (line ~538, including the `PROFILE_ARGS="--profile email" ./lab.sh up` snippet at ~555 and the `labs/email/expected_results.md` pointer at ~569)
    - docs/chaos-lab.md §3.19 broker Profile (~line 588) — the existing "Generate TLS certs before first boot: cd labs/broker && make certs" wording is the phrasing pattern being superseded for email/grpc-tls
    - labs/email/README.md and labs/grpc-tls/README.md (their prerequisite / `make certs` sections)
    - labs/email/expected_results.md and quantum-chaos-enterprise-lab/expected_results_v4.md (their `#profile-email` and `#profile-grpc-tls` sections)
  </read_first>
  <action>
Update each doc surface so it describes the lab's actual current state. Every edit is small and
factual — do not restructure or rewrite these documents.

In `docs/chaos-lab.md`: in §2 Quick Start, add a sentence stating that `./lab.sh up`, `./lab.sh all`
and `./lab.sh reset` now generate any missing self-signed profile certs automatically (email and
grpc-tls), and that `./lab.sh certs` regenerates them on demand without starting containers. In
§3.18 (email Profile), replace any implication that a manual `make certs` is required with a note
that `lab.sh` materializes `labs/email/certs/{postfix,dovecot}.{crt,key}` on first `up`; keep the
existing port table, findings list and expected-results pointer intact. Leave §3.19 (broker) alone —
broker's certs are out of D-12/D-13 scope.

In `quantum-chaos-enterprise-lab/README.md`: extend the `email` and `grpc-tls` rows' notes column
(or add a short note directly beneath the profile table) stating certs are auto-generated by
`lab.sh`, and document the new `certs` command wherever the README lists lab.sh commands.

In `labs/email/README.md` and `labs/grpc-tls/README.md`: state that `make certs` remains available
for manual regeneration but is no longer a prerequisite because `lab.sh` generates the same files
with the same CNs and permissions.

In `labs/email/expected_results.md` and `quantum-chaos-enterprise-lab/expected_results_v4.md`:
update the email and grpc-tls sections' prerequisite lines the same way. Do not change any expected
finding, port, severity or requirement ID — the observable scanner findings are unchanged by this
plan.

Then perform the LIVE-03 Obsidian sync required by CLAUDE.md: write the updated `docs/chaos-lab.md`
to the vault at `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Chaos-Lab.md`, preserving the
note's existing frontmatter block (project, type, status, source, updated) with `updated` set to
today's date and `source: docs/chaos-lab.md`. Write to the vault filesystem directly — the file is
too large for a CLI `content=` parameter.

Finally, re-run the doc-drift gates that read these files so no doc edit introduces a new failure.
  </action>
  <verify>
    <automated>grep -c "lab.sh certs\|./lab.sh certs" docs/chaos-lab.md quantum-chaos-enterprise-lab/README.md</automated>
    <automated>$HOME/.cache/quirk-ci-parity-venv/bin/python -m pytest tests/test_cli_correctness.py tests/test_phase135_docs_presence.py tests/test_phase136_docs_presence.py -q -m ""</automated>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Chaos-Lab.md" &amp;&amp; grep -c "source: docs/chaos-lab.md" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Chaos-Lab.md"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/chaos-lab.md` §2 and §3.18 both mention automatic cert generation and neither presents `make certs` as a required first step for the email profile
    - `quantum-chaos-enterprise-lab/README.md` documents the `certs` command and notes auto-generation for both the `email` and `grpc-tls` profiles
    - `labs/email/README.md`, `labs/grpc-tls/README.md`, `labs/email/expected_results.md` and `quantum-chaos-enterprise-lab/expected_results_v4.md` each state that certs are generated by `lab.sh` and that `make certs` is optional
    - No expected finding text, port number, severity or requirement ID was altered: `git diff --stat` on the two expected-results files shows only additions/edits within prerequisite prose
    - `pytest tests/test_cli_correctness.py tests/test_phase135_docs_presence.py tests/test_phase136_docs_presence.py -q -m ""` reports `0 failed`
    - The vault note `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Chaos-Lab.md` exists, carries `source: docs/chaos-lab.md` and today's `updated` date, and its body matches the updated `docs/chaos-lab.md`
  </acceptance_criteria>
  <done>lab.sh, both lab READMEs, both expected-results oracles, docs/chaos-lab.md and the Obsidian vault all describe the same lab, satisfying CLAUDE.md's Chaos Lab Maintenance and LIVE-03 rules.</done>
</task>

<task type="auto">
  <name>Task 3: Document the Docker side-effect of the CI-equivalent command in CONTRIBUTING.md (D-14)</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md (all of it — the "Running the test suite" and "What green means" sections written by plan 150-02, whose voice this addition must match)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-CONTEXT.md (D-14, and D-04 for why `-m ""` is the CI-equivalent command)
    - tests/test_chaos_lab_idempotency.py lines 1-50 (the `slow` marker + Docker-availability skipif that makes this conditional)
  </read_first>
  <action>
Add a short subsection to `CONTRIBUTING.md`, immediately after the "Running the test suite" section,
warning that `pytest -q -m ""` includes `slow`-marked chaos-lab profile tests which start Docker
containers when a Docker daemon is reachable — so a contributor reproducing CI locally should expect
containers to come up and be torn down. State that this is intentional (CI's `ubuntu-latest` runner
has Docker preinstalled, so those tests genuinely execute there), that the profile tests tear
themselves down via `./lab.sh down`, that `pytest -q` without the `-m ""` override deselects them,
and that `./lab.sh certs` can be run ahead of time to pre-generate the self-signed profile certs.

Keep it to a few sentences in the existing document's voice. Do not restructure the file, do not add
an Obsidian sync for `CONTRIBUTING.md` — per D-08 it is a standalone root file, not one of the
mapped `docs/` → vault pairs in CLAUDE.md's sync table.
  </action>
  <verify>
    <automated>grep -ci "docker" CONTRIBUTING.md</automated>
    <automated>$HOME/.cache/quirk-ci-parity-venv/bin/python -m pytest tests/test_cli_correctness.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "docker" CONTRIBUTING.md` returns at least `1`
    - `CONTRIBUTING.md` mentions both `pytest -q -m ""` starting chaos-lab containers and `./lab.sh certs`
    - The pre-existing sections ("Running the test suite", "What green means", "Why some tests are quarantined", "CI", "Before you open a PR") are all still present
    - No vault file was created for `CONTRIBUTING.md`
    - `pytest tests/test_cli_correctness.py -q -m ""` reports `0 failed`
  </acceptance_criteria>
  <done>Contributors reproducing CI locally know containers will start and how to pre-generate lab certs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lab.sh → local filesystem | The script writes private key material into `labs/*/certs/` on the developer or CI machine |
| Generated certs → chaos-lab containers | Self-signed keys are bind-mounted read-only into Postfix, Dovecot and the gRPC server |
| docs/ → public repo and Obsidian vault | Doc edits are published on a public repository and mirrored into the personal vault |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-17 | Information Disclosure | Generated private keys committed to a public repo | mitigate | `labs/email/.gitignore` and `labs/grpc-tls/.gitignore` already exclude `certs/*.key` and `certs/*.crt`; Task 1 asserts `git status --porcelain labs/ \| grep -c "certs/"` returns 0 |
| T-150-18 | Spoofing | Self-signed lab certs mistaken for trusted material | accept | These are deliberate chaos fixtures with `*.chaos.local` CNs, matching the existing tracked scenario certs; the same posture the 2026-05-27 review accepted for the lab's other scenario keys |
| T-150-19 | Tampering | Cert generation clobbering a developer's customized lab certs | mitigate | Every generation branch is guarded by an existence check, mirroring `ensure_lab_certs`; the regression test asserts byte-identical output on re-run |
| T-150-20 | Denial of Service | New `certs` arm accidentally starting or stopping containers | mitigate | The `certs` arm calls only the two cert functions and exits; Task 1 asserts it succeeds with no `compose` invocation |
| T-150-SC | Tampering | pip/npm/cargo installs | mitigate | This plan installs no packages; cert generation uses the system `openssl` already required by `ensure_lab_certs` |
</threat_model>

<verification>
1. `bash -n quantum-chaos-enterprise-lab/lab.sh` exits 0 and `./lab.sh certs` produces all six cert files.
2. `pytest tests/test_lab_profile_certs.py -q -m ""` is green in the CI-parity venv; `tests/test_skip_registry.py` still passes.
3. `git status --porcelain labs/` shows no cert files.
4. README, both lab READMEs, both expected-results oracles and `docs/chaos-lab.md` all describe automatic cert generation and the `certs` command.
5. The Obsidian `Guides/Chaos-Lab.md` note matches `docs/chaos-lab.md`.
6. `CONTRIBUTING.md` warns about Docker containers under `pytest -q -m ""`.
</verification>

<success_criteria>
- Category E's root cause is removed at the source, not skipped around.
- The Chaos Lab Maintenance rule in CLAUDE.md is satisfied for both touched profiles.
</success_criteria>

<output>
Create `.planning/phases/150-test-suite-green-baseline-ci-gate/150-05-SUMMARY.md` when done.
</output>
