---
phase: 150-test-suite-green-baseline-ci-gate
plan: 07
type: execute
wave: 3
depends_on: [150-05, 150-06]
files_modified:
  - docs/test-triage-149.md
  - docs/UAT-SERIES.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [SUITE-02, SUITE-03]

must_haves:
  truths:
    - "Every skip added by this phase is described in the public disposition ledger, not only in the registry"
    - "REQUIREMENTS.md no longer claims SUITE-02/SUITE-03 are Complete while the real CI baseline is red"
    - "ROADMAP.md lists the six plans this phase actually contains"
    - "UAT-SERIES.md carries test cases for the green baseline, the gate biting, and first-run lab cert generation"
  artifacts:
    - path: "docs/test-triage-149.md"
      provides: "Phase 150 CI-parity addendum documenting the ci_extras_gap and gitignored_planning_dir skips"
      contains: "ci_extras_gap"
    - path: "docs/UAT-SERIES.md"
      provides: "UAT-150-XX test cases for SUITE-02 / SUITE-03"
      contains: "UAT-150"
  key_links:
    - from: "tests/skip_registry.py"
      to: "docs/test-triage-149.md"
      via: "ledger rows describing each ci_extras_gap / gitignored_planning_dir entry"
      pattern: "ci_extras_gap"
---

<objective>
Bring the written record into line with what plans 150-04 through 150-06 actually changed, BEFORE
the final CI run captures the phase's evidence: document the new skips in the public ledger, correct
the stale SUITE-02/SUITE-03 status in REQUIREMENTS.md, fix the ROADMAP plan list, and add this
phase's UAT cases.

Purpose: doc edits can themselves break doc-presence and doc-drift gates, so they must land and be
verified before the green baseline run, not after it. And REQUIREMENTS.md currently claims both
requirements are Complete while the real `Linux Full Suite` job on `main` is red — exactly the class
of unverified-completion claim this milestone exists to eliminate.
Output: an accurate ledger, an honest requirements table, a correct roadmap, and UAT coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/150-test-suite-green-baseline-ci-gate/150-CONTEXT.md
@.planning/phases/150-test-suite-green-baseline-ci-gate/150-04-SUMMARY.md
@.planning/phases/150-test-suite-green-baseline-ci-gate/150-05-SUMMARY.md
@.planning/phases/150-test-suite-green-baseline-ci-gate/150-06-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Verified locations. Do not re-derive. -->

`docs/test-triage-149.md` structure: `## Cluster 1` through `## Cluster 9` sections, then a final
`## Reconciliation` section at line 368. Plan 150-01 already amended the two KDCOptions ledger rows.
The Phase 150 addendum is a NEW section appended after `## Reconciliation`, not an edit of the
Phase 149 cluster tables.

`.planning/REQUIREMENTS.md`:
  lines 45-49 — the `### Test Signal Integrity` checkbox list; SUITE-02 and SUITE-03 are `- [x]`
  lines 116-117 — traceability rows `| SUITE-02 | Phase 150 | Complete |` and
                  `| SUITE-03 | Phase 150 | Complete |`
Both are stale relative to ground truth: the real `Linux Full Suite` run
(https://github.com/0xD1g5/QU.I.R.K/actions/runs/31598809033) concluded `failure` with 38 failed.

`.planning/ROADMAP.md` lines ~175-194 — the `#### Phase 150` block. It currently says
`**Plans**: 3 plans` and lists 150-01 through 150-03 only, grouped under a `**Wave 1**` heading.

`docs/UAT-SERIES.md`: line 4 carries the document-wide `**Last Updated:**` line (currently naming
the Phase 149 wrap). Per-phase sections use the heading forms `## Phase NN: <Name> (UAT-NN-XX)` and
`## Phase NN — <Name>`; the newest sections are appended toward the end of the file. The file is
very large — locate the final phase section by heading grep, do not read the whole document.

Git tracking nuance — verify before assuming: `.gitignore:67` lists `.planning/`, but several root
`.planning/` files were tracked BEFORE that rule landed and gitignore does not apply to already-tracked
files. `git ls-files .planning` confirms `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`,
`.planning/STATE.md`, `PROJECT.md`, `HORIZON.md`, `MILESTONES.md` and `RETROSPECTIVE.md` ARE tracked
and MUST be committed, while `.planning/phases/**` IS ignored (`git check-ignore -v` confirms) and
cannot be. `docs/` files are tracked and must be committed.

Known repo gotcha: `gsd-tools.cjs commit --files` has falsely skipped files as gitignored in this
repo. Use plain `git add` + `git commit` for the docs commits in this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Phase 150 CI-parity addendum to the disposition ledger</name>
  <files>docs/test-triage-149.md</files>
  <read_first>
    - docs/test-triage-149.md lines 1-40 (header + cluster/row formatting) and lines 360-end (the `## Reconciliation` section this addendum follows)
    - tests/skip_registry.py (the Phase 150 block added by plan 150-06 — the authoritative list of new rows)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-06-SUMMARY.md (which tests were guarded and why)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-04-SUMMARY.md (the D-16 deletion and the D-17 disposition)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-CONTEXT.md (D-09, D-10, D-15, D-16 — rationale to summarize, not restate at length)
  </read_first>
  <action>
Append a new top-level section to `docs/test-triage-149.md`, after `## Reconciliation`, titled to
identify it as the Phase 150 CI-parity addendum. In the document's existing table voice, it must
cover:

  - Why the addendum exists: Phase 149's ledger and Plan 150-02's local baseline were both produced
    in sandboxes carrying `identity`, `hw` and `api` extras; CI installs `.[all]` only, which
    excludes all three by design, so 31 tests hard-crashed instead of skipping. Cite the real run
    URL https://github.com/0xD1g5/QU.I.R.K/actions/runs/31598809033 and its result line
    (38 failed, 3074 passed, 46 skipped, 73 xfailed, 7 xpassed).
  - A table of the `ci_extras_gap` skips: one row per guarded test with the test file, the test
    name, the extras group responsible (`hw`, `api`, `identity`), and the missing package.
  - A table of the four `gitignored_planning_dir` skips, naming
    `.planning/audit-2026-05-08/AUDIT-TASKS.md` and PUBREPO-01 as the reason the path is absent on a
    public clone or CI checkout.
  - A short note that `test_v41_gap_closure.py::TestV41GapClosure::test_package_manifest_version_is_4_1_0`
    was DELETED (D-16) rather than quarantined, with the one-line reason: it queried the
    distribution name `quirk`, which has not existed since the v4.10 rename to `quirk-scanner`.
  - A short note recording the D-17 `/api/sensor/push` disposition and a pointer to
    `.planning/phases/150-test-suite-green-baseline-ci-gate/150-D17-INVESTIGATION.md`; if that
    investigation deferred the disposition to the real CI run, say so plainly.
  - A one-line note that the `email` chaos-lab profile failure was fixed at the source in `lab.sh`
    (D-12/D-13), not skipped.

Derive every row from `tests/skip_registry.py` and the plan SUMMARYs — do not re-run the suite or
re-derive causes. Do not modify any Phase 149 cluster table or the Reconciliation section. Do not
state a passed/skipped/xfailed count as a target anywhere; those numbers drift.

Commit with plain git (`git add docs/test-triage-149.md` then `git commit`), not
`gsd-tools.cjs commit --files`.
  </action>
  <verify>
    <automated>grep -c "ci_extras_gap" docs/test-triage-149.md; grep -c "gitignored_planning_dir" docs/test-triage-149.md</automated>
    <automated>$HOME/.cache/quirk-ci-parity-venv/bin/python -m pytest tests/test_cli_correctness.py tests/test_phase135_docs_presence.py tests/test_phase136_docs_presence.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - `docs/test-triage-149.md` contains a Phase 150 addendum section positioned after `## Reconciliation`
    - `grep -c "ci_extras_gap" docs/test-triage-149.md` and `grep -c "gitignored_planning_dir" docs/test-triage-149.md` both return at least `1`
    - The addendum contains one ledger row per row in `tests/skip_registry.py`'s Phase 150 block — the counts match exactly
    - The addendum contains the run URL `https://github.com/0xD1g5/QU.I.R.K/actions/runs/31598809033`
    - The addendum names the deleted test `test_package_manifest_version_is_4_1_0` and the D-17 investigation artifact path
    - `git diff HEAD~1 --stat docs/test-triage-149.md` shows only additions after the Reconciliation section; no Phase 149 cluster row changed
    - `pytest tests/test_cli_correctness.py tests/test_phase135_docs_presence.py tests/test_phase136_docs_presence.py -q -m ""` reports `0 failed`
  </acceptance_criteria>
  <done>The public ledger describes every skip this phase introduced, plus the one deletion and the one source-level fix.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the stale SUITE-02 / SUITE-03 status and the ROADMAP plan list</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 40-55 (the Test Signal Integrity checkbox list) and lines 110-122 (the traceability table)
    - .planning/ROADMAP.md lines 170-200 (the Phase 150 block: goal, success criteria, `**Plans**:` count, wave grouping and plan checklist)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-03-SUMMARY.md (the evidence that the real CI baseline is red — why the current Complete status is false)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-CONTEXT.md canonical_refs (the note that REQUIREMENTS.md lines 45-49 / 116-117 are stale relative to ground truth)
  </read_first>
  <action>
Correct the record to match ground truth as of this moment — genuinely green CI has NOT yet been
observed, so neither requirement may claim Complete.

In `.planning/REQUIREMENTS.md`: flip the SUITE-02 and SUITE-03 checkboxes from `- [x]` to `- [ ]`,
and change both traceability rows from `Complete` to `In Progress`. Add a one-line note under the
Test Signal Integrity section recording that the first real CI attempt on 2026-08-12 concluded
failure with 38 failed, and that the phase is completing remediation. Do not touch SUITE-01
(Phase 149, genuinely complete) or any other requirement row.

In `.planning/ROADMAP.md`'s Phase 150 block: change `**Plans**: 3 plans` to `6 plans`, and extend
the plan checklist to list all six with a one-line objective each, preserving the block's existing
wave-grouping style. 150-01 and 150-02 stay `- [x]`. 150-03 stays `- [x]` with its objective
annotated as halted at its own stop condition and superseded by 150-08. Add 150-04 through 150-09
unchecked, with objectives: CI-parity venv plus the D-16/D-17 singletons; chaos-lab cert generation
plus doc sync; extras and planning-dir skip guards; ledger/requirements/UAT doc sync; live-fire CI
gate proof; phase closeout. Do not edit the phase Goal or the four Success Criteria — this
remediation does not change them.

Both files are tracked despite the `.gitignore:67` `.planning/` rule (gitignore does not apply to
already-tracked files) — confirm with `git ls-files .planning/REQUIREMENTS.md .planning/ROADMAP.md`,
then commit both with plain `git add` + `git commit`, not `gsd-tools.cjs commit --files`. Do not
attempt to add anything under `.planning/phases/`; that path is genuinely ignored.
  </action>
  <verify>
    <automated>grep -n "SUITE-02\|SUITE-03" .planning/REQUIREMENTS.md</automated>
    <automated>grep -c "150-0[4-9]-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SUITE-02" .planning/REQUIREMENTS.md` shows `- [ ]` in the checkbox list and `In Progress` in the traceability table; same for SUITE-03
    - `grep -c "SUITE-01" .planning/REQUIREMENTS.md` output is unchanged and SUITE-01 still reads `- [x]` / `Complete`
    - `.planning/ROADMAP.md`'s Phase 150 block reads `**Plans**: 6 plans`
    - `grep -c "150-0[4-9]-PLAN.md" .planning/ROADMAP.md` returns `6`
    - The Phase 150 Goal line and all four Success Criteria in ROADMAP.md are byte-unchanged
    - `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` are committed (`git log --oneline -1 -- .planning/ROADMAP.md` shows a new commit) and `git status --porcelain .planning/REQUIREMENTS.md .planning/ROADMAP.md` is empty
    - No attempt was made to commit anything under `.planning/phases/` (that path is genuinely gitignored)
  </acceptance_criteria>
  <done>The requirements table and roadmap describe the phase as it actually is: six plans, two requirements still in progress.</done>
</task>

<task type="auto">
  <name>Task 3: Add UAT-150 test cases and sync UAT-SERIES.md to the vault</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md lines 1-20 (document header and the `**Last Updated:**` line at line 4)
    - docs/UAT-SERIES.md — the last per-phase section in the file (locate by `grep -n "^## Phase\|^## Series" docs/UAT-SERIES.md | tail -5`), to copy its exact test-case formatting: case ID, requirement reference, steps, pass criteria, Result line
    - docs/UAT-SERIES.md §"Phase 56.1 — CI Staleness Gate Workflow" (line ~8031) — the closest analog: a CI-workflow UAT section whose cases assert a workflow exists, a gate trips, and a green run is visible in the Actions UI
    - CLAUDE.md ("Mandatory Phase Completion Steps" items 2, 3 and 4 — UAT-SERIES update, vault sync via direct filesystem write, and the commit)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-CONTEXT.md (D-07 live-fire, D-12 lab certs — the behaviors the new cases verify)
  </read_first>
  <action>
Append a new section for this phase, following the formatting of the most recent existing phase
section and using the Phase 56.1 CI-workflow section as the content analog. Add three cases:

  - UAT-150-01 (SUITE-02): the `Linux Full Suite` job concludes `success` on a real GitHub Actions
    run of `main`. Steps: open the run URL recorded in `150-CI-EVIDENCE.md`, confirm the job's green
    check and that its pytest summary line reports 0 failed. Pass criteria must reference the
    evidence artifact rather than hardcoding any passed/skipped/xfailed count, since those drift.
  - UAT-150-02 (SUITE-03): a deliberately failing test turns the same job red on a real PR run, and
    the PR is closed unmerged with its branch deleted. Steps: open the red-run URL from
    `150-CI-EVIDENCE.md`, confirm the failure names `tests/test_ci_gate_smoke.py`, confirm the PR is
    closed and not merged, and confirm `git ls-remote --heads origin ci/smoke-check-150` is empty.
  - UAT-150-03 (D-12/D-13): on a clone with no lab certs, `./lab.sh certs` (or a plain
    `PROFILE_ARGS="--profile email" ./lab.sh up`) generates
    `labs/email/certs/{postfix,dovecot}.{crt,key}` and `labs/grpc-tls/certs/grpc-tls.{crt,key}`,
    and the email profile starts without the runc "not a directory" mount error.

Leave every `**Result:**` line in the new cases as unfilled/pending — these are user-led
walkthroughs and only the user flips them. Then update the document-wide `**Last Updated:**` line at
line 4 to today's date with a short Phase 150 wrap description, preserving the existing "Earlier:"
chain style rather than deleting prior history.

Sync to the vault by writing directly to the filesystem, per CLAUDE.md step 3 — the file is far too
large for a CLI `content=` parameter. Build the vault copy by prepending the standard frontmatter
block (project QU.I.R.K., type reference, status active, `source: docs/UAT-SERIES.md`, `updated`
today) to the file contents and copying it to
`/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md`.

Commit `docs/UAT-SERIES.md` with plain `git add` + `git commit` using a
`docs(phase-150): update UAT-SERIES.md` style message. Do not use `gsd-tools.cjs commit --files`.
  </action>
  <verify>
    <automated>grep -c "UAT-150-0[123]" docs/UAT-SERIES.md</automated>
    <automated>head -6 docs/UAT-SERIES.md | grep -c "Last Updated"</automated>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" &amp;&amp; grep -c "UAT-150-01" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"</automated>
    <automated>$HOME/.cache/quirk-ci-parity-venv/bin/python -m pytest tests/test_cli_correctness.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "UAT-150-0[123]" docs/UAT-SERIES.md` returns at least `3`
    - Each of the three cases names its requirement (SUITE-02, SUITE-03) or decision (D-12/D-13) and has an unfilled `**Result:**` line
    - No case hardcodes a passed/skipped/xfailed count as a pass criterion
    - `docs/UAT-SERIES.md` line 4's `**Last Updated:**` names today's date and Phase 150, with the prior "Earlier:" history preserved
    - `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` exists, contains `UAT-150-01`, and its frontmatter has `source: docs/UAT-SERIES.md` plus today's `updated` date
    - `git log --oneline -1 -- docs/UAT-SERIES.md` shows a new commit for this phase
    - `pytest tests/test_cli_correctness.py -q -m ""` reports `0 failed`
  </acceptance_criteria>
  <done>UAT-SERIES.md documents how a human verifies the green baseline, the gate biting, and first-run lab cert generation, and the vault copy matches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Private planning artifacts → public `docs/` | Ledger and UAT content derived from `.planning/` is published on a public repository |
| Repo docs → Obsidian vault | Doc content is mirrored into the operator's personal vault filesystem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-25 | Repudiation | A requirements table claiming Complete for an unproven requirement | mitigate | Task 2 flips SUITE-02/SUITE-03 to unchecked / In Progress and records the failing run; plan 150-09 may only flip them back after the human-verified evidence artifact exists |
| T-150-26 | Information Disclosure | Ledger addendum leaking private planning content into the public repo | mitigate | The addendum carries only test names, extras groups, package names and a public Actions URL — the same class of content already published in `docs/test-triage-149.md` |
| T-150-27 | Tampering | Doc edits silently breaking doc-presence / doc-drift test gates | mitigate | Every task re-runs `test_cli_correctness.py` and the two `phase13x_docs_presence` gates in the CI-parity venv before completing |
| T-150-SC | Tampering | pip/npm/cargo installs | mitigate | This plan installs nothing |
</threat_model>

<verification>
1. `docs/test-triage-149.md` contains a Phase 150 addendum whose row count matches the Phase 150 block in `tests/skip_registry.py`.
2. `.planning/REQUIREMENTS.md` shows SUITE-02/SUITE-03 as unchecked / In Progress; SUITE-01 untouched.
3. `.planning/ROADMAP.md` Phase 150 block lists 6 plans, Goal and Success Criteria unchanged.
4. `docs/UAT-SERIES.md` has UAT-150-01..03 with unfilled Result lines and an updated header date.
5. The vault copy of UAT-SERIES.md matches and carries correct frontmatter.
6. Doc-drift and doc-presence gates are green in the CI-parity venv.
</verification>

<success_criteria>
- The public record matches what the code actually does before the phase's evidence run is taken.
- No requirement is marked Complete without proof.
</success_criteria>

<output>
Create `.planning/phases/150-test-suite-green-baseline-ci-gate/150-07-SUMMARY.md` when done.
</output>
