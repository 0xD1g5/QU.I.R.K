---
phase: 150-test-suite-green-baseline-ci-gate
plan: 08
type: execute
wave: 4
depends_on: [150-04, 150-05, 150-06, 150-07]
files_modified:
  - tests/test_ci_gate_smoke.py
  - .planning/phases/150-test-suite-green-baseline-ci-gate/150-CI-EVIDENCE.md
autonomous: false
requirements: [SUITE-02, SUITE-03]

must_haves:
  truths:
    - "The Linux Full Suite job concludes success on a real GitHub Actions run of main — the green baseline holds in CI's actual environment, not only locally (SUITE-02)"
    - "A deliberately introduced failing test makes that same job go red on a real CI run, proving the gate is wired correctly and not just syntactically valid (SUITE-03)"
    - "The deliberately failing test is removed before the phase closes and never lands on main"
    - "Both run URLs plus a failing-log excerpt are captured in a phase artifact and confirmed by the user"
  artifacts:
    - path: ".planning/phases/150-test-suite-green-baseline-ci-gate/150-CI-EVIDENCE.md"
      provides: "Green-run and red-run GitHub Actions evidence for SUITE-02 / SUITE-03"
      contains: "github.com"
  key_links:
    - from: ".github/workflows/python-ci.yml"
      to: "GitHub Actions pull_request trigger"
      via: "live PR run of the linux-full-suite job"
      pattern: "linux-full-suite"
---

<objective>
Retry and complete what plan 150-03 halted on: get a genuinely green `Linux Full Suite` run on real
GitHub Actions, then live-fire the gate with a deliberately failing test on a PR branch, capture the
red run, revert cleanly, and have the user confirm both.

Purpose: this is the same D-07 requirement 150-03 was written for. Its Task 1 stop condition fired
because the green baseline itself was red on CI; plans 150-04 through 150-07 closed all eight
failure categories. A local-only simulation still cannot catch a workflow-YAML wiring bug, and a
local-only baseline provably could not catch an environment-assumption gap — only a real run proves
either.
Output: `150-CI-EVIDENCE.md` with the green-run and red-run URLs plus the failing log excerpt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/150-test-suite-green-baseline-ci-gate/150-CONTEXT.md
@.planning/phases/150-test-suite-green-baseline-ci-gate/150-03-PLAN.md
@.planning/phases/150-test-suite-green-baseline-ci-gate/150-03-SUMMARY.md
@.planning/phases/150-test-suite-green-baseline-ci-gate/.continue-here.md
@.github/workflows/python-ci.yml

<interfaces>
<!-- Environment facts. Do not re-derive. -->

Repo is public (github.com/0xD1g5/QU.I.R.K); `gh` CLI is installed and authenticated as `0xD1g5`.
Default branch: main. Branch protection is active on main with a required status check whose context
name is a job DISPLAY name (GitHub matches check-run display names, not YAML job keys). Do NOT add or
change any required-check configuration in this plan.

Workflow triggers (.github/workflows/python-ci.yml lines 3-7):
  pull_request:            fires for any PR against main, running the workflow from the PR HEAD
  push: branches: [main]   fires only for pushes to main
  workflow_dispatch:

Job added by plan 150-02: key `linux-full-suite`, display name `Linux Full Suite`, lines 398-421.
Its previous real run took ~21 minutes.

Git state at plan start: local `main` was already pushed to `origin/main` at `0aa6c5b` by plan
150-03. The last real run, https://github.com/0xD1g5/QU.I.R.K/actions/runs/31598809033, concluded
`failure` for `Linux Full Suite` with `38 failed, 3074 passed, 46 skipped, 73 xfailed, 7 xpassed`.
All four Windows jobs in that run concluded `success`.

Useful gh invocations:
  gh run list --workflow "Python CI" --branch <branch> --limit 5
  gh run view <run-id> --json status,conclusion,url,jobs
  gh run view <run-id> --log-failed
  gh pr create --draft --base main --head <branch> --title ... --body ...
  gh pr close <number> --delete-branch

The user pre-authorized, before plan 150-03 executed, both pushing local `main` to `origin/main` and
opening a public draft PR carrying a deliberately failing test for this live-fire proof. That
authorization still stands for this retry.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Land the remediation on main and capture a genuinely green Linux Full Suite run (SUITE-02)</name>
  <files>(no files created — pushes already-committed work and records run evidence in the scratchpad)</files>
  <read_first>
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-04-SUMMARY.md, 150-05-SUMMARY.md, 150-06-SUMMARY.md, 150-07-SUMMARY.md (what landed, and any deviation each plan recorded)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-03-SUMMARY.md (the 8-category failure list this run must have eliminated)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/.continue-here.md (the BLOCKING CONSTRAINT: never treat a local run as CI proof)
    - .github/workflows/python-ci.yml lines 396-421 (confirm the `linux-full-suite` job and its install/invocation are unchanged by this phase's remediation)
  </read_first>
  <action>
Preconditions, verified before anything else:
  - `git status --porcelain` is clean and the current branch is `main`.
  - `main` still contains the `linux-full-suite` job and its `pip install -e ".[all]"` +
    `pytest -q -m ""` steps are unmodified.
  - A final confirmation run of the whole suite in the CI-parity venv from plan 150-04
    (`$HOME/.cache/quirk-ci-parity-venv/bin/python -m pytest -q -m ""` from the repo root) reports
    0 failed. If it does not, stop here and fix locally first — do not spend a 21-minute CI run to
    discover something the parity venv already knows. Record the summary line.
  - All commits from plans 150-04 through 150-07 are present in `git log` on `main`.

Then push `main` to `origin` and capture the run:
  1. `git push origin main`, then confirm `git rev-parse HEAD` equals the SHA from
     `git ls-remote origin refs/heads/main`. If `main` is already in sync (nothing new to push),
     trigger the workflow explicitly with `gh workflow run` against `main` instead — the goal is a
     real run of the current tree either way.
  2. Record that run's ID and URL.
  3. Wait for its `Linux Full Suite` job to conclude, polling with `gh run view <id> --json jobs`
     or `gh run watch <id>`. Budget for roughly 21 minutes plus chaos-lab container startup.
  4. If it concludes `success`: capture the job's pytest summary line, the runner OS and Python
     version as reported in the job log, and the head SHA. Also record whether the 5 macOS
     fork()-SIGSEGV-cluster tests (named in `docs/test-triage-149.md`) passed outright rather than
     taking their `xfail(strict=False)` path — that is D-03's expected favorable side-effect, to be
     recorded as an observation only, never asserted on or acted upon.
  5. If it concludes `failure`, triage before doing anything else, and apply exactly one of:
     - If every remaining failure is the same already-decided class — a test hard-failing because
       `identity`/`hw`/`api` is absent, or a test reading the gitignored
       `.planning/audit-2026-05-08/AUDIT-TASKS.md` — apply the identical D-09/D-10/D-15 guard and
       registry treatment plan 150-06 used, commit, and re-push for one more run. This is executing
       an existing decision, not inventing a new one.
     - If the D-17 `/api/sensor/push` failure recurs and plan 150-04 deferred its disposition to
       this run, complete that investigation now using the protocol in 150-04 Task 3.
     - For anything else — a new failure class, a flake, or a failure that contradicts the parity
       venv — STOP. Report it as a blocker in the SUMMARY with the run URL and the failing test
       list. Do not paper over it with new markers, do not add a marker without a registry entry,
       and do not proceed to Task 2. Do not attempt more than two remediation-and-re-push cycles in
       this task; a third failure means the phase needs replanning again.

Never proceed to Task 2 on anything other than a `success` conclusion for `Linux Full Suite`.
  </action>
  <verify>
    <automated>gh run list --workflow "Python CI" --branch main --limit 3 --json databaseId,conclusion,url,headSha</automated>
    <automated>git rev-parse HEAD; git ls-remote origin refs/heads/main</automated>
    <automated>$HOME/.cache/quirk-ci-parity-venv/bin/python -m pytest tests/test_skip_registry.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - The parity-venv full-suite run immediately before the push reported `0 failed`, and its summary line is recorded in the SUMMARY
    - `git rev-parse HEAD` equals `origin/main`'s SHA
    - A `Python CI` run exists for that SHA whose `Linux Full Suite` job conclusion is `success`, with its run ID and URL recorded
    - The job log's pytest summary line shows `0 failed` (equivalently, no `failed` count greater than zero)
    - The four Windows jobs in the same run are not `failure`
    - The D-03 fork()-SIGSEGV-cluster observation is recorded as an observation, with no marker changed as a result
    - If any remediation cycle was needed, the SUMMARY names each fixed test, the decision (D-09/D-10/D-15/D-17) it was applied under, and the resulting run URL
  </acceptance_criteria>
  <done>The real `Linux Full Suite` job is green on `main` in CI's own environment, with the run URL and summary line captured.</done>
</task>

<task type="auto">
  <name>Task 2: Live-fire the gate on a PR branch, capture the red run, revert, and write the evidence artifact (SUITE-03, D-07)</name>
  <files>tests/test_ci_gate_smoke.py, .planning/phases/150-test-suite-green-baseline-ci-gate/150-CI-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-03-PLAN.md Tasks 1 and 2 (the original live-fire steps, constraints on the smoke-test file, and the evidence-artifact contents this task reproduces)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-CONTEXT.md (D-07 — live-fire not local simulation, revert before phase close; D-03 — the fork()-SIGSEGV observation to record)
    - tests/skip_registry.py module docstring (confirm the smoke test introduces no skip/xfail marker that would need registering)
    - .github/workflows/python-ci.yml lines 1-11 (triggers and `permissions: contents: read`, which must be unchanged on the branch)
  </read_first>
  <action>
Live-fire, in this order:

  1. Create branch `ci/smoke-check-150` from the green `main` commit.
  2. Add `tests/test_ci_gate_smoke.py` containing exactly one test function that unconditionally
     fails via a plain `assert` whose message identifies it as the Phase 150 D-07 CI gate smoke
     check. Constraints: no `pytest.mark.skip`, `skipif` or `xfail` marker (any would require a
     `tests/skip_registry.py` entry and defeat the purpose), no `slow` marker (it must be collected
     under both the default and the emptied-`-m` invocation), no imports beyond the standard
     library, and a module docstring stating the file is temporary and deleted later in this task.
  3. Commit on the branch only and push the branch to origin. Never commit this file to `main`.
  4. Open a draft PR against `main` with `gh pr create --draft`, titled to identify it as the
     Phase 150 CI gate smoke check, with a body stating it will be closed without merge.
  5. Watch the resulting `Python CI` run for that branch until `Linux Full Suite` concludes. Its
     conclusion MUST be `failure`. If the job does not appear at all in the PR run, that is exactly
     the wiring bug this live-fire exists to catch — report it, fix the workflow on the branch, and
     re-run before proceeding.
  6. Capture the run URL, run ID, job conclusion, and a 5-15 line excerpt from
     `gh run view <run-id> --log-failed` showing the pytest failure line for
     `tests/test_ci_gate_smoke.py`. Do not capture the whole log.
  7. Tear down: `gh pr close <number> --delete-branch`, delete the local branch, and confirm
     `git ls-remote --heads origin ci/smoke-check-150` returns nothing and the file exists neither
     in the working tree nor on `main`.

Then write `.planning/phases/150-test-suite-green-baseline-ci-gate/150-CI-EVIDENCE.md` recording:
  - Green run (SUITE-02): run URL, run ID, commit SHA, `Linux Full Suite` conclusion, the job's
    pytest summary line, and the runner OS / Python version from the job log.
  - Red run (SUITE-03): run URL, run ID, branch, conclusion `failure`, and the fenced `--log-failed`
    excerpt showing the `test_ci_gate_smoke` failure.
  - Revert confirmation: the `gh pr close --delete-branch` result and the commands proving the
    branch and file are gone.
  - D-03 observation: whether the 5 macOS fork()-SIGSEGV-cluster tests passed outright on
    `ubuntu-latest` rather than taking their `xfail(strict=False)` path. Record as fact; do not
    assert on it and do not change any marker — `strict=False` tolerates either outcome and D-03 is
    an expected favorable side-effect, not a requirement.
  - Remediation history: a one-paragraph note that the first live-fire attempt (plan 150-03) hit its
    stop condition with 38 failures across 8 categories, linking run 31598809033, and that plans
    150-04 through 150-07 closed them.

Keep the artifact factual and link-bearing; it is the source `150-VERIFICATION.md` will cite.
  </action>
  <verify>
    <automated>test ! -f tests/test_ci_gate_smoke.py &amp;&amp; test -f .planning/phases/150-test-suite-green-baseline-ci-gate/150-CI-EVIDENCE.md &amp;&amp; grep -c "https://github.com" .planning/phases/150-test-suite-green-baseline-ci-gate/150-CI-EVIDENCE.md</automated>
    <automated>git ls-remote --heads origin ci/smoke-check-150 | wc -l</automated>
    <automated>git ls-tree origin/main tests/test_ci_gate_smoke.py | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - A PR existed against `main` from branch `ci/smoke-check-150` and its `Python CI` run's `Linux Full Suite` job conclusion is `failure`, with the run URL recorded
    - `gh run view <red-run-id> --log-failed` output contains the string `test_ci_gate_smoke`
    - `test -f tests/test_ci_gate_smoke.py` is false and `git ls-tree origin/main tests/test_ci_gate_smoke.py | wc -l` returns `0`
    - `git ls-remote --heads origin ci/smoke-check-150 | wc -l` returns `0` and `git branch --list ci/smoke-check-150` returns nothing
    - `150-CI-EVIDENCE.md` contains at least two distinct `https://github.com/.../actions/runs/` URLs, one attributed `success` and one attributed `failure`
    - `150-CI-EVIDENCE.md` contains the log excerpt with `test_ci_gate_smoke`, the D-03 observation sentence, and the remediation-history paragraph linking run 31598809033
    - `git status --porcelain` shows no leftover smoke-test artifacts
  </acceptance_criteria>
  <done>The gate is proven to bite on a real CI run, the smoke test is fully removed from remote and local, and a factual evidence artifact exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human confirmation of the live-fire evidence and clean revert</name>
  <what-built>
A live-fire proof that the `Linux Full Suite` CI job gates: one real green run on `main` after the
Phase 150 remediation, one real red run on a PR branch caused solely by a deliberately added failing
test, then a full revert (PR closed unmerged, remote branch deleted, test file gone). Evidence is in
`.planning/phases/150-test-suite-green-baseline-ci-gate/150-CI-EVIDENCE.md`.
  </what-built>
  <how-to-verify>
1. Open `.planning/phases/150-test-suite-green-baseline-ci-gate/150-CI-EVIDENCE.md` and click the
   green-run URL. Confirm `Linux Full Suite` shows a green check on a `main` commit and that its
   pytest summary line reports 0 failed.
2. Click the red-run URL. Confirm the same job shows a red X and that the failure is
   `tests/test_ci_gate_smoke.py`, not some unrelated test.
3. Visit the repository's Pull Requests → Closed tab and confirm the Phase 150 smoke-check PR is
   closed and NOT merged.
4. Run `git ls-remote --heads origin ci/smoke-check-150` — expect no output.
5. Run `git log origin/main --oneline -- tests/test_ci_gate_smoke.py` — expect no output.
  </how-to-verify>
  <read_first>
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-CI-EVIDENCE.md
  </read_first>
  <files>(no files modified — human verification of the Task 1 / Task 2 evidence)</files>
  <action>Present the two run URLs from `150-CI-EVIDENCE.md` to the user alongside the verification steps above, then pause. Do not proceed, do not re-open the PR, and do not re-add the smoke test. If the user reports a problem, capture their words verbatim in the SUMMARY and re-run the affected Task 1 or Task 2 step rather than editing the evidence file to match.</action>
  <verify>
    <automated>git ls-remote --heads origin ci/smoke-check-150 | wc -l</automated>
    <human-check>User confirms the green run, the red run caused by `test_ci_gate_smoke`, and the closed-unmerged PR</human-check>
  </verify>
  <acceptance_criteria>
    - User confirms both run URLs resolve and show the expected green and red `Linux Full Suite` conclusions
    - User confirms the smoke-check PR is closed and unmerged
    - `git ls-remote --heads origin ci/smoke-check-150` produces no output
    - `git log origin/main --oneline -- tests/test_ci_gate_smoke.py` produces no output
  </acceptance_criteria>
  <done>User has typed "approved" after confirming both run URLs, the unmerged closed PR, and the deleted branch.</done>
  <resume-signal>Type "approved" or describe what is wrong (e.g. wrong job red, PR still open, branch not deleted)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → public `origin` remote | Pushed branches and PRs are publicly visible on a public repository |
| PR branch workflow → GitHub Actions runner | The workflow definition comes from the PR HEAD, so branch content controls what CI executes |
| `gh` CLI credentials → remote repository state | Branch creation, PR creation/closure and branch deletion use the operator's token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-150-09 | Tampering | Deliberately failing test leaking onto `main` | mitigate | The test is committed only on `ci/smoke-check-150`; the PR is closed unmerged; Task 2 asserts `git ls-tree origin/main` returns nothing for the file and Task 3 has a human re-confirm; branch protection prevents an unreviewed merge |
| T-150-10 | Denial of Service | Orphaned branch or open PR left behind on a public repo | mitigate | `gh pr close --delete-branch` plus explicit `git ls-remote` and `git branch --list` emptiness assertions in Tasks 2 and 3 |
| T-150-11 | Information Disclosure | CI logs and the public PR exposing internal detail | accept | The smoke test contains only a standard-library `assert` and a Phase-150 identifying message; the workflow declares no secrets and does not use `pull_request_target`, so secrets are never exposed to branch-controlled code |
| T-150-12 | Elevation of Privilege | Branch-controlled workflow modification running with an elevated token | mitigate | The workflow-level `permissions: contents: read` is unchanged by this phase and applies to the PR run; no job-level override or write-scoped step is added on the branch |
| T-150-13 | Repudiation | Claiming the gate works without proof | mitigate | This plan exists to produce falsifiable proof — two run URLs plus a `--log-failed` excerpt, human-verified in Task 3 |
| T-150-28 | Repudiation | Reaching green by suppressing failures rather than fixing them | mitigate | Task 1 permits only the already-decided D-09/D-10/D-15/D-17 treatments, caps remediation at two cycles, and requires a hard STOP for any new failure class |
| T-150-SC | Tampering | pip/npm/cargo installs | mitigate | This plan installs no packages; CI installs only the pre-existing declared `.[all]` set via the plan 150-02 job |
</threat_model>

<verification>
1. Green run on `main`: `Linux Full Suite` conclusion `success`, 0 failed, URL recorded.
2. Red run on `ci/smoke-check-150`: conclusion `failure`, caused by `tests/test_ci_gate_smoke.py`, URL and log excerpt recorded.
3. `git ls-remote --heads origin ci/smoke-check-150` is empty.
4. `git ls-tree origin/main tests/test_ci_gate_smoke.py` is empty.
5. `150-CI-EVIDENCE.md` exists with both URLs, the D-03 observation and the remediation history.
6. Human checkpoint approved.
</verification>

<success_criteria>
- SUITE-02 is proven on CI's own environment, not inferred from a local run.
- SUITE-03 is proven by a deliberately introduced failure turning the real job red.
- No trace of the smoke test remains: no file, no local branch, no remote branch, no merged commit.
</success_criteria>

<output>
Create `.planning/phases/150-test-suite-green-baseline-ci-gate/150-08-SUMMARY.md` when done.
</output>
