---
phase: 152-discovery-empirical-closure
plan: 03
type: execute
wave: 2
depends_on: ["152-01"]
files_modified:
  - quantum-chaos-enterprise-lab/segmented-network/compare_discovery.py
  - .planning/phases/152-discovery-empirical-closure/152-DISC09-FINDING.md
  - quirk/discovery/nmap_provider.py
  - .planning/STATE.md
  - .planning/milestones/v5.11-MILESTONE-AUDIT.md
autonomous: true
requirements: [DISC-10]

must_haves:
  truths:
    - "The Phase 144 nmap timing-engine artifact has a written, empirically-grounded determination — either it does not reproduce (closed) or it does and a scoped mitigation with documented tradeoffs exists"
    - "The determination is based on at least 3 independent verification runs against the DISC-09 segmented-network profile, not a single noisy run"
    - "The v5.11-MILESTONE-AUDIT.md tech_debt row and STATE.md no longer carry stale 'OPEN (needs real hardware)' language once this phase closes"
  artifacts:
    - path: ".planning/phases/152-discovery-empirical-closure/152-DISC09-FINDING.md"
      provides: "Dedicated finding document citing all 3 runs and a REPRODUCES/DOES NOT REPRODUCE verdict"
    - path: "quantum-chaos-enterprise-lab/segmented-network/compare_discovery.py"
      provides: "Repeatable chunked-vs-direct nmap comparison script reusing quirk.discovery.nmap_parser"
  key_links:
    - from: "quantum-chaos-enterprise-lab/segmented-network/compare_discovery.py"
      to: "quirk.discovery.nmap_parser.parse_nmap_xml"
      via: "import + XML diff"
      pattern: "parse_nmap_xml"
    - from: "152-DISC09-FINDING.md verdict"
      to: ".planning/milestones/v5.11-MILESTONE-AUDIT.md tech_debt block"
      via: "written cross-reference update"
      pattern: "152-DISC09-FINDING"
---

<objective>
Empirically settle the Phase 144 nmap adaptive RTT/timing-engine artifact (an accepted
VERIFICATION override from v5.11 — real open ports possibly suppressed on a mostly-silent host
batch) against the DISC-09 `segmented-network` lab profile built in Plan 152-01. Build a
repeatable chunked-vs-direct comparison script, run it at least 3 times, and write a dedicated
finding document with a strict REPRODUCES / DOES NOT REPRODUCE verdict. If it reproduces,
implement the scoped (not global) mitigation CONTEXT.md already specified; either way, close the
loop in both `.planning/STATE.md` and `.planning/milestones/v5.11-MILESTONE-AUDIT.md` so the
open item does not survive this phase in stale language.

Purpose: convert a permanently-deferred, user-accepted override into a settled empirical
determination.
Output: `152-DISC09-FINDING.md`, a reusable comparison script, and (conditionally) a scoped
`nmap_provider.py` mitigation plus updated ledger entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-discovery-empirical-closure/152-CONTEXT.md
@.planning/phases/152-discovery-empirical-closure/152-RESEARCH.md
@.planning/phases/152-discovery-empirical-closure/152-01-SUMMARY.md

<interfaces>
Existing chunked discovery batch loop to mirror honestly when driving the "chunked" side of the
comparison (`run_scan.py:1464-1537`, current shipped code — read, do not modify unless the
mitigation branch fires):
```python
for batch in _chunked(host_iter, _MAX_HOSTS_PER_CIDR):
    batch_num += 1
    _batch_timeout = discovery_timeout_for_batch(len(batch))
    _batch_timing_template = discovery_timing_template_for_batch(len(batch))
    # ... liveness pre-pass (Phase 145) narrows `batch` to `sweep_targets` ...
    _batch_extra_args = list(extra_args.split()) if extra_args else []
    _batch_extra_args.append(_batch_timing_template)
    batch_open_ports = run_nmap_discovery(
        targets=sweep_targets, ports=ports, output_dir=output_dir,
        extra_args=_batch_extra_args, timeout_seconds=_batch_timeout,
    )
```

Existing liveness pre-pass up/down counting — the natural signal source if a scoped mitigation
is needed (`quirk/discovery/nmap_provider.py:276-279`):
```python
if logger:
    up_count = sum(1 for h in host_statuses if h.up)
    down_count = len(host_statuses) - up_count
    logger.stamp(f"Nmap liveness pre-pass: {up_count} up, {down_count} down.")
```

`discovery_timing_template_for_batch()` — the ONLY existing timing lever, general-case branch
must NOT be edited (Pitfall 5) if a mitigation is needed:
```python
def discovery_timing_template_for_batch(batch_size: int) -> str:
    ...
    if size <= _DISCOVERY_T4_MAX_BATCH_SIZE:
        return "-T4"
    return "-T3"
```
A scoped mitigation adds a NEW, separate conditional path keyed off liveness pre-pass silence
ratio — it does not alter this function's existing branches.

Existing XML parsing to reuse for the diff, not re-implement (`quirk/discovery/nmap_parser.py`,
already imported by `nmap_provider.py`):
```python
from quirk.discovery.nmap_parser import (
    parse_nmap_xml,
    NmapOpenPort,
    parse_nmap_host_status,
    NmapHostStatus,
    parse_nmap_run_summary,
)
```

CONTEXT.md's strict reproduction definition (quote when writing the finding — do not
paraphrase loosely): "real open ports on live hosts missed/suppressed by adaptive RTT/timing
throttling during chunked discovery, confirmed by diffing chunked-discovery output against a
direct nmap run of the same segment. Any other kind of missed port does not count as
reproduction." The diff MUST be restricted to `segnet-live` hosts only — dead-subnet
non-findings are expected/correct, never suppressions (Pitfall 4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the repeatable chunked-vs-direct comparison script</name>
  <files>quantum-chaos-enterprise-lab/segmented-network/compare_discovery.py</files>
  <read_first>
    quirk/discovery/nmap_provider.py (full file — run_nmap_discovery, run_nmap_liveness_check, discovery_timing_template_for_batch, discovery_timeout_for_batch signatures)
    quirk/discovery/nmap_parser.py (parse_nmap_xml, parse_nmap_host_status signatures/return shapes)
    run_scan.py lines 1391-1598 (the exact chunked discovery batch loop this script must mirror for the "chunked" side of the comparison)
  </read_first>
  <action>
    Create `quantum-chaos-enterprise-lab/segmented-network/compare_discovery.py`, runnable via
    `docker compose exec segnet-prober python
    /quirk/quantum-chaos-enterprise-lab/segmented-network/compare_discovery.py` (adjust the
    in-container path to match `sensor.Dockerfile`'s `WORKDIR`/`COPY` layout — verify the actual
    mounted path before hardcoding it). The script must:
    1. Build the full target list (both `segnet-live` static IPs and the `10.71.0.0/26`
       representative dead range from Plan 152-01).
    2. Run the "chunked" side by calling `quirk.discovery.nmap_provider.run_nmap_discovery()`
       inside a loop mirroring `run_scan.py`'s batching (using `discovery_timing_template_for_batch`/
       `discovery_timeout_for_batch` exactly as the real chunked-discovery path does — do not
       invent different batch sizing/timing logic).
    3. Run the "direct" side as a single non-chunked, non-timing-throttled `run_nmap_discovery()`
       (or a bare subprocess `nmap` call parsed via `parse_nmap_xml`) against the same full
       target list.
    4. Diff the two `(host, port)` open-port sets, restricted to `segnet-live` hosts only
       (exclude dead-subnet results from the diff entirely — Pitfall 4).
    5. Emit a structured report (JSON or Markdown) to stdout and to a timestamped file under
       `quantum-chaos-enterprise-lab/segmented-network/runs/`, listing: target counts, chunked
       open-port set, direct open-port set, and any `segnet-live` port found by the direct run
       but missing from the chunked run ("reproduction candidates").
    Reuse `parse_nmap_xml`/`parse_nmap_host_status` for all XML parsing — do not hand-roll a
    parser (RESEARCH.md Don't Hand-Roll).
  </action>
  <verify>
    <automated>python -m py_compile quantum-chaos-enterprise-lab/segmented-network/compare_discovery.py</automated>
    <automated>grep -n "segnet-live\|10.70.0" quantum-chaos-enterprise-lab/segmented-network/compare_discovery.py | head -5</automated>
    <!-- The actual `docker compose exec segnet-prober python .../compare_discovery.py` run against
         the live DISC-09 lab is irreducibly manual/live-fire (requires the lab topology up) — see
         152-VALIDATION.md's Manual-Only Verifications table row for DISC-10. -->
  </verify>
  <acceptance_criteria>
    `docker compose exec segnet-prober python .../compare_discovery.py` runs to completion without an unhandled exception against the Plan 152-01 lab and produces a report file under `quantum-chaos-enterprise-lab/segmented-network/runs/`
    The script's diff logic is provably scoped to `segnet-live` hosts only — `grep -n "segnet-live\|10.70.0" quantum-chaos-enterprise-lab/segmented-network/compare_discovery.py` shows the live-subnet filter applied before the diff, not after
    The report explicitly separates "chunked open ports", "direct open ports", and "reproduction candidates" as distinct labeled sections
  </acceptance_criteria>
  <done>`compare_discovery.py` exists, compiles cleanly, and its diff logic is provably scoped to `segnet-live` hosts before the diff runs (not after); the live `docker compose exec` execution against the up lab is the manual live-fire step tracked in 152-VALIDATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Run 3 independent verification rounds; write 152-DISC09-FINDING.md</name>
  <files>.planning/phases/152-discovery-empirical-closure/152-DISC09-FINDING.md</files>
  <read_first>
    .planning/phases/152-discovery-empirical-closure/152-CONTEXT.md "Timing-Artifact Resolution Methodology" section (strict reproduction definition, 3-run requirement)
    .planning/milestones/v5.11-MILESTONE-AUDIT.md tech_debt block, "Phase 144 nmap timing artifact" row (exact original framing to reference and eventually supersede)
  </read_first>
  <action>
    Run `compare_discovery.py` 3 separate times against the same lab topology (same target set,
    same batch size, same nmap args each run, per CONTEXT.md's "comparable runs" requirement).
    Capture each run's full report. Apply the strict CONTEXT.md definition to each run's
    "reproduction candidates" list — a candidate only counts if it is (a) on `segnet-live` and
    (b) the direct run found the port open while the chunked run did not, i.e. genuinely
    suppressed by throttling, not a closed port or an unrelated discovery gap.

    Write `.planning/phases/152-discovery-empirical-closure/152-DISC09-FINDING.md` containing:
    the strict reproduction definition (quoted), all 3 runs' raw evidence (target counts, ports
    found each side, candidates per run), and an explicit final verdict line — either
    `**VERDICT: DOES NOT REPRODUCE**` or `**VERDICT: REPRODUCES**` — plus a one-paragraph
    rationale. If it reproduces in some but not all 3 runs, document that as REPRODUCES
    (intermittent throttling suppression is still the same failure mode) with the frequency
    noted.
  </action>
  <verify>
    <automated>test -f .planning/phases/152-discovery-empirical-closure/152-DISC09-FINDING.md && grep -Ec "VERDICT: (DOES NOT REPRODUCE|REPRODUCES)" .planning/phases/152-discovery-empirical-closure/152-DISC09-FINDING.md</automated>
    <!-- The 3-independent-run requirement itself is irreducibly manual/live-fire (requires the
         lab topology up 3 separate times) — see 152-VALIDATION.md's Manual-Only Verifications
         table row for DISC-10; this automated check only confirms the finding artifact and its
         verdict string exist after the manual runs are complete. -->
  </verify>
  <acceptance_criteria>
    152-DISC09-FINDING.md exists and cites results from exactly 3 distinct runs (not fewer, not a single run repeated in prose)
    The file contains the literal string `VERDICT: DOES NOT REPRODUCE` or `VERDICT: REPRODUCES` (exactly one of the two)
    The file quotes or closely paraphrases CONTEXT.md's strict reproduction definition (live-subnet-only, throttling-caused-suppression-only)
  </acceptance_criteria>
  <done>`152-DISC09-FINDING.md` exists with an explicit verdict line, grounded in 3 independent live-fire runs of `compare_discovery.py` as tracked in 152-VALIDATION.md's Manual-Only Verifications table.</done>
</task>

<task type="auto">
  <name>Task 3: Apply the conditional outcome; close the ledger references</name>
  <files>quirk/discovery/nmap_provider.py, .planning/STATE.md, .planning/milestones/v5.11-MILESTONE-AUDIT.md</files>
  <read_first>
    .planning/phases/152-discovery-empirical-closure/152-DISC09-FINDING.md (this plan's own Task 2 output — read the verdict before branching)
    .planning/milestones/v5.11-MILESTONE-AUDIT.md tech_debt block (exact current "Phase 144 nmap timing artifact" and "DISC-09 segmented-network lab profile" row text to replace)
    .planning/STATE.md Deferred Items section (confirms no existing row for this item — this is a net-new addition, not an edit of an existing row)
  </read_first>
  <action>
    Branch on Task 2's verdict:

    IF `VERDICT: REPRODUCES` — implement the scoped mitigation in `quirk/discovery/nmap_provider.py`
    per CONTEXT.md's Mitigation Scope decision: add a NEW conditional path (do not edit
    `_default_nmap_args()`'s or `discovery_timing_template_for_batch()`'s existing general-case
    branches — Pitfall 5) that, ONLY when the liveness pre-pass reports an overwhelming majority
    of a batch as down (using the existing `up_count`/`down_count` values at
    `nmap_provider.py:276-279` as the signal source), selects a more conservative timing
    template or RTT-bound tuning for that batch alone. Add a targeted unit test for the new
    conditional function. Update the `v5.11-MILESTONE-AUDIT.md` tech_debt row from "OPEN (needs
    real hardware)" to a scoped-mitigation note citing the commit and `152-DISC09-FINDING.md`.
    Add a new STATE.md Deferred Items row: RESOLVED, mitigation applied, cite the finding.

    IF `VERDICT: DOES NOT REPRODUCE` — make NO change to `quirk/discovery/nmap_provider.py`.
    Update the `v5.11-MILESTONE-AUDIT.md` tech_debt row to a closure note ("CLOSED — see
    `152-DISC09-FINDING.md`; did not reproduce on a realistic segmented network"), removing the
    "OPEN (needs real hardware)" / override-acceptance framing entirely per CONTEXT.md's explicit
    instruction not to leave stale accepted-override language in place. Add the same STATE.md
    Deferred Items closure row.

    Either way: also update the `v5.11-MILESTONE-AUDIT.md` "DISC-09 segmented-network lab
    profile" tech_debt row (currently "DEFERRED BY DECISION... explicitly deferred to v5.11.x")
    to reflect that it shipped in Phase 152. Run `python -m compileall quirk/` and the discovery
    test suite.
  </action>
  <verify>
    <automated>! grep -q "OPEN (needs real hardware)" .planning/milestones/v5.11-MILESTONE-AUDIT.md && echo LEDGER_CLOSED</automated>
    <automated>grep -c "152-DISC09-FINDING" .planning/STATE.md</automated>
    <automated>python -m compileall quirk/discovery/nmap_provider.py</automated>
    <!-- IF a mitigation was applied (VERDICT: REPRODUCES), also run:
         pytest tests/ -k nmap_provider -q
         IF not reproduced, confirm instead: git diff --stat -- quirk/discovery/nmap_provider.py
         shows no changes. -->
  </verify>
  <acceptance_criteria>
    `.planning/milestones/v5.11-MILESTONE-AUDIT.md` no longer contains the literal string "OPEN (needs real hardware)" for the Phase 144 nmap timing artifact row
    `.planning/STATE.md` contains a new Deferred Items (or equivalent resolved-items) entry referencing `152-DISC09-FINDING.md`
    IF mitigation applied: `pytest tests/ -k nmap_provider -q` passes and `python -m compileall quirk/discovery/nmap_provider.py` exits 0
    IF not reproduced: `git diff --stat -- quirk/discovery/nmap_provider.py` shows no changes to that file
  </acceptance_criteria>
  <done>The v5.11-MILESTONE-AUDIT.md and STATE.md ledgers no longer carry stale 'OPEN (needs real hardware)' language, both cross-reference 152-DISC09-FINDING.md, and (conditionally) the scoped mitigation or its explicit absence is verified per the Task 2 verdict. Note: if the REPRODUCES branch fires and implementing the scoped mitigation burns significant context, the executor should consider splitting the mitigation work into a follow-up plan rather than force-fitting it into this task's budget.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `compare_discovery.py` ↔ live nmap subprocess execution | Runs inside the already-sandboxed `segnet-prober` container against the DISC-09 lab only — no production/external target |
| Conditional `nmap_provider.py` mitigation (if applied) ↔ production discovery path | Any new conditional branch executes on every real scan's batch loop, not just this lab — must not regress the false-negative tradeoff the v5.11 audit already flagged for a blanket timing change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-04 | Denial of Service (production discovery correctness, not security) | `quirk/discovery/nmap_provider.py` conditional mitigation (if applied) | mitigate | Scoped strictly to a new conditional path keyed off the existing liveness pre-pass silence ratio — the existing `_DISCOVERY_T4_MAX_BATCH_SIZE`-based general-case branch is left untouched (Pitfall 5), so normal (non-silent) batches see zero behavior change; a targeted unit test locks the new branch's trigger condition |
| T-152-SC | Tampering (supply chain) | N/A — no new pip/npm/cargo package installed this plan | accept | `compare_discovery.py` uses only stdlib + already-installed `quirk.discovery.nmap_parser`; no new dependency |
</threat_model>

<verification>
Confirm `152-DISC09-FINDING.md` exists with a clear verdict citing 3 runs, and confirm the ledger updates in `.planning/STATE.md` / `.planning/milestones/v5.11-MILESTONE-AUDIT.md` no longer carry stale "OPEN (needs real hardware)" language. If a mitigation landed, run the targeted `nmap_provider` test file and `python -m compileall quirk/`.
</verification>

<success_criteria>
The Phase 144 nmap timing artifact has a written, 3-run-grounded empirical determination, and both `.planning/STATE.md` and `.planning/milestones/v5.11-MILESTONE-AUDIT.md` reflect the closed/mitigated state rather than the prior "needs real hardware" framing — satisfying ROADMAP Phase 152 Success Criterion 2.
</success_criteria>

<output>
Create `.planning/phases/152-discovery-empirical-closure/152-03-SUMMARY.md` when done
</output>
