---
phase: 153-release-tag-cut
plan: 05
type: execute
wave: 5
depends_on: [153-04]
files_modified:
  - docs/UAT-SERIES.md
autonomous: true
requirements: [RELEASE-01]

must_haves:
  truths:
    - "docs/UAT-SERIES.md carries a Series 153 entry documenting RELEASE-01's live-fire proof, matching the UAT-144-03 Human (live) shape"
    - "An Obsidian phase note exists for Phase 153 with status: complete, per this project's Mandatory Phase Completion Steps"
    - "docs/UAT-SERIES.md's vault counterpart is re-synced with the new Series 153 content"
    - "The Series 153 entry exists BEFORE the phase-close commit that flips ROADMAP.md/STATE.md, so scripts/verify_phase_gates.py's ARTIFACT-03 check does not block that commit"
    - "docs/UAT-SERIES.md is committed via gsd-tools.cjs commit, per CLAUDE.md's Mandatory Phase Completion Step 4"
  artifacts:
    - path: "docs/UAT-SERIES.md"
      provides: "Series 153: Release Tag Cut entry with UAT-153-01 (RELEASE-01)"
      contains: "Series 153: Release Tag Cut"
  key_links:
    - from: "docs/UAT-SERIES.md Series 153"
      to: ".planning/phases/153-release-tag-cut/153-01-SUMMARY.md .. 153-04-SUMMARY.md"
      via: "cited run URLs and evidence"
      pattern: "UAT-153-01"
---

<objective>
Write the `docs/UAT-SERIES.md` Series 153 entry documenting RELEASE-01's live-fire proof (citing
the real evidence gathered in Plans 153-01 through 153-04), commit it, create the Obsidian phase
note, and sync `docs/UAT-SERIES.md` to the vault — completing this project's Mandatory Phase
Completion Steps and pre-populating the artifact `scripts/verify_phase_gates.py`'s ARTIFACT-03
check requires before the phase-close commit can succeed.

Purpose: `153-RESEARCH.md`'s "important_notes" flag that Phase 153's own close commit is the first
real dogfood test of Phase 151's pre-commit hook — the hook blocks a `ROADMAP.md`/`STATE.md`
phase-close flip if `docs/UAT-SERIES.md` lacks matching coverage. This plan must complete before
that flip happens, or the phase-close commit itself gets rejected. `153-VERIFICATION.md` is
produced separately by `/gsd:verify-phase` (not a task in this plan) — this plan's job is the
UAT-SERIES entry, its commit, and the vault sync only.
Output: `docs/UAT-SERIES.md` Series 153 entry (committed), Obsidian phase note, vault-synced
UAT-Series.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/153-release-tag-cut/153-CONTEXT.md
@.planning/phases/153-release-tag-cut/153-PATTERNS.md
@.planning/phases/153-release-tag-cut/153-01-PLAN.md
@.planning/phases/153-release-tag-cut/153-04-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Series-entry template, extracted verbatim from docs/UAT-SERIES.md Series 144 (UAT-144-03,
     the "Human (live)" shape this phase's own entry must mirror per 153-PATTERNS.md, since
     RELEASE-01 is a live tagged CI run, not a unit test). -->

Template (fill in with real facts from the 153-01..04 SUMMARYs, do not leave placeholders):

  ## Series 153: Release Tag Cut (Phase 153 — v5.12)

  **Last Updated:** (date)

  ### UAT-153-01: Real v5.12.0 tag cut proves the repaired release pipeline end-to-end (RELEASE-01) — Human (live)

  **What to test:** (one paragraph)

  **Steps performed ((date), live session):**
  1. (numbered steps citing real run URLs / commit SHAs)

  **Pass criteria:**
  - [x] (checklist items)

  **Automated gate:** N/A — this UAT is inherently a live, human-verify checkpoint (tag push
  triggers real external CI; see 153-01-SUMMARY.md .. 153-04-SUMMARY.md for full command
  transcripts).

  **Result:** - [x] PASS  - [ ] FAIL  - [ ] SKIP
  **Date:** (date)  **Tester:** Digs / Claude (gsd-executor, live session)
  **Notes:** RELEASE-01. Requirement: RELEASE-01.

  ---

CLAUDE.md "Mandatory Phase Completion Steps" (verbatim requirements this task implements):
1. Write Obsidian phase note to
   `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-153-Release-Tag-Cut.md` directly via
   filesystem (not `obsidian CLI content=` — file too large for shell expansion).
2. Update `docs/UAT-SERIES.md` (this plan's Task 1).
3. Sync `docs/UAT-SERIES.md` to vault via the documented `printf`/`cat`/`cp` pattern.
4. Commit `docs/UAT-SERIES.md` via `gsd-tools.cjs commit` (this plan's Task 1, final step).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the docs/UAT-SERIES.md Series 153 entry and commit it</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - .planning/phases/153-release-tag-cut/153-01-SUMMARY.md, 153-02-SUMMARY.md, 153-03-SUMMARY.md, 153-04-SUMMARY.md (real evidence: run URLs, tag SHA, asset confirmation, tag-hygiene result)
    - docs/UAT-SERIES.md lines 16377-16431 (UAT-144-03, the exact "Human (live)" shape to mirror)
  </read_first>
  <action>
Append a new "## Series 153: Release Tag Cut (Phase 153 — v5.12)" section immediately after the
existing "## Series 152" section, containing one entry UAT-153-01, following the UAT-144-03
template in the interfaces block above exactly: H3 heading with the "— Human (live)" suffix, a
What to test paragraph, a Steps performed (date, live session) numbered list citing the real run
URLs and commit SHAs from the 153-01 through 153-04 SUMMARYs, a Pass criteria checklist covering
pushed-SHA CI green, the workflow_dispatch dry-run self-test OK, the recorded human approval, the
tag push, the tagged release.yml run green with event push, the Windows zip attached to the
GitHub Release, and the tag-hygiene guard clean result. The Automated gate line must read exactly
"N/A — this UAT is inherently a live, human-verify checkpoint" per the established convention.
Close with Result [x] PASS, Date/Tester, and Notes citing RELEASE-01.

Do not invent evidence. Every Steps-performed bullet and Pass-criteria checkmark must trace to a
specific fact recorded in one of the four prior plans' SUMMARY files. If any of the four SUMMARYs
recorded a blocker or deviation, reflect that honestly in this entry rather than writing a clean
narrative that contradicts the evidence.

After the file is written, commit it per CLAUDE.md's Mandatory Phase Completion Step 4:
  `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(153): update UAT-SERIES.md with Series 153 entry" --files docs/UAT-SERIES.md`
Record the resulting commit SHA in the SUMMARY.
  </action>
  <verify>
    <automated>grep -n "Series 153: Release Tag Cut" docs/UAT-SERIES.md</automated>
    <automated>grep -A5 "UAT-153-01" docs/UAT-SERIES.md | grep -c "RELEASE-01"</automated>
    <automated>git log -1 --format='%H %s' -- docs/UAT-SERIES.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/UAT-SERIES.md contains a "## Series 153: Release Tag Cut (Phase 153 — v5.12)" heading
    - It contains exactly one "### UAT-153-01: ..." entry with the "— Human (live)" suffix
    - The entry's Automated gate line reads "N/A — this UAT is inherently a live, human-verify
      checkpoint" (matching the established Series 144 convention)
    - The entry's Notes line cites RELEASE-01
    - Every factual claim in the entry (run URLs, tag SHA, asset name, tag-hygiene result) matches
      a fact recorded in 153-01-SUMMARY.md through 153-04-SUMMARY.md
    - `docs/UAT-SERIES.md` has been committed via `gsd-tools.cjs commit`, and the resulting commit
      SHA is recorded in the SUMMARY
  </acceptance_criteria>
  <done>Series 153 exists in docs/UAT-SERIES.md, evidence-backed, matching the Human (live) convention, and committed to git.</done>
</task>

<task type="auto">
  <name>Task 2: Create the Obsidian phase note and sync docs/UAT-SERIES.md to the vault</name>
  <files>(vault files outside the repo — no repo files modified by this task)</files>
  <read_first>
    - CLAUDE.md "Mandatory Phase Completion Steps" and "Obsidian Vault Integration" sections (frontmatter standard, sync pattern, target paths)
    - Existing phase note pattern, e.g. an existing Phase-15x note under 20_Dev-Work/QUIRK/Phases/ (via obsidian search, for structural reference)
  </read_first>
  <action>
1. Write the Obsidian phase note directly to
   /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-153-Release-Tag-Cut.md (filesystem
   write, not obsidian CLI content=), containing: frontmatter (project: QU.I.R.K., type: phase,
   status: complete, source: .planning/phases/153-release-tag-cut/, updated: today's date), the
   Goal statement from .planning/ROADMAP.md's Phase 153 entry, Requirements Covered (RELEASE-01),
   Success Criteria, a "What Was Built" section with one subsection per plan (153-01 pre-tag
   verification, 153-02 version bump, 153-03 human-gated tag cut, 153-04 post-tag verification,
   153-05 this doc/Obsidian close-out) sourced from each plan's SUMMARY.md, and a [[Roadmap]]
   wikilink at the end.
2. Sync docs/UAT-SERIES.md to the vault per CLAUDE.md's documented pattern: prepend frontmatter
   (project: QU.I.R.K., type: reference, status: active, source: docs/UAT-SERIES.md, updated:
   today's date) to a copy of the full file content (post-commit, from Task 1), then write it to
   /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md (overwriting the prior sync).
3. Confirm both vault files exist and the UAT-Series.md vault copy contains the new
   "Series 153: Release Tag Cut" heading.
  </action>
  <verify>
    <automated>test -f /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-153-Release-Tag-Cut.md && grep -c "RELEASE-01" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-153-Release-Tag-Cut.md</automated>
    <automated>grep -c "Series 153: Release Tag Cut" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md</automated>
  </verify>
  <acceptance_criteria>
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-153-Release-Tag-Cut.md exists with
      status: complete frontmatter and mentions RELEASE-01
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md exists and contains the new
      "Series 153: Release Tag Cut" heading
    - The phase note ends with a [[Roadmap]] wikilink
  </acceptance_criteria>
  <done>The Obsidian phase note exists with status complete, and the vault UAT-Series.md copy carries the new Series 153 content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo docs → external vault filesystem | Writing to `/Users/digs/vaults/Digs/...` is outside the git repository, a separate trust domain |
| Phase-close artifact gate → this plan's output | `scripts/verify_phase_gates.py`'s ARTIFACT-03 check depends on this plan's Task 1 output existing (and being committed) before the phase-close commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153-10 | Repudiation | A Series 153 entry that overstates or fabricates evidence not present in the prior plans' SUMMARYs | mitigate | Task 1's action and acceptance criteria explicitly require every claim to trace to a fact in `153-01-SUMMARY.md` through `153-04-SUMMARY.md`; deviations must be reflected honestly, not smoothed over |
| T-153-11 | Tampering | Phase-close commit rejected by the Phase 151 pre-commit hook because this plan's output is missing, incomplete, or uncommitted | mitigate | This plan is sequenced as the final wave, strictly after all evidence-producing plans, so the UAT-SERIES entry exists and is committed before any later ROADMAP.md/STATE.md phase-close flip; Task 1 explicitly commits `docs/UAT-SERIES.md` via `gsd-tools.cjs commit` rather than leaving it uncommitted |
| T-153-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed by this plan |
</threat_model>

<verification>
1. `docs/UAT-SERIES.md` contains the Series 153 heading and one evidence-backed UAT-153-01 entry,
   and that change is committed to git.
2. The Obsidian phase note exists with `status: complete` and cites RELEASE-01.
3. The vault UAT-Series.md copy is re-synced and contains the Series 153 heading.
</verification>

<success_criteria>
- All CLAUDE.md Mandatory Phase Completion Steps relevant to this phase (Obsidian phase note,
  UAT-SERIES.md update, UAT-SERIES.md commit, vault sync) are satisfied before the phase-close
  commit.
- `scripts/verify_phase_gates.py`'s ARTIFACT-03 check will find real, matching, committed coverage
  when the phase-close commit runs.
</success_criteria>

<output>
Create `.planning/phases/153-release-tag-cut/153-05-SUMMARY.md` when done.
</output>
