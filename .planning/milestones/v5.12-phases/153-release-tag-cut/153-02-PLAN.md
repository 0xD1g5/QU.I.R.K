---
phase: 153-release-tag-cut
plan: 02
type: execute
wave: 2
depends_on: [153-01]
files_modified:
  - pyproject.toml
  - README.md
  - docs/UAT-SERIES.md
  - docs/release-notes/5.12.0.md
autonomous: true
requirements: [RELEASE-01]

must_haves:
  truths:
    - "pyproject.toml's [project.version] is the single canonical edit; all six derived version surfaces stay consistent per tests/test_version.py"
    - "Every manual (non-derived) version-string literal (README.md heading, docs/UAT-SERIES.md header and UAT-1-02) reads 5.12.0/v5.12.0, not 5.11.0/v5.11.0"
    - "A docs/release-notes/5.12.0.md exists summarizing the v5.12 milestone (Phases 148-153), matching the 5.11.0.md structure"
    - "The version-bump commit exists on main with an explicit file list (no git add -A)"
  artifacts:
    - path: "docs/release-notes/5.12.0.md"
      provides: "Milestone release notes for v5.12, matching the 5.11.0.md structure"
      contains: "5.12.0"
  key_links:
    - from: "pyproject.toml [project.version]"
      to: "tests/test_version.py six-surface parity assertions"
      via: "importlib.metadata / tomllib fallback"
      pattern: "5\\.12\\.0"
---

<objective>
Bump the single canonical version source (`pyproject.toml`), verify the six-surface parity test
passes, update the three manual (non-derived) version-string literals per this project's
"Version bump" documentation checklist, write `docs/release-notes/5.12.0.md`, and commit — all
before the locked human-confirmation checkpoint in the next plan.

Purpose: `pyproject.toml [project.version]` is the sole source of truth
(`tests/test_version.py`); everything else either derives automatically or needs an explicit,
separate literal edit. Skipping the manual literals (README heading, UAT-SERIES header/UAT-1-02)
is Pitfall 4 in `153-RESEARCH.md` — the six-surface test creates a false sense that "the version
is fully consistent" when three prose surfaces are untouched by it.

Changelog decision (resolves 153-RESEARCH.md Open Question 1): `towncrier` is not installed in
this environment (`pip show towncrier` → not found) and `changelog.d/` contains no pending
fragment files beyond its own `README.md` format doc — the identical "no fragments" situation
that recurred for v5.9 through v5.11, each of which used a standalone
`docs/release-notes/X.Y.Z.md` file instead of forcing an empty towncrier section. This plan
follows that same established precedent for v5.12.0: no towncrier invocation, no
`CHANGELOG.md` edit, a hand-written `docs/release-notes/5.12.0.md` instead.

Output: `pyproject.toml` at `5.12.0`, three manual literal edits, `docs/release-notes/5.12.0.md`,
one commit `chore(release): v5.12.0`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/153-release-tag-cut/153-CONTEXT.md
@.planning/phases/153-release-tag-cut/153-PATTERNS.md
@docs/release-process.md
@tests/test_version.py

<interfaces>
<!-- Exact diffs to replicate, extracted from commit 4b73940 (2026-08-11, 5.10.0 -> 5.11.0). -->

pyproject.toml (line 7):
```diff
 [project]
 name = "quirk-scanner"
-version = "5.11.0"
+version = "5.12.0"
```
Do NOT touch `quirk/__init__.py`, `quirk/cbom/builder.py::PLATFORM_VERSION`,
`quirk/reports/writer.py::PLATFORM_VERSION`, or `quirk/config.py::IntelligenceCfg` — all four
derive automatically via `importlib.metadata`/`tomllib`, proven by `tests/test_version.py`.

README.md (line 7):
```diff
-# QU.I.R.K. — v5.11.0
+# QU.I.R.K. — v5.12.0
```

docs/UAT-SERIES.md header (lines 3-10, prepend a new milestone-close clause, keep prior "Earlier:"
prose per the file's accretive-narrative convention — do not delete existing content):
```diff
-**Version:** 5.11.0
-**Last Updated:** 2026-08-14 (Phase 152 wrap — ...)
+**Version:** 5.12.0
+**Last Updated:** <today> (v5.12 milestone close / Phase 153 — Release Tag Cut: real v5.12.0
+tag cut and RELEASE-01 proven end-to-end on a live tagged CI run.)
+Earlier: Phase 152 wrap — Discovery Empirical Closure: ... (existing text, unchanged)
```

docs/UAT-SERIES.md UAT-1-02 (lines 211-227, exact same substitution pattern as commit 4b73940):
```diff
 **Pass Criteria:**
-- Output matches format: `QU.I.R.K. v5.11.0`
+- Output matches format: `QU.I.R.K. v5.12.0`
 - Exit code 0

 **Result:** - [ ] PASS  - [ ] FAIL  - [ ] SKIP
 **Date:**   **Tester:**
-**Notes:** Version bumped to v5.11.0 at v5.11 milestone close ...
+**Notes:** Version bumped to v5.12.0 at v5.12 milestone close (pyproject.toml sole SoT;
+importlib.metadata derives it). Re-test required against v5.12.0 install.
```

docs/release-notes/5.11.0.md full structure to copy for 5.12.0.md (H1 title / **Released:** /
**Milestone:** / `## What's New` one bullet per phase / `## Known Issues` / `## Upgrade Guidance`
/ `## See Also` / closing italic attribution line). For 5.12.0.md's `## Known Issues` section:
state explicitly that the Windows-asset gap from 5.11.0 is now closed (this tag is the first with
a verified Windows artifact — cite RELEASE-01), not "None" bare, since the whole point of this
release is closing that documented prior gap.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bump pyproject.toml and verify six-surface version parity</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml lines 1-15 ([project] block)
    - tests/test_version.py (all six assertions)
  </read_first>
  <action>
Edit `pyproject.toml`'s `[project] version = "5.11.0"` to `version = "5.12.0"`. This is the only
edit in this task — do not touch any other file. Do not run `towncrier` (see the Objective's
changelog decision — no pending fragments exist, and towncrier is not even installed in this
environment).
  </action>
  <verify>
    <automated>pytest tests/test_version.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml`'s `[project.version]` reads exactly `"5.12.0"`
    - All 6 tests in `tests/test_version.py` pass: `test_pyproject_version_is_well_formed`,
      `test_package_version_matches_pyproject`, `test_cbom_platform_version_matches_pyproject`,
      `test_reports_platform_version_matches_pyproject`,
      `test_intelligence_config_default_matches_pyproject`, `test_distribution_name_is_canonical`
    - `git diff pyproject.toml` shows exactly one changed line
  </acceptance_criteria>
  <done>pyproject.toml is the sole canonical 5.12.0 edit, and all six derived version surfaces parity-check green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update manual version literals, write release notes, and commit</name>
  <files>README.md, docs/UAT-SERIES.md, docs/release-notes/5.12.0.md</files>
  <read_first>
    - README.md line 7
    - docs/UAT-SERIES.md lines 3-10 (header) and lines 205-227 (UAT-1-02)
    - docs/release-notes/5.11.0.md (full file — the exact structure to replicate)
    - .planning/ROADMAP.md Phase 148-153 goal lines (source material for the "What's New" bullets)
  </read_first>
  <action>
1. `README.md` line 7: `# QU.I.R.K. — v5.11.0` -> `# QU.I.R.K. — v5.12.0`.
2. `docs/UAT-SERIES.md` header block (lines 3-10): update `**Version:**` to `5.12.0`, prepend a
   new `**Last Updated:**` clause naming today's date and "v5.12 milestone close / Phase 153 —
   Release Tag Cut", keep the existing "Earlier: Phase 152 wrap..." prose intact per the file's
   established accretive-narrative pattern.
3. `docs/UAT-SERIES.md` UAT-1-02 (~lines 211-227): replace every `5.11.0`/`v5.11.0` occurrence in
   that block's Pass Criteria and Notes with `5.12.0`/`v5.12.0`, following the exact substitution
   shown in the `<interfaces>` diff above.
4. Write `docs/release-notes/5.12.0.md` following `docs/release-notes/5.11.0.md`'s exact section
   structure: H1 title `# QU.I.R.K. 5.12.0 — <milestone theme>`, `**Released:**` (today's date),
   `**Milestone:** v5.12`, `## What's New` with one bullet per phase (148 Release Pipeline
   Repair, 149 Test Suite Triage, 150 Test Suite Green Baseline + CI Gate, 151 Phase-Completion
   Artifact Gates, 152 Discovery Empirical Closure, 153 Release Tag Cut), `## Known Issues`
   stating explicitly that the v5.11.0 Windows-asset gap is now closed — `v5.12.0` is the first
   version with a verified Windows artifact (cite RELEASE-01), `## Upgrade Guidance` (`pip install
   --upgrade quirk-scanner`, no breaking changes), `## See Also` (CHANGELOG.md,
   `5.11.0.md`, `docs/release-process.md`, `docs/UAT-SERIES.md`), closing italic milestone-close
   attribution line.
5. Commit with explicit paths only:
   `git add pyproject.toml README.md docs/UAT-SERIES.md docs/release-notes/5.12.0.md` then
   `git commit -m "chore(release): v5.12.0"`. Never use `git add -A`.
  </action>
  <verify>
    <automated>grep -c "5\.11\.0" README.md docs/UAT-SERIES.md | grep -v ':0$' || echo "CLEAN"</automated>
    <automated>test -f docs/release-notes/5.12.0.md && grep -c "5.12.0" docs/release-notes/5.12.0.md</automated>
    <automated>git log -1 --name-only --format='%H %s'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "5\.11\.0\|v5\.11\.0" README.md docs/UAT-SERIES.md` returns zero hits in the
      touched lines (header, UAT-1-02 Pass Criteria/Notes, README heading) — prior-milestone
      "Earlier:" prose mentioning 5.11.0 in narrative history is expected and untouched
    - `docs/release-notes/5.12.0.md` exists, contains `5.12.0`, and its `## Known Issues` section
      references RELEASE-01 / the Windows-asset gap closure, not a bare "None"
    - The most recent commit is `chore(release): v5.12.0` touching exactly
      `pyproject.toml README.md docs/UAT-SERIES.md docs/release-notes/5.12.0.md` (no unrelated
      files, confirmed via `git log -1 --name-only`)
  </acceptance_criteria>
  <done>All manual version literals read v5.12.0, release notes exist, and one clean commit lands the version bump.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editable docs/config → committed history | Version-bump edits land in a real commit on `main` ahead of the irreversible tag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153-03 | Tampering | Accidental unrelated file staged via `git add -A` | mitigate | Task 2 explicitly commits an enumerated file list only, never `-A`; acceptance criteria checks `git log -1 --name-only` for exactly those 4 files |
| T-153-04 | Repudiation | Version string drift between derived and manual surfaces | mitigate | Task 1's `pytest tests/test_version.py -x` gate plus Task 2's `grep` sweep for stale `5.11.0` literals |
| T-153-SC | Tampering | npm/pip/cargo installs | accept | No new packages — towncrier explicitly not invoked/installed per the Objective's changelog decision |
</threat_model>

<verification>
1. `pytest tests/test_version.py -x` passes (6/6).
2. No stale `5.11.0`/`v5.11.0` literal remains in README.md's heading or UAT-SERIES.md's header/
   UAT-1-02 block.
3. `docs/release-notes/5.12.0.md` exists and follows the 5.11.0.md structure.
4. Exactly one clean `chore(release): v5.12.0` commit exists with an explicit file list.
</verification>

<success_criteria>
- Version parity holds across all six derived surfaces and all three manual literals.
- Release notes exist for v5.12.0, correctly documenting the Windows-asset gap closure.
- The version-bump commit is clean, explicit, and ready for the tag.
</success_criteria>

<output>
Create `.planning/phases/153-release-tag-cut/153-02-SUMMARY.md` when done.
</output>
