---
phase: 153-release-tag-cut
plan: 04
type: execute
wave: 4
depends_on: [153-03]
files_modified: []
autonomous: true
requirements: [RELEASE-01]

must_haves:
  truths:
    - "A real release.yml run triggered by the push of the v5.12.0 tag (event=push, not workflow_dispatch) concludes success"
    - "That run's windows-package job self-test prints the OK line and the Attach-zip-to-GitHub-Release step actually runs (not skipped)"
    - "The GitHub Release for v5.12.0 carries a downloadable quirk-windows-5.12.0.zip asset"
    - "release-container.yml's run on the same tag push also concludes success (secondary, non-blocking check)"
    - "python scripts/release_tag_hygiene.py places v5.12.0 in the OK bucket, not FLAGGED, with no edit to .github/tag-hygiene-baseline.txt"
  artifacts: []
  key_links:
    - from: "refs/tags/v5.12.0 push"
      to: "release.yml windows-package job"
      via: "push event trigger (github.event_name == 'push')"
      pattern: "SELF_TEST_SIGNING: OK"
    - from: "release.yml Attach zip to GitHub Release step"
      to: "GitHub Release v5.12.0 assets"
      via: "softprops/action-gh-release"
      pattern: "quirk-windows-5.12.0.zip"
    - from: "gh release list / gh run list"
      to: "scripts/release_tag_hygiene.py evaluate_tags()"
      via: "gh CLI queries the script already wraps"
      pattern: "OK"
---

<objective>
Watch the real, tag-triggered `release.yml` and `release-container.yml` runs to completion,
confirm the Windows operator zip is attached to the `v5.12.0` GitHub Release, and confirm the
tag-hygiene guard places `v5.12.0` in its clean "OK" bucket — this is the actual RELEASE-01 proof,
not the Plan 153-01 dry-run.

Purpose: Pitfall 2 in `153-RESEARCH.md` is explicit — "Success Criterion 1 explicitly says 'not
the dry-run from Phase 148, an actual tagged run' — a plan that stops at a green dry-run and calls
RELEASE-01 satisfied fails verification." This plan's evidence must cite a run whose trigger is
`push` on `refs/tags/v5.12.0`, not `workflow_dispatch`.
Output: confirmed-green tagged `release.yml` run, confirmed Windows zip on the GitHub Release,
confirmed-green `release-container.yml` run, confirmed clean tag-hygiene guard result — all with
run URLs and command output captured for `153-VERIFICATION.md` to cite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/153-release-tag-cut/153-CONTEXT.md
@.planning/phases/153-release-tag-cut/153-RESEARCH.md
@.planning/phases/153-release-tag-cut/153-03-PLAN.md
@scripts/release_tag_hygiene.py
@.github/tag-hygiene-baseline.txt

<interfaces>
<!-- What "green" means for RELEASE-01, extracted from release.yml. Do not re-derive. -->

`windows-package` job's "CI self-test — ephemeral cert signing round-trip" step must print
`SELF_TEST_SIGNING: OK — signtool wiring verified end-to-end` (not fail, as it did on v5.11.0's
first real tag run). The "Attach zip to GitHub Release" step is gated
`if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')` — on the real tag push
this step MUST run (not skip), using `softprops/action-gh-release`.

`.github/tag-hygiene-baseline.txt` header (verbatim, read-only reference — do not add a line):
"Adding a line here is an explicit, reviewable disposition — NEVER a way to make a *new* release
failure quiet." `v5.12.0` must land in the guard's OK bucket purely because `release.yml`
produced a real successful run whose `headBranch`/`displayTitle` contains `v5.12.0`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Watch the tag-triggered release.yml and release-container.yml runs to green</name>
  <files>(no files created — watches live CI runs and records evidence in the SUMMARY)</files>
  <read_first>
    - .planning/phases/153-release-tag-cut/153-03-SUMMARY.md (the tag-push timestamp and confirmed SHA)
    - .github/workflows/release.yml (publish job + windows-package job's Attach-zip step gate)
    - .github/workflows/release-container.yml (trigger condition — confirm it also fires on this tag push)
  </read_first>
  <action>
  1. `gh run list --branch main --limit 6 --json databaseId,workflowName,event,headBranch,conclusion,createdAt`
     — identify the `release.yml` run and the `release-container.yml` run triggered by the
     `v5.12.0` tag push (both should show `event: "push"`, created at/after the tag-push
     timestamp from Plan 153-03's SUMMARY).
  2. `gh run watch <release.yml-run-id>` until conclusion. Confirm via
     `gh run view <run-id> --json conclusion,event,headBranch` that `event` is `"push"` (NOT
     `"workflow_dispatch"`) — this is the load-bearing distinction Pitfall 2 warns about.
  3. Confirm the `windows-package` job's log contains
     `SELF_TEST_SIGNING: OK — signtool wiring verified end-to-end`
     (`gh run view <run-id> --log | grep 'SELF_TEST_SIGNING'`).
  4. Confirm the "Attach zip to GitHub Release" step in that job's log shows as run (not skipped)
     — this is the step that was gated skip on the Plan 153-01 dry-run and must now actually
     execute.
  5. Confirm the `publish` job (PyPI) concluded `success`.
  6. `gh run watch <release-container.yml-run-id>` until conclusion; record its result. This is a
     secondary, non-blocking check per `153-RESEARCH.md` Open Question 3 — record it, but do not
     treat a failure here as blocking RELEASE-01 itself; if it fails, note it explicitly as a
     known gap in the SUMMARY rather than silently omitting it.
  7. If `release.yml`'s `windows-package` or `publish` job concludes anything other than
     `success`: STOP, this is a genuine RELEASE-01 failure on the real tag — report the run URL
     and log excerpt as a blocker for the SUMMARY; do not proceed to Task 2.
  </action>
  <verify>
    <automated>gh run list --branch main --limit 6 --json databaseId,workflowName,event,conclusion</automated>
  </verify>
  <acceptance_criteria>
    - A `release.yml` run exists with `event: "push"` and `headBranch`/tag context containing
      `v5.12.0`, overall conclusion `success`
    - That run's `windows-package` job log contains `SELF_TEST_SIGNING: OK — signtool wiring
      verified end-to-end`
    - That run's "Attach zip to GitHub Release" step shows as executed (not skipped)
    - That run's `publish` job concluded `success`
    - The corresponding `release-container.yml` run's conclusion is recorded (success or failure,
      either way, in the SUMMARY)
    - Run IDs/URLs for both workflows are recorded in the SUMMARY
  </acceptance_criteria>
  <done>The real tagged release.yml run is confirmed green with the self-test passing and the zip-attach step actually executing, and release-container.yml's result is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the GitHub Release asset and run the tag-hygiene guard</name>
  <files>(no files created — inspects GitHub Release state and runs an existing script, records evidence in the SUMMARY)</files>
  <read_first>
    - scripts/release_tag_hygiene.py (evaluate_tags / collect_backed_tags — what "OK" vs "FLAGGED" means)
    - .github/tag-hygiene-baseline.txt (header warning against adding a new-tag exemption line)
  </read_first>
  <action>
  1. `gh release view v5.12.0 --json assets,body,isDraft,tagName` — confirm `tagName` is
     `v5.12.0`, `isDraft` is `false`, and `assets` includes an entry whose name matches
     `quirk-windows-5.12.0.zip`.
  2. `python scripts/release_tag_hygiene.py` (requires `gh` auth + network, already available) —
     confirm `v5.12.0` appears in the script's OK/clean output, not its FLAGGED output. If the
     script's own scheduled/on-demand nature means it needs `release.yml`'s run to have fully
     registered first, re-run once after a short wait rather than adding a baseline line.
  3. Do NOT add any line to `.github/tag-hygiene-baseline.txt` for `v5.12.0` — confirm the file
     is untouched (`git status --porcelain .github/tag-hygiene-baseline.txt` is empty) regardless
     of the guard's outcome. If the guard flags `v5.12.0` for a reason other than "no backing run
     found yet", STOP and report it as a blocker rather than adding an exemption line.
  4. Record the `gh release view` JSON output (assets array) and the tag-hygiene script's output
     for `v5.12.0` in the SUMMARY.
  </action>
  <verify>
    <automated>gh release view v5.12.0 --json assets,tagName,isDraft</automated>
    <automated>python scripts/release_tag_hygiene.py</automated>
    <automated>git status --porcelain .github/tag-hygiene-baseline.txt</automated>
  </verify>
  <acceptance_criteria>
    - `gh release view v5.12.0 --json assets` lists an asset named `quirk-windows-5.12.0.zip`
    - `python scripts/release_tag_hygiene.py`'s output places `v5.12.0` in the OK/clean bucket
    - `git status --porcelain .github/tag-hygiene-baseline.txt` is empty — no baseline edit made
    - Both raw outputs are captured verbatim (or excerpted with the relevant `v5.12.0` line) in
      the SUMMARY
  </acceptance_criteria>
  <done>The GitHub Release carries the Windows zip asset and the tag-hygiene guard confirms v5.12.0 is clean, with no baseline file edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public GitHub Release page → operator downloads | The Windows zip asset is the artifact operators actually download; its presence and integrity matter |
| Tag-hygiene guard → baseline file | The guard's exemption list must stay a reviewable, deliberate ledger, never a rubber stamp for a new failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153-07 | Repudiation | Citing the Plan 153-01 dry-run as RELEASE-01 proof instead of the real tagged run | mitigate | Task 1 explicitly checks `event: "push"` (not `workflow_dispatch`) via `gh run view --json event,headBranch` before accepting any run as evidence |
| T-153-08 | Tampering | Silently adding a v5.12.0 baseline-exemption line to make the hygiene guard pass trivially | mitigate | Task 2 explicitly forbids the edit and asserts `git status --porcelain .github/tag-hygiene-baseline.txt` is empty regardless of outcome |
| T-153-09 | Denial of Service | A broken `release-container.yml` run silently waved through as "the release is fine" | mitigate | Task 1 records the container-workflow's conclusion explicitly in the SUMMARY even though it is non-blocking per Open Question 3 |
| T-153-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed by this plan |
</threat_model>

<verification>
1. `release.yml` run with `event: "push"` on the `v5.12.0` tag concludes `success`, self-test OK,
   attach-zip step executed, `publish` succeeded.
2. `release-container.yml`'s corresponding run result recorded.
3. `gh release view v5.12.0 --json assets` shows `quirk-windows-5.12.0.zip`.
4. `python scripts/release_tag_hygiene.py` places `v5.12.0` in the OK bucket with no baseline
   file edit.
</verification>

<success_criteria>
- RELEASE-01 is proven by a real tagged CI run, not the dry-run — the specific distinction
  ROADMAP.md's success criteria and 153-RESEARCH.md's Pitfall 2 both call out.
- The Windows operator zip is confirmed downloadable from the GitHub Release.
- The tag-hygiene guard confirms v5.12.0 is clean without any exemption-file edit.
</success_criteria>

<output>
Create `.planning/phases/153-release-tag-cut/153-04-SUMMARY.md` when done.
</output>
