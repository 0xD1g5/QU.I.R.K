---
phase: 153-release-tag-cut
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [RELEASE-01]

must_haves:
  truths:
    - "GitHub Actions' view of main reflects the exact same SHA as local main before any pre-tag check is trusted"
    - "Python CI, Dashboard Quality, and Python Staleness Gate all report success on that pushed SHA"
    - "A workflow_dispatch dry-run of release.yml exercises the full windows-package job with zero publish/asset-attach side effects and its CI self-test signing step prints the exact OK line"
  artifacts: []
  key_links:
    - from: "origin/main"
      to: "GitHub Actions Python CI / Dashboard Quality / Python Staleness Gate"
      via: "git push origin main + gh run list --branch main"
      pattern: "success"
    - from: "gh workflow run release.yml --ref main"
      to: ".github/workflows/release.yml windows-package job"
      via: "workflow_dispatch event (not push/tags)"
      pattern: "SELF_TEST_SIGNING: OK"
---

<objective>
Verify `origin/main` is in sync with local `main` (confirm live, do not assume), confirm real
GitHub Actions CI is green on that exact SHA, then run the Phase 148 `workflow_dispatch` dry-run
of `release.yml` and confirm its `windows-package` job's signing self-test passes — all with zero
external publish side effects.

Purpose: `docs/release-process.md` Step 1 ("verify CI is green on main") and Step 2 (the dry-run)
are both meaningless against unpushed commits — GitHub Actions only sees what is on `origin`. As
of this session, `origin/main` and local `main` are already in sync at `e619de4` (Phase 152's
close commit) — the earlier 40-commit gap between them (Phase 151 + Phase 152 work landing before
Phase 150's close commit reached `origin`) was closed by a push earlier in this session, before
this plan was written. This task's job is therefore to confirm that sync live via `git`/`gh`
rather than trust it from memory, then push any further commits that may have landed since (this
task's push step is idempotent — a no-op if nothing new is pending) and proceed with the real CI
and dry-run verification the runbook requires.
Output: `origin/main` confirmed in sync with local `main`, a confirmed-green real CI run on that
SHA, and a confirmed-green `workflow_dispatch` dry-run of `release.yml` with the self-test signing
line captured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/153-release-tag-cut/153-CONTEXT.md
@.planning/phases/153-release-tag-cut/153-RESEARCH.md
@docs/release-process.md

<interfaces>
<!-- Environment facts confirmed by research. Do not re-derive. -->

- `gh` CLI 2.97.0, authenticated as `0xD1g5`. `git` is system git.
- As of this session, `origin/main` and local `main` are already in sync at `e619de4` (Phase 152's
  close commit — `docs(152): close Phase 152 — roadmap/requirements/state checkboxes`). Any
  earlier gap between them was closed by a push earlier in this session, before this plan was
  authored. `git status --porcelain` is clean. This task re-confirms the sync live rather than
  trusting this note, and its push step is idempotent (a no-op if `origin/main` is already
  current) in case further commits land before execution.
- `gh run list --branch main` most recently showed GitHub Actions' view of `main` still at
  `b0c99df` (Phase 150's close commit) with all three jobs ("Python CI", "Dashboard Quality",
  "Python Staleness Gate") green; a subsequent push during this session moved GitHub Actions'
  view forward and this task's job is to confirm the current pushed SHA is green, not to trust
  that earlier snapshot.
- `.github/workflows/release.yml` — `windows-package` job (windows-latest, `contents: write`):
  builds the PyInstaller onedir EXE, reads the version via
  `python -c "from quirk import __version__; print(__version__)"`, runs the "CI self-test —
  ephemeral cert signing round-trip" step (must print `SELF_TEST_SIGNING: OK — signtool wiring
  verified end-to-end`), assembles `quirk-windows-<version>.zip`. On `workflow_dispatch` the zip
  uploads as an inspectable artifact named `quirk-windows-dry-run` — never attached to a Release.
  `publish` (PyPI) and "Attach zip to GitHub Release" are both gated on
  `github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')`, so a dispatch run against
  any ref, including a tag ref, never publishes or attaches — see `release.yml`'s own comments
  (RELEASE-02, D-06).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm origin/main is in sync with local main and real CI green on that SHA</name>
  <files>(no files created — verifies/pushes already-committed work and records run evidence in the SUMMARY)</files>
  <read_first>
    - .planning/phases/153-release-tag-cut/153-RESEARCH.md "Prerequisite: Unpushed Local Commits" section
    - docs/release-process.md Step 1 ("Verify CI is green on main")
    - .github/workflows/python-ci.yml (trigger conditions — confirm push:branches:[main] fires this)
  </read_first>
  <action>
Preconditions, verified before doing anything else:
  - `git status --porcelain` is clean and current branch is `main`.
  - `git rev-list --left-right --count origin/main...HEAD` — record the result. A zero/zero result
    confirms local and `origin/main` are already in sync (the expected state this session). A
    non-zero right count with zero left means local is ahead and this task's push step is not a
    no-op. A non-zero left count means diverged history — STOP and report a diverged-history
    blocker rather than force-pushing.

Then:
  1. `git push origin main` (idempotent — a no-op with nothing to push if already in sync; pushes
     any pending commits otherwise).
  2. Confirm `git rev-parse HEAD` equals `origin/main`'s SHA via
     `git ls-remote origin refs/heads/main`.
  3. `gh run list --branch main --limit 3 --json headSha,name,conclusion,createdAt,databaseId` —
     identify the run(s) triggered by this SHA (whether from this task's push or an earlier one
     this session).
  4. Wait for all three workflows ("Python CI", "Dashboard Quality", "Python Staleness Gate") to
     conclude on that SHA (`gh run watch <id>` per run, or poll `gh run list`).
  5. Record each run's ID, URL, and conclusion in the SUMMARY. All three MUST be `success`. If any
     is `failure`, STOP — do not proceed to Task 2 or any later plan in this phase. Report the
     failing run's log excerpt as a blocker; this is Phase 149/150's remediated baseline, so any
     failure here is unexpected and needs investigation before this phase can continue.
  </action>
  <verify>
    <automated>git rev-parse HEAD; git ls-remote origin refs/heads/main</automated>
    <automated>gh run list --branch main --limit 3 --json headSha,name,conclusion,createdAt</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse HEAD` equals the SHA returned by `git ls-remote origin refs/heads/main`
    - `gh run list --branch main --limit 3` shows entries for "Python CI", "Dashboard Quality",
      and "Python Staleness Gate" all with `headSha` matching the pushed SHA and
      `conclusion: "success"`
    - Each run's ID/URL is recorded in the SUMMARY
  </acceptance_criteria>
  <done>origin/main is confirmed in sync with local main, and all three required CI workflows report success on that exact SHA, with run URLs recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Run the release.yml workflow_dispatch dry-run and confirm the signing self-test passes</name>
  <files>(no files created — triggers and inspects a CI run, records evidence in the SUMMARY)</files>
  <read_first>
    - docs/release-process.md Step 2 (the dry-run mechanism and its documented zero-side-effect guarantee)
    - .github/workflows/release.yml (windows-package job, "CI self-test — ephemeral cert signing round-trip" step and the event-gated publish/attach steps)
  </read_first>
  <action>
  1. `gh workflow run release.yml --ref main` (workflow_dispatch — this run's `github.event_name`
     is `workflow_dispatch`, never `push`, so `publish` and "Attach zip to GitHub Release" both
     skip by construction; confirm this in the run's job list, do not just trust the YAML).
  2. Identify the new run ID (`gh run list --workflow release.yml --limit 3 --json
     databaseId,event,status,conclusion,createdAt`) and `gh run watch <run-id>`.
  3. Confirm the `windows-package` job runs to `success` and its "CI self-test — ephemeral cert
     signing round-trip" step's log contains the literal line
     `SELF_TEST_SIGNING: OK — signtool wiring verified end-to-end`
     (`gh run view <run-id> --log | grep 'SELF_TEST_SIGNING'`).
  4. Confirm the "Upload dry-run zip artifact" step ran (artifact name `quirk-windows-dry-run`)
     and that the `publish` job and the "Attach zip to GitHub Release" step both show as skipped
     (not run) in the job list — proving the zero-side-effect guarantee held for real, not just by
     YAML inspection.
  5. If the self-test step fails: STOP. Per `docs/release-process.md`'s explicit stop rule, do not
     proceed to any later plan in this phase — an immutable tag pushed against a broken pipeline
     is exactly the `v5.11.0` failure this phase exists to not repeat. Report the failure with the
     run URL and log excerpt as a blocker.
  6. Record the run ID, URL, conclusion, and the self-test log line in the SUMMARY.
  </action>
  <verify>
    <automated>gh run list --workflow release.yml --limit 3 --json databaseId,event,status,conclusion</automated>
  </verify>
  <acceptance_criteria>
    - A `release.yml` run exists with `event: "workflow_dispatch"` (not `push`) whose overall
      conclusion is `success`
    - That run's `windows-package` job log contains `SELF_TEST_SIGNING: OK — signtool wiring
      verified end-to-end`
    - That run's `publish` job and "Attach zip to GitHub Release" step show as skipped, not run
    - Run ID, URL, and the self-test log line are recorded in the SUMMARY
  </acceptance_criteria>
  <done>The dry-run of release.yml is green, the signing self-test passes for real, and zero publish/attach side effects occurred, with evidence captured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → public `origin` remote | Confirming (and, if needed, pushing) already-reviewed, already-committed work to a public repository |
| `workflow_dispatch` trigger → release.yml job matrix | A manually-triggered dry-run must not publish to PyPI or attach a GitHub Release asset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153-01 | Tampering | `workflow_dispatch` dry-run accidentally publishing/attaching | accept | Already mitigated by `release.yml`'s `github.event_name == 'push'` guard (Phase 148, D-06), not modified by this phase; Task 2 explicitly verifies the guard held live rather than trusting YAML alone |
| T-153-02 | Repudiation | Claiming CI is green from memory rather than a real run | mitigate | Task 1/2 require citing a `gh run` ID/URL and `conclusion: "success"` for every claim; no PASS is recorded from a local-only run |
| T-153-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed by this plan — all tooling (`gh`, `git`) is pre-existing, already-vetted project infrastructure |
</threat_model>

<verification>
1. `origin/main` SHA equals local `main` SHA.
2. `gh run list --branch main --limit 3` shows Python CI / Dashboard Quality / Python Staleness
   Gate all `success` on that SHA.
3. A `workflow_dispatch` run of `release.yml` concludes `success`, its `windows-package` job log
   contains `SELF_TEST_SIGNING: OK — signtool wiring verified end-to-end`, and `publish`/attach
   steps show as skipped.
</verification>

<success_criteria>
- origin/main confirmed in sync with local main, proven live by matching SHAs (not assumed from
  planning-time notes).
- Real GitHub Actions CI (not a local run) is green on that SHA for all three required workflows.
- The release pipeline's signing self-test is proven passing on a live dry-run with zero publish
  side effects, evidenced by run URLs and log excerpts, not inspection alone.
</success_criteria>

<output>
Create `.planning/phases/153-release-tag-cut/153-01-SUMMARY.md` when done.
</output>
</output>
