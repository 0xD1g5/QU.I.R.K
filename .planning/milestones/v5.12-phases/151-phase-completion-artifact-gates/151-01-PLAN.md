---
phase: 151-phase-completion-artifact-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify_phase_gates.py
  - tests/test_verify_phase_gates.py
autonomous: true
requirements: [ARTIFACT-01, ARTIFACT-02, ARTIFACT-03, ARTIFACT-04]

must_haves:
  truths:
    - "A pure function can decide, given only parsed file content, whether a phase's VERIFICATION.md is missing"
    - "A pure function can decide, given only parsed VALIDATION.md content, whether it is stale (nyquist_compliant: false, or a genuine pending table row) without false-positiving on the glyph legend line"
    - "A pure function can decide, given a phase's plan files_modified lists, whether docs/UAT-SERIES.md needs a matching Series entry and whether one exists"
    - "A loader can parse a phase directory's real <N>-NN-PLAN.md frontmatter files on disk and extract their files_modified lists, feeding check_phase_close()'s ARTIFACT-03 gate with real data instead of a placeholder"
    - "A pure function can decide, given a working-tree phase-directory listing and STATE.md phase-map data, whether a Complete-marked phase's content has vanished with no matching milestone archive — including when the deleted files were never git-tracked"
  artifacts:
    - path: "scripts/verify_phase_gates.py"
      provides: "check_phase_close() + check_destructive_archive() pure decision functions plus their file-loading/parsing helpers"
      contains: "def check_phase_close"
    - path: "tests/test_verify_phase_gates.py"
      provides: "Unit coverage for all four ARTIFACT gates using literal fixtures, no subprocess/network"
      contains: "def test_"
  key_links:
    - from: "scripts/verify_phase_gates.py"
      to: "tests/test_verify_phase_gates.py"
      via: "importlib.util.spec_from_file_location module loading (scripts/ has no __init__.py)"
      pattern: "spec_from_file_location"
---

<objective>
Build the pure, unit-testable core of QUIRK's phase-completion artifact gate: two decision
functions — `check_phase_close()` (ARTIFACT-01/02/03) and `check_destructive_archive()`
(ARTIFACT-04) — plus their file-loading/parsing helpers, in `scripts/verify_phase_gates.py`,
following the exact pure-function/CLI-glue split already established by
`scripts/release_tag_hygiene.py`. This plan does NOT wire the git hook itself (that is 151-02) —
it produces and tests the decision logic in isolation, with literal fixture inputs.

Purpose: this is the exact enforcement logic that would have caught all three v5.11 gaps (Phase
145's missing VERIFICATION.md, Phase 147's stale VALIDATION.md, Phase 144's missing UAT-SERIES.md
entry) and the `phases.clear` incident that deleted ~39 unrecoverable phase files
(`.planning/milestones/v5.11-phases/ARCHIVE-MANIFEST.md`).
Output: `scripts/verify_phase_gates.py` with tested, pure `check_phase_close()` and
`check_destructive_archive()` functions; `tests/test_verify_phase_gates.py` with one test per gate
condition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/151-phase-completion-artifact-gates/151-CONTEXT.md
@.planning/phases/151-phase-completion-artifact-gates/151-RESEARCH.md
@.planning/phases/151-phase-completion-artifact-gates/151-PATTERNS.md

<interfaces>
<!-- scripts/release_tag_hygiene.py is the canonical shape to mirror. Key signatures: -->

From scripts/release_tag_hygiene.py (existing, verified in this repo):
```python
REPO_ROOT = pathlib.Path(__file__).resolve().parent.parent

def load_baseline(path: pathlib.Path) -> dict[str, str]:
    """Missing file -> empty result, never raise."""

def evaluate_tags(
    tags: list[str],
    released_tags: set[str],
    baseline: dict[str, str],
) -> tuple[list[str], list[str], str]:
    """Pure: no subprocess, no network, no env reads. Returns
    (flagged, exempted, summary_markdown)."""
```

Mirror this exact split for `scripts/verify_phase_gates.py`. Target signatures (names are
mandatory, exact parameter shapes are Claude's discretion as long as they stay pure):

```python
REPO_ROOT = pathlib.Path(__file__).resolve().parent.parent
PHASES_ROOT = REPO_ROOT / ".planning" / "phases"
MILESTONES_ROOT = REPO_ROOT / ".planning" / "milestones"
STATE_PATH = REPO_ROOT / ".planning" / "STATE.md"
ROADMAP_PATH = REPO_ROOT / ".planning" / "ROADMAP.md"
UAT_SERIES_PATH = REPO_ROOT / "docs" / "UAT-SERIES.md"

# ARTIFACT-01/02/03
def check_phase_close(
    phase_num: str,
    verification_exists: bool,
    validation_frontmatter: dict | None,
    validation_body_text: str | None,
    plan_files_modified: list[list[str]],
    uat_series_text: str,
) -> tuple[bool, list[str], str]:
    """Pure. Returns (blocked, reasons, summary_markdown)."""

# ARTIFACT-04
def check_destructive_archive(
    phase_map_rows: list[tuple[str, str]],   # (phase_num, milestone_tag) for every Complete row
    disk_phase_dirs: set[str],               # non-empty dir names currently under .planning/phases/
    archived_dirs_by_milestone: dict[str, set[str]],  # milestone_tag -> archived dir names
) -> tuple[bool, list[str], str]:
    """Pure. Returns (blocked, reasons, summary_markdown)."""
```

Loader/helper functions (impure, read from disk — thin wrappers, tested via `tmp_path` per
`load_baseline`'s pattern, NOT via mocking):

```python
def load_validation_frontmatter(path: pathlib.Path) -> dict | None: ...
def load_phase_plan_files_modified(phase_dir: pathlib.Path) -> list[list[str]]: ...
def is_validation_stale(frontmatter: dict | None, body_text: str) -> tuple[bool, list[str]]: ...
def user_facing_plan_match(files_modified: list[str]) -> bool: ...
def uat_series_has_entry(uat_series_text: str, phase_num: str) -> bool: ...
def disk_phase_dirs_under(phases_root: pathlib.Path) -> set[str]: ...
def archived_phase_dirs(milestones_root: pathlib.Path, milestone_tag: str) -> set[str]: ...
def parse_state_phase_maps(state_text: str) -> list[tuple[str, str, str]]: ...  # (phase_num, milestone_tag, status_cell)
```
</interfaces>

<!-- Real on-disk fixture content the tests should reuse verbatim (verified this session) -->
@.planning/milestones/v5.11-phases/147-backlog-drain-lifecycle-ledger-tail/147-VALIDATION.md
@.planning/phases/150-test-suite-green-baseline-ci-gate/150-VERIFICATION.md
@.planning/phases/150-test-suite-green-baseline-ci-gate/150-01-PLAN.md
@scripts/release_tag_hygiene.py
@tests/test_release_tag_hygiene.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ARTIFACT-01/02/03 — check_phase_close() and its loaders</name>
  <files>scripts/verify_phase_gates.py, tests/test_verify_phase_gates.py</files>
  <read_first>
    - scripts/release_tag_hygiene.py (full file — the exact shape to mirror: module docstring, REPO_ROOT constant, pure-function/loader split, `load_baseline`'s missing-file-returns-empty pattern)
    - tests/test_release_tag_hygiene.py lines 1-33 (the `importlib.util.spec_from_file_location` module-loading pattern — mandatory, `scripts/` has no `__init__.py`)
    - .planning/milestones/v5.11-phases/147-backlog-drain-lifecycle-ledger-tail/147-VALIDATION.md (real frontmatter + the exact legend line `*Status: pending-glyph green-glyph red-glyph flaky-glyph*` — the Pitfall 4 false-positive trap; this file is fully green and must NOT block)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-VERIFICATION.md lines 1-7 (real VERIFICATION.md frontmatter shape, for context only — ARTIFACT-01's check is presence-only, not content validation)
    - docs/UAT-SERIES.md line 17228 (`## Series 150: Test Suite Green Baseline + CI Gate (Phase 150 — v5.12)` — the exact heading shape ARTIFACT-03 must detect)
    - .planning/phases/150-test-suite-green-baseline-ci-gate/150-01-PLAN.md lines 1-13 (real `files_modified:` YAML frontmatter block — the literal fixture `load_phase_plan_files_modified()` must parse: `quirk/scanner/kerberos_scanner.py`, `tests/test_identity_scanner_hardening.py`, `tests/skip_registry.py`, `docs/test-triage-149.md`)
    - 151-RESEARCH.md "Pattern 3" (VALIDATION.md frontmatter shape), "Pattern 4" (UAT-SERIES.md heading shape), "Pitfall 4" (legend-line false positive), and the Security Domain table (yaml.safe_load only)
  </read_first>
  <behavior>
    - Test: check_phase_close() blocks with a reason mentioning "VERIFICATION.md" when verification_exists=False, regardless of the other inputs' state.
    - Test: check_phase_close() does NOT block on ARTIFACT-01 when verification_exists=True.
    - Test: is_validation_stale() returns (True, [...]) when frontmatter={"nyquist_compliant": False, ...}.
    - Test: is_validation_stale() returns (True, [...]) when the body text contains a genuine pending table row (a pipe-delimited row whose Status cell is the pending glyph).
    - Test: is_validation_stale() returns (False, []) against the real, verbatim 147-VALIDATION.md content (frontmatter nyquist_compliant: true + the legend-line pending-glyph occurrence must NOT trip the pending-row detector — Pitfall 4).
    - Test: is_validation_stale() returns (True, [...]) when validation_frontmatter is None (file missing) — a phase cannot close with no VALIDATION.md either.
    - Test: user_facing_plan_match() returns True for a files_modified list containing a src/dashboard/... path, a quirk/cli/... path, and a new file under quirk/scanner/...; returns False for a list containing only tests/..., scripts/..., or .github/... paths.
    - Test: uat_series_has_entry() returns True against the real docs/UAT-SERIES.md text for phase "150" (matching the real Series 150 heading) and False for a phase number with no matching heading.
    - Test: load_phase_plan_files_modified() parses a tmp_path fixture reproducing 150-01-PLAN.md's real frontmatter verbatim (a single `<phase>-01-PLAN.md` file) and returns `[["quirk/scanner/kerberos_scanner.py", "tests/test_identity_scanner_hardening.py", "tests/skip_registry.py", "docs/test-triage-149.md"]]` — one inner list per PLAN.md file found.
    - Test: load_phase_plan_files_modified() returns one list per PLAN.md file when a tmp_path phase directory contains two literal `*-NN-PLAN.md` fixtures (e.g. `X-01-PLAN.md` and `X-02-PLAN.md`), preserving per-plan grouping rather than flattening into one list.
    - Test: load_phase_plan_files_modified() returns `[]` (never raises) when given a tmp_path phase directory containing no `*-PLAN.md` files, and when given a nonexistent directory path.
    - Test: check_phase_close(), when called with `plan_files_modified` set to the real parsed output of load_phase_plan_files_modified() against the 150-01-PLAN.md fixture (Phase 150's files_modified includes `quirk/scanner/kerberos_scanner.py`, which matches the user-facing glob) and a uat_series_text with no matching Series heading for that phase number, blocks with a reason mentioning "UAT-SERIES.md" — proving the loader's real output, not a hand-typed literal list, correctly drives the ARTIFACT-03 gate end-to-end within this plan's pure layer.
    - Test: check_phase_close() blocks on ARTIFACT-03 when plan_files_modified matches the user-facing heuristic and uat_series_text has no matching Series heading; does NOT block when plan_files_modified is entirely internal-only paths, even with no UAT-SERIES.md entry.
    - Test: check_phase_close() returns (False, [], ...) (clean) when all three conditions pass — construct this as one literal "everything green" fixture case.
  </behavior>
  <action>
Create `scripts/verify_phase_gates.py` with the module docstring/imports/REPO_ROOT-and-path-constants
shape from `scripts/release_tag_hygiene.py` (cite the three v5.11 incidents plus the
ARCHIVE-MANIFEST.md incident as the "why this exists" — this file's docstring is read by every
future contributor deciding whether to trust the gate). Implement `check_phase_close()` as a pure
function per the Interfaces block signature: it aggregates the three sub-verdicts (VERIFICATION.md
presence, VALIDATION.md staleness via `is_validation_stale()`, UAT-SERIES.md coverage via
`user_facing_plan_match()` + `uat_series_has_entry()`) into one `(blocked, reasons, summary)` tuple,
with one human-readable reason string per violated gate (not one combined string).

Implement `is_validation_stale()` to parse VALIDATION.md frontmatter (already-parsed dict, loaded
by the caller with `yaml.safe_load()` — never bare `yaml.load()`) plus the raw body text. The
pending-row detector must be scoped to actual markdown table rows (lines starting with `|` whose
cell content is the literal pending glyph), explicitly excluding any line starting with `*Status:`
(the legend line) — per Pitfall 4, write the excluding test above BEFORE the implementation and
confirm it goes red first.

Implement `user_facing_plan_match()` against a module-level glob/prefix list: `src/dashboard/`,
`quirk/cli/`, `quirk/reports/` (report-renderer surfaces), and any new file under `quirk/scanner/`
or `quirk/hardware` (new scanner/detector modules) — per CONTEXT.md D-05 and RESEARCH.md
Assumption A1. `tests/`, `scripts/`, `.github/`, and `docs/`-only paths never match.

Implement `uat_series_has_entry()` using the regex from RESEARCH.md Pattern 4: a MULTILINE search
for `^## Series \d+:.*\(Phase {phase_num}\b` against the UAT-SERIES.md text.

Also implement `load_validation_frontmatter()` as a loader (split VALIDATION.md on `---`
delimiters, `yaml.safe_load()` the first document block; missing file or missing/malformed
frontmatter returns `None`, matching `load_baseline`'s missing-file-returns-empty-not-raise
philosophy).

Also implement `load_phase_plan_files_modified(phase_dir: pathlib.Path) -> list[list[str]]` as a
loader: glob `phase_dir` for `*-PLAN.md` files (sorted, deterministic order), for each one split on
`---` delimiters and `yaml.safe_load()` the frontmatter block exactly like
`load_validation_frontmatter()`, then extract the `files_modified` key (a YAML list of path
strings) and append it as one inner list to the result. A PLAN.md with missing/malformed
frontmatter or no `files_modified` key contributes an empty inner list for that file rather than
raising or being skipped silently — every found PLAN.md file must be accounted for in the returned
list's length. A nonexistent or empty `phase_dir` returns `[]`, matching
`load_validation_frontmatter`'s missing-file-returns-empty-not-raise philosophy. This is the loader
that supplies `check_phase_close()`'s `plan_files_modified` parameter with real data in 151-02's
`main()` — RESEARCH.md's architecture diagram names this exact step ("if any `<N>-NN-PLAN.md`
files_modified matches a user-facing path glob", line 119); reuse the real
`150-01-PLAN.md` `files_modified:` block verbatim as the primary test fixture (per the Interfaces
block, do not hand-invent a synthetic frontmatter shape when a real one is available).

Write every RED test in `tests/test_verify_phase_gates.py` first (using the `importlib.util`
module-loading pattern + a module-scoped `vpg` fixture, exact mirror of
`tests/test_release_tag_hygiene.py` lines 1-33), confirm each fails for the right reason, then
implement until green.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m pytest tests/test_verify_phase_gates.py -x -k "verification_missing or validation_stale or uat_series or phase_close" -v</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify_phase_gates.py` defines `check_phase_close`, `is_validation_stale`, `user_facing_plan_match`, `uat_series_has_entry`, `load_validation_frontmatter`, and `load_phase_plan_files_modified` as free functions with no `subprocess`/network calls inside `check_phase_close`, `is_validation_stale`, `user_facing_plan_match`, or `uat_series_has_entry`
    - `load_phase_plan_files_modified()` is implemented and unit-tested against a literal fixture reproducing 150-01-PLAN.md's real `files_modified:` frontmatter block verbatim, including the multi-PLAN.md-file and empty/missing-directory cases
    - Every YAML parse call in the file uses `yaml.safe_load`, never bare `yaml.load`
    - `tests/test_verify_phase_gates.py` contains at least 10 test functions covering the behavior list above, all passing
    - The legend-line fixture test (against real `147-VALIDATION.md` content) passes — proving Pitfall 4 is closed
    - `pytest tests/test_verify_phase_gates.py -x -k "verification_missing or validation_stale or uat_series or phase_close"` exits 0
  </acceptance_criteria>
  <done>check_phase_close() and its three sub-checks are implemented, unit-tested against literal and real-fixture inputs, and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ARTIFACT-04 — check_destructive_archive() and its loaders</name>
  <files>scripts/verify_phase_gates.py, tests/test_verify_phase_gates.py</files>
  <read_first>
    - .planning/milestones/v5.11-phases/ARCHIVE-MANIFEST.md (the literal incident: `phases.clear` deleted `.planning/phases/*` as a filesystem operation with zero git-visible trigger; only 19 of ~58 phase files survived because only those had ever been `git add`-ed)
    - .planning/STATE.md "v5.12 Phase Map" and "v5.11 Phase Map" sections (real markdown table shape: `| Phase | Name | Requirements | Gate | Status |`, with `Complete (...)` cell text for finished phases) — the parser target for `parse_state_phase_maps()`
    - 151-RESEARCH.md "Pitfall 1" (staged-diff detection is structurally blind to untracked-file deletion — this is why the mechanism must be working-tree directory-listing comparison, not `git diff`) and its "Working-tree directory-listing comparison" code example
    - 151-RESEARCH.md "Pitfall 2" (a git hook cannot make `phases.clear` itself refuse to run — the achievable guarantee is that the *next* commit is blocked, not that the delete never happens; word tests and docstrings accordingly)
  </read_first>
  <behavior>
    - Test: check_destructive_archive() blocks when a phase_map_rows entry marks phase "144" Complete under milestone_tag "v5.11", "144-..." is absent from disk_phase_dirs, and archived_dirs_by_milestone["v5.11"] does not contain a matching directory — this is the literal ARCHIVE-MANIFEST.md incident shape, reproduced as a fixture.
    - Test: check_destructive_archive() does NOT block when the same phase is absent from disk_phase_dirs but archived_dirs_by_milestone["v5.11"] DOES contain the matching directory (the correct post-archival state).
    - Test: check_destructive_archive() does NOT block when the phase's directory is present in disk_phase_dirs (still live, not yet archived — normal in-progress state).
    - Test: disk_phase_dirs_under() returns the correct set of directory names when given a tmp_path fixture containing populated and empty subdirectories — empty directories are excluded (a directory left behind after all files were removed must count as "gone").
    - Test: parse_state_phase_maps() extracts (phase_num, milestone_tag, status_cell) tuples from a literal multi-section STATE.md excerpt reproducing the real "## v5.12 Phase Map" / "## v5.11 Phase Map" heading + table shape, correctly attributing each phase row to the milestone_tag from its enclosing section heading.
    - Test (untracked-file case, Pitfall 1): using tmp_path, create a phase directory with a file written via plain filesystem write (never `git add`-ed — no real git repo needed since this function takes a working-tree snapshot, not a git diff), then simulate its deletion by removing the file/directory from the tmp_path tree before calling disk_phase_dirs_under() again; confirm the before/after snapshot comparison correctly shows the phase directory as gone regardless of git-tracking status.
  </behavior>
  <action>
Implement `check_destructive_archive()` as a pure function per the Interfaces block signature:
for every `(phase_num, milestone_tag)` row extracted by `parse_state_phase_maps()` whose
`status_cell` contains the substring `"Complete"`, verify that EITHER a matching directory name
(prefixed `{phase_num}-`) exists in `disk_phase_dirs` OR a matching directory exists in
`archived_dirs_by_milestone[milestone_tag]`. If neither exists, add a blocking reason citing the
phase number and milestone tag and pointing at `.planning/milestones/v5.11-phases/ARCHIVE-MANIFEST.md`
as the precedent this guard exists to prevent recurrence of.

Implement `parse_state_phase_maps()` to regex-scan `## v(\d+\.\d+) Phase Map` section headers and,
for each, parse the markdown table rows immediately following (`| {phase_num} | ... | {status_cell} |`)
until a blank line or the next `##` heading — capture `(phase_num, f"v{version}", status_cell_text)`
per row.

Implement `disk_phase_dirs_under()` per the RESEARCH.md code example: return the set of directory
names under `.planning/phases/` that are non-empty (`any(p.iterdir())`), treating an empty
directory the same as an absent one — this is what makes the untracked-file-deletion case
(Pitfall 1) detectable via a before/after snapshot diff at the caller level, with no git
involvement in this function at all.

Implement `archived_phase_dirs()` to list directory names under
`.planning/milestones/{milestone_tag}-phases/`.

Docstring the module-level "why" for `check_destructive_archive()` explicitly per Pitfall 2's
correction: state that this function proves "the next commit after an unarchived deletion is
blocked," not "the delete never happens" — do not let the docstring or any test name imply the
stronger, unachievable claim.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m pytest tests/test_verify_phase_gates.py -x -k destructive_archive -v</automated>
  </verify>
  <acceptance_criteria>
    - `check_destructive_archive()`, `parse_state_phase_maps()`, `disk_phase_dirs_under()`, and `archived_phase_dirs()` are pure or `tmp_path`-driven with no subprocess/git calls
    - The untracked-file-deletion test passes without ever calling `git` — proving the mechanism is git-tracking-independent (closes Pitfall 1)
    - `check_destructive_archive()`'s docstring or the module docstring explicitly states the "next commit is blocked" scope, not "delete is prevented" (closes Pitfall 2)
    - `pytest tests/test_verify_phase_gates.py -x -k destructive_archive` exits 0
    - Full file: `pytest tests/test_verify_phase_gates.py -x` exits 0 (both tasks' tests green together)
  </acceptance_criteria>
  <done>check_destructive_archive() correctly reproduces and blocks the exact ARCHIVE-MANIFEST.md incident shape as a unit test, using working-tree-state comparison rather than git-diff-based detection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disk → parser | `scripts/verify_phase_gates.py` reads and parses YAML frontmatter and markdown content from files under `.planning/` and `docs/` — content that is developer-authored but not otherwise validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-01 | Tampering | `load_validation_frontmatter()` YAML parsing | mitigate | `yaml.safe_load()` only, never bare `yaml.load()` — enforced by acceptance criteria on Task 1, matching `tests/test_release_tag_hygiene.py`'s existing usage |
| T-151-02 | Information Disclosure | Reason/summary strings surfaced to the developer's terminal | accept | Content is repo-local file paths and phase numbers already visible to any contributor with a checkout — no secret material ever enters these functions |

</threat_model>

<verification>
Run `pytest tests/test_verify_phase_gates.py -x -v` — all tests green, covering ARTIFACT-01,
ARTIFACT-02 (including the Pitfall 4 legend-line false-positive guard), ARTIFACT-03, and
ARTIFACT-04 (including the Pitfall 1 untracked-file-deletion guard) as pure, literal-fixture-driven
unit tests. No subprocess, no network, no live git repo required for this plan's tests.
</verification>

<success_criteria>
`scripts/verify_phase_gates.py` exists with `check_phase_close()` and `check_destructive_archive()`
as pure, fully unit-tested functions covering all four ARTIFACT gate conditions, including both
research-identified pitfalls (VALIDATION.md legend-line false positive, untracked-file deletion
blindness). Neither function is yet wired to a git hook — that is 151-02.
</success_criteria>

<output>
Create `.planning/phases/151-phase-completion-artifact-gates/151-01-SUMMARY.md` when done
</output>
