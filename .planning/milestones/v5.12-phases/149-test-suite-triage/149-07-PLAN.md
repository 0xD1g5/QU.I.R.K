---
phase: 149-test-suite-triage
plan: 07
type: execute
wave: 7
depends_on: ["149-06"]
files_modified:
  - tests/test_dashboard_scan_history.py
  - tests/test_dashboard_theme.py
  - tests/test_route_coverage.py
  - tests/test_db_migrate_cli.py
  - tests/test_init_db_idempotent.py
  - tests/skip_registry.py
  - docs/test-triage-149.md
autonomous: true
requirements: [SUITE-01]

must_haves:
  truths:
    - "Each of Cluster 9 Group B's 12 dashboard/API/DB-migration failures has an individually-investigated disposition"
    - "test_route_coverage.py's require_auth finding is flagged distinctly since it asserts an AUTH-02 security invariant, not routine drift"
  artifacts:
    - path: "docs/test-triage-149.md"
      provides: "12 rows under Cluster 9 for the dashboard/API/DB-migration subsystem"
---

<objective>
Individually investigate and disposition Cluster 9 Group B: 12 failures across 5 files —
`test_dashboard_scan_history.py`(5, `/api/compare` 400s), `test_dashboard_theme.py`(2, missing CSS
tokens), `test_route_coverage.py`(1, missing `require_auth` — AUTH-02 violation),
`test_db_migrate_cli.py`(3, `sqlalchemy.exc.NoSuchTableError: sensor_tokens`),
`test_init_db_idempotent.py`(1, `_ensure_columns()` signature mismatch).

Purpose: RESEARCH.md flags `/api/compare` 400s as likely genuine API-contract drift (dashboard
scan-history endpoint's contract changed since the test was written) — this needs confirmation via
reading the current endpoint, not assumption. `test_route_coverage.py` is a security-invariant test
(AUTH-02: every data route must have `require_auth`) — a failure here means either a genuinely
unprotected route (a real finding, not just test staleness) or the test's route inventory is stale;
this distinction must be made explicit since it affects Phase 150's priority.
Output: 12 tests individually investigated and quarantined with distinct, evidence-backed reasons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/149-test-suite-triage/149-CONTEXT.md
@.planning/phases/149-test-suite-triage/149-RESEARCH.md
@.planning/phases/149-test-suite-triage/149-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate + quarantine test_dashboard_scan_history.py + test_dashboard_theme.py (7 tests)</name>
  <files>tests/test_dashboard_scan_history.py, tests/test_dashboard_theme.py, tests/skip_registry.py</files>
  <read_first>tests/test_dashboard_scan_history.py, tests/test_dashboard_theme.py</read_first>
  <action>
  Read `test_dashboard_scan_history.py::test_compare_schema`, `test_compare_self`,
  `test_compare_score_delta`, `test_compare_finding_diff`, `test_compare_endpoint_diff` and trace their
  target `/api/compare` route handler (search `quirk/dashboard/api/routes/` for the compare endpoint)
  to confirm whether the request/response contract changed (e.g. required query params, response shape)
  since these tests were written — record the specific contract delta found. Read
  `test_dashboard_theme.py::test_primary_color_token` and `test_accent_color_token` (`Expected
  '--primary: 210 100% 56%' token not found` / `'--accent: 210 100% 56%'`) and check the current
  dashboard CSS/theme source for whether the token value or name changed. Quarantine all 7 with
  `@pytest.mark.xfail(reason="TRIAGE-149: <specific contract/token delta found>; see
  docs/test-triage-149.md#<slug>", strict=False)` and matching `ALLOWED_SKIPS` entries, category
  `"pre_existing_triage_149"`.
  </action>
  <verify>
    <automated>pytest tests/test_dashboard_scan_history.py tests/test_dashboard_theme.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 5 `/api/compare` tests' reason string cites the specific contract element that changed
      (not a generic "endpoint drift" statement)
    - Each theme test's reason string cites the actual current token value/name found in the source
    - 7 `@pytest.mark.xfail` decorators + 7 matching `ALLOWED_SKIPS` entries added
    - `pytest tests/test_dashboard_scan_history.py tests/test_dashboard_theme.py -q -m ""` reports 7 xfailed, 0 failed
  </acceptance_criteria>
  <done>All 7 tests individually investigated against current source and quarantined with specific findings.</done>
</task>

<task type="auto">
  <name>Task 2: Investigate + quarantine test_route_coverage.py, test_db_migrate_cli.py, test_init_db_idempotent.py (5 tests)</name>
  <files>tests/test_route_coverage.py, tests/test_db_migrate_cli.py, tests/test_init_db_idempotent.py, tests/skip_registry.py</files>
  <read_first>tests/test_route_coverage.py, tests/test_db_migrate_cli.py, tests/test_init_db_idempotent.py</read_first>
  <action>
  Read `test_route_coverage.py::test_all_data_routes_have_auth_dependency` in full and determine which
  specific route(s) it flags as missing `require_auth` (`AUTH-02 violation`); grep that route's handler
  in `quirk/dashboard/api/routes/` to confirm whether it genuinely lacks `require_auth` (a real security
  finding — flag explicitly as "SECURITY: real finding, not test staleness" in the ledger, even though
  D-01 forbids fixing it in this phase) or whether the route was added/renamed and the test's inventory
  is simply stale. Read `test_db_migrate_cli.py::test_fresh_db_reports_every_column_added`,
  `test_dry_run_does_not_write`, `test_result_shape` (`sqlalchemy.exc.NoSuchTableError: sensor_tokens`)
  and confirm whether the `sensor_tokens` table exists in the current schema/migration path (grep
  `quirk/db` or migration files) — determine if the migration helper's table list is stale or the table
  was renamed/dropped. Read `test_init_db_idempotent.py::test_all_ensure_functions_idempotent`
  (`TypeError: _ensure_columns() missing 2 required positional arguments`) and check `_ensure_columns`'s
  current signature against the test's call site to confirm an added-parameter API drift. Quarantine all
  5 with xfail + matching `ALLOWED_SKIPS` entries, reason strings citing each specific finding —
  test_route_coverage.py's reason string MUST explicitly state whether it found a real unprotected route
  or stale test inventory.
  </action>
  <verify>
    <automated>pytest tests/test_route_coverage.py tests/test_db_migrate_cli.py tests/test_init_db_idempotent.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - `test_route_coverage.py`'s reason string and ledger row explicitly state real-security-finding vs.
      stale-inventory (not left ambiguous)
    - `test_db_migrate_cli.py`'s 3 rows cite the actual current state of the `sensor_tokens` table/migration path
    - `test_init_db_idempotent.py`'s row cites `_ensure_columns`'s actual current signature
    - 5 `@pytest.mark.xfail` decorators + 5 matching `ALLOWED_SKIPS` entries added
    - `pytest tests/test_route_coverage.py tests/test_db_migrate_cli.py tests/test_init_db_idempotent.py -q -m ""` reports 5 xfailed, 0 failed
  </acceptance_criteria>
  <done>All 5 tests individually investigated with the route_coverage security-relevance question explicitly answered.</done>
</task>

<task type="auto">
  <name>Task 3: Write Cluster 9 Group B ledger rows and verify the meta-gate</name>
  <files>docs/test-triage-149.md</files>
  <read_first>docs/test-triage-149.md</read_first>
  <action>
  Add 12 rows under "## Cluster 9: Remaining individually-distinct failures" for this group's tests,
  each citing the specific finding from Tasks 1-2. For `test_route_coverage.py`'s row, prefix the
  Evidence/Notes text with `SECURITY:` if Task 2 found a genuinely unprotected route, so Phase 150
  triage can prioritize it correctly; otherwise state `stale test inventory` plainly. Run
  `pytest tests/test_skip_registry.py -q -m ""` to confirm the gate remains green.
  </action>
  <verify>
    <automated>pytest tests/test_skip_registry.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - 12 new rows added under Cluster 9 for this group, each with distinct Evidence/Notes text
    - `test_route_coverage.py`'s row's Evidence/Notes begins with `SECURITY:` if a real unprotected route was found
    - `pytest tests/test_skip_registry.py -q -m ""` passes
  </acceptance_criteria>
  <done>Cluster 9 Group B fully dispositioned and ledgered; gate remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Dashboard API routes (read-only investigation) | This plan reads route auth-dependency wiring to explain `test_route_coverage.py`'s failure but does not modify any route; if a genuinely unprotected data route is found, it is flagged for Phase 150 to fix, not patched here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-07 | Elevation of Privilege | dashboard API routes (investigation only) | mitigate | If `test_route_coverage.py` confirms a genuinely unprotected data route, this plan flags it explicitly with a `SECURITY:` prefix in the ledger row so Phase 150 treats it as a priority fix, not routine test drift. |
| T-149-07b | Tampering | tests/*.py xfail markers | accept | Test-only metadata; no production code modified. |
</threat_model>

<verification>
- `pytest tests/test_dashboard_scan_history.py tests/test_dashboard_theme.py tests/test_route_coverage.py tests/test_db_migrate_cli.py tests/test_init_db_idempotent.py -q -m ""` reports 12 xfailed, 0 failed
- `pytest tests/test_skip_registry.py -q -m ""` passes
- Ledger has 12 distinct rows for Group B, with test_route_coverage.py's security relevance explicitly stated
</verification>

<success_criteria>
All 12 Cluster 9 Group B failures individually investigated, with the route_coverage security question explicitly answered.
</success_criteria>

<output>
Create `.planning/phases/149-test-suite-triage/149-07-SUMMARY.md` when done
</output>
