---
phase: 149-test-suite-triage
plan: 08
type: execute
wave: 8
depends_on: ["149-07"]
files_modified:
  - tests/test_qramm_evidence_bridge.py
  - tests/test_qramm_model_stale.py
  - tests/test_qramm_models.py
  - tests/test_qramm_staleness.py
  - tests/skip_registry.py
  - docs/test-triage-149.md
autonomous: true
requirements: [SUITE-01]

must_haves:
  truths:
    - "The two SIGSEGV (exit=-11) failures in test_qramm_staleness.py get dedicated crash-cause investigation, not a blanket quarantine"
    - "Each of Cluster 9 Group C's 6 QRAMM-subsystem failures has an individually-investigated disposition"
  artifacts:
    - path: "docs/test-triage-149.md"
      provides: "6 rows under Cluster 9 for the QRAMM subsystem, with the SIGSEGV pair's crash-cause explicitly documented"
---

<objective>
Individually investigate and disposition Cluster 9 Group C: 6 failures across 4 files —
`test_qramm_evidence_bridge.py`(2), `test_qramm_model_stale.py`(1), `test_qramm_models.py`(1),
`test_qramm_staleness.py`(2, `exit=-11` SIGSEGV in a CLI subprocess).

Purpose: RESEARCH.md explicitly calls out the `test_qramm_staleness.py` SIGSEGV pair as "unusual and
worth a few extra minutes to rule out a native-library crash (cryptography/openssl binding) vs. a
subprocess/pytest-capture artifact" — a crash is a materially different risk category than an
assertion failure and needs its own investigation, not a folded-in "quarantine and move on."
Output: crash root-cause determined for 2 tests; 4 other QRAMM failures investigated; 6 ledger rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/149-test-suite-triage/149-CONTEXT.md
@.planning/phases/149-test-suite-triage/149-RESEARCH.md
@.planning/phases/149-test-suite-triage/149-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate the SIGSEGV crash in test_qramm_staleness.py (dedicated attention)</name>
  <files>tests/test_qramm_staleness.py</files>
  <read_first>tests/test_qramm_staleness.py</read_first>
  <action>
  Read `test_qramm_status_cli_smoke_fresh` and `test_qramm_status_cli_smoke_stale_via_override` in
  full, including the CLI subprocess invocation they make (likely `subprocess.run([...], ...)` against
  a `quirk qramm status` or similar CLI entry point). Reproduce the crash directly:
  `pytest tests/test_qramm_staleness.py -q -m "" -v` and separately invoke the underlying CLI command by
  hand (read the test to determine the exact command/args) to see if it segfaults outside pytest too.
  Check whether the crash correlates with a specific native dependency (cryptography/openssl bindings,
  used elsewhere in this codebase per `test_vault_connector.py`'s OpenSSL SHA1 failure in Cluster 9
  Group A) — run `python -c "import cryptography; print(cryptography.__version__)"` and
  `openssl version` to record versions in use. Also test whether the crash reproduces when run in
  isolation vs. only after other tests execute first (a pytest-capture/fd artifact would behave
  differently in isolation vs. full-suite). Record the determination: (a) genuine native-library crash
  (requires urgent Phase 150 attention, flag prominently), (b) subprocess/pytest-capture artifact
  (lower-risk, environment quirk), or (c) inconclusive with the specific evidence gathered either way —
  do not guess; report only what was directly observed.
  </action>
  <verify>
    <automated>pytest tests/test_qramm_staleness.py -q -m "" -v</automated>
  </verify>
  <acceptance_criteria>
    - The exact CLI command/args the subprocess invokes is identified and quoted
    - Isolation-vs-full-suite reproduction behavior is recorded (does it crash standalone too?)
    - cryptography and openssl versions in use are recorded
    - A specific determination (native-library crash / subprocess artifact / inconclusive) is recorded
      with the evidence that led to it — not a guess
  </acceptance_criteria>
  <done>The SIGSEGV's likely cause category is determined and documented with concrete evidence, ready for Phase 150 prioritization.</done>
</task>

<task type="auto">
  <name>Task 2: Investigate + quarantine test_qramm_evidence_bridge.py, test_qramm_model_stale.py, test_qramm_models.py (4 tests)</name>
  <files>tests/test_qramm_evidence_bridge.py, tests/test_qramm_model_stale.py, tests/test_qramm_models.py, tests/skip_registry.py</files>
  <read_first>tests/test_qramm_evidence_bridge.py, tests/test_qramm_model_stale.py, tests/test_qramm_models.py</read_first>
  <action>
  Read `test_qramm_evidence_bridge.py::test_no_risk_engine_import` (`AssertionError:
  'quirk.engine.risk_engine' found in import graph, expected absent` — grep the actual import chain to
  confirm whether `risk_engine` is now genuinely imported somewhere it shouldn't be, or the test's
  import-graph-walking logic itself has drifted) and `test_unconfirmed_excluded_from_score`
  (`assert 422 == 200` — trace the endpoint/payload the test posts to determine what validation now
  rejects it with 422). Read `test_qramm_model_stale.py::test_is_qramm_model_stale_boundary[today1-True]`
  (`assert False is True` — a parametrized boundary-date test; check `quirk/qramm/model_meta.py`'s
  `STALENESS_THRESHOLD_DAYS` and `last_verified` against the test's boundary-date fixture to determine
  if the threshold changed or the fixture date is now on the wrong side of the boundary). Read
  `test_qramm_models.py::TestInitDbQRAMMTables::test_ensure_qramm_tables_called_after_phase46`
  (`AssertionError: _PHASE46_COLUMNS migration call not found in init_db` — grep `init_db` for whether
  the `_PHASE46_COLUMNS` migration call was removed, renamed, or reordered). Quarantine all 4 with xfail
  + matching `ALLOWED_SKIPS` entries, category `"pre_existing_triage_149"`, each reason citing the
  specific finding.
  </action>
  <verify>
    <automated>pytest tests/test_qramm_evidence_bridge.py tests/test_qramm_model_stale.py tests/test_qramm_models.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 4 tests' reason string cites a specific grep/read finding (import graph state, 422
      validation cause, staleness boundary math, or init_db migration call presence)
    - 4 `@pytest.mark.xfail` decorators + 4 matching `ALLOWED_SKIPS` entries added
    - `pytest tests/test_qramm_evidence_bridge.py tests/test_qramm_model_stale.py tests/test_qramm_models.py -q -m ""` reports 4 xfailed, 0 failed
  </acceptance_criteria>
  <done>All 4 tests individually investigated and quarantined with specific findings.</done>
</task>

<task type="auto">
  <name>Task 3: Quarantine test_qramm_staleness.py per Task 1's finding and write Cluster 9 Group C ledger rows</name>
  <files>tests/test_qramm_staleness.py, tests/skip_registry.py, docs/test-triage-149.md</files>
  <read_first>docs/test-triage-149.md</read_first>
  <action>
  Based on Task 1's determination, add `@pytest.mark.skip(reason="TRIAGE-149: <Task 1's exact
  determination — e.g. 'SIGSEGV in <library> subprocess call, likely native-library crash, HIGH
  PRIORITY for Phase 150' or 'subprocess/pytest-capture artifact, does not reproduce standalone'>; see
  docs/test-triage-149.md#<slug>")` above each of the 2 `test_qramm_staleness.py` tests (use skip, not
  xfail — a segfault should not be allowed to run again until root-caused, per D-03's "skip where
  running it is pointless or environment-broken"), with matching `ALLOWED_SKIPS` entries. Write all 6
  Cluster 9 Group C rows in `docs/test-triage-149.md`: the 2 SIGSEGV rows must carry Task 1's full
  determination text in Evidence/Notes, prefixed `CRASH:` if determined to be a genuine native-library
  issue so Phase 150 prioritizes it above routine drift. Run `pytest tests/test_skip_registry.py -q -m ""`
  to confirm the gate remains green.
  </action>
  <verify>
    <automated>pytest tests/test_skip_registry.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - 2 `@pytest.mark.skip` decorators + 2 matching `ALLOWED_SKIPS` entries added for the SIGSEGV pair
    - `docs/test-triage-149.md` has 6 rows under Cluster 9 for this group, with the SIGSEGV rows' Evidence
      containing Task 1's determination verbatim (prefixed `CRASH:` if native-library-crash was concluded)
    - `pytest tests/test_skip_registry.py -q -m ""` passes
  </acceptance_criteria>
  <done>Cluster 9 Group C fully dispositioned; SIGSEGV pair's crash cause prominently documented for Phase 150.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — test metadata, subprocess-crash investigation (read-only, no fixes), and documentation only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-08 | Denial of Service | QRAMM staleness CLI subprocess (SIGSEGV) | mitigate | This plan does not fix the crash (D-01) but investigates and documents its likely cause with a `CRASH:`-prefixed ledger row so Phase 150 prioritizes it above routine test drift, given a segfault is a more severe defect class than an assertion failure. |
| T-149-08b | Tampering | tests/*.py skip/xfail markers | accept | Test-only metadata; no production code modified. |
</threat_model>

<verification>
- `pytest tests/test_qramm_evidence_bridge.py tests/test_qramm_model_stale.py tests/test_qramm_models.py tests/test_qramm_staleness.py -q -m ""` reports 6 skipped/xfailed, 0 failed
- `pytest tests/test_skip_registry.py -q -m ""` passes
- Ledger has 6 rows for Group C, SIGSEGV pair's Evidence containing a concrete crash-cause determination
</verification>

<success_criteria>
All 6 Cluster 9 Group C failures dispositioned; the SIGSEGV pair's crash cause is concretely investigated and documented, not assumed.
</success_criteria>

<output>
Create `.planning/phases/149-test-suite-triage/149-08-SUMMARY.md` when done
</output>
