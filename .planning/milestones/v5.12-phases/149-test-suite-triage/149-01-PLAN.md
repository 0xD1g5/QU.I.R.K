---
phase: 149-test-suite-triage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/skip_registry.py
  - tests/test_skip_registry.py
  - pyproject.toml
  - docs/test-triage-149.md
autonomous: true
requirements: [SUITE-01]

must_haves:
  truths:
    - "tests/test_skip_registry.py::test_no_unregistered_skips passes (gate green) after D-04 drift is repaired"
    - "The AST walker also enforces registration for @pytest.mark.skip and @pytest.mark.xfail, not just skip()/importorskip()/skipif"
    - "A ledger skeleton exists at docs/test-triage-149.md with one heading per failure cluster, ready for later plans to append per-test rows"
  artifacts:
    - path: "tests/skip_registry.py"
      provides: "ALLOWED_SKIPS with D-04 drift repaired and a new pre_existing_triage_149 category declared"
    - path: "tests/test_skip_registry.py"
      provides: "AST walker detecting skipif, skip, and xfail decorators"
    - path: "docs/test-triage-149.md"
      provides: "Ledger skeleton: reconciliation header, status legend, 9 cluster headings"
  key_links:
    - from: "tests/test_skip_registry.py"
      to: "tests/skip_registry.py"
      via: "ALLOWED_SKIPS import + _allowed() line-tolerance match"
      pattern: "from tests\\.skip_registry import ALLOWED_SKIPS"
---

<objective>
Repair the currently-failing `tests/test_skip_registry.py::test_no_unregistered_skips` meta-gate
(D-04 drift: ~25-29 unregistered skip markers accumulated since Phase 41), extend its AST walker to
also enforce registration of `@pytest.mark.skip` and `@pytest.mark.xfail` decorators (Open Question 2,
recommended in-scope per RESEARCH.md), and create the `docs/test-triage-149.md` ledger skeleton that
every subsequent plan in this phase appends rows to.

Purpose: every later plan in Phase 149 adds `pre_existing_triage_149`-category quarantine markers to
`tests/skip_registry.py` and expects the meta-gate to enforce them. That gate must be green and must
actually detect every marker type this phase uses before any quarantine work can be verified.
Output: a working, extended meta-gate; a ledger skeleton ready for per-cluster rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-test-suite-triage/149-CONTEXT.md
@.planning/phases/149-test-suite-triage/149-RESEARCH.md
@.planning/phases/149-test-suite-triage/149-PATTERNS.md
</context>

<interfaces>
Current `ALLOWED_SKIPS` tuple shape (tests/skip_registry.py:15-45), unchanged, extend only:
`(file_relative_to_tests_dir: str, line_number: int, category: str, reason: str)`
Categories today: `"optional_extra"`, `"live_infra"`. This plan adds `"pre_existing_triage_149"` to
the docstring's declared set (line 4) — actual triage entries using it are added by Plans 02-10, not
this plan (this plan's own D-04 repairs use the two existing categories, or delete the marker).

Current detector functions in tests/test_skip_registry.py (lines 46-68):
`_is_pytest_skip_call(node) -> bool` (unchanged by this plan)
`_is_pytest_skipif_decorator(node) -> bool` (generalize into `_is_pytest_mark_decorator(node, mark_name)`)
Main loop at lines 89-106 currently hardcodes the `skipif` check only.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Triage current D-04 drift — register or delete each unregistered skip violation</name>
  <files>tests/skip_registry.py, and the ~18 test files containing unregistered skip markers</files>
  <read_first>tests/skip_registry.py, tests/test_skip_registry.py</read_first>
  <action>
  Run `pytest tests/test_skip_registry.py -q -m ""` to get the live, authoritative violation list —
  do not rely solely on RESEARCH.md's captured snapshot (it was truncated by terminal scrollback and
  may have drifted). RESEARCH.md's last-known list, for reference: test_cbom_vault_consistency.py:50,
  test_chaos_lab_idempotency.py:111, test_chaos_storage.py:44, test_chaos_storage.py:71,
  test_cmvp_refresh.py:22, test_cmvp_refresh.py:23, test_credential_leakage.py:306,
  test_db_migrate_cli.py:166, test_distributed_topology.py:48, test_dnssec_scanner.py:480,
  test_identity_scanner_hardening.py:80, test_jobs_api.py:489, test_jwt_scanner.py:209,
  test_kerberos_scanner.py:384, test_pdf_metadata_constants.py:19/20/56, test_pqc_discriminator.py:126/141,
  test_report_injection_hardening.py:240/241/254, test_report_render_undetermined_hosts.py:150/162/215,
  test_scheduler_cmd.py:314, test_snmp_scanner_contract.py:598/631/673.
  For each reported violation: open the file at the reported line, read the actual
  `pytest.skip(...)`/`pytest.importorskip(...)`/`@pytest.mark.skipif(...)` call/decorator and its
  surrounding condition. Disposition each one as either (a) register into `ALLOWED_SKIPS` using the
  existing 4-tuple shape `(file, line, category, reason)` with category `"optional_extra"` (skip guards
  an optional pip extra/import) or `"live_infra"` (skip guards a Docker/chaos-lab/external-service
  dependency) — whichever the actual condition indicates, or (b) delete the skip marker per D-05 only
  if grep/read confirms the tested behavior/module no longer exists. Do NOT use category
  `"pre_existing_triage_149"` for any of these — that category is reserved for the 116 full-suite
  failures Plans 02-10 quarantine, not this pre-existing drift cleanup. Add new tuples under a
  `# Phase 149 D-04: registered pre-existing drift` comment banner in `tests/skip_registry.py`.
  </action>
  <verify>
    <automated>pytest tests/test_skip_registry.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - `ALLOWED_SKIPS` contains one new tuple per confirmed-legitimate violation, category is
      `"optional_extra"` or `"live_infra"` only (never `"pre_existing_triage_149"`)
    - Any deleted skip marker's source line no longer contains `pytest.skip(`, `pytest.importorskip(`,
      or `@pytest.mark.skipif(` at that location
    - Re-running `pytest tests/test_skip_registry.py -q -m ""` after this task reports 0 unregistered
      violations related to this task's entries (Task 2's walker extension is verified separately)
  </acceptance_criteria>
  <done>Every D-04 violation from the fresh gate run is either registered with the correct existing
  category or deleted with grep/read-confirmed justification.</done>
</task>

<task type="auto">
  <name>Task 2: Extend AST walker to detect skip/xfail decorators, fix pyproject markers, declare new category</name>
  <files>tests/test_skip_registry.py, pyproject.toml, tests/skip_registry.py</files>
  <read_first>tests/test_skip_registry.py, pyproject.toml, tests/skip_registry.py</read_first>
  <action>
  In `tests/test_skip_registry.py`, replace `_is_pytest_skipif_decorator(node)` (lines 57-68) with a
  parametrized `_is_pytest_mark_decorator(node: ast.AST, mark_name: str) -> bool` that matches
  `@pytest.mark.<mark_name>` whether called (`ast.Call`) or bare (`ast.Attribute`) — same structural
  check, just replacing the hardcoded `"skipif"` literal with the `mark_name` parameter. In the main
  walk loop (currently lines 97-106), replace the single `_is_pytest_skipif_decorator(deco)` check with
  a loop over `("skipif", "skip", "xfail")`, calling `_is_pytest_mark_decorator(deco, mark_name)` for
  each, and on a match append `(py_file.name, deco.lineno, f"@pytest.mark.{mark_name}")` to
  `violations` if `not _allowed(py_file.name, deco.lineno)` — same shape already used for the `skipif`
  tuple. Update the module docstring (lines 1-3) to mention `skip`/`xfail` alongside `skipif`. In
  `pyproject.toml`'s `markers` list (currently at line ~152-155, two entries `slow` and `live_infra`),
  add the string `"skip_registry_gate: marks the skip-registry meta-gate test"` as a third entry —
  this closes the existing `PytestUnknownMarkWarning` on `@pytest.mark.skip_registry_gate` at
  `tests/test_skip_registry.py:71`. In `tests/skip_registry.py`'s module docstring (line 4), change
  `category in {"optional_extra", "live_infra"}` to
  `category in {"optional_extra", "live_infra", "pre_existing_triage_149"}`.
  </action>
  <verify>
    <automated>pytest tests/test_skip_registry.py -q -m "" -W error::pytest.PytestUnknownMarkWarning</automated>
  </verify>
  <acceptance_criteria>
    - `_is_pytest_mark_decorator` exists and is called for `"skipif"`, `"skip"`, and `"xfail"` in the
      main loop (grep confirms `_is_pytest_mark_decorator` appears at least 3 times or once inside a
      loop over the 3-name tuple)
    - A scratch test confirms detection: temporarily add `@pytest.mark.skip(reason="scratch")` above an
      unregistered test function in any tests/*.py file, run
      `pytest tests/test_skip_registry.py -q -m ""`, confirm it FAILS citing that file:line with
      `@pytest.mark.skip`, then revert the scratch addition and re-run to confirm it passes again
    - `pyproject.toml` markers list contains the string `"skip_registry_gate:"` and running pytest with
      `-W error::pytest.PytestUnknownMarkWarning` on `tests/test_skip_registry.py` raises no warning
    - `tests/skip_registry.py` docstring's category set literal includes `"pre_existing_triage_149"`
  </acceptance_criteria>
  <done>Walker enforces registration for skipif, skip, and xfail; pyproject markers list declares
  skip_registry_gate; skip_registry.py declares the new category.</done>
</task>

<task type="auto">
  <name>Task 3: Create the ledger skeleton and confirm the gate is fully green</name>
  <files>docs/test-triage-149.md</files>
  <read_first>.planning/audit-2026-05-27/AUDIT-TASKS.md, docs/timeout-retry-audit.md</read_first>
  <action>
  Re-run `pytest -q -m ""` (fresh full-suite count — RESEARCH.md's 116 may have drifted) and capture
  its exact summary line. Create `docs/test-triage-149.md` with: (1) a title
  `# Phase 149 Test Suite Triage Ledger (SUITE-01)` and one-paragraph purpose statement; (2) a
  reconciliation line in the exact form
  `Built against: \`pytest -q -m ""\` → <N failed, M passed, K skipped, W warnings> — <today's date>`
  using the fresh count just captured; (3) a status-legend block defining `fixed`,
  `quarantined-skip`, `quarantined-xfail`, `deleted`, `environment-fix-applied` (mirroring
  `AUDIT-TASKS.md`'s `status_legend` frontmatter block style, but as a plain markdown list since this
  file has no YAML frontmatter need); (4) nine `##` headings, one per cluster, in this exact order and
  wording: "Cluster 1: SSRF/DNS-blocked sandbox", "Cluster 2: Playwright cross-test pollution",
  "Cluster 3: Version staleness (environment)", "Cluster 4: Version staleness (stale assertions)",
  "Cluster 5: sensor_id shape / AUDIT-08 regression", "Cluster 6: pip dry-run extras-install flakiness",
  "Cluster 7: Optional GCP extras missing", "Cluster 8: Meta-gate self-failure (D-04)", "Cluster 9:
  Remaining individually-distinct failures" — each followed by an empty table with header row
  `| Test ID | Disposition | Sub-reason | Evidence/Notes | Registry entry? |`; (5) a closing footer
  `*Phase: 149-test-suite-triage*` / `*Plan: 01*` / `*Updated: <today's date>*`. Mark Cluster 8's table
  with a single row for `test_skip_registry.py::test_no_unregistered_skips`, disposition `fixed`,
  since Tasks 1-2 of this plan already resolved it.
  </action>
  <verify>
    <automated>pytest tests/test_skip_registry.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - `docs/test-triage-149.md` exists with exactly 9 `##` cluster headings in the order listed above
    - The reconciliation header line contains the literal string `pytest -q -m ""` and a failed/passed/
      skipped count
    - Cluster 8's table has exactly one row, disposition `fixed`
    - `pytest tests/test_skip_registry.py -q -m ""` exits 0 (1 passed, gate fully green)
  </acceptance_criteria>
  <done>Ledger skeleton committed; meta-gate green; every later plan can append rows without touching
  this plan's structure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — this plan modifies test-suite metadata and a documentation file only. No runtime code path,
no new input/output surface, no production execution changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-01 | Tampering | tests/skip_registry.py / tests/test_skip_registry.py | accept | Test-only metadata; no attack surface change. Enforcement mechanism (the AST gate) is itself the control being strengthened, not weakened, by this plan. |
| T-149-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan. |
</threat_model>

<verification>
- `pytest tests/test_skip_registry.py -q -m ""` passes (1 passed, 0 unregistered violations)
- `pytest -q -m ""` full-suite run still shows `test_no_unregistered_skips` passing (spot-check within
  the full run, not just in isolation)
- `docs/test-triage-149.md` exists with the 9-cluster skeleton described above
</verification>

<success_criteria>
- D-04 drift fully repaired (0 unregistered violations from the pre-149 baseline)
- AST walker detects skip/skipif/xfail decorators and skip()/importorskip() calls
- pyproject.toml markers list declares skip_registry_gate
- Ledger skeleton exists and is ready for Plans 02-10 to append rows
</success_criteria>

<output>
Create `.planning/phases/149-test-suite-triage/149-01-SUMMARY.md` when done
</output>
