---
phase: 149-test-suite-triage
plan: 11
type: execute
wave: 11
depends_on: ["149-10"]
files_modified:
  - docs/test-triage-149.md
autonomous: true
requirements: [SUITE-01]

must_haves:
  truths:
    - "A final pytest -q -m \"\" run's failed count reconciles exactly against the ledger's total disposed-row count"
    - "No ledger row has an empty Disposition or Sub-reason"
    - "Every test ID appears in the ledger exactly once (no duplicates, no omissions)"
  artifacts:
    - path: "docs/test-triage-149.md"
      provides: "Final reconciliation section confirming exact-count match against a fresh full-suite run"
---

<objective>
Reconcile the completed ledger (Plans 01-10) against a final `pytest -q -m ""` run: confirm the total
row count matches the actual current failure/disposition count, confirm every row has a non-empty
disposition and sub-reason, and confirm no test ID is missing or duplicated.

Purpose: this is Success Criterion 2 from the phase goal — "the ledger's total matches the actual
failure count" — and it must be checked mechanically against a fresh run, not assumed from the plan
sequence, since Plans 01-10 may have found additional violations, discovered shared root causes that
change counts (e.g. Cluster 3's `pip install -e .` fix reduces the live failure count by 6), or
individually re-classified tests differently than RESEARCH.md's original clustering anticipated.
Output: a reconciled, complete ledger with no gaps, ready to hand off as Phase 150's sizing input.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/149-test-suite-triage/149-CONTEXT.md
@.planning/phases/149-test-suite-triage/149-RESEARCH.md
@.planning/phases/149-test-suite-triage/149-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run a fresh full-suite pass and cross-check against the ledger</name>
  <files>docs/test-triage-149.md</files>
  <read_first>docs/test-triage-149.md</read_first>
  <action>
  Run `pytest -q -m ""` fresh (not from memory — Plans 01-10 have applied a `pip install -e .` refresh,
  deleted/edited 2 stale-assertion tests, and quarantined ~100+ others since the phase's original
  116-count snapshot, so this count has genuinely changed). Capture the exact summary line
  (`N failed, M passed, K skipped/xfailed, ...`). Cross-reference: (a) every test ID that still appears
  as `failed` in this fresh run MUST have a ledger row with a non-`fixed`/non-`deleted` disposition (if
  a test still fails and has no row, that is a gap — go back and add one, even if it means a follow-up
  disposition task); (b) every ledger row with disposition `fixed` or `environment-fix-applied` MUST
  correspond to a test that now passes in this fresh run (if a "fixed" row's test still fails, the fix
  did not actually work — flag and re-open); (c) count the total number of distinct test IDs across all
  9 cluster tables in the ledger and confirm it equals the count implied by Plans 01-10's task-level
  disposition totals (23+14+6+3+10+6+2+1+52 = 117, accounting for the 1 D-04 meta-gate row plus 116
  original failures — reconcile any arithmetic drift against the actual per-cluster row counts written).
  </action>
  <verify>
    <automated>pytest -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - Every currently-`failed` test ID from the fresh run has a corresponding ledger row (0 orphaned failures)
    - Every `fixed`/`environment-fix-applied` ledger row's test currently passes (0 false-fixed rows)
    - The ledger's total distinct-test-ID count is stated explicitly and matches the sum of the 9
      cluster tables' row counts (no arithmetic mismatch between the stated total and the actual rows)
  </acceptance_criteria>
  <done>Fresh run cross-checked against the ledger; any gap (orphaned failure or false-fixed row) identified and closed.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm no empty dispositions, no duplicates, and write the final reconciliation section</name>
  <files>docs/test-triage-149.md</files>
  <read_first>docs/test-triage-149.md</read_first>
  <action>
  Scan every row across all 9 cluster tables in `docs/test-triage-149.md`: confirm no row has an empty
  `Disposition` or `Sub-reason` cell (grep for `| |` or `|  |` patterns indicating an empty cell,
  filtering out the header/separator rows), and confirm no test ID string appears in more than one row
  (a duplicate would mean two plans both claimed the same test). Fix any empty cell or duplicate found.
  Append a final `## Reconciliation` section at the bottom of the file, above the closing footer,
  stating: the fresh `pytest -q -m ""` summary line from Task 1, the final ledger row count, confirmation
  that both counts reconcile (or an explicit note of the accounted-for delta, e.g. "6 fewer live failures
  than the original 116 due to Cluster 3's pip install -e . fix and Cluster 4's 2 test corrections/deletions"),
  and confirmation that every row has a non-empty disposition and sub-reason. Update the closing footer's
  `*Updated:*` date.
  </action>
  <verify>
    <automated>pytest tests/test_skip_registry.py -q -m "" && pytest -q -m "" && python -m compileall tests/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '| |' docs/test-triage-149.md` (filtered to exclude header/separator lines) returns 0
    - No test ID string appears in 2+ rows across the 9 cluster tables
    - A `## Reconciliation` section exists stating the fresh pytest summary line, the final row count,
      and an explicit accounted-for delta between the original 116 and the current live-failure count
    - `pytest tests/test_skip_registry.py -q -m ""` passes (meta-gate green)
    - `python -m compileall tests/` exits 0 (per CLAUDE.md: run `python -m compileall` after Python
      changes) — phase-wide compile check covering all decorator insertions from Plans 01-10 plus
      this plan's own edits
  </acceptance_criteria>
  <done>Ledger fully reconciled: no empty cells, no duplicates, exact-count match documented against a fresh full-suite run, meta-gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — this plan only reads test results and edits a documentation file. No production code or test
logic is modified.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-11 | Repudiation | docs/test-triage-149.md ledger accuracy | mitigate | The reconciliation task exists specifically to prevent the ledger from silently drifting from reality (orphaned failures, false-fixed rows, duplicates) — this is the phase's own integrity check, not a new risk. |
</threat_model>

<verification>
- `pytest -q -m ""` failure count reconciles against the ledger's row count (with any delta explicitly explained)
- No ledger row has an empty Disposition or Sub-reason
- No duplicate test IDs across the 9 cluster tables
- `pytest tests/test_skip_registry.py -q -m ""` passes
</verification>

<success_criteria>
The ledger is a complete, accurate, reconciled disposition record for every test that failed in the
phase's full-suite baseline — Success Criterion 2 ("total matches actual failure count") is
mechanically checkable and confirmed, not eyeballed. Phase 149 (SUITE-01) is complete.
</success_criteria>

<output>
Create `.planning/phases/149-test-suite-triage/149-11-SUMMARY.md` when done
</output>
