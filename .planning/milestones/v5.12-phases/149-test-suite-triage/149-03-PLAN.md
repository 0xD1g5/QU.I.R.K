---
phase: 149-test-suite-triage
plan: 03
type: execute
wave: 3
depends_on: ["149-02"]
files_modified:
  - tests/test_reports_writer.py
  - tests/test_report_injection_hardening.py
  - tests/test_pdf_metadata_constants.py
  - tests/test_writer.py
  - tests/test_pdf_export.py
  - tests/test_install_all_excludes_impacket.py
  - tests/test_install_all_excludes_pysnmp.py
  - tests/test_install_all_excludes_schemathesis.py
  - tests/test_install_all_includes_notify.py
  - tests/test_install_all_includes_tickets.py
  - tests/test_snmp_scanner_contract.py
  - tests/skip_registry.py
  - docs/test-triage-149.md
autonomous: true
requirements: [SUITE-01]

must_haves:
  truths:
    - "Cluster 2 (Playwright cross-test pollution, 14 tests) and Cluster 6 (pip dry-run flakiness, 6 tests) each have per-test isolation verification before disposition"
    - "Every Cluster 2/6 test has a quarantine marker + registry entry + ledger row citing the isolation-pass evidence"
  artifacts:
    - path: "docs/test-triage-149.md"
      provides: "20 new rows under Cluster 2 and Cluster 6 headings"
  key_links:
    - from: "quarantine marker reason= strings"
      to: "docs/test-triage-149.md ledger anchors"
      via: "TRIAGE-149: flaky; see docs/test-triage-149.md#<anchor>"
      pattern: "TRIAGE-149: flaky"
---

<objective>
Disposition Cluster 2 (Playwright cross-test pollution — 14 tests, fails only in full-suite runs, not
isolation) and Cluster 6 (pip `--dry-run` extras-install flakiness — 6 tests, same order/full-suite-only
shape) per RESEARCH.md rows 2 and 6 and Pitfall 2 ("full-suite-only failures are not per-file defects").

Purpose: RESEARCH.md verified `test_reports_writer.py` + `test_writer.py` pass together (7 passed) and
`test_install_all_excludes_impacket.py` passes standalone — both clusters are test-isolation/shared
global-state artifacts, not per-test regressions. Each disposition must re-verify this in isolation
before writing the ledger row, per Pitfall 2's explicit warning against skipping this check.
Output: 20 `flaky`-sub-reason quarantine markers + registry entries + ledger rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/149-test-suite-triage/149-CONTEXT.md
@.planning/phases/149-test-suite-triage/149-RESEARCH.md
@.planning/phases/149-test-suite-triage/149-PATTERNS.md
</context>

<interfaces>
Reason-string convention: `"TRIAGE-149: flaky (<one-line shared cause>); see docs/test-triage-149.md#<slug>"`.
Use `skip` (not `xfail`) for this cluster — running these under full-suite pollution is not useful signal
(D-03: "skip where running it is pointless"); they should run cleanly when Phase 150 fixes the shared
fixture/lifecycle issue, at which point the skip is removed entirely, not converted to xfail.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Isolation-verify + quarantine Cluster 2 (Playwright pollution, 14 tests, 5 files)</name>
  <files>tests/test_reports_writer.py, tests/test_report_injection_hardening.py, tests/test_pdf_metadata_constants.py, tests/test_writer.py, tests/test_pdf_export.py, tests/skip_registry.py</files>
  <read_first>tests/test_reports_writer.py, tests/test_writer.py, tests/test_pdf_metadata_constants.py, tests/test_report_injection_hardening.py, tests/test_pdf_export.py, tests/skip_registry.py</read_first>
  <action>
  For each file, run it standalone or in a small group and confirm it passes outside the full suite
  (RESEARCH.md already confirmed `pytest tests/test_reports_writer.py tests/test_writer.py -q -m ""` →
  7 passed; re-run this plus `pytest tests/test_report_injection_hardening.py -q -m ""`,
  `pytest tests/test_pdf_metadata_constants.py -q -m ""`, `pytest tests/test_pdf_export.py -q -m ""`
  individually to confirm each also passes standalone). Add
  `@pytest.mark.skip(reason="TRIAGE-149: flaky (Playwright PlaywrightContextManager singleton torn down by earlier full-suite test, order-dependent — passes standalone); see docs/test-triage-149.md#<slug>")`
  above each of the 14 failing tests: `test_reports_writer.py::test_json_export_preserves_description`,
  `test_json_export_preserves_deprecation_phrase`, `test_html_report_has_description_column`,
  `test_docx_emitted_by_write_reports`, `test_docx_none_on_fail_not_in_output_files`;
  `test_report_injection_hardening.py::test_script_payload_in_cert_cn_is_escaped_in_html`,
  `test_javascript_url_in_finding_recommendation_stripped`, `test_db_stored_raw_payload_preserved`,
  `test_script_payload_in_cert_cn_is_escaped_in_pdf`; `test_pdf_metadata_constants.py::test_pdf_title_is_constant`,
  `test_pdf_author_is_constant`, `test_pdf_renders_with_locked_context`;
  `test_writer.py::test_run_stats_ports_and_hosts_scanned`; `test_pdf_export.py::test_pdf_export_endpoint`.
  Add 14 matching `ALLOWED_SKIPS` entries with category `"pre_existing_triage_149"` under a
  `# Phase 149 D-02/D-03: test-suite triage quarantines` banner (same one Plan 02 started, or a fresh
  one if that file section is not present in this codebase's history — append, don't duplicate the
  banner comment if it already exists from Plan 02's edits to this same file region).
  </action>
  <verify>
    <automated>pytest tests/test_reports_writer.py tests/test_report_injection_hardening.py tests/test_pdf_metadata_constants.py tests/test_writer.py tests/test_pdf_export.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 5 files independently confirmed passing before quarantine (evidence noted in the commit or ledger)
    - 14 `@pytest.mark.skip` decorators added with `TRIAGE-149: flaky` reason strings
    - 14 matching `ALLOWED_SKIPS` entries added
    - Running the 5 files together reports 14 skipped, 0 failed
  </acceptance_criteria>
  <done>Cluster 2's 14 tests quarantined with skip + registry entries, isolation-pass evidence recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Isolation-verify + quarantine Cluster 6 (pip dry-run flakiness, 6 tests, 6 files)</name>
  <files>tests/test_install_all_excludes_impacket.py, tests/test_install_all_excludes_pysnmp.py, tests/test_install_all_excludes_schemathesis.py, tests/test_install_all_includes_notify.py, tests/test_install_all_includes_tickets.py, tests/test_snmp_scanner_contract.py, tests/skip_registry.py</files>
  <read_first>tests/test_install_all_excludes_impacket.py, tests/test_snmp_scanner_contract.py, tests/skip_registry.py</read_first>
  <action>
  Re-verify each of the 6 tests individually in isolation (RESEARCH.md confirmed
  `test_install_all_excludes_impacket.py` passes standalone in 9.39s — do not assume the other 5 share
  the exact cause without a per-test check, per Pitfall 2's explicit warning): run
  `pytest tests/test_install_all_excludes_impacket.py -q -m ""`,
  `pytest tests/test_install_all_excludes_pysnmp.py -q -m ""`,
  `pytest tests/test_install_all_excludes_schemathesis.py -q -m ""`,
  `pytest tests/test_install_all_includes_notify.py -q -m ""`,
  `pytest tests/test_install_all_includes_tickets.py -q -m ""`, and
  `pytest tests/test_snmp_scanner_contract.py::test_install_all_excludes_pysnmp -q -m ""` individually.
  For each that passes standalone, add `@pytest.mark.skip(reason="TRIAGE-149: flaky (pip --dry-run
  subprocess contention under full-suite load, passes standalone); see docs/test-triage-149.md#<slug>")`
  above the failing test function, and a matching `ALLOWED_SKIPS` entry. For any that FAILS even in
  isolation, do not apply the flaky disposition — investigate the actual pip/dry-run error and record a
  distinct, accurate disposition (fixed/quarantined-skip/deleted per D-01/D-05) instead; do not force
  the cluster-6 narrative onto a genuinely different root cause.
  </action>
  <verify>
    <automated>pytest tests/test_install_all_excludes_impacket.py tests/test_install_all_excludes_pysnmp.py tests/test_install_all_excludes_schemathesis.py tests/test_install_all_includes_notify.py tests/test_install_all_includes_tickets.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 6 tests individually re-run in isolation before disposition; result recorded
    - Tests confirmed flaky get `@pytest.mark.skip` + matching `ALLOWED_SKIPS` entry with `TRIAGE-149: flaky` reason
    - Any test that fails even in isolation has a distinct, non-flaky disposition documented instead
  </acceptance_criteria>
  <done>All 6 Cluster 6 tests individually verified and dispositioned; no blanket disposition applied without a per-test check.</done>
</task>

<task type="auto">
  <name>Task 3: Write Cluster 2 + Cluster 6 ledger rows and verify the meta-gate</name>
  <files>docs/test-triage-149.md</files>
  <read_first>docs/test-triage-149.md</read_first>
  <action>
  Under "## Cluster 2: Playwright cross-test pollution", add 14 rows (Disposition `quarantined-skip`,
  Sub-reason `flaky (test-isolation / shared Playwright singleton)`, Evidence citing the standalone-pass
  confirmation from Task 1). Under "## Cluster 6: pip dry-run extras-install flakiness", add rows for
  each of the 6 tests per Task 2's actual per-test disposition (flaky or otherwise — do not force all 6
  into `quarantined-skip` if Task 2 found a genuine failure). Run
  `pytest tests/test_skip_registry.py -q -m ""` to confirm the gate remains green.
  </action>
  <verify>
    <automated>pytest tests/test_skip_registry.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - Cluster 2 heading has 14 rows, Cluster 6 heading has 6 rows
    - No row has an empty Disposition or Sub-reason
    - `pytest tests/test_skip_registry.py -q -m ""` passes
  </acceptance_criteria>
  <done>Clusters 2 and 6 fully dispositioned; gate remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — test metadata and documentation only. No production code path or new input surface touched.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-03 | Tampering | tests/*.py skip markers | accept | Test-only metadata; enforced by the meta-gate from Plan 01. |
</threat_model>

<verification>
- Isolation runs for all 20 Cluster 2/6 tests confirm the flaky-vs-genuine disposition split
- `pytest tests/test_skip_registry.py -q -m ""` passes
- `docs/test-triage-149.md` has 14 + 6 = 20 new rows
</verification>

<success_criteria>
All 20 Cluster 2/6 failures individually isolation-verified and dispositioned with evidence.
</success_criteria>

<output>
Create `.planning/phases/149-test-suite-triage/149-03-SUMMARY.md` when done
</output>
