---
phase: 149-test-suite-triage
plan: 02
type: execute
wave: 2
depends_on: ["149-01"]
files_modified:
  - tests/test_notify_email.py
  - tests/test_notify_webhook.py
  - tests/test_ticketing_servicenow.py
  - tests/test_sensor_cmd.py
  - tests/skip_registry.py
  - docs/test-triage-149.md
autonomous: true
requirements: [SUITE-01]

must_haves:
  truths:
    - "All 23 Cluster-1 (SSRF/DNS-blocked sandbox) failures have an xfail marker + matching registry entry + ledger row"
    - "The skip-registry meta-gate remains green after Cluster 1's markers are added"
  artifacts:
    - path: "docs/test-triage-149.md"
      provides: "23 new rows under the Cluster 1 heading"
    - path: "tests/skip_registry.py"
      provides: "23 new pre_existing_triage_149 entries for Cluster 1 tests"
  key_links:
    - from: "tests/test_notify_email.py, tests/test_notify_webhook.py, tests/test_ticketing_servicenow.py, tests/test_sensor_cmd.py"
      to: "tests/skip_registry.py"
      via: "xfail reason= string cites the same ledger anchor as the ALLOWED_SKIPS reason"
      pattern: "TRIAGE-149:.*see docs/test-triage-149.md#"
---

<objective>
Disposition Cluster 1 (SSRF/DNS-blocked sandbox — 23 tests across `test_notify_email.py`,
`test_notify_webhook.py`, `test_ticketing_servicenow.py`, `test_sensor_cmd.py`) per RESEARCH.md's
Failure Clusters table row 1: these fail with `ValueError: SSRF blocked (dns_failure)` because this
sandbox has no outbound DNS resolution, not because of a real regression.

Purpose: per D-01, this phase does not fix the sandbox's DNS limitation — it records an explicit,
machine-checkable quarantine so Phase 150 knows these need re-verification on a DNS-enabled runner.
Output: 23 `xfail` markers + 23 `ALLOWED_SKIPS` entries + 23 ledger rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/149-test-suite-triage/149-CONTEXT.md
@.planning/phases/149-test-suite-triage/149-RESEARCH.md
@.planning/phases/149-test-suite-triage/149-PATTERNS.md
</context>

<interfaces>
Reason-string convention (locked, reuse exactly):
`"TRIAGE-149: <sub-reason>; see docs/test-triage-149.md#<slugified-test-id>"`
Marker convention for this cluster: use `@pytest.mark.xfail(reason="...", strict=False)` — not
`skip` — because the tests still document intent and may pass once DNS resolution is available
(per D-03's "xfail where the test still usefully documents intent and runs").
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Quarantine test_notify_email.py + test_notify_webhook.py (8 tests)</name>
  <files>tests/test_notify_email.py, tests/test_notify_webhook.py, tests/skip_registry.py</files>
  <read_first>tests/test_notify_email.py, tests/test_notify_webhook.py, tests/skip_registry.py</read_first>
  <action>
  Run `pytest tests/test_notify_email.py tests/test_notify_webhook.py -q -m ""` to confirm the 8
  failures still reproduce with `ValueError: SSRF blocked (dns_failure)`. Add
  `@pytest.mark.xfail(reason="TRIAGE-149: environment-dependent (SSRF DNS-blocked sandbox); see docs/test-triage-149.md#<slug>", strict=False)`
  directly above each of: `test_notify_email.py::test_starttls_path_timeout_and_recipients`,
  `test_ssl_path_timeout_passed`, `test_no_login_when_smtp_user_none`; `test_notify_webhook.py::test_no_hmac_when_key_env_not_set`,
  `test_hmac_header_present_when_key_set`, `test_hmac_absent_when_key_env_empty`,
  `test_body_omits_topology_keys`, `test_non_2xx_raises_runtime_error`. Use the anchor slug produced by
  lowercasing the test's fully-qualified node ID and replacing `::`/`.` with `-` (e.g.
  `test_notify_webhookpy-test_no_hmac_when_key_env_not_set`). Add one matching `ALLOWED_SKIPS` tuple
  per test in `tests/skip_registry.py` under a new
  `# Phase 149 D-02/D-03: test-suite triage quarantines — see docs/test-triage-149.md` banner comment,
  category `"pre_existing_triage_149"`, reason string identical to the decorator's.
  </action>
  <verify>
    <automated>pytest tests/test_notify_email.py tests/test_notify_webhook.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - 8 `@pytest.mark.xfail` decorators added, each with a `TRIAGE-149:` reason string
    - 8 matching `ALLOWED_SKIPS` tuples added with category `"pre_existing_triage_149"`
    - `pytest tests/test_notify_email.py tests/test_notify_webhook.py -q -m ""` reports 8 xfailed, 0 failed
  </acceptance_criteria>
  <done>All 8 tests quarantined with xfail + registry entries.</done>
</task>

<task type="auto">
  <name>Task 2: Quarantine test_ticketing_servicenow.py + test_sensor_cmd.py (15 tests)</name>
  <files>tests/test_ticketing_servicenow.py, tests/test_sensor_cmd.py, tests/skip_registry.py</files>
  <read_first>tests/test_ticketing_servicenow.py, tests/test_sensor_cmd.py, tests/skip_registry.py</read_first>
  <action>
  Run `pytest tests/test_ticketing_servicenow.py tests/test_sensor_cmd.py -q -m ""` to confirm the 15
  failures still reproduce. Apply the same xfail pattern from Task 1 to:
  `test_ticketing_servicenow.py::test_create_incident`, `test_dedup_then_work_notes`,
  `test_correlation_id_is_fingerprint`, `test_credentials_not_in_logs`,
  `test_create_issue_missing_sys_id_raises_runtime_error`,
  `test_create_issue_non_json_response_raises_runtime_error`; `test_sensor_cmd.py::test_push_posts_to_correct_url`,
  `test_push_retry_on_5xx`, `test_push_no_retry_on_4xx`, `test_push_connect_error_retries`,
  `test_push_409_treated_as_success`, `test_spool_on_connect_failure`, `test_spool_flush_delivers_and_unlinks`,
  `test_spool_409_unlinks_file`, `test_spool_filename_is_uuid_pattern`. Add 15 matching `ALLOWED_SKIPS`
  entries under the same Phase 149 banner comment added in Task 1.
  </action>
  <verify>
    <automated>pytest tests/test_ticketing_servicenow.py tests/test_sensor_cmd.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - 15 `@pytest.mark.xfail` decorators added with `TRIAGE-149:` reason strings
    - 15 matching `ALLOWED_SKIPS` tuples added
    - `pytest tests/test_ticketing_servicenow.py tests/test_sensor_cmd.py -q -m ""` reports 15 xfailed, 0 failed
  </acceptance_criteria>
  <done>All 15 tests quarantined with xfail + registry entries.</done>
</task>

<task type="auto">
  <name>Task 3: Write Cluster 1 ledger rows and verify the meta-gate</name>
  <files>docs/test-triage-149.md</files>
  <read_first>docs/test-triage-149.md</read_first>
  <action>
  Under the "## Cluster 1: SSRF/DNS-blocked sandbox" heading in `docs/test-triage-149.md`, add one
  table row per of the 23 tests quarantined in Tasks 1-2: `Test ID` = full node ID,
  `Disposition` = `quarantined-xfail`, `Sub-reason` = `environment-dependent (SSRF DNS-blocked sandbox)`,
  `Evidence/Notes` = the exact `ValueError: SSRF blocked (dns_failure)` message plus the note "expected
  to pass on a DNS-enabled runner; re-verify in Phase 150", `Registry entry?` = `yes (tests/skip_registry.py:<line>)`.
  Run `pytest tests/test_skip_registry.py -q -m ""` to confirm the gate is still green after 23 new
  decorators were added.
  </action>
  <verify>
    <automated>pytest tests/test_skip_registry.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - 23 new rows exist under the Cluster 1 heading, one per quarantined test ID from Tasks 1-2
    - Every row has a non-empty Disposition and Sub-reason
    - `pytest tests/test_skip_registry.py -q -m ""` passes (1 passed)
  </acceptance_criteria>
  <done>Cluster 1 fully dispositioned in the ledger; gate remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — test-file metadata changes only (decorators + registry entries + doc rows). No production
code path touched, no new inputs/outputs.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-02 | Tampering | tests/*.py xfail markers | accept | Test-only metadata; quarantine reasons are machine-checkable via the meta-gate from Plan 01. |
</threat_model>

<verification>
- `pytest tests/test_notify_email.py tests/test_notify_webhook.py tests/test_ticketing_servicenow.py tests/test_sensor_cmd.py -q -m ""` reports 23 xfailed, 0 failed
- `pytest tests/test_skip_registry.py -q -m ""` passes
- `docs/test-triage-149.md` Cluster 1 table has 23 rows
</verification>

<success_criteria>
All 23 Cluster 1 failures have an explicit xfail marker, a matching registry entry, and a ledger row citing evidence.
</success_criteria>

<output>
Create `.planning/phases/149-test-suite-triage/149-02-SUMMARY.md` when done
</output>
