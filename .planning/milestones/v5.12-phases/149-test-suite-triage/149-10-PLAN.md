---
phase: 149-test-suite-triage
plan: 10
type: execute
wave: 10
depends_on: ["149-09"]
files_modified:
  - tests/test_phase135_docs_presence.py
  - tests/test_phase136_docs_presence.py
  - tests/test_safe_filter_audit.py
  - tests/test_scan_error_gate.py
  - tests/test_sensor_windows_smoke.py
  - tests/skip_registry.py
  - docs/test-triage-149.md
autonomous: true
requirements: [SUITE-01]

must_haves:
  truths:
    - "Each of Cluster 9 Group D2's 5 docs-presence/security-gate/windows-smoke failures has an individually-investigated disposition"
    - "test_safe_filter_audit.py and test_scan_error_gate.py findings are flagged as security-relevant if a real unsanitized `| safe` usage or safe_str bypass is confirmed"
  artifacts:
    - path: "docs/test-triage-149.md"
      provides: "5 rows under Cluster 9 for the docs/security-gate/windows-smoke subsystem (second half of Group D)"
---

<objective>
Individually investigate and disposition Cluster 9 Group D2, the final cluster-9 sub-group: 5 failures
— `test_phase135_docs_presence.py`(1), `test_phase136_docs_presence.py`(1),
`test_safe_filter_audit.py`(1, Jinja `| safe` without `| sanitize`), `test_scan_error_gate.py`(1,
`scan_error` writes bypassing `safe_str`), `test_sensor_windows_smoke.py`(1, `exit=-11` SIGSEGV on
KeyboardInterrupt — a second SIGSEGV in this failure set, distinct from Cluster 9 Group C's QRAMM crash).

Purpose: `test_safe_filter_audit.py` and `test_scan_error_gate.py` are both security-gate meta-tests
(per `feedback_report_render_tests_presence_not_appearance` project memory: this project treats
render-injection-hardening gates as load-bearing) — a failure here could mean a genuine new unsanitized
`| safe` usage or `safe_str` bypass was introduced, which is a real security-relevant finding, not
routine drift. `test_sensor_windows_smoke.py`'s SIGSEGV needs the same crash-vs-artifact investigation
rigor as Plan 08's QRAMM SIGSEGV pair — do not assume they share a cause without checking.
Output: 5 tests individually investigated and quarantined with distinct, evidence-backed reasons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/149-test-suite-triage/149-CONTEXT.md
@.planning/phases/149-test-suite-triage/149-RESEARCH.md
@.planning/phases/149-test-suite-triage/149-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate + quarantine test_phase135_docs_presence.py, test_phase136_docs_presence.py, test_sensor_windows_smoke.py (3 tests)</name>
  <files>tests/test_phase135_docs_presence.py, tests/test_phase136_docs_presence.py, tests/test_sensor_windows_smoke.py, tests/skip_registry.py</files>
  <read_first>tests/test_phase135_docs_presence.py, tests/test_phase136_docs_presence.py, tests/test_sensor_windows_smoke.py</read_first>
  <action>
  Read `test_phase135_docs_presence.py::test_required_sections_present` (`Phase 135 docs missing
  required sections: [('README.md', 'v...')]` — check the actual current `README.md` content against
  the specific version-string section the test expects; this may relate to a version bump that moved or
  reworded that section). Read `test_phase136_docs_presence.py::test_section9_deferred_topics_absent`
  (`§9 leaks deferred Phase 137 content: ['snmpv3']` — check the current docs file's §9 content for the
  literal string `snmpv3` to confirm whether Phase 137's content genuinely leaked into a section meant
  to defer it, or the test's leak-detection list is stale). Read
  `test_sensor_windows_smoke.py::TestCleanShutdownOnKeyboardInterrupt::test_keyboard_interrupt_in_run_sensor_exits_130`
  (`Expected exit code 0, 1, or 130 on KeyboardInterrupt, got -11`) — this is a SECOND, independent
  SIGSEGV in this failure set (distinct file/subsystem from Plan 08's QRAMM CLI crash). Apply the same
  investigation rigor as Plan 08 Task 1: identify the exact code path that raises KeyboardInterrupt in
  this test, check whether the crash reproduces standalone, and record cryptography/openssl versions if
  the sensor code touches TLS. Do not assume this shares Plan 08's root cause without evidence — record
  independently whether it looks like the same native-library class of issue or a distinct cause.
  Quarantine all 3 with xfail (docs tests) / skip (the SIGSEGV test, per D-03's "environment-broken"
  guidance) + matching `ALLOWED_SKIPS` entries.
  </action>
  <verify>
    <automated>pytest tests/test_phase135_docs_presence.py tests/test_phase136_docs_presence.py tests/test_sensor_windows_smoke.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - `test_phase135_docs_presence.py`'s reason cites the exact current README.md section content found
    - `test_phase136_docs_presence.py`'s reason states explicitly whether "snmpv3" is genuinely present
      in the current docs §9 (real leak) or the detection list is stale
    - `test_sensor_windows_smoke.py`'s reason records the exact code path, standalone-reproduction result,
      and whether it appears related to Plan 08's QRAMM SIGSEGV or is an independent crash
    - 3 markers (2 xfail + 1 skip) + 3 matching `ALLOWED_SKIPS` entries added
    - `pytest tests/test_phase135_docs_presence.py tests/test_phase136_docs_presence.py tests/test_sensor_windows_smoke.py -q -m ""` reports 3 skipped/xfailed, 0 failed
  </acceptance_criteria>
  <done>All 3 tests individually investigated, with the second SIGSEGV's relationship (or lack thereof) to Plan 08's crash explicitly recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Investigate + quarantine test_safe_filter_audit.py and test_scan_error_gate.py (security-gate meta-tests)</name>
  <files>tests/test_safe_filter_audit.py, tests/test_scan_error_gate.py, tests/skip_registry.py</files>
  <read_first>tests/test_safe_filter_audit.py, tests/test_scan_error_gate.py</read_first>
  <action>
  Read `test_safe_filter_audit.py::test_safe_filter_paired_with_sanitize` in full — this scans report
  templates for Jinja `| safe` filter usages without an upstream `| sanitize` (per
  `feedback_report_render_tests_presence_not_appearance` project memory, render-injection-hardening
  gates are load-bearing security controls in this project). Identify the EXACT template file and line
  that triggers the failure (the test's own failure message should list it — quote it precisely). Grep
  that template location to confirm whether it's a genuinely new unsanitized `| safe` usage (a real
  security-relevant finding — Phase 150 must fix, flag prominently) or a pre-existing usage the test's
  scan logic newly started flagging (e.g. a path/glob pattern change in the test itself). Read
  `test_scan_error_gate.py::test_scan_error_writes_use_safe_str` similarly (`scan_error writes bypassing
  safe_str`) — identify the exact write site bypassing `safe_str` and determine real-bypass vs.
  stale-detection-list the same way. Quarantine both with xfail + matching `ALLOWED_SKIPS` entries;
  reason strings MUST explicitly state real-security-finding vs. stale-detection-logic, prefixed
  `SECURITY:` in the reason if a real bypass/unsanitized-usage is confirmed.
  </action>
  <verify>
    <automated>pytest tests/test_safe_filter_audit.py tests/test_scan_error_gate.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - Each reason string quotes the exact template/write-site location the test's own failure output identified
    - Each reason string explicitly states real-security-finding vs. stale-detection-logic — not left ambiguous
    - Reason is prefixed `SECURITY:` if a real unsanitized-usage or safe_str-bypass is confirmed
    - 2 `@pytest.mark.xfail` decorators + 2 matching `ALLOWED_SKIPS` entries added
    - `pytest tests/test_safe_filter_audit.py tests/test_scan_error_gate.py -q -m ""` reports 2 xfailed, 0 failed
  </acceptance_criteria>
  <done>Both security-gate meta-tests investigated with an explicit real-finding-vs-stale-logic determination.</done>
</task>

<task type="auto">
  <name>Task 3: Write Cluster 9 Group D2 ledger rows and verify the meta-gate</name>
  <files>docs/test-triage-149.md</files>
  <read_first>docs/test-triage-149.md</read_first>
  <action>
  Add 5 rows under "## Cluster 9: Remaining individually-distinct failures" for this group's tests, each
  citing the specific finding from Tasks 1-2. Preserve the `SECURITY:` prefix in the Evidence/Notes
  column for any row where Task 2 confirmed a real finding. Run
  `pytest tests/test_skip_registry.py -q -m ""` to confirm the gate remains green.
  </action>
  <verify>
    <automated>pytest tests/test_skip_registry.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - 5 new rows added under Cluster 9 for this group, each with distinct Evidence/Notes text
    - Any `SECURITY:`-flagged finding from Task 2 is preserved verbatim in its ledger row
    - `pytest tests/test_skip_registry.py -q -m ""` passes
  </acceptance_criteria>
  <done>Cluster 9 Group D2 fully dispositioned and ledgered; gate remains green. This completes all 116-baseline test dispositions across Plans 01-10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Report template rendering (`| safe` Jinja filter usage) | Investigated read-only in this plan; if a genuinely unsanitized `| safe` usage is confirmed, it is a real XSS-class risk in rendered reports and is flagged `SECURITY:` for Phase 150, not silently deferred. |
| scan_error write paths (`safe_str` bypass) | Investigated read-only; a confirmed bypass risks unsanitized data reaching a report/log surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-10 | Tampering/Injection | Report template `\| safe` filter audit | mitigate | If a real unsanitized usage is confirmed, flagged `SECURITY:` in the ledger for Phase 150 priority fix — not fixed in this disposition-only phase (D-01), but not silently downgraded to routine drift either. |
| T-149-10b | Tampering | scan_error safe_str bypass | mitigate | Same treatment — flagged `SECURITY:` if confirmed real, deferred to Phase 150 for the actual fix. |
| T-149-10c | Denial of Service | test_sensor_windows_smoke.py SIGSEGV | mitigate | Investigated with the same rigor as Plan 08's QRAMM crash; documented for Phase 150, not fixed here. |
</threat_model>

<verification>
- `pytest tests/test_phase135_docs_presence.py tests/test_phase136_docs_presence.py tests/test_safe_filter_audit.py tests/test_scan_error_gate.py tests/test_sensor_windows_smoke.py -q -m ""` reports 5 skipped/xfailed, 0 failed
- `pytest tests/test_skip_registry.py -q -m ""` passes
- Ledger has 5 distinct rows for Group D2, with any real security finding flagged `SECURITY:`
</verification>

<success_criteria>
All 5 Cluster 9 Group D2 failures individually investigated and dispositioned; both security-gate meta-tests have an explicit real-vs-stale determination.
</success_criteria>

<output>
Create `.planning/phases/149-test-suite-triage/149-10-SUMMARY.md` when done
</output>
