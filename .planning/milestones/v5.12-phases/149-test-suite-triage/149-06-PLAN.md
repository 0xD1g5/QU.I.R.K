---
phase: 149-test-suite-triage
plan: 06
type: execute
wave: 6
depends_on: ["149-05"]
files_modified:
  - tests/scanner/test_jwt_hardening.py
  - tests/test_broker_scanner_rabbitmq.py
  - tests/test_identity_scanner_hardening.py
  - tests/test_jwt_scanner.py
  - tests/test_openapi_scanner.py
  - tests/test_tls_scanner_chain_verified.py
  - tests/test_vault_connector.py
  - tests/test_gap_closure.py
  - tests/test_skip_registry.py
  - tests/skip_registry.py
  - docs/test-triage-149.md
autonomous: true
requirements: [SUITE-01]

must_haves:
  truths:
    - "The AST walker's tests/scanner/ subdirectory gap (Assumption A3) is resolved before this plan adds any marker inside tests/scanner/"
    - "Each of Cluster 9 Group A's 18 scanner/detection-logic failures has an individually-investigated disposition, not a blanket classification"
  artifacts:
    - path: "docs/test-triage-149.md"
      provides: "18 rows under Cluster 9 documenting per-test investigation for the scanner/detection subsystem"
  key_links:
    - from: "tests/test_skip_registry.py TESTS_DIR walk"
      to: "tests/scanner/test_jwt_hardening.py"
      via: "rglob (recursive) instead of glob (non-recursive) so subdirectory tests are enforced"
      pattern: "rglob"
---

<objective>
Resolve Assumption A3 (the meta-gate's `TESTS_DIR.glob("*.py")` walk does not descend into
`tests/scanner/`, so any marker added there would silently bypass registration enforcement), then
individually investigate and disposition Cluster 9 Group A: 18 scanner/detection-logic failures across
8 files — `tests/scanner/test_jwt_hardening.py`(2), `tests/test_broker_scanner_rabbitmq.py`(2),
`tests/test_identity_scanner_hardening.py`(2), `tests/test_jwt_scanner.py`(6),
`tests/test_openapi_scanner.py`(1), `tests/test_tls_scanner_chain_verified.py`(2),
`tests/test_vault_connector.py`(1), `tests/test_gap_closure.py`(2).

Purpose: this is the first plan in the phase to add a quarantine marker inside `tests/scanner/`, so the
non-recursive-glob gap must be closed first or the marker would be genuinely unenforced (not just
under-tested). None of these 18 failures were pre-clustered by RESEARCH.md with a shared root cause —
each needs its own grep/read pass per RESEARCH.md's cluster-9 guidance.
Output: walker gap closed; 18 tests individually investigated and dispositioned.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/149-test-suite-triage/149-CONTEXT.md
@.planning/phases/149-test-suite-triage/149-RESEARCH.md
@.planning/phases/149-test-suite-triage/149-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close the tests/scanner/ non-recursive glob gap (Assumption A3)</name>
  <files>tests/test_skip_registry.py</files>
  <read_first>tests/test_skip_registry.py</read_first>
  <action>
  In `tests/test_skip_registry.py`, change `TESTS_DIR.glob("*.py")` (line ~80) to
  `TESTS_DIR.rglob("*.py")` so the walk descends into `tests/scanner/` and any other subdirectories.
  `_allowed()` already matches on bare `py_file.name` only (not full path), so no change is needed
  there — existing `ALLOWED_SKIPS` entries for top-level `tests/*.py` files remain valid, and new
  entries for `tests/scanner/*.py` files use the same bare-filename convention (e.g.
  `"test_jwt_hardening.py"`, not `"scanner/test_jwt_hardening.py"`). Confirm this doesn't introduce
  false-positive violations from any other subdirectory under `tests/` by running the full gate and
  reading any new violation it reports (there may be additional unregistered skips in `tests/scanner/`
  or other subdirs beyond `test_jwt_hardening.py` — triage any found the same way Plan 01 triaged the
  original D-04 list: register with the correct existing category or delete per D-05).
  </action>
  <verify>
    <automated>pytest tests/test_skip_registry.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_skip_registry.py` uses `TESTS_DIR.rglob("*.py")`, confirmed via `grep -n "rglob" tests/test_skip_registry.py`
    - Any newly-surfaced violation from subdirectories is triaged (registered or deleted) before proceeding
    - `pytest tests/test_skip_registry.py -q -m ""` passes after triage
  </acceptance_criteria>
  <done>The gate now walks tests/scanner/ (and any other subdirectory) recursively; any newly-surfaced drift is resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Investigate and quarantine 12 failures (scanner/test_jwt_hardening.py, test_broker_scanner_rabbitmq.py, test_identity_scanner_hardening.py, test_jwt_scanner.py)</name>
  <files>tests/scanner/test_jwt_hardening.py, tests/test_broker_scanner_rabbitmq.py, tests/test_identity_scanner_hardening.py, tests/test_jwt_scanner.py, tests/skip_registry.py</files>
  <read_first>tests/scanner/test_jwt_hardening.py, tests/test_broker_scanner_rabbitmq.py, tests/test_identity_scanner_hardening.py, tests/test_jwt_scanner.py</read_first>
  <action>
  Read each failing test and its production-code target, and record a specific root cause per test —
  do not use a single blanket explanation across the 4 files:
  `test_jwt_hardening.py::test_allow_insecure_jwks_uses_verify_false_and_emits_advisory` (assertion:
  "Expected at least one verify=False call"), `test_scan_jwt_targets_propagates_flag` (assert False);
  `test_broker_scanner_rabbitmq.py::test_enrich_rabbitmq_mgmt_success` (`KeyError: 'rabbitmq_version'`),
  `test_enrich_rabbitmq_mgmt_401` (`Expected {'mgmt_auth': 'rejected_401'}, got {}`);
  `test_identity_scanner_hardening.py::test_kdc_udp_decode_failure_logs` (`AttributeError: module
  'quirk.scanner.kerberos_scanner' has no attribute 'decode...'`), `test_build_as_req_nonce_uses_secrets`
  (`NameError: name 'constants' is not defined`); `test_jwt_scanner.py::test_multi_key_jwks`,
  `test_jwt_rsa_key_size`, `test_jwt_ec_key_size`, `test_jwt_query_param_cred_ctx_appends_key_to_url`,
  `test_jwt_no_cred_ctx_unchanged_behavior`, `test_append_query_param_continue_iteration_skips_conflicting_target`
  (all `assert 0 == N` or mock-not-called patterns, suggesting a shared upstream change in JWT-scanning
  request flow — read `quirk/scanner/jwt_scanner.py` to confirm whether all 6 share one root cause or
  are 6 distinct assertion drifts). Per D-01, do not fix any of these — quarantine each with
  `@pytest.mark.xfail(reason="TRIAGE-149: <specific finding>; see docs/test-triage-149.md#<slug>",
  strict=False)` (use xfail since these still document intended detection behavior) and add matching
  `ALLOWED_SKIPS` entries, category `"pre_existing_triage_149"`.
  </action>
  <verify>
    <automated>pytest tests/scanner/test_jwt_hardening.py tests/test_broker_scanner_rabbitmq.py tests/test_identity_scanner_hardening.py tests/test_jwt_scanner.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 12 tests has a distinct, specific reason string (not a copy-pasted generic explanation)
    - `NameError: name 'constants' is not defined` in `test_build_as_req_nonce_uses_secrets` is
      specifically noted as a likely missing-import bug in the test file itself (not production code) —
      confirm via grep whether `constants` is imported anywhere in the test file or its target module
    - 12 `@pytest.mark.xfail` decorators + 12 matching `ALLOWED_SKIPS` entries added
    - `pytest tests/scanner/test_jwt_hardening.py tests/test_broker_scanner_rabbitmq.py tests/test_identity_scanner_hardening.py tests/test_jwt_scanner.py -q -m ""` reports 12 xfailed, 0 failed
  </acceptance_criteria>
  <done>All 12 tests individually investigated with distinct findings and quarantined.</done>
</task>

<task type="auto">
  <name>Task 3: Investigate and quarantine remaining 6 failures, then write Cluster 9 Group A ledger rows</name>
  <files>tests/test_openapi_scanner.py, tests/test_tls_scanner_chain_verified.py, tests/test_vault_connector.py, tests/test_gap_closure.py, tests/skip_registry.py, docs/test-triage-149.md</files>
  <read_first>tests/test_openapi_scanner.py, tests/test_tls_scanner_chain_verified.py, tests/test_vault_connector.py, tests/test_gap_closure.py</read_first>
  <action>
  Read and individually disposition: `test_openapi_scanner.py::test_url_scope_accepts_bare_fqdn_target`
  (`Expected 'get' to have been called once. Called 0 times.` — trace the URL-scope-matching logic to
  confirm whether a bare-FQDN target's scope check regressed); `test_tls_scanner_chain_verified.py::test_sslyze_success_chain_verified_true`
  and `test_sslyze_success_chain_verified_false` (`assert None is not None` — note this file already
  has 2 `ALLOWED_SKIPS` entries at lines 140/152 for a different sslyze optional-extra reason; confirm
  these 2 NEW failures are at different line numbers and are a distinct assertion, not accidentally the
  already-registered skip reappearing); `test_vault_connector.py::test_pki_sha1_signed_ca_high_severity`
  (`RuntimeError: openssl SHA1 cert failed` — likely an OpenSSL version/build difference in this sandbox
  rejecting SHA1 cert generation outright; confirm by running `openssl req -x509 -sha1 ...` manually if
  needed, disposition as environment-dependent if confirmed); `test_gap_closure.py::test_findings_quantum_label_dsa`
  and `test_findings_quantum_label_ecdsa` (`Expected at least one Vulnerable finding for DSA/ECDSA, got:
  []` — read the quantum-label classifier to confirm whether DSA/ECDSA detection regressed or the test
  fixture's target no longer produces a DSA/ECDSA finding). Quarantine each with xfail + matching
  `ALLOWED_SKIPS` entry, category `"pre_existing_triage_149"`, reason citing the specific finding. Then
  write all 18 Cluster 9 Group A rows (12 from Task 2 + 6 from this task) under "## Cluster 9: Remaining
  individually-distinct failures" in `docs/test-triage-149.md`, each with its own Evidence/Notes text —
  no two rows should have identical Evidence text unless the investigation genuinely found the same
  cause. Run `pytest tests/test_skip_registry.py -q -m ""` to confirm the gate stays green.
  </action>
  <verify>
    <automated>pytest tests/test_skip_registry.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - 6 more `@pytest.mark.xfail` decorators + 6 matching `ALLOWED_SKIPS` entries added
    - `docs/test-triage-149.md` has 18 new rows under Cluster 9 (this group's share), each with distinct Evidence/Notes
    - `pytest tests/test_skip_registry.py -q -m ""` passes
  </acceptance_criteria>
  <done>All 18 Group A tests individually investigated, quarantined, and ledgered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — test metadata, meta-gate walk-scope fix, and documentation only. Investigation reads production
scanner code (JWT, Kerberos, OpenAPI, TLS, Vault, quantum-label classifier) but does not modify any of it.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-06 | Tampering | tests/test_skip_registry.py glob scope | mitigate | Closing the non-recursive-glob gap strengthens the enforcement mechanism (was previously a silent bypass for any tests/scanner/ marker) rather than weakening it. |
| T-149-06b | Tampering | tests/scanner/*.py, tests/test_*.py xfail markers | accept | Test-only metadata; no production code modified. |
</threat_model>

<verification>
- `pytest tests/test_skip_registry.py -q -m ""` passes with the rglob change active
- `pytest tests/scanner/test_jwt_hardening.py tests/test_broker_scanner_rabbitmq.py tests/test_identity_scanner_hardening.py tests/test_jwt_scanner.py tests/test_openapi_scanner.py tests/test_tls_scanner_chain_verified.py tests/test_vault_connector.py tests/test_gap_closure.py -q -m ""` reports 18 xfailed, 0 failed
- Ledger has 18 distinct rows for Group A
</verification>

<success_criteria>
Glob gap closed; all 18 Cluster 9 Group A failures individually investigated with distinct, evidence-backed dispositions.
</success_criteria>

<output>
Create `.planning/phases/149-test-suite-triage/149-06-SUMMARY.md` when done
</output>
