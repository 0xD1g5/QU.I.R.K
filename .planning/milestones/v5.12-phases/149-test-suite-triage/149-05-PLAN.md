---
phase: 149-test-suite-triage
plan: 05
type: execute
wave: 5
depends_on: ["149-04"]
files_modified:
  - tests/test_auto_merge_trigger.py
  - tests/test_sensor_push_id_revalidation.py
  - tests/skip_registry.py
  - docs/test-triage-149.md
autonomous: true
requirements: [SUITE-01]

must_haves:
  truths:
    - "test_auto_merge_trigger.py's 8 fixture-mismatch failures are quarantined as outdated-fixture, distinct from test_sensor_push_id_revalidation.py's 2 failures"
    - "test_sensor_push_id_revalidation.py's row-count mismatch (Open Question 3) is individually investigated, not batch-classified with the fixture explanation"
  artifacts:
    - path: "docs/test-triage-149.md"
      provides: "10 rows under Cluster 5, with test_sensor_push_id_revalidation.py's disposition citing its own investigation, not cluster 5's shared fixture rationale"
  key_links:
    - from: "quirk/dashboard/api/routes/sensor.py"
      to: "tests/test_auto_merge_trigger.py fixtures"
      via: "_UUID_RE.match() shape guard (AUDIT-08) vs human-readable sensor-a/sensor-b fixture IDs"
      pattern: "_UUID_RE"
---

<objective>
Disposition Cluster 5 (sensor_id shape / AUDIT-08 regression, 10 tests): `test_auto_merge_trigger.py`
(8 tests, fixture IDs like `"sensor-a"` fail the new UUID-shape guard) and
`test_sensor_push_id_revalidation.py` (2 tests, a distinct row-count mismatch — "9 SensorPush row(s)
found" — that RESEARCH.md explicitly flags as needing individual investigation, per Open Question 3,
not the fixture-mismatch explanation).

Purpose: `quirk/dashboard/api/routes/sensor.py:494`'s `_UUID_RE.match()` guard (AUDIT-08,
"defense-in-depth") rejects non-UUID sensor IDs. `test_auto_merge_trigger.py`'s fixtures predate this
guard. `test_sensor_push_id_revalidation.py` is AUDIT-08's *own* test file, testing a different
assertion (row count) — RESEARCH.md's Assumption A2 flags that this could be a genuine AUDIT-08
implementation bug, not a fixture issue; this plan must determine which before writing a disposition.
Output: 8 quarantined fixture-mismatch tests + 2 individually-investigated tests + 10 ledger rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/149-test-suite-triage/149-CONTEXT.md
@.planning/phases/149-test-suite-triage/149-RESEARCH.md
@.planning/phases/149-test-suite-triage/149-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quarantine test_auto_merge_trigger.py's 8 outdated-fixture failures</name>
  <files>tests/test_auto_merge_trigger.py, tests/skip_registry.py</files>
  <read_first>tests/test_auto_merge_trigger.py, quirk/dashboard/api/routes/sensor.py</read_first>
  <action>
  Read `quirk/dashboard/api/routes/sensor.py` around line 494 to confirm the `_UUID_RE.match()` shape
  guard's exact regex and the AUDIT-08 comment context. Confirm each of
  `test_auto_merge_trigger.py::test_all_sensors_in_triggers_merge`, `test_auto_merge_disabled`,
  `test_revoked_sensor_excluded`, `test_mixed_token_sensor_is_required_for_all_in`,
  `test_zero_token_sensor_not_counted_as_active`, `test_merge_failure_isolated`,
  `test_double_fire_harmless`, `test_cadence_window_triggers` fails with `400: {"detail":"Invalid
  sensor_id shape"}` and that its fixture setup uses a non-UUID sensor ID (e.g. `"sensor-a"`,
  `"sensor-b"`). Add `@pytest.mark.xfail(reason="TRIAGE-149: outdated-fixture (AUDIT-08 UUID-shape
  guard added after these fixtures; fixture IDs need updating to valid UUIDs); see
  docs/test-triage-149.md#<slug>", strict=False)` above each — use xfail (not skip) since the test
  still documents the intended auto-merge-trigger behavior and would pass again once its fixture IDs
  are updated (a Phase 150 fix). Add 8 matching `ALLOWED_SKIPS` entries with category
  `"pre_existing_triage_149"`.
  </action>
  <verify>
    <automated>pytest tests/test_auto_merge_trigger.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - All 8 tests confirmed to use non-UUID sensor_id fixture values before quarantine
    - 8 `@pytest.mark.xfail` decorators + 8 matching `ALLOWED_SKIPS` entries added
    - `pytest tests/test_auto_merge_trigger.py -q -m ""` reports 8 xfailed, 0 failed
  </acceptance_criteria>
  <done>All 8 outdated-fixture tests quarantined with evidence tying the failure to the AUDIT-08 UUID guard.</done>
</task>

<task type="auto">
  <name>Task 2: Individually investigate test_sensor_push_id_revalidation.py's row-count mismatch (Open Question 3)</name>
  <files>tests/test_sensor_push_id_revalidation.py</files>
  <read_first>tests/test_sensor_push_id_revalidation.py, quirk/dashboard/api/routes/sensor.py</read_first>
  <action>
  Read `test_malformed_sensor_id_path_traversal_rejected` and
  `test_malformed_sensor_id_short_string_rejected` in full, including their fixture setup and the exact
  assertion that produces "AUDIT-08 RED: 9 SensorPush row(s) found; malformed sensor_id" /
  "AUDIT-08 RED: 9 SensorPush row(s) found after malformed id". Trace the row-count logic back through
  `quirk/dashboard/api/routes/sensor.py`'s SensorPush handling to determine: (a) is the test's expected
  row count itself outdated (a fixture/assertion staleness issue, same shape as Task 1), or (b) does the
  malformed-sensor_id rejection path fail to prevent a row from being written before the 400 is
  returned (a genuine AUDIT-08 implementation defect — the guard rejects the request but a
  side-effecting write already happened)? Do NOT fix any implementation bug found (D-01) — determine
  which explanation is correct via grep/read and record the finding precisely so the ledger row and
  Phase 150 handoff are accurate.
  </action>
  <verify>
    <automated>pytest tests/test_sensor_push_id_revalidation.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - A specific root-cause explanation is recorded (fixture-stale row-count expectation vs. a genuine
      write-before-reject ordering defect in the AUDIT-08 guard), citing the exact code path read
    - If the investigation finds a genuine implementation defect, this is flagged explicitly as
      "real regression — Phase 150 must fix" in the disposition, not silently folded into the
      fixture-mismatch narrative from Task 1
  </acceptance_criteria>
  <done>The row-count mismatch's actual cause is determined and distinctly recorded, not assumed to match Task 1's fixture explanation.</done>
</task>

<task type="auto">
  <name>Task 3: Quarantine test_sensor_push_id_revalidation.py per its investigated cause and write Cluster 5 ledger rows</name>
  <files>tests/test_sensor_push_id_revalidation.py, tests/skip_registry.py, docs/test-triage-149.md</files>
  <read_first>docs/test-triage-149.md</read_first>
  <action>
  Based on Task 2's finding, add `@pytest.mark.xfail(reason="TRIAGE-149: <exact sub-reason from Task 2
  — either 'outdated row-count fixture expectation' or 'AUDIT-08 write-before-reject ordering defect,
  flagged for Phase 150'>; see docs/test-triage-149.md#<slug>", strict=False)` above each of the 2
  tests, with a matching `ALLOWED_SKIPS` entry (category `"pre_existing_triage_149"`). Write ledger rows
  under "## Cluster 5: sensor_id shape / AUDIT-08 regression": 8 rows for `test_auto_merge_trigger.py`
  (Sub-reason `outdated-fixture (AUDIT-08 UUID guard)`) and 2 rows for
  `test_sensor_push_id_revalidation.py` with Sub-reason and Evidence taken directly from Task 2's
  investigation — do not reuse Task 1's fixture wording for these 2 rows unless Task 2 concluded that is
  actually the same cause. Run `pytest tests/test_skip_registry.py -q -m ""` to confirm the gate stays green.
  </action>
  <verify>
    <automated>pytest tests/test_skip_registry.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - 2 more `@pytest.mark.xfail` decorators + 2 more `ALLOWED_SKIPS` entries added
    - `docs/test-triage-149.md` Cluster 5 heading has exactly 10 rows
    - The 2 `test_sensor_push_id_revalidation.py` rows' Sub-reason text is distinct from (not copy-pasted
      from) the 8 `test_auto_merge_trigger.py` rows unless Task 2 explicitly confirmed the same cause
    - `pytest tests/test_skip_registry.py -q -m ""` passes
  </acceptance_criteria>
  <done>Cluster 5 fully dispositioned with the row-count mismatch's real cause distinctly documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Sensor push endpoint (read-only investigation) | This plan reads `quirk/dashboard/api/routes/sensor.py`'s AUDIT-08 guard to explain test failures but does not modify it — the guard itself remains in force. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-05 | Tampering/Information Disclosure | tests/test_auto_merge_trigger.py, tests/test_sensor_push_id_revalidation.py | accept | Test-only metadata changes; if Task 2 finds a genuine AUDIT-08 write-before-reject defect, it is flagged for Phase 150 to fix, not silently patched here (D-01 disposition-only boundary). |
</threat_model>

<verification>
- `pytest tests/test_auto_merge_trigger.py tests/test_sensor_push_id_revalidation.py -q -m ""` reports 10 xfailed, 0 failed
- `pytest tests/test_skip_registry.py -q -m ""` passes
- Ledger has 10 distinct rows under Cluster 5, with the 2 revalidation-test rows citing Task 2's specific finding
</verification>

<success_criteria>
All 10 Cluster 5 failures dispositioned, with the sensor_id revalidation pair individually investigated per Open Question 3 rather than batch-classified.
</success_criteria>

<output>
Create `.planning/phases/149-test-suite-triage/149-05-SUMMARY.md` when done
</output>
