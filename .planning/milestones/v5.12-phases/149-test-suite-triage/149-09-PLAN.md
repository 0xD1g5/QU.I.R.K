---
phase: 149-test-suite-triage
plan: 09
type: execute
wave: 9
depends_on: ["149-08"]
files_modified:
  - tests/test_cbom_schema_validation.py
  - tests/test_cli_correctness.py
  - tests/test_cli_init.py
  - tests/test_compliance_title_join.py
  - tests/test_email_run_scan_wiring.py
  - tests/test_errors_cmd.py
  - tests/test_install_errors.py
  - tests/test_posture_scorefix125.py
  - tests/skip_registry.py
  - docs/test-triage-149.md
autonomous: true
requirements: [SUITE-01]

must_haves:
  truths:
    - "Each of Cluster 9 Group D1's 11 CLI/compliance/posture failures has an individually-investigated disposition"
  artifacts:
    - path: "docs/test-triage-149.md"
      provides: "11 rows under Cluster 9 for the CLI/compliance/posture subsystem (first half of Group D)"
---

<objective>
Individually investigate and disposition Cluster 9 Group D1: 11 failures across 8 files —
`test_cbom_schema_validation.py`(1, docker-compose profile drift), `test_cli_correctness.py`(1,
`test_no_quirk_scan_references`), `test_cli_init.py`(2), `test_compliance_title_join.py`(1),
`test_email_run_scan_wiring.py`(1), `test_errors_cmd.py`(1), `test_install_errors.py`(2),
`test_posture_scorefix125.py`(2, GCP 403 scan_error emission).

Purpose: no shared root cause was identified across these 8 files by RESEARCH.md's clustering pass —
each requires its own grep/read investigation before disposition, per the cluster-9 guidance.
Output: 11 tests individually investigated and quarantined with distinct, evidence-backed reasons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/149-test-suite-triage/149-CONTEXT.md
@.planning/phases/149-test-suite-triage/149-RESEARCH.md
@.planning/phases/149-test-suite-triage/149-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate + quarantine test_cbom_schema_validation.py, test_cli_correctness.py, test_cli_init.py, test_compliance_title_join.py (5 tests)</name>
  <files>tests/test_cbom_schema_validation.py, tests/test_cli_correctness.py, tests/test_cli_init.py, tests/test_compliance_title_join.py, tests/skip_registry.py</files>
  <read_first>tests/test_cbom_schema_validation.py, tests/test_cli_correctness.py, tests/test_cli_init.py, tests/test_compliance_title_join.py</read_first>
  <action>
  Read `test_cbom_schema_validation.py::test_parametrize_set_matches_docker_compose_profiles`
  (`AssertionError: Drift between docker-compose.yml profiles and PROFILE_ENDPOINTS` — diff the actual
  profile lists: grep `docker-compose.yml`'s `profiles:` entries in
  `quantum-chaos-enterprise-lab/docker-compose.yml` against the test's `PROFILE_ENDPOINTS` constant to
  identify exactly which profile(s) are missing/extra; per CLAUDE.md's Chaos Lab Maintenance rule, note
  whether this points to a lab.sh/expected_results drift that Phase 150 must also fix). Read
  `test_cli_correctness.py::test_no_quirk_scan_references` (`Found 'quirk scan' references (should be
  'quirk --config') in repo` — grep the repo for the literal string `quirk scan` to list every offending
  file/line so Phase 150 has a concrete cleanup list). Read `test_cli_init.py::test_init_creates_config`
  and `test_init_no_overwrite` (`config.yaml not created at temp dir` / `FileNotFoundError` — trace
  `quirk init`'s CLI implementation to see if the config-file creation path or temp-dir handling
  changed). Read `test_compliance_title_join.py::test_every_emitted_title_is_mapped_or_allowlisted`
  (`Emitted finding titles missing from COMPLIANCE_MAP and UNMAPPED allowlist` — list the specific
  missing title(s) via the test's own diff output). Quarantine all 5 with xfail + matching
  `ALLOWED_SKIPS` entries, category `"pre_existing_triage_149"`, each reason citing the specific finding
  (exact missing profile name, exact grep-found `quirk scan` locations, exact CLI code path, exact
  missing compliance title).
  </action>
  <verify>
    <automated>pytest tests/test_cbom_schema_validation.py tests/test_cli_correctness.py tests/test_cli_init.py tests/test_compliance_title_join.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - Each reason string cites the specific missing/extra item found (profile name, quirk-scan
      reference locations, CLI code path, or compliance title) — not a generic drift statement
    - 5 `@pytest.mark.xfail` decorators + 5 matching `ALLOWED_SKIPS` entries added
    - `pytest tests/test_cbom_schema_validation.py tests/test_cli_correctness.py tests/test_cli_init.py tests/test_compliance_title_join.py -q -m ""` reports 5 xfailed, 0 failed
  </acceptance_criteria>
  <done>All 5 tests individually investigated with specific, citable findings and quarantined.</done>
</task>

<task type="auto">
  <name>Task 2: Investigate + quarantine test_email_run_scan_wiring.py, test_errors_cmd.py, test_install_errors.py, test_posture_scorefix125.py (6 tests)</name>
  <files>tests/test_email_run_scan_wiring.py, tests/test_errors_cmd.py, tests/test_install_errors.py, tests/test_posture_scorefix125.py, tests/skip_registry.py</files>
  <read_first>tests/test_email_run_scan_wiring.py, tests/test_errors_cmd.py, tests/test_install_errors.py, tests/test_posture_scorefix125.py</read_first>
  <action>
  Read `test_email_run_scan_wiring.py::test_email_branch_logger_calls_use_real_logger_signatures`
  (`AssertionError: quirk.logging_util.Logger.info must take exactly one non-self positional arg` —
  check `quirk/logging_util.py`'s current `Logger.info` signature to confirm whether it now accepts a
  different arg count than the test expects). Read
  `test_errors_cmd.py::test_lookup_single_known_returns_zero` (`'QRK-INSTALL-001' not found in
  ANSI-colored output` — check whether the error-code lookup CLI's output format or the error-code
  catalog changed). Read `test_install_errors.py::test_port_conflict_format` and
  `test_dashboard_missing_uvicorn_format` (`Expected QRK-INSTALL-004`/`QRK-INSTALL-002 in output; got
  empty` — same install-error-catalog area as `test_errors_cmd.py`; check whether both share one root
  cause, e.g. a shared error-formatting helper changed). Read
  `test_posture_scorefix125.py::test_gcp_kms_403_emits_scan_error` and
  `test_gcp_sql_403_emits_scan_error` (`GCP Cloud KMS/SQL 403 (IAM permission denied) must produce a
  scan_error` — trace the GCP scanner's 403-handling path to confirm whether it now silently swallows
  the 403 instead of emitting a scan_error). Quarantine all 6 with xfail + matching `ALLOWED_SKIPS`
  entries, each reason citing the specific finding — explicitly note if `test_errors_cmd.py` and
  `test_install_errors.py`'s 3 tests share one root cause (a shared formatting/catalog defect) rather
  than treating them as 3 unrelated failures if the investigation finds otherwise.
  </action>
  <verify>
    <automated>pytest tests/test_email_run_scan_wiring.py tests/test_errors_cmd.py tests/test_install_errors.py tests/test_posture_scorefix125.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - Each reason string cites the specific signature/output/behavior found in the current source
    - If `test_errors_cmd.py` and `test_install_errors.py`'s failures share a root cause, this is stated
      explicitly in both reason strings, not left implicit
    - 6 `@pytest.mark.xfail` decorators + 6 matching `ALLOWED_SKIPS` entries added
    - `pytest tests/test_email_run_scan_wiring.py tests/test_errors_cmd.py tests/test_install_errors.py tests/test_posture_scorefix125.py -q -m ""` reports 6 xfailed, 0 failed
  </acceptance_criteria>
  <done>All 6 tests individually investigated with specific findings and quarantined.</done>
</task>

<task type="auto">
  <name>Task 3: Write Cluster 9 Group D1 ledger rows and verify the meta-gate</name>
  <files>docs/test-triage-149.md</files>
  <read_first>docs/test-triage-149.md</read_first>
  <action>
  Add 11 rows under "## Cluster 9: Remaining individually-distinct failures" for this group's tests,
  each citing the specific finding from Tasks 1-2. For the `test_cbom_schema_validation.py` row, note
  explicitly whether a chaos-lab profile drift was found (and if so, flag it for a CLAUDE.md-mandated
  `lab.sh`/`expected_results_*.md` follow-up in Phase 150, per this repo's Chaos Lab Maintenance rule).
  Run `pytest tests/test_skip_registry.py -q -m ""` to confirm the gate remains green.
  </action>
  <verify>
    <automated>pytest tests/test_skip_registry.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - 11 new rows added under Cluster 9 for this group, each with distinct Evidence/Notes text
    - `test_cbom_schema_validation.py`'s row explicitly states whether a chaos-lab profile drift requiring lab.sh/docs follow-up was found
    - `pytest tests/test_skip_registry.py -q -m ""` passes
  </acceptance_criteria>
  <done>Cluster 9 Group D1 fully dispositioned and ledgered; gate remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — test metadata, read-only investigation of CLI/compliance/GCP-scanner source, and documentation
only. No production code modified.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-09 | Tampering | tests/*.py xfail markers | accept | Test-only metadata; if `test_posture_scorefix125.py`'s investigation confirms GCP 403 errors are now silently swallowed instead of surfaced as scan_error, this is flagged for Phase 150 to fix — a silent auth-failure swallow is a posture-accuracy concern worth flagging, not silently deferred. |
</threat_model>

<verification>
- `pytest tests/test_cbom_schema_validation.py tests/test_cli_correctness.py tests/test_cli_init.py tests/test_compliance_title_join.py tests/test_email_run_scan_wiring.py tests/test_errors_cmd.py tests/test_install_errors.py tests/test_posture_scorefix125.py -q -m ""` reports 11 xfailed, 0 failed
- `pytest tests/test_skip_registry.py -q -m ""` passes
- Ledger has 11 distinct rows for Group D1
</verification>

<success_criteria>
All 11 Cluster 9 Group D1 failures individually investigated and dispositioned with distinct, evidence-backed findings.
</success_criteria>

<output>
Create `.planning/phases/149-test-suite-triage/149-09-SUMMARY.md` when done
</output>
