---
phase: 149-test-suite-triage
plan: 04
type: execute
wave: 4
depends_on: ["149-03"]
files_modified:
  - tests/test_packaging.py
  - tests/test_v41_gap_closure.py
  - tests/test_gcs_reuse.py
  - tests/skip_registry.py
  - docs/test-triage-149.md
autonomous: true
requirements: [SUITE-01]

must_haves:
  truths:
    - "Cluster 3 (stale dist-info) is resolved via pip install -e . and recorded as environment-fix-applied, not a test edit"
    - "Cluster 4's genuinely stale hardcoded version assertions are corrected or deleted per D-01's narrow exception"
    - "Cluster 7 (optional GCP extra) is quarantined under the existing optional_extra category"
  artifacts:
    - path: "docs/test-triage-149.md"
      provides: "Rows for Clusters 3, 4, and 7 (10 tests total)"
  key_links:
    - from: "quirk/__init__.py __version__ resolution"
      to: "pyproject.toml version field"
      via: "importlib.metadata reads installed dist-info first"
      pattern: "importlib.metadata"
---

<objective>
Disposition Cluster 3 (version staleness — environment, 6 tests: `test_version.py` x5 +
`test_cli_correctness.py::test_version_consistency`), Cluster 4 (version staleness — stale test
assertions, 2 tests: `test_packaging.py::test_version_is_4_2_0`,
`test_v41_gap_closure.py::TestV41GapClosure::test_pyproject_version_field_is_4_1_0`), and Cluster 7
(optional GCP extras missing, 2 tests: `test_gcs_reuse.py`) per RESEARCH.md rows 3, 4, 7, resolving
Open Question 1 (does `pip install -e .` count as in-scope environment hygiene).

Purpose: Cluster 3's tests are *correct* — the local editable install's dist-info is stale
(`pip show quirk-scanner` reports `5.10.0` vs. `pyproject.toml`'s `5.11.0`). Per RESEARCH.md's Open
Question 1 recommendation, refresh the install as in-scope hygiene (not a D-01 "fixed" disposition,
since no judgment call about test correctness was made) rather than editing a correct test. Cluster 4
is the inverse case (Pitfall 3): the tests hardcode version strings from 3+ major versions ago and are
themselves stale — correct or delete per D-01's narrow exception. Cluster 7 is a standard
`optional_extra` quarantine, no new category needed.
Output: dist-info refreshed; 2 stale assertions corrected/deleted; 2 tests quarantined; 10 ledger rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/149-test-suite-triage/149-CONTEXT.md
@.planning/phases/149-test-suite-triage/149-RESEARCH.md
@.planning/phases/149-test-suite-triage/149-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve Open Question 1 — refresh stale dist-info for Cluster 3</name>
  <files>none (environment-only; no source files modified)</files>
  <read_first>quirk/__init__.py</read_first>
  <action>
  Confirm the staleness: run `pip show quirk-scanner | grep Version`, `grep '^version' pyproject.toml`,
  and `python -c "import quirk; print(quirk.__version__)"`. If the installed version disagrees with
  `pyproject.toml`'s `5.11.0`, run `pip install -e .` to refresh the editable install's dist-info
  metadata (per RESEARCH.md's Open Question 1 recommendation: this is in-scope environment hygiene, not
  a D-01 "fixed" disposition — no source or test file is edited). Re-run
  `pytest tests/test_version.py tests/test_cli_correctness.py::test_version_consistency -q -m ""` to
  confirm all 6 Cluster 3 tests now pass with zero code/test changes.
  </action>
  <verify>
    <automated>pytest tests/test_version.py tests/test_cli_correctness.py::test_version_consistency -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - `pip show quirk-scanner` reports the same version as `pyproject.toml` after `pip install -e .`
    - `pytest tests/test_version.py tests/test_cli_correctness.py::test_version_consistency -q -m ""` reports 6 passed, 0 failed
    - No file in `tests/` or `quirk/` was modified by this task
  </acceptance_criteria>
  <done>Cluster 3's 6 tests pass after a dist-info refresh; no code/test edit was needed.</done>
</task>

<task type="auto">
  <name>Task 2: Correct or delete Cluster 4's genuinely stale version assertions</name>
  <files>tests/test_packaging.py, tests/test_v41_gap_closure.py</files>
  <read_first>tests/test_packaging.py, tests/test_v41_gap_closure.py</read_first>
  <action>
  Read `tests/test_packaging.py::test_version_is_4_2_0` (hardcodes an expected version of `5.5.0` per
  the failure message, despite the test's own name saying `4_2_0` — confirms this test has drifted
  across multiple version bumps without being kept current) and
  `tests/test_v41_gap_closure.py::TestV41GapClosure::test_pyproject_version_field_is_4_1_0` (hardcodes
  `4.4.0`/`4.1.0`). Per D-01's narrow exception (test asserts an already-stale value), either update the
  hardcoded expected-version string to the current `pyproject.toml` value (`5.11.0`) — but note this
  test would need editing again at every future version bump, an anti-pattern per RESEARCH.md's
  cluster-4 recommendation — OR delete the test outright as obsolete-by-design (recommended: a test
  whose entire premise is pinning one historical version number forever provides no ongoing value once
  the milestone it names has shipped). Choose deletion unless the test also exercises other still-useful
  packaging assertions beyond the version string, in which case keep the test and only strip/update the
  version-specific assertion. Record the choice and rationale in the commit message.
  </action>
  <verify>
    <automated>pytest tests/test_packaging.py tests/test_v41_gap_closure.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - Neither `test_packaging.py::test_version_is_4_2_0` nor
      `test_v41_gap_closure.py::TestV41GapClosure::test_pyproject_version_field_is_4_1_0` fails
    - If deleted, `grep -c "test_version_is_4_2_0" tests/test_packaging.py` and
      `grep -c "test_pyproject_version_field_is_4_1_0" tests/test_v41_gap_closure.py` both return 0
    - If corrected instead of deleted, the hardcoded string matches `pyproject.toml`'s current version exactly
  </acceptance_criteria>
  <done>Both Cluster 4 tests no longer fail; disposition (fixed vs. deleted) recorded with rationale.</done>
</task>

<task type="auto">
  <name>Task 3: Quarantine Cluster 7 (GCP optional extra) and write ledger rows for Clusters 3/4/7</name>
  <files>tests/test_gcs_reuse.py, tests/skip_registry.py, docs/test-triage-149.md</files>
  <read_first>tests/test_gcs_reuse.py, tests/skip_registry.py, docs/test-triage-149.md</read_first>
  <action>
  Confirm `tests/test_gcs_reuse.py::test_gcs_reuse_reads_sentinel_no_api_call` and
  `test_gcs_reuse_zero_storage_buckets_list_call` fail with `ModuleNotFoundError` for `googleapiclient`/
  `google` (optional GCP extra not installed). Add `@pytest.mark.skip(reason="googleapiclient/google
  optional extra not installed; see docs/test-triage-149.md#<slug>")` above each, and add matching
  `ALLOWED_SKIPS` entries with category `"optional_extra"` (reuse the existing category exactly as the
  `broker_scanner`/`sslyze` entries do — per RESEARCH.md's Don't Hand-Roll table, this is not a new
  pattern and does not need `"pre_existing_triage_149"`). Then write ledger rows in
  `docs/test-triage-149.md`: under "## Cluster 3: Version staleness (environment)" add 6 rows,
  Disposition `environment-fix-applied`, Sub-reason `stale dist-info, resolved via pip install -e .`,
  Evidence citing the before/after version strings from Task 1; under "## Cluster 4: Version staleness
  (stale assertions)" add 2 rows with Disposition `fixed` or `deleted` per Task 2's actual choice; under
  "## Cluster 7: Optional GCP extras missing" add 2 rows, Disposition `quarantined-skip`, Sub-reason
  `optional_extra (googleapiclient/google not installed)`. Run
  `pytest tests/test_skip_registry.py -q -m ""` to confirm the gate remains green.
  </action>
  <verify>
    <automated>pytest tests/test_skip_registry.py -q -m "" &amp;&amp; pytest tests/test_gcs_reuse.py -q -m ""</automated>
  </verify>
  <acceptance_criteria>
    - 2 `@pytest.mark.skip` decorators + 2 `ALLOWED_SKIPS` entries added with category `"optional_extra"`
    - `docs/test-triage-149.md` has 6 rows under Cluster 3, 2 rows under Cluster 4, 2 rows under Cluster 7 (10 total, matching this plan's 6+2+2=10 tests)
    - `pytest tests/test_skip_registry.py -q -m ""` passes
  </acceptance_criteria>
  <done>Clusters 3, 4, and 7 fully dispositioned in the ledger; gate remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — version-metadata sync, test edits/deletions, and documentation only. No production code path
or new input surface touched.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-04 | Tampering | tests/test_packaging.py, tests/test_v41_gap_closure.py | accept | Deleting/editing a stale test assertion is the explicit D-01 exception; no production behavior changes. |
</threat_model>

<verification>
- `pip show quirk-scanner` matches `pyproject.toml` version
- `pytest tests/test_version.py tests/test_cli_correctness.py::test_version_consistency tests/test_packaging.py tests/test_v41_gap_closure.py tests/test_gcs_reuse.py -q -m ""` shows 0 unexpected failures (gcs tests skip)
- `pytest tests/test_skip_registry.py -q -m ""` passes
</verification>

<success_criteria>
Clusters 3, 4, and 7 fully dispositioned with correct environment-fix vs. fixed vs. quarantined categorization.
</success_criteria>

<output>
Create `.planning/phases/149-test-suite-triage/149-04-SUMMARY.md` when done
</output>
