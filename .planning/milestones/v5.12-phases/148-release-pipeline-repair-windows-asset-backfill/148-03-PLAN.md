---
phase: 148-release-pipeline-repair-windows-asset-backfill
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/release-notes/5.11.0.md
  - docs/release-notes/5.11.0-github-release-body.md
  - tests/test_release_notes_5_11_0.py
autonomous: true
requirements: [RELEASE-04]

must_haves:
  truths:
    - "A reader of docs/release-notes/5.11.0.md can tell that v5.11.0 is PyPI-only and that no Windows asset exists for it"
    - "The reader learns why the Windows job failed, that it was fixed in 1a6effc after the tag was cut, and that v5.12.0 is the first version with a verified Windows artifact"
    - "A ready-to-use GitHub Release body exists on disk, carrying the same disposition and linking to the full notes"
    - "The disposition wording cannot silently drift away from the required facts"
  artifacts:
    - path: "docs/release-notes/5.11.0.md"
      provides: "Full v5.11.0 release notes with the Windows-asset disposition"
      contains: "PyPI-only"
      min_lines: 40
    - path: "docs/release-notes/5.11.0-github-release-body.md"
      provides: "Body text for the bare v5.11.0 GitHub Release created in plan 148-04"
      contains: "1a6effc"
    - path: "tests/test_release_notes_5_11_0.py"
      provides: "Static assertions on the required disposition facts"
      min_lines: 40
  key_links:
    - from: "docs/release-notes/5.11.0-github-release-body.md"
      to: "docs/release-notes/5.11.0.md"
      via: "absolute GitHub blob URL link"
      pattern: "docs/release-notes/5\\.11\\.0\\.md"
    - from: "tests/test_release_notes_5_11_0.py"
      to: "docs/release-notes/5.11.0.md"
      via: "file read + substring assertions"
      pattern: "5\\.11\\.0\\.md"
---

<objective>
Close the v5.11.0 Windows-asset gap by explicit disposition (RELEASE-04, D-148-RELEASE04
Option B): write the release notes and the GitHub Release body stating that v5.11.0 is a PyPI-only
release with no Windows artifact, why, and which version is the first with a verified one.

Purpose: v5.11.0 published to PyPI but the `windows-package` job died at the signing self-test
before the Release was ever created — so there is no GitHub Release object and no Windows zip.
Backfilling a zip built from a since-changed `release.yml` (`1a6effc`) would attach an artifact
that the v5.11.0 pipeline never produced, on a project shipping Sigstore attestations. Release
history stays strictly reproducible-from-tag instead (D-01, rationale in CONTEXT.md).
Output: two markdown files plus a static test locking the required facts.

The GitHub Release object itself is created in plan 148-04, once these files are on `origin/main`
so the Release body's link resolves.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-release-pipeline-repair-windows-asset-backfill/148-CONTEXT.md
@.planning/phases/148-release-pipeline-repair-windows-asset-backfill/148-PATTERNS.md
@docs/release-notes/5.6.0.md
@docs/release-notes/5.0.0.md

<interfaces>
<!-- Facts the disposition must state. Sourced from CONTEXT.md D-01..D-04 and live scouting. -->

Milestone shipped in v5.11.0 (from ROADMAP/STATE): "Chunked Discovery" — phases 144 (chunked
discovery core), 145 (liveness pre-pass), 146 (progress/scaling/disclosure), 147 (backlog drain —
lifecycle & ledger tail). Milestone label: v5.11.

Verified live state as of 2026-08-11 (do not re-derive; re-verify only if it looks stale):
  - PyPI has `quirk-scanner` 5.11.0 (the `publish` job succeeded).
  - `gh release view v5.11.0` → "release not found" — no GitHub Release object exists.
  - Run 31510295324: `build` ✅, `publish` ✅, `windows-package` ❌ at
    "CI self-test — ephemeral cert signing round-trip".
  - Root cause: `signtool verify /pa` applies Authenticode policy, requiring a chain terminating in
    a TRUSTED root; a self-signed cert in `Cert:\CurrentUser\My` can never satisfy it.
  - Fix commit: `1a6effc` (`fix(release): trust the ephemeral root so the signing self-test can
    actually pass`) — on `main`, but landed AFTER the v5.11.0 tag was pushed.
  - `v5.12.0` (Phase 153) is the first version with a verified Windows artifact.

Existing files in `docs/release-notes/`: `4.4.0.md`, `4.5.0.md`, `4.6.0.md`, `5.0.0.md`, `5.6.0.md`.
`5.7.0.md`–`5.10.0.md` do NOT exist — pre-existing drift, explicitly out of scope (D-04). Do not
link to a non-existent file; use `5.6.0.md` as the "previous release" link.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/release-notes/5.11.0.md with the Windows-asset disposition</name>
  <files>docs/release-notes/5.11.0.md</files>
  <read_first>
    - docs/release-notes/5.6.0.md (full file — header shape, `## What's New`, `## Known Issues` → `### Windows — Unsigned Binary` nesting, `## Upgrade Guidance`, `## See Also` footer)
    - docs/release-notes/5.0.0.md (header/footer boilerplate variant)
    - .planning/phases/148-release-pipeline-repair-windows-asset-backfill/148-CONTEXT.md (D-03, D-04)
    - .planning/ROADMAP.md (v5.11 phase list, for accurate What's New content)
  </read_first>
  <action>
    Create `docs/release-notes/5.11.0.md` following the `5.6.0.md` structure exactly.

    Header: `# QU.I.R.K. 5.11.0 — <one-line milestone theme>`, `**Released:** <the actual v5.11.0
    tag-push date — read it from `git log -1 --format=%ad --date=short v5.11.0`>`,
    `**Milestone:** v5.11`.

    `## What's New` — summarize the v5.11 milestone content from ROADMAP/STATE: chunked discovery
    core (per-batch nmap with failure isolation and the `_MAX_HOSTS_PER_CIDR` cap relaxation),
    liveness pre-pass with non-root privilege fallback, per-batch progress/timing scaling and
    undetermined-host disclosure, and the lifecycle/ledger backlog drain. Keep it factual and
    short — this is a retrospective note, not a marketing page.

    `## Known Issues` → `### Windows — No Asset Produced (PyPI-only release)` — this is the
    load-bearing section (D-03). It must state, in plain prose:
      - This release is **PyPI-only**. `pip install quirk-scanner==5.11.0` works; there is no
        `quirk-windows-5.11.0.zip` and no Windows asset of any kind for this version.
      - Why: the `windows-package` job failed at the "CI self-test — ephemeral cert signing
        round-trip" step, which runs BEFORE the Release-creation step — so no GitHub Release
        assets were ever attached.
      - Root cause: `signtool verify /pa` requires an Authenticode chain terminating in a trusted
        root, which a self-signed cert in `CurrentUser\My` can never satisfy. The step had never
        executed before this tag (v5.10.0 was never pushed; `v5.9` never matched the `v*.*.*`
        glob), so it failed by construction on its first real run.
      - Fixed in commit `1a6effc`, already on `main` — but landed after the `v5.11.0` tag was
        pushed, which is why the tag itself cannot produce the artifact.
      - **`v5.12.0` is the first version with a verified Windows artifact.**
      - Operator guidance: install from PyPI, build the Windows bundle from source, or upgrade to
        `v5.12.0` or later.
      - A provenance sentence: no zip was backfilled onto this tag deliberately — attaching a
        binary built from a since-changed workflow would misrepresent what this tag's pipeline
        produced, on a project that ships Sigstore attestations and PyPI Trusted Publishers.

    `## Upgrade Guidance` — `pip install --upgrade quirk-scanner`, a "no breaking changes"
    statement, and a note that there is no Windows distributable change in this release because
    none shipped.

    `## See Also` — link `[CHANGELOG.md](../../CHANGELOG.md)`, `[5.6.0 Release Notes](5.6.0.md)`
    as the previous existing release-notes file (do NOT link 5.7.0–5.10.0, which do not exist),
    `docs/release-process.md`, and `docs/UAT-SERIES.md`. Close with the italic footer line matching
    the analogs' shape.

    Do not create or modify `5.7.0.md`–`5.10.0.md` — explicitly out of scope (D-04, Deferred).
  </action>
  <verify>
    <automated>python -c "import pathlib; t=pathlib.Path('docs/release-notes/5.11.0.md').read_text(); [t.index(s) for s in ['PyPI-only','1a6effc','v5.12.0','windows-package','Known Issues']]; assert '5.7.0.md' not in t and '5.10.0.md' not in t; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/release-notes/5.11.0.md` exists and begins with `# QU.I.R.K. 5.11.0`.
    - Contains the literal strings `**Milestone:** v5.11`, `PyPI-only`, `1a6effc`, `v5.12.0`, `windows-package`, `CI self-test`, and `signtool verify /pa`.
    - Contains headings `## What's New`, `## Known Issues`, `## Upgrade Guidance`, `## See Also`.
    - Contains no link to `5.7.0.md`, `5.8.0.md`, `5.9.0.md`, or `5.10.0.md` (files that do not exist).
    - The `**Released:**` date matches `git log -1 --format=%ad --date=short v5.11.0`.
    - States explicitly that no Windows asset exists for this version AND that v5.12.0 is the first version with a verified Windows artifact.
  </acceptance_criteria>
  <done>An operator reading the release notes for 5.11.0 knows there is no Windows artifact, why, and what to do instead — without guessing.</done>
</task>

<task type="auto">
  <name>Task 2: Write the GitHub Release body file and lock the disposition facts with a test</name>
  <files>docs/release-notes/5.11.0-github-release-body.md, tests/test_release_notes_5_11_0.py</files>
  <read_first>
    - docs/release-notes/5.11.0.md (post-Task-1 — the source of truth this body summarizes)
    - .planning/phases/148-release-pipeline-repair-windows-asset-backfill/148-CONTEXT.md (D-02, D-03, D-04)
    - tests/test_windows_ci_hardgate.py (static-assertion + remediation-message style)
  </read_first>
  <action>
    Create `docs/release-notes/5.11.0-github-release-body.md` — the exact text plan 148-04 will pass
    to `gh release create v5.11.0 --notes-file`. Keep it short (roughly 15–30 lines) and
    self-contained, because it is what an operator sees on the Releases page:
      - Title line: `## QU.I.R.K. 5.11.0 — PyPI-only release (no Windows asset)`
      - A one-paragraph disposition carrying the same four facts as the notes file: PyPI has
        5.11.0; no Windows asset exists for this version; the `windows-package` job failed at the
        signing self-test before the Release-creation step; fixed in `1a6effc` after the tag was
        cut; `v5.12.0` is the first version with a verified Windows artifact.
      - An explicit "This release intentionally has zero attached assets" line, so an operator does
        not read the empty asset list as a page that failed to load.
      - Install line: `pip install quirk-scanner==5.11.0`.
      - A link to the full notes at the canonical blob URL form
        `https://github.com/<owner>/<repo>/blob/main/docs/release-notes/5.11.0.md` — resolve
        `<owner>/<repo>` with `gh repo view --json nameWithOwner -q .nameWithOwner` rather than
        guessing (D-04 requires the Release body to link the notes file).

    Create `tests/test_release_notes_5_11_0.py` in the style of `tests/test_windows_ci_hardgate.py`
    (`REPO_ROOT` constant, one assertion per fact, remediation message naming RELEASE-04 /
    D-148-RELEASE04):
      - both files exist;
      - each contains `PyPI-only` (or `PyPI-only` in the body's title form), `1a6effc`, `v5.12.0`;
      - the notes file contains `windows-package` and `signtool verify /pa`;
      - the body file contains `pip install quirk-scanner==5.11.0` and a URL ending in
        `/docs/release-notes/5.11.0.md`;
      - the body file states zero attached assets (assert on the literal phrase used);
      - neither file links to `5.7.0.md`, `5.8.0.md`, `5.9.0.md`, or `5.10.0.md`;
      - a guard that the notes file does NOT claim a Windows zip exists: assert the string
        `quirk-windows-5.11.0.zip` never appears in a sentence asserting availability — implement
        this as: if `quirk-windows-5.11.0.zip` appears at all, the same line must also contain a
        negation token (`no`, `not`, `never`) — with a message explaining the intent.
  </action>
  <verify>
    <automated>python -m pytest tests/test_release_notes_5_11_0.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_release_notes_5_11_0.py -q` exits 0 with at least 8 tests collected.
    - `docs/release-notes/5.11.0-github-release-body.md` exists, is under 60 lines, and contains `1a6effc`, `v5.12.0`, `pip install quirk-scanner==5.11.0`.
    - The body's notes link host/path matches `gh repo view --json nameWithOwner -q .nameWithOwner` output — verify by running that command and grepping the body for the returned `owner/repo` string.
    - Deleting the `1a6effc` mention from either file turns `pytest tests/test_release_notes_5_11_0.py` red.
  </acceptance_criteria>
  <done>The Release body is ready to publish verbatim in plan 148-04, and the disposition facts are test-enforced against future drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Release notes → operator trust decision | Published provenance claims influence whether an operator trusts/installs an artifact |
| Repo file → public GitHub Release body | Text in this repo is published verbatim to a public page in plan 148-04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-12 | Spoofing | Provenance misrepresentation | mitigate | This is the core reason D-148-RELEASE04 chose disposition over backfill: no binary built from the post-`1a6effc` workflow is attached to the `v5.11.0` tag, so no artifact ever claims a provenance its tag's pipeline could not produce. The notes state the reasoning explicitly. |
| T-148-13 | Repudiation | Silent gap in release history | mitigate | The gap becomes a written, dated, linked record in `docs/release-notes/5.11.0.md` and on the Releases page (plan 148-04), rather than an absence an operator must infer. |
| T-148-14 | Tampering | Disposition text drifting to a false claim | mitigate | `tests/test_release_notes_5_11_0.py` asserts the required facts and includes a negation check preventing the notes from ever asserting a `quirk-windows-5.11.0.zip` exists. |
| T-148-15 | Information Disclosure | Publishing failure detail publicly | accept | The disclosed detail (a self-signed-cert Authenticode verify limitation in CI) is a build-hygiene fact already visible in public workflow logs and the `release.yml` source comment added by `1a6effc`. No secret, token, or internal host is disclosed. |
| T-148-SC | Tampering | Dependency supply chain | accept | This plan adds no dependency, no `uses:` step, and no `npm`/`pip`/`cargo` install — the package-legitimacy gate does not apply. |
</threat_model>

<verification>
- `python -m pytest tests/test_release_notes_5_11_0.py -q` passes.
- Both markdown files render as valid markdown (headings resolve, no unbalanced code fences).
- No `docs/release-notes/5.7.0.md`–`5.10.0.md` files are created (`ls docs/release-notes/` unchanged except the two new 5.11.0 files).
</verification>

<success_criteria>
- The v5.11.0 Windows-asset gap has an explicit, defensible written disposition in `docs/release-notes/`.
- A publishable Release body exists on disk, carrying the same facts and linking the full notes.
- The disposition is protected from silent drift by the local test suite.
</success_criteria>

<output>
Create `.planning/phases/148-release-pipeline-repair-windows-asset-backfill/148-03-SUMMARY.md` when done
</output>
