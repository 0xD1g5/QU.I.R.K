---
phase: 52-compliance-uplift-health-check
plan: 05
type: execute
wave: 1
depends_on: [01]
files_modified:
  - quantum-chaos-enterprise-lab/lab.sh
  - quirk/scanner/saml_scanner.py
  - run_scan.py
autonomous: true
requirements: [DEBT-02, DEBT-03, DEBT-04]
must_haves:
  truths:
    - "PROFILE_ARGS=\"--profile X\" ./lab.sh wins over .env defaults (CLI precedence restored)"
    - "run-stats JSON contains ports_scanned and hosts_scanned reachable either at top level or under 'counts'"
    - "saml_scanner.py uses raw lxml.etree with resolve_entities=False, no_network=True (no defusedxml.lxml)"
    - "All 27 SAML tests still GREEN after migration; no DeprecationWarning surfaces"
  artifacts:
    - path: quantum-chaos-enterprise-lab/lab.sh
      provides: "PROFILE_ARGS snapshot before .env source"
      contains: "_PROFILE_ARGS_OVERRIDE"
    - path: quirk/scanner/saml_scanner.py
      provides: "raw lxml.etree migration with security flags"
      contains: "resolve_entities=False"
    - path: run_scan.py
      provides: "ports_scanned + hosts_scanned reachable in run_stats"
      contains: "ports_scanned"
  key_links:
    - from: quirk/scanner/saml_scanner.py
      to: lxml.etree
      via: "ET.fromstring(xml, parser=ET.XMLParser(resolve_entities=False, no_network=True))"
      pattern: "no_network=True"
    - from: run_scan.py
      to: run-stats-*.json
      via: "run_stats[\"counts\"][\"ports_scanned\"] -> writer -> JSON file"
      pattern: "ports_scanned"
---

<objective>
Close three v4.6 backlog tech debt items in a single coordinated plan:

- **DEBT-02:** `lab.sh` PROFILE_ARGS CLI override gets overwritten by `.env`. Fix: snapshot before `source .env`.
- **DEBT-03:** Confirm `run-stats-*.json` contains `ports_scanned` and `hosts_scanned`. Code already exists at `run_scan.py:534–535` — verify and, if needed, also surface the fields at the top level of `run_stats`.
- **DEBT-04:** `saml_scanner.py` uses deprecated `defusedxml.lxml`. Migrate to raw `lxml.etree.XMLParser(resolve_entities=False, no_network=True)` while preserving the 3-tier import fallback chain.

Purpose: Three independent files; same wave because they're small, surgical fixes
with no cross-dependencies. Closes the v4.6 carry-forward backlog.

Output: One bash file (~3 lines changed), one Python scanner (import block only),
one `run_scan.py` block (verify + optionally add top-level fields), all 27 SAML
tests still GREEN, and `tests/test_writer.py` ports_scanned stub turns GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-compliance-uplift-health-check/52-CONTEXT.md
@.planning/phases/52-compliance-uplift-health-check/52-RESEARCH.md
@.planning/phases/52-compliance-uplift-health-check/52-PATTERNS.md
@quantum-chaos-enterprise-lab/lab.sh
@quirk/scanner/saml_scanner.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DEBT-02 — Snapshot PROFILE_ARGS in lab.sh before sourcing .env</name>
  <files>quantum-chaos-enterprise-lab/lab.sh</files>
  <read_first>
    - quantum-chaos-enterprise-lab/lab.sh (lines 1–20 — the PROFILE_ARGS lifecycle and .env source block)
    - .planning/phases/52-compliance-uplift-health-check/52-PATTERNS.md (section "lab.sh — DEBT-02 PROFILE_ARGS snapshot")
    - .planning/phases/52-compliance-uplift-health-check/52-CONTEXT.md (D-15)
  </read_first>
  <behavior>
    - `PROFILE_ARGS="--profile tls" ./lab.sh up --dry-run` produces compose invocation containing "--profile tls"
    - When `PROFILE_ARGS` is unset, the `.env` value is still honored (no regression)
  </behavior>
  <action>
Read the existing top of `lab.sh` (lines 1–14). The current sequence is roughly:

```bash
#!/usr/bin/env bash
set -euo pipefail

if [[ -f ".env" ]]; then
  set -a
  # shellcheck disable=SC1091
  source ".env"
  set +a
fi

# ---- Config ----
PROJECT_NAME="${PROJECT_NAME:-chaoslab}"
COMPOSE_FILE="${COMPOSE_FILE:-docker-compose.yml}"
PROFILE_ARGS="${PROFILE_ARGS:-}"
```

**Apply two surgical edits:**

1. INSERT a single new line BEFORE the `if [[ -f ".env" ]]; then` line that
   snapshots the inbound value:

```bash
_PROFILE_ARGS_OVERRIDE="${PROFILE_ARGS:-}"   # snapshot CLI value BEFORE .env can overwrite it (Phase 52 DEBT-02)
```

2. CHANGE the existing `PROFILE_ARGS="${PROFILE_ARGS:-}"` line to:

```bash
PROFILE_ARGS="${_PROFILE_ARGS_OVERRIDE:-${PROFILE_ARGS:-}}"   # CLI wins over .env (Phase 52 DEBT-02)
```

The fallback chain `${_PROFILE_ARGS_OVERRIDE:-${PROFILE_ARGS:-}}` means: if the
operator passed `PROFILE_ARGS=...` on the CLI, use it; otherwise fall back to
whatever `.env` set; otherwise empty string.

Do NOT touch any other line in `lab.sh` (the `all` command's ALL_PROFILES list,
service handlers, etc. — out of scope for DEBT-02). The CLAUDE.md "Chaos Lab
Maintenance" rule is satisfied because no compose profiles change.

**Smoke test:**

```
cd quantum-chaos-enterprise-lab && PROFILE_ARGS="--profile tls" bash -c 'source ./lab.sh; echo "PROFILE_ARGS=$PROFILE_ARGS"' 2>&1 | tail -3
```

Should print `PROFILE_ARGS=--profile tls`. (If the script tries to actually run
docker, use `--dry-run` or wrap in `bash -n` for syntax-only check.)

A pure syntax check that does not require docker:

```
bash -n quantum-chaos-enterprise-lab/lab.sh && echo "syntax OK"
```
  </action>
  <verify>
    <automated>bash -n quantum-chaos-enterprise-lab/lab.sh && grep -c "_PROFILE_ARGS_OVERRIDE" quantum-chaos-enterprise-lab/lab.sh && PROFILE_ARGS="--profile tls" bash -c 'set -e; head -20 quantum-chaos-enterprise-lab/lab.sh > /tmp/labhead.sh; source /tmp/labhead.sh 2>/dev/null || true; [ "$PROFILE_ARGS" = "--profile tls" ] && echo "CLI wins" || (echo "FAIL: PROFILE_ARGS=$PROFILE_ARGS" && exit 1)'</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n quantum-chaos-enterprise-lab/lab.sh` exits 0 (syntax valid)
    - `grep -c "_PROFILE_ARGS_OVERRIDE=" quantum-chaos-enterprise-lab/lab.sh` returns at least 1
    - `grep -c "_PROFILE_ARGS_OVERRIDE:-" quantum-chaos-enterprise-lab/lab.sh` returns at least 1 (the fallback expansion)
    - The snapshot line `_PROFILE_ARGS_OVERRIDE=` appears BEFORE the `source ".env"` line in source order: `awk '/source \\".env\\"/{src=NR} /_PROFILE_ARGS_OVERRIDE=/{snap=NR} END{exit !(snap < src)}' quantum-chaos-enterprise-lab/lab.sh && echo "ORDER OK"` exits 0
    - Manual smoke (executor records output in SUMMARY): `PROFILE_ARGS="--profile tls" ./quantum-chaos-enterprise-lab/lab.sh status 2>&1 | head -5` shows the CLI profile wins (or, if status command is unavailable in current state, sourced-into-subshell test confirms `PROFILE_ARGS=--profile tls`)
  </acceptance_criteria>
  <done>Two-line lab.sh edit applied; CLI PROFILE_ARGS overrides .env; syntax valid; no other lab.sh content changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DEBT-04 — Migrate saml_scanner.py from defusedxml.lxml to raw lxml.etree with security flags</name>
  <files>quirk/scanner/saml_scanner.py</files>
  <read_first>
    - quirk/scanner/saml_scanner.py (lines 1–30 — the 3-tier import chain)
    - tests/test_saml_scanner.py (full file — the 27 tests that must remain GREEN)
    - .planning/phases/52-compliance-uplift-health-check/52-PATTERNS.md (section "saml_scanner.py — DEBT-04 lxml migration")
    - .planning/phases/52-compliance-uplift-health-check/52-CONTEXT.md (D-17)
    - .planning/phases/52-compliance-uplift-health-check/52-RESEARCH.md (Pitfall 3: SAML fallback chain ordering)
  </read_first>
  <behavior>
    - When lxml is installed, _safe_ET_fromstring uses lxml.etree.fromstring with XMLParser(resolve_entities=False, no_network=True)
    - When lxml is absent but defusedxml is present, falls back to defusedxml.ElementTree (UNCHANGED behavior — preserve this branch verbatim)
    - When neither is present, raises RuntimeError (UNCHANGED)
    - `python -W error::DeprecationWarning -m pytest tests/test_saml_scanner.py -q` runs without triggering DeprecationWarning
    - All 27 (per VALIDATION.md) SAML tests GREEN
  </behavior>
  <action>
The file currently has a 3-tier try/except import chain. The first tier imports
both `lxml.etree as ET` AND `defusedxml.lxml as _defused_lxml_ET`, and the
inner `_safe_ET_fromstring` calls `_defused_lxml_ET.fromstring(xml_bytes)`.

Replace ONLY the FIRST `try` block (lines roughly 4–10). The second `try`
block (the `defusedxml.ElementTree` fallback) and the final outer `except`
that raises RuntimeError MUST be preserved verbatim.

**Before (current first try block, ~lines 4–10):**

```python
try:
    import lxml.etree as ET
    import defusedxml.lxml as _defused_lxml_ET
    def _safe_ET_fromstring(xml_bytes):  # noqa: E306
        return _defused_lxml_ET.fromstring(xml_bytes)
    LXML_AVAILABLE = True
except ImportError:
```

**After:**

```python
try:
    import lxml.etree as ET
    def _safe_ET_fromstring(xml_bytes):  # noqa: E306
        # Phase 52 DEBT-04: migrated off deprecated defusedxml.lxml.
        # Security flags: resolve_entities=False blocks XXE; no_network=True blocks SSRF.
        return ET.fromstring(
            xml_bytes,
            parser=ET.XMLParser(resolve_entities=False, no_network=True),
        )
    LXML_AVAILABLE = True
except ImportError:
```

**The two changes inside the first try block:**
1. Remove the line `import defusedxml.lxml as _defused_lxml_ET`
2. Replace the `_safe_ET_fromstring` body to call `ET.fromstring(xml_bytes, parser=ET.XMLParser(resolve_entities=False, no_network=True))`

Everything from `except ImportError:` onward (the second try, its inner
`_defused_stdlib_ET` fallback, the final ImportError raising RuntimeError) MUST
remain BYTE-IDENTICAL to its current state.

**Verify:**

```
.venv/bin/python3 -m pytest tests/test_saml_scanner.py -q
```

All tests must remain GREEN. If 25/27 pass and 2 fail, the most likely cause is
that you accidentally also removed the second try block or changed the fallback
behavior — re-read the current file and apply ONLY the two changes inside the
first try.

```
.venv/bin/python3 -W error::DeprecationWarning -m pytest tests/test_saml_scanner.py -q
```

Must also pass — if it fails with `DeprecationWarning: defusedxml.lxml is no
longer supported`, the migration is incomplete.

```
grep -n "defusedxml.lxml" quirk/scanner/saml_scanner.py
```

Must return zero matches.
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_saml_scanner.py -q && .venv/bin/python3 -W error::DeprecationWarning -m pytest tests/test_saml_scanner.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "defusedxml.lxml" quirk/scanner/saml_scanner.py` returns 0
    - `grep -c "resolve_entities=False" quirk/scanner/saml_scanner.py` returns 1
    - `grep -c "no_network=True" quirk/scanner/saml_scanner.py` returns 1
    - `grep -c "import defusedxml.ElementTree" quirk/scanner/saml_scanner.py` returns 1 (fallback chain preserved)
    - `grep -c "RuntimeError" quirk/scanner/saml_scanner.py` returns at least 1 (final ImportError-raises-RuntimeError preserved)
    - `.venv/bin/python3 -m pytest tests/test_saml_scanner.py -q` exits 0 (all 27 tests pass)
    - `.venv/bin/python3 -W error::DeprecationWarning -m pytest tests/test_saml_scanner.py -q` exits 0 (no DeprecationWarning emitted)
    - `.venv/bin/python3 -c "from quirk.scanner.saml_scanner import _safe_ET_fromstring, LXML_AVAILABLE; assert LXML_AVAILABLE; print('OK')"` exits 0 (module imports cleanly)
  </acceptance_criteria>
  <done>defusedxml.lxml import removed; raw lxml.etree with security flags in place; 3-tier fallback chain preserved; all 27 SAML tests GREEN; no DeprecationWarning.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DEBT-03 — Verify ports_scanned/hosts_scanned in run-stats; promote to top-level if needed</name>
  <files>run_scan.py</files>
  <read_first>
    - run_scan.py (lines 520–545 — locate `run_stats["counts"] = {...}` block; identify the exact line of `write_reports(...)` call)
    - quirk/reports/writer.py (lines 180–210 — confirm what fields end up in run-stats-*.json)
    - .planning/phases/52-compliance-uplift-health-check/52-CONTEXT.md (D-16)
    - .planning/phases/52-compliance-uplift-health-check/52-RESEARCH.md (sections "DEBT-03 Clarification Finding" and "Open Question 1")
    - tests/test_writer.py (the Plan 01 stub — accepts EITHER top-level OR nested-under-counts placement)
  </read_first>
  <behavior>
    - `run-stats-*.json` produced by a scan contains `ports_scanned` and `hosts_scanned` (sorted lists), reachable either at the top level OR nested under "counts"
    - The Plan 01 writer test passes
    - No fields are removed; no other run_stats keys change shape
  </behavior>
  <action>
**Step 1 — Verify current state.** Read `run_scan.py` around lines 529–536 and
confirm the existing `run_stats["counts"]` dict already contains `hosts_scanned`
and `ports_scanned`. If both are already there, the code may already meet the
requirement.

**Step 2 — Run the Plan 01 writer test against the current code:**

```
.venv/bin/python3 -m pytest tests/test_writer.py::test_run_stats_ports_and_hosts_scanned -x
```

The Plan 01 stub accepts EITHER top-level placement OR nesting under `counts`.

- **If GREEN:** the requirement is already met by existing code. Skip to Step 4.
- **If RED:** the writer is dropping fields between `run_stats["counts"]` and the
  output JSON. Diagnose by reading `quirk/reports/writer.py` lines 180–210 and
  identifying whether `run_stats` is being shallow-copied / filtered / reshaped.
  Proceed to Step 3.

**Step 3 — Promote to top-level (only if Step 2 was RED).** In `run_scan.py`,
locate the line IMMEDIATELY AFTER the closing brace of `run_stats["counts"] = {...}`
(currently around line 537) and BEFORE the `write_reports(...)` call. INSERT:

```python
        # Phase 52 DEBT-03: surface ports_scanned + hosts_scanned at run_stats top level
        # so they are reachable in run-stats-*.json regardless of writer normalization.
        run_stats["ports_scanned"] = run_stats["counts"]["ports_scanned"]
        run_stats["hosts_scanned"] = run_stats["counts"]["hosts_scanned"]
```

(Adjust indentation to match the surrounding block — read the existing
`run_stats["counts"] = {...}` lines for the indent level.)

Do NOT modify the existing `run_stats["counts"][...]` assignments. Do NOT touch
`quirk/reports/writer.py` unless Step 2 reveals the writer is actively stripping
fields — and even then, prefer adding to `run_stats` over modifying writer
filtering logic.

**Step 4 — Re-run the writer test and a quick integration sanity check:**

```
.venv/bin/python3 -m pytest tests/test_writer.py::test_run_stats_ports_and_hosts_scanned -x
```

Then a real one-host one-port scan against the chaos lab to confirm the JSON
output is right (executor records the output path in SUMMARY):

```
.venv/bin/python3 run_scan.py --target localhost --ports 443 --output-dir /tmp/qsmoke 2>&1 | tail -5
ls /tmp/qsmoke/run-stats-*.json | head -1 | xargs -I {} .venv/bin/python3 -c "import json,sys; d=json.load(open('{}')); print('top:', 'ports_scanned' in d, 'hosts_scanned' in d); print('counts:', 'ports_scanned' in d.get('counts',{}), 'hosts_scanned' in d.get('counts',{}))"
```

(If the executor cannot run a live scan due to environment constraints, the
unit test in Step 4 is sufficient.)
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_writer.py::test_run_stats_ports_and_hosts_scanned -x</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python3 -m pytest tests/test_writer.py::test_run_stats_ports_and_hosts_scanned -x` exits 0
    - `grep -c "ports_scanned" run_scan.py` returns at least 1 (existing) and at most 4 (existing + optional 2 promotion lines + optional comment)
    - `grep -c "hosts_scanned" run_scan.py` returns at least 1 (existing) and at most 4
    - If Step 3 was applied: `grep -c 'run_stats\\["ports_scanned"\\] = run_stats\\["counts"\\]\\["ports_scanned"\\]' run_scan.py` returns 1
    - Pre-existing run_scan tests still collect: `.venv/bin/python3 -m pytest tests/ --collect-only -q 2>&1 | tail -2 | grep -v error` returns non-empty
    - `.venv/bin/python3 -m pytest tests/ -q -x` exits 0 (full suite GREEN at end of plan — combined with Plans 02/03/04 having landed earlier in Wave 1 this is the final phase-wide green-bar)
  </acceptance_criteria>
  <done>Plan 01 writer test GREEN; ports_scanned/hosts_scanned present in run-stats output; full pytest suite GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → lab.sh | Operator-supplied PROFILE_ARGS environment variable consumed; bash variable expansion |
| SAML metadata XML → parser | XML payload from network/disk parsed by lxml; classic XXE/SSRF vector |
| scanner pipeline → run-stats JSON | Scan target list serialized to disk; consumed by report renderers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-05-01 | Tampering | XXE injection via SAML metadata XML | mitigate | DEBT-04: `lxml.etree.XMLParser(resolve_entities=False, no_network=True)` blocks external entity resolution; `resolve_entities=False` is grep-verified in acceptance |
| T-52-05-02 | Elevation of Privilege | SSRF via XML external entities reaching internal services | mitigate | DEBT-04: `no_network=True` parser flag prevents outbound network calls during XML parse; grep-verified |
| T-52-05-03 | Tampering | Operator passes `PROFILE_ARGS` but `.env` overwrite causes wrong profile to start | mitigate | DEBT-02: snapshot before `source .env`; ordering enforced by acceptance criterion comparing line numbers |
| T-52-05-04 | Information Disclosure | Scan target list (hosts/ports) leaks via run-stats JSON | accept | run-stats JSON is local artifact; operator already has access to scan inputs |
| T-52-05-05 | Tampering | DEBT-03 promotion line accidentally overwrites operator-supplied run_stats fields | mitigate | Promotion uses `run_stats["ports_scanned"] =` only after the `run_stats["counts"]` assignment; not overriding any pre-existing top-level field of the same name |
| T-52-05-06 | Spoofing | Removed defusedxml.lxml fallback creates a quietly-different parsing semantic | mitigate | All 27 SAML tests must remain GREEN; `-W error::DeprecationWarning` run confirms no warning regressions |
</threat_model>

<verification>
```
.venv/bin/python3 -m pytest tests/test_saml_scanner.py tests/test_writer.py -q
.venv/bin/python3 -W error::DeprecationWarning -m pytest tests/test_saml_scanner.py -q
bash -n quantum-chaos-enterprise-lab/lab.sh
```

End-of-plan full suite green-bar:

```
.venv/bin/python3 -m pytest tests/ -q
```
</verification>

<success_criteria>
- DEBT-02: lab.sh CLI PROFILE_ARGS overrides .env; bash -n syntax check clean
- DEBT-03: run-stats-*.json contains ports_scanned + hosts_scanned (Plan 01 writer test GREEN)
- DEBT-04: zero defusedxml.lxml references in saml_scanner.py; raw lxml with security flags; 27 SAML tests GREEN; no DeprecationWarning
- Full pytest suite GREEN
</success_criteria>

<output>
Create `.planning/phases/52-compliance-uplift-health-check/52-05-SUMMARY.md`
</output>
