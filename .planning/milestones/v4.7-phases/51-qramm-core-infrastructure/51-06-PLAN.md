---
phase: 51-qramm-core-infrastructure
plan: 06
type: execute
wave: 4
depends_on: [51-04]
files_modified:
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-51-QRAMM-Core-Infrastructure.md
  - docs/UAT-SERIES.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md
autonomous: true
requirements: [QRAMM-01, QRAMM-02, QRAMM-03, QRAMM-04, DEBT-01]
tags: [docs, obsidian, uat, phase-completion]

must_haves:
  truths:
    - "Obsidian phase note exists at vault Phases/ folder with status: complete frontmatter"
    - "Phase note covers all 5 success criteria, all 5 plans (51-01..51-05), and all 5 requirement IDs"
    - "docs/UAT-SERIES.md documents the 5 new /api/qramm/* endpoints, the 3 new SQLite tables, and the DEBT-01 zero-utcnow gate"
    - "Vault UAT-Series.md is byte-equivalent to docs/UAT-SERIES.md (with added vault frontmatter)"
    - "docs/UAT-SERIES.md committed to git via gsd-tools.cjs commit"
  artifacts:
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-51-QRAMM-Core-Infrastructure.md"
      provides: "Obsidian phase note for Phase 51 (CLAUDE.md mandatory step 1)"
    - path: "docs/UAT-SERIES.md"
      provides: "UAT coverage for Phase 51 endpoints, tables, and DEBT-01 gate"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
      provides: "Synced vault copy of UAT-SERIES.md (CLAUDE.md mandatory step 3)"
  key_links:
    - from: "Phase-51-QRAMM-Core-Infrastructure.md"
      to: "Roadmap.md"
      via: "[[Roadmap]] wikilink"
      pattern: "\\[\\[Roadmap\\]\\]"
    - from: "vault UAT-Series.md"
      to: "docs/UAT-SERIES.md"
      via: "filesystem copy with vault frontmatter prepended"
      pattern: "source: docs/UAT-SERIES.md"
---

<objective>
Execute the CLAUDE.md "Mandatory Phase Completion Steps" for Phase 51:

1. Create the Obsidian phase note (filesystem write — file is too large for
   shell `content=` expansion per CLAUDE.md guidance).
2. Update `docs/UAT-SERIES.md` to reflect Phase 51's surface: 5 new
   `/api/qramm/*` endpoints, 3 new SQLite tables, and the DEBT-01 zero-utcnow
   gate.
3. Sync `docs/UAT-SERIES.md` to the Obsidian vault as `UAT-Series.md`.
4. Commit `docs/UAT-SERIES.md` to git.

This plan runs in Wave 4 — after Wave 3's test plan (51-04) confirms the
phase surface is green. Without these docs steps, the phase is not complete
per project rules.

Output: One Obsidian phase note, one updated `docs/UAT-SERIES.md`, one
synced vault copy, one git commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/51-qramm-core-infrastructure/51-CONTEXT.md
@CLAUDE.md
@docs/UAT-SERIES.md
@.planning/phases/51-qramm-core-infrastructure/51-01-PLAN.md
@.planning/phases/51-qramm-core-infrastructure/51-02-PLAN.md
@.planning/phases/51-qramm-core-infrastructure/51-03-PLAN.md
@.planning/phases/51-qramm-core-infrastructure/51-04-PLAN.md
@.planning/phases/51-qramm-core-infrastructure/51-05-PLAN.md

<interfaces>
<!-- CLAUDE.md vault sync command (verbatim — must use this exact pattern) -->
```bash
printf "---\nproject: QU.I.R.K.\ntype: reference\nstatus: active\nsource: docs/UAT-SERIES.md\nupdated: $(date +%Y-%m-%d)\n---\n\n" > /tmp/uat_vault.md
cat docs/UAT-SERIES.md >> /tmp/uat_vault.md
cp /tmp/uat_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
```

<!-- CLAUDE.md commit command (verbatim) -->
```bash
node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-51): update UAT-SERIES.md" --files docs/UAT-SERIES.md
```

<!-- Obsidian phase note frontmatter (per CLAUDE.md "Frontmatter Standards") -->
```yaml
---
project: QU.I.R.K.
type: phase
status: complete
source: .planning/phases/51-qramm-core-infrastructure/
updated: <YYYY-MM-DD>
---
```

<!-- Phase 51 surface to document in UAT-SERIES.md -->
Endpoints (all under /api/qramm/):
- POST   /api/qramm/sessions                       -> 201 + session_id
- GET    /api/qramm/sessions/{id}                  -> 200 (session + score) | 404
- POST   /api/qramm/sessions/{id}/answers          -> 200 (saved_count, total_answered) | 404 | 422
- POST   /api/qramm/sessions/{id}/score            -> 200 (overall, dimensions, maturity, profile_multiplier) | 404
- DELETE /api/qramm/sessions/{id}                  -> 204 | 404

New SQLite tables (created idempotently by init_db()):
- qramm_sessions
- qramm_answers
- qramm_profiles

DEBT-01 gate:
- Zero `DeprecationWarning: datetime.utcnow()` messages in `python -m pytest tests/ -W error::DeprecationWarning` output
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Obsidian phase note to vault filesystem</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-51-QRAMM-Core-Infrastructure.md</files>
  <read_first>
    - CLAUDE.md (§"Mandatory Phase Completion Steps" → step 1; §"Frontmatter Standards"; §"Vault Folder Structure")
    - .planning/ROADMAP.md (find "Phase 51" section — extract Goal text and the 5 Success Criteria verbatim)
    - .planning/phases/51-qramm-core-infrastructure/51-CONTEXT.md (Domain section + Decisions D-01..D-13 for "What Was Built" depth)
    - .planning/phases/51-qramm-core-infrastructure/51-01-PLAN.md through 51-05-PLAN.md (each plan's <objective> drives one "What Was Built" subsection)
    - One existing vault phase note for template style (e.g. /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-50-Enterprise-Documentation.md if it exists, else any Phase-NN-*.md in that folder — use `ls /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/` to discover)
  </read_first>
  <action>
Use the **Write** tool (NOT bash heredoc, NOT `obsidian CLI content=`) to create the file at:

```
/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-51-QRAMM-Core-Infrastructure.md
```

Per CLAUDE.md: "Write the phase note directly to the vault filesystem (do NOT use `obsidian CLI content=` — files are too large for shell expansion)."

The file content MUST be structured as follows (replace `<YYYY-MM-DD>` with `date +%Y-%m-%d` output):

```markdown
---
project: QU.I.R.K.
type: phase
status: complete
source: .planning/phases/51-qramm-core-infrastructure/
updated: <YYYY-MM-DD>
---

# Phase 51 — QRAMM Core Infrastructure

## Goal

<verbatim Goal text from .planning/ROADMAP.md "Phase 51" section>

## Requirements Covered

- **QRAMM-01** — Three new SQLite tables (`qramm_sessions`, `qramm_answers`, `qramm_profiles`) created idempotently via `init_db()`
- **QRAMM-02** — FastAPI CRUD router at `/api/qramm/` with 5 endpoint families (create / read / save answers / score / delete)
- **QRAMM-03** — `QRAMM_QUESTIONS` catalog of exactly 120 questions sourced verbatim from CSNP QRAMM toolkit (qramm.org), 30 per dimension × 10 per practice area
- **QRAMM-04** — Weakest-link scoring engine (dimension score = `min()` of practice scores) with profile multiplier and CSNP-verified reference calculation in tests
- **DEBT-01** — All `datetime.utcnow()` calls replaced with `datetime.now(timezone.utc)` across `quirk/logging_util.py`, `quirk/discovery/nmap_provider.py`, and any other affected modules; test suite emits zero `DeprecationWarning: datetime.utcnow()` messages

## Success Criteria

<verbatim 5 success criteria from .planning/ROADMAP.md "Phase 51" section, as a numbered or bulleted list — copy exactly>

## What Was Built

### Plan 51-01 — <objective heading from 51-01-PLAN.md>

<2–4 sentence summary derived from 51-01-PLAN.md <objective> + must_haves.truths>

### Plan 51-02 — <objective heading from 51-02-PLAN.md>

<2–4 sentence summary derived from 51-02-PLAN.md>

### Plan 51-03 — <objective heading from 51-03-PLAN.md>

<2–4 sentence summary derived from 51-03-PLAN.md>

### Plan 51-04 — Test Suite (QRAMM-01/-02/-03/-04)

Three test files were added: `tests/test_qramm_questions.py` (catalog count, schema, distribution, CSNP verbatim spot-checks), `tests/test_qramm_scoring.py` (CSNP reference calc, weakest-link min(), profile multiplier, maturity thresholds, D-09 isolation static-source check), and `tests/test_qramm_router.py` (TestClient smoke tests for all 5 endpoint families, validation 422s, score persistence round-trip, cascade-delete verification).

### Plan 51-05 — <objective heading from 51-05-PLAN.md>

<2–4 sentence summary derived from 51-05-PLAN.md — DEBT-01 utcnow fix>

## Links

- [[Roadmap]]
- [[Requirements]]
- Source: `.planning/phases/51-qramm-core-infrastructure/`
```

**Discovery commands to run before writing (to fill in the placeholders):**

```bash
ls /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/   # confirm folder + template style
date +%Y-%m-%d                                          # for `updated:` field
grep -n -A 30 "Phase 51" /Volumes/Digs-1TB/Development/quantum-apps/QUIRK/.planning/ROADMAP.md
```

Then read each `.planning/phases/51-qramm-core-infrastructure/51-0N-PLAN.md` file's `<objective>` block to derive each "What Was Built" subsection. Do not paraphrase the Goal or Success Criteria — copy verbatim from ROADMAP.md.

If the vault Phases/ folder does not exist, create it first with `mkdir -p /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/`.
  </action>
  <acceptance_criteria>
    - File exists: `test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-51-QRAMM-Core-Infrastructure.md"` exits 0
    - Frontmatter present: `head -10 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-51-QRAMM-Core-Infrastructure.md" | grep -c "^status: complete$"` returns `1`
    - Frontmatter type is phase: `head -10 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-51-QRAMM-Core-Infrastructure.md" | grep -c "^type: phase$"` returns `1`
    - Source frontmatter set: `head -10 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-51-QRAMM-Core-Infrastructure.md" | grep -c "^source: .planning/phases/51-qramm-core-infrastructure/"` returns `1`
    - All 5 requirement IDs present: `grep -c -E "QRAMM-0[1-4]|DEBT-01" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-51-QRAMM-Core-Infrastructure.md"` returns at least `5`
    - Each plan referenced: `grep -c -E "^### Plan 51-0[1-5]" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-51-QRAMM-Core-Infrastructure.md"` returns `5`
    - Roadmap wikilink present: `grep -c "\[\[Roadmap\]\]" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-51-QRAMM-Core-Infrastructure.md"` returns at least `1`
    - Goal section present: `grep -c "^## Goal$" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-51-QRAMM-Core-Infrastructure.md"` returns `1`
    - Success Criteria section present: `grep -c "^## Success Criteria$" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-51-QRAMM-Core-Infrastructure.md"` returns `1`
    - Requirements Covered section present: `grep -c "^## Requirements Covered$" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-51-QRAMM-Core-Infrastructure.md"` returns `1`
    - File non-trivial size: `wc -l "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-51-QRAMM-Core-Infrastructure.md"` reports at least 40 lines
  </acceptance_criteria>
  <verify>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-51-QRAMM-Core-Infrastructure.md" && head -10 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-51-QRAMM-Core-Infrastructure.md" | grep -q "^status: complete$" && grep -c -E "^### Plan 51-0[1-5]" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-51-QRAMM-Core-Infrastructure.md" | grep -q "^5$"</automated>
  </verify>
  <done>Obsidian phase note exists at vault path with complete frontmatter, all 5 requirement IDs documented, all 5 plan subsections present, Goal + Success Criteria copied verbatim from ROADMAP.md, and `[[Roadmap]]` link in place.</done>
</task>

<task type="auto">
  <name>Task 2: Update docs/UAT-SERIES.md with Phase 51 coverage, sync to vault, commit</name>
  <files>docs/UAT-SERIES.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md</files>
  <read_first>
    - CLAUDE.md (§"Mandatory Phase Completion Steps" → steps 2, 3, 4 — exact sync and commit commands)
    - docs/UAT-SERIES.md (full file — discover existing series structure, find the right series for Phase 51 additions, find `**Last Updated:**` line and any version strings)
    - .planning/phases/51-qramm-core-infrastructure/51-03-PLAN.md (exact endpoint paths, request bodies, and response shapes for /api/qramm/*)
    - .planning/phases/51-qramm-core-infrastructure/51-01-PLAN.md (table names: qramm_sessions, qramm_answers, qramm_profiles)
    - .planning/phases/51-qramm-core-infrastructure/51-05-PLAN.md (DEBT-01 utcnow scope and zero-warning gate)
  </read_first>
  <action>
### Step A — Update `docs/UAT-SERIES.md`

Use the **Read** tool first to ingest `docs/UAT-SERIES.md`. Identify:
1. The existing series most relevant to API/dashboard testing (e.g. UAT-1, UAT-2, etc.). If multiple are relevant, choose the API/dashboard series; if none fits, append a new "UAT-N: QRAMM Core (Phase 51)" series at the bottom.
2. The current `**Last Updated:**` line at the top of the document.
3. Any version strings that may need bumping (only if a Phase 51 plan bumped the package version — read 51-CONTEXT.md to confirm; if no version bump occurred this phase, skip version edits).

Use the **Edit** tool (one edit per logical change — do NOT rewrite the whole file) to:

1. Update the `**Last Updated:**` line to today's date (`date +%Y-%m-%d`).

2. Add a new test-case block for Phase 51 in the chosen series. Each test case follows the existing series' format (read existing cases to match style — typically `### UAT-X-NN: <title>` with **Steps** and **Pass Criteria** subsections). Add at minimum these cases:

   - **UAT-N-NN: Create QRAMM session via API**
     Steps: `curl -X POST http://localhost:8000/api/qramm/sessions -H "Content-Type: application/json" -d '{"org_name":"UAT-Org"}'`
     Pass criteria: HTTP 201; response body contains `session_id` (integer) and `org_name == "UAT-Org"` and `status == "draft"`.

   - **UAT-N-NN: Read QRAMM session round-trip**
     Steps: After UAT above, `curl http://localhost:8000/api/qramm/sessions/{session_id}`
     Pass criteria: HTTP 200; `session_id` matches; `answers_count == 0`; `score is null`.

   - **UAT-N-NN: Save QRAMM answers**
     Steps: `curl -X POST http://localhost:8000/api/qramm/sessions/{id}/answers -H "Content-Type: application/json" -d '{"answers":[{"question_number":1,"answer_value":3}]}'`
     Pass criteria: HTTP 200; response contains `saved_count == 1`, `total_answered == 1`. Out-of-range value (`answer_value: 5`) returns HTTP 422.

   - **UAT-N-NN: Score a QRAMM session**
     Steps: After saving 120 answers (all `answer_value: 2`), `curl -X POST http://localhost:8000/api/qramm/sessions/{id}/score -d '{}'`.
     Pass criteria: HTTP 200; response contains `overall` in [1.0, 4.0]; `dimensions` keys = `{CVI, SGRM, DPE, ITR}`; `maturity` is one of `Basic|Developing|Established|Advanced|Optimizing`; `profile_multiplier == 1.0`.

   - **UAT-N-NN: Delete QRAMM session cascades answers**
     Steps: `curl -X DELETE http://localhost:8000/api/qramm/sessions/{id}` then `curl http://localhost:8000/api/qramm/sessions/{id}`.
     Pass criteria: DELETE returns HTTP 204; subsequent GET returns HTTP 404.

   - **UAT-N-NN: QRAMM tables created by init_db**
     Steps: From a fresh DB, run `python -c "from quirk.db import init_db; from sqlalchemy import inspect; e=init_db('/tmp/uat_qramm.db'); print(sorted(set(inspect(e).get_table_names()) & {'qramm_sessions','qramm_answers','qramm_profiles'}))"`.
     Pass criteria: prints `['qramm_answers', 'qramm_profiles', 'qramm_sessions']`.

   - **UAT-N-NN: DEBT-01 zero-utcnow gate**
     Steps: `python -m pytest tests/ -W error::DeprecationWarning 2>&1 | grep -c "datetime.utcnow"`.
     Pass criteria: returns `0` (no occurrences of `datetime.utcnow` in test output).

If the existing series naming/numbering scheme is `UAT-X-NN`, use the next available NN within the chosen series. If you append a new series, name it `UAT-N: QRAMM Core` where `N` is one greater than the highest existing series number.

### Step B — Sync to vault (CLAUDE.md exact command)

Run the verbatim command from CLAUDE.md:

```bash
printf "---\nproject: QU.I.R.K.\ntype: reference\nstatus: active\nsource: docs/UAT-SERIES.md\nupdated: $(date +%Y-%m-%d)\n---\n\n" > /tmp/uat_vault.md
cat docs/UAT-SERIES.md >> /tmp/uat_vault.md
cp /tmp/uat_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
```

### Step C — Commit (CLAUDE.md exact command)

```bash
node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-51): update UAT-SERIES.md" --files docs/UAT-SERIES.md
```

Do NOT commit the vault file (it lives outside the repo). Only `docs/UAT-SERIES.md` is committed.
  </action>
  <acceptance_criteria>
    - `docs/UAT-SERIES.md` mentions all 5 QRAMM endpoints: `grep -c "/api/qramm/sessions" docs/UAT-SERIES.md` returns at least `5`
    - `docs/UAT-SERIES.md` mentions all 3 QRAMM tables: `grep -c -E "qramm_(sessions|answers|profiles)" docs/UAT-SERIES.md` returns at least `3`
    - `docs/UAT-SERIES.md` mentions DEBT-01 zero-utcnow gate: `grep -c "datetime.utcnow\|DeprecationWarning" docs/UAT-SERIES.md` returns at least `1`
    - `docs/UAT-SERIES.md` `**Last Updated:**` line refreshed: `grep -c "^\*\*Last Updated:\*\* $(date +%Y-%m-%d)" docs/UAT-SERIES.md` returns `1`
    - Vault sync produced byte-equivalent body: `tail -n +8 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" | diff - docs/UAT-SERIES.md` produces no output (empty diff after stripping the 7-line vault frontmatter)
    - Vault frontmatter correct: `head -7 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" | grep -c "^source: docs/UAT-SERIES.md$"` returns `1`
    - Vault frontmatter type+status correct: `head -7 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" | grep -c -E "^(type: reference|status: active)$"` returns `2`
    - Commit landed: `git log -1 --pretty=%s` contains `docs(phase-51): update UAT-SERIES.md`
    - Only docs/UAT-SERIES.md was staged/committed: `git show --stat HEAD | grep -c "docs/UAT-SERIES.md"` returns at least `1`
    - Working tree clean for docs/UAT-SERIES.md after commit: `git status --porcelain docs/UAT-SERIES.md` produces empty output
  </acceptance_criteria>
  <verify>
    <automated>grep -q "/api/qramm/sessions" docs/UAT-SERIES.md && grep -q "qramm_sessions" docs/UAT-SERIES.md && tail -n +8 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" | diff -q - docs/UAT-SERIES.md && git log -1 --pretty=%s | grep -q "docs(phase-51): update UAT-SERIES.md"</automated>
  </verify>
  <done>docs/UAT-SERIES.md documents Phase 51 endpoints, tables, and DEBT-01 gate; vault UAT-Series.md is byte-equivalent (with vault frontmatter prepended); docs/UAT-SERIES.md committed via gsd-tools.cjs with the exact commit message from CLAUDE.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo -> Obsidian vault | Vault is outside the git repo; sync via filesystem `cp` only |
| Local docs -> Committed history | docs/UAT-SERIES.md is committed; vault files are not |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-19 | Tampering | Vault file overwritten with stale content | mitigate | Sync command always rewrites vault from current `docs/UAT-SERIES.md`; acceptance criterion diff-checks byte equivalence |
| T-51-20 | Repudiation | Phase note frontmatter date drifts from commit date | mitigate | Both Task 1 and Task 2 use `$(date +%Y-%m-%d)` so frontmatter and commit timestamp share the same calendar day |
| T-51-21 | Information Disclosure | Vault path leaks user home dir into repo | accept | Vault path is documented in CLAUDE.md and never written into the repo — only `docs/UAT-SERIES.md` is committed |
</threat_model>

<verification>
```bash
# Phase note exists with required structure
test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-51-QRAMM-Core-Infrastructure.md"
head -10 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-51-QRAMM-Core-Infrastructure.md" | grep -E "^(status: complete|type: phase)$" | wc -l   # expect 2
grep -c -E "^### Plan 51-0[1-5]" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-51-QRAMM-Core-Infrastructure.md"   # expect 5

# UAT coverage present
grep -c "/api/qramm/sessions" docs/UAT-SERIES.md      # expect >= 5
grep -c -E "qramm_(sessions|answers|profiles)" docs/UAT-SERIES.md   # expect >= 3

# Vault sync byte-equivalent
tail -n +8 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" | diff -q - docs/UAT-SERIES.md   # empty output

# Commit landed
git log -1 --pretty=%s   # contains: docs(phase-51): update UAT-SERIES.md
```
All checks must pass.
</verification>

<success_criteria>
- Obsidian phase note created at the exact vault path specified in CLAUDE.md, with `status: complete`, all 5 requirement IDs, all 5 plan subsections, verbatim Goal and Success Criteria from ROADMAP.md, and `[[Roadmap]]` wikilink.
- `docs/UAT-SERIES.md` updated with test cases covering all 5 QRAMM endpoints, all 3 QRAMM tables, and the DEBT-01 zero-utcnow gate; `**Last Updated:**` line refreshed.
- Vault `UAT-Series.md` is byte-equivalent to `docs/UAT-SERIES.md` (modulo the 7-line vault frontmatter), produced by the exact sync command from CLAUDE.md.
- `docs/UAT-SERIES.md` committed via `gsd-tools.cjs commit` with the exact message `docs(phase-51): update UAT-SERIES.md`.
- Phase 51 closes cleanly per CLAUDE.md "Mandatory Phase Completion Steps".
</success_criteria>

<output>
After completion, create `.planning/phases/51-qramm-core-infrastructure/51-06-SUMMARY.md` capturing:
- Path to the Obsidian phase note
- Lines added to docs/UAT-SERIES.md (test case identifiers)
- Vault sync diff result (should be empty)
- Commit SHA from `git log -1 --pretty=%H`
</output>
