---
phase: 56.1-close-qramm-06-07-wire-ci-staleness-gate-workflow
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - CLAUDE.md
  - quirk/qramm/model_meta.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [QRAMM-06, QRAMM-07, COMPLY-08]
must_haves:
  truths:
    - "CLAUDE.md contains a top-level '## Staleness Review Cadence' section that names both staleness-gated files, states the bump procedure, and references the workflow"
    - "quirk/qramm/model_meta.py docstring/comment points readers to the new CLAUDE.md section while keeping the existing 'Per QRAMM-06' note"
    - "REQUIREMENTS.md shows QRAMM-05/06/07 checkboxes as [x] and the cross-reference table reflects Phase 55 + 56.1 closure"
    - "REQUIREMENTS.md COMPLY-08 row notes 'CI-protected by python-staleness.yml (Phase 56.1)'"
    - "ROADMAP.md Phase 56.1 entry has a real Goal sentence and the actual plan list (no 'TBD' / 'to be planned' placeholders)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Staleness Review Cadence operational rule section"
      contains: "## Staleness Review Cadence"
    - path: "quirk/qramm/model_meta.py"
      provides: "Pointer from model file to CLAUDE.md cadence section"
      contains: "Staleness Review Cadence"
    - path: ".planning/REQUIREMENTS.md"
      provides: "QRAMM-06/07 checkboxes ticked + table rows updated"
      contains: "Complete (Phase 55 + 56.1)"
    - path: ".planning/ROADMAP.md"
      provides: "Finalized Phase 56.1 Goal + plan list"
  key_links:
    - from: "quirk/qramm/model_meta.py"
      to: "CLAUDE.md"
      via: "comment pointer to Staleness Review Cadence section"
      pattern: "Staleness Review Cadence"
    - from: "CLAUDE.md"
      to: ".github/workflows/python-staleness.yml"
      via: "section references workflow path"
      pattern: "python-staleness.yml"
---

<objective>
Land the documentation half of Phase 56.1: a new `## Staleness Review Cadence`
section in `CLAUDE.md`, a one-line pointer added to `quirk/qramm/model_meta.py`,
and the corresponding REQUIREMENTS.md / ROADMAP.md closure edits.

Purpose: Per saved feedback memory `feedback_compliance_freshness.md`, compliance
mappings need staleness pressure across six pillars; CI staleness check (Plan 01)
and documented review cadence (this plan) are the last two pillars to close.
Per D-05, the cadence rule lives on the same enforcement plane as
"Chaos Lab Maintenance" and "Mandatory Phase Completion Steps" inside CLAUDE.md.

Output: Four files edited. No new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/56.1-close-qramm-06-07-wire-ci-staleness-gate-workflow/56.1-CONTEXT.md
@CLAUDE.md
@quirk/qramm/model_meta.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Staleness Review Cadence" section to CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
Insert a new top-level `## Staleness Review Cadence` section into `CLAUDE.md`,
placed between the existing `## Code Standards` and `## Chaos Lab Maintenance`
sections (per CONTEXT.md code_context guidance and D-05).

Use this exact section body (per CONTEXT.md specifics — refine wording lightly
if needed for tone consistency, but keep all five facts: both file paths, both
cadences, the three-step bump procedure, and the workflow path reference):

```
## Staleness Review Cadence

Two project data files carry a `last_verified` date and are gated by CI to
prevent silent staleness:

- `quirk/qramm/model_meta.py` — QRAMM model (90-day cadence,
  `STALENESS_THRESHOLD_DAYS = 90`)
- `quirk/compliance/__init__.py` — Compliance mappings (365-day cadence)

When CI fails with a "STALE" assertion:

1. Re-verify the catalog against its `source_url`.
2. Bump `last_verified` to today's ISO date in the relevant file.
3. Commit with message `chore: re-verify <model> catalog (YYYY-MM-DD)`.

Enforcement workflow: `.github/workflows/python-staleness.yml` (runs on every
PR, every push to `main`, and weekly Monday 09:00 UTC cron).
```

Constraints:
- Heading text MUST be exactly `## Staleness Review Cadence` (per D-07 verification).
- Do NOT change `STALENESS_THRESHOLD_DAYS = 90` or any other code (per Out of scope).
- Do NOT create `CONTRIBUTING.md` or `docs/maintenance.md` (deferred per CONTEXT.md).
- Preserve all existing sections in CLAUDE.md unchanged.
  </action>
  <verify>
    <automated>grep -c '^## Staleness Review Cadence$' CLAUDE.md | grep -qx 1 && grep -q 'quirk/qramm/model_meta.py' CLAUDE.md && grep -q 'quirk/compliance/__init__.py' CLAUDE.md && grep -q 'python-staleness.yml' CLAUDE.md && grep -q '90-day cadence' CLAUDE.md && grep -q '365-day cadence' CLAUDE.md && echo OK</automated>
  </verify>
  <done>
- New `## Staleness Review Cadence` heading present (exactly one occurrence)
- Section names both `quirk/qramm/model_meta.py` and `quirk/compliance/__init__.py`
- Section states 90-day and 365-day cadences
- Section references `.github/workflows/python-staleness.yml`
- Section contains the 3-step bump procedure
- All other CLAUDE.md sections unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Add pointer comment to quirk/qramm/model_meta.py</name>
  <files>quirk/qramm/model_meta.py</files>
  <action>
Add a one-line comment near the existing `# Per QRAMM-06 — quarterly re-verification cadence`
pointer (or near the `STALENESS_THRESHOLD_DAYS = 90` definition) that reads:

```
# See CLAUDE.md "Staleness Review Cadence" for the bump procedure.
```

Constraints (per Out of scope + D-05):
- Do NOT change `STALENESS_THRESHOLD_DAYS = 90`
- Do NOT change `last_verified`, `qramm_version`, or `source_url`
- Do NOT remove the existing "Per QRAMM-06" comment — augment, not replace
- One-line addition only; no docstring rewrites
  </action>
  <verify>
    <automated>grep -q 'Staleness Review Cadence' quirk/qramm/model_meta.py && python -c "from quirk.qramm.model_meta import STALENESS_THRESHOLD_DAYS, QRAMM_MODEL; assert STALENESS_THRESHOLD_DAYS==90; assert 'last_verified' in QRAMM_MODEL; assert 'qramm_version' in QRAMM_MODEL; assert 'source_url' in QRAMM_MODEL; print('OK')"</automated>
  </verify>
  <done>
- File contains a comment referencing "Staleness Review Cadence"
- `STALENESS_THRESHOLD_DAYS` still equals 90
- `QRAMM_MODEL` still has `last_verified`, `qramm_version`, `source_url`
- Existing "Per QRAMM-06" comment retained
  </done>
</task>

<task type="auto">
  <name>Task 3: Close QRAMM-05/06/07 in REQUIREMENTS.md and update cross-reference table</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Per D-06 and CONTEXT.md specifics:

1. Toggle the per-requirement checkboxes from `[ ]` to `[x]` for `QRAMM-05`,
   `QRAMM-06`, `QRAMM-07` (only those that are not already `[x]`).

2. Update the cross-reference table near line 128:
   - QRAMM-05: status column → `Complete (Phase 55)`
   - QRAMM-06: status column → `Complete (Phase 55 + 56.1)`
   - QRAMM-07: status column → `Complete (Phase 55 + 56.1)`
   - COMPLY-08: append the note `CI-protected by python-staleness.yml (Phase 56.1)`
     to its row (preserve any existing "Complete" status; add as a parenthetical
     or notes-column entry consistent with the table's existing column shape).

Constraints:
- Do NOT renumber, reword, or otherwise change any other requirement.
- Do NOT add new requirement IDs.
- Preserve the table's existing column structure exactly — only edit the
  affected rows.
  </action>
  <verify>
    <automated>grep -E '^\s*-\s*\[x\]\s*\*\*?QRAMM-06' .planning/REQUIREMENTS.md && grep -E '^\s*-\s*\[x\]\s*\*\*?QRAMM-07' .planning/REQUIREMENTS.md && grep -q 'Complete (Phase 55 + 56.1)' .planning/REQUIREMENTS.md && grep -q 'CI-protected by python-staleness.yml' .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <done>
- QRAMM-05, QRAMM-06, QRAMM-07 checkboxes show `[x]`
- Cross-reference table shows "Complete (Phase 55 + 56.1)" for QRAMM-06 and QRAMM-07
- COMPLY-08 row mentions "CI-protected by python-staleness.yml (Phase 56.1)"
- No other rows changed
  </done>
</task>

<task type="auto">
  <name>Task 4: Finalize ROADMAP.md Phase 56.1 entry</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Locate the `### Phase 56.1: Close QRAMM-06/07 — wire CI staleness gate workflow (INSERTED)`
section (around line 1147) and update two fields:

1. **Goal:** Replace the placeholder (e.g., `[Urgent work - to be planned]`) with this
   one-sentence summary derived from CONTEXT.md's `<domain>` block:

   > Wire the QRAMM staleness gate, the compliance freshness gate, and the QRAMM
   > CLI smoke tests into a new GitHub Actions workflow so that QRAMM-06's
   > 90-day pytest gate is actually enforced on every PR, every push to `main`,
   > and weekly via cron — and document the cadence in `CLAUDE.md`.

2. **Plans:** Replace the `TBD` plan list with the actual plan files this PLAN.md
   set emits:

   ```
   Plans:
   - [ ] 56.1-01-PLAN.md — Create .github/workflows/python-staleness.yml
   - [ ] 56.1-02-PLAN.md — CLAUDE.md cadence section + REQUIREMENTS.md / ROADMAP.md closure + model_meta.py pointer
   - [ ] 56.1-03-PLAN.md — UAT-SERIES.md update + Obsidian phase note + UAT commit
   ```

   Update the plan count line to `**Plans:** 3 plans`.

Constraints:
- Do NOT modify any other phase entry.
- Preserve the `(INSERTED)` marker on the heading exactly as-is.
  </action>
  <verify>
    <automated>grep -n 'Phase 56.1: Close QRAMM-06/07' .planning/ROADMAP.md && ! grep -A 20 'Phase 56.1: Close QRAMM-06/07' .planning/ROADMAP.md | grep -q 'to be planned' && grep -A 20 'Phase 56.1: Close QRAMM-06/07' .planning/ROADMAP.md | grep -q '56.1-01-PLAN.md' && grep -A 20 'Phase 56.1: Close QRAMM-06/07' .planning/ROADMAP.md | grep -q '56.1-02-PLAN.md' && grep -A 20 'Phase 56.1: Close QRAMM-06/07' .planning/ROADMAP.md | grep -q '56.1-03-PLAN.md' && echo OK</automated>
  </verify>
  <done>
- Phase 56.1 Goal is a real one-sentence summary (no "to be planned" / "TBD")
- Plan list names 56.1-01-PLAN.md, 56.1-02-PLAN.md, 56.1-03-PLAN.md
- Plan count reflects 3 plans
- (INSERTED) marker preserved
- No other phase entries touched
  </done>
</task>

</tasks>

<verification>
Phase 56.1 D-07 hooks satisfied by this plan:
- [x] CLAUDE.md contains "## Staleness Review Cadence" heading
- [x] REQUIREMENTS.md shows QRAMM-06 and QRAMM-07 as `[x]`
- [x] model_meta.py points to CLAUDE.md section
- [x] ROADMAP.md finalized for Phase 56.1
</verification>

<success_criteria>
- All four files edited; no new files created
- All four `<verify>` blocks pass
- No code logic changed (thresholds, model fields, env-var contract intact)
</success_criteria>

<output>
After completion, create `.planning/phases/56.1-close-qramm-06-07-wire-ci-staleness-gate-workflow/56.1-02-SUMMARY.md`
listing the four file edits and the exact inserted/changed text snippets.
</output>
