---
milestone: v5.3
audited: 2026-05-25
status: passed
scores:
  requirements: 21/21
  phases: 5/5
  integration: 18/18
  flows: 3/3
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 105-servicenow-ticketing
    items:
      - "LOW: _NoRedirectHandler is copied verbatim in quirk/notify/channels/webhook.py and quirk/ticketing/servicenow.py. Both identical; a future SSRF-redirect fix must be mirrored. Recommend extracting to quirk/util/no_redirect.py and importing from both (design-documented under T-105-04; no runtime impact)."
human_uat_deferred:
  - phase: 101
    count: 4
    items: ["Slack live delivery", "Email live delivery", "Generic webhook + HMAC verification", "End-to-end scheduler dispatch"]
  - phase: 102
    count: 7
    items: ["Login form render", "Wrong-token inline error", "Correct-token dashboard load", "Sign out clears token", "Mid-session 401 logout", "Auth-disabled passthrough", "Live token CLI round-trip"]
  - phase: 103
    count: 2
    items: ["Live syslog/CEF delivery to a real SIEM", "After-scan scheduler SIEM hook"]
  - phase: 104
    count: 4
    items: ["Live Jira issue creation", "Idempotent dedup on re-scan", "Missing-[tickets] graceful skip", "Self-hosted token_auth path"]
  - phase: 105
    count: 2
    items: ["Live ServiceNow incident creation", "Work-notes dedup on re-scan"]
nyquist:
  compliant_phases: [101, 102, 103, 104, 105]
  partial_phases: []
  missing_phases: []
  overall: COMPLIANT
---

# Milestone v5.3 — Adoption & Integration Surface — Audit

**Audited:** 2026-05-25
**Status:** PASSED (code-complete; live-delivery items tracked as human-UAT)

## Summary

All 5 phases (101–105) shipped with full automated verification coverage. All 21
requirements are satisfied. Cross-phase integration is fully wired (18/18 shared-primitive
connections), all 3 end-to-end flows complete, and all 5 phases are Nyquist-compliant.
161 integration-subsystem tests pass; `compileall` clean.

Every phase landed at VERIFICATION status `human_needed` — meaning every automatable check
passed and the only outstanding items are live-endpoint deliveries (Slack/SMTP/webhook/syslog/
Jira/ServiceNow against real servers) which are inherently manual. These 19 items are tracked
in per-phase `*-HUMAN-UAT.md` files and will surface in `/gsd:progress` and `/gsd:audit-uat`.
They are NOT gaps — network sends are unit-tested with mocked transports.

## Requirements Coverage (21/21 satisfied)

| Phase | Requirements | Status |
|-------|--------------|--------|
| 101 Notification Fan-Out + Security Foundation | NOTIFY-01..07, ISEC-01..04 | satisfied (11/11) |
| 102 Dashboard Auth UX + Score Tax | AUTH-01..03, TRANS-04 | satisfied (4/4) |
| 103 SIEM Export | SIEM-01..02 | satisfied (2/2) |
| 104 Jira Ticketing | TICKET-01, TICKET-03, TICKET-04 | satisfied (3/3) |
| 105 ServiceNow Ticketing | TICKET-02 | satisfied (1/1) |

3-source cross-reference (VERIFICATION.md + SUMMARY frontmatter + REQUIREMENTS.md traceability):
no `unsatisfied`, no `orphaned` requirements. All REQUIREMENTS.md rows marked Complete.

## Cross-Phase Integration (18/18 wired)

The v5.3 ANCHOR (Phase 101) shipped the shared integration-security primitives; phases 103/104/105
inherit rather than re-implement them:

- **`integration_deliveries` table** — single table, four destinations (slack/email/webhook, siem, jira, servicenow), identical audit pattern (`safe_str` error_summary, commit-outside-try / WR-01).
- **`safe_str`** — Phase 101 (Slack/SMTP) + Phase 104 (basic_auth/token_auth) patterns coexist; every integration error path routes through it. No raw `str(exc)`/`repr(exc)`.
- **`validate_external_url`** — called by notify webhook/email/slack (delivery-time) and ticketing jira/servicenow (init). SIEM syslog intentionally exempt (internal collectors, documented D-02; format-only validation instead).
- **`_NoRedirectHandler`** — Phase 103 CR-01 SSRF-redirect fix; reused by ServiceNow (copied verbatim — see tech-debt note).
- **`TicketingChannel` ABC + `compute_fingerprint`** — Phase 104; ServiceNow (105) subclasses with ZERO base.py/jira.py changes (TICKET-04 verified at git level); identical SHA256(host:port::title) fingerprint.
- **CLI interception** (`export`, `ticket --backend`) coexist with existing blocks; all use `return`.
- **`[notify]` + `[tickets]` extras** joined `[all]` with CI guards; `[identity]` exclusion preserved.
- **Route-coverage CI gate** (Phase 102) protects all `/api/*` data routes (X-API-Key + bearer).

## E2E Flows (3/3 complete)

1. Scheduled scan → drift → notification dispatch (+ SIEM after-scan hook) — both hooks isolated, scan record never corrupted.
2. Completed scan → `quirk export --siem` → CEF events.
3. Completed scan → `quirk ticket create [--backend servicenow]` → fingerprint-deduped tickets.

## Tech Debt (1 LOW item)

- `_NoRedirectHandler` duplicated verbatim in webhook.py and servicenow.py. Design-documented (T-105-04); recommend extracting to `quirk/util/no_redirect.py`. No runtime impact. → backlog.

## Code Review Highlights (caught + fixed during execution)

The layered review gate caught and fixed several bugs that passed unit tests:
- Phase 101: SSRF redirect-bypass (webhook urllib followed 302→metadata); first-scan email TypeError.
- Phase 102: (TRANS-04 surfaced a real correctness bug — CLI score was re-derived as 91/EXCELLENT vs canonical 42/FAIR; now sourced from exec_content).
- Phase 103: CEF header newline injection (log forgery); missing TCP framing; dpt extension injection.
- Phase 104: JQL injection via project_key; short Jira-Server PAT scrub gap.
- Phase 105: ServiceNow `table` URL-path injection.

## Nyquist Compliance

All 5 phases `nyquist_compliant: true`. Overall: COMPLIANT.

## Verdict

Milestone v5.3 Adoption & Integration Surface is code-complete and PASSED. Proceed to
complete-milestone. 19 live-delivery human-UAT items remain (tracked per-phase) and 1 LOW
tech-debt item goes to backlog.
