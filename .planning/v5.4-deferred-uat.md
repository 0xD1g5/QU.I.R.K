---
status: testing
scope: v5.4 deferred live human-UAT (cross-phase 108/110/111/112 + GitHub gate)
source:
  - .planning/milestones/v5.4-phases/108-sensor-push-cli-windows-ci/108-VERIFICATION.md
  - .planning/milestones/v5.4-phases/110-cross-sensor-merge-scoring/110-VERIFICATION.md
  - .planning/milestones/v5.4-phases/111-console-dashboard-awareness/111-VERIFICATION.md
  - .planning/milestones/v5.4-phases/112-distributed-chaos-lab-stabilization/112-VERIFICATION.md
started: 2026-05-26T21:22:03Z
updated: 2026-05-26T21:22:03Z
---

## Current Test

number: 9
name: Live Distributed E2E (UAT-112-03)
expected: |
  lab.sh distributed up + e2e: both sensors enroll (2 bearer tokens), both push HTTP 200,
  merged CBOM has exactly two CryptoEndpoint components host=crypto.internal port=443 differing
  only by sensor_id (sensor-a / sensor-b), one quantum-readiness score, coverage_warning null.
awaiting: Docker startup, then user response

## Tests

### 1. Sensor enroll + sensor.yaml "shown once" inspection (108)
expected: sensor.yaml contains console_url, sensor_id(UUID), segment, engagement, sensor_version, hmac_key(64 hex), console_api_token; one-time token printed to stdout is absent from the file.
result: pass
note: "Ran enroll in sensor-a container to /tmp/t1-sensor.yaml. Token 9sovwb1Tx... printed once to stdout; grep -F confirmed it is ABSENT from the yaml. hmac_key=64 hex. All required keys present (engagement=null key present). Minor pre-existing: sensor_version=5.0.0 (version-string staleness, backlog 999.44). CLI/file evidence definitive."

### 2. Spool round-trip FIFO (108)
expected: push offline leaves one *.json.zst in spool dir; push online delivers it FIFO and spool dir is empty after.
result: pass
note: "Stopped console; sensor-a push spooled c547ee1c-...json.zst to /home/quirk/.local/share/quirk/spool/ (1 file, 543 bytes). Restarted console (healthy); sensor-a push again drained spool to 0 files. Console sensor_pushes table confirms c547ee1c delivered + ingested. FIFO store-and-forward verified. CLI/DB evidence definitive."

### 3. GitHub Windows hard-gate required check (108)
expected: PR that fails test_sensor_windows_smoke.py shows windows-sensor-smoke job red and blocks merge until it passes.
result: blocked
blocked_by: github-plan
reason: "Branch-protection rules require GitHub Pro or a public repo; this repo is private on a free plan (GET branches/main/protection -> HTTP 403 'Upgrade to GitHub Pro or make this repository public'). The windows-sensor-smoke job EXISTS and runs (.github/workflows/python-ci.yml:9, name 'Windows Sensor Smoke', runs-on windows-latest, runs tests/test_sensor_windows_smoke.py) — it will go red on failure and be visible in a PR — but GitHub cannot ENFORCE it as a merge-blocking required check without branch protection. The hard-gate enforcement is unachievable on the current plan. Not a code defect."
decision: "RESOLVED 2026-05-26 — accept as non-blocking CI for now (solo maintainer; job still runs + shows red). Repo planned to go public in the next milestone or two, which enables branch protection free; THEN enforce windows-sensor-smoke as a required check (one-time settings change) and re-run this test. Tracked in HORIZON v5.5 section."

### 4. sensor merge overdue-sensor WARNING (110)
expected: merge output has "Merged scan_id:", "Score: N (rating)", and a WARNING block listing overdue sensor id(s) as indented "  - sensor-id" lines. Exit 0.
result: pass
note: "Forced sensor-b overdue (aged last_push_at to 3 days ago, past 2×cadence=48h, within stale_days=30). quirk sensor merge in console container emitted: 'Merged scan_id: ...', 'Score: 95 (EXCELLENT)', 'WARNING: 1 enrolled sensor(s) have not pushed within 2× their expected cadence: sensor-b', and indented '  - sensor-b'. Exit 0. CLI evidence definitive."

### 5. Two-segment same-IP CBOM components no-collision (110)
expected: CBOM has two cert components crypto/certificate/sensor-a:10.0.0.5:443 and crypto/certificate/sensor-b:10.0.0.5:443 plus two TLS-protocol components, same per-sensor prefixes, no collision.
result: pass
note: "Validated via Test 9 (112 e2e) using crypto.internal:443 instead of 10.0.0.5:443 — principle identical. CBOM contained crypto/certificate/sensor-a:crypto.internal:443, crypto/certificate/sensor-b:crypto.internal:443, and the two matching crypto/protocol/tls/sensor-{a,b} components. No collision. MERGE-03 proven under real Docker networking."

### 6. Sensors page — table, badges, empty-state (111)
expected: Sensors nav (radio icon) shows registry table (Sensor ID monospace / Segment / Version / Last Seen relative / Status). Badges show TEXT label (Current/Stale/Unknown) + color, not color alone. Empty-state shows enroll-command message.
result: pass
note: "User-confirmed against live console :8512 with 2 current sensors (sensor-a/segment-a, sensor-b/segment-b). Table columns + monospace Sensor ID + green Current text badges + relative Last Seen confirmed. PARTIAL coverage: empty-state and Stale/Unknown badge sub-states not exercised by happy-path data (both sensors current)."

### 7. Segment filter on Findings/CBOM (111)
expected: "All segments" dropdown present; selecting a segment shows only its rows; "All segments" returns all incl NULL-segment single-host rows.
result: blocked
blocked_by: test-data
reason: "Findings page legitimately empty — nginx:1.28.0 lab targets are strong crypto, 0 findings, so the segment-filter toolbar doesn't render (correct UX). CBOM components are algorithm-level deduped (Phase 110 design) → segment=None, nothing to filter. NOT a product defect: pushed crypto_endpoints DO carry segment-a/segment-b in the console DB; the filter just has no findings to operate on. To truly exercise: needs a WEAK-crypto target in each segment to generate segmented findings. Minor noise observed: stray scanned_at=None rows for email_scanner/broker_scanner hosts (port 0) in console DB — backlog note."

### 8. Executive per-segment gauges + coverage banner (111)
expected: no-merge → only org-wide gauge, no banner; merge+multi-segment → per-segment gauges (120px) beside org-wide; merge+missing-sensors → amber banner ABOVE gauges, factual count, NO dismiss button. Labels >16 chars truncated.
result: pass
note: "User-confirmed via screenshot. Happy-path: org-wide gauge 95 + six subscore gauges + per-segment gauges segment-a(95)/segment-b(95), NO banner (coverage_warning null). Banner-present (8c): after forcing sensor-b overdue + re-merge, /api/merge/latest carried coverage_warning {missing_sensors:[sensor-b]}; user confirmed amber banner above gauges, factual count, no dismiss button. Label-truncation (>16 chars) not separately exercised (segment labels short)."

### 9. Live Distributed E2E (UAT-112-03) (112)
expected: both sensors enroll (2 tokens), both push HTTP 200, merged CBOM has exactly two CryptoEndpoint components host=crypto.internal port=443 differing only by sensor_id, one quantum-readiness score, coverage_warning null.
result: pass
note: "Initially BLOCKED by 2 defects (build-context ../.., enroll sensor_id mismatch 404 + scan-exit-2). All fixed (see Gaps fix entries + debug session sensor-enroll-id-mismatch). Clean re-run EXIT=0: both enroll, both push, merge Score 95 (EXCELLENT), CBOM JSON+XML; 2 cert + 2 tls components keyed by sensor-a/sensor-b at crypto.internal:443, no collision; coverage_warning absent. User-confirmed pass 2026-05-26."

## Summary

total: 9
passed: 7
issues: 0
pending: 0
blocked: 2
skipped: 0

notes: |
  PASS (7): 1 enroll/shown-once, 2 spool FIFO, 4 overdue WARNING, 5 same-IP CBOM,
  6 Sensors page, 8 exec gauges+banner, 9 distributed E2E. BLOCKED (2, both
  environmental — NOT defects): 7 segment filter (lab has no weak-crypto target →
  0 findings to filter; backlog 999.85), 3 GitHub windows hard-gate (branch
  protection requires GitHub Pro / public repo — unachievable on current free
  private plan; CI job exists + runs but cannot be enforced as merge-blocking).
  3 defects found+fixed+committed (84e770e, d1dbb87): compose build-context,
  enroll --sensor-id 404, _run_local_scan --output. 5 follow-ups → backlog
  999.85-999.89 + HORIZON v5.5 section (committed 88d790b). SWEEP COMPLETE.

## Gaps

- truth: "lab.sh distributed up builds and starts the two-segment topology"
  status: failed
  reason: "User reported: build context ../.. in docker-compose.distributed.yml overshoots the repo root by one level; Docker cannot find the build context dir"
  severity: blocker
  test: 9
  artifacts: ["quantum-chaos-enterprise-lab/docker-compose.distributed.yml:83,112,140"]
  missing: ["context: .. (repo root) instead of context: ../.."]
  fix_applied: "context: ../.. -> context: .. (all 3 services); lab now builds + starts, console healthy"

- truth: "distributed e2e: sensors enroll, push findings to console (HTTP 200), console ingests"
  status: failed
  reason: "push rejected HTTP 404 'Unknown sensor_id'. sensor enroll (sensor_cmd.py:230) mints a local random UUID into sensor.yaml and never registers it with the console; console enroll (step 1) provisions a DIFFERENT id (sensor-a/sensor-b). The two enroll commands mint independent IDs that never reconcile, so authenticated push cannot succeed via the documented two-command flow. Unit tests passed by injecting matching Sensor/CryptoEndpoint rows in-memory."
  severity: blocker
  test: 9
  artifacts: ["quirk/cli/sensor_cmd.py:230", "quirk/dashboard/api/routes/sensor.py:312-315", "quantum-chaos-enterprise-lab/scripts/distributed-e2e.sh:50-120"]
  missing: ["shared sensor_id between console enroll and sensor enroll (e.g. sensor enroll --sensor-id, or sensor enroll POSTs to register, or e2e writes console-provisioned id into sensor.yaml)"]

- truth: "sensor local scan of crypto.internal:443 succeeds and yields endpoints to push"
  status: failed
  reason: "WARNING: local scan exited with code 2 (secondary — masked by the 404 which fires at sensor lookup before endpoint processing). Needs separate investigation (likely crypto.internal:443 reachability / nmap return)."
  severity: major
  test: 9
  artifacts: ["quantum-chaos-enterprise-lab/scripts/distributed-e2e.sh:110,119"]
  missing: ["confirm scan reaches crypto.internal:443 from inside segment network"]
