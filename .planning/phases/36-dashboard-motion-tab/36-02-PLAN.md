---
phase: 36-dashboard-motion-tab
plan: 02
type: execute
wave: 2
depends_on: [36-01]
files_modified:
  - src/dashboard/src/types/api.ts
  - src/dashboard/src/pages/executive.tsx
  - src/dashboard/src/components/sidebar.tsx
  - src/dashboard/src/App.tsx
autonomous: true
requirements: [DASH-01, DASH-04]
tags: [dashboard, react, typescript, scaffolding]

must_haves:
  truths:
    - "TypeScript SubScores includes data_in_motion: number"
    - "TypeScript MotionFinding interface mirrors the Pydantic shape from Plan 36-01"
    - "ScanLatestResponse TS interface includes motion_findings: MotionFinding[]"
    - "Executive page renders 6 ScoreGauges, last one is Data in Motion"
    - "Sidebar navigation includes a Motion entry between Identity and Certificates"
    - "Browser route /motion is registered in App.tsx and resolves to MotionPage"
  artifacts:
    - path: "src/dashboard/src/types/api.ts"
      provides: "SubScores.data_in_motion; MotionFinding interface; ScanLatestResponse.motion_findings"
      contains: "interface MotionFinding"
    - path: "src/dashboard/src/pages/executive.tsx"
      provides: "6th ScoreGauge for data_in_motion; loading skeleton length=6"
      contains: 'label="Data in Motion"'
    - path: "src/dashboard/src/components/sidebar.tsx"
      provides: "Motion NAV_ITEMS entry with Activity icon"
      contains: 'path: "/motion"'
    - path: "src/dashboard/src/App.tsx"
      provides: "/motion Route registration"
      contains: 'path="/motion"'
  key_links:
    - from: "src/dashboard/src/App.tsx"
      to: "src/dashboard/src/pages/motion (MotionPage)"
      via: "import + Route element"
      pattern: 'element=\\{<MotionPage'
    - from: "src/dashboard/src/components/sidebar.tsx NAV_ITEMS"
      to: "/motion"
      via: 'lucide-react Activity icon'
      pattern: 'Icon: Activity'
    - from: "src/dashboard/src/pages/executive.tsx (gauge row)"
      to: "score.subscores.data_in_motion"
      via: "<ScoreGauge> 6th instance"
      pattern: 'subscores\\.data_in_motion'
---

<objective>
Land the frontend scaffolding for Phase 36: extend the TS contract to mirror Plan 36-01's Pydantic shapes, add the 6th `ScoreGauge` to the executive summary, register the Motion sidebar nav entry, and register the `/motion` route in App.tsx.

Purpose: This plan is the wiring that connects the backend API contract (Plan 36-01) to the new Motion page (Plan 36-03). Without this, MotionPage cannot be reached, the 6th score is invisible, and `motion_findings` has no TS shape.

Output: Extended `types/api.ts`; modified `executive.tsx`, `sidebar.tsx`, `App.tsx`. The new `MotionPage` import in `App.tsx` is a forward reference resolved by Plan 36-03 — TypeScript will fail until 36-03 lands the file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-dashboard-motion-tab/36-CONTEXT.md
@.planning/phases/36-dashboard-motion-tab/36-RESEARCH.md
@.planning/phases/36-dashboard-motion-tab/36-PATTERNS.md
@.planning/phases/36-dashboard-motion-tab/36-UI-SPEC.md
@.planning/phases/36-dashboard-motion-tab/36-01-PLAN.md
@src/dashboard/src/types/api.ts
@src/dashboard/src/pages/executive.tsx
@src/dashboard/src/components/sidebar.tsx
@src/dashboard/src/App.tsx

<interfaces>
<!-- TS shapes from 36-PATTERNS.md (lines 152-195) and 36-UI-SPEC.md (lines 244-282). -->
<!-- These MUST mirror the Pydantic models from Plan 36-01 — do not introduce drift. -->

src/dashboard/src/types/api.ts — SubScores extension (lines 1-7):
```ts
export interface SubScores {
  hygiene: number
  modern_tls: number
  identity_trust: number
  agility_signals: number
  data_at_rest: number
  data_in_motion: number     // NEW — Phase 36 D-06
}
```

src/dashboard/src/types/api.ts — new MotionFinding (mirror IdentityFinding at lines 80-91):
```ts
export interface MotionFinding {
  host: string
  port: number
  severity: string
  title: string
  protocol?: string
  description?: string
  remediation?: string
  quantum_risk?: string
  source?: string
  tls_version?: string
  cipher_suite?: string
  cert_not_after?: string         // ISO date string
  plaintext_exposed: boolean      // NON-OPTIONAL per D-02
  starttls_warning: boolean       // NON-OPTIONAL per D-02
}
```

src/dashboard/src/types/api.ts — ScanLatestResponse extension (lines 99-108):
```ts
export interface ScanLatestResponse {
  // ... existing fields unchanged ...
  identity_findings: IdentityFinding[]
  motion_findings: MotionFinding[]   // NEW — Phase 36 DASH-05
}
```

Sidebar NAV_ITEMS new entry (between Identity and Certificates):
```tsx
{ path: "/motion", label: "Motion", Icon: Activity },
```
Icon source: `lucide-react`'s `Activity` component (added to existing import block at sidebar.tsx lines 5-13).

Executive summary 6th gauge (after the data_at_rest gauge at line ~150):
```tsx
<ScoreGauge score={score.subscores.data_in_motion} label="Data in Motion" size={120} />
```
Plus update the loading skeleton at executive.tsx:60-67 from `length: 5` → `length: 6`.

App.tsx Route (between /identity and /certificates):
```tsx
<Route path="/motion" element={<MotionPage />} />
```
Plus add `import { MotionPage } from "@/pages/motion"` alongside the existing page imports (lines ~7-13).
</interfaces>

<critical_pitfalls>
**Pitfall 2 (RESEARCH.md):** TS and Pydantic must both default `motion_findings` to `[]`. TS-side default is implicit because the server always returns the field (default `[]`). Mark `motion_findings: MotionFinding[]` as NON-optional (no `?`); `plaintext_exposed` and `starttls_warning` are also NON-optional booleans.

**Pitfall 4 (RESEARCH.md):** react-router-dom v7 — use the existing flat `<Routes>` / `<Route>` pattern. Do NOT introduce `<Outlet />` or nested routes.

**Pitfall 3 (Tailwind JIT):** Not relevant in this plan — no `bg-[hsl(...)]` classes added here. (Those land in Plan 36-03.)

**Forward reference to MotionPage:** `App.tsx` imports `{ MotionPage } from "@/pages/motion"` even though that file does not exist until Plan 36-03 lands. This is intentional — it locks the symbol contract. `tsc -b` WILL fail at the end of Plan 36-02 with a "module not found" error on `@/pages/motion`. That is expected. Plan 36-03 will resolve it. Do not stub MotionPage here; let the executor of 36-03 define it.

Alternative if the executor prefers a clean `tsc -b` after 36-02: stub `src/dashboard/src/pages/motion.tsx` with a placeholder `export function MotionPage() { return null }` so 36-02 builds clean, then Plan 36-03 replaces the placeholder. Either approach is acceptable — note the chosen approach in the SUMMARY.
</critical_pitfalls>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend TypeScript types in api.ts (SubScores.data_in_motion + MotionFinding interface + ScanLatestResponse.motion_findings)</name>
  <files>src/dashboard/src/types/api.ts, src/dashboard/src/pages/executive.tsx, src/dashboard/src/components/sidebar.tsx, src/dashboard/src/App.tsx</files>
  <read_first>
    - src/dashboard/src/types/api.ts (full file — SubScores 1-7, IdentityFinding 80-91, ScanLatestResponse 99-108)
    - .planning/phases/36-dashboard-motion-tab/36-PATTERNS.md lines 152-195 (TS contract block)
    - .planning/phases/36-dashboard-motion-tab/36-UI-SPEC.md lines 244-282 (TS Interface Contract)
    - .planning/phases/36-dashboard-motion-tab/36-01-PLAN.md (Pydantic shapes — TS must mirror exactly)
  </read_first>
  <action>
    Edit `src/dashboard/src/types/api.ts` to:

    1. Add `data_in_motion: number` as the 6th field on `SubScores` (after `data_at_rest: number`). Field is NON-optional — server always returns an int.

    2. Add a new exported interface `MotionFinding` directly below `IdentityFinding`. Copy the IdentityFinding shape verbatim and add the two motion-specific fields at the end. Both `plaintext_exposed: boolean` and `starttls_warning: boolean` are NON-optional. `cert_not_after?: string` is the ISO date STRING (mirror Pydantic's `Optional[str]`, not a TS Date).

    3. Add `motion_findings: MotionFinding[]` to `ScanLatestResponse` directly below `identity_findings: IdentityFinding[]`. Field is NON-optional (server defaults to `[]`).

    Do NOT modify any other interface in this file. Do NOT alphabetize or reorder existing fields.

    Run `cd src/dashboard && npx tsc -b` — at this point the build should still fail only because `App.tsx` does not yet import `MotionPage` (that's Task 4). The types/api.ts file in isolation should compile clean.

    Commit:
    `feat(36-02): extend TS api.ts — SubScores.data_in_motion, MotionFinding, ScanLatestResponse.motion_findings (DASH-04, DASH-05)`
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK/src/dashboard && grep -c 'data_in_motion: number\|interface MotionFinding\|motion_findings: MotionFinding\[\]' src/types/api.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^\s*data_in_motion: number' src/dashboard/src/types/api.ts` == 1
    - `grep -c '^export interface MotionFinding' src/dashboard/src/types/api.ts` == 1
    - `grep -c 'motion_findings: MotionFinding\[\]' src/dashboard/src/types/api.ts` == 1
    - `grep -c 'plaintext_exposed: boolean' src/dashboard/src/types/api.ts` == 1
    - `grep -c 'starttls_warning: boolean' src/dashboard/src/types/api.ts` == 1
    - The two motion booleans do NOT have a `?` (non-optional)
  </acceptance_criteria>
  <done>types/api.ts exports SubScores.data_in_motion, MotionFinding interface, ScanLatestResponse.motion_findings; commit landed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add 6th ScoreGauge for Data in Motion in executive.tsx and bump loading skeleton count to 6</name>
  <files>src/dashboard/src/types/api.ts, src/dashboard/src/pages/executive.tsx, src/dashboard/src/components/sidebar.tsx, src/dashboard/src/App.tsx</files>
  <read_first>
    - src/dashboard/src/pages/executive.tsx (lines 60-67 loading skeleton; lines 128-151 gauge row)
    - src/dashboard/src/components/gauges/ScoreGauge.tsx (props: score, label, size?, strokeColor?, isOverall?)
    - .planning/phases/36-dashboard-motion-tab/36-PATTERNS.md lines 199-217 (executive.tsx pattern)
    - .planning/phases/36-dashboard-motion-tab/36-UI-SPEC.md lines 211-217 ("Executive Summary 6th gauge")
  </read_first>
  <action>
    Edit `src/dashboard/src/pages/executive.tsx` at two sites:

    1. **Loading skeleton (lines 60-67):** change `Array.from({ length: 5 })` to `Array.from({ length: 6 })`. Do NOT change the inner Skeleton classes (`h-32 w-32 rounded-full`).

    2. **Gauge flex-wrap row (~line 150):** add the 6th `<ScoreGauge>` line directly after the `data_at_rest` gauge. Position is LAST in the row per CONTEXT D-06.

       Insert this line verbatim:
       ```tsx
       <ScoreGauge score={score.subscores.data_in_motion} label="Data in Motion" size={120} />
       ```

    Do NOT change the outer `flex flex-wrap justify-around gap-8` container (UI-SPEC confirms it accommodates 6 gauges with no layout change). Do NOT introduce a wrapper div.

    Run `cd src/dashboard && npx tsc -b` — should compile clean for executive.tsx (the `data_in_motion` field exists on `SubScores` from Task 1). Build will still fail on App.tsx until Task 4.

    Commit:
    `feat(36-02): add Data in Motion ScoreGauge (6th) to executive summary; bump skeleton count (DASH-04)`
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && grep -c 'subscores.data_in_motion\|length: 6' src/dashboard/src/pages/executive.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'subscores\.data_in_motion' src/dashboard/src/pages/executive.tsx | grep -v '^#'` == 1
    - `grep -c 'label="Data in Motion"' src/dashboard/src/pages/executive.tsx | grep -v '^#'` == 1
    - `grep -c 'length: 6' src/dashboard/src/pages/executive.tsx | grep -v '^#'` ≥ 1 (skeleton bumped)
    - `grep -c 'length: 5' src/dashboard/src/pages/executive.tsx | grep -v '^#'` == 0 in the gauge-loading block (verify by inspecting the loading branch — no other length:5 should regress)
    - The new ScoreGauge appears AFTER `data_at_rest` ScoreGauge in source order (visual: last in row)
  </acceptance_criteria>
  <done>Executive summary renders 6 gauges; Data in Motion is last; skeleton count is 6; commit landed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add Motion sidebar nav entry with Activity icon between Identity and Certificates</name>
  <files>src/dashboard/src/types/api.ts, src/dashboard/src/pages/executive.tsx, src/dashboard/src/components/sidebar.tsx, src/dashboard/src/App.tsx</files>
  <read_first>
    - src/dashboard/src/components/sidebar.tsx (lines 5-13 lucide imports; lines 19-27 NAV_ITEMS)
    - .planning/phases/36-dashboard-motion-tab/36-PATTERNS.md lines 102-128 (sidebar pattern)
    - .planning/phases/36-dashboard-motion-tab/36-UI-SPEC.md lines 201-209 (Sidebar Navigation entry)
  </read_first>
  <action>
    Edit `src/dashboard/src/components/sidebar.tsx`:

    1. Add `Activity` to the `lucide-react` import block (lines 5-13). Append it to the named imports list — do not reorder existing names. Final block should read like:
       ```tsx
       import {
         LayoutDashboard, AlertTriangle, Shield, Database, GitBranch,
         Fingerprint, TrendingUp, Activity,
       } from "lucide-react"
       ```

    2. Insert the Motion NAV_ITEMS entry between the Identity entry and the Certificates entry (lines 19-27). Final NAV_ITEMS array order:
       ```tsx
       const NAV_ITEMS = [
         { path: "/", label: "Executive Summary", Icon: LayoutDashboard },
         { path: "/findings", label: "Findings", Icon: AlertTriangle },
         { path: "/identity", label: "Identity", Icon: Fingerprint },
         { path: "/motion", label: "Motion", Icon: Activity },          // NEW
         { path: "/certificates", label: "Certificates", Icon: Shield },
         { path: "/cbom", label: "CBOM Viewer", Icon: Database },
         { path: "/roadmap", label: "Migration Roadmap", Icon: GitBranch },
         { path: "/trends", label: "Trends", Icon: TrendingUp },
       ]
       ```

    Do NOT modify the Tooltip wrapper, the active-link styling, or the `min-h-[44px]` touch target. The icon convention (component import passed as `Icon` prop) is already established — Phase 36 follows it exactly.

    Commit:
    `feat(36-02): add Motion sidebar nav entry with Activity icon (DASH-01)`
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && grep -c '"/motion"\|Activity' src/dashboard/src/components/sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'path: "/motion", label: "Motion", Icon: Activity' src/dashboard/src/components/sidebar.tsx | grep -v '^#'` == 1
    - `grep -nE '\\bActivity\\b' src/dashboard/src/components/sidebar.tsx` shows `Activity` in BOTH the import block and the NAV_ITEMS array (≥ 2 lines)
    - The Motion entry is positioned between `Identity` and `Certificates` in NAV_ITEMS source order
    - Existing 7 nav entries are unchanged in label and order
  </acceptance_criteria>
  <done>Sidebar exposes 8 nav entries; Motion is position 4 with Activity icon; commit landed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Register /motion route in App.tsx (forward-imports MotionPage from Plan 36-03)</name>
  <files>src/dashboard/src/types/api.ts, src/dashboard/src/pages/executive.tsx, src/dashboard/src/components/sidebar.tsx, src/dashboard/src/App.tsx</files>
  <read_first>
    - src/dashboard/src/App.tsx (full file — imports lines 1-13, Routes block lines 27-36)
    - .planning/phases/36-dashboard-motion-tab/36-PATTERNS.md lines 132-148 (App.tsx pattern + Pitfall 4 reminder)
    - .planning/phases/36-dashboard-motion-tab/36-RESEARCH.md lines 311-315 (Pitfall 4 react-router-dom v7 flat routing)
  </read_first>
  <action>
    Edit `src/dashboard/src/App.tsx`:

    1. Add the page import alongside the existing page imports (the block around lines 7-13). Use the same alias-style import as other pages:
       ```tsx
       import { MotionPage } from "@/pages/motion"
       ```
       Verify the alias matches the convention used by existing pages — if other pages use named exports (`{ IdentityPage }`), match that. If they use default exports (`IdentityPage from "..."`), use the same. Inspect the file before editing.

    2. Insert the `<Route>` between `/identity` and `/certificates` in the existing flat `<Routes>` block (lines 27-36):
       ```tsx
       <Route path="/motion" element={<MotionPage />} />
       ```

    Do NOT introduce nested routes, `<Outlet />`, or layout wrappers (Pitfall 4: react-router-dom v7's `<Outlet />` does not apply here — keep flat siblings).

    **Note on TypeScript build state:** at the end of this task, `tsc -b` will fail with "Cannot find module '@/pages/motion'" because Plan 36-03 has not yet created that file. This is the expected handoff state. Optionally — and ONLY if the executor wants a clean build at the wave 2 boundary — create a placeholder `src/dashboard/src/pages/motion.tsx`:
    ```tsx
    export function MotionPage() { return null }
    ```
    Plan 36-03 will then replace the placeholder body. Note the chosen approach in SUMMARY.

    Commit:
    `feat(36-02): register /motion Route in App.tsx (forward-imports MotionPage from 36-03) (DASH-01)`
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && grep -c '"/motion"\|MotionPage' src/dashboard/src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'import.*MotionPage.*from.*pages/motion' src/dashboard/src/App.tsx | grep -v '^#'` == 1
    - `grep -c 'path="/motion"' src/dashboard/src/App.tsx | grep -v '^#'` == 1
    - `grep -c 'element=\\{<MotionPage' src/dashboard/src/App.tsx | grep -v '^#'` == 1
    - The Route is positioned between `/identity` and `/certificates` in source order
    - No `<Outlet />` introduced anywhere in App.tsx
  </acceptance_criteria>
  <done>App.tsx imports MotionPage and registers /motion route; commit landed; tsc -b status documented in SUMMARY (clean if placeholder used, expected red otherwise).</done>
</task>

</tasks>

<verification>
- All 4 source files modified; commits landed
- `grep -nE 'data_in_motion|MotionFinding|/motion|MotionPage' src/dashboard/src/{types/api.ts,pages/executive.tsx,components/sidebar.tsx,App.tsx}` shows hits in each file
- TS interface shape matches the Pydantic shape from Plan 36-01 byte-for-byte (field names, optionality)
- No new dependencies added to `package.json` (PEP-style minimal-diff principle applied to the dashboard)
- `tsc -b` build state at end of plan: documented in SUMMARY (red on missing motion.tsx unless placeholder used; that's resolved by Plan 36-03)
</verification>

<success_criteria>
1. `src/dashboard/src/types/api.ts` exports the extended SubScores, the new MotionFinding interface, and the extended ScanLatestResponse.
2. `src/dashboard/src/pages/executive.tsx` renders 6 ScoreGauges; Data in Motion is last; loading skeleton uses `length: 6`.
3. `src/dashboard/src/components/sidebar.tsx` NAV_ITEMS includes the Motion entry between Identity and Certificates with the `Activity` icon.
4. `src/dashboard/src/App.tsx` registers `<Route path="/motion" element={<MotionPage />} />` and imports MotionPage from `@/pages/motion`.
5. The TS shape mirrors the Pydantic shape from Plan 36-01 with no field-name drift.
</success_criteria>

<output>
After completion, create `.planning/phases/36-dashboard-motion-tab/36-02-SUMMARY.md` covering:
- types/api.ts diff summary (3 changes)
- executive.tsx changes (skeleton bump + 6th gauge)
- sidebar.tsx Motion entry + Activity import
- App.tsx Route + import
- tsc -b status at end (clean if placeholder used; red on missing motion.tsx otherwise — note which approach was taken)
- Confirmation no new dependencies added
</output>
