---
phase: 36-dashboard-motion-tab
plan: 04
type: execute
wave: 4
depends_on: [36-01, 36-02, 36-03]
files_modified:
  - docs/UAT-SERIES.md
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05]
tags: [docs, uat, obsidian, close-out]
user_setup:
  - service: docker
    why: "Manual UAT against v4.4 chaos labs (labs/email/, labs/broker/) — D-09"
    dashboard_config:
      - task: "Run `docker compose --profile email up` and `--profile broker up` from the labs/ directory"
        location: "Local Docker daemon"

must_haves:
  truths:
    - "docs/UAT-SERIES.md contains 5 new UAT cases (UAT-36-01..05) covering /motion route, STARTTLS badge, plaintext broker badge, 6 ScoreGauges, empty-state cards"
    - "docs/UAT-SERIES.md Last Updated header reflects phase-completion date"
    - "Obsidian phase note exists at /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-36-Dashboard-Motion-Tab.md with status: complete"
    - "Obsidian phase note follows the Phase-35 template (frontmatter + Goal + Requirements Covered + Success Criteria + What Was Built per plan + Out of Scope + Links)"
    - "UAT-SERIES.md synced to vault via the documented printf+cp pattern"
    - "Manual UAT cases UAT-36-01..05 sign-off captured in UAT-SERIES.md"
  artifacts:
    - path: "docs/UAT-SERIES.md"
      provides: "5 new UAT cases for Phase 36"
      contains: "UAT-36-01"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
      provides: "Vault-synced UAT-SERIES.md"
      contains: "UAT-36-01"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-36-Dashboard-Motion-Tab.md"
      provides: "Obsidian phase note (Phase-35 template structure)"
      contains: "type: phase"
  key_links:
    - from: "docs/UAT-SERIES.md"
      to: "Plan 36-03 motion.tsx visible behavior"
      via: "UAT-36-01..05 manual test cases"
      pattern: "UAT-36-0[1-5]"
    - from: "Obsidian phase note"
      to: "[[Roadmap]]"
      via: "wikilink section at end of note"
      pattern: '\\[\\[Roadmap\\]\\]'
---

<objective>
Close Phase 36: document the new manual UAT cases, run them against the chaos labs, sync UAT-SERIES.md to the Obsidian vault, write the Obsidian phase note, and commit. This plan satisfies CLAUDE.md §"Mandatory Phase Completion Steps" and CONTEXT.md D-10..D-12.

Purpose: Without explicit phase close-out, the Obsidian vault drifts from the repo and the UAT log loses Phase 36 evidence. The user has flagged docs+obsidian as a per-phase requirement (memory entry: feedback_obsidian_phase_notes).

Output: Updated `docs/UAT-SERIES.md` with 5 new cases dated and signed off; vault-synced copy at `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md`; new Obsidian phase note at `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-36-Dashboard-Motion-Tab.md`; commit landed via `gsd-tools.cjs commit`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-dashboard-motion-tab/36-CONTEXT.md
@.planning/phases/36-dashboard-motion-tab/36-RESEARCH.md
@.planning/phases/36-dashboard-motion-tab/36-VALIDATION.md
@.planning/phases/36-dashboard-motion-tab/36-01-PLAN.md
@.planning/phases/36-dashboard-motion-tab/36-02-PLAN.md
@.planning/phases/36-dashboard-motion-tab/36-03-PLAN.md
@docs/UAT-SERIES.md
@CLAUDE.md

<interfaces>
<!-- UAT case template — mirror existing UAT-35-NN block structure in docs/UAT-SERIES.md. -->
<!-- Read the existing file first to lock the exact format used by UAT-35. -->

The 5 new cases (D-11):

**UAT-36-01 — `/motion` route loads with both sections**
- Pre: `quirk serve` running; recent scan in DB (any host).
- Steps: Open `http://localhost:8000/motion`. Confirm page heading "Data in Motion". Confirm both `Email Protocols` and `Message Brokers` sections render (either as table or empty-state card).
- Pass: No console errors; both section headings visible.
- Maps to: DASH-01.

**UAT-36-02 — Port-25 STARTTLS warning badge renders**
- Pre: Run `docker compose --profile email up` from `labs/email/`. Run a deep scan against `localhost`. Open `/motion`.
- Steps: In the Email Protocols table, locate the port-25 row.
- Pass: Row shows the amber `⚠ STARTTLS` badge in the Warning column. Other rows (587, 465, etc.) do NOT show the badge.
- Maps to: DASH-02.

**UAT-36-03 — Plaintext broker shows `☠ PLAINTEXT` badge**
- Pre: Run `docker compose --profile broker up` from `labs/broker/`. Scan `localhost`. Open `/motion`.
- Steps: In the Message Brokers section, locate the Kafka subsection (KAFKA-PLAIN row, port 29092).
- Pass: Row shows the orange `☠ PLAINTEXT` badge in the Status column. Subsection title reads `Kafka · N endpoint(s) · 1 plaintext` (or higher).
- Maps to: DASH-03.

**UAT-36-04 — Executive summary shows 6 ScoreGauges**
- Pre: any scan with completed scoring.
- Steps: Open `/` (executive summary). Count the ScoreGauges in the flex-wrap row.
- Pass: 6 gauges visible; `Data in Motion` is the last gauge in the row; gauge displays an integer (not `NaN`, not blank).
- Maps to: DASH-04.

**UAT-36-05 — Empty-state cards render when no email/broker findings**
- Pre: Run a scan against a host with NEITHER email nor broker endpoints (e.g., a plain HTTPS-only target). Open `/motion`.
- Steps: Inspect both sections.
- Pass: Email section shows "No email endpoints scanned in this session — enable the email scanner in your config or scan a mail server." Broker section shows "No broker endpoints scanned in this session — enable the broker scanner in your config or scan a message broker host."
- Maps to: DASH-01, D-05.

Update the `**Last Updated:**` header at the top of UAT-SERIES.md to today's date.
</interfaces>

<obsidian_note_template>
File path: `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-36-Dashboard-Motion-Tab.md`

Write directly to the vault filesystem (file is too large for `obsidian CLI content=` per CLAUDE.md mandate).

Frontmatter (verbatim):

    (delimiter line: three dashes)
    project: QU.I.R.K.
    type: phase
    status: complete
    source: .planning/phases/36-dashboard-motion-tab/
    updated: <today YYYY-MM-DD>
    (delimiter line: three dashes)

Body sections (mirror Phase-35-CBOM-Integration.md):

1. `# Phase 36: Dashboard Motion Tab`
2. `## Goal` — one paragraph summarizing the dashboard surfacing of email + broker TLS posture.
3. `## Requirements Covered` — bullet list:
   - DASH-01 — New `/motion` route + sidebar entry
   - DASH-02 — Email per-port table + STARTTLS warning badge
   - DASH-03 — Broker grouped subsections (Kafka/AMQP/Redis) + plaintext flag + cloud chip
   - DASH-04 — Executive summary 6th ScoreGauge for Data in Motion
   - DASH-05 — `/api/scan/latest` carries `motion_findings: list[MotionFinding]`
4. `## Success Criteria` — 5 numbered criteria from ROADMAP Phase 36 entry.
5. `## What Was Built` — one `### Plan 36-NN — Title` subsection per plan (4 plans), sourced from each `36-NN-SUMMARY.md`. Pull 3-5 bullets per plan.
6. `## Out of Scope` — DEF-36-A through DEF-36-E from CONTEXT.md §"Deferred Ideas".
7. `## Links` — `[[Roadmap]]`, `[[Requirements]]`, `[[UAT-Series]]`, `[[_QUIRK-Hub]]`.
</obsidian_note_template>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append UAT-36-01..05 cases to docs/UAT-SERIES.md and bump Last Updated date</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (full file — locate the existing UAT-35-NN block to mirror its formatting; locate the `**Last Updated:**` header at the top)
    - .planning/phases/36-dashboard-motion-tab/36-CONTEXT.md (D-11 case requirements)
    - .planning/phases/36-dashboard-motion-tab/36-VALIDATION.md (manual-only verifications table)
  </read_first>
  <action>
    1. Open `docs/UAT-SERIES.md`.
    2. Update the `**Last Updated:**` line at the top of the file to today's date in YYYY-MM-DD format.
    3. Locate the end of the most-recent series block (UAT-35-NN per Phase 35 close-out).
    4. Append a new series block titled `## UAT-36 — Dashboard Motion Tab` (or follow whatever heading pattern UAT-35 uses — read the file first and mirror the exact format, including any "Series:" prefix or table structure).
    5. Add 5 cases UAT-36-01..05 with the content from the `<interfaces>` block above. Each case should have:
       - **Case ID:** UAT-36-NN
       - **Maps to requirement:** (DASH-NN)
       - **Pre-conditions:** (Docker / scan setup as listed)
       - **Steps:** (1, 2, 3...)
       - **Pass criteria:** (the bullet from <interfaces>)
       - **Status:** Pending until manual UAT in Task 2 changes it to Pass.
    6. Match the existing UAT-35 block's exact heading hierarchy and any tables/checkboxes used.

    Do NOT remove or modify any existing UAT case. Do NOT renumber.

    Commit:
    `docs(36-04): add UAT-36-01..05 cases for Dashboard Motion Tab (D-11)`
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && grep -cE '^[^#]*UAT-36-0[1-5]' docs/UAT-SERIES.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'UAT-36-0[1-5]' docs/UAT-SERIES.md` ≥ 5 (each case ID present at least once)
    - `grep -c 'UAT-36-01' docs/UAT-SERIES.md | grep -v '^#'` ≥ 1
    - `grep -c '⚠ STARTTLS\|☠ PLAINTEXT' docs/UAT-SERIES.md | grep -v '^#'` ≥ 2 (badge glyphs from UAT-36-02 and UAT-36-03)
    - `grep -c 'No email endpoints scanned\|No broker endpoints scanned' docs/UAT-SERIES.md | grep -v '^#'` ≥ 2 (UAT-36-05 empty-state copy)
    - `grep -nE '\\*\\*Last Updated:\\*\\*' docs/UAT-SERIES.md | head -1` shows today's date
    - Existing UAT-35-NN cases unchanged (sanity: `grep -c 'UAT-35-' docs/UAT-SERIES.md` returns the same count as before — record the before/after counts in SUMMARY)
  </acceptance_criteria>
  <done>UAT-36-01..05 appended; Last Updated bumped; commit landed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run UAT-36-01..05 manually against the chaos labs and record sign-off</name>
  <files>docs/UAT-SERIES.md</files>
  <action>Pause execution and run the manual UAT cases described in &lt;how-to-verify&gt;. After each case passes, update its Status field in docs/UAT-SERIES.md from Pending to Pass with today's date. If any case fails, capture the failure mode and STOP for triage.</action>
  <verify>Manual: each of UAT-36-01..05 has Status: Pass with today's date in docs/UAT-SERIES.md.</verify>
  <done>All 5 UAT cases marked Pass in docs/UAT-SERIES.md, OR a failure has been captured and a follow-up plan opened.</done>
  <what-built>
    Plans 36-01/02/03 delivered the backend Pydantic schema + derivation, the frontend types + sidebar + executive 6th gauge, and the Motion page itself. The 5 manual UAT cases written in Task 1 above need to be exercised by hand against the v4.4 chaos labs.
  </what-built>
  <how-to-verify>
    Pre-flight:
    1. `cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && cd src/dashboard && npm run build`
    2. Start the FastAPI server: `quirk serve` (or the project's standard dashboard launcher).

    UAT-36-01 — `/motion` route loads (no Docker required):
    - Open `http://localhost:8000/motion`. Confirm "Data in Motion" heading. Confirm both sections render. No console errors.

    UAT-36-02 — STARTTLS badge:
    - `cd labs/email && docker compose --profile email up -d`
    - Run a deep scan: `quirk scan --target localhost --profile deep` (or the project's invocation).
    - Reload `/motion`. Find port-25 row. Confirm `⚠ STARTTLS` amber badge present.

    UAT-36-03 — Plaintext broker badge:
    - `cd labs/broker && docker compose --profile broker up -d`
    - Run scan against `localhost`. Reload `/motion`. Find Kafka subsection. Confirm port-29092 row shows `☠ PLAINTEXT` orange badge.

    UAT-36-04 — 6 ScoreGauges:
    - Open `/` (executive summary). Count gauges in the flex-wrap row.
    - Pass if 6 visible, Data in Motion last, integer score displayed.

    UAT-36-05 — Empty-state cards:
    - Run a scan against a plain HTTPS-only host (e.g., `quirk scan --target example.com`).
    - Open `/motion`. Confirm both empty-state cards render with the locked copy.

    For each pass: update the Status of the UAT case in `docs/UAT-SERIES.md` from Pending → Pass with date and any notes. For any failure: capture the failure mode in UAT-SERIES.md and STOP — open a follow-up plan or fix.

    Tear down: `docker compose down` in each lab directory.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 cases pass and UAT-SERIES.md is updated, or describe failures observed.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Sync docs/UAT-SERIES.md to Obsidian vault using printf+cp pattern</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md</files>
  <read_first>
    - CLAUDE.md (sync workflow — "Sync UAT-SERIES.md to Obsidian" section with the printf+cp pattern)
    - docs/UAT-SERIES.md (source after Task 2 sign-off)
  </read_first>
  <action>
    Per CLAUDE.md (file is too large for `obsidian CLI content=`), write to the vault filesystem directly using the documented pattern. Today's date is the value of the `updated:` frontmatter key.

    Execute:
    ```bash
    DATE=$(date +%Y-%m-%d)
    printf "---\nproject: QU.I.R.K.\ntype: reference\nstatus: active\nsource: docs/UAT-SERIES.md\nupdated: ${DATE}\n---\n\n" > /tmp/uat_vault.md
    cat docs/UAT-SERIES.md >> /tmp/uat_vault.md
    cp /tmp/uat_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
    ```

    Verify the file landed and contains the new UAT-36 block.
  </action>
  <verify>
    <automated>grep -c 'UAT-36-0[1-5]' "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"` exits 0
    - `grep -cE 'UAT-36-0[1-5]' "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"` ≥ 5
    - First 7 lines of the vault file are the YAML frontmatter (`grep -c '^---' "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"` ≥ 2)
    - The vault file `updated:` line matches today's date
    - The vault file size is approximately the same as `docs/UAT-SERIES.md` plus the small frontmatter prefix
  </acceptance_criteria>
  <done>Vault UAT-Series.md updated and contains UAT-36-NN block.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create Obsidian phase note at /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-36-Dashboard-Motion-Tab.md</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-36-Dashboard-Motion-Tab.md</files>
  <read_first>
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-35-CBOM-Integration.md (template analog)
    - .planning/phases/36-dashboard-motion-tab/36-01-SUMMARY.md (Plan 36-01 outcome)
    - .planning/phases/36-dashboard-motion-tab/36-02-SUMMARY.md (Plan 36-02 outcome)
    - .planning/phases/36-dashboard-motion-tab/36-03-SUMMARY.md (Plan 36-03 outcome)
    - .planning/phases/36-dashboard-motion-tab/36-CONTEXT.md (Deferred Ideas DEF-36-A..E)
    - .planning/ROADMAP.md (Phase 36 entry — Goal + 5 success criteria)
    - CLAUDE.md (Mandatory Phase Completion Steps — Obsidian phase note template description)
  </read_first>
  <action>
    Write the file directly to the vault filesystem (per CLAUDE.md — file is too large for `obsidian CLI content=`):

    Path: `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-36-Dashboard-Motion-Tab.md`

    Use the structure from the `<obsidian_note_template>` block in this PLAN's `<context>`. Write the file in one Write tool call (no shell heredoc).

    Source content for the `## What Was Built` subsections:
    - Plan 36-01 — Backend wiring + Pitfall 1 fix (read 36-01-SUMMARY.md)
    - Plan 36-02 — Frontend types + executive 6th gauge + sidebar + route (read 36-02-SUMMARY.md)
    - Plan 36-03 — Motion page (read 36-03-SUMMARY.md)
    - Plan 36-04 — Close-out (this plan)

    Pull 3-5 concise bullets per plan from the SUMMARYs. Keep total length comparable to Phase-35 note (60-100 lines total).

    Frontmatter MUST include:
    ```
    ---
    project: QU.I.R.K.
    type: phase
    status: complete
    source: .planning/phases/36-dashboard-motion-tab/
    updated: <today YYYY-MM-DD>
    ---
    ```

    End with `## Links` section containing wikilinks: `[[Roadmap]]`, `[[Requirements]]`, `[[UAT-Series]]`, `[[_QUIRK-Hub]]`.
  </action>
  <verify>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-36-Dashboard-Motion-Tab.md" && grep -c 'status: complete\|DASH-01\|\[\[Roadmap\]\]' "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-36-Dashboard-Motion-Tab.md"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-36-Dashboard-Motion-Tab.md"` exits 0
    - `grep -c 'project: QU.I.R.K.' "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-36-Dashboard-Motion-Tab.md"` == 1
    - `grep -c 'type: phase' "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-36-Dashboard-Motion-Tab.md"` == 1
    - `grep -c 'status: complete' "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-36-Dashboard-Motion-Tab.md"` == 1
    - `grep -cE 'DASH-0[1-5]' "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-36-Dashboard-Motion-Tab.md"` ≥ 5 (all five requirement IDs)
    - `grep -c '\[\[Roadmap\]\]\|\[\[Requirements\]\]\|\[\[UAT-Series\]\]\|\[\[_QUIRK-Hub\]\]' "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-36-Dashboard-Motion-Tab.md"` ≥ 4
    - File has 6 sections: Goal, Requirements Covered, Success Criteria, What Was Built, Out of Scope, Links
    - `wc -l "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-36-Dashboard-Motion-Tab.md"` ≥ 50 lines
  </acceptance_criteria>
  <done>Phase-36 Obsidian note created with status: complete; commit not required (vault is not in git).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Commit docs/UAT-SERIES.md via gsd-tools.cjs commit</name>
  <files>docs/UAT-SERIES.md (git commit only)</files>
  <read_first>
    - CLAUDE.md (commit workflow — "Commit docs/UAT-SERIES.md" section)
    - docs/UAT-SERIES.md (post Task 1+2 state — has UAT-36-01..05 with sign-off)
  </read_first>
  <action>
    Run the documented commit command:
    ```bash
    cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK
    node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-36): update UAT-SERIES.md with Phase 36 cases (DASH-01..05)" --files docs/UAT-SERIES.md
    ```

    Verify:
    - `git log -1 --oneline -- docs/UAT-SERIES.md` shows the new commit
    - `git status docs/UAT-SERIES.md` reports clean working tree for that file

    Note: do NOT amend or force-push. If the commit fails (pre-commit hook), fix and create a new commit per CLAUDE.md / GSD safety protocol.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && git log -1 --oneline -- docs/UAT-SERIES.md</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=format:'%s' -- docs/UAT-SERIES.md` includes the substring `phase-36`
    - `git status --porcelain docs/UAT-SERIES.md` is empty (file committed)
    - Commit was created via `gsd-tools.cjs commit`, not a raw `git commit`
  </acceptance_criteria>
  <done>UAT-SERIES.md committed; phase close-out artifact landed in repo.</done>
</task>

</tasks>

<verification>
- All 5 UAT cases UAT-36-01..05 present in docs/UAT-SERIES.md and signed off (Pass)
- Vault UAT-Series.md mirrors docs/UAT-SERIES.md content with frontmatter
- Vault phase note Phase-36-Dashboard-Motion-Tab.md exists with status: complete
- docs/UAT-SERIES.md committed via gsd-tools.cjs
- Last Updated date in UAT-SERIES.md is today's date
</verification>

<success_criteria>
1. `docs/UAT-SERIES.md` contains UAT-36-01..05 with Pass status.
2. `docs/UAT-SERIES.md` Last Updated header is today.
3. `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` mirrors `docs/UAT-SERIES.md` with required frontmatter.
4. `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-36-Dashboard-Motion-Tab.md` exists with status: complete and the 6-section structure.
5. `docs/UAT-SERIES.md` committed via `gsd-tools.cjs commit` with a phase-36 message.
</success_criteria>

<output>
After completion, create `.planning/phases/36-dashboard-motion-tab/36-04-SUMMARY.md` covering:
- UAT cases added (with sign-off date)
- Vault sync confirmation (path + size sanity)
- Phase note path + section count
- Commit SHA / message
- Any UAT failures captured + follow-up plans created
</output>
