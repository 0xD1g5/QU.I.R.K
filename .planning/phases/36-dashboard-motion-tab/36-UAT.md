---
status: partial
phase: 36-dashboard-motion-tab
source:
  - .planning/phases/36-dashboard-motion-tab/36-01-SUMMARY.md
  - .planning/phases/36-dashboard-motion-tab/36-02-SUMMARY.md
  - .planning/phases/36-dashboard-motion-tab/36-03-SUMMARY.md
  - .planning/phases/36-dashboard-motion-tab/36-04-SUMMARY.md
started: 2026-04-28T00:00:00Z
updated: 2026-04-28T02:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Backend `/api/scan/latest` returns motion data
expected: GET /api/scan/latest returns 200 with `motion_findings` array and `score.subscores.data_in_motion` integer; findings include `plaintext_exposed`/`starttls_warning` booleans.
result: pass
note: "Verified at http://127.0.0.1:8512/api/scan/latest — 200 OK; motion_findings key present (len=0); score.subscores.data_in_motion=25; subscores has all 6 keys including data_in_motion. Findings-shape check (plaintext_exposed/starttls_warning) deferred to test 8/9 once chaos-lab data is loaded."

### 2. Executive page shows 6th "Data in Motion" gauge
expected: Open the dashboard root (/). The Executive page renders 6 ScoreGauge tiles in the top row. The 6th tile is labeled "Data in Motion" and shows a numeric score 0–100.
result: pass

### 3. Sidebar contains Motion entry
expected: Sidebar nav lists Motion between Identity and Certificates, with a lightning/activity-style icon. Clicking it navigates to /motion.
result: pass

### 4. /motion route renders MotionPage
expected: Visiting /motion shows a page with heading "Data in Motion" and two sections: "Email Protocols" and "Message Brokers". No blank page or 404.
result: pass

### 5. Email Protocols section renders findings or empty state
expected: If email findings exist (SMTP/IMAP/POP3 endpoints): a 7-column table (Port | Protocol | TLS Version | Cipher Suite | Cert Expiry | Quantum Risk | Warning). Severity badge shows next to protocol name. If none: a muted card with empty-state copy.
result: pass
note: "Empty-state path verified — motion_findings is empty in current scan."

### 6. Message Brokers section shows per-family cards
expected: Brokers grouped into Kafka, AMQP, Redis (in that fixed order). Each family card shows "{family} · N endpoint(s) · M plaintext" and a status pill — orange "AT RISK" if any plaintext, blue "OK" otherwise. Empty families are hidden.
result: pass
note: "Empty-state path verified — no broker findings in current scan."

### 7. STARTTLS warning badge gates on port 25
expected: SMTP-STARTTLS endpoint on port 25 shows a yellow ⚠ STARTTLS badge in the Warning column. The same protocol on port 587 shows no badge.
result: blocked
blocked_by: third-party
reason: "Docker Desktop containerd meta.db I/O error — cannot start labs/email lab. Requires Docker Desktop Clean/Purge data. Also covered by deferred UAT-36-01..05 in docs/UAT-SERIES.md (Phase 36-04)."

### 8. Plaintext broker badge appears for *-PLAIN
expected: A KAFKA-PLAIN/AMQP-PLAIN/REDIS-PLAIN endpoint shows a ☠ PLAINTEXT status badge in the broker row, and the TLS Version + Cipher Suite cells are empty.
result: blocked
blocked_by: third-party
reason: "Docker Desktop containerd meta.db I/O error — cannot start labs/broker lab. Requires Docker Desktop Clean/Purge data. Also covered by deferred UAT-36-01..05 in docs/UAT-SERIES.md (Phase 36-04)."

### 9. AMQPS/Azure-ServiceBus preserves slash + shows cloud chip
expected: An AMQPS/Azure-ServiceBus endpoint renders the Protocol cell verbatim with the slash ("AMQPS/Azure-ServiceBus", not normalized). The Status column also shows a ☁ Azure-ServiceBus cloud chip.
result: blocked
blocked_by: third-party
reason: "Requires real Azure Service Bus endpoint (not generated by local broker lab). Local lab also blocked by Docker Desktop meta.db I/O error. Defer to live-environment UAT."

## Summary

total: 9
passed: 6
issues: 0
pending: 0
skipped: 0
blocked: 3

## Gaps

[none yet]
