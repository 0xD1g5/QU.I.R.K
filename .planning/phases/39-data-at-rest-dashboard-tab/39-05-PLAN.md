---
phase: 39
plan: 05
type: execute
wave: 4
depends_on: [39-01, 39-02, 39-03, 39-04]
files_modified:
  - docs/UAT-SERIES.md
autonomous: false
requirements: [GAP-04]
threat_refs: []

must_haves:
  truths:
    - "Full pytest suite green; python -m compileall quirk green; npm run build green"
    - "User has manually verified /data-at-rest renders with zero browser console errors and zero React warnings"
    - "User has manually verified per-section empty states render when no DAR data exists"
    - "User has manually verified sidebar nav order matches D-11 (Executive · Findings · Identity · Motion · Data at Rest · Certificates · CBOM · Roadmap · Trends)"
    - "docs/UAT-SERIES.md is updated with the new /data-at-rest tab and committed"
    - "Obsidian phase note exists at /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-39-Data-At-Rest-Dashboard-Tab.md"
    - "docs/UAT-SERIES.md is synced to Obsidian vault"
  artifacts:
    - path: "docs/UAT-SERIES.md"
      provides: "Updated UAT series referencing /data-at-rest"
      contains: "Data at Rest"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-39-Data-At-Rest-Dashboard-Tab.md"
      provides: "Obsidian phase completion note"
      contains: "status: complete"
  key_links:
    - from: "docs/UAT-SERIES.md"
      to: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
      via: "vault filesystem cp"
      pattern: "20_Dev-Work/QUIRK/UAT-Series.md"
---

<objective>
Wave 4 — phase-completion validation gate. Execute the full validation matrix (pytest, compileall, npm build), the human-only console-error gate, the locked sidebar order check, then perform the mandatory CLAUDE.md phase-completion steps: update UAT-SERIES.md, sync to Obsidian, write the Phase 39 Obsidian note, commit.

Purpose: Close GAP-04 with a clean validation receipt and the documentation/sync rituals that CLAUDE.md mandates for every phase.

Output: Validation evidence + UAT-SERIES.md updated + Obsidian vault synced + phase note created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-data-at-rest-dashboard-tab/39-CONTEXT.md
@.planning/phases/39-data-at-rest-dashboard-tab/39-VALIDATION.md
@.planning/phases/39-data-at-rest-dashboard-tab/39-UI-SPEC.md
@CLAUDE.md
@docs/UAT-SERIES.md
@.planning/phases/39-data-at-rest-dashboard-tab/39-01-SUMMARY.md
@.planning/phases/39-data-at-rest-dashboard-tab/39-02-SUMMARY.md
@.planning/phases/39-data-at-rest-dashboard-tab/39-03-SUMMARY.md
@.planning/phases/39-data-at-rest-dashboard-tab/39-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated validation matrix</name>
  <files>(verification only — no file changes)</files>
  <read_first>
    - .planning/phases/39-data-at-rest-dashboard-tab/39-VALIDATION.md
    - CLAUDE.md (Code Standards: PEP 8, run `python -m compileall` and relevant tests)
  </read_first>
  <action>
    Run, in order, and record output for each:

    1. `python -m compileall quirk` — must exit 0
    2. `python -m compileall tests` — must exit 0
    3. `pytest tests/test_dar_dashboard.py -x -q` — all 8 DAR tests GREEN
    4. `pytest -x -q` — full backend suite GREEN (no regressions)
    5. `cd src/dashboard && npm run build` — exits 0; tail of output captured
    6. `grep -n "/data-at-rest" src/dashboard/src/App.tsx src/dashboard/src/components/sidebar.tsx` — both files contain the route/label

    If any step fails, STOP and surface the failure to the user before proceeding to Task 2.
  </action>
  <verify>
    <automated>python -m compileall quirk &amp;&amp; pytest tests/test_dar_dashboard.py -x -q &amp;&amp; pytest -x -q &amp;&amp; (cd src/dashboard &amp;&amp; npm run build)</automated>
  </verify>
  <acceptance_criteria>
    - `python -m compileall quirk` exits 0
    - `pytest tests/test_dar_dashboard.py -x -q` exits 0
    - `pytest -x -q` exits 0
    - `cd src/dashboard && npm run build` exits 0
    - Route+nav grep shows `/data-at-rest` in both App.tsx and sidebar.tsx
  </acceptance_criteria>
  <done>
    All automated gates green; ready for manual console-error checkpoint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual /data-at-rest console-error + nav-order verification</name>
  <what-built>
    Phase 39 has shipped: backend DarFinding schema + projection, frontend DataAtRestPage with ScoreGauge and four category tables, sidebar nav entry with HardDrive icon, route /data-at-rest. All automated gates are green from Task 1.
  </what-built>
  <how-to-verify>
    1. In a terminal: `cd src/dashboard && npm run dev` — wait for "ready" (Vite default port).
    2. Open the dashboard URL printed by Vite (typically `http://localhost:5173`).
    3. Open browser DevTools (Cmd+Option+I on macOS) and select the **Console** tab. Clear the console.
    4. Click the **Data at Rest** entry in the sidebar (it should have a HardDrive icon and sit between Motion and Certificates).
    5. Verify the URL becomes `/data-at-rest` and the page renders:
       - h1 "Data at Rest"
       - A ScoreGauge labeled "Data at Rest"
       - Four sections in this exact order: **Database Encryption · Object Storage · Kubernetes Secrets · Vault**
       - Each section either shows its EmptyStateCard with the locked copy from UI-SPEC, or a category-specific table
    6. **Console gate:** verify the Console shows **zero errors and zero React warnings** during page load and after switching to another tab and back.
    7. **Nav order gate:** verify the sidebar (top to bottom) reads: Executive Summary · Findings · Identity · Motion · **Data at Rest** · Certificates · CBOM Viewer · Migration Roadmap · Trends.
    8. **Empty state gate:** if your current scan has no DAR endpoints, every section shows an EmptyStateCard with copy that names the category and the relevant scanner config (e.g., "No database endpoints scanned in this session — enable the DB scanner...").

    If any of the above fails, type a description of the issue and stop. Otherwise type `approved`.
  </how-to-verify>
  <files>(no file edits — manual verification only)</files>
  <action>Wait for the user to perform the verification steps in &lt;how-to-verify&gt; above. The user will type "approved" to resume, or describe any issue found (in which case STOP and surface the issue before proceeding).</action>
  <verify>User signs off with "approved" via &lt;resume-signal&gt;</verify>
  <done>User has confirmed zero console errors, locked sidebar nav order, locked section order, and per-section empty states render correctly.</done>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update docs/UAT-SERIES.md with Phase 39 changes</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (full file — locate version-string occurrences and the `**Last Updated:**` line)
    - CLAUDE.md (Mandatory Phase Completion Steps §2)
    - .planning/phases/39-data-at-rest-dashboard-tab/39-UI-SPEC.md §Page Layout, §States Contract (for new test cases)
  </read_first>
  <action>
    Update `docs/UAT-SERIES.md`:

    1. Update the `**Last Updated:**` date at the top of the document to today's date (2026-04-29 or current date if later).

    2. If a UAT series exists for the dashboard, add new test cases under it (otherwise insert a new "UAT-39 — Data at Rest Tab" series block alongside existing series). Add at minimum:
       - **UAT-39-01:** "Navigate to /data-at-rest from sidebar" — Pre: dashboard running. Steps: click Data at Rest in sidebar. Pass: URL is /data-at-rest, page renders with h1 "Data at Rest" and ScoreGauge, no console errors.
       - **UAT-39-02:** "Empty state per section when no DAR data" — Pre: scan with all DAR scanners disabled. Pass: each of the 4 sections renders its EmptyStateCard with locked copy; page does not crash.
       - **UAT-39-03:** "Database table renders with locked columns" — Pre: scan with PostgreSQL/MySQL/RDS findings. Pass: DatabaseTable shows columns Engine · Host · Port · Severity · Title · Encryption at Rest · TLS in Transit · Quantum Risk · Remediation; severity-sorted.
       - **UAT-39-04:** "Object Storage table renders with locked columns" — Pre: scan with S3 / Azure Blob findings. Pass: ObjectStorageTable shows columns Provider · Host · Severity · Title · Encryption Mode · Public Access · KMS Key · Versioning · Quantum Risk · Remediation; nulls render as em-dash.
       - **UAT-39-05:** "Kubernetes table renders with locked columns" — Pre: scan with K8s namespace + cluster-encryption findings. Pass: KubernetesTable shows Namespace · Host · Severity · Title · Secret Type · Encryption Provider · Quantum Risk · Remediation.
       - **UAT-39-06:** "Vault table renders with locked columns" — Pre: scan with Vault transit/PKI/auth findings. Pass: VaultTable shows Host · Severity · Title · Mount Type · Seal Type · Auto-Unseal · Quantum Risk · Remediation; Seal Type and Auto-Unseal show em-dash for current scanner output (not probed).
       - **UAT-39-07:** "Sidebar nav order" — Pass: order is Executive · Findings · Identity · Motion · Data at Rest · Certificates · CBOM · Roadmap · Trends.
       - **UAT-39-08:** "Zero console errors gate" — Pass: navigate to /data-at-rest and switch to another tab and back; DevTools Console shows zero errors and zero React warnings.

    3. Match the existing UAT-SERIES.md formatting (use the same heading levels, table or bullet style, ID format) — do not introduce a new format. Read 30–60 lines of an existing series first to confirm style.

    Per CLAUDE.md: minimal diff. Per CLAUDE.md §3: this file will also be synced to Obsidian in Task 5.
  </action>
  <verify>
    <automated>grep -c "UAT-39-" docs/UAT-SERIES.md | awk '$1 >= 8 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "UAT-39-" docs/UAT-SERIES.md` returns >= 8
    - `grep -q "Data at Rest" docs/UAT-SERIES.md`
    - `grep -q "/data-at-rest" docs/UAT-SERIES.md`
    - `grep -q "Last Updated" docs/UAT-SERIES.md` (date string updated)
  </acceptance_criteria>
  <done>
    UAT-SERIES.md updated with 8+ Phase 39 test cases and current Last Updated date.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create Obsidian phase note</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-39-Data-At-Rest-Dashboard-Tab.md</files>
  <read_first>
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/ (list existing files for naming convention)
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-38-*.md if it exists (template pattern)
    - .planning/phases/39-data-at-rest-dashboard-tab/39-01-SUMMARY.md
    - .planning/phases/39-data-at-rest-dashboard-tab/39-02-SUMMARY.md
    - .planning/phases/39-data-at-rest-dashboard-tab/39-03-SUMMARY.md
    - .planning/phases/39-data-at-rest-dashboard-tab/39-04-SUMMARY.md
    - CLAUDE.md (Mandatory Phase Completion Steps §1)
  </read_first>
  <action>
    Use the **Write** tool (NOT obsidian CLI `content=` — file is too large for shell expansion per CLAUDE.md) to create the Obsidian phase note at:

    `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-39-Data-At-Rest-Dashboard-Tab.md`

    Content structure (mirror Phase 38 template):

    ```markdown
    ---
    project: QU.I.R.K.
    type: phase
    status: complete
    source: .planning/phases/39-data-at-rest-dashboard-tab/
    updated: <today's date YYYY-MM-DD>
    ---

    # Phase 39 — Data at Rest Dashboard Tab

    ## Goal
    The React dashboard has a "Data at Rest" tab that surfaces DB encryption, object storage policy, Kubernetes secrets, and Vault findings from the existing v4.3 data shape — consultants can review the full DAR surface without leaving the dashboard.

    ## Requirements Covered
    - **GAP-04** — User opens the dashboard and sees a "Data at Rest" tab alongside the existing tabs, listing DB / object storage / K8s secrets / Vault findings with the existing v4.3 data shape.

    ## Success Criteria
    1. A "Data at Rest" tab is visible in the dashboard navigation.
    2. The tab displays per-category sections for database encryption, object storage, Kubernetes secrets, and Vault findings.
    3. Empty state is shown when no DAR scan data exists.
    4. The DAR tab route appears in the browser console with zero errors.

    ## What Was Built

    ### Plan 01 — Wave 0 Test Scaffold
    [extract from 39-01-SUMMARY.md — what was built, files touched]

    ### Plan 02 — Backend Schema + Projection
    [extract from 39-02-SUMMARY.md]

    ### Plan 03 — Frontend Skeleton + Routing/Nav Lockstep
    [extract from 39-03-SUMMARY.md]

    ### Plan 04 — Per-Category Table Components
    [extract from 39-04-SUMMARY.md]

    ### Plan 05 — Validation, Docs, Sync
    [validation receipts + UAT-SERIES.md update + Obsidian sync]

    ## Closes
    - DASH-05 (deferred from Phase 27)

    ## Links
    - [[Roadmap]]
    - [[Requirements]]
    - [[UAT-Series]]
    ```

    Replace each `[extract from ...]` placeholder with a 3-5 bullet summary read from the corresponding SUMMARY.md (which exists at this point because each prior plan emits one in its `<output>` step).

    Per CLAUDE.md §Vault Targeting: `vault="Digs"`, `path="20_Dev-Work/QUIRK/Phases/..."`. Since we use the filesystem (Write tool) directly, the path is the absolute vault path. Do not use the obsidian CLI `content=` parameter (CLAUDE.md explicit guidance).
  </action>
  <verify>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-39-Data-At-Rest-Dashboard-Tab.md"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-39-Data-At-Rest-Dashboard-Tab.md"` exits 0
    - `grep -q "status: complete" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-39-Data-At-Rest-Dashboard-Tab.md"`
    - `grep -q "type: phase" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-39-Data-At-Rest-Dashboard-Tab.md"`
    - `grep -q "GAP-04" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-39-Data-At-Rest-Dashboard-Tab.md"`
    - `grep -q "DASH-05" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-39-Data-At-Rest-Dashboard-Tab.md"`
    - `grep -q "\[\[Roadmap\]\]" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-39-Data-At-Rest-Dashboard-Tab.md"`
  </acceptance_criteria>
  <done>
    Obsidian phase note exists with required frontmatter, Goal, Requirements, Success Criteria, What Was Built (one subsection per plan), and Roadmap link.
  </done>
</task>

<task type="auto">
  <name>Task 5: Sync UAT-SERIES.md to Obsidian vault</name>
  <files>(vault filesystem write — outside repo)</files>
  <read_first>
    - CLAUDE.md (Mandatory Phase Completion Steps §3)
    - docs/UAT-SERIES.md (current state from Task 3)
  </read_first>
  <action>
    Run the exact CLAUDE.md §3 sync recipe:

    ```bash
    printf "---\nproject: QU.I.R.K.\ntype: reference\nstatus: active\nsource: docs/UAT-SERIES.md\nupdated: $(date +%Y-%m-%d)\n---\n\n" > /tmp/uat_vault.md
    cat docs/UAT-SERIES.md >> /tmp/uat_vault.md
    cp /tmp/uat_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
    ```

    The vault filename is `UAT-Series.md` (not `UAT-SERIES.md`) per CLAUDE.md §3.
  </action>
  <verify>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" &amp;&amp; head -2 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" | grep -q "project: QU.I.R.K."</automated>
  </verify>
  <acceptance_criteria>
    - `test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"` exits 0
    - `head -1 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"` contains `---` (frontmatter present)
    - `grep -q "UAT-39-" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"` (Phase 39 cases synced)
    - `grep -q "source: docs/UAT-SERIES.md" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"`
  </acceptance_criteria>
  <done>
    Obsidian UAT-Series.md mirrors docs/UAT-SERIES.md with project frontmatter prepended.
  </done>
</task>

<task type="auto">
  <name>Task 6: Commit docs/UAT-SERIES.md per CLAUDE.md §4</name>
  <files>(git only — no new files)</files>
  <read_first>
    - CLAUDE.md (Mandatory Phase Completion Steps §4)
  </read_first>
  <action>
    Run the exact CLAUDE.md §4 commit recipe:

    ```bash
    node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-39): update UAT-SERIES.md" --files docs/UAT-SERIES.md
    ```

    Then verify with `git log --oneline -1` that the commit landed on the current branch.
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -q "phase-39"</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1` shows a commit referencing phase-39 and UAT-SERIES.md
    - `git status --porcelain docs/UAT-SERIES.md` shows clean (no uncommitted changes)
  </acceptance_criteria>
  <done>
    UAT-SERIES.md committed with the canonical CLAUDE.md commit message.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Validation + docs only — no new runtime surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (n/a) | — | docs + validation | accept | This plan only runs tests, updates documentation, and writes to the local Obsidian vault. No new runtime trust boundaries are created. |
</threat_model>

<verification>
- All automated gates green
- Manual console-error gate signed off by user
- UAT-SERIES.md updated, synced, and committed
- Obsidian phase note exists
</verification>

<success_criteria>
Phase 39 complete: GAP-04 closed, DASH-05 deferred-from-Phase-27 shipped, all CLAUDE.md mandatory completion steps performed.
</success_criteria>

<output>
After completion, create `.planning/phases/39-data-at-rest-dashboard-tab/39-05-SUMMARY.md` recording validation evidence + Obsidian links + UAT-SERIES.md commit hash.
</output>
