---
phase: 46-tls-finding-gaps
plan: 04
type: execute
wave: 3
depends_on: [46-01, 46-02, 46-03]
files_modified:
  - docs/UAT-SERIES.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-46-TLS-Finding-Gaps.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/_QUIRK-Hub.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Roadmap.md
autonomous: false
requirements: [TLS-FIND-01, TLS-FIND-02, TLS-FIND-03, TLS-FIND-04, TLS-FIND-05, TLS-FIND-06, TLS-FIND-07]
tags: [docs, uat, obsidian, sync]

must_haves:
  truths:
    - "docs/UAT-SERIES.md contains a TLS cert-defect test series exercising the four endpoints on ports 13444-13447"
    - "docs/UAT-SERIES.md header `**Last Updated:**` reflects the Phase 46 completion date"
    - "Obsidian vault file UAT-Series.md mirrors docs/UAT-SERIES.md content"
    - "Obsidian vault file Phase-46-TLS-Finding-Gaps.md exists with status: complete (or active during execution)"
    - "Roadmap.md and _QUIRK-Hub.md in the vault reflect Phase 46 completion"
    - "Live-fire end-to-end run (Plan 46-03 chaos lab + Plan 46-01/02 scanner) produces 4 distinct findings with severities CRITICAL/HIGH/MEDIUM/HIGH"
  artifacts:
    - path: "docs/UAT-SERIES.md"
      provides: "Updated TLS series with cert-defect test cases (UAT-46-01 through UAT-46-04+)"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-46-TLS-Finding-Gaps.md"
      provides: "Obsidian phase note with frontmatter, goal, requirements, success criteria, what-was-built sections"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
      provides: "Vault mirror of docs/UAT-SERIES.md"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/_QUIRK-Hub.md"
      provides: "Updated MOC reflecting Phase 46 entry"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Roadmap.md"
      provides: "Synced Roadmap with Phase 46 marked complete"
  key_links:
    - from: "docs/UAT-SERIES.md"
      to: ".planning/phases/46-tls-finding-gaps/46-03-SUMMARY.md"
      via: "test cases reference the chaos lab profile and ports"
      pattern: "tls-cert-defects|1344[4-7]"
---

<objective>
Documentation, UAT, and Obsidian sync per CLAUDE.md "Mandatory Phase Completion Steps" and the project memory rules (`feedback_uat_series_update.md`, `feedback_obsidian_phase_notes.md`, `feedback_docs_obsidian_per_phase.md`). Also performs the live-fire end-to-end smoke test that closes TLS-FIND-07's UAT criterion: scanning the running `tls-cert-defects` profile must produce all four expected findings with the correct severities.

Purpose: Closes the doc-sync loop and produces the human-readable evidence (UAT cases, Obsidian phase note) that operators consume.

Output: Updated UAT-SERIES.md, vault UAT-Series.md mirror, vault Phase-46-TLS-Finding-Gaps.md note, hub + roadmap refresh, end-to-end finding-set verified against the live chaos lab profile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-tls-finding-gaps/46-01-SUMMARY.md
@.planning/phases/46-tls-finding-gaps/46-02-SUMMARY.md
@.planning/phases/46-tls-finding-gaps/46-03-SUMMARY.md
@CLAUDE.md
@docs/UAT-SERIES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/UAT-SERIES.md with Phase 46 cert-defect test cases</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (full file; understand the test-case ID/format/header conventions)
    - .planning/phases/46-tls-finding-gaps/46-03-SUMMARY.md (chaos lab boot evidence)
    - .planning/phases/46-tls-finding-gaps/46-02-SUMMARY.md (severity matrix)
  </read_first>
  <action>
    1. Read `docs/UAT-SERIES.md` and locate the relevant TLS test series (or the most appropriate series for cert-defect findings). If a TLS-specific series exists, extend it. Otherwise add a new "Phase 46 — TLS Cert Defects" series.

    2. Add at least 4 test cases (one per endpoint), e.g. `UAT-46-01` through `UAT-46-04`, structured per the file's existing pattern. Each test case:
       - Pre-condition: chaos lab `tls-cert-defects` profile up via `PROFILE_ARGS="--profile tls-cert-defects" ./lab.sh up`
       - Action: `quirk scan localhost:<port>` for ports 13444, 13445, 13446, 13447
       - Pass criteria:
         - UAT-46-01 (port 13444): report contains a CRITICAL finding with title matching "expired" (TLS-FIND-01)
         - UAT-46-02 (port 13445): report contains a HIGH finding with title matching "self-signed" and NO untrusted-CA finding on the same endpoint (D-04 exclusivity, TLS-FIND-02)
         - UAT-46-03 (port 13446): report contains a MEDIUM finding with title matching "untrusted CA" (TLS-FIND-03)
         - UAT-46-04 (port 13447): report contains a HIGH finding with title matching "RSA" or "Undersized" key (TLS-FIND-04)

    3. Add at least one combined test case (UAT-46-05) covering D-02 multi-finding behavior: a single endpoint with multiple defects produces one finding per class with no rollup.

    4. Update the document header `**Last Updated:**` to `2026-05-03` (today).

    5. If the document has a section listing version-bumped pass criteria or output paths affected by Phase 46, update those entries (CLAUDE.md mandatory step #2).

    Per CLAUDE.md "Mandatory Phase Completion Steps → 2. Update docs/UAT-SERIES.md" and project memory `feedback_uat_series_update.md`.
  </action>
  <verify>
    <automated>
grep -c "tls-cert-defects\|13444\|13445\|13446\|13447" docs/UAT-SERIES.md | awk '$1 >= 4 { exit 0 } { exit 1 }' &&
grep -c "UAT-46-0[1-4]" docs/UAT-SERIES.md | awk '$1 >= 4 { exit 0 } { exit 1 }' &&
grep -c "Last Updated.*2026-05" docs/UAT-SERIES.md | awk '$1 >= 1 { exit 0 } { exit 1 }'
    </automated>
  </verify>
  <acceptance_criteria>
    - At least 4 new UAT-46-XX test cases reference ports 13444-13447 or the `tls-cert-defects` profile
    - Document header updated with current date
    - Markdown linting (if any) passes — file is well-formed markdown
  </acceptance_criteria>
  <done>UAT-SERIES.md contains Phase 46 cert-defect test cases mapped to TLS-FIND-01..05 and updated header.</done>
</task>

<task type="auto">
  <name>Task 2: Sync UAT-SERIES.md to Obsidian vault</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md</files>
  <read_first>
    - docs/UAT-SERIES.md (the just-updated content)
    - CLAUDE.md "3. Sync UAT-SERIES.md to Obsidian" section (the exact bash recipe)
  </read_first>
  <action>
    Per CLAUDE.md mandatory step 3, run the documented filesystem-write recipe (file is too large for `obsidian content=` parameter):

    ```bash
    printf "---\nproject: QU.I.R.K.\ntype: reference\nstatus: active\nsource: docs/UAT-SERIES.md\nupdated: 2026-05-03\n---\n\n" > /tmp/uat_vault.md
    cat docs/UAT-SERIES.md >> /tmp/uat_vault.md
    cp /tmp/uat_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
    ```

    Use Bash tool. Do NOT use the `obsidian content=...` form for this file.
  </action>
  <verify>
    <automated>
test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" &&
head -1 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" | grep -q "^---" &&
grep -c "tls-cert-defects\|UAT-46-0" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" | awk '$1 >= 4 { exit 0 } { exit 1 }'
    </automated>
  </verify>
  <acceptance_criteria>
    - Vault file exists with frontmatter (`project: QU.I.R.K.`, `source: docs/UAT-SERIES.md`, `updated: 2026-05-03`)
    - Vault file content matches `docs/UAT-SERIES.md` body (post-frontmatter)
    - Phase 46 test case IDs and `tls-cert-defects` references appear in the vault file
  </acceptance_criteria>
  <done>Vault UAT-Series.md mirrors docs/UAT-SERIES.md with correct frontmatter.</done>
</task>

<task type="auto">
  <name>Task 3: Create Obsidian Phase 46 note</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-46-TLS-Finding-Gaps.md</files>
  <read_first>
    - .planning/phases/46-tls-finding-gaps/46-01-SUMMARY.md
    - .planning/phases/46-tls-finding-gaps/46-02-SUMMARY.md
    - .planning/phases/46-tls-finding-gaps/46-03-SUMMARY.md
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-45-Install-Day-UX.md (template pattern reference; if absent, reference Phase-44 or any recent phase note)
    - .planning/ROADMAP.md (Phase 46 entry — goal, requirements, success criteria)
    - CLAUDE.md "1. Create Obsidian Phase Note" section
  </read_first>
  <action>
    Write directly to the vault filesystem (do NOT use `obsidian content=` — files too large for shell expansion). Use the Write tool against the absolute path:

    `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-46-TLS-Finding-Gaps.md`

    Content structure (follow the template pattern of recent Phase notes in the vault):

    ```markdown
    ---
    project: QU.I.R.K.
    type: phase
    status: complete
    source: .planning/phases/46-tls-finding-gaps/
    updated: 2026-05-03
    ---

    # Phase 46 — TLS Finding Gaps

    ## Goal
    [Copy from .planning/ROADMAP.md Phase 46 entry, line 917]

    ## Requirements Covered
    - TLS-FIND-01 — Expired cert → CRITICAL finding
    - TLS-FIND-02 — Self-signed cert → HIGH finding
    - TLS-FIND-03 — Untrusted-CA cert → MEDIUM finding
    - TLS-FIND-04 — RSA<2048 → HIGH finding
    - TLS-FIND-05 — EC<256 → HIGH finding
    - TLS-FIND-06 — sslyze → basic-ssl fallback wiring
    - TLS-FIND-07 — `tls-cert-defects` chaos lab profile

    ## Success Criteria
    [Copy from ROADMAP.md Phase 46 success-criteria block]

    ## What Was Built

    ### Plan 46-01 — Schema + scanner wiring
    [1-3 sentences from 46-01-SUMMARY.md]

    ### Plan 46-02 — Risk engine: severities + D-04 split
    [1-3 sentences from 46-02-SUMMARY.md]

    ### Plan 46-03 — Chaos lab `tls-cert-defects` profile
    [1-3 sentences from 46-03-SUMMARY.md]

    ### Plan 46-04 — Documentation + Obsidian sync
    UAT-SERIES.md updated with UAT-46-01..05 test cases. Vault UAT-Series.md and this phase
    note synced. Roadmap and Hub refreshed.

    ## Links
    [[Roadmap]]
    [[Requirements]]
    [[UAT-Series]]
    ```
  </action>
  <verify>
    <automated>
test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-46-TLS-Finding-Gaps.md" &&
head -10 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-46-TLS-Finding-Gaps.md" | grep -q "type: phase" &&
grep -c "TLS-FIND-0[1-7]" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-46-TLS-Finding-Gaps.md" | awk '$1 >= 7 { exit 0 } { exit 1 }'
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at the absolute vault path
    - Frontmatter present and valid (project/type/status/source/updated)
    - All 7 TLS-FIND-XX requirement IDs referenced
    - Per-plan "What Was Built" subsections drawn from the plan SUMMARYs
    - `[[Roadmap]]` wikilink present
  </acceptance_criteria>
  <done>Obsidian phase note created with frontmatter, requirement coverage, and per-plan summary.</done>
</task>

<task type="auto">
  <name>Task 4: Sync Roadmap and Hub to vault</name>
  <files>
    /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Roadmap.md,
    /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/_QUIRK-Hub.md
  </files>
  <read_first>
    - .planning/ROADMAP.md (must reflect Phase 46 done — verify Phase 46 line in tracking table is updated)
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/_QUIRK-Hub.md (current MOC structure)
    - CLAUDE.md "Sync Workflows → Roadmap sync" and "Hub note" sections
  </read_first>
  <action>
    1. Sync the Roadmap to the vault. Read `.planning/ROADMAP.md`, prepend frontmatter, write to vault. Use the obsidian CLI per CLAUDE.md or write directly via filesystem (whichever is reliable for this size):

       ```bash
       printf "---\nproject: QU.I.R.K.\ntype: roadmap\nstatus: active\nsource: .planning/ROADMAP.md\nupdated: 2026-05-03\n---\n\n" > /tmp/roadmap_vault.md
       cat .planning/ROADMAP.md >> /tmp/roadmap_vault.md
       cp /tmp/roadmap_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Roadmap.md"
       ```

    2. Update `_QUIRK-Hub.md` to include a wikilink to `[[Phase-46-TLS-Finding-Gaps]]` in the Phases section, and update the "Current Phase" callout (if Phase 46 is the most recent completed) to point to Phase 47 as next.

       Use the obsidian CLI append/edit pattern OR write directly to the vault file. Match the existing hub structure exactly — do not invent new sections.

    Per CLAUDE.md "Sync Workflows → Hub note".
  </action>
  <verify>
    <automated>
test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Roadmap.md" &&
grep -c "Phase 46" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Roadmap.md" | awk '$1 >= 1 { exit 0 } { exit 1 }' &&
grep -c "Phase-46-TLS-Finding-Gaps\|Phase 46" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/_QUIRK-Hub.md" | awk '$1 >= 1 { exit 0 } { exit 1 }'
    </automated>
  </verify>
  <acceptance_criteria>
    - Vault Roadmap.md has fresh content from .planning/ROADMAP.md and frontmatter
    - Hub references Phase 46 (wikilink or section)
  </acceptance_criteria>
  <done>Vault Roadmap and Hub reflect Phase 46 completion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Live-fire end-to-end finding verification</name>
  <what-built>
    The full Phase 46 stack: scanner with chain_verified plumbing (46-01), risk engine with corrected
    severities and D-04 split (46-02), and the chaos lab `tls-cert-defects` profile (46-03), plus
    docs + Obsidian sync (this plan, prior tasks).
  </what-built>
  <how-to-verify>
    1. Bring up the chaos lab profile:
       ```bash
       cd quantum-chaos-enterprise-lab
       PROFILE_ARGS="--profile tls-cert-defects" ./lab.sh up
       ./lab.sh status   # confirm 4 services Up
       ```

    2. Run the scanner against all 4 endpoints from the project root:
       ```bash
       quirk scan localhost:13444,localhost:13445,localhost:13446,localhost:13447 --output /tmp/phase46-uat.html
       ```

    3. Inspect the report (HTML or the JSON intermediate). Confirm:
       - Endpoint `localhost:13444` has a **CRITICAL** finding mentioning expiry (TLS-FIND-01)
       - Endpoint `localhost:13445` has a **HIGH** "self-signed" finding AND has NO "untrusted CA" finding (D-04 mutual exclusivity, TLS-FIND-02)
       - Endpoint `localhost:13446` has a **MEDIUM** "untrusted CA" finding (TLS-FIND-03)
       - Endpoint `localhost:13447` has a **HIGH** finding for the RSA-1024 key (TLS-FIND-04)
       - No half-populated `cert_not_after = None` rows in the SQLite DB (`sqlite3 quirk.db "SELECT host,port,cert_not_after,chain_verified FROM crypto_endpoints;"`)

    4. Tear down:
       ```bash
       cd quantum-chaos-enterprise-lab && ./lab.sh down
       ```

  </how-to-verify>
  <resume-signal>Type "approved" if all 4 expected findings appear with the correct severities and the self-signed endpoint emits NO untrusted-CA finding. If any expected finding is missing or the wrong severity, paste the report excerpt.</resume-signal>
</task>

</tasks>

<verification>
- Tasks 1-4 automated grep gates pass
- Task 5 live-fire UAT confirms all 4 findings with correct severities and D-04 exclusivity
</verification>

<success_criteria>
- docs/UAT-SERIES.md updated with Phase 46 cert-defect test cases and current date
- Vault UAT-Series.md mirrors docs/UAT-SERIES.md
- Vault Phase-46-TLS-Finding-Gaps.md exists with frontmatter, all 7 requirement IDs, per-plan summary
- Vault Roadmap.md synced; _QUIRK-Hub.md references Phase 46
- Live-fire end-to-end produces 4 distinct findings: CRITICAL (expired) / HIGH (self-signed) / MEDIUM (untrusted CA) / HIGH (RSA-1024)
- Self-signed endpoint emits NO untrusted-CA finding (D-04 exclusivity verified)
- All 7 TLS-FIND requirements demonstrably satisfied end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/46-tls-finding-gaps/46-04-SUMMARY.md` documenting:
- UAT-SERIES.md test cases added (IDs + titles)
- Vault file paths created/updated
- Live-fire report excerpt (4 findings list with severity + endpoint)
- Confirmation of D-04 exclusivity from the live report

Then commit `docs/UAT-SERIES.md` per CLAUDE.md mandatory step 4:
```bash
node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-46): update UAT-SERIES.md" --files docs/UAT-SERIES.md
```
</output>
