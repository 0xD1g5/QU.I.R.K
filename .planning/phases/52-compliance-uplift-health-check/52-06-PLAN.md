---
phase: 52-compliance-uplift-health-check
plan: 06
type: execute
wave: 2
depends_on: [02, 03, 04, 05]
files_modified:
  - docs/UAT-SERIES.md
  - docs/operators-guide.md
  - docs/configuration.md
autonomous: true
requirements: [DOCS-05]
must_haves:
  truths:
    - "docs/UAT-SERIES.md reflects Phase 52 (FIPS annotation, SOC2/ISO frameworks, quirk doctor command)"
    - "docs/operators-guide.md documents the `quirk doctor` subcommand with usage and exit codes"
    - "docs/configuration.md mentions SOC2 + ISO 27001:2022 framework coverage"
    - "Phase 52 Obsidian phase note exists at vault path; UAT-Series.md synced to vault"
  artifacts:
    - path: docs/UAT-SERIES.md
      provides: "Updated UAT series referencing Phase 52 deliverables"
      contains: "quirk doctor"
    - path: docs/operators-guide.md
      provides: "quirk doctor subcommand documentation section"
      contains: "quirk doctor"
    - path: /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-52-Compliance-Uplift-Health-Check.md
      provides: "Obsidian phase note (per CLAUDE.md mandatory step)"
      contains: "status: complete"
    - path: /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md
      provides: "Synced UAT series in vault"
      contains: "quirk doctor"
  key_links:
    - from: docs/operators-guide.md
      to: quirk/cli/doctor_cmd.py
      via: "documented usage `quirk doctor`"
      pattern: "quirk doctor"
---

<objective>
Documentation + vault-sync wave for Phase 52. Update the three operator-facing
documentation surfaces, add a UAT-SERIES entry covering the new `quirk doctor`
command and SOC2/ISO framework coverage, and complete the Obsidian sync per
CLAUDE.md "Mandatory Phase Completion Steps".

Purpose: Per persistent user feedback (memory: feedback_docs_obsidian_per_phase,
feedback_uat_series_update, feedback_obsidian_phase_notes), every phase must
explicitly schedule docs + UAT-SERIES + Obsidian work. Phase 52 ships net-new
operator surface (`quirk doctor` is brand new) and net-new compliance frameworks
(SOC2, ISO 27001:2022) — both demand documentation.

Output: Three docs files updated, UAT-SERIES.md updated and synced, Phase 52
Obsidian note created with `status: complete`, all changes committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-compliance-uplift-health-check/52-CONTEXT.md
@.planning/phases/52-compliance-uplift-health-check/52-01-SUMMARY.md
@.planning/phases/52-compliance-uplift-health-check/52-02-SUMMARY.md
@.planning/phases/52-compliance-uplift-health-check/52-03-SUMMARY.md
@.planning/phases/52-compliance-uplift-health-check/52-04-SUMMARY.md
@.planning/phases/52-compliance-uplift-health-check/52-05-SUMMARY.md
@docs/UAT-SERIES.md
@docs/operators-guide.md
@docs/configuration.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/operators-guide.md and docs/configuration.md with Phase 52 surface</name>
  <files>docs/operators-guide.md, docs/configuration.md</files>
  <read_first>
    - docs/operators-guide.md (full file — locate the subcommand reference section that documents `init`, `serve`, `compliance`)
    - docs/configuration.md (full file — locate the "Compliance Frameworks" section if present)
    - .planning/phases/52-compliance-uplift-health-check/52-04-SUMMARY.md (confirms doctor categories shipped)
    - .planning/phases/52-compliance-uplift-health-check/52-03-SUMMARY.md (confirms SOC2/ISO mappings shipped)
    - .planning/phases/52-compliance-uplift-health-check/52-CONTEXT.md (D-14 — exact 8-category list for doctor docs)
  </read_first>
  <action>
**(a) `docs/operators-guide.md`:** Locate the existing subcommand reference section
(should document `quirk init`, `quirk serve`, `quirk compliance`). Append a new
subsection titled `### quirk doctor` with this content:

```markdown
### quirk doctor

Pre-engagement health check. Runs eight diagnostic probes and prints a
Rich-formatted dashboard. Exit code is the machine-readable signal:

- `0` — all non-informational checks pass; QUIRK is ready to scan
- `1` — one or more non-informational checks failed; address before scanning

#### Usage

```bash
quirk doctor
```

No flags are accepted. Invoke before each client engagement.

#### Categories

| # | Category | Severity | Failure exits 1? |
|---|----------|----------|------------------|
| 1 | Python environment (>= 3.11) | non-informational | yes |
| 2 | Scanner binaries (`nmap`, `syft`, `semgrep` in PATH) | non-informational | yes |
| 3 | Compliance framework freshness (within `STALENESS_THRESHOLD_DAYS`) | non-informational | yes |
| 4 | QRAMM module availability | informational | **no** |
| 5 | Database (`./quirk.db` reachable) | non-informational | yes |
| 6 | Configuration (`./config.yaml` parses) | non-informational | yes (malformed); informational only if file is absent |
| 7 | Network connectivity (DNS probe) | informational | **no** |
| 8 | Dashboard process (port 8512) | informational | **no** |

#### Symbols

- `[✓]` — check passed
- `[!]` — informational status (never causes exit 1)
- `[✗]` — check failed (causes exit 1 if non-informational)

#### Examples

```text
$ quirk doctor
                        QU.I.R.K. Health Check
┏━━━━━━━━━━━━━━━━━━━━━━━━━┳━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃ Check                   ┃ Status                                        ┃
┡━━━━━━━━━━━━━━━━━━━━━━━━━╇━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┩
│ Python environment      │ [✓] Python 3.14                               │
│ Binary: nmap            │ [✓] /opt/homebrew/bin/nmap                    │
│ Binary: syft            │ [✓] /opt/homebrew/bin/syft                    │
│ Binary: semgrep         │ [✗] semgrep not found in PATH                 │
│ Compliance freshness    │ [✓] all frameworks within freshness window    │
│ QRAMM module            │ [!] QRAMM module not installed — Phase 51     │
│ Database (quirk.db)     │ [✓] ./quirk.db reachable                      │
│ Configuration           │ [✓] ./config.yaml parses cleanly              │
│ Network connectivity    │ [✓] outbound TCP to 8.8.8.8:53 OK             │
│ Dashboard process       │ [!] dashboard not running on port 8512        │
└─────────────────────────┴───────────────────────────────────────────────┘
$ echo $?
1
```

(In the example above, `semgrep` is missing — a non-informational failure that
exits 1.)
```

**(b) `docs/configuration.md`:** Locate the "Compliance Frameworks" section (or
add one if absent, near other configuration topics). Update or insert content
listing the eight frameworks now mapped:

```markdown
## Compliance Frameworks

QUIRK's `COMPLIANCE_MAP` (in `quirk/compliance/__init__.py`) maps every finding
category to one or more of the following frameworks. As of Phase 52 (v4.7), all
frameworks are kept fresh by the `STALENESS_THRESHOLD_DAYS` gate and verified
by `quirk doctor` before each scan.

| Framework | Version | Builder helper |
|-----------|---------|----------------|
| PCI-DSS | 4.0.1 | `_pci(control)` |
| HIPAA | 2024-rev (45 CFR §164.312) | `_hipaa(control)` |
| FIPS 140-3 | NIST FIPS 140-3 | `_fips(control)` |
| SOC2 | 2017-rev (Trust Services Criteria) | `_soc2(control)` — Phase 52 |
| ISO 27001 | ISO 27001:2022 (8.x clause numbering) | `_iso(control)` — Phase 52 |

**ISO 27001:2022 control assignments** (Phase 52):
- `8.24` — Use of cryptography (algorithm/key-size findings)
- `8.26` — Application security requirements (TLS/protocol transport findings)
- `8.28` — Secure coding (source-code scanner findings)

**SOC2 CC6.x control assignments** (Phase 52):
- `CC6.6` — Logical access controls (authentication, key, certificate findings)
- `CC6.7` — Transmission encryption (cipher, protocol, transport findings)

CBOM algorithm components carry a `quirk:fips140-3-status` property
(`approved` or `non-approved`) derived from the NIST quantum security level.
The `certified` tier is reserved for a future phase that will ingest CMVP
module attestation.
```

If the section already exists (e.g., from Phase 49), update in place — preserve
existing PCI/HIPAA/FIPS rows; add SOC2 + ISO rows; add the FIPS 140-3 status
property paragraph.

Do NOT touch any other docs files in this task — `getting-started.md`,
`installation.md`, etc. are out of scope.
  </action>
  <verify>
    <automated>grep -c "quirk doctor" docs/operators-guide.md && grep -c "ISO 27001:2022" docs/configuration.md && grep -c "SOC2" docs/configuration.md && grep -c "quirk:fips140-3-status" docs/configuration.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "### quirk doctor" docs/operators-guide.md` returns 1
    - `grep -c "Pre-engagement health check" docs/operators-guide.md` returns 1
    - `grep -c "STALENESS_THRESHOLD_DAYS" docs/operators-guide.md` returns at least 1
    - `grep -c "informational" docs/operators-guide.md` returns at least 3 (categories 4, 7, 8)
    - `grep -c "ISO 27001:2022" docs/configuration.md` returns at least 1
    - `grep -c "SOC2" docs/configuration.md` returns at least 1
    - `grep -c "_soc2(control)" docs/configuration.md` returns 1
    - `grep -c "_iso(control)" docs/configuration.md` returns 1
    - `grep -c "quirk:fips140-3-status" docs/configuration.md` returns at least 1
    - `grep -c "8.24\\|8.26" docs/configuration.md` returns at least 2
    - `grep -c "CC6.6\\|CC6.7" docs/configuration.md` returns at least 2
  </acceptance_criteria>
  <done>operators-guide.md has a `### quirk doctor` section; configuration.md documents SOC2 + ISO + FIPS 140-3 status property.</done>
</task>

<task type="auto">
  <name>Task 2: Update docs/UAT-SERIES.md with Phase 52 deliverables and sync to Obsidian vault</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (full file — locate the document header `**Last Updated:**` line and existing series sections)
    - .planning/phases/52-compliance-uplift-health-check/52-CONTEXT.md
    - CLAUDE.md (sections "Mandatory Phase Completion Steps" → step 2 + step 3)
  </read_first>
  <action>
**Step 1 — Update header date.** Change the `**Last Updated:**` line at the top
of `docs/UAT-SERIES.md` to `**Last Updated:** 2026-05-05`.

**Step 2 — Add or update Phase 52 entries.** Locate the appropriate test series
sections and add new test cases (or update existing ones if Phase 52 changed
behavior they cover). At minimum, add the following test cases — choose the
most-relevant series sections based on the existing structure of the document:

```markdown
### UAT-COMPLY-52-01: CBOM FIPS 140-3 status annotation

**Pass criteria:**
- Run `.venv/bin/python3 run_scan.py --target localhost --ports 443 --output-dir /tmp/uat52a`.
- Open the generated CBOM JSON file (`/tmp/uat52a/cbom-*.json`).
- Every component with `cryptoProperties.assetType == "algorithm"` has a
  `properties` array containing one entry with
  `name == "quirk:fips140-3-status"` and `value` in `{"approved", "non-approved"}`.
- The literal string `"certified"` does NOT appear in the FIPS status property
  value (deferred to a future CMVP phase per Phase 52 D-01).

### UAT-COMPLY-52-02: SOC2 + ISO 27001:2022 mapping coverage

**Pass criteria:**
- Run `.venv/bin/python3 -c "from quirk.compliance import COMPLIANCE_MAP; ..."`
  to print framework counts.
- At least 3 entries with `framework == "SOC2 CC"` and `control` starting with `CC6.`.
- At least 3 entries with `framework == "ISO 27001:2022"` using only 8.x clause numbering.
- Zero entries with framework ISO 27001:2022 and control starting with `A.`.
- All `ISO 27001:2022` entries have `version == "ISO 27001:2022"` exactly.

### UAT-DOCS-52-03: quirk doctor exit semantics

**Pass criteria:**
- On a fully provisioned dev box, `quirk doctor; echo $?` prints the Rich
  health table and exits `0`.
- After temporarily renaming nmap (`mv $(which nmap) $(which nmap).bak`),
  `quirk doctor` exits `1` and shows `[✗] nmap not found` in the output.
- After restoring nmap, with no dashboard running and no QRAMM module
  installed, `quirk doctor` still exits `0` (informational checks never
  cause exit 1).

### UAT-DEBT-52-04: lab.sh PROFILE_ARGS CLI override

**Pass criteria:**
- `cd quantum-chaos-enterprise-lab && PROFILE_ARGS="--profile tls" ./lab.sh status`
  reports the `tls` profile services regardless of any `PROFILE_ARGS=` value
  in `.env`.

### UAT-DEBT-52-05: run-stats fields present

**Pass criteria:**
- After a scan completes, `run-stats-*.json` parsed as JSON contains
  `ports_scanned` and `hosts_scanned` keys (top-level OR nested under `counts`),
  each holding a sorted list.

### UAT-DEBT-52-06: SAML scanner uses lxml without DeprecationWarning

**Pass criteria:**
- `.venv/bin/python3 -W error::DeprecationWarning -m pytest tests/test_saml_scanner.py -q`
  exits 0.
- `grep -c "defusedxml.lxml" quirk/scanner/saml_scanner.py` returns 0.
```

Insert each test case in the most appropriate existing series section (read the
existing document structure to choose sensibly — e.g., compliance tests go in
the compliance series, doctor tests in operator-tooling, debt tests in a debt
or regression series).

**Step 3 — Sync UAT-SERIES.md to Obsidian.** Per CLAUDE.md mandatory step 3,
write directly to vault filesystem (the file is too large for `obsidian CLI
content=`):

```bash
printf "---\nproject: QU.I.R.K.\ntype: reference\nstatus: active\nsource: docs/UAT-SERIES.md\nupdated: 2026-05-05\n---\n\n" > /tmp/uat_vault.md
cat docs/UAT-SERIES.md >> /tmp/uat_vault.md
cp /tmp/uat_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
```

**Step 4 — Commit UAT-SERIES.md** per CLAUDE.md step 4:

```bash
node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-52): update UAT-SERIES.md" --files docs/UAT-SERIES.md
```

(Other docs files commit later in the phase commit; this commit is specifically
required for UAT-SERIES per CLAUDE.md.)
  </action>
  <verify>
    <automated>grep -c "Last Updated:.*2026-05-05" docs/UAT-SERIES.md && grep -c "UAT-COMPLY-52-01\|UAT-DOCS-52-03\|UAT-DEBT-52-04" docs/UAT-SERIES.md && test -f /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md && grep -c "quirk doctor" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Last Updated:.*2026-05-05" docs/UAT-SERIES.md` returns 1
    - `grep -c "UAT-COMPLY-52-01" docs/UAT-SERIES.md` returns 1
    - `grep -c "UAT-COMPLY-52-02" docs/UAT-SERIES.md` returns 1
    - `grep -c "UAT-DOCS-52-03" docs/UAT-SERIES.md` returns 1
    - `grep -c "UAT-DEBT-52-04" docs/UAT-SERIES.md` returns 1
    - `grep -c "UAT-DEBT-52-05" docs/UAT-SERIES.md` returns 1
    - `grep -c "UAT-DEBT-52-06" docs/UAT-SERIES.md` returns 1
    - File `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` exists
    - `grep -c "source: docs/UAT-SERIES.md" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` returns 1 (frontmatter prepended)
    - `grep -c "quirk doctor" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` returns at least 1 (sync includes new content)
    - The UAT-SERIES.md commit landed: `git log --oneline -5 | grep -c "phase-52.*UAT-SERIES"` returns 1
  </acceptance_criteria>
  <done>UAT-SERIES.md updated with 6 Phase 52 test cases; vault sync complete; UAT-SERIES.md committed.</done>
</task>

<task type="auto">
  <name>Task 3: Create Phase 52 Obsidian phase note in vault</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-52-Compliance-Uplift-Health-Check.md</files>
  <read_first>
    - CLAUDE.md (section "Mandatory Phase Completion Steps" → step 1: "Create Obsidian Phase Note")
    - .planning/phases/52-compliance-uplift-health-check/52-01-SUMMARY.md
    - .planning/phases/52-compliance-uplift-health-check/52-02-SUMMARY.md
    - .planning/phases/52-compliance-uplift-health-check/52-03-SUMMARY.md
    - .planning/phases/52-compliance-uplift-health-check/52-04-SUMMARY.md
    - .planning/phases/52-compliance-uplift-health-check/52-05-SUMMARY.md
    - One existing example phase note at /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/ (read any one for the template pattern)
  </read_first>
  <action>
Per CLAUDE.md "Mandatory Phase Completion Steps" step 1: write the phase note
DIRECTLY to the vault filesystem (do NOT use `obsidian CLI content=` — files
are too large for shell expansion).

Create the file at the exact path:

```
/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-52-Compliance-Uplift-Health-Check.md
```

Contents (substitute the bracketed sections with concrete content sourced from
the SUMMARY files listed in `read_first`):

```markdown
---
project: QU.I.R.K.
type: phase
status: complete
source: .planning/phases/52-compliance-uplift-health-check/
updated: 2026-05-05
---

# Phase 52 — Compliance Uplift & Health Check

## Goal

The compliance module gains SOC2 and ISO 27001:2022 framework mappings; CBOM
algorithm components carry FIPS 140-3 status annotations; `quirk doctor` gives
operators a pre-engagement health dashboard; four backlog tech debt items are
closed.

This phase ran in parallel with Phase 51 (zero shared code paths).

## Requirements Covered

- **COMPLY-10** — CBOM Pass-1 algorithm components carry `quirk:fips140-3-status` property
- **COMPLY-11** — SOC2 CC6.x controls mapped via `_soc2()` helper
- **COMPLY-12** — ISO 27001:2022 controls (8.x clause numbering) mapped via `_iso()` helper
- **DOCS-05** — `quirk doctor` CLI: 8 categories, [✓]/[!]/[✗], exits 1 on non-informational failure
- **DEBT-02** — `lab.sh` PROFILE_ARGS CLI precedence fixed (snapshot before `source .env`)
- **DEBT-03** — `run-stats-*.json` includes `ports_scanned` and `hosts_scanned`
- **DEBT-04** — `saml_scanner.py` migrated from `defusedxml.lxml` to raw `lxml.etree` with security flags

## Success Criteria

1. CBOM JSON output contains `quirk:fips140-3-status` on every algorithm component (`approved` or `non-approved`; `certified` deferred to a future CMVP phase per Phase 52 D-01).
2. `_soc2()` helper present; ≥ 3 SOC2 CC6.x controls in `COMPLIANCE_MAP`.
3. `_iso()` helper present; ISO 27001:2022 entries use 8.x numbering only; unit test rejects `A.x.x` IDs.
4. `quirk doctor` runs end-to-end; exit 0 when all non-informational checks pass; exit 1 on Python/binary/freshness/DB/config failure; informational checks (QRAMM/network/dashboard) never exit 1.
5. `PROFILE_ARGS="--profile X" ./lab.sh` overrides `.env`.
6. `run-stats-*.json` contains `ports_scanned` and `hosts_scanned`.
7. `saml_scanner.py` uses raw `lxml.etree` with `resolve_entities=False, no_network=True`; all 27 SAML tests GREEN.

## What Was Built

### Plan 01 — Wave 0 test scaffolding
[Source: 52-01-SUMMARY.md — summarize what test stubs were created and which files]

### Plan 02 — CBOM FIPS 140-3 annotation (COMPLY-10)
[Source: 52-02-SUMMARY.md — summarize `_fips_status` helper + Property attachment]

### Plan 03 — SOC2 + ISO 27001:2022 mappings (COMPLY-11/12)
[Source: 52-03-SUMMARY.md — summarize helpers + COMPLIANCE_MAP coverage]

### Plan 04 — `quirk doctor` CLI (DOCS-05)
[Source: 52-04-SUMMARY.md — summarize doctor_cmd.py + run_scan.py intercept]

### Plan 05 — Tech debt closures (DEBT-02/03/04)
[Source: 52-05-SUMMARY.md — summarize lab.sh fix, run-stats verification, lxml migration]

### Plan 06 — Documentation + vault sync
[Source: this plan's SUMMARY — operators-guide.md, configuration.md, UAT-SERIES.md, Obsidian]

## Links

- [[Roadmap]]
- [[Requirements]]
- [[_QUIRK-Hub]]
- [[UAT-Series]]
```

After writing the file, verify it landed:

```bash
test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-52-Compliance-Uplift-Health-Check.md" && echo "OK"
```

The bracketed `[Source: ...]` sections must be replaced with concrete 2–4 sentence
summaries pulled from the corresponding SUMMARY.md files. Do not leave literal
brackets in the final note.
  </action>
  <verify>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-52-Compliance-Uplift-Health-Check.md" && grep -c "status: complete" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-52-Compliance-Uplift-Health-Check.md" && grep -c "COMPLY-10\|COMPLY-11\|COMPLY-12\|DOCS-05\|DEBT-02\|DEBT-03\|DEBT-04" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-52-Compliance-Uplift-Health-Check.md"</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-52-Compliance-Uplift-Health-Check.md` exists
    - `grep -c "^status: complete$" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-52-Compliance-Uplift-Health-Check.md` returns 1
    - `grep -c "^type: phase$" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-52-Compliance-Uplift-Health-Check.md` returns 1
    - `grep -c "^project: QU.I.R.K.$" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-52-Compliance-Uplift-Health-Check.md` returns 1
    - `grep -c "COMPLY-10" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-52-Compliance-Uplift-Health-Check.md` returns at least 1
    - `grep -c "COMPLY-11" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-52-Compliance-Uplift-Health-Check.md` returns at least 1
    - `grep -c "COMPLY-12" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-52-Compliance-Uplift-Health-Check.md` returns at least 1
    - `grep -c "DOCS-05" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-52-Compliance-Uplift-Health-Check.md` returns at least 1
    - `grep -c "DEBT-02" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-52-Compliance-Uplift-Health-Check.md` returns at least 1
    - `grep -c "DEBT-03" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-52-Compliance-Uplift-Health-Check.md` returns at least 1
    - `grep -c "DEBT-04" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-52-Compliance-Uplift-Health-Check.md` returns at least 1
    - `grep -c "\\[\\[Roadmap\\]\\]" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-52-Compliance-Uplift-Health-Check.md` returns 1
    - `grep -c "\\[Source:" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-52-Compliance-Uplift-Health-Check.md` returns 0 (executor must replace bracketed placeholders with real content)
  </acceptance_criteria>
  <done>Phase 52 Obsidian note exists at canonical vault path with status: complete, all 7 requirement IDs, and concrete summaries (no leftover bracket placeholders).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs → operator surface | docs/*.md describes operational behavior; misdocumentation misleads operators |
| repo → Obsidian vault | One-way sync (repo is source of truth); vault is a local mirror |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-06-01 | Information Disclosure | Docs accidentally claim `certified` FIPS tier ships in v4.7 | mitigate | Configuration.md text explicitly states `certified` is reserved for a future phase; UAT-COMPLY-52-01 pass criterion asserts `"certified"` literal absent |
| T-52-06-02 | Tampering | Vault sync overwrites operator-edited vault note | accept | Vault is treated as a one-way mirror; user has acknowledged this in CLAUDE.md (overwrite flag is the documented pattern) |
| T-52-06-03 | Repudiation | Phase note status drifts from actual phase state | mitigate | `status: complete` set only when this plan executes (after Plans 02–05 land); `updated:` date in frontmatter |
</threat_model>

<verification>
```
grep -c "quirk doctor" docs/operators-guide.md
grep -c "ISO 27001:2022\|SOC2\|quirk:fips140-3-status" docs/configuration.md
grep -c "UAT-COMPLY-52\|UAT-DOCS-52\|UAT-DEBT-52" docs/UAT-SERIES.md
test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-52-Compliance-Uplift-Health-Check.md"
test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
```
</verification>

<success_criteria>
- operators-guide.md has a `### quirk doctor` section
- configuration.md documents SOC2 + ISO 27001:2022 + FIPS 140-3 status property
- UAT-SERIES.md updated with 6 Phase 52 test cases (UAT-COMPLY-52-01/02, UAT-DOCS-52-03, UAT-DEBT-52-04/05/06) and dated 2026-05-05
- UAT-SERIES.md synced to vault (UAT-Series.md exists with frontmatter)
- Phase 52 Obsidian note exists at the canonical path with status: complete
- All bracketed placeholders replaced with concrete content
</success_criteria>

<output>
Create `.planning/phases/52-compliance-uplift-health-check/52-06-SUMMARY.md`
</output>
