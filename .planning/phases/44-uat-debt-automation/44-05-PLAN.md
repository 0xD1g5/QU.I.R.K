---
phase: 44-uat-debt-automation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/dashboard/api/routes/pdf.py
  - src/dashboard/src/pages/print.tsx
  - src/dashboard/src/pages/data-at-rest.tsx
  - src/dashboard/src/pages/motion.tsx
autonomous: true
requirements: [UAT-01, UAT-02, UAT-03, UAT-04]

must_haves:
  truths:
    - "pdf.py wraps int(os.environ.get(QUIRK_SERVE_PORT, ...)) in try/except ValueError and returns a 500 JSON response with a clear detail message on bad value"
    - "pdf.py invokes browser.close() inside a finally block so it runs even if page operations raise"
    - "print.tsx PDF executive summary renders the data_in_motion subscore as the 6th score-item (after data_at_rest)"
    - "Every <TableHead> in data-at-rest.tsx (4 TableHeader blocks) has scope='col'"
    - "Every <TableHead> in motion.tsx (2 TableHeader blocks) has scope='col'"
  artifacts:
    - path: quirk/dashboard/api/routes/pdf.py
      provides: "CR-02 fix (ValueError on bad port) + WR-01 fix (browser.close in finally)"
      contains: "except ValueError"
    - path: src/dashboard/src/pages/print.tsx
      provides: "WR-03 fix — data_in_motion subscore in PDF executive summary"
      contains: "data_in_motion"
    - path: src/dashboard/src/pages/data-at-rest.tsx
      provides: "WR-04 fix — scope='col' on every TableHead"
      contains: 'scope="col"'
    - path: src/dashboard/src/pages/motion.tsx
      provides: "WR-04 fix — scope='col' on every TableHead"
      contains: 'scope="col"'
  key_links:
    - from: pdf.py port coercion
      to: HTTP 500 JSON response
      via: "try/except ValueError → Response(..., status_code=500)"
      pattern: "except ValueError"
    - from: print.tsx data_in_motion score-item
      to: score.subscores.data_in_motion
      via: "{score.subscores.data_in_motion} JSX expression in score-row"
      pattern: "subscores\\.data_in_motion"
---

<objective>
Apply the 4 real bug fixes from the Phase 43 code review (per D-09): CR-02 (pdf.py ValueError on bad QUIRK_SERVE_PORT), WR-01 (pdf.py browser.close not in finally), WR-03 (print.tsx missing data_in_motion subscore in PDF executive summary), WR-04 (data-at-rest.tsx + motion.tsx TableHead missing scope="col" for accessibility).

Per D-10: WR-05 and WR-06 are explicitly OUT OF SCOPE for this plan.

Purpose: Closes the 4 actionable findings from `.planning/phases/43-dashboard-polish/.continue-here.md`. These bug fixes are bundled into a single plan because they are independent, small, and share the same operator-visible quality theme.
Output: 4 modified files (one Python, three TSX), no new files, no new tests beyond what already exists for these modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/44-uat-debt-automation/44-CONTEXT.md
@.planning/phases/44-uat-debt-automation/44-RESEARCH.md
@.planning/phases/44-uat-debt-automation/44-PATTERNS.md
@.planning/phases/43-dashboard-polish/.continue-here.md
@quirk/dashboard/api/routes/pdf.py
@src/dashboard/src/pages/print.tsx
@src/dashboard/src/pages/data-at-rest.tsx
@src/dashboard/src/pages/motion.tsx

<interfaces>
pdf.py current state (verified):
- Line ~45: `port = int(os.environ.get("QUIRK_SERVE_PORT", "8512"))` (no try/except)
- Line ~62: `browser.close()` inside `with sync_playwright()` block but outside `finally` — skipped on exception
- Existing error response shape (lines 71-84):
  ```python
  return Response(
      content=json.dumps({"detail": "..."}).encode(),
      status_code=500,
      media_type="application/json",
  )
  ```
- Sentinel `data-ready="true"` already in place from Phase 43; do NOT change `wait_for_selector('body[data-ready="true"]', ...)`.

print.tsx current state (verified, lines 200-225):
- Renders 5 subscores: hygiene, modern_tls, identity_trust, agility_signals, data_at_rest
- `data_at_rest` is at line 222
- Need to insert a 6th `score-item` for `data_in_motion` AFTER the data_at_rest block

data-at-rest.tsx (verified by grep — 4 TableHeader blocks, NOT 3 as PATTERNS.md initially said):
- TableHeader at line 70 (Engine, Host, Port, Severity, Title, Encryption at Rest, TLS in Transit, Quantum Risk, Remediation — 9 TableHead)
- TableHeader at line 118 (Provider, Host, Severity, Title, Encryption Mode, Public Access, KMS Key, Versioning, Quantum Risk, Remediation — 10 TableHead)
- TableHeader at line 178 (Namespace, Host, Severity, Title, Secret Type, Encryption Provider, Quantum Risk, Remediation — 8 TableHead)
- TableHeader at line 216 (Host, Severity, Title, Mount Type, Seal Type, Auto-Unseal, Quantum Risk, Remediation — 8 TableHead)
- Total TableHead in data-at-rest.tsx = 35

motion.tsx (verified by grep — 2 TableHeader blocks):
- TableHeader at line 50 (Port, Protocol, TLS Version, Cipher Suite, Cert Expiry, Quantum Risk, Warning — 7 TableHead)
- TableHeader at line 132 (Host, Port, Protocol, TLS Version, Cipher Suite, Status — 6 TableHead)
- Total TableHead in motion.tsx = 13
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply CR-02 + WR-01 fixes to quirk/dashboard/api/routes/pdf.py</name>
  <files>quirk/dashboard/api/routes/pdf.py</files>
  <read_first>
    - quirk/dashboard/api/routes/pdf.py (full file — to confirm exact line numbers of `int(os.environ.get` and `browser.close()` and surrounding try/except structure)
    - tests/test_pdf_export.py (if exists — to ensure existing tests still pass; do not modify)
    - .planning/phases/44-uat-debt-automation/44-PATTERNS.md (lines 271-339 — exact replacement snippets for both fixes)
  </read_first>
  <behavior>
    - When `QUIRK_SERVE_PORT` is set to a non-integer (e.g. "abc"), the route returns HTTP 500 with JSON body `{"detail": "QUIRK_SERVE_PORT is not a valid integer."}` instead of raising an unhandled `ValueError`.
    - When `page.goto` or `page.wait_for_selector` or `page.pdf` raises an exception, `browser.close()` still executes (verified by `finally` block placement).
    - All existing pdf.py tests in tests/test_pdf_export.py continue to pass.
  </behavior>
  <action>
Apply two edits to `quirk/dashboard/api/routes/pdf.py`:

**Edit 1 (CR-02):** Wrap the port coercion in try/except ValueError. Find the line:
```python
port = int(os.environ.get("QUIRK_SERVE_PORT", "8512"))
```
Replace with:
```python
try:
    port = int(os.environ.get("QUIRK_SERVE_PORT", "8512"))
except ValueError:
    return Response(
        content=json.dumps({"detail": "QUIRK_SERVE_PORT is not a valid integer."}).encode(),
        status_code=500,
        media_type="application/json",
    )
```
Per D-09 specifics: do NOT silently fall back to a default — surface the misconfiguration. The detail string must be EXACTLY `"QUIRK_SERVE_PORT is not a valid integer."` (with the trailing period). Status code MUST be 500. Media type MUST be `application/json`. Ensure `json` and `Response` are already imported in the file (they are, since the existing error handler uses both); if not, add the imports.

**Edit 2 (WR-01):** Move `browser.close()` into a `finally` block. Find the existing playwright block (currently shaped like):
```python
with sync_playwright() as p:
    browser = p.chromium.launch(headless=True)
    context = browser.new_context()
    page = context.new_page()
    page.goto(print_url, ...)
    page.wait_for_selector('body[data-ready="true"]', ...)
    pdf_bytes = page.pdf(...)
    browser.close()
```
Restructure to:
```python
with sync_playwright() as p:
    browser = p.chromium.launch(headless=True)
    try:
        context = browser.new_context()
        page = context.new_page()
        page.goto(print_url, ...)
        page.wait_for_selector('body[data-ready="true"]', ...)
        pdf_bytes = page.pdf(...)
    finally:
        browser.close()
```
Preserve all existing arguments to `page.goto`, `page.wait_for_selector`, and `page.pdf` exactly as they currently are (do NOT change the wait sentinel — `body[data-ready="true"]` is already correct from Phase 43). Preserve the outer `try/except Exception` wrapper that handles playwright install errors (lines ~71-84).
  </action>
  <verify>
    <automated>python -m py_compile quirk/dashboard/api/routes/pdf.py && python -c "import ast,sys; t=ast.parse(open('quirk/dashboard/api/routes/pdf.py').read()); src=open('quirk/dashboard/api/routes/pdf.py').read(); assert 'except ValueError:' in src and 'QUIRK_SERVE_PORT is not a valid integer.' in src and 'finally:' in src, 'missing required edits'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File compiles: `python -m py_compile quirk/dashboard/api/routes/pdf.py && echo OK`
    - CR-02 try/except present: `grep -q 'except ValueError:' quirk/dashboard/api/routes/pdf.py`
    - CR-02 exact detail string present: `grep -q 'QUIRK_SERVE_PORT is not a valid integer.' quirk/dashboard/api/routes/pdf.py`
    - CR-02 status_code=500 with JSON: `grep -A2 'QUIRK_SERVE_PORT is not a valid integer' quirk/dashboard/api/routes/pdf.py | grep -q 'status_code=500'`
    - WR-01 finally block present: `grep -c 'finally:' quirk/dashboard/api/routes/pdf.py` >= 1
    - WR-01 browser.close inside finally: `awk '/finally:/{flag=1; next} flag && /browser.close\(\)/{print "OK"; exit}' quirk/dashboard/api/routes/pdf.py | grep -q OK`
    - Sentinel selector unchanged from Phase 43: `grep -q 'body\[data-ready="true"\]' quirk/dashboard/api/routes/pdf.py`
    - Existing pdf tests pass: `python -m pytest tests/test_pdf_export.py -q` exits 0 (or "no tests" if file absent)
    - Quick suite green: `python -m pytest tests/ -m 'not slow' -q` exits 0
  </acceptance_criteria>
  <done>pdf.py has try/except ValueError around port coercion returning 500 JSON with exact detail string, browser.close() lives in a finally block, sentinel selector unchanged, file compiles and existing tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply WR-03 + WR-04 frontend fixes (print.tsx data_in_motion + scope="col" on TableHead)</name>
  <files>src/dashboard/src/pages/print.tsx, src/dashboard/src/pages/data-at-rest.tsx, src/dashboard/src/pages/motion.tsx</files>
  <read_first>
    - src/dashboard/src/pages/print.tsx (lines 195-230 — confirm exact JSX shape of existing 5 score-item blocks; the new data_in_motion block must mirror the data_at_rest block exactly)
    - src/dashboard/src/pages/data-at-rest.tsx (lines 70-230 — all 4 TableHeader blocks; count TableHead elements before/after to verify completeness)
    - src/dashboard/src/pages/motion.tsx (lines 50-145 — all 2 TableHeader blocks)
    - .planning/phases/44-uat-debt-automation/44-PATTERNS.md (lines 343-410 — exact JSX snippets)
  </read_first>
  <behavior>
    - WR-03: print.tsx PDF executive summary renders 6 subscores (hygiene, modern_tls, identity_trust, agility_signals, data_at_rest, data_in_motion) in that order. The new data_in_motion item uses class `score-item` and label "Data in Motion".
    - WR-04 (data-at-rest.tsx): Every `<TableHead ...>` in the file has `scope="col"` as an attribute.
    - WR-04 (motion.tsx): Every `<TableHead ...>` in the file has `scope="col"` as an attribute.
    - Counts before/after WR-04: data-at-rest.tsx has 35 TableHead elements (all must get scope="col"); motion.tsx has 13 TableHead elements (all must get scope="col").
  </behavior>
  <action>
**Edit 1 (WR-03 — print.tsx):** Find the line in `src/dashboard/src/pages/print.tsx` that renders `{score.subscores.data_at_rest}` (currently at line ~222). After its enclosing `<div className="score-item">...</div>` block (which ends around line ~225), INSERT this exact JSX block before the closing `</div>` of the parent score-row:

```tsx
              <div className="score-item">
                <div className="score-number">{score.subscores.data_in_motion}</div>
                <div className="score-label">Data in Motion</div>
              </div>
```

The indentation and shape must exactly mirror the existing data_at_rest block. Verify by reading the surrounding context — match the existing whitespace style.

**Edit 2 (WR-04 — data-at-rest.tsx):** Mechanically add `scope="col"` to EVERY `<TableHead` element in `src/dashboard/src/pages/data-at-rest.tsx`. There are 35 TableHead elements across 4 TableHeader blocks (verified by grep). Replace each occurrence of `<TableHead className=` with `<TableHead scope="col" className=`. After the change, every `<TableHead` line in the file must contain `scope="col"`.

You may use a sed-equivalent approach via the Edit tool, but you MUST verify by grep that:
- Number of `<TableHead` occurrences == 35
- Number of `<TableHead scope="col"` occurrences == 35
- Number of `<TableHead className=` (without scope) occurrences == 0

**Edit 3 (WR-04 — motion.tsx):** Same mechanical change for `src/dashboard/src/pages/motion.tsx`. There are 13 TableHead elements across 2 TableHeader blocks. Replace each `<TableHead className=` with `<TableHead scope="col" className=`. After the change:
- Number of `<TableHead` occurrences == 13
- Number of `<TableHead scope="col"` occurrences == 13
- Number of `<TableHead className=` (without scope) occurrences == 0

Do NOT change anything else in any of the 3 files — no className changes, no structural rewrites.
  </action>
  <verify>
    <automated>bash -c 'set -e; grep -q "score.subscores.data_in_motion" src/dashboard/src/pages/print.tsx; grep -q "Data in Motion" src/dashboard/src/pages/print.tsx; DAR_TOTAL=$(grep -c "<TableHead" src/dashboard/src/pages/data-at-rest.tsx); DAR_SCOPED=$(grep -c "<TableHead scope=\"col\"" src/dashboard/src/pages/data-at-rest.tsx); MO_TOTAL=$(grep -c "<TableHead" src/dashboard/src/pages/motion.tsx); MO_SCOPED=$(grep -c "<TableHead scope=\"col\"" src/dashboard/src/pages/motion.tsx); test "$DAR_TOTAL" = "$DAR_SCOPED" -a "$MO_TOTAL" = "$MO_SCOPED" && echo "OK dar=$DAR_TOTAL motion=$MO_TOTAL"'</automated>
  </verify>
  <acceptance_criteria>
    - WR-03: data_in_motion subscore present in print.tsx: `grep -q '{score.subscores.data_in_motion}' src/dashboard/src/pages/print.tsx`
    - WR-03: "Data in Motion" label present: `grep -q 'Data in Motion' src/dashboard/src/pages/print.tsx`
    - WR-03: data_in_motion appears AFTER data_at_rest: `awk '/data_at_rest/{f=NR} /data_in_motion/{if(f && NR>f) print "OK"; exit}' src/dashboard/src/pages/print.tsx | grep -q OK`
    - WR-04 data-at-rest: every TableHead has scope="col": `[ "$(grep -c '<TableHead' src/dashboard/src/pages/data-at-rest.tsx)" = "$(grep -c '<TableHead scope=\"col\"' src/dashboard/src/pages/data-at-rest.tsx)" ]`
    - WR-04 data-at-rest: no TableHead missing scope="col": `! grep -q '<TableHead className=' src/dashboard/src/pages/data-at-rest.tsx`
    - WR-04 motion: every TableHead has scope="col": `[ "$(grep -c '<TableHead' src/dashboard/src/pages/motion.tsx)" = "$(grep -c '<TableHead scope=\"col\"' src/dashboard/src/pages/motion.tsx)" ]`
    - WR-04 motion: no TableHead missing scope="col": `! grep -q '<TableHead className=' src/dashboard/src/pages/motion.tsx`
    - TSX still parses (if dashboard build available): `cd src/dashboard && (npx tsc --noEmit 2>&1 | tee /tmp/tsc.log; grep -q 'error TS' /tmp/tsc.log && exit 1; exit 0)` — skip gracefully if npx/tsc not configured
  </acceptance_criteria>
  <done>print.tsx renders data_in_motion as 6th subscore after data_at_rest with "Data in Motion" label; every TableHead in data-at-rest.tsx (35 total) and motion.tsx (13 total) has scope="col"; no className-only TableHead remains in either file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → /pdf route | External request triggers playwright; bad env config (QUIRK_SERVE_PORT) reaches route handler |
| Browser DOM → screen reader | Accessibility boundary — assistive tech relies on `scope="col"` to associate header cells with column data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-05-01 | Information Disclosure | pdf.py raw ValueError stack trace on bad QUIRK_SERVE_PORT | mitigate | CR-02: catch ValueError, return structured 500 JSON with exact detail "QUIRK_SERVE_PORT is not a valid integer." — no stack trace leakage |
| T-44-05-02 | Denial of Service | Leaked playwright browser process if page op raises (WR-01) | mitigate | WR-01: browser.close() in finally guarantees release on exception path |
| T-44-05-03 | Repudiation | Missing accessibility metadata (no scope="col") affects audit trail of WCAG compliance | mitigate | WR-04: scope="col" on every TableHead in data-at-rest.tsx and motion.tsx |
| T-44-05-04 | Tampering | Unauthorized modification to wait sentinel breaks PDF export | mitigate | Acceptance criteria explicitly grep that `body[data-ready="true"]` selector is preserved |
</threat_model>

<verification>
- `python -m py_compile quirk/dashboard/api/routes/pdf.py && echo OK`
- `grep -c 'except ValueError:' quirk/dashboard/api/routes/pdf.py` >= 1
- `grep -q 'QUIRK_SERVE_PORT is not a valid integer.' quirk/dashboard/api/routes/pdf.py`
- `grep -c 'finally:' quirk/dashboard/api/routes/pdf.py` >= 1
- `grep -q '{score.subscores.data_in_motion}' src/dashboard/src/pages/print.tsx`
- TableHead scope counts equal in both data-at-rest.tsx (35==35) and motion.tsx (13==13)
- `python -m pytest tests/ -m 'not slow' -q` exits 0
- Operator: render PDF with QUIRK_SERVE_PORT="abc" → returns 500 with detail; render PDF normally → finally executes and browser closes; visual PDF includes Data in Motion subscore.
</verification>

<success_criteria>
- pdf.py: ValueError on bad port returns structured 500 JSON; browser.close() lives in finally
- print.tsx: data_in_motion subscore renders as 6th item with label "Data in Motion"
- data-at-rest.tsx: every TableHead carries scope="col" (35/35)
- motion.tsx: every TableHead carries scope="col" (13/13)
- No regressions to existing pytest suite or TSX type checking (if available)
- Phase 43 sentinel selector `body[data-ready="true"]` preserved unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/44-uat-debt-automation/44-05-SUMMARY.md` documenting:
- Confirmation each of the 4 fixes (CR-02, WR-01, WR-03, WR-04) is in place
- Final TableHead counts: data-at-rest.tsx=35/35, motion.tsx=13/13
- Confirmation that WR-05 and WR-06 remain deferred (per D-10)
- Test command results: `python -m pytest tests/ -m 'not slow' -q`
</output>
