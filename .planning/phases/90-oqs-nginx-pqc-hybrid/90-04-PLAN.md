---
phase: 90-oqs-nginx-pqc-hybrid
plan: 04
type: execute
wave: 3
depends_on: [90-01, 90-02, 90-03]
files_modified:
  - quantum-chaos-enterprise-lab/expected_results_v4.md
  - tests/test_pqc_discriminator.py
  - docs/UAT-SERIES.md
autonomous: false
requirements: [PQC-01, PQC-02, PQC-03]
must_haves:
  truths:
    - "expected_results_v4.md ## Profile: oqs-nginx documents the genuine quantum-safe component (or advisory) and the agility before/after uplift"
    - "A discriminator test proves the hybrid-only probe succeeds against oqs-nginx and FAILS against classical tls-modern (no false positives)"
    - "docs/UAT-SERIES.md is updated with the oqs-nginx PQC scenario and synced to Obsidian"
    - "An Obsidian phase note for Phase 90 exists"
  artifacts:
    - path: "quantum-chaos-enterprise-lab/expected_results_v4.md"
      provides: "finalized oqs-nginx oracle: digest, group 4588, ML-DSA-65 cert, CBOM component/advisory, agility uplift"
      contains: "X25519MLKEM768"
    - path: "tests/test_pqc_discriminator.py"
      provides: "false-positive-free discriminator regression"
      min_lines: 20
    - path: "docs/UAT-SERIES.md"
      provides: "oqs-nginx PQC before/after UAT case"
      contains: "oqs-nginx"
  key_links:
    - from: "quantum-chaos-enterprise-lab/expected_results_v4.md"
      to: "quantum-chaos-enterprise-lab/README.md"
      via: "## Profile: oqs-nginx anchor"
      pattern: "profile-oqs-nginx"
---

<objective>
Land the consulting deliverable hook (D-04): the before/after agility demo oracle, the false-positive-free discriminator regression test, and the mandatory phase-completion documentation/Obsidian sync. This plan ties PQC-01/02/03 together against the live `oqs-nginx` profile and proves the headline claim — a PQC-hybrid endpoint outscores an equivalent classical-TLS-only scan on agility.

Purpose: Make the scoring-ceiling anchor demoable and regression-locked, and satisfy the standing CLAUDE.md phase-completion obligations.
Output: Finalized expected_results oracle, a discriminator test, updated+synced UAT-SERIES.md, and a Phase 90 Obsidian note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-oqs-nginx-pqc-hybrid/90-CONTEXT.md
@./CLAUDE.md

@.planning/phases/90-oqs-nginx-pqc-hybrid/90-01-SUMMARY.md
@.planning/phases/90-oqs-nginx-pqc-hybrid/90-02-SUMMARY.md
@.planning/phases/90-oqs-nginx-pqc-hybrid/90-03-SUMMARY.md

<notes>
- Discriminator (spike-verified): hybrid-only probe (`-groups X25519MLKEM768`) SUCCEEDS against oqs-nginx, FAILS against classical `tls-modern:443`. The test asserts both arms. Mark the live-lab arms to skip cleanly when the profiles are not up (don't hard-fail CI in an offline runner) — but the negative arm (classical fails) can be exercised against a local mocked/classical socket if a live target is unavailable.
- Live scans against loopback lab targets need `--allow-internal-targets` and `python run_scan.py` from the repo root (NOT the global shim) — per project gotchas.
- expected_results_v4.md: finalize the `## Profile: oqs-nginx` section started in Plan 01 — replace the Plan-04 TODO marker with the actual genuine-component (KEM, NIST L3) vs advisory outcome and the agility before/after numbers.
- Mandatory phase-completion steps (CLAUDE.md): Obsidian phase note at `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-90-OQS-nginx-PQC-Hybrid.md` (write directly to vault filesystem — files too large for CLI content=), docs/UAT-SERIES.md update + Obsidian sync + commit.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize the oqs-nginx oracle + discriminator regression test</name>
  <files>quantum-chaos-enterprise-lab/expected_results_v4.md, tests/test_pqc_discriminator.py</files>
  <read_first>
    - quantum-chaos-enterprise-lab/expected_results_v4.md (the Plan-01 ## Profile: oqs-nginx stub + TODO marker)
    - quirk/scanner/pqc_probe.py (from Plan 02 — the probe API to call)
    - 90-CONTEXT.md D-04 (oracle contents) + discriminator findings
  </read_first>
  <action>
    Replace the Plan-01 TODO marker in the `## Profile: oqs-nginx` section of expected_results_v4.md with the finalized oracle (D-04): pinned digest, group X25519MLKEM768 (NamedGroup 4588), ML-DSA-65 cert, the genuine `quantum-safe` CBOM algorithm component (KEM, NIST L3) on modern-OpenSSL hosts OR the scoped advisory on old-OpenSSL hosts, and the agility uplift contrast — a scan of oqs-nginx scores higher on the agility subscore than an equivalent classical-TLS-only scan (cite the +8.0 bonus from Plan 03). Create `tests/test_pqc_discriminator.py`: assert the false-positive-free discriminator — the hybrid-only probe reports detected=True against the oqs-nginx target and detected=False against a classical target (`tls-modern:443`). Guard the live-lab positive arm with a skip when the profile is down; the negative arm can run against a classical/mocked endpoint so the no-false-positive guarantee is always exercised.
  </action>
  <verify>
    <automated>QUIRK_DB_PATH=:memory: python -m pytest tests/test_pqc_discriminator.py -q && grep -q 'X25519MLKEM768' quantum-chaos-enterprise-lab/expected_results_v4.md && echo OK</automated>
  </verify>
  <done>expected_results_v4.md oqs-nginx section is finalized with the before/after agility contrast; discriminator test passes (positive arm skips cleanly when lab down, negative arm always runs).</done>
  <acceptance_criteria>
    - Oracle documents genuine-component AND advisory outcomes plus the agility uplift.
    - Discriminator test proves no false positive against a classical endpoint.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify PQC-hybrid agility uplift on the live lab</name>
  <action>Run the live before/after scan (oqs-nginx vs classical) and pause for human confirmation that PQC-hybrid agility exceeds classical agility and the genuine component/advisory appears.</action>
  <what-built>The full PQC-hybrid pipeline against the live lab. Claude has (1) brought up `--profile oqs-nginx` and a classical profile, (2) run `python run_scan.py` (repo root) with `--allow-internal-targets` against both the oqs-nginx port and a classical TLS target, and (3) captured the agility subscore for each.</what-built>
  <how-to-verify>
    1. oqs-nginx scan shows a genuine quantum-safe KEM component (NIST L3) in the CBOM (or the scoped advisory on a pre-3.5 OpenSSL host).
    2. The oqs-nginx scan's agility subscore is strictly higher than the classical-TLS-only scan's agility subscore (the +8.0 bonus, clamped at /25).
    3. The before/after numbers match what expected_results_v4.md now documents.
  </how-to-verify>
  <resume-signal>Type "approved" if the genuine component (or advisory) appears and PQC-hybrid agility > classical agility; otherwise describe the discrepancy.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Mandatory phase-completion — UAT-SERIES, Obsidian sync, phase note</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - ./CLAUDE.md (Mandatory Phase Completion Steps 1-4)
    - docs/UAT-SERIES.md (relevant series + header date)
    - existing /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/ note for the template pattern
  </read_first>
  <action>
    Per CLAUDE.md Mandatory Phase Completion Steps: (1) Write the Obsidian phase note directly to the vault filesystem at `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-90-OQS-nginx-PQC-Hybrid.md` (frontmatter status: complete, type: phase, source, updated; Goal + Requirements Covered PQC-01/02/03 + Success Criteria + What Was Built per-plan from the SUMMARY files + `[[Roadmap]]` link). (2) Update `docs/UAT-SERIES.md`: add the oqs-nginx PQC-hybrid before/after scenario to the relevant series, update the `**Last Updated:**` date. (3) Sync UAT-SERIES.md to Obsidian via the printf/cat/cp pattern in CLAUDE.md (write to vault filesystem — too large for CLI content=). (4) Commit docs/UAT-SERIES.md via the gsd-tools commit per CLAUDE.md.
  </action>
  <verify>
    <automated>grep -q 'oqs-nginx' docs/UAT-SERIES.md && test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-90-OQS-nginx-PQC-Hybrid.md" && echo OK</automated>
  </verify>
  <done>Phase 90 Obsidian note exists; docs/UAT-SERIES.md has the oqs-nginx PQC scenario and updated date; UAT synced to vault; UAT-SERIES.md committed.</done>
  <acceptance_criteria>
    - All four CLAUDE.md completion steps done.
    - Obsidian phase note written to the vault filesystem (not via CLI content=).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live lab scan → host | Demo scan crosses loopback to the lab; requires explicit `--allow-internal-targets` opt-in. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-08 | Information Disclosure | demo scan output in docs | accept | Oracle documents a local lab target only; no production data or secrets surfaced. |
| T-90-09 | Repudiation | before/after claim accuracy | mitigate | Discriminator test + human-verified live before/after lock the claim that PQC-hybrid > classical agility; oracle numbers must match the verified run. |
| T-90-SC | Tampering | dependencies | accept | No new packages; documentation + test only. No `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- `tests/test_pqc_discriminator.py` passes (no-false-positive guarantee).
- Human-verified: live oqs-nginx scan emits the genuine component (or advisory) and PQC-hybrid agility > classical agility.
- expected_results_v4.md, docs/UAT-SERIES.md, and the Obsidian phase note all updated (CLAUDE.md).
</verification>

<success_criteria>
All three ROADMAP success criteria demonstrably tied together against the live lab; the consulting before/after demo (D-04) is documented and regression-locked; mandatory phase-completion steps satisfied.
</success_criteria>

<output>
Create `.planning/phases/90-oqs-nginx-pqc-hybrid/90-04-SUMMARY.md` when done.
</output>
