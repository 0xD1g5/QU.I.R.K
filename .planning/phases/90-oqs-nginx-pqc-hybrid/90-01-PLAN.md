---
phase: 90-oqs-nginx-pqc-hybrid
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quantum-chaos-enterprise-lab/docker-compose.yml
  - quantum-chaos-enterprise-lab/oqs-nginx/nginx.conf
  - quantum-chaos-enterprise-lab/README.md
  - quantum-chaos-enterprise-lab/expected_results_v4.md
autonomous: false
requirements: [PQC-01]
must_haves:
  truths:
    - "docker compose --profile oqs-nginx up starts a digest-pinned OQS-nginx container"
    - "The container serves a TLS 1.3 endpoint negotiating X25519MLKEM768 on a non-colliding host port"
    - "lab.sh profiles auto-derives the new profile with no ALL_PROFILES edit"
    - "README profile table and expected_results_v4.md document the new profile in the same commit"
  artifacts:
    - path: "quantum-chaos-enterprise-lab/docker-compose.yml"
      provides: "oqs-nginx service, digest-pinned, profile oqs-nginx, port 39444:443"
      contains: "sha256:6ca18ac692f347ea9d4c3fdab4231189f2146570cd03c4d8fb486bba208ef870"
    - path: "quantum-chaos-enterprise-lab/oqs-nginx/nginx.conf"
      provides: "ssl_ecdh_curve X25519MLKEM768 deterministic config"
      contains: "X25519MLKEM768"
    - path: "quantum-chaos-enterprise-lab/README.md"
      provides: "oqs-nginx row in Profile Summary table"
      contains: "oqs-nginx"
  key_links:
    - from: "quantum-chaos-enterprise-lab/docker-compose.yml"
      to: "quantum-chaos-enterprise-lab/oqs-nginx/nginx.conf"
      via: "volume mount"
      pattern: "oqs-nginx/nginx.conf"
---

<objective>
Stand up the digest-pinned OQS-nginx chaos-lab profile (PQC-01): an `openquantumsafe/nginx` container serving a TLS 1.3 endpoint that negotiates the X25519MLKEM768 hybrid group with an ML-DSA-65 certificate. This is the empirical anchor every other plan in the phase depends on.

Purpose: Provide a real, reproducible PQC-hybrid endpoint so detection (PQC-02) and scoring (PQC-03) can be exercised against live traffic, and so the consulting before/after demo (D-04) has a concrete target.
Output: New `oqs-nginx` profile in the chaos lab with full four-file lab-sync (compose + auto-derived lab.sh + README + expected_results_v4.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-oqs-nginx-pqc-hybrid/90-CONTEXT.md
@./CLAUDE.md

# Pattern to mirror — the most recent TLS lab profile (grpc-tls, port 39443):
@quantum-chaos-enterprise-lab/docker-compose.yml  # lines ~1224-1245 (grpc-tls block)

<notes>
- Port collision check (done at planning time): host ports 39443 (grpc-tls), 39432, 39379/39380, 39092/39093 are taken. **39444 is free** and follows the 39xxx-for-TLS lab convention. Re-verify against docker-compose.yml at execution time before binding.
- Image MUST be pinned by digest: `openquantumsafe/nginx@sha256:6ca18ac692f347ea9d4c3fdab4231189f2146570cd03c4d8fb486bba208ef870` (built 2026-05-18). NEVER `:latest` — oqs-provider renames group names across releases (D-02, v5.0-D-04).
- `lab.sh profiles` auto-derives from docker-compose.yml as of Phase 89 — confirm no manual ALL_PROFILES edit is needed (CLAUDE.md Chaos Lab Maintenance rule); verify by running `./lab.sh profiles` and checking `oqs-nginx` appears.
- Loopback bind convention: lab targets bind to loopback; live scans need `--allow-internal-targets`. Document this in the expected_results section.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the oqs-nginx service + nginx.conf to docker-compose.yml</name>
  <files>quantum-chaos-enterprise-lab/docker-compose.yml, quantum-chaos-enterprise-lab/oqs-nginx/nginx.conf</files>
  <read_first>
    - quantum-chaos-enterprise-lab/docker-compose.yml (grpc-tls block ~1224-1245 — mirror its profile/hostname/ports/healthcheck shape)
    - 90-CONTEXT.md D-02 (digest, group target, port guidance)
  </read_first>
  <action>
    Add an `oqs-nginx` service to docker-compose.yml mirroring the grpc-tls block's structure (profiles, hostname, ports, healthcheck). Pin the image by digest exactly: `openquantumsafe/nginx@sha256:6ca18ac692f347ea9d4c3fdab4231189f2146570cd03c4d8fb486bba208ef870` (per D-02 — never `:latest`). Set `profiles: ["oqs-nginx"]`, `hostname: oqs-nginx`, and bind a verified-free host port to container 443 — use `39444:443` (re-confirm 39444 is uncollided against the full ports list before committing; pick the next free 39xxx slot if taken). Add a healthcheck (`nc -z localhost 443` shape, mirroring grpc-tls). Mount a determinism config: create `quantum-chaos-enterprise-lab/oqs-nginx/nginx.conf` pinning `ssl_ecdh_curve X25519MLKEM768` (per D-02 discretion — pin for reproducibility) on a TLS 1.3 server block, and mount it read-only into the image's nginx config path. Keep the default image's ML-DSA-65 cert/server cert unless the image requires the conf to reference them explicitly — minimal diff: pin only the curve and TLS version, reuse the image's bundled cert paths.
  </action>
  <verify>
    <automated>cd quantum-chaos-enterprise-lab && docker compose config --profile oqs-nginx 2>&1 | grep -q 'sha256:6ca18ac6' && grep -q 'X25519MLKEM768' oqs-nginx/nginx.conf && echo OK</automated>
  </verify>
  <done>docker-compose.yml has an oqs-nginx service pinned by the exact digest on a free 39xxx port; oqs-nginx/nginx.conf pins X25519MLKEM768 on TLS 1.3; `docker compose config` validates.</done>
  <acceptance_criteria>
    - Service `oqs-nginx` exists with `profiles: ["oqs-nginx"]` and the exact digest (no `:latest`).
    - Host port is verified-free (not in the existing port list) and maps to 443.
    - nginx.conf pins ssl_ecdh_curve X25519MLKEM768 and is mounted read-only.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify oqs-nginx negotiates X25519MLKEM768</name>
  <action>Bring the profile up and probe the endpoint, then pause for human confirmation of the negotiated group and ML-DSA-65 cert.</action>
  <what-built>The oqs-nginx profile and its X25519MLKEM768 nginx config. Claude has run `docker compose --profile oqs-nginx up -d`, waited for the healthcheck, and probed the endpoint with `openssl s_client -connect 127.0.0.1:39444 -groups X25519MLKEM768 </dev/null 2>/dev/null | grep -i 'Negotiated.*group'`.</what-built>
  <how-to-verify>
    1. Confirm the container is healthy: `cd quantum-chaos-enterprise-lab && docker compose --profile oqs-nginx ps`.
    2. Confirm the negotiated group: `echo | openssl s_client -connect 127.0.0.1:39444 -groups X25519MLKEM768 2>/dev/null | grep -i 'Negotiated TLS1.3 group'` should print `Negotiated TLS1.3 group: X25519MLKEM768` (requires host OpenSSL >= 3.5; on older hosts the handshake will fail — that is the advisory case handled in Plan 02, not a profile defect).
    3. Confirm the cert is ML-DSA-65: `echo | openssl s_client -connect 127.0.0.1:39444 -groups X25519MLKEM768 2>/dev/null | grep -i 'Peer signature type'` should mention `mldsa65`.
  </how-to-verify>
  <resume-signal>Type "approved" if the endpoint negotiates X25519MLKEM768 (or describe the openssl output if it differs).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Complete four-file lab-sync — lab.sh confirm, README, expected_results stub</name>
  <files>quantum-chaos-enterprise-lab/README.md, quantum-chaos-enterprise-lab/expected_results_v4.md</files>
  <read_first>
    - quantum-chaos-enterprise-lab/README.md (Profile Summary table, grpc-tls/kafka-tls rows as the template)
    - quantum-chaos-enterprise-lab/expected_results_v4.md (a recent `## Profile:` section for the network-listener schema: Port | Service | Expected protocol | Expected condition / tag | Notes)
    - ./CLAUDE.md Chaos Lab Maintenance rule
  </read_first>
  <action>
    Per the CLAUDE.md Chaos Lab Maintenance rule (lab.sh + README + expected_results in the SAME change as the profile): (1) Run `./lab.sh profiles` and confirm `oqs-nginx` is auto-derived — if it is NOT, investigate; do NOT manually edit ALL_PROFILES unless the auto-derive demonstrably fails (Phase 89 made it auto-derive). (2) Add an `oqs-nginx` row to the README Profile Summary table (Profile | Services | Published Ports | Expected Findings link | Notes) — Notes should record "v5.0; digest-pinned openquantumsafe/nginx; X25519MLKEM768 hybrid; needs --allow-internal-targets for live scan". Bump the profile count in the README intro line. (3) Add a `## Profile: oqs-nginx` section to expected_results_v4.md using the network-listener schema. Document: the host port, service `oqs-nginx`, expected protocol TLS 1.3, the negotiated group X25519MLKEM768 (NamedGroup 4588), the ML-DSA-65 cert, and the loopback/`--allow-internal-targets` note. Leave the genuine-CBOM-component vs advisory detail and the agility before/after as a TODO marker referencing Plan 90-04 (which finalizes the demo oracle after detection + scoring land).
  </action>
  <verify>
    <automated>cd quantum-chaos-enterprise-lab && ./lab.sh profiles | grep -q oqs-nginx && grep -q 'oqs-nginx' README.md && grep -q '## Profile: oqs-nginx' expected_results_v4.md && echo OK</automated>
  </verify>
  <done>`lab.sh profiles` lists oqs-nginx; README table has an oqs-nginx row with correct port; expected_results_v4.md has a `## Profile: oqs-nginx` section anchored for the README link.</done>
  <acceptance_criteria>
    - All four lab-sync surfaces are consistent (compose, lab.sh auto-derive, README, expected_results) in this plan's commit.
    - No manual ALL_PROFILES edit unless auto-derive provably failed.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lab container → host network | The oqs-nginx container exposes a TLS listener; per lab convention it binds loopback, so live external scans require `--allow-internal-targets`. |
| pinned image → supply chain | The OQS-nginx image is third-party; digest-pinning is the supply-chain control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01 | Tampering | openquantumsafe/nginx image | mitigate | Pin by sha256 digest (D-02); `:latest` forbidden — group-name drift across oqs-provider releases is a silent-break vector. |
| T-90-02 | Information Disclosure | loopback-bound lab port | accept | Lab is a local chaos sandbox; loopback bind + explicit `--allow-internal-targets` opt-in for live scans is the existing lab-wide control. |
| T-90-SC | Tampering | docker image pull | mitigate | Digest pin is the only install surface in this plan; no npm/pip/cargo installs. No `[ASSUMED]`/`[SUS]` packages introduced. |
</threat_model>

<verification>
- `docker compose config --profile oqs-nginx` validates and shows the pinned digest (Success Criterion 1).
- `openssl s_client -connect <host:port> -groups X25519MLKEM768` negotiates X25519MLKEM768 on a host with OpenSSL >= 3.5 (Success Criterion 1, human-verified).
- `./lab.sh profiles` auto-lists oqs-nginx; README + expected_results updated in the same commit (CLAUDE.md rule).
</verification>

<success_criteria>
ROADMAP Success Criterion 1 satisfied: `docker compose --profile oqs-nginx up` starts a digest-pinned container serving an X25519MLKEM768 hybrid TLS endpoint, with lab-sync obligations met in the same commit.
</success_criteria>

<output>
Create `.planning/phases/90-oqs-nginx-pqc-hybrid/90-01-SUMMARY.md` when done.
</output>
