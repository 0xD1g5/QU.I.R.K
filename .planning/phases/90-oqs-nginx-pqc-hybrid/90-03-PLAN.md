---
phase: 90-oqs-nginx-pqc-hybrid
plan: 03
type: execute
wave: 2
depends_on: [90-02]
files_modified:
  - quirk/intelligence/scoring.py
  - tests/test_score_weights_invariant.py
  - tests/test_pqc_agility_bonus.py
autonomous: true
requirements: [PQC-03]
must_haves:
  truths:
    - "SCORE_WEIGHTS gains an agility PQC-hybrid bonus key keyed off pqc_hybrid_endpoint_count"
    - "A scan with a PQC-hybrid endpoint scores higher on the agility subscore than an equivalent classical-TLS-only scan"
    - "The bonus is clamped so it cannot push the agility subscore above 25 (Phase 88 orthogonal /25 constraint)"
    - "test_score_weights_invariant.py asserts the new sum AND count atomically"
    - "The bonus does not alter the other five pillars"
  artifacts:
    - path: "quirk/intelligence/scoring.py"
      provides: "agility PQC-hybrid bonus weight + agility_impacts entry reading pqc_hybrid_endpoint_count"
      contains: "pqc_hybrid"
    - path: "tests/test_score_weights_invariant.py"
      provides: "updated sum (275.0 + bonus) and count (37) assertions"
      contains: "37"
    - path: "tests/test_pqc_agility_bonus.py"
      provides: "PQC-hybrid > classical agility regression + /25 clamp test"
      min_lines: 25
  key_links:
    - from: "quirk/intelligence/scoring.py"
      to: "quirk/intelligence/evidence.py"
      via: "evidence.get('pqc_hybrid_endpoint_count')"
      pattern: "pqc_hybrid_endpoint_count"
---

<objective>
Reward PQC-hybrid posture in the readiness model (PQC-03): add ONE new `agility` bonus weight to `SCORE_WEIGHTS` driven by the `pqc_hybrid_endpoint_count` evidence counter from Plan 02, clamped to the Phase 88 orthogonal /25 agility budget, so a PQC-hybrid endpoint visibly outscores an equivalent classical-TLS-only target on the agility dimension.

Purpose: Make PQC-hybrid the clear top agility signal — the scoring-ceiling anchor and the consulting before/after hook.
Output: One new bonus weight + agility_impacts entry, the invariant test updated atomically (sum + count), and a regression test proving PQC-hybrid > classical and the /25 clamp.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-oqs-nginx-pqc-hybrid/90-CONTEXT.md
@./CLAUDE.md

@quirk/intelligence/scoring.py
@tests/test_score_weights_invariant.py

# Depends on Plan 02 for the evidence key it reads:
@.planning/phases/90-oqs-nginx-pqc-hybrid/90-02-SUMMARY.md

<interfaces>
<!-- Contracts extracted from the codebase. -->

From quirk/intelligence/scoring.py:
```python
# SCORE_WEIGHTS (line 20): currently 36 keys, sum == 275.0.
# Existing positive bonuses (the magnitude reference):
#   "identity_mtls_ratio_bonus": 6.0,   (line 30)
#   "agility_has_ecdsa_bonus":   4.0,   (line 57)
# agility_impacts block (lines 209-218): list of (label, points) tuples;
#   positive points are bonuses, negatives are penalties.
# _apply_weighted_impacts(impacts, score_cap=25.0) (line 103) ALREADY clamps the
#   subscore total to [0, 25] via _clamp — so an agility bonus is INHERENTLY
#   capped at 25; do not add a second clamp, rely on the existing score_cap=25.
# PROFILE_MULTIPLIERS apply an "agility_" prefix multiplier — name the new key with
#   the "agility_" prefix so profile scaling applies consistently.
```

From quirk/intelligence/evidence.py (Plan 02 output):
```python
"pqc_hybrid_endpoint_count": int   # increments on confirmed PQC-hybrid endpoints (genuine OR advisory, D-05)
```
</interfaces>

<notes>
- **Magnitude (D-03):** make PQC-hybrid the clear top agility signal — must exceed the existing positive bonuses (`agility_has_ecdsa_bonus=4.0`, `identity_mtls_ratio_bonus=6.0`). **Use +8.0** (the D-03 recommended starting point; planner-confirmed value for this plan).
- **Invariant gate:** `tests/test_score_weights_invariant.py` currently asserts `sum == 275.0` AND `len == 36`. Adding one +8.0 key → `sum == 283.0`, `len == 37`. Update BOTH assertions in the SAME change or CI fails. Update the docstrings to record the +1 entry / +8.0 delta (mirror the Phase 83 rebalance note style).
- **Orthogonal /25 constraint (Phase 88):** the agility subscore is already clamped to [0,25] by `_apply_weighted_impacts(score_cap=25.0)`. Add the bonus as a positive entry in `agility_impacts`; the existing clamp guarantees agility cannot exceed 25. Do NOT touch the other five impact blocks (no cross-pillar leakage).
- **Shape:** mirror the existing bonus pattern. Read the counter (`max(0, _as_int(evidence.get("pqc_hybrid_endpoint_count", 0)))`) and append a positive agility_impacts entry when count > 0. A presence bonus (full +8.0 when count>0) is acceptable and matches the "ceiling anchor" intent; a ratio form is also fine if it stays a clear top signal — keep it simple and deterministic.
- Run `python -m compileall quirk` and the full scoring test suite (CLAUDE.md).
</notes>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the agility PQC-hybrid bonus + update the invariant test atomically</name>
  <files>quirk/intelligence/scoring.py, tests/test_score_weights_invariant.py, tests/test_pqc_agility_bonus.py</files>
  <read_first>
    - quirk/intelligence/scoring.py (SCORE_WEIGHTS line 20-58; agility_impacts 209-218; _apply_weighted_impacts 103-111)
    - tests/test_score_weights_invariant.py (both assertions + docstrings)
    - 90-CONTEXT.md D-03 (magnitude, clamp, invariant)
  </read_first>
  <behavior>
    - invariant: sum(SCORE_WEIGHTS.values()) == 283.0 and len(SCORE_WEIGHTS) == 37.
    - agility uplift: compute_readiness_score on evidence with pqc_hybrid_endpoint_count=1 yields a strictly higher agility subscore than the same evidence with pqc_hybrid_endpoint_count=0 (all else equal).
    - clamp: even with pqc_hybrid_endpoint_count set and no penalties, the agility subscore is <= 25 (never exceeds the pillar cap).
    - orthogonality: the five non-agility subscores are identical between the count=1 and count=0 evidence inputs.
  </behavior>
  <action>
    Add a new key to `SCORE_WEIGHTS` with the `agility_` prefix — `"agility_pqc_hybrid_bonus": 8.0` (per D-03, exceeds the 6.0/4.0 existing bonuses). In the `agility_impacts` block (lines 209-218), read `pqc_hybrid_count = max(0, _as_int(evidence.get("pqc_hybrid_endpoint_count", 0)))` and append a positive entry `("PQC-hybrid key exchange (X25519MLKEM768)", w["agility_pqc_hybrid_bonus"])` when `pqc_hybrid_count > 0`. Rely on the existing `_apply_weighted_impacts(score_cap=25.0)` clamp for the /25 ceiling — do not add a separate clamp and do not touch any other impact block. Update `tests/test_score_weights_invariant.py`: change the sum assertion to 283.0 and the count to 37, and amend both docstrings to record the Phase 90 PQC-03 delta (+1 entry / +8.0). Create `tests/test_pqc_agility_bonus.py` covering the four behaviors above (uplift, clamp, orthogonality, presence).
  </action>
  <verify>
    <automated>QUIRK_DB_PATH=:memory: python -m pytest tests/test_score_weights_invariant.py tests/test_pqc_agility_bonus.py -x -q && python -m compileall quirk >/dev/null && echo OK</automated>
  </verify>
  <done>SCORE_WEIGHTS has 37 keys summing to 283.0; agility bonus reads pqc_hybrid_endpoint_count; PQC-hybrid scan beats classical on agility; agility clamped at 25; other pillars unchanged; both invariant assertions updated.</done>
  <acceptance_criteria>
    - Exactly ONE new weight added (no rebalance of existing weights).
    - sum==283.0 and len==37 both asserted and passing.
    - Agility uplift proven; /25 clamp proven; cross-pillar orthogonality proven.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence dict → scoring math | The counter is internally produced (Plan 02), but scoring must tolerate missing/garbage values without crashing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-06 | Tampering | pqc_hybrid_endpoint_count read in scoring | mitigate | Read via `max(0, _as_int(evidence.get(..., 0)))` — non-int/negative/missing values coerce to 0, never raise, never produce a negative or unbounded bonus. |
| T-90-07 | Elevation (score inflation) | agility bonus magnitude | mitigate | Bonus is a single positive agility_impacts entry under the existing score_cap=25 clamp; cannot inflate overall beyond the orthogonal pillar budget; invariant test forward-locks the weight set. |
| T-90-SC | Tampering | dependencies | accept | No new packages; pure scoring-math change. No `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- `test_score_weights_invariant.py` passes with sum 283.0 / count 37.
- `test_pqc_agility_bonus.py` proves PQC-hybrid agility > classical agility, /25 clamp, and cross-pillar orthogonality.
- `python -m compileall quirk` clean (CLAUDE.md).
</verification>

<success_criteria>
ROADMAP Success Criterion 3 satisfied: SCORE_WEIGHTS gains a pqc_hybrid agility bonus key, the invariant test is updated, and a PQC-hybrid scan scores higher on agility than an equivalent classical-TLS-only scan.
</success_criteria>

<output>
Create `.planning/phases/90-oqs-nginx-pqc-hybrid/90-03-SUMMARY.md` when done.
</output>
