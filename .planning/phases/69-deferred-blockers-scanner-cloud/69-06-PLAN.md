---
phase: 69
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/engine/rate_limiter.py
  - tests/test_rate_limiter.py
autonomous: true
requirements: [BLOCK-06]
must_haves:
  truths:
    - "TokenBucket.acquire(tokens > capacity) raises ValueError immediately"
    - "TokenBucket.acquire blocks via threading.Condition.wait, not time.sleep busy-wait"
    - "TokenBucket(rate=0).acquire() returns immediately (unlimited fast path)"
  artifacts:
    - path: quirk/engine/rate_limiter.py
      provides: "TokenBucket using threading.Condition with capacity guard"
    - path: tests/test_rate_limiter.py
      provides: "New unit tests for capacity guard, Condition wakeup, fast path"
  key_links:
    - from: quirk/engine/rate_limiter.py
      to: quirk/run_scan.py
      via: "TokenBucket(rate, capacity).acquire()"
      pattern: "TokenBucket\\("
---

<objective>
Close BLOCK-06 (CR-07, CR-08): TokenBucket loops forever when `tokens > capacity` (CR-07) and uses `time.sleep(0.01)` busy-wait under contention (CR-08). Per D-01..D-03, raise ValueError on oversized acquires, replace lock+sleep with `threading.Condition`, and short-circuit when rate <= 0.

Purpose: Eliminates infinite loop hazard and thundering-herd contention on cloud connectors that share a TokenBucket.
Output: Rewritten `TokenBucket.__init__` and `acquire`; new `tests/test_rate_limiter.py` from scratch (zero existing tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-deferred-blockers-scanner-cloud/69-CONTEXT.md
@.planning/phases/69-deferred-blockers-scanner-cloud/69-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace TokenBucket lock+sleep with threading.Condition + capacity guard</name>
  <files>quirk/engine/rate_limiter.py, tests/test_rate_limiter.py</files>
  <read_first>
    - quirk/engine/rate_limiter.py (full file — current 33-line implementation)
    - .planning/phases/69-deferred-blockers-scanner-cloud/69-CONTEXT.md §D-02 code snippet (verbatim shape)
    - quirk/run_scan.py around line 716 (sole caller; verify it never passes tokens=)
  </read_first>
  <behavior>
    - test_acquire_raises_when_tokens_exceed_capacity: TokenBucket(rate=10, capacity=5).acquire(tokens=6) raises ValueError; message mentions "tokens" and "capacity"
    - test_unlimited_rate_fast_path: TokenBucket(rate=0).acquire() returns immediately (no exception, no blocking) — wrap in 100ms timeout via threading.Timer or just measure perf_counter
    - test_acquire_blocks_via_condition_no_busy_wait: monkeypatch time.sleep to raise AssertionError; call TokenBucket(rate=1, capacity=1).acquire(); assert no time.sleep was hit (must use Condition.wait instead). Use a separate test that drains tokens then re-acquires to force the wait path; assert it eventually returns within ~2s.
    - test_acquire_default_token_one_succeeds: TokenBucket(rate=10, capacity=10).acquire() with default tokens=1.0 returns without raising
  </behavior>
  <action>
    1. In `quirk/engine/rate_limiter.py`:
       - In `__init__`, replace `self.lock = threading.Lock()` with `self._cond = threading.Condition()`. Keep all other __init__ fields unchanged.
       - Replace the entire `acquire` method body with the verbatim shape from CONTEXT.md D-02:
         ```
         if self.rate <= 0: return
         if tokens > self.capacity: raise ValueError(f"tokens {tokens} > capacity {self.capacity}")
         with self._cond: while True: <refill>; if enough: deduct, notify_all, return; else: self._cond.wait(timeout=wait_secs)
         ```
       - Do NOT keep both `self.lock` and `self._cond`. Do NOT keep any `time.sleep` call. Minimal diff: only __init__ and acquire change; module-level imports remain (`threading`, `time` still needed for `time.perf_counter`).
    2. Create `tests/test_rate_limiter.py` from scratch with the four tests in <behavior>. Use `monkeypatch.setattr("quirk.engine.rate_limiter.time.sleep", _raise_if_called)` for the no-busy-wait assertion. For the wait-path test, drain the bucket then call acquire in a `threading.Thread` and join with timeout=2.0; assert thread finished.
  </action>
  <verify>
    <automated>pytest tests/test_rate_limiter.py -x -q exits 0 AND python -m compileall quirk/engine/rate_limiter.py exits 0 AND grep -c 'time.sleep' quirk/engine/rate_limiter.py | tr -d '\n' returns "0"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_rate_limiter.py::test_acquire_raises_when_tokens_exceed_capacity -x -q` exits 0
    - `pytest tests/test_rate_limiter.py::test_unlimited_rate_fast_path -x -q` exits 0
    - `pytest tests/test_rate_limiter.py::test_acquire_blocks_via_condition_no_busy_wait -x -q` exits 0
    - `grep -n 'time.sleep' quirk/engine/rate_limiter.py` returns no matches
    - `grep -n '_cond' quirk/engine/rate_limiter.py` returns at least 3 matches (init, with, notify_all/wait)
    - `grep -n 'self.lock' quirk/engine/rate_limiter.py` returns no matches
    - `python -m compileall quirk/engine/rate_limiter.py` exits 0
  </acceptance_criteria>
  <done>TokenBucket uses Condition, raises on oversized acquires, and has a fast path for rate=0; new tests cover all three behaviors.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_rate_limiter.py -x -q` green
- No `time.sleep` references in rate_limiter.py
- `python -m compileall quirk/engine/rate_limiter.py` green
</verification>

<success_criteria>
TokenBucket eliminates infinite loop and busy-wait; full test coverage from scratch.
</success_criteria>

<output>
Create `.planning/phases/69-deferred-blockers-scanner-cloud/69-06-SUMMARY.md`.
</output>
