---
phase: 69
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/engine/cache.py
  - tests/test_cache.py
  - docs/UAT-SERIES.md
autonomous: true
requirements: [BLOCK-05]
must_haves:
  truths:
    - "load_cache(dir, key, ttl_hours=0) returns None even when a fresh cache file exists"
    - "load_cache(dir, key, ttl_hours>0) still honors fresh files within window"
    - "UAT-SERIES.md documents the load_cache(ttl_hours=0) internal-API behavior change (no CLI flag exists today — this is an API contract note for future callers)"
  artifacts:
    - path: quirk/engine/cache.py
      provides: "load_cache treats ttl_hours <= 0 as cache-disabled"
    - path: tests/test_cache.py
      provides: "Coverage for ttl=0 and ttl>0 paths"
    - path: docs/UAT-SERIES.md
      provides: "Note that load_cache(ttl_hours=0) now returns None (was: returned cached object)"
  key_links:
    - from: quirk/engine/cache.py
      to: quirk/engine/cache.py:load_cache
      via: "internal API; no production callers today (verified 2026-05-14)"
      pattern: "load_cache\\("
---

<objective>
Close BLOCK-05 (CR-06): `cache.py:load_cache` currently returns the cached object when `ttl_hours <= 0`, the opposite of intended semantics. Per D-10, invert that branch so `ttl_hours <= 0` means "cache disabled, never return". Document the API behavior change in UAT-SERIES.md (mandatory CLAUDE.md step).

**Note:** `load_cache` has zero production callers today (verified 2026-05-14: `grep -rn 'load_cache' quirk/` returns only `cache.py` itself; no `--cache-ttl-hours` CLI flag exists). The fix preempts misuse by future callers — UAT-SERIES note describes the API contract, not a user-facing CLI change.

Purpose: Restores intuitive API semantics — `ttl_hours=0` means "cache disabled" (was: "cache forever").
Output: 1-line fix in `cache.py`; new `tests/test_cache.py`; UAT-SERIES.md API-contract note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-deferred-blockers-scanner-cloud/69-CONTEXT.md
@.planning/phases/69-deferred-blockers-scanner-cloud/69-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Invert ttl_hours <= 0 branch and create test_cache.py</name>
  <files>quirk/engine/cache.py, tests/test_cache.py</files>
  <read_first>
    - quirk/engine/cache.py (full file; focus on load_cache lines 50-63)
    - quirk/run_scan.py — confirm zero callers of `load_cache` (grep across quirk/ to verify; no `--cache-ttl-hours` CLI flag should be found)
  </read_first>
  <behavior>
    - test_ttl_zero_returns_none_on_fresh_file: write a fresh cache file via save_cache, call load_cache(dir, key, ttl_hours=0), assert return is None
    - test_ttl_negative_returns_none_on_fresh_file: same with ttl_hours=-1, assert None
    - test_ttl_positive_returns_obj_when_fresh: save_cache then load_cache(dir, key, ttl_hours=1), assert return is dict containing _cached_at
    - test_ttl_positive_returns_none_when_stale: save_cache, monkeypatch _now to return ts + 3600*2, ttl_hours=1, assert None
  </behavior>
  <action>
    1. In `quirk/engine/cache.py`, change line 60 from `return obj` to `return None` inside the `if ttl_hours <= 0:` branch. This is the entire production-code change. Minimal diff per CLAUDE.md.
    2. Create `tests/test_cache.py` from scratch with the four tests above. Use `tmp_path` for output_dir. Use `monkeypatch.setattr("quirk.engine.cache._now", lambda: ...)` for the staleness test.
    3. Each test calls `save_cache(str(tmp_path), "k", {"foo": "bar"})` then `load_cache(str(tmp_path), "k", ttl_hours=N)`.
  </action>
  <verify>
    <automated>pytest tests/test_cache.py -x -q exits 0 AND python -m compileall quirk/engine/cache.py exits 0</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_cache.py::test_ttl_zero_returns_none_on_fresh_file -x -q` exits 0
    - `grep -n 'return None' quirk/engine/cache.py` includes the line inside `if ttl_hours <= 0:` branch (line ~60)
    - `grep -n 'return obj' quirk/engine/cache.py` no longer appears in the `ttl_hours <= 0` branch
    - `python -m compileall quirk/engine/cache.py` exits 0
  </acceptance_criteria>
  <done>cache.py honors ttl_hours <= 0 as cache-disabled; tests cover all four paths.</done>
</task>

<task type="auto">
  <name>Task 2: Document load_cache(ttl_hours=0) API contract change in UAT-SERIES.md</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (locate the relevant series section; check existing format/style)
  </read_first>
  <action>
    Add a note to the relevant UAT-SERIES section (run-scan / discovery cache series, or a new "Internal API contracts" subsection if no cache series exists) describing:
    - "**API contract change (Phase 69 / BLOCK-05):** `quirk.engine.cache.load_cache(..., ttl_hours=0)` now returns `None` (cache disabled). Previously, 0 was treated as 'cache forever' and returned the cached object. Negative values are also treated as cache-disabled. Affects future callers; no production callers exist as of Phase 69."
    - Update the `**Last Updated:**` date at the top of UAT-SERIES.md to today's ISO date (2026-05-14).
    Minimal diff: do not restructure other content. Pure additive.
  </action>
  <verify>
    <automated>grep -n 'BLOCK-05' docs/UAT-SERIES.md returns at least 1 line AND grep -n 'ttl_hours=0\|ttl_hours = 0' docs/UAT-SERIES.md returns at least 1 line</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'BLOCK-05' docs/UAT-SERIES.md` returns at least one match
    - `grep -n 'load_cache' docs/UAT-SERIES.md` returns the new note
    - `**Last Updated:**` line at top of file shows 2026-05-14
  </acceptance_criteria>
  <done>UAT-SERIES.md flags the load_cache(ttl_hours=0) API contract change.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_cache.py -x -q` green
- `python -m compileall quirk/engine/cache.py` green
- UAT-SERIES.md updated and dated
</verification>

<success_criteria>
ttl_hours <= 0 reliably bypasses cache; behavior change documented for end users.
</success_criteria>

<output>
Create `.planning/phases/69-deferred-blockers-scanner-cloud/69-05-SUMMARY.md`.
</output>
