---
phase: 69
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/scanner/gcp_connector.py
  - tests/test_cloud_connectors.py
autonomous: true
requirements: [BLOCK-02]
must_haves:
  truths:
    - "GCP Cloud SQL findings carry SSL enforcement severity in CryptoEndpoint.severity (not cert_pubkey_alg)"
    - "Cloud SQL findings leave cert_pubkey_alg None/empty"
    - "Existing pytest cases reflect corrected behavior"
  artifacts:
    - path: quirk/scanner/gcp_connector.py
      provides: "_scan_cloud_sql writes severity to severity column, description to service_detail"
    - path: tests/test_cloud_connectors.py
      provides: "Updated assertions for cloud_sql_plaintext_allowed, _encrypted_only, _null_ssl_mode"
  key_links:
    - from: quirk/scanner/gcp_connector.py
      to: quirk/models.py:CryptoEndpoint.severity
      via: "kwarg severity=..."
      pattern: "severity="
---

<objective>
Close BLOCK-02 (CR-02): GCP Cloud SQL connector currently stuffs SSL enforcement severity strings ("HIGH"/"MEDIUM") into `cert_pubkey_alg` (a public-key algorithm column). Move severity into the `severity` column, description into `service_detail`, and rewrite the three existing tests that assert the buggy behavior.

Purpose: Stops CBOM classifier from treating "HIGH" as an algorithm name; restores semantic meaning to cert_pubkey_alg.
Output: Corrected `_scan_cloud_sql`; three rewritten test cases asserting `ep.severity` and `ep.cert_pubkey_alg in (None, "")`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-deferred-blockers-scanner-cloud/69-CONTEXT.md
@.planning/phases/69-deferred-blockers-scanner-cloud/69-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move SSL severity from cert_pubkey_alg to severity in _scan_cloud_sql</name>
  <files>quirk/scanner/gcp_connector.py</files>
  <read_first>
    - quirk/scanner/gcp_connector.py (focus on _scan_cloud_sql, ~lines 240-290)
    - quirk/models.py (confirm CryptoEndpoint.severity column exists; finding_id does NOT)
    - .planning/phases/69-deferred-blockers-scanner-cloud/69-RESEARCH.md §"GCP Cloud SQL severity fix (BLOCK-02)" code example
  </read_first>
  <action>
    In `_scan_cloud_sql`, locate the CryptoEndpoint construction (currently around line 267 per RESEARCH) that emits `cert_pubkey_alg=severity_string`. Per D-08:
    - Pass severity string ("HIGH"/"MEDIUM") to the `severity=` kwarg of CryptoEndpoint.
    - Move the human description into `service_detail=f"CLOUD_SQL/{description.replace(' ', '-')}"`.
    - Do NOT pass `cert_pubkey_alg` for Cloud SQL findings (omit the kwarg entirely; default is None).
    - Preserve `cloud_scan_json={"sslMode": ssl_mode, "finding": description}` as-is.
    - Keep all other field assignments (host, port, protocol="CLOUD_SQL") unchanged. Minimal diff per CLAUDE.md.
  </action>
  <verify>
    <automated>grep -n 'cert_pubkey_alg' quirk/scanner/gcp_connector.py | grep -i 'sql\|severity\|HIGH\|MEDIUM' returns nothing AND python -m compileall quirk/scanner/gcp_connector.py exits 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'cert_pubkey_alg' quirk/scanner/gcp_connector.py` shows no SSL-status writes (only legitimate cert algorithm writes, if any)
    - The CryptoEndpoint emitted by `_scan_cloud_sql` for an unencrypted instance has `severity="HIGH"` (or "MEDIUM") and `cert_pubkey_alg is None`
    - `service_detail` starts with `"CLOUD_SQL/"`
    - `python -m compileall quirk/scanner/gcp_connector.py` exits 0
  </acceptance_criteria>
  <done>_scan_cloud_sql writes severity to severity column, description to service_detail, and never to cert_pubkey_alg.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite three existing Cloud SQL pytest assertions</name>
  <files>tests/test_cloud_connectors.py</files>
  <read_first>
    - tests/test_cloud_connectors.py (focus on test_gcp_cloud_sql_plaintext_allowed, test_gcp_cloud_sql_encrypted_only, test_gcp_cloud_sql_null_ssl_mode — currently lines ~224, ~247, ~292 per RESEARCH)
    - quirk/scanner/gcp_connector.py (post-Task-1 state)
  </read_first>
  <behavior>
    - test_gcp_cloud_sql_plaintext_allowed: assert `ep.severity == "HIGH"` and `ep.cert_pubkey_alg in (None, "")`
    - test_gcp_cloud_sql_encrypted_only: assert `ep.severity in (None, "MEDIUM")` per existing semantic AND `ep.cert_pubkey_alg in (None, "")`
    - test_gcp_cloud_sql_null_ssl_mode: assert `ep.severity == "HIGH"` (or whatever severity description maps to per the source) and `ep.cert_pubkey_alg in (None, "")`
    - Add a 4th assertion in any one of the three: `"CLOUD_SQL/" in (ep.service_detail or "")` to lock the description-routing
  </behavior>
  <action>
    In tests/test_cloud_connectors.py, locate the three named tests. For each:
    - REMOVE the existing assertion of the form `assert "HIGH" in (ep.cert_pubkey_alg or "")` (or "MEDIUM").
    - ADD `assert ep.severity == "<expected>"` matching what the new gcp_connector code emits for that ssl_mode input.
    - ADD `assert (ep.cert_pubkey_alg or "") == ""` to lock the corrected behavior.
    - In one of the three (recommend test_gcp_cloud_sql_plaintext_allowed), add `assert (ep.service_detail or "").startswith("CLOUD_SQL/")`.
    - Preserve all other assertions, fixture setup, and test names verbatim. Minimal diff per CLAUDE.md.
  </action>
  <verify>
    <automated>pytest tests/test_cloud_connectors.py -k cloud_sql -x -q exits 0</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_cloud_connectors.py::test_gcp_cloud_sql_plaintext_allowed -x -q` exits 0
    - `pytest tests/test_cloud_connectors.py::test_gcp_cloud_sql_encrypted_only -x -q` exits 0
    - `pytest tests/test_cloud_connectors.py::test_gcp_cloud_sql_null_ssl_mode -x -q` exits 0
    - `grep -n 'cert_pubkey_alg' tests/test_cloud_connectors.py | grep -i 'HIGH\|MEDIUM'` returns nothing
  </acceptance_criteria>
  <done>All three Cloud SQL tests pass against the fixed gcp_connector.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_cloud_connectors.py -k cloud_sql -x -q` green
- `python -m compileall quirk/scanner/gcp_connector.py` green
- `grep -n 'cert_pubkey_alg' quirk/scanner/gcp_connector.py` shows no severity-string writes
</verification>

<success_criteria>
GCP Cloud SQL findings emit severity in the severity column; cert_pubkey_alg untouched; tests reflect corrected semantics.
</success_criteria>

<output>
Create `.planning/phases/69-deferred-blockers-scanner-cloud/69-02-SUMMARY.md`.
</output>
