---
phase: 59
plan: 02
type: execute
wave: 2
depends_on: [59-01]
files_modified:
  - quirk/scanner/vault_connector.py
  - quirk/scanner/gcp_connector.py
  - quirk/scanner/tls_scanner.py
  - quirk/scanner/email_scanner.py
  - quirk/scanner/broker_scanner.py
  - quirk/scanner/ssh_scanner.py
  - quirk/discovery/tls_scanner.py
  - quirk/scanner/db_connector.py
  - quirk/cbom/writer.py
  - tests/test_credential_leakage.py
autonomous: true
requirements: [LEAK-02]
must_haves:
  truths:
    - "Every scan_error write that interpolates an exception variable routes through safe_str"
    - "GCP two-step pattern is fixed at the variable assignment, not the keyword arg"
    - "db_connector.py is unified onto safe_str (eliminates AST gate false positive)"
    - "cbom/writer.py scan_error writes use safe_str so Plan 03's AST gate finds zero violations under quirk/cbom/"
    - "Per-connector integration tests confirm no credential substring lands in scan_error"
  artifacts:
    - path: "tests/test_credential_leakage.py"
      provides: "LEAK-02 per-connector regression tests"
      contains: "def test_vault_scan_error_no_token"
    - path: "quirk/scanner/vault_connector.py"
      provides: "safe_str-wrapped scan_error writes"
      contains: "safe_str(exc)"
    - path: "quirk/scanner/gcp_connector.py"
      provides: "safe_str-wrapped scan_error_msg assignment"
      contains: "safe_str(exc)"
    - path: "quirk/cbom/writer.py"
      provides: "safe_str-wrapped CBOM validator scan_error writes"
      contains: "safe_str("
  key_links:
    - from: "all 8 modified scanner/connector/cbom files"
      to: "quirk.util.safe_exc.safe_str"
      via: "import"
      pattern: "from quirk.util.safe_exc import safe_str"
    - from: "tests/test_credential_leakage.py"
      to: "scan_error string content"
      via: "regex assertion that no credential substring is present"
      pattern: "assert .* not in result.scan_error"
---

<objective>
Apply `safe_str()` mechanically to all 8 leaky `scan_error` callsites across 6 scanner files, plus the GCP two-step variable pattern, plus the `db_connector.py` `type(exc).__name__` pattern (unified onto `safe_str` to eliminate gate false positives), plus the 2 `cbom/writer.py` callsites (so Plan 03's AST gate over `quirk/cbom/` finds zero violations). Add per-connector integration tests that drive a synthetic credential-bearing exception through each connector's error path and assert no credential substring lands in `scan_error`. Closes LEAK-02.

Purpose: Eliminate every existing leak path in one mechanical sweep so Plan 03's AST gate finds zero violations across `quirk/scanner/`, `quirk/discovery/`, AND `quirk/cbom/`.

Output: 8 modified scanner/connector/cbom files (8 leaky scanner callsites + db_connector unified + cbom/writer.py 2 callsites), 1 new test file with per-connector integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-credential-leakage-sweep/59-RESEARCH.md
@.planning/phases/59-credential-leakage-sweep/59-PATTERNS.md
@.planning/phases/59-credential-leakage-sweep/59-VALIDATION.md
@.planning/phases/59-credential-leakage-sweep/59-01-SUMMARY.md

<interfaces>
<!-- Consumed from Plan 01: -->

From quirk/util/safe_exc.py:
```python
def safe_str(exc: BaseException) -> str: ...
```

Import statement to add to each file:
```python
from quirk.util.safe_exc import safe_str
```

Place after the last existing `from quirk.*` import, alphabetically within the quirk.* group.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — create per-connector integration test stubs</name>
  <files>tests/test_credential_leakage.py</files>
  <read_first>
    - tests/test_safe_exc.py (test style from Plan 01)
    - tests/test_skip_registry.py (header convention)
    - quirk/scanner/vault_connector.py (lines 415–455 — exception path structure)
    - quirk/scanner/gcp_connector.py (lines 375–395 — two-step pattern)
    - quirk/scanner/email_scanner.py (lines 460–480)
    - quirk/scanner/broker_scanner.py (lines 695–710)
    - quirk/scanner/ssh_scanner.py (lines 80–90)
    - quirk/scanner/tls_scanner.py (lines 445–460)
    - quirk/cbom/writer.py (lines 60–100 — JsonStrictValidator path, scan_error writes at lines 78 + 93)
    - .planning/phases/59-credential-leakage-sweep/59-PATTERNS.md (full file)
  </read_first>
  <behavior>
    For each connector, define a test that constructs a credential-bearing Exception and asserts the resulting `scan_error` string does NOT contain any of: "s.AbCd", "secret123", "/gcloud/", "Bearer ", or a 40+ char alphanumeric run.

    Required tests (function names exact):
    - test_vault_scan_error_strips_token: builds an Exception("https://vault:8200?token=s.AbCdEfGhIjKlMnOpQrSt1234"), calls safe_str directly, asserts result == "Exception"
    - test_gcp_scan_error_strips_adc_path: builds Exception("/home/user/.config/gcloud/application_default_credentials.json missing"), asserts safe_str(...) == "Exception"
    - test_email_scan_error_strips_smtp_password: builds Exception("smtp://user:secret123@mail.example.com:587 auth failed"), asserts safe_str(...) == "Exception"
    - test_broker_scan_error_strips_redis_password: builds Exception("redis://default:supersecret@redis:6379 auth failed"), asserts safe_str(...) == "Exception"
    - test_ssh_scan_error_class_name_only_for_creds: builds Exception("Authorization: Bearer abc.def.ghi"), asserts safe_str(...) == "Exception"
    - test_tls_scan_error_benign_passthrough: builds ConnectionRefusedError("[Errno 111] Connection refused"), asserts safe_str result starts with "ConnectionRefusedError:"
    - test_cbom_writer_scan_error_strips_creds: builds Exception("validator config: postgresql://admin:S3cret!@db:5432/x"), asserts safe_str(...) == "Exception" (covers the cbom/writer.py callsites which receive arbitrary validator/exception text)
    - test_all_callsites_import_safe_str: parametrized over the 8 modified files; reads each file and asserts `from quirk.util.safe_exc import safe_str` is present
  </behavior>
  <action>
    Create `tests/test_credential_leakage.py` with this header:

    ```python
    """Phase 59 LEAK-02: per-connector regression tests verifying that
    scan_error writes never carry credential-shaped substrings.

    Approach: each connector's exception path funnels through safe_str().
    These tests assert (a) safe_str strips the credential payloads we know
    real libraries surface, and (b) every modified file imports safe_str.
    """
    from __future__ import annotations

    import pathlib

    import pytest

    from quirk.util.safe_exc import safe_str

    PROJECT_ROOT = pathlib.Path(__file__).resolve().parent.parent

    MODIFIED_FILES = [
        "quirk/scanner/vault_connector.py",
        "quirk/scanner/gcp_connector.py",
        "quirk/scanner/tls_scanner.py",
        "quirk/scanner/email_scanner.py",
        "quirk/scanner/broker_scanner.py",
        "quirk/scanner/ssh_scanner.py",
        "quirk/discovery/tls_scanner.py",
        "quirk/cbom/writer.py",
    ]
    ```

    Implement EVERY test listed in `<behavior>`. The `test_all_callsites_import_safe_str` test must be parametrized:

    ```python
    @pytest.mark.parametrize("relpath", MODIFIED_FILES)
    def test_all_callsites_import_safe_str(relpath: str) -> None:
        path = PROJECT_ROOT / relpath
        text = path.read_text(encoding="utf-8")
        assert "from quirk.util.safe_exc import safe_str" in text, (
            f"{relpath} must import safe_str"
        )
    ```

    Run: `python -m pytest tests/test_credential_leakage.py -x -q`.
    The safe_str-direct tests will pass (Plan 01 already done). The import test will FAIL for all 8 files (RED state for this plan). That's expected.
  </action>
  <verify>
    <automated>python -m pytest tests/test_credential_leakage.py -q 2>&1 | grep -E "(test_all_callsites_import_safe_str|FAILED)"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_credential_leakage.py` exists
    - `grep -c "^def test_" tests/test_credential_leakage.py` returns >= 8
    - `grep -q "from quirk.util.safe_exc import safe_str" tests/test_credential_leakage.py` succeeds
    - `grep -q "MODIFIED_FILES" tests/test_credential_leakage.py` succeeds
    - `grep -q "quirk/cbom/writer.py" tests/test_credential_leakage.py` succeeds
    - `python -m pytest tests/test_credential_leakage.py::test_all_callsites_import_safe_str -q` produces 8 FAILED parametrize cases (RED confirmed)
    - The 7 direct safe_str behavior tests pass on first run
  </acceptance_criteria>
  <done>
    Test file with 8+ tests exists. The 7 direct safe_str tests pass; the 8 parametrized import-presence tests fail because Task 2 hasn't run yet.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply safe_str to all 8 leaky callsites + unify db_connector + sweep cbom/writer.py</name>
  <files>
    quirk/scanner/vault_connector.py,
    quirk/scanner/gcp_connector.py,
    quirk/scanner/tls_scanner.py,
    quirk/scanner/email_scanner.py,
    quirk/scanner/broker_scanner.py,
    quirk/scanner/ssh_scanner.py,
    quirk/discovery/tls_scanner.py,
    quirk/scanner/db_connector.py,
    quirk/cbom/writer.py
  </files>
  <read_first>
    - quirk/util/safe_exc.py (the helper being imported)
    - quirk/scanner/vault_connector.py (full file — see lines 1–20 for import block, 415–455 for callsites)
    - quirk/scanner/gcp_connector.py (lines 1–20 imports, 375–395 two-step pattern)
    - quirk/scanner/tls_scanner.py (lines 1–25 imports, 445–460 callsite)
    - quirk/scanner/email_scanner.py (lines 1–20 imports, 460–480 callsites)
    - quirk/scanner/broker_scanner.py (lines 1–20 imports, 695–710 callsite)
    - quirk/scanner/ssh_scanner.py (lines 1–25 imports, 80–90 callsite)
    - quirk/discovery/tls_scanner.py (lines 1–15 imports, 90–100 callsite)
    - quirk/scanner/db_connector.py (lines 1–20 imports, 160–170 + 265–275 — the `type(exc).__name__` pattern that must be unified)
    - quirk/cbom/writer.py (lines 1–25 imports, 60–100 — the two leaky writes at lines 78 and 93 inside the JsonStrictValidator block)
    - .planning/phases/59-credential-leakage-sweep/59-PATTERNS.md (sections "Pattern Assignments" + "Mechanical Substitution — Three Exact Forms" + "Gate Behavior Notes")
  </read_first>
  <behavior>
    - All 8 leaky scanner/discovery writes from RESEARCH.md "Complete Leaky Callsite Inventory" use `safe_str(exc)` instead of bare `{exc}` / `str(e)` / `repr(e)`.
    - GCP two-step pattern fixed at the variable assignment line (~381), not at the keyword arg line (~389).
    - db_connector.py lines 165 & 268 unified to `safe_str(exc)` (no more `type(exc).__name__` pattern — eliminates AST gate special case per PATTERNS.md "Gate Behavior Notes" Option 1).
    - quirk/cbom/writer.py lines 78 (`{err}`) and 93 (`{exc}`) wrapped via `safe_str(...)` — required because Plan 03's SCANNER_DIRS includes `quirk/cbom/`. Without this, the AST gate would flag both writes as violations and CI would fail.
    - Each modified file has a single new import line: `from quirk.util.safe_exc import safe_str`, placed alphabetically within the existing `from quirk.*` import group.
    - All tests in tests/test_credential_leakage.py pass (including the 8 parametrized import-presence cases).
    - Existing test suite (tests/) does not regress.
  </behavior>
  <action>
    Execute these EXACT substitutions. For each file, first add the import line `from quirk.util.safe_exc import safe_str` after the last `from quirk.*` import (alphabetical order within the quirk.* group).

    **File 1 — quirk/scanner/vault_connector.py (2 callsites):**
    - Line ~428: replace `scan_error=f"vault-client-init-failed: {exc}",` with `scan_error=f"vault-client-init-failed: {safe_str(exc)}",`
    - Line ~450: replace `scan_error=f"vault-auth-failed: {exc}",` with `scan_error=f"vault-auth-failed: {safe_str(exc)}",`
    - Opportunistic: replace `logger.v(f"Vault hvac.Client construction error: {exc}")` with `logger.v(f"Vault hvac.Client construction error: {safe_str(exc)}")` and `logger.v(f"Vault auth check network error: {exc}")` with `logger.v(f"Vault auth check network error: {safe_str(exc)}")`.

    **File 2 — quirk/scanner/gcp_connector.py (1 leaky two-step):**
    - Line ~381: replace `scan_error_msg = f"gcp-credentials-unavailable: {exc}"` with `scan_error_msg = f"gcp-credentials-unavailable: {safe_str(exc)}"`. The keyword-arg use at line ~389 (`scan_error=scan_error_msg`) is left as-is — the variable now carries a safe value.

    **File 3 — quirk/scanner/tls_scanner.py (1 callsite):**
    - Line ~454: replace `ep.scan_error = f"{cat}: {e}"` with `ep.scan_error = f"{cat}: {safe_str(e)}"`.

    **File 4 — quirk/scanner/email_scanner.py (2 callsites):**
    - Line ~470: replace `ep.scan_error = str(e)` with `ep.scan_error = safe_str(e)`.
    - Line ~474: replace `ep.scan_error = str(e)` with `ep.scan_error = safe_str(e)`.

    **File 5 — quirk/scanner/broker_scanner.py (1 callsite):**
    - Line ~704: replace `ep.scan_error = str(e)` with `ep.scan_error = safe_str(e)`.

    **File 6 — quirk/scanner/ssh_scanner.py (1 callsite):**
    - Line ~85: replace `ep.scan_error = f"SSH_ERROR: {e}"` with `ep.scan_error = f"SSH_ERROR: {safe_str(e)}"`.

    **File 7 — quirk/discovery/tls_scanner.py (1 callsite):**
    - Line ~95: replace `ep.scan_error = str(e)` with `ep.scan_error = safe_str(e)`.

    **File 8 — quirk/scanner/db_connector.py (UNIFY — 2 sites):**
    - Lines ~165 and ~268 currently use `f"connection-error: {type(exc).__name__}"`-style strings. Replace each occurrence of `f"...: {type(exc).__name__}"` with the equivalent `f"...: {safe_str(exc)}"` form, preserving the prefix string before the colon. Per PATTERNS.md "Gate Behavior Notes" Option 1, this unifies the codebase onto `safe_str` and removes the need for a `_is_type_name_call` predicate in the AST gate.

    **File 9 — quirk/cbom/writer.py (2 callsites — required for Plan 03 gate):**
    - Add `from quirk.util.safe_exc import safe_str` after the existing `from cyclonedx.*` imports (alphabetical: place under a new `from quirk.util.safe_exc import safe_str` line — `quirk` sorts after `cyclonedx`).
    - Line ~78: replace `scan_error=f"CBOM JSON failed schema validation: {err}",` with `scan_error=f"CBOM JSON failed schema validation: {safe_str(err)}",`. Note: `err` is the return value of `validator.validate_str()` — it is treated as exception-like text. If `err` is not a `BaseException`, wrap via `safe_str(Exception(str(err)))` ONLY if a TypeError surfaces; otherwise the bare `safe_str(err)` form is preferred and validated by behavior tests. Inspect `err`'s type during the read_first step and pick the correct form.
    - Line ~93: replace `scan_error=f"CBOM JSON validation error: {exc}",` with `scan_error=f"CBOM JSON validation error: {safe_str(exc)}",`. (`exc` here is a real `Exception` from the `except Exception as exc:` block — straightforward.)

    **DO NOT MODIFY** these files (PATTERNS.md confirms they are safe and the gate must not flag them, achieved via the AST attribute-read exclusion in Plan 03):
    - quirk/scanner/container_scanner.py — uses `_validation.reason` (ast.Attribute read)
    - quirk/scanner/source_scanner.py — same pattern
    - quirk/scanner/k8s_connector.py — string literals only

    After all edits run:
    1. `python -m compileall quirk/scanner quirk/discovery quirk/cbom` — must exit 0
    2. `python -m pytest tests/test_credential_leakage.py -q` — must be 15/15 green (7 behavior + 8 import parametrize)
    3. `python -m pytest tests/test_safe_exc.py -q` — must remain green
    4. `python -m pytest tests/ -q --tb=short -x` — full suite must not regress
  </action>
  <verify>
    <automated>python -m compileall quirk/scanner quirk/discovery quirk/cbom -q && python -m pytest tests/test_credential_leakage.py tests/test_safe_exc.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "from quirk.util.safe_exc import safe_str" quirk/scanner/vault_connector.py quirk/scanner/gcp_connector.py quirk/scanner/tls_scanner.py quirk/scanner/email_scanner.py quirk/scanner/broker_scanner.py quirk/scanner/ssh_scanner.py quirk/discovery/tls_scanner.py quirk/scanner/db_connector.py quirk/cbom/writer.py | wc -l` returns 9
    - `grep -v '^#' quirk/scanner/vault_connector.py | grep -E 'scan_error=f"vault-(client-init|auth)-failed: \\{exc\\}"' | wc -l` returns 0
    - `grep -v '^#' quirk/scanner/gcp_connector.py | grep -E 'scan_error_msg = f"gcp-credentials-unavailable: \\{exc\\}"' | wc -l` returns 0
    - `grep -v '^#' quirk/scanner/email_scanner.py | grep -cE 'ep\.scan_error = str\(e\)'` returns 0
    - `grep -v '^#' quirk/scanner/broker_scanner.py | grep -cE 'ep\.scan_error = str\(e\)'` returns 0
    - `grep -v '^#' quirk/scanner/ssh_scanner.py | grep -E 'ep\.scan_error = f"SSH_ERROR: \\{e\\}"' | wc -l` returns 0
    - `grep -v '^#' quirk/scanner/tls_scanner.py | grep -E 'ep\.scan_error = f"\\{cat\\}: \\{e\\}"' | wc -l` returns 0
    - `grep -v '^#' quirk/discovery/tls_scanner.py | grep -cE 'ep\.scan_error = str\(e\)'` returns 0
    - `grep -v '^#' quirk/scanner/db_connector.py | grep -cE 'type\(exc\)\.__name__'` returns 0 (unified onto safe_str)
    - `grep -v '^#' quirk/cbom/writer.py | grep -E 'scan_error=f"CBOM JSON (failed schema validation|validation error): \\{(err|exc)\\}"' | wc -l` returns 0
    - `grep -c "safe_str(" quirk/cbom/writer.py` returns >= 2
    - `grep -c "safe_str(exc)\|safe_str(e)" quirk/scanner/vault_connector.py` returns >= 2
    - `python -m pytest tests/test_credential_leakage.py tests/test_safe_exc.py -q` exits 0
    - `python -m compileall quirk/scanner quirk/discovery quirk/cbom` exits 0
  </acceptance_criteria>
  <done>
    All 8 leaky scanner/discovery callsites + db_connector unification + cbom/writer.py 2 callsites applied. 8 modified files import safe_str. Full credential-leakage and safe_exc test suites pass. No `{exc}`, `{err}`, `str(e)`, or `type(exc).__name__` patterns remain in scan_error writes within `quirk/scanner/`, `quirk/discovery/`, or `quirk/cbom/`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party lib (hvac, google.auth, paramiko, smtplib, redis, cyclonedx validator) → connector/cbom-writer | Exception messages may include token fragments, ADC paths, connection-string passwords, Authorization headers, validator config strings |
| connector → SQLite `crypto_endpoints.scan_error` | Sanitized text only after this plan |
| connector → logger.v sink | Opportunistically sanitized in vault_connector.py (out of LEAK-03 gate scope per RESEARCH.md Pitfall 5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-04 | Information Disclosure | vault_connector hvac error path | mitigate | Both `scan_error=` writes wrap exc with `safe_str`; logger.v calls also routed through safe_str opportunistically |
| T-59-05 | Information Disclosure | gcp_connector ADC error path (two-step var) | mitigate | Fix at variable assignment (line ~381) so the value reaching `scan_error=scan_error_msg` is already safe |
| T-59-06 | Information Disclosure | email/broker auth error path | mitigate | `ep.scan_error = safe_str(e)` strips SMTP/Redis password substrings via connection-string regex |
| T-59-07 | Information Disclosure | tls/ssh error categorization | mitigate | f-string preserves human-readable category prefix; only the exception payload is wrapped via `safe_str` |
| T-59-08 | Information Disclosure | db_connector pre-existing class-name pattern | mitigate | Unified onto `safe_str` to eliminate AST gate special-casing and ensure forward consistency |
| T-59-14 | Information Disclosure | cbom/writer.py validator error path | mitigate | Both scan_error writes (lines 78, 93) wrap via `safe_str`; eliminates Plan 03 AST gate violation under quirk/cbom/ |
| T-59-09 | Tampering | Reviewer adds new connector and forgets safe_str | accept | Mitigated downstream by Plan 03 LEAK-03 AST gate |
</threat_model>

<verification>
- `python -m pytest tests/test_credential_leakage.py tests/test_safe_exc.py -q` passes
- `python -m compileall quirk/scanner quirk/discovery quirk/cbom` exits 0
- Full suite `python -m pytest tests/ -q --tb=short -x` does not regress (note: any pre-existing failures unrelated to this plan are out of scope)
- Grep gates in acceptance_criteria all pass
</verification>

<success_criteria>
- All 8 leaky scanner/discovery callsites use `safe_str()`
- db_connector.py unified onto `safe_str()` (no `type(exc).__name__` left in scan_error writes)
- cbom/writer.py 2 callsites use `safe_str()` (lines 78 + 93)
- 8 modified files import `from quirk.util.safe_exc import safe_str`
- `tests/test_credential_leakage.py` 15/15 green
- All files committed
</success_criteria>

<output>
After completion, create `.planning/phases/59-credential-leakage-sweep/59-02-SUMMARY.md` with the substitution table and any deviations from the action plan.
</output>
</content>
</invoke>