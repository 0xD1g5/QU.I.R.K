---
phase: 107-distributed-data-model
plan: 02
type: execute
wave: 2
depends_on: ["107-01"]
files_modified:
  - tests/test_sensor_schema.py
  - tests/test_db_ensure_columns_generic.py
autonomous: true
requirements: [MODEL-01, MODEL-02, MODEL-03, MODEL-04]

must_haves:
  truths:
    - "A programmatically-built pre-v5.4 SQLite DB (no sensor columns/tables) migrates via init_db with no data loss and no schema error"
    - "A pre-existing CryptoEndpoint-equivalent row scores identically after migration (NULL sensor_id = local sensor)"
    - "The _V54_SENSOR_COLUMNS additive path rejects a poisoned column type via the allowlist"
    - "The sensor_tokens/sensor_pushes CASCADE FKs are enforced (deleting a sensor removes its children)"
  artifacts:
    - path: "tests/test_sensor_schema.py"
      provides: "backward-compat fixture test, column/table existence, poison-tuple rejection, CASCADE-delete test"
      contains: "def test_"
    - path: "tests/test_db_ensure_columns_generic.py"
      provides: "smoke test updated to expect sensor_id/segment + three sensor tables"
      contains: "sensor_id"
  key_links:
    - from: "tests/test_sensor_schema.py"
      to: "quirk.db.init_db / run_additive_migration"
      via: "build old-schema DB then migrate, assert no loss + scoring stability"
      pattern: "init_db\\("
    - from: "tests/test_sensor_schema.py"
      to: "quirk.scoring.compute_readiness_score"
      via: "score before vs after migration equality assertion"
      pattern: "compute_readiness_score"
---

<objective>
Prove strict backward compatibility and schema correctness for the v5.4 sensor schema landed in Plan 01. Build a pre-v5.4 SQLite database programmatically (no sensor columns, no sensor tables), migrate it via `init_db`, and assert: no data loss, no schema error, all new columns/tables present, CASCADE FKs enforced, the allowlist still rejects poisoned DDL, and an existing row scores identically post-migration.

Purpose: MODEL-01 is only half-satisfied by the schema landing — the binding requirement is that `quirk scan` runs unchanged against an existing DB (Success Criterion #2, D-05). This plan is that proof.
Output: New `tests/test_sensor_schema.py` + updated smoke assertions in `tests/test_db_ensure_columns_generic.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-distributed-data-model/107-CONTEXT.md
@.planning/phases/107-distributed-data-model/107-PATTERNS.md
@.planning/phases/107-distributed-data-model/107-01-SUMMARY.md

<interfaces>
<!-- Analog test patterns to copy. Extracted from PATTERNS.md § tests/test_sensor_schema.py. -->

From tests/test_init_db_idempotent.py:
- `_column_snapshot(engine) -> dict` helper (maps table -> sorted column names) — reuse verbatim
- `test_init_db_after_simulated_partial_migration(tmp_path)` — the direct analog: build a DB with only Base tables via `Base.metadata.create_all`, then `init_db`, assert snapshot equality
- All tests use the pytest `tmp_path: Path` fixture (preferred over tempfile)

From tests/test_db_migrations.py:
- `_POISON = (("evil_col", "TEXT; DROP TABLE x"),)` + `pytest.raises(ValueError, match="Unsafe column type")` — poison-tuple pattern

From tests/test_db_ensure_columns_generic.py:
- `test_init_db_creates_expected_columns_smoke()` — has an `expected_crypto` set of column names checked against `insp.get_columns("crypto_endpoints")`; ADD "sensor_id" and "segment" to that set (update, do not duplicate the test)

From quirk/db.py: `init_db`, `_ensure_columns`, `_V54_SENSOR_COLUMNS`
From quirk/scoring.py: `compute_readiness_score(...)` — the function whose output must be stable across migration (read its signature before writing the stability assertion)
Note: PRAGMA foreign_keys=ON is set on every connect (db.py:31) — CASCADE FKs are enforced; CASCADE tests must use an on-disk sqlite (sqlite:///path), not in-memory.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backward-compat fixture + column/table existence + CASCADE test</name>
  <files>tests/test_sensor_schema.py</files>
  <read_first>
    - tests/test_init_db_idempotent.py (the _column_snapshot helper + simulated-partial-migration analog)
    - quirk/db.py (init_db, _ensure_columns signature)
    - quirk/scoring.py (compute_readiness_score signature — read before asserting score stability)
    - quirk/models.py (CryptoEndpoint columns, to build a valid pre-existing row)
    - .planning/phases/107-distributed-data-model/107-PATTERNS.md (§ tests — programmatic old-schema fixture 7-step recipe + FK enforcement note)
    - .planning/phases/107-distributed-data-model/107-CONTEXT.md (D-05: generated-at-test-time fixture, scoring stability)
  </read_first>
  <action>
    Create tests/test_sensor_schema.py with `from __future__ import annotations`, `tmp_path` fixtures, and these tests (copy the `_column_snapshot` helper from the analog):

    1. `test_sensor_schema_columns_and_tables_exist(tmp_path)`: call `init_db` on a fresh path; assert `sensor_id` and `segment` in crypto_endpoints columns; assert `sensors`, `sensor_tokens`, `sensor_pushes` in `sa_inspect(engine).get_table_names()`; assert `ix_crypto_endpoints_sensor_id` in the crypto_endpoints indexes.

    2. `test_pre_v54_db_migrates_without_data_loss(tmp_path)` (D-05, MODEL-01 backward-compat): build an OLD-schema SQLite — create crypto_endpoints via raw `text()` DDL WITHOUT sensor_id/segment and WITHOUT the three sensor tables (use a minimal column subset sufficient to insert a row, or `Base.metadata.create_all` then DROP the new columns/tables is NOT possible in SQLite — instead hand-write a minimal crypto_endpoints CREATE TABLE with the legacy columns the scorer needs). Insert one pre-existing row. Call `init_db` on that DB. Assert: sensor_id/segment now present; three sensor tables now present; the pre-existing row still queryable (no data loss); no exception raised.

    3. `test_score_stable_across_migration(tmp_path)` (D-05 scoring stability): using the old-schema DB + row from the recipe, compute `compute_readiness_score(...)` over the row's data BEFORE adding sensor columns, then AFTER `init_db` migration, assert the two scores are equal (NULL sensor_id = local sensor must score identically). Read compute_readiness_score's actual call shape first; if it consumes endpoint dicts/objects rather than a DB, derive the score from the row's pre- and post-migration representation accordingly.

    4. `test_cascade_delete_removes_sensor_children(tmp_path)` (D-04): on a fresh on-disk init_db DB, insert a `Sensor` row plus a `SensorToken` and `SensorPush` referencing it; delete the Sensor row; assert the dependent sensor_tokens and sensor_pushes rows are gone (CASCADE enforced via the active PRAGMA). Use an on-disk sqlite path, never in-memory.
  </action>
  <verify>
    <automated>python -m pytest tests/test_sensor_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_sensor_schema.py` exists and contains 4 `def test_` functions.
    - `python -m pytest tests/test_sensor_schema.py -x -q` exits 0.
    - The backward-compat test builds the old schema programmatically (grep confirms no checked-in `.sqlite` binary referenced).
    - `grep -q "compute_readiness_score" tests/test_sensor_schema.py` succeeds (scoring-stability assertion present).
    - The CASCADE test uses an on-disk sqlite path (not `sqlite://` in-memory).
  </acceptance_criteria>
  <done>All four tests pass: schema existence, pre-v5.4 migration with no data loss, scoring stability across migration, and CASCADE-delete enforcement.</done>
</task>

<task type="auto">
  <name>Task 2: Poison-tuple rejection + update existing smoke test</name>
  <files>tests/test_sensor_schema.py, tests/test_db_ensure_columns_generic.py</files>
  <read_first>
    - tests/test_db_migrations.py (the _POISON + pytest.raises ValueError "Unsafe column type" pattern)
    - tests/test_db_ensure_columns_generic.py (the test_init_db_creates_expected_columns_smoke expected_crypto set to extend)
    - .planning/phases/107-distributed-data-model/107-PATTERNS.md (§ tests — poisoned-tuple pattern + smoke-test update note)
  </read_first>
  <action>
    Two edits:

    1. Append to tests/test_sensor_schema.py a `test_v54_sensor_columns_path_rejects_poisoned_col_type(tmp_path)`: `init_db` a fresh DB, then call `_ensure_columns(engine, "crypto_endpoints", (("evil_col", "TEXT; DROP TABLE x"),))` inside `pytest.raises(ValueError, match="Unsafe column type")`. This proves the v5.4 columns still flow through the same allowlist guard. Import `_ensure_columns` from quirk.db.

    2. In tests/test_db_ensure_columns_generic.py, UPDATE the existing `test_init_db_creates_expected_columns_smoke` — add `"sensor_id"` and `"segment"` to its `expected_crypto` set, and add a parallel assertion that `sensors`, `sensor_tokens`, `sensor_pushes` appear in `sa_inspect(engine).get_table_names()`. Do not create a new test; extend the existing one (per PATTERNS.md).
  </action>
  <verify>
    <automated>python -m pytest tests/test_sensor_schema.py tests/test_db_ensure_columns_generic.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_sensor_schema.py` contains `test_v54_sensor_columns_path_rejects_poisoned_col_type` and it passes.
    - `grep -q "sensor_id" tests/test_db_ensure_columns_generic.py` succeeds and the updated smoke test passes.
    - `python -m pytest tests/test_sensor_schema.py tests/test_db_ensure_columns_generic.py -x -q` exits 0.
  </acceptance_criteria>
  <done>Poison-tuple test proves the allowlist still guards the v5.4 path; the existing smoke test now expects the two new columns and three new tables; both files pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-built DDL → SQLite | Poison-tuple test deliberately probes the allowlist guard from the attacker's side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-05 | Tampering | `_ensure_columns` allowlist on the v5.4 path | mitigate | `test_v54_sensor_columns_path_rejects_poisoned_col_type` asserts `_SAFE_COL_TYPE_RE` rejects `"TEXT; DROP TABLE x"` with ValueError — the guard is proven, not assumed |
| T-107-06 | Denial of Service / data integrity | pre-v5.4 DB migration | mitigate | Backward-compat test proves migration is non-destructive (no data loss, no schema error) and score-stable — a regression that breaks existing DBs fails CI |
| T-107-SC | Tampering | npm/pip installs | accept | No package installs — test-only phase using existing pytest/SQLAlchemy. No new dependencies. |
</threat_model>

<verification>
- `python -m pytest tests/test_sensor_schema.py tests/test_db_ensure_columns_generic.py -x -q` exits 0.
- Broader regression: `python -m pytest tests/test_init_db_idempotent.py tests/test_db_migrations.py -q` still passes (no collateral breakage to existing migration tests).
- `python -m compileall quirk/` exits 0.
</verification>

<success_criteria>
- MODEL-01 (backward-compat half): a pre-v5.4 SQLite DB migrates with no data loss, no schema error, and identical scoring (Success Criterion #2 / D-05).
- The allowlist still rejects poisoned DDL on the v5.4 column path.
- CASCADE delete semantics (D-04) are proven against an on-disk DB.
- The existing smoke test recognizes the new columns + tables.
</success_criteria>

<output>
Create `.planning/phases/107-distributed-data-model/107-02-SUMMARY.md` when done
</output>
