---
phase: 95-code-signing-certificate-inventory
plan: 02
type: execute
wave: 2
depends_on: [95-01]
files_modified:
  - quirk/cbom/builder.py
  - quirk/intelligence/evidence.py
  - quirk/intelligence/scoring.py
  - tests/test_score_weights_invariant.py
  - tests/test_codesign_cbom.py
  - tests/test_evidence.py
autonomous: true
requirements: [CSIGN-03, SCORE-01]
must_haves:
  truths:
    - "CODE_SIGNING endpoints register an algorithm component in CBOM Pass-1 and skip Pass-2/Pass-3"
    - "Two CODE_SIGNING endpoints with the same SHA-256 fingerprint produce a stable (non-duplicated) CBOM component count"
    - "Code-signing weak-algo events contribute to agility_signals; SCORE_WEIGHTS sum == 299.0 and count == 40"
  artifacts:
    - path: "quirk/cbom/builder.py"
      provides: "CODE_SIGNING Pass-1 branch + Pass-2/Pass-3 skips + fingerprint dedup pass"
      contains: "CODE_SIGNING"
    - path: "quirk/intelligence/scoring.py"
      provides: "agility_codesign_weak_algo_ratio weight (+6.0) and agility_impacts entry"
      contains: "agility_codesign_weak_algo_ratio"
    - path: "quirk/intelligence/evidence.py"
      provides: "CODE_SIGNING in _PROTOCOL_KEYS + codesign_weak_algo_count counter"
      contains: "codesign_weak_algo_count"
  key_links:
    - from: "quirk/intelligence/scoring.py"
      to: "quirk/intelligence/evidence.py"
      via: "evidence.get('codesign_weak_algo_count')"
      pattern: "codesign_weak_algo_count"
    - from: "quirk/cbom/builder.py"
      to: "service_detail fingerprint token"
      via: "_extract_fp"
      pattern: "fingerprint="
---

<objective>
Wire the CODE_SIGNING protocol through the CBOM builder, evidence summary, and scoring layers,
and add the SHA-256 fingerprint dedup that keeps the CBOM component count stable when the same
cert is seen via two CODE_SIGNING paths. Advance SCORE_WEIGHTS +6.0 (293.0→299.0, count 39→40),
updating BOTH invariant assertions in the same task.

Purpose: CSIGN-03 (fingerprint dedup, stable component count) and SCORE-01 (agility_signals +6.0).
Output: builder.py / evidence.py / scoring.py edits, both invariant assertions updated, dedup + evidence tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-code-signing-certificate-inventory/95-RESEARCH.md
@.planning/phases/95-code-signing-certificate-inventory/95-PATTERNS.md
@.planning/phases/95-code-signing-certificate-inventory/95-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: scanner emits these. Consumed here. -->
CryptoEndpoint with protocol == "CODE_SIGNING" (UPPERCASE), cert_pubkey_alg, cert_pubkey_size,
service_detail containing "|fingerprint=<sha256hex>" and a "weak" token when severity HIGH,
smime_scan_json holding {"reasons": [...]}.

Current SCORE_WEIGHTS state (Phase 94): sum == 293.0, count == 39.
Phase 94 analog weights to mirror: "agility_weak_jwt_alg_ratio": 6.0, "agility_openapi_plaintext_ratio": 4.0.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CBOM CODE_SIGNING branch + fingerprint dedup (CSIGN-03)</name>
  <files>quirk/cbom/builder.py, tests/test_codesign_cbom.py</files>
  <read_first>
    - quirk/cbom/builder.py lines 508-527 (SMIME/ADCS Pass-1), 619-627 (Pass-2 skip), 705-714 (Pass-3 skip), 644-663 (TLS CertificateProperties pattern)
    - .planning/phases/95-code-signing-certificate-inventory/95-PATTERNS.md (section "quirk/cbom/builder.py" + Critical Caveat 3)
    - .planning/phases/95-code-signing-certificate-inventory/95-RESEARCH.md (Pattern 4 + Open Question 1: dedup contract)
  </read_first>
  <behavior>
    - test_codesign_pass1_registers_algorithm: a single CODE_SIGNING endpoint with cert_pubkey_alg=RSA registers exactly one algorithm component
    - test_cbom_dedup_stable_count: two CODE_SIGNING endpoints sharing one fingbrint=<hex> produce the SAME component count as one endpoint (no duplicate cert component)
    - test_codesign_distinct_fingerprints_not_deduped: two CODE_SIGNING endpoints with different fingerprints produce two cert components
  </behavior>
  <action>
    In quirk/cbom/builder.py add an `elif ep.protocol == "CODE_SIGNING":` branch in Pass-1 (after the ADCS elif) calling _register_algorithm(ep.cert_pubkey_alg, algo_registry, key_size=ep.cert_pubkey_size) when cert_pubkey_alg is set. Add the literal "CODE_SIGNING" to BOTH the Pass-2 skip tuple (lines 619-627) and the Pass-3 skip tuple (lines 705-714). Implement the CSIGN-03 dedup contract among CODE_SIGNING endpoints: add a module-or-local helper _extract_fp(service_detail) that splits on "|" and returns the value after "fingerprint=". Add a dedup pass that builds a {fp_hex: bom_ref} dict over CODE_SIGNING endpoints; the FIRST endpoint for a fingerprint emits a cert component (build CertificateProperties mirroring the TLS Pass-2 pattern at lines 644-663, bom_ref = f"crypto/certificate/codesign/{fp}"), subsequent endpoints with the same fingerprint do NOT emit a new component — instead annotate the existing component with a CycloneDX Property "quirk:code-signing-eku"="true" (TLS-derived component WINS per CONTEXT). The dedup must guarantee a stable component count regardless of how many times the same fingerprint recurs.
    Write tests/test_codesign_cbom.py covering the three behaviors above by constructing CryptoEndpoint objects directly (protocol="CODE_SIGNING", cert_pubkey_alg="RSA", service_detail with shared vs distinct fingerprint= tokens) and calling build_cbom(eps). Assert exact component counts.
  </action>
  <verify>
    <automated>.venv/bin/python -m compileall quirk/cbom/builder.py -q && .venv/bin/python -m pytest tests/test_codesign_cbom.py -x -q</automated>
  </verify>
  <done>CODE_SIGNING registers a Pass-1 algorithm component, is skipped in Pass-2/3, and dedups by fingerprint with a proven-stable component count.</done>
  <acceptance_criteria>
    - Pass-2 AND Pass-3 skip tuples both contain the literal "CODE_SIGNING"
    - _extract_fp parses the "fingerprint=<hex>" token from service_detail
    - test_cbom_dedup_stable_count proves duplicate-fingerprint endpoints do NOT inflate the component count (stable)
    - test_codesign_distinct_fingerprints_not_deduped proves distinct fingerprints emit distinct components
    - dedup precedence keeps the first/TLS-derived component and annotates rather than duplicating
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Evidence counter + scoring weight + dual SCORE_WEIGHTS invariant (SCORE-01)</name>
  <files>quirk/intelligence/evidence.py, quirk/intelligence/scoring.py, tests/test_score_weights_invariant.py, tests/test_evidence.py</files>
  <read_first>
    - quirk/intelligence/evidence.py lines 11-17 (_PROTOCOL_KEYS), 305-321 (Phase 94 BEARER_TOKEN/OPENAPI counter block), return-dict region ~471
    - quirk/intelligence/scoring.py lines 54-60 (SCORE_WEIGHTS agility block), 225-233 (Phase 94 agility_impacts)
    - tests/test_score_weights_invariant.py lines 5-44 (both sum + count assertions and docstring)
    - .planning/phases/95-code-signing-certificate-inventory/95-PATTERNS.md (sections "evidence.py", "scoring.py", "test_score_weights_invariant.py" + Critical Caveat 5)
  </read_first>
  <behavior>
    - test_score_weights_sum_invariant: sum(SCORE_WEIGHTS.values()) == 299.0
    - test_score_weights_count_invariant: len(SCORE_WEIGHTS) == 40
    - test_evidence (k=codesign): a CODE_SIGNING endpoint whose service_detail contains "weak" increments codesign_weak_algo_count and the returned dict carries codesign_weak_algo_count + agility_codesign_weak_algo_ratio
  </behavior>
  <action>
    In quirk/intelligence/evidence.py add "CODE_SIGNING" to the _PROTOCOL_KEYS tuple (after "OPENAPI"). Declare codesign_weak_algo_count = 0 alongside the Phase 94 counters. Add an `elif proto == "CODE_SIGNING":` branch (mirror the BEARER_TOKEN block at 305-321) that increments codesign_weak_algo_count when str(getattr(ep,"service_detail","") or "").lower() contains "weak". Add to the return dict: "codesign_weak_algo_count": codesign_weak_algo_count and "agility_codesign_weak_algo_ratio": round(codesign_weak_algo_count/total_endpoints,4) if total_endpoints else 0.0.
    In quirk/intelligence/scoring.py add one SCORE_WEIGHTS entry "agility_codesign_weak_algo_ratio": 6.0 (Phase 95 SCORE-01) after the Phase 94 pair — this is the single +6.0 entry (sum 293.0→299.0, count 39→40). In the agility_impacts assembly, append ("Code-signing cert weak algorithm", -_ratio(max(0,_as_int(evidence.get("codesign_weak_algo_count",0))), denom) * w["agility_codesign_weak_algo_ratio"]).
    In tests/test_score_weights_invariant.py change the sum assertion 293.0→299.0 and the count assertion 39→40 in the SAME edit, and append a Phase 95 line to the docstring history. Add (or extend) tests/test_evidence.py with a codesign-keyed test asserting the counter + ratio appear in the evidence dict.
  </action>
  <verify>
    <automated>.venv/bin/python -m compileall quirk/intelligence/evidence.py quirk/intelligence/scoring.py -q && .venv/bin/python -m pytest tests/test_score_weights_invariant.py tests/test_evidence.py -x -q</automated>
  </verify>
  <done>SCORE_WEIGHTS sum 299.0 / count 40 with both invariants green; CODE_SIGNING in _PROTOCOL_KEYS; codesign_weak_algo_count flows from evidence into the agility scoring impact.</done>
  <acceptance_criteria>
    - exactly ONE new SCORE_WEIGHTS entry "agility_codesign_weak_algo_ratio": 6.0
    - test_score_weights_sum_invariant asserts 299.0; test_score_weights_count_invariant asserts 40 — BOTH updated
    - "CODE_SIGNING" present in evidence._PROTOCOL_KEYS
    - evidence dict contains codesign_weak_algo_count and agility_codesign_weak_algo_ratio
    - scoring agility_impacts contains the "Code-signing cert weak algorithm" entry
    - `.venv/bin/python -m pytest tests/test_score_weights_invariant.py tests/test_evidence.py -x -q` green
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanner endpoints → CBOM builder | CODE_SIGNING service_detail (fingerprint token) parsed by the dedup pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-05 | Tampering | _extract_fp parsing service_detail | mitigate | parses only a fixed "fingerprint=" token via split; no eval/exec; non-matching detail yields None and is treated as non-deduped |
| T-95-06 | Denial of Service | dedup pass over endpoints | accept | bounded by endpoint list length already capped upstream (paged_size=500 per LDAP page) |
| T-95-07 | Tampering | npm/pip/cargo installs | accept | no new packages this phase (RESEARCH Package Legitimacy Audit: none introduced) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_codesign_cbom.py tests/test_score_weights_invariant.py tests/test_evidence.py -x -q` green
- `grep -c "CODE_SIGNING" quirk/cbom/builder.py` ≥ 3 (Pass-1 branch + Pass-2 + Pass-3)
</verification>

<success_criteria>
CSIGN-03 dedup proven stable; SCORE-01 weights advance to 299.0/40 with both invariants green
and code-signing weak-algo feeding the agility subscore.
</success_criteria>

<output>
Create `.planning/phases/95-code-signing-certificate-inventory/95-02-SUMMARY.md` when done
</output>
