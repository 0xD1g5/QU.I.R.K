---
phase: 95-code-signing-certificate-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/scanner/codesign_scanner.py
  - quirk/config.py
  - tests/test_codesign_scanner.py
  - tests/fixtures/codesign/codesign_rsa1024_sha1.der
  - tests/fixtures/codesign/codesign_ec192.der
  - tests/fixtures/codesign/codesign_rsa2048_sha256.der
  - tests/fixtures/codesign/codesign_rsa2048_sha256_noncoding.der
  - tests/fixtures/codesign/regen.sh
autonomous: true
requirements: [CSIGN-01, CSIGN-02]
must_haves:
  truths:
    - "Scanner discovers code-signing certs from LDAP userCertificate filtered to CodeSigning EKU"
    - "Scanner checks already-captured TLS endpoints in-process for CodeSigning EKU (no new network I/O)"
    - "RSA<2048 / EC<256 / SHA-1 code-signing certs raise HIGH severity"
    - "Certs lacking the CodeSigning EKU are filtered out and emit no endpoint"
  artifacts:
    - path: "quirk/scanner/codesign_scanner.py"
      provides: "scan_codesign_from_ldap, scan_codesign_from_tls_endpoints, CODE_SIGNING constant, EKU + severity helpers"
      contains: "CODE_SIGNING ="
    - path: "tests/test_codesign_scanner.py"
      provides: "Unit coverage for CSIGN-01/02 (EKU filter, weak-algo classification, fingerprint emission)"
    - path: "tests/fixtures/codesign/codesign_rsa1024_sha1.der"
      provides: "Weak RSA-1024/SHA-1 CodeSigning fixture"
  key_links:
    - from: "quirk/scanner/codesign_scanner.py"
      to: "quirk/util/weak_crypto.py"
      via: "is_weak_cipher(sig_hash)"
      pattern: "is_weak_cipher"
    - from: "quirk/scanner/codesign_scanner.py"
      to: "cryptography ExtendedKeyUsageOID.CODE_SIGNING"
      via: "_has_codesigning_eku"
      pattern: "ExtendedKeyUsageOID.CODE_SIGNING"
---

<objective>
Build the code-signing scanner module: LDAP `userCertificate` discovery filtered to the
CodeSigning EKU (OID 1.3.6.1.5.5.7.3.3), plus an in-process EKU check over already-captured
TLS endpoints. Classify weak algorithms (RSA<2048, EC<256, SHA-1) as HIGH. Compute SHA-256
fingerprints for downstream CBOM dedup (consumed in Plan 02/03). Add the `ConnectorsCfg`
enable flag + target fields. Ship the test suite and DER fixtures test-first (Wave 0).

Purpose: CSIGN-01 (discovery from two passive sources) and CSIGN-02 (weak-algo HIGH findings).
Output: `quirk/scanner/codesign_scanner.py`, `ConnectorsCfg` fields, full unit suite + fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-code-signing-certificate-inventory/95-RESEARCH.md
@.planning/phases/95-code-signing-certificate-inventory/95-PATTERNS.md

<interfaces>
<!-- Contracts the scanner must satisfy; consumed by Plan 02 (CBOM/evidence) and Plan 03 (run_scan wiring). -->
Module constant: CODE_SIGNING = "CODE_SIGNING"  (UPPERCASE — exact-match consumers; never use the literal elsewhere)
EKU_CODE_SIGNING = ExtendedKeyUsageOID.CODE_SIGNING  (OID 1.3.6.1.5.5.7.3.3)
_CODESIGN_ATTRS = ("userCertificate",)  (NOT userSMIMECertificate)

scan_codesign_from_ldap(targets, timeout=10, logger=None, session_start=None, *, search_base=None) -> list[CryptoEndpoint]
scan_codesign_from_tls_endpoints(tls_endpoints, session_start=None, logger=None) -> list[CryptoEndpoint]

Emitted CryptoEndpoint fields: protocol=CODE_SIGNING, cert_pubkey_alg, cert_pubkey_size,
cert_sig_alg, severity ("HIGH" when weak), service_detail containing "|fingerprint=<sha256hex>"
and a "weak" token when weak, smime_scan_json (reuses existing column) holding the reasons list.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate DER fixtures + write failing scanner unit suite (Wave 0)</name>
  <files>tests/fixtures/codesign/regen.sh, tests/fixtures/codesign/codesign_rsa1024_sha1.der, tests/fixtures/codesign/codesign_ec192.der, tests/fixtures/codesign/codesign_rsa2048_sha256.der, tests/fixtures/codesign/codesign_rsa2048_sha256_noncoding.der, tests/test_codesign_scanner.py</files>
  <read_first>
    - quantum-chaos-enterprise-lab/smime/certs/regen.sh (DER generation analog)
    - tests/test_smime_scanner.py (near-exact test structure to mirror)
    - .planning/phases/95-code-signing-certificate-inventory/95-PATTERNS.md (sections "tests/fixtures/codesign/" and "tests/test_codesign_scanner.py")
  </read_first>
  <behavior>
    - test_rsa1024_sha1_emits_high: RSA-1024/SHA-1 + CodeSigning EKU → 1 endpoint, protocol == "CODE_SIGNING", severity == "HIGH", reasons include "weak-signing-alg" and "weak-rsa-key"
    - test_ec192_emits_high: EC-192 + CodeSigning EKU → severity HIGH, reasons include "weak-ec-key"
    - test_strong_rsa2048_sha256_safe: RSA-2048/SHA-256 + CodeSigning EKU → zero endpoints (SAFE)
    - test_non_codesign_eku_filtered: RSA-2048/SHA-256 WITHOUT CodeSigning EKU → zero endpoints
    - test_fingerprint_in_service_detail: a weak endpoint's service_detail contains "fingerprint=" with a 64-char hex SHA-256
    - test_protocol_constant_uppercase: codesign_scanner.CODE_SIGNING == "CODE_SIGNING"
  </behavior>
  <action>
    Create tests/fixtures/codesign/regen.sh mirroring quantum-chaos-enterprise-lab/smime/certs/regen.sh, generating four DER fixtures via openssl with `-addext "extendedKeyUsage=codeSigning"` on the three EKU-bearing certs and OMITTING that addext on the noncoding fixture: codesign_rsa1024_sha1.der (rsa:1024 -sha1), codesign_ec192.der (ec prime192v1 -sha256), codesign_rsa2048_sha256.der (rsa:2048 -sha256), codesign_rsa2048_sha256_noncoding.der (rsa:2048 -sha256, NO EKU). Use -days 36500 -nodes and subjects under O=QUIRK Chaos Lab. Run regen.sh to materialize the .der files.
    Write tests/test_codesign_scanner.py mirroring tests/test_smime_scanner.py: patch codesign_scanner.LDAP3_AVAILABLE True and codesign_scanner._bind_and_search_codesign to return a synthetic iterable of searchResEntry dicts (raw_attributes.userCertificate = [der]). Cover all six behaviors above. Assert exact UPPERCASE "CODE_SIGNING" protocol. Tests MUST fail initially (module absent) — this establishes RED.
  </action>
  <verify>
    <automated>test -f tests/fixtures/codesign/codesign_rsa1024_sha1.der && test -f tests/fixtures/codesign/codesign_ec192.der && test -f tests/fixtures/codesign/codesign_rsa2048_sha256.der && test -f tests/fixtures/codesign/codesign_rsa2048_sha256_noncoding.der && .venv/bin/python -m pytest tests/test_codesign_scanner.py -q 2>&1 | grep -qiE "error|failed|no tests ran|ModuleNotFound" && echo RED-CONFIRMED</automated>
  </verify>
  <done>Four DER fixtures exist; test_codesign_scanner.py exists with the six named tests and they fail because codesign_scanner is not yet implemented (RED state).</done>
  <acceptance_criteria>
    - tests/fixtures/codesign/ contains exactly the four named .der files plus regen.sh
    - noncoding fixture has NO CodeSigning EKU; the other three carry EKU OID 1.3.6.1.5.5.7.3.3
    - test file asserts protocol literal "CODE_SIGNING" (uppercase) and reasons "weak-signing-alg"/"weak-rsa-key"/"weak-ec-key"
    - suite is RED (module not yet implemented)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement codesign_scanner.py to GREEN + ConnectorsCfg fields</name>
  <files>quirk/scanner/codesign_scanner.py, quirk/config.py</files>
  <read_first>
    - quirk/scanner/smime_scanner.py (near-exact analog: _parse_target, _realm_to_base_dn, _bind_and_search, scan structure, error-endpoint pattern)
    - quirk/scanner/adcs_scanner.py (EKU OID constant style)
    - quirk/util/weak_crypto.py (is_weak_cipher token set; confirm SHA-1 coverage, ECDSA NOT covered)
    - quirk/util/safe_exc.py (safe_str for all LDAP-derived logging)
    - quirk/models.py (CryptoEndpoint fields: protocol, cert_pubkey_alg, cert_pubkey_size, cert_sig_alg, severity, service_detail, smime_scan_json, scan_error, scan_error_category, scanned_at)
    - quirk/config.py lines 223-238 (SMIME ConnectorsCfg block to mirror)
    - .planning/phases/95-code-signing-certificate-inventory/95-PATTERNS.md (section "quirk/scanner/codesign_scanner.py")
  </read_first>
  <action>
    Create quirk/scanner/codesign_scanner.py with: ldap3 import-guard (LDAP3_AVAILABLE), module constants CODE_SIGNING = "CODE_SIGNING", EKU_CODE_SIGNING = ExtendedKeyUsageOID.CODE_SIGNING, _CODESIGN_ATTRS = ("userCertificate",). Implement _has_codesigning_eku(cert_obj) using cert.extensions.get_extension_for_class(x509.ExtendedKeyUsage) and `EKU_CODE_SIGNING in eku_ext.value`, returning False on ExtensionNotFound/Exception. Implement _parse_codesign_cert(cert_bytes) with DER-then-PEM dual-parse (debug-log via safe_str on failure), extracting key_alg/key_bits (RSA via rsa.RSAPublicKey, ECDSA via ec.EllipticCurvePublicKey), sig_hash, serial (hex), not_after iso, expired bool, and fingerprint = cert.fingerprint(hashes.SHA256()).hex(). Implement _classify_codesign_severity(parsed): append "weak-signing-alg" when is_weak_cipher(sig_hash), "weak-rsa-key" when RSA and key_bits<2048, "weak-ec-key" when ECDSA and key_bits<256 (inline — NOT via is_weak_cipher; do NOT add ECDSA tokens to weak_crypto). Return ("HIGH", reasons) if any reason else (None, reasons). Copy _parse_target and _realm_to_base_dn verbatim from smime_scanner; implement _bind_and_search_codesign with anonymous bind, search_filter "(userCertificate=*)" (hardcoded literal — no user interpolation), attributes ["userCertificate","cn","uid"], paged_size=500, generator=True. Implement scan_codesign_from_ldap(targets, timeout=10, logger=None, session_start=None, *, search_base=None): loop targets, on bind/search Exception append an error CryptoEndpoint (protocol=CODE_SIGNING, scan_error=safe_str(exc), scan_error_category="exception") and continue; per entry parse each userCertificate bytes, skip if not _has_codesigning_eku, skip if severity is None (SAFE emits nothing), else emit CryptoEndpoint(protocol=CODE_SIGNING, cert_pubkey_alg, cert_pubkey_size, cert_sig_alg, severity, service_detail=f"{user_dn}|attr=userCertificate|serial=...|fingerprint={fp}|weak", smime_scan_json=json.dumps({**parsed,"reasons":reasons,"user_dn":user_dn})). Implement scan_codesign_from_tls_endpoints(tls_endpoints, session_start=None, logger=None): for each TLS CryptoEndpoint that carries the CodeSigning EKU (read EKU from tls_scan_json if present, else skip), emit a CODE_SIGNING endpoint reusing the surrogate compound key (cert_subject + cert_pubkey_alg + cert_not_after) in service_detail since raw DER is unavailable (resolves RESEARCH Open Question 1); never fetch network. Ensure the "weak" token appears in service_detail only when severity is HIGH.
    Add to quirk/config.py ConnectorsCfg after the SMIME block: enable_codesign: bool = False, codesign_targets: list = field(default_factory=list), codesign_search_base: Optional[str] = None, codesign_timeout: int = 10. Do NOT touch _KNOWN_CONNECTOR_KEYS (derived from dataclass fields).
  </action>
  <verify>
    <automated>.venv/bin/python -m compileall quirk/scanner/codesign_scanner.py quirk/config.py -q && .venv/bin/python -m pytest tests/test_codesign_scanner.py -x -q</automated>
  </verify>
  <done>codesign_scanner.py implemented; all six unit tests GREEN; ConnectorsCfg exposes enable_codesign/codesign_targets/codesign_search_base/codesign_timeout.</done>
  <acceptance_criteria>
    - Module defines CODE_SIGNING == "CODE_SIGNING" and _CODESIGN_ATTRS == ("userCertificate",)
    - EKU check uses ExtendedKeyUsageOID.CODE_SIGNING (OID 1.3.6.1.5.5.7.3.3)
    - EC<256 handled by inline key-size check; weak_crypto._WEAK_CIPHER_TOKENS NOT modified (no "ECDSA-256" token)
    - fingerprint computed via cert.fingerprint(hashes.SHA256()).hex() and embedded as "fingerprint=<hex>" in service_detail
    - search_filter is the hardcoded literal "(userCertificate=*)" — no user-controlled DN/filter interpolation
    - no `import impacket` anywhere; ldap3 only
    - all logging of LDAP-derived/exception data routed through safe_str()
    - `.venv/bin/python -m pytest tests/test_codesign_scanner.py -x -q` is green
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LDAP server → scanner | untrusted userCertificate DER bytes + DN strings cross here |
| TLS-captured cert metadata → scanner | already-parsed in-memory metadata; no fresh network read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01 | Tampering | `_bind_and_search_codesign` LDAP filter | mitigate | search_filter is the hardcoded literal `(userCertificate=*)`; no DN/filter built from user input (LDAP injection closed) |
| T-95-02 | Denial of Service | `_parse_codesign_cert` on malformed DER | mitigate | DER/PEM parse wrapped in try/except returning None; paged_size=500 bounds entries per page |
| T-95-03 | Information Disclosure | logging of LDAP exceptions/DN | mitigate | all exception/LDAP-derived strings routed through `safe_str()`; service_detail embeds only fingerprint + key metadata + DN (internal consultant-tool scope, mirrors smime) |
| T-95-04 | Tampering | npm/pip/cargo installs | accept | no new packages this phase (cryptography + ldap3 already installed; RESEARCH Package Legitimacy Audit: none introduced) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_codesign_scanner.py -x -q` green
- `grep -L impacket quirk/scanner/codesign_scanner.py` confirms no impacket import
- `.venv/bin/python -m compileall quirk/scanner/codesign_scanner.py quirk/config.py -q`
</verification>

<success_criteria>
Scanner discovers and weak-classifies code-signing certs from LDAP + TLS-EKU sources;
CSIGN-01 and CSIGN-02 unit-verified; ConnectorsCfg flags present; ldap3-only.
</success_criteria>

<output>
Create `.planning/phases/95-code-signing-certificate-inventory/95-01-SUMMARY.md` when done
</output>
