---
phase: 95-code-signing-certificate-inventory
plan: 03
type: execute
wave: 3
depends_on: [95-01, 95-02]
files_modified:
  - run_scan.py
  - quantum-chaos-enterprise-lab/docker-compose.yml
  - quantum-chaos-enterprise-lab/ldaps/ldif/codesign-users.ldif
  - quantum-chaos-enterprise-lab/ldaps/certs/regen.sh
  - quantum-chaos-enterprise-lab/expected_results_v4.md
  - quantum-chaos-enterprise-lab/README.md
  - tests/test_run_scan_codesign_wiring.py
autonomous: true
requirements: [CSIGN-01, LAB-01]
must_haves:
  truths:
    - "Running a scan with --inventory-code-signing invokes the code-signing phase and folds endpoints into the final assembly"
    - "CODE_SIGNING participates in resume/_dar_protocols logic"
    - "The ldaps chaos profile carries a userCertificate with CodeSigning EKU + weak RSA-1024/SHA-1, with expected_results + README updated in the same change"
  artifacts:
    - path: "run_scan.py"
      provides: "--inventory-code-signing flag, _run_codesign_phase, codesign_endpoints assembly, _dar_protocols + resume entry"
      contains: "inventory_code_signing"
    - path: "quantum-chaos-enterprise-lab/ldaps/ldif/codesign-users.ldif"
      provides: "code-signing fixture user under dc=chaos,dc=local"
    - path: "quantum-chaos-enterprise-lab/expected_results_v4.md"
      provides: "ldaps code-signing oracle row (CODE-SIGN/weak-algorithm HIGH)"
      contains: "CODE-SIGN/weak-algorithm"
  key_links:
    - from: "run_scan.py"
      to: "quirk.scanner.codesign_scanner.scan_codesign_from_ldap"
      via: "_run_codesign_phase"
      pattern: "scan_codesign_from_ldap"
    - from: "quantum-chaos-enterprise-lab/docker-compose.yml"
      to: "ldaps/ldif/codesign-users.ldif"
      via: "ldaps-codesign-seed sidecar"
      pattern: "codesign-users.ldif"
---

<objective>
Wire `--inventory-code-signing` into run_scan.py (CLI flag, `_run_codesign_phase` via
`_wrapped_phase`, endpoints assembly, `_dar_protocols` + resume entry), and add the
chaos-lab code-signing fixture to the existing `ldaps` profile with the mandatory triple
update (seed/LDIF + expected_results_v4.md + README). lab.sh needs no edit — profiles are
dynamically discovered — but status/logs must remain clean.

Purpose: CSIGN-01 (end-to-end invocation) and LAB-01 (chaos-lab fixture coverage).
Output: run_scan.py wiring, ldaps-codesign-seed sidecar + LDIF + DER fixture, oracle + README updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-code-signing-certificate-inventory/95-RESEARCH.md
@.planning/phases/95-code-signing-certificate-inventory/95-PATTERNS.md
@.planning/phases/95-code-signing-certificate-inventory/95-01-SUMMARY.md

<interfaces>
<!-- From Plan 01/02. -->
quirk.scanner.codesign_scanner.scan_codesign_from_ldap(targets, timeout, logger, session_start, *, search_base)
ConnectorsCfg fields: enable_codesign, codesign_targets, codesign_search_base, codesign_timeout
ldaps profile uses LDAP_ROOT "dc=chaos,dc=local" (NOT dc=quirk,dc=lab); admin cn=admin/admin; port 389 internal / 636 LDAPS.
smime-seed sidecar (docker-compose.yml lines 784-801) is the idempotent ldapadd -c / swallow-68 analog.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run_scan.py --inventory-code-signing wiring</name>
  <files>run_scan.py, tests/test_run_scan_codesign_wiring.py</files>
  <read_first>
    - run_scan.py lines 728-739 (--openapi-spec argparse flag shape), 1503 (_dar_protocols), 1512-1513 (resume rebuild), 1691-1733 (_run_smime_phase / _run_adcs_phase + _wrapped_phase), 1948-1965 (endpoints assembly)
    - .planning/phases/95-code-signing-certificate-inventory/95-PATTERNS.md (section "run_scan.py")
  </read_first>
  <behavior>
    - test_flag_off_no_phase: with inventory_code_signing False, _run_codesign_phase returns [] and no scanner import occurs
    - test_flag_on_invokes_scanner: with inventory_code_signing True and codesign_targets set, scan_codesign_from_ldap is called (patched) and its endpoints land in the final endpoints list
    - test_dar_protocols_contains_codesign: _dar_protocols tuple contains "CODE_SIGNING"
  </behavior>
  <action>
    Add an argparse argument --inventory-code-signing (dest=inventory_code_signing, action="store_true", default False, help referencing CSIGN-01). Add "CODE_SIGNING" to the _dar_protocols tuple at line 1503. Add a _run_codesign_phase() that returns [] unless getattr(args,"inventory_code_signing",False) and getattr(cfg.connectors,"codesign_targets",None); inside, lazy-import scan_codesign_from_ldap and call it with targets/timeout/logger/session_start/search_base from cfg.connectors, then log endpoint+target counts. Wrap via codesign_endpoints = _wrapped_phase(run_stats,"codesign_scanning","codesign_scanner",_run_codesign_phase,error_endpoints,logger) or []. In the data_at_rest resume block (near 1512-1513) add codesign_endpoints = [e for e in _resumed_endpoints if getattr(e,"protocol","")=="CODE_SIGNING"]. Add `+ codesign_endpoints` to the endpoints assembly tuple after adcs_endpoints (line ~1965). Keep the lazy import inside _run_codesign_phase so the flag-off path imports nothing.
    Write tests/test_run_scan_codesign_wiring.py covering the three behaviors by patching quirk.scanner.codesign_scanner.scan_codesign_from_ldap and a minimal args/cfg namespace. Keep diffs minimal (CLAUDE.md).
  </action>
  <verify>
    <automated>.venv/bin/python -m compileall run_scan.py -q && .venv/bin/python -m pytest tests/test_run_scan_codesign_wiring.py -x -q</automated>
  </verify>
  <done>--inventory-code-signing flag exists; _run_codesign_phase wired via _wrapped_phase; CODE_SIGNING in _dar_protocols and resume; codesign_endpoints in final assembly.</done>
  <acceptance_criteria>
    - argparse exposes --inventory-code-signing maps to args.inventory_code_signing
    - flag-off path performs NO scanner import and returns []
    - "CODE_SIGNING" present in _dar_protocols tuple
    - codesign_endpoints appended to the endpoints assembly
    - `.venv/bin/python -m pytest tests/test_run_scan_codesign_wiring.py -x -q` green
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Chaos-lab ldaps code-signing fixture + triple update (LAB-01)</name>
  <files>quantum-chaos-enterprise-lab/docker-compose.yml, quantum-chaos-enterprise-lab/ldaps/ldif/codesign-users.ldif, quantum-chaos-enterprise-lab/ldaps/certs/regen.sh, quantum-chaos-enterprise-lab/expected_results_v4.md, quantum-chaos-enterprise-lab/README.md</files>
  <read_first>
    - quantum-chaos-enterprise-lab/docker-compose.yml lines 736-801 (ldaps profile + smime-seed sidecar analog)
    - quantum-chaos-enterprise-lab/smime/certs/regen.sh (DER generation analog)
    - quantum-chaos-enterprise-lab/expected_results_v4.md (ldaps profile section to extend)
    - quantum-chaos-enterprise-lab/lab.sh lines 58-168 (_derive_all_profiles — confirm no edit needed)
    - .planning/phases/95-code-signing-certificate-inventory/95-PATTERNS.md (Critical Caveat 4: base DN dc=chaos,dc=local)
  </read_first>
  <action>
    Create quantum-chaos-enterprise-lab/ldaps/certs/regen.sh generating a weak RSA-1024/SHA-1 cert with `-addext "extendedKeyUsage=codeSigning"` (mirror smime/certs/regen.sh) and emit a base64 DER body suitable for an LDIF `userCertificate;binary::` value; run it to produce the cert artifact. Create quantum-chaos-enterprise-lab/ldaps/ldif/codesign-users.ldif adding uid=codesign-weak under ou=people,dc=chaos,dc=local (create the ou entry if absent) with the userCertificate;binary value. Add an ldaps-codesign-seed sidecar to docker-compose.yml under profiles: ["ldaps"], depends_on ldaps service_started, mounting ./ldaps/ldif:/ldif:ro, running the idempotent ldapadd -c -x -H ldap://ldaps:389 -D 'cn=admin,dc=chaos,dc=local' -w admin -f /ldif/codesign-users.ldif with the rc-0-or-68-swallow pattern copied from smime-seed (restart: "no"). Update expected_results_v4.md: append a "Profile: ldaps — Code-Signing Fixture" subsection with the oracle row (uid=codesign-weak | RSA-1024/SHA-1 + CodeSigning EKU | CODE-SIGN/weak-algorithm | HIGH) and the expected "1 HIGH CODE_SIGNING finding" statement. Update README.md ldaps section to describe the new code-signing fixture. Do NOT edit lab.sh — verify _derive_all_profiles reads ldaps dynamically (no ALL_PROFILES edit per RESEARCH).
  </action>
  <verify>
    <automated>test -f quantum-chaos-enterprise-lab/ldaps/ldif/codesign-users.ldif && grep -q "userCertificate" quantum-chaos-enterprise-lab/ldaps/ldif/codesign-users.ldif && grep -q "CODE-SIGN/weak-algorithm" quantum-chaos-enterprise-lab/expected_results_v4.md && grep -q "ldaps-codesign-seed" quantum-chaos-enterprise-lab/docker-compose.yml && grep -qi "code-signing" quantum-chaos-enterprise-lab/README.md && echo TRIPLE-UPDATE-OK</automated>
  </verify>
  <done>--inventory-code-signing fully wired; ldaps profile carries a CodeSigning-EKU weak fixture with expected_results_v4.md + README updated in the same change; lab.sh untouched.</done>
  <acceptance_criteria>
    - docker-compose.yml has an ldaps-codesign-seed sidecar under profiles: ["ldaps"] binding cn=admin,dc=chaos,dc=local with the rc-0-or-68 idempotent ldapadd
    - codesign-users.ldif places uid=codesign-weak under dc=chaos,dc=local (NOT dc=quirk,dc=lab) with userCertificate;binary
    - expected_results_v4.md contains the literal "CODE-SIGN/weak-algorithm" oracle row at HIGH
    - README.md ldaps section mentions the code-signing fixture
    - lab.sh NOT modified (profiles dynamically discovered)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → run_scan | --inventory-code-signing is a boolean opt-in gate; targets come from cfg.connectors |
| chaos-lab LDAP fixture → scanner | seeded test cert; local-only loopback target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-08 | Elevation of Privilege | --inventory-code-signing scope | mitigate | feature is opt-in; _run_codesign_phase returns [] unless flag set AND codesign_targets configured |
| T-95-09 | Information Disclosure | seed sidecar admin bind | accept | chaos-lab fixture uses local throwaway admin/admin on loopback; not a production credential surface |
| T-95-10 | Tampering | npm/pip/cargo installs | accept | no new packages this phase (RESEARCH Package Legitimacy Audit: none introduced) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_run_scan_codesign_wiring.py -x -q` green
- Triple-update grep gate (Task 2 automated) prints TRIPLE-UPDATE-OK
- `git diff --name-only quantum-chaos-enterprise-lab/lab.sh` is empty
</verification>

<success_criteria>
CSIGN-01 end-to-end invocation works behind --inventory-code-signing; LAB-01 ldaps fixture
present with the mandatory triple update (LDIF/seed + expected_results + README) and lab.sh untouched.
</success_criteria>

<output>
Create `.planning/phases/95-code-signing-certificate-inventory/95-03-SUMMARY.md` when done
</output>
