---
phase: 16-v4-1-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_v41_gap_closure.py
autonomous: true
requirements:
  - CLI-04
  - SCORE-04

must_haves:
  truths:
    - "RED test for CLI-04 fails because importlib.metadata.version('quirk') returns '4.0.0'"
    - "RED test for SCORE-04 fails because interactive.py output dir default is 'output' not 'quirk-output'"
    - "Both tests are valid pytest tests that can be collected and executed"
  artifacts:
    - path: "tests/test_v41_gap_closure.py"
      provides: "RED TDD scaffold for CLI-04 and SCORE-04 gap closure"
      contains: "test_package_manifest_version_is_4_1_0"
      min_lines: 30
  key_links:
    - from: "tests/test_v41_gap_closure.py"
      to: "importlib.metadata.version"
      via: "importlib.metadata stdlib call"
      pattern: "importlib\\.metadata\\.version"
    - from: "tests/test_v41_gap_closure.py"
      to: "quirk/interactive.py"
      via: "pathlib source inspection"
      pattern: "pathlib\\.Path.*interactive\\.py"
---

<objective>
Create RED TDD test scaffold for the two v4.1 gap closure requirements (CLI-04 and SCORE-04).

Purpose: Prove both gaps exist before fixing them, following the project's established TDD pattern from Phases 12-15 where Plan 1 writes RED tests and Plan 2 applies GREEN fixes.

Output: `tests/test_v41_gap_closure.py` with 4 test functions that all FAIL against current code.
</objective>

<execution_context>
@/Users/digs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/digs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-v4-1-gap-closure/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write RED test scaffold for CLI-04 and SCORE-04</name>
  <files>tests/test_v41_gap_closure.py</files>
  <read_first>
    - pyproject.toml (confirm line 7 still reads version = "4.0.0")
    - quirk/interactive.py (confirm lines 165-166 still use "output" defaults)
    - tests/test_scoring_correctness.py (reference pattern for Phase 14 RED scaffold)
    - tests/test_cli_correctness.py (reference pattern for source inspection)
    - quirk/__init__.py (confirm __version__ = "4.1.0")
  </read_first>
  <action>
Create `tests/test_v41_gap_closure.py` with the following 4 test functions. Use the exact patterns from RESEARCH.md code examples.

**Module docstring:**
```
"""Phase 16 — v4.1 Gap Closure RED scaffold.

Tests assert the correct end-state for CLI-04 and SCORE-04.
Both tests MUST FAIL before Plan 02 fixes land:
- CLI-04: importlib.metadata.version("quirk") returns "4.0.0" (pyproject.toml stale)
- SCORE-04: interactive.py output dir default is "output" (dashboard expects "quirk-output")
"""
```

**Imports:** `import importlib.metadata`, `import pathlib`, `import unittest`

**Test 1 — `test_package_manifest_version_is_4_1_0`:**
- Calls `importlib.metadata.version("quirk")`
- Asserts result `== "4.1.0"`
- Assertion message: `f"importlib.metadata.version('quirk') = {version!r}; expected '4.1.0' — bump pyproject.toml version field and reinstall"`
- RED because: pyproject.toml has `version = "4.0.0"`, installed egg-info reflects that

**Test 2 — `test_pyproject_version_field_is_4_1_0`:**
- Reads `pathlib.Path("pyproject.toml").read_text(encoding="utf-8")`
- Asserts `'version = "4.1.0"'` is in the source text
- Assertion message: `"pyproject.toml does not contain 'version = \"4.1.0\"' — current value is '4.0.0'"`
- RED because: pyproject.toml line 7 literally says `version = "4.0.0"`

**Test 3 — `test_interactive_output_dir_default_is_quirk_output`:**
- Reads `pathlib.Path("quirk/interactive.py").read_text(encoding="utf-8")`
- Asserts `'_prompt("Output directory", "quirk-output")' in source`
- Assertion message: `"interactive.py does not use 'quirk-output' as output dir default — current default is 'output'"`
- RED because: line 165 has `_prompt("Output directory", "output")`

**Test 4 — `test_interactive_db_path_default_is_quirk_output`:**
- Reads `pathlib.Path("quirk/interactive.py").read_text(encoding="utf-8")`
- Asserts `'_prompt("SQLite DB path", "quirk-output/quirk.db")' in source`
- Assertion message: `"interactive.py does not use 'quirk-output/quirk.db' as db_path default — current default is 'output/quirk.db'"`
- RED because: line 166 has `_prompt("SQLite DB path", "output/quirk.db")`

Each test must use `unittest.TestCase` class named `TestV41GapClosure`. No `@unittest.expectedFailure` decorator — these tests are meant to be RED (actually failing) to prove the gap exists before Plan 02 fixes them.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python3 -m pytest tests/test_v41_gap_closure.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_v41_gap_closure.py exists and contains exactly 4 test methods
    - File contains `class TestV41GapClosure`
    - File contains `import importlib.metadata`
    - File contains `import pathlib`
    - File contains `def test_package_manifest_version_is_4_1_0`
    - File contains `def test_pyproject_version_field_is_4_1_0`
    - File contains `def test_interactive_output_dir_default_is_quirk_output`
    - File contains `def test_interactive_db_path_default_is_quirk_output`
    - `python3 -m pytest tests/test_v41_gap_closure.py -v` reports exactly 4 FAILED (all RED)
    - No existing tests break: `python3 -m pytest -q` shows 229 passed (or more) with exactly 4 new failures from this file
  </acceptance_criteria>
  <done>All 4 RED tests exist, are collected by pytest, and fail against current code — proving both gaps (CLI-04 stale manifest version, SCORE-04 wrong output dir default) exist before fixes are applied.</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/test_v41_gap_closure.py -v` shows 4 FAILED
- `python3 -m pytest -q --ignore=tests/test_v41_gap_closure.py` shows 229 passed, 0 failed (existing suite unbroken)
- `grep -c "def test_" tests/test_v41_gap_closure.py` returns 4
</verification>

<success_criteria>
- tests/test_v41_gap_closure.py committed with 4 RED tests
- Tests prove both gaps exist: CLI-04 (importlib.metadata returns "4.0.0") and SCORE-04 (interactive.py defaults to "output")
- No regression in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/16-v4-1-gap-closure/16-01-SUMMARY.md`
</output>
