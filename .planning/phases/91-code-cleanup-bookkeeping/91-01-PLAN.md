---
phase: 91-code-cleanup-bookkeeping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_dashboard_scan_history.py
  - quirk/models.py
  - quirk/db.py
  - quirk/scanner/tls_scanner.py
  - quirk/assessment/operator_context.py
  - quirk/scanner/fingerprint.py
  - quirk/engine/findings_evaluator.py
  - quirk/scanner/jwt_scanner.py
  - docs/operators-guide.md
  - docs/configuration.md
  - .planning/codebase/CONCERNS.md
  - .planning/phases/87-dependency-hygiene/87-VALIDATION.md
  - .planning/phases/88-scoring-residuals/88-VALIDATION.md
  - .planning/phases/89-chaos-lab-profiles/89-VALIDATION.md
  - .planning/phases/90-oqs-nginx-pqc-hybrid/90-VALIDATION.md
autonomous: true
requirements: [CLEAN-01, CLEAN-03, CLEAN-04]
must_haves:
  truths:
    - "Deprecation-as-error pytest gate passes on tests/test_dashboard_scan_history.py (was failing at line 71)"
    - "Test suite collects with 0 errors without QUIRK_DB_PATH set (was 7 collection errors)"
    - "No v3.x version string is printed to stdout by operator_context.py"
    - "VALIDATION.md frontmatter for phases 87/88/89 reads nyquist_compliant: true; a 90-VALIDATION.md exists"
    - "Stale CONCERNS.md entries describing deleted files/dual-engine are removed or marked resolved"
    - "JWT verify=False inspection-mode is documented in both jwt_scanner.py (inline) and docs/operators-guide.md"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Autouse QUIRK_DB_PATH=tmp_path isolation fixture"
      contains: "QUIRK_DB_PATH"
    - path: ".planning/phases/90-oqs-nginx-pqc-hybrid/90-VALIDATION.md"
      provides: "Phase 90 Nyquist validation contract"
      contains: "nyquist_compliant"
    - path: "docs/operators-guide.md"
      provides: "JWT allow_insecure_jwks security note"
      contains: "allow_insecure_jwks"
  key_links:
    - from: "tests/conftest.py"
      to: "quirk/dashboard/api/deps.py _default_db_path()"
      via: "monkeypatch.setenv(QUIRK_DB_PATH) sidesteps the ambient resolver"
      pattern: "monkeypatch\\.setenv.*QUIRK_DB_PATH"
    - from: "quirk/scanner/jwt_scanner.py httpx.get"
      to: "docs/operators-guide.md security note"
      via: "verify=verify_tls / allow_insecure_jwks documented in both surfaces"
      pattern: "WHY:"
---

<objective>
Close CLEAN-01 (Tier-A dead-code/comment/deprecation cleanup), CLEAN-03 (bookkeeping: conftest DB isolation, VALIDATION.md currency, stale CONCERNS.md removal), and CLEAN-04 (JWT verify=False advisory documentation). This is the Tier-A plan and MUST ship before Plan 91-02 (Tier-B deletions) per v5.0-D-06.

Purpose: Eliminate the deprecation-as-error CI failure, permanently fix the 7-module "Multiple QU.I.R.K. DBs found" collection error, bring v5.0 Nyquist bookkeeping current, and document the intentional JWT inspection-mode TLS bypass.
Output: Working CI gates (deprecation-as-error green, 0 collection errors), current VALIDATION.md files, cleaned CONCERNS.md, documented JWT advisory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-code-cleanup-bookkeeping/91-CONTEXT.md
@.planning/phases/91-code-cleanup-bookkeeping/91-RESEARCH.md
@.planning/phases/91-code-cleanup-bookkeeping/91-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Confirmed by planner from the codebase. Use directly — no exploration needed. -->

tests/test_dashboard_scan_history.py line 13 ALREADY imports:
    from datetime import datetime, timedelta
  → Add `timezone` to THIS existing line. Do NOT add a second import inside any function
    (Pitfall 4: local-import shadow trap makes timezone function-local and breaks the function).

quirk/scanner/jwt_scanner.py — `verify=verify_tls` httpx.get call sites:
    line ~73: resp = httpx.get(url, timeout=timeout, follow_redirects=True, verify=verify_tls)
    line ~88: resp2 = httpx.get(jwks_uri, timeout=timeout, follow_redirects=True, verify=verify_tls)
  (validate_external_url() already runs on jwks_uri before the line ~88 fetch.)

quirk/dashboard/api/deps.py — `_default_db_path()` raises ValueError("Multiple QU.I.R.K. DBs found")
  when ≥2 candidate .db files exist. The autouse fixture sets QUIRK_DB_PATH to sidestep it.

tests/conftest.py already defines a SHA1-signing shim and a dashboard_client() fixture.
  ADD the autouse fixture; keep both existing pieces intact. dashboard_client() uses
  app.dependency_overrides[get_db] and is unaffected by the env-var fixture (Pitfall 6).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add autouse QUIRK_DB_PATH conftest fixture (CLEAN-03a — Wave 0 unblocker)</name>
  <files>tests/conftest.py</files>
  <read_first>tests/conftest.py (existing SHA1 shim + dashboard_client fixture), quirk/dashboard/api/deps.py:12-33 (_default_db_path)</read_first>
  <action>Add a module-scope autouse pytest fixture `_isolate_quirk_db(tmp_path, monkeypatch)` to tests/conftest.py that calls `monkeypatch.setenv("QUIRK_DB_PATH", str(tmp_path / "quirk_test.db"))`. This sidesteps `_default_db_path()` in quirk/dashboard/api/deps.py so the suite never hits the "Multiple QU.I.R.K. DBs found" ValueError (per D-03a). Keep the existing SHA1 signing shim and `dashboard_client()` fixture exactly as-is — the autouse env-var fixture is harmless for dashboard_client() which overrides get_db via dependency injection (Pitfall 6). Add a docstring citing CLEAN-03 D-03a. PEP 8.</action>
  <verify>
    <automated>python -m pytest tests/ --collect-only -q 2>&1 | grep -c "Multiple QU.I.R.K. DBs found" | grep -qx 0 && echo PASS</automated>
  </verify>
  <done>Running `python -m pytest tests/ --collect-only -q` with NO QUIRK_DB_PATH env var produces 0 "Multiple QU.I.R.K. DBs found" collection errors (was 7 affected modules).</done>
</task>

<task type="auto">
  <name>Task 2: Fix datetime.utcnow() deprecation + sweep stale v3.x/v4.x comments (CLEAN-01 / BACK-55, BACK-56)</name>
  <files>tests/test_dashboard_scan_history.py, quirk/models.py, quirk/db.py, quirk/scanner/tls_scanner.py, quirk/assessment/operator_context.py, quirk/scanner/fingerprint.py, quirk/engine/findings_evaluator.py</files>
  <read_first>tests/test_dashboard_scan_history.py (line 13 import + 9 utcnow sites: 71,95,115,156,191,223,248,286,314), 91-RESEARCH.md BACK-55 + BACK-56 tables</read_first>
  <action>BACK-56: In tests/test_dashboard_scan_history.py replace all 9 `datetime.utcnow()` occurrences (lines ~71,95,115,156,191,223,248,286,314) with `datetime.now(timezone.utc)`, and add `timezone` to the EXISTING `from datetime import datetime, timedelta` at line 13 (→ `from datetime import datetime, timedelta, timezone`). Do NOT add a per-function import (Pitfall 4). Do NOT touch the chaos-lab JWT containers under quantum-chaos-enterprise-lab/jwt/*/main.py — they are out of scope (not the quirk package, not pytest-gated; Pitfall 3).
  BACK-55: Remove/replace the `# v3.x`/`# v4.x` era-tagging code comments in quirk/models.py (lines ~41,53,58,66,75,80,86,92), quirk/db.py (lines ~173-175), quirk/scanner/tls_scanner.py (line ~441) — keep the field/column meaning, drop the misleading version tag. In quirk/assessment/operator_context.py line ~33 remove the `(v3.5.1)` from the printed string `print("\n🧠 Assessment Context (v3.5.1)")` (highest priority — user-visible false version). Clean the version references in the quirk/scanner/fingerprint.py:138 docstring and quirk/engine/findings_evaluator.py:343 comment. Minimal diffs, PEP 8; do NOT do a wholesale Phase/D-ref comment purge (BACK-55 scope is the worst CONCERNS §6.3 examples only).</action>
  <verify>
    <automated>QUIRK_DB_PATH=/tmp/quirk91.db python -W error::DeprecationWarning -m pytest tests/test_dashboard_scan_history.py -x -q 2>&1 | tail -3; grep -c 'utcnow' tests/test_dashboard_scan_history.py | grep -qx 0 && grep -v '^#' quirk/assessment/operator_context.py | grep -c 'v3\.5\.1' | grep -qx 0 && echo PASS</automated>
  </verify>
  <done>The deprecation-as-error gate on tests/test_dashboard_scan_history.py passes; 0 `utcnow` occurrences remain in that file; no `v3.5.1` appears in a non-comment line of operator_context.py; the v3.x/v4.x era comments in models.py/db.py/tls_scanner.py/fingerprint.py/findings_evaluator.py are removed or de-versioned.</done>
</task>

<task type="auto">
  <name>Task 3: Bookkeeping — VALIDATION.md currency, stale CONCERNS.md removal, JWT advisory docs (CLEAN-03b, CLEAN-04)</name>
  <files>.planning/phases/87-dependency-hygiene/87-VALIDATION.md, .planning/phases/88-scoring-residuals/88-VALIDATION.md, .planning/phases/89-chaos-lab-profiles/89-VALIDATION.md, .planning/phases/90-oqs-nginx-pqc-hybrid/90-VALIDATION.md, .planning/codebase/CONCERNS.md, quirk/scanner/jwt_scanner.py, docs/operators-guide.md, docs/configuration.md</files>
  <read_first>91-RESEARCH.md "CLEAN-03 Carry-In Details" (VALIDATION.md table + CONCERNS stale-section table) and "Security Domain / BACK-58 Threat Model", existing 87/88/89-VALIDATION.md frontmatter, .planning/codebase/CONCERNS.md sections §1.1-§1.8/§4.1-§4.4/§6.1-§6.2</read_first>
  <action>CLEAN-03b VALIDATION.md (BACK-62): In 87/88/89-VALIDATION.md frontmatter set `nyquist_compliant: true`, `wave_0_complete: true`, `status: complete`. Create .planning/phases/90-oqs-nginx-pqc-hybrid/90-VALIDATION.md (none exists — phase 90 has VERIFICATION.md only) following the 91-VALIDATION.md frontmatter+section shape, sourced from the phase 90 VERIFICATION.md success signals, with `nyquist_compliant: true`, `wave_0_complete: true`, `status: complete`. Scope is v5.0 phases 87-90 ONLY — do NOT touch the 13 v4.x milestone files (Open Question 1 recommendation).
  CLEAN-03b CONCERNS.md: Remove or annotate `**Resolved:**` the stale sections enumerated in 91-RESEARCH (describe-deleted-files §1.1, §1.2, §1.3, §1.8, §6.1, §6.2; mostly-deleted §1.4 leaving only _extract_cert_key_type which Plan 91-02 removes; deleted intelligence modules §1.6, §1.7; and the dual-engine sections §4.1/§4.2/§4.3/§4.4 — now a single engine since readiness_score.py is deleted and writer.py imports compute_readiness_score from quirk.intelligence.scoring). Use planner discretion (delete outright vs. **Resolved:** annotation) but the dual-scoring-engine claim MUST no longer assert two live engines.
  CLEAN-04 (BACK-58): Add a `# WHY:` inline comment block above BOTH httpx.get call sites in jwt_scanner.py (~lines 73 and 88) documenting the intentional inspection-mode `verify=verify_tls` and the accepted-risk threat model — passive inventory scanner, allow_insecure_jwks defaults false, a HIGH ADVISORY_JWKS_VERIFY_DISABLED finding is emitted when enabled, validate_external_url still runs. Follow the Phase 77 D-01/IN-01 tls_capabilities.py comment pattern. Add a security note to docs/operators-guide.md (near the JWT scanner entry) explaining what `allow_insecure_jwks: true` disables, when an operator should use it, and why it is safe in the scanner threat model. Document the `allow_insecure_jwks` config key in docs/configuration.md (it exists at quirk/config.py:310, accepted by run_scan.py:67, currently undocumented).</action>
  <verify>
    <automated>grep -l 'nyquist_compliant: true' .planning/phases/87-dependency-hygiene/87-VALIDATION.md .planning/phases/88-scoring-residuals/88-VALIDATION.md .planning/phases/89-chaos-lab-profiles/89-VALIDATION.md .planning/phases/90-oqs-nginx-pqc-hybrid/90-VALIDATION.md | wc -l | grep -qx 4 && grep -q 'WHY:' quirk/scanner/jwt_scanner.py && grep -q 'allow_insecure_jwks' docs/operators-guide.md && echo PASS</automated>
  </verify>
  <done>All four of 87/88/89/90-VALIDATION.md exist with `nyquist_compliant: true`; the dual-scoring-engine and deleted-file CONCERNS.md entries are removed/resolved; jwt_scanner.py has a `# WHY:` advisory comment at the httpx.get sites; docs/operators-guide.md and docs/configuration.md document `allow_insecure_jwks`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanner → JWKS endpoint (network) | JWKS URI fetched over TLS; verify can be disabled via allow_insecure_jwks |
| test process → ambient DB files | Local working tree may contain stray scan DBs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-01 | Information Disclosure / Tampering | quirk/scanner/jwt_scanner.py `verify=verify_tls` (allow_insecure_jwks) | accept | Documented (CLEAN-04). MITM on JWKS URI could inject attacker key material, but: passive inventory scanner not a relying party; allow_insecure_jwks defaults false (explicit opt-in); a HIGH ADVISORY_JWKS_VERIFY_DISABLED finding fires when enabled; validate_external_url() still runs on the URI. No behavioral change in this plan — documentation only. |
| T-91-02 | Tampering | tests/conftest.py autouse fixture | mitigate | Fixture only sets QUIRK_DB_PATH to an isolated tmp_path; does not weaken the production resolver. Existing dashboard_client() override path unaffected. |
| T-91-SC | Tampering | pip installs (vulture) | mitigate | No package install in this plan (Tier-A is source/docs only). vulture install + legitimacy is handled in Plan 91-02. |
</threat_model>

<verification>
- CLEAN-01: `QUIRK_DB_PATH=/tmp/quirk91.db python -W error::DeprecationWarning -m pytest tests/test_dashboard_scan_history.py -x -q` PASS; `grep -c utcnow tests/test_dashboard_scan_history.py` == 0; `grep -v '^#' quirk/assessment/operator_context.py | grep -c v3.5.1` == 0.
- CLEAN-03: `python -m pytest tests/ --collect-only -q` (no QUIRK_DB_PATH) → 0 "Multiple QU.I.R.K. DBs found" errors; 4 VALIDATION.md files (87/88/89/90) carry `nyquist_compliant: true`; `tests/test_infra03_nyquist_coverage.py` still passes; dual-engine CONCERNS entry gone.
- CLEAN-04: `grep -n 'WHY:' quirk/scanner/jwt_scanner.py` non-empty; `grep -n allow_insecure_jwks docs/operators-guide.md` non-empty.
- Regression gate: `QUIRK_DB_PATH=/tmp/quirk91.db python -m pytest tests/ -q` shows no NEW failures vs the pre-phase baseline (~39 known DB-independent failures unchanged); `python -m compileall quirk tests` clean.
</verification>

<success_criteria>
- Deprecation-as-error gate green on tests/test_dashboard_scan_history.py.
- 0 collection errors without QUIRK_DB_PATH.
- 87/88/89-VALIDATION.md compliant; 90-VALIDATION.md created.
- Stale dual-scoring-engine + deleted-file CONCERNS.md entries removed.
- JWT verify=False inspection-mode documented in code and operator docs.
- No new test failures vs baseline; compileall clean.
</success_criteria>

<output>
Create `.planning/phases/91-code-cleanup-bookkeeping/91-01-SUMMARY.md` when done.
</output>
