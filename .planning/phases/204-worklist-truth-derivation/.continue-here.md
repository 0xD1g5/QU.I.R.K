---
context: phase
phase: 204-worklist-truth-derivation
task: 0
total_tasks: 5
status: in_progress
last_updated: 2026-09-13T13:40:54.866Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure **in this
> session**. Acknowledge each one explicitly before proceeding.

- [ ] **CONSTRAINT: The case-ID regex trap.** This session's scouting parser matched UAT case IDs
      with `^### UAT-(\d+)-(\d+)\b` and **silently dropped 37 of the 878 cases**. It did not crash,
      did not warn, and returned a confident wrong count (841 headings, 69 GAPs) which was then used
      to accuse a *correct* `ROADMAP.md` of having drifted. **ROADMAP.md was right on every count.**
      — **Structural mitigation:** any parser written in this phase must be validated against
      `grep -c '^### UAT-' docs/UAT-SERIES.md` (**878**) before its output is trusted for anything,
      and COV-02's gate must be demonstrated RED against a case in a **non-numeric ID shape**.
      A gate that shares the generator's blind spot passes while both under-count.

- [ ] **CONSTRAINT: Never transcribe a count; recompute it.** Every count in `204-CONTEXT.md`,
      `ROADMAP.md`, and this file is a measurement with a timestamp, not a fact. The corpus grows.
      — **Structural mitigation:** plans 204-01 and 204-02 carry explicit
      `<critical_measurement_rule>` blocks. A task that transcribes a number instead of recomputing
      it is a defect, even if the number happens to be right today.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Truncating ID regex | `UAT-<int>-<int>` silently drops named-prefix (`UAT-COMPLY-52-01`), decimal (`UAT-56.1-01`, `UAT-186.1-03`), backlog (`UAT-999.83-01`) and three-segment (`UAT-89-01-01`) IDs. One dropped case (`UAT-186.1-03`) **is GAP-annotated**, so the blind spot under-reported the actual drain target | **blocking** | Cross-check any parser against `grep -c '^### UAT-'` = 878; prove COV-02's gate RED against a non-numeric-shape case |
| Accusing a correct document of drift | The wrong count led to a confident, wrong "ROADMAP has drifted" claim. The disagreement was real; the error was in the *new* measurement | **blocking** | When two counts disagree, instrument both before concluding either is stale. ROADMAP's own instruction already said this |
| HTTP 200 ≠ content (Phase 203) | Cisco served "No Data Found" with 200; Intel served a product selector with 200; Fortinet silently 301'd to "Getting started" with 200 | advisory | Compare against the *claim*, never the status code. Applies to any future link checking |
| Bumping a date to clear a gate | The staleness gate is red and will stay red | **blocking** | Never bump `last_verified` without re-reading the source; never set `QUIRK_CI_STALENESS_OVERRIDE_DATE` |

<current_state>
**Phase 203 is CLOSED.** STALE-02 complete; STALE-01 **partial** (1 of 8 vendors verified, 7 source
documents gone or access-gated). The staleness gate is **deliberately RED** under a dated deferral
in `STATE.md`.

**Phase 204 is PLANNED but NOT STARTED.** `204-CONTEXT.md` (D-01..D-12, auto-selected in autonomous
mode) and five PLAN.md files exist. Zero execution has happened. Working tree is clean; the plan
list is committed as `17824493`.

Paused mid-`/gsd-autonomous --from 204 --to 206`, immediately before executing plan 204-01.
</current_state>

<completed_work>

- Phase 203 — all 7 plans, 12 commits (`9e2e6bd9`..`b7cd2817`)
  - 203-01 D-02 invariant tests + live red-proof + SC#4 enumeration
  - 203-02 `hw_cve` re-verified against live NVD (6/6 rows, bounded delta clean)
  - 203-03/04 Chrome pass — 1 of 8 vendors verified (Fortinet, **corrected on two counts**)
  - 203-05/06/07 close-out, dated deferral, D-07 trip-date table, CLAUDE.md SC#4 fix
- Phase 204 discuss → `204-CONTEXT.md` (corrected after the ID-regex failure)
- Phase 204 plan → `204-01`..`204-05-PLAN.md`, roadmap plan list committed `17824493`
</completed_work>

<remaining_work>

- **204-01** COV-03 parser + reconciliation verdict — **START HERE**
- **204-02** per-case verdicts (7 DEFERRED-vs-GAP conflicts) + OBSOLETE grammar + COV-09 retirements
- **204-03** COV-01 generator + byte-reproducibility drift gate
- **204-04** COV-02 standing gate + RED proof (two inductions)
- **204-05** docs/vault/planning closure — `autonomous: no`, operator review checkpoint
- Phase 205 Guard Integrity
- Phase 206 Dashboard UI Coverage Drain
- **HALT.** Phase 207 is manual and operator-led — do not autonomously enter it
</remaining_work>

<decisions_made>

- **v5.24 autonomy is segmented**: 203 in-session (Chrome) → `--from 204 --to 206` → **manual 207**
  → `--from 208`. Authoritative copy in `ROADMAP.md` §"Autonomy Plan".
- **D-01 (203):** `HARDWARE_MATRIX` top-level `last_verified` = `min()` of entry dates, enforced by
  a test. This is *why* the gate is honestly red rather than falsely green at 1-of-8 verified.
- **STALE-01 was left partial and its requirement NOT flipped** — flipping it would be the same
  falsehood as bumping the date.
- **Source-rot split (operator, 2026-09-13):** operator owns re-sourcing the 7 URLs; QUIRK owns the
  `doc_id`-decoupled-from-`source_url` structural fix. Both deferred until after 204–208.
- **COV-03 verdict (204 D-02):** collapse to `docs/UAT-SERIES.md` as the single authoritative
  source and resolve the 12 discrepancies *into* it. The ledger becomes historical evidence, not a
  live generator input — it structurally cannot cover series 164+.
</decisions_made>

<blockers>
- **Staleness gate RED** — deliberate, dated deferral in `STATE.md`. Will remain red through
  204–208. Not a blocker to progress; a blocker to *claiming* a green baseline.
- **`test_non_vacuity_skipped_substitute_is_flagged` fails under pytest 9.0.2** — PRE-EXISTING,
  proven at commit `4c743f74` in a detached worktree. Do not misattribute it to 203 or 204.
</blockers>

## Required Reading (in order)

1. `.planning/phases/204-worklist-truth-derivation/204-CONTEXT.md` — **especially the corrected
   measurement baseline and the ID-SHAPES table.** The correction notice explains the failure that
   produced the blocking constraints above.
2. `.planning/phases/204-worklist-truth-derivation/204-01-PLAN.md` — the next unit of work.
3. `CLAUDE.md` §"UAT Corpus Integrity Gate (UATREC-04)" — the standing gate parses
   `docs/UAT-SERIES.md` **directly and deliberately does not consult the ledger**; `GAP` is a valid
   *passing* disposition.
4. `.planning/STATE.md` §"Deferred Items" — Phase 203's deferral and the D-07 trip-date table.
5. `.planning/phases/203-catalog-freshness-drain/203-01-SUMMARY.md` — the red-proof protocol that
   204-04's COV-02 gate must copy.

## Infrastructure State

- **Chrome extension: CONNECTED** (one local macOS browser, `Browser 1`). Needed only by Phase 203
  (done) and possibly 206/207. Not needed for 204.
- **pytest 9.0.2** — causes the one pre-existing UAT failure noted above.
- Node v26.8.2, `src/dashboard/node_modules` present, vitest available (needed from Phase 206).
- `gh` authenticated as `0xD1g5`.
- Working tree clean; branch `main`; unpushed commits present.
- **`git add` on `.planning/` paths prints an "ignored" hint.** For tracked files
  (`STATE.md`/`ROADMAP.md`/`REQUIREMENTS.md`) the add usually succeeds anyway, but the planner
  observed it **exit 1** at least once — so `git add -f` may be required, and never chain
  `git add … && git commit` (the `&&` will swallow the commit). `gsd-sdk query commit` falsely
  reports failure here; use plain git.

<context>
The through-line of this session: **every mechanism that regenerates its answer from source beat
every recorded number.** D-01's `min()` invariant caught a partial verification that prose would
have papered over. The claim-match bar caught three vendors serving HTTP 200 with the wrong content.
And the ID-regex failure caught *me* — a hand-derived pattern assumption that lost 37 entries
silently, which is the precise thing Phase 204's COV-01/COV-02 exist to eliminate.

Phase 204 should be executed in that spirit: the generator and the gate must not share a parser
(plan 204-04 carries an `<independence_rule>` forbidding the gate from importing
`scripts/uat_corpus.py`, so a parser bug cannot make both green at once), and every count must be
derived at run time.
</context>

<next_action>
Start with: read `204-CONTEXT.md` (corrected baseline + ID-shapes table), then execute
`.planning/phases/204-worklist-truth-derivation/204-01-PLAN.md` — COV-03's parser and
reconciliation verdict. Validate any parser against `grep -c '^### UAT-' docs/UAT-SERIES.md` = 878
before trusting its output.
</next_action>
