---
phase: 84-release-engineering
plan: 03
type: execute
wave: 2
depends_on: [84-01]
files_modified:
  - .github/workflows/release.yml
autonomous: true
requirements: [RELENG-02, RELENG-03]
user_setup:
  - service: pypi
    why: "Trusted Publishers + Sigstore attestations on tag-triggered release publish"
    env_vars: []
    dashboard_config:
      - task: "After this workflow file lands on main and the first v*.*.* tag is pushed, configure a Trusted Publisher at https://pypi.org/manage/account/publishing/ with: project name (`quirk` or `qu-i-r-k` per Plan 84-01), repo owner/name (digirolamo-rob or current GitHub org), workflow filename `release.yml`, environment name `release`"
        location: "PyPI -> Account -> Publishing"
      - task: "Create a `release` GitHub environment in repo Settings (no protection rules required initially; can add tag-pattern restriction `v*` later)"
        location: "GitHub repo -> Settings -> Environments"
must_haves:
  truths:
    - "Pushing a `v*.*.*` tag to GitHub triggers `.github/workflows/release.yml`"
    - "The workflow builds wheel + sdist via `python -m build` and uploads them as artifacts"
    - "The workflow publishes to PyPI via `pypa/gh-action-pypi-publish@release/v1` using Trusted Publishers (OIDC) with NO stored API tokens"
    - "The publish job declares `attestations: true` so Sigstore attestations are generated automatically"
    - "The publish job runs in an environment named `release` per Trusted Publishers convention"
    - "The publish job declares the OIDC permission: `id-token: write`"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Tag-triggered PyPI release pipeline with Sigstore attestations"
      contains: "pypa/gh-action-pypi-publish"
      min_lines: 40
  key_links:
    - from: ".github/workflows/release.yml"
      to: "PyPI Trusted Publishers"
      via: "OIDC token issued by GHA to PyPI"
      pattern: "id-token: write"
    - from: ".github/workflows/release.yml"
      to: "Sigstore attestations"
      via: "attestations: true flag on publish action"
      pattern: "attestations: true"
---

<objective>
Create the tag-triggered release workflow that publishes wheel + sdist to PyPI via Trusted Publishers (GitHub OIDC, no stored API tokens) and emits Sigstore attestations automatically.

Purpose: Closes RELENG-02 (Trusted Publishers) and RELENG-03 (Sigstore attestations) — the secrets-free, auditable release pipeline that the v5.0 GA cut depends on.

Output: `.github/workflows/release.yml` is wired and ready; the PyPI-side Trusted Publishers config + GitHub `release` environment are documented as user-setup steps for the user to complete after this workflow file lands on main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-release-engineering/84-CONTEXT.md
@.planning/phases/84-release-engineering/84-01-SUMMARY.md

<interfaces>
Existing workflows in `.github/workflows/`:
- `dashboard-quality.yml` — dashboard CI (not relevant)
- `python-staleness.yml` — model/compliance staleness gate (not relevant)

This plan ADDS a third workflow. It does not touch the existing two.

Trusted Publishers + attestations reference (modern pypa/gh-action-pypi-publish v1.12+):
- Trigger: `on.push.tags: ['v*.*.*']`
- Permissions block on publish job: `id-token: write` (REQUIRED for OIDC) + `contents: read`
- Environment: `environment: { name: release, url: https://pypi.org/p/<package-name> }`
- Action: `uses: pypa/gh-action-pypi-publish@release/v1` with `with: { attestations: true }`

Package name comes from Plan 84-01 (either `quirk` or `qu-i-r-k`). The environment URL must match.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author .github/workflows/release.yml — build + publish + attest</name>
  <files>.github/workflows/release.yml</files>
  <action>
    Create `.github/workflows/release.yml` with the following structure:

    Top-level:
    - `name: Release`
    - `on.push.tags: ['v*.*.*']` (tag pattern matches semver tags only)

    Job 1 — `build` (runs-on: ubuntu-latest):
      - Checkout (`actions/checkout@v4`)
      - Set up Python 3.11 (`actions/setup-python@v5` with `python-version: '3.11'`)
      - Install build tooling: `python -m pip install --upgrade pip build`
      - Build: `python -m build` (produces both wheel and sdist in `dist/`)
      - Upload artifacts: `actions/upload-artifact@v4` with `name: dist`, `path: dist/`

    Job 2 — `publish` (needs: build, runs-on: ubuntu-latest):
      - `environment:` block with `name: release` and `url: https://pypi.org/p/<package-name>` (substitute the verified name from Plan 84-01 — if the name is `qu-i-r-k`, the URL uses that)
      - `permissions:` block declaring `id-token: write` and `contents: read`
      - Download artifacts step: `actions/download-artifact@v4` with `name: dist`, `path: dist/`
      - Publish step: `uses: pypa/gh-action-pypi-publish@release/v1` with `with: { attestations: true }`. Do NOT pass `password` or `username` — Trusted Publishers + OIDC supplies the credential automatically.

    Header comment block at top of file documenting:
    - Closes RELENG-02 (Trusted Publishers) and RELENG-03 (Sigstore attestations)
    - Prerequisite: configure Trusted Publisher at PyPI side + create `release` environment (see `docs/release-process.md` for the manual setup runbook)
    - Sigstore attestations are verified downstream via `gh attestation verify <wheel> --repo <owner>/<repo>` or `cosign verify-blob`

    Do NOT create the optional `.github/workflows/changelog-fragment-check.yml` (deferred per CONTEXT.md — scope discipline).
  </action>
  <verify>
    <automated>test -f .github/workflows/release.yml &amp;&amp; grep -q 'pypa/gh-action-pypi-publish' .github/workflows/release.yml &amp;&amp; grep -q 'attestations: true' .github/workflows/release.yml &amp;&amp; grep -q 'id-token: write' .github/workflows/release.yml &amp;&amp; grep -qE "tags:.*v\*" .github/workflows/release.yml</automated>
  </verify>
  <done>release.yml exists with build + publish jobs, OIDC permissions, `release` environment, and `attestations: true`; no API token or password is referenced anywhere in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Lint workflow YAML and confirm no token leakage; atomic commit</name>
  <files>(commit only — no new edits)</files>
  <action>
    1. Validate YAML syntax: `python -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"` (must not raise).
    2. Grep guard against accidental token references: `grep -nE 'password:|TWINE_|PYPI_API_TOKEN|secrets\.PYPI' .github/workflows/release.yml` MUST return zero matches. If any match is found, remove it — Trusted Publishers is OIDC-only.
    3. Stage with explicit path: `git add .github/workflows/release.yml`
    4. Commit: `feat(84-03): trusted publishers release workflow + sigstore attestations (RELENG-02, RELENG-03)` with a body noting the manual PyPI-side Trusted Publishers configuration step required before the first tag push.
  </action>
  <verify>
    <automated>python -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))" &amp;&amp; ! grep -qE 'password:|TWINE_|PYPI_API_TOKEN|secrets\.PYPI' .github/workflows/release.yml</automated>
  </verify>
  <done>YAML parses cleanly; no token references exist; one atomic commit lands the workflow; commit message references RELENG-02 and RELENG-03.</done>
</task>

</tasks>

<verification>
- `.github/workflows/release.yml` exists and is valid YAML
- `grep -E 'attestations: true|id-token: write|pypa/gh-action-pypi-publish'` finds all three patterns in the workflow
- No token/password references appear in the file
- `git log -1 --format='%s'` matches `feat(84-03):`
</verification>

<success_criteria>
- RELENG-02 closed: tag-triggered release workflow uses Trusted Publishers (OIDC, no stored tokens)
- RELENG-03 closed: Sigstore attestations are auto-generated via `attestations: true`
- Manual PyPI-side Trusted Publisher configuration is documented in user_setup frontmatter and will appear again in Plan 84-04's `docs/release-process.md` runbook
</success_criteria>

<output>
After completion, create `.planning/phases/84-release-engineering/84-03-SUMMARY.md`
</output>
