---
phase: 84-release-engineering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - quirk/__init__.py
  - tests/test_version.py
autonomous: true
requirements: [RELENG-01, RELENG-08]
must_haves:
  truths:
    - "PyPI distribution name is verified available (or fallback `qu-i-r-k` selected and v4.10 D-NN logged in PROJECT.md)"
    - "`pyproject.toml [project.version]` is the canonical version source — single literal"
    - "`quirk/__init__.py::__version__` resolves dynamically via `importlib.metadata.version(\"quirk\")` with a graceful fallback for unpackaged dev runs"
    - "`tests/test_version.py` enforces parity in the new direction: pyproject.toml value == all six derived surfaces"
    - "`pytest tests/test_version.py -v` is green"
  artifacts:
    - path: "pyproject.toml"
      provides: "Canonical version literal at [project.version]"
      contains: "version = \"4.10.0\""
    - path: "quirk/__init__.py"
      provides: "Dynamic __version__ derived from installed package metadata"
      contains: "importlib.metadata"
    - path: "tests/test_version.py"
      provides: "Parity test asserting pyproject.toml is the SoT"
      contains: "pyproject"
  key_links:
    - from: "quirk/__init__.py"
      to: "pyproject.toml [project.version]"
      via: "importlib.metadata.version(\"quirk\")"
      pattern: "importlib\\.metadata\\.version"
    - from: "tests/test_version.py"
      to: "pyproject.toml"
      via: "tomllib parse of [project.version]"
      pattern: "tomllib|tomli"
---

<objective>
Verify PyPI distribution name and consolidate the version source of truth to `pyproject.toml [project.version]`, with all other surfaces deriving from it.

Purpose: Modern Python packaging best practice (PEP 621 + importlib.metadata) per D-84-R1 — REVERSES the legacy RELENG-08 wording that named `__init__.py` as canonical. Eliminates the multi-edit drift risk that the Phase 37 parity test was created to catch.

Output: Verified PyPI name, single canonical version literal at `pyproject.toml [project.version]`, dynamic `__version__` in `quirk/__init__.py`, and a parity test that asserts the new direction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-release-engineering/84-CONTEXT.md
@pyproject.toml
@quirk/__init__.py
@tests/test_version.py

<interfaces>
Current state (4.4.0 version literal pinned in three places):

From quirk/__init__.py:
```python
"""QU.I.R.K. -- Quantum Infrastructure Readiness Kit"""
__version__ = "4.4.0"
```

From pyproject.toml [project]:
```toml
name = "quirk"
version = "4.4.0"
```

Other surfaces that import __version__ (already correctly dynamic — no edits needed in this plan):
- quirk/cbom/builder.py:36 — `from quirk import __version__ as PLATFORM_VERSION`
- quirk/reports/writer.py:15 — `from quirk import __version__ as PLATFORM_VERSION`

Additional version-bearing surface that DOES carry its own literal (must be flipped to derive):
- quirk/config.py:279 — `intelligence_version: str = "4.4.0"` (IntelligenceCfg default)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify PyPI distribution name `quirk` availability</name>
  <files>(no file edits in this task — verification only; may produce a CONTEXT update if fallback triggered)</files>
  <action>
    Run `pip index versions quirk` and capture stdout/stderr. Three outcomes:
    (a) Command reports "No matching distribution found" or empty results → name `quirk` is available. Proceed to Task 2 with `name = "quirk"`.
    (b) Command lists existing versions → name is TAKEN. Switch to fallback `qu-i-r-k` per D-84-R1. Append a v4.10 D-NN decision entry to `.planning/PROJECT.md` Key Decisions section documenting the name switch and rationale. Stop execution and surface to user before continuing (this is a directional change requiring confirmation).
    (c) Command errors (network / pip not present) → retry once; if still failing, surface the error to user and stop. Do NOT proceed on unverified assumption.
    Record the verification output verbatim in the plan SUMMARY for audit traceability.
  </action>
  <verify>
    <automated>pip index versions quirk 2>&amp;1 | tee /tmp/pypi-quirk-check.txt; test -s /tmp/pypi-quirk-check.txt</automated>
  </verify>
  <done>PyPI name decision is recorded (either confirmed `quirk` or fallback `qu-i-r-k` with v4.10 D-NN logged in PROJECT.md). Verification output captured.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bump pyproject.toml [project.version] to 4.10.0 as the canonical literal</name>
  <files>pyproject.toml</files>
  <behavior>
    - After edit: `grep -E '^version = ' pyproject.toml` returns exactly one line: `version = "4.10.0"`
    - `[project] name` matches the verified PyPI name from Task 1 (`quirk` or `qu-i-r-k`)
    - No other version literals are added; this is the single canonical declaration
  </behavior>
  <action>
    Edit `pyproject.toml`:
    - Change `version = "4.4.0"` → `version = "4.10.0"` on line 7.
    - If Task 1 selected the fallback, also change `name = "quirk"` → `name = "qu-i-r-k"` on line 6.
    - Do not edit any other fields. The `[project.version]` literal is now the canonical SoT per D-84-R1.
  </action>
  <verify>
    <automated>grep -E '^(name|version) = ' pyproject.toml | grep -v '^#'</automated>
  </verify>
  <done>pyproject.toml carries `version = "4.10.0"` and the verified package name. No other version literals introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make `quirk/__init__.py::__version__` derive from installed package metadata</name>
  <files>quirk/__init__.py</files>
  <behavior>
    - `import quirk; quirk.__version__` returns "4.10.0" when installed via `pip install -e .`
    - For unpackaged dev runs where `importlib.metadata.version("quirk")` raises `PackageNotFoundError`, the module falls back to reading `pyproject.toml [project.version]` via `tomllib` rather than crashing import (importing the package must never fail just because someone is running from a fresh checkout pre-install)
    - No hardcoded version literal remains in `quirk/__init__.py`
  </behavior>
  <action>
    Rewrite `quirk/__init__.py` to use `importlib.metadata.version("quirk")` (or `"qu-i-r-k"` if Task 1 selected fallback) with a `PackageNotFoundError` fallback that parses `pyproject.toml [project.version]` via `tomllib` from the repo root. The module must export `__version__` as before — downstream `from quirk import __version__ as PLATFORM_VERSION` callers in `quirk/cbom/builder.py` and `quirk/reports/writer.py` must continue to work unchanged. Use `pathlib` to locate `pyproject.toml` relative to the package file for the fallback path. Keep the module docstring intact.
  </action>
  <verify>
    <automated>python -c "import quirk; assert quirk.__version__ == '4.10.0', quirk.__version__; print('OK:', quirk.__version__)"</automated>
  </verify>
  <done>`quirk.__version__` resolves to 4.10.0 dynamically; no literal version string remains in `quirk/__init__.py`; downstream PLATFORM_VERSION imports still work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Flip tests/test_version.py to assert pyproject.toml is the SoT across six surfaces</name>
  <files>tests/test_version.py, quirk/config.py</files>
  <behavior>
    - Test reads `pyproject.toml [project.version]` via `tomllib` as the truth value
    - Test asserts truth value == `quirk.__version__`
    - Test asserts truth value == `quirk.cbom.builder.PLATFORM_VERSION`
    - Test asserts truth value == `quirk.reports.writer.PLATFORM_VERSION`
    - Test asserts truth value == `quirk.config.IntelligenceCfg().intelligence_version`
    - Test asserts truth value appears in `quirk --version` CLI output (slow-marked subprocess test, unchanged structure but reads truth from pyproject)
    - All assertions read truth dynamically — NO hardcoded "4.10.0" string in the test
    - `quirk/config.py` IntelligenceCfg default updates from the hardcoded `"4.4.0"` to derive from `quirk.__version__`
  </behavior>
  <action>
    Rewrite `tests/test_version.py` so that the first lines load truth from `pyproject.toml [project.version]` via `tomllib`. Replace every `assert ... == "4.4.0"` with `assert ... == TRUTH` where TRUTH is the parsed pyproject value. The CLI subprocess test asserts `TRUTH in output`. Update the module docstring to reflect the new direction ("pyproject.toml is the canonical source; every other surface derives from it"). Also edit `quirk/config.py:279` so that `IntelligenceCfg.intelligence_version` defaults to `quirk.__version__` rather than a hardcoded literal — import `quirk` at module top or use a `default_factory` lambda; keep the field type as `str`.
  </action>
  <verify>
    <automated>pytest tests/test_version.py -v -m 'not slow'</automated>
  </verify>
  <done>All non-slow assertions in `tests/test_version.py` pass; no hardcoded version literal remains in either `tests/test_version.py` or `quirk/config.py`; the parity test now enforces the new SoT direction.</done>
</task>

<task type="auto">
  <name>Task 5: Atomic commit — version SoT consolidation + PyPI name verification</name>
  <files>(commit only — no new edits)</files>
  <action>
    Stage ONLY the files explicitly modified by this plan using explicit paths (NEVER `git add -A`):
    `git add pyproject.toml quirk/__init__.py quirk/config.py tests/test_version.py`
    If Task 1 produced a PROJECT.md decision entry, also: `git add .planning/PROJECT.md`
    Commit with message: `feat(84-01): pypi name verify + version single-source-of-truth (RELENG-01, RELENG-08)` and include a short body noting D-84-R1 reverses RELENG-08 wording (pyproject.toml is canonical, __init__.py derives via importlib.metadata).
  </action>
  <verify>
    <automated>git log -1 --format='%s' | grep -q '^feat(84-01)'</automated>
  </verify>
  <done>One atomic commit lands all version-SoT changes; commit message references RELENG-01 and RELENG-08.</done>
</task>

</tasks>

<verification>
After all tasks:
- `pytest tests/test_version.py -v -m 'not slow'` passes
- `python -c "import quirk; print(quirk.__version__)"` prints `4.10.0`
- `grep -rn '"4.4.0"' quirk/ tests/test_version.py` returns no matches (regression detection)
- `git log -1 --format='%H %s'` shows the 84-01 atomic commit
</verification>

<success_criteria>
- RELENG-01 closed: PyPI name verified (or fallback decision logged as v4.10 D-NN)
- RELENG-08 closed in the D-84-R1 direction: `pyproject.toml [project.version]` is canonical; `quirk/__init__.py::__version__` derives via `importlib.metadata`; `tests/test_version.py` enforces parity in this direction
- No hardcoded version literal remains in `quirk/__init__.py`, `quirk/config.py`, or `tests/test_version.py`
</success_criteria>

<output>
After completion, create `.planning/phases/84-release-engineering/84-01-SUMMARY.md`
</output>
