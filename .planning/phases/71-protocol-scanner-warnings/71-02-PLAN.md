---
phase: 71-protocol-scanner-warnings
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/scanner/fingerprint.py
  - tests/test_fingerprint_subprocess_logging.py
  - .planning/audit-2026-05-08/AUDIT-TASKS.md
autonomous: true
requirements: [PROTO-02]
must_haves:
  truths:
    - "The single bare-except subprocess swallow site flagged by WR-03 now logs the error and continues with a partial result"
    - "The narrowed handler catches specific subprocess/OS exceptions, not blanket Exception"
    - "Audit row scanners-protocol/WR-03 is flipped to [x] closed under Phase 71"
  artifacts:
    - path: "quirk/scanner/fingerprint.py"
      provides: "Narrowed except clause with logger.warning"
      contains: "logger ="
    - path: "tests/test_fingerprint_subprocess_logging.py"
      provides: "Test that subprocess failure emits a log record and returns partial result"
      contains: "caplog"
  key_links:
    - from: "quirk/scanner/fingerprint.py"
      to: "module logger"
      via: "logging.getLogger(__name__)"
      pattern: "logging\\.getLogger\\(__name__\\)"
    - from: "quirk/scanner/fingerprint.py subprocess call site"
      to: "logger.warning"
      via: "narrowed except clause"
      pattern: "logger\\.warning"
---

<objective>
Close PROTO-02 / WR-03 by replacing the bare `except Exception` that swallows subprocess errors in the protocol-scanner subsystem with a narrowed exception handler that logs the failure via `logger.warning` and allows the scan to continue with a partial result. Per D-08, narrow ONLY the WR-03 site — not other broad excepts in the same file.

Purpose: Silent subprocess failures hide nmap/dig/openssl invocation errors from operators and from the audit trail. Logging them is the minimum bar.
Output: Patched scanner module, new test verifying log emission, audit row WR-03 flipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/71-protocol-scanner-warnings/71-CONTEXT.md
@.planning/audit-2026-05-08/AUDIT-TASKS.md

<interfaces>
Logger idiom (project-wide convention, established post-Phase 70):
```python
import logging
logger = logging.getLogger(__name__)
# ...
logger.warning("X failed for %r: %s", target, e)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Locate WR-03 site, narrow except, add module logger</name>
  <files>quirk/scanner/fingerprint.py</files>
  <read_first>
    - .planning/audit-2026-05-08/AUDIT-TASKS.md (WR-03 row — note the specific symptom: bare except Exception around subprocess invocation)
    - quirk/scanner/fingerprint.py (full file; identify the subprocess.run / subprocess.Popen / subprocess.check_output site whose handler is `except Exception:`)
    - quirk/dashboard/api/routes/scan.py (reference logger.warning idiom established in Phase 70)
  </read_first>
  <behavior>
    - When the subprocess call raises subprocess.CalledProcessError, FileNotFoundError, OSError, or subprocess.TimeoutExpired, the scanner logs a WARNING with the failing command/target context and continues with a partial result (returns whatever empty/sentinel value the current code returns).
    - Other narrow excepts already present in fingerprint.py (e.g. except socket.timeout, except ssl.SSLError, except BaseException at line ~165) are NOT touched — only the WR-03 site.
    - The module gains exactly one `logger = logging.getLogger(__name__)` at module scope if not already present.
  </behavior>
  <action>
    1. Search fingerprint.py for `except Exception:` that wraps a `subprocess.*` call. Audit row WR-03 wording: "Bare except Exception swallowing subprocess errors silently." There is exactly one such site per D-08; if you find ambiguity (two candidates), prefer the one nearest a `subprocess.run`/`subprocess.check_output`/`subprocess.Popen` invocation and surface the question rather than guessing.

    2. If `logger = logging.getLogger(__name__)` is not already at module scope, add it (with `import logging` if needed).

    3. Replace the bare `except Exception:` with:
       ```python
       except (subprocess.CalledProcessError, subprocess.TimeoutExpired, FileNotFoundError, OSError) as e:
           logger.warning("subprocess failed for %r: %s", <target_or_cmd_var>, e)
           # fall through to existing partial-result return
       ```
       Use the existing variable in scope (target, host, cmd) for the `%r` formatter.

    4. Do NOT touch any other `except` in this file. Do NOT touch other files. Per D-08 / D-15.

    PEP 8, minimal diff.
  </action>
  <verify>
    <automated>python -m compileall quirk/scanner/fingerprint.py &amp;&amp; pytest tests/test_fingerprint_subprocess_logging.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^except Exception:" quirk/scanner/fingerprint.py` is one less than before the change (verify with git diff; the WR-03 site is gone)
    - `grep -n "logging.getLogger(__name__)" quirk/scanner/fingerprint.py` returns exactly one hit
    - `grep -n "logger.warning" quirk/scanner/fingerprint.py` returns at least one hit
    - `grep -nE "except \\(subprocess\\." quirk/scanner/fingerprint.py` returns at least one hit
    - `python -m compileall quirk/scanner/fingerprint.py` exits 0
  </acceptance_criteria>
  <done>
    The single WR-03 swallow site is narrowed and logs; partial-result return path preserved; no other excepts touched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test logging + flip WR-03 audit row</name>
  <files>tests/test_fingerprint_subprocess_logging.py, .planning/audit-2026-05-08/AUDIT-TASKS.md</files>
  <read_first>
    - quirk/scanner/fingerprint.py (post-Task 1)
    - tests/ for a neighbor test that uses `caplog` (pytest standard fixture for log records)
  </read_first>
  <action>
    Create `tests/test_fingerprint_subprocess_logging.py` with:
    - `test_subprocess_failure_logs_warning_and_returns_partial(monkeypatch, caplog)` — monkeypatch the subprocess function the WR-03 site invokes to raise `subprocess.CalledProcessError(1, "nmap")` (or whichever exception the site now catches); call the public function that contains that site; assert (a) no exception propagates, (b) `caplog.records` contains at least one WARNING-level record from `quirk.scanner.fingerprint`, (c) the return value matches the documented partial-result shape (empty dict/list/None per existing code).
    - `test_subprocess_filenotfound_logs(monkeypatch, caplog)` — same but with `FileNotFoundError("nmap not installed")`.

    Then flip audit ledger row WR-03 (line 58): change `| — | [ ] open |` to `| Phase 71 | [x] closed |`.
  </action>
  <verify>
    <automated>pytest tests/test_fingerprint_subprocess_logging.py -x &amp;&amp; grep -c "scanners-protocol/WR-03.*Phase 71.*\\[x\\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_fingerprint_subprocess_logging.py exists and passes with ≥ 2 tests
    - `grep -c "scanners-protocol/WR-03.*Phase 71.*\\[x\\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 1
    - No other audit rows touched
  </acceptance_criteria>
  <done>
    Logging behavior proven by tests; WR-03 closed in ledger.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| protocol scanner → subprocess (nmap/openssl/etc.) | External binary may be missing, time out, return non-zero, or be killed; silent swallow hides operational issues |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-03 | Repudiation | fingerprint.py subprocess wrapper | mitigate | Narrow except + logger.warning produces audit trail of subprocess failures so operators can diagnose missing-binary, timeout, or non-zero-exit conditions instead of mistaking them for empty scan results |
| T-71-04 | Denial of Service | fingerprint.py partial result | accept | Continuing with partial result on subprocess failure is intentional graceful degradation — the alternative (aborting the whole scan) is worse for consulting engagements |
</threat_model>

<verification>
- `python -m compileall quirk/scanner/fingerprint.py` clean
- `pytest tests/test_fingerprint_subprocess_logging.py -x` passes
- AUDIT-TASKS.md WR-03 row shows `Phase 71 | [x] closed`
- No collateral except-narrowing (verify via `git diff quirk/scanner/fingerprint.py` — at most one `except Exception:` removed)
</verification>

<success_criteria>
- WR-03 subprocess swallow narrowed and logged
- 2+ new tests pass
- Audit ledger row WR-03 closed
- Other broad excepts in fingerprint.py untouched (D-08, D-15)
</success_criteria>

<output>
Create `.planning/phases/71-protocol-scanner-warnings/71-02-SUMMARY.md` documenting the narrowed site (line number before/after), audit row closure, and confirmation that no other excepts were touched.
</output>
