---
phase: 114-automatic-merge-trigger
plan: 02
type: execute
wave: 2
depends_on: ["114-01"]
files_modified:
  - tests/test_auto_merge_trigger.py
autonomous: true
requirements: [AUTOMERGE-01, AUTOMERGE-02, AUTOMERGE-03]
must_haves:
  truths:
    - "All 6 CONTEXT <specifics> acceptance tests exist and pass: all-sensors-in trigger, disabled, failure-isolated, double-fire harmless, cadence-window, manual-merge regression"
    - "Tests assert MergeRun state immediately after client.post() (BackgroundTasks runs synchronously under TestClient — no sleep)"
    - "Merge-failure test asserts push response is accepted AND an IntegrationDelivery destination=auto_merge status=failed row exists"
    - "Manual-merge regression test proves _cmd_merge / merge_scan output is unchanged vs v5.4 (Option-A union, coverage_warning, sensor-local scanned_at)"
  artifacts:
    - path: "tests/test_auto_merge_trigger.py"
      provides: "6 acceptance tests covering AUTOMERGE-01/02/03 + D-05 + cadence-window"
      contains: "def test_all_sensors_in_triggers_merge"
  key_links:
    - from: "tests/test_auto_merge_trigger.py"
      to: "quirk.dashboard.api.routes.sensor (run_auto_merge / _eval_trigger_condition)"
      via: "TestClient push + QUIRK_CONFIG_PATH monkeypatch"
      pattern: "client\\.post\\(.*/api/sensor/push"
---

<objective>
Author `tests/test_auto_merge_trigger.py` with the 6 acceptance tests mandated
by CONTEXT <specifics>. These tests are the Nyquist gate for the entire phase
(VALIDATION Dimension 8) and the regression proof for AUTOMERGE-03.

Purpose: Prove auto-merge fires/doesn't-fire per config, is failure-isolated,
coalesces double-fires, supports cadence-window, and leaves manual merge regression-free.

Output: One new test file. No production code changes (production lives in 114-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/114-automatic-merge-trigger/114-CONTEXT.md
@.planning/phases/114-automatic-merge-trigger/114-RESEARCH.md
@.planning/phases/114-automatic-merge-trigger/114-PATTERNS.md

<interfaces>
<!-- Test harness analogs (copy verbatim) from tests/test_sensor_ingest.py -->
_app_with_db() -> (app, TestClient(raise_server_exceptions=False), engine, TestingSession)
  uses shared in-memory sqlite + dependency_overrides[get_db]
_seed_sensor(TestingSession, sensor_id=..., segment=...)  — last_push_at=None, cadence=1440
_seed_token(TestingSession, sensor_id, raw_token=None, revoked=False) -> raw_token (sha256 hash)
_build_envelope(sensor_id=..., pushed_at=...) + _compress(envelope)
push: client.post("/api/sensor/push", content=body, headers={"Authorization": f"Bearer {raw_token}"})
  -> 200, json()["status"] == "accepted"

Config isolation helper (PATTERNS): write console.auto_merge yaml to tmp_path,
monkeypatch.setenv("QUIRK_CONFIG_PATH", path), monkeypatch.delenv("QUIRK_API_TOKEN", raising=False).

BackgroundTasks runs SYNCHRONOUSLY under TestClient (starlette 0.49.3 / fastapi 0.128.8) —
assert MergeRun immediately after client.post(), no time.sleep.

Models: MergeRun, IntegrationDelivery (destination="auto_merge"), Sensor, SensorToken.
Manual merge entry: quirk.cli.sensor_cmd._cmd_merge / quirk.merge.scan.merge_scan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build test harness and all-sensors-in / disabled tests</name>
  <files>tests/test_auto_merge_trigger.py</files>
  <read_first>
    tests/test_sensor_ingest.py (L40-63 _app_with_db, L105-154 _seed_sensor/_seed_token, push pattern)
    tests/conftest.py (_isolate_quirk_db autouse fixture)
    .planning/phases/114-automatic-merge-trigger/114-PATTERNS.md (test section + config-isolation helper)
  </read_first>
  <behavior>
    - test_all_sensors_in_triggers_merge (AUTOMERGE-01): seed 2 sensors + tokens, auto-merge ON / all-sensors-in; push sensor A → 0 MergeRun; push sensor B (last enrolled) → exactly 1 MergeRun, no manual merge call.
    - test_auto_merge_disabled (AUTOMERGE-02a): same seeding, config enabled=false; after final push → 0 MergeRun.
    - test_revoked_sensor_excluded (D-04): seed 2 sensors, revoke sensor B's token; pushing only sensor A (the sole active sensor) → 1 MergeRun (B does not block all-in).
  </behavior>
  <action>
    Copy the harness helpers verbatim from tests/test_sensor_ingest.py
    (_make_test_engine, _app_with_db, _seed_sensor, _seed_token, _build_envelope,
    _compress). Add a `_write_auto_merge_config(tmp_path, enabled=True,
    trigger_condition="all-sensors-in", cadence_window_minutes=None)` helper that
    yaml.dumps `{console: {auto_merge: {...}}}` to tmp_path and returns the path.
    Each test sets QUIRK_CONFIG_PATH via monkeypatch and deletes QUIRK_API_TOKEN.
    Query MergeRun via a fresh TestingSession() immediately after client.post()
    (synchronous BackgroundTasks). Assert push json status == "accepted" each time.
  </action>
  <verify>
    <automated>python -m pytest tests/test_auto_merge_trigger.py -k "all_sensors_in or disabled or revoked" -q</automated>
  </verify>
  <done>Three tests pass: all-sensors-in produces exactly one MergeRun on the final push, disabled produces none, revoked sensor is excluded from the all-in set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Failure-isolation and double-fire tests</name>
  <files>tests/test_auto_merge_trigger.py</files>
  <read_first>
    tests/test_sensor_ingest.py (push pattern)
    quirk/dashboard/api/routes/sensor.py (run_auto_merge — from 114-01)
    .planning/phases/114-automatic-merge-trigger/114-RESEARCH.md (Pitfall 6, D-05 re-check)
  </read_first>
  <behavior>
    - test_merge_failure_isolated (AUTOMERGE-02b): monkeypatch quirk.merge.scan.merge_scan (as imported in sensor.run_auto_merge) to raise; final push → response status "accepted", ingested payload rows intact, 0 MergeRun, exactly 1 IntegrationDelivery destination="auto_merge" status="failed" with non-null error_summary.
    - test_double_fire_harmless (D-05): two near-simultaneous final pushes (e.g. last sensor pushes twice, or two sensors complete back-to-back) → at most a small number of MergeRun rows (assert >=1 and tolerate the accepted residual duplicate); idempotent re-check coalesces the no-op case (assert a second push with no newer data produces no additional MergeRun).
  </behavior>
  <action>
    Use monkeypatch.setattr on the merge_scan symbol resolved inside run_auto_merge
    (it is imported locally inside the function, so patch
    `quirk.merge.scan.merge_scan`). Assert the push still returns 200/"accepted"
    and that the failed IntegrationDelivery row uses safe_str output (error_summary
    is a non-empty str, not a raw traceback). For double-fire: after the all-in
    MergeRun exists, push again with no newer last_push_at and assert MergeRun count
    unchanged (idempotent re-check no-op).
  </action>
  <verify>
    <automated>python -m pytest tests/test_auto_merge_trigger.py -k "failure_isolated or double_fire" -q</automated>
  </verify>
  <done>Merge-failure test: push accepted, no MergeRun, one auto_merge/failed audit row. Double-fire test: idempotent re-check prevents a redundant MergeRun when no newer push exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: cadence-window and manual-merge regression tests</name>
  <files>tests/test_auto_merge_trigger.py</files>
  <read_first>
    tests/test_sensor_ingest.py (push pattern)
    quirk/cli/sensor_cmd.py (_cmd_merge L770-797)
    quirk/merge/scan.py (merge_scan, _build_coverage_warning — coverage_warning + scanned_at semantics)
    .planning/phases/114-automatic-merge-trigger/114-CONTEXT.md (specifics 5 and 6)
  </read_first>
  <behavior>
    - test_cadence_window_triggers (AUTOMERGE-02c): config trigger_condition="cadence-window" with a tiny cadence_window_minutes (e.g. 0) and a prior MergeRun whose merged_at is in the past; seed 2 sensors but push only one; the push that crosses the elapsed window fires a merge producing a MergeRun whose coverage_warning lists the not-yet-in sensor.
    - test_manual_merge_regression (AUTOMERGE-03): with auto-merge OFF, seed + push all sensors, then invoke _cmd_merge (or merge_scan directly) and assert the produced MergeRun + CBOM is the Option-A union with coverage_warning and sensor-local scanned_at preserved (scanned_at not rewritten) — identical shape to v5.4.
  </behavior>
  <action>
    For cadence-window: insert a MergeRun row with merged_at set well in the past
    (naive UTC), set cadence_window_minutes=0 (or 1) in config, push a single sensor,
    assert a new MergeRun is produced and its coverage_warning_json names the missing
    sensor. For regression: drive the manual path exactly as v5.4 does (build an args
    namespace for _cmd_merge or call merge_scan with output_dir=tmp_path) and assert
    the result dict / MergeRun fields (endpoint_count, sensor_count, coverage_warning,
    per-sensor scanned_at unchanged). Do not modify production code.
  </action>
  <verify>
    <automated>python -m pytest tests/test_auto_merge_trigger.py -q && python -m pytest tests/ -k "sensor_ingest or sensor_merge" -q</automated>
  </verify>
  <done>All 6 acceptance tests in tests/test_auto_merge_trigger.py pass; cadence-window emits coverage_warning for missing sensors; manual merge regression matches v5.4 Option-A union + coverage_warning + sensor-local scanned_at. Pre-existing sensor push/merge tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test config → trigger eval | Tests exercise the config-driven enable/disable + trigger_condition boundary |
| merge failure → push txn | Tests prove the structural isolation boundary holds (failure never rolls back push) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-T1 | Information Disclosure | failed audit row content | mitigate | test_merge_failure_isolated asserts error_summary is scrubbed (safe_str output), not a raw traceback |
| T-114-T2 | Denial of Service | merge failure isolation | mitigate | test_merge_failure_isolated asserts push still returns accepted + rows intact |
| T-114-T3 | Spoofing | revoked sensor in all-in set | mitigate | test_revoked_sensor_excluded proves revoked sensors do not block/contribute to all-in |
| T-114-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH audit: none) |
</threat_model>

<verification>
- `python -m pytest tests/test_auto_merge_trigger.py -q` — all 6 acceptance tests green.
- `python -m pytest tests/ -q` full suite shows no new failures attributable to this phase.
</verification>

<success_criteria>
- All 6 CONTEXT <specifics> acceptance tests implemented and passing.
- AUTOMERGE-03 regression proven by test_manual_merge_regression.
- Nyquist: every phase requirement has an automated test (VALIDATION Dimension 8 satisfied).
</success_criteria>

<output>
Create `.planning/phases/114-automatic-merge-trigger/114-02-SUMMARY.md` when done
</output>
