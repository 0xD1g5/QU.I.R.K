---
phase: 114-automatic-merge-trigger
plan: 03
type: execute
wave: 2
depends_on: ["114-01"]
files_modified:
  - docs/operators-guide.md
  - quantum-chaos-enterprise-lab/expected_results_distributed.md
  - docs/UAT-SERIES.md
autonomous: true
requirements: [AUTOMERGE-03]
must_haves:
  truths:
    - "Operators can read in operators-guide.md how to enable/disable auto-merge, select the trigger condition, and find auto-merge audit rows"
    - "The distributed lab e2e oracle notes the auto-merge MergeRun fires before the manual Step 3 merge (harmless duplicate), keeping manual merge documented as regression-free"
    - "docs/UAT-SERIES.md reflects the auto-merge behavior and is synced to Obsidian"
  artifacts:
    - path: "docs/operators-guide.md"
      provides: "Auto-merge operator section (toggle, two trigger conditions, default-ON, reading auto_merge audit rows)"
      contains: "auto_merge"
    - path: "quantum-chaos-enterprise-lab/expected_results_distributed.md"
      provides: "Oracle note: auto-merge MergeRun before manual Step 3"
      contains: "auto_merge"
  key_links: []
---

<objective>
Document the auto-merge feature for operators, update the distributed lab e2e
oracle to reflect the new auto-merge IntegrationDelivery row / pre-Step-3
MergeRun, and update + sync docs/UAT-SERIES.md per the mandatory per-phase doc
workflow.

Purpose: Operators understand the default-ON behavior, how to disable it, and
how to read auto-merge outcomes; the lab oracle stays a faithful reflection of
distributed e2e (CLAUDE.md lab-maintenance rule); UAT series stays current.

Output: Updated operators-guide.md, expected_results_distributed.md, UAT-SERIES.md
+ Obsidian sync. No production code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/114-automatic-merge-trigger/114-CONTEXT.md
@.planning/phases/114-automatic-merge-trigger/114-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auto-merge section to operators-guide.md</name>
  <files>docs/operators-guide.md</files>
  <read_first>
    docs/operators-guide.md (existing section structure + merge/console sections)
    .planning/phases/114-automatic-merge-trigger/114-CONTEXT.md (D-07, D-08, D-09, D-10)
  </read_first>
  <action>
    Add an "Automatic Merge" operator section documenting: auto-merge is ON by
    default (D-07); the `console.auto_merge.enabled` toggle and how to disable it
    for explicit manual-only control; the `trigger_condition` selector with both
    values explained — `all-sensors-in` (merge once every non-revoked enrolled
    sensor has checked in) and `cadence-window` (push-evaluated; merges whatever has
    arrived once the window elapses, with coverage_warning); the optional
    `cadence_window_minutes` (defaults to per-sensor expected_cadence_minutes / 1440);
    that toggling never affects in-flight pushes; and how to read auto-merge outcomes
    via IntegrationDelivery rows with destination="auto_merge" (status ok/failed,
    error_summary). State that `quirk sensor merge` remains available and unchanged
    (AUTOMERGE-03). Match existing doc voice; no code dumps beyond a small config snippet.
  </action>
  <verify>
    <automated>grep -q "auto_merge" docs/operators-guide.md && grep -q "all-sensors-in" docs/operators-guide.md && grep -q "cadence-window" docs/operators-guide.md</automated>
  </verify>
  <done>operators-guide.md has an Automatic Merge section covering toggle, both trigger conditions, default-ON, in-flight safety, reading auto_merge audit rows, and the unchanged manual command.</done>
</task>

<task type="auto">
  <name>Task 2: Update distributed lab e2e oracle</name>
  <files>quantum-chaos-enterprise-lab/expected_results_distributed.md</files>
  <read_first>
    quantum-chaos-enterprise-lab/expected_results_distributed.md (Step 3 merge oracle ~L96)
    .planning/phases/114-automatic-merge-trigger/114-RESEARCH.md (Pitfall 5, Open Question 3)
  </read_first>
  <action>
    Update the merge step in the oracle to note: with auto-merge ON by default,
    after the final sensor (sensor-b) push completes, one MergeRun row already
    exists (auto-merge) and an IntegrationDelivery destination="auto_merge"
    status="ok" row is written; the explicit Step 3 `quirk sensor merge` then
    produces a second harmless duplicate MergeRun (D-06) and remains in place to
    demonstrate the manual path is regression-free (AUTOMERGE-03). Do not remove the
    manual merge step. Keep wording consistent with the rest of the oracle.
  </action>
  <verify>
    <automated>grep -q "auto_merge\|auto-merge" quantum-chaos-enterprise-lab/expected_results_distributed.md</automated>
  </verify>
  <done>Oracle documents the auto-merge MergeRun + auto_merge audit row firing before the manual Step 3, with the manual step retained as regression proof.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs/UAT-SERIES.md and sync to Obsidian</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    docs/UAT-SERIES.md (header, Last Updated, relevant distributed/merge series)
    CLAUDE.md (Mandatory Phase Completion Steps 2-4 — UAT-SERIES update, Obsidian sync, commit)
  </read_first>
  <action>
    Update the relevant UAT-SERIES.md test series to add auto-merge test cases:
    a default-ON all-sensors-in merge produced without a manual command, the disable
    toggle, and the merge-failure audit row. Bump the `**Last Updated:**` date.
    Then sync to Obsidian per CLAUDE.md Step 3: write frontmatter +
    docs/UAT-SERIES.md content to
    "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" via the documented
    printf+cat+cp sequence (file too large for CLI content=). Do not run the git
    commit step here — phase completion handles commits.
  </action>
  <verify>
    <automated>grep -qi "auto.merge\|auto_merge" docs/UAT-SERIES.md && test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"</automated>
  </verify>
  <done>UAT-SERIES.md has auto-merge test cases + updated Last Updated date; the Obsidian vault UAT-Series.md reflects the updated content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Documentation-only plan — no runtime trust boundaries introduced. The operator
guidance reinforces the security posture documented in 114-01 (safe_str audit
rows, failure isolation).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-D1 | Information Disclosure | operator docs examples | mitigate | Config/audit examples use placeholder values; no real tokens or secrets in docs |
| T-114-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `grep auto_merge docs/operators-guide.md` returns the new section.
- `grep auto_merge quantum-chaos-enterprise-lab/expected_results_distributed.md` returns the oracle note.
- Obsidian UAT-Series.md exists and matches docs/UAT-SERIES.md.
</verification>

<success_criteria>
- Operators-guide documents the toggle, both trigger conditions, default-ON, and auto_merge audit rows.
- Lab oracle is a faithful reflection of distributed e2e with auto-merge ON (CLAUDE.md lab rule).
- UAT-SERIES.md updated + synced to Obsidian (mandatory per-phase doc workflow).
- AUTOMERGE-03 reinforced in docs: manual merge documented as unchanged and retained.
</success_criteria>

<output>
Create `.planning/phases/114-automatic-merge-trigger/114-03-SUMMARY.md` when done
</output>
