---
phase: 94-openapi-bearer-token-analysis
plan: 03
type: execute
wave: 3
depends_on: ["94-01", "94-02"]
files_modified:
  - docs/UAT-SERIES.md
  - docs/configuration.md
  - docs/getting-started.md
autonomous: true
requirements: [TOKEN-01, TOKEN-02, TOKEN-03, SPEC-01, SPEC-02, SPEC-03, SCORE-01, PKG-01]
must_haves:
  truths:
    - "docs describe the analyze-token command and the --openapi-spec flag / openapi: config block"
    - "docs/UAT-SERIES.md has test cases for analyze-token (incl. alg:none CRITICAL exit) and OpenAPI spec analysis (incl. SSRF/oversize rejection)"
    - "the QUIRK Obsidian vault Phase-94 note + UAT-Series note reflect this phase"
  artifacts:
    - path: "docs/UAT-SERIES.md"
      provides: "Phase 94 manual UAT test cases"
      contains: "analyze-token"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-94-OpenAPI-Bearer-Token-Analysis.md"
      provides: "Obsidian phase note"
  key_links:
    - from: "docs/UAT-SERIES.md"
      to: "Obsidian vault UAT-Series.md"
      via: "filesystem copy per CLAUDE.md sync workflow"
      pattern: "analyze-token"
---

<objective>
Satisfy the CLAUDE.md mandatory per-phase documentation steps: update user docs for the new
analyze-token command, --openapi-spec flag, and [api] extras; add Phase 94 UAT test cases; and sync
the Phase-94 note + UAT-Series to the Obsidian Digs vault. Runs last (wave 3) once both feature plans
have shipped so docs reflect the final CLI/flag/exit-code behavior.

Purpose: CLAUDE.md requires every phase to ship documentation + UAT-SERIES + Obsidian sync tasks.
Output: edits to docs/UAT-SERIES.md, docs/configuration.md, docs/getting-started.md; vault notes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/94-openapi-bearer-token-analysis/94-CONTEXT.md
@.planning/phases/94-openapi-bearer-token-analysis/94-01-SUMMARY.md
@.planning/phases/94-openapi-bearer-token-analysis/94-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update user docs for analyze-token, --openapi-spec, [api] extras</name>
  <files>docs/configuration.md, docs/getting-started.md</files>
  <read_first>
    - docs/configuration.md (existing structure / extras + config-block sections)
    - docs/getting-started.md (existing command examples / install-extras section)
    - .planning/phases/94-openapi-bearer-token-analysis/94-01-SUMMARY.md (final analyze-token UX)
    - .planning/phases/94-openapi-bearer-token-analysis/94-02-SUMMARY.md (final OpenAPI flag + extras)
  </read_first>
  <action>
    In docs/getting-started.md add a usage section for `quirk analyze-token` (positional token / @file /
    stdin, `--json`, alg:none CRITICAL non-zero exit for CI gating) and for `quirk scan --openapi-spec
    <file|url>` (local-file default, scope-gated URL fetch, 10MB cap, SSRF $ref rejection). Note
    `pip install quirk[api]` installs openapi-spec-validator and that the scanner degrades gracefully
    when [api] is not installed. In docs/configuration.md document the `openapi:` config block
    (openapi_spec_path under scan config) and the [api] extras group. Keep diffs minimal; match existing
    doc tone/format. Do not invent behavior — source exact flags/exit codes from the two SUMMARY files.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && grep -q "analyze-token" docs/getting-started.md && grep -q "openapi-spec" docs/getting-started.md && grep -q "openapi" docs/configuration.md && echo DOCS_OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/getting-started.md mentions analyze-token, --json, alg:none non-zero exit, --openapi-spec, quirk[api]
    - docs/configuration.md documents the openapi: config block and [api] extras group
    - no fabricated flags/exit codes — all match the SUMMARY files
  </acceptance_criteria>
  <done>User docs accurately describe analyze-token, --openapi-spec, and the [api] extras group.</done>
</task>

<task type="auto">
  <name>Task 2: Update + sync docs/UAT-SERIES.md and Obsidian Phase-94 note</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (top header date, version strings, series structure, existing per-phase cases)
    - CLAUDE.md "Mandatory Phase Completion Steps" (Obsidian note + UAT sync workflow + commit)
    - .planning/phases/94-openapi-bearer-token-analysis/94-01-SUMMARY.md and 94-02-SUMMARY.md (what was built per plan)
  </read_first>
  <action>
    Update docs/UAT-SERIES.md: bump the `**Last Updated:**` date to today; add Phase 94 test cases to
    the relevant series covering (a) `quirk analyze-token <jwt>` reports alg/key-size/expiry/quantum-safety;
    (b) alg:none token → CRITICAL + non-zero exit; (c) opaque token → INFO + exit 0; (d) `--openapi-spec`
    local file → plaintext-server/unauthenticated/security-scheme findings in the findings table; (e)
    out-of-scope URL rejected before network; (f) internal-network $ref → SpecParsingError; (g) oversized
    spec rejected; (h) `pip install quirk[all]` does not pull schemathesis. Each case states explicit
    pass criteria.
    Then per CLAUDE.md mandatory steps: (1) write the Obsidian Phase-94 note directly to the vault
    filesystem at `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-94-OpenAPI-Bearer-Token-Analysis.md`
    with frontmatter (status: complete, type: phase, source, updated) + Goal + Requirements Covered
    (SPEC-01..03, TOKEN-01..03, SCORE-01, PKG-01) + Success Criteria + What Was Built (one subsection per
    plan from the SUMMARY files) + `[[Roadmap]]` link, following an existing Phases/ note as template;
    (2) sync UAT-SERIES.md to the vault via the printf-frontmatter + cat + cp filesystem pattern in
    CLAUDE.md (file too large for CLI content=); (3) commit docs/UAT-SERIES.md with
    `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-94): update UAT-SERIES.md" --files docs/UAT-SERIES.md`.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && grep -q "analyze-token" docs/UAT-SERIES.md && grep -Eq "169\.254\.169\.254|SpecParsingError|schemathesis" docs/UAT-SERIES.md && test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-94-OpenAPI-Bearer-Token-Analysis.md" && echo UAT_SYNC_OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/UAT-SERIES.md `**Last Updated:**` is today's date
    - Phase 94 UAT cases cover analyze-token (incl. alg:none non-zero exit), opaque token, OpenAPI local file, out-of-scope URL, $ref SSRF, oversize, schemathesis exclusion — each with explicit pass criteria
    - Obsidian Phase-94 note exists at the vault path with required frontmatter + sections
    - UAT-Series.md synced to vault; docs/UAT-SERIES.md committed
  </acceptance_criteria>
  <done>UAT-SERIES.md updated with Phase 94 cases, Obsidian Phase-94 note + UAT-Series synced to the Digs vault, UAT-SERIES.md committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → end user | Documentation describes security behavior (SSRF/scope/exit codes); inaccuracy could mislead operators |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-09 | Information Disclosure | UAT examples leaking real tokens | mitigate | All doc/UAT examples use synthetic placeholder tokens and example.com hosts — never real credentials |
| T-94-SC | Tampering | npm/pip installs | accept | Docs-only plan; no package installs |
</threat_model>

<verification>
- `grep -q analyze-token docs/getting-started.md docs/UAT-SERIES.md`
- Phase-94 Obsidian note exists at the vault path
- docs/UAT-SERIES.md committed via gsd-tools commit
</verification>

<success_criteria>
User docs + UAT-SERIES.md describe the analyze-token command, --openapi-spec flag, [api] extras, and the SSRF/scope/exit-code behaviors; Obsidian Phase-94 note + UAT-Series synced to the Digs vault.
</success_criteria>

<output>
Create `.planning/phases/94-openapi-bearer-token-analysis/94-03-SUMMARY.md` when done
</output>
