---
phase: 87-dependency-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/dashboard-quality.yml
autonomous: false
requirements: [DEP-01]
must_haves:
  truths:
    - "The dashboard-quality CI job runs against Node 24 and passes green on a real GitHub Actions run"
    - "No node-version: '20' reference remains in any workflow file"
  artifacts:
    - path: ".github/workflows/dashboard-quality.yml"
      provides: "Node 24 runtime pin for dashboard quality CI"
      contains: "node-version: '24'"
  key_links:
    - from: ".github/workflows/dashboard-quality.yml"
      to: "actions/setup-node@v4"
      via: "node-version input"
      pattern: "node-version: '24'"
---

<objective>
Raise the GitHub Actions Node runtime for the `dashboard-quality` workflow from Node 20 to Node 24, clearing GitHub's runner default-switch deadline (2026-06-16) before it lands.

Purpose: Node 20 actions begin failing/deprecating at the default-switch; this is a deadline-driven minimal CI change with the lowest churn risk.
Output: A single-line edit to `.github/workflows/dashboard-quality.yml`, validated by a real GitHub Actions run (D-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-dependency-hygiene/87-CONTEXT.md
@.github/workflows/dashboard-quality.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump node-version from '20' to '24' (DEP-01, D-01)</name>
  <files>.github/workflows/dashboard-quality.yml</files>
  <read_first>
    - .github/workflows/dashboard-quality.yml (the only Node pin in any workflow; the `Setup Node` step at lines 19-24)
  </read_first>
  <action>
    Change the single value `node-version: '20'` to `node-version: '24'` at line 22 of `.github/workflows/dashboard-quality.yml`. Do NOT change `actions/setup-node@v4` (D-01: it already supports Node 24; do not bump the action major to v5/v6 and do not audit third-party actions' bundled Node). Do NOT touch `cache: 'npm'` or `cache-dependency-path`. This is the smallest diff that clears the deadline. No other workflow file contains a Node pin.
  </action>
  <verify>
    <automated>grep -rn "node-version: '20'" .github/workflows/ ; test -z "$(grep -rn "node-version: '20'" .github/workflows/)" && grep -q "node-version: '24'" .github/workflows/dashboard-quality.yml && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "node-version: '20'" .github/workflows/` returns empty (no Node 20 pin remains in any workflow).
    - `.github/workflows/dashboard-quality.yml` contains the literal string `node-version: '24'`.
    - `actions/setup-node@v4` is unchanged (grep still shows `actions/setup-node@v4`).
  </acceptance_criteria>
  <done>The dashboard-quality workflow pins Node 24, no Node 20 pin remains anywhere, and the setup-node action major is unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify dashboard-quality CI runs green on Node 24 (DEP-01, D-02)</name>
  <action>Push the phase branch to trigger the dashboard-quality workflow, then human-verify the real GitHub Actions run goes green on Node 24 (per the steps below). This is the definition of done for DEP-01 and cannot be satisfied locally.</action>
  <what-built>Bumped the `dashboard-quality` workflow to Node 24 (single-line `node-version` change), keeping `actions/setup-node@v4`.</what-built>
  <how-to-verify>
    Per D-02, "done" requires a REAL GitHub Actions run — local validation alone is insufficient and CANNOT be done by pytest.
    1. Push the phase branch to origin so the `dashboard-quality` workflow triggers.
    2. Open the workflow run in the GitHub Actions UI (Actions tab -> dashboard-quality).
    3. Confirm the `Setup Node` step reports Node 24.x.
    4. Confirm the workflow completes GREEN (npm build + lint + a11y all pass on Node 24).
  </how-to-verify>
  <resume-signal>Type "approved" once the dashboard-quality workflow run is green on Node 24, or describe the failure.</resume-signal>
</task>

</tasks>

<verification>
- `grep -rn "node-version: '20'" .github/workflows/` returns empty.
- `.github/workflows/dashboard-quality.yml` contains `node-version: '24'` and still `actions/setup-node@v4`.
- A real GitHub Actions `dashboard-quality` run is green on Node 24 (human-verified, D-02).
</verification>

<success_criteria>
Phase success criterion #1 is met: the `dashboard-quality` CI job runs against Node 24 and passes green on a real run; no remaining `node-version: 20` references in any workflow file.
</success_criteria>

<output>
Create `.planning/phases/87-dependency-hygiene/87-01-SUMMARY.md` when done.
</output>
