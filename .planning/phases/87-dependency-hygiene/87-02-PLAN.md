---
phase: 87-dependency-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/util/xml_safe.py
  - quirk/discovery/nmap_parser.py
  - quirk/scanner/saml_scanner.py
  - pyproject.toml
  - tests/test_xml_safe.py
  - tests/test_nmap_hardening.py
  - tests/test_packaging.py
  - tests/test_identity_infra.py
autonomous: true
requirements: [DEP-02]
must_haves:
  truths:
    - "quirk/util/xml_safe.py is the single hardened lxml parser entry point used by both nmap_parser.py and saml_scanner.py (D-03, D-05, D-06)"
    - "make_safe_parser() returns a fresh hardened parser per call (thread-safe; not a shared module constant) (D-04)"
    - "A billion-laughs / XXE payload raises rather than expands or fetches (D-07)"
    - "defusedxml no longer appears in pyproject.toml or any quirk/ import statement (D-03)"
    - "WR-06 stays mitigated and the audit ledger zero-open gate stays green (D-08)"
    - "Full test suite passes with no regressions; this plan is parallel-safe with Plan 87-01 — disjoint files, independent atomic commits (D-09)"
  artifacts:
    - path: "quirk/util/xml_safe.py"
      provides: "make_safe_parser() factory + parse_safely() helper — the hardened lxml chokepoint"
      exports: ["make_safe_parser", "parse_safely"]
      min_lines: 25
    - path: "tests/test_xml_safe.py"
      provides: "Forward-locking XXE/billion-laughs invariant + grep gate for defusedxml-free quirk/"
      contains: "make_safe_parser"
  key_links:
    - from: "quirk/discovery/nmap_parser.py"
      to: "quirk.util.xml_safe.make_safe_parser"
      via: "ET.parse(xml_path, parser=make_safe_parser())"
      pattern: "make_safe_parser"
    - from: "quirk/scanner/saml_scanner.py"
      to: "quirk.util.xml_safe.make_safe_parser"
      via: "_safe_ET_fromstring delegates to make_safe_parser()"
      pattern: "make_safe_parser"
---

<objective>
Replace `defusedxml` with a single hardened `lxml` chokepoint (`quirk/util/xml_safe.py`), re-route both XML consumers (`nmap_parser.py`, `saml_scanner.py`) through it, remove `defusedxml` from `pyproject.toml`, and lock the XXE/billion-laughs protection as a permanent forward-locking CI invariant.

Purpose: defusedxml has had no substantive release since 2021; lxml's explicit 5-flag XMLParser provides stronger, auditable XXE control and is already a mandatory core dep. Closes the lxml/XXE migration debt and keeps audit finding WR-06 mitigated (DEP-02).
Output: New `quirk/util/xml_safe.py`; migrated `nmap_parser.py` + `saml_scanner.py`; `defusedxml` removed from `pyproject.toml`; four invariant-encoding tests flipped to the new (lxml) invariant; new `tests/test_xml_safe.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-dependency-hygiene/87-CONTEXT.md
@.planning/phases/87-dependency-hygiene/87-PATTERNS.md
@.planning/phases/87-dependency-hygiene/87-VALIDATION.md
@quirk/scanner/saml_scanner.py
@quirk/discovery/nmap_parser.py
@tests/test_audit_ledger_zero_open.py

<decision_precedence>
CONTEXT.md decisions D-01..D-09 are AUTHORITATIVE and override any conflicting code example in RESEARCH/STACK/PATTERNS. CRITICAL: D-04 mandates a `make_safe_parser()` FACTORY returning a FRESH parser per call — NOT a module-level shared constant — because lxml parsers are not thread-safe and the SAML/SSH scanners run threaded. Any `_SAFE_PARSER = etree.XMLParser(...)` at module scope is FORBIDDEN.

Resolved discretion (D-05, RESEARCH Open Question 1): `parse_safely(source)` wraps `etree.parse()` ONLY (the nmap file-path consumer). `saml_scanner.py` (a `fromstring(bytes)` consumer) calls `make_safe_parser()` directly inside `_safe_ET_fromstring`. This keeps each call site explicit about its data shape.
</decision_precedence>

<interfaces>
New public surface of quirk/util/xml_safe.py (callers depend on these):
- `make_safe_parser() -> lxml.etree.XMLParser` — fresh hardened parser per call. Flags: resolve_entities=False, no_network=True, load_dtd=False, dtd_validation=False, huge_tree=False.
- `parse_safely(source) -> lxml.etree._ElementTree` — wraps `etree.parse(source, parser=make_safe_parser())`.

Existing test helper to reuse (tests/test_packaging.py lines 51-58):
- `_core_deps_section()` — returns the `[project]` dependencies list text only (before optional-dependencies).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): Create xml_safe chokepoint + flip the four invariant tests to the new lxml invariant</name>
  <files>quirk/util/xml_safe.py, tests/test_xml_safe.py, tests/test_nmap_hardening.py, tests/test_packaging.py, tests/test_identity_infra.py</files>
  <read_first>
    - quirk/scanner/saml_scanner.py (lines 4-13: the existing lxml per-call XMLParser pattern to mirror — fresh parser inside the function body)
    - quirk/util/optional_extra.py (module docstring "Public surface:" convention for quirk/util modules)
    - tests/test_audit_ledger_zero_open.py (canonical forward-locking CI-invariant test structure)
    - tests/test_packaging.py (the `_core_deps_section()` helper at lines 51-58 and `test_defusedxml_in_core_deps` at lines 71-73)
    - tests/test_nmap_hardening.py (the two defusedxml-dependent tests at lines 121-136)
    - tests/test_identity_infra.py (the `assertIn('"defusedxml>=0.7.1"', source, ...)` block at lines 239-243)
  </read_first>
  <behavior>
    - make_safe_parser() returns a NEW object on each call (`is not` between two calls) — thread-safety invariant.
    - make_safe_parser() returns a parser configured with the 5 hardening flags.
    - parse_safely() round-trips a minimal valid XML file (e.g. a tiny nmaprun fixture).
    - A billion-laughs payload passed through parse_safely() raises lxml.etree.XMLSyntaxError (load_dtd=False refuses the DTD before any expansion — NOT defusedxml's EntitiesForbidden).
    - An external-entity (file:///etc/passwd) XXE payload through parse_safely() raises lxml.etree.XMLSyntaxError.
    - No file under quirk/ contains a `defusedxml` import (grep gate returns zero hits).
  </behavior>
  <action>
    Create `quirk/util/xml_safe.py` per D-04/D-05: `from __future__ import annotations`; module docstring listing the "Public surface:" (`make_safe_parser`, `parse_safely`) in the optional_extra.py style. Define `make_safe_parser() -> etree.XMLParser` returning `etree.XMLParser(resolve_entities=False, no_network=True, load_dtd=False, dtd_validation=False, huge_tree=False)` — construct the parser INSIDE the function (fresh per call). Define `parse_safely(source)` returning `etree.parse(source, parser=make_safe_parser())`. Do NOT create any module-level parser constant (D-04 — forbidden). Import lxml via `from lxml import etree`.

    Create `tests/test_xml_safe.py` mirroring the forward-locking-invariant structure of test_audit_ledger_zero_open.py (module docstring naming D-04/D-07 and the permanence intent; `from __future__ import annotations`; `Path(__file__).resolve().parent.parent` for repo-root; named `BILLION_LAUGHS` and XXE payload constants; function docstrings citing the decision number). Include exactly these test functions: `test_make_safe_parser_returns_fresh_instance` (two calls `is not` each other), `test_make_safe_parser_flags` (assert the 5-flag set on the returned parser), `test_parse_safely_accepts_valid_xml`, `test_parse_safely_blocks_billion_laughs` (pytest.raises lxml.etree.XMLSyntaxError), `test_parse_safely_blocks_xxe` (pytest.raises lxml.etree.XMLSyntaxError on the file:// external-entity payload), and `test_no_defusedxml_import_in_quirk` (rglob/grep `quirk/` for `defusedxml`, assert zero hits — permanent grep gate covering BOTH nmap_parser.py and saml_scanner.py).

    Flip the four invariant tests that currently encode the OLD (defusedxml-present) contract — they will RED on a correct change, so update them in this Wave 0 task BEFORE the migration:
    1. tests/test_nmap_hardening.py: rename `test_nmap_parser_uses_defusedxml` -> `test_nmap_parser_uses_xml_safe`; assert `quirk.util.xml_safe.make_safe_parser` is callable (drop the `nmap_parser.ET.__name__.startswith("defusedxml")` assertion).
    2. tests/test_nmap_hardening.py: rename `test_nmap_parser_blocks_xxe` -> `test_nmap_parser_blocks_xxe_lxml`; import `from lxml import etree`; drive the XXE payload through `nmap_parser.parse_nmap_xml(str(p))` (end-to-end path) and assert it raises `etree.XMLSyntaxError` (NOT `defusedxml.common.EntitiesForbidden`).
    3. tests/test_packaging.py: replace `test_defusedxml_in_core_deps` with `test_defusedxml_not_in_core_deps` asserting `"defusedxml" not in _core_deps_section()`; reuse the existing `_core_deps_section()` helper unchanged; leave `test_lxml_in_core_deps` untouched.
    4. tests/test_identity_infra.py: delete the 5-line `self.assertIn('"defusedxml>=0.7.1"', source, ...)` block at lines 239-243; leave the adjacent `lxml>=6.0` and `signxml>=4.4.0` assertIn blocks unchanged.

    NOTE: nmap_parser.py source migration happens in Task 2; the two rewritten test_nmap_hardening tests reference the post-migration API (make_safe_parser usage, parse_nmap_xml raising lxml errors) and may RED until Task 2 lands — that is expected within this plan's commit sequence. Run `python -m compileall quirk/util/xml_safe.py` to confirm the new module compiles.
  </action>
  <verify>
    <automated>python -m compileall quirk/util/xml_safe.py && python -m pytest tests/test_xml_safe.py tests/test_packaging.py tests/test_identity_infra.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `quirk/util/xml_safe.py` exists, compiles, exports `make_safe_parser` and `parse_safely`, and contains NO module-level `XMLParser(` assignment (grep: `grep -nE "^[A-Z_]+ *= *etree\.XMLParser" quirk/util/xml_safe.py` returns empty).
    - `make_safe_parser` constructs the parser with all five literal flags `resolve_entities=False`, `no_network=True`, `load_dtd=False`, `dtd_validation=False`, `huge_tree=False` (grep confirms all five present in the file).
    - `python -m pytest tests/test_xml_safe.py -v` passes all six listed test functions (parser-construction + billion-laughs + XXE + grep-gate). `test_no_defusedxml_import_in_quirk` may pass only after Task 2/3 remove the source imports — if RED here, it goes green by end of plan.
    - `tests/test_packaging.py::test_defusedxml_not_in_core_deps` exists; `test_defusedxml_in_core_deps` no longer exists (grep returns empty).
    - `tests/test_identity_infra.py` no longer contains `'"defusedxml>=0.7.1"'` (grep returns empty).
    - `tests/test_nmap_hardening.py` contains `test_nmap_parser_uses_xml_safe` and `test_nmap_parser_blocks_xxe_lxml`; no longer contains `test_nmap_parser_uses_defusedxml` or `from defusedxml.common import EntitiesForbidden`.
  </acceptance_criteria>
  <done>The xml_safe chokepoint exists with the factory + helper (no shared constant), the new XXE/billion-laughs invariant test file exists, and all four OLD defusedxml-asserting tests are flipped to the new lxml invariant. Commit: `feat(87-02): add xml_safe chokepoint and flip XXE invariant tests to lxml`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate nmap_parser.py and saml_scanner.py to the xml_safe chokepoint (D-03, D-06, D-08)</name>
  <files>quirk/discovery/nmap_parser.py, quirk/scanner/saml_scanner.py</files>
  <read_first>
    - quirk/discovery/nmap_parser.py (lines 1-6: defusedxml import + WR-06 comment; line 22: the `ET.parse(xml_path)` call)
    - quirk/scanner/saml_scanner.py (lines 4-24: lxml primary path + the defusedxml fallback block to delete)
    - .planning/phases/87-dependency-hygiene/87-PATTERNS.md (the exact before/after blocks for both files)
  </read_first>
  <action>
    nmap_parser.py (D-06, D-08): replace `import defusedxml.ElementTree as ET` with `from lxml import etree as ET` plus `from quirk.util.xml_safe import make_safe_parser`. Rewrite the WR-06 mitigation comment (currently `# defusedxml per audit WR-06 — ...`) to cite the new chokepoint: state that XML is parsed via `quirk/util/xml_safe.py` hardened lxml parser, list the five flags, note "Replaces defusedxml per Phase 87 DEP-02", cite the invariant test (`tests/test_xml_safe.py` / `test_nmap_parser_blocks_xxe_lxml`) per the Phase 71 audit-trail convention, and add "DO NOT replace make_safe_parser() with a shared parser constant — see D-04". Change the parse call at line 22 from `ET.parse(xml_path)` to `ET.parse(xml_path, parser=make_safe_parser())`. Keep ALL existing ElementTree-style navigation (`getroot`, `findall`, `find`, `.get`) unchanged — lxml supports these near-identically (D-06: smallest behavioral change, no xpath rewrite).

    saml_scanner.py (D-03): delete the entire `except ImportError:` fallback block (lines 14-24, the `_defused_stdlib_ET` branch and the RuntimeError branch). Replace the `try: import lxml.etree as ET ...` block with an unconditional `import lxml.etree as ET` + `from quirk.util.xml_safe import make_safe_parser`, and redefine `_safe_ET_fromstring(xml_bytes)` to `return ET.fromstring(xml_bytes, parser=make_safe_parser())` (delegating to the chokepoint, keeping the per-call fresh-parser pattern). Set `LXML_AVAILABLE = True` unconditionally. lxml>=6.0 is a mandatory core dep; a missing lxml must fail loudly, not silently downgrade.

    Run `python -m compileall quirk/discovery/nmap_parser.py quirk/scanner/saml_scanner.py`.
  </action>
  <verify>
    <automated>python -m compileall quirk/discovery/nmap_parser.py quirk/scanner/saml_scanner.py && python -m pytest tests/test_nmap_hardening.py tests/test_xml_safe.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "defusedxml" quirk/discovery/nmap_parser.py quirk/scanner/saml_scanner.py` returns empty.
    - nmap_parser.py contains `from quirk.util.xml_safe import make_safe_parser` and `ET.parse(xml_path, parser=make_safe_parser())`; the WR-06 comment cites the xml_safe chokepoint and the five flags.
    - saml_scanner.py contains an unconditional `import lxml.etree as ET`, no `except ImportError` defusedxml branch, and `_safe_ET_fromstring` delegates to `make_safe_parser()`.
    - `python -m pytest tests/test_nmap_hardening.py -v` passes (regression: `parse_nmap_xml` succeeds on a valid fixture; `test_nmap_parser_blocks_xxe_lxml` raises lxml.etree.XMLSyntaxError end-to-end).
    - `python -m pytest tests/test_xml_safe.py::test_no_defusedxml_import_in_quirk -v` passes (grep gate now green — both source files cleaned).
  </acceptance_criteria>
  <done>Both XML consumers route through the xml_safe chokepoint, no defusedxml import remains in quirk/, the WR-06 comment cites the new mitigation, and nmap regression + XXE tests pass. Commit: `refactor(87-02): route nmap/saml XML parsing through xml_safe chokepoint`.</done>
</task>

<task type="auto">
  <name>Task 3: Remove defusedxml from pyproject.toml and prove the full suite + audit gate stay green (D-03, D-08)</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (lines 27-33: core deps list; line 30 is `"defusedxml>=0.7.1",`. Keep `"lxml>=6.0",` at line 28.)
    - tests/test_audit_ledger_zero_open.py (the zero-open-ledger gate that must stay green — WR-06 stays mitigated)
  </read_first>
  <action>
    Delete the single line `"defusedxml>=0.7.1",` from the `[project]` core dependencies list in pyproject.toml (line 30). Nothing replaces it. Do NOT touch `"lxml>=6.0",` (line 28) or any other dependency. Confirm defusedxml is not separately listed in the `[identity]` extras group (it is not — no further edits there). This is a controlled mitigation swap (D-08): WR-06 stays mitigated via the xml_safe chokepoint, so the audit ledger entry stays `[x] closed` and no AUDIT-TASKS.md edit is required. Run the full suite to confirm zero regressions across all four flipped tests, the new test file, and the audit-ledger gate.
  </action>
  <verify>
    <automated>python -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "defusedxml" pyproject.toml` returns empty.
    - `grep -n '"lxml>=6.0"' pyproject.toml` still returns line 28 (lxml retained).
    - `grep -rn "defusedxml" quirk/` returns empty (no source import anywhere).
    - `python -m pytest tests/test_packaging.py::test_defusedxml_not_in_core_deps -v` passes; `python -m pytest tests/test_packaging.py::test_lxml_in_core_deps -v` passes.
    - `python -m pytest tests/test_audit_ledger_zero_open.py -v` passes (WR-06 stays mitigated; ledger zero-open gate green).
    - `python -m pytest tests/ -v` passes with no regressions (phase success criterion #4).
  </acceptance_criteria>
  <done>defusedxml is gone from pyproject.toml and from every quirk/ import; lxml retained; the full test suite and the audit-ledger zero-open gate are green. Commit: `chore(87-02): remove defusedxml from pyproject.toml core deps`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanned-host XML output -> nmap_parser | nmap XML output (untrusted, attacker may influence target) crosses into the parser |
| remote IdP metadata -> saml_scanner | SAML/OIDC metadata XML fetched over the network (untrusted) crosses into the parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01 | Information disclosure / Tampering | XXE external entity in nmap/SAML XML | mitigate | `resolve_entities=False` + `no_network=True` in `make_safe_parser()`; end-to-end test `test_nmap_parser_blocks_xxe_lxml` + `test_parse_safely_blocks_xxe` assert raise |
| T-87-02 | Denial of service | Billion-laughs internal entity recursion | mitigate | `load_dtd=False` refuses the DTD before expansion; `huge_tree=False` keeps depth/size limits; `test_parse_safely_blocks_billion_laughs` asserts raise |
| T-87-03 | Spoofing / Information disclosure | SSRF via DTD external URI | mitigate | `no_network=True` blocks network access; `load_dtd=False` prevents DTD URI fetch |
| T-87-04 | Tampering | lxml XMLParser instance reused across scanner threads | mitigate | `make_safe_parser()` FACTORY returns a fresh parser per call (D-04); `test_make_safe_parser_returns_fresh_instance` enforces; module-level constant forbidden |
| T-87-05 | Tampering | Future developer weakens the parser (e.g. load_dtd=True) or re-introduces defusedxml/unsafe parser | mitigate | Forward-locking invariant tests (`test_xml_safe.py`, `test_no_defusedxml_import_in_quirk`) fail CI on regression (D-07) |
| T-87-SC | Tampering | npm/pip installs | accept | No package installs in this phase — defusedxml REMOVED, lxml already a verified core dep (RESEARCH Package Legitimacy Audit: zero new installs) |
</threat_model>

<verification>
- `grep -rn "defusedxml" quirk/ pyproject.toml` returns empty.
- `quirk/util/xml_safe.py` exposes `make_safe_parser()` (factory) + `parse_safely()`, with all five hardening flags and no module-level parser constant.
- Both `nmap_parser.py` and `saml_scanner.py` route through the chokepoint.
- `python -m pytest tests/ -v` is fully green, including the new XXE/billion-laughs invariant and the audit-ledger zero-open gate.
</verification>

<success_criteria>
Phase success criteria #2, #3, #4 met: defusedxml removed from pyproject.toml and all imports with xml_safe as the single hardened entry point used by both consumers; a billion-laughs/XXE pytest asserts the payload raises (confirming resolve_entities=False, no_network=True, load_dtd=False active); full suite passes with no regressions.
</success_criteria>

<output>
Create `.planning/phases/87-dependency-hygiene/87-02-SUMMARY.md` when done.
</output>
