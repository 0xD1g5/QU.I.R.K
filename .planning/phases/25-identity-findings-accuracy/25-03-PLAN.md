---
phase: 25-identity-findings-accuracy
plan: "03"
type: execute
wave: 2
depends_on: []
files_modified:
  - quantum-chaos-enterprise-lab/expected_results_v3.md
autonomous: true
requirements: [INFRA-03]

must_haves:
  truths:
    - "expected_results_v3.md contains three new identity chaos lab sections for DNSSEC, SAML/OIDC, and Kerberos"
    - "Each section follows the existing Phase 4 profile table format (heading, table, validation command, expected outcome)"
    - "DNSSEC section covers 4 zone profiles with expected classification outcomes"
    - "SAML/OIDC section covers the RSA-1024 signing cert finding from the SimpleSAMLphp lab"
    - "Kerberos section covers the RC4-HMAC etype finding from the Samba DC lab"
  artifacts:
    - path: "quantum-chaos-enterprise-lab/expected_results_v3.md"
      provides: "Identity chaos lab expected results for all three v4.2 scanner profiles"
      contains: "Phase 25 — DNSSEC Profile"
  key_links:
    - from: "quantum-chaos-enterprise-lab/expected_results_v3.md"
      to: "quantum-chaos-enterprise-lab/docker-compose.yml"
      via: "profile names referenced in validation commands"
      pattern: "docker compose --profile (bind9|simpla-samlphp|samba-dc)"
---

<objective>
Close NEW-ISSUE-3 from the v4.2 milestone audit: update expected_results_v3.md with identity
chaos lab entries for DNSSEC (bind9 profile), SAML/OIDC (simpla-samlphp profile), and Kerberos
(samba-dc profile). This is a documentation-only change — no code, no tests.

Purpose: Provides the oracle reference for the three v4.2 identity scanner chaos lab profiles,
so QA and future phases have documented expected outcomes to validate against.

Output: quantum-chaos-enterprise-lab/expected_results_v3.md extended with three new sections,
committed to git.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-identity-findings-accuracy/25-CONTEXT.md
@.planning/phases/25-identity-findings-accuracy/25-PATTERNS.md
</context>

<interfaces>
<!-- Existing file structure for format reference. -->

From quantum-chaos-enterprise-lab/expected_results_v3.md (lines 189–220 — SSH-Weak section):
```markdown
## Phase 4 — SSH-Weak Profile (profile: ssh-weak)

| Port | Service | Algorithm Class | Expected ssh-audit Finding | Severity |
|-----:|---------|----------------|---------------------------|----------|
| 20022 | ssh-weak | KEX | diffie-hellman-group1-sha1 | CRITICAL |
| 20022 | ssh-weak | KEX | diffie-hellman-group14-sha1 | WARNING |
...

**Scanner validation command:**
```
docker compose --profile ssh-weak up -d && sleep 5 && ssh-audit localhost:20022
```
**Expected:** ssh-audit returns >= 3 critical/warning findings ...
```

Pattern elements:
1. `## Phase NN — <Name> Profile (profile: <profile-name>)` heading
2. Markdown table with columns appropriate to scanner type
3. `**Scanner validation command:**` with fenced code block
4. `**Expected:**` plain-English summary sentence

Three identity scanner profile names from the v4.2 chaos lab:
- DNSSEC: `--profile bind9`
- SAML/OIDC: `--profile simpla-samlphp`
- Kerberos: `--profile samba-dc`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Append three identity chaos lab sections to expected_results_v3.md</name>
  <files>quantum-chaos-enterprise-lab/expected_results_v3.md</files>

  <read_first>
    - quantum-chaos-enterprise-lab/expected_results_v3.md (full file — read to find the current end
      and understand table format; file is ~220 lines)
    - .planning/phases/25-identity-findings-accuracy/25-PATTERNS.md (Pattern Map section on
      expected_results_v3.md for the 4 zone/profile expected outcomes)
    - .planning/phases/25-identity-findings-accuracy/25-CONTEXT.md (D-05 for the three chaos lab
      profile details and expected classification outcomes)
  </read_first>

  <action>
Append three new sections to the end of quantum-chaos-enterprise-lab/expected_results_v3.md.
Do NOT rewrite or truncate the file. Append after the last existing line (currently line 220,
the LDAPS Expected line).

Append the following content verbatim (adjust whitespace only if needed for readability):

```markdown


## Phase 25 — DNSSEC Profile (profile: bind9)

| Zone | Algorithm | Algorithm ID | Expected Finding | Severity |
|------|-----------|-------------|-----------------|----------|
| weak.example.com | RSASHA1 | 5 | DNSSEC weak signing algorithm (SHA-1 collision-vulnerable) | CRITICAL |
| weak.example.com | RSASHA1-NSEC3-SHA1 | 7 | DNSSEC weak signing algorithm (SHA-1) | CRITICAL |
| unsigned.example.com | NONE | — | Unsigned zone — DNS responses are unauthenticated | HIGH |
| nsec.example.com | ECDSAP256SHA256 | 13 | NSEC zone enumeration exposure | MEDIUM |

**Scanner validation command:**
```
docker compose --profile bind9 up -d && sleep 5 && quirk scan --targets weak.example.com unsigned.example.com nsec.example.com
```
**Expected:** DNSSEC scanner returns >= 1 CRITICAL finding (RSASHA1) for weak.example.com, 1 HIGH finding (unsigned zone) for unsigned.example.com, and 1 MEDIUM finding (NSEC) for nsec.example.com. ECDSAP256SHA256 zones produce no algorithm severity finding.



## Phase 25 — SAML/OIDC Profile (profile: simpla-samlphp)

| Port | Service | Certificate | Expected Finding | Severity |
|-----:|---------|-------------|-----------------|----------|
| 8880 | simpla-samlphp | RSA-1024 signing cert | Weak SAML signing certificate: RSA-1024 | CRITICAL |
| 8880 | simpla-samlphp | SHA-1 algorithm URI | SHA-1 algorithm URI detected in SAML metadata | HIGH |

**Scanner validation command:**
```
docker compose --profile simpla-samlphp up -d && sleep 10 && quirk scan --targets http://localhost:8880/simplesaml/saml2/idp/metadata.php
```
**Expected:** SAML scanner returns 1 CRITICAL finding for RSA-1024 signing certificate and optionally 1 HIGH finding if SHA-1 algorithm URI is present in metadata. Findings appear in the Identity tab (source="saml"), not the Findings tab.



## Phase 25 — Kerberos Profile (profile: samba-dc)

| Port | Service | Etype ID | Etype Name | Expected Finding | Severity |
|-----:|---------|---------|-----------|-----------------|----------|
| 88 | samba-dc | 23 | rc4-hmac | Kerberos weak etype: rc4-hmac | HIGH |
| 88 | samba-dc | 17 | aes128-cts-hmac-sha1-96 | Kerberos weak etype: aes128-cts-hmac-sha1-96 | HIGH |

**Scanner validation command:**
```
docker compose --profile samba-dc up -d && sleep 15 && quirk scan --targets localhost:88
```
**Expected:** Kerberos scanner returns >= 1 HIGH finding for RC4-HMAC (etype 23). AES-256 (etype 18/20) produces no weakness finding. Findings appear in the Identity tab (source="kerberos").
```

After appending, verify the file ends with the Kerberos section and the three new section
headings are present:
```bash
grep "## Phase 25" quantum-chaos-enterprise-lab/expected_results_v3.md
```
Should return exactly 3 lines.
  </action>

  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && grep -c "## Phase 25" quantum-chaos-enterprise-lab/expected_results_v3.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep "## Phase 25" quantum-chaos-enterprise-lab/expected_results_v3.md` returns exactly 3 matches:
      "Phase 25 — DNSSEC Profile", "Phase 25 — SAML/OIDC Profile", "Phase 25 — Kerberos Profile"
    - Each section contains a Markdown table with at least 2 data rows
    - Each section contains a `**Scanner validation command:**` fenced code block referencing
      one of: `--profile bind9`, `--profile simpla-samlphp`, `--profile samba-dc`
    - Each section contains a `**Expected:**` summary sentence
    - The DNSSEC section table includes RSASHA1 with severity CRITICAL and unsigned zone with HIGH
    - The SAML/OIDC section table includes RSA-1024 with severity CRITICAL
    - The Kerberos section table includes rc4-hmac (etype 23) with severity HIGH
    - The file's original content is intact: `grep "## Phase 4 — SSH-Weak Profile" quantum-chaos-enterprise-lab/expected_results_v3.md` still returns a match
    - No code files were modified: `git diff --name-only` shows only expected_results_v3.md
  </acceptance_criteria>

  <done>
    expected_results_v3.md appended with three identity chaos lab sections; NEW-ISSUE-3 closed;
    file committed to git.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → developer/QA | Plain Markdown file; no executable content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05 | Information Disclosure | expected_results_v3.md chaos lab port/profile details | accept | File documents chaos lab configuration for internal QA use. No production credentials or secrets. Port numbers are chaos lab ports (non-standard, non-production). Content is appropriate for a version-controlled test oracle. |

This is a documentation-only plan. No code, no imports, no attack surface changes.
</threat_model>

<verification>
After Task 1 completes:

1. Section count: `grep -c "## Phase 25" quantum-chaos-enterprise-lab/expected_results_v3.md` → 3
2. DNSSEC content: `grep "RSASHA1" quantum-chaos-enterprise-lab/expected_results_v3.md` → found
3. SAML content: `grep "RSA-1024" quantum-chaos-enterprise-lab/expected_results_v3.md` → found
4. Kerberos content: `grep "rc4-hmac" quantum-chaos-enterprise-lab/expected_results_v3.md` → found
5. Original content intact: `grep "SSH-Weak Profile" quantum-chaos-enterprise-lab/expected_results_v3.md` → found
6. Only doc file changed: `git diff --name-only` → only expected_results_v3.md
</verification>

<success_criteria>
Phase 25 Plan 03 is complete when:
- expected_results_v3.md contains three new ## Phase 25 sections (DNSSEC, SAML/OIDC, Kerberos)
- Each section has the correct table format, validation command, and expected outcome statement
- No code files were modified
- File committed to git
- NEW-ISSUE-3 is closed (STATE.md Deferred Items row can be removed in phase summary)
</success_criteria>

<output>
After completion, create `.planning/phases/25-identity-findings-accuracy/25-03-SUMMARY.md`
using the standard summary template at @$HOME/.claude/get-shit-done/templates/summary.md.
</output>
