---
phase: 201-score-lift-roadmap-re-frame
plan: 08
type: execute
wave: 5
depends_on: ["201-01", "201-02", "201-03", "201-04", "201-05", "201-06", "201-07"]
files_modified:
  - docs/report-interpretation.md
  - docs/operators-guide.md
  - docs/UAT-SERIES.md
  - .planning/HORIZON.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Report-Interpretation.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Operators-Guide.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-201-Score-Lift-Roadmap-Re-frame.md
  - .planning/phases/201-score-lift-roadmap-re-frame/201-VALIDATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [LIFT-01, LIFT-02, LIFT-03, LIFT-04, LIFT-05]

must_haves:
  truths:
    - "An operator reading docs/report-interpretation.md learns what the +N pts badge means, that it is a real second scoring pass rather than a heuristic, why some items carry no number, that the projection is one independent rescore and not a sum, and that none of it affects the readiness score"
    - "docs/report-interpretation.md records the console Migration Waves semantic change: the column now counts roadmap items per phase, not findings per severity bucket (BACK-51)"
    - "BACK-51's closure is recorded as an explicit decision in the HORIZON.md row — the CONTEXT requirement that it close by decision, not by accident"
    - "docs/UAT-SERIES.md carries a Series 201 whose every case has exactly one checked Result box, keeping the zero-undispositioned gate green (UATREC-04)"
    - "The Obsidian vault counterparts of every touched doc are re-synced in the same phase (LIVE-03) and a Phase 201 note exists with status complete"
    - "201-VALIDATION.md is closed green (nyquist_compliant: true, wave_0_complete: true, zero non-green rows) BEFORE the ROADMAP.md checkbox commit, so the pre-commit hook does not block the phase close"
    - "LIFT-01..LIFT-05 and the Phase 201 checkbox are hand-flipped with no mutating GSD verb invoked"
  artifacts:
    - path: "docs/report-interpretation.md"
      provides: "score-lift and projected-score interpretation plus the Migration Waves semantic change"
      contains: "Projected score if all items resolved"
    - path: "docs/UAT-SERIES.md"
      provides: "Series 201 covering per-item lift, aggregate projection, firewall, unification, and the four surfaces"
      contains: "Series 201"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-201-Score-Lift-Roadmap-Re-frame.md"
      provides: "Vault phase note with per-plan What Was Built"
  key_links:
    - from: "docs/report-interpretation.md"
      to: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Report-Interpretation.md"
      via: "vault re-sync of the same content below standard frontmatter"
      pattern: "Report-Interpretation"
    - from: ".planning/HORIZON.md BACK-51 row"
      to: ".planning/phases/201-score-lift-roadmap-re-frame/201-03-SUMMARY.md"
      via: "recorded closure decision citing the deletion and the replacement"
      pattern: "BACK-51"
---

<objective>
Discharge CLAUDE.md's Per-Phase Documentation Checklist and Mandatory Phase Completion Steps for Phase
201, record BACK-51's closure decision, and close the phase's planning artifacts by hand.

Purpose: this phase triggers two checklist rows — a new report section and a new dashboard UI feature —
and changes the meaning of an existing console table. It also carries an explicit CONTEXT obligation that
BACK-51 close by a recorded decision with a HORIZON.md row update. None of that is complete until an
operator can learn it from the docs and the vault mirrors it.

Output: two updated docs, UAT Series 201, an updated HORIZON row, four vault writes, a closed
201-VALIDATION.md, and hand-flipped ROADMAP/REQUIREMENTS checkboxes.

Hard constraints:
- Do NOT invoke `phase.complete`, `milestone.complete`, `requirements mark-complete`, or any mutating
  `state.*` / `roadmap.*` GSD verb. They write well-formed WRONG values on this machine
  (`phase.complete` marked a phase complete at 5/7 plans with an impossible `completed_plans`).
  Checkboxes are hand-edited text edits.
- Use plain `git add` + `git commit`; the `gsd-tools`/`gsd-sdk` `commit --files` wrapper misreports
  failure on this repo's gitignored `.planning/` paths. `.planning/HORIZON.md` is a tracked file —
  commit it by pathspec.
- Close `201-VALIDATION.md` BEFORE (or in the same commit as) the `.planning/ROADMAP.md` change — the
  pre-commit hook blocks ROADMAP commits over a non-green validation file (recurred on phases 157/158/159).
- Do NOT write a verification command that greps `201-VALIDATION.md` for the pending glyph: a row
  containing that glyph in its own command text can never reach zero. Verify by frontmatter key and a
  count of green rows.
- Never check a UAT Result box just to satisfy the gate. `GAP — no substitute coverage` is a valid,
  passing, honest disposition; a fabricated pytest citation is not.
- No version bump this phase — leave the `**Version:**` line in `docs/UAT-SERIES.md` alone.
- `requirements mark-complete` is specifically banned here: LIFT IDs are single-phase but the verb has
  no per-phase granularity and has falsely over-flipped multi-phase requirements twice.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/201-score-lift-roadmap-re-frame/201-CONTEXT.md
@.planning/phases/201-score-lift-roadmap-re-frame/201-UI-SPEC.md
@.planning/phases/201-score-lift-roadmap-re-frame/201-01-SUMMARY.md
@.planning/phases/201-score-lift-roadmap-re-frame/201-02-SUMMARY.md
@.planning/phases/201-score-lift-roadmap-re-frame/201-03-SUMMARY.md
@.planning/phases/201-score-lift-roadmap-re-frame/201-04-SUMMARY.md
@.planning/phases/201-score-lift-roadmap-re-frame/201-05-SUMMARY.md
@.planning/phases/201-score-lift-roadmap-re-frame/201-06-SUMMARY.md
@.planning/phases/201-score-lift-roadmap-re-frame/201-07-SUMMARY.md

<interfaces>
<!-- Source -> vault mapping (vault name is always "Digs"). Large files MUST be written directly -->
<!-- to the vault filesystem, never via the obsidian CLI `content=` parameter. -->
docs/report-interpretation.md -> /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Report-Interpretation.md
docs/operators-guide.md       -> /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Operators-Guide.md
docs/UAT-SERIES.md            -> /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md
phase note                    -> /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-201-Score-Lift-Roadmap-Re-frame.md

Vault frontmatter keys: project, type, status, source, updated.

<!-- UAT gate grammar (all three burned Phase 188) -->
- `**Result:**` must be ONE physical line with exactly one checked box (PASS / FAIL / SKIP)
- DEFERRED pytest citations need FULL class-scoped node IDs and must be `--collect-only` resolvable
- vitest citations from multi-test files cannot pass the zero-skip execution leg -> use GAP honestly

Gate commands:
  .venv/bin/python -m pytest -q tests/test_uat_zero_undispositioned_gate.py \
      tests/test_uat_disposition_integrity.py tests/test_error_codes_freshness.py

<!-- Locked copy to document verbatim -->
  (+N pts) · Projected score if all items resolved: {N}
  Advisory — this projection is a simulation and does not affect the readiness score.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document score-lift, the projection, and the Migration Waves semantic change</name>
  <files>docs/report-interpretation.md, docs/operators-guide.md</files>
  <read_first>
    - docs/report-interpretation.md (the existing per-section structure and how the roadmap section is described today; also how other score surfaces' honest-absence conventions are already explained, so the new passage matches the house voice)
    - docs/operators-guide.md (the section describing report/console output and the dashboard surfaces — find where the Migration Waves console table and the roadmap page are described today)
    - .planning/phases/201-score-lift-roadmap-re-frame/201-03-SUMMARY.md (the BACK-51 closure decision prose written at execution time — reuse it rather than re-deriving the semantics)
    - .planning/phases/201-score-lift-roadmap-re-frame/201-02-SUMMARY.md (the 9 modelable and 5 unmodelable item kinds as actually shipped — document what shipped, not what was planned)
    - .planning/phases/201-score-lift-roadmap-re-frame/201-05-SUMMARY.md and 201-07-SUMMARY.md (which surfaces carry which lines, and the Open-Question-1 scorecard decision)
  </read_first>
  <action>
`docs/report-interpretation.md`: add a score-lift subsection inside (or directly adjacent to) the roadmap
section explaining, in the document's existing voice:
- what `(+N pts)` means — the readiness score this scan would have if that one item were resolved,
  computed by re-running the real scoring function over a copy of the scan's evidence with that item's
  findings resolved. State explicitly that it is not a heuristic points table.
- why some items carry no number: their resolution changes no input the scoring model reads (name the
  5 kinds as shipped — the process/governance items and the two coverage/lifecycle items). State that a
  blank is "not measurable," not "worth zero."
- what `Projected score if all items resolved: N` means, and why it is smaller than the sum of the
  individual numbers: each subscore is capped, so two fixes can compete for the same headroom. Say this
  in operator language, not in terms of the code.
- that neither number affects the readiness score, quoting the advisory line verbatim.
- which surfaces show it: CLI roadmap markdown, the scorecard's top-3 NOW actions, HTML/PDF, DOCX, the
  dashboard roadmap detail panel and its Projected Score card, and the print view.
Also record the console Migration Waves change: the table's second column now counts roadmap ITEMS per
NOW/NEXT/LATER phase rather than findings per severity bucket, because one categorization system
(`build_phased_roadmap`) now feeds every surface. Cite Phase 201 / LIFT-04 / BACK-51 the way neighbouring
passages cite their phases.

`docs/operators-guide.md`: add a short operator-facing passage covering the same three points in brief
(what the badge is, why some items lack one, the advisory nature of the projection) plus the Migration
Waves column change, and point to `docs/report-interpretation.md` for the full explanation. Cite
Phase 201 / LIFT-01..LIFT-05.

Do not invent behaviour. Every documented claim must match what the plan SUMMARYs record as shipped.
  </action>
  <verify>
    <automated>grep -c "Projected score if all items resolved" docs/report-interpretation.md && grep -c "Migration Waves" docs/report-interpretation.md && grep -c "pts" docs/operators-guide.md && grep -c "does not affect the readiness score" docs/report-interpretation.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/report-interpretation.md` contains: the `(+N pts)` explanation naming a real rescore, the not-a-heuristic statement, the 5 unmodelable kinds named as shipped, the non-additivity explanation in operator language, the verbatim advisory line, the six-surface list, and the Migration Waves items-per-phase change
    - `docs/operators-guide.md` contains the brief version plus the Migration Waves change and a pointer to report-interpretation.md
    - Every claim is traceable to a plan SUMMARY; the SUMMARY for this plan lists each documented claim beside its source SUMMARY
    - The 5 unmodelable kinds listed in the docs match 201-02-SUMMARY's shipped `_DELTAS` complement exactly — verify by diffing the two lists, not by memory
    - No version string is changed in either file
  </acceptance_criteria>
  <done>An operator can learn what the numbers mean, what a blank means, and that neither touches the real score.</done>
</task>

<task type="auto">
  <name>Task 2: UAT Series 201 with honest dispositions</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (the most recent series block — copy its case structure verbatim: ID / Title / Maps to / What to test / Steps / Pass Criteria / Falsifiability / Result / Date / Tester / Notes; also the document header block you must update)
    - tests/test_uat_zero_undispositioned_gate.py (module docstring — what the gate parses and why GAP is a passing disposition)
    - tests/test_uat_disposition_integrity.py (module docstring — DEFERRED citation conventions and the substitute-proof execution leg, including why vitest citations cannot pass it)
    - all seven 201-NN-SUMMARY.md files (the evidence you are dispositioning against)
  </read_first>
  <action>
Append `## Series 201: Score-Lift Roadmap Re-frame (Phase 201 — v5.23)` after the last existing series,
with an intro paragraph naming LIFT-01 through LIFT-05.

Write one case per verifiable behavior, following the existing case template exactly:
- `UAT-201-01` — a roadmap item with resolvable findings shows a `+N pts` number equal to a real rescore
  with that item resolved. Maps to LIFT-01.
- `UAT-201-02` — changing the scoring calibration changes the lift values, proving they are computed and
  not a lookup table. Maps to LIFT-01.
- `UAT-201-03` — process/governance items show no number at all (not `0 pts`, not `N/A`). Maps to LIFT-01.
- `UAT-201-04` — the projected aggregate is smaller than the sum of the individual lifts on a scan where
  a subscore's cap binds. Maps to LIFT-02.
- `UAT-201-05` — an unassessed scan shows no lifts and no projection. Maps to LIFT-01/LIFT-02.
- `UAT-201-06` — the forward-projection firewall fails when a scoring or persistence module imports the
  projection module, and the projection touches no DB session and does not mutate the scan's evidence.
  Maps to LIFT-03.
- `UAT-201-07` — no projected value appears in the intelligence JSON's `score` block or in the API's
  score object; the displayed readiness score is unchanged by the presence of the projection.
  Maps to LIFT-03.
- `UAT-201-08` — CLI markdown, HTML, DOCX, and the dashboard agree on the same NOW/NEXT/LATER assignment
  for one scan, and `categorize_waves()` no longer exists. Maps to LIFT-04.
- `UAT-201-09` — the console Migration Waves table counts roadmap items per phase, matching the roadmap's
  own phase counts. Maps to LIFT-04.
- `UAT-201-10` — the dashboard roadmap detail panel shows the `+N pts` badge and the Projected Score card
  with the verbatim advisory, in the placement the UI-SPEC locks; absence renders nothing.
  Maps to LIFT-05.
- `UAT-201-11` — the same scan's per-item and aggregate numbers are identical on all four surfaces.
  Maps to LIFT-05.

Disposition each case honestly against the SUMMARY evidence. Cases with automated backend coverage get
SKIP with `DEFERRED — covered by <full class-scoped pytest node ID>` citing nodes in
`tests/test_score_lift.py`, `tests/test_forward_projection_firewall.py`,
`tests/test_roadmap_categorization_unification.py`, `tests/test_scan_roadmap_score_lift.py`,
`tests/test_score_lift_report_surfaces.py`, or `tests/test_score_lift_cross_surface_numbers.py`, plus the
originating SUMMARY. UAT-201-10's visual-placement aspect has no automated substitute — this repo's
render tests assert presence, not appearance, and vitest citations cannot pass the integrity gate's
execution leg — so disposition its appearance aspect honestly. If plan 201-06's human walkthrough was
approved by the operator, record that as PASS citing 201-06-SUMMARY's verbatim approval; otherwise record
`GAP — no substitute coverage` and note that a live operator walkthrough is the honest path.

Every `**Result:**` line must be ONE physical line with exactly one box checked. Add `**Date:**` and
`**Tester:**` lines in the existing style. Update the document header's `**Last Updated:**` block to
today's date with a one-sentence description of the Series 201 addition, following the header's
running-log style. Do NOT change the `**Version:**` line.

Then run the UAT gates. If the integrity gate rejects a citation, fix the citation or downgrade to an
honest GAP — never add an allowlist entry and never check a box to make a gate pass.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_uat_zero_undispositioned_gate.py tests/test_uat_disposition_integrity.py tests/test_error_codes_freshness.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Series 201' docs/UAT-SERIES.md` returns 1 and `grep -c '^### UAT-201-' docs/UAT-SERIES.md` returns 11
    - Every `**Result:**` line in the Series 201 block has exactly one `[x]` on one physical line
    - The three gate tests exit 0
    - Every DEFERRED citation is confirmed collectible via `.venv/bin/python -m pytest --collect-only -q <node-id>`, with the transcripts in the SUMMARY
    - The header's `**Last Updated:**` shows today's date; `**Version:**` is unchanged
    - `git status --porcelain tests/` shows no modified gate file — no allowlist or gate-code change
  </acceptance_criteria>
  <done>Series 201 exists, every case is honestly dispositioned, and all three gates are green.</done>
</task>

<task type="auto">
  <name>Task 3: BACK-51 HORIZON row, vault sync, phase note, validation close-out, and checkbox flips</name>
  <files>.planning/HORIZON.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Report-Interpretation.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Operators-Guide.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-201-Score-Lift-Roadmap-Re-frame.md, .planning/phases/201-score-lift-roadmap-re-frame/201-VALIDATION.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - CLAUDE.md (sections "Obsidian vault sync (LIVE-03)", "Mandatory Phase Completion Steps", "Chaos Lab Maintenance", and the `state.*` verb-integrity section's clause (f) standing gotchas)
    - .planning/HORIZON.md around the BACK-51 row (line ~46) and the 999.101 row — read the existing closed-row prose style (e.g. 999.105's Tier-1 CLOSED wording) and match it
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-200-Report-Branding-Templates.md (the immediately-preceding phase note — copy its structure and frontmatter exactly)
    - .planning/phases/201-score-lift-roadmap-re-frame/201-VALIDATION.md (the Per-Task Verification Map you must close row by row)
    - .planning/phases/200-report-branding-templates/200-07-SUMMARY.md (the close-out conventions this task repeats, including the `tail -n +9` vault-diff offset rationale)
    - all seven 201-NN-SUMMARY.md files (source material for the phase note's "What Was Built")
  </read_first>
  <action>
`.planning/HORIZON.md`: update the BACK-51 row to CLOSED, recording the decision in the row's prose the
way the 999.105 Tier-1 row records its closure: `build_phased_roadmap()` is the single categorization
system as of Phase 201 (LIFT-04); `categorize_waves()` was DELETED rather than adapted, because an unused
severity-bucketed categorizer is itself the second system; the console Migration Waves column changed
meaning from findings-per-severity to items-per-phase; 8 test files' patch decorators were removed in the
same change. Cite `201-03-SUMMARY.md`. Also update the 999.47/BACK-51 cross-reference row so it does not
still read OPEN. `.planning/HORIZON.md` is a tracked file — stage it by pathspec.

Sync the three touched docs to the vault by writing directly to the vault filesystem — never via the
obsidian CLI `content=` parameter. Prepend the standard frontmatter block (project `QU.I.R.K.`, type
`guide` or `reference`, status `active`, the repo-relative `source` path, `updated` = today) above the
source content, preserving that content byte-for-byte below the frontmatter.

Write the phase note at
`/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-201-Score-Lift-Roadmap-Re-frame.md` with
frontmatter (`status: complete`, `type: phase`, `source`, `updated`) and sections: Goal, Requirements
Covered (LIFT-01..LIFT-05), Success Criteria (the five from the ROADMAP), What Was Built (one subsection
per plan 201-01 through 201-08, sourced from the SUMMARYs — state the ADVISORY-02 firewall's RED
verification and the BACK-51 deletion explicitly, because those are the phase's load-bearing claims), and
a `[[Roadmap]]` link. Match the Phase 200 note's structure.

Chaos lab: this phase adds no Compose profile and no scanner signal. Confirm that explicitly rather than
assuming — grep the `expected_results_*.md` oracle files for any roadmap, wave, or score expectation that
the categorization change or the new lift lines could perturb, and record the finding verbatim in the
SUMMARY as "chaos lab checked, no oracle impact" (or the contrary finding with the offending file and line).

Before touching `.planning/ROADMAP.md`, close out `201-VALIDATION.md`. Fill/confirm one row per task
across all eight plans in its Per-Task Verification Map, confirm each row's Automated Command actually
passed during this phase (re-run it if the evidence is not already in a plan SUMMARY), then set that row's
Status cell to the green check glyph and its File Exists cell away from the Wave-0 marker. Set
`nyquist_compliant: true` and `wave_0_complete: true` in the frontmatter, flip the six Validation Sign-Off
checkboxes and `**Approval:**`, and record the Manual-Only row (dashboard lift badge and projected-stat
visual appearance) as either discharged by plan 201-06's approved operator walkthrough — citing it — or
outstanding-for-human-UAT. If any row's command did NOT pass, mark that row red and STOP; do not flip the
ROADMAP checkbox over a red validation row.

Make these edits as plain text edits to the specific cells. Do NOT write a verification command that
greps this file for the pending glyph — a row containing that glyph in its own command text can never
reach zero. Verify by frontmatter key and by a count of green rows.

Then hand-flip the Phase 201 checkbox in `.planning/ROADMAP.md` (both the phase-list entry at line ~164
and its plan checkboxes) and the LIFT-01..LIFT-05 checkboxes plus their status-table rows in
`.planning/REQUIREMENTS.md`. Text edits only — no `phase.complete`, no `milestone.complete`, no
`requirements mark-complete`, no mutating `state.*` verb. If you touch `.planning/STATE.md` at all, take a
pre-image first and diff it afterward against the two known corruption signatures (a garbled bold-field
code span; a dropped frontmatter key).

Commit with plain `git add` + `git commit`, with the `201-VALIDATION.md` change landing no later than the
`.planning/ROADMAP.md` change.
  </action>
  <verify>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-201-Score-Lift-Roadmap-Re-frame.md" && diff <(tail -n +9 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md") docs/UAT-SERIES.md > /dev/null && diff <(tail -n +9 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Report-Interpretation.md") docs/report-interpretation.md > /dev/null && grep -c "nyquist_compliant: true" .planning/phases/201-score-lift-roadmap-re-frame/201-VALIDATION.md && grep -c "CLOSED" .planning/HORIZON.md && echo CLOSEOUT_OK</automated>
  </verify>
  <acceptance_criteria>
    - The HORIZON BACK-51 row reads CLOSED with the decision prose (deletion not adaptation, the column's changed meaning, the 8-file cleanup) and cites `201-03-SUMMARY.md`; the 999.47/BACK-51 cross-reference row no longer reads OPEN
    - Each vault file's body below its frontmatter is byte-identical to its source. Verify with `diff <(tail -n +9 "<vault file>") <source>` — `+9`, not `+7`: the standard frontmatter block is 7 lines plus a blank separator, so the body starts at line 9. If you deviate from that block, recompute the offset with `awk '/^---$/{n++; next} n>=2' <vault file>` instead of hardcoding
    - All three vault doc files carry the five standard frontmatter keys with `updated` = today and `source` = the repo-relative path
    - `Phase-201-Score-Lift-Roadmap-Re-frame.md` exists with `status: complete`, one "What Was Built" subsection per plan (201-01 .. 201-08), names all five LIFT IDs, states the ADVISORY-02 RED verification and the BACK-51 deletion, and contains a `[[Roadmap]]` wikilink
    - The SUMMARY records the chaos-lab grep verbatim, command and output
    - `201-VALIDATION.md` frontmatter reads `nyquist_compliant: true` and `wave_0_complete: true`; its Per-Task Verification Map has one row per task across all eight plans with zero non-green Status cells (count green glyphs against the row count; do not grep for the pending glyph); the Manual-Only row is explicitly dispositioned
    - The `201-VALIDATION.md` edit is committed BEFORE or with the `.planning/ROADMAP.md` change
    - `.planning/ROADMAP.md` Phase 201 checkbox is `[x]` with its plan list checked; `.planning/REQUIREMENTS.md` shows LIFT-01..LIFT-05 checked with status-table rows updated to Phase 201 / Complete
    - `git log --oneline -8` shows no commit authored by a `phase.complete` / `milestone.complete` / `requirements mark-complete` invocation; `git diff` on `.planning/STATE.md` (if any) shows no garbled bold-field code span and no dropped frontmatter key
    - Full suites run once: `.venv/bin/python -m pytest -q -m ""` failing-node SET compared against the empty pre-phase baseline (compare SETS, never raw counts — chaos-lab collection moves with Docker health), and `cd src/dashboard && npm run test` green
  </acceptance_criteria>
  <done>Docs, UAT series, HORIZON, and vault are in sync; the phase note exists; BACK-51's closure is recorded as a decision; 201-VALIDATION.md is closed green; checkboxes are hand-flipped safely, in that order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation, UAT corpus, HORIZON ledger, and vault files only. No code, no inputs, no endpoints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-201-28 | Repudiation | UAT corpus integrity — a checked box that does not reflect real coverage | mitigate | Every DEFERRED citation resolves via `pytest --collect-only`; the visual-placement case takes an honest GAP or cites 201-06's verbatim operator approval; no allowlist or gate-code edit permitted |
| T-201-29 | Tampering | `.planning/STATE.md` / ROADMAP corruption by mutating GSD state verbs (TOOL-01/04/05 defect class and the `phase.complete` semantic class) | mitigate | No mutating verb invoked; checkboxes hand-edited; any STATE.md touch pre-imaged and signature-diffed |
| T-201-30 | Repudiation | Docs presenting the projected score as an achievable or real score, or presenting a blank as "zero value", misleading a client deliverable | mitigate | Acceptance criteria require the verbatim advisory line, the not-a-heuristic statement, and the explicit "not measurable, not worth zero" framing |
| T-201-31 | Repudiation | BACK-51 recorded as closed without a decision, the exact failure CONTEXT names ("closes with a recorded decision, not an accident") | mitigate | HORIZON row prose states what was deleted, what replaced it, what changed meaning, and cites the SUMMARY; the cross-reference row is corrected too |
| T-201-32 | Information disclosure | Vault sync copying content outside the QUIRK doc set | accept | Only the three named `docs/` files and one generated phase note are written, to the four paths in `<interfaces>` |
| T-201-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this plan |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q tests/test_uat_zero_undispositioned_gate.py tests/test_uat_disposition_integrity.py tests/test_error_codes_freshness.py` exits 0
- All three vault doc bodies match their sources byte-for-byte; the Phase 201 note exists with `status: complete`
- HORIZON's BACK-51 row reads CLOSED with the recorded decision; the cross-reference row is corrected
- `201-VALIDATION.md` reads `nyquist_compliant: true` / `wave_0_complete: true` with zero non-green rows, committed no later than the ROADMAP change
- `.venv/bin/python -m pytest -q -m ""` failing-node SET matches the empty pre-phase baseline; `npm run test` green
- No mutating GSD state verb was invoked
</verification>

<success_criteria>
- Per-Phase Documentation Checklist rows triggered by this phase (new report section, new dashboard UI
  feature) are actioned and synced to the vault
- UAT Series 201 exists with 11 honestly-dispositioned cases; all three gates green
- BACK-51 closes by an explicit recorded decision in HORIZON.md citing the SUMMARY
- Vault carries current Report-Interpretation, Operators-Guide, UAT-Series, and a Phase 201 note
- Chaos-lab no-impact finding recorded with evidence, not assumed
- `201-VALIDATION.md` closed green first, then ROADMAP and REQUIREMENTS checkboxes hand-flipped, all
  without touching a mutating GSD verb
</success_criteria>

<output>
Create `.planning/phases/201-score-lift-roadmap-re-frame/201-08-SUMMARY.md` when done. Include the
chaos-lab grep output verbatim, the UAT gate results, the `--collect-only` transcripts for every DEFERRED
citation, the claim-to-SUMMARY traceability list from Task 1, the HORIZON BACK-51 row before/after, the
list of vault files written, and the full-suite failing-node SET comparison.
</output>
