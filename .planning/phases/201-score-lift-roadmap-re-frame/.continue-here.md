---
context: phase
phase: 201-score-lift-roadmap-re-frame
task: 8
total_tasks: 8
status: in_progress
last_updated: 2026-09-12T11:42:10.682Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure on this repo.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **Every mutating GSD verb is unsafe on this machine** — `phase.complete`,
      `milestone.complete`, `requirements mark-complete`, and all `state.*` / `roadmap.*` write
      verbs produce syntactically well-formed WRONG values (`phase.complete` marked a phase
      complete at 5/7 plans with an impossible `completed_plans: 142`). Mitigation: every
      STATE.md / ROADMAP.md / REQUIREMENTS.md change is a hand text edit, taken under a
      pre-image + signature-diff. Never invoke the verb "just to save typing."
- [ ] CONSTRAINT: **The `gsd-tools` / `gsd-sdk` `commit --files` wrapper misreports failure** on
      this repo's gitignored `.planning/` paths — it aborts before committing even though
      `git add` on the tracked files actually succeeds. Mitigation: use plain `git add` +
      `git commit` by pathspec, always.
- [ ] CONSTRAINT: **`201-VALIDATION.md` must be green BEFORE (or in the same commit as) the
      `.planning/ROADMAP.md` checkbox change** — the pre-commit hook blocks a ROADMAP commit over
      a non-green validation file. This recurred on phases 157/158/159. Mitigation: close
      VALIDATION first; verify by frontmatter key + a count of green rows.
- [ ] CONSTRAINT: **Never grep `201-VALIDATION.md` for the pending glyph in a verification
      command** — a row containing that glyph in its own command text can never reach zero
      (self-referentially unclosable). Mitigation: verify by frontmatter key and green-row count.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Mutating GSD state verbs | Write well-formed but wrong values to STATE/ROADMAP/REQUIREMENTS; both known corruption signatures read CLEAN on this semantic class | blocking | Hand-edit only, under pre-image + signature-diff. The plan file's own `<objective>` re-states this as a hard constraint. |
| `commit --files` wrapper on `.planning/` | Reports failure on gitignored-but-tracked paths and aborts before the commit lands | blocking | Plain `git add` + `git commit` by pathspec |
| ROADMAP commit over non-green VALIDATION | Pre-commit hook blocks it; recurred on 157/158/159 | blocking | Close `201-VALIDATION.md` first or in the same commit |
| Self-referential pending-glyph grep | A verification command containing the glyph can never report zero | advisory | Verify by frontmatter key + green-row count |
| Checking a UAT Result box to satisfy the gate | Fabricated dispositions defeat the corpus-integrity gate's purpose | advisory | `GAP — no substitute coverage` is a valid, passing, honest disposition |
| `requirements mark-complete` | No per-phase granularity; has falsely over-flipped multi-phase requirements twice | blocking | Hand-flip the checkbox text |

<current_state>
Phase 201 is **7 of 8 plans complete**. The human-verify checkpoint that blocked the last two
sessions is **CLEARED** — the operator approved the 201-06 walkthrough on 2026-09-12 after a
hard-refresh, and that approval is now recorded, committed, and requirement-flipped.

This session did exactly two things: finalized 201-06 (SUMMARY disposition, `requirements-completed:
[LIFT-05]`, REQUIREMENTS.md hand-flip to `[x]`, consumption of the prior HANDOFF.json — all in
commit `0d1d3654`), then paused on a session limit.

**Nothing is blocked. No human action is outstanding.** The next session picks up plan 201-08 cold.
</current_state>

<completed_work>

Completed Plans (all committed):
- 201-01: ADVISORY-02 firewall + RED score-lift spec — Done (`5bb1ff6e`/`e3c2d2ef`)
- 201-02: `score_lift.py` — 9 deltas + one-rescore aggregate, 32/32 green — Done (`a267282c`)
- 201-03: BACK-51 — `categorize_waves` deleted, console waves re-derived, 8-file patch removal — Done (`153bbe1f`/`6a0786cb`/`78780fc4`)
- 201-04: Dashboard API — `RoadmapNode.score_lift` + `projected_score` + TS mirrors — Done (`1bb13767`/`876b3ba7`)
- 201-05: Content-model + writer/executive attachment + CLI markdown/scorecard — Done (`4e018afa`/`d76a1acc`)
- 201-06: roadmap.tsx lift badge + Projected Score card + print.tsx + rebuilt statics — **Done, all 3 tasks** (`c15ccde3`/`940a0c24`/`0d1d3654`)
- 201-07: HTML + DOCX rendering + four-surface numeric equality — Done (`caab97dc`/`d72ac958`)

Requirements flipped so far: LIFT-05 `[x]` (this session). LIFT-01..LIFT-04 are flipped by 201-08.
</completed_work>

<remaining_work>

- **201-08 (not started)** — the phase close-out. Its plan file is fully written. It must deliver:
  - `docs/report-interpretation.md`: what the `+N pts` badge means, that it is a real second
    scoring pass rather than a heuristic, why some items carry no number, that the projection is
    one independent rescore and **not a sum**, that none of it moves the readiness score, and the
    console Migration Waves semantic change (counts roadmap items per phase, not findings per
    severity bucket)
  - `docs/operators-guide.md`: the dashboard UI feature
  - `docs/UAT-SERIES.md`: Series 201, every case with exactly one checked Result box
  - `.planning/HORIZON.md`: BACK-51 closure recorded as an explicit **decision** row (a CONTEXT
    obligation — it must close by decision, not by accident)
  - 4 Obsidian vault writes (2 guides + UAT-Series + a Phase 201 note with `status: complete`)
  - `201-VALIDATION.md` closed green, **then** the ROADMAP/REQUIREMENTS hand-flips
- **post-execution chain (orchestrator's job, NOT 201-08's)** — code review + fix, then
  **`gsd-ui-review`** (required because `201-UI-SPEC.md` exists; 201-08's objective explicitly
  disclaims it and says do not mark the phase heading complete before it runs), then
  `gsd-verifier`, then the hand-written phase close (STATE/ROADMAP/PROJECT)
- **Phase 202** — discuss→plan→execute (last phase of v5.23)
- **v5.23 milestone lifecycle** — audit→complete→cleanup, all by hand
</remaining_work>

<decisions_made>

- The operator's "no badge appears" report was attributed to a **stale browser cache, not a
  defect** — now confirmed by the operator's own re-verification after Cmd+Shift+R. Root-caused
  via a fresh-tab reproduction showing the +9 pts badge working per UI-SPEC, with the served
  bundle hash verified fresh; Phase 195 hit the identical symptom and the identical fix. The
  mechanism: the dashboard serves content-hashed Vite statics, but `index.html` itself can be
  browser-cached, so a stale HTML keeps pointing at the previous bundle after a rebuild.
- 201-07 was run before 201-06 inside wave 4, because the walkthrough compares cross-surface
  numbers and every other surface had to exist first.
- The plan-checker's 1 blocker + 4 warnings were fixed by the orchestrator directly (RESOLVED
  markers, filename alignment, `depends_on` widening, `weights=None` pin, ui-review-is-orchestrator
  note) — doc-state fixes only, no plan redesign.
</decisions_made>

<blockers>
None. The previous session's only blocker (the 201-06 human-verify gate) is cleared.
</blockers>

## Required Reading (in order)

1. `.planning/phases/201-score-lift-roadmap-re-frame/201-08-PLAN.md` — the next unit of work; its
   `<objective>` block carries six hard constraints that have each burned a prior phase here
2. `.planning/phases/201-score-lift-roadmap-re-frame/201-06-SUMMARY.md` — records the operator's
   approval and what shipped on the dashboard surface
3. `.planning/phases/201-score-lift-roadmap-re-frame/201-CONTEXT.md` — the BACK-51-closes-by-decision
   obligation and the post-execution UI-review obligation both originate here
4. `CLAUDE.md` §Per-Phase Documentation Checklist and §Mandatory Phase Completion Steps — 201-08
   exists to discharge these

## Infrastructure State

- **Dashboard server**: was running on `http://127.0.0.1:8512` (PID 60636) against the canonical DB
  `./quirk-output/quirk.db` for the walkthrough. It will **not** survive this session. 201-08 is a
  docs/close-out plan and does not need it; restart only if re-verifying UI, with
  `.venv/bin/python run_scan.py serve --port 8512`.
- **Git**: working tree clean at `0d1d3654`. **85 unpushed commits on `main`** — batch-push still
  awaits operator go-ahead.
- **Python**: use `.venv/bin/python`, never bare `python` — the system interpreter lacks `sslyze`
  and silently produces 9 phantom connector failures in the full suite.

<context>
This is an autonomous `/gsd-autonomous` run for milestone v5.23 (Deliverable Experience, 4 phases).
Phases 199 and 200 are complete and operator-closed. Phase 201 adds a score-lift re-frame to the
remediation roadmap: each item carries a `+N pts` badge computed by a genuine second scoring pass,
and an aggregate "projected score if all items resolved" derived from **one** independent rescore
rather than a sum of the per-item deltas — the non-additivity is the whole point of the design, and
the docs plan is explicit that an operator must be able to learn that from the text.

The pause is purely a session limit. The work is in a clean, resumable state with the hardest part
(the blocking human gate) behind it.
</context>

<next_action>
Start with: read `201-08-PLAN.md` in full, then execute it. No human input is needed to begin.
</next_action>
