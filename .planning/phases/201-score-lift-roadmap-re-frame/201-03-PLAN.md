---
phase: 201-score-lift-roadmap-re-frame
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/reports/writer.py
  - tests/test_reports_writer.py
  - tests/test_cmvp_report_column.py
  - tests/test_cbom_vex.py
  - tests/test_report_template_sandbox.py
  - tests/test_report_injection_hardening.py
  - tests/test_report_branding_cli.py
  - tests/test_burndown_writer_load.py
  - tests/test_cbom_integration.py
  - tests/test_roadmap_categorization_unification.py
autonomous: true
requirements: [LIFT-04]

must_haves:
  truths:
    - "Only ONE categorization system survives: build_phased_roadmap(). categorize_waves() no longer exists anywhere in the codebase (LIFT-04 / BACK-51; CONTEXT 'BACK-51 unification')"
    - "The console Migration Waves table re-derives its rows from build_phased_roadmap()'s items, counting items per NOW/NEXT/LATER phase, with bucket labels and timeframes unchanged"
    - "A test proves CLI markdown, HTML context, DOCX phase-split, and the dashboard roadmap all agree on the same NOW/NEXT/LATER assignment for one fixture scan"
    - "All 8 test files that patched quirk.reports.writer.categorize_waves are updated in this same change, so no test fails at patch time with AttributeError (PATTERNS structural constraint (b))"
    - "The semantic change is recorded: the console column counts roadmap ITEMS per phase, not findings per severity bucket"
  artifacts:
    - path: "quirk/reports/writer.py"
      provides: "roadmap-derived console Migration Waves table; no categorize_waves symbol"
      contains: "Migration Waves"
    - path: "tests/test_roadmap_categorization_unification.py"
      provides: "one-categorization-source cross-surface assertion + a regression guard that categorize_waves stays deleted"
      min_lines: 60
  key_links:
    - from: "quirk/reports/writer.py console Migration Waves table"
      to: "roadmap_raw (build_phased_roadmap output, writer.py:533)"
      via: "items-per-phase counting with the _roadmap_markdown timeframe-fallback idiom"
      pattern: "roadmap_raw"
---

<objective>
Close BACK-51 (LIFT-04): delete the second, severity-bucketed categorization system and re-derive the
console "Migration Waves" table from the canonical `build_phased_roadmap()` items.

Purpose: two independently-derived answers to "what should I fix and when" both reach the operator in
the same report pass — confirmed live since Phase 189. This plan makes `build_phased_roadmap()` the
single source and records the closure as an explicit decision rather than an accident.

Output: `categorize_waves()` deleted, the console table re-derived, 8 test files' now-invalid patch
decorators mechanically removed, and a new test that pins one-categorization-source across all four
surfaces.

Hard constraints:
- `patch("quirk.reports.writer.categorize_waves", ...)` raises AttributeError at PATCH TIME, not
  assertion time. Every one of the 8 files must be updated in THIS change — a later plan cannot
  absorb them (PATTERNS structural constraint (b)).
- `tests/test_reports_writer.py`'s `_stub_roadmap` returns items with NO `phase` key and a PHASE label
  stored in `timeframe`. The new counting code must tolerate that or those tests crash on KeyError
  (Pitfall 3). Use `_roadmap_markdown`'s existing `r.get("timeframe") == tf or r.get("phase") == tf`
  idiom at writer.py:435-436.
- Do NOT keep `categorize_waves` as a deprecated unused symbol, and do NOT write an "adapter" that
  re-derives severity buckets. An unused severity-bucketed categorizer IS the second system BACK-51
  exists to kill (CONTEXT allows a thin adapter, RESEARCH recommends deletion — take deletion).
- Bucket labels and timeframes are unchanged: NOW / NEXT / LATER and "0-30 days" / "31-90 days" /
  "90+ days".
- This plan adds no score-lift rendering. Lift attachment lands in plan 201-05 (same file, later wave).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/201-score-lift-roadmap-re-frame/201-CONTEXT.md
@.planning/phases/201-score-lift-roadmap-re-frame/201-RESEARCH.md
@.planning/phases/201-score-lift-roadmap-re-frame/201-PATTERNS.md

<interfaces>
<!-- The canonical categorization output already in scope at writer.py:533 -->
  build_phased_roadmap(evidence, scoring) -> {
      "roadmap_version": str, "item_count": int,
      "phase_counts": {"NOW": int, "NEXT": int, "LATER": int},
      "items": [ {"phase", "title", "why", "owner_placeholder", "dependencies", "timeframe"} ],
  }
  quirk/intelligence/roadmap.py:7-8
      _PHASE_ORDER = {"NOW": 0, "NEXT": 1, "LATER": 2}
      _TIMEFRAME_BY_PHASE = {"NOW": "0-30 days", "NEXT": "31-90 days", "LATER": "90+ days"}

<!-- The symbol being deleted -->
  quirk/reports/writer.py:293  categorize_waves(findings)   # CRITICAL->NOW, HIGH->NEXT, else->LATER
  quirk/reports/writer.py:922  the Rich Table(title="Migration Waves") render site (sole consumer)

<!-- The 8 test files holding patch decorators on the deleted symbol (verified by grep at HEAD) -->
  tests/test_reports_writer.py (6 decorators + the _stub_waves helper)
  tests/test_cmvp_report_column.py, tests/test_cbom_vex.py, tests/test_report_template_sandbox.py,
  tests/test_report_injection_hardening.py, tests/test_report_branding_cli.py,
  tests/test_burndown_writer_load.py, tests/test_cbom_integration.py (3 sites)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete categorize_waves and re-derive the console Migration Waves table from roadmap items</name>
  <files>quirk/reports/writer.py</files>
  <read_first>
    - quirk/reports/writer.py lines 285-320 (the `categorize_waves` definition being deleted — read it to confirm nothing else in the module references it)
    - quirk/reports/writer.py lines 910-935 (the sole consumer: the Rich `Table(title="Migration Waves")` render site and its "Wave"/"Findings" columns)
    - quirk/reports/writer.py lines 430-450 (`_roadmap_markdown`'s `section(tf)` helper — the exact `r.get("timeframe") == tf or r.get("phase") == tf` fallback idiom to copy, which is what survives stub items lacking a `phase` key)
    - quirk/reports/writer.py lines 520-545 (`roadmap_raw = build_phased_roadmap(...)` at :533 — confirm it is in scope at the console render site)
    - quirk/intelligence/roadmap.py lines 1-10 (the canonical phase labels and timeframe strings — reuse these spellings, do not re-declare new literals if importing them is cleaner)
  </read_first>
  <action>
Delete the `categorize_waves` function from `quirk/reports/writer.py` entirely, including its
docstring. Grep the whole `quirk/` tree afterward to confirm zero remaining production references.

Replace the console render block: instead of `waves = categorize_waves(findings)`, count roadmap items
per phase from `roadmap_raw` (already in scope). Initialize an ordered `{"NOW": 0, "NEXT": 0, "LATER":
0}` counter, iterate `roadmap_raw.get("items", [])`, and resolve each item's phase using the tolerant
idiom: take `item.get("phase")` when it names one of the three phases, otherwise map
`item.get("timeframe")` through the phase/timeframe correspondence, otherwise skip the item. This must
not raise on items that carry only `title`/`why`/`timeframe`, and must correctly bucket a stub whose
`timeframe` holds a PHASE label rather than a timeframe string.

Rename the table's second column from "Findings" to "Items" — the number now means roadmap items per
phase, not findings per severity bucket. Keep the table title "Migration Waves", the "Wave" column, and
the row order NOW/NEXT/LATER. Keep the existing Rich styling arguments unchanged.

Record the BACK-51 closure as a source comment at the render site: one sentence stating that
`build_phased_roadmap()` is the single categorization system as of Phase 201 / LIFT-04, that
`categorize_waves()` was deleted, and that the column's meaning changed from findings-per-severity to
items-per-phase. This comment is the in-code half of "closes with a recorded decision, not an accident."

Do not change `findings` handling anywhere else, do not touch `_roadmap_markdown`, and do not add any
lift rendering here.
  </action>
  <verify>
    <automated>.venv/bin/python -m compileall -q quirk/reports/writer.py && test $(grep -rc "categorize_waves" quirk/ | grep -v ":0$" | wc -l) -eq 0 && .venv/bin/python -m pytest -q tests/test_reports_writer.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "categorize_waves" quirk/` returns nothing
    - `grep -n "Migration Waves" quirk/reports/writer.py` still returns exactly one site, whose second column header is "Items" and which reads from `roadmap_raw`
    - The phase resolution tolerates all three stub shapes — verified by a scratch run against: an item with `phase="NOW"`, an item with only `timeframe="NOW"` (the `_stub_roadmap` shape), and an item with only `timeframe="0-30 days"`; transcript in the SUMMARY
    - A source comment at the render site names BACK-51, LIFT-04, the deletion, and the column's changed meaning
    - `tests/test_reports_writer.py` passes (after Task 2's decorator removal if run together; if run before Task 2 it is expected to fail at patch time — state which order was used in the SUMMARY)
    - `git diff --stat quirk/reports/writer.py` shows a contained diff: the deleted function plus the render block, no incidental reformatting
  </acceptance_criteria>
  <done>One categorization system remains in production code, and the console table counts its items.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the 8 test files' patch decorators for the deleted symbol</name>
  <files>tests/test_reports_writer.py, tests/test_cmvp_report_column.py, tests/test_cbom_vex.py, tests/test_report_template_sandbox.py, tests/test_report_injection_hardening.py, tests/test_report_branding_cli.py, tests/test_burndown_writer_load.py, tests/test_cbom_integration.py</files>
  <read_first>
    - tests/test_reports_writer.py lines 95-140 (the full decorator stack and the `_stub_waves` helper — note that removing a `@patch` decorator ALSO requires removing its positional mock parameter from the test signature, in the correct bottom-up order, and any `mock_waves.side_effect = ...` assignment in the body)
    - each of the other 7 files at its `categorize_waves` line (found via `grep -rn "categorize_waves" tests/`) — read enough surrounding context in each to identify the decorator, its mock parameter position, and any body references
  </read_first>
  <action>
For each of the 8 files: remove the `@patch("quirk.reports.writer.categorize_waves", ...)` decorator
(or `patch(...)` context-manager entry), remove the corresponding mock parameter from the decorated
function's signature — remembering that stacked `@patch` decorators bind bottom-up, so deleting the
wrong parameter silently shifts every other mock — and remove any body statement that configures or
asserts on that mock. In `tests/test_reports_writer.py`, also delete the now-orphaned `_stub_waves`
helper.

Change nothing else in these files: no assertion edits, no fixture changes, no reformatting. These
stubs existed purely for hermeticity of a call that no longer happens; their removal is honest cleanup.

Confirm the parameter-shift hazard did not bite by running each touched file individually and reading
its output, not just the aggregate run — a mis-shifted mock frequently still passes some tests in the
file while breaking others.
  </action>
  <verify>
    <automated>test -z "$(grep -rn 'categorize_waves' tests/ | grep -v test_roadmap_categorization_unification)" && .venv/bin/python -m pytest -q tests/test_reports_writer.py tests/test_cmvp_report_column.py tests/test_cbom_vex.py tests/test_report_template_sandbox.py tests/test_report_injection_hardening.py tests/test_report_branding_cli.py tests/test_burndown_writer_load.py tests/test_cbom_integration.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "categorize_waves" tests/` returns only lines inside `tests/test_roadmap_categorization_unification.py` (the deliberate regression guard from Task 3)
    - `grep -rn "_stub_waves" tests/` returns nothing
    - All 8 files pass when run individually; the SUMMARY lists the 8 per-file pytest result lines separately, not one aggregate line
    - `git diff tests/` shows only decorator lines, mock parameters, mock-configuration statements, and the `_stub_waves` helper removed — no assertion or fixture text changed
  </acceptance_criteria>
  <done>No test references the deleted symbol, and no test silently regressed from a shifted mock parameter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin one-categorization-source across all four surfaces</name>
  <files>tests/test_roadmap_categorization_unification.py</files>
  <behavior>
    - For one fixture scan, the NOW/NEXT/LATER assignment per item title is identical across: `_roadmap_markdown`'s section grouping, the HTML template context's roadmap_now/next/later split, `docx_renderer`'s phase split of `ExecContent.roadmap_items`, and `routes/scan.py::_derive_roadmap`'s `RoadmapNode.phase`
    - `quirk.reports.writer` has no `categorize_waves` attribute (regression guard against reintroduction)
    - The console Migration Waves counts equal `build_phased_roadmap()`'s `phase_counts` for the same evidence
  </behavior>
  <read_first>
    - tests/test_cross_surface_parity.py (the existing presence-based cross-surface assertion style and its fixture-construction helpers — mirror its shape; do NOT edit that file, add a sibling)
    - quirk/reports/writer.py lines 430-450 (`_roadmap_markdown` grouping) and the console render site as rewritten in Task 1
    - quirk/reports/content_model.py lines 744-800 (`_enrich_roadmap_item` / sorting — how raw dicts become `RoadmapItem`s carrying `phase`)
    - quirk/reports/docx_renderer.py lines 360-380 (the phase split over `exec_content.roadmap_items`)
    - quirk/dashboard/api/routes/scan.py lines 1103-1184 (`_derive_roadmap` — how `phase` reaches `RoadmapNode`)
  </read_first>
  <action>
Create `tests/test_roadmap_categorization_unification.py`.

Build one shared evidence fixture that produces a roadmap spanning all three phases. Derive the
canonical mapping `{title: phase}` from `build_phased_roadmap(evidence, compute_readiness_score(evidence))`.
Then assert each surface reproduces it:
- CLI markdown: call `_roadmap_markdown` with the items and parse which phase heading each title falls
  under; compare to the canonical mapping.
- HTML: build `ExecContent` via `build_exec_content` and compute the same three-way split the template
  receives (`roadmap_now` / `roadmap_next` / `roadmap_later`); compare.
- DOCX: apply `docx_renderer`'s phase-split expression over `exec_content.roadmap_items`; compare.
- Dashboard: call `_derive_roadmap(evidence, score_raw)` and compare `{node.title: node.phase}`.

Add `test_categorize_waves_is_gone` asserting `not hasattr(quirk.reports.writer, "categorize_waves")`,
with a docstring naming BACK-51 and stating that reintroducing any second categorization system —
including a deprecated unused one — is the regression this test exists to catch.

Add `test_console_wave_counts_match_roadmap_phase_counts` comparing the counter logic's output against
`build_phased_roadmap()`'s own `phase_counts` dict for the same evidence.

Keep the assertions structural (phase assignment per title, and counts) — do not assert on rendered
visual order or styling; this repo's render tests assert presence, not appearance.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_roadmap_categorization_unification.py tests/test_cross_surface_parity.py tests/test_intelligence_roadmap.py</automated>
  </verify>
  <acceptance_criteria>
    - The file contains four surface comparisons (CLI markdown, HTML context split, DOCX split, dashboard `_derive_roadmap`) plus the two guard tests, and all pass
    - The fixture is verified to span all three phases: the SUMMARY records the canonical `{title: phase}` mapping and shows at least one title in each of NOW, NEXT, LATER (a fixture covering only one phase makes the agreement assertion nearly vacuous)
    - `test_categorize_waves_is_gone` fails if the symbol is restored — verified live by temporarily re-adding a trivial `def categorize_waves(findings): return {}` to writer.py, recording the failure, and reverting; `git diff quirk/` empty afterward
    - `tests/test_cross_surface_parity.py` and `tests/test_intelligence_roadmap.py` pass unmodified (`git status --porcelain` empty for both)
  </acceptance_criteria>
  <done>One categorization source is machine-pinned across four surfaces, and its reintroduction is guarded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Refactor of an internal categorization path plus test cleanup. No new inputs, endpoints, or rendering of untrusted data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-201-08 | Repudiation | Console table silently changing meaning (findings-per-severity -> items-per-phase) without the operator or the docs being told, misinforming a consulting deliverable | mitigate | Column renamed "Findings" -> "Items"; source comment records the semantic change; plan 201-08 documents it in `docs/report-interpretation.md` |
| T-201-09 | Tampering | A shifted `@patch` mock parameter after decorator removal silently disabling a security-relevant stub in `test_report_injection_hardening.py` / `test_report_template_sandbox.py` | mitigate | Each of the 8 files is run and reported individually, not as one aggregate line; diff restricted to decorator/parameter/mock lines |
| T-201-10 | Repudiation | A second categorization system creeping back in as a "deprecated" unused symbol, re-opening BACK-51 invisibly | mitigate | `test_categorize_waves_is_gone` with a live RED-verification, plus deletion rather than adapter |
| T-201-SC | Tampering | npm/pip/cargo installs | accept | No packages installed |
</threat_model>

<verification>
- `grep -rn "categorize_waves" quirk/` returns nothing; `grep -rn "categorize_waves" tests/` returns only the regression guard
- All 8 formerly-patching test files pass individually
- `.venv/bin/python -m pytest -q tests/test_roadmap_categorization_unification.py tests/test_reports_writer.py tests/test_cross_surface_parity.py tests/test_intelligence_roadmap.py` exits 0
- `.venv/bin/python -m compileall -q quirk` exits 0
</verification>

<success_criteria>
- `categorize_waves()` is deleted with no adapter and no deprecated stub
- The console Migration Waves table counts roadmap items per phase, with unchanged labels/timeframes
  and an honestly renamed column
- The 8 patch sites are removed in this same change with no mock-parameter shift regressions
- One-categorization-source is pinned across CLI markdown, HTML, DOCX, and dashboard, with the
  reintroduction guard RED-verified
</success_criteria>

<output>
Create `.planning/phases/201-score-lift-roadmap-re-frame/201-03-SUMMARY.md` when done. Include: the
BACK-51 closure decision in prose (what was deleted, what replaced it, what changed meaning — this text
is reused verbatim by plan 201-08's HORIZON.md row), the stub-shape scratch transcript, the 8 per-file
pytest result lines, the canonical `{title: phase}` fixture mapping, and the
`test_categorize_waves_is_gone` RED-verification transcript.
</output>
