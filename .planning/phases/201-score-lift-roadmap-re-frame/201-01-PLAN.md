---
phase: 201-score-lift-roadmap-re-frame
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_forward_projection_firewall.py
  - tests/test_score_lift.py
autonomous: true
requirements: [LIFT-01, LIFT-02, LIFT-03]

must_haves:
  truths:
    - "A machine firewall test exists that fails if any scoring/persistence module imports score_lift — the reverse of ADVISORY-01 (LIFT-03; CONTEXT 'Score-lift computation' ADVISORY-02 clause)"
    - "The firewall's AST walk is proven to actually detect a planted forbidden import (negative control), and is RED-verified live against a real guarded module"
    - "A unit suite exists that pins per-item lift as rescore(resolved)-current, aggregate as one independent rescore, and honest absence for unmodelable items — failing RED because quirk/intelligence/score_lift.py does not exist yet (LIFT-01/LIFT-02)"
    - "The existing ADVISORY-01 guard (tests/test_remediation_advisory_guard.py) stays green and unedited"
  artifacts:
    - path: "tests/test_forward_projection_firewall.py"
      provides: "ADVISORY-02 reverse-import ban over 8 guarded modules + negative control + runtime purity legs"
      min_lines: 120
      contains: "_GUARDED_MODULES"
    - path: "tests/test_score_lift.py"
      provides: "LIFT-01/02 behavioral spec written failing-first"
      min_lines: 120
      contains: "compute_projected_score"
  key_links:
    - from: "tests/test_forward_projection_firewall.py"
      to: "quirk/intelligence/scoring.py"
      via: "AST source read of each guarded module path"
      pattern: "scoring\\.py"
    - from: "tests/test_score_lift.py"
      to: "quirk.intelligence.score_lift"
      via: "import of the not-yet-existing module (RED)"
      pattern: "from quirk.intelligence.score_lift import"
---

<objective>
Land Wave 0: the ADVISORY-02 forward-projection firewall guard (FIRST, per the phase brief) and the
LIFT-01/02 behavioral unit suite, both authored before any production code exists.

Purpose: the firewall is this phase's credibility core — a projected score that ever leaks into a
real score surface is the exact failure mode Phase 201 exists to prevent. It must be machine-enforced
and RED-verified before `score_lift.py` is written, not retrofitted after.

Output: `tests/test_forward_projection_firewall.py` (AST reverse-ban + negative control + runtime
purity legs) and `tests/test_score_lift.py` (per-item / aggregate / absence spec). The AST legs and
negative control are GREEN on landing; the runtime-purity legs and the whole of
`test_score_lift.py` are RED until plan 201-02 lands the module.

Hard constraints:
- Do NOT create `quirk/intelligence/score_lift.py` in this plan. RED is the deliverable.
- Do NOT edit `tests/test_remediation_advisory_guard.py`. It stays green as-is; `score_lift.py` must
  NOT be added to its `_GUARDED_MODULES` (score_lift legitimately imports scoring — CONTEXT allows it).
- `quirk/db.py` is CRLF. The guard READS it only; never write it, never scripted-text-mode it.
- Test file names are fixed by `201-VALIDATION.md`'s quick-run command: `test_forward_projection_firewall.py`
  and `test_score_lift.py`. Do not rename them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/201-score-lift-roadmap-re-frame/201-CONTEXT.md
@.planning/phases/201-score-lift-roadmap-re-frame/201-RESEARCH.md
@.planning/phases/201-score-lift-roadmap-re-frame/201-PATTERNS.md

<interfaces>
<!-- The API the tests are written AGAINST. Plan 201-02 implements exactly this. -->
<!-- quirk/intelligence/score_lift.py (does not exist yet): -->
  compute_item_lifts(evidence, items, *, profile=None, weights=None) -> Dict[str, int]
      keyed by remediation slug; unmodelable slugs and lift <= 0 are ABSENT from the dict;
      returns {} when compute_readiness_score(evidence)["score"] is None (SCORE-06)
  compute_projected_score(evidence, items, *, profile=None, weights=None) -> Optional[int]
      ONE rescore of a single all-resolved deepcopy; None when base score is None

<!-- Already existing, called by the tests: -->
  quirk.intelligence.scoring.compute_readiness_score(evidence, *, profile=None, weights=None) -> Dict[str, Any]
      returns "score": Optional[int]  (int(round(...)) at scoring.py:425)
  quirk.intelligence.roadmap.build_phased_roadmap(evidence, scoring, *, min_items=6, max_items=12)
      -> {"roadmap_version", "item_count", "phase_counts", "items": [6-key dicts]}
  quirk.intelligence.remediation.slug_for_title(title) -> Optional[str]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ADVISORY-02 forward-projection firewall guard (RED-verified)</name>
  <files>tests/test_forward_projection_firewall.py</files>
  <read_first>
    - tests/test_remediation_advisory_guard.py (FULL file — the scaffold to mirror: module tuple, `path.exists()` skip for wave-order independence, companion `test_guarded_modules_all_exist`, the rising `checked >=` floor and its D-39 rationale, the AST walk at lines 61-82, and the negative-control docstring at lines 133-157 recording the Phase 179/180 RED protocol)
    - .planning/phases/201-score-lift-roadmap-re-frame/201-RESEARCH.md (§ADVISORY-02 Firewall Design — the 8-module guarded list, the real-score-surface inventory, and the 3-part runtime purity leg)
    - quirk/reports/writer.py lines 585-625 (the intelligence-JSON `"score"` allowlist block — the firewall asserts no projected/lift key ever appears inside it)
    - quirk/dashboard/api/schemas.py lines 477-600 (ScoreData / RoadmapNode / ScanLatestResponse — the firewall asserts ScoreData gains no projected field; `projected_score` lives at response top level, outside ScoreData)
  </read_first>
  <action>
Create `tests/test_forward_projection_firewall.py`, mirroring `tests/test_remediation_advisory_guard.py`'s
scaffold with the ban direction REVERSED: the guarded modules must never import score_lift.

Module-level: `_FORBIDDEN_MODULE_NAMES = frozenset({"quirk.intelligence.score_lift", "score_lift"})`
and `_imports_forbidden_module(source)` copied verbatim from the analog's AST walk (handles
`ast.Import` aliases, `ast.ImportFrom` module, and `ImportFrom` aliases — the `from quirk.intelligence
import score_lift` shape).

`_GUARDED_MODULES` — exactly these 8 paths, built off the repo root the same way the analog builds
`_QUIRK_INTELLIGENCE`: `quirk/intelligence/scoring.py`, `quirk/intelligence/evidence.py`,
`quirk/intelligence/trends.py`, `quirk/merge/scan.py`, `quirk/db.py`,
`quirk/intelligence/remediation_persist.py`, `quirk/intelligence/closure.py`,
`quirk/intelligence/burndown.py`. Do NOT guard `routes/scan.py`, `writer.py`, or `executive.py` —
they are the legitimate consumers.

Tests to write:
1. `test_scoring_and_persistence_modules_never_import_score_lift` — iterate `_GUARDED_MODULES`,
   `continue` on non-existent paths, count `checked`, assert `not _imports_forbidden_module(source)`
   with a failure message naming ADVISORY-02 and the path, then assert `checked >= 8` (the floor
   rises with the tuple, per the analog's D-39 note).
2. `test_guarded_modules_all_exist` — companion existence assertion so the skip idiom can never
   silently hollow the guard.
3. `test_negative_control_ast_walk_detects_a_real_forbidden_import` — three inline source fixtures:
   plain `import quirk.intelligence.score_lift` -> True; `from quirk.intelligence import score_lift`
   -> True; a clean fixture mentioning `quirk.intelligence.score_lift` only inside a comment and a
   string literal -> False.
4. `test_projection_api_does_not_mutate_input_evidence` — runtime purity leg 1: build a nested
   evidence fixture (`totals`, `protocol_counts`, `certificate_observations`, `cert_key_type_counts`,
   `scan_error`, `finding_severity_counts`), take a `copy.deepcopy` pre-image, call
   `compute_item_lifts` and `compute_projected_score`, assert the evidence dict is deep-equal to the
   pre-image (catches the shallow-copy leak, RESEARCH Pitfall 5).
5. `test_projection_api_touches_no_db_session` — runtime purity leg 2: monkeypatch
   `quirk.db.get_session` to a function that raises `AssertionError`, then call both projection
   functions and assert they still return successfully.
6. `test_base_score_is_invariant_across_projection` — runtime purity leg 3: capture
   `compute_readiness_score(evidence, profile=None)["score"]`, run both projection functions over the
   same dict, recompute, assert equality.
7. `test_real_score_surfaces_carry_no_projected_key` — read `quirk/reports/writer.py` source and
   assert that the intelligence-JSON `"score"` allowlist block contains no key whose name contains
   `projected` or `lift` (scope the scan to that block's source span by locating the block's opening
   marker, not the whole file); and assert via `ScoreData.model_fields` that no ScoreData field name
   contains `projected` or `lift`.

Tests 4-6 import from `quirk.intelligence.score_lift` at module scope, so this whole file is RED
until plan 201-02 — that is intended and must be recorded. Do NOT add a `pytest.importorskip` or a
`path.exists()` skip around those legs; a skipped purity leg is not a passing one.

RED-verify the AST guard live, using the analog's recorded protocol: temporarily add
`import quirk.intelligence.score_lift` to `quirk/intelligence/scoring.py`, run test 1, record the
failure output verbatim in the SUMMARY, then revert the edit and confirm `git diff quirk/` is empty.
Do NOT use `quirk/db.py` as the RED-verification target (CRLF hazard) — use `scoring.py`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_forward_projection_firewall.py -k "never_import or all_exist or negative_control or real_score_surfaces" && .venv/bin/python -m pytest -q tests/test_remediation_advisory_guard.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "quirk/db.py\|db.py" tests/test_forward_projection_firewall.py` > 0 and the file contains exactly 8 entries in `_GUARDED_MODULES` (count the tuple members)
    - The 4 static legs (tests 1, 2, 3, 7) pass; the 3 runtime legs (tests 4, 5, 6) fail with `ModuleNotFoundError`/`ImportError` for `quirk.intelligence.score_lift` — the SUMMARY records the exact RED node IDs and error text
    - `assert checked >= 8` (not `>= 5`) is present — the floor matches the tuple length
    - The negative control asserts both True cases AND the prose-only False case
    - RED-verification transcript in the SUMMARY: the failure message produced by the temporary forbidden import in `quirk/intelligence/scoring.py`, followed by `git diff --stat quirk/` showing no residue
    - `tests/test_remediation_advisory_guard.py` passes and `git status --porcelain tests/test_remediation_advisory_guard.py` is empty
    - `quirk/db.py` is unmodified: `git status --porcelain quirk/db.py` is empty
  </acceptance_criteria>
  <done>The reverse firewall exists, its detector is proven to detect, and its runtime legs are honestly RED pending the module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LIFT-01/02 behavioral unit suite, failing-first</name>
  <files>tests/test_score_lift.py</files>
  <behavior>
    - Per-item: for an evidence fixture with expired certs, `compute_item_lifts` returns a strictly positive int for slug `expired-certificates` equal to `rescore(expired_count=0) - base`, computed independently in the test
    - Realness (not a table): re-running with a different `weights` mapping changes the lift value for the same slug — a heuristic table could not do this
    - Honest absence: `tls-enum-coverage`, `mtls-lifecycle-operations`, `assign-owners-and-slas`, `automate-evidence-refresh`, `crypto-governance-review` never appear as keys; no key ever maps to 0 or a negative number
    - Aggregate: `compute_projected_score` equals an independently-computed single `compute_readiness_score` over one all-resolved deepcopy — exact equality, not approximate
    - Non-additivity (LIFT-02's stated reason): on a clamp-binding fixture, `sum(per-item lifts) > (projected - base)` strictly
    - SCORE-06: an evidence fixture yielding `score is None` produces `{}` lifts and `None` projection
    - Determinism: two identical calls return equal results
  </behavior>
  <read_first>
    - tests/test_intelligence_roadmap.py (the fixture-dict-in / dict-out pure-function test style to mirror, including `test_output_is_deterministic`, AND lines 42-70's exact 6-key `set(item.keys())` assertion — the reason lifts attach outside `build_phased_roadmap`; this file must stay green unedited)
    - quirk/intelligence/scoring.py lines 150-270 and 390-440 (the 25-point `score_cap` clamp in `_apply_weighted_impacts`, the assessed-domain predicates, the exclude-and-rescale headline, and `int(round(...))` — you need these to build a fixture where the clamp actually binds)
    - quirk/intelligence/roadmap.py lines 99-160 (the per-item triggers — your fixture's evidence must actually trigger the items whose lifts you assert)
    - quirk/intelligence/remediation.py lines 32-90 and 151-158 (`REMEDIATION_KIND_SLUGS`, `REMEDIATION_EXCLUDED_TITLES`, `slug_for_title`)
    - .planning/phases/201-score-lift-roadmap-re-frame/201-RESEARCH.md (§Evidence-Delta Map — treat the 9 modelable / 5 absent rows as the spec; §Aggregate Projection for the clamp-fixture recipe)
  </read_first>
  <action>
Create `tests/test_score_lift.py` importing `compute_item_lifts` and `compute_projected_score` from
`quirk.intelligence.score_lift` (RED — module does not exist yet).

Build module-level fixture helpers in the style of `tests/test_intelligence_roadmap.py`'s `_evidence()`:
- `_evidence()` — a healthy-but-flawed scan that triggers several modelable items at once:
  non-zero `plaintext_http_count` / `http_on_tls_port_count`, `certificate_observations` with
  `expired_count` / `expiring_count` / `self_signed_count` / `certs_observed`,
  `finding_severity_counts` with HIGH/CRITICAL/LOW, `protocol_counts` with UNKNOWN plus at least one
  identity/DAR/motion protocol key so `domains_assessed > 0`, `cert_key_type_counts` with RSA>0 and
  ECDSA==0, `scan_error` with a rate, and `totals` with endpoints/findings.
- `_clamp_binding_evidence()` — same shape but with penalties inside ONE subscore deliberately
  exceeding its 25-point headroom (large `plaintext_http_count` + `http_on_tls_port_count` against
  few endpoints), so per-item lifts computed in isolation each claim headroom the aggregate cannot
  grant twice.
- `_unassessed_evidence()` — zero endpoints, no `certs_observed`, no protocol keys, so
  `compute_readiness_score` returns `score: None` / `rating: "NOT_ASSESSED"`.
- `_items(evidence)` — `build_phased_roadmap(evidence, compute_readiness_score(evidence))["items"]`,
  so the test consumes real roadmap output rather than hand-written item dicts.

Write one test per `<behavior>` bullet. Compute every expected value INDEPENDENTLY inside the test by
deep-copying the fixture, applying the mutation from RESEARCH's Evidence-Delta Map by hand, and
calling `compute_readiness_score` — never by calling the module under test twice. For the realness
test, pass a `weights` mapping that changes the weight of the subscore the asserted slug moves, and
assert the two lifts differ.

Also assert the absence contract as a property, not just by example: for every slug present in the
returned dict, `value > 0` and `isinstance(value, int)`; and the returned key set is a subset of the
9 modelable slugs named in the Evidence-Delta Map.

Add a test asserting `build_phased_roadmap`'s items are UNCHANGED by lift computation — the 6-key
set assertion from `test_intelligence_roadmap.py:52-56`, re-asserted here after calling
`compute_item_lifts(evidence, items)`, proving attachment happens outside the builder (the structural
constraint from 201-PATTERNS).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_score_lift.py 2>&1 | tail -5; .venv/bin/python -m pytest -q tests/test_intelligence_roadmap.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_score_lift.py` collects successfully in the sense that pytest reports a collection/import error naming `quirk.intelligence.score_lift` — the SUMMARY records that exact RED output as the failing-first evidence
    - The file contains at least one test per `<behavior>` bullet (7 behaviors) plus the 6-key roadmap-invariance test — count the `def test_` occurrences and name each in the SUMMARY
    - Every expected lift/projection value is derived in-test from a hand-applied evidence mutation plus `compute_readiness_score`; `grep -c "compute_readiness_score" tests/test_score_lift.py` > 3
    - `_clamp_binding_evidence` is verified to actually bind: the SUMMARY records a scratch run showing the un-clamped penalty sum for the targeted subscore exceeds 25 (if it does not, the fixture is retuned until it does — a non-binding fixture makes the LIFT-02 inequality vacuous)
    - `tests/test_intelligence_roadmap.py` passes unmodified: `git status --porcelain tests/test_intelligence_roadmap.py` is empty
    - No `pytest.importorskip`, no `path.exists()` skip, and no `xfail` marker anywhere in the file
  </acceptance_criteria>
  <done>The LIFT-01/02 contract is pinned as executable spec and fails for the right reason: the module does not exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| projected score -> real score surface | The phase's own named threat boundary. Nothing computed from synthetic evidence may cross into `ScoreData`, the intelligence-JSON `"score"` block, the writer compat `score` dict, or `ScanLatestResponse.score`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-201-01 | Tampering | Projected/simulated score value reaching a real score surface | mitigate | This plan's AST reverse-ban over 8 guarded modules + the real-score-surface assertion (no `projected`/`lift` key in the intelligence-JSON `score` allowlist or `ScoreData.model_fields`) |
| T-201-02 | Tampering | Projection silently mutating the caller's live evidence dict, moving the real score computed later | mitigate | Runtime purity leg 1: deep-equality pre-image assertion around both projection calls |
| T-201-03 | Repudiation | A guard that cannot actually detect a violation (vacuously green AST walk, or a hollowed guard after a file rename) | mitigate | Negative control with three fixtures + `test_guarded_modules_all_exist` + `checked >= 8` floor + live RED-verification against `scoring.py` recorded in the SUMMARY |
| T-201-04 | Tampering | Projection reaching a DB session and persisting | mitigate | Runtime purity leg 2: monkeypatched `quirk.db.get_session` raises; projection must still succeed |
| T-201-SC | Tampering | npm/pip/cargo installs | accept | No packages installed — RESEARCH §Package Legitimacy Audit records zero install rows for this phase |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q tests/test_forward_projection_firewall.py -k "never_import or all_exist or negative_control or real_score_surfaces"` exits 0
- `.venv/bin/python -m pytest -q tests/test_remediation_advisory_guard.py` exits 0 with the file unmodified
- `.venv/bin/python -m pytest -q tests/test_score_lift.py` fails on `quirk.intelligence.score_lift` import, transcript recorded
- `git status --porcelain quirk/` is empty — no production code written by this plan
</verification>

<success_criteria>
- ADVISORY-02 firewall exists with 8 guarded modules, a rising floor, an existence companion, a
  three-fixture negative control, and three runtime purity legs
- The AST detector is RED-verified live against `quirk/intelligence/scoring.py` and reverted cleanly
- `tests/test_score_lift.py` pins LIFT-01/02 behavior with independently-computed expectations and a
  clamp-binding fixture proven to bind
- ADVISORY-01 and the roadmap key-set test are green and untouched
</success_criteria>

<output>
Create `.planning/phases/201-score-lift-roadmap-re-frame/201-01-SUMMARY.md` when done. Include: the
live RED-verification transcript (forbidden import planted in scoring.py, failure text, revert
proof), the exact RED node IDs and error text for the runtime legs and `test_score_lift.py`, the
clamp-binding scratch calculation, and the `def test_` inventory.
</output>
