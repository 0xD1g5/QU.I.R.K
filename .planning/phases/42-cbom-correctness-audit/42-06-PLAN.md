---
phase: 42-cbom-correctness-audit
plan: 06
type: execute
wave: 2
depends_on: [42-01, 42-02, 42-03, 42-04, 42-05]
files_modified:
  - docs/UAT-SERIES.md
autonomous: true
requirements: [CBOM-01, CBOM-02, CBOM-03, CBOM-04]
tags: [docs, uat, obsidian, phase-closeout]

must_haves:
  truths:
    - "`docs/UAT-SERIES.md` has new UAT-42-01..04 rows (one per CBOM-01/02/03/04 test slice) with `**Last Updated:**` date refreshed"
    - "Vault file `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` is byte-identical to `docs/UAT-SERIES.md` body (with the standard frontmatter prepended) per CLAUDE.md sync workflow"
    - "Obsidian phase note `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-42-CBOM-Correctness-Audit.md` exists with frontmatter (`status: complete`, `type: phase`, `source: .planning/phases/42-cbom-correctness-audit/`, `updated: 2026-04-30`) + Goal + Requirements Covered + Success Criteria + What Was Built (one subsection per plan, sourced from each `42-NN-SUMMARY.md`) + `[[Roadmap]]` link"
    - "`python -m compileall quirk/ tests/` exits 0"
    - "Full pytest suite (`pytest`) exits 0"
    - "Phase closeout commit covers `docs/UAT-SERIES.md` per CLAUDE.md Mandatory Phase Completion Steps step 4"
  artifacts:
    - path: "docs/UAT-SERIES.md"
      provides: "UAT-42-01..04 test rows for CBOM correctness audit"
      contains: "UAT-42-01"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-42-CBOM-Correctness-Audit.md"
      provides: "Obsidian phase note"
      contains: "status: complete"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
      provides: "Vault mirror of UAT-SERIES.md"
      contains: "UAT-42-01"
  key_links:
    - from: "docs/UAT-SERIES.md"
      to: "tests/test_cbom_schema_validation.py / test_cbom_classifier_coverage.py / test_cbom_motion_golden.py / test_cbom_skip_lists.py"
      via: "UAT-42-NN rows reference the pytest invocation that proves them"
      pattern: "UAT-42-0"
    - from: "Vault Phase-42 note"
      to: ".planning/phases/42-cbom-correctness-audit/42-{01..05}-SUMMARY.md"
      via: "What Was Built subsections sourced from each plan summary"
      pattern: "Phase-42-CBOM-Correctness-Audit"
---

<objective>
Close out Phase 42 per CLAUDE.md Mandatory Phase Completion Steps:
1. Update `docs/UAT-SERIES.md` with UAT-42-01..04 rows (one per requirement) and refresh `**Last Updated:**`.
2. Sync UAT-SERIES.md to the Obsidian vault filesystem at `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` with the standard frontmatter prepended.
3. Write the Phase 42 Obsidian phase note at `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-42-CBOM-Correctness-Audit.md`.
4. Run final `python -m compileall` and full `pytest` suite to confirm green.
5. Commit `docs/UAT-SERIES.md` (the only repo file this plan touches — vault writes are filesystem-only).

Purpose: Phase is not complete until docs + Obsidian + UAT-SERIES are aligned and committed (per CLAUDE.md user-instruction memory).

Output: 1 modified repo file (`docs/UAT-SERIES.md`), 2 vault files written, 1 commit on the repo file, full pytest green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-cbom-correctness-audit/42-CONTEXT.md
@.planning/phases/42-cbom-correctness-audit/42-01-SUMMARY.md
@.planning/phases/42-cbom-correctness-audit/42-02-SUMMARY.md
@.planning/phases/42-cbom-correctness-audit/42-03-SUMMARY.md
@.planning/phases/42-cbom-correctness-audit/42-04-SUMMARY.md
@.planning/phases/42-cbom-correctness-audit/42-05-SUMMARY.md
@CLAUDE.md
@docs/UAT-SERIES.md

<interfaces>
CLAUDE.md Mandatory Phase Completion Steps (the spec for this plan):

```markdown
### 1. Create Obsidian Phase Note
Write to: /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-NN-<Slug>.md
Format: frontmatter (status: complete, type: phase, source, updated) +
        Goal + Requirements Covered + Success Criteria + What Was Built
        (one subsection per plan, sourced from SUMMARY.md files) +
        [[Roadmap]] link.

### 2. Update docs/UAT-SERIES.md
- New features added: add new test cases to the relevant series
- Update **Last Updated:** date at the top

### 3. Sync UAT-SERIES.md to Obsidian
Write directly to vault filesystem (file is too large for CLI content= parameter):
  printf "---\nproject: QU.I.R.K.\ntype: reference\n..." > /tmp/uat_vault.md
  cat docs/UAT-SERIES.md >> /tmp/uat_vault.md
  cp /tmp/uat_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"

### 4. Commit docs/UAT-SERIES.md
node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit \
  "docs(phase-42): update UAT-SERIES.md" --files docs/UAT-SERIES.md
```

Existing Phase 41 closeout note as a structural template:
- `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-41-CI-Stability-Scanner-Robustness.md` (read as analog before writing Phase-42 note)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/UAT-SERIES.md with UAT-42-01..04 rows</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (full — to find the right series anchor for CBOM-related UAT rows; if a "Phase 35 / CBOM" series exists, add UAT-42-NN rows there; otherwise create a new "Phase 42 / CBOM Correctness" subsection following the existing per-phase pattern)
    - .planning/phases/42-cbom-correctness-audit/42-01-SUMMARY.md through 42-05-SUMMARY.md (so the UAT pass criteria reference the actual artifacts produced)
    - The previous Phase 41 closeout (UAT-41-01..04 rows) for row-format reference
  </read_first>
  <action>
    Add four new UAT rows under a "Phase 42 / CBOM Correctness Audit" heading (or under the existing CBOM series if one is already established — match prior precedent). Use the row format already established in `docs/UAT-SERIES.md`. Suggested rows:

    - **UAT-42-01 — CBOM JSON+XML schema validation (CBOM-01)**
      - Pass criteria: `pytest tests/test_cbom_schema_validation.py -x -v` exits 0; every shipped chaos lab profile parametrize case shows JSON and XML validating against CycloneDX 1.6 with `is None`; the drift sentinel `test_parametrize_set_matches_docker_compose_profiles` passes.

    - **UAT-42-02 — Classifier coverage gate + report (CBOM-02)**
      - Pass criteria: `pytest tests/test_cbom_classifier_coverage.py -x` exits 0; `docs/cbom-classifier-coverage.md` exists, contains `# CBOM Classifier Coverage Report` and a `| Algorithm Name |` table; re-running `REGEN_CBOM_COVERAGE=1 pytest tests/test_cbom_classifier_coverage.py::test_regenerate_coverage_report -s` produces empty `git diff` on the report.

    - **UAT-42-03 — Shape goldens + CHANGELOG (CBOM-03)**
      - Pass criteria: `pytest tests/test_cbom_motion_golden.py -x -v` shows 5 snapshot tests passing (email + broker + pki + vault + saml); `tests/fixtures/cbom/CHANGELOG.md` contains a `Phase 42` entry naming the three new fixtures; `git diff tests/fixtures/cbom/expected_email_cbom.json tests/fixtures/cbom/expected_broker_cbom.json` is empty.

    - **UAT-42-04 — Skip-list unit tests (CBOM-04)**
      - Pass criteria: `pytest tests/test_cbom_skip_lists.py -x -v` exits 0 with at least 12 parametrized cases (3 motion + 9 DAR) plus 1 sanity guard; `from quirk.cbom.builder import MOTION_PLAINTEXT_PROTOCOLS, DAR_SKIP_PROTOCOLS` succeeds.

    Update the `**Last Updated:** YYYY-MM-DD` line at the top of `docs/UAT-SERIES.md` to today's date (2026-04-30).
  </action>
  <verify>
    <automated>grep -q 'UAT-42-01' docs/UAT-SERIES.md &amp;&amp; grep -q 'UAT-42-02' docs/UAT-SERIES.md &amp;&amp; grep -q 'UAT-42-03' docs/UAT-SERIES.md &amp;&amp; grep -q 'UAT-42-04' docs/UAT-SERIES.md &amp;&amp; grep -q '2026-04-30' docs/UAT-SERIES.md</automated>
  </verify>
  <acceptance_criteria>
    - All four UAT rows present
    - `**Last Updated:**` is `2026-04-30`
    - Each row references the exact pytest invocation a reviewer can run
    - Row format matches existing UAT-NN-NN style
  </acceptance_criteria>
  <done>
    UAT-SERIES.md updated; ready for vault sync and commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sync UAT-SERIES.md to vault, write Phase-42 Obsidian note, run final tests, commit</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-42-CBOM-Correctness-Audit.md</files>
  <read_first>
    - CLAUDE.md §"Mandatory Phase Completion Steps" (the verbatim sync command and frontmatter spec)
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-41-CI-Stability-Scanner-Robustness.md (structural template for the new phase note)
    - All five phase summaries: .planning/phases/42-cbom-correctness-audit/42-{01..05}-SUMMARY.md (for "What Was Built" content)
    - .planning/ROADMAP.md §"Phase 42" (Goal + Requirements + Success Criteria text)
  </read_first>
  <action>
    Step 1 — Sync UAT-SERIES.md to vault (per CLAUDE.md Step 3):
    ```bash
    printf "---\nproject: QU.I.R.K.\ntype: reference\nstatus: active\nsource: docs/UAT-SERIES.md\nupdated: 2026-04-30\n---\n\n" > /tmp/uat_vault.md
    cat docs/UAT-SERIES.md >> /tmp/uat_vault.md
    cp /tmp/uat_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
    ```

    Step 2 — Write the Phase 42 Obsidian note. Use the Write tool directly (NOT the obsidian CLI — file is large and the Phase 41 precedent uses filesystem write per CLAUDE.md). Path: `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-42-CBOM-Correctness-Audit.md`.

    Required sections (in order):

    ```markdown
    ---
    project: QU.I.R.K.
    type: phase
    status: complete
    source: .planning/phases/42-cbom-correctness-audit/
    updated: 2026-04-30
    ---

    # Phase 42 — CBOM Correctness Audit

    ## Goal
    <copy verbatim from .planning/ROADMAP.md §"Phase 42">

    ## Requirements Covered
    - CBOM-01 — CycloneDX 1.6 schema validation gate
    - CBOM-02 — classifier coverage report + UNKNOWN-fallback gate
    - CBOM-03 — shape goldens (pki / vault / saml) + CHANGELOG.md
    - CBOM-04 — Pass-2 / Pass-3 skip-list unit tests

    ## Success Criteria
    <copy verbatim from .planning/ROADMAP.md §"Phase 42 Success Criteria">

    ## What Was Built

    ### Plan 01 — Wave 0 prerequisites
    <one paragraph sourced from 42-01-SUMMARY.md>

    ### Plan 02 — Schema validation harness
    <from 42-02-SUMMARY.md>

    ### Plan 03 — Shape goldens
    <from 42-03-SUMMARY.md>

    ### Plan 04 — Classifier coverage gate + report
    <from 42-04-SUMMARY.md>

    ### Plan 05 — Skip-list unit tests
    <from 42-05-SUMMARY.md>

    ## Artifacts
    - `pyproject.toml` — `cyclonedx-python-lib[validation]>=11.7.0,<12`
    - `quirk/cbom/builder.py` — `MOTION_PLAINTEXT_PROTOCOLS` + `DAR_SKIP_PROTOCOLS`
    - `quirk/cbom/classifier.py` — closed `_ALGORITHM_TABLE` gaps
    - `tests/test_cbom_schema_validation.py` (new)
    - `tests/test_cbom_classifier_coverage.py` (new)
    - `tests/test_cbom_skip_lists.py` (new)
    - `tests/test_cbom_motion_endpoints.py` (3 new endpoint synthesizers)
    - `tests/test_cbom_motion_golden.py` (3 new snapshot tests)
    - `tests/fixtures/cbom/expected_pki_cbom.json` (new)
    - `tests/fixtures/cbom/expected_vault_cbom.json` (new)
    - `tests/fixtures/cbom/expected_saml_cbom.json` (new)
    - `tests/fixtures/cbom/CHANGELOG.md` (new)
    - `docs/cbom-classifier-coverage.md` (new, generated)
    - `docs/UAT-SERIES.md` (UAT-42-01..04)

    ## Links
    - [[Roadmap]]
    - [[Requirements]]
    - [[UAT-Series]]
    ```

    Step 3 — Final test run before committing:
    ```bash
    python -m compileall quirk/ tests/
    pytest
    ```
    Both must exit 0.

    Step 4 — Commit `docs/UAT-SERIES.md` (the only repo file touched here — vault files are filesystem-only and NOT in git):
    ```bash
    node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit \
      "docs(phase-42): update UAT-SERIES.md" --files docs/UAT-SERIES.md
    ```

    Step 5 — Update ROADMAP.md to mark Phase 42 complete (`[ ]` → `[x]` in the v4.5 milestone summary; mark `**Plans**: 6/6 complete` under §"Phase 42"). The ROADMAP commit is performed by the orchestrator during `/gsd-execute-phase` close-out (NOT this task) — leave the file edit ready but do not commit ROADMAP here unless the orchestrator instructs.
  </action>
  <verify>
    <automated>test -f /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md &amp;&amp; grep -q 'UAT-42-01' /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md &amp;&amp; test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-42-CBOM-Correctness-Audit.md" &amp;&amp; grep -q 'status: complete' "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-42-CBOM-Correctness-Audit.md" &amp;&amp; python -m compileall quirk/ tests/ &amp;&amp; pytest</automated>
  </verify>
  <acceptance_criteria>
    - `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` exists, contains UAT-42-01, and starts with the standard frontmatter (project: QU.I.R.K., type: reference, status: active, source: docs/UAT-SERIES.md, updated: 2026-04-30)
    - `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-42-CBOM-Correctness-Audit.md` exists with frontmatter (status: complete, type: phase, source: .planning/phases/42-cbom-correctness-audit/, updated: 2026-04-30) and includes Goal, Requirements Covered, Success Criteria, What Was Built (5 subsections), and `[[Roadmap]]` link
    - `python -m compileall quirk/ tests/` exits 0
    - `pytest` (full suite — excludes `slow` per `addopts`) exits 0
    - `git log -1 --oneline docs/UAT-SERIES.md` shows the new commit with the message `docs(phase-42): update UAT-SERIES.md`
  </acceptance_criteria>
  <done>
    Vault mirror present and current; Obsidian phase note complete; full test suite green; UAT-SERIES.md committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo `docs/UAT-SERIES.md` ↔ vault `UAT-Series.md` | The two must remain in sync per project convention |
| Phase-42 closeout artifacts | Missing closeout = phase considered incomplete by future tooling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-08 | Repudiation | Vault ↔ repo UAT divergence | mitigate | Vault sync uses verbatim `cat docs/UAT-SERIES.md >> ...` per CLAUDE.md (no manual transcription) |
| T-42-09 | Repudiation | Missing phase commit | mitigate | Explicit `gsd-tools.cjs commit` step (CLAUDE.md Step 4) |
</threat_model>

<verification>
- All four UAT-42 rows present in repo and vault
- Phase note exists with `status: complete` frontmatter
- Full pytest exits 0 (no regressions across the entire repo)
- `compileall` clean
- UAT-SERIES.md committed with the prescribed message
</verification>

<success_criteria>
- CLAUDE.md Mandatory Phase Completion Steps 1-4 all executed
- Vault filesystem reflects the new phase note and UAT mirror
- Phase 42 ready for the orchestrator to mark `[x]` in ROADMAP and update STATE.md
</success_criteria>

<output>
After completion, create `.planning/phases/42-cbom-correctness-audit/42-06-SUMMARY.md` with:
- The four UAT row IDs and their referenced pytest invocations
- Vault filesystem write confirmation (paths + line counts)
- Final pytest output summary (passed/failed counts)
- The commit hash for the `docs/UAT-SERIES.md` commit
</output>
