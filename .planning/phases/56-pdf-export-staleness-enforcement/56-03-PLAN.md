---
phase: 56-pdf-export-staleness-enforcement
plan: 03
type: execute
wave: 3
depends_on: ["56-02"]
files_modified:
  - docs/UAT-SERIES.md
  - docs/report-interpretation.md
autonomous: false
requirements: [QRAMM-16]
tags: [docs, uat, obsidian]

must_haves:
  truths:
    - "docs/UAT-SERIES.md includes a UAT case for the /print QRAMM section (both scored-session and no-session paths)"
    - "docs/UAT-SERIES.md Last Updated date is 2026-05-08 or later"
    - "docs/report-interpretation.md describes the QRAMM section of the combined PDF (radar, scorecard, compliance summary)"
    - "Obsidian vault has a Phase-56 note at 20_Dev-Work/QUIRK/Phases/Phase-56-PDF-Export-Staleness-Enforcement.md"
    - "Obsidian vault UAT-Series.md is synced from docs/UAT-SERIES.md"
  artifacts:
    - path: "docs/UAT-SERIES.md"
      provides: "Updated UAT cases covering Phase 56"
      contains: "QRAMM Governance Assessment"
    - path: "docs/report-interpretation.md"
      provides: "Documentation for the QRAMM PDF section"
      contains: "QRAMM"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-56-PDF-Export-Staleness-Enforcement.md"
      provides: "Obsidian phase note for Phase 56"
      contains: "status: complete"
  key_links:
    - from: "docs/UAT-SERIES.md"
      to: "Phase 56 /print QRAMM section"
      via: "new test case"
      pattern: "QRAMM Governance Assessment"
    - from: "Obsidian Phase-56 note"
      to: "docs/UAT-SERIES.md and SUMMARYs"
      via: "vault filesystem write"
      pattern: "20_Dev-Work/QUIRK/Phases/Phase-56"
---

<objective>
Complete the mandatory phase-end documentation and Obsidian sync steps from CLAUDE.md for Phase 56. Update `docs/UAT-SERIES.md` to add manual test cases that exercise the new /print QRAMM section (both scored and no-session paths), update `docs/report-interpretation.md` so consultants reading the docs know the PDF includes the QRAMM section, and create / sync the Obsidian phase note + UAT mirror.

Purpose: Per project memory `feedback_docs_obsidian_per_phase` and `feedback_uat_series_update`, every phase ends with explicit documentation + Obsidian vault sync. Without these, the deliverable is incomplete from the project's perspective.

Output:
- `docs/UAT-SERIES.md` — updated with Phase 56 test cases and refreshed Last Updated date
- `docs/report-interpretation.md` — gains a QRAMM Governance Assessment section description
- Obsidian vault: `Phase-56-PDF-Export-Staleness-Enforcement.md` (new) and `UAT-Series.md` (overwritten from docs)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/56-pdf-export-staleness-enforcement/56-CONTEXT.md
@.planning/phases/56-pdf-export-staleness-enforcement/56-01-SUMMARY.md
@.planning/phases/56-pdf-export-staleness-enforcement/56-02-SUMMARY.md
@docs/UAT-SERIES.md
@docs/report-interpretation.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update docs/UAT-SERIES.md with Phase 56 test cases</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (full file — identify the relevant series for /print and the existing structure of pass criteria; confirm how prior phases added test cases)
    - .planning/phases/56-pdf-export-staleness-enforcement/56-CONTEXT.md (D-01, D-05, D-06, D-07 — visual contract)
    - .planning/phases/56-pdf-export-staleness-enforcement/56-UI-SPEC.md (Copywriting Contract for the locked strings)
  </read_first>
  <behavior>
    - The relevant series gains at least 2 new manual test cases:
      1. Scored-session QRAMM section visible on /print (radar + scorecard + 8-row compliance + per-framework detail)
      2. No-session placeholder copy on /print when no QRAMM session has been scored
    - At least one regression check is added (or noted) confirming Technical Findings / Certs / CBOM / Roadmap sections still render correctly
    - The document header `**Last Updated:**` field is updated to `2026-05-08`
    - Existing test cases are NOT modified or deleted
  </behavior>
  <action>
Open `docs/UAT-SERIES.md`. Identify the series that covers the dashboard `/print` route (likely UAT-1 PDF/print series — confirm by reading the full file). Append the following new test cases to that series, preserving the existing pass-criteria style.

Add **at the top of the document**: update the `**Last Updated:**` line to `2026-05-08` (replace the existing date). Do not introduce any other date format.

Add the following test cases to the `/print`-relevant series (use the next sequential test case ID from the series, e.g. UAT-1-NN where NN is the next number after the highest existing case):

```markdown
### UAT-1-NN: QRAMM Governance section appears in /print PDF

**Setup:** A QRAMM session has been completed and scored via the dashboard.

**Steps:**
1. Open `http://localhost:<port>/print` in a browser
2. Wait for `document.body.getAttribute('data-ready') === "true"`
3. Open browser print preview (Cmd-P / Ctrl-P)
4. Scroll to the QRAMM Governance Assessment section

**Pass criteria:**
- The "QRAMM Governance Assessment" heading appears on its own page (page-break-before)
- An inline SVG radar chart is visible with 4 axes labeled CVI / SGRM / DPE / ITR
- A "Dimension Scorecard" table shows 4 rows (one per dimension) with raw and weighted scores
- "Compliance Framework Coverage" h3 is followed by an 8-row table with `Scanner-informed` or `Manual only` badges
- The footnote "Coverage reflects QUIRK scanner findings for CVI only — SGRM, DPE, ITR require manual assessment." appears below the 8-row table
- 8 per-framework detail tables (NIST PQC, NSM-10, CNSA 2.0, ISO 27001:2022, ETSI Quantum-Safe, PCI-DSS v4.0, Common Criteria, BSI TR-02102) flow continuously below — no forced page breaks between them

### UAT-1-NN+1: No-session placeholder copy appears when QRAMM has no scored session

**Setup:** No QRAMM session has been scored (fresh database, or all sessions are still `in_progress`).

**Steps:**
1. Open `http://localhost:<port>/print`
2. Locate the QRAMM Governance Assessment section

**Pass criteria:**
- The "QRAMM Governance Assessment" heading still appears (section is always present per D-05)
- The body of the section contains exactly: "No QRAMM assessment completed — run an assessment from the dashboard to populate this section."
- No radar SVG, no scorecard table, no compliance tables render in this state

### UAT-1-NN+2: Existing /print sections regression-free after Phase 56

**Setup:** Same as UAT-1-NN above.

**Steps:**
1. Open `http://localhost:<port>/print` and view in browser print preview

**Pass criteria:**
- Technical Findings section renders with the same column widths and severity badges as v4.6
- Certificate Inventory table is unchanged
- CBOM section is unchanged
- Migration Roadmap section is unchanged
- The QRAMM section appears AFTER the Migration Roadmap section, never before
```

Use real sequential numbers when authoring (replace `NN`, `NN+1`, `NN+2` with actual numbers based on the last existing case in that series).
  </action>
  <verify>
    <automated>grep -q "QRAMM Governance Assessment" docs/UAT-SERIES.md && grep -q "No QRAMM assessment completed" docs/UAT-SERIES.md && grep -q "Coverage reflects QUIRK scanner findings for CVI only" docs/UAT-SERIES.md && grep -q "2026-05-08" docs/UAT-SERIES.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "QRAMM Governance Assessment" docs/UAT-SERIES.md` returns at least 1
    - `grep -c "No QRAMM assessment completed — run an assessment from the dashboard to populate this section." docs/UAT-SERIES.md` returns at least 1
    - `grep -c "Coverage reflects QUIRK scanner findings for CVI only" docs/UAT-SERIES.md` returns at least 1
    - `grep -c "2026-05-08" docs/UAT-SERIES.md` returns at least 1
    - At least 3 new pass-criteria blocks added (regression case + scored case + no-session case)
    - Existing test cases not removed: `wc -l docs/UAT-SERIES.md` returns a value strictly greater than the prior file size
  </acceptance_criteria>
  <done>UAT-SERIES.md has Phase 56 test cases with locked copy strings, refreshed Last Updated date, and existing cases preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update docs/report-interpretation.md with QRAMM section description</name>
  <files>docs/report-interpretation.md</files>
  <read_first>
    - docs/report-interpretation.md (full file — identify the section structure and add a new subsection covering the QRAMM section of the combined PDF)
    - .planning/phases/56-pdf-export-staleness-enforcement/56-CONTEXT.md (D-01, D-05, D-06, D-07 — describe what consultants see)
    - .planning/phases/56-pdf-export-staleness-enforcement/56-UI-SPEC.md (radar spec, copywriting contract)
  </read_first>
  <behavior>
    - A new section / subsection titled "QRAMM Governance Assessment" is added describing what appears in the PDF
    - Description covers: executive intro paragraph, inline SVG radar (4 axes CVI/SGRM/DPE/ITR), Dimension Scorecard table, 8-row Compliance Framework Coverage summary with Scanner-informed vs Manual only tiers, footnote, and per-framework practice detail
    - No-session behavior is explicitly documented (consultants need to know the section appears even when no assessment exists)
    - Existing content is not modified or deleted
  </behavior>
  <action>
Read `docs/report-interpretation.md`. Locate the section that describes the combined PDF / `/print` output (typically after the Technical Findings or Migration Roadmap subsections — match the existing organization). Append a new subsection at an appropriate location:

```markdown
## QRAMM Governance Assessment Section

The combined PDF includes a QRAMM (Quantum Readiness & Maturity Model) Governance Assessment section that begins on a new page after the Migration Roadmap. This section is intended to be read alongside the technical findings — it gives executives and CISOs the governance context for the scanner-derived crypto inventory.

### What the section contains

- **Executive intro paragraph** — A one-sentence summary identifying that the section reflects the most recent completed QRAMM assessment.
- **Radar chart (inline SVG)** — A four-axis polygon plotting the four QRAMM dimensions: CVI (Cryptographic Visibility & Inventory), SGRM (Standards & Governance Risk Management), DPE (Data Protection & Encryption), and ITR (Incident & Transition Readiness). The polygon shows the session's raw dimension scores on a 0–4 scale; closer to the outer edge means higher maturity.
- **Dimension Scorecard table** — Four rows (one per dimension) with raw score, weighted score, and overall maturity level.
- **Compliance Framework Coverage summary** — An 8-row table mapping the assessment to NIST PQC Standards, NSM-10, CNSA 2.0, ISO 27001:2022, ETSI Quantum-Safe, PCI-DSS v4.0, Common Criteria, and BSI TR-02102. Each framework shows a coverage tier badge: **Scanner-informed** (the QUIRK scanner contributes signal — currently CVI only) or **Manual only** (the framework's relevance is derived purely from manual assessment answers).
- **Per-framework practice detail** — Below the summary table, eight subsections (one per framework) flow continuously without page breaks. Each shows every practice area's relevance score and whether it was scanner-informed.

### When no QRAMM assessment has been completed

The QRAMM section heading still appears in the PDF, but the body shows only:

> "No QRAMM assessment completed — run an assessment from the dashboard to populate this section."

This keeps the PDF structure consistent across all engagements and signals that the QRAMM capability exists.

### Coverage caveat

QUIRK's scanner directly informs the CVI dimension (cryptographic visibility from real findings). SGRM, DPE, and ITR require manual assessment input via the dashboard's QRAMM questionnaire. The footnote in the PDF reflects this:

> "Coverage reflects QUIRK scanner findings for CVI only — SGRM, DPE, ITR require manual assessment."
```

Adjust heading levels (`##` vs `###`) to match the surrounding document hierarchy. Do not modify any existing content.
  </action>
  <verify>
    <automated>grep -q "QRAMM Governance Assessment Section" docs/report-interpretation.md && grep -q "Cryptographic Visibility &amp; Inventory\|Cryptographic Visibility & Inventory" docs/report-interpretation.md && grep -q "Scanner-informed" docs/report-interpretation.md && grep -q "Manual only" docs/report-interpretation.md && grep -q "No QRAMM assessment completed" docs/report-interpretation.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "QRAMM Governance Assessment Section" docs/report-interpretation.md` returns at least 1
    - `grep -c "Scanner-informed" docs/report-interpretation.md` returns at least 1
    - `grep -c "Manual only" docs/report-interpretation.md` returns at least 1
    - `grep -c "No QRAMM assessment completed — run an assessment from the dashboard to populate this section." docs/report-interpretation.md` returns at least 1
    - `grep -cE "CVI|SGRM|DPE|ITR" docs/report-interpretation.md` returns at least 4
    - File size strictly greater than prior version
  </acceptance_criteria>
  <done>report-interpretation.md describes the QRAMM PDF section, both scored and no-session states.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create Obsidian Phase-56 note and sync UAT-Series.md to vault</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-56-PDF-Export-Staleness-Enforcement.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md</files>
  <read_first>
    - CLAUDE.md (Mandatory Phase Completion Steps — full section, esp. steps 1 and 3 with exact paths and frontmatter)
    - .planning/ROADMAP.md (lines 1122-1132 for the Phase 56 Goal and Success Criteria)
    - .planning/phases/56-pdf-export-staleness-enforcement/56-01-SUMMARY.md (what Plan 01 built — read at execution time)
    - .planning/phases/56-pdf-export-staleness-enforcement/56-02-SUMMARY.md (what Plan 02 built — read at execution time)
    - One existing phase note for the template pattern: `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-55-QRAMM-Compliance-Mapping-View.md` (read first to mirror frontmatter and section structure)
  </read_first>
  <behavior>
    - File `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-56-PDF-Export-Staleness-Enforcement.md` exists and is written via the filesystem (Write tool), NOT via `obsidian CLI content=` (per CLAUDE.md — files are too large for shell expansion)
    - Note frontmatter contains: `project: QU.I.R.K.`, `type: phase`, `status: complete`, `source: .planning/phases/56-pdf-export-staleness-enforcement/`, `updated: 2026-05-08`
    - Note body contains: Goal (from ROADMAP.md line 1123), Requirements Covered (QRAMM-16), Success Criteria (3 bullets from ROADMAP.md), What Was Built (one subsection per plan, sourced from 56-01-SUMMARY.md and 56-02-SUMMARY.md), and `[[Roadmap]]` link
    - File `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` is overwritten with the QUIRK frontmatter block followed by the full content of `docs/UAT-SERIES.md` (per CLAUDE.md step 3 — use the documented bash pattern)
  </behavior>
  <action>
**Sub-step A — Create the Obsidian Phase-56 note via Write tool (NOT obsidian CLI):**

Use the Write tool to create `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-56-PDF-Export-Staleness-Enforcement.md` with the following structure (mirror the existing Phase-55 note's exact format — read it first):

```markdown
---
project: QU.I.R.K.
type: phase
status: complete
source: .planning/phases/56-pdf-export-staleness-enforcement/
updated: 2026-05-08
---

# Phase 56 — PDF Export & Staleness Enforcement

## Goal

The combined PDF export includes a QRAMM section (executive summary, dimension scorecard, static SVG radar chart, compliance framework mapping summary) that starts on a new page — and the quarterly staleness CI gate rejects builds when QRAMM model metadata is more than 90 days old.

## Requirements Covered

- **QRAMM-16** — Combined PDF export (`/print` route) includes a QRAMM section starting on a new page; existing Technical Findings layout not regressed.

(Note: staleness CI gate `tests/test_qramm_staleness.py` already shipped in Phase 55 — no further work in Phase 56 per CONTEXT D-08.)

## Success Criteria

1. The PDF produced via the `/print` route includes a QRAMM section starting on a new page (via `@media print { page-break-before: always }`) containing: executive QRAMM summary paragraph, dimension scorecard table, the static SVG radar chart rendered at print resolution, and a compliance framework mapping summary for the 8 frameworks.
2. The existing Technical Findings section layout is not regressed — pagination, finding table column widths, and cert inventory layout in the PDF are unchanged from v4.6 output.
3. A consultant running a full QRAMM assessment and clicking "Export PDF" receives a single file containing both the governance assessment and the technical scanner findings.

## What Was Built

### Plan 01 — useQRAMMPrintData hook

[Insert summary content from 56-01-SUMMARY.md "Files Modified" and "What Was Built" sections — read at execution time]

### Plan 02 — print.tsx QRAMM section

[Insert summary content from 56-02-SUMMARY.md — describe PRINT_CSS additions, PrintQRAMM component (radar + scorecard + compliance tables), data-ready gate update, and the new section placement after Migration Roadmap]

### Plan 03 — Documentation & Obsidian sync

- Updated `docs/UAT-SERIES.md` with Phase 56 manual test cases (scored-session, no-session, regression)
- Updated `docs/report-interpretation.md` with a QRAMM Governance Assessment Section description
- Synced this phase note and `UAT-Series.md` to the Obsidian vault

## Links

- [[Roadmap]]
- [[Phase-55-QRAMM-Compliance-Mapping-View]]
- [[Phase-54-QRAMM-Assessment-UI-Scorecard]]
- [[UAT-Series]]
```

When filling the "What Was Built" subsections, read the actual `.planning/phases/56-pdf-export-staleness-enforcement/56-01-SUMMARY.md` and `56-02-SUMMARY.md` files at execution time and summarize their "Files Modified" and "Tasks Completed" content into 3-6 bullet points each.

**Sub-step B — Sync UAT-Series.md to vault** (per CLAUDE.md step 3 — exact bash pattern):

Run this exact command sequence:

```bash
printf "---\nproject: QU.I.R.K.\ntype: reference\nstatus: active\nsource: docs/UAT-SERIES.md\nupdated: 2026-05-08\n---\n\n" > /tmp/uat_vault.md
cat docs/UAT-SERIES.md >> /tmp/uat_vault.md
cp /tmp/uat_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
```

This is mandatory because `docs/UAT-SERIES.md` is too large for the obsidian CLI `content=` parameter.
  </action>
  <verify>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-56-PDF-Export-Staleness-Enforcement.md" && grep -q "status: complete" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-56-PDF-Export-Staleness-Enforcement.md" && grep -q "QRAMM-16" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-56-PDF-Export-Staleness-Enforcement.md" && test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" && head -1 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" | grep -q "^---$" && grep -q "QRAMM Governance Assessment" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-56-PDF-Export-Staleness-Enforcement.md` exists
    - That file contains `status: complete` (grep)
    - That file contains `QRAMM-16` (grep)
    - That file contains `[[Roadmap]]` (grep)
    - That file contains `## What Was Built` (grep)
    - That file contains `updated: 2026-05-08` (grep)
    - File `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` exists
    - That file's first line is `---` (frontmatter)
    - That file contains `source: docs/UAT-SERIES.md` (grep)
    - That file contains `QRAMM Governance Assessment` (grep — confirms it includes the new test cases written in Task 1)
  </acceptance_criteria>
  <done>Obsidian vault has the Phase-56 note marked complete and a fresh copy of UAT-SERIES.md mirrored to UAT-Series.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human confirmation of docs + Obsidian sync</name>
  <what-built>
    Plan 03 updated `docs/UAT-SERIES.md` with three new manual test cases for the /print QRAMM section (scored, no-session, regression), updated `docs/report-interpretation.md` with a QRAMM Governance Assessment Section description, created the Obsidian Phase-56 phase note marked `status: complete`, and synced `docs/UAT-SERIES.md` into the vault as `UAT-Series.md`.
  </what-built>
  <how-to-verify>
    1. In the QUIRK repo: `head -20 docs/UAT-SERIES.md` → confirm `**Last Updated:** 2026-05-08`
    2. `grep -A 5 "QRAMM Governance Assessment" docs/UAT-SERIES.md | head` → confirm the new test cases are present
    3. `grep "QRAMM Governance Assessment Section" docs/report-interpretation.md` → confirm description added
    4. In Obsidian (vault `Digs`): open `20_Dev-Work/QUIRK/Phases/Phase-56-PDF-Export-Staleness-Enforcement.md` → confirm status is `complete`, content covers all three plans, and `[[Roadmap]]` link works
    5. In Obsidian: open `20_Dev-Work/QUIRK/UAT-Series.md` → confirm it shows the updated date and new Phase 56 cases
    6. (Optional) Run any documentation lint or link checker if the project has one
  </how-to-verify>
  <resume-signal>Type "approved" or describe any documentation / vault issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo → Obsidian vault filesystem | Documentation files written from repo to local vault path; no network or auth boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-13 | Information Disclosure | Vault contains project documentation including Phase 56 summary | accept | Vault is local to the developer's machine (`/Users/digs/vaults/Digs`); no upload path. Documentation contains no secrets — only feature descriptions. |
| T-56-14 | Tampering | UAT-SERIES.md sync overwrites vault file | mitigate | Sync uses an explicit bash pattern from CLAUDE.md; the source file is checked into git so any unintended drift can be reverted. The vault copy is a mirror, not a source of truth. |
| T-56-15 | Repudiation | n/a (developer-local documentation) | accept | Git history of `docs/UAT-SERIES.md` and `docs/report-interpretation.md` provides full audit trail in the repo. |
| T-56-16 | Spoofing / Elevation / DoS | n/a | accept | Plan only writes documentation files; no code execution surface; no auth. |
</threat_model>

<verification>
1. UAT-SERIES.md updated with new cases (grep checks pass)
2. report-interpretation.md updated with QRAMM section description (grep checks pass)
3. Obsidian phase note exists at the documented path with `status: complete`
4. Obsidian UAT-Series.md mirror updated and contains the new Phase 56 cases
5. Human verification (Task 4) confirms the docs render correctly in Obsidian
</verification>

<success_criteria>
- `docs/UAT-SERIES.md` includes 3 new Phase 56 test cases and refreshed Last Updated date
- `docs/report-interpretation.md` documents the QRAMM PDF section
- Obsidian vault Phase-56 note exists with frontmatter `status: complete` and links back to Roadmap
- Obsidian vault UAT-Series.md mirrors the updated docs/UAT-SERIES.md
- Mandatory phase completion steps from CLAUDE.md are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/56-pdf-export-staleness-enforcement/56-03-SUMMARY.md`
</output>
