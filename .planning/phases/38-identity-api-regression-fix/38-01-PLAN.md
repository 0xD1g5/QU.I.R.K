---
phase: 38-identity-api-regression-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/dashboard/api/routes/scan.py
  - tests/test_identity_surface.py
autonomous: true
requirements: [GAP-01, GAP-02]
must_haves:
  truths:
    - "GET /api/scan/latest returns SAML, OIDC, and DNSSEC entries in identity_findings[] even when Kerberos endpoints are timestamp-skewed by up to 5 minutes"
    - "tests/test_identity_surface.py::Issue3ScanWindowRegressionTest::test_issue3_scan_window_returns_all_identity_protocols exits 0"
    - "The explicit ?scan_id= branch retains its 1-second exact-second window (unchanged behavior)"
  artifacts:
    - path: "quirk/dashboard/api/routes/scan.py"
      provides: "5-minute backward bracket scan-window query on implicit-latest branch"
      contains: "SESSION_BRACKET = timedelta(minutes=5)"
    - path: "tests/test_identity_surface.py"
      provides: "Regression test class Issue3ScanWindowRegressionTest with at least one additional bracket-coverage assertion"
      contains: "Issue3ScanWindowRegressionTest"
  key_links:
    - from: "quirk/dashboard/api/routes/scan.py implicit-latest branch (lines ~589-608)"
      to: "CryptoEndpoint table query"
      via: "scanned_at >= latest_ts - SESSION_BRACKET AND scanned_at <= latest_ts"
      pattern: "SESSION_BRACKET"
---

<objective>
Replace the buggy 1-second forward scan-window filter on the implicit-latest branch of `GET /api/scan/latest` with a 5-minute backward bracket anchored on `MAX(scanned_at)`, restoring SAML/OIDC visibility in `identity_findings[]` and turning the failing `Issue3ScanWindowRegressionTest` GREEN.

Purpose: Closes GAP-01 (real bug, ISSUE-3 from Phase 24) and GAP-02 (the deferred regression test goes from hard FAIL to GREEN). The fix is purely API-layer per D-05 — no scanner, schema, or persistence changes.

Output: Modified `scan.py` with `SESSION_BRACKET` module constant; extended regression test class; both pytest assertions green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-identity-api-regression-fix/38-CONTEXT.md
@.planning/phases/38-identity-api-regression-fix/38-RESEARCH.md
@CLAUDE.md
@quirk/dashboard/api/routes/scan.py
@tests/test_identity_surface.py
</context>

<interfaces>
<!-- Existing imports already present at top of scan.py: -->
<!--   from datetime import datetime, timedelta -->
<!--   from sqlalchemy import func -->
<!--   from quirk.models import CryptoEndpoint -->
<!-- No new imports required. -->

Current broken block (scan.py:589-608, implicit-latest branch):
```python
else:
    latest_ts_str = db.query(
        func.strftime("%Y-%m-%d %H:%M:%S", func.max(CryptoEndpoint.scanned_at))
    ).scalar()
    if latest_ts_str is None:
        raise HTTPException(
            status_code=404,
            detail="No scan results found. Run your first scan: quirk --config config.yaml",
        )
    latest_ts = datetime.fromisoformat(latest_ts_str)
    endpoints: list[CryptoEndpoint] = (
        db.query(CryptoEndpoint)
        .filter(
            CryptoEndpoint.scanned_at >= latest_ts,
            CryptoEndpoint.scanned_at < latest_ts + timedelta(seconds=1),
        )
        .all()
    )
```

The explicit `?scan_id=` branch at lines 573-588 is correct (1-second exact-second semantics) — DO NOT modify it.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SESSION_BRACKET constant and replace 1-second forward filter with 5-minute backward bracket on implicit-latest branch</name>
  <files>quirk/dashboard/api/routes/scan.py</files>
  <read_first>
    - quirk/dashboard/api/routes/scan.py (full file — confirm imports of datetime, timedelta, func, CryptoEndpoint are already present; confirm no other module-level constants need ordering)
    - tests/test_identity_surface.py lines 464-563 (the regression test that defines correct behavior)
    - .planning/phases/38-identity-api-regression-fix/38-RESEARCH.md "Pattern 1 — Session-Bracket Window" section for the exact code sketch
  </read_first>
  <behavior>
    - When `MAX(scanned_at)` is None, endpoint still raises HTTP 404 with the existing "No scan results found..." detail message (unchanged).
    - When endpoints exist, the implicit-latest branch returns all rows with `scanned_at` in `[MAX - SESSION_BRACKET, MAX]` (inclusive on both ends).
    - The explicit `?scan_id=` branch (lines 573-588) is byte-for-byte unchanged — its 1-second forward window remains.
    - SESSION_BRACKET is defined at module scope as `SESSION_BRACKET = timedelta(minutes=5)` so tests can monkeypatch it.
  </behavior>
  <action>
Per D-01: introduce `SESSION_BRACKET = timedelta(minutes=5)` as a module-level constant in `quirk/dashboard/api/routes/scan.py`. Place it near the other module-level constants/imports at the top of the file (search for existing module-level constants; if none, place it directly below the imports block, before any function or `router = APIRouter(...)` definition). Add a one-line comment: `# Phase 38 (D-01): backward bracket from MAX(scanned_at) restores SAML/OIDC visibility under timestamp skew`.

Then modify the implicit-latest branch (the `else:` clause currently at lines 589-608). Replace the existing block with:

```python
else:
    # D-01: anchor on MAX(scanned_at), then load all endpoints in the
    # SESSION_BRACKET window before that maximum. This restores SAML/OIDC
    # findings that the previous 1-second forward window silently excluded
    # when Kerberos finished last (ISSUE-3 / DEF-v4.4-02).
    latest_ts = db.query(func.max(CryptoEndpoint.scanned_at)).scalar()
    if latest_ts is None:
        raise HTTPException(
            status_code=404,
            detail="No scan results found. Run your first scan: quirk --config config.yaml",
        )
    endpoints: list[CryptoEndpoint] = (
        db.query(CryptoEndpoint)
        .filter(
            CryptoEndpoint.scanned_at >= latest_ts - SESSION_BRACKET,
            CryptoEndpoint.scanned_at <= latest_ts,
        )
        .all()
    )
```

DO NOT touch the explicit `?scan_id=` branch (lines 573-588) — its 1-second window is correct (per D-01 scope, A4 in research). DO NOT modify any other function in `scan.py`. Keep the diff minimal per CLAUDE.md PEP 8 / minimal-diff rule. After editing, run `python -m compileall quirk/dashboard/api/routes/scan.py`.
  </action>
  <verify>
    <automated>python -m compileall quirk/dashboard/api/routes/scan.py 2>&1 | grep -v "^Listing\|^Compiling" ; grep -n "SESSION_BRACKET" quirk/dashboard/api/routes/scan.py ; grep -n "latest_ts - SESSION_BRACKET" quirk/dashboard/api/routes/scan.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -m compileall quirk/dashboard/api/routes/scan.py` exits 0 with no syntax errors.
    - `grep -c "^SESSION_BRACKET = timedelta(minutes=5)" quirk/dashboard/api/routes/scan.py` returns `1`.
    - `grep -c "latest_ts - SESSION_BRACKET" quirk/dashboard/api/routes/scan.py` returns `1`.
    - `grep -c "scanned_at < latest_ts + timedelta(seconds=1)" quirk/dashboard/api/routes/scan.py` returns `1` (the unchanged explicit `?scan_id=` branch — proves we did not touch it).
    - `grep -c "func.strftime" quirk/dashboard/api/routes/scan.py | grep -v '^#'` shows the implicit branch no longer uses strftime for MAX (the constant was needed only because the old code stringified — the new code uses the datetime directly).
  </acceptance_criteria>
  <done>SESSION_BRACKET defined; implicit-latest branch uses backward bracket; explicit branch untouched; module compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend Issue3ScanWindowRegressionTest with bracket-edge and explicit-scan_id guard assertions</name>
  <files>tests/test_identity_surface.py</files>
  <read_first>
    - tests/test_identity_surface.py lines 464-563 (existing Issue3ScanWindowRegressionTest class — reuse fixture pattern verbatim)
    - .planning/phases/38-identity-api-regression-fix/38-RESEARCH.md "Phase Requirements → Test Map" rows 2 and 3
  </read_first>
  <behavior>
    - New test `test_saml_visible_with_earlier_dnssec`: seeds DNSSEC at T-60s and SAML at T; asserts both protocols appear in identity_findings[] (proves bracket covers ≥60s skew on the early side).
    - New test `test_explicit_scan_id_uses_exact_second`: seeds two scans at distinct seconds; calls `/api/scan/latest?scan_id=<earlier-iso>` and asserts the response only contains the earlier scan's endpoints (proves the explicit branch was NOT widened).
    - Both new tests use the same in-memory SQLite + TestClient harness already used by `test_issue3_scan_window_returns_all_identity_protocols`.
  </behavior>
  <action>
Open `tests/test_identity_surface.py`. Inside `class Issue3ScanWindowRegressionTest(unittest.TestCase):` add two NEW methods immediately AFTER the existing `test_issue3_scan_window_returns_all_identity_protocols` method (i.e., before the closing of the class at line 563).

For both new tests, replicate the in-memory-SQLite + `TestClient` fixture setup pattern from the existing test method (lines 479-507). To avoid duplication you may extract a helper method `_make_client_and_session(self)` returning `(client, TestingSession)` and have all three test methods call it; if you do extract a helper, the existing test must keep its current behavior.

**Test A — `test_saml_visible_with_earlier_dnssec`:**
- early_ts = `datetime(2026, 1, 15, 12, 0, 0)`
- saml_ts = `datetime(2026, 1, 15, 12, 1, 0)` (60s later — well within 5-min bracket)
- Insert one DNSSEC endpoint (host="example.com", port=53, protocol="DNSSEC", cert_pubkey_alg="RSASHA1", service_detail="dnskey:tag=12345:role=ZSK", scanned_at=early_ts) and one SAML endpoint (host="idp.example.com", port=443, protocol="SAML", cert_pubkey_alg="RSA", cert_pubkey_size=1024, service_detail="https://idp.example.com|use=signing|serial=ABC", scanned_at=saml_ts).
- GET `/api/scan/latest`; assert `resp.status_code == 200`.
- Extract `protocols = {f["protocol"] for f in resp.json().get("identity_findings", [])}` and assert `"SAML" in protocols` AND `"DNSSEC" in protocols`.

**Test B — `test_explicit_scan_id_uses_exact_second`:**
- ts_a = `datetime(2026, 1, 15, 12, 0, 0)` (an "older" scan — Kerberos endpoint with weak rc4-hmac so it produces an identity finding)
- ts_b = `datetime(2026, 1, 15, 12, 5, 0)` (a "newer" scan, 5 minutes later — Kerberos with rc4-hmac too, different host)
- Insert one Kerberos endpoint at ts_a (host="dc-old.example.com", port=88, protocol="KERBEROS", cert_pubkey_alg="rc4-hmac", service_detail="etype:23:rc4-hmac:HIGH", scanned_at=ts_a) and one at ts_b (host="dc-new.example.com", port=88, protocol="KERBEROS", cert_pubkey_alg="rc4-hmac", service_detail="etype:23:rc4-hmac:HIGH", scanned_at=ts_b).
- Build the explicit scan_id matching ts_a's exact-second ISO string: `scan_id = ts_a.isoformat(sep=" ")` (or `ts_a.strftime("%Y-%m-%d %H:%M:%S")` — match whatever the existing `?scan_id=` branch in `scan.py:573-588` parses with `datetime.fromisoformat`; the safest token is `ts_a.isoformat()` which fromisoformat accepts).
- GET `/api/scan/latest?scan_id={scan_id}`; assert `resp.status_code == 200`.
- Extract hosts: `hosts = {f["host"] for f in resp.json().get("identity_findings", [])}`.
- Assert `"dc-old.example.com" in hosts` AND `"dc-new.example.com" not in hosts` (the explicit branch must NOT have widened to 5 minutes).

Do not modify any test outside `Issue3ScanWindowRegressionTest`. PEP 8 imports (datetime/timedelta) are already imported inside the existing method's `try:` — keep new tests consistent with that pattern. After editing, run `python -m compileall tests/test_identity_surface.py`.
  </action>
  <verify>
    <automated>python -m compileall tests/test_identity_surface.py 2>&1 | grep -v "^Listing\|^Compiling" ; python -m pytest tests/test_identity_surface.py::Issue3ScanWindowRegressionTest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m compileall tests/test_identity_surface.py` exits 0.
    - `python -m pytest tests/test_identity_surface.py::Issue3ScanWindowRegressionTest -x -q` exits 0 with **3 passed** (the original test plus the two new ones).
    - `grep -c "def test_saml_visible_with_earlier_dnssec" tests/test_identity_surface.py` returns `1`.
    - `grep -c "def test_explicit_scan_id_uses_exact_second" tests/test_identity_surface.py` returns `1`.
    - `grep -c "def test_issue3_scan_window_returns_all_identity_protocols" tests/test_identity_surface.py` returns `1` (original test still present).
  </acceptance_criteria>
  <done>Three Issue3ScanWindowRegressionTest methods all pass; explicit `?scan_id=` branch confirmed unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Run full pytest and confirm GAP-01/02 closed without new regressions in scan-window-adjacent tests</name>
  <files>(no file modifications — verification only)</files>
  <read_first>
    - tests/test_dashboard_api.py (skim for any test that asserts a specific endpoint count on `/api/scan/latest` — flagged in research R1)
    - tests/test_identity_findings_accuracy.py (must remain green per research)
  </read_first>
  <action>
Run the full pytest suite and capture the result. Do NOT touch the pre-existing `test_all_completed_phase_validations_nyquist_compliant` failure here — that is closed in PLAN 38-03. The acceptance criteria below explicitly tolerate that one pre-existing failure for this task; PLAN 38-03 will eliminate it.

```
python -m pytest -q 2>&1 | tail -40
```

If any test OTHER than `test_all_completed_phase_validations_nyquist_compliant` newly fails after the scan.py change, investigate whether it asserted endpoint counts on `/api/scan/latest` that were previously masked by the 1-second window (research R1, Pitfall 1). The fix is to update that test's expected count to reflect the now-correct bracket behavior — NOT to revert the scan.py fix. Document any such adjustment in the SUMMARY with the test name, old expected count, and new expected count.
  </action>
  <verify>
    <automated>python -m pytest tests/test_identity_surface.py tests/test_identity_findings_accuracy.py tests/test_dashboard_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_identity_surface.py tests/test_identity_findings_accuracy.py tests/test_dashboard_api.py -q` exits 0.
    - `python -m pytest tests/test_identity_surface.py::Issue3ScanWindowRegressionTest::test_issue3_scan_window_returns_all_identity_protocols -x` exits 0 (proves GAP-02 closed).
    - Full-suite run `python -m pytest -q` reports exactly 1 failure or 0 failures, where the only acceptable failure is `test_all_completed_phase_validations_nyquist_compliant` (closed by PLAN 38-03). Any OTHER newly red test must be triaged and noted in the SUMMARY before this task is marked done.
  </acceptance_criteria>
  <done>GAP-01 and GAP-02 closed; identity-surface, identity-findings-accuracy, and dashboard-api test files all green; full-suite delta is at most the one known pre-existing hygiene failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → FastAPI route | `/api/scan/latest` is unauthenticated by design (local consultant dashboard); query parameters parsed via FastAPI/Pydantic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Information Disclosure (negative — under-reporting) | `scan.py` implicit-latest branch | mitigate | Replace 1-second forward filter with 5-min backward bracket so SAML/OIDC findings reach the consultant (this is the phase's primary fix; under-reporting weak crypto is a real consulting-correctness risk) |
| T-38-02 | Information Disclosure (positive — bleeding prior scans) | `scan.py` SESSION_BRACKET window width | accept | 5-min bracket is small enough that consecutive distinct scans within 5 minutes are extremely rare in QU.I.R.K. usage; constant is centralized for future tuning |
| T-38-03 | Tampering | `?scan_id=` query param | mitigate | Existing `datetime.fromisoformat` validation + HTTP 400 on bad input retained — Task 1 explicitly does NOT modify the explicit branch |
</threat_model>

<verification>
- `python -m compileall quirk/dashboard/api/routes/scan.py tests/test_identity_surface.py` exits 0
- `python -m pytest tests/test_identity_surface.py::Issue3ScanWindowRegressionTest -x -q` reports 3 passed
- Full-suite `python -m pytest -q` reports at most the single pre-existing hygiene failure (closed in PLAN 38-03)
- `grep` gates from Task 1 and Task 2 acceptance criteria all return expected counts
</verification>

<success_criteria>
- GAP-01 closed: SAML/OIDC entries restored in `identity_findings[]` for the implicit-latest branch
- GAP-02 closed: `Issue3ScanWindowRegressionTest::test_issue3_scan_window_returns_all_identity_protocols` exits 0
- Two new bracket-coverage assertions added (DNSSEC-earlier-than-SAML; explicit-scan_id exact-second guard) — both green
- Explicit `?scan_id=` branch byte-for-byte unchanged
- `scan.py` and `test_identity_surface.py` byte-compile cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/38-identity-api-regression-fix/38-01-SUMMARY.md` documenting:
- Exact diff applied to `scan.py` (the SESSION_BRACKET constant + the rewritten implicit-latest block)
- Exact tests added (names, assertions, fixture pattern)
- Full-suite pytest result (passed/failed/skipped counts)
- Any test outside `test_identity_surface.py` whose expected count changed due to the bracket fix (per Pitfall 1) — none expected, but document if encountered
</output>
