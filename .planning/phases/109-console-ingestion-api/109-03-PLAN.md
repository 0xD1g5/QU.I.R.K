---
phase: 109-console-ingestion-api
plan: 03
type: execute
wave: 3
depends_on: [109-02]
files_modified:
  - tests/test_sensor_ingest.py
  - tests/test_console_enroll.py
  - tests/scanner/test_phase57_invariants.py
  - docs/UAT-SERIES.md
autonomous: true
requirements: [CONSOLE-02, CONSOLE-03, CONSOLE-04, CONSOLE-05]
must_haves:
  truths:
    - "An automated test proves an unauthenticated POST /api/sensor/push returns 401"
    - "Automated tests prove 413 (oversize), 409 (duplicate payload_id), 422 (replay window + console_utc), and 200 (valid push persists rows)"
    - "An automated test proves an IntegrationDelivery row is written on success AND on each failure branch"
    - "An automated test proves extra fields are dropped (extra='ignore') and version skew does not 422/500"
    - "An automated test proves quirk console enroll writes sensors + sensor_tokens rows and prints a bearer token once"
    - "The safe_str AST gate covers quirk/cli/console_cmd.py and quirk/dashboard/api/routes/sensor.py"
    - "docs/UAT-SERIES.md reflects the new ingestion endpoint and is synced to Obsidian"
  artifacts:
    - path: "tests/test_sensor_ingest.py"
      provides: "401/413/409/422/200/audit/extra-ignore/version-skew tests for POST /api/sensor/push"
      min_lines: 80
    - path: "tests/test_console_enroll.py"
      provides: "console enroll provisioning tests (rows written, token hash-only, duplicate fails)"
      min_lines: 30
    - path: "tests/scanner/test_phase57_invariants.py"
      provides: "INGEST_FILES safe_str AST gate covering console_cmd.py + sensor.py"
      contains: "INGEST_FILES"
    - path: "docs/UAT-SERIES.md"
      provides: "UAT series entry for the console ingestion endpoint"
  key_links:
    - from: "tests/test_sensor_ingest.py"
      to: "quirk/dashboard/api/app.py::create_app"
      via: "TestClient + dependency_overrides[get_db]"
      pattern: "TestClient\\("
    - from: "tests/scanner/test_phase57_invariants.py"
      to: "quirk/dashboard/api/routes/sensor.py + quirk/cli/console_cmd.py"
      via: "INGEST_FILES parametrized AST gate"
      pattern: "INGEST_FILES"
---

<objective>
Author the Wave 0 test suite proving CONSOLE-02..05 (401 gating, 413/409/422/200, audit-per-attempt,
extra='ignore' + version-skew graceful) and the console enroll provisioning behavior; extend the
existing safe_str AST gate to cover the two new/modified ingest modules; then update docs/UAT-SERIES.md
and sync to Obsidian per CLAUDE.md mandatory phase-completion steps.

Purpose: Lock the security contract behind automated tests and the static safe_str gate.
Output: Two new test files, extended AST gate, updated + synced UAT-SERIES.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-console-ingestion-api/109-CONTEXT.md
@.planning/phases/109-console-ingestion-api/109-RESEARCH.md
@.planning/phases/109-console-ingestion-api/109-PATTERNS.md
@.planning/phases/109-console-ingestion-api/109-01-SUMMARY.md
@.planning/phases/109-console-ingestion-api/109-02-SUMMARY.md

<interfaces>
<!-- Extracted from codebase — executor needs no further exploration. -->

tests/test_api_auth.py — in-memory engine + TestClient factory + dependency_overrides[get_db]
  pattern (109-PATTERNS.md lines 262-313); authed fixture sets QUIRK_API_TOKEN="test-token".
quirk/dashboard/api/deps.py::get_db — override target.
quirk/models.py::Base.metadata.create_all(engine) — schema bootstrap for in-memory DB.
quirk/cli/console_cmd.py::run_console / _cmd_enroll — enroll entrypoint (Plan 01).
quirk/cli/sensor_cmd.py::_build_envelope + _build_compressed_payload — canonical zstd body
  serializers (108-02-D-03): reuse to build a valid push body for the 200 test.
tests/scanner/test_phase57_invariants.py — REPO_ROOT, _strip_comments helper, SCANNER_FILES,
  @pytest.mark.parametrize gate pattern (lines 10-54).
pushed_at format: "%Y-%m-%dT%H:%M:%SZ".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tests/test_sensor_ingest.py — full POST /api/sensor/push contract</name>
  <files>tests/test_sensor_ingest.py</files>
  <read_first>
    - tests/test_api_auth.py (TestClient factory + auth fixtures — lines 28-72, 303-338)
    - 109-PATTERNS.md (test file analog, lines 258-315)
    - 109-RESEARCH.md (TestClient 401 pattern lines 670-710; Test Map lines 793-805)
    - quirk/cli/sensor_cmd.py (_build_envelope + _build_compressed_payload — for the valid-body 200 test)
  </read_first>
  <action>
    Create tests/test_sensor_ingest.py. Build an in-memory shared SQLite engine + TestClient factory
    overriding get_db (mirror test_api_auth.py); a helper to seed a Sensor row (so valid-push and
    dedup tests have a known sensor_id). Tests:
    (a) test_push_endpoint_exists — route registered (CONSOLE-01).
    (b) test_push_requires_auth — no auth header → 401 with QUIRK_API_TOKEN set (CONSOLE-02).
    (c) test_push_413_body_too_large — body > 10MB → 413 (CONSOLE-03).
    (d) test_push_409_duplicate_payload — same payload_id pushed twice → first 200, second 409 (CONSOLE-03).
    (e) test_push_422_replay_window — pushed_at > 15 min from now → 422, body contains "console_utc" (CONSOLE-03).
    (f) test_push_200_accepted — valid compressed envelope (built via sensor_cmd serializers) → 200,
        one sensor_pushes row, sensors.last_push_at set, CryptoEndpoint rows written tagged sensor_id/segment (CONSOLE-03).
    (g) test_audit_row_written — after success AND after a failure (e.g. 422), an IntegrationDelivery
        row with destination="sensor_push" exists; failure row status="failed" (CONSOLE-04).
    (h) test_extra_fields_ignored — envelope with an unknown extra field still parses + 200 (CONSOLE-05).
    (i) test_version_skew_graceful — mismatched schema_version → not 422/500 (graceful) (CONSOLE-05).
    (j) test_unknown_sensor_id_4xx — push with unregistered sensor_id → 4xx, audited.
    Set the bearer token via Authorization header for non-auth tests. Name tests so
    `pytest -k "sensor_push or ingest or valid or unauth or size or replay or skew or audit or version_skew or extra_ignore"`
    selects them (align with 109-VALIDATION.md commands).
  </action>
  <verify>
    <automated>pytest tests/test_sensor_ingest.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All ten tests pass.
    - test_push_requires_auth asserts status_code == 401.
    - test_push_200_accepted asserts a sensor_pushes row + last_push_at + CryptoEndpoint rows exist.
    - test_audit_row_written asserts an IntegrationDelivery row on both success and a failure branch.
    - test_version_skew_graceful asserts the response is not 422 and not 500.
  </acceptance_criteria>
  <done>The full POST /api/sensor/push security contract is locked behind automated tests.</done>
</task>

<task type="auto">
  <name>Task 2: tests/test_console_enroll.py + extend safe_str AST gate</name>
  <files>tests/test_console_enroll.py, tests/scanner/test_phase57_invariants.py</files>
  <read_first>
    - tests/scanner/test_phase57_invariants.py (REPO_ROOT, _strip_comments, SCANNER_FILES, parametrize gate — lines 10-54)
    - 109-RESEARCH.md (AST gate extension, lines 712-735)
    - 109-PATTERNS.md (AST gate extend pattern, lines 319-359)
    - quirk/cli/console_cmd.py::_cmd_enroll (Plan 01)
  </read_first>
  <action>
    Create tests/test_console_enroll.py: using a temp DB path (QUIRK_DB_PATH conftest fixture or a
    tmp_path + monkeypatch on _default_db_path), invoke `run_console(["enroll","--sensor-id","S1","--segment","dmz"])`,
    capture stdout, and assert: one sensors row (sensor_id="S1", segment="dmz") and one sensor_tokens
    row exist; the printed raw token's SHA-256 hex equals sensor_tokens.token_hash; the raw token does
    not appear in any DB column. Add test_console_enroll_duplicate: re-running with the same sensor_id
    exits non-zero (pytest.raises(SystemExit)) and writes no second row pair. Name tests so
    `pytest -k "console_enroll"` selects them.
    In tests/scanner/test_phase57_invariants.py: add an `INGEST_FILES` list
    [REPO_ROOT/"quirk"/"cli"/"console_cmd.py", REPO_ROOT/"quirk"/"dashboard"/"api"/"routes"/"sensor.py"]
    and a parametrized `test_ingest_no_raw_exception_stringification` that strips comments via the
    existing `_strip_comments` helper and asserts no `\bstr\s*\(\s*exc\b` and no `\brepr\s*\(\s*exc\b`
    matches (use `grep -v '^#'`-equivalent comment stripping already provided). ID by file name.
  </action>
  <verify>
    <automated>pytest tests/test_console_enroll.py tests/scanner/test_phase57_invariants.py -q</automated>
  </verify>
  <acceptance_criteria>
    - test_console_enroll proves rows written + token hash-only persistence.
    - test_console_enroll_duplicate proves clean failure with no partial second write.
    - INGEST_FILES gate runs over console_cmd.py and sensor.py and finds no raw str(exc)/repr(exc).
    - `python -m compileall quirk run_scan.py` clean.
  </acceptance_criteria>
  <done>Provisioning behavior + safe_str static gate are both covered for the new ingest modules.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs/UAT-SERIES.md and sync to Obsidian</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (header date + existing series structure)
    - CLAUDE.md (Mandatory Phase Completion Steps 2-4 — UAT-SERIES update + Obsidian sync commands)
  </read_first>
  <action>
    Update docs/UAT-SERIES.md: add UAT cases to the relevant series for the console ingestion endpoint
    — operator runs `quirk console enroll`, configures the sensor, runs `quirk sensor push`, and the
    console returns 200 + persists rows; plus negative cases (unauthenticated → 401, replay/oversize/
    duplicate). Update the `**Last Updated:**` date to today. Then sync to the Obsidian vault by writing
    the file (with the project frontmatter block) to
    `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` per the CLAUDE.md printf+cat+cp recipe
    (file too large for CLI content=). Do NOT use `obsidian content=` for this file.
  </action>
  <verify>
    <automated>grep -q "sensor/push\|console enroll\|Console Ingestion" docs/UAT-SERIES.md && test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"</automated>
  </verify>
  <acceptance_criteria>
    - docs/UAT-SERIES.md references the console ingestion endpoint and has today's Last Updated date.
    - The Obsidian vault copy at 20_Dev-Work/QUIRK/UAT-Series.md exists with frontmatter.
  </acceptance_criteria>
  <done>UAT series documents the ingestion endpoint and is synced to Obsidian.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app under test | TestClient exercises the auth/replay/dedup boundaries with in-memory DB |
| source files → static AST gate | safe_str gate inspects ingest modules for raw exception stringification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-12 | Repudiation | regression of audit-per-attempt | mitigate | test_audit_row_written asserts a row on success AND failure branches |
| T-109-13 | Information Disclosure | reintroduced raw str(exc) in ingest path | mitigate | INGEST_FILES AST gate fails CI on any str(exc)/repr(exc) in console_cmd.py or sensor.py |
| T-109-14 | Elevation of Privilege | future auth-bypass regression | mitigate | test_push_requires_auth (401) + existing test_all_mutating_routes_have_auth_dependency gate |
| T-109-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages; N/A per 109-RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- `pytest tests/ -k "sensor_push or console_enroll or ingest" -q` green (109-VALIDATION quick command).
- `pytest tests/ -q && python -m compileall quirk run_scan.py` green (full suite).
- Obsidian UAT-Series.md present.
</verification>

<success_criteria>
- CONSOLE-02..05 each proven by at least one automated test (per 109-VALIDATION map).
- safe_str AST gate extended to the two ingest modules.
- docs/UAT-SERIES.md updated and synced to Obsidian.
</success_criteria>

<output>
Create `.planning/phases/109-console-ingestion-api/109-03-SUMMARY.md` when done.
</output>
