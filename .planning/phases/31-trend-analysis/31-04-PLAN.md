---
phase: 31-trend-analysis
plan: 04
type: execute
wave: 3
depends_on: ["31-03"]
files_modified:
  - docs/UAT-SERIES.md
  - docs/intelligence-schema.md
  - README.md
autonomous: true
requirements: [TREND-01, TREND-02, TREND-03, TREND-04]
tags: [docs, uat-series, obsidian, trend-analysis]
must_haves:
  truths:
    - "docs/UAT-SERIES.md has new test entries UAT-9-09 and UAT-9-10 appended after UAT-9-08"
    - "docs/UAT-SERIES.md header **Last Updated:** line includes a Phase 31 update note covering TREND-01/02/03/04"
    - "docs/intelligence-schema.md documents the TrendReport schema (sessions block, score_delta, new_finding_counts, resolved_finding_counts, scan_errors_*, sample arrays) — at least one schema block + one example response"
    - "README.md Features section names Trend Analysis as a v4.3 capability"
    - "Obsidian phase note Phase-31-Trend-Analysis.md exists with frontmatter (project, type phase, status complete, source, updated) + Goal + Requirements Covered + Success Criteria + What Was Built (one subsection per plan) + Key Decisions + Roadmap link"
    - "Obsidian UAT-Series.md is overwritten with the latest docs/UAT-SERIES.md content (frontmatter + body)"
    - "Obsidian Roadmap.md is overwritten with the latest .planning/ROADMAP.md content (frontmatter + body) — Phase 31 line shows the 4 plans"
    - "No regressions: python -m pytest tests/ -x -q still passes"
  artifacts:
    - path: "docs/UAT-SERIES.md"
      provides: "UAT-9-09 + UAT-9-10 entries documenting trend feature acceptance"
      contains: "UAT-9-09"
    - path: "docs/intelligence-schema.md"
      provides: "TrendReport schema documentation"
      contains: "TrendReport"
    - path: "README.md"
      provides: "Trend Analysis listed in v4.3 features"
      contains: "Trend"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-31-Trend-Analysis.md"
      provides: "Obsidian phase note"
      min_lines: 60
      contains: "type: phase"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
      provides: "Obsidian UAT series mirror"
      contains: "UAT-9-09"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Roadmap.md"
      provides: "Obsidian roadmap mirror"
      contains: "Phase 31: Trend Analysis"
  key_links:
    - from: "docs/UAT-SERIES.md"
      to: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
      via: "filesystem copy with frontmatter prepended"
      pattern: "UAT-9-09"
    - from: ".planning/phases/31-trend-analysis/31-XX-SUMMARY.md"
      to: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-31-Trend-Analysis.md"
      via: "What Was Built per-plan subsections"
      pattern: "Plan 31-01"
---

<objective>
Wave 3 — exit ceremony for Phase 31. Updates docs/UAT-SERIES.md with the trend feature acceptance tests, documents the TrendReport schema in docs/intelligence-schema.md, mentions the feature in README.md, then mirrors UAT-SERIES.md, ROADMAP.md, and a fresh Phase 31 note into the Obsidian vault per CLAUDE.md mandatory phase completion steps.

Purpose: The CLAUDE.md mandatory phase completion section requires all four artifacts (Obsidian phase note, UAT-SERIES.md update, vault UAT-Series.md sync, Roadmap.md sync) at the end of every phase. They are gating: /gsd-execute-phase completion is invalid without them. Per MEMORY.md feedback "All backlog phase plans must include explicit documentation update and Obsidian vault sync tasks" + "After every phase, update docs/UAT-SERIES.md (relevant series) and sync to Obsidian before offering next steps".

Output: Two new UAT entries, schema reference docs, README mention, three vault notes (Phase, UAT, Roadmap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-trend-analysis/31-CONTEXT.md
@.planning/phases/31-trend-analysis/31-RESEARCH.md
@.planning/phases/31-trend-analysis/31-UI-SPEC.md
@.planning/phases/31-trend-analysis/31-01-PLAN.md
@.planning/phases/31-trend-analysis/31-02-PLAN.md
@.planning/phases/31-trend-analysis/31-03-PLAN.md

<!-- Phase 30 was the most recent phase to ship the same docs/UAT/Obsidian pattern.
     Use it as the format analog. -->
@.planning/phases/30-hashicorp-vault-connector/30-03-SUMMARY.md

<interfaces>
<!-- Existing UAT-SERIES.md insertion points (the executor should NOT re-discover these — use them directly) -->

UAT-9-08 ends at line ~3840 of docs/UAT-SERIES.md (just before "### UAT-10-01"). Append:

  ### UAT-9-09: Trend Report — Score Delta + New/Resolved Counts (Phase 31)
  ### UAT-9-10: Trends Tab — Baseline Empty State (Phase 31)

Insert these BEFORE the closing horizontal-rule that precedes "# Series 10:" (or "### UAT-10-01").

Header **Last Updated:** line is at line 4 of the file. The current value reads:

  **Last Updated:** 2026-04-26 (Phase 29 complete: ... Phase 30: added UAT-30-01/02/03 ...)

Append a Phase 31 update note: `; Phase 31: added UAT-9-09/10 for Trend Analysis — score delta + new/resolved finding counts via /api/trends and React /trends tab`.

<!-- Phase 30 vault note format (template to mirror) -->

The Phase 30 vault note at /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-30-HashiCorp-Vault-Connector.md uses this structure:

  (frontmatter: project, type: phase, status: complete, source, updated)
  # Phase 30 — HashiCorp Vault Connector
  ## Goal
  ## Requirements Covered
  ## Success Criteria
  ## What Was Built
    ### Plan 30-01: ...
    ### Plan 30-02: ...
    ### Plan 30-03: ...
  ## Key Decisions
  ## Deferred (post-v4.3)
  ## Links
    - [[Roadmap]]
    - [[Phase-29-Kubernetes-Secrets-Inspection]] — preceding DAR scanner

<!-- Roadmap.md vault frontmatter pattern -->

  project: QU.I.R.K.
  type: roadmap
  status: active
  source: .planning/ROADMAP.md
  updated: 2026-04-26

<!-- UAT-Series.md vault frontmatter pattern -->

  project: QU.I.R.K.
  type: reference
  status: active
  source: docs/UAT-SERIES.md
  updated: 2026-04-26
</interfaces>

<read_first>
- .planning/phases/31-trend-analysis/31-01-SUMMARY.md (must exist after Plan 01 completes)
- .planning/phases/31-trend-analysis/31-02-SUMMARY.md (must exist after Plan 02 completes)
- .planning/phases/31-trend-analysis/31-03-SUMMARY.md (must exist after Plan 03 completes)
- docs/UAT-SERIES.md (lines 1–10 for header, lines 3820–3845 for append point — DO NOT read the whole file)
- docs/intelligence-schema.md (entire file — small reference doc)
- README.md (entire file — find the Features section)
- /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-30-HashiCorp-Vault-Connector.md (template analog)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append UAT-9-09 + UAT-9-10 to docs/UAT-SERIES.md and update Last Updated header</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
- Read docs/UAT-SERIES.md lines 1-10 to confirm **Last Updated:** line position and current text
- Read docs/UAT-SERIES.md lines 3820-3845 to find the exact append point (end of UAT-9-08, before "### UAT-10-01")
- Read .planning/phases/31-trend-analysis/31-CONTEXT.md to confirm acceptance behaviors for UAT entries
- Read .planning/phases/31-trend-analysis/31-UI-SPEC.md to source exact copy strings for the empty-state UAT
  </read_first>
  <action>
Two edits to docs/UAT-SERIES.md.

EDIT A — update the **Last Updated:** header (line 4):

Append the Phase 31 note INSIDE the existing parenthesized list. The current line ends with "token always-HIGH unconditional)" — change the closing ")" to:

  "; Phase 31: added UAT-9-09/10 for Trend Analysis — score delta + new/resolved finding counts via /api/trends and React /trends tab)"

EDIT B — append two new UAT entries before the "# Series 10:" boundary.

Locate the line "### UAT-9-08: CBOM XML — Schema Validation" block in docs/UAT-SERIES.md (around line 3805). Find the closing horizontal-rule that ends the UAT-9-08 block and immediately precedes "# Series 10:" (or "### UAT-10-01"). BEFORE that boundary, insert the following two UAT entries.

UAT-9-09 entry:

  Heading: `### UAT-9-09: Trend Report — Score Delta + New/Resolved Counts (Phase 31)`

  Blockquote intro: "Added Phase 31 (2026-04-26): Validates TREND-01/02/03 — compute_trend_report() correctly identifies score delta and per-severity new/resolved finding counts between the two most recent distinct scan sessions, via (host, port, protocol, severity) match key."

  Prerequisites: SQLite DB with at least 2 distinct scan sessions completed (run quirk twice against any chaos lab profile, with at least 1 second between runs).

  Steps (numbered):
  1. Confirm DB has ≥2 distinct sessions: open quirk-output/*.sqlite and run "SELECT DISTINCT strftime('%Y-%m-%d %H:%M:%S', scanned_at) FROM crypto_endpoint WHERE scanned_at IS NOT NULL ORDER BY scanned_at DESC LIMIT 5;" — expect ≥2 rows.
  2. Start the dashboard backend: "quirk serve" (or "uvicorn quirk.dashboard.api.app:app").
  3. "curl -s http://localhost:8000/api/trends | jq ." — capture response.

  Expected:
  - HTTP 200 with the documented schema: sessions, score_delta, new_finding_counts, resolved_finding_counts, scan_errors_new_count, scan_errors_resolved_count, new_findings_sample, resolved_findings_sample.
  - sessions.previous_ts is non-null when ≥2 sessions exist; score_delta is a non-null integer (positive, negative, or zero).
  - new_finding_counts and resolved_finding_counts each contain exactly the keys high, medium, low with non-negative integer values.
  - Sample arrays are length-capped at 5.

  Pass Criteria:
  - python -m pytest tests/test_intelligence_trends.py tests/test_dashboard_trends.py -q is green
  - Response schema matches docs/intelligence-schema.md TrendReport block
  - Sample arrays do not contain INFO-severity rows (D-05 — INFO is excluded from buckets)
  - scan_errors_new_count and scan_errors_resolved_count are reported separately from the severity buckets (D-04 — scan_error rows excluded from finding delta)

  Result/Date/Tester/Notes lines (mirror UAT-30 format).

UAT-9-10 entry:

  Heading: `### UAT-9-10: Trends Tab — Baseline Empty State (Phase 31)`

  Blockquote intro: "Added Phase 31 (2026-04-26): Validates TREND-04 + D-06 — when fewer than 2 distinct scan sessions exist, /trends renders the 'Baseline scan recorded' empty state and score_delta is null (NOT zero). NULL scanned_at rows from v4.2-era data are excluded from session counting (D-13)."

  Prerequisites: Either an empty SQLite DB OR a DB with exactly 1 distinct non-NULL scanned_at session.

  Steps (numbered):
  1. Confirm session count: "SELECT COUNT(DISTINCT strftime('%Y-%m-%d %H:%M:%S', scanned_at)) FROM crypto_endpoint WHERE scanned_at IS NOT NULL;" — expect 0 or 1.
  2. Open the dashboard at http://localhost:8000, click the "Trends" tab in the sidebar.
  3. Confirm the rendered state matches the UI-SPEC empty state copy: "Baseline scan recorded".
  4. Hit GET /api/trends directly via curl and inspect the response.

  Expected:
  - Sidebar shows a Trends nav entry with the TrendingUp lucide icon.
  - /trends page renders the baseline empty state — NO score delta card, NO new/resolved counts, just the empty-state messaging from 31-UI-SPEC.md.
  - API response: score_delta is null (JSON null, not 0); sessions.previous_ts is null; new_finding_counts / resolved_finding_counts / sample arrays are zeroed/empty.

  Pass Criteria:
  - Trends nav entry visible and active-state styling matches other nav entries
  - Empty-state component rendered (no score delta card)
  - score_delta is JSON null in API response (verify with "jq '.score_delta'" returning null, not 0)
  - A row with scanned_at IS NULL (manually inserted v4.2-era simulation) does NOT count toward the session total — /trends still shows the empty state if that NULL row is the only data

  Result/Date/Tester/Notes lines.

Use Edit for Edit A (single line replacement). Use Edit for Edit B — find the unique line "### UAT-10-01: No Reachable Targets — Graceful Handling" and prepend the new content + a horizontal-rule separator before it.
  </action>
  <verify>
    <automated>grep -c '^### UAT-9-09' docs/UAT-SERIES.md | grep -q '^1$' &amp;&amp; grep -c '^### UAT-9-10' docs/UAT-SERIES.md | grep -q '^1$' &amp;&amp; grep -q 'Phase 31' docs/UAT-SERIES.md &amp;&amp; grep -q 'UAT-9-09/10 for Trend Analysis' docs/UAT-SERIES.md</automated>
  </verify>
  <acceptance_criteria>
- "grep -c '^### UAT-9-09:' docs/UAT-SERIES.md" returns exactly 1
- "grep -c '^### UAT-9-10:' docs/UAT-SERIES.md" returns exactly 1
- "grep 'Phase 31: added UAT-9-09/10' docs/UAT-SERIES.md" matches in the header **Last Updated:** line
- New entries sit between "### UAT-9-08:" and "### UAT-10-01:" in document order (verify with "grep -n '^### UAT-9-0[8-9]\|^### UAT-9-10\|^### UAT-10-01' docs/UAT-SERIES.md" — line numbers must be ascending)
- File still parses as well-formed Markdown (no orphan code fences)
  </acceptance_criteria>
  <done>UAT-9-09 and UAT-9-10 are present in docs/UAT-SERIES.md, the **Last Updated:** header records the Phase 31 update, and ordering is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Document TrendReport schema in docs/intelligence-schema.md and mention in README.md</name>
  <files>docs/intelligence-schema.md, README.md</files>
  <read_first>
- Read docs/intelligence-schema.md to find the existing schema documentation pattern (most likely a series of sections like ScanReport / SubScores — mirror that format)
- Read README.md and locate the Features / What's New / v4.3 section
- Read .planning/phases/31-trend-analysis/31-02-SUMMARY.md to confirm the final TrendReportResponse field list
  </read_first>
  <action>
EDIT A — docs/intelligence-schema.md:

Append a new section at the end of the file (after the last existing schema section). Use the same heading depth as the surrounding sections (likely "##"). Insert:

Section heading: "## TrendReport (v4.3, Phase 31)"

Intro paragraph: explains compute_trend_report() in quirk/intelligence/trends.py produces a session-over-session comparison between the two most recent distinct scan sessions; sessions are grouped by second-truncated scanned_at (matching list_scans() in the dashboard API); the endpoint GET /api/trends returns this schema.

Subsection "### Wire format": a fenced JSON block showing the full two-session response with every field (sessions.current_ts, sessions.previous_ts, score_delta, current_score, previous_score, new_finding_counts {high, medium, low}, resolved_finding_counts {high, medium, low}, scan_errors_new_count, scan_errors_resolved_count, new_findings_sample [array of {host, port, protocol, severity}], resolved_findings_sample).

Subsection "### Single-session response (D-06)": a fenced JSON block showing the collapsed single-session form where previous_ts is null, score_delta is JSON null (NOT 0), all counts are zero, all sample arrays are empty. Add a sentence: "score_delta is JSON null, NOT 0 — null signals 'no comparison available', which the dashboard renders as the baseline empty state."

Subsection "### Match key (D-03)": text — Findings are matched across sessions by the tuple (host, port, protocol, severity). A finding is "new" if it appears in the current session but not the previous. A finding is "resolved" if it appears in the previous session but not the current. A severity change on the same (host, port, protocol) surfaces as one resolved + one new entry.

Subsection "### Severity buckets (D-05)": a Markdown table with two columns (ORM severity, Bucket) — CRITICAL→high, HIGH→high, MEDIUM→medium, LOW→low, INFO→excluded (does not appear in counts or samples).

Subsection "### Excluded rows": bullet list:
- Rows with scan_error IS NOT NULL (D-04) — counted separately as scan_errors_new_count / scan_errors_resolved_count, NOT in the severity buckets.
- Rows with scanned_at IS NULL (D-13) — v4.2-era data ignored entirely; does not count toward session total.

Subsection "### Sample arrays (D-08)": text — Sample arrays are capped at 5 entries. Sort order is severity descending (high→low) then host ascending then port ascending. Sample entries contain only host, port, protocol, and severity — no remediation text, no algorithm details.

EDIT B — README.md:

Locate the v4.3 section (or the most recent Features / What's New block). Append one bullet that names the trend feature similar to existing v4.3 bullets:

  "**Trend Analysis (v4.3, Phase 31)** — quirk/intelligence/trends.py produces a session-over-session score delta plus per-severity new/resolved finding counts between the two most recent scan sessions. Surfaced via GET /api/trends and a new dashboard /trends tab. No new SQLite table — uses existing scanned_at grouping."

If the README has a Phase / Roadmap reference table, also flip Phase 31's status from "📋 planned" to "✅ shipped" (only if such a table exists — otherwise skip).
  </action>
  <verify>
    <automated>grep -q '## TrendReport' docs/intelligence-schema.md &amp;&amp; grep -q 'score_delta' docs/intelligence-schema.md &amp;&amp; grep -q 'new_findings_sample' docs/intelligence-schema.md &amp;&amp; grep -qi 'trend' README.md</automated>
  </verify>
  <acceptance_criteria>
- docs/intelligence-schema.md contains a "## TrendReport" heading
- The new section contains both the wire-format example AND the single-session example AND the severity-bucket table
- README.md mentions trend analysis (case-insensitive grep "trend" succeeds)
- No existing schema sections in docs/intelligence-schema.md were modified or removed
- python -m pytest tests/ -x -q still passes (sanity — these are docs-only edits)
  </acceptance_criteria>
  <done>TrendReport schema is documented with both wire-format examples and the severity bucket table, and README.md mentions the new feature.</done>
</task>

<task type="auto">
  <name>Task 3: Sync Roadmap.md, UAT-Series.md, and create Phase-31-Trend-Analysis.md in the Obsidian vault</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Roadmap.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-31-Trend-Analysis.md</files>
  <read_first>
- Read .planning/phases/31-trend-analysis/31-01-SUMMARY.md, 31-02-SUMMARY.md, 31-03-SUMMARY.md (REQUIRED — these are the source of truth for "What Was Built" subsections)
- Read .planning/phases/31-trend-analysis/31-CONTEXT.md Decisions section to source D-XX rationale lines for Key Decisions block
- Read .planning/ROADMAP.md (full file — small) to source roadmap content for vault sync
- Read docs/UAT-SERIES.md line 1 only to confirm new version date for vault frontmatter
- Read /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-30-HashiCorp-Vault-Connector.md as the format template
  </read_first>
  <action>
This task has THREE write actions. Use the Write tool directly — do NOT use cat heredocs (CLAUDE.md mandates filesystem writes for files this large).

ACTION A — Roadmap.md vault sync (overwrite):

Path: /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Roadmap.md

Content: prepend a YAML frontmatter block with project: QU.I.R.K., type: roadmap, status: active, source: .planning/ROADMAP.md, updated: 2026-04-26 — then concatenate the full content of .planning/ROADMAP.md verbatim.

ACTION B — UAT-Series.md vault sync (overwrite):

Path: /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md

Content: prepend a YAML frontmatter block with project: QU.I.R.K., type: reference, status: active, source: docs/UAT-SERIES.md, updated: 2026-04-26 — then concatenate the full content of docs/UAT-SERIES.md (now containing UAT-9-09/10).

Because docs/UAT-SERIES.md is ~4,340 lines, do this with two Read calls (offset 0 limit 2000, offset 2000 limit 2400) followed by a single Write call concatenating the prepended frontmatter + both halves. Do NOT modify the body.

ACTION C — Phase 31 vault note (create):

Path: /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-31-Trend-Analysis.md

Content: write a complete phase note mirroring the Phase 30 template. Required structure:

Frontmatter: project: QU.I.R.K., type: phase, status: complete, source: .planning/phases/31-trend-analysis, updated: 2026-04-26.

Title: "# Phase 31 — Trend Analysis"

"## Goal" section: The intelligence layer can compare the current scan session against the most recent previous session — surfacing readiness score delta, net-new findings (present in current, absent in previous), and resolved findings (present in previous, absent in current). All session grouping uses the existing scanned_at-based second-truncated grouping established by list_scans() in scan.py — no new SQLite table required.

"## Requirements Covered" section (4 bullets):
- TREND-01: compute_trend_report() in quirk/intelligence/trends.py returns a score delta between the two most recent distinct scan sessions
- TREND-02: Net-new findings counted per severity bucket using the (host, port, protocol, severity) match key (D-03)
- TREND-03: Resolved findings counted per severity bucket using the same match key; severity changes surface as one resolved + one new entry
- TREND-04: GET /api/trends exposes the report; React /trends tab renders score delta, per-severity counts, and top-5 sample tables; baseline empty state when fewer than two distinct sessions exist (D-06)

"## Success Criteria" section: bullets for pytest, dashboard build, sidebar nav, baseline + comparison rendering, NULL scanned_at exclusion (D-13), scan_error exclusion (D-04).

"## What Was Built" section with 4 subsections:
- "### Plan 31-01: Wave 0 RED test scaffold" — source from 31-01-SUMMARY.md, list created test files and test count
- "### Plan 31-02: Intelligence layer + API endpoint (Wave 1 GREEN)" — source from 31-02-SUMMARY.md, list compute_trend_report() helpers, schemas, route registration
- "### Plan 31-03: React Trends dashboard tab (Wave 2 + human-verify checkpoint)" — source from 31-03-SUMMARY.md, list types, hook, page component, route, sidebar entry
- "### Plan 31-04: Documentation, UAT, and Obsidian sync (Wave 3)" — list UAT-9-09/10, intelligence-schema.md TrendReport section, README mention, vault sync (this note + UAT + Roadmap)

"## Key Decisions" section (D-03, D-04, D-05, D-06, D-07, D-08, D-12, D-13 from CONTEXT.md):
- D-03: Match key is (host, port, protocol, severity) — severity change surfaces as one resolved + one new entry
- D-04: scan_error IS NOT NULL rows excluded from severity-bucket finding delta; counted separately
- D-05: Severity buckets: CRITICAL/HIGH→high; MEDIUM→medium; LOW→low; INFO→excluded
- D-06: When fewer than 2 distinct sessions exist, score_delta is JSON null (NOT 0)
- D-07: score_delta is Optional[int] (resolved RESEARCH.md Open Questions item 2)
- D-08: Sample arrays capped at 5; sorted by severity desc, then host asc, then port asc
- D-12: compute_trend_report() is a pure function — no datetime.now() (mirrors ISSUE-3 design discipline)
- D-13: Rows with scanned_at IS NULL (v4.2-era data) excluded from session counting

"## Deferred (post-v4.3)" section:
- Multi-session trend lines (more than 2 sessions on a chart)
- Per-target trend filtering (?host=... query params)
- CSV export of trend report from the dashboard

"## Links" section:
- [[Roadmap]]
- [[Phase-30-HashiCorp-Vault-Connector]] — preceding v4.3 phase

When sourcing the per-plan What Was Built subsections, populate placeholders with concrete file/line/component names from each SUMMARY. If a SUMMARY is missing (because earlier plans did not write one), inline a short summary based on the corresponding PLAN.md must_haves truths.

Important: The Links section MUST link to [[Phase-30-HashiCorp-Vault-Connector]] (the actual filename without the .md extension), NOT a fabricated link.
  </action>
  <verify>
    <automated>test -f /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-31-Trend-Analysis.md &amp;&amp; test -f /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md &amp;&amp; test -f /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Roadmap.md &amp;&amp; grep -q '^type: phase' /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-31-Trend-Analysis.md &amp;&amp; grep -q 'UAT-9-09' /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md &amp;&amp; grep -q 'Phase 31: Trend Analysis' /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Roadmap.md</automated>
  </verify>
  <acceptance_criteria>
- All three vault paths exist
- Phase-31-Trend-Analysis.md frontmatter has type: phase, status: complete, updated: 2026-04-26
- Phase note has all required sections: Goal, Requirements Covered, Success Criteria, What Was Built (with 4 subsections — one per plan), Key Decisions, Deferred, Links
- Phase note's What Was Built section has subsections for Plans 31-01, 31-02, 31-03, AND 31-04
- UAT-Series.md vault note contains UAT-9-09 AND UAT-9-10 (full sync was done)
- UAT-Series.md vault note has frontmatter with type: reference, source: docs/UAT-SERIES.md
- Roadmap.md vault note contains the Phase 31 entry with the 4 plan list
- Roadmap.md vault note has frontmatter with type: roadmap, source: .planning/ROADMAP.md
- All three notes have updated: 2026-04-26
- [[Roadmap]] and [[Phase-30-HashiCorp-Vault-Connector]] wikilinks present in phase note
  </acceptance_criteria>
  <done>Vault has the Phase 31 note, the latest UAT-Series.md, and the latest Roadmap.md — all with correct frontmatter and wikilinks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo filesystem → Obsidian vault filesystem | Both are local; the vault is on the same disk under /Users/digs/vaults/Digs/. No network crosses. |
| Editor / Markdown viewer → vault note content | Obsidian renders Markdown locally; no remote rendering of these notes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04-01 | Tampering | Markdown injection in vault notes (e.g., crafted host name from a finding lands in the phase note's Key Decisions block) | accept | Phase note content is sourced from project planning files (REQUIREMENTS.md, CONTEXT.md, SUMMARY.md), all of which are authored by the developer. No untrusted scan data flows into the phase note body. |
| T-31-04-02 | Information Disclosure | Vault notes may capture internal hostnames or filesystem paths from SUMMARY files | accept | The Obsidian vault Digs is local-only and not synced to any remote service. The user controls the vault contents. |
| T-31-04-03 | Repudiation | Vault writes do not produce a git commit / audit trail | accept | The vault is a personal knowledge store outside the repo; the corresponding source-of-truth changes (UAT-SERIES.md, ROADMAP.md, intelligence-schema.md, README.md, PLAN files) ARE committed to the repo. The vault is a derived artifact. |

No high-severity threats; all data flows are local + developer-authored.
</threat_model>

<verification>
After all three tasks complete:
1. python -m pytest tests/ -x -q — exit 0 (sanity — docs-only changes should not regress tests)
2. grep -c "^### UAT-9-09" docs/UAT-SERIES.md returns 1
3. grep -c "^### UAT-9-10" docs/UAT-SERIES.md returns 1
4. grep -q "## TrendReport" docs/intelligence-schema.md succeeds
5. grep -qi "trend" README.md succeeds
6. test -f /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-31-Trend-Analysis.md succeeds
7. grep -q "UAT-9-09" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md succeeds
8. grep -q "Phase 31: Trend Analysis" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Roadmap.md succeeds
</verification>

<success_criteria>
- docs/UAT-SERIES.md has UAT-9-09 + UAT-9-10 entries before Series 10 boundary
- docs/UAT-SERIES.md **Last Updated:** header records Phase 31 update
- docs/intelligence-schema.md documents the TrendReport schema with wire format and severity bucket map
- README.md mentions Trend Analysis as a v4.3 capability
- Obsidian vault has fresh Roadmap.md, UAT-Series.md, and Phase-31-Trend-Analysis.md (all with correct frontmatter)
- Phase 31 vault note has 4 What Was Built subsections (one per plan), Key Decisions, Deferred, Links
- All test suites still pass — no regressions from docs work
</success_criteria>

<output>
After completion, create .planning/phases/31-trend-analysis/31-04-SUMMARY.md documenting:
- UAT entries added (line numbers in docs/UAT-SERIES.md)
- intelligence-schema.md section added (line numbers)
- README.md edit (which section)
- Vault note paths created/updated (with file sizes)
- Confirmation python -m pytest tests/ -x -q exits 0
- Phase 31 status: ready for /gsd-verify-work and final /gsd-execute-phase close-out
</output>
