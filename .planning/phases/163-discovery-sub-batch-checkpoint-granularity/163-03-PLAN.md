---
phase: 163-discovery-sub-batch-checkpoint-granularity
plan: 03
type: execute
wave: 3
depends_on: ["163-02"]
files_modified:
  - docs/operators-guide.md
  - docs/UAT-SERIES.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Operators-Guide.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-163-Discovery-Batch-Checkpoint-Granularity.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/_QUIRK-Hub.md
autonomous: false
requirements: [DISC-08]

must_haves:
  truths:
    - "An operator reading docs/operators-guide.md learns that discovery resume is now batch-granular, what it costs in disk, and that changing target scope between runs invalidates batch alignment"
    - "docs/UAT-SERIES.md carries a Series 163 with executable steps for verifying batch-granular resume"
    - "The Obsidian vault reflects Phase 163 as complete with a current Operators-Guide and UAT-Series"
  artifacts:
    - path: "docs/operators-guide.md"
      provides: "Section 13 — Discovery Batch Resume"
      contains: "Discovery Batch Resume"
    - path: "docs/UAT-SERIES.md"
      provides: "Series 163 test cases"
      contains: "Series 163"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-163-Discovery-Batch-Checkpoint-Granularity.md"
      provides: "Obsidian phase note, status complete"
      contains: "status: complete"
  key_links:
    - from: "docs/operators-guide.md"
      to: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Operators-Guide.md"
      via: "direct filesystem copy with frontmatter prepended"
      pattern: "Discovery Batch Resume"
    - from: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/_QUIRK-Hub.md"
      to: "Phase-163-Discovery-Batch-Checkpoint-Granularity"
      via: "wikilink"
      pattern: "\\[\\[Phase-163"
---

<objective>
Close the mandatory per-phase documentation and Obsidian-sync obligations for Phase 163, and
land the operator-facing mitigation text that T-163-02 (unbounded per-batch cache growth),
T-163-05 (batch-ordinal drift on target-scope change), and T-163-06 (cache contents in the
output directory) each require.

Purpose: CLAUDE.md's Per-Phase Documentation Checklist is not optional — a scanner behavior
change with an operator-visible resume characteristic maps to `docs/operators-guide.md`, which
in turn REQUIRES an Obsidian vault re-sync in the same phase. Plus the three Mandatory Phase
Completion Steps (phase note, UAT-SERIES update, UAT-SERIES vault sync).

Output: a new operators-guide section, a UAT Series 163, three vault files written directly to
the vault filesystem, and a human-verified end-to-end resume walkthrough.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/163-discovery-sub-batch-checkpoint-granularity/163-CONTEXT.md
@.planning/phases/163-discovery-sub-batch-checkpoint-granularity/163-VALIDATION.md
@.planning/phases/163-discovery-sub-batch-checkpoint-granularity/163-01-SUMMARY.md
@.planning/phases/163-discovery-sub-batch-checkpoint-granularity/163-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Operators-guide section 13 + Obsidian Guides sync</name>
  <files>docs/operators-guide.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Operators-Guide.md</files>
  <read_first>
    - docs/operators-guide.md lines 1709-1860 (sections 10 "Discovery Liveness Pre-Pass",
      11 "Chunked Discovery Progress and Per-Batch Scaling", and 12 "OT/ICS Recurring-Scan Safety"
      — section 11 is the direct predecessor and the heading/subsection/table style to match;
      section 12 is currently the last section, so the new one is section 13)
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Operators-Guide.md lines 1-10 (the
      frontmatter block to preserve when re-syncing)
    - .planning/phases/163-discovery-sub-batch-checkpoint-granularity/163-02-SUMMARY.md
      (the shipped behavior, constant names, and cache key format to document accurately)
  </read_first>
  <action>
    Append a new `## 13. Discovery Batch Resume` section to `docs/operators-guide.md`, after
    section 12, matching sections 10-12's prose-plus-table style. It must state, concretely:

    1. What changed — `--resume-scan-id <scan_run_id>` now skips discovery batches that completed
      before the interruption instead of re-probing every batch from zero. Give the concrete
      scale example from CONTEXT.md: a /16 interrupted at batch 60 of 64 previously re-probed
      all ~65,000 hosts on resume; it now re-probes only the ~4,000 hosts in the unfinished
      batches.
    2. How to use it — note the `scan_run_id` from the original scan's output, then re-run the
      same command with `--resume-scan-id <id>`. State explicitly that `--cache` is NOT required
      for batch resume (D-02): resume state is written on every run that has a database path,
      independently of the `--cache` / `--cache-ttl-hours` whole-discovery-stage cache.
    3. Where the state lives — a `ScanCheckpoint` row per completed batch with stage
      `discovery:batch-N`, plus one small JSON file per batch at
      `{output_dir}/.cache/discovery-batch-{scan_run_id}-{N}.json`. No new table or column.
    4. **T-163-02 mitigation text (required):** resume state now occupies disk proportional to
      batch count. State the arithmetic: batches = ceil(total_hosts / 1024), so a /16 produces
      ~64 batch files and a /12 produces ~1,024. Each file holds only that batch's open-ports
      list, not raw nmap output. State the 30-day (720h) retention window after which a batch
      file is ignored on resume, and that `{output_dir}/.cache/` can be deleted at any time — the
      only consequence is that a subsequent resume re-probes those batches.
    5. **T-163-06 note (required):** those cache files contain the discovered host/port inventory
      and live in the same `{output_dir}` that already holds the CBOM, report, and SQLite DB —
      apply the same handling as the rest of the output directory.
    6. **T-163-05 / Pitfall 3 limitation (required):** batch numbering is a pure ordinal over the
      expanded target list. Resume assumes the target scope is unchanged since the original scan.
      If CIDRs, include-IPs, or `exclude_ips` are edited between the original run and the resume,
      batch alignment is undefined and the resumed scan may restore the wrong hosts' results.
      State the safe procedure: start a fresh scan rather than resuming after any target-scope
      edit. Note this is the same assumption the pre-existing stage-level resume already makes.
    7. A short table of the three failure/skip behaviors: batch completed + cache present →
      skipped; batch completed + cache expired or deleted → re-probed (checkpoint alone never
      causes a skip); batch failed with a `RuntimeError` → no checkpoint written, re-attempted on
      resume, and its error endpoint still recorded.

    Then re-sync to the vault. Write DIRECTLY to the vault filesystem — do NOT use the obsidian
    CLI `content=` parameter, the file is far too large for shell expansion. Preserve the existing
    frontmatter block (project / type / status / source / updated) with `updated` bumped to
    today's date, then the full current `docs/operators-guide.md` body. Use vault `Digs`, target
    `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Operators-Guide.md`.
  </action>
  <verify>
    <automated>grep -c "^## 13. Discovery Batch Resume" docs/operators-guide.md</automated>
    <automated>grep -c "resume-scan-id" docs/operators-guide.md</automated>
    <automated>diff <(tail -n +7 /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Operators-Guide.md) docs/operators-guide.md && echo VAULT_IN_SYNC</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## 13. Discovery Batch Resume" docs/operators-guide.md` returns 1
    - `grep -c "discovery:batch-" docs/operators-guide.md` returns at least 1
    - `grep -c "720" docs/operators-guide.md` returns at least 1
    - `grep -ci "proportional to batch count\|proportional to the batch count" docs/operators-guide.md` returns at least 1 (T-163-02)
    - `grep -ci "target scope is unchanged\|undefined" docs/operators-guide.md` returns at least 1 (T-163-05)
    - `grep -c "does NOT require\|not required" docs/operators-guide.md` returns at least 1, on a line mentioning `--cache` (D-02)
    - The vault file `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Operators-Guide.md` exists,
      its first line is `---`, its frontmatter contains `source: docs/operators-guide.md` and
      today's date in `updated:`, and its body after the frontmatter is byte-identical to
      `docs/operators-guide.md`
  </acceptance_criteria>
  <done>Section 13 documents batch resume including all three required threat/limitation notes, and the vault Operators-Guide is byte-identical to the repo doc.</done>
</task>

<task type="auto">
  <name>Task 2: UAT Series 163 + UAT-SERIES vault sync</name>
  <files>docs/UAT-SERIES.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md</files>
  <read_first>
    - docs/UAT-SERIES.md lines 1-30 (document header including the `**Last Updated:**` line to bump)
    - docs/UAT-SERIES.md from line 18912 to end (Series 162 — the immediately preceding series,
      whose case-ID scheme `UAT-162-NN`, table columns, Steps/Pass Criteria/Result structure, and
      heading format must be matched exactly)
    - .planning/phases/163-discovery-sub-batch-checkpoint-granularity/163-VALIDATION.md
      (the "Manual-Only Verifications" row — its test instructions are the basis for UAT-163-04)
  </read_first>
  <action>
    Append `## Series 163: Discovery Batch Checkpoint Granularity (Phase 163 — v5.15)` to
    `docs/UAT-SERIES.md`, matching Series 162's structure exactly. Four cases:

    - **UAT-163-01** (automated): run
      `pytest tests/test_discovery_batch_checkpoint.py -x -q` and
      `pytest tests/test_cli_dashboard_discovery_parity.py -x -q`. Pass criteria: both exit 0;
      the second file is unmodified from before Phase 163.
    - **UAT-163-02** (semi-automated, no lab required): run a small discovery scan with a
      `--db-path` set, then inspect `{output_dir}/.cache/` for
      `discovery-batch-<scan_run_id>-1.json` and query the SQLite DB for
      `SELECT stage, status FROM scan_checkpoints WHERE scan_run_id = '<id>'`. Pass criteria:
      at least one `discovery:batch-N` row with `status = 'completed'` AND the matching cache
      file exists — even though `--cache` was NOT passed (this is the D-02 canary).
    - **UAT-163-03** (semi-automated): re-run the same command with `--resume-scan-id <id>` and
      confirm the console prints `Discovery: batch 1/N — skipped (completed on a prior run)` for
      the previously completed batches. Pass criteria: skip lines appear; total wall time is
      materially shorter; the resulting inventory host/port count matches the original run's.
    - **UAT-163-04** (HUMAN-UAT, chaos lab / real range): the manual-only row from
      163-VALIDATION.md. Start `quirk scan --discovery nmap` against a range larger than 1024
      hosts, note the `scan_run_id`, `Ctrl-C` after several batches complete, then re-run with
      `--resume-scan-id <id>`. Pass criteria: the log shows completed batches skipped rather than
      re-probed, and the final inventory is equivalent to an uninterrupted full scan.

    Bump the `**Last Updated:**` line in the document header to today's date. Do not change any
    version string — Phase 163 is not a version bump.

    Then sync to the vault using the exact procedure from CLAUDE.md's Mandatory Phase Completion
    Step 3: write the frontmatter block (`project: QU.I.R.K.`, `type: reference`,
    `status: active`, `source: docs/UAT-SERIES.md`, `updated: <today>`) to a temp file, append
    `docs/UAT-SERIES.md`, then copy to
    `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md`. Do NOT use the obsidian CLI
    `content=` parameter.
  </action>
  <verify>
    <automated>grep -c "^## Series 163:" docs/UAT-SERIES.md</automated>
    <automated>grep -c "UAT-163-0" docs/UAT-SERIES.md</automated>
    <automated>diff <(tail -n +8 /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md) docs/UAT-SERIES.md && echo VAULT_IN_SYNC</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Series 163:" docs/UAT-SERIES.md` returns 1
    - `grep -c "UAT-163-0[1-4]" docs/UAT-SERIES.md` returns at least 4
    - `grep -c "HUMAN-UAT" docs/UAT-SERIES.md` increased by at least 1 versus the pre-edit count
    - `grep -m1 "Last Updated" docs/UAT-SERIES.md` contains today's date
    - `head -1 /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` is `---` and the
      frontmatter contains `source: docs/UAT-SERIES.md`
    - The vault UAT-Series body after the frontmatter is byte-identical to `docs/UAT-SERIES.md`
  </acceptance_criteria>
  <done>Series 163 exists with four cases including one HUMAN-UAT, the header date is current, and the vault copy matches.</done>
</task>

<task type="auto">
  <name>Task 3: Obsidian phase note + hub wikilink</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-163-Discovery-Batch-Checkpoint-Granularity.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/_QUIRK-Hub.md</files>
  <read_first>
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-162-Check-in-Scan-Scheduling.md
      (the most recent phase note — the exact frontmatter keys, section order, and heading depth
      to replicate)
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/_QUIRK-Hub.md (the Phases wikilink list and the
      current-phase callout to update)
    - .planning/phases/163-discovery-sub-batch-checkpoint-granularity/163-01-SUMMARY.md
    - .planning/phases/163-discovery-sub-batch-checkpoint-granularity/163-02-SUMMARY.md
  </read_first>
  <action>
    Write `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-163-Discovery-Batch-Checkpoint-Granularity.md`
    DIRECTLY to the vault filesystem (not via obsidian CLI `content=`).

    Frontmatter: `project: QU.I.R.K.`, `type: phase`, `status: complete`,
    `source: .planning/phases/163-discovery-sub-batch-checkpoint-granularity/`,
    `updated: <today>`.

    Body sections, matching Phase-162's structure: **Goal** (use CONTEXT.md's corrected framing —
    build the missing per-batch checkpoint/resume layer; explicitly note that ROADMAP.md's
    "tighten the existing v5.11 Phase 144 per-batch system" premise did not survive verification
    and that true sub-batch/intra-batch resume is deliberately deferred), **Requirements Covered**
    (DISC-08), **Success Criteria** (CONTEXT.md's four rewritten criteria, NOT the roadmap's),
    **What Was Built** with one subsection per plan sourced from the SUMMARY files (163-01
    serializer pair + Part A tests; 163-02 run_scan.py wiring + Part B AST tests; 163-03 docs and
    vault sync), **Decisions** (a compact D-01..D-07 list), **Threats** (T-163-01 through
    T-163-06 with their dispositions), and a closing `[[Roadmap]]` link.

    Then update `_QUIRK-Hub.md`: add
    `[[Phase-163-Discovery-Batch-Checkpoint-Granularity]]` to the Phases list in numeric order
    and move the current-phase callout off Phase 162. Verify no other wikilink in the hub is
    broken by the edit.
  </action>
  <verify>
    <automated>test -f /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-163-Discovery-Batch-Checkpoint-Granularity.md && grep -c "status: complete" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-163-Discovery-Batch-Checkpoint-Granularity.md</automated>
    <automated>grep -c "Phase-163-Discovery-Batch-Checkpoint-Granularity" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/_QUIRK-Hub.md</automated>
  </verify>
  <acceptance_criteria>
    - The phase note file exists and its frontmatter contains `type: phase`, `status: complete`,
      and today's date in `updated:`
    - `grep -c "DISC-08" <phase note>` returns at least 1
    - `grep -c "T-163-0" <phase note>` returns at least 6
    - `grep -c "\[\[Roadmap\]\]" <phase note>` returns 1
    - `grep -c "\[\[Phase-163-Discovery-Batch-Checkpoint-Granularity\]\]" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/_QUIRK-Hub.md` returns 1
    - Every `[[Phase-...]]` wikilink in `_QUIRK-Hub.md` resolves to an existing file under
      `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/` (verify by extracting the links and
      testing each path)
  </acceptance_criteria>
  <done>The Phase 163 vault note exists as complete with corrected goal framing, and the hub links to it with no dead wikilinks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of end-to-end batch resume (UAT-163-04)</name>
  <files>none (verification only)</files>
  <read_first>
    - docs/operators-guide.md section 13 (the text under review in step 8)
    - docs/UAT-SERIES.md Series 163, case UAT-163-04 (the canonical steps this checkpoint executes)
    - .planning/phases/163-discovery-sub-batch-checkpoint-granularity/163-02-SUMMARY.md
  </read_first>
  <action>
    Run the automated gate, then present the numbered walkthrough below to the user verbatim and
    WAIT for their visual confirmation. This is a user-led HUMAN-UAT: CLI output the agent
    gathers is corroborating evidence only and never a substitute for the user's own observation.
    Do not self-approve. Record the user's verdict, and the observed host/port counts from both
    the interrupted-then-resumed run and the reference full scan, in 163-03-SUMMARY.md.
  </action>
  <what-built>
    Batch-granular discovery resume: `run_scan.py`'s chunked discovery loop now writes a
    `discovery:batch-N` ScanCheckpoint row plus a small per-batch cache payload after each
    successful (or fully-dead) batch, and skips those batches on a `--resume-scan-id` run.
    A failed batch writes nothing and is re-attempted. `docs/operators-guide.md` section 13 and
    UAT Series 163 document the behavior; the Obsidian vault is synced.
  </what-built>
  <how-to-verify>
    1. Confirm the automated gate first — run `pytest tests/test_discovery_batch_checkpoint.py -x -q`
       and `pytest -x -q`. Both must exit 0.
    2. Start a discovery scan against a range larger than 1024 hosts (so it produces multiple
       batches), with `--discovery nmap` and a `--db-path` set. Example shape:
       `python run_scan.py --discovery nmap --db-path ./quirk.db --output-dir ./out` against your
       configured multi-batch CIDR. Do NOT pass `--cache` — that is the point of the test.
    3. Watch for `Discovery: batch N/M (X hosts checked)` lines. After at least 3 batches have
       printed, press `Ctrl-C`.
    4. Note the `scan_run_id` from the scan output. Confirm state landed:
       `sqlite3 ./quirk.db "SELECT stage, status FROM scan_checkpoints WHERE scan_run_id='<id>';"`
       should list `discovery:batch-1`, `discovery:batch-2`, ... as `completed`, and
       `ls ./out/.cache/discovery-batch-<id>-*.json` should list one file per completed batch.
    5. Re-run the identical command with `--resume-scan-id <id>` added.
    6. Expected: the previously completed batches print
       `Discovery: batch N/M — skipped (completed on a prior run)` and complete near-instantly;
       only the unfinished batches actually probe. Total time to reach the interruption point is
       dramatically shorter than step 2's.
    7. Expected: the final inventory (host/port count in the report or DB) is equivalent to what
       an uninterrupted full scan produces — no hosts silently dropped. This is the T-163-01 check
       and the most important one.
    8. Confirm the docs read correctly: open `docs/operators-guide.md` section 13 and check the
       disk-usage note and the target-scope-change warning make sense to you as the operator.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 163, or describe what did not match — especially any host count discrepancy between the resumed and uninterrupted inventories.</resume-signal>
  <acceptance_criteria>
    - `pytest tests/test_discovery_batch_checkpoint.py -x -q` and `pytest -x -q` both exit 0
    - The interrupted run produced at least 3 `discovery:batch-N` rows with `status = 'completed'`
      in `scan_checkpoints` for that `scan_run_id`, and one matching JSON file per row under
      `{output_dir}/.cache/`, WITHOUT `--cache` having been passed
    - The resumed run printed a `— skipped (completed on a prior run)` line for each of those
      batches and issued no nmap subprocess for them
    - The resumed run's final host/port inventory count equals the uninterrupted reference scan's
      (T-163-01: zero silently dropped hosts)
    - The user has explicitly typed "approved" (or equivalent) after performing the steps
  </acceptance_criteria>
  <done>The user has confirmed batch-granular resume works end to end on real targets and that the resumed inventory matches an uninterrupted scan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo `docs/` → Obsidian vault filesystem | One-way sync of operator documentation to a local vault outside the repo. No untrusted input; writes are to a fixed, enumerated path set. |
| operator → `{output_dir}/.cache/` | The operator reads and may delete batch cache files; documentation is the only control surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-02 | Denial of Service | per-batch cache file accumulation in `{output_dir}/.cache/` | mitigate (documentation) | This plan delivers the required operator-facing text: disk usage proportional to `ceil(total_hosts / 1024)` batch files, the 720h retention window, and the safe "delete `.cache/` at any time" guidance. Asserted by Task 1's grep acceptance criteria. |
| T-163-05 | Tampering | batch-ordinal drift when target scope changes between the original and resumed run | mitigate (documentation) | Operators-guide section 13 states explicitly that resume assumes unchanged target scope and that editing CIDRs/excludes produces undefined batch alignment, with "start a fresh scan instead" as the prescribed procedure. |
| T-163-06 | Information Disclosure | discovered host/port inventory persisted to `{output_dir}/.cache/` even without `--cache` | mitigate (documentation) | Section 13 notes the cache files carry the discovered inventory and belong to the same handling class as the CBOM, report, and SQLite DB already in that directory. |
| T-163-07 | Tampering | vault sync writing to a path outside the intended QUIRK folder | mitigate | All six write targets are absolute, enumerated in `files_modified`, and confined to `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/`. Direct filesystem writes only — no obsidian CLI `content=` shell expansion, which is both a size hazard and a quoting hazard on multi-thousand-line documents. |
</threat_model>

<verification>
- `grep -c "^## 13. Discovery Batch Resume" docs/operators-guide.md` returns 1
- `grep -c "^## Series 163:" docs/UAT-SERIES.md` returns 1
- Vault `Guides/Operators-Guide.md` and `UAT-Series.md` bodies are byte-identical to their repo
  sources (frontmatter-stripped diff is empty)
- Phase note exists at
  `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-163-Discovery-Batch-Checkpoint-Granularity.md`
  with `status: complete`
- Every `[[Phase-...]]` wikilink in `_QUIRK-Hub.md` resolves to an existing file
- `pytest -x -q` still green (docs-only plan must not regress anything)
- Human approval recorded for UAT-163-04
</verification>

<success_criteria>
- CLAUDE.md's Per-Phase Documentation Checklist row "New scanner signal / detector" and
  "New config option"-adjacent obligations are satisfied for the operator-guide surface
- All three T-163-02 / T-163-05 / T-163-06 documentation mitigations are present and greppable
- All three Mandatory Phase Completion Steps are done: Obsidian phase note, UAT-SERIES.md update,
  UAT-SERIES.md vault sync
- The human operator has confirmed end-to-end that a resumed interrupted scan skips completed
  batches AND produces an inventory equivalent to an uninterrupted scan
</success_criteria>

<output>
Create `.planning/phases/163-discovery-sub-batch-checkpoint-granularity/163-03-SUMMARY.md` when done.
</output>
