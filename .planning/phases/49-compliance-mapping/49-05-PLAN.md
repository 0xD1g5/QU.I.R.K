---
phase: 49-compliance-mapping
plan: 05
type: execute
wave: 3
depends_on: [49-03, 49-04]
files_modified:
  - docs/report-interpretation.md
  - docs/UAT-SERIES.md
autonomous: true
requirements: [COMPLY-05, COMPLY-09]
must_haves:
  truths:
    - "docs/report-interpretation.md contains a 'Compliance Summary' subsection explaining the new report section"
    - "docs/UAT-SERIES.md contains UAT-49-01..05 covering schema, freshness, title-join, CLI smoke, and report-render"
    - "docs/UAT-SERIES.md is mirrored to /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
    - "An Obsidian phase note exists at /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-49-Compliance-Mapping.md"
    - "The Obsidian _QUIRK-Hub.md MOC links the new phase note"
    - "docs/UAT-SERIES.md and the Obsidian phase note carry a forward-pointer to docs/operators-guide.md (Phase 50) for the full Compliance Map Maintenance prose"
  artifacts:
    - path: "docs/report-interpretation.md"
      provides: "Compliance Summary reading guide"
      contains: "Compliance Summary"
    - path: "docs/UAT-SERIES.md"
      provides: "UAT-49-01..05 + bumped Last Updated date"
      contains: "UAT-49-"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-49-Compliance-Mapping.md"
      provides: "Obsidian phase note per CLAUDE.md mandatory step 1"
      contains: "Phase 49"
  key_links:
    - from: "docs/UAT-SERIES.md"
      to: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
      via: "filesystem cp from /tmp build per CLAUDE.md mandatory step 3"
      pattern: "UAT-49-"
---

<objective>
Close Phase 49 by satisfying the four CLAUDE.md "Mandatory Phase Completion
Steps" (Obsidian phase note + UAT-SERIES update + UAT sync + commit) plus the
in-repo documentation update for the new Compliance Summary report section.
This plan also drops a forward-pointer stub for Phase 50's operators-guide
"Compliance Map Maintenance" section (per CONTEXT.md specifics — Phase 49
leaves a TODO marker; Phase 50 fills it).

Purpose: Close COMPLY-05 (operator documentation of the new section) and
COMPLY-09 (operator-facing maintenance signposting; full prose in Phase 50).
Land all CLAUDE.md sync requirements so the phase can be marked complete.
Output: 2 in-repo docs + 1 vault phase note + 1 vault UAT mirror + 1 vault
hub refresh + 1 git commit covering the in-repo docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/49-compliance-mapping/49-CONTEXT.md
@.planning/phases/49-03-SUMMARY.md
@.planning/phases/49-04-SUMMARY.md
@docs/UAT-SERIES.md
@docs/report-interpretation.md

<interfaces>
CLAUDE.md mandatory completion steps (verbatim):
  1. Create Obsidian Phase Note via filesystem write (NOT `obsidian content=`)
     at /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-49-Compliance-Mapping.md
  2. Update docs/UAT-SERIES.md (add UAT-49-01..05 + bump Last Updated)
  3. Sync UAT-SERIES.md to /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md
     via filesystem (build /tmp/uat_vault.md with frontmatter + cp)
  4. Commit docs/UAT-SERIES.md via gsd-tools.cjs commit

Obsidian frontmatter standard for the phase note:
  project: QU.I.R.K.
  type: phase
  status: complete
  source: .planning/phases/49-compliance-mapping/
  updated: <ISO date>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/report-interpretation.md with Compliance Summary subsection</name>
  <read_first>
    - docs/report-interpretation.md (entire file — find the existing section structure to mirror)
    - .planning/phases/49-compliance-mapping/49-CONTEXT.md §canonical_refs (mention of "add a 'Compliance Summary' sub-section")
    - .planning/phases/49-03-SUMMARY.md (the rendered HTML structure that the doc describes)
  </read_first>
  <action>
    Add a new subsection titled "Compliance Summary" to docs/report-interpretation.md. Place it AFTER the existing section that describes the "All Findings" / Technical Appendix and BEFORE any appendix/glossary section (or at end-of-file if no such pivot exists). Read the file's existing heading hierarchy and match it (## vs ###).

    Required prose (paraphrase to fit the doc's voice; the substrings below MUST appear verbatim for the smoke check):

    - Heading: "Compliance Summary" at the appropriate level.
    - Paragraph 1: Explain that QUIRK now maps each finding to PCI-DSS 4.0.1, HIPAA 45 CFR §164.312, and FIPS 140-3 control references and renders them in a "Compliance Summary" section of the HTML and PDF reports. Include the literal phrase: "PCI-DSS 4.0.1, HIPAA 45 CFR, and FIPS 140-3".
    - Paragraph 2: Describe the table columns (Severity, Finding, Control + version, Source URL with last-verified date). Note the source URL points to the authoritative regulator publication.
    - Paragraph 3: Describe the "Findings without compliance mapping" subsection — surfaces coverage gaps so the assessor can confirm whether absence is intentional (informational findings, observability advisories) or a real gap.
    - Paragraph 4 (forward-pointer per CONTEXT.md specifics): "The compliance map's review cadence and upgrade procedure for regulator revisions is documented in `docs/operators-guide.md` (Phase 50 — TODO at the time of writing)."
    - Paragraph 5 (operator pre-engagement note): "Operators can verify compliance map freshness before a client engagement by running `quirk compliance status` (use `--format json` for machine-readable output)."

    Per CLAUDE.md: minimal diff. Do NOT restructure existing sections.
  </action>
  <verify>
    <automated>grep -q "Compliance Summary" docs/report-interpretation.md && grep -q "PCI-DSS 4.0.1, HIPAA 45 CFR, and FIPS 140-3" docs/report-interpretation.md && grep -q "quirk compliance status" docs/report-interpretation.md && grep -q "Findings without compliance mapping" docs/report-interpretation.md</automated>
  </verify>
  <done>
    All four required substrings present in docs/report-interpretation.md. File still parses as valid Markdown (no broken headings).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add UAT-49-01..05 to docs/UAT-SERIES.md and bump Last Updated date</name>
  <read_first>
    - docs/UAT-SERIES.md (entire file — find the most recent UAT-48-* block to mirror format)
    - .planning/phases/49-01-SUMMARY.md, 49-02-SUMMARY.md, 49-03-SUMMARY.md, 49-04-SUMMARY.md (test commands + behaviors to cover in pass criteria)
    - CLAUDE.md "Mandatory Phase Completion Steps" §2
  </read_first>
  <action>
    1. Update the document-header `Last Updated:` line to today's ISO date (the date Plan 49-05 executes).

    2. Locate the UAT-48-* series block. Add a new UAT-49 series block immediately AFTER the UAT-48 block, mirroring its format exactly (heading level, table or list shape, "Pass criteria" wording).

    3. Required cases (each with Test ID, Setup, Steps, Pass criteria — fit to the existing UAT-SERIES.md format):

       - UAT-49-01 — Compliance schema gate
         - Run: `pytest tests/test_compliance_schema.py -x -q`
         - Pass: All assertions green; every COMPLIANCE_MAP entry has framework + control + version + last_verified (ISO) + source_url (https://).

       - UAT-49-02 — Compliance freshness gate
         - Run: `pytest tests/test_compliance_freshness.py -x -q`
         - Pass: No entry's last_verified is older than STALENESS_THRESHOLD_DAYS (365). Failure message names every stale entry with a fix recipe.

       - UAT-49-03 — Compliance title-join gate
         - Run: `pytest tests/test_compliance_title_join.py -x -q`
         - Pass: Every emitted finding title is in COMPLIANCE_MAP or in UNMAPPED_TITLES (with inline-comment justification per D-04).

       - UAT-49-04 — `quirk compliance status` CLI smoke
         - Run: `python run_scan.py compliance status` then `python run_scan.py compliance status --format json`
         - Pass: Text variant prints PCI-DSS / HIPAA / FIPS rows; JSON variant parses as a dict; both exit code 0.
         - Operator note: This is the pre-engagement freshness verification command per COMPLY-09 (see also Phase 50 operators-guide for review cadence).

       - UAT-49-05 — Compliance Summary section in HTML/PDF report
         - Setup: Run a chaos-lab scan that produces at least one mapped and one unmapped finding (e.g., `lab.sh up` + scan localhost via the tls-cert-defects profile).
         - Steps: Open the rendered report.html; visually confirm the Compliance Summary section with three framework subsections renders cleanly and source-URL links are clickable. Then check the rendered PDF inherits the section.
         - Pass: HTML smoke `pytest tests/test_compliance_report_section.py -x -q` is green AND visual confirmation passes.

    Per CLAUDE.md: minimal diff. Use the same heading levels and table/list conventions as UAT-48-*.
  </action>
  <verify>
    <automated>test $(grep -c "UAT-49-0[1-5]" docs/UAT-SERIES.md) -ge 5 && grep -q "quirk compliance status" docs/UAT-SERIES.md && head -30 docs/UAT-SERIES.md | grep -E "Last Updated:.*202[6-9]" -q</automated>
  </verify>
  <done>
    All five UAT-49 entries present (UAT-49-01..05). `quirk compliance status` mentioned. Header Last Updated line bumped to current year/date. File still renders cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Obsidian phase note + sync UAT-SERIES.md to vault + refresh _QUIRK-Hub.md + commit docs</name>
  <read_first>
    - CLAUDE.md "Mandatory Phase Completion Steps" §1, §3, §4
    - CLAUDE.md "Obsidian Vault Integration" + "Frontmatter Standards" + "Hub note"
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-48-Rich-Finding-Context.md (exact template pattern to mirror — read first, replicate frontmatter + section structure)
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/_QUIRK-Hub.md (current state of the hub MOC; will be edited to add Phase 49 link)
    - .planning/phases/49-compliance-mapping/49-01-SUMMARY.md, 49-02-SUMMARY.md, 49-03-SUMMARY.md, 49-04-SUMMARY.md (source for "What Was Built" subsections)
  </read_first>
  <action>
    1. Write the Obsidian phase note via filesystem (NOT `obsidian content=` per CLAUDE.md — phase notes are too large for shell expansion). Write to:
       /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-49-Compliance-Mapping.md

       Use the Write tool. Required structure:
       - Frontmatter: project: QU.I.R.K., type: phase, status: complete, source: .planning/phases/49-compliance-mapping/, updated: <today ISO date>.
       - "# Phase 49 — Compliance Mapping" heading.
       - "## Goal" — copy verbatim from ROADMAP.md Phase 49 Goal line.
       - "## Requirements Covered" — bulleted list of COMPLY-01..09 with one-line description each (lift from REQUIREMENTS.md).
       - "## Success Criteria" — copy verbatim from ROADMAP.md Phase 49 Success Criteria block.
       - "## What Was Built" — one "### Plan 49-NN — <title>" subsection per plan (49-01 through 49-05), each summarizing the SUMMARY.md contents in 2-4 bullet points.
       - "## Forward Pointer" — note that Phase 50 (Enterprise Documentation) will fill the docs/operators-guide.md "Compliance Map Maintenance" section that Phase 49 stubbed.
       - Trailing wikilink: [[Roadmap]].

    2. Sync docs/UAT-SERIES.md to the vault per CLAUDE.md §3 verbatim recipe:

       printf "---\nproject: QU.I.R.K.\ntype: reference\nstatus: active\nsource: docs/UAT-SERIES.md\nupdated: <today ISO date>\n---\n\n" > /tmp/uat_vault.md
       cat docs/UAT-SERIES.md >> /tmp/uat_vault.md
       cp /tmp/uat_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"

       Substitute the literal `<today ISO date>` with the actual date string.

    3. Refresh _QUIRK-Hub.md to add a wikilink to the new Phase 49 note. Read the current hub file, locate the Phases section, add [[Phase-49-Compliance-Mapping]] in the same format as existing entries (likely between Phase 48 and the next placeholder). If the hub tracks "current in-progress phase", flip Phase 49 to complete and any next-up phase to active per CLAUDE.md status conventions. Use the Edit tool for minimal diff.

    4. Commit the in-repo docs per CLAUDE.md §4:

       node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-49): update UAT-SERIES.md + report-interpretation.md (Compliance Summary)" --files docs/UAT-SERIES.md docs/report-interpretation.md

       Do NOT commit vault files (vault is outside the repo). Do NOT commit other files in this task — the production code commits land in their own plans (49-01..04).
  </action>
  <verify>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-49-Compliance-Mapping.md" && grep -q "type: phase" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-49-Compliance-Mapping.md" && grep -q "status: complete" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-49-Compliance-Mapping.md" && grep -q "UAT-49-0" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" && grep -q "Phase-49-Compliance-Mapping" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/_QUIRK-Hub.md" && git -C "/Volumes/Digs-1TB/Development/quantum-apps/QUIRK" log -1 --format=%s | grep -q "phase-49"</automated>
  </verify>
  <done>
    Vault phase note exists with correct frontmatter and status: complete. UAT-Series.md vault mirror contains UAT-49-* entries. _QUIRK-Hub.md links the new phase note. Most recent git commit on the repo references phase-49 docs. All four CLAUDE.md mandatory completion steps satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → vault filesystem | One-way write: docs/UAT-SERIES.md content cp'd into vault. No vault data flows back into the repo. |
| repo → git | Single commit covers two doc files only. No production code in this commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-14 | T (Tampering) | /tmp/uat_vault.md staging file | accept | /tmp is per-user; staging file is overwritten on each sync; no secrets flow through it. |
| T-49-15 | I (Information disclosure) | vault sync | accept | UAT-SERIES.md is already in the public repo; vault mirror adds no new information surface. |
</threat_model>

<verification>
- All three automated verify commands pass (Tasks 1, 2, 3)
- `git log -1 --format=%s` shows the docs commit on the current branch
- `pytest tests/test_compliance_*.py tests/test_pqc_terminology_gate.py -x -q` (regression check across the phase) all green
- Phase 49 ROADMAP.md entry can now be flipped from `[ ]` to `[x]` (handled by `/gsd-execute-phase` finalization)
</verification>

<success_criteria>
- COMPLY-05 fully closed (in-repo doc + visual UAT covered)
- COMPLY-09 closed at the level Phase 49 owns (CLI + signposting); Phase 50 owns the full operators-guide review-cadence prose
- All four CLAUDE.md "Mandatory Phase Completion Steps" satisfied
- No vault files committed to git; no production code in the docs commit
- Phase 49 ready to be marked complete in ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/49-compliance-mapping/49-05-SUMMARY.md`
</output>
