---
phase: 116-windows-packaging-spike
plan: 02
type: execute
wave: 2
depends_on: ["116-01"]
files_modified:
  - docs/windows-packaging-spike.md
  - docs/UAT-SERIES.md
autonomous: true
requirements: [WINPKG-01]

must_haves:
  truths:
    - "docs/windows-packaging-spike.md exists and covers all five WINPKG-01 criterion-1 topics: PyInstaller spec viability, hidden-import surface, Scheduled-Task-vs-Service trade-offs, CI validation results, and the v5.6 effort estimate"
    - "The assessment ends with a single unambiguous go / no-go / defer line plus rationale"
    - "The assessment recommends Windows Scheduled Task as primary v5.6 host model and documents Windows Service (NSSM) as the always-on alternative"
    - "The assessment documents the --onefile-vs-onedir trade-off and recommends --onedir for v5.6 production (sensor does not need report/dashboard dirs)"
    - "The assessment cites the windows-packaging-spike CI job + its uploaded evidence artifact, and carries a 'CI EXE is evidence-only, not for production' warning (D-06)"
    - "docs/UAT-SERIES.md has a Series 116 section and the Obsidian vault copy is synced"
  artifacts:
    - path: "docs/windows-packaging-spike.md"
      provides: "Windows packaging feasibility + sizing assessment with go/no-go"
      min_lines: 80
      contains: "Scheduled Task"
    - path: "docs/UAT-SERIES.md"
      provides: "UAT Series 116 entry"
      contains: "Series 116"
  key_links:
    - from: "docs/windows-packaging-spike.md"
      to: "windows-packaging-spike CI job"
      via: "cites pyinstaller-spike-evidence artifact"
      pattern: "windows-packaging-spike"
---

<objective>
Author `docs/windows-packaging-spike.md` — the Phase 116 feasibility + sizing assessment — from the RESEARCH evidence and the CI job created in Plan 01, ending in a single unambiguous go / no-go / defer recommendation (D-05). Then update `docs/UAT-SERIES.md` (Series 116) and sync the doc set to the Obsidian vault per CLAUDE.md mandatory phase-completion steps.

Purpose: WINPKG-01's primary deliverable. The document IS the spike's output — it turns the CI evidence and the research into a v5.6 go/no-go decision and effort estimate.
Output: `docs/windows-packaging-spike.md` (new), `docs/UAT-SERIES.md` (Series 116 added), Obsidian vault sync.

SCOPE GUARD (D-06): The doc MUST state the CI EXE is evidence-only and NOT a shipped artifact. Do NOT commit any `.spec`, EXE, installer, or NSIS file. An illustrative `.spec` listing inside the markdown (a fenced code block in the doc) is permitted as an appendix — that is documentation, not a committed build script.
NO scope reduction language: this is a spike; the assessment is the full deliverable, not a "v1".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-windows-packaging-spike/116-CONTEXT.md
@.planning/phases/116-windows-packaging-spike/116-RESEARCH.md
@.planning/phases/116-windows-packaging-spike/116-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/windows-packaging-spike.md assessment</name>
  <read_first>
    - 116-RESEARCH.md in full — it is the evidence base: §Freeze Target/Hook Coverage Matrix, §__file__ Risk Inventory, §Sensor Wire-Contract Compatibility, §Common Pitfalls, §Windows Host Model table, §Effort Sizing table, §Spec File Pattern (for the illustrative appendix)
    - 116-CONTEXT.md D-04 (Scheduled Task primary), D-05 (go/no-go threshold), D-06 (scope guard)
    - 116-01-SUMMARY.md — the exact job name, flag set, and evidence-artifact name to cite
  </read_first>
  <action>
    Create docs/windows-packaging-spike.md. Structure it around the five WINPKG-01 criterion-1 topics, each as a top-level section:
    1. "## PyInstaller Spec Viability" — run_scan.py is the freeze target (not the module:function string); the illustrative .spec appendix; --onefile vs --onedir trade-off table from RESEARCH, recommending --onedir for v5.6 production (sensor does not need report/dashboard dirs, and --onefile breaks _TEMPLATES_DIR/_STATIC_DIR + has AV/startup friction).
    2. "## Hidden-Import Surface" — the hook-coverage matrix; call out SQLAlchemy and FastAPI as the no-hook HIGH-risk deps needing --collect-all + the sqlalchemy.dialects.sqlite hidden-imports; the __file__ risk inventory and its conclusion that sensor-critical paths are already frozen-safe (STAB-02) while report/dashboard paths break only in --onefile.
    3. "## Windows Host Model: Scheduled Task vs Service" — reproduce the trade-off table; recommend Windows Scheduled Task as primary (D-04); document Windows Service via NSSM as the always-on alternative with its trade-offs.
    4. "## CI Validation Results" — describe the windows-packaging-spike job (non-blocking, --onefile build of run_scan.py with the full flag set, evidence uploaded as pyinstaller-spike-evidence). State explicitly that at authoring time the doc records the CI invocation + expected evidence and that the live build result posts on push/PR; include a placeholder line for the observed RESULT (BUILD_SUCCESS/EXE size or BUILD_FAILED + root cause) to be filled from the artifact. Include the D-06 warning that the CI EXE is evidence-only and not a production binary.
    5. "## v5.6 Effort Estimate" — reproduce the effort-sizing table (~4-5 days / one focused phase) with the t-shirt size.
    End the document with a section "## Recommendation" containing a SINGLE unambiguous line of the form `**GO**` / `**NO-GO**` / `**DEFER**` followed by rationale tied to the D-05 threshold (runnable EXE or only documented/fixable hidden-imports, AND effort fits one v5.6 phase). Default to GO if the evidence supports it per D-05; if the CI result is not yet observed, state the recommendation is GO conditional on the CI build producing a runnable EXE or only documented hidden-import failures, and note the condition explicitly. Do NOT use fenced code blocks for the recommendation line itself.
    Add an "## Appendix: Illustrative .spec (not committed)" section containing the spec listing from RESEARCH §Spec File Pattern, clearly labelled illustrative-only per D-06.
  </action>
  <verify>
    <automated>test -f docs/windows-packaging-spike.md && grep -qi "scheduled task" docs/windows-packaging-spike.md && grep -qi "onedir" docs/windows-packaging-spike.md && grep -qiE "GO|NO-GO|DEFER" docs/windows-packaging-spike.md && grep -qi "windows-packaging-spike" docs/windows-packaging-spike.md && grep -qi "evidence-only\|evidence only\|not.*production" docs/windows-packaging-spike.md && [ "$(grep -c . docs/windows-packaging-spike.md)" -ge 80 ]</automated>
  </verify>
  <acceptance_criteria>
    - docs/windows-packaging-spike.md exists with five topic sections (PyInstaller spec viability, hidden-import surface, Scheduled-Task-vs-Service, CI validation results, v5.6 effort estimate) — each grep-findable by heading keyword
    - `grep -ciE "GO|NO-GO|DEFER" docs/windows-packaging-spike.md` >= 1 and a "## Recommendation" heading exists
    - `grep -qi "scheduled task"` AND `grep -qi "service"` AND `grep -qi "NSSM"` all succeed (D-04 coverage)
    - `grep -qi "onedir"` AND `grep -qi "onefile"` succeed (trade-off documented)
    - `grep -qi "windows-packaging-spike"` AND `grep -qi "pyinstaller-spike-evidence"` succeed (cites the Plan 01 job + artifact)
    - An evidence-only / not-for-production warning is present (D-06)
    - Non-empty line count >= 80
    - No `.spec` or `.nsi` FILE committed to the repo: `! ls *.spec *.nsi docs/*.spec 2>/dev/null` (the illustrative spec lives inside the .md, not as a file)
  </acceptance_criteria>
  <done>docs/windows-packaging-spike.md covers all five topics, recommends Scheduled Task primary + Service/NSSM alternative, documents onedir-for-production, cites the non-blocking CI job + evidence artifact with the evidence-only warning, includes the effort estimate, and ends with a single GO/NO-GO/DEFER recommendation + rationale.</done>
</task>

<task type="auto">
  <name>Task 2: Update UAT-SERIES.md (Series 116) and sync to Obsidian</name>
  <read_first>
    - docs/UAT-SERIES.md tail (the existing Series 113/114/115 entries — match their format and heading style)
    - CLAUDE.md "Mandatory Phase Completion Steps" §2/§3 (UAT-SERIES update + vault sync mechanics) and "Sync Workflows" / Frontmatter Standards
    - docs/windows-packaging-spike.md (the deliverable being tested by the series)
  </read_first>
  <action>
    Append a new "## UAT Series 116 — Windows Packaging Spike (Phase 116)" section to docs/UAT-SERIES.md, mirroring the structure of the existing Series 113-115 entries. Include test cases that verify the spike deliverable: (a) docs/windows-packaging-spike.md exists and covers the five topics; (b) the windows-packaging-spike CI job is present and non-blocking; (c) the assessment ends with a clear go/no-go/defer recommendation; (d) scope guard — no frozen EXE/installer/NSIS committed (only the CI artifact + the assessment doc). Update the `**Last Updated:**` date at the top of the document to today's date.
    Then sync to the Obsidian vault per CLAUDE.md §3: write the UAT-SERIES vault copy via the printf-frontmatter + cat + cp pattern (the file is too large for CLI content=), targeting /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md. Do NOT use `obsidian CLI content=` for this large file. (The phase note and final commits are handled by the execute-phase completion flow; this task delivers the UAT-SERIES update + its vault sync.)
  </action>
  <verify>
    <automated>grep -qi "Series 116" docs/UAT-SERIES.md && grep -q "Last Updated" docs/UAT-SERIES.md && test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" && grep -qi "Series 116" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "Series 116" docs/UAT-SERIES.md` >= 1 with a heading matching the existing Series-NN format
    - The Series 116 entry includes a scope-guard test case (no EXE/installer/NSIS committed) and a go/no-go-present test case
    - `**Last Updated:**` reflects today's date
    - The Obsidian vault file /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md exists, contains "Series 116", and has the required frontmatter block (project/type/status/source/updated)
  </acceptance_criteria>
  <done>docs/UAT-SERIES.md has a Series 116 section (with scope-guard + go/no-go test cases) and an updated Last Updated date, and the Obsidian vault UAT-Series.md copy is synced with frontmatter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| assessment doc → reader | The go/no-go and "evidence-only" framing must be accurate; an over-promised "go" could authorize v5.6 work on a false premise |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-05 | Repudiation | go/no-go recommendation not backed by evidence | mitigate | Recommendation must be tied to the D-05 threshold and cite the CI evidence artifact; if CI result not yet observed, recommendation is stated as conditional on the build outcome |
| T-116-06 | Elevation of Privilege | reader treats illustrative .spec or CI EXE as production build script | mitigate | Appendix .spec labelled illustrative-only; explicit evidence-only / not-for-production warning per D-06 |
| T-116-SC | Tampering | npm/pip installs | accept | This plan installs no packages (doc authoring + file sync only) |
</threat_model>

<verification>
- docs/windows-packaging-spike.md exists, >= 80 non-empty lines, five topics + Recommendation
- Scheduled Task primary + Service/NSSM alternative + onedir-for-production all present
- Cites windows-packaging-spike job + pyinstaller-spike-evidence artifact + evidence-only warning
- docs/UAT-SERIES.md Series 116 added; Last Updated bumped; vault copy synced
- No `.spec` / `.nsi` / EXE committed (D-06)
</verification>

<success_criteria>
- Five-topic assessment with single go/no-go/defer recommendation + rationale (D-05)
- D-04 host-model recommendation present (Scheduled Task primary, Service/NSSM alternative)
- D-06 scope guard honored: evidence-only framing, no committed packaging artifact
- UAT Series 116 added and synced to Obsidian
</success_criteria>

<output>
Create `.planning/phases/116-windows-packaging-spike/116-02-SUMMARY.md` when done
</output>
