---
phase: 116-windows-packaging-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/python-ci.yml
  - run_scan.py
autonomous: true
requirements: [WINPKG-01]

must_haves:
  truths:
    - "A non-blocking windows-packaging-spike CI job exists and runs a real pyinstaller --onefile build of run_scan.py on windows-latest"
    - "The spike CI job cannot gate the pipeline (continue-on-error: true) so a build failure is documented, not blocking"
    - "PyInstaller is installed CI-only inline at the pinned version, not added to project deps or extras"
    - "Build evidence (build log, warn-quirk.txt, EXE if produced) is uploaded as a CI artifact for the assessment doc to cite"
    - "run_scan.py calls multiprocessing.freeze_support() first in its __main__ guard so a frozen Windows spawn does not recurse"
  artifacts:
    - path: ".github/workflows/python-ci.yml"
      provides: "windows-packaging-spike job mirroring windows-sensor-smoke"
      contains: "windows-packaging-spike"
    - path: "run_scan.py"
      provides: "freeze_support guard for Windows frozen multiprocessing"
      contains: "freeze_support"
  key_links:
    - from: ".github/workflows/python-ci.yml"
      to: "run_scan.py"
      via: "pyinstaller --onefile ... run_scan.py freeze target"
      pattern: "pyinstaller .*run_scan.py"
    - from: ".github/workflows/python-ci.yml"
      to: "pyinstaller-spike-evidence artifact"
      via: "actions/upload-artifact@v4"
      pattern: "upload-artifact"
---

<objective>
Add the non-blocking `windows-packaging-spike` CI job that runs a real `pyinstaller --onefile` build of the `quirk` entrypoint (`run_scan.py`) on `windows-latest`, capturing hidden-import/collection evidence as an uploaded artifact (D-01, D-02, D-03). Add the one-line `multiprocessing.freeze_support()` guard to `run_scan.py` to de-risk Windows frozen `spawn` (RESEARCH Open Q2 / Pitfall 7 / A6).

Purpose: Produce the live CI evidence that the Phase 116 assessment document (Plan 02) cites. This is the only executable half of the spike — the build either yields a runnable EXE or surfaces documented hidden-import failures, both of which feed the go/no-go.
Output: Updated `.github/workflows/python-ci.yml` (new job) and a single-line addition to `run_scan.py`.

SCOPE GUARD (D-06): No `.spec`, EXE, installer, or NSIS script is committed. The EXE only ever exists as a transient CI artifact (`retention-days: 30`). Do NOT add `dist/`, `build/`, `*.spec`, or `*.nsi` to the repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-windows-packaging-spike/116-CONTEXT.md
@.planning/phases/116-windows-packaging-spike/116-RESEARCH.md

<interfaces>
<!-- Existing windows-sensor-smoke job to mirror (.github/workflows/python-ci.yml lines 9-27) -->
The new job mirrors this exactly for setup (checkout@v4, setup-python@v5 python 3.11, `pip install -e .`):

```yaml
  windows-sensor-smoke:
    name: Windows Sensor Smoke
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install project (editable) and pytest
        run: |
          pip install -e .
          pip install pytest
      - name: Run Windows sensor smoke tests
        run: pytest tests/test_sensor_windows_smoke.py ...
```

<!-- run_scan.py entrypoint guard (lines 2254-2255) — freeze_support goes FIRST inside this block -->
```python
if __name__ == "__main__":
    _run_main_with_job_guard()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add multiprocessing.freeze_support() guard to run_scan.py</name>
  <read_first>
    - run_scan.py lines 2225-2255 (the _run_main_with_job_guard def + __main__ guard)
    - 116-RESEARCH.md Pitfall 7 + Open Question 2 + Assumption A6 (freeze_support rationale)
  </read_first>
  <action>
    Import the `multiprocessing` standard-library module (add it to the existing top-of-file import block, alphabetically placed among stdlib imports — verify it is not already imported before adding). Inside the existing `if __name__ == "__main__":` block at the bottom of run_scan.py, insert `multiprocessing.freeze_support()` as the FIRST statement, before `_run_main_with_job_guard()`. This is the documented PyInstaller-required guard so a frozen Windows EXE using the `spawn` start method does not recursively re-import the main module. Keep the diff minimal per CLAUDE.md — this is a two-line change (one import line if absent, one call line). Do NOT modify _run_main_with_job_guard itself.
  </action>
  <verify>
    <automated>grep -q "import multiprocessing" run_scan.py && grep -A2 'if __name__ == "__main__":' run_scan.py | grep -q "freeze_support" && python -m compileall -q run_scan.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import multiprocessing" run_scan.py` returns exactly 1
    - `freeze_support()` appears within the `if __name__ == "__main__":` block, on a line preceding `_run_main_with_job_guard()`
    - `python -m compileall -q run_scan.py` exits 0 (no syntax error)
    - `python run_scan.py --help` still exits 0 (entrypoint unbroken)
  </acceptance_criteria>
  <done>run_scan.py imports multiprocessing and calls freeze_support() as the first statement in the __main__ guard; module still compiles and --help works.</done>
</task>

<task type="auto">
  <name>Task 2: Add non-blocking windows-packaging-spike CI job</name>
  <read_first>
    - .github/workflows/python-ci.yml (full file — the windows-sensor-smoke job to mirror)
    - 116-RESEARCH.md §CI Job Pattern + §Code Examples (CI Job YAML) — authoritative flag list and PowerShell line-continuation form
    - 116-CONTEXT.md D-01/D-02/D-03 (the locked CI shape)
  </read_first>
  <action>
    Add a new top-level job `windows-packaging-spike` (name: "Windows Packaging Spike") under `jobs:` in .github/workflows/python-ci.yml, after the existing windows-sensor-smoke job. Set `runs-on: windows-latest` and `continue-on-error: true` at the JOB level (D-02 — non-blocking; a spike failure must not gate the pipeline). Steps:
    1. Checkout: actions/checkout@v4.
    2. Setup Python: actions/setup-python@v5 with python-version '3.11'.
    3. Install: `pip install -e .` then `pip install pyinstaller==6.20.0` (D-03 — CI-only inline, pinned; NOT in pyproject.toml).
    4. Build step (also `continue-on-error: true` at step level so the evidence-upload step still runs): invoke `pyinstaller --onefile --name quirk` freezing `run_scan.py` (NOT the module:function string — D-01). Include the RESEARCH-mandated flags: `--collect-all quirk`, `--collect-all sqlalchemy`, `--collect-all fastapi`, `--copy-metadata quirk-scanner`, the sqlalchemy/uvicorn `--hidden-import` entries from RESEARCH §Code Examples, and three `--add-data` entries using the WINDOWS `;` separator (the #1 silent-failure trap): `quirk/compliance/cmvp_cache.json;quirk/compliance`, `quirk/compliance/cmvp_curated.csv;quirk/compliance`, `quirk/reports/templates;quirk/reports/templates`. Use PowerShell backtick (`` ` ``) line continuations (this is a Windows runner; default shell is pwsh) and pipe to `Tee-Object -FilePath pyinstaller-build.log`.
    5. Report step (shell: pwsh): if `dist/quirk.exe` exists, print `RESULT: BUILD_SUCCESS` with the rounded MB size; else print `RESULT: BUILD_FAILED`.
    6. Upload evidence: actions/upload-artifact@v4 with `if: always()`, name `pyinstaller-spike-evidence`, paths `pyinstaller-build.log`, `build/quirk/warn-quirk.txt`, `dist/quirk.exe`, `retention-days: 30`.
    Match the exact YAML from 116-RESEARCH.md §Code Examples (CI Job YAML) — that block is the source of truth for the flag ordering and PowerShell syntax.
  </action>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('.github/workflows/python-ci.yml')); j=d['jobs']['windows-packaging-spike']; assert j['continue-on-error'] is True; assert j['runs-on']=='windows-latest'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import yaml; yaml.safe_load(open('.github/workflows/python-ci.yml'))"` exits 0 (valid YAML)
    - `jobs.windows-packaging-spike.continue-on-error` is `true` AND `runs-on` is `windows-latest`
    - `grep -c "pyinstaller==6.20.0" .github/workflows/python-ci.yml` returns at least 1
    - `grep -c "run_scan.py" .github/workflows/python-ci.yml` returns at least 1 (freeze target is the .py, not module:function)
    - `grep -c 'cmvp_cache.json;quirk/compliance' .github/workflows/python-ci.yml` returns at least 1 (Windows `;` separator, NOT `:`)
    - `grep -c "upload-artifact" .github/workflows/python-ci.yml` returns at least 1
    - `grep -v '^#' .github/workflows/python-ci.yml | grep -c 'pyinstaller' ` is 0 inside `pyproject.toml`: confirm `grep -c pyinstaller pyproject.toml` returns 0 (D-03 — not a project dep)
    - No `.spec`/`.nsi`/`dist/`/`build/` paths are committed (D-06): `! ls *.spec *.nsi 2>/dev/null`
  </acceptance_criteria>
  <done>A non-blocking windows-packaging-spike job exists in python-ci.yml that pip-installs pyinstaller==6.20.0 CI-only, runs a --onefile build of run_scan.py with the full collect/hidden-import/add-data flag set (Windows `;` separator), reports BUILD_SUCCESS/FAILED, and uploads the build evidence artifact. PyInstaller is absent from pyproject.toml.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → repo | The spike build runs on a GitHub-hosted windows-latest VM; its output (EXE) must never be treated as a release artifact |
| pip → CI environment | PyInstaller is fetched from PyPI into the CI VM at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-01 | Tampering | `pip install pyinstaller` in CI | mitigate | Pin exact `pyinstaller==6.20.0`; RESEARCH §Package Legitimacy Audit slopcheck [OK] |
| T-116-02 | Elevation of Privilege | uploaded `quirk.exe` artifact | mitigate | D-06 scope guard — artifact is evidence only, `retention-days: 30`; assessment doc (Plan 02) carries an explicit "not for production use" warning |
| T-116-03 | Information Disclosure | secrets embedded at build time | accept | No credentials/tokens are passed to the pyinstaller command; sensor reads creds from sensor.yaml at runtime, not build time |
| T-116-04 | Denial of Service | spike build failure gating CI | mitigate | `continue-on-error: true` at job AND build-step level — the spike cannot block the pipeline (D-02) |
| T-116-SC | Tampering | npm/pip installs | mitigate | Only pip install is `pyinstaller==6.20.0` (pinned, slopcheck [OK] in RESEARCH); no [ASSUMED]/[SUS] packages → no blocking checkpoint required |
</threat_model>

<verification>
- `python -c "import yaml; yaml.safe_load(open('.github/workflows/python-ci.yml'))"` parses cleanly
- `python -m compileall -q run_scan.py` exits 0
- `grep -c pyinstaller pyproject.toml` returns 0 (D-03)
- No `*.spec` / `*.nsi` / committed `dist/` (D-06)
</verification>

<success_criteria>
- windows-packaging-spike job present, non-blocking, freezes run_scan.py with the full flag set
- freeze_support() guard added to run_scan.py
- PyInstaller installed CI-only; absent from pyproject.toml
- No production packaging artifact committed
</success_criteria>

<output>
Create `.planning/phases/116-windows-packaging-spike/116-01-SUMMARY.md` when done
</output>
