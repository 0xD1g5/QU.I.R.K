---
phase: 100-professional-editable-report-delivery
plan: 02
type: execute
wave: 2
depends_on: ["100-01"]
files_modified:
  - quirk/reports/docx_renderer.py
  - quirk/reports/writer.py
  - pyproject.toml
  - tests/test_docx_report.py
  - tests/test_reports_writer.py
  - tests/test_cross_surface_parity.py
  - docs/UAT-SERIES.md
autonomous: true
requirements: [FMT-03]
must_haves:
  truths:
    - "Every report run auto-emits report-{stamp}.docx alongside HTML/PDF"
    - "The DOCX opens as a valid Word document with cover/executive/findings/roadmap/score sections, Heading 1/2 styles, and native tables"
    - "The DOCX is derived from the same exec_content + findings model as HTML/PDF (no parallel content)"
    - "When python-docx is absent the run skips DOCX gracefully with a stderr advisory and continues"
    - "DOCX content matches HTML/CLI content (cross-surface parity)"
  artifacts:
    - path: "quirk/reports/docx_renderer.py"
      provides: "render_docx_report() — python-docx structural renderer"
      exports: ["render_docx_report"]
    - path: "quirk/reports/writer.py"
      provides: "DOCX auto-emit after render_pdf_report + docx_path in output_files"
      contains: "render_docx_report"
    - path: "pyproject.toml"
      provides: "[docx] optional extra; joined into [all]"
      contains: "python-docx"
    - path: "tests/test_docx_report.py"
      provides: "DOCX structure + graceful-skip + advisory tests"
  key_links:
    - from: "quirk/reports/writer.py"
      to: "quirk/reports/docx_renderer.py"
      via: "render_docx_report(path, cfg, findings, exec_content) after PDF step"
      pattern: "render_docx_report"
    - from: "quirk/reports/docx_renderer.py"
      to: "quirk/reports/content_model.py ExecContent"
      via: "exec_content routing (single content pipeline, D-10)"
      pattern: "exec_content"
---

<objective>
Add an editable DOCX export (FMT-03): a new quirk/reports/docx_renderer.py module that renders the SAME exec_content + findings content model into a structural Word document, auto-emitted on every report run via write_reports, gated behind an optional [docx] extra with graceful skip when python-docx is absent.

Purpose: A consultant can open report-{stamp}.docx in Word/Google Docs, insert a logo, and edit narrative text before client handoff — without reconstructing document structure.
Output: docx_renderer.py, writer.py wiring, [docx] pyproject extra, DOCX/parity tests, docs+Obsidian sync.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-professional-editable-report-delivery/100-CONTEXT.md
@.planning/phases/100-professional-editable-report-delivery/100-UI-SPEC.md
@.planning/phases/100-professional-editable-report-delivery/100-PATTERNS.md

CONTEXT precedence: CONTEXT.md D-09..D-12 and 100-UI-SPEC.md §DOCX Structure Contract (exact strings, layout order, table columns) are authoritative. RESEARCH is advisory on HOW.

<interfaces>
ExecContent (quirk/reports/content_model.py): narrative_lead, narrative_drivers, top_risks, roadmap_items (each RoadmapItem has .phase in {NOW,NEXT,LATER}, plus action/rationale/effort/impact fields), subscores. Mirror html_renderer.py's exec_content if/else routing (~lines 175-233) — route through exec_content when present, fall back to empty when None.

writer.py: exec_content built at line 165; stamp at line 139; render_pdf_report call at lines 235-236 (pdf_path = os.path.join(outdir, f"report-{stamp}.pdf")); output_files list at lines 294-299 (filtered by `if p`); imports at line 21 `from quirk.reports.html_renderer import render_html_report, render_pdf_report`.

render_pdf_report graceful-skip pattern (html_renderer.py ~295-299): try: from playwright... except ImportError: return False. Mirror with a lazy `from docx import Document` INSIDE render_docx_report (never module-level — optional-extra import trap) plus a stderr advisory before returning False.

pyproject.toml: [project.optional-dependencies] at line 34; [all] at lines 87-95 uses self-referential `quirk-scanner[...]` entries; quirk[identity] is intentionally excluded from [all] (impacket conflict — do NOT touch that).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: docx_renderer.py — structural DOCX from shared content model + graceful skip</name>
  <files>quirk/reports/docx_renderer.py, tests/test_docx_report.py</files>
  <read_first>
    quirk/reports/html_renderer.py (render_pdf_report graceful-skip ~290-341, exec_content routing ~175-233, double-getattr ~236-238, os.makedirs ~260);
    quirk/reports/content_model.py (ExecContent / RoadmapItem / RiskItem fields);
    100-UI-SPEC.md §DOCX Structure Contract (Document Layout Order, Word Table Column Contracts, entry-point signature) + §Copywriting Contract C (verbatim locked strings);
    100-PATTERNS.md §quirk/reports/docx_renderer.py (module skeleton, table construction, exec_content routing, empty-findings handling).
  </read_first>
  <behavior>
    - render_docx_report(path, cfg, findings=[]) returns True and writes a valid .docx openable by docx.Document(path).
    - First paragraph text equals the exact string "[ Insert organization logo here ]".
    - Heading paragraphs include "Executive Summary", "Findings", "Remediation Roadmap", "Score Breakdown".
    - First findings table has 7 columns with header row ["Severity","Title","Host","Port","Description","Recommendation","Quantum Risk"].
    - With findings empty, the findings table has the header row + a single data row whose first cell is "No findings recorded for this scan."
    - When `docx` is unavailable (sys.modules["docx"] = None), returns False and writes "python-docx is not installed" to stderr; does not raise.
  </behavior>
  <action>
    Create quirk/reports/docx_renderer.py mirroring html_renderer.py module structure. Module-level imports are stdlib only (os, sys, datetime). Define render_docx_report(path, cfg, findings, exec_content=None) -> bool with the entry-point signature from 100-UI-SPEC.md. Lazy-import `from docx import Document` INSIDE the function body inside try/except ImportError → print the verbatim advisory "DOCX export skipped: python-docx is not installed. Install with: pip install quirk-scanner[docx]" to sys.stderr and return False (T-100-DEP; mirrors render_pdf_report). Read org_name/report_owner/data_classification via double-getattr on cfg.assessment; build generated_at as datetime.now(timezone.utc).strftime("%Y-%m-%d %H:%M UTC"). Route content through exec_content when present (narrative_lead, narrative_drivers, top_risks, roadmap_now/next/later split by RoadmapItem.phase, subscores) and fall back to empty when None — D-10 single content pipeline, NOT a hand-built parallel doc and NOT an HTML→DOCX conversion. Build the document in the exact layout order from 100-UI-SPEC.md §Document Layout Order using add_paragraph (Normal), add_heading(level=1/2), and add_table: logo placeholder paragraph (exact string), title Heading 1, org Heading 2, pipe-separated metadata Normal line, Executive Summary (narrative_lead + Readiness Assessment + Score Decomposition 3-col + Priority Business Risks 2-col + Top Findings 4-col), Findings (7-col table per column contract), Remediation Roadmap (NOW/NEXT/LATER Heading 2 each + 4-col table Phase|Action|Rationale|Effort / Impact), Score Breakdown (rollup sentence + 3-col Score Decomposition table). Set tbl.style = 'Table Grid' but wrap that assignment so a missing/unknown style name does not crash (research A3 — 'Table Grid' is ASSUMED; on KeyError/exception leave the default style). Handle empty findings per the behavior above (always write the header row). os.makedirs the parent dir, doc.save(path), print "DOCX report written to {path}", return True. Create tests/test_docx_report.py with test_docx_written, test_logo_placeholder, test_docx_section_headings, test_docx_findings_table_cols, test_docx_graceful_skip, test_docx_skip_advisory per 100-PATTERNS.md §tests (SimpleNamespace cfg fixture; monkeypatch.setitem(sys.modules, "docx", None) for the skip path; open the rendered .docx with docx.Document to inspect structure).
  </action>
  <verify>
    <automated>python -m pytest tests/test_docx_report.py -q</automated>
  </verify>
  <acceptance_criteria>
    - quirk/reports/docx_renderer.py exports render_docx_report with signature (path, cfg, findings, exec_content=None) -> bool
    - `from docx import Document` appears only inside the function body (grep -v '^#' quirk/reports/docx_renderer.py | grep -c "from docx import Document" == 1, and no module-level docx import)
    - First DOCX paragraph is exactly "[ Insert organization logo here ]"
    - DOCX has Heading-1 sections Executive Summary / Findings / Remediation Roadmap / Score Breakdown
    - First findings table has 7 columns with the locked header order
    - test_docx_graceful_skip asserts return False + stderr advisory when docx absent
    - `python -m pytest tests/test_docx_report.py -q` exits 0
  </acceptance_criteria>
  <done>render_docx_report produces a structurally-faithful Word document from the shared content model and skips gracefully when python-docx is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire DOCX into write_reports + [docx] extra + parity/writer tests</name>
  <files>quirk/reports/writer.py, pyproject.toml, tests/test_reports_writer.py, tests/test_cross_surface_parity.py</files>
  <read_first>
    quirk/reports/writer.py (imports line 21, exec_content line 165, render_pdf_report call lines 235-236, output_files lines 294-299);
    pyproject.toml ([project.optional-dependencies] line 34, dashboard/identity/api extras, [all] lines 87-95);
    tests/test_reports_writer.py (existing @patch stub pattern ~118-135);
    tests/test_cross_surface_parity.py (test_narrative_content_parity ~91-134);
    100-PATTERNS.md §quirk/reports/writer.py (call-site + import + output_files extension) + §test_reports_writer.py + §test_cross_surface_parity.py;
    100-UI-SPEC.md §Copywriting Contract C (success log line "DOCX report written to {path}").
  </read_first>
  <behavior>
    - After write_reports, a report-{stamp}.docx file exists in the output dir (when python-docx available) and docx_path appears in the output_files list.
    - When render_docx_report returns False, docx_path is None and is dropped from output_files by the existing `if p` filter.
    - Cross-surface parity: exec_content.narrative_lead appears in the rendered DOCX paragraph text alongside the HTML/CLI surfaces.
  </behavior>
  <action>
    In writer.py: extend the line-21 import to also import render_docx_report from quirk.reports.docx_renderer. Immediately after the render_pdf_report block (after line 236, before the run-stats block), add the D-11 DOCX auto-emit: docx_path = os.path.join(outdir, f"report-{stamp}.docx"); docx_ok = render_docx_report(path=docx_path, cfg=cfg, findings=findings, exec_content=exec_content); if not docx_ok: docx_path = None. Add docx_path to the output_files list at lines 294-299 (the existing `if p` filter handles None). In pyproject.toml: add a `docx = ["python-docx>=1.1.0"]` entry under [project.optional-dependencies], and add `"quirk-scanner[docx]"` to the [all] list (research recommendation A2 / Open Question 1 — no dep conflict, so DOCX bundles with full installs; do NOT touch the intentionally-excluded quirk[identity] note). Extend tests/test_reports_writer.py to assert a report-*.docx is produced / docx_path is in the artifact set (follow the existing @patch stub pattern). Extend tests/test_cross_surface_parity.py to render the DOCX and assert exec_content.narrative_lead is in the joined DOCX paragraph text alongside the existing HTML/CLI assertions.
  </action>
  <verify>
    <automated>python -m pytest tests/test_reports_writer.py tests/test_cross_surface_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - writer.py imports render_docx_report and calls it after render_pdf_report with exec_content
    - docx_path is appended to the output_files list
    - pyproject.toml has `docx = ["python-docx>=1.1.0"]` and `"quirk-scanner[docx]"` in [all]
    - test_cross_surface_parity asserts narrative_lead present in DOCX
    - `python -m pytest tests/test_reports_writer.py tests/test_cross_surface_parity.py -q` exits 0
  </acceptance_criteria>
  <done>DOCX auto-emits on every report run, joins the output files list, is installable via [docx]/[all], and carries the same narrative content as HTML/CLI.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs/UAT-SERIES.md + sync to Obsidian</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    docs/UAT-SERIES.md (header + version strings + relevant report-output series);
    ./CLAUDE.md §Mandatory Phase Completion Steps 2 & 3 (UAT-SERIES update + Obsidian sync via vault filesystem write).
  </read_first>
  <action>
    Update docs/UAT-SERIES.md: bump the **Last Updated:** date to today; add/update test cases in the report-output series covering (a) the PDF branded cover page + clean pagination (FMT-01/FMT-02) and (b) the new report-{stamp}.docx auto-emit + open-in-Word/Google-Docs structural check + graceful-skip when python-docx absent (FMT-03). Then sync to the Obsidian vault by writing directly to the vault filesystem (file too large for CLI content=): prepend the standard frontmatter block (project: QU.I.R.K., type: reference, status: active, source: docs/UAT-SERIES.md, updated: today) to a copy and write it to /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md per the CLAUDE.md step-3 procedure. Do NOT use `obsidian CLI content=` for this large file.
  </action>
  <verify>
    <automated>grep -q "docx" docs/UAT-SERIES.md && test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/UAT-SERIES.md mentions the DOCX export and the PDF cover page, with an updated Last Updated date
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md exists with the synced content + frontmatter
  </acceptance_criteria>
  <done>UAT series documents the new PDF cover/pagination and DOCX export; Obsidian vault copy is in sync.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| optional dependency → writer module | python-docx is an optional extra imported by docx_renderer, which writer.py always imports |
| content model → DOCX artifact | DOCX is generated from the already-sanitized exec_content + findings model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-DEP | Tampering (supply chain) / DoS | python-docx [docx] extra; lazy import in render_docx_report | mitigate | One well-known established PyPI lib (python-docx, Package Legitimacy Audit [OK]); lazy-imported inside the function so a missing/failed import yields a graceful stderr advisory + return False, never a crash on minimal installs |
| T-100-DOCX | Tampering | DOCX content generation | accept | python-docx builds the doc from structured API calls (paragraphs + tables), not raw XML or user template files; no macro execution surface; content derives from the same sanitized model as HTML (D-10) — no new untrusted input path |
| T-100-SC | Tampering | pip install of python-docx | mitigate | RESEARCH Package Legitimacy Audit verdict [OK] (established, multi-million downloads/mo, github.com/python-openxml/python-docx); no [ASSUMED]/[SUS] package → no blocking checkpoint required |
</threat_model>

<verification>
- `python -m pytest tests/test_docx_report.py tests/test_reports_writer.py tests/test_cross_surface_parity.py -q` exits 0
- `python -m compileall quirk/reports/docx_renderer.py quirk/reports/writer.py` exits 0
- Full report-module suite green: `python -m pytest tests/test_html_report.py tests/test_docx_report.py tests/test_reports_writer.py -q`
- No module-level `from docx import` in docx_renderer.py (minimal-install safe)
- docs/UAT-SERIES.md updated + Obsidian vault copy present
</verification>

<success_criteria>
FMT-03: report-{stamp}.docx auto-emits every run, opens as a valid structural Word document (cover/exec/findings/roadmap/score, Heading 1/2, native tables, logo placeholder), derives from the shared content model, and skips gracefully when python-docx is absent. Docs + Obsidian synced.
</success_criteria>

<output>
Create `.planning/phases/100-professional-editable-report-delivery/100-02-SUMMARY.md` when done
</output>
