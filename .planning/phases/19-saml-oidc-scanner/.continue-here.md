---
phase: 19-saml-oidc-scanner
task: 0
total_tasks: 4
status: not_started
last_updated: 2026-04-09T04:06:29.464Z
---

<current_state>
Phase 19 (SAML/OIDC Scanner) is fully planned and researched — all docs committed — but NO code has been written yet. Both PLANs exist, CONTEXT.md has all design decisions, RESEARCH.md has implementation patterns, VALIDATION.md has the test strategy. This is a clean pre-execution pause.
</current_state>

<completed_work>
- discuss-phase 19 — 5 gray areas resolved, CONTEXT.md committed (b63688e / 255f448)
- research-phase 19 — RESEARCH.md committed (fa0cc1d)
- VALIDATION.md — committed (fa3c269)
- 19-01-PLAN.md — TDD RED scaffold plan committed (b63688e)
- 19-02-PLAN.md — Full implementation plan committed (b63688e)
</completed_work>

<remaining_work>
- Plan 01 Task 1: Create `quirk/scanner/saml_scanner.py` stub module (import guard, SAML_NS, helper stubs)
- Plan 01 Task 2: Create `tests/test_saml_scanner.py` RED test scaffold (20+ tests, SAML-01 through SAML-06)
- Plan 02 Task 1-N: Implement full SAML/OIDC scanner (defusedxml.lxml XPath, cert extraction, OIDC discovery, severity)
- Plan 02: Wire into run_scan.py, update CBOM builder/classifier, add SimpleSAMLphp chaos lab docker profile
</remaining_work>

<decisions_made>
- Use defusedxml.lxml for XML parsing (prevents XXE on untrusted IdP metadata)
- lxml behind LXML_AVAILABLE import guard (consistent with DNSSEC/JWT scanner pattern)
- TDD RED-GREEN split: Plan 01 creates stub+tests, Plan 02 makes tests pass
- SimpleSAMLphp chaos lab with RSA-1024 cert under `--profile saml` (weak cert = CRITICAL finding for E2E validation)
- OIDC discovery: only enumerate signing algs; RS*/PS* → HIGH, ECDSA/EdDSA → no severity
- scan_saml_targets(targets, timeout=10, logger=None) signature — matches DNSSEC/JWT scanner API
</decisions_made>

<blockers>
(none)
</blockers>

<context>
Phase 18 (DNSSEC Scanner) used the same TDD RED-GREEN two-plan pattern and completed cleanly. Phase 19 follows the same pattern. All implementation patterns are documented in RESEARCH.md: XPath for SAML namespace parsing, cert DER base64 decoding via cryptography library, OIDC .well-known routing, defusedxml.lxml for safe parsing. The chaos lab uses SimpleSAMLphp (PHP-based IdP) rather than BIND9 — docker-compose.yml gets a `saml` profile added.
</context>

<next_action>
Start with: /gsd:execute-phase (fresh context). This will pick up 19-01-PLAN.md (stub + RED tests) then 19-02-PLAN.md (full implementation). No setup required — dependencies are already in requirements.txt (lxml, defusedxml, cryptography, httpx).
</next_action>
