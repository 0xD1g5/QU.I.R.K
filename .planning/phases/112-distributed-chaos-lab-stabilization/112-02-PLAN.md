---
phase: 112-distributed-chaos-lab-stabilization
plan: 02
type: execute
wave: 2
depends_on: ["112-01"]
files_modified:
  - quantum-chaos-enterprise-lab/expected_results_distributed.md
  - quantum-chaos-enterprise-lab/README.md
  - docs/operators-guide.md
autonomous: true
requirements: [LAB-03, STAB-01]
must_haves:
  truths:
    - "A new expected_results_distributed.md oracle documents the two networks, per-segment targets/ports, the same-host:port→two-components expectation, and the one-CBOM/one-score outcome"
    - "The chaos-lab README.md documents the distributed topology and cross-references the oracle"
    - "operators-guide.md has a §8 Distributed Sensor Deployment section covering enroll → push → merge + air-gap export/import + Windows sensor install"
    - "The 999.59 all-configurations/settings coverage gap is closed in the operators-guide"
  artifacts:
    - path: "quantum-chaos-enterprise-lab/expected_results_distributed.md"
      provides: "Distributed E2E oracle (LAB-03)"
      contains: "Topology: distributed"
    - path: "docs/operators-guide.md"
      provides: "§8 distributed workflow + Windows install + settings reference"
      contains: "Distributed Sensor Deployment"
  key_links:
    - from: "quantum-chaos-enterprise-lab/README.md"
      to: "expected_results_distributed.md"
      via: "cross-reference link to the ## Topology: distributed anchor"
      pattern: "expected_results_distributed"
---

<objective>
Document the distributed topology: a new expected_results_distributed.md oracle, the chaos-lab README
distributed section, and operators-guide §8 (enroll→push→merge + air-gap + Windows sensor install,
closing the 999.59 settings-coverage gap).

Purpose: Satisfy the CLAUDE.md chaos-lab no-drift rule (lab.sh + README + expected_results updated in
the same change as the topology — LAB-03) and the operators-guide stabilization requirement (STAB-01).
Output: expected_results_distributed.md, an updated chaos-lab README.md, and operators-guide §8.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-distributed-chaos-lab-stabilization/112-CONTEXT.md
@.planning/phases/112-distributed-chaos-lab-stabilization/112-RESEARCH.md
@.planning/phases/112-distributed-chaos-lab-stabilization/112-PATTERNS.md
@.planning/phases/112-distributed-chaos-lab-stabilization/112-01-SUMMARY.md

<interfaces>
<!-- The actual network/service/IP names + the confirmed host-recording behavior come from Plan 01's -->
<!-- docker-compose.distributed.yml and 112-01-SUMMARY.md. READ THE BUILT FILE — do not invent names. -->
<!-- Expected from Plan 01: segment-a (10.10.0.0/24), segment-b (10.20.0.0/24), console-net (10.30.0.0/24); -->
<!-- tls-target-a @10.10.0.10, tls-target-b @10.20.0.10; sensor-a, sensor-b, console; both sensors scan -->
<!-- 10.10.0.10:443; merge → 2 CBOM components for 10.10.0.10:443 differing by sensor_id. -->

<!-- operators-guide.md currently ends at "## 7. Compliance Map Maintenance" — append "## 8.". -->
<!-- Commands to document: quirk console enroll; quirk sensor enroll <url> --segment <label> --api-token; -->
<!-- quirk sensor push; quirk sensor merge; quirk sensor export-results + quirk console import-results. -->
<!-- Windows: pip install quirk-scanner[all], sensor.yaml at %APPDATA%\quirk\sensor.yaml (platformdirs), -->
<!-- PowerShell snippet, sys.platform != 'win32' SIGTERM guard note. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write expected_results_distributed.md oracle + update chaos-lab README</name>
  <files>quantum-chaos-enterprise-lab/expected_results_distributed.md, quantum-chaos-enterprise-lab/README.md</files>
  <read_first>
    - quantum-chaos-enterprise-lab/docker-compose.distributed.yml (the AS-BUILT network/service/IP names from Plan 01 — source of truth)
    - .planning/phases/112-distributed-chaos-lab-stabilization/112-01-SUMMARY.md (confirmed host-recording file:line)
    - quantum-chaos-enterprise-lab/expected_results_v4.md (header + scope + per-section format to mirror)
    - quantum-chaos-enterprise-lab/README.md (where the existing profile table lives — add a distributed section after it)
    - .planning/phases/112-distributed-chaos-lab-stabilization/112-RESEARCH.md (oracle format example, MERGE-03 validation block)
  </read_first>
  <action>
    Create quantum-chaos-enterprise-lab/expected_results_distributed.md mirroring the expected_results_v4.md
    header/scope/status format. Use a `## Topology: distributed` H2 (there is no --profile flag; this is a
    separate compose invoked via `lab.sh distributed up`). Include: a Networks table (segment-a 10.10.0.0/24,
    segment-b 10.20.0.0/24, console-net 10.30.0.0/24 with purpose); a Services table (tls-target-a @10.10.0.10:443,
    tls-target-b @10.20.0.10:443, sensor-a, sensor-b, console @8512); an Expected E2E Outcome table (2 sensors
    enrolled, 2 pushes HTTP 200, **2 distinct CBOM components for 10.10.0.10:443** differing by sensor_id,
    single merged score, coverage_warning null); and a MERGE-03 Validation prose block explaining that both
    sensors record the IDENTICAL host:port (10.10.0.10:443) because QUIRK records the configured scan-target
    string on CryptoEndpoint.host (cite the file:line confirmed in 112-01-SUMMARY.md) — NOT the resolved Docker
    IP — so the (sensor_id, host, port) key yields two rows. Note that the live enroll→push→merge run is human-UAT.
    Use the AS-BUILT names from the compose file; if any differ from the above, follow the compose file.

    Update quantum-chaos-enterprise-lab/README.md: add a "Distributed Topology" section after the existing
    profile table describing the two-network layout, the `./lab.sh distributed up|e2e|down|status|logs`
    commands, and a cross-reference link to expected_results_distributed.md (anchor the `## Topology: distributed`
    heading, matching the v4 oracle cross-reference convention). State that ALL_PROFILES (main compose) is
    unaffected by the distributed file (no-drift).
  </action>
  <verify>
    <automated>test -f quantum-chaos-enterprise-lab/expected_results_distributed.md && grep -q "Topology: distributed" quantum-chaos-enterprise-lab/expected_results_distributed.md && grep -q "expected_results_distributed" quantum-chaos-enterprise-lab/README.md</automated>
  </verify>
  <acceptance_criteria>
    - expected_results_distributed.md exists with a `## Topology: distributed` heading
    - Oracle network/service/IP names match docker-compose.distributed.yml exactly (no invented names)
    - Oracle states "2 distinct CBOM components" for 10.10.0.10:443 differing by sensor_id (MERGE-03)
    - Oracle cites the confirmed tls_scanner host-recording file:line from 112-01-SUMMARY.md
    - README.md references expected_results_distributed.md and documents `lab.sh distributed`
  </acceptance_criteria>
  <done>Distributed oracle written mirroring v4 format; README cross-links it; MERGE-03 expectation documented from the as-built compose.</done>
</task>

<task type="auto">
  <name>Task 2: Add operators-guide §8 (distributed workflow + Windows install + settings coverage)</name>
  <files>docs/operators-guide.md</files>
  <read_first>
    - docs/operators-guide.md (§7 Compliance Map Maintenance at L337+ is the structural template for a new numbered top-level §; §2 Configure + §5 Troubleshooting for the settings-coverage gap)
    - .planning/phases/112-distributed-chaos-lab-stabilization/112-RESEARCH.md (operators-guide gap analysis: missing §8, Windows steps, 999.59 settings gap details)
    - quirk/cli/sensor_cmd.py + quirk/cli/console_cmd.py (exact command/arg surfaces: enroll --segment --api-token, push, merge, export-results, import-results)
  </read_first>
  <action>
    Append `## 8. Distributed Sensor Deployment` to docs/operators-guide.md, following the §7 structure
    (numbered H2 + prose intro + numbered runbook subsections with bash code blocks). Subsections:
      - §8.1 Enroll a sensor: `quirk console enroll` (provision in console DB, prints one-time bearer token)
        then `quirk sensor enroll <console_url> --segment <label> --api-token <token>` (writes sensor.yaml).
      - §8.2 Push findings: `quirk sensor push` (run scan + push; spool if offline).
      - §8.3 Merge into a unified CBOM: `quirk sensor merge` (one CBOM + one score over the union; note
        coverage_warning behavior).
      - §8.4 Windows sensor installation: `pip install quirk-scanner[all]`, sensor.yaml path via platformdirs
        (`%APPDATA%\quirk\sensor.yaml`), a PowerShell snippet for enroll/push, and a note that SIGTERM
        handling is guarded by `sys.platform != 'win32'` (Windows uses a different stop path).
      - §8.5 Air-gap path: `quirk sensor export-results` → transfer → `quirk console import-results`.
    Close the 999.59 settings-coverage gap: add an all-configurations/settings reference (in §8 or by
    extending §2) covering the scan.timeouts.* knob names, output.directory default, and the sensor_id/segment
    fields that appear in scan output — the items RESEARCH flagged as missing. Keep diffs minimal; do not
    rewrite existing sections.
  </action>
  <verify>
    <automated>grep -q "## 8. Distributed Sensor Deployment" docs/operators-guide.md && grep -q "quirk sensor merge" docs/operators-guide.md && grep -qi "APPDATA" docs/operators-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - operators-guide.md contains `## 8. Distributed Sensor Deployment` with §8.1–§8.5 subsections
    - The enroll → push → merge commands appear with correct arg surfaces (--segment, --api-token)
    - Windows install steps present (pip install quirk-scanner[all], %APPDATA%\quirk\sensor.yaml, PowerShell)
    - Air-gap export-results/import-results path documented
    - Settings reference (scan.timeouts.*, output.directory, sensor_id/segment) present (999.59 closed)
  </acceptance_criteria>
  <done>operators-guide §8 covers the full distributed + Windows + air-gap workflow; 999.59 settings gap closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → operator action | operators-guide commands are executed by a human; no runtime trust boundary crossed by the docs themselves |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-04 | Information Disclosure | operators-guide token/enroll examples | mitigate | Document tokens as one-time-use, shown once; examples use placeholders not real secrets |
| T-112-05 | Tampering | `quirk serve --host 0.0.0.0` guidance in oracle | accept | Oracle explicitly notes 0.0.0.0 bind is lab-network-only, not a production exposure recommendation |
| T-112-SC | Tampering | npm/pip/cargo installs | mitigate | Docs-only plan; no installs. RESEARCH Package Legitimacy: N/A (no new packages) |
</threat_model>

<verification>
- `grep -q "Topology: distributed" quantum-chaos-enterprise-lab/expected_results_distributed.md`
- `grep -q "expected_results_distributed" quantum-chaos-enterprise-lab/README.md`
- `grep -q "## 8. Distributed Sensor Deployment" docs/operators-guide.md`
</verification>

<success_criteria>
- Distributed oracle exists, mirrors v4 format, documents the MERGE-03 two-component expectation from the as-built compose (LAB-03)
- README cross-references the oracle and the lab.sh distributed command (LAB-03 no-drift)
- operators-guide §8 covers enroll→push→merge + Windows + air-gap; 999.59 settings gap closed (STAB-01)
</success_criteria>

<output>
Create `.planning/phases/112-distributed-chaos-lab-stabilization/112-02-SUMMARY.md` when done.
</output>
