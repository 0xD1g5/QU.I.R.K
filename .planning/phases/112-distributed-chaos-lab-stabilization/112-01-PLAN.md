---
phase: 112-distributed-chaos-lab-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quantum-chaos-enterprise-lab/docker-compose.distributed.yml
  - quantum-chaos-enterprise-lab/sensor.Dockerfile
  - quantum-chaos-enterprise-lab/scripts/distributed-e2e.sh
  - quantum-chaos-enterprise-lab/lab.sh
  - tests/test_distributed_topology.py
autonomous: true
requirements: [LAB-01, LAB-02, LAB-03]
must_haves:
  truths:
    - "QUIRK records the configured scan-target string on CryptoEndpoint.host (not a resolved IP), confirmed in-code"
    - "A distributed Docker Compose topology exists with two isolated bridge networks (distinct subnets), a TLS target per segment, one sensor container per segment, and one console container"
    - "Both per-segment sensors are configured to scan the SAME recorded host:port (10.10.0.10:443), differing only by sensor_id/segment"
    - "lab.sh has a `distributed` command that targets the separate compose file without touching ALL_PROFILES"
    - "distributed-e2e.sh orchestrates enroll → push → merge in that order"
    - "A static topology pytest test asserts the two networks, same-last-octet target IP, sensor-per-segment + 1 console, pinned sensor base image, and e2e enroll→push→merge order"
  artifacts:
    - path: "quantum-chaos-enterprise-lab/docker-compose.distributed.yml"
      provides: "Two-network multi-segment topology"
      contains: "ipv4_address"
    - path: "quantum-chaos-enterprise-lab/sensor.Dockerfile"
      provides: "Sensor/console container image, patch-pinned base"
      contains: "python:3.11.12-slim"
    - path: "quantum-chaos-enterprise-lab/scripts/distributed-e2e.sh"
      provides: "enroll→push→merge orchestration"
    - path: "tests/test_distributed_topology.py"
      provides: "Static topology assertions (CI floor)"
  key_links:
    - from: "sensor-a / sensor-b config"
      to: "10.10.0.10:443"
      via: "scan-target string preserved on CryptoEndpoint.host"
      pattern: "10.10.0.10"
    - from: "lab.sh distributed arm"
      to: "docker-compose.distributed.yml"
      via: "COMPOSE_FILE reassignment before compose() call"
      pattern: "docker-compose.distributed.yml"
---

<objective>
Build the distributed multi-segment chaos-lab topology that physically reproduces the
same-host:port-in-two-segments scenario (MERGE-03) under real Docker networking, plus the
lab.sh `distributed` command and a static topology pytest test as the automated verification floor.

Purpose: Deliver everything runnable for the live distributed E2E (LAB-01/02), kept drift-free
per the CLAUDE.md chaos-lab rule (LAB-03). The live enroll→push→merge run is human-UAT (Plan handles
the runnable artifacts + automated static floor).
Output: docker-compose.distributed.yml, sensor.Dockerfile, distributed-e2e.sh, a `distributed` arm
in lab.sh, and tests/test_distributed_topology.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-distributed-chaos-lab-stabilization/112-CONTEXT.md
@.planning/phases/112-distributed-chaos-lab-stabilization/112-RESEARCH.md
@.planning/phases/112-distributed-chaos-lab-stabilization/112-PATTERNS.md

<interfaces>
<!-- LINCHPIN (LAB-02), confirmed in-code during planning — Task 1 re-confirms before building the oracle dependency: -->
<!-- quirk/scanner/tls_scanner.py:189 and :352 construct CryptoEndpoint(host=host, ...) where `host` is the -->
<!-- configured scan-target string passed in verbatim (socket.create_connection((host, port)) connects, but -->
<!-- the recorded host field is the literal token, NOT a resolved IP). So two sensors both configured to scan -->
<!-- "10.10.0.10:443" each record host=10.10.0.10 regardless of their actual Docker network IP. -->

<!-- lab.sh structure (do not break): PROJECT_NAME (L14) + COMPOSE_FILE (L15) defaults; compose() helper (L51-54) -->
<!-- reads both from outer scope; _validate_pinned_tags (L77+) checks ${COMPOSE_FILE}; ALL_PROFILES derived -->
<!-- from ${COMPOSE_FILE} in the `all` arm (L125-158); case "${cmd}" block L111-216, insert new arm before -->
<!-- the catch-all *) at L208. -->

<!-- sensor_cmd.py enroll accepts --api-token (writes console_api_token to sensor.yaml). console enroll prints -->
<!-- the raw bearer token to stdout (one-time). quirk serve default host is 127.0.0.1 — console MUST pass -->
<!-- --host 0.0.0.0. Sensor container entrypoint: quirk = run_scan:_run_main_with_job_guard. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm host-recording behavior, then build the distributed compose file + sensor.Dockerfile</name>
  <files>quantum-chaos-enterprise-lab/docker-compose.distributed.yml, quantum-chaos-enterprise-lab/sensor.Dockerfile</files>
  <read_first>
    - quirk/scanner/tls_scanner.py (lines ~109-200 and ~344-360 — confirm CryptoEndpoint(host=host) records the configured target string, NOT a resolved IP)
    - quantum-chaos-enterprise-lab/docker-compose.yml (nginx TLS service definitions, volume mounts, pinned image tags — reuse as per-segment targets)
    - Dockerfile (repo root — apt layer + pip install pattern for the sensor image)
    - .planning/phases/112-distributed-chaos-lab-stabilization/112-RESEARCH.md (compose skeleton, IPAM syntax, sensor.Dockerfile example, anti-patterns)
  </read_first>
  <action>
    LINCHPIN CONFIRMATION FIRST (LAB-02): Read quirk/scanner/tls_scanner.py and confirm that the
    `host=` kwarg passed to CryptoEndpoint is the configured scan-target string verbatim (not resolved
    to a Docker IP). If confirmed (it is, per planning verification at lines 189 and 352), the hostname/IP
    workaround works directly. Record the confirmed file:line in the SUMMARY so Plan 02 can cite it in the
    oracle. If — contrary to the planning finding — the scanner records a resolved IP, STOP and surface
    this; do NOT silently build an oracle that cannot hold.

    Build quantum-chaos-enterprise-lab/sensor.Dockerfile: `FROM python:3.11.12-slim` (patch-pinned per
    CHAOS-05 / _validate_pinned_tags — NOT bare `3.11-slim`). apt-get install nmap, ca-certificates, curl
    with --no-install-recommends + clean. WORKDIR /quirk, COPY . /quirk/, `pip install --no-cache-dir ".[all]"`
    (`.[all]` because the console needs [dashboard] for quirk serve; [identity] is intentionally excluded).
    ENTRYPOINT ["quirk"], CMD ["--help"] so compose can override `command:` per service.

    Build quantum-chaos-enterprise-lab/docker-compose.distributed.yml with three bridge networks under
    distinct subnets (Docker REFUSES overlapping subnets — research-verified): `segment-a` 10.10.0.0/24,
    `segment-b` 10.20.0.0/24, `console-net` 10.30.0.0/24. Services:
      - tls-target-a: image nginx pinned (reuse the modern nginx config + certs volume mounts from the main
        compose), `expose: ["443"]` (NOT `ports:` — avoids host-port collision with the main lab), attached
        to segment-a at ipv4_address 10.10.0.10.
      - tls-target-b: same nginx config, segment-b at ipv4_address 10.20.0.10 (same last octet .10).
      - sensor-a: build context ../.. dockerfile quantum-chaos-enterprise-lab/sensor.Dockerfile; networks
        [segment-a, console-net]; depends_on tls-target-a + console. Its scan-target is 10.10.0.10:443.
      - sensor-b: same build; networks [segment-b, console-net]; depends_on tls-target-b + console. Its
        scan-target is also 10.10.0.10:443 (the target IP as seen from within its own segment) so BOTH
        sensors record the identical host:port differing only by sensor_id/segment.
      - console: same build; command ["serve","--host","0.0.0.0","--port","8512"] (quirk serve defaults to
        loopback — MUST bind 0.0.0.0); networks [console-net]; a healthcheck curling http://localhost:8512
        health endpoint; a named volume console-data for /home/quirk so the SQLite DB persists across the
        e2e steps. Use depends_on: condition: service_healthy on sensors so the e2e script avoids fixed sleeps.
    Every `image:` key must be a pinned tag (CHAOS-05). Build-only services are exempt at the compose level
    but the Dockerfile FROM is pinned above.
  </action>
  <verify>
    <automated>docker compose -f quantum-chaos-enterprise-lab/docker-compose.distributed.yml config -q</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY records the confirmed tls_scanner.py file:line proving host=configured-string (LAB-02 linchpin)
    - `docker compose -f .../docker-compose.distributed.yml config -q` exits 0
    - grep shows two segment networks with distinct subnets (10.10.0.0/24, 10.20.0.0/24) and a console-net
    - grep shows tls-target-a at 10.10.0.10 and tls-target-b at 10.20.0.10 (same last octet)
    - grep shows console command includes `--host 0.0.0.0`
    - sensor.Dockerfile FROM is `python:3.11.12-slim` (patch-pinned), install is `.[all]`
    - segment targets use `expose:` not `ports:`
  </acceptance_criteria>
  <done>Compose file validates via `config -q`; sensor.Dockerfile patch-pinned; host-recording confirmed in-code and recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Add distributed-e2e.sh orchestrator + lab.sh `distributed` command arm</name>
  <files>quantum-chaos-enterprise-lab/scripts/distributed-e2e.sh, quantum-chaos-enterprise-lab/lab.sh</files>
  <read_first>
    - quantum-chaos-enterprise-lab/lab.sh (compose() helper L51-54; _validate_pinned_tags L77+; case block L111-216; the `all` arm ALL_PROFILES derivation L125-158; insert before catch-all *) at L208)
    - quantum-chaos-enterprise-lab/scripts/gen-certs.sh (shebang + set -euo pipefail header pattern)
    - quirk/cli/sensor_cmd.py (enroll --api-token arg; sensor.yaml console_api_token field)
    - quirk/cli/console_cmd.py (quirk console enroll token-print-to-stdout behavior)
    - .planning/phases/112-distributed-chaos-lab-stabilization/112-RESEARCH.md (token-capture with -T flag; lab.sh distributed arm skeleton)
  </read_first>
  <action>
    Create quantum-chaos-enterprise-lab/scripts/distributed-e2e.sh with `#!/usr/bin/env bash` + `set -euo pipefail`.
    Orchestrate, using `docker compose -p quirk-dist -f <distributed compose>` against the running stack:
    (1) `quirk console enroll` once per segment inside the console container, capturing the printed bearer
        token via `$()` with `docker compose exec -T` (the -T disables pseudo-TTY so stdout capture is clean;
        the raw token is on stdout, warnings on stderr); (2) write each sensor's sensor.yaml by running
        `quirk sensor enroll <console_url> --segment <label> --api-token "$TOKEN"` inside each sensor
        container (console_url = http://console:8512); (3) `quirk sensor push` in each sensor (run scan of
        10.10.0.10:443 + push to console); (4) `quirk sensor merge` on the console to produce the unified
        CBOM + score. The script MUST reference enroll, then push, then merge in that textual order (asserted
        by the topology test). Do NOT hardcode tokens in compose; capture dynamically. Rely on
        console healthcheck/service_healthy for readiness rather than fixed sleeps (a short fallback sleep is
        acceptable). Do NOT echo the bearer token to logs beyond what enroll already prints (lab-ephemeral).

    Modify quantum-chaos-enterprise-lab/lab.sh: add a `distributed)` arm to the `case "${cmd}"` block,
    inserted BEFORE the catch-all `*)` arm. The arm FIRST reassigns `COMPOSE_FILE="$(dirname "$0")/docker-compose.distributed.yml"`
    and `PROJECT_NAME="quirk-dist"` (so the existing compose() helper, which reads both from outer scope,
    and _validate_pinned_tags, which checks ${COMPOSE_FILE}, both pick up the distributed file), then reads
    a subcmd (default `up`) and dispatches: `up` (run _validate_pinned_tags first, refuse on violation, then
    `compose up -d` + `compose ps`), `down` (`compose down --remove-orphans`), `status` (`compose ps`),
    `logs` (`compose logs -f --tail=200 "${1:-}"`), `e2e` (bash scripts/distributed-e2e.sh), and an unknown-subcmd
    error. These reassignments are scoped to the distributed arm only — they must NOT leak to the up/all/ALL_PROFILES
    paths. ALL_PROFILES is derived from the main docker-compose.yml in the `all` arm and is unaffected because
    distributed lives in a separate file (this is the LAB-03 no-drift guarantee). Update the usage()/help text
    to document the `distributed` command.
  </action>
  <verify>
    <automated>bash -n quantum-chaos-enterprise-lab/lab.sh && bash -n quantum-chaos-enterprise-lab/scripts/distributed-e2e.sh && grep -n 'distributed)' quantum-chaos-enterprise-lab/lab.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n lab.sh` and `bash -n distributed-e2e.sh` both parse clean (exit 0)
    - lab.sh contains a `distributed)` arm that reassigns COMPOSE_FILE + PROJECT_NAME before any compose() call
    - distributed-e2e.sh references enroll, push, merge with enroll-index < push-index < merge-index
    - distributed-e2e.sh uses `docker compose exec -T` for token capture (no hardcoded token)
    - lab.sh `all` arm / ALL_PROFILES derivation is byte-for-byte unchanged
  </acceptance_criteria>
  <done>lab.sh has a working distributed arm; e2e script orchestrates enroll→push→merge; ALL_PROFILES untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Static topology pytest test (CI floor for LAB-01/02/03)</name>
  <files>tests/test_distributed_topology.py</files>
  <behavior>
    - test_distributed_compose_file_exists: the compose file exists
    - test_config_validates: `docker compose -f <dist> config` exits 0 (skip if docker unavailable)
    - test_two_bridge_networks: >= 2 networks defined
    - test_segment_networks_have_ipam_subnets: each segment network has an ipam.config subnet
    - test_same_host_ip_in_segment_networks: the segment targets share the same last octet (.10)
    - test_one_sensor_service_per_segment: >= 2 services with "sensor" in the name
    - test_one_console_service: >= 1 service with "console" in the name
    - test_sensor_dockerfile_base_pinned: sensor.Dockerfile FROM is python:3.11.12-slim (patch-pinned)
    - test_e2e_script_enroll_push_merge_order: enroll index < push index < merge index in distributed-e2e.sh
  </behavior>
  <read_first>
    - tests/test_chaos_lab_image_pinning.py (PyYAML + pathlib parse pattern, Path(__file__).resolve().parent.parent root)
    - .planning/phases/112-distributed-chaos-lab-stabilization/112-RESEARCH.md (topology test code examples)
    - quantum-chaos-enterprise-lab/docker-compose.distributed.yml (built in Task 1 — the file under test)
  </read_first>
  <action>
    Create tests/test_distributed_topology.py mirroring tests/test_chaos_lab_image_pinning.py: define
    LAB_DIR = Path(__file__).resolve().parent.parent / "quantum-chaos-enterprise-lab", DIST_COMPOSE,
    SENSOR_DOCKERFILE, and E2E_SCRIPT constants. Parse the compose with yaml.safe_load(...read_text()).
    Implement the nine tests in <behavior>. For test_config_validates, run `docker compose -f <dist> config`
    via subprocess and pytest.skip if the docker binary is absent (so the suite stays green on machines
    without Docker — the live run is human-UAT). For test_same_host_ip_in_segment_networks, collect each
    service's per-network ipv4_address, group by network, and assert the segment-network targets share the
    same last octet. For test_sensor_dockerfile_base_pinned, read sensor.Dockerfile and assert the FROM line
    contains `python:3.11.12-slim`. For the e2e order test, read distributed-e2e.sh and assert
    text.index("enroll") < text.index("push") < text.index("merge"). Use grep-friendly assertions; no
    watch-mode flags.
  </action>
  <verify>
    <automated>pytest tests/test_distributed_topology.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_distributed_topology.py -q` passes (test_config_validates may skip if docker absent)
    - All nine behaviors are implemented as distinct test functions
    - No relative-path open(); uses Path(__file__).resolve()-rooted constants
    - `python -m compileall tests/test_distributed_topology.py` succeeds
  </acceptance_criteria>
  <done>Topology test green; asserts two networks, same-last-octet target, sensor-per-segment + console, pinned base, e2e order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sensor container → console push endpoint | bearer-token-authenticated push over the lab console-net |
| host → Docker daemon | compose build pulls base images / installs packages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Tampering | sensor.Dockerfile base image | mitigate | Patch-pin `FROM python:3.11.12-slim` (CHAOS-05); _validate_pinned_tags runs in the `distributed up` arm |
| T-112-02 | Information Disclosure | bearer token in distributed-e2e.sh | accept | Lab-ephemeral one-time tokens; captured via `exec -T` stdout, not written to compose; lab network is isolated |
| T-112-03 | Elevation/Exposure | `quirk serve --host 0.0.0.0` | accept | Bind is 0.0.0.0 ONLY inside the isolated console-net bridge (no host port published for the console push surface); not a production exposure — documented in oracle (Plan 02) |
| T-112-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced (RESEARCH Package Legitimacy: N/A); container installs existing core deps via `.[all]` |
</threat_model>

<verification>
- `docker compose -f quantum-chaos-enterprise-lab/docker-compose.distributed.yml config -q` exits 0
- `bash -n quantum-chaos-enterprise-lab/lab.sh` and `bash -n .../scripts/distributed-e2e.sh` exit 0
- `pytest tests/test_distributed_topology.py -q` passes
- lab.sh `all`/ALL_PROFILES path unchanged (LAB-03 no-drift)
</verification>

<success_criteria>
- LAB-02 host-recording linchpin confirmed in-code and recorded in SUMMARY
- Distributed topology runnable (`config -q` clean); two isolated networks; same recorded host:port across segments
- lab.sh `distributed` command works without disturbing ALL_PROFILES
- distributed-e2e.sh orchestrates enroll→push→merge; static topology test green
</success_criteria>

<output>
Create `.planning/phases/112-distributed-chaos-lab-stabilization/112-01-SUMMARY.md` when done.
Record the confirmed tls_scanner.py file:line for the host-recording linchpin so Plan 02 can cite it.
</output>
