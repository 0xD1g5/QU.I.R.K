---
phase: 73-cbom-intel-reports-warnings
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/intelligence/scoring.py
  - quirk/intelligence/roadmap.py
  - quirk/reports/executive.py
  - quirk/cbom/builder.py
  - quirk/intelligence/confidence.py
  - tests/test_score_weights_invariant.py
  - tests/test_intelligence_roadmap.py
  - tests/test_executive_score_guard.py
  - tests/test_tls_kex_label.py
  - tests/test_intelligence_confidence.py
  - .planning/audit-2026-05-08/AUDIT-TASKS.md
autonomous: true
requirements: [INTEL-03]
must_haves:
  truths:
    - "`quirk/intelligence/scoring.py` has a module-level docstring above SCORE_WEIGHTS naming the invariant (sum=261.0, NOT probabilities, NOT normalized) and citing Phase 60 cap-sharing rationale (D-04, WR-06)"
    - "`tests/test_score_weights_invariant.py` asserts `abs(sum(SCORE_WEIGHTS.values()) - 261.0) < 1e-9` (D-04)"
    - "No SCORE_WEIGHTS values change — only docstring + new invariant test (D-04, D-14)"
    - "`quirk/intelligence/roadmap.py::_why` strips trailing period on `hint` before appending its own period — no `..` artifact (D-05, WR-07)"
    - "`quirk/intelligence/roadmap.py::_add_candidate` has a multi-line docstring explaining the lower-key-wins merge rule (D-06, WR-08); regression test asserts duplicate-title merge picks the lower (phase, _priority, title) tuple"
    - "`quirk/reports/executive.py::_build_interpretation` guards `score['score']` access via `.get(...)` and returns a `_INTERPRETATION_UNAVAILABLE` fallback when missing/non-dict (D-07, WR-09)"
    - "`quirk/cbom/builder.py::_KEX_MAP['RSA'] = 'RSA-kex'` (relabel path per RESEARCH C-4 path (a); the simplest fix per the user's narrowing instruction) — TLS 1.2 non-PFS RSA suites emit `RSA-kex` instead of `RSA` (D-08, WR-12)"
    - "`quirk/intelligence/confidence.py` weight-override inline block at lines 46-49 (NOT a separate `apply_weight_overrides` function — per RESEARCH C-5) coerces each value through `float()`, clamps to `[0.0, 1.0]`, raises `ValueError(f\"Confidence override {k!r} must be numeric in [0.0, 1.0], got {v!r}\")` on non-numeric, and emits a WARNING log for unknown keys (D-09, WR-13)"
    - "Audit rows cbom-intel-reports/WR-06, WR-07, WR-08, WR-09, WR-12, WR-13 are flipped to `Phase 73 | [x] closed`"
  artifacts:
    - path: "quirk/intelligence/scoring.py"
      provides: "SCORE_WEIGHTS invariant docstring (no value changes)"
      contains: "261"
    - path: "tests/test_score_weights_invariant.py"
      provides: "CI gate asserting SCORE_WEIGHTS sum"
      contains: "sum(SCORE_WEIGHTS"
    - path: "quirk/intelligence/roadmap.py"
      provides: "Double-period strip in _why + merge-rule docstring on _add_candidate"
      contains: "rstrip"
    - path: "quirk/reports/executive.py"
      provides: "_build_interpretation score['score'] guard + fallback constant"
      contains: "_INTERPRETATION_UNAVAILABLE"
    - path: "quirk/cbom/builder.py"
      provides: "_KEX_MAP['RSA'] = 'RSA-kex' relabel"
      contains: "RSA-kex"
    - path: "quirk/intelligence/confidence.py"
      provides: "Inline clamp + fail-loud + unknown-key WARNING on weight overrides"
      contains: "max(0.0, min(1.0"
  key_links:
    - from: "quirk/intelligence/scoring.py SCORE_WEIGHTS docstring"
      to: "tests/test_score_weights_invariant.py CI gate"
      via: "abs(sum(SCORE_WEIGHTS.values()) - 261.0) < 1e-9"
      pattern: "abs\\(sum\\(SCORE_WEIGHTS"
    - from: "quirk/intelligence/roadmap.py::_why"
      to: "no-double-period output"
      via: "hint.rstrip('.') + '.'"
      pattern: "rstrip\\('\\.'\\)"
    - from: "quirk/intelligence/roadmap.py::_add_candidate"
      to: "merge-rule docstring + regression test"
      via: 'docstring naming "lower (phase, _priority, title) tuple wins"'
      pattern: "lower.*tuple wins|lower-key wins"
    - from: "quirk/reports/executive.py::_build_interpretation"
      to: "_INTERPRETATION_UNAVAILABLE fallback"
      via: "score.get('score') if isinstance(score, dict) else None"
      pattern: "_INTERPRETATION_UNAVAILABLE"
    - from: "quirk/cbom/builder.py::_KEX_MAP"
      to: "RSA-kex label for TLS 1.2 non-PFS RSA suites"
      via: '_KEX_MAP["RSA"] = "RSA-kex"'
      pattern: '"RSA": "RSA-kex"'
    - from: "quirk/intelligence/confidence.py weight-override block"
      to: "clamp + ValueError on non-numeric + WARNING on unknown key"
      via: "inline at lines 46-49 inside compute_confidence"
      pattern: "must be numeric in \\[0\\.0, 1\\.0\\]"
---

<objective>
Close INTEL-03 with six surgical fixes plus one new invariant test, mirroring the Phase 60 / 70 / 71 precedent of small documented hardening per WR row:

1. **D-04 / WR-06** — `quirk/intelligence/scoring.py` SCORE_WEIGHTS: **documentation + invariant test only**, no value changes. Per the user's narrowing instruction and RESEARCH Pitfall 6/7: `total_score` is already clamped at `scoring.py:219-223` (closed by Phase 60); the runtime arithmetic is correct. The task is a module-level docstring (sum=261.0 by design, NOT probabilities, NOT normalized; cap-sharing rationale per Phase 60 SCORE-04 / CR-06) plus new `tests/test_score_weights_invariant.py` asserting `abs(sum(SCORE_WEIGHTS.values()) - 261.0) < 1e-9`. Per D-14: no SCORE_WEIGHTS value changes; no `_apply_weighted_impacts` edits.

2. **D-05 / WR-07** — `quirk/intelligence/roadmap.py::_why` (lines 48-51 at HEAD): replace `f"{base} Driver: {hint}."` with `f"{base} Driver: {hint.rstrip('.')}."` so a hint already ending in `.` does not produce `..`.

3. **D-06 / WR-08** — `quirk/intelligence/roadmap.py::_add_candidate` (lines 54-80): per RESEARCH C-6 / Pitfall 4, the module is **not a generator** — CONTEXT's "mutation-after-yield" phrasing is figurative. The real undocumented contract is the lower-key-wins merge rule at lines 74-80. Add a multi-line docstring on `_add_candidate` explaining: (a) duplicate titles collide via `items[title]` lookup, (b) the candidate with the lower `(_PHASE_ORDER[phase], int(_priority), title)` tuple replaces the existing entry, (c) cite WR-08 closure. Add regression test asserting duplicate-title behavior picks the lower-key candidate.

4. **D-07 / WR-09** — `quirk/reports/executive.py::_build_interpretation` (lines 29-31): define module-level constant `_INTERPRETATION_UNAVAILABLE = "Score data unavailable for this run."`; in `_build_interpretation`, replace direct `score['score']` subscript with `score_val = score.get('score') if isinstance(score, dict) else None`; if `score_val is None`, return the function's natural return shape with `_INTERPRETATION_UNAVAILABLE` substituted (preserves caller's dict/list expectations).

5. **D-08 / WR-12** — `quirk/cbom/builder.py:177` (NOT `evidence.py` — per RESEARCH C-1 / Pitfall 1 / user narrowing): change `_KEX_MAP["RSA"] = "RSA"` to `_KEX_MAP["RSA"] = "RSA-kex"`. Per the user's narrowing instruction and RESEARCH C-4 path (a), this is the simplest fix matching D-08 intent. Add parametrized test across the 8 known non-PFS RSA TLS 1.2 cipher suites named in CONTEXT `<test_strategy>`.

6. **D-09 / WR-13** — `quirk/intelligence/confidence.py:46-49` (per RESEARCH C-5 / Pitfall 8 / user narrowing: there is **no** `apply_weight_overrides` function — apply the clamp **inline** at the existing override site inside `compute_confidence`): coerce each override value through `float()` (catching `(TypeError, ValueError)` → re-raise as `ValueError(f"Confidence override {k!r} must be numeric in [0.0, 1.0], got {v!r}")`); clamp to `max(0.0, min(1.0, num))`; surface unknown override keys as `logging.WARNING` (forward-compat per CONTEXT, not a hard error).

Then flip audit rows WR-06, WR-07, WR-08, WR-09, WR-12, WR-13 (lines 154-157, 160, 161 of `AUDIT-TASKS.md`) to `Phase 73 | [x] closed`.

Purpose: Six independent documentation/guard/clamp fixes; zero customer-facing score regression (D-04, D-14); explicit invariants and merge rules; safe access patterns for malformed dicts and user-supplied weights.
Output: Edits across 5 source files, 5 new/extended test modules, 6 audit rows flipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/73-cbom-intel-reports-warnings/73-CONTEXT.md
@.planning/phases/73-cbom-intel-reports-warnings/73-RESEARCH.md
@.planning/audit-2026-05-08/AUDIT-TASKS.md

<interfaces>
SCORE_WEIGHTS invariant (verified via session-time Python execution per RESEARCH Pitfall 6):
- `sum(SCORE_WEIGHTS.values()) == 261.0` exactly (29 weights, no FP rounding pressure)
- `total_score` already clamped to `[0, 100]` at `scoring.py:219-223` since Phase 60 SCORE-01

_why current shape (verified at roadmap.py:48-51):
```
def _why(base: str, hint: str) -> str:
    if hint:
        return f"{base} Driver: {hint}."
    return base
```
Fix: `return f"{base} Driver: {hint.rstrip('.')}."`

_add_candidate merge rule (verified at roadmap.py:54-80):
- duplicate title → compare `(_PHASE_ORDER[phase], int(_priority), title)` tuples
- lower tuple wins (replaces existing); higher tuple is dropped

executive.py current shape (verified at lines 29-31):
```
bullets.append(
    f"Quantum Readiness Score is **{score['score']}/100** (**{score['rating']}**)."
)
```
Fix: wrap with `.get()` guard + early-return-fallback shape per D-07.

_KEX_MAP current shape (verified at cbom/builder.py:137-142):
```
_KEX_MAP: dict[str, str] = {
    "ECDHE": "X25519",
    "ECDH":  "X25519",
    "DHE":   "DH-GroupExchange",
    "DH":    "DH-2048",
    "RSA":   "RSA",  # → "RSA-kex" per D-08
}
```

TLS 1.3 path skips _KEX_MAP entirely (verified at cbom/builder.py:192) — D-08 fix scoped to non-TLSv1.3 only (RESEARCH open question 1).

confidence.py current shape (verified at lines 46-49):
```
w = dict(CONFIDENCE_WEIGHTS)
if weights:
    for key, value in weights.items():
        w[key] = _as_float(value)
```

Eight non-PFS RSA TLS 1.2 cipher suites for D-08 parametrized test (per CONTEXT <test_strategy>):
AES128-SHA, AES256-SHA, AES128-SHA256, AES256-SHA256, AES128-GCM-SHA256, AES256-GCM-SHA384, NULL-SHA, NULL-MD5
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SCORE_WEIGHTS docstring + invariant test (D-04, WR-06)</name>
  <files>quirk/intelligence/scoring.py, tests/test_score_weights_invariant.py</files>
  <read_first>
    - quirk/intelligence/scoring.py lines 1-40 (current top-of-module + SCORE_WEIGHTS dict)
    - quirk/intelligence/scoring.py lines 215-225 (total_score clamp, already in place per Phase 60)
    - .planning/phases/60-score-arithmetic-correctness/60-02-SUMMARY.md lines 80-100 (Phase 60 SCORE-04 / CR-06 cap-sharing rationale for the docstring's cross-reference)
    - .planning/phases/73-cbom-intel-reports-warnings/73-CONTEXT.md (D-04, D-14)
  </read_first>
  <behavior>
    - `sum(SCORE_WEIGHTS.values())` equals 261.0 within 1e-9 tolerance.
    - The invariant test fails loudly if any future contributor adds, removes, or modifies a weight value such that the sum drifts.
    - No SCORE_WEIGHTS value changes — golden-file regression tests in existing scoring suite continue to pass (verified by full `pytest -x` at wrap time).
  </behavior>
  <action>
    **Part A — Docstring (no value changes).**
    Immediately before the `SCORE_WEIGHTS = {...}` definition in `quirk/intelligence/scoring.py`, insert a multi-line comment block (or extend the existing module docstring) with:

    ```
    # SCORE_WEIGHTS invariant (D-04, WR-06 — Phase 73 documentation, NOT normalization)
    # ----------------------------------------------------------------------------
    # These values are ABSOLUTE per-ratio coefficients, NOT probabilities, NOT
    # a normalized PMF. Their sum is 261.0 BY DESIGN.
    #
    # The total_score arithmetic at lines 219-223 below already clamps to [0, 100]
    # (closed by Phase 60 SCORE-01) and `_apply_weighted_impacts` shares score caps
    # across these weights — see Phase 60 SCORE-04 / CR-06 closure for the
    # cap-sharing rationale that this docstring deliberately preserves.
    #
    # Any contributor adding, removing, or modifying a weight value MUST update
    # `tests/test_score_weights_invariant.py` to match the new expected sum.
    # CI will fail loudly otherwise.
    ```

    Pick the lighter-touch form (a leading comment block above the dict, OR an extension of the module docstring at the very top of the file if one already exists) — whichever produces the smaller diff while keeping the documentation discoverable from the SCORE_WEIGHTS site. Do NOT change a single weight value. Do NOT renumber lines outside the comment insert (per D-14).

    **Part B — Invariant test.**
    Create `tests/test_score_weights_invariant.py`:

    ```
    """D-04 / WR-06: SCORE_WEIGHTS sum invariant (Phase 73)."""
    from quirk.intelligence.scoring import SCORE_WEIGHTS

    def test_score_weights_sum_invariant():
        """SCORE_WEIGHTS sum must be 261.0 by design (NOT normalized).

        See quirk/intelligence/scoring.py docstring above SCORE_WEIGHTS for
        rationale (Phase 73 / D-04). Any contributor changing this value must
        update this test AND document the rebalance in a phase plan.
        """
        assert abs(sum(SCORE_WEIGHTS.values()) - 261.0) < 1e-9, (
            f"SCORE_WEIGHTS sum drifted from 261.0 to {sum(SCORE_WEIGHTS.values())}. "
            "Per D-04 this is intentional — update this test ONLY if rebalance is documented."
        )

    def test_score_weights_count_invariant():
        """Anchors the 29-weight count alongside the sum invariant."""
        assert len(SCORE_WEIGHTS) == 29
    ```

    Per D-14: do NOT touch `_apply_weighted_impacts`; do NOT change any value in SCORE_WEIGHTS; do NOT normalize.

    PEP 8. Minimal diff.
  </action>
  <verify>
    <automated>python -m compileall quirk/intelligence/scoring.py && pytest tests/test_score_weights_invariant.py tests/test_intelligence_scoring.py tests/test_score_clamp_property.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "SCORE_WEIGHTS invariant" quirk/intelligence/scoring.py` returns >= 1 hit
    - `grep -nE "261\.0 BY DESIGN" quirk/intelligence/scoring.py` returns >= 1 hit (or equivalent uppercase-sensitive wording from Part A — alternative phrasings acceptable as long as the "261.0 by design / NOT probabilities" claim is present)
    - `grep -nE "Phase 60 SCORE-04|CR-06" quirk/intelligence/scoring.py` returns >= 1 hit (cap-sharing cross-reference)
    - `tests/test_score_weights_invariant.py` exists
    - `grep -nE "abs\(sum\(SCORE_WEIGHTS" tests/test_score_weights_invariant.py` returns 1 hit
    - `pytest tests/test_score_weights_invariant.py -x` exits 0
    - `pytest tests/test_intelligence_scoring.py tests/test_score_clamp_property.py -x` exits 0 (golden-file regression — no scorecard moved)
    - `git diff quirk/intelligence/scoring.py | grep -cE "^[-+][^-+]" | head -1` reflects ONLY the comment-block insertion (no SCORE_WEIGHTS value lines changed)
  </acceptance_criteria>
  <done>
    SCORE_WEIGHTS sum invariant documented in source + asserted by CI test; no behavior change.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Roadmap _why double-period + _add_candidate merge-rule docstring (D-05, D-06; WR-07, WR-08)</name>
  <files>quirk/intelligence/roadmap.py, tests/test_intelligence_roadmap.py</files>
  <read_first>
    - quirk/intelligence/roadmap.py lines 1-110 (imports + _why + _add_candidate)
    - tests/test_intelligence_roadmap.py (current shape — for extension)
    - .planning/phases/73-cbom-intel-reports-warnings/73-CONTEXT.md (D-05, D-06)
    - .planning/phases/73-cbom-intel-reports-warnings/73-RESEARCH.md (Pitfall 4, C-6, Code Examples for _why + _add_candidate)
  </read_first>
  <behavior>
    - WR-07 (D-05): `_why("Base statement.", "Driver text.")` returns `"Base statement. Driver: Driver text."` — exactly one trailing period.
    - WR-07: `_why("Base statement.", "Driver text")` (no trailing period on hint) also returns `"Base statement. Driver: Driver text."` — behavior preserved for the no-period input.
    - WR-07: `_why("Base statement.", "")` returns `"Base statement."` — unchanged behavior for empty hint.
    - WR-08 (D-06): when `_add_candidate` is called twice with the same title and the second candidate has a lower `(phase_order, _priority, title)` tuple, the second candidate replaces the first in `items`. When the second candidate has a higher tuple, the original is preserved. Equal tuples preserve the original (current behavior — `new_key < old_key`).
  </behavior>
  <action>
    **Part A — D-05 (WR-07).**
    Edit `quirk/intelligence/roadmap.py::_why` (lines 48-51 at HEAD). Replace the return line:
    ```
    return f"{base} Driver: {hint}."
    ```
    with:
    ```
    return f"{base} Driver: {hint.rstrip('.')}."
    ```
    No other change to the function body. The empty-hint early-return at the previous line is untouched.

    **Part B — D-06 (WR-08).**
    Edit `quirk/intelligence/roadmap.py::_add_candidate` (lines 54-80 at HEAD). Add a multi-line docstring immediately under the `def _add_candidate(...)` signature:

    ```
    """Merge a candidate into the items dict by title (D-06 / WR-08 Phase 73).

    Merge rule (the previously-undocumented contract):
      - If `title` is not in `items`, the candidate is inserted as-is.
      - If `title` is already in `items`, the existing entry and the new
        candidate are compared via the tuple
        `(_PHASE_ORDER[phase], int(_priority), title)`.
      - The candidate with the LEXICOGRAPHICALLY-LOWER tuple wins and replaces
        the existing entry. Equal tuples preserve the original (strict `<`).

    This is intentional merge accumulator behavior — earlier-phase / higher-
    priority candidates take precedence regardless of insertion order, so
    callers can register candidates in any order without affecting the final
    roadmap shape. See `.planning/audit-2026-05-08/AUDIT-TASKS.md`
    cbom-intel-reports/WR-08 closure (Phase 73) for context.
    """
    ```

    No code change in the function body — the existing `new_key < old_key` comparison IS the documented rule.

    **Part C — Tests.**
    Extend `tests/test_intelligence_roadmap.py`:
    - `test_why_no_double_period_when_hint_ends_with_period` — `_why("Base.", "Hint.")` returns no `..`; assert exact equality to `"Base. Driver: Hint."`.
    - `test_why_preserves_no_period_hint` — `_why("Base.", "Hint")` returns `"Base. Driver: Hint."`.
    - `test_why_empty_hint_unchanged` — `_why("Base.", "")` returns `"Base."`.
    - `test_add_candidate_merge_lower_key_wins` — call `_add_candidate` twice with the same title; the second call has a lower phase / priority tuple; assert `items[title]` equals the second candidate. Use the existing `_PHASE_ORDER` constants in the module for realistic phase values.
    - `test_add_candidate_merge_higher_key_loses` — same shape but the second call has a higher tuple; assert `items[title]` still equals the first candidate.
    - `test_add_candidate_merge_equal_key_preserves_original` — equal tuples; original survives (strict `<`).

    Per D-14: do NOT change `_add_candidate`'s body; the docstring is the entire WR-08 fix. Do NOT touch `trends.py`.

    PEP 8. Minimal diff.
  </action>
  <verify>
    <automated>python -m compileall quirk/intelligence/roadmap.py && pytest tests/test_intelligence_roadmap.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "hint\.rstrip\('\.'\)" quirk/intelligence/roadmap.py` returns 1 hit
    - `grep -nE "Merge rule" quirk/intelligence/roadmap.py` returns 1 hit (docstring presence)
    - `grep -nE "WR-08" quirk/intelligence/roadmap.py` returns 1 hit (cite in docstring)
    - `grep -nE "LEXICOGRAPHICALLY-LOWER tuple wins" quirk/intelligence/roadmap.py` returns 1 hit (or equivalent — the docstring text from Part B)
    - `python -m compileall quirk/intelligence/roadmap.py` exits 0
    - `pytest tests/test_intelligence_roadmap.py -x` exits 0
    - `grep -cE "def test_(why_no_double_period|why_preserves_no_period|why_empty_hint|add_candidate_merge)" tests/test_intelligence_roadmap.py` >= 5
  </acceptance_criteria>
  <done>
    `_why` no longer emits `..` artifacts; `_add_candidate` merge rule documented + regression-tested.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Executive _build_interpretation score guard (D-07, WR-09)</name>
  <files>quirk/reports/executive.py, tests/test_executive_score_guard.py</files>
  <read_first>
    - quirk/reports/executive.py (full file, focus on _build_interpretation at lines 20-50 — confirm return shape)
    - .planning/phases/73-cbom-intel-reports-warnings/73-CONTEXT.md (D-07)
    - .planning/phases/70-* or wherever Phase 70 D-07 guard discipline lives (reference pattern — read at most one comparable guard site)
  </read_first>
  <behavior>
    - When `score` is a dict with `"score"` and `"rating"` keys present, `_build_interpretation` returns its existing pre-fix output unchanged.
    - When `score` is `None`, `_build_interpretation` returns the function's natural output shape with `_INTERPRETATION_UNAVAILABLE` as the bullet/text content (no KeyError).
    - When `score` is `{}` (empty dict — missing `"score"` key), `_build_interpretation` returns the fallback shape (no KeyError).
    - When `score` is a non-dict (e.g., a string or int), `_build_interpretation` returns the fallback shape (no AttributeError on `.get`).
  </behavior>
  <action>
    Edit `quirk/reports/executive.py::_build_interpretation`:

    1. At module level (top of file, alongside other module constants), add:
       ```
       _INTERPRETATION_UNAVAILABLE = "Score data unavailable for this run."
       ```
       (Place near the existing module-level constants — read the file top to find the appropriate insertion point.)

    2. Inside `_build_interpretation`, BEFORE the existing `bullets.append(f"Quantum Readiness Score is **{score['score']}/100** ...")` line at line 29-31, insert the guard:
       ```
       score_val = score.get("score") if isinstance(score, dict) else None
       if score_val is None:
           return {"bullets": [_INTERPRETATION_UNAVAILABLE]}  # or the function's natural empty-shape return — read existing return statements to determine the canonical shape
       ```

       **Critical:** read the existing return shape of `_build_interpretation` first. If the function returns a dict with `bullets` key, return `{"bullets": [_INTERPRETATION_UNAVAILABLE], ...}` preserving any other required keys with their natural empty defaults. If it returns a different shape (list, str, tuple), match that shape. The fallback should preserve caller's destructuring expectations.

    3. After the guard, replace the `score['score']` and `score['rating']` direct subscripts with `score_val` and `score.get('rating', 'Unknown')` (or the equivalent `.get(...)` chain). The `isinstance(score, dict)` branch above guarantees `score` is a dict at this point.

    Create `tests/test_executive_score_guard.py`:
    - `test_build_interpretation_with_full_score_dict` — `_build_interpretation({"score": 75, "rating": "Good"})` produces a non-fallback output containing "75" and "Good".
    - `test_build_interpretation_with_none_score` — `_build_interpretation(None)` returns the fallback shape; no KeyError; output contains `_INTERPRETATION_UNAVAILABLE`.
    - `test_build_interpretation_with_empty_dict` — `_build_interpretation({})` returns the fallback shape.
    - `test_build_interpretation_with_non_dict` — `_build_interpretation("not a dict")` returns the fallback shape (no AttributeError).
    - `test_build_interpretation_with_missing_rating_key` — `_build_interpretation({"score": 50})` produces a non-fallback output (the `score` key is present), using a default rating value.

    Per D-14: do NOT touch `reports/technical.py` (Phase 61 closed it); do NOT touch the rest of `executive.py` beyond the guard + constant.

    PEP 8. Minimal diff.
  </action>
  <verify>
    <automated>python -m compileall quirk/reports/executive.py && pytest tests/test_executive_score_guard.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "_INTERPRETATION_UNAVAILABLE" quirk/reports/executive.py` returns >= 2 hits (definition + use)
    - `grep -nE "isinstance\(score, dict\)" quirk/reports/executive.py` returns 1 hit
    - `grep -nE "score\.get\(.score.\)" quirk/reports/executive.py` returns >= 1 hit
    - `grep -v '^#' quirk/reports/executive.py | grep -cE 'score\[.score.\]'` == 0 (direct subscript removed)
    - `python -m compileall quirk/reports/executive.py` exits 0
    - `pytest tests/test_executive_score_guard.py -x` exits 0
    - `grep -cE "def test_build_interpretation" tests/test_executive_score_guard.py` >= 5
  </acceptance_criteria>
  <done>
    `_build_interpretation` returns a fallback for missing/non-dict score; 5 RED-then-GREEN tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: _KEX_MAP RSA-kex relabel (D-08, WR-12)</name>
  <files>quirk/cbom/builder.py, tests/test_tls_kex_label.py</files>
  <read_first>
    - quirk/cbom/builder.py lines 130-220 (full _KEX_MAP definition + _decompose_cipher_suite body + TLS 1.3 skip branch at line 192)
    - .planning/phases/73-cbom-intel-reports-warnings/73-CONTEXT.md (D-08, D-14)
    - .planning/phases/73-cbom-intel-reports-warnings/73-RESEARCH.md (Pitfall 1, Pitfall 5, C-1, C-4)
  </read_first>
  <behavior>
    - For each of the 8 non-PFS RSA TLS 1.2 cipher suites (AES128-SHA, AES256-SHA, AES128-SHA256, AES256-SHA256, AES128-GCM-SHA256, AES256-GCM-SHA384, NULL-SHA, NULL-MD5), `_decompose_cipher_suite` returns a parts list whose KEX token is `"RSA-kex"` (not `"RSA"`).
    - For an ECDHE-RSA suite (e.g., `ECDHE-RSA-AES256-GCM-SHA384`), the KEX token remains the existing ECDHE-mapped value (`"X25519"` per current _KEX_MAP) — RSA-kex relabel does NOT propagate into ECDHE suites.
    - For TLS 1.3 suites (TLS_AES_256_GCM_SHA384, etc.), `_decompose_cipher_suite` skips _KEX_MAP entirely (verified at line 192) and behavior is unchanged. (RESEARCH open question 1 adjudication: scope D-08 to non-TLS1.3 only.)
  </behavior>
  <action>
    Per the user's narrowing instruction and RESEARCH C-4 path (a), this is a single-line change:

    Edit `quirk/cbom/builder.py:142` (the `_KEX_MAP` entry):
    ```
    "RSA":   "RSA",
    ```
    becomes:
    ```
    "RSA":   "RSA-kex",
    ```

    No other change to `_decompose_cipher_suite` or to the auth-loop (per D-14 / "no Pass-1/2/3 logic changes" + per RESEARCH C-4 minimal-diff recommendation). Do NOT emit `RSA-auth` as a separate role label (RESEARCH C-4 path (b) is explicitly NOT chosen).

    Create `tests/test_tls_kex_label.py`:
    - Parametrize over the 8 known non-PFS RSA TLS 1.2 cipher suites listed in `<interfaces>`. Each test calls `_decompose_cipher_suite(cipher_suite, tls_version="TLSv1.2")` (or the function's actual signature — read the file first to confirm the argument shape) and asserts the resulting parts list contains `"RSA-kex"` and does NOT contain a bare `"RSA"` token.
    - One additional test: `test_ecdhe_rsa_does_not_get_rsa_kex_label` — assert ECDHE-RSA-AES256-GCM-SHA384 still maps via the ECDHE branch (KEX in parts is the ECDHE mapping, not `"RSA-kex"`).
    - One additional test: `test_tls13_path_unaffected` — call `_decompose_cipher_suite("TLS_AES_256_GCM_SHA384", tls_version="TLSv1.3")` and assert no `"RSA"` or `"RSA-kex"` token in the result (TLS 1.3 skips _KEX_MAP entirely per builder.py line 192).

    Per D-14: do NOT touch CBOM builder's Pass-1/2/3 logic; do NOT touch the auth-loop at builder.py:220-237.

    PEP 8. Minimal diff (the source change is a one-token edit).
  </action>
  <verify>
    <automated>python -m compileall quirk/cbom/builder.py && pytest tests/test_tls_kex_label.py tests/test_cbom_writer.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '"RSA": *"RSA-kex"' quirk/cbom/builder.py` returns 1 hit
    - `grep -v '^#' quirk/cbom/builder.py | grep -cE '"RSA": *"RSA"(,| *$)'` == 0 (old label gone)
    - `tests/test_tls_kex_label.py` exists with parametrized table covering 8 suites + 2 negative tests
    - `pytest tests/test_tls_kex_label.py -x` exits 0
    - `pytest tests/test_cbom_writer.py -x` exits 0 (no regression in existing CBOM output)
    - `grep -cE "@pytest\.mark\.parametrize" tests/test_tls_kex_label.py` >= 1
  </acceptance_criteria>
  <done>
    `_KEX_MAP["RSA"] = "RSA-kex"`; 10+ KEX-label tests pass; no Pass-1/2/3 logic change.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Confidence weight overrides — inline clamp + fail-loud + WARNING (D-09, WR-13)</name>
  <files>quirk/intelligence/confidence.py, tests/test_intelligence_confidence.py</files>
  <read_first>
    - quirk/intelligence/confidence.py (full file — confirm lines 40-55 weight-override block + existing `_as_float` helper if any)
    - tests/test_intelligence_confidence.py (current shape — for extension)
    - .planning/phases/71-protocol-scanner-warnings/71-* PROTO-01 plan (Phase 71 D-06 clamp precedent)
    - .planning/phases/73-cbom-intel-reports-warnings/73-CONTEXT.md (D-09)
    - .planning/phases/73-cbom-intel-reports-warnings/73-RESEARCH.md (Pitfall 8, C-5, Code Examples for confidence.py)
  </read_first>
  <behavior>
    - For a known override key with a value in `[0.0, 1.0]`: the value is stored unchanged (float-coerced).
    - For a known override key with a value `< 0.0`: clamped to 0.0; no exception.
    - For a known override key with a value `> 1.0`: clamped to 1.0; no exception.
    - For a non-numeric value (e.g., `"abc"`, a list, None): raises `ValueError` whose message contains `must be numeric in [0.0, 1.0]` and the offending key + value.
    - For an unknown override key (not in `CONFIDENCE_WEIGHTS`): the value is still applied (forward-compat) AND a WARNING is logged via `logging.getLogger(__name__).warning(...)` with a message mentioning the unknown key.
    - Pre-existing tests in `tests/test_intelligence_confidence.py` that exercise the no-overrides path continue to pass unchanged (confidence calc behavior unchanged when `weights` is None / empty).
  </behavior>
  <action>
    Per RESEARCH C-5 / Pitfall 8 / user narrowing: the function `apply_weight_overrides` does NOT exist. Apply the fix **inline** at `quirk/intelligence/confidence.py:46-49` inside `compute_confidence`.

    Edit `quirk/intelligence/confidence.py`:

    1. At module top, add `import logging` (if not already present — grep first).

    2. At module top (alongside CONFIDENCE_WEIGHTS), add:
       ```
       _LOGGER = logging.getLogger(__name__)
       _KNOWN_CONFIDENCE_KEYS = frozenset(CONFIDENCE_WEIGHTS.keys())
       ```

    3. Replace the existing block at lines 46-49:
       ```
       w = dict(CONFIDENCE_WEIGHTS)
       if weights:
           for key, value in weights.items():
               w[key] = _as_float(value)
       ```
       with:
       ```
       w = dict(CONFIDENCE_WEIGHTS)
       if weights:
           for key, value in weights.items():
               try:
                   num = float(value)
               except (TypeError, ValueError):
                   raise ValueError(
                       f"Confidence override {key!r} must be numeric in [0.0, 1.0], got {value!r}"
                   )
               w[key] = max(0.0, min(1.0, num))
               if key not in _KNOWN_CONFIDENCE_KEYS:
                   _LOGGER.warning(
                       "Unknown confidence override key %r — forward-compat (Phase 73 / D-09)", key
                   )
       ```

    If the file already has an `_as_float` helper used elsewhere, leave that helper untouched — D-14 forbids incidental refactor. The new inline path uses its own bare `float()` call inside `try/except (TypeError, ValueError)` per CONTEXT D-09 / RESEARCH C-5 spec.

    Extend `tests/test_intelligence_confidence.py`:
    - `test_override_clamps_below_zero` — pass `weights={"some_known_key": -0.5}`; assert resolved weight is `0.0`.
    - `test_override_clamps_above_one` — pass `weights={"some_known_key": 1.5}`; assert resolved weight is `1.0`.
    - `test_override_in_range_passes_through` — pass `weights={"some_known_key": 0.7}`; assert resolved weight is `0.7`.
    - `test_override_non_numeric_raises_value_error` — pass `weights={"some_known_key": "abc"}`; assert `pytest.raises(ValueError, match="must be numeric in \\[0\\.0, 1\\.0\\]")`.
    - `test_override_none_value_raises_value_error` — pass `weights={"some_known_key": None}`; assert `pytest.raises(ValueError)`.
    - `test_override_list_value_raises_value_error` — pass `weights={"some_known_key": [1.0]}`; assert `pytest.raises(ValueError)`.
    - `test_override_unknown_key_logs_warning_and_accepts(caplog)` — pass `weights={"unknown_xyz": 0.5}`; assert no exception raised; assert `caplog.records` contains a WARNING-level record whose message contains `"unknown_xyz"` and `"forward-compat"` (or equivalent text per D-09).

    Use the first key in `CONFIDENCE_WEIGHTS` as `"some_known_key"` (read the file to identify it; expected to be one of the four canonical keys per RESEARCH open question 2).

    Per D-14: do NOT change the unrelated parts of `confidence.py`; do NOT tighten the unknown-key WARNING into a ValueError (CONTEXT explicitly chose WARNING for forward-compat); do NOT extract a separate `apply_weight_overrides` function (RESEARCH C-5: inline is the locked path).

    PEP 8. Minimal diff.
  </action>
  <verify>
    <automated>python -m compileall quirk/intelligence/confidence.py && pytest tests/test_intelligence_confidence.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "max\(0\.0, min\(1\.0," quirk/intelligence/confidence.py` returns 1 hit
    - `grep -nE "must be numeric in \[0\.0, 1\.0\]" quirk/intelligence/confidence.py` returns 1 hit
    - `grep -nE "_KNOWN_CONFIDENCE_KEYS" quirk/intelligence/confidence.py` returns >= 2 hits
    - `grep -nE "_LOGGER\.warning" quirk/intelligence/confidence.py` returns >= 1 hit
    - `grep -nE "Unknown confidence override key" quirk/intelligence/confidence.py` returns 1 hit
    - `python -m compileall quirk/intelligence/confidence.py` exits 0
    - `pytest tests/test_intelligence_confidence.py -x` exits 0
    - `grep -cE "def test_override_" tests/test_intelligence_confidence.py` >= 7
  </acceptance_criteria>
  <done>
    Confidence override block clamps + fails loud on non-numeric + WARNs on unknown keys; 7 RED-then-GREEN tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 6: Flip WR-06, WR-07, WR-08, WR-09, WR-12, WR-13 audit rows</name>
  <files>.planning/audit-2026-05-08/AUDIT-TASKS.md</files>
  <read_first>
    - .planning/audit-2026-05-08/AUDIT-TASKS.md lines 154-157, 160, 161
  </read_first>
  <action>
    Update `.planning/audit-2026-05-08/AUDIT-TASKS.md`:
    - Line 154 (WR-06): `| — | [ ] open |` → `| Phase 73 | [x] closed |`
    - Line 155 (WR-07): same flip
    - Line 156 (WR-08): same flip
    - Line 157 (WR-09): same flip
    - Line 160 (WR-12): same flip
    - Line 161 (WR-13): same flip

    Six rows, single docs commit at end. No other rows touched. Per-row evidence summaries land in the SUMMARY.md.
  </action>
  <verify>
    <automated>grep -cE "cbom-intel-reports/WR-(06|07|08|09|12|13).*Phase 73.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "cbom-intel-reports/WR-(06|07|08|09|12|13).*Phase 73.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 6
    - Combined with the row flips from Plans 73-01 and 73-02, all 13 open `cbom-intel-reports/WR-*` rows (WR-01..WR-04, WR-06..WR-14) should now show `Phase 73 | [x] closed` (WR-05 already closed by Phase 60).
    - `grep -cE "cbom-intel-reports/WR-.*\[ \] open" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 0 (full INTEL cluster closed)
    - `git diff --stat .planning/audit-2026-05-08/AUDIT-TASKS.md` shows only the six row lines for this plan modified
  </acceptance_criteria>
  <done>
    AUDIT-TASKS.md shows WR-06, WR-07, WR-08, WR-09, WR-12, WR-13 closed under Phase 73 — completing the full 13-row INTEL cluster closure for the phase.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-supplied confidence weight override → confidence compute pipeline | Unbounded values poison `factor_breakdown.points` shown in JSON output (WR-13) |
| Malformed `score` dict → executive report | Direct `score['score']` subscript raises KeyError when key missing (WR-09) |
| Future contributor → SCORE_WEIGHTS | Accidental rebalance silently changes every customer scorecard (D-04, WR-06) |
| Roadmap candidate emitters → consumer | Undocumented merge-rule produces order-dependent output if rule is violated (WR-08) |
| Cipher decomposer → CBOM consumer | Wrong KEX label (`RSA` vs `RSA-kex`) misleads remediation downstream (WR-12) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-08 | Tampering (output integrity) | `confidence.py` weight override path | mitigate | Inline clamp + fail-loud + WARNING on unknown keys (D-09) |
| T-73-09 | Denial of Service (KeyError) | `executive.py::_build_interpretation` | mitigate | `score.get(...)` guard + `_INTERPRETATION_UNAVAILABLE` fallback (D-07) |
| T-73-10 | Tampering (silent rebalance) | `scoring.py::SCORE_WEIGHTS` | mitigate | CI invariant test + module docstring (D-04); no value changes (D-14) |
| T-73-11 | Tampering (output ambiguity) | `cbom/builder.py::_KEX_MAP["RSA"]` | mitigate | Relabel to `"RSA-kex"` to disambiguate from cert-signature `RSA-auth` (D-08) |
| T-73-12 | Information Disclosure (typographic artifact) | `roadmap.py::_why` double-period | mitigate | `hint.rstrip('.')` normalization (D-05); cosmetic but auditable |
| T-73-13 | Tampering (undocumented contract) | `roadmap.py::_add_candidate` merge rule | mitigate | Docstring + regression test (D-06) — no behavior change |
</threat_model>

<verification>
- `python -m compileall quirk/intelligence/scoring.py quirk/intelligence/roadmap.py quirk/reports/executive.py quirk/cbom/builder.py quirk/intelligence/confidence.py` clean
- `pytest tests/test_score_weights_invariant.py tests/test_intelligence_roadmap.py tests/test_executive_score_guard.py tests/test_tls_kex_label.py tests/test_intelligence_confidence.py tests/test_intelligence_scoring.py tests/test_score_clamp_property.py tests/test_cbom_writer.py -x` passes
- AUDIT-TASKS.md WR-06, WR-07, WR-08, WR-09, WR-12, WR-13 rows show `Phase 73 | [x] closed`
- Combined with Plans 73-01 / 73-02, **all 13 open `cbom-intel-reports/WR-*` rows are closed** under Phase 73 (WR-05 was closed by Phase 60).
- No SCORE_WEIGHTS value changes (per D-14); no `_apply_weighted_impacts` touched; no Pass-1/2/3 CBOM builder logic touched; no `trends.py` touched; no `technical.py` touched.
</verification>

<success_criteria>
- SCORE_WEIGHTS invariant documented + CI-gated (sum=261.0); zero customer-facing score regression
- `roadmap.py::_why` no double-period
- `roadmap.py::_add_candidate` merge rule documented
- `executive.py::_build_interpretation` guards `score['score']` + fallback constant
- `cbom/builder.py::_KEX_MAP["RSA"] = "RSA-kex"`
- `confidence.py` weight overrides clamp + fail-loud + WARN on unknown keys (inline at lines 46-49)
- 25+ tests across 5 new/extended modules pass
- WR-06, WR-07, WR-08, WR-09, WR-12, WR-13 closed; full 13-row INTEL cluster closed for Phase 73
</success_criteria>

<output>
Create `.planning/phases/73-cbom-intel-reports-warnings/73-03-SUMMARY.md` documenting:
- D-04 docstring + invariant test (no value changes; cites Phase 60 cap-sharing rationale)
- D-05 _why rstrip
- D-06 _add_candidate docstring (RESEARCH C-6: "mutation-after-yield" is figurative — module is not a generator)
- D-07 executive guard + fallback constant
- D-08 _KEX_MAP relabel at `cbom/builder.py:142` (NOT `evidence.py` per RESEARCH C-1; path (a) per RESEARCH C-4)
- D-09 inline clamp + fail-loud + WARNING (RESEARCH C-5: no `apply_weight_overrides` function — inline at `confidence.py:46-49`)
- Six audit rows flipped (WR-06/07/08/09/12/13)
- Cluster-level note: all 13 INTEL `cbom-intel-reports/WR-*` rows now closed under Phase 73 (combined with plans 73-01 and 73-02)
</output>
</content>
