---
phase: 102-dashboard-auth-ux-score-tax
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/reports/executive.py
  - tests/test_score_parity.py
autonomous: true
requirements: [TRANS-04]
must_haves:
  truths:
    - "The CLI executive markdown score total, band, and subscores come from the shared exec_content, not a local re-derivation"
    - "The emitted score numbers are unchanged versus the current HTML/PDF (HTML/PDF are the reference)"
    - "A cross-surface parity test asserts the CLI score equals exec_content (which HTML/PDF/DOCX already source)"
  artifacts:
    - path: "quirk/reports/executive.py"
      provides: "build_exec_markdown score section sourced from exec_content.score_total/score_band/subscores/raw_sum"
      contains: "exec_content.score_total"
    - path: "tests/test_score_parity.py"
      provides: "TRANS-04 cross-surface score parity test"
      contains: "def test_score_parity_across_surfaces"
  key_links:
    - from: "quirk/reports/executive.py::build_exec_markdown"
      to: "exec_content.score_total / .score_band / .subscores / .raw_sum"
      via: "score section reads exec_content fields in the exec_content-is-not-None branch"
      pattern: "exec_content\\.(score_total|score_band|subscores|raw_sum)"
---

<objective>
Pay down the v5.2 score-tax tech debt (TRANS-04): the CLI executive markdown sources score total / band / subscores / rollup from the shared `exec_content` model instead of re-deriving from the local `score_raw` dict. Pure sourcing refactor — emitted numbers must not change.

Purpose: Guarantees the CLI executive report shows numerically identical scores to HTML/PDF/DOCX, closing the carried-forward tech-debt item.
Output: Modified `quirk/reports/executive.py` score section, new `tests/test_score_parity.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/102-dashboard-auth-ux-score-tax/102-CONTEXT.md
@.planning/phases/102-dashboard-auth-ux-score-tax/102-PATTERNS.md

<interfaces>
quirk/reports/executive.py::build_exec_markdown(cfg, endpoints, findings, *, exec_content) — already receives exec_content and uses it for narrative/risks/roadmap. The SCORE section still reads score_raw:
- line ~204: `**Score:** **{score_raw['score']}/100**  \n**Rating:** **{score_raw['rating']}**`
- line ~227-228: `subscores = score_raw.get("subscores") or {}`
- line ~233: `raw_sum = exec_content.raw_sum if exec_content is not None else sum(...)`  ← already partially sourced
- line ~238: `**Rollup:** {raw_sum} ÷ 1.5 = **{score_raw['score']} / 100**`

quirk/reports/content_model.py ExecContent (VERIFIED fields):
- score_total: int
- score_band: str
- subscores: Dict[str, Any]
- raw_sum: int

The `exec_content is None` backward-compat branch (lines ~192-202) keeps reading score_raw — writer.py ALWAYS passes exec_content, so that legacy path is for external callers only and stays unchanged.

tests/test_cross_surface_parity.py (analog) — reuse module-level fixtures _SCORE_RAW, _FINDINGS, _ROADMAP_ITEMS_RAW, _make_minimal_cfg; build_exec_content from quirk.reports.content_model; build_exec_markdown from quirk.reports.executive.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing TRANS-04 score parity test (Wave 0)</name>
  <files>tests/test_score_parity.py</files>
  <read_first>
    - tests/test_cross_surface_parity.py (fixtures _SCORE_RAW/_FINDINGS/_make_minimal_cfg + test_narrative_content_parity template)
    - .planning/phases/102-dashboard-auth-ux-score-tax/102-PATTERNS.md (test_cross_surface_parity extension section)
  </read_first>
  <behavior>
    - test_score_parity_across_surfaces: build exec_content via build_exec_content(score_raw=_SCORE_RAW,...); render CLI via build_exec_markdown(..., exec_content=exec_content); assert str(exec_content.score_total) in cli_output; assert exec_content.score_band in cli_output; for each (key,val) in exec_content.subscores: assert str(val) in cli_output
  </behavior>
  <action>
    Create tests/test_score_parity.py importing the shared fixtures from tests/test_cross_surface_parity.py (or re-declaring minimal copies of _SCORE_RAW/_FINDINGS/_make_minimal_cfg if cross-file import is awkward — match the analog's fixture shape exactly). Implement test_score_parity_across_surfaces per the behavior block. Because the CLI currently renders score_raw['score'] which numerically equals exec_content.score_total for the SAME scan, this test may pass even before the refactor for the total — so ALSO add an assertion that the score section line does NOT depend on score_raw by checking band/subscores parity (exec_content.score_band must literally appear; if the CLI re-derived a different band string this would fail). The strict RED signal for this plan is the implementation diff; if the test is GREEN pre-refactor, that confirms numeric stability (acceptable) — the refactor's purpose is sourcing, verified by grep in Task 2's acceptance.
  </action>
  <verify>
    <automated>python -m pytest tests/test_score_parity.py -q 2>&1 | grep -qE "passed|failed" && echo COLLECTED-OK</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_score_parity.py::test_score_parity_across_surfaces exists and runs
    - Asserts score_total, score_band, and every subscore value appear in the CLI markdown
  </acceptance_criteria>
  <done>Score parity test exists and exercises the CLI markdown surface</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Source the executive score section from exec_content</name>
  <files>quirk/reports/executive.py</files>
  <read_first>
    - quirk/reports/executive.py lines 180-240 (score section + the exec_content-is-not-None narrative branch above it)
    - .planning/phases/102-dashboard-auth-ux-score-tax/102-PATTERNS.md (executive.py TRANS-04 section)
  </read_first>
  <action>
    In build_exec_markdown, inside the exec_content-is-not-None path, replace the three score_raw reads with exec_content fields: `score_raw['score']` → `exec_content.score_total` (the Score line ~204 and the Rollup line ~238); `score_raw['rating']` → `exec_content.score_band` (line ~204); `score_raw.get("subscores") or {}` → `exec_content.subscores` (line ~227-228). The raw_sum read already prefers exec_content.raw_sum — keep it. Do NOT touch the `exec_content is None` backward-compat branch (it must keep reading score_raw for external callers). This is a pure sourcing substitution; the emitted numbers must be identical to before because exec_content carries the same canonical values. Do NOT introduce any new score computation. Out of scope: writer.py::_scorecard_markdown (separate artifact) unless the parity test reveals a discrepancy.
  </action>
  <verify>
    <automated>python -m compileall quirk/reports/executive.py -q && python -m pytest tests/test_score_parity.py tests/test_cross_surface_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m compileall` clean
    - tests/test_score_parity.py + tests/test_cross_surface_parity.py GREEN
    - `grep -c 'exec_content\.score_total' quirk/reports/executive.py` >= 2 (Score line + Rollup line)
    - The score section inside the exec_content-is-not-None branch contains NO `score_raw['score']` or `score_raw['rating']` reads: `sed -n '180,245p' quirk/reports/executive.py | grep -v '^#' | grep -c "score_raw\['score'\]\|score_raw\['rating'\]"` — count reflects only the legacy None-branch, not the active branch (verify visually that active-branch reads are gone)
  </acceptance_criteria>
  <done>CLI executive score section sourced from exec_content; numbers unchanged; parity tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scan score model → rendered reports | a single canonical score must cross consistently into CLI/HTML/PDF/DOCX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-10 | Tampering | divergent score across report surfaces (integrity of the deliverable) | mitigate | Single source (exec_content) for CLI score; cross-surface parity test asserts numeric equality so a future change cannot silently desync CLI from HTML/PDF/DOCX. |
| T-102-11 | Repudiation | score numbers change unexpectedly during refactor | mitigate | Pure sourcing substitution; HTML/PDF are the reference; parity test + grep gate confirm no new computation introduced. |
| T-102-SC | Tampering | npm/pip installs | mitigate | No new packages. No slopcheck checkpoint required. |
</threat_model>

<verification>
- `python -m compileall quirk/reports/executive.py` clean
- `python -m pytest tests/test_score_parity.py tests/test_cross_surface_parity.py -q` green
</verification>

<success_criteria>
TRANS-04 satisfied: CLI executive markdown score total/band/subscores/rollup are sourced from exec_content; numbers are identical to HTML/PDF; parity test enforces it.
</success_criteria>

<output>
Create `.planning/phases/102-dashboard-auth-ux-score-tax/102-03-SUMMARY.md` when done
</output>
