---
phase: 102-dashboard-auth-ux-score-tax
plan: 05
type: execute
wave: 3
depends_on: [102-01, 102-02, 102-03, 102-04]
files_modified:
  - docs/configuration.md
  - docs/UAT-SERIES.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, TRANS-04]
must_haves:
  truths:
    - "Operator documentation describes `quirk token generate/rotate/show` and X-API-Key auth"
    - "docs/UAT-SERIES.md reflects the new login flow + token CLI test cases and is synced to Obsidian"
    - "An Obsidian phase note for Phase 102 exists with status and what-was-built"
  artifacts:
    - path: "docs/configuration.md"
      provides: "quirk token CLI + X-API-Key dashboard auth documentation"
      contains: "quirk token"
    - path: "docs/UAT-SERIES.md"
      provides: "updated test series for token CLI + login flow + Last Updated date"
      contains: "quirk token"
  key_links:
    - from: "docs/UAT-SERIES.md"
      to: "Obsidian vault UAT-Series.md"
      via: "sync per CLAUDE.md mandatory step 3"
      pattern: "quirk token"
---

<objective>
Complete the CLAUDE.md mandatory phase-completion documentation steps for Phase 102: document the token CLI + X-API-Key auth + login flow, update and sync docs/UAT-SERIES.md, and create the Obsidian phase note.

Purpose: Operators and the project knowledge base must reflect the new auth surface; CLAUDE.md requires these steps at the end of every phase.
Output: Updated docs/configuration.md + docs/UAT-SERIES.md; synced Obsidian phase note + UAT-Series note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/102-dashboard-auth-ux-score-tax/102-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document token CLI + X-API-Key auth + login flow</name>
  <files>docs/configuration.md</files>
  <read_first>
    - docs/configuration.md (existing structure + security/dashboard section if present)
    - .planning/phases/102-dashboard-auth-ux-score-tax/102-01-SUMMARY.md, 102-02-SUMMARY.md, 102-04-SUMMARY.md (what shipped)
  </read_first>
  <action>
    Add a dashboard-auth subsection to docs/configuration.md documenting: (1) `quirk token generate` / `quirk token rotate` / `quirk token show` (what each does, the --config flag, that rotate invalidates the old token immediately, that QUIRK_API_TOKEN env var takes precedence over the YAML security.api_token); (2) the dashboard accepts both `X-API-Key: <token>` and `Authorization: Bearer <token>` (X-API-Key precedence), timing-safe; (3) the browser login flow — open the dashboard, enter the token in the login form, Sign out to clear it; (4) auth-disabled passthrough (empty token = no auth, dev convenience). Keep diffs minimal; match existing doc tone. Do NOT document deferred items (multi-tenant, OAuth, token TTL).
  </action>
  <verify>
    <automated>grep -qi "quirk token" docs/configuration.md && grep -qi "X-API-Key" docs/configuration.md && echo DOCS-OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/configuration.md describes generate/rotate/show, X-API-Key precedence, login flow, and auth-disabled passthrough
    - No deferred features documented as available
  </acceptance_criteria>
  <done>configuration.md covers the Phase 102 auth surface</done>
</task>

<task type="auto">
  <name>Task 2: Update + sync docs/UAT-SERIES.md and create Obsidian phase note</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (header, Last Updated, relevant series structure)
    - CLAUDE.md (Mandatory Phase Completion Steps 1-4 — exact vault paths + sync commands)
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/ (existing phase-note template pattern)
    - all four 102-0N-SUMMARY.md files
  </read_first>
  <action>
    Update docs/UAT-SERIES.md per CLAUDE.md step 2: add/extend the relevant series with test cases for the token CLI (generate/rotate/show), X-API-Key auth on a protected route, the browser login flow (correct token loads dashboard, wrong token shows inline error, Sign out returns to login, auth-disabled passthrough), and TRANS-04 CLI/HTML/PDF score parity; bump the `**Last Updated:**` date to today. Then sync to Obsidian per CLAUDE.md step 3 by writing the framed file to /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md (printf the frontmatter, append docs/UAT-SERIES.md, cp to vault — file too large for CLI content=). Then per CLAUDE.md step 1 write the Phase 102 Obsidian note directly to /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-102-Dashboard-Auth-UX-Score-Tax.md (filesystem write, NOT obsidian content=): frontmatter (status: complete, type: phase, source, updated) + Goal + Requirements Covered (AUTH-01/02/03, TRANS-04) + Success Criteria (the 4 from ROADMAP) + What Was Built (one subsection per plan sourced from the SUMMARY files) + `[[Roadmap]]` link, following the existing Phase note template.
  </action>
  <verify>
    <automated>grep -qi "quirk token" docs/UAT-SERIES.md && test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-102-Dashboard-Auth-UX-Score-Tax.md" && echo SYNC-OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/UAT-SERIES.md has new/updated test cases for token CLI + login flow + score parity and a bumped Last Updated date
    - Obsidian UAT-Series.md and Phase-102 note exist in the vault with correct frontmatter
  </acceptance_criteria>
  <done>UAT-SERIES.md updated + synced; Phase 102 Obsidian note created</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → operator behavior | docs must not instruct operators to expose tokens insecurely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-17 | Information Disclosure | docs encouraging tokens in URLs/shell history | mitigate | Documentation states tokens are sent via header only and notes `quirk token show` echoes to the terminal (local-tool tradeoff); no guidance to put tokens in query strings or commit config.yaml. |
| T-102-SC | Tampering | npm/pip installs | mitigate | Docs-only plan; no package installs. No slopcheck checkpoint required. |
</threat_model>

<verification>
- `grep -i "quirk token" docs/configuration.md docs/UAT-SERIES.md` matches
- Obsidian Phase-102 note + UAT-Series.md present in the vault
</verification>

<success_criteria>
CLAUDE.md mandatory completion steps satisfied: auth surface documented, UAT-SERIES.md updated + synced, Phase 102 Obsidian note created.
</success_criteria>

<output>
Create `.planning/phases/102-dashboard-auth-ux-score-tax/102-05-SUMMARY.md` when done
</output>
