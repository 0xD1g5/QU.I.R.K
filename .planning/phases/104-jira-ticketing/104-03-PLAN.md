---
phase: 104-jira-ticketing
plan: 03
type: execute
wave: 3
depends_on: ["104-01", "104-02"]
files_modified:
  - quirk/cli/ticket_cmd.py
  - run_scan.py
  - pyproject.toml
  - quirk/util/optional_extra.py
  - tests/test_ticket_cmd.py
  - tests/test_install_all_includes_tickets.py
autonomous: true
requirements: [TICKET-01, TICKET-03]
must_haves:
  truths:
    - "`quirk ticket create` loads the latest findings-*.json (or --input PATH) and dispatches one issue per finding through JiraChannel"
    - "When [tickets] is not installed, `quirk ticket create` prints a pip-install advisory and exits 2 — never an ImportError traceback"
    - "`pip install quirk[all]` resolves jira into the install set (CI guard)"
    - "`quirk ticket` is intercepted in run_scan.py before scan argparse, mirroring the export block, using `return`"
  artifacts:
    - path: "quirk/cli/ticket_cmd.py"
      provides: "run_ticket(argv) + _find_latest_findings; argparse create subcommand; missing-extra/missing-config/missing-file exits; per-finding dispatch loop"
      contains: "def run_ticket"
    - path: "quirk/util/optional_extra.py"
      provides: "tickets entry in REGISTRY (modules=('jira',), enabled_attrs=())"
      contains: 'extra="tickets"'
    - path: "pyproject.toml"
      provides: "[tickets] = jira>=3.10.5 extra joined into [all]"
      contains: "jira>=3.10.5"
    - path: "tests/test_install_all_includes_tickets.py"
      provides: "slow CI guard: quirk[all] dry-run resolves jira"
      contains: "test_install_all_includes_tickets"
  key_links:
    - from: "run_scan.py"
      to: "quirk.cli.ticket_cmd.run_ticket"
      via: 'argv[1] == "ticket" interception using return (not _sys.exit)'
      pattern: 'argv\\[1\\] == "ticket"'
    - from: "quirk/cli/ticket_cmd.py::run_ticket"
      to: "quirk.ticketing.jira.JiraChannel.dispatch_finding"
      via: "loop over findings inside a get_session context"
      pattern: "dispatch_finding"
---

<objective>
Wire `quirk ticket create` into the CLI: the command (`quirk/cli/ticket_cmd.py`), the run_scan.py
interception, the `[tickets]` extra in pyproject.toml joined into `[all]` with a CI guard, and the
`tickets` REGISTRY entry in optional_extra.py for the graceful-skip advisory.

Purpose: Make the Jira backend usable end-to-end from the CLI and lock the optional-extra discipline.
Output: ticket_cmd.py + run_scan.py wiring + pyproject extra + optional_extra registry + 2 test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/104-jira-ticketing/104-RESEARCH.md
@.planning/phases/104-jira-ticketing/104-PATTERNS.md
@.planning/phases/104-jira-ticketing/104-02-SUMMARY.md

<interfaces>
<!-- run_scan.py export interception (lines 497-500) — copy this shape; use `return` -->
    if len(_sys.argv) > 1 and _sys.argv[1] == "export":
        from quirk.cli.export_cmd import run_export
        run_export(_sys.argv[2:])
        return

<!-- JiraChannel (Plan 02) + dispatch contract (Plan 01) -->
from quirk.ticketing.jira import JiraChannel          # lazy jira import inside __init__
from quirk.ticketing.config import load_ticketing_config   # -> TicketingCfg | None (.jira)
channel.dispatch_finding(finding, db, scan_id=...)         # never raises

<!-- optional_extra REGISTRY entry shape (quirk/util/optional_extra.py) -->
OptionalExtra(extra=..., modules=(...), scanner_label=..., install_hint=..., enabled_attrs=(...))
from quirk.util.optional_extra import is_extra_available
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pyproject [tickets] extra + optional_extra REGISTRY entry</name>
  <files>pyproject.toml, quirk/util/optional_extra.py</files>
  <read_first>
    - 104-PATTERNS.md "pyproject.toml" section (lines 546-564) — add tickets block after notify; join [all] after quirk-scanner[notify]; PRESERVE the [identity] INTENTIONAL EXCLUSION comment unchanged
    - 104-PATTERNS.md "quirk/util/optional_extra.py" section (lines 568-587) — the tickets REGISTRY entry (enabled_attrs=() always-probe, mirrors dashboard/cbom)
    - pyproject.toml lines ~87-109 to confirm exact insertion points
    - quirk/util/optional_extra.py existing REGISTRY entries + OptionalExtra field names
  </read_first>
  <action>
    In pyproject.toml `[project.optional-dependencies]`, add a `tickets = ["jira>=3.10.5"]` entry
    (comment: Phase 104 TICKET-01) immediately after the notify block. In the `[all]` list, add
    `"quirk-scanner[tickets]",` after the `quirk-scanner[notify]` line. Leave the `[identity]`
    INTENTIONAL EXCLUSION comment untouched. In quirk/util/optional_extra.py, append an OptionalExtra
    to REGISTRY: extra="tickets", modules=("jira",), scanner_label="jira_ticketing", install_hint a
    `pip install quirk[tickets]` advisory string, enabled_attrs=() (always-probe like dashboard/cbom).
    Match the existing OptionalExtra field names exactly. Minimal diff.
  </action>
  <acceptance_criteria>
    - source: `pyproject.toml` contains `jira>=3.10.5` under a `tickets` extra and `quirk-scanner[tickets]` inside `[all]`
    - source: `quirk/util/optional_extra.py` REGISTRY contains an entry with `extra="tickets"` and `modules=("jira",)`
    - behavior: `python -c "from quirk.util.optional_extra import is_extra_available; print(is_extra_available('tickets'))"` runs without error (returns True/False based on install)
    - source: the `[identity]` intentional-exclusion comment is still present in pyproject.toml
  </acceptance_criteria>
  <verify>
    <automated>python -c "import tomllib,io; d=tomllib.load(open('pyproject.toml','rb')); opt=d['project']['optional-dependencies']; assert any('jira' in x for x in opt['tickets']); assert any('tickets' in x for x in opt['all']); print('ok')" && python -c "from quirk.util.optional_extra import is_extra_available; is_extra_available('tickets'); print('ok')"</automated>
  </verify>
  <done>[tickets] extra defined, joined into [all]; tickets in REGISTRY; identity exclusion preserved.</done>
</task>

<task type="auto">
  <name>Task 2: ticket_cmd.py + run_scan.py interception</name>
  <files>quirk/cli/ticket_cmd.py, run_scan.py</files>
  <read_first>
    - 104-PATTERNS.md "quirk/cli/ticket_cmd.py" section (lines 407-515) — docstring/exit codes, _find_latest_findings, run_ticket flow (extra check → findings resolve → config load → get_session loop) — copy this shape exactly
    - 104-PATTERNS.md "run_scan.py (modify)" section (lines 519-543) — the ticket interception block placed after the export block, using `return`
    - quirk/cli/export_cmd.py (full) — the exact mirror for run_ticket
    - run_scan.py lines 497-506 (export + errors interception) for placement
  </read_first>
  <action>
    Create `quirk/cli/ticket_cmd.py` mirroring export_cmd.py. Include `_find_latest_findings(output_dir)`
    (newest findings-*.json glob) and `run_ticket(argv)`: argparse prog "quirk ticket" with positional
    `action` choices=["create"], `--input PATH`, `--output-dir DIR` (default "output"). Flow:
    (1) `if not is_extra_available("tickets")`: print pip-install advisory to stderr, `sys.exit(2)`;
    (2) resolve findings_path from --input or _find_latest_findings; if missing → stderr error +
    exit(2); (3) read+json.load the file; non-list or read error → stderr (safe_str) + exit(2);
    (4) `cfg = load_ticketing_config()`; if cfg is None or cfg.jira is None → stderr error
    ("set QUIRK_CONFIG_PATH") + exit(2); (5) in a try: import get_session + os + JiraChannel,
    db_path = QUIRK_DB_PATH or "quirk.db", scan_id = findings filename, channel = JiraChannel(cfg.jira),
    `with get_session(db_path) as db:` loop `channel.dispatch_finding(finding, db, scan_id=scan_id)`;
    print completion summary; re-raise SystemExit; other Exception → stderr (safe_str) + exit(2).
    Document exit codes 0/2 in the docstring. In run_scan.py, add the ticket interception block
    immediately AFTER the export block (after line 500), mirroring it exactly but using `run_ticket`
    and `argv[1] == "ticket"`, ending with `return` (NOT _sys.exit). Minimal diff; PEP 8.
  </action>
  <acceptance_criteria>
    - source: `quirk/cli/ticket_cmd.py` defines `run_ticket(argv)` and `_find_latest_findings`; all
      stderr error paths use `safe_str` where an exception is involved; exits use code 2 on failure
    - source: `run_scan.py` contains `_sys.argv[1] == "ticket"` interception calling `run_ticket` and
      ending with `return`, placed after the export block
    - behavior: invoking with [tickets] absent prints `pip install quirk[tickets]` and exits 2 (no traceback)
    - test-command: `python -c "from quirk.cli.ticket_cmd import run_ticket; print('ok')"` prints ok
  </acceptance_criteria>
  <verify>
    <automated>python -m compileall quirk/cli/ticket_cmd.py run_scan.py -q && grep -q 'argv\[1\] == "ticket"' run_scan.py && python -c "from quirk.cli.ticket_cmd import run_ticket; print('ok')"</automated>
  </verify>
  <done>ticket_cmd.run_ticket implemented mirroring export_cmd; run_scan.py intercepts `ticket` with return; graceful missing-extra exit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CLI tests + [all] CI guard</name>
  <files>tests/test_ticket_cmd.py, tests/test_install_all_includes_tickets.py</files>
  <read_first>
    - 104-PATTERNS.md "tests/test_ticket_cmd.py" section (lines 715-737) — 5 CLI test cases + capsys/SystemExit pattern
    - 104-PATTERNS.md "tests/test_install_all_includes_tickets.py" section (lines 591-646) — exact mirror of test_install_all_includes_notify.py with @pytest.mark.slow
    - tests/test_install_all_includes_notify.py (full) — copy structure; swap notify→tickets, slack→jira
  </read_first>
  <action>
    Create `tests/test_ticket_cmd.py`: `test_missing_extra_advisory` (patch
    `quirk.cli.ticket_cmd.is_extra_available`→False; run_ticket(["create"]) raises SystemExit code 2;
    capsys.err contains "pip install quirk[tickets]"), `test_no_findings_file` (empty tmp output dir →
    exit 2), `test_input_flag` (--input points to a tmp findings json; mock JiraChannel.dispatch_finding
    and load_ticketing_config to confirm the specified file is read), `test_missing_config`
    (load_ticketing_config→None → exit 2), `test_exit_0_all_dispatched` (mock is_extra_available→True,
    load_ticketing_config→cfg with .jira, JiraChannel + get_session patched; happy path exits 0). Use
    capsys + pytest.raises(SystemExit). Create `tests/test_install_all_includes_tickets.py` mirroring
    test_install_all_includes_notify.py exactly: @pytest.mark.slow, pip install --dry-run --report on
    `-e <REPO_ROOT>[all]`, assert returncode 0, assert "does not provide the extra 'tickets'" absent,
    assert "jira" in the resolved install set. Minimal; PEP 8.
  </action>
  <acceptance_criteria>
    - test-command: `python -m pytest tests/test_ticket_cmd.py -x -q` passes with ≥5 tests
    - behavior: `test_missing_extra_advisory` asserts exit code 2 + advisory in stderr
    - test-command: `python -m pytest tests/test_install_all_includes_tickets.py -m slow -q` passes
      (asserts jira in quirk[all] resolved set)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_ticket_cmd.py -x -q && python -m pytest tests/test_install_all_includes_tickets.py -m slow -q</automated>
  </verify>
  <done>CLI tests green (exit codes, advisory, --input, missing-config, happy path); slow CI guard proves jira in quirk[all].</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → ticket_cmd | user-supplied --input path and output dir |
| minimal install → ticket_cmd | [tickets] may be absent at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-09 | Availability | ticket_cmd missing-extra path | mitigate | is_extra_available("tickets") advisory + exit 2 before any jira import; test_missing_extra_advisory asserts no ImportError (ISEC-04) |
| T-104-10 | Info Disclosure | ticket_cmd error output | mitigate | all exception-bearing stderr uses safe_str(exc); never raw str(exc) |
| T-104-SC | Tampering | jira install via [tickets]/[all] | mitigate | jira slopcheck [OK], pycontribs/jira, PyPI-verified 3.10.5 (RESEARCH Package Legitimacy Audit — no [ASSUMED]/[SUS]); CI guard test asserts clean resolution. Verify `pip install "jira>=3.10.5"` resolves no-conflict against public PyPI at execute. |
</threat_model>

<verification>
- `python -m compileall quirk/cli/ticket_cmd.py run_scan.py -q` clean
- `python -m pytest tests/test_ticket_cmd.py -x -q` green
- `python -m pytest tests/test_install_all_includes_tickets.py -m slow -q` green
- `quirk ticket create` with [tickets] absent → advisory + exit 2 (no traceback)
</verification>

<success_criteria>
`quirk ticket create` dispatches one issue per finding through JiraChannel, degrades gracefully on
missing [tickets], and [all] pulls jira (CI-guarded).
</success_criteria>

<output>
Create `.planning/phases/104-jira-ticketing/104-03-SUMMARY.md` when done.
</output>
