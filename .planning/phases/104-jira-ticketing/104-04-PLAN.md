---
phase: 104-jira-ticketing
plan: 04
type: execute
wave: 4
depends_on: ["104-01", "104-02", "104-03"]
files_modified:
  - docs/configuration.md
  - docs/sample-config.yaml
  - docs/UAT-SERIES.md
autonomous: true
requirements: [TICKET-01, TICKET-03, TICKET-04]
must_haves:
  truths:
    - "Documentation describes the [ticketing] config block, env-var-NAME credential model, cloud/server auth modes, and `quirk ticket create` usage"
    - "docs/UAT-SERIES.md has a Jira ticketing test case (create + dedup + graceful-skip) and is synced to Obsidian and committed"
    - "An Obsidian phase note exists for Phase 104 at the canonical vault path"
  artifacts:
    - path: "docs/configuration.md"
      provides: "[ticketing] config block reference + quirk ticket create usage + credential-isolation note"
      contains: "ticketing"
    - path: "docs/sample-config.yaml"
      provides: "ticketing.jira sample block (env-var NAMES only)"
      contains: "jira_token_env"
    - path: "docs/UAT-SERIES.md"
      provides: "Jira ticketing UAT case + updated Last Updated date"
      contains: "ticket create"
  key_links:
    - from: "docs/configuration.md"
      to: "quirk/ticketing/config.py JiraTicketingCfg"
      via: "documented field names match the dataclass exactly"
      pattern: "jira_url"
---

<objective>
Document the Jira ticketing feature and complete the mandatory CLAUDE.md phase-completion steps:
configuration docs, sample config, UAT-SERIES.md update + Obsidian sync + commit, and the Phase 104
Obsidian phase note.

Purpose: Ship discoverable docs and satisfy the recurring-miss phase-completion checklist (esp. the
UAT-SERIES commit, step 4).
Output: updated docs/configuration.md, docs/sample-config.yaml, docs/UAT-SERIES.md (+ Obsidian sync +
commit) and a Phase-104 Obsidian phase note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@./CLAUDE.md
@.planning/phases/104-jira-ticketing/104-01-SUMMARY.md
@.planning/phases/104-jira-ticketing/104-02-SUMMARY.md
@.planning/phases/104-jira-ticketing/104-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configuration docs + sample config</name>
  <files>docs/configuration.md, docs/sample-config.yaml</files>
  <read_first>
    - docs/configuration.md existing [siem] and [notifications] sections — mirror the structure/depth for [ticketing]
    - docs/sample-config.yaml existing siem/notifications blocks
    - quirk/ticketing/config.py (Plan 01) — JiraTicketingCfg field names must match docs exactly
    - 104-RESEARCH.md "Sample YAML config block" + Open Questions 1/4 (auth_mode default cloud; allow_internal for self-hosted)
  </read_first>
  <action>
    Add a `[ticketing]` / `ticketing:` section to docs/configuration.md documenting: the `ticketing.jira`
    block fields (jira_url, jira_user_env, jira_token_env, project_key, issue_type default "Bug",
    auth_mode "cloud"|"server", allow_internal default false); the credential model (config holds env
    var NAMES only — never values; QUIRK reads the actual creds from the environment at run time and
    never writes them to SQLite or logs); cloud vs server auth (cloud = email + API token basic_auth;
    server = PAT token_auth); SSRF note (jira_url validated; self-hosted internal URLs require
    allow_internal: true); and `quirk ticket create [--input PATH] [--output-dir DIR]` usage with the
    dedup behavior (re-runs add a rediscovery comment, never duplicate issues) and the `[tickets]` extra
    install requirement (`pip install quirk[tickets]`). Add the matching `ticketing.jira` block to
    docs/sample-config.yaml using env-var NAMES (QUIRK_JIRA_USER, QUIRK_JIRA_TOKEN). Keep field names
    identical to JiraTicketingCfg. Minimal, accurate prose.
  </action>
  <acceptance_criteria>
    - source: docs/configuration.md documents all JiraTicketingCfg fields with names matching the dataclass
    - source: docs/configuration.md states credentials are env-var NAMES only and never persisted/logged
    - source: docs/sample-config.yaml contains a `ticketing:` → `jira:` block with jira_user_env/jira_token_env
    - test-command: `grep -q "quirk ticket create" docs/configuration.md && grep -q "jira_token_env" docs/sample-config.yaml`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "quirk ticket create" docs/configuration.md && grep -q "jira_token_env" docs/sample-config.yaml && grep -q "allow_internal" docs/configuration.md && echo ok</automated>
  </verify>
  <done>Configuration docs + sample config cover [ticketing], auth modes, credential isolation, SSRF, and CLI usage.</done>
</task>

<task type="auto">
  <name>Task 2: UAT-SERIES update + Obsidian sync + commit + Phase note</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md — header Last Updated line + the SIEM/export series (freshest integration analog) for the case format
    - CLAUDE.md "Mandatory Phase Completion Steps" (steps 1-4) — exact Obsidian + UAT sync + commit commands
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-99-Per-Finding-Context-Code-Signing-Expiry.md — phase-note template pattern
  </read_first>
  <action>
    (Step 2) Add a Jira ticketing UAT case to docs/UAT-SERIES.md in the relevant integration series:
    configure [ticketing], run `quirk ticket create` against a completed scan → one Jira issue per
    finding with QRAMM evidence in the description; re-run → rediscovery comment added, zero duplicate
    issues; with [tickets] uninstalled → advisory + graceful skip, no traceback. Note it requires a
    live Jira instance + API token (human-UAT). Update the `**Last Updated:**` date at the top to today.
    (Step 3) Sync UAT-SERIES.md to Obsidian: write frontmatter + content to
    `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` per the CLAUDE.md printf+cat recipe
    (file too large for CLI content=).
    (Step 4 — DO NOT SKIP, recurring miss) Commit docs/UAT-SERIES.md via
    `node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-104): update UAT-SERIES.md" --files docs/UAT-SERIES.md`.
    (Step 1) Write the Phase 104 Obsidian note directly to the vault filesystem at
    `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-104-Jira-Ticketing.md`: frontmatter
    (status: complete, type: phase, source, updated) + Goal + Requirements Covered (TICKET-01/03/04) +
    Success Criteria (from ROADMAP) + What Was Built (one subsection per plan, sourced from the three
    SUMMARY.md files) + `[[Roadmap]]` link. Do NOT use `obsidian content=` for large files; write to
    the filesystem.
  </action>
  <acceptance_criteria>
    - source: docs/UAT-SERIES.md contains a `quirk ticket create` case (create + dedup + graceful-skip) and an updated Last Updated date
    - behavior: `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` exists with QUIRK frontmatter and the updated content
    - behavior: docs/UAT-SERIES.md is committed via gsd-tools (step 4)
    - behavior: `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-104-Jira-Ticketing.md` exists with status: complete, requirements, and per-plan What Was Built
    - test-command: `grep -q "ticket create" docs/UAT-SERIES.md && test -f /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-104-Jira-Ticketing.md`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "ticket create" docs/UAT-SERIES.md && test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-104-Jira-Ticketing.md" && test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" && echo ok</automated>
  </verify>
  <done>UAT-SERIES.md has a Jira ticketing case + updated date, synced to Obsidian and committed (step 4); Phase 104 Obsidian phase note written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → operators | sample config must not encourage storing raw credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-11 | Info Disclosure | sample-config.yaml | mitigate | sample uses env-var NAMES (QUIRK_JIRA_USER/QUIRK_JIRA_TOKEN) only; docs explicitly state credentials are never persisted/logged |
</threat_model>

<verification>
- `grep -q "quirk ticket create" docs/configuration.md`
- `grep -q "ticket create" docs/UAT-SERIES.md`
- Phase 104 Obsidian note + UAT-Series.md exist in the vault
- docs/UAT-SERIES.md committed via gsd-tools
</verification>

<success_criteria>
Ticketing is documented (config + CLI + credential model), the UAT series has a Jira case synced and
committed, and the Phase 104 Obsidian note exists — all mandatory CLAUDE.md completion steps satisfied.
</success_criteria>

<output>
Create `.planning/phases/104-jira-ticketing/104-04-SUMMARY.md` when done.
</output>
