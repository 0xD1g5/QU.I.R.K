# Phase 64.1: Audit Residual Blockers - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-10
**Phase:** 64.1-audit-residual-blockers
**Areas discussed:** Session window fix (CR-05), Disposition framework (14 BLOCKERs), CR-03 fix scope, Plan structure

---

## Session Window Fix (CR-05)

| Option | Description | Selected |
|--------|-------------|----------|
| Sub-second strftime | Change `"%Y-%m-%d %H:%M:%S"` → `"%Y-%m-%d %H:%M:%f"` in 3 functions; no schema change; millisecond precision makes collision vanishingly unlikely | ✓ |
| scan_id UUID column | Add `scan_id TEXT` to `CryptoEndpoint`, set at scan start, group by UUID; cleanest semantically but requires schema migration and Phase 65 wiring | |

**User's choice:** Sub-second strftime
**Notes:** Preferred because it avoids a schema migration. The UUID approach was noted as a deferred improvement for Phase 65 when scan-dispatch infrastructure is being built anyway.

---

## Disposition Framework (14 BLOCKERs)

| Option | Description | Selected |
|--------|-------------|----------|
| One-liner + phase/tag | `deferred-v4.9 — not in Phase 65 critical path; revisit before Phase 67` — short, scannable | |
| Structured block | Rationale + safe-to-defer reasoning + fix phase + risk level per entry — thorough audit trail | ✓ |

**User's choice:** Structured block
**Notes:** Fields: Rationale (what the bug is / when it triggers), Safe to defer (code path + test coverage + user-visible risk), Fix phase (which future phase would address it), Risk level (low/medium/high + one-line impact).

---

## CR-03 Fix Scope (SOURCE Algo Hint)

| Option | Description | Selected |
|--------|-------------|----------|
| Add specifics + generic fallback | Fix `des→DES`, add `aes-256` and `aes-128` entries before catch-all, change `aes→AES`; ordered-list invariant preserved | ✓ |
| DES fix only (minimal) | Fix only the `des→3DES` mapping; leave AES for a future change; but success criteria explicitly requires AES variant specificity | |

**User's choice:** Add specifics + generic fallback
**Notes:** The ordered `algo_hints` list already documents the "more-specific before substrings" invariant; adding aes-256/aes-128 before aes follows this existing pattern naturally. Catch-all becomes `"AES"` (generic) rather than `"AES-256-GCM"` (unjustified assumption).

---

## Plan Structure

| Option | Description | Selected |
|--------|-------------|----------|
| 2 plans: fixes / dispositions | Plan 1: 5 bug fixes + tests. Plan 2: 14 structured disposition blocks in AUDIT-TASKS.md | ✓ |
| 3 plans: grouped by concern | Plan 1: data-integrity (BL-03, BL-04, CR-08). Plan 2: CBOM+trend (CR-03, CR-05). Plan 3: dispositions | |
| 1 plan: everything atomic | All 5 fixes + 14 dispositions in one plan; fast but harder to review | |

**User's choice:** 2 plans
**Notes:** Separating fixes from dispositions keeps each plan's verification clear — Plan 1 has concrete passing/failing tests, Plan 2 is a structured documentation pass.

---

## Claude's Discretion

None — all four areas had clear user selections.

## Deferred Ideas

- **scan_id UUID column** — better long-term session disambiguation; deferred to Phase 65 scan-dispatch work
- **WARNING/INFO audit items** — all WR-* and IN-* rows left for v4.9 triage
