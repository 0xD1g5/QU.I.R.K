---
plan_id: 85-01
phase: 85-public-launch-polish
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
objective: "Ship `quirk db migrate` CLI subcommand (idempotent, additive-only) and the v4.x → v4.10 upgrade guide that documents its use."
files_modified:
  - run_scan.py
  - quirk/db.py
  - tests/test_db_migrate_cli.py
  - docs/upgrade-guide.md
task_count: 3
requirements_closed: [LAUNCH-04]
tags: [cli, db, migration, upgrade-guide, launch]
requirements: [LAUNCH-04]

must_haves:
  truths:
    - "Running `quirk db migrate --db <path>` against any v4.x quirk.db exits 0 and prints a column-by-column report of `already-present` vs `added`."
    - "Running `quirk db migrate --dry-run` against the same DB prints the same report without writing, exits 0."
    - "Running `quirk db migrate` twice in a row produces identical `already-present` output on the second run (idempotence)."
    - "`docs/upgrade-guide.md` exists and walks a v4.x user through pip upgrade → dry-run → migrate → verify → rollback."
  artifacts:
    - path: "quirk/db.py"
      provides: "`run_additive_migration(engine, dry_run: bool) -> list[ColumnMigrationResult]` helper exposing the existing `_ensure_columns` machinery as a public migration entrypoint with per-column reporting."
    - path: "run_scan.py"
      provides: "`db` subcommand with `migrate` action, mirroring the existing `compliance` / `qramm` subcommand-interception pattern; supports `--db PATH`, `--dry-run`, exits 0 on success."
    - path: "tests/test_db_migrate_cli.py"
      provides: "Pytest coverage: fresh DB → all `added`; second run → all `already-present`; --dry-run does not write; exits 0 in both states."
    - path: "docs/upgrade-guide.md"
      provides: "Single-document upgrade guide (pre-upgrade checklist → `pip install -U qu-i-r-k[all]` → `quirk db migrate --dry-run` → `quirk db migrate` → verify → rollback note)."
  key_links:
    - from: "run_scan.py db subcommand"
      to: "quirk.db.run_additive_migration"
      via: "direct import + invocation in subcommand handler"
      pattern: "from quirk.db import run_additive_migration"
    - from: "docs/upgrade-guide.md"
      to: "quirk db migrate"
      via: "fenced bash block executed verbatim by upgrading users"
      pattern: "quirk db migrate"

---

<objective>
Deliver a net-new `quirk db migrate` CLI command and the upgrade guide that drives it, closing LAUNCH-04. The migrate command is idempotent, additive-only, and exits 0 whether the DB is fresh, partially-migrated, or already current.

Purpose: A returning user on v4.x must be able to `pip install -U` and run a single command against their existing `quirk.db` to bring its schema current — with no destructive operations and no manual SQL.

Output: A new `db` argparse subcommand wired into `run_scan.main()`, a public `run_additive_migration` helper in `quirk/db.py`, pytest coverage in `tests/test_db_migrate_cli.py`, and `docs/upgrade-guide.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-public-launch-polish/85-CONTEXT.md
@.planning/REQUIREMENTS.md

# Existing migration machinery to surface, not reinvent:
@quirk/db.py

# CLI subcommand-interception pattern to copy from (the `compliance` block):
@run_scan.py

# Phase 84 release-process backdrop (mentioned in the upgrade guide):
@docs/release-process.md
</context>

<interfaces>
From `quirk/db.py` (already exists — read it first; the migrate command wraps these):
- `init_db(db_path: str) -> Engine` — current entry point; internally calls `_ensure_columns`, `_ensure_qramm_tables`, `_ensure_scheduled_tables`, `_ensure_scan_jobs_table`, `_ensure_scan_checkpoints_table`, `_ensure_qramm_profiles_fk`.
- `_ensure_columns(engine, table: str, columns: list[tuple[str, str]]) -> None` — the additive-only column installer. Uses `_SAFE_COL_RE` and `_SAFE_COL_TYPE_RE` allowlists; SQL-injection-safe.
- The migrate command must NOT bypass these allowlists; it MUST reuse them.

From `run_scan.py` (subcommand pattern — copy the `compliance` / `qramm` shape):
- `main()` intercepts subcommands BEFORE the main `scan` argparse. Pattern: check `sys.argv[1] == "<name>"`, build a dedicated argparse, dispatch, return/exit.
- The `db` subcommand must follow the same intercept pattern — placed alongside the existing `compliance` / `qramm` / `doctor` / `schedule` blocks.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `run_additive_migration` helper + tests</name>
  <files>quirk/db.py, tests/test_db_migrate_cli.py</files>
  <behavior>
    - Fresh empty DB → helper reports every additive column from existing `_ensure_columns` calls as `added`, returns a list[ColumnMigrationResult].
    - Same DB on second invocation → every column reports `already-present`; no writes occur.
    - `dry_run=True` → returns the same diagnostic list a real run would, but issues zero ALTER TABLE statements (verified via SQLAlchemy event listener or by snapshotting `PRAGMA table_info` before and after).
    - Returned objects expose at minimum `table: str`, `column: str`, `status: Literal["added","already-present"]`.
    - Helper never drops, renames, or retypes columns. Never raises on `already-present`.
  </behavior>
  <action>
    Extract the additive-column logic from the existing `init_db` flow into a public `run_additive_migration(engine, *, dry_run: bool = False)` function in `quirk/db.py`. Iterate every table+column pair already governed by `_ensure_columns`/`_ensure_*_tables` callsites, classify each as `added` vs `already-present` via `sqlalchemy.inspect(engine).get_columns(table)` PRIOR to the call, then either skip (dry_run) or invoke the existing allowlist-guarded installer. Leave `init_db` working unchanged — `init_db` can call `run_additive_migration` internally to avoid duplication, or both can share a private `_iter_additive_columns()` enumerator. Return `list[ColumnMigrationResult]` (use a `@dataclass(frozen=True)` or `NamedTuple`). Per D-LAUNCH-04 (CONTEXT decision): additive-only — the helper must never drop, rename, or retype columns. Write `tests/test_db_migrate_cli.py` first with the four behaviors above using a tempfile sqlite DB; then implement until green.
  </action>
  <verify>
    <automated>pytest tests/test_db_migrate_cli.py -x -q</automated>
  </verify>
  <done>All four tests pass; `python -m compileall quirk/db.py` exits 0; `_ensure_columns`/`_SAFE_COL_RE` allowlists are still the only path that issues ALTER TABLE.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire `quirk db migrate` subcommand in run_scan.py</name>
  <files>run_scan.py, tests/test_db_migrate_cli.py</files>
  <behavior>
    - `quirk db migrate --db /tmp/x.db` exits 0 on a fresh DB, prints one line per column with status (`added` / `already-present`), prints a summary footer.
    - `quirk db migrate --db /tmp/x.db --dry-run` does the same listing but ends with `(dry-run; no changes written)` and provably does not modify the DB file mtime/size.
    - `quirk db migrate --help` is informative (lists `--db`, `--dry-run`, brief description).
    - Re-running without `--dry-run` produces all-`already-present` output (idempotence at the CLI level).
  </behavior>
  <action>
    Add a `db` subcommand intercept in `run_scan.main()` — mirror the structural shape of the existing `compliance` block (search for `# --- compliance subcommand:`) — placed alongside it. Build an `argparse.ArgumentParser(prog="quirk db")` with one action subparser `migrate`. The `migrate` parser accepts `--db PATH` (default: resolve via existing config-loading path used by `init_db`/`get_session` callers — read the config to find `cfg.output.db_path`, with `--db` overriding) and `--dry-run` (store_true). Handler: call `get_engine(db_path)` then `run_additive_migration(engine, dry_run=args.dry_run)`, print results as `table.column: status` lines plus a summary count. Exit 0 always on success; non-zero only on genuine I/O / permission errors. Per Phase 84 anti-pattern: do NOT touch unrelated subcommand blocks. Extend `tests/test_db_migrate_cli.py` with subprocess-based tests that invoke `quirk db migrate ...` (via `python -m run_scan db migrate ...` or by importing and calling `main()` with patched `sys.argv`) and assert exit code + stdout markers.
  </action>
  <verify>
    <automated>pytest tests/test_db_migrate_cli.py -x -q && python -m compileall run_scan.py</automated>
  </verify>
  <done>CLI tests pass; `python run_scan.py db migrate --help` exits 0 and shows the documented flags; double-run idempotence test passes.</done>
</task>

<task type="auto">
  <name>Task 3: Write docs/upgrade-guide.md</name>
  <files>docs/upgrade-guide.md</files>
  <action>
    Create `docs/upgrade-guide.md` covering v4.x → v4.10 per D-LAUNCH-04 (CONTEXT.md decision: single guide; additive-only schema; reasoning: schema changes are additive-only across v4.x per REQUIREMENTS.md). Required sections in order:
    1. **Scope** — one paragraph: applies to any v4.0+ install upgrading to v4.10; additive-only migrations; no destructive operations.
    2. **Pre-upgrade checklist** — back up `quirk.db` (copy file or `sqlite3 .backup`); note current `quirk --version`; confirm no scan jobs in progress.
    3. **Install the new version** — fenced bash: `pip install -U qu-i-r-k[all]` (use the canonical PyPI distribution name `qu-i-r-k` per Phase 84 D-02).
    4. **Dry-run the migration** — fenced bash: `quirk db migrate --dry-run`; explain that `already-present` lines are expected on partially-upgraded DBs.
    5. **Apply the migration** — fenced bash: `quirk db migrate`; expected output shape; exits 0.
    6. **Verify** — `quirk --version` shows v4.10; `quirk db migrate` re-run shows all `already-present`.
    7. **Rollback** — restore the pre-upgrade `quirk.db` backup; reinstall the previous version with `pip install qu-i-r-k==<prev>`; explicit note that there is no in-tool rollback (additive-only schema means rolling forward is preferred; backups are the rollback path).
    Cross-link `docs/release-process.md` for attestation verification of the upgrade artifact (relevant for security-conscious upgraders).
    Audience: existing v4.x users. Tone: direct, no marketing; assume shell familiarity. Length target: 80-180 lines.
  </action>
  <verify>
    <automated>test -f docs/upgrade-guide.md && grep -q "quirk db migrate" docs/upgrade-guide.md && grep -q "pip install -U qu-i-r-k" docs/upgrade-guide.md && grep -q -i "rollback" docs/upgrade-guide.md && grep -q -i "additive" docs/upgrade-guide.md && [ "$(wc -l < docs/upgrade-guide.md)" -ge 80 ]</automated>
  </verify>
  <done>Guide exists, all required sections present, references the canonical CLI invocations from Tasks 1-2, links to release-process.md, ≥ 80 lines.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_db_migrate_cli.py -x -q` green
- `python -m compileall quirk/db.py run_scan.py` exits 0
- `python run_scan.py db migrate --help` prints help text
- `docs/upgrade-guide.md` validates the grep gates in Task 3
- `grep -rn "_ensure_columns\|_SAFE_COL_RE" quirk/db.py` confirms allowlists still the only ALTER TABLE path
</verification>

<success_criteria>
ROADMAP Phase 85 success criterion #3 ("A returning user upgrading from any v4.x release can follow `docs/upgrade-guide.md` — `quirk db migrate` runs against their existing `quirk.db`, reports all schema columns already present (additive-only), and exits 0 with no data loss") is provable via the automated tests above plus a manual readthrough of `docs/upgrade-guide.md`.
</success_criteria>

<deviation_protocol>
If the existing `_ensure_columns` machinery cannot be cleanly factored into a public helper without behavioral risk (e.g., it has side effects beyond column installation like FK pragma reordering), DO NOT refactor `init_db`. Instead, write `run_additive_migration` as a parallel walker that enumerates the same `(table, column, type)` tuples (factored into a module-level constant) and invokes `_ensure_columns` directly. Record the divergence in `85-01-SUMMARY.md` under `## Deviations from Plan` and continue with Tasks 2-3 unblocked.
</deviation_protocol>

<output>
After completion, create `.planning/phases/85-public-launch-polish/85-01-SUMMARY.md` per the standard template.
</output>
