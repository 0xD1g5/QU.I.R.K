---
plan_id: 85-05
phase: 85-public-launch-polish
plan: 05
type: execute
wave: 3
depends_on: [85-01, 85-02, 85-03, 85-04]
autonomous: true
objective: "Marketing-grade README polish, getting-started 3-step quickstart, curl|bash non-decision in docs/release-process.md, Phase 85 closure (UAT-SERIES + Obsidian)."
files_modified:
  - README.md
  - docs/getting-started.md
  - docs/release-process.md
  - docs/images/dashboard-hero.png
  - docs/UAT-SERIES.md
task_count: 3
requirements_closed: [LAUNCH-01, LAUNCH-06, LAUNCH-07]
tags: [readme, marketing, getting-started, release-process, uat, obsidian, launch, closure]
requirements: [LAUNCH-01, LAUNCH-06, LAUNCH-07]

must_haves:
  truths:
    - "Root README.md augmented (NOT rewritten — per CONTEXT.md): badge row above title, persona triptych (security consultant / IT generalist / compliance officer), dashboard hero screenshot embed, asciinema demo link, 3-command quickstart (`pip install qu-i-r-k[all]` → `quirk init` → `quirk run --config quirk.yaml`)."
    - "docs/getting-started.md walks the same 3-step path (install → scan → view dashboard) end-to-end and matches the README quickstart verbatim on the install commands."
    - "docs/release-process.md has an explicit '`curl | bash` Non-Decision' section framed as a security posture statement (per CONTEXT.md <specifics>)."
    - "docs/images/dashboard-hero.png exists and is a real screenshot of the running dashboard against the chaos lab — not a placeholder."
    - "docs/UAT-SERIES.md has a new UAT-85 series block + bumped header date + synced to Obsidian vault per CLAUDE.md Mandatory Phase Completion Steps."
    - "Obsidian Phase-85 note exists at /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-85-Public-Launch-Polish.md with status: complete."
  artifacts:
    - path: "README.md"
      provides: "Augmented landing-page README — badges, persona triptych, hero image, asciinema link, 3-command quickstart at the top; existing content (doc table, etc.) preserved; `git clone` quickstart relocated under a 'Develop from source' collapsed section."
    - path: "docs/getting-started.md"
      provides: "3-step quickstart that any new user can follow on a clean macOS arm64 machine — install, scan a target, view dashboard."
    - path: "docs/release-process.md"
      provides: "`## curl | bash Non-Decision` section (already noted in Phase 84 SUMMARY but only as a one-liner; this plan expands into a full subsection per LAUNCH-07)."
    - path: "docs/images/dashboard-hero.png"
      provides: "Hero screenshot (PNG, ≥ 100 KB suggested, ≤ 2 MB), captured from dashboard running against the chaos lab `tls-cert-defects` (or whichever profile plan 85-04 used for the tls-only fixture)."
    - path: "docs/UAT-SERIES.md"
      provides: "New UAT-85 series block (UAT-85-01..N) + `**Last Updated:**` header bumped to today."
  key_links:
    - from: "README.md badge row"
      to: "shields.io + CI + PyPI + license + security policy"
      via: "markdown image links"
      pattern: "img.shields.io"
    - from: "README.md hero image"
      to: "docs/images/dashboard-hero.png"
      via: "markdown image embed"
      pattern: "!\\[.*\\]\\(docs/images/dashboard-hero.png\\)"
    - from: "README.md quickstart"
      to: "PyPI qu-i-r-k"
      via: "pip install qu-i-r-k[all]"
      pattern: "pip install qu-i-r-k"

---

<objective>
Close Phase 85 by landing the marketing/quickstart polish that prior plans (01-04) leave un-done: README augmentation, getting-started 3-step path, curl|bash non-decision write-up, dashboard hero screenshot, and the mandatory phase-closure deliverables from CLAUDE.md (UAT-SERIES update + Obsidian phase note).

Purpose: A first-time visitor to the GitHub repo sees a polished landing page, can copy the 3-command quickstart from the README or getting-started.md, and gets a working dashboard within ~10 minutes.

Output: README.md + docs/getting-started.md + docs/release-process.md (curl|bash section added) + docs/images/dashboard-hero.png + docs/UAT-SERIES.md + Obsidian phase note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/85-public-launch-polish/85-CONTEXT.md
@.planning/REQUIREMENTS.md

# Current README + getting-started (augment, do not rewrite):
@README.md
@docs/getting-started.md

# Release process (curl|bash section addition):
@docs/release-process.md

# Phase 84 SUMMARYs for the closure note:
@.planning/phases/84-release-engineering/84-04-SUMMARY.md

# Prior plans in this phase (for the Phase 85 Obsidian note "What Was Built" subsections):
@.planning/phases/85-public-launch-polish/85-01-PLAN.md
@.planning/phases/85-public-launch-polish/85-02-PLAN.md
@.planning/phases/85-public-launch-polish/85-03-PLAN.md
@.planning/phases/85-public-launch-polish/85-04-PLAN.md

# Phase-completion workflow:
@CLAUDE.md
</context>

<interfaces>
**README augmentation philosophy** (locked in CONTEXT.md): augment, do not rewrite. The existing README at `README.md` is 62 lines with good bones. This plan layers on:
- Badge row (above title)
- Persona triptych (3 short paragraphs)
- Hero screenshot embed
- Asciinema demo link (link only, no embed — keep clone size small)
- 3-command quickstart REPLACING the current `git clone` quickstart (the existing block becomes a collapsed `<details>` "Develop from source" section)

**3-command quickstart canonical form** (locked in CONTEXT.md D-LAUNCH README):
```
pip install qu-i-r-k[all]
quirk init
quirk run --config quirk.yaml
```
This MUST appear identically in README.md and docs/getting-started.md.

**Note on `qu-i-r-k` vs `quirk`** (Phase 84 D-02 single-source-of-truth invariant): the PyPI distribution name is `qu-i-r-k`; the installed binary and Python import remain `quirk`. Anti-pattern per Phase 84: ALWAYS grep the entire repo for stale `pip install quirk` instances before claiming completion (`grep -rn "pip install quirk" --include="*.md"`); fix any survivors.

**Persona triptych** (CONTEXT.md):
- "For the security consultant" — value prop centers on consulting deliverable: CBOM + quantum-readiness score + PDF report you hand a client.
- "For the IT generalist" — value prop centers on inventory + visibility: "what crypto do we even have?".
- "For the compliance officer" — value prop centers on FIPS / CMVP / quantum-readiness posture artifacts for audit.

**Badge providers** (Claude's discretion per CONTEXT.md): use shields.io. Standard set:
- CI status (`https://img.shields.io/github/actions/workflow/status/0xD1g5/QU.I.R.K./ci.yml`) — check the actual CI workflow filename in `.github/workflows/` first.
- PyPI version (`https://img.shields.io/pypi/v/qu-i-r-k`)
- License (`https://img.shields.io/github/license/0xD1g5/QU.I.R.K.`)
- Sigstore attestation (use a static badge: `https://img.shields.io/badge/sigstore-attested-blue` — there's no live data feed for this)
- Security policy link (`https://img.shields.io/badge/security-policy-blue` linking to `SECURITY.md`)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Augment README.md + docs/getting-started.md + capture dashboard hero screenshot</name>
  <files>README.md, docs/getting-started.md, docs/images/dashboard-hero.png</files>
  <action>
    **A. Hero screenshot capture:**
    Bring up the dashboard against a chaos lab profile that produces rich findings (use the same profile that plan 85-04 used for `tls-only.cbom.json` — typically `tls-cert-defects` or default phaseA). Steps:
    1. `cd quantum-chaos-enterprise-lab && ./lab.sh up --profile tls-cert-defects` (or the alternate per 85-04 deviation).
    2. Run a `quirk run` scan against the lab targets.
    3. `cd src/dashboard && npm run build` (per MEMORY.md feedback: dashboard requires explicit build step) — IF the dashboard has been edited; if untouched, skip rebuild.
    4. Launch the FastAPI dashboard, navigate to the most-impactful summary view (overall readiness + top findings).
    5. Capture a clean screenshot (~1400-1600 px wide, viewport only — no browser chrome if avoidable). On macOS: `Cmd+Shift+4` then space then click window.
    6. Save to `docs/images/dashboard-hero.png`. Optimize to ≤ 2 MB (e.g. `pngquant`) but keep ≥ 100 KB so visible quality is preserved.
    7. `./lab.sh down`.

    **B. README.md augmentation** (augment, do not rewrite — preserve all existing content except the existing `git clone` quickstart block which relocates):
    1. Above the `# QU.I.R.K. — vX.Y.Z` title, add a single-line badge row with the five badges from <interfaces>. Verify the CI workflow filename first by `ls .github/workflows/`.
    2. After the one-paragraph value-prop intro, add a `## For your role` (or equivalent) section with the three persona paragraphs from <interfaces>, each 2-4 sentences.
    3. After the persona triptych, add a hero image: `![QU.I.R.K. dashboard against the chaos lab](docs/images/dashboard-hero.png)` and a one-line caption.
    4. Replace the existing `## Quick Start` `git clone` block with the canonical 3-command quickstart from <interfaces>. Wrap it in a fenced bash block.
    5. Add an "Animated demo" line just under the quickstart: `Watch a 60-second run: <asciinema link placeholder — to be inserted post-recording>`. Leave the link as `<asciinema-link-here>` placeholder text; recording the asciinema is a manual post-merge task documented in 85-05-SUMMARY.md.
    6. Move the displaced `git clone` content under a `<details><summary>Develop from source</summary>...</details>` block lower in the README.
    7. Preserve the existing `## Documentation` table and everything below it untouched.

    **C. docs/getting-started.md polish:**
    Read current contents (99 lines). Restructure (or augment in place) so that the top of the file has a clear `## 3-step quickstart` section matching the README exactly:
    ```
    pip install qu-i-r-k[all]
    quirk init
    quirk run --config quirk.yaml
    ```
    Add a sentence after each command explaining what it does. Preserve all existing detailed setup content below the 3-step block (it's the "if you need more" section). Verify the quickstart commands by reading `run_scan.py` `init` and `run` argparse blocks — if `quirk init` or `quirk run --config quirk.yaml` are not exact existing CLI invocations, adjust the documented commands to match reality and record in SUMMARY.md.

    **D. Cross-file consistency grep:**
    After edits, run `grep -rn "pip install quirk" --include="*.md" .` (the OLD form, without hyphens) — every hit must be either deliberately on a documentation `pip install quirk[xyz]` pattern that's actually correct OR fixed to `pip install qu-i-r-k[...]`. Per Phase 84 single-source-of-truth anti-pattern: grep the entire repo, do not assume.
  </action>
  <verify>
    <automated>test -f docs/images/dashboard-hero.png && [ "$(wc -c < docs/images/dashboard-hero.png)" -ge 100000 ] && [ "$(wc -c < docs/images/dashboard-hero.png)" -le 2200000 ] && grep -q "img.shields.io" README.md && grep -q "docs/images/dashboard-hero.png" README.md && grep -q "pip install qu-i-r-k\[all\]" README.md && grep -q "quirk init" README.md && grep -q "quirk run --config quirk.yaml" README.md && grep -q -i "security consultant" README.md && grep -q -i "IT generalist" README.md && grep -q -i "compliance officer" README.md && grep -q "Develop from source" README.md && grep -q "pip install qu-i-r-k\[all\]" docs/getting-started.md && grep -q "quirk init" docs/getting-started.md && ! grep -rn "pip install quirk\[" --include="*.md" . | grep -v -- "qu-i-r-k"</automated>
  </verify>
  <done>All grep gates green; hero PNG present and within size band; README preserves existing content below `## Documentation`; getting-started's 3-step block matches README verbatim; no stale `pip install quirk[...]` survives in any .md file.</done>
</task>

<task type="auto">
  <name>Task 2: Add curl|bash Non-Decision section to docs/release-process.md</name>
  <files>docs/release-process.md</files>
  <action>
    Append (do not rewrite) a `## curl | bash Non-Decision` section to `docs/release-process.md`. Frame per CONTEXT.md <specifics>: a security-posture statement, NOT a "TODO" or "consider later" note. Required content:
    - **Decision (anti-feature):** QU.I.R.K. deliberately does not ship a `curl <url> | bash` installer.
    - **Rationale:** piping HTTP to a shell defeats the integrity guarantees of Sigstore attestations + PyPI Trusted Publishers. The attestation chain ends at the wheel/sdist; a curl|bash trampoline introduces an untrusted intermediary script that the attestation chain does not cover.
    - **Supported install paths** (cross-reference): `pip install qu-i-r-k[all]` (PyPI + attestations), `brew install 0xD1g5/quirk/quirk` (Homebrew tap; see plan 85-03's section), `docker run ghcr.io/0xd1g5/quirk:latest` (GHCR multi-arch; see plan 85-02's section).
    - **For consumers who need automated provisioning:** point at `gh attestation verify` (already documented in this file's "Attestation Verification" section) and recommend wrapping `pip install` in their config-management tooling (Ansible/Chef/etc.) which can verify attestations as a first-class step.
    - **Status:** permanent — this is not revisited on later milestones. The "Future Requirements" in REQUIREMENTS.md explicitly lists `curl|bash` as "anti-feature for a security tool — never".

    Place this section near the end of the file (after the Homebrew Tap and Container Image sections added by plans 85-02 + 85-03). Length target: 30-80 lines.

    Note: Phase 84's `docs/release-process.md` already contains a one-line "explicit non-feature note that `curl | bash` installers are not shipped" in the Attestation Verification subsection. THIS task expands that into the dedicated section; do NOT remove the existing one-liner — let them cross-reinforce.
  </action>
  <verify>
    <automated>grep -q "curl | bash Non-Decision" docs/release-process.md && grep -q -i "anti-feature" docs/release-process.md && grep -q -i "Sigstore" docs/release-process.md && grep -q "pip install qu-i-r-k" docs/release-process.md && grep -q "brew install" docs/release-process.md && grep -q "docker run" docs/release-process.md && grep -q -i "permanent" docs/release-process.md</automated>
  </verify>
  <done>Section appended, all grep markers present, original Phase 84 + plan 85-02/85-03 sections untouched (`git diff` review confirms additions only).</done>
</task>

<task type="auto">
  <name>Task 3: Phase 85 closure — UAT-SERIES update, Obsidian sync, phase note</name>
  <files>docs/UAT-SERIES.md</files>
  <action>
    Per CLAUDE.md "Mandatory Phase Completion Steps" — apply all four numbered steps:

    **Step 1 — Create Obsidian Phase Note.** Write directly to vault filesystem (NEVER via `obsidian CLI content=` — large files break shell expansion):
    Path: `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-85-Public-Launch-Polish.md`
    Format:
    ```yaml
    ---
    project: QU.I.R.K.
    type: phase
    status: complete
    source: .planning/phases/85-public-launch-polish/
    updated: <today YYYY-MM-DD>
    ---
    ```
    Then: `# Phase 85: Public-Launch Polish` heading + Goal (from ROADMAP) + Requirements Covered table (LAUNCH-01..07 each mapped to its closing plan) + Success Criteria (5 criteria from ROADMAP Phase 85 verbatim) + `## What Was Built` with one subsection per plan (85-01..85-05) sourced from each plan's SUMMARY.md if present at write-time, else from the PLAN.md objective + `[[Roadmap]]` trailing wikilink.

    **Step 2 — Update docs/UAT-SERIES.md.** Append a new UAT-85 series block with one test case per LAUNCH-xx requirement. Suggested 7 cases:
    - UAT-85-01: `quirk db migrate` idempotence (`pytest tests/test_db_migrate_cli.py`; double-run check)
    - UAT-85-02: `docs/upgrade-guide.md` presence + required markers
    - UAT-85-03: `.github/workflows/release-container.yml` YAML lint + multi-arch presence
    - UAT-85-04: `Formula/quirk.rb` Ruby syntax + DSL markers
    - UAT-85-05: Sample CBOM fixtures present + valid JSON + crypto components
    - UAT-85-06: README badge row + persona triptych + 3-command quickstart + hero image
    - UAT-85-07: `docs/release-process.md` curl|bash non-decision section markers
    Bump the `**Last Updated:**` header to today's date with a Phase 85 wrap blurb prefix.

    **Step 3 — Sync UAT-SERIES.md to Obsidian.** Use the documented printf+cat+cp pattern (file is too large for `obsidian CLI content=`):
    ```bash
    printf -- "---\nproject: QU.I.R.K.\ntype: reference\nstatus: active\nsource: docs/UAT-SERIES.md\nupdated: <today>\n---\n\n" > /tmp/uat_vault.md
    cat docs/UAT-SERIES.md >> /tmp/uat_vault.md
    cp /tmp/uat_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
    ```

    **Step 4 — Commit docs/UAT-SERIES.md** via the standard tooling:
    ```bash
    node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-85): update UAT-SERIES.md" --files docs/UAT-SERIES.md
    ```

    Phase 84 anti-patterns: NEVER `git add -A`; commits use explicit `--files` paths. Single-source-of-truth grep: confirm no stray `pip install quirk[...]` survives anywhere in `docs/` or root markdown (cross-reference Task 1.D).
  </action>
  <verify>
    <automated>test -f /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-85-Public-Launch-Polish.md && grep -q "status: complete" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-85-Public-Launch-Polish.md && grep -q "LAUNCH-01" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-85-Public-Launch-Polish.md && grep -q "LAUNCH-07" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-85-Public-Launch-Polish.md && grep -c "UAT-85-0[1-9]" docs/UAT-SERIES.md | awk '{exit ($1<5)?1:0}' && test -f /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md && grep -q "UAT-85" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md && git log --oneline -5 | grep -q "phase-85.*UAT-SERIES"</automated>
  </verify>
  <done>Obsidian phase note exists with frontmatter status:complete + all 7 LAUNCH IDs referenced; UAT-SERIES.md has ≥5 UAT-85 entries + today's date; vault UAT-Series.md refreshed; commit landed on HEAD with the documented message.</done>
</task>

</tasks>

<verification>
- All Task 1, 2, 3 grep gates green
- `git log --oneline -10` shows Phase 85 plan commits in order (85-01 through 85-05)
- ROADMAP Phase 85 progress row updatable (orchestrator handles, not this plan)
- `grep -rn "pip install quirk\[" --include="*.md" . | grep -v "qu-i-r-k"` returns no results (single-source-of-truth invariant)
</verification>

<success_criteria>
ROADMAP Phase 85 success criteria #4 and the LAUNCH-07 half of #5 (the curl|bash non-feature documentation) are fully satisfied by this plan. Combined with plans 85-01..85-04, all five ROADMAP Phase 85 success criteria are TRUE on landing. Phase 85 is closed per CLAUDE.md mandatory completion steps (Obsidian phase note + UAT-SERIES update + vault sync + explicit-path commit).
</success_criteria>

<deviation_protocol>
**If the hero screenshot capture cannot proceed** (dashboard build fails, chaos lab won't start cleanly, screenshot tooling unavailable on the executor's machine), do NOT commit a placeholder PNG. Instead: (a) stub `docs/images/dashboard-hero.png` with a 1x1 transparent PNG and note in `85-05-SUMMARY.md` under `## Known Stubs` that the real screenshot is a manual post-merge task; (b) update README.md's image embed to reference the placeholder; (c) add a TODO line at the top of SUMMARY.md so the orchestrator can surface this gap to the user.

**If `quirk init` or `quirk run --config quirk.yaml` are not exact existing CLI invocations**, adjust the documented quickstart commands to match reality (read `run_scan.py main()` for ground truth) AND record the divergence from CONTEXT.md D-LAUNCH README in `85-05-SUMMARY.md`. The 3-command shape (install → init → scan) is what matters; the exact flags must be real.

**If the asciinema demo recording is out of scope for this plan** (it requires recording a real terminal session — not something an executor can do in a CI-like context), leave the link as `<asciinema-link-here>` placeholder text in README.md and document it as a manual post-merge task in `85-05-SUMMARY.md` under `## Known Stubs`.

**If LICENSE file shows the project is not MIT-licensed**, and plan 85-03 already committed a license declaration in `Formula/quirk.rb`, cross-check that they agree. Mismatch → fix the formula in this plan's wrap (explicit `git add Formula/quirk.rb` add to this plan's files_modified). Record in SUMMARY.md.

Phase 84 anti-pattern reminder: at NO point in this plan should a commit use `git add -A` or `git add .` — every commit lists explicit paths. The closure commit in Task 3 uses `gsd-tools.cjs commit --files docs/UAT-SERIES.md`.
</deviation_protocol>

<output>
After completion, create `.planning/phases/85-public-launch-polish/85-05-SUMMARY.md` per the standard template, and ensure the Obsidian Phase-85 note's `status: complete` frontmatter reflects the final state of all 5 plans.
</output>
