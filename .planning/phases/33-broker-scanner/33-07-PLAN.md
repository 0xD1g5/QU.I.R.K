---
phase: 33-broker-scanner
plan: 07
type: execute
wave: 5
depends_on: [33-02]
files_modified:
  - labs/broker/Makefile
  - labs/broker/README.md
  - labs/broker/expected_results.md
  - labs/broker/kafka/server.properties
  - labs/broker/rabbitmq/rabbitmq.conf
  - labs/broker/redis/redis.conf
  - labs/broker/.gitignore
  - quantum-chaos-enterprise-lab/docker-compose.yml
autonomous: true
requirements: [BROKER-LAB-01, BROKER-LAB-02]
must_haves:
  truths:
    - "docker compose --profile broker up starts kafka, rabbitmq, redis containers"
    - "Kafka exposes 29092 (plaintext) + 29093 (TLS); RabbitMQ exposes 25672 (plaintext) + 25671 (AMQPS); Redis exposes 26379 (plaintext) + 26380 (TLS)"
    - "Lab uses official images (apache/kafka:3.6 OR confluentinc/cp-kafka:7.5; rabbitmq:3.12-management; redis:7-alpine) per CONTEXT.md D-15 2026-04-27 revision"
    - "Each container negotiates weak TLS (TLS 1.1/1.2 with non-PFS RSA cipher suites or 3DES) so the scanner emits weak-cipher HIGH on at least 2 of 3 brokers"
    - "Self-signed RSA-2048 certs generated by labs/broker/Makefile target 'certs'"
    - "expected_results.md documents per-port findings (plaintext HIGH ×3, weak-cipher HIGH ≥2)"
  artifacts:
    - path: "labs/broker/Makefile"
      provides: "cert-gen target + clean target"
      contains: "openssl req -x509 -newkey rsa:2048"
    - path: "quantum-chaos-enterprise-lab/docker-compose.yml"
      provides: "Three services under profiles: [broker]"
      contains: "profiles: [\"broker\"]"
    - path: "labs/broker/expected_results.md"
      provides: "BROKER-LAB-02 expected findings table"
      min_lines: 40
  key_links:
    - from: "quantum-chaos-enterprise-lab/docker-compose.yml"
      to: "labs/broker/{kafka,rabbitmq,redis}/*.{conf,properties}"
      via: "bind mounts"
      pattern: "labs/broker/"
tags: [phase-33, chaos-lab, docker, weak-tls]
---

<objective>
Build the `labs/broker/` chaos lab — three weak-TLS broker containers (Kafka, RabbitMQ, Redis) using official images per CONTEXT.md D-15 2026-04-27 revision, with bind-mounted minimal config files providing cipher-level weak-TLS control. Produces the test fixture for Success Criterion 4 (`docker compose --profile broker up`).

Purpose: Without a chaos lab, the scanner cannot demonstrate end-to-end weak-cipher HIGH findings on broker TLS endpoints. BROKER-LAB-01 requires three containers; BROKER-LAB-02 requires expected_results.md.

Output: Lab directory tree + compose profile + cert generation Makefile + expected_results.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-broker-scanner/33-CONTEXT.md
@.planning/phases/33-broker-scanner/33-RESEARCH.md
@.planning/phases/33-broker-scanner/33-PATTERNS.md
@CLAUDE.md
@labs/email/Makefile
@labs/email/expected_results.md

<interfaces>
Compose profile: `broker` (D-18). Service names: `kafka-broker`, `rabbitmq-broker`, `redis-broker`.

Port mapping (D-16):
  Kafka:    29092 (plaintext PLAINTEXT://) + 29093 (TLS SSL://)
  RabbitMQ: 25672 (AMQP) + 25671 (AMQPS) + 25015672 (mgmt UI optional, may be omitted to reduce noise)
  Redis:    26379 (plaintext) + 26380 (TLS)

Weak-TLS requirements (BROKER-LAB-01):
  - TLS 1.1 enabled OR a non-PFS RSA cipher OR 3DES suite present
  - RSA-2048 self-signed cert
  - Plaintext port intentionally listening
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: labs/broker/Makefile + cert generation + .gitignore + README</name>
  <files>labs/broker/Makefile, labs/broker/.gitignore, labs/broker/README.md</files>
  <read_first>
    - labs/email/Makefile (cert-gen pattern; PHONY targets; openssl invocation)
    - labs/email/README.md (lab quickstart prose)
    - .planning/phases/33-broker-scanner/33-RESEARCH.md "Chaos Lab" section (cert generation snippets)
  </read_first>
  <behavior>
    - `cd labs/broker && make certs` generates `certs/{kafka,rabbitmq,redis}.{crt,key}` with RSA-2048 keys, /CN=<service>.chaos.local, 3650-day validity.
    - `make clean` removes generated certs.
    - `.gitignore` excludes `certs/`.
  </behavior>
  <action>
1. Create `labs/broker/Makefile` modeled on `labs/email/Makefile`:

       .PHONY: certs clean help

       CERTS_DIR := certs

       help:
       	@echo "labs/broker targets:"
       	@echo "  make certs   - regenerate self-signed RSA-2048 certs (D-17)"
       	@echo "  make clean   - remove generated certs"

       certs:
       	@mkdir -p $(CERTS_DIR)
       	openssl req -x509 -newkey rsa:2048 -keyout $(CERTS_DIR)/kafka.key \
       	    -out $(CERTS_DIR)/kafka.crt -days 3650 -nodes \
       	    -subj "/CN=kafka.chaos.local" 2>/dev/null
       	openssl req -x509 -newkey rsa:2048 -keyout $(CERTS_DIR)/rabbitmq.key \
       	    -out $(CERTS_DIR)/rabbitmq.crt -days 3650 -nodes \
       	    -subj "/CN=rabbitmq.chaos.local" 2>/dev/null
       	openssl req -x509 -newkey rsa:2048 -keyout $(CERTS_DIR)/redis.key \
       	    -out $(CERTS_DIR)/redis.crt -days 3650 -nodes \
       	    -subj "/CN=redis.chaos.local" 2>/dev/null
       	chmod 644 $(CERTS_DIR)/*.crt
       	chmod 600 $(CERTS_DIR)/*.key
       	@echo "Generated $(CERTS_DIR)/{kafka,rabbitmq,redis}.{crt,key}"

       clean:
       	rm -f $(CERTS_DIR)/kafka.* $(CERTS_DIR)/rabbitmq.* $(CERTS_DIR)/redis.*

2. Create `labs/broker/.gitignore`:

       certs/

3. Create `labs/broker/README.md` ~30 lines covering: lab purpose, image versions per D-15 revision, port mapping table, quickstart commands (`make certs`, `docker compose --profile broker up`, scan command, `make clean`), link to expected_results.md.

4. `cd labs/broker && make certs && ls certs/` lists 6 files (3 .crt + 3 .key).
  </action>
  <verify>
    <automated>cd labs/broker && make certs >/dev/null 2>&1 && ls certs/ | grep -cE "(kafka|rabbitmq|redis)\.(crt|key)$"</automated>
  </verify>
  <done>Cert generation works; .gitignore excludes certs/; README documents quickstart.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Weak-TLS config files for kafka/rabbitmq/redis</name>
  <files>labs/broker/kafka/server.properties, labs/broker/rabbitmq/rabbitmq.conf, labs/broker/redis/redis.conf</files>
  <read_first>
    - .planning/phases/33-broker-scanner/33-RESEARCH.md "Chaos Lab" section (rabbitmq.conf snippet lines 740-761; redis.conf lines 778-788)
  </read_first>
  <behavior>
    - rabbitmq.conf enables TLS 1.0/1.1/1.2 with `DES-CBC3-SHA`, `AES128-SHA`, `AES256-SHA` ciphers; references certs at /etc/rabbitmq/certs/server.{crt,key}.
    - redis.conf enables port 6379 (plaintext) + tls-port 6380; tls-protocols "TLSv1.2"; tls-ciphers "DES-CBC3-SHA:AES128-SHA:AES256-SHA"; references certs at /etc/redis/certs/redis.{crt,key}.
    - kafka/server.properties (or env-var-driven server.properties for apache/kafka) sets ssl.enabled.protocols=TLSv1.1,TLSv1.2 and ssl.cipher.suites=TLS_RSA_WITH_AES_128_CBC_SHA,TLS_RSA_WITH_AES_256_CBC_SHA.
  </behavior>
  <action>
1. **labs/broker/rabbitmq/rabbitmq.conf** — RESEARCH.md verbatim:

       listeners.tcp.default = 5672
       listeners.ssl.default = 5671

       ssl_options.cacertfile = /etc/rabbitmq/certs/ca.crt
       ssl_options.certfile   = /etc/rabbitmq/certs/server.crt
       ssl_options.keyfile    = /etc/rabbitmq/certs/server.key
       ssl_options.verify     = verify_none
       ssl_options.fail_if_no_peer_cert = false

       ssl_options.versions.1 = tlsv1.1
       ssl_options.versions.2 = tlsv1.2
       ssl_options.ciphers.1 = DES-CBC3-SHA
       ssl_options.ciphers.2 = AES128-SHA
       ssl_options.ciphers.3 = AES256-SHA

       management.listener.port = 15672

   (Note: dropped `ssl_options.versions.tlsv1` — TLS 1.0 is rejected by modern OpenSSL/Erlang as a hard failure on rabbitmq:3.12. TLS 1.1+1.2 with weak ciphers is sufficient to fire the weak-cipher HIGH.)

2. **labs/broker/redis/redis.conf** — RESEARCH.md verbatim:

       port 6379
       tls-port 6380
       tls-cert-file /etc/redis/certs/redis.crt
       tls-key-file  /etc/redis/certs/redis.key
       tls-ca-cert-file /etc/redis/certs/redis.crt
       tls-auth-clients no
       tls-protocols "TLSv1.2"
       tls-ciphers "DES-CBC3-SHA:AES128-SHA:AES256-SHA"
       tls-prefer-server-ciphers yes

3. **labs/broker/kafka/server.properties** — minimal weak-TLS server.properties for `apache/kafka:3.6` (planner's choice per CONTEXT.md D-15 — pick `apache/kafka:3.6` if confluent licensing is not desired). The compose service mounts this file via `KAFKA_OPTS` or volume:

       process.roles=broker,controller
       node.id=1
       controller.quorum.voters=1@kafka-broker:9094
       listeners=PLAINTEXT://:29092,SSL://:29093,CONTROLLER://:9094
       advertised.listeners=PLAINTEXT://localhost:29092,SSL://localhost:29093
       listener.security.protocol.map=PLAINTEXT:PLAINTEXT,SSL:SSL,CONTROLLER:CONTROLLER
       inter.broker.listener.name=SSL
       controller.listener.names=CONTROLLER

       ssl.enabled.protocols=TLSv1.2
       ssl.cipher.suites=TLS_RSA_WITH_AES_128_CBC_SHA,TLS_RSA_WITH_AES_256_CBC_SHA
       ssl.keystore.type=PEM
       ssl.keystore.location=/etc/kafka/secrets/kafka.keystore.pem
       ssl.client.auth=none

   (Discretion: if `apache/kafka:3.6` proves to require additional config or Java keystore format, the executor may switch to `confluentinc/cp-kafka:7.5` and adjust the file accordingly. Document the choice in the SUMMARY.)

4. Verify all three files are valid plain text and parseable by their respective servers (lazy check: `cat` returns the content; non-empty).
  </action>
  <verify>
    <automated>test -s labs/broker/kafka/server.properties && test -s labs/broker/rabbitmq/rabbitmq.conf && test -s labs/broker/redis/redis.conf && grep -c "DES-CBC3-SHA\|AES128-SHA\|TLS_RSA_WITH" labs/broker/rabbitmq/rabbitmq.conf labs/broker/redis/redis.conf labs/broker/kafka/server.properties</automated>
  </verify>
  <done>Three config files exist, each containing at least one weak cipher suite reference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add 'broker' compose profile to quantum-chaos-enterprise-lab/docker-compose.yml</name>
  <files>quantum-chaos-enterprise-lab/docker-compose.yml</files>
  <read_first>
    - quantum-chaos-enterprise-lab/docker-compose.yml (existing profile blocks — vault, kerberos, etc.)
    - .planning/phases/33-broker-scanner/33-PATTERNS.md "labs/broker/" section
  </read_first>
  <behavior>
    - `docker compose --profile broker config` parses without error.
    - Three services declared under `profiles: ["broker"]`: kafka-broker, rabbitmq-broker, redis-broker.
    - Each service has correct port mappings, image, volume mounts, and healthcheck.
  </behavior>
  <action>
Append three service blocks to `quantum-chaos-enterprise-lab/docker-compose.yml` under the `services:` map (placement: after the existing email block if present, otherwise at end of services). Use the same indentation as existing blocks:

       # ── Broker Chaos Lab (profile: broker) ─────────────────────
       # Phase 33 / BROKER-LAB-01. Official images per CONTEXT.md D-15 (2026-04-27 revision).
       # Weak TLS via bind-mounted config files. Plaintext ports intentionally listening.
       kafka-broker:
         image: apache/kafka:3.6
         profiles: ["broker"]
         hostname: kafka-broker
         ports:
           - "29092:29092"
           - "29093:29093"
         volumes:
           - ../labs/broker/kafka/server.properties:/etc/kafka/server.properties:ro
           - ../labs/broker/certs/kafka.crt:/etc/kafka/secrets/kafka.crt:ro
           - ../labs/broker/certs/kafka.key:/etc/kafka/secrets/kafka.key:ro
         healthcheck:
           test: ["CMD-SHELL", "/opt/kafka/bin/kafka-broker-api-versions.sh --bootstrap-server localhost:29092 || exit 1"]
           interval: 15s
           timeout: 10s
           retries: 6
           start_period: 30s

       rabbitmq-broker:
         image: rabbitmq:3.12-management
         profiles: ["broker"]
         hostname: rabbitmq-broker
         ports:
           - "25672:5672"
           - "25671:5671"
         environment:
           - RABBITMQ_CONFIG_FILE=/etc/rabbitmq/rabbitmq.conf
         volumes:
           - ../labs/broker/rabbitmq/rabbitmq.conf:/etc/rabbitmq/rabbitmq.conf:ro
           - ../labs/broker/certs/rabbitmq.crt:/etc/rabbitmq/certs/server.crt:ro
           - ../labs/broker/certs/rabbitmq.key:/etc/rabbitmq/certs/server.key:ro
           - ../labs/broker/certs/rabbitmq.crt:/etc/rabbitmq/certs/ca.crt:ro
         healthcheck:
           test: ["CMD-SHELL", "rabbitmq-diagnostics -q ping || exit 1"]
           interval: 15s
           timeout: 10s
           retries: 6
           start_period: 30s

       redis-broker:
         image: redis:7-alpine
         profiles: ["broker"]
         hostname: redis-broker
         command: ["redis-server", "/etc/redis/redis.conf"]
         ports:
           - "26379:6379"
           - "26380:6380"
         volumes:
           - ../labs/broker/redis/redis.conf:/etc/redis/redis.conf:ro
           - ../labs/broker/certs/redis.crt:/etc/redis/certs/redis.crt:ro
           - ../labs/broker/certs/redis.key:/etc/redis/certs/redis.key:ro
         healthcheck:
           test: ["CMD-SHELL", "redis-cli ping || exit 1"]
           interval: 15s
           timeout: 10s
           retries: 6
           start_period: 30s

Verify: `docker compose -f quantum-chaos-enterprise-lab/docker-compose.yml --profile broker config` exits 0 (parse-only; no need to `up`).

If apache/kafka:3.6 proves incompatible with the bind-mounted server.properties (Apache image expects different config path), switch to `confluentinc/cp-kafka:7.5` with appropriate KAFKA_* env-var overrides per RESEARCH.md alternative path. Document choice in SUMMARY.
  </action>
  <verify>
    <automated>docker compose -f quantum-chaos-enterprise-lab/docker-compose.yml --profile broker config 2>&1 | grep -cE "kafka-broker|rabbitmq-broker|redis-broker"</automated>
  </verify>
  <done>Compose profile parses cleanly; three services declared with correct port mappings + bind mounts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: labs/broker/expected_results.md (BROKER-LAB-02)</name>
  <files>labs/broker/expected_results.md</files>
  <read_first>
    - labs/email/expected_results.md (template)
  </read_first>
  <behavior>
    - Document expected findings for each port: plaintext HIGH on 29092/25672/26379, weak-cipher HIGH on at least 2 of {29093, 25671, 26380}.
    - Document the lab boot command, scan command, and how to verify findings.
  </behavior>
  <action>
Create `labs/broker/expected_results.md` modeled on `labs/email/expected_results.md`:

    # Phase 33 — Broker Scanner Expected Results

    **Lab:** apache/kafka + rabbitmq:3.12-management + redis:7-alpine (Docker Compose profile `broker`)
    **Phase:** 33 — Broker TLS Scanner
    **Requirements:** BROKER-LAB-01, BROKER-LAB-02
    **Image source:** Official images per CONTEXT.md D-15 (2026-04-27 revision) — bitnami images cannot configure cipher suites via env vars.

    ## Lab Setup

    ```bash
    cd labs/broker
    make certs                      # generate self-signed RSA-2048 certs
    cd ../../quantum-chaos-enterprise-lab
    docker compose --profile broker up -d
    # wait ~30s for healthchecks
    ```

    Boot the QU.I.R.K. scan with broker scanning enabled:

    ```bash
    cfg='{"connectors":{"enable_broker":true}}' \\
        python run_scan.py --profile standard --config <(echo "$cfg") \\
        --target localhost
    ```

    ## Expected Scan Output

    | port  | protocol            | tls_version | cipher_suite                      | finding                    | severity |
    |-------|---------------------|-------------|-----------------------------------|----------------------------|----------|
    | 29092 | KAFKA-PLAIN         | -           | -                                 | kafka-plaintext-listener   | HIGH     |
    | 29093 | KAFKA-TLS           | TLSv1.2     | TLS_RSA_WITH_AES_128_CBC_SHA      | weak-cipher                | HIGH     |
    | 25672 | AMQP-PLAIN          | -           | -                                 | amqp-plaintext-listener    | HIGH     |
    | 25671 | AMQPS               | TLSv1.2     | DES-CBC3-SHA / AES128-SHA / AES256-SHA | weak-cipher           | HIGH     |
    | 26379 | REDIS-PLAIN         | -           | -                                 | redis-plaintext-no-auth    | HIGH     |
    | 26380 | REDIS-TLS           | TLSv1.2     | DES-CBC3-SHA / AES128-SHA / AES256-SHA | weak-cipher           | HIGH     |

    **Success criteria for the lab (per ROADMAP Phase 33 SC4):**
    - Plaintext HIGH on all three brokers (29092, 25672, 26379)
    - Weak-cipher HIGH on at least 2 of 3 TLS broker ports (29093, 25671, 26380)

    ## Verification

    ```bash
    sqlite3 quirk.db "SELECT host, port, protocol, cipher_suite FROM crypto_endpoints WHERE port IN (29092,29093,25672,25671,26379,26380)"
    sqlite3 quirk.db "SELECT json_extract(broker_scan_json, '\$.kafka') FROM crypto_endpoints WHERE broker_scan_json IS NOT NULL LIMIT 1"
    ```

    The `broker_scan_json` column on the first broker endpoint contains a nested dict with keys `kafka`, `rabbitmq`, `redis`, `azure_servicebus`, `aws_sqs` (per D-12).
  </action>
  <verify>
    <automated>test -s labs/broker/expected_results.md && grep -c "kafka-plaintext-listener\|amqp-plaintext-listener\|redis-plaintext-no-auth\|weak-cipher" labs/broker/expected_results.md</automated>
  </verify>
  <done>expected_results.md exists, ≥40 lines, documents all 6 expected findings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chaos lab containers <-> host | bind mounts read-only; ports bound to localhost-only by default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-17 | E (Elevation of privilege) | Plaintext Kafka/Redis/AMQP listeners on consultant workstation | accept | Lab-only; Compose profile must be explicitly opted into; intentional weak posture for scanner exercise |
| T-33-18 | I (Information disclosure) | self-signed certs in labs/broker/certs/ | mitigate | `.gitignore` excludes the `certs/` directory; certs regenerated locally per consultant |
</threat_model>

<verification>
- `cd labs/broker && make certs && ls certs/ | wc -l` returns 6.
- `docker compose -f quantum-chaos-enterprise-lab/docker-compose.yml --profile broker config` exits 0.
- `grep -c "broker" quantum-chaos-enterprise-lab/docker-compose.yml` shows the three new service entries.
- `wc -l labs/broker/expected_results.md` returns ≥40.
</verification>

<success_criteria>
BROKER-LAB-01 and BROKER-LAB-02 satisfied. Compose profile boots three brokers with weak TLS; expected_results.md documents the per-port findings backing ROADMAP Phase 33 Success Criterion 4.
</success_criteria>

<output>
After completion, create `.planning/phases/33-broker-scanner/33-07-SUMMARY.md` documenting the Kafka image choice (apache vs confluent), any TLS-version/cipher adjustments needed for image-version compatibility, and the executed `docker compose ... config` output.
</output>
