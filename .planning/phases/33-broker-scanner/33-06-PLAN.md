---
phase: 33-broker-scanner
plan: 06
type: execute
wave: 5
depends_on: [33-01, 33-02, 33-05]
files_modified:
  - run_scan.py
  - quirk/engine/risk_engine.py
  - tests/test_broker_run_integration.py
autonomous: true
requirements: [BROKER-00]
must_haves:
  truths:
    - "run_scan.py invokes scan_kafka_targets, scan_rabbitmq_targets, scan_redis_targets when cfg.connectors.enable_broker"
    - "CLI flags --azure-servicebus-namespace and --aws-sqs-region (both repeatable, action='append') populate cfg.connectors.broker_azure_namespaces / broker_sqs_regions"
    - "broker_scan_json is a JSON string with top-level keys kafka, rabbitmq, redis, azure_servicebus, aws_sqs (D-12)"
    - "broker_scan_json is attached to the FIRST broker endpoint only (D-14); other endpoints have NULL"
    - "evaluate_broker_endpoints emits findings: kafka-plaintext-listener (HIGH), amqp-plaintext-listener (HIGH), redis-plaintext-no-auth (HIGH), weak-cipher (HIGH)"
    - "Layered findings on the same host:port survive _dedupe_findings (different titles)"
  artifacts:
    - path: "run_scan.py"
      provides: "Broker scan block + CLI flags + aggregation"
      contains: "scan_kafka_targets"
    - path: "quirk/engine/risk_engine.py"
      provides: "evaluate_broker_endpoints() finding emitter"
      contains: "evaluate_broker_endpoints"
    - path: "tests/test_broker_run_integration.py"
      provides: "Aggregation shape + finding emission tests"
      min_lines: 120
  key_links:
    - from: "run_scan.py"
      to: "quirk.scanner.broker_scanner.scan_kafka_targets"
      via: "top-level import + gated call"
      pattern: "scan_kafka_targets"
    - from: "run_scan.py"
      to: "first_broker_endpoint.broker_scan_json"
      via: "attribute assignment with json.dumps payload"
      pattern: "broker_scan_json"
tags: [phase-33, broker-scanner, integration, run-scan, risk-engine]
---

<objective>
Wire `broker_scanner` into `run_scan.py`: add CLI flags for cloud targets, call the three driver functions when `cfg.connectors.enable_broker`, aggregate per-protocol-family endpoints into a nested JSON attached to the first broker endpoint (D-12/D-14), and emit broker-specific findings via `evaluate_broker_endpoints()` in `risk_engine.py`.

Purpose: Without this plan, the scanner code from Plans 03–05 is unreachable — no scan picks up broker probes and no rows persist `broker_scan_json`.

Output: Modified `run_scan.py` with broker block + CLI flags + aggregation, new `evaluate_broker_endpoints()` in `risk_engine.py`, and an integration test confirming the JSON shape and finding emission.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-broker-scanner/33-CONTEXT.md
@.planning/phases/33-broker-scanner/33-RESEARCH.md
@.planning/phases/33-broker-scanner/33-PATTERNS.md
@.planning/phases/33-broker-scanner/33-VALIDATION.md
@CLAUDE.md
@run_scan.py
@quirk/engine/risk_engine.py
@quirk/scanner/email_scanner.py

<interfaces>
CLI flags added to run_scan.py argparse:
  --azure-servicebus-namespace <name>   action="append", default=[], dest="azure_servicebus_namespaces"
  --aws-sqs-region <region>             action="append", default=[], dest="aws_sqs_regions"

After config load, before scanning:
  cfg.connectors.broker_azure_namespaces = (cfg.connectors.broker_azure_namespaces or []) + (args.azure_servicebus_namespaces or [])
  cfg.connectors.broker_sqs_regions      = (cfg.connectors.broker_sqs_regions      or []) + (args.aws_sqs_regions          or [])

broker_scan_json shape (D-12, attached to first broker endpoint only per D-14):
  {
    "kafka":            [<ep_dict>, ...],
    "rabbitmq":         [<ep_dict>, ...],
    "redis":            [<ep_dict>, ...],
    "azure_servicebus": [<ep_dict>, ...],
    "aws_sqs":          [<ep_dict>, ...],
    "session_start":    "<ISO8601>"
  }
  ep_dict: {host, port, protocol, tls_version, cipher_suite, cert_pubkey_alg, cert_subject, scan_error}

evaluate_broker_endpoints(endpoints) -> List[Dict]:
  - kafka-plaintext-listener  (HIGH) for ep.protocol == "KAFKA-PLAIN"
  - amqp-plaintext-listener   (HIGH) for ep.protocol == "AMQP-PLAIN"
  - redis-plaintext-no-auth   (HIGH) for ep.protocol == "REDIS-PLAIN"
  - weak-cipher               (HIGH) for ep on ANY broker TLS protocol with TLS_RSA_WITH_*, 3DES, RC4, or non-AEAD *-SHA cipher
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI flags + broker scan block in run_scan.py</name>
  <files>run_scan.py</files>
  <read_first>
    - run_scan.py lines 160-200 (argparse setup; existing flags)
    - run_scan.py lines 691-724 (Phase 32 email block — exact analog)
    - .planning/phases/33-broker-scanner/33-PATTERNS.md section "run_scan.py"
  </read_first>
  <behavior>
    - `python run_scan.py --help` shows `--azure-servicebus-namespace` and `--aws-sqs-region` flags (both repeatable).
    - When `cfg.connectors.enable_broker` is True, the three scanner drivers are called with `tls_targets`-derived hosts, the timeout, the supplied azure/sqs lists, the logger, and `session_start`.
    - The `endpoints = (...)` aggregation includes kafka_endpoints + rabbit_endpoints + redis_endpoints.
    - When no broker endpoints exist, no broker_scan_json is attached.
  </behavior>
  <action>
1. Add CLI flag declarations near the existing `--config`/`--profile` flags in `run_scan.py`:

       parser.add_argument(
           "--azure-servicebus-namespace",
           action="append", default=[], dest="azure_servicebus_namespaces",
           help="Azure Service Bus namespace to probe (repeatable). Phase 33 / D-01.",
       )
       parser.add_argument(
           "--aws-sqs-region",
           action="append", default=[], dest="aws_sqs_regions",
           help="AWS SQS region to probe (repeatable). Phase 33 / D-01.",
       )

2. After config load (find where args are merged into cfg — mirror existing flag-merging idiom), add:

       # Phase 33 / D-01 cloud broker target plumbing — CLI extends config-supplied lists
       if getattr(args, "azure_servicebus_namespaces", None):
           cfg.connectors.broker_azure_namespaces = (
               list(cfg.connectors.broker_azure_namespaces or []) + list(args.azure_servicebus_namespaces)
           )
       if getattr(args, "aws_sqs_regions", None):
           cfg.connectors.broker_sqs_regions = (
               list(cfg.connectors.broker_sqs_regions or []) + list(args.aws_sqs_regions)
           )

3. Top-level imports — add alongside existing `from quirk.scanner.email_scanner import scan_email_targets`:

       from quirk.scanner.broker_scanner import (
           scan_kafka_targets, scan_rabbitmq_targets, scan_redis_targets,
       )
       from quirk.engine.risk_engine import evaluate_broker_endpoints

4. Locate the email block (lines ~691–724). Directly after it (BEFORE the `endpoints = (... + email_endpoints)` aggregation), insert the broker block:

       # ── Broker TLS scanning (Phase 33) ────────────────────────
       kafka_endpoints = []
       rabbit_endpoints = []
       redis_endpoints = []
       with _phase_timer(run_stats, "broker_scanning"):
           if cfg.connectors.enable_broker:
               broker_hosts = list(dict.fromkeys(h for h, _ in tls_targets))
               if broker_hosts:
                   kafka_endpoints = scan_kafka_targets(
                       hosts=broker_hosts,
                       timeout=cfg.scan.timeout_seconds,
                       profile=cfg.scan.profile,
                       logger=logger,
                       session_start=session_start,
                   )
                   rabbit_endpoints = scan_rabbitmq_targets(
                       hosts=broker_hosts,
                       azure_namespaces=cfg.connectors.broker_azure_namespaces,
                       sqs_regions=cfg.connectors.broker_sqs_regions,
                       timeout=cfg.scan.timeout_seconds,
                       logger=logger,
                       session_start=session_start,
                   )
                   redis_endpoints = scan_redis_targets(
                       hosts=broker_hosts,
                       timeout=cfg.scan.timeout_seconds,
                       logger=logger,
                       session_start=session_start,
                   )
                   logger.info(
                       f"Broker scan: kafka={len(kafka_endpoints)} "
                       f"rabbit={len(rabbit_endpoints)} redis={len(redis_endpoints)}"
                   )

5. Update `endpoints = (...)` aggregation to include the three broker lists (add after `email_endpoints`):

       endpoints = (... + email_endpoints + kafka_endpoints + rabbit_endpoints + redis_endpoints)

6. `python -m compileall run_scan.py` exits 0; `python run_scan.py --help` returns exit 0 and shows both new flags.
  </action>
  <verify>
    <automated>python -m compileall run_scan.py 2>&1 | grep -E "Error|SyntaxError"; test $? -ne 0 && python run_scan.py --help 2>&1 | grep -c "azure-servicebus-namespace\|aws-sqs-region"</automated>
  </verify>
  <done>run_scan.py compiles; --help lists both new CLI flags; broker block placed after email block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add broker_scan_json aggregation block in run_scan.py + evaluate_broker_endpoints in risk_engine.py</name>
  <files>run_scan.py, quirk/engine/risk_engine.py</files>
  <read_first>
    - run_scan.py (state after Task 1)
    - quirk/engine/risk_engine.py:evaluate_email_endpoints (Phase 32 PATTERNS analog)
    - quirk/scanner/email_scanner.py:550-599 (per-host JSON aggregation pattern)
    - .planning/phases/33-broker-scanner/33-RESEARCH.md Pattern 9
  </read_first>
  <behavior>
    - After broker scanning, broker_scan_json is built as a nested dict per D-12 and attached to the first broker endpoint via `setattr(ep, "broker_scan_json", json.dumps(payload, default=str))`.
    - When all three scanner lists are empty, no aggregation runs (no endpoint to attach to).
    - `evaluate_broker_endpoints(endpoints)` returns a finding for each plaintext endpoint and each weak-cipher TLS endpoint.
    - Finding shape matches `evaluate_email_endpoints` exactly: dict with severity, host, port, title, recommendation.
  </behavior>
  <action>
1. **run_scan.py** — append AFTER the broker scan block (and BEFORE the `endpoints = ...` line that uses these lists), build the aggregation:

       # broker_scan_json aggregation (Phase 33 / D-12, D-14)
       all_broker_eps = kafka_endpoints + rabbit_endpoints + redis_endpoints
       if all_broker_eps:
           def _ep_dict(ep):
               return {
                   "host": getattr(ep, "host", None),
                   "port": getattr(ep, "port", None),
                   "protocol": getattr(ep, "protocol", None),
                   "tls_version": getattr(ep, "tls_version", None),
                   "cipher_suite": getattr(ep, "cipher_suite", None),
                   "cert_pubkey_alg": getattr(ep, "cert_pubkey_alg", None),
                   "cert_subject": getattr(ep, "cert_subject", None),
                   "scan_error": getattr(ep, "scan_error", None),
               }
           azure_eps = [e for e in rabbit_endpoints if getattr(e, "protocol", "") == "AMQPS/Azure-ServiceBus"]
           sqs_eps   = [e for e in rabbit_endpoints if getattr(e, "protocol", "") == "HTTPS/AWS-SQS"]
           rabbit_self = [e for e in rabbit_endpoints if e not in azure_eps and e not in sqs_eps]
           payload = {
               "kafka":            [_ep_dict(e) for e in kafka_endpoints],
               "rabbitmq":         [_ep_dict(e) for e in rabbit_self],
               "redis":            [_ep_dict(e) for e in redis_endpoints],
               "azure_servicebus": [_ep_dict(e) for e in azure_eps],
               "aws_sqs":          [_ep_dict(e) for e in sqs_eps],
               "session_start":    session_start.isoformat() if session_start else None,
           }
           setattr(all_broker_eps[0], "broker_scan_json", json.dumps(payload, default=str))

2. **run_scan.py** — directly after the existing email-findings merge (mirroring Phase 32 pattern), call:

       broker_findings = evaluate_broker_endpoints(all_broker_eps if 'all_broker_eps' in dir() else [])
       # broker_findings merged into main findings list before _dedupe_findings runs

   (Adapt to the exact merge idiom in run_scan.py — match how `email_findings` is integrated.)

3. **quirk/engine/risk_engine.py** — add `evaluate_broker_endpoints` function modeled on `evaluate_email_endpoints`:

       def evaluate_broker_endpoints(endpoints):
           """Phase 33: emit broker-specific findings.

           Plaintext findings (HIGH):
             - kafka-plaintext-listener  (ep.protocol == "KAFKA-PLAIN")
             - amqp-plaintext-listener   (ep.protocol == "AMQP-PLAIN")
             - redis-plaintext-no-auth   (ep.protocol == "REDIS-PLAIN")

           Weak-cipher findings (HIGH) for TLS_RSA_WITH_*, 3DES, RC4, or non-AEAD *-SHA on
           any broker TLS protocol (KAFKA-TLS, AMQPS, AMQPS/Azure-ServiceBus, HTTPS/AWS-SQS, REDIS-TLS).
           """
           findings = []
           for e in endpoints:
               host = getattr(e, "host", "")
               port = int(getattr(e, "port", 0) or 0)
               protocol = getattr(e, "protocol", "") or ""
               cipher = (getattr(e, "cipher_suite", "") or "").upper()
               tls_version = getattr(e, "tls_version", "") or ""

               if protocol == "KAFKA-PLAIN":
                   findings.append({
                       "severity": "HIGH", "host": host, "port": port,
                       "title": "Plaintext Kafka listener detected",
                       "recommendation": "Disable PLAINTEXT listener or restrict access; enforce SSL/SASL_SSL on broker port 9092.",
                   })
                   continue
               if protocol == "AMQP-PLAIN":
                   findings.append({
                       "severity": "HIGH", "host": host, "port": port,
                       "title": "Plaintext AMQP listener detected",
                       "recommendation": "Disable plaintext AMQP on port 5672; require AMQPS (port 5671) for all clients.",
                   })
                   continue
               if protocol == "REDIS-PLAIN":
                   findings.append({
                       "severity": "HIGH", "host": host, "port": port,
                       "title": "Plaintext Redis listener (no auth)",
                       "recommendation": "Enable TLS on port 6380 and require AUTH; bind plaintext port to localhost only or disable.",
                   })
                   continue

               # Weak-cipher detection on any broker TLS protocol
               broker_tls = protocol in {"KAFKA-TLS", "AMQPS", "AMQPS/Azure-ServiceBus", "HTTPS/AWS-SQS", "REDIS-TLS"}
               if broker_tls and tls_version:
                   is_rsa_kex = (
                       cipher.startswith("TLS_RSA_WITH_")
                       or any(s in cipher for s in ("AES128-SHA", "AES256-SHA", "3DES", "RC4", "DES-CBC"))
                   ) and "ECDHE" not in cipher and "DHE" not in cipher
                   if is_rsa_kex:
                       findings.append({
                           "severity": "HIGH", "host": host, "port": port,
                           "title": "Weak cipher suite on broker TLS endpoint",
                           "recommendation": (
                               "Broker is negotiating non-PFS RSA / 3DES / RC4 / weak-CBC suites. "
                               "Disable TLS_RSA_WITH_* and legacy ciphers; require TLS 1.2+ AEAD or TLS 1.3."
                           ),
                       })
           return findings

4. `python -m compileall run_scan.py quirk/engine/risk_engine.py` exits 0.
  </action>
  <verify>
    <automated>python -m compileall run_scan.py quirk/engine/risk_engine.py 2>&1 | grep -E "Error|SyntaxError"; test $? -ne 0 && python -c "from quirk.engine.risk_engine import evaluate_broker_endpoints; print('OK')"</automated>
  </verify>
  <done>run_scan.py builds and attaches broker_scan_json correctly; evaluate_broker_endpoints emits four finding types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tests/test_broker_run_integration.py — aggregation shape + finding emission</name>
  <files>tests/test_broker_run_integration.py</files>
  <read_first>
    - tests/test_broker_scanner_redis.py
    - quirk/engine/risk_engine.py (just-modified evaluate_broker_endpoints)
    - .planning/phases/33-broker-scanner/33-RESEARCH.md Pattern 9
  </read_first>
  <behavior>
    - Test 1 (D-12 shape): build a list of fake endpoints across all five protocol families; replicate the aggregation logic from run_scan.py inline; assert payload has keys {kafka, rabbitmq, redis, azure_servicebus, aws_sqs, session_start} and each is a list.
    - Test 2 (D-14 attachment): aggregation attaches `broker_scan_json` ONLY to the first endpoint; other endpoints have no such attribute.
    - Test 3 (kafka-plaintext-listener finding): evaluate_broker_endpoints with one KAFKA-PLAIN ep -> exactly one finding with title containing "Plaintext Kafka", severity HIGH.
    - Test 4 (amqp-plaintext-listener finding): one AMQP-PLAIN ep -> one HIGH finding with "Plaintext AMQP" in title.
    - Test 5 (redis-plaintext-no-auth finding): one REDIS-PLAIN ep -> one HIGH finding with "Plaintext Redis" in title.
    - Test 6 (weak-cipher TLS_RSA_WITH_*): KAFKA-TLS ep with cipher_suite="TLS_RSA_WITH_AES_128_CBC_SHA", tls_version="TLSv1.2" -> one HIGH finding with "Weak cipher" in title.
    - Test 7 (weak-cipher 3DES on AMQPS): AMQPS ep with cipher "DES-CBC3-SHA" -> HIGH weak-cipher finding.
    - Test 8 (no false-positive on PFS ECDHE): KAFKA-TLS ep with cipher_suite="ECDHE-RSA-AES256-GCM-SHA384" -> NO weak-cipher finding.
    - Test 9 (layered findings survive dedupe): one host has both KAFKA-PLAIN (port 9092) and KAFKA-TLS weak-cipher (port 9093). Run findings through `_dedupe_findings` if importable; assert both findings still present (different titles -> dedupe key differs).
    - Test 10 (BROKER-00 column write): create in-memory engine, init_db, insert a CryptoEndpoint with broker_scan_json='{"kafka":[]}', SELECT and decode; assert decoded JSON is correct.
  </behavior>
  <action>
Create tests/test_broker_run_integration.py with the 10 tests above. For Test 1/2 reproduce the aggregation logic from run_scan.py inline (do not depend on run_scan.py module-level execution). Pattern:

    import json
    from unittest.mock import MagicMock
    from quirk.engine.risk_engine import evaluate_broker_endpoints

    def _ep(host="h", port=9093, protocol="KAFKA-TLS", cipher=None, tls=None):
        e = MagicMock()
        e.host = host; e.port = port; e.protocol = protocol
        e.cipher_suite = cipher; e.tls_version = tls
        e.cert_pubkey_alg = None; e.cert_subject = None; e.scan_error = None
        return e

    def test_kafka_plaintext_finding():
        eps = [_ep(port=9092, protocol="KAFKA-PLAIN")]
        findings = evaluate_broker_endpoints(eps)
        assert len(findings) == 1 and findings[0]["severity"] == "HIGH"
        assert "Plaintext Kafka" in findings[0]["title"]

    def test_aggregation_shape_and_attachment():
        # Replicate run_scan.py aggregation logic inline
        kafka_eps    = [_ep(host="k", port=9093, protocol="KAFKA-TLS")]
        rabbit_eps   = [_ep(host="r", port=5671, protocol="AMQPS"),
                        _ep(host="ns.servicebus.windows.net", port=5671, protocol="AMQPS/Azure-ServiceBus"),
                        _ep(host="sqs.us-east-1.amazonaws.com", port=443, protocol="HTTPS/AWS-SQS")]
        redis_eps    = [_ep(host="rd", port=6380, protocol="REDIS-TLS")]
        all_eps = kafka_eps + rabbit_eps + redis_eps
        # ... (build payload as run_scan.py does) ...
        assert "kafka" in payload and "rabbitmq" in payload and "redis" in payload
        assert "azure_servicebus" in payload and "aws_sqs" in payload
        # broker_scan_json attaches to all_eps[0] only
        first = all_eps[0]
        first.broker_scan_json = json.dumps(payload, default=str)
        decoded = json.loads(first.broker_scan_json)
        assert isinstance(decoded["kafka"], list)
        assert len(decoded["azure_servicebus"]) == 1

For Test 10, use real `quirk.db.init_db` and `quirk.models.CryptoEndpoint` against an in-memory engine.

Run `python -m pytest tests/test_broker_run_integration.py -v` — all 10 tests green.
  </action>
  <verify>
    <automated>python -m pytest tests/test_broker_run_integration.py -v --no-header 2>&1 | tail -15</automated>
  </verify>
  <done>10 tests green covering aggregation shape (D-12), attachment rule (D-14), four finding types, layered finding survival.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> cfg.connectors lists | argparse strings concatenated into broker hostnames in Plan 04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-15 | T (Tampering) | broker_scan_json JSON construction | mitigate | `json.dumps(default=str)` is safe; no user input is keys; values come from probe results |
| T-33-16 | I (Information disclosure) | broker_scan_json persists cert subject + cipher names | accept | Same trust boundary as existing email_scan_json (CONTEXT.md D-11) |
</threat_model>

<verification>
- `python -m compileall run_scan.py quirk/engine/risk_engine.py` exits 0.
- `python run_scan.py --help` lists `--azure-servicebus-namespace` and `--aws-sqs-region` flags.
- `python -m pytest tests/test_broker_run_integration.py tests/test_broker_db_schema.py tests/test_broker_config_and_profile.py tests/test_broker_scanner_kafka.py tests/test_broker_scanner_rabbitmq.py tests/test_broker_scanner_redis.py -v` reports all green.
</verification>

<success_criteria>
BROKER-00 fully realised end-to-end: scanner output flows into a single nested-JSON column on the first broker endpoint per scan, broker findings emit at HIGH severity, and CLI flags accept Azure SB / AWS SQS targets per D-01.
</success_criteria>

<output>
After completion, create `.planning/phases/33-broker-scanner/33-06-SUMMARY.md` documenting the exact line-number insertions in run_scan.py and the dedupe-survival decision for layered findings.
</output>
