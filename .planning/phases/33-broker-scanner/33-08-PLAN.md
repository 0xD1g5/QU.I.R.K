---
phase: 33-broker-scanner
plan: 08
type: execute
wave: 6
depends_on: [33-06, 33-07]
files_modified:
  - docs/UAT-SERIES.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
autonomous: false
requirements: [BROKER-00, KAFKA-01, KAFKA-02, KAFKA-03, KAFKA-04, RABBIT-01, RABBIT-02, RABBIT-03, RABBIT-04, RABBIT-05, REDIS-01, REDIS-02, REDIS-03, BROKER-LAB-01, BROKER-LAB-02, BROKER-ARCH, STRUCT-01, STRUCT-02, STRUCT-03]
tags: [phase-completion, uat, obsidian, docs]

must_haves:
  truths:
    - "Phase 33 end-to-end smoke run produces ≥3 plaintext HIGH findings and ≥2 weak-cipher HIGH findings"
    - "docs/UAT-SERIES.md contains a UAT-33 series with broker scanner test cases and updated Last Updated date"
    - "Obsidian vault has Phase-33-Broker-Scanner.md note with status: complete"
    - "Obsidian vault UAT-Series.md mirrors docs/UAT-SERIES.md"
    - "ROADMAP.md Phase 33 entry is marked complete with all success criteria checked"
  artifacts:
    - path: "docs/UAT-SERIES.md"
      provides: "Phase 33 UAT series + bumped Last Updated"
      contains: "UAT-33"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-33-Broker-Scanner.md"
      provides: "Obsidian phase note"
      contains: "status: complete"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
      provides: "Vault-mirrored UAT series"
      contains: "UAT-33"
  key_links:
    - from: "docs/UAT-SERIES.md"
      to: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
      via: "cp after frontmatter prepend"
      pattern: "UAT-33"
    - from: ".planning/ROADMAP.md"
      to: "Phase 33 entry"
      via: "checkbox state"
      pattern: "Phase 33.*\\[x\\]"
---

<objective>
Close out Phase 33 by running the end-to-end smoke test against the broker chaos lab,
documenting UAT cases, syncing the Obsidian vault, and committing per CLAUDE.md mandatory
phase-completion steps.

Purpose: Make Phase 33 reproducible (UAT) and discoverable (Obsidian + roadmap).
Output: Updated UAT-SERIES, vault-mirrored copies, finalized ROADMAP, signed commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-broker-scanner/33-CONTEXT.md
@.planning/phases/33-broker-scanner/33-PATTERNS.md
@docs/UAT-SERIES.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end smoke against broker chaos lab</name>
  <files>(no source modification — runs scanner against running compose)</files>
  <action>
    Pre-req: from quantum-chaos-enterprise-lab/, run `docker compose --profile broker up -d`
    and wait for healthchecks (all three of kafka-broker, rabbitmq-broker, redis-broker
    must report healthy via `docker compose ps`).

    Then from project root:
      python -m compileall quirk run_scan.py
      pytest tests/test_broker_db_schema.py tests/test_broker_config_and_profile.py \
             tests/test_broker_scanner_kafka.py tests/test_broker_scanner_rabbitmq.py \
             tests/test_broker_scanner_redis.py tests/test_broker_run_integration.py -x

    Then the actual end-to-end smoke:
      python run_scan.py \
        --profile standard \
        --kafka-host localhost:29093 \
        --rabbitmq-host localhost:25671 \
        --redis-host localhost:26380 \
        --output quirk-output/phase33-smoke.json

    Verify the resulting CBOM JSON contains at minimum:
      - 1 endpoint with kafka-plaintext-listener HIGH (port 29092 detected via lab profile)
      - 1 endpoint with amqp-plaintext-listener HIGH (port 25672)
      - 1 endpoint with redis-plaintext-no-auth HIGH (port 26379)
      - ≥2 weak-cipher HIGH findings across the TLS ports (29093 / 25671 / 26380)
      - broker_scan_json populated on the first endpoint per scan with kafka/rabbitmq/redis keys

    Use `jq` (or python json.tool) to count findings:
      jq '[.findings[] | select(.severity=="HIGH")] | length' quirk-output/phase33-smoke.json
  </action>
  <verify>
    <automated>python -m compileall quirk run_scan.py &amp;&amp; pytest -x tests/test_broker_db_schema.py tests/test_broker_config_and_profile.py tests/test_broker_scanner_kafka.py tests/test_broker_scanner_rabbitmq.py tests/test_broker_scanner_redis.py tests/test_broker_run_integration.py</automated>
  </verify>
  <done>
    All broker pytest files green; smoke JSON contains ≥3 plaintext HIGH + ≥2 weak-cipher HIGH;
    quirk-output/phase33-smoke.json committed to .gitignore-tracked output dir or attached to
    the phase note.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update docs/UAT-SERIES.md with UAT-33 series</name>
  <files>docs/UAT-SERIES.md</files>
  <action>
    Read current docs/UAT-SERIES.md. Append a new section "## UAT-33 — Broker Scanner"
    after the UAT-32 series, with at minimum these cases (numbering UAT-33-01..UAT-33-08):

      UAT-33-01: Broker scan disabled by default
        - Steps: `python run_scan.py --kafka-host localhost:9092` with default profile
        - Pass: scan completes, no broker_scan_json column populated, no broker findings

      UAT-33-02: Standard profile enables broker scan
        - Steps: `python run_scan.py --profile standard --kafka-host localhost:29093`
        - Pass: broker_scan_json populated; kafka key present

      UAT-33-03: Kafka plaintext detection
        - Steps: scan localhost:29092 from lab
        - Pass: kafka-plaintext-listener HIGH finding

      UAT-33-04: Kafka TLS weak cipher
        - Steps: scan localhost:29093 from lab
        - Pass: weak-cipher HIGH finding (TLS_RSA_WITH_AES_128_CBC_SHA)

      UAT-33-05: RabbitMQ AMQP plaintext detection
        - Steps: scan localhost:25672 with `--profile standard`
        - Pass: amqp-plaintext-listener HIGH finding (validates len(data) > 0 rule)

      UAT-33-06: RabbitMQ AMQPS weak cipher
        - Steps: scan localhost:25671
        - Pass: weak-cipher HIGH finding (DES-CBC3-SHA or AES128-SHA)

      UAT-33-07: Redis plaintext no-auth detection
        - Steps: scan localhost:26379
        - Pass: redis-plaintext-no-auth HIGH finding

      UAT-33-08: broker_scan_json persisted to DB
        - Steps: run scan, then `sqlite3 quirk.db "SELECT broker_scan_json FROM endpoint WHERE broker_scan_json IS NOT NULL LIMIT 1;"`
        - Pass: row returned with valid JSON containing kafka/rabbitmq/redis keys

    Bump the document header **Last Updated:** date to today (2026-04-27 or current).

    Mirror the email-scan UAT-32 entries in tone/structure.
  </action>
  <verify>
    <automated>grep -c "UAT-33-0" docs/UAT-SERIES.md | grep -E "^[8-9]|^1[0-9]"</automated>
  </verify>
  <done>
    docs/UAT-SERIES.md contains UAT-33-01 through UAT-33-08 minimum, Last Updated bumped.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write Obsidian phase note to vault filesystem</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-33-Broker-Scanner.md</files>
  <action>
    Use the Write tool (NOT obsidian CLI content=) to create the phase note at:
      /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-33-Broker-Scanner.md

    Frontmatter:
      ---
      project: QU.I.R.K.
      type: phase
      status: complete
      source: .planning/phases/33-broker-scanner/
      updated: 2026-04-27
      ---

    Body sections:
      # Phase 33 — Broker Scanner

      ## Goal
      (paste from ROADMAP.md Phase 33 goal)

      ## Requirements Covered
      (bulleted list of all 19 req IDs from this phase)

      ## Success Criteria
      (the 6 ROADMAP success criteria)

      ## What Was Built
      (one ### subsection per plan 33-01..33-07, sourced from each *-SUMMARY.md
       once execute-phase has produced them; if SUMMARYs not yet written, use the
       PLAN.md objective lines as placeholders flagged with TODO)

      ## Links
      - [[Roadmap]]
      - [[Phase-32-Email-Scanner]]
      - [[_QUIRK-Hub]]

    Then update the hub note _QUIRK-Hub.md to add a wikilink to [[Phase-33-Broker-Scanner]]
    in the Phases section.
  </action>
  <verify>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-33-Broker-Scanner.md" &amp;&amp; grep -q "status: complete" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-33-Broker-Scanner.md"</automated>
  </verify>
  <done>
    Phase note exists in vault with status: complete frontmatter, all 19 req IDs listed,
    6 success criteria, per-plan What Was Built sections; hub note links to phase 33.
  </done>
</task>

<task type="auto">
  <name>Task 4: Sync UAT-SERIES.md to vault filesystem</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md</files>
  <action>
    Per CLAUDE.md (file is too large for obsidian CLI content= parameter), use the
    filesystem cp pattern:

      printf "---\nproject: QU.I.R.K.\ntype: reference\nstatus: active\nsource: docs/UAT-SERIES.md\nupdated: 2026-04-27\n---\n\n" > /tmp/uat_vault.md
      cat docs/UAT-SERIES.md >> /tmp/uat_vault.md
      cp /tmp/uat_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"

    Note: this overwrites the vault copy. The vault copy is a mirror, not authoritative.
  </action>
  <verify>
    <automated>grep -c "UAT-33-0" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"</automated>
  </verify>
  <done>
    Vault UAT-Series.md contains UAT-33 entries matching docs/UAT-SERIES.md.
  </done>
</task>

<task type="auto">
  <name>Task 5: Update ROADMAP.md and PROJECT.md status</name>
  <files>.planning/ROADMAP.md, .planning/PROJECT.md</files>
  <action>
    In .planning/ROADMAP.md:
      - Change Phase 33 entry from `### Phase 33:` heading status to complete (mark `[x]`
        on the phase line and on each of the 6 success criteria checkboxes).
      - Update the milestone progress counter (e.g., "X of N phases complete").

    In .planning/PROJECT.md:
      - If a "Current Phase" or "Recent Phases" section exists, move Phase 33 into completed
        and surface Phase 34 as next (per ROADMAP order).

    Do not touch other phases.
  </action>
  <verify>
    <automated>grep -E "Phase 33.*\[x\]|33-broker-scanner.*complete" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    ROADMAP shows Phase 33 complete with all 6 success criteria checked; PROJECT.md
    reflects new current phase.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Human verification of Phase 33 closure</name>
  <what-built>
    End-to-end broker scanner pipeline (Plans 33-01..07) plus phase wrap (this plan):
    UAT-SERIES updated, vault synced, ROADMAP closed.
  </what-built>
  <how-to-verify>
    1. Open quirk-output/phase33-smoke.json and confirm visually:
       - kafka, rabbitmq, redis findings all present
       - broker_scan_json on first endpoint contains nested kafka/rabbitmq/redis keys
    2. Open Obsidian vault (Digs) → 20_Dev-Work/QUIRK/Phases/Phase-33-Broker-Scanner.md
       - Confirm frontmatter renders, status is complete
       - Confirm [[Roadmap]] backlink resolves
    3. Open Obsidian → 20_Dev-Work/QUIRK/UAT-Series.md
       - Confirm UAT-33 section is visible
    4. Visit ROADMAP.md and confirm Phase 33 row is checked.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual / linkage issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 7: Commit phase 33 docs + UAT update</name>
  <files>(commit only — no file edits)</files>
  <action>
    Per CLAUDE.md, commit docs/UAT-SERIES.md (and ROADMAP / PROJECT updates) using the
    GSD commit helper:

      node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit \
        "docs(phase-33): update UAT-SERIES.md, ROADMAP, PROJECT for broker scanner" \
        --files docs/UAT-SERIES.md .planning/ROADMAP.md .planning/PROJECT.md

    Vault files are NOT committed (vault is its own git repo or unversioned).
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "phase-33"</automated>
  </verify>
  <done>
    Single commit on QUIRK-v4 branch with subject containing phase-33 and updating the
    three repo files; working tree clean for those paths.
  </done>
</task>

</tasks>

<verification>
- All broker pytest suites green
- End-to-end smoke produces expected finding counts (≥3 plaintext HIGH + ≥2 weak-cipher HIGH)
- docs/UAT-SERIES.md updated and mirrored to vault
- Obsidian phase note exists with status: complete
- ROADMAP Phase 33 marked complete; commit landed
</verification>

<success_criteria>
Phase 33 is closed: documentation, vault, roadmap, and commit all reflect the shipped
broker scanner. The next phase (34) is ready to start.
</success_criteria>

<output>
Create `.planning/phases/33-broker-scanner/33-08-SUMMARY.md` capturing the smoke-run
finding counts, the UAT case IDs added, the vault paths written, and the commit SHA.
</output>
