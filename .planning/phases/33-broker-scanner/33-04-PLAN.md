---
phase: 33-broker-scanner
plan: 04
type: execute
wave: 3
depends_on: [33-03]
files_modified:
  - quirk/scanner/broker_scanner.py
  - tests/test_broker_scanner_rabbitmq.py
autonomous: true
requirements: [STRUCT-01, RABBIT-01, RABBIT-02, RABBIT-03, RABBIT-04, RABBIT-05]
must_haves:
  truths:
    - "scan_rabbitmq_targets() probes 5671 (AMQPS via sslyze), 5672 (AMQP plaintext via len(data)>0), 15672 (mgmt API)"
    - "scan_rabbitmq_targets() also probes Azure Service Bus and AWS SQS via sslyze when namespaces/regions supplied"
    - "Plaintext detection sends b'AMQP\\x00\\x00\\x09\\x01' and treats len(data)>0 as positive (NOT b'AMQP' prefix match)"
    - "Cloud probes set ep.protocol = 'AMQPS/Azure-ServiceBus' and 'HTTPS/AWS-SQS' per D-03/D-04"
    - "Management API uses urllib.request only — no requests dependency added (D-09)"
    - "401 from mgmt API returns {'mgmt_auth': 'rejected_401'} not an error (D-09)"
  artifacts:
    - path: "quirk/scanner/broker_scanner.py"
      provides: "RabbitMQ + Azure SB + AWS SQS probe functions appended"
      contains: "def scan_rabbitmq_targets"
    - path: "tests/test_broker_scanner_rabbitmq.py"
      provides: "RABBIT-01..05 + AMQP plaintext + cloud probe + mgmt API tests"
      min_lines: 200
  key_links:
    - from: "quirk/scanner/broker_scanner.py:scan_rabbitmq_targets"
      to: "_scan_one_sslyze_kafka (renamed/aliased)"
      via: "shared sslyze probe helper"
      pattern: "_scan_one_sslyze"
tags: [phase-33, broker-scanner, rabbitmq, amqp, azure-servicebus, aws-sqs]
---

<objective>
Append RabbitMQ + Azure Service Bus + AWS SQS scanning to `quirk/scanner/broker_scanner.py`. Covers RABBIT-01..05, including the AMQP-header plaintext probe (with the 2026-04-27 `len(data)>0` revision per CONTEXT.md), the stdlib management API enrichment, and the two cloud broker probes folded into the same function (per CONTEXT.md Specifics).

Purpose: Sit between Kafka (Plan 03) and Redis (Plan 05) — uses the same sslyze probe helper from Plan 03 for direct-TLS ports, adds AMQP header plaintext detection, adds urllib.request management API enrichment.

Output: Three new functions on `broker_scanner.py` (`_detect_amqp_plaintext`, `_enrich_rabbitmq_mgmt`, `_scan_one_sslyze_amqps` if not aliased, plus public `scan_one_rabbitmq` and `scan_rabbitmq_targets`) and a focused test file proving the five requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-broker-scanner/33-CONTEXT.md
@.planning/phases/33-broker-scanner/33-RESEARCH.md
@.planning/phases/33-broker-scanner/33-PATTERNS.md
@.planning/phases/33-broker-scanner/33-VALIDATION.md
@CLAUDE.md
@quirk/scanner/broker_scanner.py
@quirk/scanner/email_scanner.py

<interfaces>
Public functions added in this plan:

def _detect_amqp_plaintext(host: str, port: int, timeout: int = 2) -> bool
    # RABBIT-02. Sends AMQP_HEADER and returns len(data) > 0 (CONTEXT.md 2026-04-27 revision).

def _enrich_rabbitmq_mgmt(host: str, port: int = 15672, logger=None) -> dict
    # RABBIT-03. urllib.request GET /api/overview with Basic guest:guest. Returns rabbitmq_version,
    # erlang_version, listeners, node — or {"mgmt_auth": "rejected_401"} on 401 (D-09 informational).

def scan_one_rabbitmq(host, port, timeout, *, protocol_label="AMQPS", logger=None, session_start=None) -> Optional[CryptoEndpoint]
    # protocol_label: "AMQPS" (5671), "AMQPS/Azure-ServiceBus", or "HTTPS/AWS-SQS"
    # Port 5672 -> plaintext detection emitting protocol="AMQP-PLAIN".

def scan_rabbitmq_targets(
    hosts: List[str],
    azure_namespaces: List[str] = None,
    sqs_regions: List[str] = None,
    timeout: int = 5,
    logger=None,
    session_start: Optional[datetime] = None,
) -> List[CryptoEndpoint]
    # RABBIT-01..05. self-hosted (5671/5672/15672) + Azure SB (5671) + AWS SQS (443).

Endpoint protocol labels emitted (consumed by Phase 35 CBOM):
- "AMQPS"                      for self-hosted port 5671
- "AMQP-PLAIN"                 for self-hosted port 5672 plaintext finding-bearing endpoint
- "AMQPS/Azure-ServiceBus"     for {namespace}.servicebus.windows.net:5671
- "HTTPS/AWS-SQS"              for sqs.{region}.amazonaws.com:443
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _detect_amqp_plaintext + _enrich_rabbitmq_mgmt + sslyze probe alias</name>
  <files>quirk/scanner/broker_scanner.py</files>
  <read_first>
    - quirk/scanner/broker_scanner.py (Plan 03 output — find AMQP_HEADER constant and existing _scan_one_sslyze_kafka)
    - .planning/phases/33-broker-scanner/33-RESEARCH.md Pattern 3 (AMQP plaintext) and Pattern 8 (mgmt API)
    - .planning/phases/33-broker-scanner/33-CONTEXT.md (2026-04-27 revision: len(data)>0)
  </read_first>
  <behavior>
    - `_detect_amqp_plaintext` sends `AMQP_HEADER` and returns True iff `len(data) > 0` after recv with 2-second timeout.
    - `_enrich_rabbitmq_mgmt` performs GET /api/overview with Basic guest:guest using urllib.request only.
    - 401 response returns `{"mgmt_auth": "rejected_401"}` not an exception.
    - Connection failures return `{}` and log at DEBUG (D-09).
    - sslyze probe is reused — either rename `_scan_one_sslyze_kafka` to `_scan_one_sslyze_broker` and alias OR add a thin wrapper. Recommendation: rename to `_scan_one_sslyze_broker` (it is protocol-agnostic) and update Plan 03's call sites. The function returns a generic `CryptoEndpoint` whose protocol is set by the caller.
  </behavior>
  <action>
1. Refactor `_scan_one_sslyze_kafka` to a protocol-agnostic helper. Rename to `_scan_one_sslyze_broker(host, port, timeout, logger=None) -> Optional[CryptoEndpoint]`. The function now returns an endpoint with `protocol=None` (or a placeholder) — callers set the final `ep.protocol` before returning. Update `scan_one_kafka` (Plan 03 code) to set `ep.protocol = "KAFKA-TLS"` after calling the helper. This is a minimal-diff refactor: rename + add a placeholder protocol. Run the Plan 03 tests to confirm green after rename.

2. Append `_detect_amqp_plaintext` (RESEARCH.md Pattern 3, with the 2026-04-27 revision):

       def _detect_amqp_plaintext(host: str, port: int, timeout: int = 2) -> bool:
           """RABBIT-02. Send AMQP 0-9-1 header; len(data) > 0 = AMQP speaker (CONTEXT.md 2026-04-27).

           NOTE: original spec said b'AMQP' prefix match — that yields false negatives because
           the AMQP 0-9-1 Connection.Start response is a binary METHOD frame, not ASCII-prefixed.
           """
           try:
               with socket.create_connection((host, port), timeout=timeout) as sock:
                   sock.sendall(AMQP_HEADER)
                   sock.settimeout(timeout)
                   data = sock.recv(256)
                   return len(data) > 0
           except (ConnectionRefusedError, OSError, socket.timeout):
               return False

3. Append `_enrich_rabbitmq_mgmt` (RESEARCH.md Pattern 8 verbatim):

       def _enrich_rabbitmq_mgmt(host: str, port: int = 15672, logger=None) -> dict:
           """RABBIT-03 / D-09. urllib.request GET /api/overview with Basic guest:guest.

           Returns {} on connection failure or non-401 HTTP error.
           Returns {"mgmt_auth": "rejected_401"} on 401 (informational data point, NOT an error).
           No `requests` dependency (D-09).
           """
           url = f"http://{host}:{port}/api/overview"
           credentials = base64.b64encode(b"guest:guest").decode()
           req = urllib.request.Request(url, headers={"Authorization": f"Basic {credentials}"})
           try:
               with urllib.request.urlopen(req, timeout=5) as resp:
                   data = json.loads(resp.read())
                   return {
                       "rabbitmq_version": data.get("rabbitmq_version"),
                       "erlang_version": data.get("erlang_version"),
                       "listeners": data.get("listeners", []),
                       "node": data.get("node"),
                   }
           except urllib.error.HTTPError as e:
               if e.code == 401:
                   if logger:
                       logger.debug(f"RabbitMQ mgmt API guest:guest rejected on {host}:{port} (401 — informational)")
                   return {"mgmt_auth": "rejected_401"}
               if logger:
                   logger.debug(f"RabbitMQ mgmt API HTTP error {e.code} on {host}:{port}")
               return {}
           except Exception as e:
               if logger:
                   logger.debug(f"RabbitMQ mgmt API failed on {host}:{port}: {e}")
               return {}

4. `python -m compileall quirk/scanner/broker_scanner.py` exits 0; `python -m pytest tests/test_broker_scanner_kafka.py -v` still green after the rename refactor.
  </action>
  <verify>
    <automated>python -m compileall quirk/scanner/broker_scanner.py 2>&1 | grep -E "Error|SyntaxError"; test $? -ne 0 && python -m pytest tests/test_broker_scanner_kafka.py -v --no-header 2>&1 | tail -5</automated>
  </verify>
  <done>Plan 03 tests still green; broker_scanner.py exposes _detect_amqp_plaintext, _enrich_rabbitmq_mgmt, _scan_one_sslyze_broker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add scan_one_rabbitmq + scan_rabbitmq_targets with cloud-target probes</name>
  <files>quirk/scanner/broker_scanner.py</files>
  <read_first>
    - quirk/scanner/broker_scanner.py (current state after Task 1)
    - .planning/phases/33-broker-scanner/33-CONTEXT.md D-03/D-04 (cloud probe protocol labels)
    - .planning/phases/33-broker-scanner/33-RESEARCH.md "Architecture Patterns" diagram
  </read_first>
  <behavior>
    - `scan_one_rabbitmq(host, 5672, timeout)` returns ep.protocol == "AMQP-PLAIN" when AMQP detection true.
    - `scan_one_rabbitmq(host, 5671, timeout, protocol_label="AMQPS")` returns ep.protocol == "AMQPS" with TLS metadata.
    - `scan_one_rabbitmq("ns.servicebus.windows.net", 5671, timeout, protocol_label="AMQPS/Azure-ServiceBus")` returns ep.protocol == "AMQPS/Azure-ServiceBus".
    - `scan_rabbitmq_targets(hosts=[h], azure_namespaces=["ns"], sqs_regions=["us-east-1"], ...)` produces endpoints for: 5672 (host), 5671 (host), 5671 (ns.servicebus.windows.net), 443 (sqs.us-east-1.amazonaws.com), and (best-effort) 15672 mgmt enrichment attached to the 5671 endpoint as `_rabbit_mgmt_enrichment` attribute.
    - Empty `azure_namespaces` / `sqs_regions` lists produce zero cloud endpoints.
  </behavior>
  <action>
Append:

    def scan_one_rabbitmq(
        host: str,
        port: int,
        timeout: int,
        *,
        protocol_label: str = "AMQPS",
        logger: Optional[Logger] = None,
        session_start: Optional[datetime] = None,
    ) -> Optional[CryptoEndpoint]:
        """Probe a single RabbitMQ-family endpoint.

        Port 5672 -> AMQP plaintext detection (RABBIT-02).
        Other ports -> sslyze direct-TLS probe with caller-supplied protocol_label.
        protocol_label values: "AMQPS" (5671 self-hosted), "AMQPS/Azure-ServiceBus" (5671 cloud),
        "HTTPS/AWS-SQS" (443 cloud).
        """
        if port == 5672:
            if _detect_amqp_plaintext(host, port):
                ep = CryptoEndpoint(host=host, port=port, protocol="AMQP-PLAIN")
                ep.service_detail = f"AMQP-PLAIN:{port}"
                ep.scanned_at = (session_start or datetime.now(timezone.utc)).replace(tzinfo=None)
                return ep
            return None

        ep = _scan_one_sslyze_broker(host, port, timeout, logger)
        if ep is None:
            return None
        ep.protocol = protocol_label
        ep.service_detail = f"{protocol_label}:{port}"
        ep.scanned_at = (session_start or datetime.now(timezone.utc)).replace(tzinfo=None)
        return ep


    def scan_rabbitmq_targets(
        hosts: List[str],
        azure_namespaces: Optional[List[str]] = None,
        sqs_regions: Optional[List[str]] = None,
        timeout: int = 5,
        logger: Optional[Logger] = None,
        session_start: Optional[datetime] = None,
    ) -> List[CryptoEndpoint]:
        """RABBIT-01..05. Probe self-hosted RabbitMQ + Azure SB + AWS SQS in parallel."""
        results: List[CryptoEndpoint] = []
        azure_namespaces = azure_namespaces or []
        sqs_regions = sqs_regions or []

        # Self-hosted: 5671 (AMQPS), 5672 (AMQP plaintext)
        self_tasks = [(h, 5672, "AMQPS") for h in hosts] + [(h, 5671, "AMQPS") for h in hosts]
        # Azure SB: {ns}.servicebus.windows.net:5671 (D-03)
        azure_tasks = [(f"{ns}.servicebus.windows.net", 5671, "AMQPS/Azure-ServiceBus") for ns in azure_namespaces]
        # AWS SQS: sqs.{region}.amazonaws.com:443 (D-04)
        sqs_tasks = [(f"sqs.{r}.amazonaws.com", 443, "HTTPS/AWS-SQS") for r in sqs_regions]
        all_tasks = self_tasks + azure_tasks + sqs_tasks
        if not all_tasks:
            return results
        if logger:
            logger.stamp(f"Starting RabbitMQ scans: {len(all_tasks)} probes "
                         f"(self={len(self_tasks)} azure={len(azure_tasks)} sqs={len(sqs_tasks)})")
        with ThreadPoolExecutor(max_workers=min(len(all_tasks), 50)) as ex:
            futs = {
                ex.submit(scan_one_rabbitmq, host, port, timeout,
                          protocol_label=label, logger=logger, session_start=session_start): (host, port)
                for host, port, label in all_tasks
            }
            for f in as_completed(futs):
                ep = f.result()
                if ep is not None:
                    results.append(ep)

        # RABBIT-03: management API enrichment per self-hosted host (best-effort, attaches to first AMQPS ep for that host)
        for host in hosts:
            mgmt = _enrich_rabbitmq_mgmt(host, port=15672, logger=logger)
            if mgmt:
                # Attach to the first AMQPS endpoint for this host so Plan 06 can fold it into broker_scan_json
                for ep in results:
                    if ep.host == host and ep.protocol == "AMQPS":
                        setattr(ep, "_rabbit_mgmt_enrichment", mgmt)
                        break
        if logger:
            logger.stamp(f"RabbitMQ scans complete: {len(results)} endpoints")
        return results

`python -m compileall quirk/scanner/broker_scanner.py` exits 0.
  </action>
  <verify>
    <automated>python -m compileall quirk/scanner/broker_scanner.py 2>&1 | grep -E "Error|SyntaxError"; test $? -ne 0 && python -c "from quirk.scanner.broker_scanner import scan_rabbitmq_targets, scan_one_rabbitmq; print('OK')"</automated>
  </verify>
  <done>scan_rabbitmq_targets exposed; cloud-target hostnames constructed correctly; module compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tests/test_broker_scanner_rabbitmq.py — RABBIT-01..05 + AMQP detection + mgmt API tests</name>
  <files>tests/test_broker_scanner_rabbitmq.py</files>
  <read_first>
    - tests/test_broker_scanner_kafka.py (mock helpers; pytest.importorskip pattern)
    - .planning/phases/33-broker-scanner/33-RESEARCH.md Pattern 3, 8
  </read_first>
  <behavior>
    - Test 1 (RABBIT-01): mocked SslyzeScanner -> scan_one_rabbitmq(host, 5671) returns ep.protocol == "AMQPS".
    - Test 2 (RABBIT-02 positive): patch socket.create_connection so recv returns binary METHOD frame (e.g. b"\\x01\\x00\\x00\\x00\\x...") -> _detect_amqp_plaintext True.
    - Test 3 (RABBIT-02 empty bytes negative): recv returns b"" -> _detect_amqp_plaintext False (proves len(data)>0 logic, not b'AMQP' prefix).
    - Test 4 (RABBIT-02 ConnectionRefused negative): create_connection raises ConnectionRefusedError -> False.
    - Test 5 (RABBIT-02 plaintext endpoint emission): scan_one_rabbitmq(host, 5672) with detection True -> ep.protocol == "AMQP-PLAIN", ep.service_detail == "AMQP-PLAIN:5672".
    - Test 6 (RABBIT-03 success): mock urllib.request.urlopen returning JSON with rabbitmq_version+listeners; _enrich_rabbitmq_mgmt returns dict with those keys.
    - Test 7 (RABBIT-03 401): urlopen raises HTTPError(code=401); returns {"mgmt_auth": "rejected_401"}.
    - Test 8 (RABBIT-03 connection refused): urlopen raises URLError; returns {}.
    - Test 9 (D-09 no requests dep): grep that broker_scanner.py does NOT import the `requests` package (`grep -E "^import requests|^from requests" quirk/scanner/broker_scanner.py` returns no match).
    - Test 10 (RABBIT-04 azure SB hostname): patch scan_one_rabbitmq to capture args; call scan_rabbitmq_targets(hosts=[], azure_namespaces=["my-ns"]); assert one call had host="my-ns.servicebus.windows.net", port=5671, protocol_label="AMQPS/Azure-ServiceBus".
    - Test 11 (RABBIT-05 sqs hostname): same pattern; sqs_regions=["us-east-1"]; one call had host="sqs.us-east-1.amazonaws.com", port=443, protocol_label="HTTPS/AWS-SQS".
    - Test 12 (RABBIT-04 empty list yields zero cloud probes): azure_namespaces=[] -> no Azure-tagged endpoints.
    - Test 13 (mgmt enrichment attachment): patch scan_one_rabbitmq to return AMQPS ep; patch _enrich_rabbitmq_mgmt to return {"rabbitmq_version":"3.12"}; call scan_rabbitmq_targets; assert returned ep has `_rabbit_mgmt_enrichment` attribute set.
    - Test 14 (STRUCT-01): scan_one_rabbitmq(..., session_start=fixed_time) sets ep.scanned_at == fixed_time.
  </behavior>
  <action>
Create tests/test_broker_scanner_rabbitmq.py mirroring tests/test_broker_scanner_kafka.py structure.

Mock pattern for Test 6/7/8 (urllib.request):

    @patch("quirk.scanner.broker_scanner.urllib.request.urlopen")
    def test_enrich_rabbitmq_mgmt_success(mock_urlopen):
        mock_resp = MagicMock()
        mock_resp.read.return_value = json.dumps({
            "rabbitmq_version": "3.12.0",
            "erlang_version": "25.0",
            "listeners": [{"protocol": "amqp", "port": 5672}],
            "node": "rabbit@node1",
        }).encode()
        mock_urlopen.return_value.__enter__.return_value = mock_resp
        result = _enrich_rabbitmq_mgmt("host.example.com", 15672)
        assert result["rabbitmq_version"] == "3.12.0"

    @patch("quirk.scanner.broker_scanner.urllib.request.urlopen")
    def test_enrich_rabbitmq_mgmt_401(mock_urlopen):
        import urllib.error
        mock_urlopen.side_effect = urllib.error.HTTPError(
            url="x", code=401, msg="Unauthorized", hdrs=None, fp=None
        )
        result = _enrich_rabbitmq_mgmt("host.example.com")
        assert result == {"mgmt_auth": "rejected_401"}

Cloud-target tests use `@patch("quirk.scanner.broker_scanner.scan_one_rabbitmq")` to capture call args:

    @patch("quirk.scanner.broker_scanner.scan_one_rabbitmq")
    def test_azure_servicebus_hostname_construction(mock_scan_one):
        mock_scan_one.return_value = None
        scan_rabbitmq_targets(hosts=[], azure_namespaces=["my-ns"], sqs_regions=[])
        # Check that one call was made with the constructed Azure SB hostname
        calls = [c for c in mock_scan_one.call_args_list]
        azure_calls = [c for c in calls if c.kwargs.get("protocol_label") == "AMQPS/Azure-ServiceBus"]
        assert len(azure_calls) == 1
        assert azure_calls[0].args[0] == "my-ns.servicebus.windows.net"
        assert azure_calls[0].args[1] == 5671

D-09 source check (no `import requests`):

    def test_no_requests_dependency():
        import inspect, quirk.scanner.broker_scanner as mod
        src = inspect.getsource(mod)
        for line in src.splitlines():
            stripped = line.lstrip()
            if stripped.startswith("#"):
                continue
            assert not stripped.startswith("import requests"), "D-09: requests dep prohibited"
            assert not stripped.startswith("from requests"), "D-09: requests dep prohibited"

Run `python -m pytest tests/test_broker_scanner_rabbitmq.py -v` — all 14 tests green.
  </action>
  <verify>
    <automated>python -m pytest tests/test_broker_scanner_rabbitmq.py -v --no-header 2>&1 | tail -20</automated>
  </verify>
  <done>14 tests green covering RABBIT-01..05, AMQP detection (with len(data)>0 rule), mgmt API + 401 path, cloud probe hostname construction, D-09 source check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RabbitMQ mgmt API (untrusted) -> scanner | scanner sends Basic guest:guest credentials over plaintext HTTP on port 15672 |
| AMQP broker (untrusted) -> scanner | sends 8-byte AMQP header; reads up to 256 bytes of binary response |
| Azure SB / AWS SQS endpoints | TLS-only via sslyze; SNI = hostname |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-08 | I (Information disclosure) | guest:guest credential transmitted to mgmt API over HTTP | accept | Default RabbitMQ credentials are well-known; sending them over HTTP is the documented detection method (RABBIT-03 / D-09); CONTEXT.md D-11 footprint accepted |
| T-33-09 | T (Tampering) | AMQP header byte sequence sent to non-AMQP services | accept | Sent over TCP only when targeting port 5672; conservative `len(data)>0` detection avoids false positives via port specificity |
| T-33-10 | D (Denial of service) | urllib.request hang on slow mgmt API | mitigate | `timeout=5` on urlopen call |
| T-33-11 | E (Elevation of privilege) | namespace/region strings interpolated into hostnames | mitigate | strings only used in hostname construction; sslyze rejects malformed hostnames at connectivity test |
</threat_model>

<verification>
- `python -m compileall quirk/scanner/broker_scanner.py` exits 0.
- `python -m pytest tests/test_broker_scanner_kafka.py tests/test_broker_scanner_rabbitmq.py -v` reports all green / 0 failing.
- `grep -v '^#' quirk/scanner/broker_scanner.py | grep -cE "^import requests|^from requests"` returns 0 (D-09).
- `grep -v '^#' quirk/scanner/broker_scanner.py | grep -c "len(data) > 0"` returns ≥1 (the AMQP detection literal).
</verification>

<success_criteria>
RABBIT-01..05 satisfied. Cloud probes produce endpoints with the exact protocol labels Phase 35 CBOM expects. Plan 03 Kafka tests still green after the helper rename.
</success_criteria>

<output>
After completion, create `.planning/phases/33-broker-scanner/33-04-SUMMARY.md` documenting whether the sslyze helper was renamed or aliased, and the AMQP detection rule (`len(data)>0`) decision per CONTEXT.md 2026-04-27 revision.
</output>
