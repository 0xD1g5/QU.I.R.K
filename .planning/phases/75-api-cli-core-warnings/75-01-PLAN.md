---
phase: 75-api-cli-core-warnings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/cli/doctor_cmd.py
  - quirk/dashboard/api/deps.py
  - tests/test_doctor_actionable.py
  - .planning/audit-2026-05-08/AUDIT-TASKS.md
autonomous: true
requirements: [APCL-01]
must_haves:
  truths:
    - "`quirk/cli/doctor_cmd.py::_check_dashboard` and `_check_network` return typed status dicts `{\"ok\": bool, \"detail\": str, \"remediation\": str}` populated by real probes (HTTP HEAD against dashboard, DNS lookup for network) per D-01 (WR-01)"
    - "`quirk/cli/doctor_cmd.py::_check_db` reads `os.environ.get(\"QUIRK_DB_PATH\")` first; validates path exists+readable; falls back to `_default_db_path()` only when env is unset per D-02 (WR-02)"
    - "`quirk/dashboard/api/deps.py::_default_db_path` replaces mtime-newest-wins with single canonical resolution (`./quirk-output/quirk.db` per RESEARCH A1 / Phase 74 D-05 precedent); raises `ValueError(f\"Multiple QU.I.R.K. DBs found at {paths}; set QUIRK_DB_PATH explicitly\")` when multiple legacy DBs are present per D-03 (WR-03)"
    - "`tests/test_doctor_actionable.py` exists with >= 6 test functions covering typed-dict shape, QUIRK_DB_PATH precedence, fail-loud multi-DB ValueError, and single-DB-OK happy path"
    - "Audit rows api-cli-core/WR-01, WR-02, WR-03 flipped to `Phase 75 | [x] closed`"
  artifacts:
    - path: "quirk/cli/doctor_cmd.py"
      provides: "Typed dict doctor checks (D-01) + QUIRK_DB_PATH-aware _check_db (D-02)"
      contains: "remediation"
    - path: "quirk/dashboard/api/deps.py"
      provides: "Fail-loud _default_db_path single-canonical resolution (D-03)"
      contains: "set QUIRK_DB_PATH explicitly"
    - path: "tests/test_doctor_actionable.py"
      provides: "RED-then-GREEN coverage of D-01..D-03"
      contains: "def test_"
  key_links:
    - from: "quirk/cli/doctor_cmd.py::_check_dashboard"
      to: "typed status dict"
      via: "return {'ok': bool, 'detail': str, 'remediation': str}"
      pattern: "remediation"
    - from: "quirk/cli/doctor_cmd.py::_check_db"
      to: "QUIRK_DB_PATH env"
      via: "os.environ.get('QUIRK_DB_PATH')"
      pattern: "QUIRK_DB_PATH"
    - from: "quirk/dashboard/api/deps.py::_default_db_path"
      to: "ValueError on multi-DB"
      via: "raise ValueError(...)"
      pattern: "Multiple QU\\.I\\.R\\.K\\. DBs found"
---

<objective>
Close APCL-01 (WR-01, WR-02, WR-03) with surgical fixes in `quirk/cli/doctor_cmd.py` and `quirk/dashboard/api/deps.py`, plus a new RED-then-GREEN test module. Per RESEARCH C-1, CONTEXT's `quirk/cli/doctor.py` is shorthand for the actual file `quirk/cli/doctor_cmd.py` — all edits land there.

1. **WR-01 (D-01)** — `_check_dashboard` (line 118) and `_check_network` (line 108) currently return `Tuple[bool, str]`. Replace with typed status dict `{"ok": bool, "detail": str, "remediation": str}`. `_check_dashboard` performs an HTTP HEAD against the dashboard endpoint; `_check_network` performs a DNS lookup. On failure, `detail` describes the symptom and `remediation` gives the next-step action string. Update `run_doctor()` table renderer (line 128-176) to consume the new dict shape.

2. **WR-02 (D-02)** — `_check_db` (line 77) currently uses `_DB_DEFAULT_PATH = "./quirk.db"` module constant. Read `os.environ.get("QUIRK_DB_PATH")` first; if set, validate path exists and is readable; if unset, fall back to `_default_db_path()` (the shared helper in `deps.py`, see D-03). Return the typed dict shape from D-01.

3. **WR-03 (D-03)** — `quirk/dashboard/api/deps.py::_default_db_path` (lines 12-26) currently picks the mtime-newest file from a search list. Replace with single canonical path resolution (`./quirk-output/quirk.db` per RESEARCH A1, matching Phase 74 D-05 + `interactive.py:180` precedent). If multiple legacy DB files are discovered, raise `ValueError(f"Multiple QU.I.R.K. DBs found at {paths}; set QUIRK_DB_PATH explicitly")` — fail loud, never silently pick. Mirrors Phase 71 D-06 fail-loud pattern.

Then flip audit rows WR-01 (line 186), WR-02 (line 187), WR-03 (line 188) of `AUDIT-TASKS.md` to `Phase 75 | [x] closed`.

Purpose: Doctor output becomes actionable (operators see remediation strings, not bare True/False), DB resolution is deterministic across `quirk doctor` and `quirk dashboard`, and a duplicate-DB state surfaces an explicit error instead of silently picking the wrong one.
Output: Edits in 2 source files, 1 new test module, 3 audit rows flipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/75-api-cli-core-warnings/75-CONTEXT.md
@.planning/phases/75-api-cli-core-warnings/75-RESEARCH.md
@.planning/audit-2026-05-08/AUDIT-TASKS.md

<interfaces>
Existing surfaces (verified at HEAD 0f290e6):

quirk/cli/doctor_cmd.py:
- `_DB_DEFAULT_PATH = "./quirk.db"` module constant at line 25 (to be removed/replaced per D-02)
- `_check_db(db_path)` at line 77 (D-02 fix site)
- `_check_network()` at line 108 (D-01 fix site; currently `Tuple[bool, str]`)
- `_check_dashboard()` at line 118 (D-01 fix site; currently `Tuple[bool, str]`)
- `run_doctor()` at line 128-176 (consumer; must adapt to new dict return shape)

quirk/dashboard/api/deps.py:
- `_default_db_path()` at lines 12-26 (D-03 fix site; mtime-newest-wins → fail-loud single canonical)

Target dict shape (D-01):
- {"ok": True,  "detail": "Dashboard reachable at http://127.0.0.1:8000", "remediation": ""}
- {"ok": False, "detail": "DNS lookup failed for example.com", "remediation": "Check /etc/resolv.conf or run `quirk config --dns ...`"}

Target resolution rule (D-03):
- Canonical: `./quirk-output/quirk.db` (RESEARCH A1 — matches Phase 74 D-05 + `interactive.py:180`)
- Legacy search dirs (if any): if exactly one exists, return it; if multiple, raise ValueError; if none, return canonical (caller decides whether to create it)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing tests for D-01..D-03 in tests/test_doctor_actionable.py</name>
  <files>tests/test_doctor_actionable.py</files>
  <read_first>
    - quirk/cli/doctor_cmd.py (full file)
    - quirk/dashboard/api/deps.py lines 1-50
    - tests/test_qramm_practice_scoring.py (Phase 74 RED idiom reference)
    - .planning/phases/75-api-cli-core-warnings/75-CONTEXT.md (D-01..D-03)
    - .planning/phases/75-api-cli-core-warnings/75-RESEARCH.md (C-1, Pitfall 4)
  </read_first>
  <behavior>
    - D-01 (WR-01) — `_check_dashboard()` returns a dict with keys `{"ok", "detail", "remediation"}`; `ok` is `bool`; both string fields are `str`
    - D-01 — `_check_network()` returns the same dict shape
    - D-01 — When the dashboard endpoint is unreachable (mock HTTP HEAD to fail), `ok is False` and `remediation` is non-empty
    - D-02 (WR-02) — Set `os.environ["QUIRK_DB_PATH"]` to a tempfile path; `_check_db()` reports `ok=True` with that path; clearing env falls back to `_default_db_path()`
    - D-02 — Set `os.environ["QUIRK_DB_PATH"]` to a nonexistent path; `_check_db()` reports `ok=False` with remediation mentioning `QUIRK_DB_PATH`
    - D-03 (WR-03) — `_default_db_path()` with a single canonical DB present returns that path
    - D-03 — `_default_db_path()` with multiple legacy DB files in search dirs raises `ValueError` matching `r"Multiple QU\.I\.R\.K\. DBs found.*set QUIRK_DB_PATH explicitly"`
    - D-03 — `_default_db_path()` with no DBs returns the canonical path (`./quirk-output/quirk.db`) without raising
  </behavior>
  <action>
    Create `tests/test_doctor_actionable.py`. Use `pytest.monkeypatch` for env var manipulation, `tmp_path` fixture for filesystem isolation, and `unittest.mock.patch` for the dashboard HTTP HEAD probe + DNS lookup.

    Import surfaces: `from quirk.cli.doctor_cmd import _check_db, _check_dashboard, _check_network` and `from quirk.dashboard.api.deps import _default_db_path`.

    For D-01 typed-dict tests: assert `isinstance(result, dict)`, `set(result.keys()) == {"ok", "detail", "remediation"}`, `isinstance(result["ok"], bool)`. Patch the dashboard probe (likely `urllib.request.urlopen` or `requests.head` — discover via read_first) to raise `URLError` and assert `ok is False`, `remediation` is non-empty.

    For D-02 tests: parametrize `(env_value, expected_ok)` over `(valid_tempfile, True)`, `("/nonexistent/path/quirk.db", False)`, `(None, fallback_default_behavior)`. Use `monkeypatch.setenv` / `monkeypatch.delenv`.

    For D-03 tests: use `tmp_path` to construct fake legacy directories with 0/1/2 DB files; use `monkeypatch.chdir(tmp_path)` so the canonical `./quirk-output/quirk.db` resolves under the tempdir. Use `pytest.raises(ValueError, match=r"Multiple QU\.I\.R\.K\. DBs found.*set QUIRK_DB_PATH explicitly")` for the multi-DB case.

    This RED phase MUST fail because the production code still returns `Tuple[bool, str]` from `_check_dashboard` / `_check_network` and still uses mtime-newest-wins in `_default_db_path`. Run `pytest tests/test_doctor_actionable.py -x` and confirm failure.

    Commit message: `test(75-01): add failing tests for APCL-01 (D-01 typed dict, D-02 QUIRK_DB_PATH, D-03 fail-loud)`.

    PEP 8.
  </action>
  <verify>
    <automated>pytest tests/test_doctor_actionable.py -x 2>&1 | grep -E "FAILED|ERROR|AssertionError" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_doctor_actionable.py` exists
    - `grep -cE "^def test_" tests/test_doctor_actionable.py` >= 6
    - `grep -cE "remediation" tests/test_doctor_actionable.py` >= 2
    - `grep -cE "QUIRK_DB_PATH" tests/test_doctor_actionable.py` >= 2
    - `grep -cE "Multiple QU\\.I\\.R\\.K\\. DBs" tests/test_doctor_actionable.py` >= 1
    - `pytest tests/test_doctor_actionable.py -x` exits NON-ZERO (RED state)
  </acceptance_criteria>
  <done>
    Failing tests committed; pytest exits non-zero with assertion / shape errors tied to D-01..D-03.
  </done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement D-01..D-03 in doctor_cmd.py + deps.py</name>
  <files>quirk/cli/doctor_cmd.py, quirk/dashboard/api/deps.py</files>
  <read_first>
    - quirk/cli/doctor_cmd.py (full file)
    - quirk/dashboard/api/deps.py lines 1-50
    - quirk/interactive.py lines 175-200 (canonical path precedent — `./quirk-output/quirk.db` per RESEARCH A1)
    - tests/test_doctor_actionable.py (post-Task 1 — confirm expected probe sites and dict keys)
  </read_first>
  <action>
    Apply three surgical edits per `<interfaces>` target shapes.

    **deps.py edits (D-03 — land FIRST, since doctor_cmd.py will call it per D-02):**
    1. Replace the body of `_default_db_path()` (lines 12-26). Logic:
       - Canonical path: `Path("./quirk-output/quirk.db").resolve()` (per RESEARCH A1).
       - Legacy search list: keep the existing search-dir list but invert the rule. Collect all DB files that actually exist.
       - If `len(found) > 1`: raise `ValueError(f"Multiple QU.I.R.K. DBs found at {sorted(found)}; set QUIRK_DB_PATH explicitly")`.
       - If `len(found) == 1` and that path differs from canonical: return that path (legacy compatibility).
       - Otherwise: return canonical.
       - NO mtime sort; NO silent pick.

    **doctor_cmd.py edits (D-01 + D-02):**
    2. (D-01) Replace `_check_dashboard()` body. Probe the dashboard endpoint via stdlib `urllib.request.Request(..., method="HEAD")` + `urllib.request.urlopen(req, timeout=2)`. On 2xx return `{"ok": True, "detail": f"Dashboard reachable at {url}", "remediation": ""}`. On `URLError`/`HTTPError`/`socket.timeout` return `{"ok": False, "detail": f"Dashboard unreachable: {safe_str(e)}", "remediation": "Start the dashboard with `quirk dashboard up` and retry."}`.

       Replace `_check_network()` body. Probe via stdlib `socket.gethostbyname("example.com")` (or whatever benign external host the existing code already uses — read_first verifies). On success return `{"ok": True, "detail": "DNS lookup succeeded", "remediation": ""}`. On `socket.gaierror` return `{"ok": False, "detail": f"DNS lookup failed: {safe_str(e)}", "remediation": "Verify /etc/resolv.conf or run `quirk config --dns ...`."}`.

       Import `from quirk.util.safe_exc import safe_str` at top-of-file (Phase 59 helper; D-08, D-09 also route through it).

    3. (D-02) Replace `_check_db` body. Logic:
       - `env_path = os.environ.get("QUIRK_DB_PATH")`
       - If `env_path` is set: validate `os.path.exists(env_path)` and `os.access(env_path, os.R_OK)`. On failure return `{"ok": False, "detail": f"QUIRK_DB_PATH={env_path!r} not readable", "remediation": "Unset QUIRK_DB_PATH or point it at a readable file."}`.
       - If `env_path` is unset: call `_default_db_path()` (import from `quirk.dashboard.api.deps`). If `_default_db_path()` raises `ValueError` (multi-DB case), catch it and return `{"ok": False, "detail": safe_str(ve), "remediation": "Set QUIRK_DB_PATH to disambiguate."}`.
       - On success: `{"ok": True, "detail": f"DB at {resolved_path}", "remediation": ""}`.

    4. Update `run_doctor()` table renderer (lines 128-176): replace any `ok, detail = ...` unpacking with `result = _check_*(); ok = result["ok"]; detail = result["detail"]; remediation = result["remediation"]`. Render `remediation` as an extra column or appended line on failure rows. Per D-18, the `quirk doctor` EXIT-CODE semantics from Phase 52 DOCS-05 MUST NOT change — only the `_check_*` content + table cells.

    Per D-18 do-not-touch: do NOT alter exit-code semantics, do NOT add a JSON output format (deferred), do NOT touch QRAMM taxonomy.

    PEP 8. Minimal diff.

    Commit message: `feat(75-01): implement APCL-01 fixes (D-01 typed dicts, D-02 QUIRK_DB_PATH, D-03 fail-loud)`.
  </action>
  <verify>
    <automated>python -m compileall quirk/cli/doctor_cmd.py quirk/dashboard/api/deps.py && pytest tests/test_doctor_actionable.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -m compileall quirk/cli/doctor_cmd.py quirk/dashboard/api/deps.py` exits 0
    - `grep -cE "remediation" quirk/cli/doctor_cmd.py` >= 4 (each of dashboard/network/db check + at least one OK path)
    - `grep -cE "QUIRK_DB_PATH" quirk/cli/doctor_cmd.py` >= 2
    - `grep -cE "from quirk\\.util\\.safe_exc import safe_str" quirk/cli/doctor_cmd.py` == 1
    - `grep -cE "Multiple QU\\.I\\.R\\.K\\. DBs found" quirk/dashboard/api/deps.py` == 1
    - `grep -v '^#' quirk/dashboard/api/deps.py | grep -cE "mtime" ` == 0 (old heuristic gone)
    - `pytest tests/test_doctor_actionable.py -x` exits 0 (GREEN)
    - `pytest tests/ -x -k "doctor or deps" 2>&1 | tail -5` shows no regressions
  </acceptance_criteria>
  <done>
    All Task 1 tests pass; no collateral doctor / deps test regression.
  </done>
</task>

<task type="auto">
  <name>Task 3: Flip WR-01, WR-02, WR-03 audit rows</name>
  <files>.planning/audit-2026-05-08/AUDIT-TASKS.md</files>
  <read_first>
    - .planning/audit-2026-05-08/AUDIT-TASKS.md lines 184-204 (api-cli-core WR rows)
  </read_first>
  <action>
    Update `.planning/audit-2026-05-08/AUDIT-TASKS.md`, flipping these three rows from `| — | [ ] open |` to `| Phase 75 | [x] closed |`:
    - Line 186 (WR-01): _check_dashboard / _check_network always return True
    - Line 187 (WR-02): _check_db opens DB at default path regardless of QUIRK_DB_PATH
    - Line 188 (WR-03): _default_db_path mtime-newest-wins is non-deterministic

    No other rows touched. Per-row evidence summaries land in the Plan SUMMARY, not the audit row body. Single docs commit.

    Commit message: `docs(75-01): close api-cli-core WR-01/WR-02/WR-03 in audit ledger`.
  </action>
  <verify>
    <automated>grep -cE "api-cli-core/WR-(01|02|03).*Phase 75.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "api-cli-core/WR-(01|02|03).*Phase 75.*\\[x\\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 3
    - `grep -cE "api-cli-core/WR-(04|05|06|07|08|09|10|11|12|13|14|15|16|17).*\\[ \\] open" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 14 (other api-cli-core rows untouched — closed by 75-02/75-03/75-04)
  </acceptance_criteria>
  <done>
    AUDIT-TASKS.md shows WR-01, WR-02, WR-03 closed under Phase 75; remaining 14 api-cli-core rows still open pending 75-02/75-03/75-04.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `QUIRK_DB_PATH` env var → `_check_db` | Operator-controlled env input crosses into DB-path resolution |
| Filesystem search → `_default_db_path` | Multiple parallel DB files (legacy artifacts) could mislead resolution |
| External network → `_check_dashboard` / `_check_network` | Probe results inform operator action; exception strings must not leak credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-01 | Tampering | `_default_db_path` silently picking attacker-planted legacy DB via mtime | mitigate | Fail-loud `ValueError` on multi-DB per D-03; operator must explicitly set `QUIRK_DB_PATH` |
| T-75-02 | Repudiation | `_check_dashboard` / `_check_network` returning bare True with no actionable detail | mitigate | Typed dict with `remediation` field per D-01; renderer surfaces remediation to operator |
| T-75-03 | Info Disclosure | Exception stringification leaking credentials in `detail` field | mitigate | Route exception messages through `safe_str` (Phase 59); same shape as Phase 72 `db_connector.py:175` |
| T-75-04 | Tampering | `QUIRK_DB_PATH` pointing to a non-readable path silently fallback-resolving | mitigate | `_check_db` validates env-supplied path exists+readable; surfaces failure dict, never falls through to default per D-02 |
</threat_model>

<verification>
- `python -m compileall quirk/cli/doctor_cmd.py quirk/dashboard/api/deps.py` clean
- `pytest tests/test_doctor_actionable.py -x` passes
- `pytest tests/ -x -k "doctor or deps"` passes (no collateral regression)
- AUDIT-TASKS.md WR-01, WR-02, WR-03 rows show `Phase 75 | [x] closed`
- `quirk doctor` exit-code semantics unchanged (Phase 52 DOCS-05 preserved per D-18)
</verification>

<success_criteria>
- `_check_dashboard()` and `_check_network()` return typed dict `{"ok", "detail", "remediation"}` with real probe results
- `_check_db()` honors `QUIRK_DB_PATH`; falls back to `_default_db_path()` only on env-unset
- `_default_db_path()` fails loud with `ValueError` on multi-DB; deterministic on single-DB
- 6+ tests pass in `tests/test_doctor_actionable.py`
- WR-01, WR-02, WR-03 closed in AUDIT-TASKS.md
</success_criteria>

<output>
Create `.planning/phases/75-api-cli-core-warnings/75-01-SUMMARY.md` documenting: C-1 file-path correction landed in `doctor_cmd.py` (not `doctor.py`), D-03 canonical path choice (`./quirk-output/quirk.db` per RESEARCH A1), `run_doctor()` renderer update for typed-dict consumption, three audit rows flipped (WR-01, WR-02, WR-03), test count, RED→GREEN commit cadence.
</output>
