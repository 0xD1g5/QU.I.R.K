---
phase: 93-credential-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["93-01", "93-02"]
files_modified:
  - run_scan.py
  - quirk/scanner/jwt_scanner.py
  - tests/test_credential_leakage.py
  - .planning/phases/93-credential-infrastructure/93-SECURITY-REVIEW-GATE.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-04]
must_haves:
  truths:
    - "An authenticated scan run completes: --auth-bearer/--auth-api-key/--auth-basic flags build a CredentialContext and the JWT/API scanner attaches the auth header"
    - "httpx debug logging never emits auth headers (event_hooks strip them before any log handler)"
    - "A known sentinel credential appears in NONE of: SQLite row, CBOM output, dashboard API response, log files"
    - "The credential bytearray is zeroed in a finally block covering the full scan body (BaseException-safe)"
    - "A committed security-review gate doc audits all 11 leakage surfaces and states the best-effort zeroization caveat"
  artifacts:
    - path: "run_scan.py"
      provides: "argparse --auth-* flags + CredentialContext build + closure capture + finally-zeroize"
      contains: "CredentialContext"
    - path: "quirk/scanner/jwt_scanner.py"
      provides: "cred_ctx-aware scan_jwt_targets + event_hooks auth-header strip"
      contains: "cred_ctx"
    - path: "tests/test_credential_leakage.py"
      provides: "sentinel leak-detection suite across stored/rendered surfaces"
      contains: "QUIRK_SENTINEL_CRED_d41d8cd9"
    - path: ".planning/phases/93-credential-infrastructure/93-SECURITY-REVIEW-GATE.md"
      provides: "11-surface audit + safe_str coverage + best-effort caveat"
      contains: "best-effort"
  key_links:
    - from: "run_scan.py _run_jwt_phase closure"
      to: "quirk/scanner/jwt_scanner.scan_jwt_targets(cred_ctx=...)"
      via: "closure capture of cred_ctx (no _wrapped_phase signature change — D-14)"
      pattern: "cred_ctx=cred_ctx"
    - from: "quirk/scanner/jwt_scanner.py httpx request"
      to: "CredentialContext.as_headers()"
      via: "headers= injection with event_hooks auth-strip"
      pattern: "as_headers\\(\\)"
    - from: "tests/test_credential_leakage.py sentinel"
      to: "SQLite/CBOM/dashboard/log surfaces"
      via: "grep-absence assertion"
      pattern: "QUIRK_SENTINEL_CRED_d41d8cd9"
---

<objective>
Prove the credential seam end-to-end (AUTH-01) and prove credentials never leak (AUTH-02), then commit the security-review gate deliverable (AUTH-04). Wire `--auth-bearer/--auth-api-key/--auth-basic` argparse flags + `CredentialContext` build into `run_scan.py`, capture `cred_ctx` into the existing `_run_jwt_phase` closure (no `_wrapped_phase` signature change — D-14), have the JWT/API scanner attach headers via `as_headers()` with an httpx `event_hooks` auth-strip (D-10), zero the buffer in a BaseException-safe `finally`, then build the sentinel leak-detection suite and the committed markdown audit.

Purpose: This is the milestone's committed security-review gate (D-07) — it MUST be green before any later phase sends authenticated traffic to a live target. Phase-93 consumer is the existing JWT/API scanner ONLY (D-12) — no new finding types, no generic HTTP/TLS probes.
Output: Wired run_scan.py + jwt_scanner.py, the leak-detection suite, and 93-SECURITY-REVIEW-GATE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-credential-infrastructure/93-CONTEXT.md
@.planning/phases/93-credential-infrastructure/93-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/phases/93-credential-infrastructure/93-01-SUMMARY.md
@.planning/phases/93-credential-infrastructure/93-02-SUMMARY.md

<interfaces>
From quirk/auth/credentials.py (Plan 01):
  CredentialContext.from_cli(*, bearer=None, api_key=None, api_key_query=None, basic=None) -> Optional[CredentialContext]
  CredentialContext.as_headers() -> dict[str, str]
  CredentialContext.query_param() -> tuple[str, str] | None
  CredentialContext.close() -> None   # context manager too

From quirk/util/safe_exc.py (Plan 02): safe_str scrubs API-key/Basic/query shapes.

From run_scan.py:
  _wrapped_phase(run_stats, phase_name, scanner_label, fn, error_endpoints, logger)  # SIGNATURE UNCHANGED (D-14)
  existing _run_jwt_phase closure at lines 1190-1202
  config load at line 685: cfg = load_config(args.config)

From quirk/scanner/jwt_scanner.py:
  scan_jwt_targets(targets, timeout=10, logger=None, *, allow_insecure_jwks=False) -> List[CryptoEndpoint]
  _fetch_jwks(...) one-shot httpx.get calls (lines 71-101)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire CredentialContext into run_scan.py + JWT scanner consumption (AUTH-01, D-10/D-12/D-14)</name>
  <files>run_scan.py, quirk/scanner/jwt_scanner.py</files>
  <read_first>
    - run_scan.py lines 110-145 (_wrapped_phase — signature must NOT change), lines 680-740 (config-load seam), lines 1185-1215 (_run_jwt_phase closure)
    - quirk/scanner/jwt_scanner.py lines 1-115, 202-234 (httpx call sites + scan_jwt_targets signature)
    - quirk/auth/credentials.py (Plan 01 — as_headers/query_param/close)
    - .planning/phases/93-credential-infrastructure/93-PATTERNS.md lines 225-340 (exact run_scan + jwt_scanner wiring excerpts)
    - .planning/research/PITFALLS.md lines 280-287, 64-68 (httpx event_hooks auth-strip; ephemeral lifetime)
  </read_first>
  <action>
    In `run_scan.py`: add argparse flags `--auth-bearer`, `--auth-api-key`, `--auth-basic` (each `nargs="?"`, `const="PROMPT"`, default None — so a bare flag yields the "PROMPT" sentinel that from_cli maps to getpass; D-01). Add help text that teaches the reference-not-secret model (`@file`, ENV_VAR name, or bare flag to prompt) and why (avoids argv/ps/history leakage). After `cfg = load_config(args.config)` and the security overrides, build `cred_ctx: Optional[CredentialContext] = None`; if `cfg.connectors.enable_authenticated_mode` (or any --auth-* flag present), `from quirk.auth.credentials import CredentialContext` (lazy import) and `cred_ctx = CredentialContext.from_cli(bearer=getattr(args,"auth_bearer",None), api_key=getattr(args,"auth_api_key",None), basic=getattr(args,"auth_basic",None))`. Update the existing `_run_jwt_phase` closure to pass `cred_ctx=cred_ctx` (closure capture only — do NOT change `_wrapped_phase`'s signature, D-14). Wrap the scan-phase body in `try: ... finally: if cred_ctx is not None: cred_ctx.close()` so zeroization runs even on KeyboardInterrupt/BaseException (PITFALLS "Looks Done" item 1).

    In `quirk/scanner/jwt_scanner.py`: add keyword-only `cred_ctx=None` to `scan_jwt_targets` (and the inner endpoint helper). Compute `_auth_headers = cred_ctx.as_headers() if cred_ctx is not None else {}` once. When `_auth_headers` is non-empty, route the httpx GET through a short-lived `httpx.Client(... event_hooks={"request": [_strip_auth_from_log]})` passing `headers=_auth_headers`; otherwise keep the existing one-shot `httpx.get`. Define module-level `_strip_auth_from_log(request)` that pops `Authorization`, `X-Api-Key`, `X-Auth-Token` from `request.headers` before any log handler sees them (D-10). Per D-12: attach auth headers only — emit NO new finding types and add NO new probe targets.
  </action>
  <verify>
    <automated>python -m compileall run_scan.py quirk/scanner/jwt_scanner.py && python -m pytest tests/ -k "jwt_scanner or run_scan" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_wrapped_phase(" run_scan.py` confirms the helper signature is unchanged (still 6 params; D-14)
    - run_scan.py contains `--auth-bearer`, `--auth-api-key`, `--auth-basic` argparse flags with `const="PROMPT"`
    - `_run_jwt_phase` passes `cred_ctx=cred_ctx`; cred_ctx is built only when enable_authenticated_mode/flags set
    - A `finally` block calls `cred_ctx.close()` and wraps the full scan body
    - jwt_scanner.scan_jwt_targets accepts `cred_ctx=` and defines `_strip_auth_from_log` popping Authorization/X-Api-Key/X-Auth-Token
    - No new CryptoEndpoint finding type added in jwt_scanner (D-12) — diff shows only header-injection + event_hooks changes
    - `python -m compileall run_scan.py quirk/scanner/jwt_scanner.py` exits 0; existing jwt/run_scan tests stay green
  </acceptance_criteria>
  <done>An authenticated scan completes end-to-end: flags build a CredentialContext, the JWT/API scanner attaches the auth header with log-stripping, the buffer zeroes in a BaseException-safe finally, and _wrapped_phase is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Sentinel leak-detection suite across stored/rendered surfaces (AUTH-02, D-06/D-07)</name>
  <files>tests/test_credential_leakage.py</files>
  <read_first>
    - tests/test_credential_leakage.py (full file — MODIFIED_FILES, import-presence gate, existing structure)
    - quirk/db.py (scan-record write path / column names to grep for sentinel)
    - .planning/phases/93-credential-infrastructure/93-PATTERNS.md lines 418-453, 489-493 (sentinel test + import-presence excerpts)
    - .planning/research/PITFALLS.md lines 39-77 (the 11-surface table this suite must cover)
  </read_first>
  <action>
    Add `"quirk/auth/credentials.py"` to `MODIFIED_FILES` so the import-presence gate covers it. Define `SENTINEL = "QUIRK_SENTINEL_CRED_d41d8cd9"`. Add tests asserting the sentinel survives NONE of the stored/rendered surfaces: (a) `safe_str(Exception(f"Authorization: Bearer {SENTINEL}"))` and the API-key/Basic/query variants do not contain SENTINEL; (b) a CryptoEndpoint whose scan_error was built via safe_str does not contain SENTINEL after `json.dumps`; (c) a round-trip through the DB scan-record write+read (use the conftest QUIRK_DB_PATH fixture pattern) — write a scan with a sentinel-bearing CredentialContext active and assert the sentinel is absent from the persisted row, the CBOM JSON output, and the dashboard `/api/scan/latest` JSON shape. Use a real CredentialContext (Plan 01) seeded with the sentinel, then close() it; assert the buffer is zeroed afterward.
  </action>
  <verify>
    <automated>python -m pytest tests/test_credential_leakage.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `SENTINEL = "QUIRK_SENTINEL_CRED_d41d8cd9"` defined
    - `quirk/auth/credentials.py` is in MODIFIED_FILES (import-presence gate covers it)
    - Tests assert SENTINEL absent from: safe_str output (all 4 shapes), json.dumps of a scan_error field, the persisted SQLite scan row, the CBOM JSON, and the dashboard API JSON
    - A test asserts the CredentialContext buffer is zeroed after close()
    - `python -m pytest tests/test_credential_leakage.py -q` is green
  </acceptance_criteria>
  <done>An automated suite injects the sentinel and proves it appears in none of the stored/rendered surfaces, and that the credential buffer zeroes on close.</done>
</task>

<task type="auto">
  <name>Task 3: Author the committed security-review gate deliverable (AUTH-04, D-05/D-06/D-07)</name>
  <files>.planning/phases/93-credential-infrastructure/93-SECURITY-REVIEW-GATE.md</files>
  <read_first>
    - .planning/research/PITFALLS.md lines 39-77 (the 11-surface table — source of truth), lines 11-37 (best-effort zeroization caveat)
    - .planning/phases/93-credential-infrastructure/93-CONTEXT.md D-06/D-07/D-08 (gate scope)
    - quirk/util/safe_exc.py (final _SENSITIVE_PATTERNS to cite by pattern)
    - tests/test_credential_leakage.py (the suite this doc references as the automated half of the gate)
  </read_first>
  <action>
    Write the markdown audit. Enumerate ALL 11 credential-leakage surfaces from PITFALLS.md (SQLite scan_error, SQLite api_scan_json/cbom_json, CBOM JSON/XML files, dashboard /api/scan/latest, dashboard PDF export, CLI HTML report, structured/debug logs, Python traceback, quirk.db WAL file, process swap/core dump, scheduler scheduled_scans). For each: state HOW it is controlled in Phase 93 (e.g., safe_str pattern, AST gate, event_hooks strip, schema gate, ephemeral-only/no-persist, best-effort) and cite the controlling artifact (file + Plan). Document the `safe_str()` extension covering API-key header + query-param shapes (D-08), and state explicitly the best-effort (NOT provable) nature of Python in-memory zeroization (D-05: interning/GC may retain heap copies; "ephemeral" means never-persisted, not provably-erased). Reference the automated leak-detection suite (Task 2) as the executable half of the gate (D-06) and mark this as the milestone gate that must be GREEN before any later phase sends authenticated live traffic (D-07).
  </action>
  <verify>
    <automated>test -f .planning/phases/93-credential-infrastructure/93-SECURITY-REVIEW-GATE.md && grep -ci "best-effort" .planning/phases/93-credential-infrastructure/93-SECURITY-REVIEW-GATE.md | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the phase dir path
    - Contains a row/section for each of the 11 leakage surfaces, each with a control + cited artifact
    - States the best-effort (not provable) zeroization caveat explicitly (D-05)
    - Documents the safe_str API-key header + query-param coverage (D-08)
    - References tests/test_credential_leakage.py as the automated gate half and marks it the milestone gate (D-07)
  </acceptance_criteria>
  <done>A committed markdown deliverable audits all 11 leakage surfaces, documents the safe_str extension and the best-effort zeroization caveat, and ties to the automated leak-detection suite as the milestone's security-review gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CredentialContext → httpx request header | str materialized only here; event_hooks strip before logging |
| authenticated scan → SQLite/CBOM/dashboard/log | sentinel suite proves no credential crosses into stored/rendered surfaces |
| scan body → process exit (incl. interrupt) | finally-zeroize guarantees buffer overwrite on BaseException |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-10 | Information Disclosure | httpx DEBUG logging emits Authorization header | mitigate | D-10: event_hooks request filter strips Authorization/X-Api-Key/X-Auth-Token before any log handler |
| T-93-11 | Information Disclosure | Credential in SQLite/CBOM/dashboard/PDF/log (11 surfaces) | mitigate (gated) | Sentinel leak-detection suite asserts absence across all surfaces; 93-SECURITY-REVIEW-GATE.md audits each |
| T-93-12 | Information Disclosure | Secret residency after scan (no zeroization on interrupt) | mitigate (best-effort) | finally block wraps full scan body, BaseException-safe close(); D-05 caveat documented |
| T-93-13 | Spoofing/Scope creep | Auth headers attached to out-of-scope or new probe targets | mitigate | D-12: consumer is existing JWT/API scanner only; no new finding types or probe targets in Phase 93 |
| T-93-SC | Tampering | npm/pip installs | accept | No new dependencies (httpx already present; stdlib otherwise) |
</threat_model>

<verification>
- `python -m pytest tests/test_credential_leakage.py -q` green (the automated gate half)
- `python -m compileall run_scan.py quirk/scanner/jwt_scanner.py` exits 0
- `grep -n "_wrapped_phase(" run_scan.py` shows unchanged 6-arg signature (D-14)
- 93-SECURITY-REVIEW-GATE.md present with all 11 surfaces + best-effort caveat
- Full suite `python -m pytest tests/ -q` green (no regressions)
</verification>

<success_criteria>
An authenticated scan completes end-to-end via the JWT/API scanner with auth headers and log-stripping; the sentinel credential leaks into none of the 11 surfaces; the buffer zeroes on close (incl. interrupt); and a committed security-review gate doc audits every surface with the best-effort caveat — the milestone gate is GREEN.
</success_criteria>

<output>
Create `.planning/phases/93-credential-infrastructure/93-03-SUMMARY.md` when done.
</output>
