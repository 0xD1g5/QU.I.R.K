---
phase: 93-credential-infrastructure
plan: 04
type: execute
wave: 3
depends_on: ["93-03"]
files_modified:
  - docs/configuration.md
  - docs/UAT-SERIES.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]
must_haves:
  truths:
    - "User-facing docs explain the --auth-* reference-not-secret model and the ephemeral-only / no-scheduled-auth constraint"
    - "docs/UAT-SERIES.md gains test cases for authenticated scan, scrubbing verification, and scheduler rejection"
    - "Obsidian vault has a Phase-93 note and synced UAT-Series"
  artifacts:
    - path: "docs/configuration.md"
      provides: "authenticated-scanning usage section"
      contains: "--auth-bearer"
    - path: "docs/UAT-SERIES.md"
      provides: "Phase 93 UAT cases"
      contains: "QRK-SCHED-AUTH-001"
  key_links:
    - from: "docs/UAT-SERIES.md"
      to: "Obsidian vault UAT-Series.md"
      via: "filesystem copy (CLAUDE.md sync workflow)"
      pattern: "20_Dev-Work/QUIRK"
---

<objective>
Satisfy the CLAUDE.md mandatory per-phase documentation and Obsidian-sync obligations for Phase 93: document the authenticated-scanning usage (reference-not-secret model, ephemeral-only, scheduler-rejection), add Phase 93 UAT cases to docs/UAT-SERIES.md, create the Obsidian Phase-93 note, and sync UAT-Series + Roadmap to the vault.

Purpose: Per project convention (CLAUDE.md "Mandatory Phase Completion Steps" + "docs + Obsidian sync per phase") every phase ships with updated docs and a synced vault. This plan makes those obligations explicit, executable tasks rather than implicit afterthoughts.
Output: Updated docs/configuration.md + docs/UAT-SERIES.md, Obsidian Phase-93 note, synced UAT-Series.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-credential-infrastructure/93-CONTEXT.md
@CLAUDE.md
@.planning/phases/93-credential-infrastructure/93-01-SUMMARY.md
@.planning/phases/93-credential-infrastructure/93-02-SUMMARY.md
@.planning/phases/93-credential-infrastructure/93-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document authenticated scanning in docs/configuration.md</name>
  <files>docs/configuration.md</files>
  <read_first>
    - docs/configuration.md (existing structure / section headings)
    - .planning/phases/93-credential-infrastructure/93-CONTEXT.md (D-01 reference model, D-11 scheduler rejection)
    - .planning/phases/93-credential-infrastructure/93-01-SUMMARY.md, 93-03-SUMMARY.md (final flag names + behavior)
  </read_first>
  <action>
    Add an "Authenticated Scanning (ephemeral credentials)" section. Document: the three flags --auth-bearer, --auth-api-key, --auth-basic; the reference-not-secret model with the three input forms (@file, ENV_VAR name, bare flag triggering an interactive prompt) and WHY (raw secrets in argv leak to ps/shell history); source precedence (prompt over env over flag reference); the enable_authenticated_mode config flag; the ephemeral-only invariant (credentials never persisted to SQLite/CBOM/logs/dashboard); and that scheduled authenticated scans are prohibited (error QRK-SCHED-AUTH-001). Keep diffs minimal; match existing doc voice.
  </action>
  <verify>
    <automated>grep -q -- "--auth-bearer" docs/configuration.md && grep -q "QRK-SCHED-AUTH-001" docs/configuration.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/configuration.md documents all three --auth-* flags and the @file/ENV/bare-prompt input forms
    - States the ephemeral-only invariant and the QRK-SCHED-AUTH-001 scheduler rejection
    - Explains the reference-not-secret rationale (argv/ps/history leakage)
  </acceptance_criteria>
  <done>Configuration docs explain authenticated scanning usage, the reference-not-secret model, and the ephemeral-only / no-scheduled-auth constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 93 UAT cases + sync UAT-SERIES.md to Obsidian (CLAUDE.md steps 2-4)</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (header date + series structure + existing pass-criteria style)
    - CLAUDE.md "Mandatory Phase Completion Steps" (UAT update + sync + commit workflow)
    - .planning/phases/93-credential-infrastructure/93-03-SUMMARY.md (final behaviors to test)
  </read_first>
  <action>
    Add UAT cases to the relevant series covering: (a) an authenticated scan run via --auth-bearer @token.txt and a bare-flag getpass prompt completing without error; (b) scrubbing verification — grep the SQLite row, CBOM output, dashboard API response, and log file for a sentinel credential and confirm absence; (c) quirk schedule add against an enable_authenticated_mode: true config exits with QRK-SCHED-AUTH-001. Update the Last Updated date at the top to today. Then sync to the vault by writing docs/UAT-SERIES.md (prepended with the QUIRK frontmatter block per CLAUDE.md step 3) to the vault path 20_Dev-Work/QUIRK/UAT-Series.md via filesystem copy (do NOT use obsidian CLI content= — file too large for shell expansion). Use only the synthetic sentinel and placeholder token files in examples — never a real credential. Finally, commit the updated doc (CLAUDE.md step 4): run `node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-93): update UAT-SERIES.md" --files docs/UAT-SERIES.md`.
  </action>
  <verify>
    <automated>grep -q "QRK-SCHED-AUTH-001" docs/UAT-SERIES.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/UAT-SERIES.md has new cases for authenticated scan, scrubbing verification, and scheduler rejection
    - The Last Updated date reflects today
    - Vault UAT-Series.md exists under 20_Dev-Work/QUIRK and contains the QRK-SCHED-AUTH-001 case plus the QUIRK frontmatter block
    - docs/UAT-SERIES.md is committed to git with the `docs(phase-93)` commit-message prefix
  </acceptance_criteria>
  <done>UAT-SERIES.md gains Phase 93 cases and is synced to the Obsidian vault per CLAUDE.md steps 2-4.</done>
</task>

<task type="auto">
  <name>Task 3: Create Obsidian Phase-93 note + sync Roadmap (CLAUDE.md step 1)</name>
  <files>Obsidian vault: 20_Dev-Work/QUIRK/Phases/Phase-93-Credential-Infrastructure.md</files>
  <read_first>
    - vault Phases dir 20_Dev-Work/QUIRK/Phases/ (existing phase-note template pattern — read one neighbor note)
    - CLAUDE.md "Create Obsidian Phase Note" + frontmatter standards
    - .planning/phases/93-credential-infrastructure/93-01-SUMMARY.md, 93-02-SUMMARY.md, 93-03-SUMMARY.md (What Was Built source)
    - .planning/ROADMAP.md Phase 93 section (Goal + 5 success criteria + requirements)
  </read_first>
  <action>
    Write the phase note directly to the vault filesystem (NOT via obsidian CLI content= — files too large) at 20_Dev-Work/QUIRK/Phases/Phase-93-Credential-Infrastructure.md under the Digs vault root. Include: frontmatter (project: QU.I.R.K., type: phase, status: complete, source: .planning/phases/93-credential-infrastructure/, updated: today), Goal (from ROADMAP), Requirements Covered (AUTH-01..05), Success Criteria (the 5 from ROADMAP), What Was Built (one subsection per plan 01-04 sourced from each SUMMARY.md), and a [[Roadmap]] link. Then sync the updated .planning/ROADMAP.md to the vault Roadmap note per the CLAUDE.md Roadmap-sync workflow (frontmatter + full content, overwrite).
  </action>
  <verify>
    <automated>ls "$HOME/vaults/Digs/20_Dev-Work/QUIRK/Phases/" | grep -q "Phase-93"</automated>
  </verify>
  <acceptance_criteria>
    - The vault Phase-93 note exists with frontmatter (status: complete) and references AUTH-01..05
    - What Was Built has one subsection per plan (01-04) sourced from the SUMMARY files
    - Vault Roadmap note re-synced from .planning/ROADMAP.md with QUIRK frontmatter
  </acceptance_criteria>
  <done>The Obsidian vault has a complete Phase-93 note and a freshly synced Roadmap per CLAUDE.md step 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs to vault filesystem | Documentation sync writes to the local Obsidian vault; no secrets involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-14 | Information Disclosure | UAT examples could embed a real credential | mitigate | Use only the synthetic sentinel QUIRK_SENTINEL_CRED_d41d8cd9 and placeholder @token.txt in docs/UAT examples |
| T-93-SC | Tampering | npm/pip installs | accept | Docs-only plan; no dependencies installed |
</threat_model>

<verification>
- grep confirms --auth-bearer and QRK-SCHED-AUTH-001 in docs/configuration.md
- grep confirms QRK-SCHED-AUTH-001 in docs/UAT-SERIES.md
- Vault files present: Phase-93 note, UAT-Series.md, Roadmap.md
- No real credential strings in any doc example (sentinel/placeholder only)
</verification>

<success_criteria>
Configuration docs and UAT-SERIES.md cover Phase 93's authenticated-scanning behavior; the Obsidian vault has a complete Phase-93 note and synced UAT-Series + Roadmap — all CLAUDE.md mandatory per-phase obligations satisfied with no real secrets in examples.
</success_criteria>

<output>
Create `.planning/phases/93-credential-infrastructure/93-04-SUMMARY.md` when done.
</output>
