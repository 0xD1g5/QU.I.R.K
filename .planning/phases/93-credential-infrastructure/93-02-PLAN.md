---
phase: 93-credential-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/util/safe_exc.py
  - quirk/errors.py
  - quirk/cli/schedule_cmd.py
  - tests/test_scan_error_gate.py
  - tests/test_safe_exc.py
autonomous: true
requirements: [AUTH-03, AUTH-05]
must_haves:
  truths:
    - "safe_str() scrubs API-key header, query-param API-key, and HTTP Basic credential shapes (not just Bearer) (D-08)"
    - "The AST CI gate flags credential field names reaching json.dumps()/model_dump() and walks quirk/auth/ (D-09)"
    - "A schema-level CI assertion fails if any scheduled_scans/scan_checkpoints column is named key/token/password/secret/credential (D-09)"
    - "quirk schedule add against an authenticated-mode config exits with QRK-SCHED-AUTH-001 (D-11)"
  artifacts:
    - path: "quirk/util/safe_exc.py"
      provides: "extended _SENSITIVE_PATTERNS covering API-key and Basic shapes"
      contains: "X-Api-Key"
    - path: "quirk/errors.py"
      provides: "SCHED-AUTH-001 error registry entry"
      contains: "SCHED-AUTH-001"
    - path: "quirk/cli/schedule_cmd.py"
      provides: "authenticated-mode rejection in _cmd_add"
      contains: "SCHED-AUTH-001"
    - path: "tests/test_scan_error_gate.py"
      provides: "extended deny-list AST gate + schema column gate"
      contains: "CREDENTIAL_FIELD_NAMES"
  key_links:
    - from: "quirk/cli/schedule_cmd.py _cmd_add"
      to: "quirk/errors.py format_error('SCHED-AUTH-001')"
      via: "rejection branch before db write"
      pattern: "format_error\\(['\"]SCHED-AUTH-001"
    - from: "tests/test_scan_error_gate.py"
      to: "quirk/auth/ + quirk/db.py"
      via: "AST walk scope + schema column grep"
      pattern: "CREDENTIAL_FIELD_NAMES"
---

<objective>
Build the scrubbing and regression-guard layer (AUTH-05) and the scheduler hard-rejection (AUTH-03): extend `safe_str()` `_SENSITIVE_PATTERNS` to API-key/Basic shapes, register `QRK-SCHED-AUTH-001`, reject authenticated-mode configs in `quirk schedule add`, extend the LEAK-03 AST gate deny-list to credential field names + the `quirk/auth/` walk scope, and add a schema-level column-name CI assertion.

Purpose: These guards must exist before any credential is wired into a live request (Plan 03 consumes them). They make credential leakage a CI-failing regression rather than a silent escape (PITFALLS Pitfalls 2, 6, 9).
Output: Extended scrubber, error code, scheduler rejection, AST + schema gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-credential-infrastructure/93-CONTEXT.md
@.planning/phases/93-credential-infrastructure/93-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
From quirk/errors.py:
  ERROR_REGISTRY: dict[str, ErrorEntry]   # add "SCHED-AUTH-001"
  format_error(code: str) -> str          # emits "[QRK-<code>] ... Fix: ..."
  class ErrorEntry(code, cause, fix)

From quirk/util/safe_exc.py:
  _SENSITIVE_PATTERNS: tuple[re.Pattern, ...]   # extend (lines 21-33)
  safe_str(exc: BaseException) -> str           # signature unchanged

From quirk/cli/schedule_cmd.py:
  _cmd_add(args, console)                 # SCHED-001/002 rejection precedent (lines 37-48)
  format_error already imported (line 16)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend safe_str() _SENSITIVE_PATTERNS for API-key + Basic shapes (D-08)</name>
  <files>quirk/util/safe_exc.py, tests/test_safe_exc.py</files>
  <read_first>
    - quirk/util/safe_exc.py lines 21-53 (current pattern tuple + safe_str)
    - tests/test_safe_exc.py (existing corpus shape)
    - .planning/phases/93-credential-infrastructure/93-PATTERNS.md lines 134-161 (exact patterns to append)
    - .planning/research/PITFALLS.md lines 39-77 (D-08 rationale; query-param keys leak via URLs)
  </read_first>
  <action>
    Append re.compile entries to `_SENSITIVE_PATTERNS` (do not modify existing entries, do not touch the safe_str signature): `X-Api-Key\s*:\s*\S+` (IGNORECASE), `X-Auth-Token\s*:\s*\S+` (IGNORECASE), query-param shape `[?&](api_key|token|key|auth_token)=[^&\s]{8,}` (IGNORECASE) — covers the D-03 query-param API-key leak surface, and `Authorization:\s*Basic\s+[A-Za-z0-9+/]{8,}={0,2}` (IGNORECASE). Add corpus cases to tests/test_safe_exc.py asserting each new shape (including a query-param URL `https://api.host/v1?api_key=QUIRK_SENTINEL_CRED_d41d8cd9`) is scrubbed by safe_str.
  </action>
  <verify>
    <automated>python -m pytest tests/test_safe_exc.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "re.compile" quirk/util/safe_exc.py` increases by 4 over baseline (count via `grep -v '^#' | grep -c`)
    - safe_str(Exception("X-Api-Key: QUIRK_SENTINEL_CRED_d41d8cd9")) does not contain the sentinel
    - safe_str(Exception("https://h/v1?api_key=QUIRK_SENTINEL_CRED_d41d8cd9")) does not contain the sentinel
    - safe_str(Exception("Authorization: Basic <base64>")) is scrubbed
    - tests/test_safe_exc.py passes
  </acceptance_criteria>
  <done>safe_str scrubs API-key header, query-param API-key, and HTTP Basic shapes; corpus tests prove it.</done>
</task>

<task type="auto">
  <name>Task 2: Register QRK-SCHED-AUTH-001 and reject authenticated-mode in schedule add (D-11)</name>
  <files>quirk/errors.py, quirk/cli/schedule_cmd.py, tests/test_scan_error_gate.py</files>
  <read_first>
    - quirk/errors.py lines 141-160, 254-275 (SCHED domain entries + format_error wire format + __all__)
    - quirk/cli/schedule_cmd.py lines 37-49 (SCHED-001/002 rejection precedent)
    - .planning/phases/93-credential-infrastructure/93-PATTERNS.md lines 165-193, 344-370 (error entry + rejection helper excerpts)
  </read_first>
  <action>
    In `quirk/errors.py`, add `"SCHED-AUTH-001": ErrorEntry(code="SCHED-AUTH-001", cause="Authenticated scan configs cannot be scheduled — credentials are ephemeral and cannot be persisted.", fix="Run an authenticated scan interactively with `quirk --auth-bearer` (or --auth-api-key / --auth-basic).")` after the SCHED-004 entry. `format_error("SCHED-AUTH-001")` will emit the `[QRK-SCHED-AUTH-001]` prefix automatically.

    In `quirk/cli/schedule_cmd.py`, add a private helper `_config_has_authenticated_mode(config_path) -> bool` that reads the target YAML and checks `connectors.enable_authenticated_mode` truthy via a single key lookup (use yaml.safe_load; do NOT import load_config — avoid pulling scanner deps). In `_cmd_add`, after the cron validation and before `db_path = _resolve_db_path(...)`, add: if `_config_has_authenticated_mode(args.config)` → `console.print(f"[red]{format_error('SCHED-AUTH-001')}[/red]")` then `sys.exit(2)`.

    Add a unit test (in tests/test_scan_error_gate.py or a sibling) asserting `format_error("SCHED-AUTH-001")` contains the literal `QRK-SCHED-AUTH-001`.
  </action>
  <verify>
    <automated>python -c "from quirk.errors import format_error; assert 'QRK-SCHED-AUTH-001' in format_error('SCHED-AUTH-001')" && python -m compileall quirk/cli/schedule_cmd.py</automated>
  </verify>
  <acceptance_criteria>
    - `format_error("SCHED-AUTH-001")` returns a string containing `QRK-SCHED-AUTH-001` and a Fix: clause
    - `quirk schedule add` with a config where `connectors.enable_authenticated_mode: true` exits code 2 and prints the QRK-SCHED-AUTH-001 message (verified by invoking _cmd_add or the CLI on a temp YAML)
    - `_config_has_authenticated_mode` uses yaml.safe_load, not load_config (`grep -n "load_config" quirk/cli/schedule_cmd.py` shows no new import)
    - `python -m compileall quirk/cli/schedule_cmd.py` exits 0
  </acceptance_criteria>
  <done>The scheduler rejects authenticated-mode configs with stable code QRK-SCHED-AUTH-001 and a human-readable message.</done>
</task>

<task type="auto">
  <name>Task 3: Extend AST deny-list gate + add schema column-name gate (D-09)</name>
  <files>tests/test_scan_error_gate.py</files>
  <read_first>
    - tests/test_scan_error_gate.py (full file — SCANNER_DIRS, _collect_violations, self-test pattern)
    - tests/test_adcs_ast_gate.py (positive + negative self-test precedent)
    - quirk/db.py (schema creation SQL — grep for CREATE TABLE / column names)
    - .planning/phases/93-credential-infrastructure/93-PATTERNS.md lines 374-414 (deny-list + schema gate excerpts)
    - .planning/research/PITFALLS.md lines 248-261 (Pitfall 9 schema-gate rationale)
  </read_first>
  <action>
    Add `PROJECT_ROOT / "quirk" / "auth"` to `SCANNER_DIRS` so the credential module is covered. Define `CREDENTIAL_FIELD_NAMES = frozenset({"bearer","api_key","authorization","token","password","credential","secret","key"})`. Add a new AST gate test that walks `ast.Call` nodes in SCANNER_DIRS where the callee is `json.dumps` or `.dumps`/`.model_dump`, and fails if any keyword/dict-key argument name is in CREDENTIAL_FIELD_NAMES. Include a positive self-test (synthetic source string with `json.dumps(token=...)` is flagged) and a negative self-test (clean source is not flagged), mirroring tests/test_adcs_ast_gate.py.

    Add `test_no_credential_column_in_schema()`: read quirk/db.py source and assert no quoted column literal in CREDENTIAL_FIELD_NAMES appears in the scheduled_scans / scan_checkpoints table DDL (scope the assertion to those CREATE TABLE blocks to avoid false positives on unrelated identifiers).
  </action>
  <verify>
    <automated>python -m pytest tests/test_scan_error_gate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `CREDENTIAL_FIELD_NAMES` defined and includes bearer, api_key, authorization, token, password, credential
    - SCANNER_DIRS includes `quirk/auth`
    - Positive self-test proves a synthetic `json.dumps`/`model_dump` with a credential field name is flagged
    - Negative self-test proves clean code is not flagged
    - test_no_credential_column_in_schema passes against current quirk/db.py
    - Full file `python -m pytest tests/test_scan_error_gate.py -q` is green
  </acceptance_criteria>
  <done>The AST gate covers quirk/auth and credential field names in serialization calls (with self-tests), and a schema gate forbids credential-named columns in scheduler/checkpoint tables.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exception text → SQLite scan_error / logs | safe_str chokepoint must scrub credential shapes |
| code → json.dumps / model_dump | AST gate prevents credential field names crossing into serialized output |
| config → scheduled_scans table | scheduler must reject persistence of authenticated configs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-06 | Information Disclosure | API-key/Basic value in exception/log text | mitigate | D-08: extend _SENSITIVE_PATTERNS to header, query-param, and Basic shapes; corpus tests with sentinel |
| T-93-07 | Information Disclosure | Credential field reaches json.dumps/model_dump | mitigate (CI gate) | D-09: AST deny-list over SCANNER_DIRS incl quirk/auth, with positive/negative self-tests |
| T-93-08 | Repudiation/Persistence | Credential persisted via scheduled_scans/scan_checkpoints column | mitigate (CI gate) | D-09: schema column-name assertion fails CI on key/token/password/secret/credential columns |
| T-93-09 | Elevation/Misuse | Authenticated config scheduled for unattended re-run | mitigate | D-11: schedule add hard-rejects with QRK-SCHED-AUTH-001 (exit 2) |
| T-93-SC | Tampering | npm/pip installs | accept | No new dependencies (stdlib + existing yaml only) |
</threat_model>

<verification>
- `python -m pytest tests/test_safe_exc.py tests/test_scan_error_gate.py -q` green
- `python -c "from quirk.errors import format_error; print(format_error('SCHED-AUTH-001'))"` shows the QRK prefix
- `python -m compileall quirk/errors.py quirk/cli/schedule_cmd.py quirk/util/safe_exc.py` exits 0
</verification>

<success_criteria>
safe_str scrubs all three new credential shapes; the AST gate and schema gate fail CI on credential leakage/persistence regressions (with self-tests proving they bite); and quirk schedule add rejects authenticated-mode configs with QRK-SCHED-AUTH-001.
</success_criteria>

<output>
Create `.planning/phases/93-credential-infrastructure/93-02-SUMMARY.md` when done.
</output>
