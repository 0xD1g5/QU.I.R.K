---
phase: 77-info-code-quality-audit-ledger
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/scanner/tls_capabilities.py
  - quirk/scanner/dnssec_scanner.py
  - quirk/scanner/saml_scanner.py
  - quirk/scanner/fingerprint.py
  - quirk/scanner/kerberos_scanner.py
  - quirk/scanner/broker_scanner.py
  - quirk/scanner/email_scanner.py
  - quirk/scanner/tls_scanner.py
  - quirk/util/weak_crypto.py
  - tests/test_tls_capabilities_comment.py
  - tests/test_dnssec_scanner_reserved_algs.py
  - tests/test_saml_scanner_sha1_word_boundary.py
  - tests/test_fingerprint_host_header.py
  - tests/test_weak_crypto_pfs_weak_helpers.py
  - tests/test_kerberos_scanner_realm_ipaddress.py
autonomous: true
requirements: [INFO-01]
must_haves:
  truths:
    - "`quirk/scanner/tls_capabilities.py::_try_handshake` (lines 39-79) carries a 3-line comment block above the function body explaining WHY the probe optionally restricts to TLS 1.0/1.1 (legacy-server posture inventory, not a real handshake downgrade) per D-01"
    - "`quirk/scanner/dnssec_scanner.py::DNSSEC_ALG_MAP` includes entries 9 and 11 marked Reserved (per IANA registry / RFC 8624; alg 7 RSASHA1-NSEC3-SHA1 was already present — RESEARCH C-9) per D-02"
    - "`quirk/scanner/saml_scanner.py` SHA1 detection uses word-boundary matching via Phase 74 `quirk/assessment/migration_advisor.py::_matches` (Discretion D-03 — researcher-recommended path) instead of bare substring `in` per D-03"
    - "`quirk/scanner/fingerprint.py::_http_probe_plain` (line 111) sets the Host header to the actual target hostname, not the literal string `localhost` per D-04"
    - "`quirk/util/weak_crypto.py` (Phase 73 module) exports `is_pfs_cipher` and `is_weak_cipher_classification` helpers; the three local `_is_pfs / _is_weak` inner functions in broker_scanner, email_scanner, tls_scanner are deleted and replaced by calls to the centralized helpers per D-05"
    - "`quirk/scanner/kerberos_scanner.py::_derive_realm` (lines 50-64) replaces the `parts.isdigit()` heuristic with `ipaddress.ip_address(host)` try/except per D-06"
    - "`python -m compileall quirk/scanner quirk/util` exits 0 and `pytest tests/test_tls_capabilities_comment.py tests/test_dnssec_scanner_reserved_algs.py tests/test_saml_scanner_sha1_word_boundary.py tests/test_fingerprint_host_header.py tests/test_weak_crypto_pfs_weak_helpers.py tests/test_kerberos_scanner_realm_ipaddress.py -x` passes"
  artifacts:
    - path: "quirk/scanner/tls_capabilities.py"
      provides: "3-line probe-rationale comment above _try_handshake (D-01)"
      contains: "legacy-server posture"
    - path: "quirk/scanner/dnssec_scanner.py"
      provides: "DNSSEC_ALG_MAP entries 9 and 11 (Reserved) per D-02 / RESEARCH C-9"
      contains: "Reserved"
    - path: "quirk/scanner/saml_scanner.py"
      provides: "Word-boundary SHA1 detection via _matches helper (D-03)"
      contains: "_matches"
    - path: "quirk/scanner/fingerprint.py"
      provides: "Host header uses target hostname (D-04)"
      contains: "host:"
    - path: "quirk/util/weak_crypto.py"
      provides: "Centralized is_pfs_cipher + is_weak_cipher_classification helpers (D-05)"
      contains: "def is_pfs_cipher"
    - path: "quirk/scanner/kerberos_scanner.py"
      provides: "ipaddress.ip_address-based IPv4 detection in _derive_realm (D-06)"
      contains: "ipaddress.ip_address"
    - path: "tests/test_weak_crypto_pfs_weak_helpers.py"
      provides: "RED-then-GREEN evidence the centralized helpers exist and the 3 scanners consume them"
      contains: "is_pfs_cipher"
  key_links:
    - from: "quirk/scanner/broker_scanner.py"
      to: "quirk/util/weak_crypto.py"
      via: "from quirk.util.weak_crypto import is_pfs_cipher, is_weak_cipher_classification"
      pattern: "from quirk\\.util\\.weak_crypto import.*is_pfs_cipher"
    - from: "quirk/scanner/email_scanner.py"
      to: "quirk/util/weak_crypto.py"
      via: "from quirk.util.weak_crypto import is_pfs_cipher, is_weak_cipher_classification"
      pattern: "from quirk\\.util\\.weak_crypto import.*is_pfs_cipher"
    - from: "quirk/scanner/tls_scanner.py"
      to: "quirk/util/weak_crypto.py"
      via: "from quirk.util.weak_crypto import is_pfs_cipher, is_weak_cipher_classification"
      pattern: "from quirk\\.util\\.weak_crypto import.*is_pfs_cipher"
    - from: "quirk/scanner/saml_scanner.py"
      to: "quirk/assessment/migration_advisor.py::_matches"
      via: "Phase 74 word-boundary regex helper reused for SHA1_INDICATORS"
      pattern: "from quirk\\.assessment\\.migration_advisor import _matches"
    - from: "quirk/scanner/kerberos_scanner.py"
      to: "ipaddress stdlib"
      via: "ipaddress.ip_address(host) try/except ValueError"
      pattern: "ipaddress\\.ip_address"
---

<objective>
Close INFO-01 — all 6 protocol scanner INFO findings (scanners-protocol/IN-01..06) — via comment/metadata/dedup edits and stdlib refactors. No new pip deps (D-32). Per-row adjudications applied from RESEARCH 77-RESEARCH.md:

1. **D-01 (IN-01)** — `quirk/scanner/tls_capabilities.py::_try_handshake` lines 39-79 (RESEARCH C-8: no single "downgrade" — the function optionally probes TLS 1.0/1.1 for legacy-server inventory). Add a 3-line `# WHY:` comment block above the function body (line 40) explaining the probe rationale and citing the audit row.

2. **D-02 (IN-02)** — `quirk/scanner/dnssec_scanner.py::DNSSEC_ALG_MAP` (lines 44-57). Per RESEARCH C-9: alg 7 (RSASHA1-NSEC3-SHA1) is already present; the actual gap is alg 9 and alg 11 which are **Reserved per IANA / RFC 8624**. Add: `9: ("Reserved", "HIGH")` and `11: ("Reserved", "HIGH")`. CONTEXT D-02's identification was incorrect (acknowledged in research); the fix as applied still closes IN-02 (the map is now complete for the Reserved range that previously had no entry).

3. **D-03 (IN-03)** — `quirk/scanner/saml_scanner.py:58, 188` `SHA1_INDICATORS` substring `in` matching is too loose (matches `SHA1024` as containing `SHA1`). Replace with word-boundary matching via Phase 74 `quirk/assessment/migration_advisor.py::_matches` (Discretion D-03 — Researcher recommendation: `_matches` is the existing word-boundary helper).

4. **D-04 (IN-04)** — `quirk/scanner/fingerprint.py::_http_probe_plain` (line 111) sends `Host: localhost`. Replace with the target hostname argument (the function already receives `host` per `read_first`).

5. **D-05 (IN-05)** — 3 scanners (`broker_scanner.py:115,120`, `email_scanner.py:103,108`, `tls_scanner.py:248,254`) define local `_is_pfs / _is_weak` inner functions duplicating cipher classification. Consolidate into `quirk/util/weak_crypto.py` (Phase 73 home — Discretion D-05) as module-level `is_pfs_cipher(cipher: str) -> bool` and `is_weak_cipher_classification(cipher: str) -> bool`. Replace the 3 local copies with imports.

6. **D-06 (IN-06)** — `quirk/scanner/kerberos_scanner.py::_derive_realm` (lines 50-64) uses dotted-quad `parts.isdigit()` heuristic. Replace with `ipaddress.ip_address(stripped)` try/except (Pattern 4 in RESEARCH).

All six fixes are surgical (each < 30 lines). RED→GREEN test per requirement, then a final compileall + targeted pytest run. Audit-ledger flips for IN-01..06 are deferred to PLAN 77-05 (the consolidator).

Purpose: Close 6 protocol-subsystem INFO findings with reusable helpers (D-05) and stdlib-correct IPv4 detection (D-06); harden SHA1 detection against false-positives (D-03); make TLS probe rationale auditable (D-01); fix HTTP virtual-host routing (D-04); fill the DNSSEC algorithm coverage map (D-02).
Output: 6 production-source edits, 6 new test modules, 1 new util helper pair, 0 audit row flips (deferred to 77-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/77-info-code-quality-audit-ledger/77-CONTEXT.md
@.planning/phases/77-info-code-quality-audit-ledger/77-RESEARCH.md

<interfaces>
Existing surfaces (verified at HEAD cf2417a per RESEARCH primary sources):

quirk/scanner/tls_capabilities.py (lines 39-79):
- `_try_handshake(host, port, tls_min, tls_max, ...)` probes a single TLS protocol band. Comment to be added immediately above line 40 (function body start).

quirk/scanner/dnssec_scanner.py (lines 44-57):
- `DNSSEC_ALG_MAP: dict[int, tuple[str, str]]` keyed by IANA algorithm number → (name, severity). Already includes entries 1, 3, 5, 6, 7, 8, 10, 12, 13, 14, 15, 16. Missing 9 and 11 (Reserved). Append: `9: ("Reserved", "HIGH"), 11: ("Reserved", "HIGH")`.

quirk/scanner/saml_scanner.py (lines 58, 188):
- Line 58: `SHA1_INDICATORS = ("sha1", "SHA-1", ...)` (or similar tuple)
- Line 188: detection site uses `if any(ind in algo_str for ind in SHA1_INDICATORS)`
- Phase 74 helper at `quirk/assessment/migration_advisor.py::_matches(text, token)` is a word-boundary regex check: returns True iff token matches `\\b<token>\\b` (case-insensitive).

quirk/scanner/fingerprint.py (lines 97, 111, 184):
- `_http_probe_plain(sock, host, port)` at lines 97+. Line 111 currently `f"Host: localhost\\r\\n"`. Change to `f"Host: {host}\\r\\n"`.

quirk/scanner/kerberos_scanner.py (lines 50-64):
- `_derive_realm(host: str) -> str` — current `parts.isdigit()` heuristic; replace with Pattern 4 (ipaddress.ip_address).

Cipher duplication sites (D-05):
- `quirk/scanner/broker_scanner.py:115` defines local `_is_pfs(cipher)` and `:120` `_is_weak(cipher)`
- `quirk/scanner/email_scanner.py:103, 108` same pair
- `quirk/scanner/tls_scanner.py:248, 254` same pair (inner functions inside the scan function)
- Target: `quirk/util/weak_crypto.py` (Phase 73; already exports `is_weak_cipher`, `is_legacy_tls_version`). Add `is_pfs_cipher` and `is_weak_cipher_classification` as module-level functions matching the existing public-API style.

Phase 74 helper (D-03 reuse):
```
# quirk/assessment/migration_advisor.py
def _matches(text: str, token: str) -> bool:
    """Word-boundary case-insensitive containment check."""
    return bool(re.search(rf"\\b{re.escape(token)}\\b", text, re.IGNORECASE))
```

ipaddress pattern (Pattern 4 in RESEARCH):
```python
import ipaddress
try:
    ipaddress.ip_address(stripped)
    return stripped.upper()
except ValueError:
    pass
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing tests for D-01..D-06 (6 new test modules)</name>
  <files>tests/test_tls_capabilities_comment.py, tests/test_dnssec_scanner_reserved_algs.py, tests/test_saml_scanner_sha1_word_boundary.py, tests/test_fingerprint_host_header.py, tests/test_weak_crypto_pfs_weak_helpers.py, tests/test_kerberos_scanner_realm_ipaddress.py</files>
  <read_first>
    - quirk/scanner/tls_capabilities.py (lines 30-90 — `_try_handshake` definition)
    - quirk/scanner/dnssec_scanner.py (lines 1-90 — `DNSSEC_ALG_MAP` shape)
    - quirk/scanner/saml_scanner.py (lines 50-200 — `SHA1_INDICATORS` definition + call site)
    - quirk/scanner/fingerprint.py (lines 90-130 — `_http_probe_plain`)
    - quirk/scanner/kerberos_scanner.py (lines 40-80 — `_derive_realm`)
    - quirk/scanner/broker_scanner.py (lines 110-130 — local `_is_pfs/_is_weak`)
    - quirk/scanner/email_scanner.py (lines 95-115)
    - quirk/scanner/tls_scanner.py (lines 240-265)
    - quirk/util/weak_crypto.py (full file — Phase 73 public API to extend)
    - quirk/assessment/migration_advisor.py (find `_matches`)
    - tests/test_kerberos_scanner.py (if exists — Vitest-equivalent pytest idiom to follow); else any tests/test_*.py for fixture pattern
    - .planning/phases/77-info-code-quality-audit-ledger/77-CONTEXT.md (D-01..D-06)
    - .planning/phases/77-info-code-quality-audit-ledger/77-RESEARCH.md (C-8, C-9, Patterns 4)
  </read_first>
  <behavior>
    - D-01 (IN-01): Source file `quirk/scanner/tls_capabilities.py` contains a comment block with the phrase `legacy-server posture` immediately above the `_try_handshake` function body. AST-style string assert.
    - D-02 (IN-02): `from quirk.scanner.dnssec_scanner import DNSSEC_ALG_MAP; assert 9 in DNSSEC_ALG_MAP and 11 in DNSSEC_ALG_MAP; assert DNSSEC_ALG_MAP[9][0] == "Reserved" and DNSSEC_ALG_MAP[11][0] == "Reserved"`.
    - D-03 (IN-03): `from quirk.scanner.saml_scanner import _is_sha1` (or whatever the consolidated check name is post-fix); assert `_is_sha1("SHA1")` is True; `_is_sha1("SHA1024")` is **False** (RED proves the bug); `_is_sha1("RSA-SHA1")` is True; `_is_sha1("SHA256")` is False.
    - D-04 (IN-04): Mock socket.sendall via `unittest.mock.patch`; call `_http_probe_plain(mock_sock, host="example.org", port=80)`; capture the bytes; assert `b"Host: example.org" in sent_bytes` and `b"Host: localhost" not in sent_bytes`.
    - D-05 (IN-05): `from quirk.util.weak_crypto import is_pfs_cipher, is_weak_cipher_classification`; assert each is callable. AST-grep test: `import ast; tree = ast.parse(open("quirk/scanner/broker_scanner.py").read()); assert no FunctionDef with name in {"_is_pfs", "_is_weak"} appears` (same for email_scanner.py and tls_scanner.py). Behavior: `is_pfs_cipher("ECDHE-RSA-AES128-GCM-SHA256")` True; `is_pfs_cipher("AES128-SHA")` False.
    - D-06 (IN-06): `from quirk.scanner.kerberos_scanner import _derive_realm`; assert `_derive_realm("10.0.0.1")` returns `"10.0.0.1"` (IPv4 detected by ipaddress, returned upper-case unchanged); `_derive_realm("::1")` returns `"::1"` (IPv6 detected — proves `isdigit()` would fail); `_derive_realm("host.example.com")` returns `"EXAMPLE.COM"`; `_derive_realm("server")` returns `"SERVER"`. AST-grep: `import ast; assert any "ipaddress.ip_address" call in kerberos_scanner.py source`.
  </behavior>
  <action>
    Create six pytest modules — one per requirement. Each test MUST fail before implementation (RED state):

    - `tests/test_tls_capabilities_comment.py`: `import pathlib; src = pathlib.Path("quirk/scanner/tls_capabilities.py").read_text(); assert "legacy-server posture" in src`. RED because the comment is not yet added.
    - `tests/test_dnssec_scanner_reserved_algs.py`: import `DNSSEC_ALG_MAP`, assert keys 9 and 11 present with `Reserved` name. RED because keys absent.
    - `tests/test_saml_scanner_sha1_word_boundary.py`: tests the consolidated detection via `_matches` indirection. RED because current substring `in` matches `SHA1024` as positive.
    - `tests/test_fingerprint_host_header.py`: mock-socket + bytes assertion. RED because `localhost` is hardcoded.
    - `tests/test_weak_crypto_pfs_weak_helpers.py`: import-availability + AST-grep on the 3 caller files for absence of local `_is_pfs/_is_weak`. RED because helpers don't exist and inner functions still present.
    - `tests/test_kerberos_scanner_realm_ipaddress.py`: IPv4 + IPv6 + DNS-style cases. RED because IPv6 case fails under current `isdigit()` heuristic, AND AST-grep finds no `ipaddress` import.

    Run `pytest tests/test_tls_capabilities_comment.py tests/test_dnssec_scanner_reserved_algs.py tests/test_saml_scanner_sha1_word_boundary.py tests/test_fingerprint_host_header.py tests/test_weak_crypto_pfs_weak_helpers.py tests/test_kerberos_scanner_realm_ipaddress.py 2>&1 | tail -30` — confirm all 6 FAIL (RED).

    Commit message: `test(77-01): add failing tests for INFO-01 (D-01..D-06)`.
  </action>
  <verify>
    <automated>pytest tests/test_tls_capabilities_comment.py tests/test_dnssec_scanner_reserved_algs.py tests/test_saml_scanner_sha1_word_boundary.py tests/test_fingerprint_host_header.py tests/test_weak_crypto_pfs_weak_helpers.py tests/test_kerberos_scanner_realm_ipaddress.py 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All 6 test files exist
    - `grep -c "def test_" tests/test_tls_capabilities_comment.py` >= 1
    - `grep -c "def test_" tests/test_dnssec_scanner_reserved_algs.py` >= 1
    - `grep -c "def test_" tests/test_saml_scanner_sha1_word_boundary.py` >= 3
    - `grep -c "def test_" tests/test_fingerprint_host_header.py` >= 1
    - `grep -c "def test_" tests/test_weak_crypto_pfs_weak_helpers.py` >= 2
    - `grep -c "def test_" tests/test_kerberos_scanner_realm_ipaddress.py` >= 3
    - `pytest tests/test_tls_capabilities_comment.py tests/test_dnssec_scanner_reserved_algs.py tests/test_saml_scanner_sha1_word_boundary.py tests/test_fingerprint_host_header.py tests/test_weak_crypto_pfs_weak_helpers.py tests/test_kerberos_scanner_realm_ipaddress.py` exits NON-ZERO (RED)
  </acceptance_criteria>
  <done>
    Six failing test modules committed; pytest exits non-zero with assertion errors tied to D-01..D-06.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement D-01..D-06 in 8 source files</name>
  <files>quirk/scanner/tls_capabilities.py, quirk/scanner/dnssec_scanner.py, quirk/scanner/saml_scanner.py, quirk/scanner/fingerprint.py, quirk/scanner/kerberos_scanner.py, quirk/scanner/broker_scanner.py, quirk/scanner/email_scanner.py, quirk/scanner/tls_scanner.py, quirk/util/weak_crypto.py</files>
  <read_first>
    - All 8 source files at the line ranges in `<interfaces>`
    - quirk/util/weak_crypto.py (full — confirm public-API style to match)
    - quirk/assessment/migration_advisor.py (locate `_matches`)
    - The 6 test files written in Task 1 (so this task matches their expected import names)
  </read_first>
  <action>
    Apply six edits per the `<interfaces>` shapes. Keep diffs surgical (PEP 8; minimal). Cite the audit row in each comment:

    **D-01 (tls_capabilities.py):** Insert a 3-line comment block immediately above the `_try_handshake` body (between the `def` line and the first statement):
    ```
    # WHY: This function intentionally probes TLS 1.0 / 1.1 / SSLv3 bands to
    # inventory legacy-server posture for the CBOM. It is NOT a real
    # handshake-downgrade — see audit scanners-protocol/IN-01.
    ```
    Cite: `# closes scanners-protocol/IN-01`.

    **D-02 (dnssec_scanner.py):** Append to `DNSSEC_ALG_MAP`:
    ```
    9: ("Reserved", "HIGH"),    # IANA Reserved per RFC 8624 — closes scanners-protocol/IN-02
    11: ("Reserved", "HIGH"),   # IANA Reserved per RFC 8624
    ```
    (RESEARCH C-9: alg 7 RSASHA1-NSEC3-SHA1 was already present; adjudication corrected the CONTEXT identification.)

    **D-03 (saml_scanner.py):** Add `from quirk.assessment.migration_advisor import _matches` at the imports block. Replace the existing substring scan around line 188:
    - Before: `if any(ind in algo_str for ind in SHA1_INDICATORS)`
    - After: introduce a module-level helper `def _is_sha1(algo_str: str) -> bool: return any(_matches(algo_str, ind) for ind in SHA1_INDICATORS)` and call `_is_sha1(algo_str)` at the use site. Cite `# closes scanners-protocol/IN-03`.

    **D-04 (fingerprint.py):** Line 111 — replace `f"Host: localhost\\r\\n"` with `f"Host: {host}\\r\\n"`. Cite `# closes scanners-protocol/IN-04`.

    **D-05 (weak_crypto.py + 3 scanners):**
    1. In `quirk/util/weak_crypto.py`, add two module-level helpers next to existing `is_weak_cipher` / `is_legacy_tls_version`:
       ```python
       def is_pfs_cipher(cipher: str) -> bool:
           """Return True iff cipher provides Perfect Forward Secrecy (ECDHE/DHE families).

           Consolidates the duplicated _is_pfs from broker_scanner, email_scanner, tls_scanner.
           Closes scanners-protocol/IN-05.
           """
           if not cipher:
               return False
           token = cipher.upper()
           return token.startswith(("ECDHE", "DHE", "TLS_ECDHE", "TLS_DHE"))

       def is_weak_cipher_classification(cipher: str) -> bool:
           """Return True iff cipher is weak by classification (NULL, EXPORT, RC4, DES, 3DES, MD5, anonymous).

           Consolidates the duplicated _is_weak from broker_scanner, email_scanner, tls_scanner.
           Mirrors but does NOT replace is_weak_cipher (which is the Phase 73 broader classifier).
           Closes scanners-protocol/IN-05.
           """
           if not cipher:
               return False
           token = cipher.upper()
           weak_tokens = ("NULL", "EXPORT", "RC4", "_DES_", "DES-", "3DES", "MD5", "ANON", "ADH", "AECDH")
           return any(t in token for t in weak_tokens)
       ```
       (Researcher note: do NOT collapse this into the existing `is_weak_cipher` — the existing helper has different semantics; preserve API surface.)
    2. In `broker_scanner.py`, `email_scanner.py`, and `tls_scanner.py`:
       - Add `from quirk.util.weak_crypto import is_pfs_cipher, is_weak_cipher_classification` to imports
       - Delete the local `_is_pfs` / `_is_weak` (inner or outer) function definitions
       - Replace call sites: `_is_pfs(x)` → `is_pfs_cipher(x)`, `_is_weak(x)` → `is_weak_cipher_classification(x)`

    **D-06 (kerberos_scanner.py):** Replace `_derive_realm` body per RESEARCH Pattern 4:
    ```python
    import ipaddress  # add at module top if not present

    def _derive_realm(host: str) -> str:
        stripped = host.strip()
        try:
            ipaddress.ip_address(stripped)
            return stripped.upper()
        except ValueError:
            pass
        parts = stripped.split(".")
        if len(parts) >= 2:
            return ".".join(parts[-2:]).upper()
        return stripped.upper()
    ```
    Cite `# closes scanners-protocol/IN-06`.

    Run `python -m compileall quirk/scanner quirk/util` then the 6 targeted pytests. All green.

    Commit message: `feat(77-01): close INFO-01 (D-01..D-06) — protocol scanner INFOs`.
  </action>
  <verify>
    <automated>python -m compileall quirk/scanner quirk/util 2>&1 | tail -5; pytest tests/test_tls_capabilities_comment.py tests/test_dnssec_scanner_reserved_algs.py tests/test_saml_scanner_sha1_word_boundary.py tests/test_fingerprint_host_header.py tests/test_weak_crypto_pfs_weak_helpers.py tests/test_kerberos_scanner_realm_ipaddress.py 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "legacy-server posture" quirk/scanner/tls_capabilities.py` >= 1
    - `grep -cE "^\\s*9:.*Reserved" quirk/scanner/dnssec_scanner.py` >= 1
    - `grep -cE "^\\s*11:.*Reserved" quirk/scanner/dnssec_scanner.py` >= 1
    - `grep -c "from quirk.assessment.migration_advisor import _matches" quirk/scanner/saml_scanner.py` == 1
    - `grep -c "Host: {host}" quirk/scanner/fingerprint.py` >= 1
    - `grep -c "Host: localhost" quirk/scanner/fingerprint.py` == 0
    - `grep -c "def is_pfs_cipher" quirk/util/weak_crypto.py` == 1
    - `grep -c "def is_weak_cipher_classification" quirk/util/weak_crypto.py` == 1
    - `grep -cE "def _is_pfs|def _is_weak" quirk/scanner/broker_scanner.py quirk/scanner/email_scanner.py quirk/scanner/tls_scanner.py` == 0
    - `grep -c "from quirk.util.weak_crypto import" quirk/scanner/broker_scanner.py` >= 1
    - `grep -c "from quirk.util.weak_crypto import" quirk/scanner/email_scanner.py` >= 1
    - `grep -c "from quirk.util.weak_crypto import" quirk/scanner/tls_scanner.py` >= 1
    - `grep -c "ipaddress.ip_address" quirk/scanner/kerberos_scanner.py` >= 1
    - `python -m compileall quirk/scanner quirk/util` exits 0
    - `pytest tests/test_tls_capabilities_comment.py tests/test_dnssec_scanner_reserved_algs.py tests/test_saml_scanner_sha1_word_boundary.py tests/test_fingerprint_host_header.py tests/test_weak_crypto_pfs_weak_helpers.py tests/test_kerberos_scanner_realm_ipaddress.py` exits 0 (GREEN)
  </acceptance_criteria>
  <done>
    All six tests pass; compileall clean; three scanners now import from quirk.util.weak_crypto; kerberos uses ipaddress; SHA1 uses word boundary; DNSSEC alg map has 9 and 11; tls comment cites IN-01.
  </done>
</task>

<task type="auto">
  <name>Task 3: Regression sweep — full protocol scanner test suite</name>
  <files>tests/test_tls_capabilities_comment.py</files>
  <read_first>
    - Output of `pytest tests/test_broker_scanner.py tests/test_email_scanner.py tests/test_tls_scanner.py tests/test_saml_scanner.py tests/test_fingerprint.py tests/test_kerberos_scanner.py tests/test_dnssec_scanner.py -x 2>&1 | tail -40`
  </read_first>
  <action>
    Run the protocol scanner regression band to confirm D-05 dedup didn't break the 3 caller scanners and D-03 word-boundary tightening didn't drop legitimate SHA1 detections:

    ```
    pytest tests/test_broker_scanner.py tests/test_email_scanner.py tests/test_tls_scanner.py tests/test_saml_scanner.py tests/test_fingerprint.py tests/test_kerberos_scanner.py tests/test_dnssec_scanner.py -x
    ```

    If any existing test fails:
    - For D-05 callers (broker/email/tls): the call-site rename `_is_pfs → is_pfs_cipher` may need an additional `not None` guard if the local version handled None differently. Add the guard inside the new helper, not the call site.
    - For D-03 (saml): word-boundary may now reject a previously-matching string that contained `SHA1` as part of a longer token. Verify the test fixture: if it was asserting on `SHA1024` matching positively, that was the bug — update the fixture to the correct expected value and document via a `# Phase 77 D-03: SHA1024 is not SHA-1 — closes scanners-protocol/IN-03` comment in the test.
    - For D-06 (kerberos): if an existing test fed `_derive_realm` an exotic input (e.g., `"1.2.3"` — 3 parts, not a valid IP) it should still produce `"2.3"` per the post-IP-check path. Run; if it breaks, the new code's "len(parts) >= 2 → last two" branch handles it.

    No new edits expected — this task is a verification gate. If failures occur, fix them in this task's commit; do NOT roll back Task 2.

    Commit message (only if fixes applied): `fix(77-01): adjust regression tests for D-03/D-05/D-06 semantic shifts`. If no failures: no commit (verification only).
  </action>
  <verify>
    <automated>pytest tests/test_broker_scanner.py tests/test_email_scanner.py tests/test_tls_scanner.py tests/test_saml_scanner.py tests/test_fingerprint.py tests/test_kerberos_scanner.py tests/test_dnssec_scanner.py -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 7 listed test modules pass under `pytest -x`
    - No new pip dependency added (grep `pyproject.toml` unchanged)
  </acceptance_criteria>
  <done>
    Protocol scanner regression band green; D-05 dedup safe; D-03 word boundary safe; D-06 ipaddress refactor safe.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted host string → IPv4 detection | `kerberos_scanner._derive_realm` receives operator-supplied hostnames; classification feeds realm derivation |
| Cipher string → classification helper | Scanner-emitted cipher strings cross into the centralized weak_crypto helpers |
| Untrusted DNS responses → DNSSEC algorithm map | Algorithm numbers from external DNS responses index into DNSSEC_ALG_MAP |
| Untrusted SHA1 token → SAML algo detection | XML-parsed algorithm URIs/strings cross into _is_sha1 word-boundary check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01-01 | Tampering / Spoofing | kerberos_scanner._derive_realm accepting non-IP strings as IPs (current isdigit heuristic could mis-classify "1.2.3" as a quad-form IP and skip DNS realm derivation) | mitigate | D-06 — ipaddress.ip_address try/except is strict per RFC; ValueError fallthrough drives DNS-style realm derivation |
| T-77-01-02 | Information Disclosure | fingerprint sending Host: localhost to remote vhost-routed servers (could leak intent / unintentionally hit localhost vhosts) | mitigate | D-04 — Host header uses target hostname |
| T-77-01-03 | Tampering | DNSSEC alg 9 / 11 responses currently fall through to UNKNOWN classification (defense-in-depth gap) | mitigate | D-02 — explicit Reserved entries with HIGH severity |
| T-77-01-04 | False-positive (operator-facing) | SHA1 detection matching SHA1024 / SHA1_INTERNAL as SHA-1 weak | mitigate | D-03 — word-boundary via _matches |
| T-77-01-05 | Information Disclosure | Centralized weak_crypto helpers logging or echoing cipher strings | accept | Helpers are pure-functional (no logging); cipher strings are not secrets |
</threat_model>

<verification>
- `pytest tests/test_tls_capabilities_comment.py tests/test_dnssec_scanner_reserved_algs.py tests/test_saml_scanner_sha1_word_boundary.py tests/test_fingerprint_host_header.py tests/test_weak_crypto_pfs_weak_helpers.py tests/test_kerberos_scanner_realm_ipaddress.py -x` passes
- `pytest tests/test_broker_scanner.py tests/test_email_scanner.py tests/test_tls_scanner.py tests/test_saml_scanner.py tests/test_fingerprint.py tests/test_kerberos_scanner.py tests/test_dnssec_scanner.py -x` passes (regression band)
- `python -m compileall quirk/scanner quirk/util` exits 0
- No new pip dependency introduced (D-32)
- 3 scanner files no longer define local `_is_pfs / _is_weak` (D-05)
- 0 audit row flips in this plan (LEDGER-01 consolidated by 77-05)
</verification>

<success_criteria>
- 6 new test modules with RED→GREEN history
- 8 production source files edited
- INFO-01 (IN-01..06) fixes landed; audit-row flips deferred to PLAN 77-05
- Phase 73 weak_crypto module extended with is_pfs_cipher + is_weak_cipher_classification
- Phase 74 migration_advisor._matches reused for SHA1 detection
- ipaddress stdlib used for IPv4 detection in kerberos
- compileall + targeted pytest + regression band all green
</success_criteria>

<output>
Create `.planning/phases/77-info-code-quality-audit-ledger/77-01-SUMMARY.md` documenting: 6 INFO-01 fixes landed, RESEARCH C-8/C-9 adjudications applied (comment-block targeting at line 40 of tls_capabilities.py; alg 9/11 as Reserved not RSASHA1-NSEC3-SHA1), Phase 73 + Phase 74 reuse confirmed (weak_crypto + migration_advisor._matches), regression band passes, audit-row flips deferred to PLAN 77-05.
</output>
