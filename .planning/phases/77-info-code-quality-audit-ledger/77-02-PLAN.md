---
phase: 77-info-code-quality-audit-ledger
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/cbom/builder.py
  - quirk/reports/writer.py
  - quirk/intelligence/trends.py
  - quirk/intelligence/evidence.py
  - quirk/intelligence/roadmap.py
  - quirk/reports/executive.py
  - quirk/reports/html_renderer.py
  - quirk/intelligence/schema.py
  - quirk/intelligence/__init__.py
  - tests/test_intelligence_schema.py
  - tests/test_platform_version_single_source.py
  - tests/test_cbom_builder_ssh_json_error.py
  - tests/test_trends_yield_per.py
  - tests/test_evidence_protocol_keys.py
  - tests/test_roadmap_baseline_governance.py
  - tests/test_executive_truncation_indicator.py
  - tests/test_html_renderer_roadmap_section.py
  - tests/test_writer_hosts_count_filters_falsy.py
  - tests/test_intelligence_public_api.py
autonomous: true
requirements: [INFO-02]
must_haves:
  truths:
    - "`quirk/cbom/builder.py:128` and `quirk/reports/writer.py:23` no longer define their own `PLATFORM_VERSION = \"4.4.0\"` literal — both `from quirk import __version__ as PLATFORM_VERSION` from the canonical `quirk/__init__.py` source per D-07"
    - "`quirk/cbom/builder.py::_extract_ssh_algorithms` (line ~316) catch of `json.JSONDecodeError` logs via `logger.warning(\"...: %s\", safe_str(e))` (Phase 59 `quirk/util/safe_exc.py::safe_str`) and returns `[]` — no longer silent per D-08"
    - "`quirk/intelligence/trends.py::_fetch_session_endpoints` (line 84-99) uses `.yield_per(1000)` instead of `.all()` per D-09 (Discretion: 1000 = SQLAlchemy idiomatic chunk)"
    - "`quirk/intelligence/evidence.py::_PROTOCOL_KEYS` (lines 11-12) extended with the 6 missing keys per RESEARCH inventory: CONTAINER, SOURCE, AWS, AZURE, GCP, CLOUD_SQL (NOT K8S — KUBERNETES already present per RESEARCH C-10) per D-10"
    - "`quirk/intelligence/roadmap.py` (lines 368-437) emits the baseline-governance item BOTH when no baseline-governance item exists AND when total item count < min_items per D-11"
    - "`quirk/reports/executive.py` (lines 228-240) Migration Paths truncation at 10 now appends a `... and {remaining} more (see full report)` indicator when len > 10 per D-12"
    - "`quirk/reports/html_renderer.py::roadmap_section` (lines 82-83) — researcher-flagged C-7: the 2-line filter has no visible dead branch; Wave 0 mutation test proves both `r.get(\"timeframe\") == tf` and `r.get(\"phase\") == tf` branches are reachable. Disposition: D-13 closes as audit-flip-only with mutation evidence (no code change unless test proves a dead branch)"
    - "`quirk/reports/writer.py::hosts_count` (line 222) filters out falsy hosts via comprehension predicate `{h for h in hosts if h}` before set construction per D-14"
    - "`quirk/intelligence/schema.py::IntelligenceReport` dataclass deleted; `quirk/intelligence/__init__.py` no longer exports `IntelligenceReport` (lines 8, 24 removed); `tests/test_intelligence_schema.py` replaced by `tests/test_intelligence_public_api.py` asserting absence from public API (RESEARCH C-1 cascade, Q4 recommendation) per D-15"
    - "`python -m compileall quirk/cbom quirk/reports quirk/intelligence` exits 0; all 9 targeted tests pass"
  artifacts:
    - path: "quirk/cbom/builder.py"
      provides: "PLATFORM_VERSION via __version__ import (D-07); SSH JSONDecodeError logged via safe_str (D-08)"
      contains: "from quirk import __version__"
    - path: "quirk/reports/writer.py"
      provides: "PLATFORM_VERSION via __version__ import (D-07); hosts_count filters falsy (D-14)"
      contains: "from quirk import __version__"
    - path: "quirk/intelligence/trends.py"
      provides: "yield_per(1000) streaming (D-09)"
      contains: "yield_per"
    - path: "quirk/intelligence/evidence.py"
      provides: "Extended _PROTOCOL_KEYS (D-10)"
      contains: "CONTAINER"
    - path: "quirk/intelligence/roadmap.py"
      provides: "Separate baseline-governance check (D-11)"
      contains: "baseline"
    - path: "quirk/reports/executive.py"
      provides: "Migration Paths '... and N more' truncation indicator (D-12)"
      contains: "and {remaining} more"
    - path: "quirk/reports/html_renderer.py"
      provides: "Dead-branch disposition (D-13 — verified via mutation test)"
      contains: "roadmap_section"
    - path: "tests/test_intelligence_public_api.py"
      provides: "Replaces test_intelligence_schema.py — asserts IntelligenceReport NOT in public API (D-15 cascade per RESEARCH C-1, Q4)"
      contains: "IntelligenceReport"
  key_links:
    - from: "quirk/cbom/builder.py"
      to: "quirk/__init__.py::__version__"
      via: "from quirk import __version__ as PLATFORM_VERSION"
      pattern: "from quirk import __version__"
    - from: "quirk/reports/writer.py"
      to: "quirk/__init__.py::__version__"
      via: "from quirk import __version__ as PLATFORM_VERSION"
      pattern: "from quirk import __version__"
    - from: "quirk/cbom/builder.py::_extract_ssh_algorithms"
      to: "quirk/util/safe_exc.py::safe_str"
      via: "logger.warning(...: %s, safe_str(e))"
      pattern: "safe_str"
    - from: "quirk/intelligence/__init__.py"
      to: "quirk/intelligence/schema.py"
      via: "IntelligenceReport export REMOVED per D-15"
      pattern: "IntelligenceReport"
---

<objective>
Close INFO-02 — all 9 CBOM/intelligence INFO findings (cbom-intel-reports/IN-01..09). Per-row adjudications applied from RESEARCH:

1. **D-07 (IN-01)** — PLATFORM_VERSION single source. RESEARCH inventory: only **2 duplicate definitions** at `quirk/cbom/builder.py:128` and `quirk/reports/writer.py:23` (CONTEXT overcounted; the other 2 sites are usages of the duplicates). Replace both with `from quirk import __version__ as PLATFORM_VERSION`. Canonical: `quirk/__init__.py:2 __version__ = "4.4.0"`.

2. **D-08 (IN-02)** — `quirk/cbom/builder.py::_extract_ssh_algorithms` line 316. Replace silent `except json.JSONDecodeError: return []` with `logger.warning("Failed to parse SSH algorithms JSON: %s", safe_str(e)); return []`. Phase 59 `quirk/util/safe_exc.py::safe_str` reuse.

3. **D-09 (IN-03)** — `quirk/intelligence/trends.py::_fetch_session_endpoints` line 99 uses `.all()`. Replace with `.yield_per(1000)` per Discretion D-09 recommendation. Existing SQLAlchemy import at line 21 makes this a one-line change; downstream iteration shape (list-of-rows) is preserved because callers iterate with `for row in result:`.

4. **D-10 (IN-04)** — `quirk/intelligence/evidence.py::_PROTOCOL_KEYS` lines 11-12. Per RESEARCH C-10 inventory: missing 6 keys `CONTAINER, SOURCE, AWS, AZURE, GCP, CLOUD_SQL`. NOT K8S (KUBERNETES already present). NOT VAULT (already present). Adjudication corrects CONTEXT's list.

5. **D-11 (IN-05)** — `quirk/intelligence/roadmap.py:368, 406, 437` baseline-governance check. Currently the item is added when no baseline-governance exists. Add an OR clause: also add when `len(items) < min_items`.

6. **D-12 (IN-06)** — `quirk/reports/executive.py:228-240` Migration Paths truncation at 10. Add `... and {remaining} more (see full report)` indicator when len > 10.

7. **D-13 (IN-07)** — RESEARCH C-7 / Pitfall 9 / A3: `quirk/reports/html_renderer.py:82-83` is a 2-line filter with **no visible dead branch**. Run a mutation test in Task 1 — mutate `r.get("timeframe") == tf` to `False` then `r.get("phase") == tf` to `False`; if BOTH mutations break tests, both branches are reachable and IN-07 is misfiled. Adjudication: if mutation proves both branches alive, IN-07 closes as audit-flip-only with mutation evidence. If mutation shows a dead branch, remove it.

8. **D-14 (IN-08)** — `quirk/reports/writer.py:222` `hosts_count` set construction collapses falsy hosts (`None`, `""`) into a single `""`. Add comprehension predicate `{h for h in hosts if h}`.

9. **D-15 (IN-09)** — RESEARCH C-1 / Pitfall 1: `IntelligenceReport` is NOT zero-importers. Cascade-delete:
   - Delete `quirk/intelligence/schema.py::IntelligenceReport` dataclass (line 83)
   - Remove `IntelligenceReport` from `quirk/intelligence/__init__.py:8` import and `:24` `__all__`
   - **Replace** `tests/test_intelligence_schema.py` with `tests/test_intelligence_public_api.py` asserting `IntelligenceReport` is NOT importable from `quirk.intelligence` (Q4 recommendation: replace, so cascade is enforced via test rather than just applied).

Per D-32: no new pip deps, no schema migrations, no feature work. Audit-row flips for IN-01..09 deferred to PLAN 77-05.

Purpose: Centralize version source (D-07), surface previously-silent SSH parse errors (D-08), stream trend queries (D-09), complete the protocol taxonomy (D-10), make roadmap governance unconditional (D-11), make executive truncation transparent (D-12), close the html_renderer dead-branch uncertainty (D-13), filter empty hosts (D-14), and remove the dead IntelligenceReport dataclass (D-15).
Output: 7 production source edits, 2 source deletions (schema.py dataclass + __init__.py export), 1 test-module replacement, 9 new/replacement test files, 0 audit row flips (deferred to 77-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/77-info-code-quality-audit-ledger/77-CONTEXT.md
@.planning/phases/77-info-code-quality-audit-ledger/77-RESEARCH.md

<interfaces>
Existing surfaces (verified at HEAD cf2417a per RESEARCH primary sources):

quirk/__init__.py:2 — `__version__ = "4.4.0"` (canonical for D-07)

quirk/cbom/builder.py:128 — `PLATFORM_VERSION = "4.4.0"` (duplicate; D-07)
quirk/cbom/builder.py:316 — `_extract_ssh_algorithms` JSON parse site (D-08); needs `from quirk.util.safe_exc import safe_str` import added
quirk/cbom/builder.py:659 — `version=PLATFORM_VERSION` (consumer; unchanged after D-07 import switch)

quirk/reports/writer.py:23 — `PLATFORM_VERSION = "4.4.0"` (duplicate; D-07)
quirk/reports/writer.py:222 — `hosts_count` set construction (D-14)
quirk/reports/writer.py:235 — `PLATFORM_VERSION` consumer (unchanged)

quirk/intelligence/trends.py:21 — SQLAlchemy Session import (already present)
quirk/intelligence/trends.py:84-99 — `_fetch_session_endpoints` (D-09)

quirk/intelligence/evidence.py:11-12 — `_PROTOCOL_KEYS` tuple (D-10).
Current (per RESEARCH primary): `("TLS", "HTTP", "SSH", "UNKNOWN", "KERBEROS", "SAML", "DNSSEC", "POSTGRESQL", "MYSQL", "RDS", "S3", "AZURE_BLOB", "KUBERNETES", "VAULT")`.
Append: `"CONTAINER", "SOURCE", "AWS", "AZURE", "GCP", "CLOUD_SQL"` (6 keys per RESEARCH inventory).

quirk/intelligence/roadmap.py:368, 406, 437 — baseline-governance check sites (D-11)

quirk/reports/executive.py:228-240 — Migration Paths truncation loop (D-12)

quirk/reports/html_renderer.py:82-83 — `roadmap_section` filter (D-13)
Current:
```python
def roadmap_section(tf: str) -> List[Dict]:
    return [r for r in (roadmap_items or []) if r.get("timeframe") == tf or r.get("phase") == tf]
```

quirk/intelligence/schema.py:83 — `IntelligenceReport` dataclass (D-15 delete target)
quirk/intelligence/__init__.py:8 — `from quirk.intelligence.schema import IntelligenceReport`
quirk/intelligence/__init__.py:24 — `"IntelligenceReport"` in `__all__`
tests/test_intelligence_schema.py:7,16,58 — importer + 2 constructor call sites (DELETE entire module; replace with test_intelligence_public_api.py)

Phase 59 helper:
```python
# quirk/util/safe_exc.py
def safe_str(exc: BaseException, limit: int = 200) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing tests for D-07..D-15 (9 new/replacement test modules)</name>
  <files>tests/test_platform_version_single_source.py, tests/test_cbom_builder_ssh_json_error.py, tests/test_trends_yield_per.py, tests/test_evidence_protocol_keys.py, tests/test_roadmap_baseline_governance.py, tests/test_executive_truncation_indicator.py, tests/test_html_renderer_roadmap_section.py, tests/test_writer_hosts_count_filters_falsy.py, tests/test_intelligence_public_api.py</files>
  <read_first>
    - quirk/cbom/builder.py (lines 120-145, 300-330, 650-670)
    - quirk/reports/writer.py (lines 15-30, 215-240)
    - quirk/intelligence/trends.py (lines 80-110, full _fetch_session_endpoints)
    - quirk/intelligence/evidence.py (full file — _PROTOCOL_KEYS)
    - quirk/intelligence/roadmap.py (lines 360-450 — baseline-governance section)
    - quirk/reports/executive.py (lines 220-250 — Migration Paths section)
    - quirk/reports/html_renderer.py (lines 75-110 — roadmap_section + callers)
    - quirk/intelligence/schema.py (full file — IntelligenceReport)
    - quirk/intelligence/__init__.py (lines 1-30)
    - tests/test_intelligence_schema.py (full file — for the test_intelligence_public_api.py replacement design)
    - quirk/util/safe_exc.py (confirm safe_str signature)
    - .planning/phases/77-info-code-quality-audit-ledger/77-CONTEXT.md (D-07..D-15)
    - .planning/phases/77-info-code-quality-audit-ledger/77-RESEARCH.md (C-1, C-7, C-10, Pitfalls 1, 9)
  </read_first>
  <behavior>
    - D-07: AST gate — `import ast; tree = ast.parse(open("quirk/cbom/builder.py").read())` — assert NO `Assign` node with target `PLATFORM_VERSION` and `Constant` value (literal). Similarly for writer.py. Assert `from quirk import __version__` is present in both via grep.
    - D-08: Mock `json.loads` to raise `json.JSONDecodeError("synthetic", "doc", 0)`. Capture `logger.warning` calls via `caplog`. Assert the catch logged a `WARNING`-level message containing `synthetic` (sanitized via safe_str) AND returned `[]`.
    - D-09: Patch `quirk.intelligence.trends.Session.query` to a `MagicMock`. Call `_fetch_session_endpoints(session, scan_id)`. Assert that `.yield_per` was called with `1000` (mock-spy assertion).
    - D-10: `from quirk.intelligence.evidence import _PROTOCOL_KEYS; expected = {"CONTAINER","SOURCE","AWS","AZURE","GCP","CLOUD_SQL"}; assert expected.issubset(set(_PROTOCOL_KEYS))`. Also assert KUBERNETES is still present and VAULT is still present (regression guard).
    - D-11: Build a fixture roadmap input with `min_items = 5` and 2 existing non-baseline items; call the roadmap-build function; assert the output contains a baseline-governance item. Then build a fixture with 6 non-baseline items (>= min_items) AND no baseline-governance item; assert baseline-governance is STILL added (the missing-baseline branch).
    - D-12: Build a fixture with 15 migration paths; render executive report section; assert output contains `"and 5 more"` substring.
    - D-13: Mutation/coverage test. Two sub-tests: (a) call `roadmap_section("NOW")` with fixture having `r.get("timeframe") == "NOW"`; assert non-empty result; (b) call `roadmap_section("ROAD")` with fixture having `r.get("phase") == "ROAD"` (no timeframe match); assert non-empty result. Both branches reachable proves NO dead branch — IN-07 audit-flip-only.
    - D-14: `from quirk.reports.writer import _build_hosts_count` (or whatever the helper is; if inline, refactor minimally). Pass `[None, "", "host1", "host1", "host2"]`; assert result is `2` (deduped real hosts, falsy excluded). Without fix: returns `3` (includes the empty-string entry).
    - D-15 (replacement test): `tests/test_intelligence_public_api.py` — `def test_intelligence_report_not_in_public_api(): from quirk import intelligence; assert not hasattr(intelligence, "IntelligenceReport"); assert "IntelligenceReport" not in getattr(intelligence, "__all__", [])`. Also `def test_intelligence_report_import_raises(): with pytest.raises(ImportError): from quirk.intelligence import IntelligenceReport`. Also `def test_intelligence_schema_module_does_not_define_class(): import quirk.intelligence.schema as s; assert not hasattr(s, "IntelligenceReport")`.
  </behavior>
  <action>
    Create 9 pytest modules per shapes above. RED requirement: each test must fail BEFORE Task 2 lands the fixes.

    Special handling:
    - **D-15 cascade**: do NOT delete `tests/test_intelligence_schema.py` in this task — let it stay GREEN (it will become RED naturally once Task 2 deletes the dataclass). Task 2 deletes `tests/test_intelligence_schema.py` AS PART of the D-15 implementation.
    - **D-13 mutation**: if both branches prove reachable (expected per RESEARCH C-7), document in the test docstring: `"""C-7 verification: both branches reachable; IN-07 closes audit-flip-only."""`. No production code change needed for D-13 in Task 2.
    - **D-14**: if the falsy-filter must operate on inline code rather than an extractable helper, the test can use `subprocess` to import and call `compute_writer_summary` (or whatever public function emits hosts_count) with a fixture scan_json. Prefer extracting the falsy filter logic into a small helper that the test can import directly.

    Run `pytest tests/test_platform_version_single_source.py tests/test_cbom_builder_ssh_json_error.py tests/test_trends_yield_per.py tests/test_evidence_protocol_keys.py tests/test_roadmap_baseline_governance.py tests/test_executive_truncation_indicator.py tests/test_html_renderer_roadmap_section.py tests/test_writer_hosts_count_filters_falsy.py tests/test_intelligence_public_api.py 2>&1 | tail -40` — confirm all 9 FAIL except possibly D-13 (which may PASS if both branches are already reachable; that's the C-7 verification path).

    Commit message: `test(77-02): add failing tests for INFO-02 (D-07..D-15) — RESEARCH C-1/C-7/C-10 adjudications applied`.
  </action>
  <verify>
    <automated>pytest tests/test_platform_version_single_source.py tests/test_cbom_builder_ssh_json_error.py tests/test_trends_yield_per.py tests/test_evidence_protocol_keys.py tests/test_roadmap_baseline_governance.py tests/test_executive_truncation_indicator.py tests/test_html_renderer_roadmap_section.py tests/test_writer_hosts_count_filters_falsy.py tests/test_intelligence_public_api.py 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All 9 test files exist
    - `grep -c "def test_" tests/test_platform_version_single_source.py` >= 2
    - `grep -c "def test_" tests/test_cbom_builder_ssh_json_error.py` >= 1
    - `grep -c "def test_" tests/test_trends_yield_per.py` >= 1
    - `grep -c "def test_" tests/test_evidence_protocol_keys.py` >= 1
    - `grep -c "def test_" tests/test_roadmap_baseline_governance.py` >= 2
    - `grep -c "def test_" tests/test_executive_truncation_indicator.py` >= 1
    - `grep -c "def test_" tests/test_html_renderer_roadmap_section.py` >= 2
    - `grep -c "def test_" tests/test_writer_hosts_count_filters_falsy.py` >= 1
    - `grep -c "def test_" tests/test_intelligence_public_api.py` >= 3
    - `pytest tests/test_platform_version_single_source.py tests/test_cbom_builder_ssh_json_error.py tests/test_trends_yield_per.py tests/test_evidence_protocol_keys.py tests/test_roadmap_baseline_governance.py tests/test_executive_truncation_indicator.py tests/test_writer_hosts_count_filters_falsy.py tests/test_intelligence_public_api.py` exits NON-ZERO (RED for at least D-07,08,09,10,11,12,14,15)
    - D-13 (`tests/test_html_renderer_roadmap_section.py`) may PASS (C-7 verification) — log status in commit body
  </acceptance_criteria>
  <done>
    Nine pytest modules committed; 8 in RED state; D-13 status logged (RED = real dead branch; GREEN = C-7 confirmed audit-flip-only).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement D-07..D-15 in 9 source files</name>
  <files>quirk/cbom/builder.py, quirk/reports/writer.py, quirk/intelligence/trends.py, quirk/intelligence/evidence.py, quirk/intelligence/roadmap.py, quirk/reports/executive.py, quirk/reports/html_renderer.py, quirk/intelligence/schema.py, quirk/intelligence/__init__.py, tests/test_intelligence_schema.py</files>
  <read_first>
    - All 9 source files at line ranges in `<interfaces>`
    - quirk/util/safe_exc.py (confirm safe_str signature)
    - tests/test_intelligence_schema.py (full — for deletion)
    - Test files written in Task 1 (so this task matches their expected interfaces)
  </read_first>
  <action>
    Apply edits per `<interfaces>` shapes. Cite the IN-NN row in each comment block:

    **D-07 (cbom/builder.py + reports/writer.py):**
    - In `quirk/cbom/builder.py:128`: replace `PLATFORM_VERSION = "4.4.0"` with `from quirk import __version__ as PLATFORM_VERSION  # closes cbom-intel-reports/IN-01`. Move the import to the imports block if pep8 demands.
    - In `quirk/reports/writer.py:23`: same replacement.

    **D-08 (cbom/builder.py:316):**
    - Add `from quirk.util.safe_exc import safe_str` to imports if absent
    - Add `import logging; logger = logging.getLogger(__name__)` if absent
    - At line 316 `except json.JSONDecodeError as e:` (currently silent), add `logger.warning("Failed to parse SSH algorithms JSON: %s", safe_str(e))` then `return []`. Cite `# closes cbom-intel-reports/IN-02`.

    **D-09 (intelligence/trends.py:99):**
    - At line 99 replace `.all()` with `.yield_per(1000)`. The caller iterates — semantics preserved. Cite `# closes cbom-intel-reports/IN-03 — yield_per(1000) per Phase 77 D-09`.

    **D-10 (intelligence/evidence.py:11-12):**
    - Append to `_PROTOCOL_KEYS` tuple: `"CONTAINER", "SOURCE", "AWS", "AZURE", "GCP", "CLOUD_SQL"` (6 keys per RESEARCH C-10 inventory). Cite `# closes cbom-intel-reports/IN-04`.

    **D-11 (intelligence/roadmap.py:368-437):**
    - Wherever the baseline-governance item is conditionally added (RESEARCH cites lines 368, 406, 437), refactor to an OR check: `if no_baseline_item or len(items) < min_items: items.append(baseline_item)`. Be conservative — only add a single baseline item even when both conditions fire. Cite `# closes cbom-intel-reports/IN-05`.

    **D-12 (reports/executive.py:228-240):**
    - Where the loop currently breaks at index 10, after the break add:
      ```python
      if remaining := max(0, len(paths) - 10):
          rendered.append(f"... and {remaining} more (see full report)")
      ```
      Cite `# closes cbom-intel-reports/IN-06`.

    **D-13 (reports/html_renderer.py:82-83):**
    - If Task 1's D-13 test PASSED (both branches reachable per RESEARCH C-7): NO CODE CHANGE; close as audit-flip-only. Add a `# C-7 verification: both branches reachable; closes cbom-intel-reports/IN-07 as audit-flip-only` comment above the function.
    - If Task 1's D-13 test FAILED revealing a real dead branch: remove the dead branch. Document the mutation evidence in the commit message.

    **D-14 (reports/writer.py:222):**
    - At line 222 (or wherever hosts_count set is constructed), add `if h` predicate: `{h for h in hosts if h}` instead of `{h for h in hosts}`. Cite `# closes cbom-intel-reports/IN-08`.
    - If the construction is inlined into a larger comprehension, refactor minimally — extract to a tiny helper `def _unique_hosts(hosts: Iterable[str]) -> set[str]: return {h for h in hosts if h}` so the test can import it.

    **D-15 (intelligence/schema.py + __init__.py + delete tests/test_intelligence_schema.py):**
    1. Delete the `IntelligenceReport` dataclass from `quirk/intelligence/schema.py:83` (the class and any leading/trailing decorators or comments tied to it ONLY).
    2. Remove line 8 `from quirk.intelligence.schema import IntelligenceReport` from `quirk/intelligence/__init__.py`.
    3. Remove `"IntelligenceReport"` from `__all__` at line 24.
    4. **Delete `tests/test_intelligence_schema.py`** entirely (Q4 recommendation: the replacement test is `tests/test_intelligence_public_api.py` from Task 1).
    5. Cite `# closes cbom-intel-reports/IN-09 — IntelligenceReport removed in Phase 77 (D-15); RESEARCH C-1 cascade applied` in the `__init__.py` location where the import was.

    Run `python -m compileall quirk/cbom quirk/reports quirk/intelligence` then all 9 targeted pytests. All green.

    Commit message: `feat(77-02): close INFO-02 (D-07..D-15) — CBOM/intelligence INFOs; D-15 IntelligenceReport cascade-deleted per RESEARCH C-1`.
  </action>
  <verify>
    <automated>python -m compileall quirk/cbom quirk/reports quirk/intelligence 2>&1 | tail -5; pytest tests/test_platform_version_single_source.py tests/test_cbom_builder_ssh_json_error.py tests/test_trends_yield_per.py tests/test_evidence_protocol_keys.py tests/test_roadmap_baseline_governance.py tests/test_executive_truncation_indicator.py tests/test_html_renderer_roadmap_section.py tests/test_writer_hosts_count_filters_falsy.py tests/test_intelligence_public_api.py 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from quirk import __version__" quirk/cbom/builder.py` >= 1
    - `grep -c "from quirk import __version__" quirk/reports/writer.py` >= 1
    - `grep -cE "^PLATFORM_VERSION = \"4\\.4\\.0\"" quirk/cbom/builder.py` == 0
    - `grep -cE "^PLATFORM_VERSION = \"4\\.4\\.0\"" quirk/reports/writer.py` == 0
    - `grep -c "safe_str" quirk/cbom/builder.py` >= 1
    - `grep -c "yield_per" quirk/intelligence/trends.py` >= 1
    - `grep -cE "CONTAINER|SOURCE|CLOUD_SQL" quirk/intelligence/evidence.py` >= 3
    - `grep -c "and {remaining} more" quirk/reports/executive.py` >= 1
    - `grep -c "IntelligenceReport" quirk/intelligence/schema.py` == 0
    - `grep -c "IntelligenceReport" quirk/intelligence/__init__.py` == 0
    - `test -f tests/test_intelligence_schema.py` returns 1 (file does NOT exist post-delete)
    - `test -f tests/test_intelligence_public_api.py` returns 0 (replacement exists)
    - `python -m compileall quirk/cbom quirk/reports quirk/intelligence` exits 0
    - `pytest tests/test_platform_version_single_source.py tests/test_cbom_builder_ssh_json_error.py tests/test_trends_yield_per.py tests/test_evidence_protocol_keys.py tests/test_roadmap_baseline_governance.py tests/test_executive_truncation_indicator.py tests/test_html_renderer_roadmap_section.py tests/test_writer_hosts_count_filters_falsy.py tests/test_intelligence_public_api.py` exits 0 (GREEN)
  </acceptance_criteria>
  <done>
    All 9 INFO-02 tests pass; IntelligenceReport fully removed (schema + export + test); compileall clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Regression sweep — CBOM/intelligence/reports test bands</name>
  <files>tests/test_evidence_protocol_keys.py</files>
  <read_first>
    - Output of full Python suite scoped to the affected subsystems
  </read_first>
  <action>
    Run the regression band to confirm the 9 D-NN edits did not break siblings:

    ```
    pytest tests/test_cbom_builder.py tests/test_writer.py tests/test_executive_report.py tests/test_html_renderer.py tests/test_roadmap.py tests/test_trends.py tests/test_evidence.py -x
    ```

    If any failure traces back to a D-NN change:
    - D-09 yield_per may break a test that asserted on `.all()` invocation — update the test to assert on `.yield_per(1000)`.
    - D-10 evidence keys may shift a count in an existing test expecting len(_PROTOCOL_KEYS) == N — bump the literal to N+6 with `# Phase 77 D-10` comment.
    - D-11 baseline duplication: add a defensive `if not any(item.id == "baseline-governance" for item in items)` guard inside the new branch to avoid double-add.
    - D-15 cascade: search `grep -rn "IntelligenceReport" quirk/ tests/` should return zero matches. If still matches, finish the cascade in this task's commit.

    Commit message (only if fixes applied): `fix(77-02): adjust regression tests for INFO-02 semantic shifts`. If clean: verification only — no commit.
  </action>
  <verify>
    <automated>pytest tests/test_cbom_builder.py tests/test_writer.py tests/test_executive_report.py tests/test_html_renderer.py tests/test_roadmap.py tests/test_trends.py tests/test_evidence.py -x 2>&1 | tail -25; grep -rcn "IntelligenceReport" quirk/ tests/ 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - All 7 regression test modules pass under `pytest -x`
    - `grep -rcn "IntelligenceReport" quirk/ tests/` returns 0 total matches (cascade complete)
    - No new pip dependency added
  </acceptance_criteria>
  <done>
    CBOM/intelligence/reports regression band green; IntelligenceReport fully eradicated; D-09/D-10/D-11 semantic shifts absorbed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External SSH banner → JSON parser | Untrusted SSH text crosses into `json.loads` inside `_extract_ssh_algorithms`; previously swallowed errors masked malformed input |
| Scanner emission → _PROTOCOL_KEYS membership | Protocol strings from container/source/aws/azure/gcp/cloud_sql connectors cross into evidence filtering |
| Roadmap input items → baseline-governance synthesizer | Item list shape governs the baseline-governance presence; missing-OR-low-count branches widen the coverage |
| External version string → CBOM output | PLATFORM_VERSION embedded into CBOM `version=` field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-02-01 | Repudiation | Silent JSONDecodeError on SSH algorithms (operator cannot diagnose why algorithms missing from CBOM) | mitigate | D-08 — logger.warning with safe_str |
| T-77-02-02 | Tampering | PLATFORM_VERSION drift across modules (CBOM emits stale 4.4.0 after a version bump) | mitigate | D-07 — single source via quirk.__version__ |
| T-77-02-03 | Information Disclosure | safe_str wrapping JSON exception text could leak partial document — preserved Phase 59 sanitization | accept | safe_str is the established sanitization path |
| T-77-02-04 | Denial of Service | Trend session fetch materializes all rows in memory — large session blows RSS | mitigate | D-09 — yield_per(1000) bounded streaming |
| T-77-02-05 | Information Disclosure | Migration Paths truncation hides next 5-50 rows silently — operator may make decisions on incomplete data | mitigate | D-12 — explicit "... and N more" indicator |
| T-77-02-06 | Tampering | Dead `IntelligenceReport` dataclass in public API surface area enables future drift / misuse | mitigate | D-15 cascade-delete + test guard |
</threat_model>

<verification>
- All 9 INFO-02 targeted tests pass
- Regression band (`pytest tests/test_cbom_builder.py tests/test_writer.py tests/test_executive_report.py tests/test_html_renderer.py tests/test_roadmap.py tests/test_trends.py tests/test_evidence.py -x`) passes
- `python -m compileall quirk/cbom quirk/reports quirk/intelligence` exits 0
- `grep -rn "IntelligenceReport" quirk/ tests/` returns 0 matches
- `tests/test_intelligence_schema.py` deleted; `tests/test_intelligence_public_api.py` exists
- No new pip dependency (D-32)
- 0 audit row flips (consolidated by 77-05)
</verification>

<success_criteria>
- 9 new/replacement test modules with RED→GREEN history (D-13 may be GREEN-from-start per C-7)
- 10 files edited (7 production + 2 module-surface + 1 test deletion)
- INFO-02 (IN-01..09) fixes landed; audit-row flips deferred to PLAN 77-05
- D-15 cascade complete (schema + __init__ + test deleted)
- Single-source PLATFORM_VERSION confirmed
- safe_str reused for D-08 (Phase 59)
</success_criteria>

<output>
Create `.planning/phases/77-info-code-quality-audit-ledger/77-02-SUMMARY.md` documenting: 9 INFO-02 fixes landed, RESEARCH C-1 cascade-delete applied (IntelligenceReport eradicated), RESEARCH C-7 D-13 disposition (audit-flip-only if both branches reachable), RESEARCH C-10 evidence keys (6 added, not 7 — KUBERNETES already present), D-09 yield_per chunk size 1000 per Discretion, D-15 test replacement rationale (Q4: replace not just delete), audit-row flips deferred to PLAN 77-05.
</output>
