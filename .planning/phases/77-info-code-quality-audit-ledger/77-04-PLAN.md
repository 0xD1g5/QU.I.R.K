---
phase: 77-info-code-quality-audit-ledger
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dashboard/src/pages/qramm-assessment.tsx
  - src/dashboard/src/pages/cbom.tsx
  - src/dashboard/src/pages/roadmap.tsx
  - src/dashboard/src/pages/findings.tsx
  - src/dashboard/src/pages/identity.tsx
  - src/dashboard/src/pages/print.tsx
  - src/dashboard/src/hooks/useQRAMMSession.ts
  - src/dashboard/src/hooks/useScanData.ts
  - src/dashboard/src/pages/__tests__/qramm-assessment-tab-comment.test.tsx
  - src/dashboard/src/pages/__tests__/cbom-cytoscape-catch.test.tsx
  - src/dashboard/src/pages/__tests__/findings-columns-memo.test.tsx
  - src/dashboard/src/hooks/__tests__/useQRAMMSession-reset.test.ts
  - src/dashboard/src/pages/__tests__/cbom-compByAlg-statistic.test.tsx
  - src/dashboard/src/pages/__tests__/print-no-createElement.test.tsx
  - src/dashboard/src/hooks/__tests__/useScanData-error-url.test.ts
autonomous: true
requirements: [INFO-04]
must_haves:
  truths:
    - "`src/dashboard/src/pages/qramm-assessment.tsx:246` comment is corrected from `/* 5-tab assessment layout */` to match the 6 tabs actually rendered at lines 248-254 (CVI, SGRM, DPE, ITR, Scorecard, Compliance Map) per D-23"
    - "`src/dashboard/src/pages/cbom.tsx:19-23` AND `src/dashboard/src/pages/roadmap.tsx:13` cytoscape extension catch blocks log via `console.error('cytoscape.use(...) failed:', e)` AND re-throw non-\"already registered\" errors (guarded by message regex per RESEARCH C-12 / Pattern 8 — naive re-throw would break HMR) per D-24"
    - "`src/dashboard/src/pages/findings.tsx:53` AND `src/dashboard/src/pages/identity.tsx:71` column-array definitions are wrapped in `useMemo<ColumnDef<...>[]>(() => [...], [])` per D-25"
    - "`src/dashboard/src/hooks/useQRAMMSession.ts` exports a new `resetSession()` callback that sets `seededRef.current = null` (or `false`), AND a caller-facing API consumer (qramm-assessment or qramm-profile) wires this into the New Assessment flow per D-26 + RESEARCH code example"
    - "`src/dashboard/src/pages/cbom.tsx:148, 285, 317, 390` `compByAlg[d.label]?.[0]` representative lookups are replaced with a 'first non-zero' helper (Discretion D-27 — researcher recommendation: O(1), preserves existing 'any representative' semantic) per D-27"
    - "`src/dashboard/src/pages/print.tsx:1, 377-380` no longer imports/uses `React.createElement` for style injection; uses standard JSX `<style>{PRINT_CSS}</style>` element per D-28 + RESEARCH Pattern 7"
    - "`src/dashboard/src/hooks/useScanData.ts:51` error message includes the actual fetch URL per D-29 + RESEARCH Pitfall 8 (`Failed to fetch ${url}: ${resp.status} ${resp.statusText}`); URL hoisted out of try-block per Pitfall 8"
    - "`cd src/dashboard && npm run build` exits 0 (build-gate per CONTEXT test_strategy)"
    - "`cd src/dashboard && npm test -- qramm-assessment-tab-comment cbom-cytoscape-catch findings-columns-memo useQRAMMSession-reset cbom-compByAlg-statistic print-no-createElement useScanData-error-url` exits 0"
  artifacts:
    - path: "src/dashboard/src/pages/qramm-assessment.tsx"
      provides: "Corrected tab-count comment (D-23)"
      contains: "6-tab"
    - path: "src/dashboard/src/pages/cbom.tsx"
      provides: "Cytoscape catch + console.error + guarded re-throw (D-24); compByAlg first-non-zero (D-27)"
      contains: "console.error"
    - path: "src/dashboard/src/pages/roadmap.tsx"
      provides: "Same cytoscape catch pattern (D-24)"
      contains: "console.error"
    - path: "src/dashboard/src/pages/findings.tsx"
      provides: "useMemo-wrapped columns (D-25)"
      contains: "useMemo"
    - path: "src/dashboard/src/pages/identity.tsx"
      provides: "useMemo-wrapped columns (D-25)"
      contains: "useMemo"
    - path: "src/dashboard/src/pages/print.tsx"
      provides: "JSX <style> instead of createElement (D-28)"
      contains: "<style>"
    - path: "src/dashboard/src/hooks/useQRAMMSession.ts"
      provides: "resetSession callback (D-26)"
      contains: "resetSession"
    - path: "src/dashboard/src/hooks/useScanData.ts"
      provides: "URL-bearing error message (D-29)"
      contains: "Failed to fetch ${"
  key_links:
    - from: "src/dashboard/src/pages/cbom.tsx try/catch"
      to: "console.error + guarded re-throw"
      via: "if (!(e instanceof Error) || !/already/i.test(e.message)) throw e"
      pattern: "/already/i"
    - from: "src/dashboard/src/hooks/useQRAMMSession.ts::resetSession"
      to: "qramm-assessment.tsx New Assessment flow"
      via: "destructure resetSession from useQRAMMSession; call after archiveAndReset()"
      pattern: "resetSession\\(\\)"
    - from: "src/dashboard/src/hooks/useScanData.ts:51"
      to: "fetch URL variable hoisted out of try-block"
      via: "const url = ... before try; interpolated into setError"
      pattern: "Failed to fetch \\$\\{url"
    - from: "src/dashboard/src/pages/print.tsx"
      to: "JSX <style> element"
      via: "<style>{PRINT_CSS}</style> inside <head>"
      pattern: "<style>\\{PRINT_CSS\\}</style>"
---

<objective>
Close INFO-04 — all 7 React frontend INFO findings (react-frontend/IN-01..07). Per-row adjudications applied from RESEARCH:

1. **D-23 (IN-01)** — `qramm-assessment.tsx:246` comment claims `/* 5-tab */`; renders 6 tabs at lines 248-254 (CVI, SGRM, DPE, ITR, Scorecard, Compliance Map per RESEARCH primary). Comment-only fix.

2. **D-24 (IN-02)** — `cbom.tsx:19-23` and `roadmap.tsx:13` cytoscape extension registration uses `try { cytoscape.use(X) } catch { /* swallow */ }`. Per RESEARCH C-12 / Pitfall (naive re-throw breaks HMR): apply Pattern 8 (message-regex-guarded re-throw):
   ```typescript
   try {
       cytoscape.use(coseBilkent)
   } catch (e) {
       console.error('cytoscape.use(coseBilkent) failed:', e)
       if (!(e instanceof Error) || !/already/i.test(e.message)) throw e
   }
   ```
   This satisfies CONTEXT D-24 (log + re-throw) while preserving HMR safety.

3. **D-25 (IN-03)** — `findings.tsx:53` and `identity.tsx:71` `columns` arrays recreated each render. Wrap in `useMemo<ColumnDef<RowType>[]>(() => [...], [])` per RESEARCH Pattern 6 (likely empty deps since cells reference `row.original`; if a cell closes over external state, include the relevant dep).

4. **D-26 (IN-04)** — `useQRAMMSession.ts:19, 67-68, 101-102` `seededRef` is set to `latest.session_id` after seed; no reset path on "New Assessment" flow trigger. Add `resetSession` callback (RESEARCH code example): `const resetSession = useCallback(() => { seededRef.current = null }, [])`. Wire into the New Assessment flow caller (qramm-assessment.tsx or qramm-profile.tsx — confirm via read_first which page initiates the flow).

5. **D-27 (IN-05)** — `cbom.tsx:148, 285, 317, 390` `compByAlg[d.label]?.[0]` representative. Per Discretion D-27 (Researcher recommendation: "first non-zero" — O(1), preserves existing semantic), introduce a small helper:
   ```typescript
   function firstNonZeroComp<T extends { count: number }>(comps: T[] | undefined): T | undefined {
       return comps?.find((c) => c.count > 0) ?? comps?.[0]
   }
   ```
   Replace the 4 `[0]` representative lookups with `firstNonZeroComp(compByAlg[d.label])`.

6. **D-28 (IN-06)** — `print.tsx:1, 377-380` uses `React.createElement("style", null, PRINT_CSS)`. Replace with JSX `<style>{PRINT_CSS}</style>` per RESEARCH Pattern 7. Remove the `createElement` import if it becomes unused.

7. **D-29 (IN-07)** — `useScanData.ts:51` error message lacks URL. Per RESEARCH Pitfall 8: URL is constructed at lines 29-31 inside the try-block. Hoist the URL constant out of try, then interpolate into the error: `setError(\`Failed to fetch ${url}: ${resp.status} ${resp.statusText}\`)`.

Per D-32: no Recharts swaps, no QRAMM-120-question taxonomy edits, no new npm deps. Phase 76 fixes preserved exactly (especially the BR-05 cleanup effect in print.tsx — not touched here either). Audit-row flips for IN-01..07 deferred to PLAN 77-05.

**Build gate (CONTEXT test_strategy):** `cd src/dashboard && npm run build` exit 0 is REQUIRED after the implementation task and before considering the plan complete.

Purpose: Polish 7 frontend code-quality findings — accurate tab-count comment (D-23), HMR-safe loud cytoscape failures (D-24), memoized column arrays for TanStack-table stable identity (D-25), proper QRAMM session reset on New Assessment (D-26), variance-preserving compByAlg representative (D-27), idiomatic JSX style injection (D-28), URL-bearing fetch errors (D-29).
Output: 8 production source edits (7 .tsx + 1 hook .ts; plus useScanData hook), 7 new Vitest modules, `npm run build` exit 0, 0 audit row flips (deferred to 77-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/77-info-code-quality-audit-ledger/77-CONTEXT.md
@.planning/phases/77-info-code-quality-audit-ledger/77-RESEARCH.md
@.planning/phases/76-react-frontend-warnings/76-01-PLAN.md

<interfaces>
Existing surfaces (verified at HEAD cf2417a per RESEARCH primary sources):

src/dashboard/src/pages/qramm-assessment.tsx:
- Line 246: `/* 5-tab assessment layout */` (D-23 target — comment)
- Lines 248-254: 6 `<TabsTrigger>` for CVI, SGRM, DPE, ITR, Scorecard, Compliance Map
- (Also contains the New Assessment flow consumer — wire D-26 here)

src/dashboard/src/pages/cbom.tsx:
- Lines 19-23: `try { cytoscape.use(coseBilkent) } catch { /* already registered */ }` (D-24)
- Lines 148, 285, 317, 390: `compByAlg[d.label]?.[0]` (D-27)

src/dashboard/src/pages/roadmap.tsx:
- Line 13: `try { cytoscape.use(dagre) } catch { /* already */ }` (D-24)

src/dashboard/src/pages/findings.tsx:
- Line 53: `const columns: ColumnDef<FindingItem>[] = [ ... ]` (D-25)
- Line 87: TanStack table init consumes `columns`

src/dashboard/src/pages/identity.tsx:
- Line 71: same shape (D-25)
- Line 91: TanStack consumer

src/dashboard/src/pages/print.tsx:
- Line 1: `import React, { createElement } from 'react'`
- Lines 377-380: `createElement("style", null, PRINT_CSS)` (D-28)
- Lines 332-335: BR-05 cleanup (Phase 62) — DO NOT TOUCH (carry-over from Phase 76)
- Lines 346-350: D-03 sentinel from Phase 76 — already fixed; DO NOT TOUCH

src/dashboard/src/hooks/useQRAMMSession.ts:
- Line 19: `const seededRef = useRef<string | null>(null)`
- Lines 67-68, 101-102: `seededRef.current = latest.session_id` (set sites)
- No reset method exported (D-26 adds one)

src/dashboard/src/hooks/useScanData.ts:
- Lines 29-31: `const url = selectedScanId ? \`/api/scans/${selectedScanId}\` : \`/api/scans/latest\`` (inside try)
- Line 51: `setError(\`API error: ${resp.status} ${resp.statusText}\`)` (no URL — D-29)

RESEARCH Pattern 8 (D-24 — HMR-safe catch):
```typescript
try { cytoscape.use(coseBilkent) }
catch (e) {
    console.error('cytoscape.use(coseBilkent) failed:', e)
    if (!(e instanceof Error) || !/already/i.test(e.message)) throw e
}
```

RESEARCH Pattern 7 (D-28):
```typescript
return (
  <html>
    <head>
      <style>{PRINT_CSS}</style>
    </head>
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing Vitest modules for D-23..D-29 (7 new test files)</name>
  <files>src/dashboard/src/pages/__tests__/qramm-assessment-tab-comment.test.tsx, src/dashboard/src/pages/__tests__/cbom-cytoscape-catch.test.tsx, src/dashboard/src/pages/__tests__/findings-columns-memo.test.tsx, src/dashboard/src/hooks/__tests__/useQRAMMSession-reset.test.ts, src/dashboard/src/pages/__tests__/cbom-compByAlg-statistic.test.tsx, src/dashboard/src/pages/__tests__/print-no-createElement.test.tsx, src/dashboard/src/hooks/__tests__/useScanData-error-url.test.ts</files>
  <read_first>
    - src/dashboard/src/pages/qramm-assessment.tsx (lines 240-260)
    - src/dashboard/src/pages/cbom.tsx (lines 10-30, 140-160, 280-330, 380-400)
    - src/dashboard/src/pages/roadmap.tsx (lines 1-30)
    - src/dashboard/src/pages/findings.tsx (lines 1-100)
    - src/dashboard/src/pages/identity.tsx (lines 1-100)
    - src/dashboard/src/pages/print.tsx (lines 1-30, 325-385)
    - src/dashboard/src/hooks/useQRAMMSession.ts (full file)
    - src/dashboard/src/hooks/useScanData.ts (full file)
    - src/dashboard/src/pages/__tests__/print-pdf-cleanup.test.tsx (Phase 76 — Vitest idiom reference; carry-over reading)
    - src/dashboard/src/pages/__tests__/executive-error-coercion.test.tsx (Phase 76 — Vitest idiom reference)
    - .planning/phases/77-info-code-quality-audit-ledger/77-RESEARCH.md (Patterns 6, 7, 8; C-12)
  </read_first>
  <behavior>
    - D-23: Source-string assert. `src = readFileSync('src/dashboard/src/pages/qramm-assessment.tsx', 'utf-8'); expect(src).not.toContain('5-tab'); expect(src).toMatch(/6.tab/i)` (the corrected comment matches `/6-tab/i` or `6 tab`).
    - D-24 (cbom): Render `<CbomPage />` after `vi.mock('cytoscape', () => ({ default: { use: vi.fn(() => { throw new Error('rendering issue, unrelated') }) } }))` (non-"already" error → must re-throw). Assert the render throws OR an `errorBoundary` catches a thrown error AND `console.error` was called. Then a second test: mock `cytoscape.use` to throw `Error('already registered')` and assert it is SWALLOWED (HMR safety per C-12).
    - D-24 (roadmap): same pattern for `roadmap.tsx`.
    - D-25: render `<FindingsPage />` (or a memoized helper that exports the columns array); call `renderHook`-style helper to extract a stable reference. Test: `const { result, rerender } = renderHook(() => useColumnsForFindings()); const ref1 = result.current; rerender(); expect(result.current).toBe(ref1)` (same reference after rerender — memoization works). RED because columns are inline (new ref each render).
       Alternative: source-string AST grep for `useMemo<ColumnDef` in findings.tsx and identity.tsx.
    - D-26: `import { renderHook, act } from '@testing-library/react'; const { result } = renderHook(() => useQRAMMSession()); act(() => { result.current.resetSession() }); expect(result.current.seededRef.current).toBeNull()` — or, since seededRef is internal, expose via the public API: assert subsequent `seed` calls re-trigger seeding logic. RED because resetSession doesn't exist.
    - D-27: import `firstNonZeroComp` from cbom.tsx (export it for testing); `expect(firstNonZeroComp([{count: 0}, {count: 5}, {count: 0}])).toEqual({count: 5})`; `expect(firstNonZeroComp([{count: 0}, {count: 0}])).toEqual({count: 0})` (fallback to [0]); `expect(firstNonZeroComp(undefined)).toBeUndefined()`. RED until helper exists.
    - D-28: source-string AST grep: `src = readFileSync('src/dashboard/src/pages/print.tsx', 'utf-8'); expect(src).not.toContain('createElement("style"')`; `expect(src).toMatch(/<style>\\{PRINT_CSS\\}<\\/style>/)`. RED until conversion.
    - D-29: `vi.stubGlobal('fetch', vi.fn(async () => ({ ok: false, status: 500, statusText: 'Internal Server Error', json: async () => ({}) })))`; `renderHook(() => useScanData('scan-123'))`; `await waitFor` until error set; `expect(result.current.error).toMatch(/Failed to fetch.*scan-123/)`. RED because URL not in error.
  </behavior>
  <action>
    Create 7 Vitest modules per shapes above. Use `@testing-library/react` for component renders; use `renderHook` from `@testing-library/react` for hooks. Mock `fetch` via `vi.stubGlobal`. For source-string asserts, use `fs.readFileSync` from `node:fs`.

    For D-24 cytoscape mock: use `vi.mock('cytoscape', ...)` at the top of the test module. For D-25 useMemo: prefer the source-string AST gate (`grep -E "useMemo<ColumnDef"`) over render-stability since it doesn't depend on TanStack internals.

    For D-27, export `firstNonZeroComp` from `cbom.tsx` — Task 2 will create the export.

    Run `cd src/dashboard && npm test -- qramm-assessment-tab-comment cbom-cytoscape-catch findings-columns-memo useQRAMMSession-reset cbom-compByAlg-statistic print-no-createElement useScanData-error-url 2>&1 | tail -40` — confirm all 7 FAIL (RED).

    Commit message: `test(77-04): add failing Vitest modules for INFO-04 (D-23..D-29) — per RESEARCH C-12 HMR-safe re-throw`.
  </action>
  <verify>
    <automated>cd src/dashboard && npm test -- qramm-assessment-tab-comment cbom-cytoscape-catch findings-columns-memo useQRAMMSession-reset cbom-compByAlg-statistic print-no-createElement useScanData-error-url 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All 7 test files exist
    - `grep -cE "it\\(|describe\\(" src/dashboard/src/pages/__tests__/qramm-assessment-tab-comment.test.tsx` >= 1
    - `grep -cE "it\\(|describe\\(" src/dashboard/src/pages/__tests__/cbom-cytoscape-catch.test.tsx` >= 2
    - `grep -cE "it\\(|describe\\(" src/dashboard/src/pages/__tests__/findings-columns-memo.test.tsx` >= 1
    - `grep -cE "it\\(|describe\\(" src/dashboard/src/hooks/__tests__/useQRAMMSession-reset.test.ts` >= 1
    - `grep -cE "it\\(|describe\\(" src/dashboard/src/pages/__tests__/cbom-compByAlg-statistic.test.tsx` >= 3
    - `grep -cE "it\\(|describe\\(" src/dashboard/src/pages/__tests__/print-no-createElement.test.tsx` >= 1
    - `grep -cE "it\\(|describe\\(" src/dashboard/src/hooks/__tests__/useScanData-error-url.test.ts` >= 1
    - `grep -c "already" src/dashboard/src/pages/__tests__/cbom-cytoscape-catch.test.tsx` >= 1
    - `grep -c "Failed to fetch" src/dashboard/src/hooks/__tests__/useScanData-error-url.test.ts` >= 1
    - `cd src/dashboard && npm test -- qramm-assessment-tab-comment cbom-cytoscape-catch findings-columns-memo useQRAMMSession-reset cbom-compByAlg-statistic print-no-createElement useScanData-error-url` exits NON-ZERO (RED)
  </acceptance_criteria>
  <done>
    Seven failing Vitest modules committed; npm test exits non-zero with assertions tied to D-23..D-29.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement D-23..D-29 in 8 source files</name>
  <files>src/dashboard/src/pages/qramm-assessment.tsx, src/dashboard/src/pages/cbom.tsx, src/dashboard/src/pages/roadmap.tsx, src/dashboard/src/pages/findings.tsx, src/dashboard/src/pages/identity.tsx, src/dashboard/src/pages/print.tsx, src/dashboard/src/hooks/useQRAMMSession.ts, src/dashboard/src/hooks/useScanData.ts</files>
  <read_first>
    - All 8 source files at line ranges in `<interfaces>`
    - The 7 test files from Task 1
  </read_first>
  <action>
    Apply edits per shapes:

    **D-23 (qramm-assessment.tsx):** Rewrite the line-246 comment to `{/* 6-tab assessment layout: CVI, SGRM, DPE, ITR, Scorecard, Compliance Map — closes react-frontend/IN-01 */}`.

    **D-24 (cbom.tsx + roadmap.tsx):** Apply RESEARCH Pattern 8 to both:
    ```typescript
    try {
        cytoscape.use(coseBilkent)
    } catch (e) {
        console.error('cytoscape.use(coseBilkent) failed:', e)  // closes react-frontend/IN-02
        if (!(e instanceof Error) || !/already/i.test(e.message)) throw e
    }
    ```
    Same shape for `roadmap.tsx:13` (replace `coseBilkent` with `dagre`).

    **D-25 (findings.tsx + identity.tsx):**
    - Add `useMemo` to React imports if absent
    - Wrap the `columns` array assignment:
      ```typescript
      const columns = useMemo<ColumnDef<FindingItem>[]>(() => [
          // ... existing column defs unchanged
      ], [/* deps if any closures over external state, else [] */])
      // closes react-frontend/IN-03
      ```
    - Confirm cells reference `row.original` (not external state) before settling on empty deps. If a cell closes over a state variable, include that variable in the deps array.

    **D-26 (useQRAMMSession.ts + qramm-assessment.tsx caller):**
    1. In the hook:
       ```typescript
       const resetSession = useCallback(() => {
           seededRef.current = null  // closes react-frontend/IN-04
       }, [])

       return { /* existing fields */, resetSession }
       ```
    2. Update the return type/interface if explicitly declared.
    3. In `qramm-assessment.tsx` (or wherever the New Assessment flow is triggered), destructure `resetSession` and call after the archive-and-reset action.

    **D-27 (cbom.tsx):** Add a small exported helper near the top of `cbom.tsx`:
    ```typescript
    export function firstNonZeroComp<T extends { count: number }>(comps: T[] | undefined): T | undefined {
        if (!comps || comps.length === 0) return undefined
        return comps.find((c) => c.count > 0) ?? comps[0]
    }
    ```
    Replace the 4 `compByAlg[d.label]?.[0]` sites at lines 148, 285, 317, 390 with `firstNonZeroComp(compByAlg[d.label])`. Cite `// closes react-frontend/IN-05`.

    **D-28 (print.tsx):**
    - Locate the `createElement("style", null, PRINT_CSS)` call at lines 377-380 and replace with JSX `<style>{PRINT_CSS}</style>` inside `<head>` per Pattern 7.
    - Remove the `createElement` import if it becomes unused (check via grep).
    - **DO NOT TOUCH** the BR-05 cleanup effect at lines 332-335 (Phase 62 — preserved by Phase 76; D-32 preservation rule).
    - **DO NOT TOUCH** the Phase 76 D-03 sentinel guard at lines 346-350.
    - Cite `// closes react-frontend/IN-06`.

    **D-29 (useScanData.ts):** Hoist the URL constant out of the try-block (per RESEARCH Pitfall 8):
    ```typescript
    const url = selectedScanId ? `/api/scans/${selectedScanId}` : `/api/scans/latest`
    try {
        const resp = await fetch(url)
        if (!resp.ok) {
            setError(`Failed to fetch ${url}: ${resp.status} ${resp.statusText}`)  // closes react-frontend/IN-07
            return
        }
        // ...
    } catch (e) {
        setError(`Failed to fetch ${url}: ${e instanceof Error ? e.message : String(e)}`)
    }
    ```

    Run `cd src/dashboard && npm test -- qramm-assessment-tab-comment cbom-cytoscape-catch findings-columns-memo useQRAMMSession-reset cbom-compByAlg-statistic print-no-createElement useScanData-error-url` — all green.

    Commit message: `feat(77-04): close INFO-04 (D-23..D-29) — React frontend INFOs; RESEARCH C-12 HMR-safe re-throw applied`.
  </action>
  <verify>
    <automated>cd src/dashboard && npm test -- qramm-assessment-tab-comment cbom-cytoscape-catch findings-columns-memo useQRAMMSession-reset cbom-compByAlg-statistic print-no-createElement useScanData-error-url 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "5-tab" src/dashboard/src/pages/qramm-assessment.tsx` == 0
    - `grep -cE "6.tab" src/dashboard/src/pages/qramm-assessment.tsx` >= 1
    - `grep -c "console.error" src/dashboard/src/pages/cbom.tsx` >= 1
    - `grep -c "/already/i" src/dashboard/src/pages/cbom.tsx` >= 1
    - `grep -c "console.error" src/dashboard/src/pages/roadmap.tsx` >= 1
    - `grep -c "useMemo" src/dashboard/src/pages/findings.tsx` >= 1
    - `grep -c "useMemo" src/dashboard/src/pages/identity.tsx` >= 1
    - `grep -c "resetSession" src/dashboard/src/hooks/useQRAMMSession.ts` >= 2
    - `grep -c "resetSession" src/dashboard/src/pages/qramm-assessment.tsx` >= 1
    - `grep -c "firstNonZeroComp" src/dashboard/src/pages/cbom.tsx` >= 5 (1 def + 4 call sites)
    - `grep -cE "createElement\\(\"style\"" src/dashboard/src/pages/print.tsx` == 0
    - `grep -cE "<style>\\{PRINT_CSS\\}</style>" src/dashboard/src/pages/print.tsx` >= 1
    - `grep -cE "document\\.body\\.removeAttribute\\('data-ready'\\)" src/dashboard/src/pages/print.tsx` >= 2 (BR-05 + Phase 76 sentinel still present)
    - `grep -cE "Failed to fetch \\$\\{url" src/dashboard/src/hooks/useScanData.ts` >= 1
    - `cd src/dashboard && npm test -- qramm-assessment-tab-comment cbom-cytoscape-catch findings-columns-memo useQRAMMSession-reset cbom-compByAlg-statistic print-no-createElement useScanData-error-url` exits 0 (GREEN)
  </acceptance_criteria>
  <done>
    All 7 INFO-04 tests pass; Phase 62 + Phase 76 fixes preserved (BR-05 cleanup + D-03 sentinel intact); cytoscape catches log + guard-rethrow; columns memoized; QRAMM session resettable.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build gate + frontend regression sweep</name>
  <files>src/dashboard/src/hooks/useScanData.ts</files>
  <read_first>
    - Output of `cd src/dashboard && npm run build 2>&1 | tail -30`
    - Output of `cd src/dashboard && npm test 2>&1 | tail -40`
  </read_first>
  <action>
    **Build gate (CONTEXT test_strategy requirement):**

    1. Run `cd src/dashboard && npm run build`. Confirm exit 0. If non-zero, diagnose:
       - TS errors from new exports (e.g., `firstNonZeroComp` generic) — adjust signature
       - JSX errors from `<style>{PRINT_CSS}</style>` — ensure PRINT_CSS is a string, not an object
       - Hook signature changes (D-26 resetSession) breaking consumers — propagate type updates

    2. Run the full Vitest band:
       ```
       cd src/dashboard && npm test
       ```
       Catch any regression caused by:
       - D-25 useMemo deps mismatch (columns referencing closure state)
       - D-26 resetSession breaking an existing test expecting the old hook shape
       - D-28 createElement removal breaking print fixture (snapshot diffs)
       - D-29 URL hoisting changing the inner try/catch path

    3. Confirm Phase 76 carry-over tests still pass:
       ```
       cd src/dashboard && npm test -- useScanList executive-error-coercion print-pdf-cleanup qramm-profile-submit-error
       ```

    Commit message (only if fixes applied): `fix(77-04): adjust regression coverage for INFO-04 semantic shifts`. If clean: no commit.
  </action>
  <verify>
    <automated>cd src/dashboard && npm run build 2>&1 | tail -10; cd src/dashboard && npm test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/dashboard && npm run build` exits 0
    - `cd src/dashboard && npm test` exits 0 (full Vitest suite green)
    - Phase 76 tests (useScanList, executive-error-coercion, print-pdf-cleanup, qramm-profile-submit-error) all still pass
    - No new npm dependency added (`git diff src/dashboard/package.json` empty for dependencies/devDependencies sections)
  </acceptance_criteria>
  <done>
    `npm run build` exits 0 (CONTEXT test_strategy gate satisfied); full Vitest suite green; Phase 76 carry-over preserved; no new npm deps.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External fetch API → useScanData error path | Untrusted response body + URL crosses into operator-visible error string (D-29) |
| Cytoscape extension registration → page renders | Failed `cytoscape.use` previously silently broke graph rendering (D-24) |
| Print page → PDF renderer | JSX style vs createElement style — sentinel-relevant for Phase 62/76 path |
| QRAMM session ref → New Assessment flow | seededRef persistence across flow transitions controlled by hook (D-26) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-04-01 | Repudiation | Silent cytoscape registration failure leaves operator without graph and without diagnostic | mitigate | D-24 — console.error + guarded re-throw per Pattern 8 (HMR-safe per C-12) |
| T-77-04-02 | Tampering | useQRAMMSession seededRef leaks across New Assessment flow (operator sees stale session) | mitigate | D-26 — resetSession callback + caller wiring |
| T-77-04-03 | Information Disclosure | URL omission in useScanData error obscures which endpoint failed (operator cannot triage) | mitigate | D-29 — URL hoisted out of try, interpolated into error |
| T-77-04-04 | Information Disclosure | URL interpolation could leak path tokens / scan IDs to user-facing alerts | accept | URLs are operator-facing in this dashboard; CONTEXT scope is code-quality polish only |
| T-77-04-05 | Tampering | compByAlg [0] representative dropping variance — first-non-zero preserves "any representative" semantic while avoiding hidden-zero artifacts | mitigate | D-27 — firstNonZeroComp helper |
</threat_model>

<verification>
- 7 INFO-04 Vitest modules pass
- `cd src/dashboard && npm run build` exits 0
- `cd src/dashboard && npm test` (full suite) passes
- Phase 76 tests (useScanList, executive-error-coercion, print-pdf-cleanup, qramm-profile-submit-error) still pass
- Phase 62 BR-05 cleanup at print.tsx:332-335 unchanged
- Phase 76 D-03 sentinel guard at print.tsx:346-350 unchanged
- D-32 honored: no Recharts swaps, no QRAMM-120-question taxonomy edits, no new npm deps
- 0 audit row flips (consolidated by 77-05)
</verification>

<success_criteria>
- 7 new Vitest modules with RED→GREEN history
- 8 production source files edited
- INFO-04 (IN-01..07) fixes landed; audit-row flips deferred to PLAN 77-05
- RESEARCH C-12 HMR-safe re-throw pattern applied to D-24
- Phase 62 + Phase 76 fixes preserved exactly
- `npm run build` exit 0 gate satisfied
- No new npm dependency
</success_criteria>

<output>
Create `.planning/phases/77-info-code-quality-audit-ledger/77-04-SUMMARY.md` documenting: 7 INFO-04 fixes landed, RESEARCH C-12 (HMR-safe re-throw) applied to D-24, D-27 'first non-zero' semantic per Discretion, Phase 62 BR-05 + Phase 76 D-03 sentinel preserved exactly, `npm run build` confirmed exit 0, full Vitest suite green, audit-row flips deferred to PLAN 77-05.
</output>
