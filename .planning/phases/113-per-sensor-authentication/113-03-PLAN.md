---
phase: 113-per-sensor-authentication
plan: 03
type: execute
wave: 3
depends_on: [113-02]
files_modified:
  - tests/test_sensor_ingest.py
  - tests/test_console_enroll.py
  - quirk/cli/console_cmd.py
  - quirk/cli/sensor_cmd.py
  - docs/operators-guide.md
  - docs/UAT-SERIES.md
  - quantum-chaos-enterprise-lab/expected_results_distributed.md
autonomous: true
requirements: [AUTH-03, AUTH-04]
must_haves:
  truths:
    - "Existing push tests in test_sensor_ingest.py seed a SensorToken row and use the enrollment token as the Bearer header (full suite green)"
    - "Enrollment printout states the enrollment token IS the push credential"
    - "operators-guide.md §8.1.1 documents the per-sensor migration off the shared-token model (D-10/D-11)"
    - "expected_results_distributed.md reflects the per-sensor auth model so lab.sh distributed e2e stays valid"
  artifacts:
    - path: "tests/test_sensor_ingest.py"
      provides: "_seed_token usage + updated Bearer headers"
      contains: "_seed_token"
    - path: "docs/operators-guide.md"
      provides: "per-sensor migration section (replaces v5.4 shared-token §8.1.1)"
      contains: "per-sensor"
    - path: "quirk/cli/console_cmd.py"
      provides: "corrected enrollment printout"
  key_links:
    - from: "tests/test_sensor_ingest.py push tests"
      to: "sensor_tokens"
      via: "_seed_token + Bearer enrollment token"
      pattern: "_seed_token"
---

<objective>
Complete the clean cutover: update existing tests for the new auth path, fix operator-facing artifacts, and run the mandatory phase-completion docs/sync steps (AUTH-03 enroll correctness, AUTH-04 documented migration).

Purpose: Plan 02 swapped the push route's auth — the existing test_sensor_ingest.py push tests (shared `test-token`) now break by design (VALIDATION Wave 0). This plan seeds per-sensor tokens in those tests, corrects the enrollment printout and sensor-side credential semantics (D-11), writes the operators-guide migration (D-10/D-11), keeps the distributed-lab oracle truthful (CLAUDE.md no-drift rule), and runs the project's mandatory per-phase docs + Obsidian sync steps.

Output: green full suite, corrected enroll messaging, operators-guide migration §, updated lab oracle, UAT-SERIES + Obsidian sync.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-per-sensor-authentication/113-CONTEXT.md
@.planning/phases/113-per-sensor-authentication/113-PATTERNS.md
@.planning/phases/113-per-sensor-authentication/113-02-SUMMARY.md
@CLAUDE.md

<interfaces>
From tests/test_sensor_ingest.py (PATTERNS.md "test_sensor_ingest.py MODIFIED" table — tests to update):
  test_push_413_body_too_large (L160), test_push_409_duplicate_payload (L183, sensor-dedup-01),
  test_push_200_accepted (L247, sensor-ok-01), test_audit_row_written (L316, sensor-audit-01),
  test_extra_fields_ignored (L388, sensor-extra-01), test_version_skew_graceful (L414),
  test_push_422_replay_window (L216), test_unknown_sensor_id_4xx (L441, comment-only),
  test_push_requires_auth (L145, no change), test_push_endpoint_exists (L130, no change).
  Add _seed_token helper (sha256(raw).hexdigest() → SensorToken row), return raw token for Bearer header.

From quirk/cli/console_cmd.py: enrollment printout L217-224 ("NOT the push credential" — replace).
From quirk/cli/sensor_cmd.py: console_api_token read (L574) + Bearer header (L596-601) — comment-only change; wire mechanism unchanged.
From docs/operators-guide.md: §8.1.1 v5.4 shared-token section (replace target).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update existing push tests + enroll test for per-sensor auth (full suite green)</name>
  <read_first>
    - tests/test_sensor_ingest.py (entire file — every push test listed in the interfaces block; _seed_sensor analog for the new _seed_token helper)
    - tests/test_console_enroll.py (entire file — confirm/add a revoked_at IS NULL assertion on new enrollment)
    - .planning/phases/113-per-sensor-authentication/113-PATTERNS.md (test_sensor_ingest.py MODIFIED table — exact per-test fixes)
  </read_first>
  <action>
    In tests/test_sensor_ingest.py add a `_seed_token(TestingSession, sensor_id, raw_token=None, revoked=False)` helper (same shape as in test_sensor_auth_per_sensor.py: mint secrets.token_urlsafe(32) if none, write SensorToken with sha256 hash + created_at + revoked_at). For each push test that currently sends `Authorization: Bearer test-token`, seed a token for that test's sensor_id and send `Authorization: Bearer {raw_token}` instead. Apply per the PATTERNS.md table:
    - test_push_200_accepted (sensor-ok-01), test_push_409_duplicate_payload (sensor-dedup-01), test_audit_row_written (sensor-audit-01), test_extra_fields_ignored (sensor-extra-01), test_version_skew_graceful, test_push_422_replay_window (seed a real sensor + token, keep stale pushed_at so it stays 422): seed token + update header so the envelope.sensor_id matches the token's sensor_id (avoid a spurious 403).
    - test_push_413_body_too_large: seed any token + update header (413 fires before the identity check).
    - test_unknown_sensor_id_4xx: now returns 401 (no token row) — update only the comment; the existing `400 <= status < 500` assertion still holds.
    - test_push_requires_auth (no Authorization header → still 401) and test_push_endpoint_exists: no change.
    In tests/test_console_enroll.py, ensure there is an assertion that a freshly enrolled SensorToken row has `revoked_at IS NULL` (AUTH-03); add it if absent. Keep diffs minimal (PEP 8).
  </action>
  <verify>
    <automated>pytest tests/test_sensor_ingest.py tests/test_console_enroll.py tests/test_sensor_auth_per_sensor.py -q 2>&1 | tail -25 && pytest tests/ -q -m 'not slow' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `_seed_token` present in test_sensor_ingest.py; every updated push test uses a seeded Bearer token matching its envelope sensor_id.
    - test_console_enroll.py asserts new-enrollment `revoked_at IS NULL`.
    - `pytest tests/test_sensor_ingest.py tests/test_console_enroll.py tests/test_sensor_auth_per_sensor.py -q` all green; full `pytest tests/ -m 'not slow'` shows no NEW failures vs. the pre-existing baseline.
  </acceptance_criteria>
  <done>Existing push tests pass under per-sensor auth; enroll test asserts revoked_at NULL; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Correct enroll printout + sensor-side credential semantics (D-11)</name>
  <read_first>
    - quirk/cli/console_cmd.py (L215-228 enrollment printout — "NOT the push credential") — file being modified
    - quirk/cli/sensor_cmd.py (L574 console_api_token read, L596-601 Bearer header) — file being modified; PATTERNS.md confirms comment-only
  </read_first>
  <action>
    In quirk/cli/console_cmd.py replace the enrollment printout block (L217-224) so it states the enrollment token IS the per-sensor push credential: the operator must place this raw value in the sensor's `console_api_token` field in sensor.yaml; it is shown once and never recoverable; if lost the sensor must be revoked + re-enrolled (D-08). Remove the obsolete "uses the CONSOLE'S shared API token / QUIRK_API_TOKEN" wording. Keep the `print(f"sensor_id: {sensor_id}", ...)` line and the WR-04 return-normally behavior.

    In quirk/cli/sensor_cmd.py update only the inline comment / help text near `console_api_token` (L574) and any enrollment-step docstring referencing `--api-token <console-QUIRK_API_TOKEN>` so it describes the per-sensor token. Do NOT change the header-construction code — the wire contract (`Authorization: Bearer`) is unchanged (OS-agnostic constraint). PEP 8, minimal diff.
  </action>
  <verify>
    <automated>python -m compileall quirk/cli/console_cmd.py quirk/cli/sensor_cmd.py && grep -v '^[[:space:]]*#' quirk/cli/console_cmd.py | grep -c "NOT the push credential" | grep -q '^0$' && echo "OK stale message removed"</automated>
  </verify>
  <acceptance_criteria>
    - Both files compile.
    - The "NOT the push credential" wording no longer appears in non-comment lines of console_cmd.py.
    - sensor_cmd.py header-construction code unchanged; only comment/help text describes the per-sensor token.
  </acceptance_criteria>
  <done>Enroll printout states the token IS the push credential; sensor-side docs describe per-sensor token; wire mechanism unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Operators-guide migration section + distributed-lab oracle (AUTH-04, no-drift)</name>
  <read_first>
    - docs/operators-guide.md (§8.1.1 v5.4 shared-token section — replace target)
    - quantum-chaos-enterprise-lab/expected_results_distributed.md (push-auth expectations referencing the shared token)
    - .planning/phases/113-per-sensor-authentication/113-CONTEXT.md (D-10/D-11 migration steps)
  </read_first>
  <action>
    Replace the v5.4 shared-token model description in docs/operators-guide.md §8.1.1 with a per-sensor migration section covering (D-10/D-11): (1) what changed — per-sensor tokens replace the shared console token at POST /api/sensor/push; (2) per sensor host, change `console_api_token` in sensor.yaml from the shared QUIRK_API_TOKEN value to the sensor's enrollment token (printed by `quirk console enroll`); (3) if the raw enrollment token was lost, run `quirk console revoke-sensor <sensor_id>` then re-enroll to mint a fresh token + sensor_id (D-08); (4) the shared QUIRK_API_TOKEN still governs operator/dashboard auth and is unaffected (D-02). State this is a clean cutover with no dual-accept (D-10).

    Update quantum-chaos-enterprise-lab/expected_results_distributed.md so any push-auth expectation reflects the per-sensor token model (each sensor uses its own enrollment token, not the shared console token) — keeping the oracle truthful so `lab.sh distributed e2e` stays a valid check (CLAUDE.md no-drift rule). Only the push-credential semantics change; CBOM/score expectations are unchanged. Note: this phase does not add/rename/reconfigure a compose profile, so lab.sh ALL_PROFILES and the chaos-lab README need no change — confirm and state this in the SUMMARY.
  </action>
  <verify>
    <automated>grep -ic "per-sensor" docs/operators-guide.md | grep -vq '^0$' && grep -iq "revoke-sensor" docs/operators-guide.md && echo "OK operators-guide migration present"</automated>
  </verify>
  <acceptance_criteria>
    - operators-guide.md §8.1.1 describes the per-sensor migration with all four points and references `revoke-sensor`.
    - expected_results_distributed.md push-auth wording reflects per-sensor tokens.
    - No shared-token-only push instructions remain in either file.
  </acceptance_criteria>
  <done>Operators guide documents the backward-compatible migration; lab oracle truthful; no profile drift.</done>
</task>

<task type="auto">
  <name>Task 4: Mandatory phase-completion docs + Obsidian sync (CLAUDE.md)</name>
  <read_first>
    - CLAUDE.md (Mandatory Phase Completion Steps — Obsidian phase note, UAT-SERIES update + sync + commit)
    - docs/UAT-SERIES.md (the sensor/distributed push-auth series to update)
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/ (existing phase-note template pattern)
  </read_first>
  <action>
    Per CLAUDE.md mandatory steps: (1) Create the Obsidian phase note at /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-113-Per-Sensor-Authentication.md via the Write tool (frontmatter status: complete, type: phase, source, updated; Goal; Requirements Covered AUTH-01..04; Success Criteria; What Was Built — one subsection per plan sourced from the 113-01/02/03 SUMMARY.md files; `[[Roadmap]]` link), matching the existing phase-note template. (2) Update docs/UAT-SERIES.md: in the relevant sensor/distributed push series, add/adjust test cases for per-sensor token auth (valid token → 200, unknown/revoked → 401, mismatch → 403, revoke-sensor isolation, operator migration), update any version strings, and bump `**Last Updated:**` to today. (3) Sync UAT-SERIES.md to the vault per the CLAUDE.md printf+cat recipe (write to /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md). (4) Commit docs/UAT-SERIES.md via the gsd-tools commit recipe with message `docs(phase-113): update UAT-SERIES.md`. Use the Write tool for vault files (too large for CLI content=).
  </action>
  <verify>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-113-Per-Sensor-Authentication.md" && grep -iq "AUTH-0" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-113-Per-Sensor-Authentication.md" && grep -iq "revoke\|per-sensor" docs/UAT-SERIES.md && echo "OK phase note + UAT updated"</automated>
  </verify>
  <acceptance_criteria>
    - Phase-113 Obsidian note exists with AUTH-01..04 coverage and per-plan What-Was-Built sections.
    - docs/UAT-SERIES.md has per-sensor auth test cases + bumped Last Updated date, synced to the vault and committed.
  </acceptance_criteria>
  <done>Obsidian phase note created; UAT-SERIES updated, synced, committed per CLAUDE.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → migration procedure | Operator follows documented steps to re-point each sensor's credential; misconfiguration → 401 lockout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-09 | Information disclosure | enrollment token shown once | mitigate | Printout states token shown once / never recoverable; lost → revoke + re-enroll (D-08); raw never persisted (only SHA-256 hash) |
| T-113-10 | Denial of service | operator lockout on cutover | mitigate | operators-guide §8.1.1 documents the clean-cutover migration so sensors re-point credentials before upgrade activates per-sensor auth (D-10/D-11) |
| T-113-SC | Tampering | npm/pip installs | accept | No new packages introduced (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `pytest tests/ -q -m 'not slow'` fully green (existing push tests updated, gating tests green).
- "NOT the push credential" stale wording removed from console_cmd.py non-comment lines.
- operators-guide.md §8.1.1 documents per-sensor migration incl. revoke-sensor; lab oracle truthful.
- Phase-113 Obsidian note exists; UAT-SERIES updated + synced + committed.
</verification>

<success_criteria>
- AUTH-03: enroll writes SHA-256 hash, revoked_at NULL on new rows (test asserts it).
- AUTH-04: migration off the shared-token model is documented and backward-compatible.
- Full test suite green; lab oracle and operator docs no-drift; mandatory phase-completion steps done.
</success_criteria>

<output>
Create `.planning/phases/113-per-sensor-authentication/113-03-SUMMARY.md` when done.
</output>
