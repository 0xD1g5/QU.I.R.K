---
phase: 113-per-sensor-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/models.py
  - quirk/db.py
  - tests/test_sensor_auth_per_sensor.py
autonomous: true
requirements: [AUTH-03]
must_haves:
  truths:
    - "sensor_tokens table has a nullable revoked_at column after init_db on an existing DB"
    - "New enrollment rows have revoked_at IS NULL"
    - "The AUTH-04 gating test file exists with 8 test functions and runs (RED is expected until Plan 02)"
  artifacts:
    - path: "quirk/models.py"
      provides: "SensorToken.revoked_at column"
      contains: "revoked_at = Column(DateTime"
    - path: "quirk/db.py"
      provides: "_V55_SENSOR_TOKEN_COLUMNS additive migration entry"
      contains: "_V55_SENSOR_TOKEN_COLUMNS"
    - path: "tests/test_sensor_auth_per_sensor.py"
      provides: "AUTH-01..04 gating test scaffold + _seed_token helper"
      contains: "def _seed_token"
  key_links:
    - from: "quirk/db.py _ADDITIVE_MIGRATIONS"
      to: "sensor_tokens table"
      via: "_ensure_columns ALTER TABLE ADD COLUMN"
      pattern: "sensor_tokens.*_V55_SENSOR_TOKEN_COLUMNS"
---

<objective>
Lay the schema foundation and Wave-0 gating test for per-sensor authentication (AUTH-03 storage layer + AUTH-01..04 test scaffold).

Purpose: The `revoked_at` column and the gating test must exist BEFORE the auth cutover in Plan 02 so that post-cutover red is expected-and-fixed, not a regression (per VALIDATION.md Wave 0). This plan adds the additive nullable column (D-06) and writes the full gating test file. The auth wiring that makes these tests pass lands in Plan 02 — these tests are RED at the end of this plan, which is correct.

Output: `SensorToken.revoked_at` column + `_V55_SENSOR_TOKEN_COLUMNS` migration + `tests/test_sensor_auth_per_sensor.py` (8 functions + `_seed_token` helper).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-per-sensor-authentication/113-CONTEXT.md
@.planning/phases/113-per-sensor-authentication/113-RESEARCH.md
@.planning/phases/113-per-sensor-authentication/113-PATTERNS.md
@.planning/phases/113-per-sensor-authentication/113-VALIDATION.md

<interfaces>
From quirk/models.py (SensorToken, ~L291-313):
  class SensorToken(Base):
    __tablename__ = "sensor_tokens"
    id = Column(Integer, primary_key=True, autoincrement=True)
    sensor_id = Column(String(36), ForeignKey("sensors.sensor_id", ondelete="CASCADE"), nullable=False)
    token_hash = Column(String(64), nullable=False, unique=True)
    created_at = Column(DateTime, nullable=False)
  # DateTime is already imported at module scope.

From quirk/db.py:
  _SAFE_COL_TYPE_RE = r"^(TEXT|INTEGER|REAL|BOOLEAN|DATETIME|VARCHAR\(\d{1,4}\))$"  (L52 — DATETIME allowed)
  _V54_SENSOR_COLUMNS: tuple[tuple[str,str],...] (L125-130 — analog to copy)
  _ADDITIVE_MIGRATIONS: tuple[...] (L178-188 — append one entry)
  _ensure_columns(...) (L133-163 — skips existing columns, idempotent)
  init_db(...) (L386-433 — iterates _ADDITIVE_MIGRATIONS)

From tests/test_sensor_ingest.py (analog for new test file):
  _make_test_engine(), _app_with_db() (L38-61), _seed_sensor() (L103-123),
  _build_envelope() + _compress() (L68-100), audit-row assertion pattern (L336-348).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add nullable revoked_at column to SensorToken (model + additive migration)</name>
  <read_first>
    - quirk/models.py (SensorToken class, ~L291-313) — the file being modified
    - quirk/db.py (L52 _SAFE_COL_TYPE_RE, L125-130 _V54_SENSOR_COLUMNS analog, L133-163 _ensure_columns, L178-188 _ADDITIVE_MIGRATIONS, L386-433 init_db) — analog from PATTERNS.md
  </read_first>
  <behavior>
    - On a fresh in-memory DB, Base.metadata.create_all creates sensor_tokens WITH a revoked_at column (nullable).
    - On an existing DB lacking the column, init_db adds revoked_at via ALTER TABLE ADD COLUMN (idempotent — second init_db is a no-op).
    - New enrollment rows have revoked_at IS NULL.
  </behavior>
  <action>
    In quirk/models.py add `revoked_at = Column(DateTime, nullable=True)` to the SensorToken class immediately after `created_at` (None = active; set = revoked, Phase 113 AUTH-02 / D-06). DateTime is already imported at module scope — do not add a new import.

    In quirk/db.py: define `_V55_SENSOR_TOKEN_COLUMNS: tuple[tuple[str, str], ...]` containing a single tuple `("revoked_at", "DATETIME")` immediately after `_V54_SENSOR_COLUMNS`, mirroring that tuple's shape and comment style. Append `("sensor_tokens", _V55_SENSOR_TOKEN_COLUMNS),  # Phase 113 AUTH-02` as the last entry of the `_ADDITIVE_MIGRATIONS` tuple. Do not touch `_ensure_columns` or `init_db` — they iterate the migrations tuple automatically. `DATETIME` is already in the `_SAFE_COL_TYPE_RE` allowlist, so no regex change is needed. Keep the diff minimal (PEP 8, no unrelated refactors).
  </action>
  <verify>
    <automated>python -m compileall quirk/models.py quirk/db.py && python -c "import sqlalchemy as sa; from quirk.models import SensorToken; assert 'revoked_at' in SensorToken.__table__.columns and SensorToken.__table__.columns['revoked_at'].nullable, 'revoked_at missing or not nullable'; from quirk.db import _ADDITIVE_MIGRATIONS; assert any(t=='sensor_tokens' for t,_ in _ADDITIVE_MIGRATIONS), 'sensor_tokens migration not registered'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `SensorToken.__table__.columns['revoked_at']` exists and `.nullable is True`.
    - `_ADDITIVE_MIGRATIONS` contains a `("sensor_tokens", _V55_SENSOR_TOKEN_COLUMNS)` entry.
    - `python -m compileall` exits 0 for both files.
  </acceptance_criteria>
  <done>revoked_at column present on the model, nullable; additive migration registered; compileall clean.</done>
</task>

<task type="auto">
  <name>Task 2: Write the AUTH-01..04 gating test file (Wave 0)</name>
  <read_first>
    - tests/test_sensor_ingest.py (entire file — the exact analog: _make_test_engine, _app_with_db L38-61, _seed_sensor L103-123, _build_envelope/_compress L68-100, audit assertion L336-348)
    - .planning/phases/113-per-sensor-authentication/113-PATTERNS.md (the test_sensor_auth_per_sensor.py section — _seed_token helper + per-branch error_summary filters)
  </read_first>
  <action>
    Create tests/test_sensor_auth_per_sensor.py. Copy `_make_test_engine`, `_app_with_db`, `_seed_sensor`, `_build_envelope`, `_compress` from test_sensor_ingest.py verbatim. Add a `_seed_token(TestingSession, sensor_id, raw_token=None, revoked=False)` helper that mints `secrets.token_urlsafe(32)` when no raw token is given, writes a `SensorToken(sensor_id, token_hash=sha256(raw).hexdigest(), created_at=now, revoked_at=now if revoked else None)` row, and returns the raw token string.

    Write exactly these 8 test functions, each posting to `/api/sensor/push` with `Authorization: Bearer <raw_token>` and NOT relying on QUIRK_API_TOKEN:
    - `test_valid_sensor_token_accepted` — seed sensor + token; valid push → 200 (AUTH-01).
    - `test_token_identity_is_authoritative` — push attributed to the token-resolved sensor_id, not a body-supplied one (AUTH-01).
    - `test_revoked_token_returns_401` — seed token with revoked=True; push → 401 (AUTH-02/AUTH-04).
    - `test_revoke_isolates_to_one_sensor` — seed sensor-a (revoked) + sensor-b (active); sensor-a → 401, sensor-b → 200 (AUTH-02).
    - `test_unknown_token_returns_401` — Bearer of a never-seeded token → 401 (AUTH-04).
    - `test_sensor_id_mismatch_returns_403` — valid token for sensor-a, envelope body claims sensor-b → 403 (AUTH-04 / D-05).
    - `test_all_branches_write_audit_rows` — assert IntegrationDelivery rows with destination='sensor_push' and distinct error_summary values: "unknown_sensor_token" (401 unknown), "revoked_sensor_token" (401 revoked), "sensor_id_mismatch" (403), plus a status='ok' row for the valid push (AUTH-04 / D-09).
    - `test_missing_token_returns_401` — no Authorization header → 401.

    These tests will be RED until Plan 02 wires require_sensor_auth — that is expected per VALIDATION.md Wave 0. Do NOT implement any production auth code in this plan. Follow the test_sensor_ingest.py audit-row assertion style (query IntegrationDelivery, filter on destination + status/error_summary).
  </action>
  <verify>
    <automated>python -m compileall tests/test_sensor_auth_per_sensor.py && python -c "import ast,inspect; src=open('tests/test_sensor_auth_per_sensor.py').read(); t=[n.name for n in ast.walk(ast.parse(src)) if isinstance(n,ast.FunctionDef) and n.name.startswith('test_')]; assert len(t)>=8, f'expected >=8 tests, got {len(t)}: {t}'; assert '_seed_token' in src; print('OK', len(t), 'tests')"</automated>
  </verify>
  <acceptance_criteria>
    - File compiles; defines `_seed_token` and at least 8 `test_*` functions covering valid/unknown/revoked/mismatch/missing/isolation/identity/audit-rows.
    - `pytest tests/test_sensor_auth_per_sensor.py -q` collects 8+ tests without collection errors (failures expected — auth not yet wired).
  </acceptance_criteria>
  <done>Gating test file exists, collects cleanly, RED pending Plan 02. _seed_token helper present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sensor → console push route | Untrusted Bearer token + envelope crosses here; this plan establishes the storage that later identifies/revokes the sensor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | Tampering | additive `revoked_at` migration on existing DBs | mitigate | Nullable additive column via `_ensure_columns` (idempotent, existing-column skip); no data rewrite, no destructive DDL (D-06) |
| T-113-02 | Information disclosure | gating test seeding raw tokens | mitigate | Raw token only held in-memory in tests; only SHA-256 hash written to sensor_tokens (mirrors enroll) |
| T-113-SC | Tampering | npm/pip installs | accept | No new packages introduced this plan (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `python -m compileall quirk/models.py quirk/db.py tests/test_sensor_auth_per_sensor.py` exits 0.
- `revoked_at` column present + nullable on SensorToken; migration registered.
- Gating test collects 8+ tests (RED expected — auth wiring is Plan 02).
</verification>

<success_criteria>
- SensorToken.revoked_at nullable column exists (AUTH-03 storage prerequisite for AUTH-02).
- _V55_SENSOR_TOKEN_COLUMNS additive migration registered and idempotent.
- tests/test_sensor_auth_per_sensor.py exists with 8 functions + _seed_token, collecting cleanly.
</success_criteria>

<output>
Create `.planning/phases/113-per-sensor-authentication/113-01-SUMMARY.md` when done.
</output>
