---
phase: 08-legacy-debt-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/connectors/__init__.py
  - quirk/connectors/aws_stub.py
  - quirk/connectors/azure_stub.py
  - quirk/connectors/windows_adcs_stub.py
  - quirk/engine/rules.py
  - quirk/intelligence/driver_text.py
  - quirk/intelligence/calibration.py
  - quirk/reports/writer.py
  - quirk/assessment/migration_advisor.py
  - run_scan.py
  - quirk/logging_util.py
  - quirk/discovery/nmap_provider.py
  - data/qcscan-legacy.sqlite
autonomous: true
requirements: [D-09, D-10, D-11, D-12, D-13, D-15, D-17, D-19, D-20, D-21]

must_haves:
  truths:
    - "quirk/connectors/ directory does not exist"
    - "quirk/engine/rules.py does not exist"
    - "quirk/intelligence/driver_text.py does not exist"
    - "quirk/intelligence/calibration.py does not exist"
    - "data/qcscan-legacy.sqlite does not exist"
    - "migration_advisor.py matches actual finding title 'Legacy TLS' not 'deprecated tls'"
    - "cfg.scan mutation in run_scan.py is wrapped in try/finally"
    - "No datetime.utcnow() calls remain in logging_util.py or nmap_provider.py"
    - "tqdm=None assignment and dead if-tqdm branch are removed from run_scan.py"
    - "Dead helper functions removed from writer.py (4 of 5 -- _extract_cert_key_type stays)"
  artifacts:
    - path: "quirk/assessment/migration_advisor.py"
      provides: "Corrected string pattern matching"
      contains: "legacy tls"
    - path: "run_scan.py"
      provides: "try/finally around cfg.scan mutation"
      contains: "finally"
    - path: "quirk/logging_util.py"
      provides: "Modern datetime usage"
      contains: "datetime.now(timezone.utc)"
  key_links:
    - from: "quirk/assessment/migration_advisor.py"
      to: "quirk/engine/risk_engine.py"
      via: "finding title string must match"
      pattern: "legacy tls"
    - from: "run_scan.py"
      to: "quirk/config.py"
      via: "cfg.scan mutation + restore in try/finally"
      pattern: "try:.*finally:.*cfg\\.scan"
---

<objective>
Delete all dead code files, fix migration_advisor string patterns, wrap cfg.scan mutation in try/finally, replace deprecated datetime.utcnow() calls, and clean tqdm dead branch.

Purpose: Remove misleading dead code, fix silent pattern-match failures in migration recommendations, ensure config state is always restored on exception, and eliminate Python 3.12+ deprecation warnings.

Output: 5 files/directories deleted, migration_advisor patterns fixed, run_scan.py hardened with try/finally, datetime modernized, tqdm dead code removed, 4 dead writer.py functions removed.
</objective>

<execution_context>
@/Users/digs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/digs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-legacy-debt-cleanup/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead files and directories (D-09, D-10, D-11, D-12, D-19)</name>
  <read_first>
    - quirk/connectors/__init__.py
    - quirk/connectors/aws_stub.py
    - quirk/engine/rules.py
    - quirk/intelligence/driver_text.py
    - quirk/intelligence/calibration.py
  </read_first>
  <files>quirk/connectors/, quirk/engine/rules.py, quirk/intelligence/driver_text.py, quirk/intelligence/calibration.py, data/qcscan-legacy.sqlite</files>
  <action>
Delete the following files and directories using `rm -rf` or equivalent:

1. **D-09:** Delete the entire `quirk/connectors/` directory:
   - `quirk/connectors/__init__.py`
   - `quirk/connectors/aws_stub.py`
   - `quirk/connectors/azure_stub.py`
   - `quirk/connectors/windows_adcs_stub.py`
   Verify zero imports exist first: `grep -rn "from quirk.connectors\|import quirk.connectors" quirk/ run_scan.py tests/` should return nothing.

2. **D-10:** Delete `quirk/engine/rules.py` (empty reserved file, never imported).

3. **D-11:** Delete `quirk/intelligence/driver_text.py` (polish_drivers() never called, key mismatches with evidence.py).

4. **D-12:** Delete `quirk/intelligence/calibration.py` (get_calibration() never called, wrong config field name).

5. **D-19:** Delete `data/qcscan-legacy.sqlite` (artifact from pre-rename package name).

After deletion, verify `quirk/intelligence/__init__.py` does not import from `driver_text` or `calibration`. If it does, remove those import lines. Same for `quirk/engine/__init__.py` and `rules`.
  </action>
  <verify>
    <automated>python -c "
import os, sys
dead = [
    'quirk/connectors/',
    'quirk/engine/rules.py',
    'quirk/intelligence/driver_text.py',
    'quirk/intelligence/calibration.py',
    'data/qcscan-legacy.sqlite',
]
for p in dead:
    if os.path.exists(p):
        print(f'FAIL: {p} still exists')
        sys.exit(1)
print('All dead files/dirs removed')
# Verify no broken imports
import quirk.engine
import quirk.intelligence
print('Import verification PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - Directory `quirk/connectors/` does not exist
    - File `quirk/engine/rules.py` does not exist
    - File `quirk/intelligence/driver_text.py` does not exist
    - File `quirk/intelligence/calibration.py` does not exist
    - File `data/qcscan-legacy.sqlite` does not exist
    - `python -c "import quirk.engine; import quirk.intelligence"` exits 0 (no broken imports)
  </acceptance_criteria>
  <done>All 5 dead code artifacts are deleted; no import errors result</done>
</task>

<task type="auto">
  <name>Task 2: Fix migration_advisor patterns, cfg.scan try/finally, datetime.utcnow, tqdm dead branch, dead writer functions (D-13, D-15, D-17, D-20, D-21)</name>
  <read_first>
    - quirk/assessment/migration_advisor.py
    - run_scan.py
    - quirk/logging_util.py
    - quirk/discovery/nmap_provider.py
    - quirk/reports/writer.py
  </read_first>
  <files>quirk/assessment/migration_advisor.py, run_scan.py, quirk/logging_util.py, quirk/discovery/nmap_provider.py, quirk/reports/writer.py</files>
  <action>
**D-15 -- Fix migration_advisor.py string patterns:**

In `quirk/assessment/migration_advisor.py`:

1. Line 24: Change `if "deprecated tls" in title:` to `if "legacy tls" in title:` -- this matches the actual finding title `"Legacy TLS versions allowed (TLS 1.0/1.1)"` produced by risk_engine.py (lowercased comparison).

2. Line 46: Change `if "quantum" in title or "public key" in title:` to `if "quantum" in title:` -- remove the dead `"public key"` pattern (no finding title contains this string).

**D-17 -- Wrap cfg.scan mutation in try/finally in run_scan.py:**

In `run_scan.py`, the TLS scan phase (around lines 366-386) mutates `cfg.scan.timeout_seconds` and `cfg.scan.concurrency` then restores them. Wrap in try/finally:

```python
# TLS scan phase
base_timeout = cfg.scan.timeout_seconds
base_conc = cfg.scan.concurrency
cfg.scan.timeout_seconds = tls_timeout
cfg.scan.concurrency = tls_conc

try:
    tls_endpoints = []
    with _phase_timer(run_stats, "tls_scanning"):
        if tls_targets:
            tls_endpoints = scan_tls_targets(
                cfg,
                tls_targets,
                logger=logger,
                progress_cb=None
            )
            for ep in tls_endpoints:
                key = (getattr(ep, "host", ""), int(getattr(ep, "port", 0)))
                ep.service_detail = classified_details.get(key, "")
finally:
    cfg.scan.timeout_seconds = base_timeout
    cfg.scan.concurrency = base_conc
```

Apply the same try/finally pattern to the SSH scan phase (around lines 391-411):

```python
cfg.scan.timeout_seconds = ssh_timeout
cfg.scan.concurrency = ssh_conc

try:
    ssh_endpoints = []
    with _phase_timer(run_stats, "ssh_scanning"):
        if ssh_targets:
            ssh_endpoints = scan_ssh_targets(
                cfg,
                ssh_targets,
                logger=logger,
                progress_cb=None
            )
            for ep in ssh_endpoints:
                key = (getattr(ep, "host", ""), int(getattr(ep, "port", 0)))
                ep.service_detail = classified_details.get(key, "")
finally:
    cfg.scan.timeout_seconds = base_timeout
    cfg.scan.concurrency = base_conc
```

**D-20 -- Replace datetime.utcnow():**

1. `quirk/logging_util.py` line 43: Change `ts = datetime.utcnow().strftime("%H:%M:%S")` to `ts = datetime.now(timezone.utc).strftime("%H:%M:%S")`. Add `from datetime import datetime, timezone` at the top (replace the existing `from datetime import datetime`).

2. `quirk/discovery/nmap_provider.py` line 50: Change `stamp = datetime.utcnow().strftime("%Y%m%d-%H%M%S")` to `stamp = datetime.now(timezone.utc).strftime("%Y%m%d-%H%M%S")`. Add `timezone` to the existing datetime import: `from datetime import datetime, timezone`.

**D-21 -- Remove tqdm dead branch from run_scan.py:**

1. Remove line 161-162: the `tqdm = None` assignment and its comment `# rich Progress used throughout; tqdm removed (D-04)`.

2. Remove lines 283-284: the `if tqdm:` branch and the `bar = tqdm(...)` line inside it. (The entire `if tqdm:` block including the `bar = tqdm(total=len(targets), desc="Fingerprinting", unit="target")` line.)

**D-13 -- Remove 4 dead functions from writer.py:**

Remove these 4 functions from `quirk/reports/writer.py` (keep `_extract_cert_key_type` per CONTEXT.md):
- `_count_findings()` (line 37-39)
- `_extract_cert_dates()` (line 60-74)
- `_is_self_signed()` (line 77-94)
- `_mtls_present()` (line 97-102)

Do NOT remove `_extract_cert_key_type()` (lines 42-57) -- it was fixed in Phase 1 and is the canonical cert key extraction path.
  </action>
  <verify>
    <automated>python -c "
import ast
# Check migration_advisor
with open('quirk/assessment/migration_advisor.py') as f:
    src = f.read()
assert 'deprecated tls' not in src, 'stale deprecated tls pattern'
assert '\"public key\"' not in src, 'stale public key pattern'
assert 'legacy tls' in src, 'missing legacy tls pattern'

# Check run_scan.py try/finally
with open('run_scan.py') as f:
    rs = f.read()
assert 'tqdm = None' not in rs, 'dead tqdm assignment still present'
assert rs.count('finally:') >= 2, 'need at least 2 finally blocks for TLS+SSH cfg restore'

# Check datetime
with open('quirk/logging_util.py') as f:
    lu = f.read()
assert 'utcnow' not in lu, 'utcnow still in logging_util'

with open('quirk/discovery/nmap_provider.py') as f:
    np_src = f.read()
assert 'utcnow' not in np_src, 'utcnow still in nmap_provider'

# Check dead writer functions removed
with open('quirk/reports/writer.py') as f:
    wr = f.read()
assert '_count_findings' not in wr, '_count_findings still present'
assert '_extract_cert_dates' not in wr, '_extract_cert_dates still present'
assert '_is_self_signed' not in wr, '_is_self_signed still present'
assert '_mtls_present' not in wr, '_mtls_present still present'
assert '_extract_cert_key_type' in wr, '_extract_cert_key_type must be kept'

print('All fixes verified')
" && python -m compileall quirk/assessment/migration_advisor.py quirk/logging_util.py quirk/discovery/nmap_provider.py quirk/reports/writer.py run_scan.py
    </automated>
  </verify>
  <acceptance_criteria>
    - `quirk/assessment/migration_advisor.py` contains `"legacy tls"` (not `"deprecated tls"`)
    - `quirk/assessment/migration_advisor.py` does NOT contain `"public key"`
    - `run_scan.py` contains at least 2 `finally:` blocks guarding cfg.scan restoration
    - `run_scan.py` does NOT contain `tqdm = None`
    - `run_scan.py` does NOT contain `if tqdm:`
    - `quirk/logging_util.py` contains `datetime.now(timezone.utc)` (not `datetime.utcnow()`)
    - `quirk/discovery/nmap_provider.py` contains `datetime.now(timezone.utc)` (not `datetime.utcnow()`)
    - `quirk/reports/writer.py` does NOT contain `_count_findings`, `_extract_cert_dates`, `_is_self_signed`, or `_mtls_present`
    - `quirk/reports/writer.py` DOES contain `_extract_cert_key_type`
    - `python -m compileall` succeeds for all modified files
  </acceptance_criteria>
  <done>All dead code pruned, migration patterns fixed, cfg.scan mutation guarded, datetime modernized, tqdm dead branch removed</done>
</task>

</tasks>

<verification>
1. `ls quirk/connectors/ 2>/dev/null` returns error (directory gone)
2. `grep -rn "deprecated tls" quirk/` returns empty
3. `grep -n "finally:" run_scan.py` returns 2+ matches
4. `grep -rn "utcnow" quirk/` returns empty
5. `grep -n "tqdm = None" run_scan.py` returns empty
6. `python -m compileall quirk/ run_scan.py` succeeds
7. `python -m pytest tests/ -x --timeout=60` passes (no broken imports)
</verification>

<success_criteria>
- All dead files/directories deleted
- Migration advisor patterns match actual finding titles
- cfg.scan mutation wrapped in try/finally for both TLS and SSH phases
- No datetime.utcnow() calls remain
- tqdm dead branch removed
- 4 dead writer.py functions removed (keeping _extract_cert_key_type)
</success_criteria>

<output>
After completion, create `.planning/phases/08-legacy-debt-cleanup/08-03-SUMMARY.md`
</output>
