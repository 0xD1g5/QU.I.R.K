---
phase: 80-windows-adcs-scanner
plan: 03
type: execute
wave: 2
depends_on: [01]
parallel_safe_with: [80-02]
parallel_safe_note: >
  This plan touches scoring.py + evidence.py only. Plan 80-02 touches
  adcs_scanner.py + cbom/builder.py + run_scan.py + models.py. Zero file
  overlap → safe to execute in parallel within Wave 2. Both depend only
  on Plan 80-01.
files_modified:
  - quirk/intelligence/scoring.py
  - quirk/intelligence/evidence.py
autonomous: true
requirements:
  - ADCS-04
must_haves:
  truths:
    - "SCORE_WEIGHTS gains FOUR new entries (identity_adcs_weak_template_count, identity_adcs_misconfig_count, identity_adcs_weak_signing_count, identity_adcs_coverage_gap_count) each at weight 2.0 (D-80-R6)"
    - "identity_trust_impacts list gains FOUR corresponding rows feeding the existing identity_trust subscore (NO new top-level subscore)"
    - "evidence.py gains FOUR counters that increment when a CryptoEndpoint has protocol='ADCS' matching the corresponding pattern, driven off service_detail substring tags (PATTERNS.md §4)"
    - "evidence.py export dict includes adcs_weak_template_count, adcs_misconfig_count, adcs_weak_signing_count, adcs_coverage_gap_count"
    - "SCORE_WEIGHTS sum goes from 267.0 (post-P79) to 275.0 (267 + 4 × 2.0) per D-80-R5"
    - "tests/test_score_weights_invariant.py is NOT touched (Phase 83 owns the consolidated bump per D-80-R5)"
  artifacts:
    - path: "quirk/intelligence/scoring.py"
      provides: "Four SCORE_WEIGHTS entries + four identity_trust_impacts rows + four evidence.get() extractions"
      contains: 'identity_adcs_weak_template_count'
    - path: "quirk/intelligence/evidence.py"
      provides: "Four zero-initialised counters + ADCS branch in proto loop + four export-dict entries"
      contains: 'adcs_weak_template_count'
  key_links:
    - from: "adcs_scanner.py CryptoEndpoint(protocol='ADCS')"
      to: "evidence.py ADCS branch"
      via: "ep.protocol == 'ADCS' dispatch"
      pattern: 'proto == "ADCS"'
    - from: "evidence.py exports"
      to: "scoring.py identity_trust_impacts"
      via: "evidence.get('adcs_*_count', 0)"
      pattern: 'evidence\.get\("adcs_'
---

<objective>
Wire the ADCS findings into the scoring pipeline by (1) adding FOUR counters
to `evidence.py` that count weak-template / general-misconfig / weak-signing /
coverage-gap ADCS endpoints, and (2) adding FOUR `SCORE_WEIGHTS` entries +
matching `identity_trust_impacts` rows to `scoring.py` so the counters reduce
the existing `identity_trust` subscore.

Purpose: Implements ADCS-04 — without this, findings appear in the dashboard
and CBOM but do not move the score, which would be misleading to consultants.

Output:
- FOUR `SCORE_WEIGHTS` entries at weight 2.0 each (per D-80-R6 — REQUIREMENTS
  cites 3 counters, but CONTEXT D-Area-1 "all 8 best-effort" requires the
  4th `identity_adcs_coverage_gap_count`).
- FOUR `identity_trust_impacts` rows.
- FOUR `evidence.py` counters with full lifecycle: declaration, ADCS-branch
  accumulator (driven off service_detail substring tags), and export-dict
  entry.
- **DOES NOT touch** `tests/test_score_weights_invariant.py` — per D-80-R5
  the invariant test will be temporarily red (asserts current value; new
  sum is 275.0). Phase 83 CLEAN-01 owns the consolidated bump.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/80-windows-adcs-scanner/80-CONTEXT.md
@.planning/phases/80-windows-adcs-scanner/80-RESEARCH.md
@.planning/phases/80-windows-adcs-scanner/80-PATTERNS.md
@.planning/phases/80-windows-adcs-scanner/80-01-SUMMARY.md
@.planning/phases/79-smime-ldap-discovery-scanner/79-03-SUMMARY.md
@quirk/intelligence/scoring.py
@quirk/intelligence/evidence.py
@CLAUDE.md
</context>

<interfaces>
<!-- Anchors verified 2026-05-16. -->

scoring.py SMIME entries (Phase 79 just landed) at lines ~32-34 — the new
ADCS entries land immediately after these:
```
"identity_smime_weak_signing_count": 2.0,
"identity_smime_expired_count":      2.0,
"identity_smime_weak_key_count":     2.0,
```

scoring.py SMIME identity_trust_impacts rows landed at lines ~189-191 — the
new ADCS rows land immediately after.

scoring.py SMIME `_as_int` extractions landed at lines ~156-158 — the new
ADCS extractions land immediately after.

evidence.py SMIME lifecycle (per Phase 79 03-SUMMARY): declaration ~line 90,
SMIME accumulator branch ~line 195, export-dict entries ~line 363. Grep
`smime_weak_signing_count` to find all three sites precisely (PATTERNS.md
analog).

Service-detail tag contract from Plan 80-02 (see PATTERNS.md §4):
- CA weak-signing endpoints encode `service_detail=f"ca|cn=...|reason=weak-signing-alg"`
- ESC1 misconfig endpoints encode `service_detail=f"template|cn=...|esc1-..."`
- ESC2/ESC3/ESC6 misconfig endpoints encode `service_detail=f"template|cn=...|esc2-..."` etc.
- COVERAGE-GAP endpoints encode `service_detail=f"coverage-gap|ESCN|reason=not-ldap-observable"`
- ADCS-UNREACH endpoints encode `service_detail=f"adcs-unreachable|base=..."` — does
  NOT contribute to any of the four counters (it is its own scan_error_category)

Counter naming — D-80-R6 / CONTEXT locks the `_count` suffix (consistent with
SMIME Phase 79 convention). Honor `_count`. The math at
`-_ratio(num, denom) * w["..._count"]` is unchanged; the suffix is purely a
naming convention.

Current SCORE_WEIGHTS sum: 267.0 (after Phase 79). After Plan 80-03:
267.0 + 4 * 2.0 = 275.0 (D-80-R5).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add four SCORE_WEIGHTS entries + identity_trust_impacts rows + extractions</name>
  <files>quirk/intelligence/scoring.py</files>
  <action>
    Three edits, all anchored on the just-landed SMIME analogs (grep for
    `identity_smime_weak_signing_count` to confirm exact line offsets):

    Edit A — Append four entries to the `SCORE_WEIGHTS` dict immediately after
    the SMIME triplet (around line ~34). Per D-80-R6 use the `_count` suffix
    verbatim from CONTEXT D-scoring-weights:
    ```
    "identity_adcs_weak_template_count":  2.0,   # Phase 80 ADCS-04
    "identity_adcs_misconfig_count":      2.0,   # Phase 80 ADCS-04
    "identity_adcs_weak_signing_count":   2.0,   # Phase 80 ADCS-04
    "identity_adcs_coverage_gap_count":   2.0,   # Phase 80 D-80-R6 / CONTEXT D-Area-1
    ```

    Edit B — Append four corresponding rows to the `identity_trust_impacts`
    list, immediately after the SMIME triplet (around lines ~189-191):
    ```
    ("Weak AD CS template",         -_ratio(adcs_weak_template_count, denom) * w["identity_adcs_weak_template_count"]),
    ("AD CS template misconfig",    -_ratio(adcs_misconfig_count, denom)     * w["identity_adcs_misconfig_count"]),
    ("Weak AD CS signing algo",     -_ratio(adcs_weak_signing_count, denom)  * w["identity_adcs_weak_signing_count"]),
    ("AD CS coverage gap (ESC4/5/7/8)", -_ratio(adcs_coverage_gap_count, denom) * w["identity_adcs_coverage_gap_count"]),
    ```

    Edit C — Add four `_as_int` / `max(0, ...)` extractions in the same style
    as the existing SMIME extractions (grep `smime_weak_signing_count = max(0`
    to find the analog block precisely):
    ```
    adcs_weak_template_count  = max(0, _as_int(evidence.get("adcs_weak_template_count", 0)))
    adcs_misconfig_count      = max(0, _as_int(evidence.get("adcs_misconfig_count", 0)))
    adcs_weak_signing_count   = max(0, _as_int(evidence.get("adcs_weak_signing_count", 0)))
    adcs_coverage_gap_count   = max(0, _as_int(evidence.get("adcs_coverage_gap_count", 0)))
    ```

    DO NOT touch `tests/test_score_weights_invariant.py`. Per D-80-R5 the
    invariant test will be temporarily red until Phase 83 lands. This is
    intentional and consolidated by Phase 83 CLEAN-01.

    Implements ADCS-04 (scoring leg).
  </action>
  <verify>
    <automated>python -m compileall quirk/intelligence/scoring.py &amp;&amp; python -c "from quirk.intelligence.scoring import SCORE_WEIGHTS; assert SCORE_WEIGHTS['identity_adcs_weak_template_count'] == 2.0; assert SCORE_WEIGHTS['identity_adcs_misconfig_count'] == 2.0; assert SCORE_WEIGHTS['identity_adcs_weak_signing_count'] == 2.0; assert SCORE_WEIGHTS['identity_adcs_coverage_gap_count'] == 2.0; s = sum(SCORE_WEIGHTS.values()); print('sum =', s); assert s == 275.0, f'expected 275.0 got {s}'"</automated>
  </verify>
  <done>
    Four new SCORE_WEIGHTS entries present at 2.0 each. Four
    identity_trust_impacts rows present. Four `adcs_*_count` extractions feed
    `evidence.get(...)`. Sum of SCORE_WEIGHTS is exactly 275.0.
    `tests/test_score_weights_invariant.py` is untouched and is expected to
    FAIL on this commit — intentional per D-80-R5.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add four ADCS counters to evidence.py (driven off service_detail tags)</name>
  <files>quirk/intelligence/evidence.py</files>
  <action>
    Three surgical edits modeled on the SMIME counter lifecycle that landed in
    Phase 79 (grep `smime_weak_signing_count` to find all three sites):

    Edit A — Declaration. Immediately after the SMIME declarations (around
    line ~90, the `smime_*_count = 0` block):
    ```
    adcs_weak_template_count = 0
    adcs_misconfig_count     = 0
    adcs_weak_signing_count  = 0
    adcs_coverage_gap_count  = 0
    ```

    Edit B — Accumulator branch. Add an `elif proto == "ADCS":` branch
    immediately after the SMIME branch (PATTERNS.md §4 — drive off
    service_detail substring tags emitted by the scanner; the contract is
    documented in Plan 80-02 task 1):
    ```
    elif proto == "ADCS":
        _adcs_detail  = str(getattr(ep, "service_detail", "") or "").lower()
        _adcs_alg     = str(getattr(ep, "cert_pubkey_alg", "") or "").upper()
        _adcs_size    = getattr(ep, "cert_pubkey_size", None)
        _adcs_sig_raw = getattr(ep, "cert_sig_alg", "") or ""

        # Weak CA signing: scanner tags service_detail with "weak-signing-alg"
        # and/or populates cert_sig_alg with a weak hash.
        if "weak-signing-alg" in _adcs_detail or is_weak_cipher(_adcs_sig_raw):
            adcs_weak_signing_count += 1

        # Weak template: ESC1/ESC2/ESC3 → severe template misconfig.
        elif any(tag in _adcs_detail for tag in ("esc1-", "esc2-", "esc3-")):
            adcs_weak_template_count += 1

        # General template misconfig (ESC6 MEDIUM bucket).
        elif "esc6-" in _adcs_detail:
            adcs_misconfig_count += 1

        # Coverage-gap (ESC4/5/7/8 — not LDAP-observable per D-80-R8).
        elif _adcs_detail.startswith("coverage-gap|"):
            adcs_coverage_gap_count += 1

        # ADCS-UNREACH (adcs-unreachable|base=...) deliberately falls through
        # to none of the four counters — it is its own scan_error_category
        # and surfaces as a LOW finding rather than a score-impacting count.
    ```
    Confirm the existing `is_weak_cipher` import is in scope (it is per the
    SMIME branch).

    Edit C — Export at the dict-build site (PATTERNS.md says ~line 363; grep
    `"smime_weak_signing_count":` to find it). Insert immediately after the
    SMIME export triplet:
    ```
    "adcs_weak_template_count": adcs_weak_template_count,
    "adcs_misconfig_count":     adcs_misconfig_count,
    "adcs_weak_signing_count":  adcs_weak_signing_count,
    "adcs_coverage_gap_count":  adcs_coverage_gap_count,
    ```

    Implements ADCS-04 (evidence leg).
  </action>
  <verify>
    <automated>python -m compileall quirk/intelligence/evidence.py &amp;&amp; HITS=$(grep -cE 'adcs_weak_template_count|adcs_misconfig_count|adcs_weak_signing_count|adcs_coverage_gap_count' quirk/intelligence/evidence.py) &amp;&amp; test "$HITS" -ge 12</automated>
  </verify>
  <done>
    Four counters declared, accumulated under `proto == "ADCS"` with
    service_detail-tag dispatch, and exported. `python -m compileall` exits 0.
    Grep count for the four counter names is ≥ 12 (4 names × 3 sites). The
    scoring.py + evidence.py wiring closes the loop from
    `CryptoEndpoint(protocol="ADCS")` → score impact.
  </done>
</task>

</tasks>

<verification>
- `python -m compileall quirk/intelligence/` exits 0
- `python -c "from quirk.intelligence.scoring import SCORE_WEIGHTS; assert sum(SCORE_WEIGHTS.values()) == 275.0"`
  exits 0 (D-80-R5 sum)
- `tests/test_score_weights_invariant.py` is INTENTIONALLY red on this commit.
  Phase 83 CLEAN-01 owns the consolidated invariant bump after Phase 80
  lands (per D-80-R5).
- Atomic commit with EXPLICIT file paths (NEVER `git add -A` — Plan 80-02
  runs in parallel and MUST commit only its own files):
  `feat(80-03): adcs scoring weights + evidence counters (invariant temporarily red — Phase 83 owns bump)`
  Files: `quirk/intelligence/scoring.py`, `quirk/intelligence/evidence.py`.
  Commit message MUST include a note line:
  "Note: tests/test_score_weights_invariant.py is temporarily red (sum
  drifted to 275.0); Phase 83 CLEAN-01 owns the consolidated bump per D-80-R5."
</verification>

<success_criteria>
- ADCS-04 satisfied: four counters in `evidence.py`, four SCORE_WEIGHTS
  entries + four impact rows in `scoring.py`, all feeding the existing
  `identity_trust` subscore (no new top-level subscore).
- Naming honors D-80-R6 / CONTEXT scoring-weights: `_count` suffix.
- D-80-R6 satisfied: 4 counters, NOT the 3 cited in REQUIREMENTS — the 4th
  (`identity_adcs_coverage_gap_count`) follows from CONTEXT D-Area-1
  "all 8 best-effort" decision.
- `tests/test_score_weights_invariant.py` is untouched (D-80-R5).
- SCORE_WEIGHTS sum is exactly 275.0.
- Plan 80-04 (tests + closure) unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/80-windows-adcs-scanner/80-03-SUMMARY.md`
summarizing: counter names, weight values, new SCORE_WEIGHTS sum (275.0),
service_detail tag dispatch table used in the ADCS evidence branch, and the
deliberate red state of `test_score_weights_invariant` (Phase 83 closes it).
</output>
