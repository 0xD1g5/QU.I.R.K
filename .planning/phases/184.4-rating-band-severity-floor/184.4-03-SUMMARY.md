---
phase: 184.4-rating-band-severity-floor
plan: 03
subsystem: scoring
tags: [severity-bands, congruence-guard, shared-module, refactor]

requires:
  - phase: 184.4-02
    provides: "D-13 RED-first regression test, fenced with strict xfail pending the producer-side fix"
provides:
  - "quirk/severity_bands.py — stdlib-only shared module: BAND_ORDER, BAND_THRESHOLDS, BAND_CRITICAL_ALLOWANCE, band_for_score(), cap_band_for_severity(), cap_reason()"
  - "quirk/reports/content_model.py rewired to import BAND_CRITICAL_ALLOWANCE as _BAND_CRITICAL_THRESHOLD instead of hardcoding the table"
  - "38-test unit + import-boundary suite proving the shared module's contracts and content_model.py's stdlib-only-plus-one-import property"
affects: [184.4-04, 184.4-05]

tech-stack:
  added: []
  patterns:
    - "Single stdlib-only shared module consumed by two packages that must never import each other (quirk/intelligence and quirk/reports)"
    - "ast.parse-at-test-run-time import-boundary gate (derive, don't enumerate — CLAUDE.md TOOL-04/182-07 discipline)"

key-files:
  created:
    - quirk/severity_bands.py
    - tests/test_severity_bands.py
  modified:
    - quirk/reports/content_model.py

key-decisions:
  - "D-04: band thresholds and CRITICAL allowance now live in exactly one stdlib-only module (quirk/severity_bands.py), read by both quirk/intelligence and quirk/reports"
  - "D-02: cap_band_for_severity() caps any critical_count>=1 to FAIR, never graduated by count (verified for 1, 2, 7)"
  - "_BAND_CRITICAL_THRESHOLD kept as the module-local alias name in content_model.py (not renamed) — _check_congruence() and existing tests reference it by that name"

patterns-established:
  - "Shared-table modules for cross-package constants must be stdlib-only and import-boundary-tested with ast, not with a hand-written allowed-imports list"

requirements-completed: []  # SCORE-05 spans plans 03-05; not marked complete until D-11/D-12 gates land

duration: 25min
completed: 2026-09-06
---

# Phase 184.4 Plan 03: Shared Severity-Band Module Summary

**Extracted the three-way-duplicated 85/70/55/35 band-threshold chain and the CRITICAL-allowance table into one new stdlib-only `quirk/severity_bands.py`, rewired `content_model.py`'s consumer-side table to read from it, and proved via ast-parsed import-boundary tests that the pre-I/O congruence guard's stdlib-only property survived the change.**

## Performance

- **Duration:** ~25 min
- **Completed:** 2026-09-06
- **Tasks:** 3/3 completed
- **Files modified:** 3 (1 created module, 1 created test file, 1 modified)

## Accomplishments

- Created `quirk/severity_bands.py`: `BAND_ORDER`, `BAND_THRESHOLDS`, `BAND_CRITICAL_ALLOWANCE`, `band_for_score()`, `cap_band_for_severity()` (D-01/D-02 cap-to-FAIR, no graduated ladder), and `cap_reason()` (D-09 structured, finding-text-free sentence) — verified stdlib-only (`__future__` + `typing` only).
- Rewired `quirk/reports/content_model.py`'s `_BAND_CRITICAL_THRESHOLD` to alias `quirk.severity_bands.BAND_CRITICAL_ALLOWANCE` — `_check_congruence()` and `assert_congruent()` bodies are byte-identical to before (confirmed via `git diff` scoped to those functions), and `content_model.py` still imports exactly one thing from `quirk`.
- Added `tests/test_severity_bands.py` (38 tests): boundary walk, a full 0-100 cross-check against the pre-existing `scoring._rating()` (migration-safety proof run BEFORE plan 184.4-04 rewires `_rating()`, as instructed), the no-graduated-ladder assertion at critical_count {1,2,7}, the D-13 89+1-CRITICAL→FAIR reproduction, `cap_reason()` shape/signature checks, and two `ast.parse`-at-runtime import-boundary gates.

## Task Commits

1. **Task 1: Create quirk/severity_bands.py as the stdlib-only shared table** - `9c60446e` (feat)
2. **Task 2: Source content_model.py's threshold table from the shared module without touching the guard** - `273445b8` (refactor)
3. **Task 3: Unit + import-boundary coverage for the shared module** - `50b80deb` (test)

**Plan metadata:** (this commit, following SUMMARY.md creation)

## Files Created/Modified

- `quirk/severity_bands.py` - New stdlib-only shared module (143 lines): band thresholds, CRITICAL allowance, `band_for_score()`, `cap_band_for_severity()`, `cap_reason()`.
- `quirk/reports/content_model.py` - `_BAND_CRITICAL_THRESHOLD` now aliases `quirk.severity_bands.BAND_CRITICAL_ALLOWANCE`; `_check_congruence()`/`assert_congruent()` unchanged.
- `tests/test_severity_bands.py` - New test file (38 tests): unit + import-boundary coverage.

## Verification Results

- `python -m pytest tests/test_severity_bands.py tests/test_congruence_guard.py -q` → **55 passed** (38 new + 17 pre-existing `test_congruence_guard.py`, same count as before this plan's edit).
- `python -m pytest tests/test_congruence_guard.py tests/test_intelligence_scoring.py tests/test_severity_bands.py -q` → **64 passed**.
- `python -m compileall quirk/` → success.
- `grep -E '^from quirk' quirk/reports/content_model.py` → exactly one line, importing `quirk.severity_bands`.
- `python -c "from quirk.reports.content_model import _BAND_CRITICAL_THRESHOLD as t; from quirk.severity_bands import BAND_CRITICAL_ALLOWANCE as a; assert t is a"` → succeeds (same object, not a copy).
- `git diff quirk/reports/content_model.py` scoped review confirms zero changes inside `_check_congruence()` / `assert_congruent()` bodies and the `ReportCongruenceError` f-string is untouched.

## Deviations from Plan

None — plan executed exactly as written. All acceptance criteria in the PLAN.md verified directly (see Verification Results above).

## Threat Flags

None — this plan's threat register items (T-184.4-03, T-184.4-04) were mitigations applied as specified (guard body untouched; ast-parsed import-boundary test in place). No new surface introduced.

## Self-Check: PASSED

- FOUND: `quirk/severity_bands.py`
- FOUND: `tests/test_severity_bands.py`
- FOUND: `quirk/reports/content_model.py` (modified)
- FOUND commit `9c60446e`
- FOUND commit `273445b8`
- FOUND commit `50b80deb`
