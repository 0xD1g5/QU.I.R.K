---
phase: 184.4-rating-band-severity-floor
verified: 2026-09-06T02:07:30Z
status: passed
score: 6/6 must-haves verified
overrides_applied: 0
---

# Phase 184.4: Rating Band Severity Floor Verification Report

**Phase Goal:** A scan that scores in a healthy band while holding open CRITICAL findings must
not produce a self-contradictory rating, and must not halt report generation. The readiness band
carries a CRITICAL severity floor; exactly one band producer exists with shared thresholds; the
cap is explained to the reader on every report surface.
**Verified:** 2026-09-06T02:07:30Z
**Status:** passed — 6/6 must-haves verified programmatically; both human-verification items confirmed live on 2026-09-06 (see 184.4-HUMAN-UAT.md)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth (ROADMAP Success Criterion) | Status | Evidence |
|---|---|---|---|
| 1 | A scan scoring >= 85 with one open CRITICAL finding produces a report (not a halt) | VERIFIED | `tests/test_score_severity_floor_regression.py::test_high_score_with_one_critical_still_produces_a_report` drives the real `build_evidence_summary` → `compute_readiness_score` → `write_reports()` chain (no mocks on the scoring path) with the exact UAT-184.2-05 reproduction fixture (91/100 real score, one CRITICAL). Independently re-run: 2 passed. `write_reports()` completes without raising `ReportCongruenceError` and the resulting `rating` is `FAIR` with a non-empty `rating_cap_reason`. |
| 2 | The fix is on the producer; `_check_congruence()` is untouched and remains fail-closed | VERIFIED | `quirk/reports/content_model.py::_check_congruence()`/`_BAND_CRITICAL_THRESHOLD` unchanged in shape; `tests/test_congruence_guard.py` still passes (verified in the targeted run below). The floor lives in `quirk/severity_bands.py::cap_band_for_severity()`, consumed by `quirk/intelligence/scoring.py::compute_readiness_score()` (line ~312) and `quirk/reports/html_renderer.py`'s fallback path (line ~841) — both upstream of the guard, not inside it. |
| 3 | The two contracts (producer band, guard's allowance) are proven to agree by a run-time-derived matrix walk, never a hand-copied table | VERIFIED | Read `tests/test_band_severity_matrix_gate.py` in full: probe points and CRITICAL-count range are derived from `quirk.severity_bands.BAND_THRESHOLDS`/`BAND_CRITICAL_ALLOWANCE` at test-run time (`_derive_score_probe_points`, `_derive_critical_count_range`), walks every combination against the real `_check_congruence()`, and includes a synthetic "hostile table" test (`test_matrix_walk_fails_when_allowance_table_is_mutated`) proving the walk is load-bearing, not vacuously green. Re-run independently: all pass. |
| 4 | Severity's existing score contribution (agility `high_impact`/`agility_high_impact_ratio`) is left untouched and the phase states which mechanism it uses (floor supplements, does not replace) | VERIFIED | `quirk/intelligence/scoring.py:155,217,54` — `high_impact`/`agility_high_impact_ratio=14.0` path present unchanged; `tests/test_scoring_orthogonal_contract.py` (part of the Validation per-task map, re-run independently, passing) locks non-double-counting. `severity_bands.py::cap_band_for_severity()` docstring states the orthogonality reasoning explicitly (D-03). |
| 5 | Not a Phase 184.2 regression — recorded in ROADMAP with verification method | VERIFIED | `.planning/ROADMAP.md:487-491` records the file-set comparison of all six `184.2-*-PLAN.md` `files_modified` frontmatter blocks (none touch `scoring.py` or `quirk/reports/`) as evidence the defect predates Phase 184.2's port widening. |
| 6 | BACK-89 is closed by reference (questions a-d answered) and the empty backlog dir is resolved | VERIFIED | `.planning/backlog/999.82-executive-summary-score-vs-severity-consistency/RESOLVED.md` exists with a substantive closure narrative; ROADMAP.md:497-535 answers (a)-(d) individually with evidence (e.g. (a) confirms `quirk/api/score.py` no longer exists via a direct existence check). |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `quirk/severity_bands.py` | New stdlib-only shared module: `BAND_THRESHOLDS`, `BAND_CRITICAL_ALLOWANCE`, `band_for_score()`, `cap_band_for_severity()`, `cap_reason()` | VERIFIED | Read in full. Stdlib-only (`from __future__ import annotations`, `typing` only). All five symbols present, substantively implemented, docstrings explain D-01/D-02/D-03/D-09 reasoning inline. |
| `quirk/intelligence/scoring.py` (`_rating()`, `compute_readiness_score()`) | Reads shared module; `_rating()` delegates to `band_for_score()`; `compute_readiness_score()` applies the severity floor and emits `rating_cap_reason` | VERIFIED | `from quirk.severity_bands import band_for_score, cap_band_for_severity, cap_reason` (line 5); `_rating()` is a 1-line delegation (line 108); `compute_readiness_score()` computes `numeric_band`, `critical_count` from `sev["CRITICAL"]`, `rating`, `rating_cap_reason`, and returns all three (lines 301-324) — `total_score` computed once, before and unaffected by the band cap (D-01 confirmed by code order). |
| `quirk/reports/html_renderer.py` (`_score_band()` deletion + fallback path) | `_score_band()` deleted outright; fallback path computes a severity-aware band before `assert_congruent()` | VERIFIED | `grep -n "_score_band" quirk/reports/html_renderer.py` returns zero matches — confirmed deleted, not wrapped. Fallback path (lines 828-843) computes `numeric_band = band_for_score(total_score)`, counts CRITICAL directly from `findings` (independently of the `sev_counts` display tally, per an explicit code comment addressing WR-02's exact hazard), then `band = cap_band_for_severity(...)`, `rating_cap_reason = cap_reason(...)`, before `assert_congruent(band, findings or [])` at line 885. |
| `quirk/reports/content_model.py` | Imports only the new shared module from `quirk`; `_check_congruence()`/`_BAND_CRITICAL_THRESHOLD` unmodified | VERIFIED (by test) | `tests/test_congruence_guard.py` and `tests/test_severity_bands.py`'s import-boundary assertion both pass; `_BAND_CRITICAL_THRESHOLD` table values (0/0/0/None/None) match `severity_bands.BAND_CRITICAL_ALLOWANCE` exactly per the matrix-gate's shared-source requirement. |
| `tests/test_band_severity_matrix_gate.py`, `tests/test_band_producer_scan_gate.py`, `tests/test_evidence_summary_dispositions_gate.py` | Three run-time-derived gates (D-11 x2, D-12), no hand-coded occurrence lists | VERIFIED | All three read in full. Each uses `Path.rglob`/module-table introspection at test-collection or test-run time (never a hard-coded site/value list), ships a non-empty `_DISPOSITIONS`/derived-probe ledger with self-validating tests, and includes synthetic `tmp_path` tests that prove the detector logic can actually fail (a fresh `_score_band` clone, a ninth findings-less call site, a hostile allowance table). This directly satisfies CLAUDE.md's TOOL-04 "derive, don't enumerate" standard, verified by reading the mechanism, not trusting the docstring's self-description. |
| Dashboard React surfaces (`executive.tsx`, `print.tsx`, `types/api.ts`) + rebuilt bundle | Cap reason rendered on the dashboard Executive page; production bundle rebuilt (FastAPI serves pre-built statics) | VERIFIED | `rating_cap_reason`/`ratingCapReason` present in all three source files. Bundle freshness confirmed independently: `git log` shows `quirk/dashboard/static/` committed one minute after `src/dashboard/src/pages/executive.tsx`'s last change (21:03 vs 21:02), and `grep -l rating_cap_reason quirk/dashboard/static/assets/*.js` finds it inside the actual shipped, minified JS bundle — not just the source. |
| `quirk/reports/executive.py`, `quirk/reports/writer.py`, `report.html.j2`, `docx_renderer.py`, `quirk/dashboard/api/schemas.py` | Cap reason surfaced on CLI, DOCX/scorecard, HTML/PDF, and dashboard API schema | VERIFIED | Grep confirms `rating_cap_reason` present and conditionally rendered (`if _rating_cap_reason`/`{% if rating_cap_reason %}`) in all five files; `docx_renderer.py:594-595` renders `f"Cap reason: {rating_cap_reason}"` when capped. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `scoring.py::compute_readiness_score()` | `severity_bands.cap_band_for_severity()` | direct import + call | WIRED | Confirmed at scoring.py:5,312. |
| `html_renderer.py` fallback path | `severity_bands.cap_band_for_severity()` | direct import + call, before `assert_congruent()` | WIRED | Confirmed at html_renderer.py:15,841,885 — call ordering means the guard sees the already-capped band. |
| `quirk/dashboard/api/routes/scan.py` (3 severity-blind call sites: dashboard score, /compare A, /compare B) | `build_evidence_summary(endpoints, findings)` | now pass real findings, not `None` | WIRED | Confirmed at lines 1403 (multi-line call, findings supplied), 1621, 1826-1827 — all pass a `findings`/`[f.model_dump() ...]` argument, closing the exact D-07 gap the context doc flagged. |
| `severity_bands.py` | `quirk` package | import direction | CORRECTLY UNWIRED (by design) | `severity_bands.py` imports nothing from `quirk` (D-04 stdlib-only constraint) — confirmed by reading the file's import block. |
| React `ScanData`/`ScoreData` types | dashboard pages (`executive.tsx`, `print.tsx`) | `score.rating_cap_reason` field access, conditionally rendered | WIRED | Confirmed via grep; render-parity vitest suites (`executive-pdf-cleanup.test.tsx`, `print-pdf-cleanup.test.tsx`) pass. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `compute_readiness_score()` return dict | `rating_cap_reason` | `severity_bands.cap_reason(numeric_band, rating, critical_count, total_score)` — a real function of real evidence-derived inputs, not a static string | Yes | FLOWING |
| Dashboard Executive page `score.rating_cap_reason` | fetched from `/api/scans/{id}` `ScoreData.rating_cap_reason` | `quirk/dashboard/api/routes/scan.py:1409,1445,1666,1911,1923` — `.get("rating_cap_reason")` off the real recomputed `score_dict`, not hardcoded | Yes | FLOWING |
| Rebuilt JS bundle | `rating_cap_reason` string literal present in minified asset | `npm run build` output committed alongside source change (timestamp-verified) | Yes | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| D-13 regression reproduces BACK-89 exactly and now passes | `pytest tests/test_score_severity_floor_regression.py -v` | 2 passed | PASS |
| Congruence guard remains fail-closed (untouched) | `pytest tests/test_congruence_guard.py` | passed (part of 156-test targeted run, 0 failures) | PASS |
| Matrix gate agrees across full derived matrix | `pytest tests/test_band_severity_matrix_gate.py` | passed | PASS |
| Producer-scan AST gate finds no un-dispositioned fourth band producer | `pytest tests/test_band_producer_scan_gate.py` | passed | PASS |
| Evidence-summary dispositions gate finds no un-dispositioned findings-less call site | `pytest tests/test_evidence_summary_dispositions_gate.py` | passed | PASS |
| `_score_band()` genuinely deleted (not merely renamed) | `grep -n "_score_band" quirk/reports/html_renderer.py` | zero matches | PASS |
| React bundle actually rebuilt with the new field | `grep -l rating_cap_reason quirk/dashboard/static/assets/*.js` | match found | PASS |
| Full test suite regression check | `pytest -q -m "" --ignore=tests/test_chaos_lab_idempotency.py` | 1 failed (pre-existing `test_skip_registry.py::test_no_unregistered_skips`, documented baseline unrelated to this phase), 4187 passed, 57 skipped, 72 xfailed, 5 xpassed | PASS (matches documented baseline exactly, no new regressions) |
| Dashboard vitest suite (34 files incl. `executive-pdf-cleanup`, `print-pdf-cleanup`) | `npx vitest run` (src/dashboard) | 229 passed / 229 | PASS |

### Probe Execution

Not applicable — this phase is a scoring/reporting-logic change, not a migration or CLI-tooling phase with `scripts/*/tests/probe-*.sh` conventions. No probes declared in PLAN/SUMMARY files. SKIPPED per Step 7c's own scoping (no probe files found, no phase declaration of probe-based verification).

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|---|---|---|---|---|
| SCORE-04 | 184.4-02, 184.4-04, 184.4-06, 184.4-07, 184.4-08 | Readiness band carries a CRITICAL severity floor; successful scan with open CRITICAL always produces a report | SATISFIED | D-13 regression passes end-to-end; `rating_cap_reason` surfaced on all 6 headline surfaces (CLI, HTML/PDF, DOCX, scorecard, dashboard API, dashboard React); 3 severity-blind dashboard call sites fixed. |
| SCORE-05 | 184.4-03, 184.4-05, 184.4-09 | Exactly one band producer exists, thresholds shared with the congruence guard, agreement/completeness proven by run-time-derived gates | SATISFIED | `severity_bands.py` is the single producer; `_score_band()` deleted; two D-11 gates + one D-12 gate all present, run-time-derived, self-proving (synthetic failure tests), and green. |

No orphaned requirements found — `.planning/REQUIREMENTS.md`'s traceability table lists SCORE-04/SCORE-05 against exactly the plans that declare them in PLAN frontmatter, with no extra Phase-184.4-mapped IDs left unclaimed.

### Anti-Patterns Found

None. Scanned `quirk/severity_bands.py`, `quirk/intelligence/scoring.py`, `quirk/reports/html_renderer.py`, `quirk/reports/executive.py`, `quirk/reports/writer.py`, `quirk/dashboard/api/routes/scan.py` for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER` and prose stub markers — zero matches.

Three WARNING-level code-review findings exist (not blockers, not phase-goal-relevant defects — logged for completeness, all classified by `184.4-REVIEW.md` itself as "0 blockers, 3 warnings"):

- **WR-01**: `rating_cap_reason` is threaded through six independent `.get()` call sites rather than living on the shared `ExecContent` dataclass — an architectural inconsistency with D-03's stated single-seam intent, not a functional defect (all six sites independently verified correct by this verification's grep/read).
- **WR-02**: `_check_congruence()`'s CRITICAL tally and the severity floor's CRITICAL tally use two different counting bases (coverage_gap-filtered vs. unfiltered), inert today only because `coverage_gap` findings are always INFO severity, with no regression test locking that invariant.
- **WR-03**: `cap_band_for_severity()` raises an unguarded `ValueError` (via `list.index()`) for an out-of-`BAND_ORDER` input; not reachable via any current call site but exists as unvalidated public API surface.

These are legitimate hardening opportunities but do not block the phase goal: the current, tested call graph cannot trigger either hazard, and the review itself did not classify them as blockers.

### Human Verification Required — BOTH CONFIRMED 2026-09-06

> **Resolved.** Both items below were verified live by the user via `/gsd-verify-work 184.4` and
> recorded in `184.4-HUMAN-UAT.md` (status: complete, 2 passed / 0 issues). Item 1 was run against
> a live Docker chaos lab and scored 86/100 — still above the EXCELLENT threshold of 85, so the
> floor demonstrably fired on a real scan and reports generated where they previously halted.
> Item 2 passed a visual read across all six surfaces. Retained below as the original record.


Two items are explicitly deferred with substitute automated coverage, honestly disclosed in `184.4-VALIDATION.md` and `docs/UAT-SERIES.md` (UAT-184.4-05, UAT-184.4-06) rather than silently skipped:

### 1. Live chaos-lab re-verification of the original BACK-89/UAT-184.2-05 reproduction

**Test:** Start the chaos lab TLS profile, scan `127.0.0.1` with the shipped 17-port `CONSULTING_TLS_PORTS` default, confirm the live scan (not a synthetic fixture) scores ~89/100 with a CRITICAL `TLS certificate expired` on port 9443, and confirm all report formats (HTML/PDF/DOCX/CBOM/scorecard) generate with a `FAIR` band and visible cap reason.
**Expected:** Report generation completes for the real chaos-lab target exactly as `test_score_severity_floor_regression.py`'s synthetic-fixture test proves for a fixture of the same shape.
**Why human:** Requires a running Docker chaos-lab container, unavailable in this verification session (same constraint noted by the close-out session in `184.4-VALIDATION.md`). The synthetic fixture (91/100 score, same CRITICAL/HIGH pair) is a faithful stand-in for the scoring/reporting logic under test but does not exercise the real scanner's TLS-probe-to-finding pipeline end-to-end.

### 2. Visual placement/appearance of the cap reason across all six render surfaces

**Test:** Open the CLI exec summary, HTML report, DOCX, scorecard, and dashboard Executive page for a capped scan; confirm the cap reason reads naturally adjacent to the band/score headline on each and does not visually read as a scoring driver.
**Expected:** Consistent, legible placement across all surfaces.
**Why human:** This project's render-parity tests assert field/column presence, not visual order or appearance (a documented, standing project limitation — see MEMORY.md `feedback_report_render_tests_presence_not_appearance`). Line-adjacency regression tests exist as a proxy (CLI: cap-reason line immediately follows the Rollup line; HTML: cap-reason block positioned near the Score Drivers list) but a genuine visual read was not performed.

### Gaps Summary

No gaps found. All 6 ROADMAP Success Criteria are independently verified against the actual codebase (not SUMMARY.md claims): the severity floor is implemented on the producer side only, the congruence guard is provably untouched and still fail-closed, exactly one band producer exists (the old `_score_band()` is confirmed deleted via grep, not merely renamed), both D-11 gates and the D-12 gate are genuinely run-time-derived with self-proving synthetic failure tests (not vacuously passing), the cap reason is wired through all 6 headline surfaces including a verifiably-rebuilt production dashboard bundle, and the full regression suite shows zero new failures beyond the pre-existing documented baseline. REQUIREMENTS.md's SCORE-04/SCORE-05 "Complete" status is earned. Three non-blocking code-review warnings (WR-01/02/03) remain as legitimate future hardening but do not affect current correctness of the shipped behavior. Two manual verification items are honestly deferred with substitute automated coverage rather than silently marked done.

---

_Verified: 2026-09-06T02:07:30Z_
_Verifier: Claude (gsd-verifier)_
