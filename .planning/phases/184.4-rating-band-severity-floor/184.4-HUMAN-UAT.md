---
status: complete
phase: 184.4-rating-band-severity-floor
source: [184.4-VERIFICATION.md]
started: 2026-09-06T02:07:30Z
updated: 2026-09-06T00:00:00Z
---

## Current Test

[all tests complete]

## Tests

### 1. Live chaos-lab re-verification of the original BACK-89 / UAT-184.2-05 reproduction
expected: Start the chaos lab TLS profile, run `quirk --config config-lab-core.yaml` against
`127.0.0.1` with the shipped 17-port `CONSULTING_TLS_PORTS` default. The live scan (not a synthetic
fixture) should score ~89/100 with a CRITICAL `TLS certificate expired` on port 9443, and ALL report
formats (HTML/PDF/DOCX/CBOM/scorecard) should generate with a `FAIR` band and a visible cap reason —
where before this phase, report generation halted with ReportCongruenceError.
why human: Requires a running Docker chaos-lab container, unavailable in the execution session.
substitute coverage: `tests/test_score_severity_floor_regression.py` drives the identical
CRITICAL/HIGH pair and the same real `write_reports()` chain through synthetic TLS endpoint
fixtures (91/100, verified empirically rather than hard-coded). It does not exercise the real
scanner's TLS-probe-to-finding pipeline.
result: pass
verified: 2026-09-06 (user-run, live Docker chaos lab, QU.I.R.K. v5.18.0)
observed: Live scan of 127.0.0.1 scored 86/100 (not the ~89 from the 2026-09-05 reproduction —
  live scores drift with cert-expiry math and probe timing). 86 still lands ABOVE the EXCELLENT
  threshold of 85, so the numeric band was EXCELLENT and the floor capped it to FAIR — the exact
  defect condition BACK-89 described. Report generation COMPLETED with all artifact types written;
  before this phase this same scan halted with ReportCongruenceError and zero artifacts.
  The score difference is not a defect: the contract under test is "score >= 85 with >= 1 CRITICAL
  yields FAIR and a generated report", which 86 satisfies.
note: The config block documented in docs/chaos-lab.md is INCOMPLETE and raises
  `TypeError: ScanCfg.__init__() missing 2 required positional arguments: 'concurrency' and
  'ports_tls'`. Pre-existing (that block last changed in Phase 161), out of 184.4 scope, recorded
  separately as a backlog item.

### 2. Visual placement / appearance of the cap reason across all six render surfaces
expected: Open the CLI exec summary, HTML report, PDF, DOCX, scorecard and dashboard Executive page
for a capped scan. The cap reason should read naturally adjacent to the band/score headline on each,
and should not visually read as a scoring driver.
why human: This project's render-parity tests assert field/column PRESENCE, not visual order or
appearance — a documented standing limitation.
substitute coverage: index-based line-adjacency assertions exist as a proxy (CLI: the `**Cap
reason:**` line must be the line immediately following `**Rollup:**`, asserted on both branches;
HTML: conditional block positioned beside the score card). Proxy only — no visual read performed.
result: pass
verified: 2026-09-06 (user-led visual read of the six surfaces, against reports generated by the
  live chaos-lab scan in Test 1)
observed: Cap reason reads naturally beside the band/score headline on all six surfaces and is
  not mistaken for a scoring driver. Surfaces 3-6 (PDF, DOCX, scorecard, dashboard) carry NO
  positional/appearance test coverage — this human read is their only placement evidence, so a
  future refactor of those four can regress placement silently. Recorded as a standing limitation,
  consistent with this project's documented render-test gap (presence, not appearance).

## Summary

total: 2
passed: 2
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

No gaps against Phase 184.4's own scope — both deferred items passed on live verification.

One PRE-EXISTING, out-of-scope defect was surfaced while running Test 1 and is recorded as a
durable backlog entry rather than left in prose: the `config-lab-core.yaml` example in
`docs/chaos-lab.md` is incomplete and cannot load (missing the required `scan.concurrency` and
`scan.ports_tls`). See `.planning/backlog/999.93-chaos-lab-doc-config-incomplete/IDEA.md`.
