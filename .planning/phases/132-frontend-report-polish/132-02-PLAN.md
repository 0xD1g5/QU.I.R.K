---
phase: 132-frontend-report-polish
plan: 02
type: execute
wave: 1
depends_on: ["132-00"]
files_modified:
  - quirk/reports/templates/report.html.j2
autonomous: false
requirements: [AUDIT-15]

must_haves:
  truths:
    - ".cover-meta-block uses a fixed margin-top: 40px, not margin-top: auto"
    - "The cover title/org-name block and the REPORT OWNER metadata box appear together without scrolling on a 1080p screen"
    - "Dark-mode body text (--text and --text-muted) meets WCAG AA contrast (>=4.5:1) against --bg"
    - "The report remains fully self-contained (no external CSS/CDN references)"
  artifacts:
    - path: "quirk/reports/templates/report.html.j2"
      provides: "Corrected cover-page layout + normalized typography"
      contains: "margin-top: 40px"
  key_links:
    - from: "quirk/reports/templates/report.html.j2 .cover-meta-block"
      to: "fixed-margin layout"
      via: "margin-top: 40px replacing margin-top: auto"
      pattern: "margin-top: 40px"
---

<objective>
Close AUDIT-15 (UI-01): fix the HTML report cover-page layout so the title/org block and the REPORT OWNER metadata box sit together without an excessive flexbox-pushed gap, verify dark-mode body-text contrast meets WCAG AA, and apply a consulting-grade general visual pass (font normalization + section spacing).

Purpose: The cover page is the first thing a client sees; the `margin-top: auto` flexbox gap pushes the metadata box to the bottom of the page, looking unfinished. This is one of the two most visible public-repo presentation gaps.
Output: report.html.j2 cover CSS corrected; Plan 00's cover-page RED test turns GREEN; contrast test stays GREEN; a human confirms the rendered cover on a 1080p screen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-frontend-report-polish/132-CONTEXT.md
@.planning/phases/132-frontend-report-polish/132-PATTERNS.md

<interfaces>
D-06 target: `.cover-meta-block { margin-top: 40px; ... }` (replace `margin-top: auto`)
:root palette (verify-only, already compliant): --bg #0a0a0f, --text #e8e8f0, --text-muted #8888aa
Self-containment constraint: all CSS stays inside the embedded <style> tag; no external refs (enforced by test_html_is_self_contained).
Light-mode output (if any) must be unaffected.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix cover-meta-block margin and apply general visual pass</name>
  <files>quirk/reports/templates/report.html.j2</files>
  <read_first>
    - quirk/reports/templates/report.html.j2 (cover CSS L190-255, cover HTML L333-361, :root vars L12-27, body font L32-33)
    - tests/test_132_cover_page.py (the RED margin + GREEN contrast contracts from Plan 00)
    - tests/test_html_report.py (test_html_is_self_contained — the no-external-refs guard)
    - .planning/phases/132-frontend-report-polish/132-PATTERNS.md (report.html.j2 section, lines 127-167)
  </read_first>
  <behavior>
    - Rendered .cover-meta-block CSS rule contains `margin-top: 40px` and does NOT contain `margin-top: auto`
    - --text and --text-muted contrast vs --bg stays >= 4.5:1
    - Report has no external CSS/CDN references (remains self-contained)
  </behavior>
  <action>
    In quirk/reports/templates/report.html.j2, in the `.cover-meta-block` rule replace `margin-top: auto;` with `margin-top: 40px;` (D-06); keep the border-top, background, padding, and border-radius declarations as-is. Remove the now-stale flexbox comment ("fills remaining space, pushes block down") since the block no longer auto-fills.

    Then apply a restrained general visual pass (Claude's discretion per D-07) entirely within the embedded `<style>` tag, no external references: (a) font normalization — review the body `font-family`/`font-size` and any per-section font-size overrides for consistency, normalizing obviously mismatched sizes toward a consistent scale; (b) section spacing — tighten oversized section margins/padding for a denser consulting-grade look without crowding. Keep all changes conservative and additive to existing rules; do NOT introduce new color values that would regress contrast (the contrast test will catch any --text/--text-muted/--bg change). Do NOT alter the cover HTML markup (L333-361) — only CSS. Do NOT add any `<link>`, `@import`, or CDN URL.
  </action>
  <verify>
    <automated>python -m pytest tests/test_132_cover_page.py tests/test_html_report.py -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - quirk/reports/templates/report.html.j2 `.cover-meta-block` rule contains `margin-top: 40px` and does NOT contain `margin-top: auto`
    - `grep -v '^#' quirk/reports/templates/report.html.j2 | grep -c "margin-top: auto"` returns 0 within the cover-meta-block context (the rule no longer auto-pushes)
    - tests/test_132_cover_page.py::test_cover_meta_block_no_margin_auto PASSES
    - tests/test_132_cover_page.py::test_dark_mode_contrast_ratio PASSES
    - tests/test_html_report.py::test_html_is_self_contained PASSES (no external refs introduced)
  </acceptance_criteria>
  <done>The cover-meta-block uses a fixed 40px margin; contrast and self-containment tests stay green; the general visual pass introduced no external refs or contrast regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Render a sample report artifact for human review</name>
  <files>quirk/reports/templates/report.html.j2</files>
  <read_first>
    - quirk/reports/html_renderer.py (render_html_report signature)
    - tests/test_html_report.py (minimal cfg construction)
  </read_first>
  <action>
    Generate a real sample HTML report to an output path (e.g. /tmp/quirk_132_cover.html) by calling render_html_report with a representative minimal cfg (assessment name="ACME Corp", report_owner="Security Team", data_classification="CONFIDENTIAL", timezone="UTC"), a non-empty score (total ~75 with a couple of subscores) and a couple of sample findings/roadmap_items so the cover page and first sections render with realistic content. Print the absolute output path. This artifact is the input to the human-verify checkpoint in Task 3 — do not skip generating it (per the report-render feedback note: tests assert presence, not appearance, so a real artifact must be produced for visual review).
  </action>
  <verify>
    <automated>python -c "import os; p='/tmp/quirk_132_cover.html'; assert os.path.exists(p) and os.path.getsize(p) > 1000, 'sample report not generated'; print('OK', p)"</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/quirk_132_cover.html exists and is > 1000 bytes
    - The file contains the org name "ACME Corp" and "REPORT OWNER" (or the rendered owner label)
    - The absolute path is printed to stdout for the reviewer
  </acceptance_criteria>
  <done>A realistic sample report HTML artifact exists on disk and its path is reported.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual review of rendered cover page on 1080p</name>
  <action>Pause for the user to visually review the rendered sample report at /tmp/quirk_132_cover.html. Do not auto-advance; wait for the resume signal before marking the plan complete.</action>
  <what-built>The HTML report cover-page layout fix (margin-top: auto -> 40px) plus a general font/spacing visual pass. A sample report was rendered to /tmp/quirk_132_cover.html.</what-built>
  <how-to-verify>
    1. Open /tmp/quirk_132_cover.html in a browser maximized on a 1080p (1920x1080) monitor.
    2. Confirm the cover title / org name ("ACME Corp") and the REPORT OWNER metadata box (REPORT OWNER / Scan Date / Data Classification) are BOTH visible together without scrolling — the metadata box should sit a controlled ~40px below the title block, not pushed to the bottom of the page.
    3. Confirm there is no large empty whitespace gap between the subtitle line and the metadata box.
    4. Toggle/confirm dark-mode body text is comfortably readable (WCAG AA contrast already asserted by test).
    5. Skim the rest of the report for consistent font sizing and reasonable (not crowded, not sparse) section spacing.
  </how-to-verify>
  <resume-signal>Type "approved" if the cover and spacing look consulting-grade, or describe the specific layout/spacing issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scan data → report template | Findings/org metadata rendered into HTML (Jinja2 autoescape on) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-02-01 | Tampering / XSS | report.html.j2 rendered fields | accept | Jinja2 environment uses select_autoescape(["html","j2"]) and a `sanitize` filter; this plan only edits CSS, not data-binding markup, so the existing escaping posture is unchanged |
| T-132-02-02 | Information Disclosure | external CSS/CDN ref leaking referrer | mitigate | Visual pass stays inside the embedded <style> tag; test_html_is_self_contained enforces no external refs, keeping the report offline/airgap-safe |
| T-132-02-03 | Tampering | contrast/color regression | mitigate | test_dark_mode_contrast_ratio (Plan 00) parses live --text/--text-muted/--bg and fails on any <4.5:1 regression introduced by the visual pass |
| T-132-02-SC | Tampering | npm/pip installs | mitigate | No new packages; CSS-only template edit + existing jinja2/pytest. No legitimacy gate required. |
</threat_model>

<verification>
- tests/test_132_cover_page.py GREEN (no margin-top: auto; contrast >= 4.5:1)
- tests/test_html_report.py GREEN (self-contained, no external refs)
- Sample artifact /tmp/quirk_132_cover.html generated and reviewed
- Human confirms title + metadata box visible together on 1080p, no excessive gap
</verification>

<success_criteria>
The cover-meta-block uses margin-top: 40px; dark-mode body text meets WCAG AA; the report stays self-contained; a human approves the rendered cover page and general spacing on a 1080p screen.
</success_criteria>

<output>
Create `.planning/phases/132-frontend-report-polish/132-02-SUMMARY.md` when done.
</output>
