---
phase: 132-frontend-report-polish
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/dashboard/src/test-setup.ts
  - src/dashboard/src/context/__tests__/AuthProvider.test.tsx
  - tests/test_132_csp_and_auth.py
  - tests/test_132_cover_page.py
autonomous: true
requirements: [AUDIT-14, AUDIT-15]

must_haves:
  truths:
    - "A failing test asserts the auth token lives in sessionStorage and NOT localStorage"
    - "A failing test asserts /api/* responses carry a Content-Security-Policy header"
    - "A failing test asserts .cover-meta-block does not use margin-top: auto"
    - "A test asserts dark-mode --text and --text-muted meet WCAG AA (>=4.5:1) against --bg"
  artifacts:
    - path: "tests/test_132_csp_and_auth.py"
      provides: "CSP header presence RED test"
      contains: "Content-Security-Policy"
    - path: "tests/test_132_cover_page.py"
      provides: "Cover-page layout + contrast RED tests"
      contains: "cover-meta-block"
    - path: "src/dashboard/src/context/__tests__/AuthProvider.test.tsx"
      provides: "sessionStorage migration RED test"
      contains: "sessionStorage"
    - path: "src/dashboard/src/test-setup.ts"
      provides: "sessionStorage shim for jsdom tests"
      contains: "sessionStorage"
  key_links:
    - from: "tests/test_132_csp_and_auth.py"
      to: "quirk.dashboard.api.app.create_app"
      via: "TestClient app factory"
      pattern: "create_app"
    - from: "tests/test_132_cover_page.py"
      to: "quirk.reports.html_renderer.render_html_report"
      via: "render + regex on output"
      pattern: "render_html_report"
---

<objective>
Write the Wave 0 RED tests for Phase 132. These tests MUST fail (or, for already-compliant assertions, pass against current values while pinning the contract) before any implementation runs in Wave 1.

Purpose: Lock the observable contracts for both audit tracks (AUDIT-14 sessionStorage + CSP, AUDIT-15 cover-page layout + contrast) so the Wave 1 implementations are verified by tests written first.
Output: Two new Python test files, one new React test file, and a sessionStorage shim added to the test setup harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-frontend-report-polish/132-CONTEXT.md
@.planning/phases/132-frontend-report-polish/132-PATTERNS.md

<interfaces>
From quirk/reports/html_renderer.py:
```python
def render_html_report(path, cfg, endpoints, findings, score, conf, roadmap_items, *, exec_content=None) -> None
```
From quirk/dashboard/api/app.py:
```python
def create_app() -> FastAPI  # SecurityHeadersMiddleware registered at L100
```
CSS variable target values in quirk/reports/templates/report.html.j2 :root block:
  --bg: #0a0a0f ; --text: #e8e8f0 ; --text-muted: #8888aa
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sessionStorage shim to test setup harness</name>
  <files>src/dashboard/src/test-setup.ts</files>
  <read_first>
    - src/dashboard/src/test-setup.ts (the existing localStorage shim block to mirror)
    - .planning/phases/132-frontend-report-polish/132-PATTERNS.md (the test-setup.ts section, lines 171-213)
  </read_first>
  <action>
    Append a sessionStorage shim immediately after the existing localStorage shim block in src/dashboard/src/test-setup.ts. Mirror the existing localStorage shim exactly: guard with `if (typeof globalThis.sessionStorage === "undefined")`, back it with a `new Map<string, string>()`, implement the full `Storage` interface (`length` getter, `clear`, `getItem` returning null when absent, `key`, `removeItem`, `setItem` coercing value via `String(value)`), then `Object.defineProperty(globalThis, "sessionStorage", { value: shim, writable: true, configurable: true })`. Do not modify the existing localStorage shim. NOTE: the file is at src/dashboard/src/test-setup.ts (NOT under __tests__/).
  </action>
  <verify>
    <automated>grep -c "sessionStorage" src/dashboard/src/test-setup.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/dashboard/src/test-setup.ts contains `globalThis.sessionStorage === "undefined"`
    - src/dashboard/src/test-setup.ts contains `Object.defineProperty(globalThis, "sessionStorage"`
    - The existing localStorage shim block is unchanged (grep still finds `globalThis.localStorage === "undefined"`)
  </acceptance_criteria>
  <done>Both localStorage and sessionStorage shims exist in test-setup.ts with identical Storage-interface implementations.</done>
</task>

<task type="auto">
  <name>Task 2: Write Python RED tests for CSP header and cover-page/contrast</name>
  <files>tests/test_132_csp_and_auth.py, tests/test_132_cover_page.py</files>
  <read_first>
    - tests/test_console_hardening.py (the SecurityHeadersMiddleware section + _client app factory)
    - tests/test_html_report.py (the render helper + self-contained regex pattern)
    - quirk/reports/html_renderer.py (render_html_report signature, L264-274)
    - quirk/reports/templates/report.html.j2 (the :root CSS vars and .cover-meta-block rule)
    - .planning/phases/132-frontend-report-polish/132-PATTERNS.md (test file sections, lines 217-340)
  </read_first>
  <action>
    Create tests/test_132_csp_and_auth.py with a `_client()` factory returning `TestClient(create_app(), raise_server_exceptions=False)`, and `test_csp_header_present()` that GETs `/api/health`, reads the `Content-Security-Policy` response header, and asserts `"script-src 'self'"` and `"object-src 'none'"` are both substrings (per D-05). This test MUST currently fail (CSP not yet added in Wave 1).

    Create tests/test_132_cover_page.py with two tests. (a) `test_cover_meta_block_no_margin_auto(tmp_path)`: call `render_html_report(path=str(tmp_path/"report.html"), cfg=<minimal SimpleNamespace>, endpoints=[], findings=[], score={"total":75,"subscores":{},"drivers":[]}, conf={"confidence":80,"confidence_factors":{}}, roadmap_items=[])`, read the output, regex-extract the `.cover-meta-block { ... }` rule body, assert the rule was found and assert `"margin-top: auto"` is NOT in it (per D-06). This MUST currently fail (template still has margin-top: auto). Build the minimal cfg as a SimpleNamespace with `.assessment` (name, report_owner, data_classification, timezone) and `.output.directory`. (b) `test_dark_mode_contrast_ratio()`: read quirk/reports/templates/report.html.j2 text, regex-parse `--bg`, `--text`, `--text-muted` six-digit hex values from the `:root` block, implement WCAG relative-luminance + contrast-ratio math inline (sRGB linearization with the 0.04045 threshold and 0.2126/0.7152/0.0722 coefficients, contrast = (Llighter+0.05)/(Ldarker+0.05)), and assert both `--text` and `--text-muted` give contrast >= 4.5 against `--bg` (per D-07). This test passes today (current palette is compliant) and pins the contrast contract.

    Use the import blocks from the PATTERNS analogs. Do not hardcode color values — parse them from the template so the test fails if a future edit regresses contrast.
  </action>
  <verify>
    <automated>python -m pytest tests/test_132_csp_and_auth.py tests/test_132_cover_page.py -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_132_csp_and_auth.py contains `def test_csp_header_present` and references `Content-Security-Policy`, `script-src 'self'`, `object-src 'none'`
    - tests/test_132_cover_page.py contains `def test_cover_meta_block_no_margin_auto` and `def test_dark_mode_contrast_ratio`
    - `test_csp_header_present` FAILS now (no CSP header yet)
    - `test_cover_meta_block_no_margin_auto` FAILS now (template still uses margin-top: auto)
    - `test_dark_mode_contrast_ratio` PASSES now (current palette compliant) and parses values via regex, not hardcoded
  </acceptance_criteria>
  <done>Three Python test functions exist; the two RED tests fail for the documented reasons; the contrast test passes by parsing the live template.</done>
</task>

<task type="auto">
  <name>Task 3: Write React RED test for sessionStorage auth-token migration</name>
  <files>src/dashboard/src/context/__tests__/AuthProvider.test.tsx</files>
  <read_first>
    - src/dashboard/src/context/__tests__/QRAMMProvider.test.tsx (vi.mock + MSW server pattern)
    - src/dashboard/src/context/AuthProvider.tsx (setToken / logout / mount-probe callsites under test)
    - src/dashboard/src/context/auth-context.ts (useAuth hook + AuthContext type)
    - .planning/phases/132-frontend-report-polish/132-PATTERNS.md (AuthProvider.test.tsx section, lines 344-405)
  </read_first>
  <action>
    Create src/dashboard/src/context/__tests__/AuthProvider.test.tsx. Mock `@/lib/api` to export `setUnauthorizedHandler: vi.fn()` (and `fetchApi` if AuthProvider imports it). Set up an MSW server handling `GET /api/scans` -> `HttpResponse.json([], { status: 200 })` with the standard listen/resetHandlers/close lifecycle. In a `describe("AuthProvider — sessionStorage migration (AUDIT-14 / D-01)")` block with `beforeEach` clearing both `sessionStorage` and `localStorage`: (1) render AuthProvider wrapping a consumer of `useAuth`, call `setToken("tok")` inside `act`, then assert `sessionStorage.getItem("quirk_api_token") === "tok"` AND `localStorage.getItem("quirk_api_token") === null`. (2) seed `sessionStorage.setItem("quirk_api_token","tok")`, call `logout()` inside `act`, then assert `sessionStorage.getItem("quirk_api_token") === null`. Both assertions MUST currently fail because AuthProvider still writes to localStorage. Use the useAuth hook from auth-context.ts to reach setToken/logout (render a probe component, or use renderHook if available in this codebase — check QRAMMProvider.test.tsx for which approach is used).
  </action>
  <verify>
    <automated>cd src/dashboard && npx vitest run src/context/__tests__/AuthProvider.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - src/dashboard/src/context/__tests__/AuthProvider.test.tsx contains `sessionStorage.getItem("quirk_api_token")` and `localStorage.getItem("quirk_api_token")`
    - The file imports AuthProvider from "../AuthProvider" and uses the useAuth hook
    - The "setToken writes to sessionStorage, NOT localStorage" test FAILS now (AuthProvider still uses localStorage)
    - The "logout removes from sessionStorage" test FAILS now
  </acceptance_criteria>
  <done>Two RED vitest assertions exist and fail because the implementation still targets localStorage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → none | Test-only code; no production trust boundary crossed in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-00-01 | Tampering | test-setup.ts sessionStorage shim | accept | Shim is test-only, guarded by `typeof === "undefined"`, never bundled into the production SPA (Vite excludes test-setup from build) |
| T-132-00-02 | Information Disclosure | RED tests reading template/palette | accept | Tests read repo-local files only; no secrets, no network beyond MSW in-process mock |
| T-132-00-SC | Tampering | npm/pip installs | mitigate | No new packages installed in this plan; uses existing pytest, vitest, msw, fastapi testclient. No legitimacy gate needed. |
</threat_model>

<verification>
- `python -m pytest tests/test_132_csp_and_auth.py tests/test_132_cover_page.py` runs; CSP + margin tests RED, contrast test GREEN
- `cd src/dashboard && npx vitest run src/context/__tests__/AuthProvider.test.tsx` runs; both assertions RED
- `python -m compileall tests/test_132_csp_and_auth.py tests/test_132_cover_page.py` exits 0
</verification>

<success_criteria>
All four test contracts exist. The three behavior-change tests fail for the documented Wave-1 reasons; the contrast-ratio test passes against the live template. No production code is modified in this plan.
</success_criteria>

<output>
Create `.planning/phases/132-frontend-report-polish/132-00-SUMMARY.md` when done.
</output>
