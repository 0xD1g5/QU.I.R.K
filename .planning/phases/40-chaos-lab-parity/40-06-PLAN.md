---
phase: 40-chaos-lab-parity
plan: 06
type: execute
wave: 4
depends_on: ["40-01", "40-02", "40-03", "40-04", "40-05"]
files_modified:
  - docs/UAT-SERIES.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [LAB-01, LAB-02, LAB-03, LAB-04]
must_haves:
  truths:
    - "docs/UAT-SERIES.md gains a UAT entry referencing expected_results_v4.md as the stable v4 oracle (D-19)"
    - "Obsidian Phase-40 phase note exists at /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-40-Chaos-Lab-Parity.md with frontmatter status: complete and What Was Built sections from each plan SUMMARY"
    - "Obsidian Chaos-Lab.md guide note re-synced from updated docs/chaos-lab.md"
    - "Obsidian UAT-Series.md re-synced from updated docs/UAT-SERIES.md"
    - "./lab.sh status and ./lab.sh logs <service> verified working against vault, database, storage-s3, email, broker (LAB-04 evidence captured)"
  artifacts:
    - path: docs/UAT-SERIES.md
      provides: UAT entry for v4 oracle
      contains: "expected_results_v4.md"
    - path: /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-40-Chaos-Lab-Parity.md
      provides: Obsidian phase note
      contains: "status: complete"
    - path: /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Chaos-Lab.md
      provides: synced guide note
      contains: "expected_results_v4.md"
    - path: /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md
      provides: synced UAT series
      contains: "expected_results_v4.md"
  key_links:
    - from: docs/UAT-SERIES.md
      to: quantum-chaos-enterprise-lab/expected_results_v4.md
      via: "UAT entry naming the v4 oracle as reference"
      pattern: "expected_results_v4.md"
---

<objective>
Phase completion plan. Three concrete deliverables per CLAUDE.md "Mandatory Phase Completion Steps" + memory feedbacks `feedback_uat_series_update` + `feedback_obsidian_phase_notes`:

1. Update `docs/UAT-SERIES.md` to add a UAT entry referencing the new v4 oracle (D-19a).
2. Sync three Obsidian notes: Phase-40 phase note (new), Chaos-Lab.md guide note (re-sync), UAT-Series.md (re-sync) (D-19b).
3. Smoke-verify LAB-04 (`./lab.sh status` + `./lab.sh logs <service>` work cleanly against vault, database, storage-s3, email, broker) and capture evidence in the phase summary — this is the human-checkpoint per D-17.

Purpose: closes LAB-01..04 audit by ensuring every artifact ships and is discoverable from the project's documentation index.

Output: `docs/UAT-SERIES.md` updated; three vault filesystem files written; LAB-04 smoke evidence in summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-chaos-lab-parity/40-CONTEXT.md
@.planning/phases/40-chaos-lab-parity/40-RESEARCH.md
@CLAUDE.md
@docs/UAT-SERIES.md
@docs/chaos-lab.md
@quantum-chaos-enterprise-lab/expected_results_v4.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/UAT-SERIES.md with v4-oracle UAT entry</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (entire file — find the existing pattern for UAT entries, identify the right series for chaos-lab oracle reference)
    - quantum-chaos-enterprise-lab/expected_results_v4.md (file being referenced — use anchor format `expected_results_v4.md#profile-<name>` for any per-profile pointers)
    - .planning/phases/40-chaos-lab-parity/40-CONTEXT.md (D-19a — must add UAT entry naming v4 oracle as the stable reference)
    - CLAUDE.md (§"Mandatory Phase Completion Steps" — UAT-SERIES update is item #2, sync is item #3, commit is item #4)
  </read_first>
  <action>
    Add a new UAT-40-XX series entry to `docs/UAT-SERIES.md` introducing `expected_results_v4.md` as the stable v4 oracle for chaos-lab UAT runs.

    The entry should:
    - Be added in the appropriate location (after the most recent UAT series — read the file first to determine its current ordering)
    - Identify itself as `UAT-40-01: Chaos lab v4 oracle reference`
    - Have a 1-paragraph description: "Use `quantum-chaos-enterprise-lab/expected_results_v4.md` as the authoritative expected-findings oracle for every chaos-lab profile (v4.0 baseline through v4.4 messaging). Each profile is documented under a `## Profile: <name>` H2 anchor; cross-link via `expected_results_v4.md#profile-<name>`."
    - Include a Pass Criteria block listing:
      - `expected_results_v4.md` exists
      - Every profile in `./lab.sh profiles` output has a matching `## Profile: <name>` section
      - `expected_results_v3.md` carries a "Superseded by" notice
      - `quantum-chaos-enterprise-lab/README.md` Profile Summary Table links each row to a `#profile-<name>` anchor

    Also update the document header `**Last Updated:**` field to today's date (2026-04-29 or the actual execution date).

    Do not invent a new series number scheme — match the existing UAT-NN-NN pattern in the file.
  </action>
  <verify>
    <automated>
      grep -q 'UAT-40-01' docs/UAT-SERIES.md &amp;&amp; \
      grep -q 'expected_results_v4.md' docs/UAT-SERIES.md &amp;&amp; \
      grep -q 'Chaos lab v4 oracle' docs/UAT-SERIES.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `docs/UAT-SERIES.md` contains identifier `UAT-40-01`
    - The entry references `expected_results_v4.md` by name
    - The entry has a Pass Criteria block enumerating verifiable conditions
    - Document header Last Updated date is bumped to execution date
  </acceptance_criteria>
  <done>UAT-SERIES.md gains the v4-oracle reference entry per D-19a.</done>
</task>

<task type="auto">
  <name>Task 2: Sync Obsidian — Phase-40 phase note + Chaos-Lab.md guide + UAT-Series.md</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-40-Chaos-Lab-Parity.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Chaos-Lab.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md</files>
  <read_first>
    - .planning/phases/40-chaos-lab-parity/40-01-SUMMARY.md, 40-02-SUMMARY.md, 40-03-SUMMARY.md, 40-04-SUMMARY.md, 40-05-SUMMARY.md (all created by prior plans — source for "What Was Built" section of the phase note)
    - .planning/phases/40-chaos-lab-parity/40-CONTEXT.md (D-19b — three notes to sync; goal + decisions to summarize in phase note)
    - docs/chaos-lab.md (just-updated source for the Chaos-Lab guide note sync)
    - docs/UAT-SERIES.md (just-updated source for the UAT-Series sync)
    - CLAUDE.md (§"Mandatory Phase Completion Steps" — write directly to vault filesystem; do NOT use `obsidian CLI content=`; vault path patterns)
    - One existing Phase note for template reference (e.g. /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-39-* if present)
  </read_first>
  <action>
    Three filesystem writes (per CLAUDE.md "write directly to the vault filesystem — do NOT use `obsidian CLI content=` — files are too large for shell expansion"):

    **A) Phase-40 phase note**

    Write `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-40-Chaos-Lab-Parity.md` with this structure (use the same format as existing Phase-NN-* notes in that folder):

    ```markdown
    ---
    project: QU.I.R.K.
    type: phase
    status: complete
    source: .planning/phases/40-chaos-lab-parity/
    updated: <YYYY-MM-DD>
    ---

    # Phase 40 — Chaos Lab Parity

    ## Goal
    `lab.sh`, the README, and a new `expected_results_v4.md` oracle document fully cover every profile shipped through v4.4 so consultants running the lab see complete scanner-equivalent coverage and UAT can reference a stable oracle.

    ## Requirements Covered
    - LAB-01 — `./lab.sh all` covers every profile (structurally — dynamic derivation from docker-compose.yml)
    - LAB-02 — README + docs/chaos-lab.md document every shipped profile (v4.0 → v4.4)
    - LAB-03 — `expected_results_v4.md` is the stable v4 oracle for all 18 named profiles + core
    - LAB-04 — `./lab.sh status` and `./lab.sh logs <service>` work cleanly against v4.3 + v4.4 profiles (smoke-verified in plan 40-06)

    ## Success Criteria
    1. `./lab.sh all` starts every profile defined in docker-compose.yml (verified via `./lab.sh profiles | wc -l` returns 18)
    2. `./lab.sh status` / `./lab.sh logs` work against vault, database, storage-s3, email, broker
    3. `quantum-chaos-enterprise-lab/README.md` documents every shipped profile with ports + expected-findings link
    4. `expected_results_v4.md` exists and contains the per-profile oracle for every named profile + core

    ## What Was Built
    ### Plan 40-01 — lab.sh dynamic profile parser
    <summarize from 40-01-SUMMARY.md>

    ### Plan 40-02 — Oracle listener-profile sections
    <summarize from 40-02-SUMMARY.md>

    ### Plan 40-03 — Oracle DAR + messaging sections
    <summarize from 40-03-SUMMARY.md>

    ### Plan 40-04 — README rewrite
    <summarize from 40-04-SUMMARY.md>

    ### Plan 40-05 — docs/chaos-lab.md extension
    <summarize from 40-05-SUMMARY.md>

    ### Plan 40-06 — UAT-SERIES + Obsidian sync (this plan)

    ## Links
    - [[Roadmap]]
    - [[Chaos-Lab]] (operator guide)
    - [[UAT-Series]]
    ```

    **B) Chaos-Lab.md guide re-sync**

    Build the file in `/tmp` then copy:
    ```bash
    printf -- "---\nproject: QU.I.R.K.\ntype: guide\nstatus: active\nsource: docs/chaos-lab.md\nupdated: <YYYY-MM-DD>\n---\n\n" > /tmp/chaos_lab_vault.md
    cat docs/chaos-lab.md >> /tmp/chaos_lab_vault.md
    cp /tmp/chaos_lab_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Chaos-Lab.md"
    ```

    **C) UAT-Series.md re-sync** (per CLAUDE.md "Sync UAT-SERIES.md to Obsidian" snippet)

    ```bash
    printf -- "---\nproject: QU.I.R.K.\ntype: reference\nstatus: active\nsource: docs/UAT-SERIES.md\nupdated: <YYYY-MM-DD>\n---\n\n" > /tmp/uat_vault.md
    cat docs/UAT-SERIES.md >> /tmp/uat_vault.md
    cp /tmp/uat_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
    ```

    Replace `<YYYY-MM-DD>` with the actual execution date.
  </action>
  <verify>
    <automated>
      test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-40-Chaos-Lab-Parity.md" &amp;&amp; \
      grep -q 'status: complete' "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-40-Chaos-Lab-Parity.md" &amp;&amp; \
      grep -q 'LAB-01' "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-40-Chaos-Lab-Parity.md" &amp;&amp; \
      grep -q 'LAB-04' "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-40-Chaos-Lab-Parity.md" &amp;&amp; \
      test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Chaos-Lab.md" &amp;&amp; \
      grep -q 'expected_results_v4.md' "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Chaos-Lab.md" &amp;&amp; \
      test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" &amp;&amp; \
      grep -q 'UAT-40-01' "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-40-Chaos-Lab-Parity.md` with `status: complete`
    - Phase note frontmatter includes type: phase, source path, updated date
    - Phase note covers all four LAB-XX requirements
    - Chaos-Lab.md guide note exists at vault path with frontmatter and full doc body
    - UAT-Series.md vault note contains UAT-40-01 entry (proves it's the latest version)
  </acceptance_criteria>
  <done>Three Obsidian notes synced per CLAUDE.md mandatory completion steps + D-19b.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify LAB-04 — ./lab.sh status + ./lab.sh logs work cleanly against v4.3+v4.4 profiles</name>
  <action>
    Plan 40-01 made `./lab.sh all` use a dynamic compose-derived profile list. LAB-04 asks for evidence that `./lab.sh status` and `./lab.sh logs <service>` work cleanly against the previously-missing profiles (vault, database, storage-s3, email, broker). Per D-17 this needs operator-side verification because it requires Docker to be running.
  
    Run these commands locally (requires Docker daemon up):

    ```bash
    cd quantum-chaos-enterprise-lab
    ./lab.sh profiles                             # Should print 18 profiles, sorted, alphabetical
    PROFILE_ARGS="--profile vault" ./lab.sh up    # Boots vault-30 on 28200
    ./lab.sh status                               # Should show vault-30 running, no orphan/error rows
    ./lab.sh logs vault-30 &amp;                     # Should tail logs cleanly; Ctrl+C to stop
    ./lab.sh down

    PROFILE_ARGS="--profile database" ./lab.sh up
    ./lab.sh status                               # postgres-ssl-off (25432) + mysql-ssl-off (23306) up
    ./lab.sh logs postgres-ssl-off                # Tail cleanly
    ./lab.sh down

    PROFILE_ARGS="--profile storage-s3" ./lab.sh up
    ./lab.sh status                               # minio (29000) + minio-seed up
    ./lab.sh logs minio
    ./lab.sh down

    PROFILE_ARGS="--profile email" ./lab.sh up
    ./lab.sh status                               # postfix-email + dovecot-email up
    ./lab.sh logs postfix-email
    ./lab.sh down

    PROFILE_ARGS="--profile broker" ./lab.sh up
    ./lab.sh status                               # kafka-broker + rabbitmq-broker + redis-broker up
    ./lab.sh logs kafka-broker
    ./lab.sh down
    ```

    Expected outcomes:
    - Each `./lab.sh status` lists only the expected services (no broken / orphan rows referencing renamed services)
    - Each `./lab.sh logs <service>` attaches without "service not found" errors
    - All five profile teardowns are clean (no stuck containers)

    Capture pass/fail per profile. Any failure → blocker; revise compose or service references and re-run.
  </action>
  <resume-signal>Type "approved" with a one-line confirmation that all 5 profiles passed status + logs checks, OR describe specific failures (which profile, which command, error text).</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Update STATE.md, finalize phase summaries, commit</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/STATE.md (current — Phase 40 status to bump from "context gathered" to "complete")
    - .planning/REQUIREMENTS.md (lines 124-127 — current LAB-01..LAB-04 traceability rows; format `| LAB-0N | Phase 40 | pending |`)
    - .planning/phases/40-chaos-lab-parity/40-0*-SUMMARY.md (all six SUMMARY files — should now exist)
    - CLAUDE.md (§"Mandatory Phase Completion Steps" item #4 — commit pattern)
  </read_first>
  <action>
    1. Update `.planning/STATE.md`:
       - `stopped_at`: change from "Phase 40 context gathered" to "Phase 40 complete"
       - `last_activity`: bump to today's date with "Phase 40 complete" message
       - Update `## Current Position` block: Phase 40 (chaos-lab-parity) — COMPLETE; Plan: 6 of 6
       - Add a row to `## Accumulated Context` capturing this phase's stats

    2. Verify each plan has its SUMMARY.md (the plan executors create these per <output> blocks; this task just confirms they exist).

    3. **Edit `.planning/REQUIREMENTS.md` lines 124-127 directly** — change `pending` to `Complete` for each of LAB-01, LAB-02, LAB-03, LAB-04. The four rows have format `| LAB-0N | Phase 40 | pending |`; replace `pending` with `Complete` on each row so the verify regex `^\| LAB-01 \| Phase 40 \| Complete` matches. Then run roadmap update so Phase 40 is marked `[x]` in `.planning/ROADMAP.md` (the roadmap helper does NOT touch REQUIREMENTS.md — that is why step 3 edits it directly).

    4. Commit:
    ```bash
    node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-40): close phase 40 — chaos lab parity (lab.sh dynamic profiles + v4 oracle + README + chaos-lab.md + UAT entry + Obsidian sync)" \
      --files .planning/STATE.md \
      --files .planning/ROADMAP.md \
      --files .planning/REQUIREMENTS.md \
      --files .planning/phases/40-chaos-lab-parity/40-01-SUMMARY.md \
      --files .planning/phases/40-chaos-lab-parity/40-02-SUMMARY.md \
      --files .planning/phases/40-chaos-lab-parity/40-03-SUMMARY.md \
      --files .planning/phases/40-chaos-lab-parity/40-04-SUMMARY.md \
      --files .planning/phases/40-chaos-lab-parity/40-05-SUMMARY.md \
      --files .planning/phases/40-chaos-lab-parity/40-06-SUMMARY.md
    ```

    Also commit the docs+lab artifacts (separate commit OK or combined per executor judgment):
    ```bash
    node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-40): update UAT-SERIES.md (add UAT-40-01 v4 oracle reference)" --files docs/UAT-SERIES.md
    ```

    The lab.sh + oracle + README + docs/chaos-lab.md changes will already have been committed by their respective plan executors. This task closes the phase.
  </action>
  <verify>
    <automated>
      grep -q 'Phase 40 complete' .planning/STATE.md &amp;&amp; \
      grep -E '^\| LAB-01 \| Phase 40 \| Complete' .planning/REQUIREMENTS.md &amp;&amp; \
      grep -E '^\| LAB-04 \| Phase 40 \| Complete' .planning/REQUIREMENTS.md &amp;&amp; \
      ls .planning/phases/40-chaos-lab-parity/40-0[1-6]-SUMMARY.md | wc -l | grep -q '^6$' &amp;&amp; \
      git log -1 --pretty=%s | grep -q 'phase-40'
    </automated>
  </verify>
  <acceptance_criteria>
    - STATE.md `stopped_at` is "Phase 40 complete"
    - REQUIREMENTS.md traceability rows for LAB-01..LAB-04 all show Status: Complete
    - All six SUMMARY.md files exist at .planning/phases/40-chaos-lab-parity/
    - Latest git commit references phase-40 in subject
  </acceptance_criteria>
  <done>Phase 40 closed. All four LAB requirements marked complete in REQUIREMENTS.md. Repo state shipped.</done>
</task>

</tasks>

<threat_model>
No new runtime attack surface — documentation, vault notes (local filesystem only), and git commits.
</threat_model>

<verification>
- UAT-SERIES.md gains UAT-40-01 entry referencing expected_results_v4.md
- 3 Obsidian vault files exist with correct frontmatter
- LAB-04 human-verified across all 5 v4.3+v4.4 profiles
- STATE.md + REQUIREMENTS.md + ROADMAP.md updated
- Phase 40 closing commit present
</verification>

<success_criteria>
- All four ROADMAP Phase 40 Success Criteria satisfied (LAB-01 via plan 40-01, LAB-04 via plan 40-06 task 3, LAB-02 via plans 40-04+40-05, LAB-03 via plans 40-02+40-03)
- CLAUDE.md "Mandatory Phase Completion Steps" all four items completed
- D-19a (UAT-SERIES update) and D-19b (Obsidian sync) satisfied
</success_criteria>

<output>
Create `.planning/phases/40-chaos-lab-parity/40-06-SUMMARY.md` with:
- UAT-SERIES.md entry added
- Obsidian sync evidence (3 file paths verified)
- LAB-04 smoke-test results per profile (vault, database, storage-s3, email, broker)
- Final state of REQUIREMENTS.md traceability rows
- Closing commit SHA
</output>
