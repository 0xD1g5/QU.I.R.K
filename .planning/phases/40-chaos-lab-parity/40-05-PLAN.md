---
phase: 40-chaos-lab-parity
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/chaos-lab.md
autonomous: true
requirements: [LAB-02]
must_haves:
  truths:
    - "docs/chaos-lab.md covers v4.2 profiles (dnssec, saml, kerberos), v4.3 profiles (vault, database, storage-s3), and v4.4 profiles (email, broker) — 8 new profile sub-sections"
    - "docs/chaos-lab.md intro contains a top-level pointer to expected_results_v4.md (D-12)"
    - "Section 5 Complete Port Reference is appended with rows for the missing 9 profiles' ports"
    - "Section 4 Starting Multiple Profiles documents the new ./lab.sh profiles subcommand"
  artifacts:
    - path: docs/chaos-lab.md
      provides: full prose operator guide updated for v4.2+v4.3+v4.4
      contains: "expected_results_v4.md"
  key_links:
    - from: docs/chaos-lab.md
      to: quantum-chaos-enterprise-lab/expected_results_v4.md
      via: "top-level intro pointer (D-12) + per-profile pointer to labs/<x>/expected_results.md where applicable"
      pattern: "expected_results_v4.md"
---

<objective>
Extend `docs/chaos-lab.md` (currently 425 lines, covers profiles up through v3.9-era / Phase 4) to cover the 8 missing profiles: v4.2 (`dnssec`, `saml`, `kerberos`), v4.3 (`vault`, `database`, `storage-s3`), v4.4 (`email`, `broker`). Add the D-12 top-level pointer to `expected_results_v4.md`. Append the missing port-reference rows to Section 5. Update Section 4 to mention the new `./lab.sh profiles` subcommand.

Per D-10 this is mandatory in this phase — without it, the README's "Full operator guide" link points at incomplete docs.

Purpose: LAB-02 (every shipped profile documented in operator guide).

Output: Updated `docs/chaos-lab.md` with new 3.12–3.19 sub-sections, an updated Overview pointing at the v4 oracle, and Section 5 port table extended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-chaos-lab-parity/40-CONTEXT.md
@.planning/phases/40-chaos-lab-parity/40-RESEARCH.md
@docs/chaos-lab.md
@labs/email/expected_results.md
@labs/broker/expected_results.md
@labs/storage/expected_results.md
@labs/vault/expected_results.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v4.2/v4.3/v4.4 prose sub-sections (3.12 through 3.19) + D-12 oracle pointer</name>
  <files>docs/chaos-lab.md</files>
  <read_first>
    - docs/chaos-lab.md (entire file — 425 lines; section structure §1 Overview, §2 Quick Start, §3.x per-profile sub-sections, §4 Starting Multiple Profiles, §5 Complete Port Reference, §6 Troubleshooting, §7 Historical Reference)
    - .planning/phases/40-chaos-lab-parity/40-RESEARCH.md (§"Starting-State Summary" `docs/chaos-lab.md` table — line ranges and missing sub-sections; §"Docker Compose Profile Inventory" — port + service detail truth for each new section)
    - .planning/phases/40-chaos-lab-parity/40-CONTEXT.md (D-10 — must update for v4.3+v4.4; D-12 — top-level pointer at oracle)
    - labs/email/expected_results.md, labs/broker/expected_results.md, labs/storage/expected_results.md, labs/vault/expected_results.md (per-profile detail to summarize)
  </read_first>
  <action>
    Edit `docs/chaos-lab.md` with these changes, in this order:

    **1. §1 Overview — add D-12 pointer.** Insert this paragraph immediately after the existing intro paragraph (line ~5 area):

    ```markdown
    > **For UAT-grade expected scanner findings, see `quantum-chaos-enterprise-lab/expected_results_v4.md`** — the authoritative per-profile oracle used by chaos lab UAT runs and dashboard cross-references.
    ```

    **2. §1 Overview — refresh category enumeration.** The current §1 (line area near the "Phase 4 scanner coverage (JWT, container registry, source code, cloud storage, legacy SSH, and LDAPS)" sentence) is outdated. Replace with:

    ```markdown
    The lab covers TLS / SSH / HTTP baseline (core), service inventory + cert chain scenarios (phaseA), cloud storage (cloud), identity (identity + pki), JWT misconfigurations (jwt), container registries (registry), source-code crypto anti-patterns (source), legacy storage (storage), legacy SSH (ssh-weak), LDAPS (ldaps), DNSSEC weakness (dnssec), SAML weakness (saml), Kerberos etype enumeration (kerberos), DAR — databases (database), DAR — object storage (storage-s3), DAR — Vault (vault), email transport (email), and message brokers (broker).
    ```

    **3. §3 — append 8 new sub-sections** in this order, each with: an H3 heading `### 3.NN <profile> (<era>)`, a 1-paragraph "what it ships + why it exists" intro, ports table (Markdown), the `PROFILE_ARGS="--profile <name>" ./lab.sh up` invocation, expected scanner findings summary (3-5 bullets), and a "See: `expected_results_v4.md#profile-<name>`" pointer. Use these sources:

    - **3.12 dnssec (v4.2)** — bind9-dnssec on UDP/TCP 15353, weak DNSSEC zones (RSASHA1). Findings: `RSASHA1` weak-algo CRITICAL, `unsigned zone` HIGH, `NSEC` MEDIUM. Per RESEARCH §Style A dnssec row.
    - **3.13 saml (v4.2)** — simplesamlphp on port **8080** (not 8880; compose source of truth, fix drift here too). Findings: `RSA-1024 signing cert` CRITICAL, `SHA-1 algorithm URI` HIGH. Note port shares with Keycloak's container-internal 8080 (no host conflict because Keycloak uses `expose:` not `ports:`).
    - **3.14 kerberos (v4.2)** — samba-dc on privileged 88 + 389. Findings: `weak etype: rc4-hmac` HIGH, `weak etype: aes128-cts-hmac-sha1-96` HIGH. ⚠ Port collision warning with system DNS/AD.
    - **3.15 vault (v4.3)** — vault-30 (image 1.17) on 28200, independent of legacy storage profile. Seeds transit/PKI/auth resources. Findings: HIGH `PKI/<mount>` (RSA<4096), HIGH `auth/token`, MEDIUM `auth/userpass`, MEDIUM exportable transit keys. Pointer: `labs/vault/expected_results.md`.
    - **3.16 database (v4.3)** — postgres-ssl-off on 25432 + mysql-ssl-off on 23306. Findings: HIGH `PostgreSQL/ssl-off`, HIGH `PostgreSQL/plaintext-connections-allowed`, HIGH `MySQL/ssl-off`. Pointer: `expected_results_v4.md#profile-database`.
    - **3.17 storage-s3 (v4.3)** — minio on 29000 + console on 29001. Seeds 2 buckets: encrypted-bucket (SSE-S3, no finding) and unencrypted-bucket (HIGH `S3/unencrypted`). Pointer: `labs/storage/expected_results.md`.
    - **3.18 email (v4.4)** — postfix-email + dovecot-email on host ports 30025/30465/30587/30143/30993/30110/30995. Findings: 3 HIGH (weak-cipher: ports 25/465/587), 1 MEDIUM (STARTTLS-downgrade: port 25). Caveat: Dovecot defaults to TLS 1.3 → no weak-cipher finding. Pointer: `labs/email/expected_results.md`.
    - **3.19 broker (v4.4)** — kafka-broker + rabbitmq-broker + redis-broker on host ports 29092/29093/25672/25671/26379/26380. Findings: 6 HIGH (Kafka plaintext + Kafka weak-cipher TLS + AMQP plaintext + AMQPS weak cipher + Redis plaintext + REDIS-TLS weak cipher). Pointer: `labs/broker/expected_results.md`.

    Each section should mirror the formatting of the existing 3.x sections (do not invent a new sub-section template — match the existing one).

    **4. §4 Starting Multiple Profiles — add ./lab.sh profiles example.** Append:

    ```markdown
    To list every profile defined in the compose file (live read — never out of date):

    ```bash
    ./lab.sh profiles
    ```
    ```

    **5. §5 Complete Port Reference — append rows.** Add the missing port rows to the existing port table. Per RESEARCH §"Starting-State Summary" docs/chaos-lab.md row, the missing ports are: 28200 (vault), 25432 (postgres-ssl-off), 23306 (mysql-ssl-off), 29000 (minio), 29001 (minio-console), 30025/30465/30587/30143/30993/30110/30995 (email), 29092/29093/25672/25671/26379/26380 (broker), 88/389 (kerberos), 15353 (dnssec UDP/TCP), 8080 (saml). Use the same column layout as the existing port table.

    Per D-10: the section 5 line 366 `MTLS_REQUIRED` apparent duplicate (mtls-gateway @16443 + tls-mtls-required @11443) is correct — do NOT "fix" it (RESEARCH explicitly says don't).
  </action>
  <verify>
    <automated>
      grep -q 'expected_results_v4.md' docs/chaos-lab.md &amp;&amp; \
      grep -q '### 3\.12' docs/chaos-lab.md &amp;&amp; \
      grep -q '### 3\.19' docs/chaos-lab.md &amp;&amp; \
      grep -qi 'dnssec' docs/chaos-lab.md &amp;&amp; \
      grep -qi '## .* saml\| saml ' docs/chaos-lab.md &amp;&amp; \
      grep -q 'kerberos' docs/chaos-lab.md &amp;&amp; \
      grep -q '28200' docs/chaos-lab.md &amp;&amp; \
      grep -q '25432' docs/chaos-lab.md &amp;&amp; \
      grep -q '23306' docs/chaos-lab.md &amp;&amp; \
      grep -q '29000' docs/chaos-lab.md &amp;&amp; \
      grep -q '30025' docs/chaos-lab.md &amp;&amp; \
      grep -q '29092' docs/chaos-lab.md &amp;&amp; \
      grep -q './lab.sh profiles' docs/chaos-lab.md &amp;&amp; \
      grep -q 'PostgreSQL/ssl-off' docs/chaos-lab.md &amp;&amp; \
      grep -q 'S3/unencrypted' docs/chaos-lab.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `docs/chaos-lab.md` contains 8 new H3 sub-sections numbered 3.12 through 3.19
    - D-12 pointer to `expected_results_v4.md` present in §1 Overview
    - Each new sub-section names the profile (dnssec, saml, kerberos, vault, database, storage-s3, email, broker)
    - All new ports referenced: 28200, 25432, 23306, 29000, 29001, 30025, 29092, 88, 389, 15353, 8080
    - SAML port is 8080 (not 8880)
    - §4 documents `./lab.sh profiles`
    - Literal scanner finding strings present: `PostgreSQL/ssl-off`, `S3/unencrypted`
  </acceptance_criteria>
  <done>docs/chaos-lab.md is the canonical full prose authority for every shipped profile through v4.4.</done>
</task>

</tasks>

<threat_model>
No runtime attack surface — documentation only.
</threat_model>

<verification>
- 8 new H3 sub-sections (3.12–3.19) cover dnssec/saml/kerberos/vault/database/storage-s3/email/broker
- §1 Overview points at expected_results_v4.md
- §5 port table extended with all v4.2+v4.3+v4.4 ports
- §4 mentions `./lab.sh profiles`
</verification>

<success_criteria>
- LAB-02 prose-half complete (README is the compact navigation surface; this is the deep operator guide)
- README's "Full operator guide" link now resolves to a complete document
</success_criteria>

<output>
Create `.planning/phases/40-chaos-lab-parity/40-05-SUMMARY.md` listing the new sub-sections, the D-12 pointer, and the port-table additions.
</output>
