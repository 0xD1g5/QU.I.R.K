---
phase: 40-chaos-lab-parity
plan: 04
type: execute
wave: 3
depends_on: ["40-02", "40-03"]
files_modified:
  - quantum-chaos-enterprise-lab/README.md
autonomous: true
requirements: [LAB-02]
must_haves:
  truths:
    - "README.md documents every shipped profile (v4.0 through v4.4) with port assignments and a link into the v4 oracle"
    - "Profile Summary Table has one row per named profile (18 rows) plus a row for core baseline"
    - "Each row's `Expected Findings` cell is a markdown link to the matching `## Profile: <name>` anchor in expected_results_v4.md (D-11)"
    - "Quick Start, Phase C (mTLS + step-ca), and Historical Reference sections are preserved"
  artifacts:
    - path: quantum-chaos-enterprise-lab/README.md
      provides: navigation surface for the chaos lab
      contains: "Profile Summary"
  key_links:
    - from: quantum-chaos-enterprise-lab/README.md
      to: quantum-chaos-enterprise-lab/expected_results_v4.md
      via: "per-row markdown anchor links (e.g. [Expected Findings](expected_results_v4.md#profile-storage-s3))"
      pattern: "expected_results_v4.md#profile-"
---

<objective>
Rewrite `quantum-chaos-enterprise-lab/README.md` (currently 28 lines) into the navigation surface defined by D-08 + D-09: title + intro, Quick Start, Profile Summary Table with per-row links into `expected_results_v4.md`, link block to `docs/chaos-lab.md` and the v4 oracle, Phase C helper section preserved, Historical Reference preserved.

Per D-08 the README delegates prose authority to `docs/chaos-lab.md` — README is a compact navigation surface, not a full operator guide. Per D-11 every Profile Summary row's "Expected Findings" cell is a navigable markdown link to the matching oracle anchor.

Purpose: LAB-02 (every shipped profile documented in README with ports + expected findings + setup notes).

Output: Rewritten `README.md` ordered: Title → intro → Quick Start → Profile Summary Table → Link block → Phase C → Historical Reference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-chaos-lab-parity/40-CONTEXT.md
@.planning/phases/40-chaos-lab-parity/40-RESEARCH.md
@quantum-chaos-enterprise-lab/README.md
@quantum-chaos-enterprise-lab/docker-compose.yml
@quantum-chaos-enterprise-lab/expected_results_v4.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md with navigation structure + Profile Summary Table</name>
  <files>quantum-chaos-enterprise-lab/README.md</files>
  <read_first>
    - quantum-chaos-enterprise-lab/README.md (current 28 lines — preserve Phase C and Historical Reference verbatim)
    - .planning/phases/40-chaos-lab-parity/40-CONTEXT.md (D-08, D-09, D-11 — section order, table schema, link convention)
    - .planning/phases/40-chaos-lab-parity/40-RESEARCH.md (§"Docker Compose Profile Inventory" — full 18-profile table is the source of truth for the rows; §"Starting-State Summary" README sub-section — disposition of current lines)
    - quantum-chaos-enterprise-lab/expected_results_v4.md (verify GitHub anchor format `#profile-<name>` — lowercase, `:` stripped, spaces → `-`)
  </read_first>
  <action>
    Rewrite `quantum-chaos-enterprise-lab/README.md` end-to-end with this structure:

    **§1. Title + intro (1 paragraph)**
    ```markdown
    # QU.I.R.K. Chaos Lab

    A Docker-Compose-based crypto-misconfiguration playground used by QU.I.R.K. for end-to-end scanner UAT. Eighteen named profiles plus an always-on core baseline cover TLS / SSH / JWT / container / source / cloud / DAR / messaging chaos scenarios — each with an expected-findings oracle in `expected_results_v4.md` and detailed prose in `docs/chaos-lab.md`.
    ```

    **§2. Quick Start**
    ```markdown
    ## Quick Start

    ```bash
    # Start the always-on baseline only (10 services, ~ports 443-12443 + 2222 + 5555 + 8000 + 8444)
    ./lab.sh up

    # Start every shipped profile (18 named profiles + core)
    ./lab.sh all

    # Start a specific profile
    PROFILE_ARGS="--profile identity" ./lab.sh up

    # List all available profiles (read live from docker-compose.yml)
    ./lab.sh profiles
    ```
    ```

    **§3. Profile Summary Table** — D-09 schema: `Profile | Services / What it ships | Published Ports | Expected Findings (link) | Notes`. Order rows by version era (core → v4.0 → v4.1 → v4.2 → v4.3 → v4.4) then alphabetically within era. Each "Expected Findings" cell is a markdown link `[Expected Findings](expected_results_v4.md#profile-<name>)` (lowercase, GitHub anchor convention; for `(core / always-on)` use `#profile-core--always-on`). Use the 18-row table from RESEARCH §"Docker Compose Profile Inventory" as the source.

    Example rows (apply the same pattern to all 19 rows: core + 18 named):
    ```markdown
    | Profile | Services / What it ships | Published Ports | Expected Findings | Notes |
    |---------|--------------------------|-----------------|-------------------|-------|
    | (core)   | tls-modern, tls-legacy, tls-expired, tls-selfsigned, tls-mtls-required, http-on-8444, legacy-http, ssh-alt, unknown-port, tls-slow-proxy | 443, 8443, 9443, 10443, 11443, 8444, 8000, 2222, 5555, 12443 | [Expected Findings](expected_results_v4.md#profile-core--always-on) | v4.0; always-on, no `--profile` flag needed |
    | phaseA   | tls-altport, http-redirect, unknown-port-2, postgres-plain, redis-plain, rabbitmq-mgmt, tls-missing-intermediate, tls-rsa1024, tls-sha1, ingress-sni, whoami | 15001, 18000, 5556, 15432, 16379, 15672, 13443, 14443, 15443, 24443 | [Expected Findings](expected_results_v4.md#profile-phasea) | v4.0 |
    | cloud    | localstack, localstack-tls, azurite, azurite-blob-tls, azurite-queue-tls, azurite-table-tls | 24566, 21000, 21001, 21002 | [Expected Findings](expected_results_v4.md#profile-cloud) | v4.0 |
    | identity | id-postgres, keycloak, keycloak-tls, step-ca, openldap, phpldapadmin, whoami, mtls-gateway | 15449, 19000, 13890, 18082, 16443 | [Expected Findings](expected_results_v4.md#profile-identity) | v4.0 |
    | pki      | mtls-stepca-gateway | 17443 | [Expected Findings](expected_results_v4.md#profile-pki) | v4.0; depends on `identity` profile |
    | jwt      | jwt-rs256, jwt-hs256, jwt-rsa1024, jwt-algnone | 20001, 20002, 20003, 20004 | [Expected Findings](expected_results_v4.md#profile-jwt) | v4.1 |
    | registry | registry, registry-seed | 20005 | [Expected Findings](expected_results_v4.md#profile-registry) | v4.1 |
    | source   | gitea, gitea-seed | 20006 | [Expected Findings](expected_results_v4.md#profile-source) | v4.1 |
    | storage  | localstack-kms, vault (1.15), postgres-pgcrypto | 20007, 20009, 20010 | [Expected Findings](expected_results_v4.md#profile-storage) | v4.1; **deprecated** — see `database` / `storage-s3` / `vault` |
    | ssh-weak | ssh-weak | 20022 | [Expected Findings](expected_results_v4.md#profile-ssh-weak) | v4.1 |
    | ldaps    | ldaps | 636 | [Expected Findings](expected_results_v4.md#profile-ldaps) | v4.1 |
    | dnssec   | bind9-dnssec | 15353/udp, 15353/tcp | [Expected Findings](expected_results_v4.md#profile-dnssec) | v4.2 |
    | saml     | simplesamlphp | 8080 | [Expected Findings](expected_results_v4.md#profile-saml) | v4.2; ⚠ shares 8080 with Keycloak's container-internal port |
    | kerberos | samba-dc | 88, 389 | [Expected Findings](expected_results_v4.md#profile-kerberos) | v4.2; ⚠ privileged ports — collides with system DNS/AD |
    | database | postgres-ssl-off, mysql-ssl-off | 25432, 23306 | [Expected Findings](expected_results_v4.md#profile-database) | v4.3 (DAR) |
    | storage-s3 | minio, minio-seed | 29000, 29001 | [Expected Findings](expected_results_v4.md#profile-storage-s3) | v4.3 (DAR) |
    | vault    | vault-30 (1.17), vault-30-seed | 28200 | [Expected Findings](expected_results_v4.md#profile-vault) | v4.3 (DAR); independent of legacy `storage` profile |
    | email    | postfix-email, dovecot-email | 30025, 30465, 30587, 30143, 30993, 30110, 30995 | [Expected Findings](expected_results_v4.md#profile-email) | v4.4 |
    | broker   | kafka-broker, rabbitmq-broker, redis-broker | 29092, 29093, 25672, 25671, 26379, 26380 | [Expected Findings](expected_results_v4.md#profile-broker) | v4.4 |
    ```

    **§4. Link block**
    ```markdown
    ## Documentation

    - **Full operator guide:** [`docs/chaos-lab.md`](../docs/chaos-lab.md)
    - **Expected scanner findings (UAT oracle):** [`expected_results_v4.md`](./expected_results_v4.md)
    - **Historical reference:** [`CHAOS_LAB_BUILD_AND_OPERATIONS_text_only.md`](./CHAOS_LAB_BUILD_AND_OPERATIONS_text_only.md)
    ```

    **§5. Phase C (mTLS + step-ca) helper** — preserve existing content from current README lines 19-28 verbatim.

    **§6. Historical Reference** — preserve existing pointer line from current README line 16 verbatim (will be partially redundant with §4, that's OK; D-08 §6 explicitly preserves it).

    Use the Write tool to overwrite the file. Per D-08: do NOT duplicate `docs/chaos-lab.md` content. Per D-11: every Profile row links into the oracle.
  </action>
  <verify>
    <automated>
      grep -q '^# QU.I.R.K. Chaos Lab' quantum-chaos-enterprise-lab/README.md &amp;&amp; \
      grep -q '## Quick Start' quantum-chaos-enterprise-lab/README.md &amp;&amp; \
      grep -q './lab.sh all' quantum-chaos-enterprise-lab/README.md &amp;&amp; \
      grep -q './lab.sh profiles' quantum-chaos-enterprise-lab/README.md &amp;&amp; \
      [[ "$(grep -c 'expected_results_v4.md#profile-' quantum-chaos-enterprise-lab/README.md)" -ge 18 ]] &amp;&amp; \
      grep -q 'expected_results_v4.md#profile-database' quantum-chaos-enterprise-lab/README.md &amp;&amp; \
      grep -q 'expected_results_v4.md#profile-storage-s3' quantum-chaos-enterprise-lab/README.md &amp;&amp; \
      grep -q 'expected_results_v4.md#profile-vault' quantum-chaos-enterprise-lab/README.md &amp;&amp; \
      grep -q 'expected_results_v4.md#profile-email' quantum-chaos-enterprise-lab/README.md &amp;&amp; \
      grep -q 'expected_results_v4.md#profile-broker' quantum-chaos-enterprise-lab/README.md &amp;&amp; \
      grep -q '28200' quantum-chaos-enterprise-lab/README.md &amp;&amp; \
      grep -q '25432' quantum-chaos-enterprise-lab/README.md &amp;&amp; \
      grep -q '30025' quantum-chaos-enterprise-lab/README.md &amp;&amp; \
      grep -q 'CHAOS_LAB_BUILD_AND_OPERATIONS_text_only.md' quantum-chaos-enterprise-lab/README.md
    </automated>
  </verify>
  <acceptance_criteria>
    - H1 `# QU.I.R.K. Chaos Lab` present
    - `## Quick Start` section with `./lab.sh up`, `./lab.sh all`, `./lab.sh profiles` examples
    - Profile Summary Table contains at least 18 `expected_results_v4.md#profile-` link occurrences (one per row including core)
    - All v4.3 profile anchors present: `#profile-database`, `#profile-storage-s3`, `#profile-vault`
    - All v4.4 profile anchors present: `#profile-email`, `#profile-broker`
    - All v4.3+v4.4 ports present: 28200, 25432, 23306, 29000, 29001, 30025, 29092, 25672, 26379
    - SAML row uses port 8080 (not 8880)
    - Historical reference pointer to `CHAOS_LAB_BUILD_AND_OPERATIONS_text_only.md` preserved
    - Phase C / mTLS step-ca helper content preserved (find a unique string from current README's Phase C block and grep for it)
  </acceptance_criteria>
  <done>README.md is the navigation surface defined by D-08; all 18 profiles + core appear in the Profile Summary Table with working anchor links into the v4 oracle.</done>
</task>

</tasks>

<threat_model>
No runtime attack surface — documentation only. Markdown links are inert.
</threat_model>

<verification>
- README contains all 18 named profiles + core in the summary table
- All "Expected Findings" cells link to oracle anchors in `expected_results_v4.md`
- Phase C and Historical Reference content preserved
- Quick Start documents the new `./lab.sh profiles` subcommand
</verification>

<success_criteria>
- LAB-02 satisfied: every shipped profile documented in README with ports, expected findings link, and notes
- Anchor links resolve correctly (manual click-test in plan 40-06 smoke verification)
</success_criteria>

<output>
Create `.planning/phases/40-chaos-lab-parity/40-04-SUMMARY.md` listing the new README structure, the 19-row Profile Summary Table, and the anchor link pattern used.
</output>
