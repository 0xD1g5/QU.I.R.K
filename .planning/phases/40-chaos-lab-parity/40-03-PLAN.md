---
phase: 40-chaos-lab-parity
plan: 03
type: execute
wave: 2
depends_on: ["40-02"]
files_modified:
  - quantum-chaos-enterprise-lab/expected_results_v4.md
autonomous: true
requirements: [LAB-03]
must_haves:
  truths:
    - "expected_results_v4.md contains a `## Profile: <name>` H2 section for each DAR / config-introspection / messaging profile (database, storage-s3, vault, storage [legacy hybrid], email, broker)"
    - "Each DAR section uses the D-06 category-tuned schema for its category (DB / S3 / Vault / hybrid)"
    - "Each row's `Expected condition / tag` cell uses the literal scanner output strings from RESEARCH §Style B (e.g. `protocol=POSTGRESQL, service_detail=PostgreSQL/ssl-off`) — not invented oracle aliases"
    - "The legacy `storage` profile section carries a deprecation note pointing at database/storage-s3/vault"
  artifacts:
    - path: quantum-chaos-enterprise-lab/expected_results_v4.md
      provides: DAR + messaging profile oracle sections
      contains: "## Profile: database"
  key_links:
    - from: quantum-chaos-enterprise-lab/expected_results_v4.md
      to: quirk/scanner/db_connector.py + aws_connector.py + vault_connector.py + email_scanner.py + broker_scanner.py
      via: "Literal protocol + service_detail strings copied from scanner source"
      pattern: "service_detail="
---

<objective>
Append the **DAR / config-introspection / messaging** profile sections to `expected_results_v4.md`. Five profiles total: `database` (Phase 27), `storage-s3` (Phase 28), `vault` (Phase 30), the legacy `storage` profile (v4.1 hybrid), `email` (Phase 32), `broker` (Phase 33). Each uses the D-06 category-tuned schema; each row's expected-condition cell uses the **literal scanner output strings** from RESEARCH §"Scanner Finding Tags" Style B (verbatim — not invented).

This plan runs in parallel with 40-02 (different sections of the same file; both append to disjoint regions, but to avoid concurrent-write conflict the executor should `touch` an empty `expected_results_v4.md` only if 40-02 hasn't yet, then append below 40-02's content). See files_modified note: shared file → executor must coordinate by appending sections after 40-02's listener sections, or be run after 40-02 completes if executed sequentially.

**WAVE NOTE:** Although both 40-02 and 40-03 modify the same file, they edit disjoint sections (listener vs DAR). To avoid append-order ambiguity, the orchestrator may serialize 40-03 to wave 2 if needed; planner conservative position is wave 1 with explicit append semantics, but executor may bump to wave 2 if running fully parallel. The frontmatter declares wave 1 with the understanding that 40-03 appends AFTER 40-02's content.

Purpose: LAB-03 (DAR + messaging half of the v4 oracle).

Output: 6 additional `## Profile: <name>` sections in `expected_results_v4.md`, bringing total to 19 (13 listener + 6 DAR/messaging — the count is 19 not 18 because the legacy `storage` is included alongside its v4.3 successors, per D-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-chaos-lab-parity/40-CONTEXT.md
@.planning/phases/40-chaos-lab-parity/40-RESEARCH.md
@quantum-chaos-enterprise-lab/expected_results_v3.md
@labs/email/expected_results.md
@labs/broker/expected_results.md
@labs/storage/expected_results.md
@labs/vault/expected_results.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append database, storage-s3, vault, and legacy storage sections (DAR core)</name>
  <files>quantum-chaos-enterprise-lab/expected_results_v4.md</files>
  <read_first>
    - quantum-chaos-enterprise-lab/expected_results_v4.md (file being appended to — created/populated by plan 40-02; if not yet present, create scaffold per plan 40-02 Task 1 header)
    - .planning/phases/40-chaos-lab-parity/40-RESEARCH.md (§"Scanner Finding Tags" Style B — `database`, `storage-s3`, `vault` sub-tables provide literal `protocol` + `service_detail` strings to paste verbatim)
    - .planning/phases/40-chaos-lab-parity/40-CONTEXT.md (D-06 — category-tuned schemas for each)
    - quantum-chaos-enterprise-lab/expected_results_v3.md (lines 270-293 — Phase 27 database content; reformat into D-06 schema)
    - labs/storage/expected_results.md (entire — legacy storage hybrid)
    - labs/vault/expected_results.md (entire — vault transit/PKI/auth findings)
    - quirk/scanner/db_connector.py (verify service_detail strings at L101, L137, L147, L227, L236, L245)
    - quirk/scanner/aws_connector.py (verify service_detail strings at L252-263)
    - quirk/scanner/vault_connector.py (verify service_detail strings at L155, L251, L288, L352)
  </read_first>
  <action>
    Append the following sections to `expected_results_v4.md` AFTER the `## Profile: kerberos` section (added by plan 40-02 Task 2). Use the D-06 schemas exactly as specified.

    **1. `## Profile: database`** (D-06 schema: `Port | Service | Engine | Expected protocol | TLS in Transit | Encryption-at-Rest | Expected condition / tag | Notes`)

    Intro: "PostgreSQL + MySQL with SSL explicitly disabled. Phase 27 DAR scanner targets — literal scanner output strings shown in `Expected condition / tag`."
    Invocation: `PROFILE_ARGS="--profile database" ./lab.sh up`
    Rows (paste literal `service_detail` strings verbatim from RESEARCH §Style B database table):
    ```
    | 25432 | postgres-ssl-off | PostgreSQL 15 | POSTGRESQL | OFF | none | DB_POSTGRESQL_SSL_OFF → `protocol=POSTGRESQL, service_detail=PostgreSQL/ssl-off` (HIGH) | `SHOW ssl` returns 'off'; db_connector.py L101 |
    | 25432 | postgres-ssl-off | PostgreSQL 15 | POSTGRESQL | partial | none | `protocol=POSTGRESQL, service_detail=PostgreSQL/plaintext-connections-allowed (N non-SSL)` (HIGH) | `pg_stat_ssl` shows non-SSL connections; db_connector.py L137 |
    | 23306 | mysql-ssl-off | MySQL 8 | MYSQL | OFF | none | DB_MYSQL_SSL_OFF → `protocol=MYSQL, service_detail=MySQL/ssl-off` (HIGH) | `SHOW STATUS LIKE 'Ssl_cipher'` empty; db_connector.py L227 |
    ```
    Add a "Reference" line: "Scanner: `quirk/scanner/db_connector.py`. Risk titles via risk_engine `evaluate_db_endpoints`. Oracle aliases (`DB_POSTGRESQL_SSL_OFF`, `DB_MYSQL_SSL_OFF`) used by `docs/UAT-SERIES.md`."

    **2. `## Profile: storage-s3`** (D-06 schema: `Port | Service | Provider | Expected protocol | Encryption Mode | Public Access | KMS Key | Versioning | Expected condition / tag | Notes`)

    Intro: "MinIO S3-compatible server. Seed creates `encrypted-bucket` (SSE-S3) + `unencrypted-bucket` (no SSE) for STOR-01."
    Invocation: `PROFILE_ARGS="--profile storage-s3" ./lab.sh up`
    Rows (from RESEARCH §Style B storage-s3 table + `labs/storage/expected_results.md`):
    ```
    | 29000 | minio (encrypted-bucket) | MinIO | S3 | SSE-S3 (AES256) | private | S3-managed | n/a | (no finding) → `protocol=S3, service_detail=S3/sse-s3` | aws_connector.py L257 |
    | 29000 | minio (unencrypted-bucket) | MinIO | S3 | none | private | none | n/a | `protocol=S3, service_detail=S3/unencrypted` (HIGH) | aws_connector.py L252,263,268 |
    | 29001 | minio-console | MinIO Console | HTTP | n/a | local-only | n/a | n/a | not scanned | management UI |
    ```
    Reference: "Scanner: `quirk/scanner/aws_connector.py`. Evidence keys: `dar_storage_unencrypted_count`, `dar_storage_aws_managed_count`, `dar_storage_unencrypted_ratio`. Detail in `labs/storage/expected_results.md`."

    **3. `## Profile: vault`** (D-06 schema: `Port | Service | Mount Type | Seal Type | Auto-Unseal | Expected condition / tag | Notes`)

    Intro: "Dedicated Vault dev server (vault-30, image 1.17) on 28200. Independent of the legacy `storage` profile's Vault on 20009. Seeds transit keys, PKI mount, and auth methods for VAULT-01/02/03."
    Invocation: `PROFILE_ARGS="--profile vault" ./lab.sh up`
    Rows (from RESEARCH §Style B vault table + `labs/vault/expected_results.md`):
    ```
    | 28200 | vault-30 | transit/rsa-2048-classification | shamir | no | (no finding — exportable=false) → `protocol=VAULT, service_detail=transit/rsa-2048-classification` | vault_connector.py L155 |
    | 28200 | vault-30 | transit/rsa-2048-exportable | shamir | no | `protocol=VAULT, service_detail=transit/rsa-2048-exportable` (MEDIUM) | exportable=true; vault_connector.py L158 |
    | 28200 | vault-30 | PKI/pki | shamir | no | `protocol=VAULT, service_detail=PKI/pki` (HIGH) | RSA<4096 root CA; vault_connector.py L251 |
    | 28200 | vault-30 | auth/token | shamir | no | `protocol=VAULT, service_detail=auth/token` (HIGH) | token auth enabled; vault_connector.py L352 |
    | 28200 | vault-30 | auth/userpass | shamir | no | `protocol=VAULT, service_detail=auth/userpass` (MEDIUM) | userpass auth enabled; vault_connector.py L354 |
    ```
    Reference: "Scanner: `quirk/scanner/vault_connector.py`. Evidence: `dar_vault_weak_count` (HIGH-only per Phase 30 D-11). Detail in `labs/vault/expected_results.md`."

    **4. `## Profile: storage` (legacy hybrid — DEPRECATED)** (D-06 hybrid: port table for listener bits + config-introspection findings sub-list)

    Intro: "**⚠ Deprecated** — split in v4.3 into `database` (PostgreSQL/MySQL SSL detection), `storage-s3` (MinIO/S3 buckets), and `vault` (Vault transit/PKI/auth audit). Retained for backwards compatibility with v4.1 / v4.2 UAT runs. Predates the clean per-resource split."
    Invocation: `PROFILE_ARGS="--profile storage" ./lab.sh up`
    Listener table:
    ```
    | Port | Service | Expected protocol | Expected condition / tag | Notes |
    |------|---------|-------------------|--------------------------|-------|
    | 20007 | localstack-kms | HTTPS | LocalStack KMS endpoint | KMS key seed via localstack-kms-seed |
    | 20009 | vault (legacy, image 1.15) | HTTPS | Vault dev mode | Predates the v4.3 vault profile on 28200 |
    | 20010 | postgres-pgcrypto | POSTGRESQL | pgcrypto extension probe target | DB-side encryption-at-rest demo |
    ```
    Config-introspection findings (per RESEARCH §Style A storage row):
    - LocalStack KMS keys: `RSA_2048`, `RSA_1024`, `AES_256`, `ECC_P256` finding tags (per-key)
    - postgres-pgcrypto: `pgp_sym_encrypt (weak passphrase)` finding tag
    Reference: "See `labs/storage/expected_results.md` for full per-resource detail. v4.3 successors give cleaner per-category coverage."
  </action>
  <verify>
    <automated>
      grep -q '^## Profile: database' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q '^## Profile: storage-s3' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q '^## Profile: vault' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q '^## Profile: storage' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q 'PostgreSQL/ssl-off' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q 'MySQL/ssl-off' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q 'S3/unencrypted' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q 'transit/rsa-2048-exportable' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q 'PKI/pki' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q 'auth/token' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q 'Deprecated' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q '28200' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q '25432' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q '29000' quantum-chaos-enterprise-lab/expected_results_v4.md
    </automated>
  </verify>
  <acceptance_criteria>
    - 4 new H2 sections present: `database`, `storage-s3`, `vault`, `storage`
    - Literal scanner strings appear verbatim: `PostgreSQL/ssl-off`, `MySQL/ssl-off`, `S3/unencrypted`, `S3/sse-s3`, `transit/rsa-2048-exportable`, `PKI/pki`, `auth/token`, `auth/userpass`
    - Ports present: 25432, 23306, 29000, 29001, 28200, 20007, 20009, 20010
    - `storage` section contains "Deprecated" annotation pointing at database/storage-s3/vault
  </acceptance_criteria>
  <done>4 DAR / hybrid sections complete with literal scanner output strings.</done>
</task>

<task type="auto">
  <name>Task 2: Append email and broker sections (v4.4 messaging)</name>
  <files>quantum-chaos-enterprise-lab/expected_results_v4.md</files>
  <read_first>
    - quantum-chaos-enterprise-lab/expected_results_v4.md (file being appended to)
    - .planning/phases/40-chaos-lab-parity/40-RESEARCH.md (§"Scanner Finding Tags" Style B — `email` and `broker` sub-tables; risk-engine titles)
    - labs/email/expected_results.md (entire — port map, expected counts, TLS 1.3 caveat)
    - labs/broker/expected_results.md (entire — Kafka/RabbitMQ/Redis port map, expected 6 HIGH findings)
    - quirk/scanner/email_scanner.py (verify protocol labels at L499)
    - quirk/scanner/broker_scanner.py (verify service_detail strings at L390, L401, L472, L482, L674, L683)
  </read_first>
  <action>
    Append two more sections to `expected_results_v4.md` after the `## Profile: storage` section. Use the D-05 listener schema (these are TLS endpoints, not config-introspection): `Port | Service | Expected protocol | Expected condition / tag | Notes`.

    **5. `## Profile: email`**

    Intro: "Postfix (SMTP / SMTPS / Submission) + Dovecot (IMAP / IMAPS / POP3 / POP3S) with weak RSA-2048 TLS, non-PFS suites, TLS 1.2 floor on Postfix. Dovecot defaults to TLS 1.3 → no weak-cipher finding without explicit pin (caveat). Expected total: 3 HIGH (weak-cipher: 25/465/587) + 1 MEDIUM (STARTTLS-downgrade: 25)."
    Invocation: `PROFILE_ARGS="--profile email" ./lab.sh up`
    Rows (host port mapping from RESEARCH §"Scanner Finding Tags" Style B email table + `labs/email/expected_results.md`):
    ```
    | 30025 | postfix-email (smtp) | SMTP-STARTTLS | `protocol=SMTP-STARTTLS, service_detail=SMTP-STARTTLS:25`; risk: "STARTTLS downgrade risk on SMTP" (MEDIUM, EMAIL-08) + "Weak cipher suite on email TLS endpoint" (HIGH, EMAIL-09) | Container port 25 |
    | 30465 | postfix-email (smtps) | SMTPS | `protocol=SMTPS, service_detail=SMTPS:465`; risk: "Weak cipher suite on email TLS endpoint" (HIGH, EMAIL-09) | Container port 465 |
    | 30587 | postfix-email (submission) | SMTP-STARTTLS | `protocol=SMTP-STARTTLS, service_detail=SMTP-STARTTLS:587`; risk: "Weak cipher suite on email TLS endpoint" (HIGH, EMAIL-09) | Container port 587 |
    | 30143 | dovecot-email (imap) | IMAP-STARTTLS | `protocol=IMAP-STARTTLS, service_detail=IMAP-STARTTLS:143`; no weak-cipher finding by default | Container port 143; TLS 1.3 default |
    | 30993 | dovecot-email (imaps) | IMAPS | `protocol=IMAPS, service_detail=IMAPS:993`; no weak-cipher finding by default | Container port 993; TLS 1.3 default |
    | 30110 | dovecot-email (pop3) | POP3-STARTTLS | `protocol=POP3-STARTTLS, service_detail=POP3-STARTTLS:110`; no weak-cipher finding by default | Container port 110; TLS 1.3 default |
    | 30995 | dovecot-email (pop3s) | POP3S | `protocol=POP3S, service_detail=POP3S:995`; no weak-cipher finding by default | Container port 995; TLS 1.3 default |
    ```
    Reference: "Scanner: `quirk/scanner/email_scanner.py`. Risk titles from `risk_engine.evaluate_email_endpoints`. Detail + TLS 1.3 caveat in `labs/email/expected_results.md`."

    **6. `## Profile: broker`**

    Intro: "Kafka + RabbitMQ + Redis with intentional plaintext + weak-cipher TLS listeners. Expected total: 6 HIGH findings."
    Invocation: `PROFILE_ARGS="--profile broker" ./lab.sh up`
    Rows (from RESEARCH §"Scanner Finding Tags" Style B broker table):
    ```
    | 29092 | kafka-broker (plaintext) | KAFKA-PLAIN | `protocol=KAFKA-PLAIN, service_detail=KAFKA-PLAIN:29092`; risk: "Kafka plaintext listener detected" (HIGH, KAFKA-02) | broker_scanner.py L390 |
    | 29093 | kafka-broker (TLS) | KAFKA-TLS | `protocol=KAFKA-TLS, service_detail=KAFKA-TLS:29093`; risk: "Weak cipher suite on broker TLS endpoint" (HIGH, KAFKA-01) | broker_scanner.py L401 |
    | 25672 | rabbitmq-broker (plaintext) | AMQP-PLAIN | `protocol=AMQP-PLAIN, service_detail=AMQP-PLAIN:25672`; risk: "AMQP plaintext listener detected" (HIGH, RABBIT-02) | broker_scanner.py L472 |
    | 25671 | rabbitmq-broker (TLS) | AMQPS | `protocol=AMQPS, service_detail=AMQPS:25671`; risk: "Weak cipher suite on broker TLS endpoint" (HIGH, RABBIT-01) | broker_scanner.py L482 |
    | 26379 | redis-broker (plaintext) | REDIS-PLAIN | `protocol=REDIS-PLAIN, service_detail=REDIS-PLAIN:26379`; risk: "Redis plaintext listener (no authentication)" (HIGH, REDIS-02) | broker_scanner.py L674 |
    | 26380 | redis-broker (TLS) | REDIS-TLS | `protocol=REDIS-TLS, service_detail=REDIS-TLS:26380`; risk: "Weak cipher suite on broker TLS endpoint" (HIGH, REDIS-01) | broker_scanner.py L683 |
    ```
    Reference: "Scanner: `quirk/scanner/broker_scanner.py`. Risk titles from `risk_engine.evaluate_broker_endpoints`. Detail in `labs/broker/expected_results.md`. Expected total: 6 HIGH."
  </action>
  <verify>
    <automated>
      grep -q '^## Profile: email' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q '^## Profile: broker' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q 'STARTTLS downgrade risk on SMTP' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q 'Weak cipher suite on email TLS endpoint' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q 'Kafka plaintext listener detected' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q 'AMQP plaintext listener detected' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q 'Redis plaintext listener' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q 'Weak cipher suite on broker TLS endpoint' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q 'KAFKA-PLAIN:29092' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q 'AMQPS:25671' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q 'REDIS-TLS:26380' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q '30025' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      grep -q '30995' quantum-chaos-enterprise-lab/expected_results_v4.md &amp;&amp; \
      [[ "$(grep -c '^## Profile:' quantum-chaos-enterprise-lab/expected_results_v4.md)" -ge 19 ]]
    </automated>
  </verify>
  <acceptance_criteria>
    - 2 new H2 sections present: `email`, `broker`
    - 4 risk-engine titles present verbatim: "STARTTLS downgrade risk on SMTP", "Weak cipher suite on email TLS endpoint", "Kafka plaintext listener detected", "AMQP plaintext listener detected", "Redis plaintext listener", "Weak cipher suite on broker TLS endpoint"
    - All 7 email host ports present (30025/30465/30587/30143/30993/30110/30995)
    - All 6 broker host ports present (29092/29093/25672/25671/26379/26380)
    - Total `## Profile:` H2 sections in v4 oracle is at least 19 (13 listener + 6 DAR/messaging)
  </acceptance_criteria>
  <done>v4 oracle complete: 19 profile sections covering every shipped chaos lab profile through v4.4.</done>
</task>

</tasks>

<threat_model>
No runtime attack surface — documentation only.
</threat_model>

<verification>
- 6 additional `## Profile:` sections appended (database, storage-s3, vault, storage [legacy], email, broker)
- Literal scanner output strings used (no invented oracle aliases for v4.3+v4.4)
- v4 oracle now totals 19 profile sections covering all 18 named profiles + core
</verification>

<success_criteria>
- LAB-03 fully satisfied (combined with plan 40-02)
- Anchor stability preserved for plan 40-04 README cross-links
- Scanner regression tests can match `Expected condition / tag` cells without translation
</success_criteria>

<output>
Create `.planning/phases/40-chaos-lab-parity/40-03-SUMMARY.md` listing the 6 sections added, the literal scanner strings used, and the legacy `storage` deprecation annotation.
</output>
