---
phase: 88-scoring-residuals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_scoring_orthogonal_contract.py
  - tests/test_score_render_parity.py
  - tests/test_score_transparency.py
  - quirk/reports/writer.py
  - quirk/reports/executive.py
  - quirk/reports/html_renderer.py
  - quirk/reports/templates/report.html.j2
autonomous: true
requirements:
  - EVIDENCE-TALLY-01
  - RENDER-CLI-01
  - RENDER-PDF-01
  - SCORE-XPARENCY-01
must_haves:
  truths:
    - "A parametrized test forward-locks the orthogonal subscore contract: a finding in one category leaves all other subscores at 25 (D-01, D-02 / EVIDENCE-TALLY-01)"
    - "A data-layer parity test proves CLI markdown, executive markdown, and HTML/PDF surfaces receive the same overall + six subscore integers as the canonical engine, anchored to the Phase 86 0-100 contract (D-04 / RENDER-CLI-01, RENDER-PDF-01)"
    - "CLI scorecard markdown, executive markdown, and the HTML/PDF report each display all six subscores as Label: N/25 plus the sum->÷1.5->overall rollup math (D-07 / SCORE-XPARENCY-01)"
    - "EVIDENCE-TALLY-01 is closed with an inline won't-fix rationale in the orthogonality test module (D-01)"
  artifacts:
    - path: "tests/test_scoring_orthogonal_contract.py"
      provides: "Forward-locking orthogonality invariant (D-02)"
      contains: "compute_readiness_score"
    - path: "tests/test_score_render_parity.py"
      provides: "Data-layer parity gate (D-04)"
      contains: "compute_readiness_score"
    - path: "tests/test_score_transparency.py"
      provides: "Subscore decomposition render gate (D-07)"
      contains: "/25"
    - path: "quirk/reports/writer.py"
      provides: "Scorecard markdown decomposition block"
      contains: "Score Decomposition"
    - path: "quirk/reports/executive.py"
      provides: "Executive markdown decomposition section"
      contains: "Score Decomposition"
    - path: "quirk/reports/templates/report.html.j2"
      provides: "HTML/PDF subscore table"
      contains: "Score Decomposition"
  key_links:
    - from: "quirk/reports/html_renderer.py"
      to: "report.html.j2"
      via: "subscores template variable"
      pattern: "subscores=score.get"
    - from: "quirk/reports/writer.py"
      to: "score dict"
      via: "wrapped {total, subscores} access"
      pattern: "score.get..subscores"
---

<objective>
Lock the orthogonal subscore contract, prove cross-surface score parity, and bring the three report surfaces (CLI markdown, executive markdown, HTML/PDF) to subscore-transparency parity with the dashboard.

Purpose: Resolves EVIDENCE-TALLY-01 honestly (won't-fix at subscore level, per D-01) and closes the BACK-89 optics tension via transparency rather than a math change (D-08). Verifies RENDER-CLI-01/RENDER-PDF-01 at the data layer per D-04 (verify-first; fix only on divergence via a single shared helper).
Output: 3 new forward-locking test modules + subscore decomposition rendering on 3 report surfaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-scoring-residuals/88-CONTEXT.md
@.planning/phases/88-scoring-residuals/88-RESEARCH.md
@.planning/phases/88-scoring-residuals/88-PATTERNS.md
@.planning/phases/88-scoring-residuals/88-VALIDATION.md

<interfaces>
<!-- Score dict key disambiguation — CRITICAL (PATTERNS.md "Score dict key disambiguation") -->
From quirk/intelligence/scoring.py:
  compute_readiness_score(evidence: dict) -> dict
  Returns RAW dict with key "score" (overall 0-100) and "subscores" (dict of six int 0-25).
  Subscore keys: hygiene, modern_tls, identity_trust, agility_signals, data_at_rest, data_in_motion.
  Overall = int(round(sum(six subscores) / 1.5)). Subscores already int — NEVER re-round.

From quirk/reports/writer.py (lines 166-170):
  WRAPPED dict: {"total": score_raw["score"], "subscores": score_raw["subscores"], "drivers": [...]}.
  _scorecard_markdown and html_renderer.py receive the WRAPPED dict — use score.get("total") / score.get("subscores").

From quirk/reports/executive.py:
  build_exec_markdown calls compute_readiness_score independently — uses score_raw["score"] / score_raw["subscores"].

Canonical subscore label mapping (mirror dashboard executive.tsx):
  [("hygiene","Hygiene"),("modern_tls","Modern TLS"),("identity_trust","Identity"),
   ("agility_signals","Agility"),("data_at_rest","Data at Rest"),("data_in_motion","Data in Motion")]

Evidence trigger keys per category (PATTERNS.md "Evidence key catalogue"):
  hygiene=plaintext_http_count, modern_tls=finding_severity_counts, identity_trust=identity_weak_etype_count,
  agility_signals=cert_key_type_counts, data_at_rest=dar_db_plaintext_count, data_in_motion=motion_email_plaintext_num.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Forward-locking orthogonality + render-parity test gates (Wave 0)</name>
  <files>tests/test_scoring_orthogonal_contract.py, tests/test_score_render_parity.py</files>
  <read_first>
    - tests/test_score_weights_invariant.py (forward-locking assertion style)
    - tests/test_xml_safe.py (parametrized gate + inline-rationale docstring style)
    - quirk/intelligence/scoring.py lines 103-111, 182-258 (subscore + overall rounding sites)
    - quirk/intelligence/evidence.py (build_evidence_summary signature)
  </read_first>
  <behavior>
    - Orthogonality: for each of the six categories, evidence with findings ONLY in that category yields that subscore < 25 while the other five subscores == 25 (parametrized over all six per the PATTERNS Evidence key catalogue).
    - Parity: build_evidence_summary(FIXTURE_ENDPOINTS, FIXTURE_FINDINGS) -> compute_readiness_score yields one canonical dict; the writer.py-style wrap {"total": canonical["score"], "subscores": canonical["subscores"]} equals canonical, and a second independent compute_readiness_score call (dashboard path) returns identical "score" and "subscores". Anchor: assert canonical["score"] is an int in range 0..100 (Phase 86 contract).
  </behavior>
  <action>Create tests/test_scoring_orthogonal_contract.py implementing test_subscore_orthogonality, parametrized over the six (category, trigger_key, trigger_value, clean_categories) tuples from PATTERNS.md "Evidence key catalogue" (data_in_motion case must include totals.endpoints >= 1). Module docstring MUST contain the D-01/EVIDENCE-TALLY-01 won't-fix resolution wording from RESEARCH.md "Resolution wording" verbatim-in-spirit (cite quirk/intelligence/scoring.py, the 25+category-local-penalties model, and the explicit rejection of cross-category penalties per D-01). Assertion failure messages name the violating category and state "Orthogonality contract violated". Create tests/test_score_render_parity.py implementing test_render_parity_all_surfaces per PATTERNS.md "Data-layer parity gate pattern": import build_evidence_summary + compute_readiness_score, define FIXTURE_ENDPOINTS/FIXTURE_FINDINGS (minimal realistic, may be empty for baseline), compute canonical once, assert the writer-wrap and dashboard-recall paths agree on "score"/"subscores" and that the overall is an int 0..100 anchored to the Phase 86 contract (D-04). Do NOT call round() on any subscore — they are already int. These are pure unit tests: no DB, no endpoints object beyond fixtures.</action>
  <verify>
    <automated>QUIRK_DB_PATH=./quirk.db python -m pytest tests/test_scoring_orthogonal_contract.py tests/test_score_render_parity.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_scoring_orthogonal_contract.py exists; test_subscore_orthogonality parametrized over all six categories; passes.
    - Module docstring contains "won't-fix" rationale citing scoring.py and D-01.
    - tests/test_score_render_parity.py exists; test_render_parity_all_surfaces passes asserting cross-surface equality + 0..100 anchor.
    - `QUIRK_DB_PATH=./quirk.db python -m pytest tests/test_scoring_orthogonal_contract.py tests/test_score_render_parity.py -x -q` exits 0.
  </acceptance_criteria>
  <done>Both forward-locking gates exist and pass; EVIDENCE-TALLY-01 resolution rationale committed inline; RENDER-CLI-01/RENDER-PDF-01 data-layer parity locked (no divergence found = verified-no-bug, no shared-helper change needed unless a divergence surfaces).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Subscore decomposition on CLI markdown, executive markdown, and HTML/PDF report</name>
  <files>quirk/reports/writer.py, quirk/reports/executive.py, quirk/reports/html_renderer.py, quirk/reports/templates/report.html.j2, tests/test_score_transparency.py</files>
  <read_first>
    - quirk/reports/writer.py lines 91-109, 157-204 (_scorecard_markdown, wrapped score dict)
    - quirk/reports/executive.py lines 111-184 (build_exec_markdown, score_raw access)
    - quirk/reports/html_renderer.py lines 145-207 (render_html_report template-context dict)
    - quirk/reports/templates/report.html.j2 lines 148-154 (score-card block)
    - src/dashboard/src/pages/executive.tsx lines 251-256 (canonical /25 label mapping)
  </read_first>
  <behavior>
    - _scorecard_markdown(cfg, score, conf, drivers, roadmap) output contains a "## Score Decomposition" block with all six "Label" rows, the string "/25", and a rollup line containing "÷ 1.5" and the overall "/ 100".
    - build_exec_markdown output contains a "Score Decomposition" section with the six labels, "/25", and the "÷ 1.5" rollup.
    - render_html_report passes subscores=score.get("subscores", {}); rendered HTML contains "Score Decomposition" and the six labels.
  </behavior>
  <action>In quirk/reports/writer.py _scorecard_markdown (after the "## Score" line ~109), append a "## Score Decomposition" markdown table per PATTERNS.md "writer.py addition": iterate the canonical _SUBSCORE_LABELS list, render `| {label} | {subscores.get(key,'—')} | /25 |`, then a rollup line `**Rollup:** {raw_sum} ÷ 1.5 = **{score.get('total')} / 100**`. Access via score.get("subscores") (WRAPPED dict). Labels are hardcoded literals — do NOT wrap them in md_cell and do NOT sanitize integer values. In quirk/reports/executive.py build_exec_markdown (after the Score Drivers section, before "## Confidence & Coverage"), append an analogous "### Score Decomposition" section using score_raw.get("subscores") and score_raw["score"]. In quirk/reports/html_renderer.py render_html_report, add subscores=score.get("subscores", {}) to the template.render(...) kwargs (score is the WRAPPED dict — Pitfall 6). In quirk/reports/templates/report.html.j2 after the score-card block (~line 154), add the {% if subscores %} decomposition table from PATTERNS.md "report.html.j2" with the six hardcoded label rows, /25 budgets, and the `{{ subscores.values() | sum }} ÷ 1.5 = {{ total_score }} / 100` rollup. Define _SUBSCORE_LABELS once per Python module using the canonical mapping. Create tests/test_score_transparency.py asserting _scorecard_markdown and build_exec_markdown outputs contain "/25" and ("÷1.5" or "/ 1.5"), constructing a mock score/score_raw dict with the six subscore keys per PATTERNS.md "test_score_transparency.py" pattern.</action>
  <verify>
    <automated>QUIRK_DB_PATH=./quirk.db python -m pytest tests/test_score_transparency.py -x -q && python -m compileall quirk/reports/writer.py quirk/reports/executive.py quirk/reports/html_renderer.py</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_score_transparency.py passes: both markdown surfaces contain "/25" and a "÷ 1.5"/"/ 1.5" rollup.
    - `grep -v '^#' quirk/reports/templates/report.html.j2 | grep -c "Score Decomposition"` >= 1.
    - `grep -c "subscores=score.get" quirk/reports/html_renderer.py` >= 1.
    - `python -m compileall quirk/reports/writer.py quirk/reports/executive.py quirk/reports/html_renderer.py` exits 0.
    - No new failures vs the 39-failure baseline: `QUIRK_DB_PATH=./quirk.db python -m pytest tests/ -q` shows no NEW failures.
  </acceptance_criteria>
  <done>All three report surfaces render the six subscores as Label: N/25 plus the sum->÷1.5->overall rollup; dashboard already at parity. SCORE-XPARENCY-01 satisfied. Labels are hardcoded literals (no injection vector); integer subscore values need no escaping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanner-derived strings → report markdown/HTML | Untrusted scanner output flows into report surfaces; Phase 78 sanitize chokepoint governs scanner-derived cells |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-01 | Tampering | report.html.j2 subscore table | accept | Subscore values are Python `int` from scoring.py (not scanner-derived); category labels are hardcoded Jinja2/Python literals. No `\| sanitize` / `md_cell` needed; no scanner input reaches these cells. |
| T-88-02 | Tampering | writer.py / executive.py decomposition block | mitigate | Labels and budget strings are hardcoded literals; only score.get("total")/subscores integers are interpolated. Do NOT route scanner strings through the new block. |
| T-88-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH §Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `QUIRK_DB_PATH=./quirk.db python -m pytest tests/test_scoring_orthogonal_contract.py tests/test_score_render_parity.py tests/test_score_transparency.py -x -q` green.
- `QUIRK_DB_PATH=./quirk.db python -m pytest tests/ -q` shows no NEW failures vs the 39-failure pre-phase baseline (VALIDATION.md caveat).
- `python -m compileall quirk/reports/` exits 0.
</verification>

<success_criteria>
- EVIDENCE-TALLY-01 closed won't-fix with inline rationale + parametrized orthogonality lock (D-01, D-02).
- RENDER-CLI-01 / RENDER-PDF-01 verified at the data layer; parity gate green; fix applied only if a divergence surfaced (none expected per RESEARCH).
- SCORE-XPARENCY-01 satisfied: CLI markdown, executive markdown, and HTML/PDF all show six subscores N/25 + rollup math (dashboard already at parity).
</success_criteria>

<output>
Create `.planning/phases/88-scoring-residuals/88-01-SUMMARY.md` when done.
</output>
