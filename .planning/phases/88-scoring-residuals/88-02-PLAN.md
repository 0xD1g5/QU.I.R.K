---
phase: 88-scoring-residuals
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/cbom/classifier.py
  - quirk/cbom/builder.py
  - tests/test_cbom_motion_endpoints.py
  - tests/test_cbom_zero_algo_profiles.py
autonomous: true
requirements:
  - SCORE-CBOM-01
must_haves:
  truths:
    - "Each of the five formerly-zero-algo profiles (database, registry, source, ssh-weak, storage-s3) either emits real algorithm components OR carries an affirmative quirk:coverage-note property — never a vacuous zero (D-05, D-06 / SCORE-CBOM-01, closes Phase 42 OBS-1)"
    - "The ssh-weak profile emits real weak SSH algorithm components (diffie-hellman-group1-sha1, ssh-dss, hmac-md5) because the fixture now reflects real ssh-audit output and the classifier resolves them to non-UNKNOWN primitives (D-05)"
    - "Genuinely-plaintext profiles (database ssl-off, storage-s3 unencrypted) and non-algorithm observations (source non-algo rules) emit affirmative no-crypto markers with hardcoded literal values (D-06)"
    - "The classifier coverage gate sees no UNKNOWN classifications for in-scope algorithms (no spurious raw rule-ID / library-name registrations)"
  artifacts:
    - path: "quirk/cbom/classifier.py"
      provides: "Weak SSH algorithm entries in _ALGORITHM_TABLE"
      contains: "diffie-hellman-group1-sha1"
    - path: "quirk/cbom/builder.py"
      provides: "D-06 coverage-note emission in Pass-1 + ssl-off/unencrypted/SOURCE markers"
      contains: "quirk:coverage-note"
    - path: "tests/test_cbom_zero_algo_profiles.py"
      provides: "Per-profile components-or-marker gate (D-05/D-06)"
      contains: "quirk:coverage-note"
  key_links:
    - from: "quirk/cbom/builder.py"
      to: "bom.metadata.component.properties"
      via: "Property(name=quirk:coverage-note)"
      pattern: "quirk:coverage-note"
    - from: "quirk/cbom/builder.py SSH branch"
      to: "quirk/cbom/classifier.py _ALGORITHM_TABLE"
      via: "_register_algorithm of ssh-audit algorithms"
      pattern: "ssh-dss|hmac-md5|group1-sha1"
---

<objective>
Make the five zero-algo CBOM profiles emit real algorithm components where crypto is observed, and affirmative no-crypto markers where the endpoint is genuinely plaintext — closing Phase 42 OBS-1 without any new scanning (D-05/D-06).

Purpose: SCORE-CBOM-01. Surfaces crypto the scanners ALREADY observe but Pass-1 currently drops (ssh-weak), and replaces vacuous zeros with affirmative "we looked, it's plaintext" markers (database ssl-off, storage-s3 unencrypted, source non-algo rules).
Output: classifier table entries + builder Pass-1 emission/marker logic + a realistic ssh-weak fixture + a per-profile coverage gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-scoring-residuals/88-CONTEXT.md
@.planning/phases/88-scoring-residuals/88-RESEARCH.md
@.planning/phases/88-scoring-residuals/88-PATTERNS.md
@.planning/phases/88-scoring-residuals/88-VALIDATION.md

<interfaces>
From quirk/cbom/builder.py:
  build_cbom(endpoints: list[CryptoEndpoint]) -> Bom
  Pass-1 loop ~lines 400-545 with per-protocol branches: SSH (~401), CONTAINER (~422), SOURCE (~426),
    MYSQL (~497), POSTGRESQL/RDS (~509), S3/AZURE_BLOB (~514).
  _register_algorithm(name, algo_registry, key_size=...) registers an algorithm component.
  Bom is assembled at ~line 712 with metadata=BomMetaData(component=root_component) (~line 703-706).
  root_component.properties is the attach point for D-06 coverage notes (PATTERNS Option A, verified available).
  Property already imported: from cyclonedx.model import Property (line 19).

From quirk/cbom/classifier.py:
  classify_algorithm(name) -> (CryptoPrimitive, nist_level, security_bits)
  _ALGORITHM_TABLE keyed by raw algorithm string; UNKNOWN (except name=="none") fails the coverage gate.
  Existing SSH entries: diffie-hellman-group14-sha1, ssh-rsa, ssh-ed25519, hmac-sha2-256, hmac-sha1.

From tests/_cbom_profiles.py:
  PROFILE_ENDPOINTS maps profile name -> builder fn. ssh-weak builder is _build_ssh_weak_lab_endpoints
  in tests/test_cbom_motion_endpoints.py (~lines 608-622), currently ssh_audit_json=None.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add weak SSH algorithm entries to the classifier table</name>
  <files>quirk/cbom/classifier.py</files>
  <read_first>
    - quirk/cbom/classifier.py lines 57-91 (existing SSH KEX / host-key / MAC _ALGORITHM_TABLE sections)
    - tests/test_cbom_classifier_coverage.py lines 62-80 (UNKNOWN gate logic)
  </read_first>
  <behavior>
    - classify_algorithm("diffie-hellman-group1-sha1") returns a non-UNKNOWN KEY_AGREE primitive.
    - classify_algorithm("ssh-dss") returns a non-UNKNOWN SIGNATURE primitive.
    - classify_algorithm("hmac-md5"), classify_algorithm("hmac-md5-96"), classify_algorithm("hmac-sha1-96") return non-UNKNOWN HASH primitives.
  </behavior>
  <action>In quirk/cbom/classifier.py _ALGORITHM_TABLE, add the weak SSH entries from PATTERNS.md "classifier.py additions" alongside the existing SSH sections: KEX "diffie-hellman-group1-sha1" as (CryptoPrimitive.KEY_AGREE, 0, 80); host-key "ssh-dss" as (CryptoPrimitive.SIGNATURE, 0, 80); MAC "hmac-md5" (HASH, 0, 64), "hmac-md5-96" (HASH, 0, 64), "hmac-sha1-96" (HASH, 0, 80). Place each next to its existing same-category SSH entries and keep the inline "ssh-weak profile — Phase 88 SCORE-CBOM-01" comment. Do not alter SCORE_WEIGHTS or any scoring file (Pitfall 5). Match the existing tuple shape exactly (primitive, nist_level, security_bits).</action>
  <verify>
    <automated>QUIRK_DB_PATH=./quirk.db python -m pytest tests/test_cbom_classifier_coverage.py -x -q && python -c "from quirk.cbom.classifier import classify_algorithm as c; from cyclonedx.model.crypto import CryptoPrimitive as P; assert c('ssh-dss')[0]!=P.UNKNOWN and c('hmac-md5')[0]!=P.UNKNOWN and c('diffie-hellman-group1-sha1')[0]!=P.UNKNOWN"</automated>
  </verify>
  <acceptance_criteria>
    - classify_algorithm returns non-UNKNOWN for all five new algorithm names (one-liner assertion above exits 0).
    - `grep -c "diffie-hellman-group1-sha1" quirk/cbom/classifier.py` >= 1.
    - tests/test_cbom_classifier_coverage.py still green.
    - SCORE_WEIGHTS untouched: `QUIRK_DB_PATH=./quirk.db python -m pytest tests/test_score_weights_invariant.py -q` green.
  </acceptance_criteria>
  <done>The five weak SSH algorithm names resolve to non-UNKNOWN primitives, unblocking the coverage gate once they appear in the ssh-weak fixture (Task 2).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Builder Pass-1 emission + D-06 coverage markers + realistic ssh-weak fixture + coverage gate</name>
  <files>quirk/cbom/builder.py, tests/test_cbom_motion_endpoints.py, tests/test_cbom_zero_algo_profiles.py</files>
  <read_first>
    - quirk/cbom/builder.py lines 397-545 (Pass-1 per-protocol branches) and 700-716 (Bom/BomMetaData/root_component assembly)
    - tests/test_cbom_motion_endpoints.py lines 608-622 (_build_ssh_weak_lab_endpoints fixture)
    - tests/test_cbom_classifier_coverage.py (full file — gate + PROFILE_ENDPOINTS usage)
    - tests/_cbom_profiles.py (PROFILE_ENDPOINTS mapping)
  </read_first>
  <behavior>
    - build_cbom for ssh-weak emits algorithm components named diffie-hellman-group1-sha1, ssh-dss, and hmac-md5 (real ssh-audit observation, D-05).
    - build_cbom for database ssl-off and storage-s3 unencrypted emits zero algorithm components but attaches a quirk:coverage-note Property on the Bom root component (D-06).
    - build_cbom for a SOURCE endpoint whose rule ID encodes no specific algorithm attaches a quirk:coverage-note instead of registering the raw rule ID as an algorithm (no UNKNOWN).
    - For all five profiles: algorithm components present OR a quirk:coverage-note present — never both absent.
  </behavior>
  <action>In quirk/cbom/builder.py: initialize a coverage_notes list[str] at the top of build_cbom before the Pass-1 loop. Add a module-level helper _emit_coverage_note(root_component, note) per PATTERNS.md that appends a Property(name="quirk:coverage-note", value=note) to root_component.properties (note values are HARDCODED string literals — never scanner-derived, per the threat model). In the MYSQL branch, when cipher_name.upper()=="SSL-OFF" append the plaintext note; in the POSTGRESQL/RDS branch, when ep.cert_pubkey_alg is absent append the plaintext note; in the S3/AZURE_BLOB branch, when the detail is unencrypted (no encrypted posture matched) append the unencrypted note; in the SOURCE branch, register only when _extract_algo_from_rule_id returns a hint, else append the "crypto library/pattern observed; algorithm-level detail not captured" note (do NOT register the raw rule ID). Use the exact literal note strings from PATTERNS.md "builder.py" section. After the Pass-1 loop (before Pass-2 / Bom assembly), call _emit_coverage_note(root_component, n) for each accumulated note so they land on bom.metadata.component.properties. In tests/test_cbom_motion_endpoints.py _build_ssh_weak_lab_endpoints, replace ssh_audit_json=None with a realistic JSON string containing kex=diffie-hellman-group1-sha1, key=ssh-dss, enc (an existing-classified weak cipher), and mac=hmac-md5 so the SSH branch registers them (Pitfall 1: fix the fixture to reflect real scanner output, not just to pass). Create tests/test_cbom_zero_algo_profiles.py implementing the PATTERNS.md "test_cbom_zero_algo_profiles.py" parametrized gate over the five profiles asserting algorithm components OR a quirk:coverage-note property exists for each. Do not modify scoring files (Pitfall 5).</action>
  <verify>
    <automated>QUIRK_DB_PATH=./quirk.db python -m pytest tests/test_cbom_zero_algo_profiles.py tests/test_cbom_classifier_coverage.py tests/test_cbom_motion_endpoints.py -x -q && python -m compileall quirk/cbom/builder.py</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_cbom_zero_algo_profiles.py parametrized over all five profiles passes (each has components or a coverage-note).
    - `grep -c "quirk:coverage-note" quirk/cbom/builder.py` >= 1.
    - ssh-weak emits real algorithm components: classifier coverage gate green with no UNKNOWN for the ssh-weak profile.
    - `python -m compileall quirk/cbom/builder.py` exits 0.
    - No NEW failures vs the 39-failure baseline: `QUIRK_DB_PATH=./quirk.db python -m pytest tests/ -q`.
  </acceptance_criteria>
  <done>All five profiles satisfy SCORE-CBOM-01: ssh-weak emits real components (D-05); database/storage-s3/source emit affirmative hardcoded no-crypto markers (D-06). Phase 42 OBS-1 closed. CLAUDE.md note: no chaos-lab compose/profile/port change here, so lab.sh / expected_results updates are not triggered by this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanner-derived strings → CBOM Property values | Untrusted scanner output (service_detail, rule IDs, library names) flows into the CBOM builder |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-03 | Tampering | quirk:coverage-note Property values | mitigate | Every coverage-note value is a HARDCODED string literal (PATTERNS finding); scanner-derived fields (ep.cipher_suite, service_detail) are NEVER interpolated into a note value. The SOURCE note must NOT embed the raw rule ID into the Property value. |
| T-88-04 | Information disclosure | CBOM emitted to client deliverable | accept | Markers describe absence of crypto in neutral language; no secrets or internal paths surfaced. |
| T-88-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH §Package Legitimacy Audit: not applicable; no new deps). |
</threat_model>

<verification>
- `QUIRK_DB_PATH=./quirk.db python -m pytest tests/test_cbom_zero_algo_profiles.py tests/test_cbom_classifier_coverage.py tests/test_cbom_motion_endpoints.py -x -q` green.
- `QUIRK_DB_PATH=./quirk.db python -m pytest tests/ -q` shows no NEW failures vs the 39-failure baseline (VALIDATION.md caveat).
- `python -m compileall quirk/cbom/` exits 0.
</verification>

<success_criteria>
- SCORE-CBOM-01 satisfied: all five profiles emit real algorithm components or affirmative hardcoded no-crypto markers; Phase 42 OBS-1 closed.
- ssh-weak fixture reflects real ssh-audit output (no synthetic-only pass, Pitfall 1).
- No UNKNOWN classifier failures introduced; SCORE_WEIGHTS invariant untouched.
</success_criteria>

<output>
Create `.planning/phases/88-scoring-residuals/88-02-SUMMARY.md` when done.
</output>
