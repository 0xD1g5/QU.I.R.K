---
phase: 11-dashboard-wiring-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_dashboard_wiring.py
  - quirk/dashboard/api/deps.py
  - quirk/dashboard/server.py
autonomous: true
requirements:
  - UI-01
  - UI-04

must_haves:
  truths:
    - "deps.py default db_path matches config_template.yaml value ./quirk.db"
    - "server.py sets QUIRK_SERVE_PORT env var before uvicorn.run() so PDF exporter inherits the correct port"
    - "Tests exist and pass for both fixes"
  artifacts:
    - path: "quirk/dashboard/api/deps.py"
      provides: "Corrected default db_path"
      contains: "./quirk.db"
    - path: "quirk/dashboard/server.py"
      provides: "QUIRK_SERVE_PORT env var propagation"
      contains: "os.environ[\"QUIRK_SERVE_PORT\"]"
    - path: "tests/test_dashboard_wiring.py"
      provides: "Unit tests for all three Phase 11 fixes"
      min_lines: 40
  key_links:
    - from: "quirk/dashboard/api/deps.py"
      to: "quirk/config_template.yaml"
      via: "matching default db_path value"
      pattern: "./quirk.db"
    - from: "quirk/dashboard/server.py"
      to: "quirk/dashboard/api/routes/pdf.py"
      via: "QUIRK_SERVE_PORT env var inheritance"
      pattern: "QUIRK_SERVE_PORT"
---

<objective>
Fix the two wiring issues that break the default E2E dashboard flow: (1) deps.py default db_path
disagrees with config_template.yaml causing /api/scan/latest to 404 for fresh installs, and
(2) server.py never sets QUIRK_SERVE_PORT before starting uvicorn so PDF export always targets
port 8512 regardless of the actual serving port.

Purpose: Close GAP-INT-01 and GAP-INT-02 from the v3.9 milestone audit.
Output: Fixed deps.py and server.py, plus test scaffold covering all Phase 11 fixes.
</objective>

<execution_context>
@/Users/digs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/digs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dashboard-wiring-fixes/11-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From quirk/dashboard/api/deps.py (full file — 38 lines):
```python
def _default_db_path() -> str:
    """Resolve default SQLite path — same logic as run_scan.py."""
    return os.environ.get("QUIRK_DB_PATH", "data/quirk.db")  # BUG: wrong default

def get_db() -> Generator[Session, None, None]:
    # uses _default_db_path() to resolve DB location
```

From quirk/dashboard/server.py (full file — 44 lines):
```python
def serve(port: int = 8512, host: str = "127.0.0.1", no_open: bool = False) -> None:
    # BUG: no os.environ["QUIRK_SERVE_PORT"] assignment before uvicorn.run()
    uvicorn.run("quirk.dashboard.api.app:app", host=host, port=port, log_level="info")
```

From quirk/dashboard/api/routes/pdf.py:45:
```python
port = int(os.environ.get("QUIRK_SERVE_PORT", "8512"))  # reads env var set by server.py
```

From quirk/config_template.yaml:44:
```yaml
db_path: "./quirk.db"  # ground truth default
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test scaffold for all Phase 11 wiring fixes</name>
  <files>tests/test_dashboard_wiring.py</files>
  <read_first>
    - quirk/dashboard/api/deps.py
    - quirk/dashboard/server.py
    - quirk/dashboard/api/routes/scan.py
    - quirk/dashboard/api/routes/pdf.py
    - tests/test_dashboard_api.py
    - tests/conftest.py
  </read_first>
  <behavior>
    - test_deps_default_db_path: _default_db_path() returns "./quirk.db" when QUIRK_DB_PATH env var is not set
    - test_deps_db_path_env_override: _default_db_path() returns custom path when QUIRK_DB_PATH is set
    - test_server_sets_quirk_serve_port: serve() sets os.environ["QUIRK_SERVE_PORT"] to str(port) before calling uvicorn.run — mock uvicorn.run and verify env var is set when mock is called
    - test_derive_cbom_ssh_algorithms: _derive_cbom() with an endpoint containing ssh_audit_json returns CbomComponent entries for SSH algorithms (RED — will fail until Plan 02)
    - test_derive_cbom_ssh_only_scan: _derive_cbom() with only SSH endpoints (no TLS) returns a non-empty list (RED — will fail until Plan 02)
  </behavior>
  <action>
Create `tests/test_dashboard_wiring.py` with 5 test functions:

1. `test_deps_default_db_path()`:
   - Use `unittest.mock.patch.dict(os.environ, {}, clear=True)` to ensure QUIRK_DB_PATH is unset
   - Import `_default_db_path` from `quirk.dashboard.api.deps`
   - Assert return value `== "./quirk.db"`

2. `test_deps_db_path_env_override()`:
   - Use `unittest.mock.patch.dict(os.environ, {"QUIRK_DB_PATH": "/tmp/custom.db"})`
   - Assert `_default_db_path() == "/tmp/custom.db"`

3. `test_server_sets_quirk_serve_port()`:
   - Mock `uvicorn.run` to capture the moment it is called
   - Inside the mock, assert `os.environ.get("QUIRK_SERVE_PORT") == "9999"`
   - Call `serve(port=9999, no_open=True)` — the mock prevents uvicorn from actually starting
   - Use `unittest.mock.patch("uvicorn.run", side_effect=check_env_and_stop)` where `check_env_and_stop` reads the env var and raises SystemExit to stop execution

4. `test_derive_cbom_ssh_algorithms()`:
   - Create a mock CryptoEndpoint with `protocol="SSH"`, `ssh_audit_json=json.dumps({"kex": [{"algorithm": "curve25519-sha256"}], "key": [{"algorithm": "rsa-sha2-512", "keysize": 3072}], "enc": [{"algorithm": "aes256-ctr"}], "mac": [{"algorithm": "hmac-sha2-256"}]})`, other fields None/empty
   - Call `_derive_cbom([ep])`
   - Assert result contains entries for "curve25519-sha256", "rsa-sha2-512", "aes256-ctr", "hmac-sha2-256"

5. `test_derive_cbom_ssh_only_scan()`:
   - Create a mock CryptoEndpoint with only SSH data (cert_pubkey_alg=None, tls_version=None, jwt_scan_json=None, cloud_scan_json=None)
   - Call `_derive_cbom([ep])`
   - Assert `len(result) > 0`

For the mock CryptoEndpoint, create a simple dataclass or use `unittest.mock.MagicMock(spec=CryptoEndpoint)` with attributes set. Reference `tests/test_dashboard_api.py` and `tests/conftest.py` patterns for style.

Import `json`, `os`, `unittest.mock`, `pytest`. Use `from quirk.dashboard.api.deps import _default_db_path` and `from quirk.dashboard.api.routes.scan import _derive_cbom`.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_dashboard_wiring.py::test_deps_default_db_path tests/test_dashboard_wiring.py::test_deps_db_path_env_override -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_dashboard_wiring.py exists and contains `def test_deps_default_db_path(`
    - tests/test_dashboard_wiring.py contains `def test_deps_db_path_env_override(`
    - tests/test_dashboard_wiring.py contains `def test_server_sets_quirk_serve_port(`
    - tests/test_dashboard_wiring.py contains `def test_derive_cbom_ssh_algorithms(`
    - tests/test_dashboard_wiring.py contains `def test_derive_cbom_ssh_only_scan(`
    - test_deps_default_db_path FAILS (expects "./quirk.db" but deps.py still returns "data/quirk.db") — this is the RED state
    - test_server_sets_quirk_serve_port FAILS (server.py doesn't set env var yet)
    - test_derive_cbom_ssh_algorithms FAILS (scan.py doesn't parse SSH yet)
  </acceptance_criteria>
  <done>Test file exists with 5 test functions. db_path and serve_port tests are RED (will go GREEN in Task 2). SSH tests are RED (will go GREEN in Plan 02).</done>
</task>

<task type="auto">
  <name>Task 2: Fix deps.py db_path default and server.py QUIRK_SERVE_PORT propagation</name>
  <files>quirk/dashboard/api/deps.py, quirk/dashboard/server.py</files>
  <read_first>
    - quirk/dashboard/api/deps.py
    - quirk/dashboard/server.py
    - quirk/config_template.yaml
    - quirk/dashboard/api/routes/pdf.py
    - tests/test_dashboard_wiring.py
  </read_first>
  <action>
**Fix 1 — deps.py (GAP-INT-01):**
In `quirk/dashboard/api/deps.py`, line 14, change the default value in `_default_db_path()`:
- FROM: `return os.environ.get("QUIRK_DB_PATH", "data/quirk.db")`
- TO: `return os.environ.get("QUIRK_DB_PATH", "./quirk.db")`

This aligns the dashboard fallback default with `config_template.yaml:44` which specifies `db_path: "./quirk.db"`. The env var override continues to work for non-default setups.

**Fix 2 — server.py (GAP-INT-02):**
In `quirk/dashboard/server.py`, add one line immediately before the `uvicorn.run(...)` call (before line 39):
```python
    os.environ["QUIRK_SERVE_PORT"] = str(port)
```

This must appear BEFORE `uvicorn.run()` because `uvicorn.run()` blocks until the server stops, and the env var must be inherited by worker threads that handle the PDF export route.

After both fixes, run `python -m compileall quirk/dashboard/api/deps.py quirk/dashboard/server.py` per CLAUDE.md.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_dashboard_wiring.py::test_deps_default_db_path tests/test_dashboard_wiring.py::test_deps_db_path_env_override tests/test_dashboard_wiring.py::test_server_sets_quirk_serve_port -x -q && python -m compileall quirk/dashboard/api/deps.py quirk/dashboard/server.py -q</automated>
  </verify>
  <acceptance_criteria>
    - quirk/dashboard/api/deps.py contains `"./quirk.db"` (not `"data/quirk.db"`)
    - quirk/dashboard/server.py contains `os.environ["QUIRK_SERVE_PORT"] = str(port)` on a line before `uvicorn.run(`
    - test_deps_default_db_path passes (GREEN)
    - test_deps_db_path_env_override passes (GREEN)
    - test_server_sets_quirk_serve_port passes (GREEN)
    - python -m compileall exits 0 for both files
    - Existing tests still pass: `python3 -m pytest tests/test_dashboard_api.py tests/test_gap_closure.py -x -q` exits 0
  </acceptance_criteria>
  <done>deps.py default is "./quirk.db" matching config_template.yaml. server.py sets QUIRK_SERVE_PORT before uvicorn starts. All 3 wiring tests for UI-01 and UI-04 are GREEN.</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/test_dashboard_wiring.py::test_deps_default_db_path tests/test_dashboard_wiring.py::test_deps_db_path_env_override tests/test_dashboard_wiring.py::test_server_sets_quirk_serve_port -x -q` — all 3 pass
- `python3 -m pytest tests/test_dashboard_api.py tests/test_gap_closure.py -x -q` — no regressions
- `grep -n "quirk.db" quirk/dashboard/api/deps.py` — shows `./quirk.db`
- `grep -n "QUIRK_SERVE_PORT" quirk/dashboard/server.py` — shows env var assignment
</verification>

<success_criteria>
- deps.py _default_db_path() returns "./quirk.db" when QUIRK_DB_PATH is unset
- server.py sets os.environ["QUIRK_SERVE_PORT"] = str(port) before uvicorn.run()
- tests/test_dashboard_wiring.py exists with 5 tests (3 GREEN for this plan, 2 RED awaiting Plan 02)
- No regressions in existing dashboard/gap closure tests
</success_criteria>

<output>
After completion, create `.planning/phases/11-dashboard-wiring-fixes/11-01-SUMMARY.md`
</output>
