---
phase: 17-identity-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_identity_infra.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "RED test file exists and all 6 tests FAIL when run against the current codebase (columns missing, config fields missing, extras group missing)"
    - "Test file covers both fresh-DB and upgrade-DB scenarios for INFRA-01"
    - "Test file covers config flag acceptance and backward-compat for INFRA-02"
    - "Test file covers pyproject.toml extras group and config template identity section for INFRA-02/INFRA-03"
  artifacts:
    - path: "tests/test_identity_infra.py"
      provides: "RED test scaffold for INFRA-01, INFRA-02, INFRA-03"
      min_lines: 80
      contains: "class TestIdentityInfra"
  key_links: []
---

<objective>
Create the RED test scaffold for Phase 17 Identity Infrastructure requirements.

Purpose: Prove that all three INFRA requirements are currently unmet before any production code changes. These tests will turn GREEN in Plan 17-02. Following the project's established TDD pattern (see Phase 16: test_v41_gap_closure.py).

Output: `tests/test_identity_infra.py` with 6 failing tests covering INFRA-01, INFRA-02, and INFRA-03.
</objective>

<execution_context>
@/Users/digs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/digs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-identity-infrastructure/17-CONTEXT.md
@.planning/phases/17-identity-infrastructure/17-RESEARCH.md
@.planning/phases/17-identity-infrastructure/17-VALIDATION.md

@quirk/models.py
@quirk/db.py
@quirk/config.py
@quirk/config_template.yaml
@pyproject.toml
@tests/test_v41_gap_closure.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write RED test scaffold for INFRA-01, INFRA-02, INFRA-03</name>
  <files>tests/test_identity_infra.py</files>
  <read_first>
    - quirk/models.py (current ScanResult columns — confirm kerberos_scan_json, saml_scan_json, dnssec_scan_json do NOT exist yet)
    - quirk/db.py (current init_db() — confirm no _ensure_identity_columns call exists)
    - quirk/config.py (current ConnectorsCfg — confirm enable_kerberos, enable_saml, enable_dnssec fields do NOT exist yet)
    - quirk/config_template.yaml (confirm no identity section exists)
    - pyproject.toml (confirm no [identity] extras group exists)
    - tests/test_v41_gap_closure.py (established RED scaffold pattern — unittest.TestCase with descriptive assertions)
  </read_first>
  <action>
Create `tests/test_identity_infra.py` with a `TestIdentityInfra(unittest.TestCase)` class containing 6 RED tests. Follow the exact test style from `tests/test_v41_gap_closure.py` (unittest.TestCase, descriptive assert messages explaining what's wrong and what needs fixing).

**Test 1: `test_schema_fresh_db_has_identity_columns`** (INFRA-01)
- Create an in-memory SQLite engine via `sqlalchemy.create_engine("sqlite:///:memory:")`
- Import `Base` from `quirk.models` and call `Base.metadata.create_all(engine)`
- Import `inspect as sa_inspect` from `sqlalchemy`
- Get columns via `sa_inspect(engine).get_columns("scan_results")`
- Build a set of column names
- Assert `"kerberos_scan_json" in col_names` with message: `"ScanResult model missing kerberos_scan_json column -- add Column(Text, nullable=True) to quirk/models.py"`
- Assert `"saml_scan_json" in col_names` with similar message
- Assert `"dnssec_scan_json" in col_names` with similar message

**Test 2: `test_schema_migration_idempotent`** (INFRA-01)
- Create an in-memory SQLite engine
- Call `Base.metadata.create_all(engine)` to create the table
- If `_ensure_identity_columns` is importable from `quirk.db`, call it twice on the same engine to test idempotency — both calls should succeed without error
- If `_ensure_identity_columns` is NOT importable (which is the expected RED state), use `self.fail("quirk.db does not export _ensure_identity_columns -- add inspector-first migration helper per D-01")`
- Use a try/except ImportError pattern:
  ```python
  try:
      from quirk.db import _ensure_identity_columns
  except ImportError:
      self.fail("quirk.db does not export _ensure_identity_columns -- add inspector-first migration helper per D-01")
  ```
- If import succeeds, call it twice: `_ensure_identity_columns(engine)` then `_ensure_identity_columns(engine)` — the second call must not raise. Then verify all three columns exist via inspector.

**Test 3: `test_config_flags_accepted`** (INFRA-02)
- Import `ConnectorsCfg` from `quirk.config`
- Try to instantiate: `cfg = ConnectorsCfg(enable_aws=False, enable_azure=False, enable_kerberos=True, enable_saml=True, enable_dnssec=True)`
- If this raises `TypeError` (unexpected keyword argument), the test fails with message: `"ConnectorsCfg does not accept enable_kerberos/enable_saml/enable_dnssec -- add bool fields per D-04"`
- Assert `cfg.enable_kerberos is True`
- Assert `cfg.enable_saml is True`
- Assert `cfg.enable_dnssec is True`

**Test 4: `test_config_old_yaml_backward_compat`** (INFRA-02)
- Import `config_from_dict` from `quirk.config`
- Build a minimal raw dict mimicking a v4.1 config.yaml that has NO identity fields:
  ```python
  raw = {
      "assessment": {"name": "Test", "report_owner": "Tester", "data_classification": "INTERNAL", "timezone": "UTC"},
      "scan": {"timeout_seconds": 10, "concurrency": 5, "ports_tls": [443], "include_sni": True},
      "targets": {"fqdns": [], "cidrs": [], "include_ips": ["127.0.0.1"], "exclude_ips": []},
      "connectors": {"enable_aws": False, "enable_azure": False},
      "output": {"directory": "./out", "db_path": "./quirk.db"},
  }
  ```
- Call `cfg = config_from_dict(raw)` — must not raise
- Assert `cfg.connectors.enable_kerberos is False` with message: `"ConnectorsCfg.enable_kerberos default missing -- must default to False per D-04"`
- Assert `cfg.connectors.kerberos_targets == []` with message: `"ConnectorsCfg.kerberos_targets default missing -- must default to [] per D-05"`
- Assert same for `enable_saml`, `saml_targets`, `enable_dnssec`, `dnssec_targets`

**Test 5: `test_pyproject_identity_extras_declared`** (INFRA-03)
- Read `pyproject.toml` via `pathlib.Path("pyproject.toml").read_text(encoding="utf-8")`
- Assert `'identity = [' in source` with message: `"pyproject.toml does not declare [identity] extras group -- add after [dashboard] group per D-07"`
- Assert `'"impacket>=0.13.0,<0.14"' in source` with message explaining the package is missing
- Assert `'"dnspython[dnssec]>=2.8.0"' in source`
- Assert `'"lxml>=6.0"' in source`
- Assert `'"defusedxml>=0.7.1"' in source`
- Assert `'"signxml>=4.4.0"' in source`

**Test 6: `test_config_template_has_identity_section`** (INFRA-02)
- Read `quirk/config_template.yaml` via `pathlib.Path("quirk/config_template.yaml").read_text(encoding="utf-8")`
- Assert `"enable_kerberos" in source` with message: `"config_template.yaml missing enable_kerberos -- add identity connectors section per D-09"`
- Assert `"enable_saml" in source`
- Assert `"enable_dnssec" in source`
- Assert `"kerberos_targets" in source`
- Assert `"saml_targets" in source`
- Assert `"dnssec_targets" in source`

Module docstring: `"""Phase 17 -- Identity Infrastructure RED scaffold. Tests MUST FAIL before Plan 02 implementation lands. Covers INFRA-01 (schema), INFRA-02 (config), INFRA-03 (extras group)."""`

Include `if __name__ == "__main__": unittest.main()` at the bottom.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python3 -m pytest tests/test_identity_infra.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_identity_infra.py exists and contains `class TestIdentityInfra(unittest.TestCase)`
    - File contains exactly 6 test methods: test_schema_fresh_db_has_identity_columns, test_schema_migration_idempotent, test_config_flags_accepted, test_config_old_yaml_backward_compat, test_pyproject_identity_extras_declared, test_config_template_has_identity_section
    - `python3 -m pytest tests/test_identity_infra.py -v` runs and reports 6 FAILED (all RED -- none pass against current codebase)
    - No test passes -- if any test passes, the RED scaffold is wrong (the feature already exists or the assertion is incorrect)
  </acceptance_criteria>
  <done>All 6 tests exist, all 6 FAIL against the current unmodified codebase, confirming the three INFRA requirements are unmet.</done>
</task>

</tasks>

<verification>
Run the full test file and confirm all 6 tests fail:
```bash
python3 -m pytest tests/test_identity_infra.py -v
```
Expected: 6 failed, 0 passed.

Run existing test suite to confirm no regressions:
```bash
python3 -m pytest tests/ -x -q
```
Expected: all pre-existing tests still pass.
</verification>

<success_criteria>
- tests/test_identity_infra.py exists with 6 test methods in TestIdentityInfra class
- All 6 tests FAIL when run against unmodified codebase (RED state)
- No existing tests broken by the new test file
- Test names match VALIDATION.md task IDs for traceability
</success_criteria>

<output>
After completion, create `.planning/phases/17-identity-infrastructure/17-01-SUMMARY.md`
</output>
