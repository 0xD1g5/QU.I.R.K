---
phase: 108-sensor-push-cli-windows-ci
plan: 03
type: execute
wave: 3
depends_on: ["108-02"]
files_modified:
  - quirk/cli/sensor_cmd.py
  - quirk/cli/console_cmd.py
  - run_scan.py
  - tests/test_sensor_cmd.py
  - tests/test_console_cmd.py
autonomous: true
requirements: [SENSOR-04]
must_haves:
  truths:
    - "quirk sensor export-results writes a .qpush file whose body is byte-identical to the HTTPS push body (same envelope, same zstd compression)"
    - "quirk console import-results reads a .qpush file, decompresses and validates the envelope, and routes it to the same ingest path (stub for Phase 109) skipping the replay window but keeping payload_id dedup intent"
    - "export and push share one envelope/compression code path — export is push-to-file"
  artifacts:
    - path: "quirk/cli/console_cmd.py"
      provides: "run_console() — import-results subcommand"
      exports: ["run_console"]
    - path: "tests/test_console_cmd.py"
      provides: "SENSOR-04 import-results coverage"
  key_links:
    - from: "quirk/cli/sensor_cmd.py export-results"
      to: "quirk/cli/sensor_cmd.py _build_compressed_payload"
      via: "export reuses the identical envelope + compression helpers as push"
      pattern: "_build_compressed_payload"
    - from: "run_scan.py"
      to: "quirk/cli/console_cmd.py"
      via: "lazy import run_console in the 'console' argv dispatch block"
      pattern: "from quirk.cli.console_cmd import run_console"
---

<objective>
Deliver the air-gapped sneakernet path (SENSOR-04): quirk sensor export-results writes the wire payload to a transferable .qpush file, and quirk console import-results ingests it on a disconnected console.

Purpose: Some segments have no network path to the console at all. export-results is literally "push to a file" — it MUST reuse the exact envelope and zstd-compression helpers from Plan 02 so the .qpush body is byte-identical to an HTTPS push body. This single shared path is what lets Phase 109 have one ingest implementation and Phase 110 one merge. import-results validates and routes the payload through the same ingest entry (a Phase-108 stub that deserializes, validates, and summarizes; the DB write is Phase 109), skipping the ±15-minute replay window per D-15 transport carve-out while preserving payload_id dedup intent.

Output: export-results subcommand in sensor_cmd.py, new console_cmd.py with import-results, run_scan.py console dispatch block, two test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-sensor-push-cli-windows-ci/108-CONTEXT.md
@.planning/phases/108-sensor-push-cli-windows-ci/108-PATTERNS.md
@docs/architecture-distributed.md

<interfaces>
From quirk/cli/sensor_cmd.py (Plan 02 — reuse, do NOT duplicate):
- _build_envelope(sensor_cfg, endpoints) -> dict (canonical wire envelope)
- _build_compressed_payload(envelope) -> bytes (zstandard ZstdCompressor level=3)
- _run_local_scan(scan_config, output_dir) -> int (subprocess scan)
- _endpoint_to_dict(ep) -> dict
- _write_sensor_config / sensor.yaml reader

From quirk/cli/token_cmd.py / console_cmd analog (PATTERNS.md console_cmd section):
- run_console(argv) subparser: import-results positional file, --config default config.yaml

From run_scan.py main():
- Insert the "console" dispatch block adjacent to the "sensor" block (after sensor, before errors): if argv[1] == "console": from quirk.cli.console_cmd import run_console; run_console(_sys.argv[2:]); return

D-15 (CONTEXT Air-Gap section):
- .qpush naming: {sensor_id}-{payload_id}.qpush ; one payload per file
- import runs the SAME ingest + dedup path; skips ±15-min replay window (air-gap carve-out) but keeps payload_id dedup → 409 on replay (full DB dedup is Phase 109)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: quirk sensor export-results — byte-identical .qpush file (SENSOR-04)</name>
  <files>quirk/cli/sensor_cmd.py, tests/test_sensor_cmd.py</files>
  <read_first>
    - quirk/cli/sensor_cmd.py (Plan 02 envelope + compression helpers and push flow)
    - .planning/phases/108-sensor-push-cli-windows-ci/108-CONTEXT.md (Air-Gap Export/Import section)
    - .planning/phases/108-sensor-push-cli-windows-ci/108-PATTERNS.md (sensor_cmd subparser; export_p definition)
  </read_first>
  <behavior>
    - Test: export-results runs the local scan and writes {sensor_id}-{payload_id}.qpush to --output dir; one payload per file
    - Test: for the same scan endpoints and a fixed payload_id/pushed_at (monkeypatched), the bytes written to .qpush equal the bytes that push would send as the request body (byte-identical invariant)
    - Test: the .qpush body decompresses (zstandard) back to an envelope with the same key set as a push envelope
    - Test: export does NOT contact the network (no httpx call) and does NOT require console reachability
  </behavior>
  <action>
    Add the export-results subparser (--config override, --scan-config default config.yaml, --output default ".") and _cmd_export_results to sensor_cmd.py. _cmd_export_results: read sensor.yaml; _run_local_scan to a temp dir; read CryptoEndpoint rows; build the envelope via the SAME _build_envelope and compress via the SAME _build_compressed_payload used by push (do NOT fork the serialization — refactor push in Plan 02 if needed so both call one helper that returns the exact request-body bytes); write those exact bytes to Path(output)/f"{sensor_id}-{payload_id}.qpush". No httpx, no validate_external_url (no network). sys.exit(0). Print the written path. Ensure the .qpush body is the compressed envelope bytes only (the HMAC signature travels in the push header; for air-gap, recompute/verify on import using the stored hmac_key, or embed the signature in a tiny envelope — Claude's discretion on .qpush internal layout AS LONG AS the compressed-payload bytes are byte-identical to the push body; if a wrapper is used, the push body equality test must compare the compressed payload segment).
    Extend tests/test_sensor_cmd.py with the export behaviors; assert byte-equality against the push body produced by the Plan 02 path with identical inputs.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m pytest tests/test_sensor_cmd.py -k "export" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - export-results writes a file named {sensor_id}-{payload_id}.qpush in the --output directory
    - with monkeypatched fixed payload_id/pushed_at and identical endpoints, the .qpush compressed-payload bytes equal the push request-body bytes
    - the .qpush body decompresses to an envelope dict with the canonical key set
    - no httpx network call occurs during export (verified by a fail-on-call mock)
    - test_sensor_cmd.py export tests pass
  </acceptance_criteria>
  <done>export-results produces a transferable file whose payload is byte-identical to an HTTPS push, sharing one serialization code path — the Phase 109/110 single-ingest invariant holds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: quirk console import-results — air-gap ingest stub (SENSOR-04)</name>
  <files>quirk/cli/console_cmd.py, run_scan.py, tests/test_console_cmd.py</files>
  <read_first>
    - quirk/cli/token_cmd.py (lines 53-130: run_X subparser + dispatch shape)
    - run_scan.py (the sensor dispatch block added in Plan 02; insert console block adjacent)
    - .planning/phases/108-sensor-push-cli-windows-ci/108-PATTERNS.md (console_cmd.py section + import-results handler note)
    - quirk/cli/sensor_cmd.py (_build_compressed_payload / envelope key set for the decompress+validate)
  </read_first>
  <behavior>
    - Test: import-results reads a .qpush file, decompresses it, and validates the envelope has the required keys (payload_id, schema_version, sensor_id, segment, findings); prints a summary (sensor_id, segment, payload_id, finding count)
    - Test: a corrupt/non-zstd file yields a clean non-zero exit with a clear error (not a traceback)
    - Test: an envelope missing required keys yields a clean validation error exit
    - Test: import-results routes the validated envelope to a single ingest entry point (_ingest_envelope stub) so Phase 109 can replace the stub body without touching the CLI; the air-gap path passes a flag/parameter indicating the ±15-minute replay window is skipped while payload_id dedup intent is preserved
  </behavior>
  <action>
    Create quirk/cli/console_cmd.py with run_console(argv) per the token_cmd subparser shape: import-results positional file, --config default config.yaml. _cmd_import_results: read the .qpush file bytes; zstandard.ZstdDecompressor().decompress(); json.loads; validate required envelope keys (raise SystemExit(1) with a clear stderr message on missing keys or decompress failure); call _ingest_envelope(envelope, config_path, skip_replay_window=True) — a Phase-108 stub that validates and prints a summary (the actual sensor_pushes dedup + CryptoEndpoint write is Phase 109; the stub documents this with a comment and the skip_replay_window=True air-gap carve-out per D-15). sys.exit(0) on success.
    Add the run_scan.py console dispatch block adjacent to the sensor block: if argv[1] == "console": from quirk.cli.console_cmd import run_console; run_console(_sys.argv[2:]); return.
    Create tests/test_console_cmd.py covering the behaviors above; generate a .qpush via the Plan 03 Task 1 export path (or a hand-built compressed envelope) as the import fixture.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m pytest tests/test_console_cmd.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - quirk console import-results <file.qpush> decompresses, validates, and prints a summary with sensor_id, segment, payload_id, and finding count
    - a non-zstd / corrupt file exits non-zero with a stderr message (no raw traceback)
    - an envelope missing a required key exits non-zero with a validation message
    - _ingest_envelope is the single call the CLI makes (Phase 109 replaces its body); it is invoked with skip_replay_window=True on the air-gap path
    - run_scan dispatch reaches run_console for argv[1] == "console"
    - test_console_cmd.py passes
  </acceptance_criteria>
  <done>import-results ingests a .qpush on a disconnected console through one ingest entry point, skipping the replay window per the air-gap carve-out while leaving payload_id dedup for Phase 109; both CLI groups are wired in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .qpush file → console import | file crosses an air-gap via untrusted media; contents are attacker-influenceable |
| operator CLI → console_cmd | file path is operator-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-09 | Tampering | malformed/oversized .qpush on import | mitigate | decompress + key-validation wrapped in try/except → clean SystemExit(1), never a traceback; envelope key whitelist validated before any ingest |
| T-108-10 | Repudiation | replayed .qpush re-import | mitigate | payload_id dedup intent preserved (full DB dedup → 409 is Phase 109); air-gap skips only the ±15-min clock window per D-15, not the payload_id check |
| T-108-11 | Tampering | .qpush diverges from push body | mitigate | export reuses the SAME _build_envelope/_build_compressed_payload as push; byte-equality regression test enforces the invariant |
</threat_model>

<verification>
- python -m pytest tests/test_sensor_cmd.py tests/test_console_cmd.py -q is green
- export/import round-trips: a .qpush written by export-results imports cleanly via import-results
- byte-identical invariant test passes (export body == push body for identical inputs)
- python -m compileall quirk/cli/console_cmd.py quirk/cli/sensor_cmd.py run_scan.py exits 0
</verification>

<success_criteria>
- SENSOR-04: export-results writes byte-identical .qpush; import-results validates + routes through one ingest stub; air-gap carve-out (skip replay window, keep payload_id dedup) honored
- Single shared serialization path between push and export (no fork)
</success_criteria>

<output>
Create .planning/phases/108-sensor-push-cli-windows-ci/108-03-SUMMARY.md when done
</output>
