---
phase: 108-sensor-push-cli-windows-ci
plan: 04
type: execute
wave: 4
depends_on: ["108-02", "108-03"]
files_modified:
  - .github/workflows/python-ci.yml
  - tests/test_sensor_windows_smoke.py
  - tests/test_windows_ci_hardgate.py
  - docs/UAT-SERIES.md
autonomous: true
requirements: [SENSOR-06]
must_haves:
  truths:
    - "a windows-latest GitHub Actions job runs the sensor smoke tests as a hard gate (no continue-on-error at job or step level)"
    - "the smoke test asserts the serialized wire payload contains no backslash path separators"
    - "the smoke test asserts the sensor handles SIGINT/KeyboardInterrupt with a clean shutdown (no traceback, exit 0 or 130)"
    - "docs/UAT-SERIES.md is updated for Phase 108 and synced to Obsidian"
  artifacts:
    - path: ".github/workflows/python-ci.yml"
      provides: "windows-latest hard-gate sensor smoke job"
      contains: "windows-latest"
    - path: "tests/test_sensor_windows_smoke.py"
      provides: "SENSOR-06 backslash + clean-shutdown assertions"
    - path: "tests/test_windows_ci_hardgate.py"
      provides: "static guard that the CI job is windows-latest with no continue-on-error"
  key_links:
    - from: ".github/workflows/python-ci.yml"
      to: "tests/test_sensor_windows_smoke.py"
      via: "pytest invocation in the windows job step"
      pattern: "test_sensor_windows_smoke"
---

<objective>
Land the Windows correctness hard gate (SENSOR-06) and complete the mandatory per-phase documentation: a windows-latest GitHub Actions smoke job (no continue-on-error) that runs cross-OS sensor assertions, the smoke test file it executes, a static guard that the job stays a hard gate, and the docs/UAT-SERIES.md update + Obsidian sync.

Purpose: The Linux chaos lab cannot validate Windows sensor correctness (v5.4-D-10) — only a real windows-latest runner can. The smoke job is the sole owner of Windows validation: it proves the serialized wire payload has no backslash path separators (Windows Path.__str__ hazard, RESEARCH Pitfall 5) and that the sensor shuts down cleanly on SIGINT. As a hard gate it blocks merges on failure. The phase is incomplete until docs/UAT-SERIES.md reflects the new sensor commands and is synced to the vault (project convention).

Output: .github/workflows/python-ci.yml windows job, tests/test_sensor_windows_smoke.py, tests/test_windows_ci_hardgate.py, docs/UAT-SERIES.md update + Obsidian sync.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-sensor-push-cli-windows-ci/108-CONTEXT.md
@.planning/phases/108-sensor-push-cli-windows-ci/108-PATTERNS.md

<interfaces>
From quirk/cli/sensor_cmd.py (Plans 02/03):
- _build_envelope(sensor_cfg, endpoints) -> dict ; _build_compressed_payload(envelope) -> bytes
- run_sensor(argv) entrypoint (for the clean-shutdown SIGINT test)

From .github/workflows/python-staleness.yml (analog — verified structure):
- runs-on: ubuntu-latest; actions/checkout@v4; actions/setup-python@v5 (python-version '3.11'); two-step pip install -e . then pip install pytest; named test step. NO continue-on-error anywhere.
- existing triggers: on pull_request, push to main, and a weekly cron.

SENSOR-06 hard-gate requirements (CONTEXT specifics + RESEARCH GitHub Actions section):
- runs-on: windows-latest (mandatory)
- NO continue-on-error: true at job or step level (mandatory — this is the hard gate)
- assert no backslash paths in serialized payload
- assert clean shutdown on SIGINT/KeyboardInterrupt

CLAUDE.md mandatory per-phase doc steps:
- update docs/UAT-SERIES.md; sync framed copy to /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md (printf frontmatter then append the doc — file too large for CLI content=); commit via gsd-tools.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Windows sensor smoke tests — no-backslash + clean shutdown (SENSOR-06)</name>
  <files>tests/test_sensor_windows_smoke.py</files>
  <read_first>
    - quirk/cli/sensor_cmd.py (_build_envelope / _build_compressed_payload signatures and SIGINT handling in run_sensor/push)
    - .planning/phases/108-sensor-push-cli-windows-ci/108-RESEARCH.md (Pitfall 5 backslash paths; Wire Contract Serialization)
    - .planning/phases/108-sensor-push-cli-windows-ci/108-PATTERNS.md (test_sensor_windows_smoke.py assertions)
  </read_first>
  <behavior>
    - Test: json.dumps(_build_envelope(sample_cfg, sample_endpoints)) contains no backslash character (chr(92)) — runs on any OS, meaningful on Windows where Path strings would introduce backslashes
    - Test: every string value in the envelope (recursively, including findings host/path fields) is free of backslash separators
    - Test: a simulated KeyboardInterrupt during the push/scan flow is caught and results in a clean exit (exit code 0 or 130), not an uncaught traceback
    - Test: the smoke module imports cleanly with only core deps installed (no [all] extras)
  </behavior>
  <action>
    Create tests/test_sensor_windows_smoke.py. Build a sample sensor_cfg (fixed sensor_id/segment/version + dummy hmac_key) and a small list of fake endpoint stand-ins exposing the CryptoEndpoint columns _endpoint_to_dict reads. test_no_backslash_in_payload asserts the backslash char is absent from json.dumps(_build_envelope(...)) and recursively from every string value. test_clean_shutdown invokes the push/scan flow with the subprocess scan and httpx mocked to raise KeyboardInterrupt, asserting a clean exit (pytest.raises(SystemExit) with code in 0/130, or no escaping exception). Keep every assertion OS-independent (no sys.platform gating) so the same file is the hard-gate payload on windows-latest. Tests must run with only core deps.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m pytest tests/test_sensor_windows_smoke.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - the backslash character is absent from json.dumps(_build_envelope(...)) and from every recursive string value
    - simulated KeyboardInterrupt yields a clean exit (code 0 or 130) with no escaping traceback
    - the module imports and passes with only core dependencies installed
    - tests/test_sensor_windows_smoke.py passes locally
  </acceptance_criteria>
  <done>The Windows smoke assertions exist and pass locally, ready to be the hard-gate payload on the windows-latest runner.</done>
</task>

<task type="auto">
  <name>Task 2: windows-latest hard-gate CI job + static guard (SENSOR-06)</name>
  <files>.github/workflows/python-ci.yml, tests/test_windows_ci_hardgate.py</files>
  <read_first>
    - .github/workflows/python-staleness.yml (full file — job structure, install steps, triggers)
    - .planning/phases/108-sensor-push-cli-windows-ci/108-PATTERNS.md (.github/workflows/python-ci.yml Windows smoke job section)
    - tests/test_sensor_windows_smoke.py (the test target from Task 1)
  </read_first>
  <action>
    Create .github/workflows/python-ci.yml with a single job windows-sensor-smoke: name "Windows Sensor Smoke"; runs-on: windows-latest; triggers on pull_request and push to main (mirror the staleness workflow trigger shape). Steps: actions/checkout@v4; actions/setup-python@v5 with python-version '3.11'; the two-step install (pip install -e . then pip install pytest) so the Plan 01 core deps install; a step running python -m pytest tests/test_sensor_windows_smoke.py tests/test_sensor_no_verify_false.py -v. CRITICAL SENSOR-06 constraint: do NOT add continue-on-error at the job level or on any step — a failure must block the merge. Do not reference [all] extras (sensor runtime is core-dep only).
    Create tests/test_windows_ci_hardgate.py: load .github/workflows/python-ci.yml with yaml.safe_load and assert the windows-sensor-smoke job has runs-on equal to windows-latest, that the job dict does not set continue-on-error true, and that no step sets continue-on-error true. This is the local enforcement that the hard gate cannot be silently softened.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m pytest tests/test_windows_ci_hardgate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/python-ci.yml defines job windows-sensor-smoke with runs-on windows-latest
    - the job runs python -m pytest tests/test_sensor_windows_smoke.py (and the verify=False grep gate) and has no continue-on-error at job or step level
    - grep -c "windows-latest" .github/workflows/python-ci.yml returns at least 1
    - grep -c "continue-on-error" .github/workflows/python-ci.yml returns 0
    - tests/test_windows_ci_hardgate.py passes
  </acceptance_criteria>
  <done>A real windows-latest job runs the sensor smoke + TLS-verify gate as a non-bypassable hard gate, and a local test prevents the gate from being softened to continue-on-error.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs/UAT-SERIES.md for Phase 108 + sync to Obsidian</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (current series structure + header date + version strings)
    - CLAUDE.md (Mandatory Phase Completion Steps: UAT-SERIES update + Obsidian sync + commit)
    - .planning/phases/108-sensor-push-cli-windows-ci/108-02-SUMMARY.md and 108-03-SUMMARY.md (what shipped)
  </read_first>
  <action>
    Update docs/UAT-SERIES.md to cover the Phase 108 sensor CLI surface: add test cases exercising quirk sensor enroll (writes sensor.yaml, prints one-time token), quirk sensor push (local scan then HTTPS push with retry; offline spool behavior), quirk sensor export-results plus quirk console import-results (air-gap round-trip), and the windows-latest CI hard gate as a referenced gate. Bump the Last Updated date at the top to today. Update any version strings if a version bump occurred this phase (check 108 SUMMARYs). Then sync to the vault per CLAUDE.md: write the framed copy to the vault UAT-Series.md path by printf-ing the project frontmatter to a temp file, appending docs/UAT-SERIES.md, and copying it over (file too large for CLI content= per CLAUDE.md). Commit docs/UAT-SERIES.md via the gsd-tools commit helper with message docs(phase-108): update UAT-SERIES.md. All steps here are automatable — no human action required; the vault path is a local filesystem write.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && grep -q "sensor enroll" docs/UAT-SERIES.md && grep -q "import-results" docs/UAT-SERIES.md && echo DOC_OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/UAT-SERIES.md references quirk sensor enroll, quirk sensor push, quirk sensor export-results, quirk console import-results
    - the Last Updated date at the top is today
    - the vault UAT-Series.md exists with matching content + frontmatter
    - docs/UAT-SERIES.md is committed via the gsd-tools commit helper
  </acceptance_criteria>
  <done>UAT-SERIES.md documents the full Phase 108 sensor workflow, is synced to the Obsidian vault, and is committed — satisfying the mandatory per-phase documentation convention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| serialized wire payload to cross-OS console | Windows path separators could leak into JSON and break Linux-side parsing/identity |
| CI configuration to merge gate | softening continue-on-error would silently disable Windows validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-12 | Tampering | backslash path leaking into wire payload on Windows | mitigate | smoke test asserts the backslash char is absent from json.dumps(envelope) and every string value; runs on a real windows-latest runner |
| T-108-13 | Denial of Service | sensor process hangs / dirty shutdown on Windows SIGINT | mitigate | clean-shutdown smoke test asserts exit code 0/130 with no escaping traceback; pairs with the Plan 01 SIGTERM platform guard |
| T-108-14 | Repudiation | Windows hard gate silently softened to continue-on-error | mitigate | tests/test_windows_ci_hardgate.py statically asserts no continue-on-error and runs-on windows-latest |
</threat_model>

<verification>
- python -m pytest tests/test_sensor_windows_smoke.py tests/test_windows_ci_hardgate.py -q is green
- grep -c "continue-on-error" .github/workflows/python-ci.yml returns 0
- grep -c "windows-latest" .github/workflows/python-ci.yml returns at least 1
- docs/UAT-SERIES.md updated + synced to vault + committed
</verification>

<success_criteria>
- SENSOR-06: windows-latest job is a hard gate (no continue-on-error); smoke test asserts no-backslash payload + clean shutdown; static guard prevents softening
- Project convention: docs/UAT-SERIES.md updated for Phase 108 and synced to Obsidian
</success_criteria>

<output>
Create .planning/phases/108-sensor-push-cli-windows-ci/108-04-SUMMARY.md when done
</output>
