---
phase: 96-active-rest-fuzzing
plan: 04
type: execute
wave: 3
depends_on: [96-02]
files_modified:
  - quantum-chaos-enterprise-lab/docker-compose.yml
  - quantum-chaos-enterprise-lab/fuzz-target/Dockerfile
  - quantum-chaos-enterprise-lab/fuzz-target/main.py
  - quantum-chaos-enterprise-lab/fuzz-target/requirements.txt
  - quantum-chaos-enterprise-lab/expected_results_v4.md
  - quantum-chaos-enterprise-lab/README.md
autonomous: true
requirements: [LAB-01]
must_haves:
  truths:
    - "A new fuzz-target chaos profile exists and is runnable via PROFILE_ARGS=\"--profile fuzz-target\" ./lab.sh up"
    - "./lab.sh profiles lists fuzz-target without any ALL_PROFILES source edit (dynamic discovery)"
    - "The fuzz-target service deliberately omits HSTS, exposes /.well-known/jwks.json, and accepts a forged JWT at /probe"
    - "expected_results_v4.md and chaos README document the fuzz-target profile, ports, services, and expected findings"
  artifacts:
    - path: "quantum-chaos-enterprise-lab/docker-compose.yml"
      provides: "fuzz-target service with profiles: [fuzz-target]"
      contains: "fuzz-target"
    - path: "quantum-chaos-enterprise-lab/fuzz-target/main.py"
      provides: "Deliberately-weak FastAPI: /openapi.json, /probe, /.well-known/jwks.json, no HSTS"
      contains: "openapi.json"
    - path: "quantum-chaos-enterprise-lab/expected_results_v4.md"
      provides: "Profile: fuzz-target oracle section"
      contains: "fuzz-target"
  key_links:
    - from: "quantum-chaos-enterprise-lab/docker-compose.yml"
      to: "quantum-chaos-enterprise-lab/fuzz-target/"
      via: "build context"
      pattern: "context: ./fuzz-target"
---

<objective>
Add the final LAB-01 deliverable: an isolated, deliberately-weak fuzz-target chaos profile
that exercises every REST-fuzz probe (HSTS-missing, http:// server, alg-confusion acceptance),
plus the mandatory triple-update of compose + expected_results + README per CLAUDE.md.

Purpose: Give the active fuzzer a safe, reproducible validation target and close LAB-01
(final). Per CLAUDE.md Chaos Lab Maintenance, a new profile requires compose + README +
expected_results updates in the SAME change; lab.sh needs NO ALL_PROFILES edit because
_derive_all_profiles parses compose dynamically — this rationale is an explicit acceptance
criterion.
Output: fuzz-target service dir + compose profile + oracle + README rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-active-rest-fuzzing/96-RESEARCH.md
@.planning/phases/96-active-rest-fuzzing/96-PATTERNS.md
@.planning/phases/96-active-rest-fuzzing/96-CONTEXT.md
@.planning/phases/96-active-rest-fuzzing/96-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the fuzz-target service + docker-compose profile</name>
  <files>quantum-chaos-enterprise-lab/fuzz-target/Dockerfile, quantum-chaos-enterprise-lab/fuzz-target/main.py, quantum-chaos-enterprise-lab/fuzz-target/requirements.txt, quantum-chaos-enterprise-lab/docker-compose.yml</files>
  <read_first>
    - quantum-chaos-enterprise-lab/jwt/rs256/Dockerfile, main.py (lines 1-44), requirements.txt — exact copy template
    - quantum-chaos-enterprise-lab/docker-compose.yml jwt profile block (lines 533-567) — service-block analog
    - quantum-chaos-enterprise-lab/lab.sh _derive_all_profiles (line 58) — confirms dynamic profile discovery
    - 96-PATTERNS.md "fuzz-target/" + "docker-compose.yml" sections; 96-RESEARCH.md Chaos Lab fuzz-target skeleton, Assumptions A1 (port 20100) / A2
  </read_first>
  <action>
    Create quantum-chaos-enterprise-lab/fuzz-target/ with: Dockerfile (copy jwt/rs256/Dockerfile
    verbatim — python:3.12-slim, uvicorn main:app on port 8000); requirements.txt (fastapi,
    uvicorn[standard], cryptography, PyJWT — versions per PATTERNS); main.py — a deliberately-weak
    FastAPI app exposing GET /openapi.json (minimal OpenAPI 3.0 with an http:// server URL and a
    /probe path so schemathesis can consume it), GET /probe (accepts any Authorization header
    WITHOUT proper alg verification — simulates alg-confusion acceptance, returns 2xx), and
    GET /.well-known/jwks.json (exposes the RS256 public key, mirroring jwt/rs256/main.py key-gen).
    Deliberately OMIT the Strict-Transport-Security header on all responses (HSTS probe target).

    Add a fuzz-target service block to docker-compose.yml following the jwt service-block format:
    build context ./fuzz-target, profiles: ["fuzz-target"], ports "20100:8000",
    restart: unless-stopped, with the PHASE 96 / LAB-01 comment header. Verify port 20100 does not
    collide with an existing ports mapping in the compose file (A1); if it does, pick the next free
    20xxx port and update the oracle/README accordingly.
  </action>
  <acceptance_criteria>
    - quantum-chaos-enterprise-lab/fuzz-target/ contains Dockerfile, main.py, requirements.txt
    - main.py defines routes /openapi.json, /probe, /.well-known/jwks.json
    - main.py emits NO Strict-Transport-Security header (`grep -ci "strict-transport-security" quantum-chaos-enterprise-lab/fuzz-target/main.py` returns 0)
    - /openapi.json server URL uses http:// (not https://)
    - docker-compose.yml has a fuzz-target service with `profiles: ["fuzz-target"]` and `20100:8000` (or documented free port)
    - chosen port does not duplicate any other host port in docker-compose.yml
    - `python -m compileall quantum-chaos-enterprise-lab/fuzz-target/main.py` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK/quantum-chaos-enterprise-lab && grep -q 'fuzz-target' docker-compose.yml && python -m compileall fuzz-target/main.py</automated>
  </verify>
  <done>fuzz-target service + compose profile created; deliberately weak (no HSTS, http server, forged-JWT-accepting /probe).</done>
</task>

<task type="auto">
  <name>Task 2: Update expected_results_v4.md + chaos README for fuzz-target</name>
  <files>quantum-chaos-enterprise-lab/expected_results_v4.md, quantum-chaos-enterprise-lab/README.md</files>
  <read_first>
    - quantum-chaos-enterprise-lab/expected_results_v4.md jwt profile section (lines 153-174) — oracle-section analog
    - quantum-chaos-enterprise-lab/README.md profiles table (jwt row) — README-row analog
    - 96-PATTERNS.md "expected_results_v4.md — fuzz-target section"; CLAUDE.md Chaos Lab Maintenance rule
  </read_first>
  <action>
    Add a `## Profile: fuzz-target` section to expected_results_v4.md following the jwt section
    structure: the PROFILE_ARGS up command, a port/service/expected-probe/expected-finding/severity
    table covering TLS downgrade (HIGH if TLS configured), HSTS missing (HIGH), HTTP-only creds
    (HIGH, http:// server declared), and JWT alg-confusion (CRITICAL, /probe accepts forged HS256),
    and a scanner validation command (quirk scan --targets http://localhost:20100 --fuzz
    --openapi-spec http://localhost:20100/openapi.json) with the expected >= 2 findings note.

    Add a fuzz-target row to the README.md profiles table following the jwt row pattern (profile
    name, port 20100, service, brief description). Per CLAUDE.md, both the README and the oracle
    are updated in THIS change alongside the compose change from Task 1; note in the SUMMARY that
    lab.sh requires NO ALL_PROFILES edit because _derive_all_profiles discovers profiles dynamically
    from docker-compose.yml.
  </action>
  <acceptance_criteria>
    - expected_results_v4.md contains a "## Profile: fuzz-target" heading
    - That section lists port 20100, the four expected probes, and severities (alg-confusion=CRITICAL, others=HIGH)
    - The section includes the quirk scan --fuzz --openapi-spec validation command
    - README.md profiles table includes a fuzz-target row referencing port 20100
    - SUMMARY explicitly records that lab.sh ALL_PROFILES needs no edit (dynamic _derive_all_profiles)
    - `grep -c "fuzz-target" quantum-chaos-enterprise-lab/expected_results_v4.md quantum-chaos-enterprise-lab/README.md` returns >= 1 per file
  </acceptance_criteria>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK/quantum-chaos-enterprise-lab && grep -q '## Profile: fuzz-target' expected_results_v4.md && grep -q 'fuzz-target' README.md</automated>
  </verify>
  <done>Oracle + README document the fuzz-target profile, ports, probes, and findings — CLAUDE.md triple-update complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chaos lab → host | fuzz-target binds 20100 on the host; isolated, deliberately-weak, never production |
| oracle vs. compose | Drift between compose profiles and expected_results must not occur (CLAUDE.md rule) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-11 | Information Disclosure | deliberately-weak fuzz-target service | accept | Intentional vulnerable lab target; isolated compose profile, off by default, documented as deliberately weak (RESEARCH A2) |
| T-96-12 | Tampering | profile/oracle drift | mitigate | CLAUDE.md triple-update enforced in one change; ./lab.sh profiles lists fuzz-target via dynamic discovery |
| T-96-13 | Tampering | base image supply chain | mitigate | Reuse the existing vetted python:3.12-slim base + pinned fastapi/uvicorn already used by jwt/rs256 |
</threat_model>

<verification>
- `grep -q 'fuzz-target' quantum-chaos-enterprise-lab/docker-compose.yml`
- `grep -q '## Profile: fuzz-target' quantum-chaos-enterprise-lab/expected_results_v4.md`
- `grep -q 'fuzz-target' quantum-chaos-enterprise-lab/README.md`
</verification>

<success_criteria>
- fuzz-target chaos profile runnable, deliberately weak, exercises all probes
- compose + expected_results_v4.md + README updated in one change (CLAUDE.md triple-update)
- lab.sh needs no ALL_PROFILES edit (dynamic discovery) — rationale recorded
</success_criteria>

<output>
Create `.planning/phases/96-active-rest-fuzzing/96-04-SUMMARY.md` when done
</output>
