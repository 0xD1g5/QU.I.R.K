---
phase: 96-active-rest-fuzzing
plan: 05
type: execute
wave: 4
depends_on: [96-03, 96-04]
files_modified:
  - docs/configuration.md
  - docs/chaos-lab.md
  - docs/UAT-SERIES.md
autonomous: true
requirements: [FUZZ-01, FUZZ-02, FUZZ-03, FUZZ-04, SCORE-01, LAB-01]
must_haves:
  truths:
    - "docs/configuration.md documents the --fuzz / --fuzz-jwt-alg-confusion / --fuzz-budget flags, the six guardrails, and the non-TTY hard-abort"
    - "docs/chaos-lab.md documents the fuzz-target profile"
    - "docs/UAT-SERIES.md has a Phase 96 test series and updated Last Updated line"
    - "UAT-SERIES.md is synced to the Obsidian vault and an Obsidian Phase-96 note exists"
  artifacts:
    - path: "docs/configuration.md"
      provides: "REST fuzzing section"
      contains: "--fuzz"
    - path: "docs/UAT-SERIES.md"
      provides: "Phase 96 UAT series"
      contains: "UAT-96"
  key_links:
    - from: "docs/UAT-SERIES.md"
      to: "Obsidian vault UAT-Series.md"
      via: "printf-prepend sync pattern"
      pattern: "UAT-96"
---

<objective>
Close Phase 96 (and the v5.1 milestone's feature work) with the CLAUDE.md mandatory
per-phase documentation steps: document the REST fuzzer in the user docs, add a Phase 96
UAT series, sync UAT-SERIES.md to Obsidian, and write the Obsidian Phase-96 note.

Purpose: Per CLAUDE.md and the recurring docs/Obsidian-per-phase feedback, no phase is
complete until docs + UAT-SERIES.md + Obsidian are updated. This is the final feature plan
of v5.1; subsequent milestone close-out (version bump, tag) is out of scope for this phase.
Output: configuration + chaos-lab docs; Phase 96 UAT series; vault sync; Phase-96 note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-active-rest-fuzzing/96-CONTEXT.md
@.planning/phases/96-active-rest-fuzzing/96-03-SUMMARY.md
@.planning/phases/96-active-rest-fuzzing/96-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document REST fuzzing in configuration.md + chaos-lab.md</name>
  <files>docs/configuration.md, docs/chaos-lab.md</files>
  <read_first>
    - docs/configuration.md OpenAPI Spec Analysis section (Phase 94 analog — the section style to mirror) and the authenticated-scanning section (Phase 93)
    - docs/chaos-lab.md — existing profile documentation style (jwt / ldaps entries)
    - 96-CONTEXT.md (six guardrails, CONFIRM gate, non-TTY abort, alg-confusion sub-flag)
    - 96-03-SUMMARY.md + 96-04-SUMMARY.md — actual flags, findings, profile delivered
  </read_first>
  <action>
    Add a "REST Fuzzing (active crypto-posture probes)" section to docs/configuration.md mirroring
    the Phase 94 OpenAPI section style. Document: the --fuzz flag (requires --openapi-spec +
    interactive confirmation), --fuzz-jwt-alg-confusion (RS256→HS256 probe), --fuzz-budget (default
    50, hard max 500); the six guardrails (GET-only default, hard budget ceiling, 5 req/s rate cap,
    CONFIRM prompt, per-request scope enforcement, 3x5xx-cascade pause); the non-TTY hard-abort
    behavior (fuzzing never runs headless); the [api] extras requirement (schemathesis); the
    findings produced (TLS downgrade / weak cipher / HSTS missing / HTTP-only creds = HIGH,
    alg-confusion acceptance = CRITICAL); and the SCORE_WEIGHTS contribution to agility_signals.

    Add a fuzz-target profile entry to docs/chaos-lab.md following the existing profile-doc style:
    profile name, port 20100, what it exposes (weak REST target), and the validation scan command.
  </action>
  <acceptance_criteria>
    - docs/configuration.md contains the literal strings --fuzz, --fuzz-jwt-alg-confusion, --fuzz-budget, CONFIRM, and "hard max 500"
    - docs/configuration.md states fuzzing hard-aborts in non-TTY / non-interactive contexts
    - docs/configuration.md lists all six guardrails
    - docs/chaos-lab.md contains a fuzz-target profile entry referencing port 20100
    - `grep -c "fuzz" docs/configuration.md docs/chaos-lab.md` returns >= 1 per file
  </acceptance_criteria>
  <verify>
    <automated>grep -q -- '--fuzz-jwt-alg-confusion' docs/configuration.md && grep -q 'fuzz-target' docs/chaos-lab.md && grep -q 'CONFIRM' docs/configuration.md</automated>
  </verify>
  <done>configuration.md + chaos-lab.md document the fuzzer flags, guardrails, non-TTY abort, and fuzz-target profile.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 96 UAT series, sync to Obsidian, write Phase-96 note</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md header + Last Updated line (line 4) + the Phase 94/95 UAT series sections (the format to mirror)
    - CLAUDE.md "Mandatory Phase Completion Steps" (Obsidian note + UAT-SERIES update + vault sync + commit) and "Obsidian Vault Integration"
    - An existing vault phase note at /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/ for the template
    - 96-03-SUMMARY.md + 96-04-SUMMARY.md — what was actually built
  </read_first>
  <action>
    Add a "UAT Series 96" section to docs/UAT-SERIES.md mirroring the Phase 95 series format, with
    cases covering: UAT-96-01 --fuzz TTY CONFIRM gate (literal CONFIRM proceeds; other input aborts,
    no requests); UAT-96-02 non-TTY hard-abort (piped stdin aborts before any request);
    UAT-96-03 budget hard ceiling (--fuzz-budget 501 rejected; default 50); UAT-96-04 GET-only +
    scope + rate guardrails; UAT-96-05 --fuzz-jwt-alg-confusion against the fuzz-target lab profile
    yields a CRITICAL alg-confusion finding; UAT-96-06 SCORE_WEIGHTS sum 303.0 / count 41 invariant
    green; UAT-96-07 schemathesis present in [api] / absent from [all]; UAT-96-08 fuzz-target chaos
    profile appears in ./lab.sh profiles. Update the **Last Updated:** line (line 4) to a Phase 96
    summary entry following the prepend-history convention.

    Sync UAT-SERIES.md to the vault using the printf-prepend pattern from CLAUDE.md (write frontmatter
    to /tmp then append docs/UAT-SERIES.md, copy to
    /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md). Write the Obsidian Phase-96 note directly
    to the vault filesystem at
    /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-96-Active-REST-Fuzzing.md (status: complete,
    type: phase, source, updated) with Goal + Requirements Covered (FUZZ-01..04, SCORE-01, LAB-01) +
    Success Criteria + What Was Built (one subsection per plan from the SUMMARY.md files) + [[Roadmap]]
    link. Do NOT use the obsidian CLI content= parameter (files too large) — write to the vault
    filesystem directly.
  </action>
  <acceptance_criteria>
    - docs/UAT-SERIES.md contains a "UAT Series 96" section with cases UAT-96-01 through UAT-96-08
    - The Last Updated line references Phase 96 / REST fuzzing
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md exists and contains UAT-96
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-96-Active-REST-Fuzzing.md exists with status: complete and the FUZZ-01..04/SCORE-01/LAB-01 requirements
    - `grep -c "UAT-96" docs/UAT-SERIES.md` returns >= 8
  </acceptance_criteria>
  <verify>
    <automated>grep -q "UAT Series 96" docs/UAT-SERIES.md && test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-96-Active-REST-Fuzzing.md" && grep -q "UAT-96" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"</automated>
  </verify>
  <done>Phase 96 UAT series added; UAT-SERIES.md synced to vault; Phase-96 Obsidian note written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → operator | Documentation must accurately convey the non-TTY hard-abort and budget ceiling so operators do not assume headless fuzzing is possible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-14 | Repudiation | misleading fuzzing docs | mitigate | configuration.md explicitly documents non-TTY hard-abort + hard max 500 + CONFIRM literal so the safety model is operator-visible |
| T-96-SC | Tampering | vault write path | accept | Local filesystem write to the user's own vault; no external surface |
</threat_model>

<verification>
- `grep -q "UAT Series 96" docs/UAT-SERIES.md`
- `test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-96-Active-REST-Fuzzing.md"`
- `grep -q -- '--fuzz' docs/configuration.md`
</verification>

<success_criteria>
- configuration.md + chaos-lab.md document the fuzzer + fuzz-target profile
- Phase 96 UAT series added with the safety-critical cases
- UAT-SERIES.md synced to vault; Phase-96 Obsidian note written
</success_criteria>

<output>
Create `.planning/phases/96-active-rest-fuzzing/96-05-SUMMARY.md` when done
</output>
