---
phase: 74-qramm-compliance-warnings
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/qramm/evidence_bridge.py
  - quirk/assessment/operator_context.py
  - tests/test_evidence_bridge_correctness.py
  - .planning/audit-2026-05-08/AUDIT-TASKS.md
autonomous: true
requirements: [QWARN-02]
must_haves:
  truths:
    - "Evidence bridge date compare uses `datetime.date.fromisoformat(...)` parsing; downstream Python-side comparisons compare `datetime.date` objects, not raw strings. The existing SQL filter at lines 43-50 is TZ-symmetric (both sides `func.date()`) — keep unchanged per RESEARCH C-3 path (a), add inline comment + regression test asserting symmetry. The `evidence_source` string at line 111 still embeds raw `max_date_str` (no functional break) (D-05, WR-01)"
    - "Idempotent UPDATE at `evidence_bridge.py:114-127`: BEFORE each `.update(...)`, a pre-query reads existing rows for the same `(session_id, dimension='CVI', practice_area)` triple; if `r.suggested_answer == suggested_value AND r.evidence_source == evidence_source` for every existing row, the `.update(...)` is skipped (`continue`) (D-06, WR-03)"
    - "`db.commit()` wrapped in `try/except SQLAlchemyError as e: logger.warning(\"evidence_bridge UPDATE failed: %s\", e); db.rollback(); return` — failures logged + rolled back, never silent (D-06, WR-07)"
    - "`quirk/assessment/operator_context.py::attach_context` BOTH `except Exception:` blocks (lines 85, 94) replaced with narrowed `except AttributeError as e: logger.warning(\"attach_context skipped — source object missing attribute: %s\", e)` PLUS a fallback `except Exception as e: logger.warning(\"attach_context unexpected: %s\", e); raise` per user input override — preserves unexpected-error visibility while narrowing the silent-swallow path (D-07, WR-08)"
    - "`tests/test_evidence_bridge_correctness.py` exists with >= 6 test functions covering D-05 TZ-safe parsing, D-06 idempotent skip, D-06 commit-failure logged + rollback, D-07 AttributeError logged (not swallowed), D-07 unexpected exception re-raised after logging"
    - "Audit rows qramm-compliance/WR-01, WR-03, WR-07, WR-08 are flipped to `Phase 74 | [x] closed`"
  artifacts:
    - path: "quirk/qramm/evidence_bridge.py"
      provides: "TZ-safe date parse + idempotent UPDATE + commit-failure handler"
      contains: "SQLAlchemyError"
    - path: "quirk/assessment/operator_context.py"
      provides: "Narrowed AttributeError + fallback Exception re-raise"
      contains: "attach_context skipped"
    - path: "tests/test_evidence_bridge_correctness.py"
      provides: "RED-then-GREEN coverage of D-05..D-07"
      contains: "def test_"
  key_links:
    - from: "quirk/qramm/evidence_bridge.py UPDATE loop"
      to: "pre-query idempotency check"
      via: "existing = db.query(QRAMMAnswer).filter(...).all(); if all(r.suggested_answer == ... and r.evidence_source == ... for r in existing): continue"
      pattern: "synchronize_session"
    - from: "quirk/qramm/evidence_bridge.py db.commit()"
      to: "SQLAlchemyError handler + rollback"
      via: "try: db.commit() except SQLAlchemyError as e: logger.warning(...); db.rollback(); return"
      pattern: "SQLAlchemyError"
    - from: "quirk/assessment/operator_context.py attach_context"
      to: "logger.warning on AttributeError + Exception re-raise"
      via: "except AttributeError as e: logger.warning(...); except Exception as e: logger.warning(...); raise"
      pattern: "attach_context skipped"
---

<objective>
Close QWARN-02 with three surgical hardening fixes across the evidence bridge and operator-context attachment, mirroring Phase 73's per-WR-row shape:

1. **WR-01 (D-05)** — TZ-safe date comparison. Per RESEARCH C-3 path (a) recommendation: the existing SQL filter at `evidence_bridge.py:43-50` uses `func.date()` on BOTH sides (already TZ-symmetric — same SQLite engine evaluates both). Keep the SQL filter unchanged; add an inline comment naming the TZ-symmetry invariant, AND a regression test that exercises the filter with TZ-aware fixture rows. For any DOWNSTREAM Python-side date use of `max_date_str`, parse via `datetime.date.fromisoformat(max_date_str)` before any `==` comparison. The `evidence_source` string embed at line 111 stays raw (it is identifier, not a compared date).

2. **WR-03 + WR-07 (D-06)** — Idempotent UPDATE + commit-failure handler at `evidence_bridge.py:114-127`. Wrap the per-practice loop with a pre-query: read existing `QRAMMAnswer` rows for `(session_id, dimension='CVI', practice_area)`; if every existing row's `suggested_answer == suggested_value` AND `evidence_source == evidence_source`, `continue` (skip the UPDATE). Then wrap `db.commit()` in `try/except SQLAlchemyError as e: logger.warning("evidence_bridge UPDATE failed: %s", e); db.rollback(); return`. Closes WR-03 (idempotency) and WR-07 (commit failure handling) in the same task.

3. **WR-08 (D-07)** — `attach_context` AttributeError visibility. Per RESEARCH C-7 + user input override: current code at `operator_context.py:85, 94` is BARE `except Exception:` (NOT `except AttributeError:` as CONTEXT D-07's textual diff implied). Narrow BOTH bare excepts to `except AttributeError as e: logger.warning("attach_context skipped — source object missing attribute: %s", e)` PLUS keep a trailing fallback `except Exception as e: logger.warning("attach_context unexpected: %s", e); raise` (user override — preserves unexpected-error visibility AND surfaces dataclass-frozen `FrozenInstanceError` family). Add `logger = logging.getLogger(__name__)` at top of module if not already present.

Then flip audit rows WR-01 (line 124), WR-03 (126), WR-07 (130), WR-08 (131) of `AUDIT-TASKS.md` to `Phase 74 | [x] closed`.

Purpose: Evidence bridge is repeat-call safe, surfaces commit errors loudly, never silently drops operator context, and the TZ-symmetry invariant on the date filter is named in code + asserted in test.
Output: Edits in 2 source files, 1 new test module, 4 audit rows flipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/74-qramm-compliance-warnings/74-CONTEXT.md
@.planning/phases/74-qramm-compliance-warnings/74-RESEARCH.md
@.planning/audit-2026-05-08/AUDIT-TASKS.md

<interfaces>
Existing surfaces (verified):

quirk/qramm/evidence_bridge.py:
- `logger = logging.getLogger(__name__)` already present at line 21 (D-06/D-07 logging precondition satisfied)
- `max_date_str = db.query(func.date(func.max(CryptoEndpoint.scanned_at))).scalar()` at line 43 (D-05 site — kept; TZ-symmetric)
- `.filter(func.date(CryptoEndpoint.scanned_at) == max_date_str)` at line 50 (D-05 — kept; add invariant comment above)
- `for practice_area, suggested_value in practice_scores.items()` at line 114 (D-06 idempotency loop site)
- `db.commit()` at end of populate_cvi_suggestions (D-06 commit-failure wrap site)
- `QRAMMAnswer` model imported earlier in file (verify via read)

quirk/assessment/operator_context.py:
- `def attach_context(cfg, ctx: OperatorContext) -> None:` at line 74
- BARE `except Exception:` at line 85 (first attach attempt)
- BARE `except Exception:` at line 94 (second attach attempt)
- Module currently lacks `import logging` / `logger = ...` — add at top of file

SQLAlchemy exception import (verified available — already used elsewhere in project):
- `from sqlalchemy.exc import SQLAlchemyError`

Target shape (D-06 idempotent UPDATE + commit handler):
```
for practice_area, suggested_value in practice_scores.items():
    existing = (
        db.query(QRAMMAnswer)
        .filter(
            QRAMMAnswer.session_id == session_id,
            QRAMMAnswer.dimension == "CVI",
            QRAMMAnswer.practice_area == practice_area,
        )
        .all()
    )
    if existing and all(
        r.suggested_answer == suggested_value and r.evidence_source == evidence_source
        for r in existing
    ):
        continue  # D-06: idempotent skip — desired state already persisted
    db.query(QRAMMAnswer).filter(...).update({...}, synchronize_session="fetch")

try:
    db.commit()
except SQLAlchemyError as e:
    logger.warning("evidence_bridge UPDATE failed: %s", e)
    db.rollback()
    return
```

Target shape (D-07 attach_context, per user input override):
```
import logging
logger = logging.getLogger(__name__)

def attach_context(cfg, ctx: OperatorContext) -> None:
    ctx_dict = ctx.to_dict()
    try:
        setattr(cfg, "assessment_context", ctx_dict)
        return
    except AttributeError as e:
        logger.warning("attach_context skipped — source object missing attribute: %s", e)
    except Exception as e:
        logger.warning("attach_context unexpected: %s", e)
        raise

    try:
        assessment = getattr(cfg, "assessment", None)
        if assessment is not None:
            setattr(assessment, "context", ctx_dict)
            return
    except AttributeError as e:
        logger.warning("attach_context skipped — source object missing attribute: %s", e)
    except Exception as e:
        logger.warning("attach_context unexpected: %s", e)
        raise
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing tests for D-05..D-07 in tests/test_evidence_bridge_correctness.py</name>
  <files>tests/test_evidence_bridge_correctness.py</files>
  <read_first>
    - quirk/qramm/evidence_bridge.py (full file ~204 lines)
    - quirk/assessment/operator_context.py (full file)
    - tests/test_qramm_staleness.py (in-memory SQLite fixture idiom reference if present)
    - .planning/phases/74-qramm-compliance-warnings/74-CONTEXT.md (D-05..D-07)
    - .planning/phases/74-qramm-compliance-warnings/74-RESEARCH.md (Pitfalls 3, 4 + C-3, C-7)
  </read_first>
  <behavior>
    - D-05 (WR-01, TZ-safe) — `test_max_date_filter_is_tz_symmetric`: insert two `CryptoEndpoint` rows with `scanned_at` differing only by timezone-equivalent offsets but resolving to the same calendar date; assert the SQL filter at `populate_cvi_suggestions` resolves both rows into the same `max_date_str` bucket.
    - D-05 — `test_max_date_str_parses_as_datetime_date`: assert `datetime.date.fromisoformat(max_date_str)` succeeds and returns a `datetime.date` for a populated bridge run.
    - D-06 (WR-03, idempotent) — `test_idempotent_repeat_call_does_not_rewrite`: call `populate_cvi_suggestions(session_id, db)` twice in a row; spy/patch the SQLAlchemy `.update(...)` call to assert it is invoked on the FIRST call and SKIPPED on the SECOND.
    - D-06 (WR-07, commit failure) — `test_commit_failure_logs_and_rolls_back`: patch `db.commit` to raise `SQLAlchemyError("boom")`; assert function returns without propagating, assert `db.rollback` was called, assert `caplog` captures `"evidence_bridge UPDATE failed: boom"` at WARNING level.
    - D-07 (WR-08, AttributeError logged) — `test_attach_context_attribute_error_logged`: pass a `cfg` object whose `setattr` raises `AttributeError` (e.g., a `__slots__`-locked dummy); assert `logger.warning` captured the "attach_context skipped" message; assert function returns without raising.
    - D-07 (WR-08, unexpected re-raised) — `test_attach_context_unexpected_exception_reraised`: pass a `cfg` whose `setattr` raises `RuntimeError("boom")`; assert `pytest.raises(RuntimeError)`; assert `caplog` captures `"attach_context unexpected: boom"` at WARNING level.
  </behavior>
  <action>
    Create `tests/test_evidence_bridge_correctness.py`. Use pytest `caplog` fixture for log assertions. Use `unittest.mock.patch` for `db.commit` failure injection. Use an in-memory SQLite SQLAlchemy session for D-05/D-06 happy-path tests (mirror any existing fixture in `tests/test_qramm_evidence_bridge.py` if present — search via `ls tests/ | grep evidence_bridge`).

    For D-07, do NOT need a DB session — construct a minimal dummy `cfg` with controlled `__setattr__` behavior. Use `__slots__ = ()` to force `AttributeError` on attribute set; or override `__setattr__` to raise `RuntimeError`. Pass a real `OperatorContext` (or a stub with `to_dict()`).

    Commit message: `test(74-02): add failing tests for QWARN-02 (D-05..D-07)`. Run `pytest tests/test_evidence_bridge_correctness.py -x`; confirm failure (idempotency check missing, commit not wrapped, attach_context bare-except).

    PEP 8.
  </action>
  <verify>
    <automated>pytest tests/test_evidence_bridge_correctness.py -x 2>&1 | grep -E "FAILED|ERROR" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_evidence_bridge_correctness.py` exists
    - `grep -cE "^def test_" tests/test_evidence_bridge_correctness.py` >= 6
    - `grep -cE "SQLAlchemyError|sqlalchemy\.exc" tests/test_evidence_bridge_correctness.py` >= 1
    - `grep -cE "attach_context" tests/test_evidence_bridge_correctness.py` >= 2
    - `grep -cE "caplog" tests/test_evidence_bridge_correctness.py` >= 3
    - `pytest tests/test_evidence_bridge_correctness.py -x` exits NON-ZERO (RED state)
  </acceptance_criteria>
  <done>
    Failing tests committed; pytest exits non-zero with clear assertion failures tied to D-05..D-07.
  </done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement D-05..D-07 in evidence_bridge.py + operator_context.py</name>
  <files>quirk/qramm/evidence_bridge.py, quirk/assessment/operator_context.py</files>
  <read_first>
    - quirk/qramm/evidence_bridge.py (full file)
    - quirk/assessment/operator_context.py (full file)
    - tests/test_evidence_bridge_correctness.py (post-Task 1)
  </read_first>
  <action>
    Apply three surgical edits per `<interfaces>` target shapes:

    **evidence_bridge.py edits:**

    1. (D-05) Above the SQL filter at line 50, add a one-line invariant comment:
       ```python
       # D-05 (WR-01): TZ-symmetric — both sides use SQLAlchemy func.date(), evaluated by the same engine.
       ```
       Add `import datetime` at top-of-file if not already imported. Do NOT change the SQL filter expressions themselves. If any DOWNSTREAM Python-side comparison of `max_date_str` exists in the file (researcher confirms during read_first via grep `max_date_str`), wrap it with `datetime.date.fromisoformat(max_date_str)` before the `==`. The `evidence_source` string at line 111 stays raw (identifier, not date compare).

    2. (D-06, idempotent UPDATE) Add `from sqlalchemy.exc import SQLAlchemyError` to top-of-file imports. Restructure the loop at lines 114-127 per `<interfaces>` target shape. The pre-query reads existing rows for `(session_id, dimension="CVI", practice_area)`; the `continue` skip predicate is `existing and all(...)` — empty `existing` falls through to the UPDATE (first-time write path preserved).

    3. (D-06, commit-failure handler) Wrap `db.commit()` in `try/except SQLAlchemyError as e: logger.warning("evidence_bridge UPDATE failed: %s", e); db.rollback(); return`. The exact format string is locked by D-06.

    **operator_context.py edits:**

    4. (D-07) Add at top of module if missing:
       ```python
       import logging
       logger = logging.getLogger(__name__)
       ```
       Replace BOTH bare `except Exception:` blocks (lines 85, 94) with the two-clause pattern in `<interfaces>` target shape — `except AttributeError as e: logger.warning(...)` followed by `except Exception as e: logger.warning(...); raise`. The first clause closes WR-08; the second is the user-override safety net preserving unexpected-error visibility.

    Per D-14: do NOT touch `questions.py`, do NOT touch `tests/test_compliance_freshness.py`, do NOT modify QRAMM BLOCKERs (already closed by Phase 70).

    PEP 8. Minimal diff.

    Commit message: `feat(74-02): implement QWARN-02 fixes (D-05 TZ comment, D-06 idempotent UPDATE + commit handler, D-07 narrowed AttributeError + Exception fallback)`.
  </action>
  <verify>
    <automated>python -m compileall quirk/qramm/evidence_bridge.py quirk/assessment/operator_context.py && pytest tests/test_evidence_bridge_correctness.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -m compileall quirk/qramm/evidence_bridge.py quirk/assessment/operator_context.py` exits 0
    - `grep -cE "from sqlalchemy\.exc import SQLAlchemyError" quirk/qramm/evidence_bridge.py` >= 1
    - `grep -cE "SQLAlchemyError" quirk/qramm/evidence_bridge.py` >= 2
    - `grep -cE "evidence_bridge UPDATE failed" quirk/qramm/evidence_bridge.py` >= 1
    - `grep -cE "db\.rollback" quirk/qramm/evidence_bridge.py` >= 1
    - `grep -cE "TZ-symmetric" quirk/qramm/evidence_bridge.py` >= 1
    - `grep -cE "idempotent skip|D-06" quirk/qramm/evidence_bridge.py` >= 1
    - `grep -cE "^import logging" quirk/assessment/operator_context.py` >= 1
    - `grep -cE "except AttributeError as e" quirk/assessment/operator_context.py` >= 2
    - `grep -cE "attach_context skipped" quirk/assessment/operator_context.py` >= 1
    - `grep -cE "attach_context unexpected" quirk/assessment/operator_context.py` >= 1
    - `grep -v '^#' quirk/assessment/operator_context.py | grep -cE "^\s*except Exception:\s*$" ` == 0 (no bare `except Exception: pass` remains)
    - `pytest tests/test_evidence_bridge_correctness.py -x` exits 0 (GREEN)
    - `pytest tests/test_qramm_staleness.py tests/test_compliance_freshness.py -x` exits 0 (no collateral regression)
  </acceptance_criteria>
  <done>
    All Task 1 tests pass; no collateral regression in pre-existing QRAMM tests.
  </done>
</task>

<task type="auto">
  <name>Task 3: Flip WR-01, WR-03, WR-07, WR-08 audit rows</name>
  <files>.planning/audit-2026-05-08/AUDIT-TASKS.md</files>
  <read_first>
    - .planning/audit-2026-05-08/AUDIT-TASKS.md lines 124-136 (qramm-compliance WR rows)
  </read_first>
  <action>
    Update `.planning/audit-2026-05-08/AUDIT-TASKS.md`, flipping these four rows from `| — | [ ] open |` to `| Phase 74 | [x] closed |`:
    - Line 124 (WR-01): Evidence bridge date-string equality vulnerable to TZ drift
    - Line 126 (WR-03): evidence_bridge synchronize_session=fetch suboptimal; no idempotency
    - Line 130 (WR-07): evidence_bridge does not handle db.commit failure
    - Line 131 (WR-08): attach_context swallows AttributeError; user context dropped

    No other rows touched. Single docs commit.

    Commit message: `docs(74-02): close qramm-compliance WR-01/WR-03/WR-07/WR-08 in audit ledger`.
  </action>
  <verify>
    <automated>grep -cE "qramm-compliance/WR-(01|03|07|08).*Phase 74.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "qramm-compliance/WR-(01|03|07|08).*Phase 74.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 4
    - `grep -cE "qramm-compliance/WR-(09|10|11|12|13).*\[ \] open" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 5 (remaining QWARN-03 rows still open pending 74-03)
  </acceptance_criteria>
  <done>
    AUDIT-TASKS.md shows WR-01, WR-03, WR-07, WR-08 closed under Phase 74.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLAlchemy DB commit → on-disk SQLite state | UPDATE/commit failures must not be silent corruption |
| Source object (cfg) → `attach_context` | Untrusted runtime shape; missing-attribute path must be logged not swallowed |
| Local-TZ scanner write → server-side `func.date()` filter | TZ drift across scanners must not partition same-day rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-05 | Tampering (silent state corruption) | `evidence_bridge` non-idempotent UPDATE rewriting same value on every call | mitigate | Pre-query check + `continue` skip per D-06 |
| T-74-06 | Tampering (lost commit) | `db.commit()` raising `SQLAlchemyError` silently | mitigate | `try/except SQLAlchemyError`, `logger.warning`, `db.rollback`, return per D-06 |
| T-74-07 | Repudiation (silent context drop) | `attach_context` bare `except Exception` swallowing AttributeError | mitigate | Narrow to `except AttributeError as e: logger.warning(...)` + fallback `except Exception as e: logger.warning(...); raise` per D-07 + user override |
| T-74-08 | Information Disclosure (missing audit trail) | Unexpected exceptions in `attach_context` lost in bare except | mitigate | Trailing `except Exception as e: logger.warning(...); raise` preserves visibility AND propagation |
| T-74-09 | Tampering (date partition drift) | Different scanners writing TZ-naive `scanned_at` → same-day rows split across `func.date()` buckets | accept | SQL filter is engine-symmetric (both sides `func.date()`); regression test asserts invariant. Cross-TZ scanner fix is a writer-side concern (deferred to v5.0+) |
</threat_model>

<verification>
- `python -m compileall quirk/qramm/evidence_bridge.py quirk/assessment/operator_context.py` clean
- `pytest tests/test_evidence_bridge_correctness.py -x` passes (6+ tests)
- `pytest tests/test_qramm_staleness.py tests/test_compliance_freshness.py -x` passes (no collateral regression)
- AUDIT-TASKS.md WR-01, WR-03, WR-07, WR-08 rows show `Phase 74 | [x] closed`
- No collateral changes to `questions.py` or QRAMM BLOCKERs per D-14
</verification>

<success_criteria>
- Evidence bridge UPDATE is idempotent (repeat call skips no-op writes)
- `db.commit()` failure logged + rolled back, never silent
- `attach_context` AttributeError surfaces via `logger.warning`; unexpected exceptions log AND re-raise
- TZ-symmetry of SQL filter named in code comment + asserted in test
- 6+ tests pass in `tests/test_evidence_bridge_correctness.py`
- WR-01, WR-03, WR-07, WR-08 closed in AUDIT-TASKS.md
</success_criteria>

<output>
Create `.planning/phases/74-qramm-compliance-warnings/74-02-SUMMARY.md` documenting: D-05 path (a) — SQL filter kept; invariant comment + regression test per RESEARCH C-3; D-07 user override — AttributeError + Exception fallback re-raise pattern (per Pitfall 4 + C-7); four audit rows flipped (WR-01, WR-03, WR-07, WR-08), test count, RED→GREEN commit cadence.
</output>
