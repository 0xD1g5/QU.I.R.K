---
phase: 74-qramm-compliance-warnings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/qramm/scoring.py
  - quirk/qramm/evidence_bridge.py
  - tests/test_qramm_practice_scoring.py
  - .planning/audit-2026-05-08/AUDIT-TASKS.md
autonomous: true
requirements: [QWARN-01]
must_haves:
  truths:
    - "`compute_practice_score` validates each answer ∈ {0,1,2,3,4} BEFORE summation and raises `ValueError(f\"Practice score answer {answer!r} for {practice_id} out of range [0, 4]\")` on out-of-range (D-01, WR-02); function signature gains `practice_id: str = \"\"` parameter (per RESEARCH Pattern 3)"
    - "`quirk/qramm/evidence_bridge.py` Practice 1.1 block (lines 78-87) multiplies `score_1_1` by `discovery_factor = min(1.0, max(0.25, math.log10(max(total_endpoints, 1)) / 3.0))` using local `total_endpoints` from line 59 (NOT `evidence_summary.endpoint_count` per RESEARCH C-1 + user input override) (D-02, WR-04)"
    - "`vuln_pct = None` sentinel emitted when `total_algos == 0`; `_maturity_label` returns `\"Indeterminate\"` for `None` input; signature widened to `score: float | None` (D-03, WR-05)"
    - "`_maturity_label` top-band threshold lowered from `>= 4.0` to `>= 3.95` at `scoring.py:67` (path (a) per RESEARCH C-2 — minimal one-line diff); per D-04 this only fixes FP-noise at sub-1.0 multipliers (4.0 was already reachable at multiplier=1.0 per RESEARCH Pitfall 2)"
    - "`tests/test_qramm_practice_scoring.py` exists with >= 8 test functions covering D-01 ValueError, D-02 endpoint factor curve (1→0.25, 10→~0.33, 100→~0.67, 1000+→1.0), D-03 Indeterminate sentinel, D-04 parametrized 3.99 + 4.0 both land in 'Optimizing' (WR-02, WR-04, WR-05, WR-06)"
    - "Audit rows qramm-compliance/WR-02, WR-04, WR-05, WR-06 are flipped to `Phase 74 | [x] closed`"
  artifacts:
    - path: "quirk/qramm/scoring.py"
      provides: "Fail-loud ValueError on out-of-range; 'Indeterminate' label arm; >= 3.95 top-band threshold"
      contains: "out of range [0, 4]"
    - path: "quirk/qramm/evidence_bridge.py"
      provides: "Practice 1.1 endpoint-count discovery_factor; vuln_pct=None sentinel"
      contains: "discovery_factor"
    - path: "tests/test_qramm_practice_scoring.py"
      provides: "RED-then-GREEN coverage of D-01..D-04"
      contains: "def test_"
  key_links:
    - from: "quirk/qramm/scoring.py::compute_practice_score"
      to: "ValueError on out-of-range"
      via: "for answer in answers: if answer not in (0,1,2,3,4): raise ValueError(...)"
      pattern: "out of range \\[0, 4\\]"
    - from: "quirk/qramm/evidence_bridge.py Practice 1.1 block"
      to: "discovery_factor"
      via: "math.log10(max(total_endpoints, 1)) / 3.0"
      pattern: "discovery_factor"
    - from: "quirk/qramm/scoring.py::_maturity_label"
      to: "'Indeterminate' arm + >= 3.95 top band"
      via: "if score is None: return 'Indeterminate'; if score >= 3.95: return 'Optimizing'"
      pattern: "Indeterminate"
---

<objective>
Close QWARN-01 with four surgical fixes in `quirk/qramm/scoring.py` and `quirk/qramm/evidence_bridge.py`, mirroring Phase 73's per-WR-row hardening shape:

1. **WR-02 (D-01)** — Fail-loud out-of-range validation in `compute_practice_score`. Per RESEARCH Pattern 3, current signature `compute_practice_score(answers: List[int])` lacks the `practice_id` referenced in D-01's error message — add `practice_id: str = ""` as a keyword parameter (no existing callers reference the function under non-test paths per RESEARCH). Validate each `answer ∈ {0, 1, 2, 3, 4}` BEFORE the existing `if not answers: return 0.0` early return. The 0-value is intentional per D-01 (CONTEXT widens the docstring's "1-4" range to `{0,1,2,3,4}`).

2. **WR-04 (D-02)** — Practice 1.1 Discovery endpoint-count factor at `evidence_bridge.py:78-87`. Per RESEARCH C-1 + user input override: source `endpoint_count` from the **local `total_endpoints` variable at line 59**, NOT from `evidence_summary.endpoint_count` (that field does not exist in scope; importing `evidence_summary` would violate D-09 isolation invariant). Compute `discovery_factor = min(1.0, max(0.25, math.log10(max(total_endpoints, 1)) / 3.0))` and multiply `score_1_1` by it, rounded to 4 decimals. Curve shape: log10 per D-02 default (D-02a discretion).

3. **WR-05 (D-03)** — `vuln_pct = None` sentinel when `total_algos == 0`. The existing block at `evidence_bridge.py:89-98` divides by `total_endpoints`; the D-03 trigger is `total_algos == 0` (zero algorithms after classification — `algorithm_set` empty). Propagate `None` upward; `_maturity_label` at `scoring.py:66-84` gains a leading `if score is None: return "Indeterminate"` arm. Signature widened to `score: float | None`. The new `"Indeterminate"` label is a sibling (NOT a numeric tier per D-14); sorts outside 0..4 in any downstream cohort logic.

4. **WR-06 (D-04)** — Top-band threshold from `>= 4.0` to `>= 3.95` at `scoring.py:67` (path (a) per RESEARCH C-2). Per user input override: 4.0 is already reachable at multiplier=1.0 (RESEARCH Pitfall 2 VERIFIED); the threshold change only fixes FP-noise at sub-1.0 multipliers. One-line diff; minimal change.

Then flip audit rows WR-02 (line 125), WR-04 (127), WR-05 (128), WR-06 (129) of `AUDIT-TASKS.md` to `Phase 74 | [x] closed`.

Purpose: Practice scoring is fail-loud on bad input, reflects scan reality (endpoint-count factor), distinguishes "no algos found" from "Advanced maturity", and the top band is reachable across the full multiplier range.
Output: Edits in 2 source files, 1 new test module, 4 audit rows flipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/74-qramm-compliance-warnings/74-CONTEXT.md
@.planning/phases/74-qramm-compliance-warnings/74-RESEARCH.md
@.planning/audit-2026-05-08/AUDIT-TASKS.md

<interfaces>
Existing surfaces (verified):

quirk/qramm/scoring.py:
- `compute_practice_score(answers: List[int]) -> float` at line 20 (D-01 fix site; add `practice_id: str = ""` param)
- `compute_overall_score(dims: Dict[str, float], multiplier: float = 1.0) -> Dict[str, Any]` at line 42 (clamp at line 56 `min(4.0, ...)` — leave as-is per RESEARCH C-2 / user input)
- `_maturity_label(score: float) -> str` at line 66 (D-03 'Indeterminate' arm + D-04 >= 3.95 threshold both land here; widen signature to `float | None`)

quirk/qramm/evidence_bridge.py:
- `populate_cvi_suggestions(session_id: int, db: Session) -> None` at line 35
- `total_endpoints = len(endpoints)` at line 59 (D-02 source)
- Practice 1.1 block at lines 78-87 (D-02 multiply site)
- vuln_pct block at lines 89-98 (D-03 sentinel site; trigger condition is `total_algos == 0` based on classification, not `total_endpoints == 0`)

Target shape (compute_practice_score, D-01):
```
def compute_practice_score(answers: List[int], practice_id: str = "") -> float:
    for answer in answers:
        if answer not in (0, 1, 2, 3, 4):
            raise ValueError(
                f"Practice score answer {answer!r} for {practice_id} out of range [0, 4]"
            )
    if not answers:
        return 0.0
    return round(sum(answers) / len(answers), 4)
```

Target shape (Practice 1.1 endpoint factor, D-02):
```
import math  # add to top-of-file imports if missing
# existing block at lines 78-87 ends with score_1_1 ∈ {1,2,3,4}
discovery_factor = min(1.0, max(0.25, math.log10(max(total_endpoints, 1)) / 3.0))
score_1_1 = round(score_1_1 * discovery_factor, 4)
```

Target shape (_maturity_label, D-03 + D-04):
```
def _maturity_label(score: float | None) -> str:
    if score is None:
        return "Indeterminate"
    if score >= 3.95:
        return "Optimizing"
    # remaining bands unchanged
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing tests for D-01..D-04 in tests/test_qramm_practice_scoring.py</name>
  <files>tests/test_qramm_practice_scoring.py</files>
  <read_first>
    - quirk/qramm/scoring.py (full file — ~85 lines)
    - quirk/qramm/evidence_bridge.py lines 1-130
    - tests/test_qramm_staleness.py (parametrized-test idiom reference)
    - .planning/phases/74-qramm-compliance-warnings/74-CONTEXT.md (D-01..D-04)
    - .planning/phases/74-qramm-compliance-warnings/74-RESEARCH.md (Pattern 3, Pitfalls 1 + 2)
  </read_first>
  <behavior>
    - D-01 (WR-02) — `compute_practice_score([5], practice_id="1.1")` raises `ValueError` matching `r"5.*for 1\.1 out of range \[0, 4\]"`
    - D-01 — `compute_practice_score([-1])` raises `ValueError`
    - D-01 — `compute_practice_score([0, 1, 2, 3, 4])` returns `2.0` (no raise; 0 is now valid)
    - D-01 — `compute_practice_score([])` returns `0.0` (empty list path preserved)
    - D-02 (WR-04) — parametrized discovery_factor curve: `total_endpoints=1` → factor==0.25; `total_endpoints=10` → factor≈0.333; `total_endpoints=100` → factor≈0.667; `total_endpoints=1000` → factor==1.0; `total_endpoints=0` → factor==0.25 (the `max(endpoint_count, 1)` floor). Asserts `score_1_1 == round(raw * factor, 4)`. May test via direct invocation of a helper or by stubbing the bridge — researcher discretion; preferred path is unit-testing `discovery_factor` computation inline via a small private helper extracted in Task 2 OR computing the expected value in-test from the public score output.
    - D-03 (WR-05) — `_maturity_label(None) == "Indeterminate"`
    - D-03 — `_maturity_label(2.5) == "Defined"` (or whatever the existing band returns — verify against current bands; assertion is "label is NOT 'Indeterminate' for non-None input")
    - D-04 (WR-06) — parametrized `_maturity_label(3.99) == "Optimizing"` AND `_maturity_label(4.0) == "Optimizing"` (both top band)
    - D-04 (negative) — `_maturity_label(3.94) != "Optimizing"` (boundary respected)
  </behavior>
  <action>
    Create `tests/test_qramm_practice_scoring.py`. Import `from quirk.qramm.scoring import compute_practice_score, _maturity_label`. Use `pytest.raises(ValueError, match=...)` for D-01; use `pytest.mark.parametrize` for D-02 curve and D-04 boundary.

    For D-02 (Practice 1.1 endpoint factor): since the multiplication happens inline in `evidence_bridge.py`, write the test in one of two ways — choose whichever the executor finds cleaner during read_first:
    (a) Extract a helper `_discovery_factor(endpoint_count: int) -> float` in `evidence_bridge.py` during Task 2 and unit-test it directly here. PREFERRED.
    (b) Stub the surrounding bridge call (mock the DB session) and assert the persisted `suggested_answer` for practice "1.1" matches `round(raw_score * expected_factor, 4)`.

    If (a) is chosen, this RED-phase test imports `_discovery_factor` and asserts the curve values; the import will fail until Task 2 lands — that IS the RED state.

    Commit this test module as the RED commit: `test(74-01): add failing tests for QWARN-01 (D-01..D-04)`. Run `pytest tests/test_qramm_practice_scoring.py -x` and confirm failure; capture failure output in the commit body if helpful.

    PEP 8. Match the parametrize idiom from existing `tests/test_qramm_staleness.py`.
  </action>
  <verify>
    <automated>pytest tests/test_qramm_practice_scoring.py -x 2>&1 | grep -E "FAILED|ERROR|ImportError" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_qramm_practice_scoring.py` exists
    - `grep -cE "^def test_" tests/test_qramm_practice_scoring.py` >= 8
    - `grep -cE "Indeterminate" tests/test_qramm_practice_scoring.py` >= 1
    - `grep -cE "out of range" tests/test_qramm_practice_scoring.py` >= 1
    - `grep -cE "discovery_factor|_discovery_factor" tests/test_qramm_practice_scoring.py` >= 1
    - `grep -cE "3\.95|3\.99|4\.0" tests/test_qramm_practice_scoring.py` >= 2
    - `pytest tests/test_qramm_practice_scoring.py -x` exits NON-ZERO (RED state — tests fail because implementation does not yet exist)
  </acceptance_criteria>
  <done>
    Failing tests committed; pytest exits non-zero with assertion / import errors clearly tied to D-01..D-04.
  </done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement D-01..D-04 in scoring.py + evidence_bridge.py</name>
  <files>quirk/qramm/scoring.py, quirk/qramm/evidence_bridge.py</files>
  <read_first>
    - quirk/qramm/scoring.py (full file)
    - quirk/qramm/evidence_bridge.py lines 1-130
    - tests/test_qramm_practice_scoring.py (post-Task 1 — to confirm expected helper name)
  </read_first>
  <action>
    Apply four surgical edits per `<interfaces>` target shapes:

    **scoring.py edits:**
    1. (D-01) Update `compute_practice_score` signature to `(answers: List[int], practice_id: str = "") -> float`. Add the for-loop ValueError BEFORE `if not answers:`. Format string MUST exactly match D-01: `f"Practice score answer {answer!r} for {practice_id} out of range [0, 4]"`.
    2. (D-03 + D-04) Widen `_maturity_label` signature to `score: float | None`. Add leading arm `if score is None: return "Indeterminate"`. Change `if score >= 4.0:` (line 67) to `if score >= 3.95:`. Leave the remaining four band thresholds untouched (D-14 protects the 5-band scale itself).

    **evidence_bridge.py edits:**
    3. (D-02) Add `import math` at top-of-file (verify it's not already imported via grep first). After the existing Practice 1.1 block at lines 78-87, add a helper invocation that multiplies `score_1_1`:
       ```python
       def _discovery_factor(endpoint_count: int) -> float:
           """D-02: scale Practice 1.1 by log10(endpoint_count)/3 in [0.25, 1.0]."""
           return min(1.0, max(0.25, math.log10(max(endpoint_count, 1)) / 3.0))
       ```
       Extract `_discovery_factor` as a module-private helper near the top of the module (after imports, before `populate_cvi_suggestions`). Then at the Practice 1.1 block, append:
       ```python
       score_1_1 = round(score_1_1 * _discovery_factor(total_endpoints), 4)
       ```
       This matches the unit-testable shape RED Task 1 assumed.

    4. (D-03) Locate where `total_algos` / `algorithm_set` is computed in the bridge (researcher verifies — likely between Practice 1.1 and the vuln_pct block at lines 89-98). At the vuln_pct block: when `total_algos == 0`, set `vuln_pct = None` instead of computing the ratio. The downstream `score_1_2` derivation must handle `None` — set `score_1_2 = None` in that branch and propagate the sentinel through `practice_scores`. Downstream `_maturity_label(None)` returns `"Indeterminate"` (Task already covers this in scoring.py).

    Per D-14: do NOT touch `questions.py`, do NOT modify the 5-band thresholds other than the >= 4.0 → >= 3.95 change, do NOT touch `tests/test_compliance_freshness.py`.

    PEP 8. Minimal diff.

    Commit message: `feat(74-01): implement QWARN-01 fixes (D-01 ValueError, D-02 discovery_factor, D-03 None sentinel, D-04 >= 3.95)`.
  </action>
  <verify>
    <automated>python -m compileall quirk/qramm/scoring.py quirk/qramm/evidence_bridge.py && pytest tests/test_qramm_practice_scoring.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -m compileall quirk/qramm/scoring.py quirk/qramm/evidence_bridge.py` exits 0
    - `grep -cE "out of range \[0, 4\]" quirk/qramm/scoring.py` >= 1
    - `grep -cE "practice_id" quirk/qramm/scoring.py` >= 1
    - `grep -cE "Indeterminate" quirk/qramm/scoring.py` >= 1
    - `grep -cE "score >= 3\.95" quirk/qramm/scoring.py` == 1
    - `grep -v '^#' quirk/qramm/scoring.py | grep -cE "score >= 4\.0" ` == 0 (old threshold gone)
    - `grep -cE "_discovery_factor|discovery_factor" quirk/qramm/evidence_bridge.py` >= 2
    - `grep -cE "^import math" quirk/qramm/evidence_bridge.py` >= 1
    - `grep -cE "vuln_pct = None" quirk/qramm/evidence_bridge.py` >= 1
    - `pytest tests/test_qramm_practice_scoring.py -x` exits 0 (GREEN)
    - `pytest tests/test_qramm_staleness.py -x` exits 0 (no collateral regression)
  </acceptance_criteria>
  <done>
    All Task 1 tests pass; no other QRAMM tests regress.
  </done>
</task>

<task type="auto">
  <name>Task 3: Flip WR-02, WR-04, WR-05, WR-06 audit rows</name>
  <files>.planning/audit-2026-05-08/AUDIT-TASKS.md</files>
  <read_first>
    - .planning/audit-2026-05-08/AUDIT-TASKS.md lines 124-136 (qramm-compliance WR rows)
  </read_first>
  <action>
    Update `.planning/audit-2026-05-08/AUDIT-TASKS.md`, flipping these four rows from `| — | [ ] open |` to `| Phase 74 | [x] closed |`:
    - Line 125 (WR-02): compute_practice_score accepts out-of-range answers
    - Line 127 (WR-04): Practice 1.1 Discovery score ignores endpoint count entirely
    - Line 128 (WR-05): vuln_pct unbounded division — zero algos scores 4 (Advanced)
    - Line 129 (WR-06): Maturity label >=4.0 unreachable at multiplier=1.0 (FP noise)

    No other rows touched. Per-row evidence summaries land in the Plan SUMMARY, not the audit row body. Single docs commit.

    Commit message: `docs(74-01): close qramm-compliance WR-02/WR-04/WR-05/WR-06 in audit ledger`.
  </action>
  <verify>
    <automated>grep -cE "qramm-compliance/WR-(02|04|05|06).*Phase 74.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "qramm-compliance/WR-(02|04|05|06).*Phase 74.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 4
    - `grep -cE "qramm-compliance/WR-(01|03|07|08|09|10|11|12|13).*\[ \] open" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 9 (other qramm-compliance rows untouched — closed by 74-02 and 74-03)
  </acceptance_criteria>
  <done>
    AUDIT-TASKS.md shows WR-02, WR-04, WR-05, WR-06 closed under Phase 74; remaining 9 rows still open pending 74-02/74-03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller-supplied answer list → `compute_practice_score` | Untrusted integer input crosses into the scoring engine |
| Empty-scan state → maturity label rendering | Zero-algo result must not masquerade as `Advanced` maturity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | Tampering (score integrity) | `compute_practice_score` accepting out-of-range answers (5, -1, 99) | mitigate | Fail-loud `ValueError` BEFORE summation per D-01; consistent with Phase 70 `_SAFE_COL_TYPE_RE` pattern |
| T-74-02 | Spoofing (false maturity claim) | Zero-endpoint scan claiming `Advanced` Discovery via static `score_1_1` | mitigate | Multiply by `_discovery_factor(total_endpoints)` per D-02 — single-endpoint floor 0.25 |
| T-74-03 | Spoofing (false maturity claim) | Zero-algo scan computing `vuln_pct = 0` → top band | mitigate | `vuln_pct = None` sentinel + `"Indeterminate"` label per D-03 |
| T-74-04 | Repudiation (silent FP-noise band drift) | `>= 4.0` threshold sometimes unreachable due to multiplier rounding | mitigate | Lower threshold to `>= 3.95` per D-04 path (a) |
</threat_model>

<verification>
- `python -m compileall quirk/qramm/scoring.py quirk/qramm/evidence_bridge.py` clean
- `pytest tests/test_qramm_practice_scoring.py -x` passes
- `pytest tests/test_qramm_staleness.py tests/test_compliance_freshness.py -x` passes (no collateral regression on existing QRAMM/compliance tests)
- AUDIT-TASKS.md WR-02, WR-04, WR-05, WR-06 rows show `Phase 74 | [x] closed`
- No collateral changes to `questions.py` or the 5-band scale per D-14
</verification>

<success_criteria>
- `compute_practice_score` raises `ValueError` on out-of-range with the D-01 message format
- Practice 1.1 score scaled by `_discovery_factor(total_endpoints)` log10 curve
- `vuln_pct = None` when `total_algos == 0`; `_maturity_label(None) == "Indeterminate"`
- `_maturity_label(3.99) == _maturity_label(4.0) == "Optimizing"`
- 8+ tests pass in `tests/test_qramm_practice_scoring.py`
- WR-02, WR-04, WR-05, WR-06 closed in AUDIT-TASKS.md
</success_criteria>

<output>
Create `.planning/phases/74-qramm-compliance-warnings/74-01-SUMMARY.md` documenting: D-02 `total_endpoints` source choice (per RESEARCH C-1 + user input), D-04 path (a) threshold-only fix (4.0 already reachable at multiplier=1.0 per Pitfall 2), four audit rows flipped (WR-02, WR-04, WR-05, WR-06), test count, RED→GREEN commit cadence.
</output>
