---
phase: 35-cbom-integration
plan: 04
type: execute
wave: 4
depends_on: ["35-03"]
files_modified:
  - .planning/REQUIREMENTS.md
  - docs/UAT-SERIES.md
autonomous: true
requirements:
  - CBOM-01
  - CBOM-03
must_haves:
  truths:
    - "REQUIREMENTS.md CBOM-01 wording enumerates all 6 email TLS labels (SMTP-STARTTLS, SMTPS, IMAP-STARTTLS, IMAPS, POP3-STARTTLS, POP3S) — no longer lists only 4 (D-02 + D-06)."
    - "REQUIREMENTS.md CBOM-03 wording uses AMQP-PLAIN (not AMQP) (D-01 + D-06)."
    - "docs/UAT-SERIES.md gains UAT-35-01 (golden email CBOM matches snapshot), UAT-35-02 (golden broker CBOM matches snapshot), UAT-35-03 (no hollow cert components for plaintext brokers) per D-07."
    - "docs/UAT-SERIES.md header date and version-string commentary updated to reflect Phase 35 wrap (per CLAUDE.md mandatory step 2)."
    - "Obsidian phase note exists at /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-35-CBOM-Integration.md with frontmatter status=complete (D-08 + memory carry-forward)."
    - "Obsidian UAT-Series.md vault copy synced to match docs/UAT-SERIES.md (per CLAUDE.md mandatory step 3)."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated CBOM-01 + CBOM-03 wording matching code ground truth"
      contains: "AMQP-PLAIN"
    - path: "docs/UAT-SERIES.md"
      provides: "UAT-35-01 / UAT-35-02 / UAT-35-03 entries + updated header date"
      contains: "UAT-35-01"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-35-CBOM-Integration.md"
      provides: "Obsidian phase note with goal, requirements covered, success criteria, what was built"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
      provides: "Vault-synced copy of docs/UAT-SERIES.md"
  key_links:
    - from: ".planning/REQUIREMENTS.md CBOM-01"
      to: "quirk/scanner/email_scanner.py EMAIL_PORTS table (lines 76-88)"
      via: "literal label enumeration matches scanner-emitted ep.protocol values"
      pattern: "POP3-STARTTLS"
    - from: ".planning/REQUIREMENTS.md CBOM-03"
      to: "quirk/cbom/builder.py Pass 2/3 skip tuples"
      via: "literal label match — REQUIREMENTS wording matches code"
      pattern: "AMQP-PLAIN"
    - from: "Obsidian Phase-35-CBOM-Integration.md"
      to: ".planning/phases/35-cbom-integration/35-0[123]-SUMMARY.md"
      via: "What Was Built section sourced from each plan's SUMMARY"
      pattern: "## What Was Built"
---

<objective>
Close Phase 35 per CLAUDE.md mandatory phase-completion steps and per D-06/D-07/D-08 in
35-CONTEXT.md. Three documentation surfaces are updated: REQUIREMENTS.md (wording aligned
to code), docs/UAT-SERIES.md (3 new test cases + header refresh), and the Obsidian vault
(phase note + UAT-Series mirror).

Purpose: Honor the user's locked decision (memory carry-forward) that every phase plan set
MUST include explicit documentation update tasks AND Obsidian sync tasks. This is the
phase-close artifact bundle — not optional, not deferred.

Output: 2 repo file edits + 2 Obsidian vault writes + 1 commit per CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/REQUIREMENTS.md
@docs/UAT-SERIES.md
@.planning/phases/35-cbom-integration/35-CONTEXT.md
@.planning/phases/35-cbom-integration/35-01-SUMMARY.md
@.planning/phases/35-cbom-integration/35-02-SUMMARY.md
@.planning/phases/35-cbom-integration/35-03-SUMMARY.md

<interfaces>
REQUIREMENTS.md CBOM-01 (current text, line ~115):
"Email endpoints produce Pass 1 algorithm component entries (cert pubkey algorithm,
negotiated TLS cipher suite) via the existing `cbom/classifier.py` + `cbom/builder.py`
pipeline. No changes to `builder.py` required — `ep.protocol` label (`"SMTP-STARTTLS"`,
`"SMTPS"`, `"IMAPS"`, `"POP3S"`) distinguishes email from HTTPS endpoints."
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                lists only 4 — must enumerate all 6 (add IMAP-STARTTLS, POP3-STARTTLS)

REQUIREMENTS.md CBOM-03 (current text, line ~117):
"Plaintext-only broker endpoints (`"AMQP"`, `"KAFKA-PLAIN"`, `"REDIS-PLAIN"`) added to
                                  ^^^^^^^^
                                  must be "AMQP-PLAIN" — code is ground truth (D-01)
Pass 2 and Pass 3 skip lists in `builder.py` to prevent hollow `CertificateProperties`
entries for endpoints with no TLS cert."

Obsidian phase note path (per CLAUDE.md + memory):
/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-35-CBOM-Integration.md

Obsidian UAT-Series mirror path (per CLAUDE.md mandatory step 3):
/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md
(write via filesystem cp — file too large for obsidian CLI content= argument)

Phase note frontmatter (per CLAUDE.md frontmatter standards):
---
project: QU.I.R.K.
type: phase
status: complete
source: .planning/phases/35-cbom-integration/
updated: <YYYY-MM-DD>
---
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md (CBOM-01 + CBOM-03) and docs/UAT-SERIES.md (3 new cases + header)</name>
  <files>.planning/REQUIREMENTS.md, docs/UAT-SERIES.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (entire CBOM Integration section, lines ~113-118)
    - docs/UAT-SERIES.md (read header lines 1-30 + locate the most recent UAT-34 series
      to find the insertion point; then read the UAT-34 entries to copy the format)
    - .planning/phases/35-cbom-integration/35-CONTEXT.md D-01 + D-02 + D-06 + D-07
    - tests/test_cbom_motion_golden.py (to confirm UAT pass criteria reference the
      correct test names from Plan 03)
  </read_first>
  <action>
    PART A — `.planning/REQUIREMENTS.md` edits:

    1. CBOM-01 — replace `"SMTP-STARTTLS"`, `"SMTPS"`, `"IMAPS"`, `"POP3S"`)` with
       `"SMTP-STARTTLS"`, `"SMTPS"`, `"IMAP-STARTTLS"`, `"IMAPS"`, `"POP3-STARTTLS"`, `"POP3S"`)`
       — enumerate all 6 labels per D-02.

    2. CBOM-03 — replace `"AMQP"` with `"AMQP-PLAIN"` per D-01. Final text:
       `Plaintext-only broker endpoints (`"KAFKA-PLAIN"`, `"AMQP-PLAIN"`, `"REDIS-PLAIN"`) added to Pass 2 and Pass 3 skip lists in `builder.py` to prevent hollow `CertificateProperties` entries for endpoints with no TLS cert.`

    3. In the Traceability table at the bottom, set CBOM-01..CBOM-04 status to "Complete"
       (currently "Pending").

    PART B — `docs/UAT-SERIES.md` edits:

    1. Append three test cases in the appropriate series (the existing convention is
       UAT-34-01..03 for motion intelligence — UAT-35-01..03 follows in the same series
       block; use the format from UAT-34-01..03):

       ```
       ---

       ID: UAT-35-01
       Title: Golden email CBOM matches committed snapshot
       Prerequisites: Phase 35 merged; tests/fixtures/cbom/expected_email_cbom.json present
       Steps:
         1. Run `python -m pytest tests/test_cbom_motion_golden.py::test_email_cbom_matches_snapshot -v`
       Expected: Test passes — normalized CBOM emitted from the 7-endpoint email lab
                 fixture matches the committed JSON snapshot exactly.
       Pass Criteria: Exit code 0; the test reports 1 PASSED; if a divergence is reported,
                      the divergence is intentional (scanner/builder change) and the snapshot
                      has been regenerated via REGEN_CBOM_FIXTURES=1 and re-committed.

       ---

       ID: UAT-35-02
       Title: Golden broker CBOM matches committed snapshot
       Prerequisites: Phase 35 merged; tests/fixtures/cbom/expected_broker_cbom.json present
       Steps:
         1. Run `python -m pytest tests/test_cbom_motion_golden.py::test_broker_cbom_matches_snapshot -v`
       Expected: Test passes — 6-endpoint broker lab CBOM (3 TLS + 3 plaintext) matches
                 the committed snapshot.
       Pass Criteria: Exit code 0; AMQPS/Azure-ServiceBus passthrough verified by
                      test_amqps_azure_servicebus_protocol_component_present in same suite.

       ---

       ID: UAT-35-03
       Title: No hollow cert components for plaintext brokers
       Prerequisites: Phase 35 merged
       Steps:
         1. Run `python -m pytest tests/test_cbom_motion_golden.py::test_no_certificate_components_for_plaintext_brokers tests/test_cbom_motion_golden.py::test_no_tls_protocol_components_for_plaintext_brokers -v`
         2. Inspect tests/fixtures/cbom/expected_broker_cbom.json — grep should find
            zero `crypto/certificate/localhost:29092`, `:25672`, `:26379` and zero
            `crypto/protocol/tls/localhost:29092`, `:25672`, `:26379` entries.
       Expected: Both tests pass; grep returns no matches for plaintext-port hollow refs.
       Pass Criteria: Both tests exit 0; no plaintext-port bom_refs in the broker snapshot.
       ```

    2. Update the `**Last Updated:**` line at the top to today's date with a brief
       Phase 35 note appended to the existing commentary, following the established
       prepend-most-recent style:
       `**Last Updated:** <YYYY-MM-DD> (Phase 35 wrap: UAT-35-01..03 added for CBOM
       integration — golden email + broker CBOM snapshots assert the 6 email TLS
       labels, 4 broker TLS labels including AMQPS/Azure-ServiceBus passthrough, and
       3 plaintext broker labels (KAFKA-PLAIN/AMQP-PLAIN/REDIS-PLAIN) skipped from
       Pass 2 + Pass 3 of build_cbom(). Earlier: <previous commentary preserved verbatim>)`

    3. Do NOT bump the document version string — Phase 37 owns the v4.4.0 cut.
  </action>
  <verify>
    <automated>grep -c "AMQP-PLAIN" .planning/REQUIREMENTS.md && grep -c "POP3-STARTTLS" .planning/REQUIREMENTS.md && grep -c "UAT-35-01" docs/UAT-SERIES.md && grep -c "UAT-35-02" docs/UAT-SERIES.md && grep -c "UAT-35-03" docs/UAT-SERIES.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AMQP-PLAIN" .planning/REQUIREMENTS.md` returns >= 1 (was 0 — code label now in spec)
    - `grep -c "\"AMQP\"," .planning/REQUIREMENTS.md` returns 0 in the CBOM-03 line
      (verify the bare `"AMQP"` token in the CBOM-03 sentence is gone — careful: other
      sections like RABBIT-04 may legitimately reference AMQP elsewhere; scope grep to
      line range or use sed-style precise check on CBOM-03 line)
    - `grep -c "IMAP-STARTTLS\|POP3-STARTTLS" .planning/REQUIREMENTS.md` returns >= 2
      after CBOM-01 enumerates all 6 labels (was previously absent in CBOM-01 line)
    - Traceability table shows `| CBOM-01 | 35 | Complete |` (and CBOM-02..CBOM-04)
    - `grep -c "UAT-35-01" docs/UAT-SERIES.md` returns 1
    - `grep -c "UAT-35-02" docs/UAT-SERIES.md` returns 1
    - `grep -c "UAT-35-03" docs/UAT-SERIES.md` returns 1
    - First line of docs/UAT-SERIES.md matches `# QU.I.R.K. — UAT Test Series (Gating Document)`
    - `**Last Updated:**` line of docs/UAT-SERIES.md mentions "Phase 35 wrap" or "UAT-35"
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md wording matches code ground truth (CBOM-01 = 6 labels, CBOM-03 = AMQP-PLAIN);
    docs/UAT-SERIES.md has 3 new test cases following the established UAT-34 format and an
    updated header date / commentary.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Obsidian phase note + sync UAT-Series mirror to vault</name>
  <files>
    /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-35-CBOM-Integration.md,
    /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md
  </files>
  <read_first>
    - CLAUDE.md (Mandatory Phase Completion Steps section + Obsidian Vault Integration)
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-34-Motion-Intelligence.md
      (template pattern — read for structure: frontmatter + Goal + Requirements Covered +
      Success Criteria + What Was Built + [[Roadmap]] link)
    - .planning/phases/35-cbom-integration/35-01-SUMMARY.md
    - .planning/phases/35-cbom-integration/35-02-SUMMARY.md
    - .planning/phases/35-cbom-integration/35-03-SUMMARY.md
    - .planning/ROADMAP.md (Phase 35 entry — copy goal + 4 success criteria verbatim)
  </read_first>
  <action>
    Write the phase note DIRECTLY to the vault filesystem using the Write tool — do NOT
    use `obsidian` CLI's `content=` argument (file may be too large for shell expansion
    once SUMMARY content is included, per CLAUDE.md mandatory step 1 caveat).

    PART A — Create `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-35-CBOM-Integration.md`:

    ```markdown
    ---
    project: QU.I.R.K.
    type: phase
    status: complete
    source: .planning/phases/35-cbom-integration/
    updated: <today's YYYY-MM-DD>
    ---

    # Phase 35: CBOM Integration

    ## Goal

    <copy verbatim from ROADMAP.md Phase 35 Goal line>

    ## Requirements Covered

    - **CBOM-01** — Email endpoints produce Pass 1 algorithm components for all 6 TLS labels
      (SMTP-STARTTLS, SMTPS, IMAP-STARTTLS, IMAPS, POP3-STARTTLS, POP3S).
    - **CBOM-02** — Broker TLS endpoints produce Pass 1 algorithm components for AMQPS,
      AMQPS/Azure-ServiceBus, KAFKA-TLS, REDIS-TLS.
    - **CBOM-03** — Plaintext-only broker endpoints (KAFKA-PLAIN, AMQP-PLAIN, REDIS-PLAIN)
      added to Pass 2 + Pass 3 skip lists — no hollow CertificateProperties.
    - **CBOM-04** — Quantum-safety classification verified for representative cipher suites
      (TLS_RSA_WITH_AES_128_CBC_SHA → quantum-vulnerable HIGH; TLS_AES_256_GCM_SHA384 →
      AES level 1, SHA-384 level 2).

    ## Success Criteria

    <copy the 4 numbered success criteria verbatim from ROADMAP.md Phase 35 entry>

    ## What Was Built

    ### Plan 35-01 — RED synthesized-endpoint tests
    <one-paragraph summary sourced from .planning/phases/35-cbom-integration/35-01-SUMMARY.md>

    ### Plan 35-02 — GREEN: skip-list addition in builder.py
    <one-paragraph summary sourced from 35-02-SUMMARY.md>

    ### Plan 35-03 — Golden CBOM snapshots
    <one-paragraph summary sourced from 35-03-SUMMARY.md>

    ### Plan 35-04 — Documentation + Obsidian sync
    REQUIREMENTS.md wording aligned to code (CBOM-01 enumerates all 6 email labels;
    CBOM-03 uses AMQP-PLAIN). docs/UAT-SERIES.md gains UAT-35-01..03. This phase note
    created and finalized.

    ## Links

    - [[Roadmap]]
    - [[Requirements]]
    - [[UAT-Series]]
    ```

    PART B — Sync UAT-Series.md to vault (file too large for obsidian CLI content= per
    CLAUDE.md mandatory step 3):

    ```bash
    printf "---\nproject: QU.I.R.K.\ntype: reference\nstatus: active\nsource: docs/UAT-SERIES.md\nupdated: <today YYYY-MM-DD>\n---\n\n" > /tmp/uat_vault_35.md
    cat docs/UAT-SERIES.md >> /tmp/uat_vault_35.md
    cp /tmp/uat_vault_35.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
    ```

    Substitute the literal date for `<today YYYY-MM-DD>` (use the value of `currentDate`
    from the working session — currently 2026-04-28; if running on a later date use that).
  </action>
  <verify>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-35-CBOM-Integration.md" && test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" && grep -c "status: complete" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-35-CBOM-Integration.md" && grep -c "UAT-35-01" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-35-CBOM-Integration.md`
    - File contains frontmatter `status: complete` and `type: phase`
    - File contains all 4 plan subsections under "## What Was Built"
    - File ends with `[[Roadmap]]` link
    - File exists: `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md`
    - Vault UAT-Series.md contains `UAT-35-01`, `UAT-35-02`, `UAT-35-03`
    - Vault UAT-Series.md frontmatter has `source: docs/UAT-SERIES.md`
  </acceptance_criteria>
  <done>
    Obsidian phase note exists at the canonical path with status=complete and a What Was
    Built section sourced from each of the 4 plan SUMMARYs; the vault UAT-Series.md mirror
    matches the repo's docs/UAT-SERIES.md plus the required vault frontmatter.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit docs/UAT-SERIES.md per CLAUDE.md mandatory step 4</name>
  <files>(commit only — no new file content)</files>
  <read_first>
    - CLAUDE.md (Mandatory Phase Completion Steps — section 4)
  </read_first>
  <action>
    Stage and commit `docs/UAT-SERIES.md` as a standalone documentation commit per
    CLAUDE.md step 4. Use the gsd-tools commit helper exactly as documented:

    ```bash
    node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit \
      "docs(phase-35): update UAT-SERIES.md" --files docs/UAT-SERIES.md
    ```

    Do NOT amend prior commits. Do NOT include other files in this commit (REQUIREMENTS.md
    edits and SUMMARY.md files belong to their own respective commits handled by the
    standard execute-plan close flow + gsd-tools, not this manual step).
  </action>
  <verify>
    <automated>git log -1 --oneline -- docs/UAT-SERIES.md | head -1 | grep -E "phase-35|35:"</automated>
  </verify>
  <acceptance_criteria>
    - Most recent commit touching docs/UAT-SERIES.md has subject containing "phase-35" or "35:"
    - Working tree shows docs/UAT-SERIES.md is clean (`git status --porcelain docs/UAT-SERIES.md`
      returns empty string)
  </acceptance_criteria>
  <done>
    docs/UAT-SERIES.md committed as a discrete `docs(phase-35): update UAT-SERIES.md`
    commit per CLAUDE.md mandatory step 4.
  </done>
</task>

</tasks>

<verification>
- `grep -c "AMQP-PLAIN" .planning/REQUIREMENTS.md` >= 1
- `grep -c "POP3-STARTTLS" .planning/REQUIREMENTS.md` >= 1 (CBOM-01 enumerates all 6)
- Traceability table reports CBOM-01..CBOM-04 as Complete
- `grep -c "UAT-35-0[123]" docs/UAT-SERIES.md` returns 3
- Obsidian phase note exists with status=complete
- Vault UAT-Series.md mirror exists and contains UAT-35-01..03
- `git log -1 --oneline -- docs/UAT-SERIES.md` shows the phase-35 commit
</verification>

<success_criteria>
- D-06 satisfied: REQUIREMENTS.md wording aligned to code (CBOM-01 + CBOM-03)
- D-07 satisfied: UAT-35-01..03 added to docs/UAT-SERIES.md
- D-08 satisfied: Obsidian phase note + UAT-Series vault sync complete
- CLAUDE.md mandatory phase-completion steps 1-4 all executed
- Phase 35 ready for the standard close-phase wrap (SUMMARY.md per plan + STATE.md update
  via the standard execute-phase close flow — those are owned by the orchestrator, not
  by this plan)
</success_criteria>

<output>
After completion, create `.planning/phases/35-cbom-integration/35-04-SUMMARY.md`
</output>
