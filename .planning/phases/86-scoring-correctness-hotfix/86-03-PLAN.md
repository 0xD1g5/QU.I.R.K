---
phase: 86-scoring-correctness-hotfix
plan: 03
type: execute
wave: 3
depends_on:
  - 86-01
  - 86-02
files_modified:
  - pyproject.toml
  - changelog.d/v4.10.1.bugfix.md
  - .planning/phases/86-scoring-correctness-hotfix/HUMAN-UAT.md
autonomous: false
requirements:
  - RELEASE-01
  - RELEASE-02
must_haves:
  truths:
    - "pyproject.toml [project.version] reads exactly 4.10.1 (string match, no quotes drift)."
    - "A changelog.d/ fragment exists describing the scoring-correctness fix in operator language."
    - "The changelog fragment includes the canonical 25+25+23+3+25+19 before-and-after example (100 → 80)."
    - "The changelog fragment notes that old stored scores will display lower after upgrade; underlying penalty math unchanged."
    - "A scan against the tls-cert-defects chaos lab profile, rendered in the dashboard via quirk serve, shows overall < 100 with a non-EXCELLENT rating."
    - "All six subscore radials on the executive page AND the standalone Data at Rest tab gauge render colors matching their fraction of 25 (red <50%, amber 50-79%, green >=80%)."
    - "HUMAN-UAT.md documents the manual walkthrough with operator-confirmed pass criteria + screenshot path."
  artifacts:
    - path: "pyproject.toml"
      provides: "Version SoT bumped to 4.10.1"
      contains: "version = \"4.10.1\""
    - path: "changelog.d/v4.10.1.bugfix.md"
      provides: "Towncrier news fragment for the scoring-correctness fix"
      contains: "25+25+23+3+25+19"
    - path: ".planning/phases/86-scoring-correctness-hotfix/HUMAN-UAT.md"
      provides: "Operator-led UAT walkthrough + signed pass record"
      contains: "tls-cert-defects"
  key_links:
    - from: "pyproject.toml version"
      to: "changelog.d/ fragment naming convention"
      via: "v4.10.1.<type>.md pattern matching the v4.10.feature.md precedent"
      pattern: "v4\\.10\\.1\\..*\\.md"
    - from: "HUMAN-UAT.md pass record"
      to: "dashboard at http://127.0.0.1:8765 (quirk serve)"
      via: "operator visually confirms colors + numeric values on a real scan"
      pattern: "tls-cert-defects"
---

<objective>
Land the release-engineering artifacts and run the operator-led UAT that closes Phase 86. Bump `pyproject.toml` to `4.10.1`, add a towncrier news fragment that documents the fix in operator language with the canonical before/after example and the visual-jump caveat, then walk through a live scan-and-render against the `tls-cert-defects` chaos lab profile to confirm the dashboard reflects the corrected math end-to-end (Executive Summary page + standalone Data at Rest tab).

Purpose: Plan 01 fixed the math; Plan 02 fixed the rendering on every real ScoreGauge caller (six executive-page subscores + the Data at Rest tab gauge); this plan packages the fix as a shippable patch release and verifies the full user-visible behavior. Per project memory `feedback_human_uat_walkthrough.md`, HUMAN-UAT is operator-led — Claude prepares the environment, presents the literal steps, and waits for the user's visual confirmation before flipping status.

Output:
- `pyproject.toml` `[project.version]` = `4.10.1` (per D-12)
- `changelog.d/v4.10.1.bugfix.md` operator-facing entry with canonical example + visual-jump note (per D-13)
- `.planning/phases/86-scoring-correctness-hotfix/HUMAN-UAT.md` with pass/fail record + screenshot reference
- Phase 86 mandatory completion steps (Obsidian note, UAT-SERIES.md sync) — handled at the orchestrator's `update_roadmap` step, but this plan includes the UAT prep that those steps depend on.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-scoring-correctness-hotfix/CONTEXT.md
@.planning/phases/86-scoring-correctness-hotfix/86-01-PLAN.md
@.planning/phases/86-scoring-correctness-hotfix/86-02-PLAN.md
@pyproject.toml
@changelog.d/README.md
@changelog.d/v4.10.feature.md

<interfaces>
<!-- Towncrier fragment naming convention, inferred from changelog.d/v4.10.feature.md precedent. -->

Existing pattern: `changelog.d/v<version>.<type>.md` — `v4.10.feature.md` shipped at v4.10.0.
This plan adds: `changelog.d/v4.10.1.bugfix.md` (type = bugfix; if the local towncrier config uses a different type-key such as `fix` or `bug`, match the configured key — check `pyproject.toml [tool.towncrier]` or `changelog.d/README.md` before naming the file).

pyproject.toml current state (line 7): `version = "4.10.0"` → must become `version = "4.10.1"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version bump + changelog fragment</name>
  <files>pyproject.toml, changelog.d/v4.10.1.bugfix.md</files>
  <action>
    Edit `pyproject.toml` line 7 only: change `version = "4.10.0"` to `version = "4.10.1"` per D-12. Do NOT touch any other field. Then inspect `changelog.d/README.md` and `pyproject.toml [tool.towncrier]` (if present) to confirm the configured fragment type-keys; if `bugfix` is not in the configured set use the closest equivalent (`fix` or `bug`) and adjust the filename. Create `changelog.d/v4.10.1.bugfix.md` (or the type-corrected name) with operator-facing prose per D-13:

    - Lead sentence: "Overall readiness no longer caps at 100 on real scans."
    - One-sentence root cause: the previous aggregator summed six 0-25 subscores and clamped at 100, masking real posture issues.
    - Fix description: overall readiness is now `int(round(sum_of_subscores / 1.5))`; dashboard subscore radials now render against `maxValue=25` so a perfect category shows green and a depleted category shows red.
    - Canonical before/after example as a short table or two-line excerpt: subscores 25+25+23+3+25+19 = 120 → before: overall **100 EXCELLENT** / after: overall **80 GOOD**.
    - Closing operator note: "Old stored scores will display lower after upgrade. The underlying per-category penalty math is unchanged — only the aggregation and dashboard scale are corrected. To refresh a stored score, re-render or re-scan."
    - Tone: terse, operator-facing, matching the v4.10.feature.md voice. No marketing language.

    Run `python -m compileall pyproject.toml 2>/dev/null || true` (compileall doesn't apply to toml — instead validate parse) by running `python -c "import tomllib; tomllib.loads(open('pyproject.toml','rb').read().decode())"`. Commit as `release(86-03): bump version to 4.10.1 + scoring-correctness changelog fragment`.
  </action>
  <verify>
    <automated>grep -c '^version = "4.10.1"' pyproject.toml && ls changelog.d/v4.10.1.* && grep -c "25+25+23+3+25+19" changelog.d/v4.10.1.*.md</automated>
  </verify>
  <done>`pyproject.toml` line 7 reads exactly `version = "4.10.1"`. A `changelog.d/v4.10.1.*.md` fragment exists, contains the canonical `25+25+23+3+25+19` example, and includes both the visual-jump note and the "underlying penalty math is unchanged" reassurance. `python -c "import tomllib; tomllib.load(open('pyproject.toml','rb'))"` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Prepare the UAT environment + capture pre-flight evidence</name>
  <files>(no committed edits in this task — environment prep + draft HUMAN-UAT.md scaffold)</files>
  <action>
    Prepare a clean repro environment for the operator UAT:

    1. Confirm Plan 01 + Plan 02 changes are present: `grep -n "int(round" quirk/intelligence/scoring.py | head -3`; `grep -c "maxValue={25}" src/dashboard/src/pages/executive.tsx` returns 6; `grep -c "maxValue={25}" src/dashboard/src/pages/data-at-rest.tsx` returns 1.
    2. Confirm dashboard build is fresh: `ls -lt src/dashboard/dist/ | head -3` and verify mtime is after Plan 02's commit. If stale, run `cd src/dashboard && npm run build`.
    3. Bring up the `tls-cert-defects` chaos lab profile per the existing runbook (`./quantum-chaos-enterprise-lab/lab.sh up` with the `tls-cert-defects` profile). Confirm services are healthy via `./quantum-chaos-enterprise-lab/lab.sh status`.
    4. Run a scan that drives the canonical bug: `quirk scan --profile tls-cert-defects --db /tmp/uat-86.db` (or the project's documented invocation — adapt to the actual CLI). Capture the resulting scan ID.
    5. Use `quirk export-json --db /tmp/uat-86.db --scan-id <id>` and confirm the JSON shows `score.score < 100` and `score.rating != "EXCELLENT"`. Record the actual subscore tuple and the computed overall in the draft HUMAN-UAT.md.
    6. Start `quirk serve --db /tmp/uat-86.db` in the background (or instruct the operator to start it). Confirm the FastAPI process is listening (`curl -sf http://127.0.0.1:8765/api/health` or the documented health endpoint).

    Create `.planning/phases/86-scoring-correctness-hotfix/HUMAN-UAT.md` with:

    - Header (date, scan ID, environment, build hash).
    - Pre-flight evidence block: the CLI export-json output snippet showing the corrected overall + subscores.
    - Four operator-led pass criteria slots (filled in Task 3):
      1. Executive Summary — Overall gauge shows the number from the export-json (e.g., 80) with a non-EXCELLENT rating badge.
      2. Executive Summary — Each of the six subscore radials renders the color the operator expects given that subscore's fraction of 25 (provide the fraction → expected color table).
      3. Data at Rest tab — The standalone Data at Rest ScoreGauge (data-at-rest.tsx:301) renders the same value as the Executive page's Data at Rest subscore radial, and its color matches that value's fraction of 25 (e.g., DAR 25 → green; DAR 10 → amber; DAR 3 → red).
      4. Screenshot saved to `.planning/phases/86-scoring-correctness-hotfix/uat-86-dashboard.png` (operator captures via OS screenshot tool against the live dashboard — should show both the Executive Summary view and the Data at Rest tab, either as one stitched screenshot or two saved files).
    - "Pass / Fail" line left blank for the operator to fill.

    Do NOT commit yet — Task 3 fills in the operator-confirmed results and the same commit captures both.
  </action>
  <verify>
    <automated>grep -c "int(round" quirk/intelligence/scoring.py && grep -c "maxValue={25}" src/dashboard/src/pages/executive.tsx && grep -c "maxValue={25}" src/dashboard/src/pages/data-at-rest.tsx && test -f .planning/phases/86-scoring-correctness-hotfix/HUMAN-UAT.md && curl -sf http://127.0.0.1:8765/ -o /dev/null -w "%{http_code}\n" 2>/dev/null || echo "serve not yet started (operator step)"</automated>
  </verify>
  <done>HUMAN-UAT.md draft exists with header + pre-flight evidence + four pass-criteria slots. CLI `quirk export-json` for the captured scan confirms overall < 100. Dashboard service is reachable on its configured port OR the operator has been given the explicit one-line command to start it.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator walks the live dashboard + signs HUMAN-UAT.md</name>
  <what-built>
    Plan 01 corrected `compute_readiness_score` (sum / 1.5). Plan 02 added `maxValue` to `ScoreGauge` and wired `maxValue={25}` on the six executive-page subscore gauges AND on the standalone Data at Rest tab gauge (data-at-rest.tsx:301). Task 1 of this plan bumped the version to 4.10.1 and added the operator-facing changelog fragment. Task 2 ran a fresh scan against the `tls-cert-defects` chaos lab profile, drafted HUMAN-UAT.md with pre-flight CLI evidence, and brought up `quirk serve`.
  </what-built>
  <how-to-verify>
    1. Open the dashboard executive summary in a browser: `http://127.0.0.1:8765/` (or the port `quirk serve` reported on startup). Navigate to the Executive Summary view for the UAT scan.
    2. Confirm pass criterion 1 — Overall Readiness gauge:
       - The number matches the CLI export-json overall (e.g., 80, captured in Task 2's pre-flight block).
       - The rating badge under or next to the gauge is NOT "EXCELLENT" — it should be GOOD / MODERATE / FAIR / POOR per the `_rating()` thresholds.
       - The arc color reflects the standard 0-100 thresholds (overall keeps `maxValue` default).
    3. Confirm pass criterion 2 — Six subscore radials. For each (Hygiene, Modern TLS, Identity, Agility, Data at Rest, Data in Motion), look at the displayed number, compute `value / 25` mentally, and confirm the color matches:
       - `>= 20` → green
       - `12.5 - 19` → amber
       - `< 12.5` → red
       Example from the canonical bug scan: Hygiene 25 should now be GREEN (was red); Agility 3 should be RED; Data in Motion 19 should be AMBER.
    4. Confirm pass criterion 3 — Navigate to the standalone Data at Rest tab in the sidebar. Confirm the large ScoreGauge at the top of that page (rendered from data-at-rest.tsx:301):
       - Shows the SAME numeric value as the Data at Rest subscore radial on the Executive Summary.
       - Color matches that value's fraction of 25 using the same thresholds (>=20 green, 12.5-19 amber, <12.5 red).
       - If the canonical scan shows Data at Rest = 25, this gauge MUST render green (it was previously red because the gauge interpreted 25 against a 0-100 scale).
    5. Confirm pass criterion 4 — Take a screenshot of the dashboard showing the corrected Executive Summary overall + colored subscores, AND a screenshot of the Data at Rest tab. Save to `.planning/phases/86-scoring-correctness-hotfix/uat-86-dashboard.png` (one stitched image or replace with two files named `uat-86-executive.png` + `uat-86-data-at-rest.png` and reference both in HUMAN-UAT.md).
    6. Fill the four pass/fail slots in `.planning/phases/86-scoring-correctness-hotfix/HUMAN-UAT.md` with your observations and a final `**Result:** PASS` (or FAIL with notes).

    Note on out-of-scope renderers: the CLI HTML report (`quirk/reports/html_renderer.py`) and the PDF report (`quirk/reports/executive.py`) are deferred to v5.0 per D-14 / RENDER-CLI-01 / RENDER-PDF-01. They are NOT in scope for this UAT — do NOT open them and do NOT fail this UAT on their behalf. Mismatch between dashboard and HTML/PDF reports is EXPECTED in v4.10.1 and is tracked in REQUIREMENTS.md Future Requirements.

    If any pass criterion fails, stop and report — do not flip the status. Re-open Plan 01 or 02 as appropriate and re-run.
  </how-to-verify>
  <resume-signal>Reply `approved` (and confirm the screenshot path) OR describe what you saw if anything didn't match — e.g., "subscore Hygiene 25 still rendering amber, not green" or "Data at Rest tab gauge color disagrees with Executive Summary value" — so I can diagnose without you re-typing the whole criterion.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Finalize HUMAN-UAT.md + commit release artifacts</name>
  <files>.planning/phases/86-scoring-correctness-hotfix/HUMAN-UAT.md, .planning/phases/86-scoring-correctness-hotfix/uat-86-dashboard.png</files>
  <action>
    After the operator returns `approved`, ensure `HUMAN-UAT.md` is fully filled (pre-flight evidence, four pass-criterion observations including the Data at Rest tab parity check, screenshot path(s), final `**Result:** PASS`, operator signature line with date 2026-05-22). Verify the screenshot(s) exist at the documented path(s). Commit the UAT record plus the screenshot:

    `node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "release(86-03): v4.10.1 UAT pass — corrected scoring verified on tls-cert-defects scan" --files .planning/phases/86-scoring-correctness-hotfix/HUMAN-UAT.md .planning/phases/86-scoring-correctness-hotfix/uat-86-dashboard.png`

    Do NOT cut a git tag in this plan — tagging is the orchestrator's `update_roadmap` step responsibility for milestone close.
  </action>
  <verify>
    <automated>test -f .planning/phases/86-scoring-correctness-hotfix/HUMAN-UAT.md && grep -c "Result:" .planning/phases/86-scoring-correctness-hotfix/HUMAN-UAT.md && ls .planning/phases/86-scoring-correctness-hotfix/uat-86-*.png 2>/dev/null | head -3 && git log --oneline -1 | grep -E "86-03|v4.10.1"</automated>
  </verify>
  <done>HUMAN-UAT.md contains a `**Result:** PASS` line dated 2026-05-22 with the operator's confirmation of all four pass criteria (Executive overall + six subscores + Data at Rest tab parity + screenshot). At least one `uat-86-*.png` screenshot file exists. Commit lands on `main` referencing the UAT pass.</done>
</task>

</tasks>

<verification>
- `grep '^version = "4.10.1"' pyproject.toml` — exactly one match.
- `ls changelog.d/v4.10.1.*` — at least one fragment file exists.
- `grep -c "25+25+23+3+25+19" changelog.d/v4.10.1.*.md` — at least one match (canonical example present).
- `grep -ci "old stored scores\|visual jump\|display lower" changelog.d/v4.10.1.*.md` — at least one match (visual-jump caveat present).
- `grep -ci "underlying penalty math\|underlying.*unchanged" changelog.d/v4.10.1.*.md` — at least one match (reassurance present).
- `test -f .planning/phases/86-scoring-correctness-hotfix/HUMAN-UAT.md` AND `grep "Result:" ... | grep -i PASS` — operator signed off.
- `python -c "import tomllib; print(tomllib.load(open('pyproject.toml','rb'))['project']['version'])"` prints `4.10.1`.
</verification>

<success_criteria>
1. `pyproject.toml [project.version]` = `4.10.1` (D-12, RELEASE-02).
2. `changelog.d/v4.10.1.*.md` fragment exists with operator language + canonical example + visual-jump note + penalty-math-unchanged reassurance (D-13, RELEASE-01).
3. HUMAN-UAT.md documents a PASS on a live `tls-cert-defects` scan: overall < 100, six subscore radials color-match their fraction of 25, Data at Rest tab gauge agrees with the Executive Summary subscore radial, screenshot(s) captured.
4. No edits to `quirk/reports/executive.py`, `quirk/reports/html_renderer.py`, the evidence summarizer, or the stored-score migration path (D-14, D-15, D-16). (Note: `print.tsx` is NOT a ScoreGauge caller — no out-of-scope guard needed for it.)
5. Phase 86 mandatory completion steps (Obsidian phase note at `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-86-Scoring-Correctness-Hotfix.md`, `docs/UAT-SERIES.md` update + vault sync, and the `docs/UAT-SERIES.md` commit) are handed off to the orchestrator's post-phase routine — this plan supplies the SUMMARY content those steps consume.
</success_criteria>

<output>
After completion, create `.planning/phases/86-scoring-correctness-hotfix/86-03-SUMMARY.md` listing: the version bump, the changelog fragment path + excerpt, the UAT pass record reference (including the Data at Rest tab parity check), and a one-line note that the CLI HTML / PDF renderers (executive.py / html_renderer.py) and the evidence summarizer were intentionally NOT touched (deferred to v5.0 Phase 01 per D-14 / D-15).
</output>
