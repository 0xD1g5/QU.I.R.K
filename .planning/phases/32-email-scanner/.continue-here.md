---
context: phase
phase: 32-email-scanner
task: null
total_tasks: 16
status: paused_before_execution
last_updated: 2026-04-27T13:36:49.112Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through inspection of the planning artifacts before any code ran.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: SDK wave-grouping defect for plan 32-07 — `gsd-sdk query phase-plan-index 32` reports `wave: 1` for plan 32-07, but the plan file's frontmatter declares `wave: 5` with `depends_on: ["32-01","32-02","32-03","32-04","32-05","32-06"]`. Trusting the SDK output would launch the phase-completion ritual (Obsidian note + UAT-SERIES update) in Wave 1 alongside the test scaffolding, producing completion docs that document work that has not happened. **Structural mitigation:** Always read `32-07-PLAN.md` frontmatter directly when grouping waves for this phase. Do not pass plan 32-07 to any executor before plans 32-01..32-06 all have committed `*-SUMMARY.md` files.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| SDK-vs-frontmatter wave drift | `phase-plan-index` returned `wave: 1` for plan 32-07 while the file declares `wave: 5`. A naive orchestrator would dispatch the phase-completion docs plan in parallel with scaffolding plans, writing premature "complete" docs. | blocking | Re-derive the wave assignment from each plan file's frontmatter (`wave:` and `depends_on:`) before spawning. Cross-check the SDK output; if they disagree, the file wins. |
| Dispatching non-autonomous plans inside a parallel wave | Plans 32-05, 32-06, 32-07 are `autonomous: false`. Spawning them as background worktree agents skips the human checkpoints those plans were planned around. | advisory | Run non-autonomous plans either inline (no `Task()` subagent) or sequentially with explicit checkpoint heartbeats so the user sees the prompt. |

<current_state>
Phase 32 (email-scanner, milestone v4.4 Data in Motion) was just opened with `/gsd-execute-phase 32`. The orchestrator loaded init context and the plan index, then caught the wave-grouping defect above and presented the user with three options (`--wave 1` only, run all waves with corrected order, or fix the SDK first). Before the user answered, `/gsd-pause-work` was invoked. **No subagents spawned, no code written, no worktrees created, no STATE.md update via `state.begin-phase`.** The phase is in pristine pre-execution state.
</current_state>

<completed_work>

Completed Tasks:
- (none — pre-execution pause)

Completed pre-flight checks:
- `gsd-sdk query init.execute-phase 32` succeeded (phase_found, 7 plans, 7 incomplete, branching: none, parallelization: true, executor_model: sonnet)
- `gsd-sdk query phase-plan-index 32` succeeded but produced the wave-drift defect noted above
- Read 32-07-PLAN.md frontmatter to confirm `wave: 5` and full `depends_on` chain
- Verified Wave 1 (corrected: 32-01, 32-02, 32-05) has no `files_modified` overlap → parallel-safe
</completed_work>

<remaining_work>

**Corrected wave order** (use this, not the SDK output):

| Wave | Plans | What it builds |
|------|-------|----------------|
| 1 | 32-01, 32-02, 32-05 | RED tests, DB/config foundation, chaos lab (parallel) |
| 2 | 32-03 | `quirk/scanner/email_scanner.py` (needs 01+02) |
| 3 | 32-04 | Wire scanner into risk_engine + run_scan + profiles |
| 4 | 32-06 | Live scan vs lab → `labs/email/expected_results.md` |
| 5 | 32-07 | Obsidian phase note + UAT-SERIES update + commit |

All 7 plans incomplete. Total task count: 16 across the phase.
</remaining_work>

<decisions_made>

- Treat plan 32-07 as Wave 5, not Wave 1 — the frontmatter and `depends_on` chain are authoritative; the SDK index has a bug for this phase.
- Recommend the user start with `--wave 1` only rather than a bare `/gsd-execute-phase 32`, so the scaffolding output is reviewable before committing to the scanner implementation in Wave 2.
- Plan 32-05 (chaos lab) sits in Wave 1 because it has no code dependencies on 32-01/32-02 (it builds Docker config under `labs/email/` and edits `quantum-chaos-enterprise-lab/docker-compose.yml`); it just needs Docker available at execution time.
</decisions_made>

<blockers>
- SDK wave-grouping defect (see Blocking Constraint above) — workaround: pass `--wave N` explicitly OR re-derive wave order from plan frontmatter when the orchestrator resumes.
- User has not yet chosen between the three execution paths offered before the pause.
</blockers>

## Required Reading (in order)
1. `.planning/phases/32-email-scanner/32-07-PLAN.md` (frontmatter only) — confirms `wave: 5` and `depends_on` chain that contradicts the SDK index.
2. `.planning/phases/32-email-scanner/32-RESEARCH.md` — technical approach: sslyze primary, stdlib STARTTLS fallback, port matrix.
3. `.planning/phases/32-email-scanner/32-VALIDATION.md` — 6 success criteria the verifier will check at end-of-phase.
4. `CLAUDE.md` "Mandatory Phase Completion Steps" — the four-step ritual that plan 32-07 codifies; the resuming agent must run these whether or not 32-07 executes cleanly.

## Critical Anti-Patterns (do NOT repeat these)
- ANTI-PATTERN: Trusting SDK wave grouping for this phase → re-derive from plan frontmatter.
- ANTI-PATTERN: Background-spawning `autonomous: false` plans → run them inline or with visible checkpoints.

## Infrastructure State
- Git branch: `QUIRK-v4` (feature branch — branching strategy: `none` for this phase, no new branch was cut)
- Working tree dirty (pre-existing, not introduced by this session): `.DS_Store`, `quantum-chaos-enterprise-lab/certs/ca.srl`, plus untracked `.clone/`, `planning/`, `quantum-chaos-enterprise-lab/certs/{mtls.*,scenarios/}`, `quirk-output/`. None blocks resumption but Wave 1 dispatch should snapshot the dirty state so worktree merges don't surprise the user.
- Docker daemon state: not verified this session — Plan 32-05 will fail or checkpoint if Docker is not running.
- No worktrees created this session (`git worktree list` should still show only the main worktree).
- STATE.md not yet bumped to "phase 32 in progress" — the workflow's `state.begin-phase` step never ran.

## Pre-Execution Critique Required
- Design artifact: `.planning/phases/32-email-scanner/32-07-PLAN.md` (wave assignment)
- Critique focus: Confirm whether the SDK bug is local to this phase or systemic across other phases in this milestone — a quick `gsd-sdk query phase-plan-index 31` (or whatever was last shipped) would tell us. If systemic, file an issue against `gsd-sdk` before relying on auto-grouping again.
- Gate: Do NOT execute Wave 5 until Waves 1–4 each produce a committed `*-SUMMARY.md`.

<context>
The orchestrator was deliberate about NOT just running the phase end-to-end. Three signals pushed it to pause and present options instead of dispatching: (1) the wave-grouping discrepancy is a real footgun, (2) three of seven plans are `autonomous: false` so silent parallel dispatch would skip human review on the chaos lab and the live-scan recording, and (3) the user has a documented preference (in feedback memory) for explicit doc + Obsidian sync at end of phase, which is exactly what plan 32-07 codifies — getting that wrong would be highly visible. The recommendation given was Option 1 (`--wave 1` only) because Wave 1's three plans are parallel-safe, produce reviewable artifacts (RED tests, schema migration, Docker lab), and let the user gate the scanner implementation in Wave 2.
</context>

<next_action>
Resume with: `/gsd-execute-phase 32 --wave 1`

This will execute only plans 32-01, 32-02, 32-05 (parallel-safe, no `files_modified` overlap). Before launching: (a) re-confirm the wave-grouping defect by reading 32-07-PLAN.md frontmatter, (b) verify Docker is running so 32-05 doesn't checkpoint immediately, (c) snapshot the dirty working tree so worktree merges are clean. After Wave 1 completes, present plan 32-03 for Wave 2 launch.

Alternative: `/gsd-execute-phase 32 --interactive` to run all plans inline with checkpoints between tasks (lower token usage, no parallel worktrees, lets the user steer in real time).

Do NOT run a bare `/gsd-execute-phase 32` until the SDK wave-grouping defect is fixed — it would dispatch plan 32-07 in Wave 1 and write premature completion docs.
</next_action>
