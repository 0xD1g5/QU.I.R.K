---
phase: 32-email-scanner
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/models.py
  - quirk/db.py
  - quirk/config.py
  - pyproject.toml
autonomous: true
requirements: [STRUCT-02, STRUCT-03, EMAIL-00]
must_haves:
  truths:
    - "Fresh DB after init_db() has email_scan_json TEXT NULL column on crypto_endpoints"
    - "Existing DB without the column gets it added idempotently via ALTER TABLE"
    - "cfg.connectors.enable_email exists with default False"
    - "pyproject.toml declares [project.optional-dependencies] motion group (empty body acceptable per Phase 32)"
  artifacts:
    - path: "quirk/models.py"
      provides: "email_scan_json Column declaration on CryptoEndpoint"
      contains: "email_scan_json = Column(Text, nullable=True)"
    - path: "quirk/db.py"
      provides: "_ensure_email_columns(engine) idempotent migration; called from init_db()"
      contains: "_ensure_email_columns"
    - path: "quirk/config.py"
      provides: "ConnectorsCfg.enable_email: bool = False"
      contains: "enable_email"
    - path: "pyproject.toml"
      provides: "[project.optional-dependencies] motion group"
      contains: "motion"
  key_links:
    - from: "quirk/db.py:init_db"
      to: "_ensure_email_columns"
      via: "function call after _ensure_v43_columns"
      pattern: "_ensure_email_columns\\(engine\\)"
    - from: "quirk/config.py:ConnectorsCfg"
      to: "cfg.connectors.enable_email"
      via: "dataclass field"
      pattern: "enable_email:\\s*bool"
tags: [email-scanner, db-migration, config, pyproject, struct-carryforward, quirk]
---

<objective>
Land the foundation layer: SQLite schema column (`email_scan_json`), idempotent migration helper, config flag (`cfg.connectors.enable_email`), and the `[motion]` optional-dependencies group in `pyproject.toml`. This plan creates the storage and configuration substrate that Plans 03 (scanner module) and 05 (run_scan integration) consume.

Purpose: Honor STRUCT-02/STRUCT-03 (declare extras at plan time + ship pyproject diff) and EMAIL-00 (model + migration). Independent of the scanner module itself — runs parallel to Plan 03.

Output: 4 file deltas; one new function (`_ensure_email_columns`); one new model column; one new config field; one new pyproject extras group.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-email-scanner/32-CONTEXT.md
@.planning/phases/32-email-scanner/32-RESEARCH.md
@.planning/phases/32-email-scanner/32-PATTERNS.md
@CLAUDE.md
@quirk/models.py
@quirk/db.py
@quirk/config.py
@pyproject.toml

<interfaces>
<!-- Existing patterns this plan extends. Read these — do not deviate from the analog shape. -->

quirk/db.py existing migration helpers (lines 42–128):
  _SAFE_COL_RE — column-name allowlist regex (reuse, do NOT redefine)
  _ensure_identity_columns(engine) — v4.2 pattern (lines 42–63)
  _ensure_gcp_columns(engine)      — v4.3 pattern
  _ensure_v43_columns(engine)      — v4.3 pattern (lines 87–106)
  init_db() calls all three sequentially (lines 126–128)

quirk/models.py existing scan_json columns (lines 67–80):
  kerberos_scan_json = Column(Text, nullable=True)
  saml_scan_json     = Column(Text, nullable=True)
  dnssec_scan_json   = Column(Text, nullable=True)

quirk/config.py existing connector flags (lines 63–65, 99):
  enable_kerberos: bool = False
  enable_saml:     bool = False
  enable_dnssec:   bool = False
  vault_tls_verify: bool = True   # last existing field in ConnectorsCfg

CRITICAL: config namespace is `cfg.connectors.enable_email` — NOT `cfg.scanners.email_enabled`
(CONTEXT.md D-06 was wrong; see RESEARCH.md "Pattern 4: Profile-Gating" + "Pitfall 1").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add email_scan_json column to CryptoEndpoint model + _ensure_email_columns migration helper</name>
  <files>quirk/models.py, quirk/db.py</files>
  <read_first>
    - quirk/models.py (full file, 80 lines — locate the v4.3 DAR block ending around line 80)
    - quirk/db.py lines 1–159 (full file — read existing _SAFE_COL_RE regex, _ensure_identity_columns, _ensure_v43_columns, init_db call list)
    - .planning/phases/32-email-scanner/32-PATTERNS.md lines 247–279 (db.py exact pattern) and 283–300 (models.py exact pattern)
  </read_first>
  <behavior>
    - Test A (EMAIL-00 unit, in tests/test_email_scanner.py from Plan 01): test_email_scan_json_column_exists — calls init_db on a fresh in-memory SQLite engine; SQLAlchemy inspector reports `email_scan_json` column on `crypto_endpoints`.
    - Test B (idempotency): re-running init_db on the same engine does NOT raise (column already exists path).
  </behavior>
  <action>
**`quirk/models.py`** — append after the v4.3 DAR block (around line 80, immediately after the last existing `Column(...)` in the v4.3 section). Add a clearly-marked v4.4 block:

```python
    # ==========================
    # v4.4 Data in Motion fields
    # ==========================
    email_scan_json = Column(Text, nullable=True)  # Per-host email port scan summary JSON (Phase 32)
```

If `Text` is not already imported in models.py, do NOT add a new import — verify it's already in the import line for `Column`. (It is — `kerberos_scan_json` already uses Text.)

**`quirk/db.py`** — make two edits:

(1) After the existing `_V43_COLUMNS = [...]` constant (around line 86), add:

```python
_EMAIL_COLUMNS = ["email_scan_json"]
```

(2) After the existing `_ensure_v43_columns()` function (around line 106), add the new function — copy the structure verbatim from `_ensure_identity_columns`:

```python
def _ensure_email_columns(engine) -> None:
    """Add v4.4 email scanner column (email_scan_json TEXT) if absent (idempotent).

    Phase 32 — uses SQLAlchemy inspector to check existing columns before ALTER TABLE.
    Called from init_db() after _ensure_v43_columns().
    """
    existing = {c["name"] for c in sa_inspect(engine).get_columns("crypto_endpoints")}
    with engine.connect() as conn:
        for col in _EMAIL_COLUMNS:
            if not _SAFE_COL_RE.match(col):
                raise ValueError(f"Unsafe column name in migration: {col!r}")
            if col not in existing:
                conn.execute(text(f"ALTER TABLE crypto_endpoints ADD COLUMN {col} TEXT"))
        conn.commit()
```

(3) In `init_db()` (around line 126–128), add a single line AFTER the existing `_ensure_v43_columns(engine)` call:

```python
    _ensure_email_columns(engine)      # v4.4 Phase 32
```

Do NOT modify `_SAFE_COL_RE` — `email_scan_json` is alphanumeric+underscore, already covered.
Do NOT delete or reorder existing migration calls.

This task addresses requirement: EMAIL-00.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_email_scanner.py::test_email_scan_json_column_exists -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "email_scan_json = Column(Text, nullable=True)" quirk/models.py` returns 1.
    - `grep -c "_ensure_email_columns" quirk/db.py` returns ≥3 (constant section, def, call site in init_db).
    - `grep -c "_EMAIL_COLUMNS" quirk/db.py` returns ≥2 (definition + use inside function).
    - `python3 -m compileall quirk/models.py quirk/db.py` exits 0.
    - `python3 -c "from quirk.db import init_db, _ensure_email_columns; from sqlalchemy import create_engine; e = create_engine('sqlite:///:memory:'); init_db(e); from sqlalchemy import inspect; cols = {c['name'] for c in inspect(e).get_columns('crypto_endpoints')}; assert 'email_scan_json' in cols, cols; init_db(e); print('idempotent OK')"` prints "idempotent OK" with exit 0.
  </acceptance_criteria>
  <done>email_scan_json column declared, migration helper present, init_db() invokes it, idempotent on second call.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ConnectorsCfg.enable_email config field + declare [motion] extras group in pyproject.toml</name>
  <files>quirk/config.py, pyproject.toml</files>
  <read_first>
    - quirk/config.py lines 40–110 (full ConnectorsCfg dataclass — read all existing enable_* and *_targets fields; do not duplicate any name)
    - pyproject.toml (full file — locate the [project.optional-dependencies] table; verify [motion] is absent)
    - .planning/phases/32-email-scanner/32-PATTERNS.md lines 304–321 (exact config pattern)
    - .planning/phases/32-email-scanner/32-RESEARCH.md lines 125–134 (exact pyproject.toml diff)
  </read_first>
  <behavior>
    - Test C: `from quirk.config import AppConfig; cfg = AppConfig(); assert cfg.connectors.enable_email is False` succeeds.
    - Test D: `python3 -c "import tomllib; d = tomllib.load(open('pyproject.toml','rb')); assert 'motion' in d['project']['optional-dependencies']"` exits 0.
  </behavior>
  <action>
**`quirk/config.py`** — locate `ConnectorsCfg` dataclass. After the LAST field (which is `vault_tls_verify: bool = True` around line 99 per the patterns map), append:

```python
    # Email scanner enable flag (v4.4, Phase 32)
    enable_email: bool = False
```

Do NOT relocate any existing fields. Do NOT introduce a `cfg.scanners` namespace — research validated that `cfg.connectors.*` is the canonical scanner-flag location.

**`pyproject.toml`** — locate the `[project.optional-dependencies]` table. Append a new `motion` group with an empty list. The exact diff per RESEARCH.md:

```toml
motion = [
    # Phase 32: email scanner — sslyze is a soft import in tls_scanner/email_scanner; no hard dep needed.
    # Phase 33 will add broker deps (kafka-python, etc.) here.
]
```

Place this after the LAST existing optional-dependencies group (whichever group is last in the current file — preserve alphabetical or chronological convention used in pyproject.toml). If the existing groups are `[project.optional-dependencies]` table-style with named keys, insert as a new key. If the convention is one TOML key per line, follow that. Read the file first; mirror the existing style.

Do NOT add `sslyze` as a hard dep — it remains optional/soft-imported. STRUCT-02 only requires the group be DECLARED at plan time, not populated.

STRUCT-03 deliverable (the `pyproject.toml` diff itself) is satisfied by this task. Document the diff in the plan SUMMARY.

This task addresses requirements: STRUCT-02, STRUCT-03.
  </action>
  <verify>
    <automated>python3 -c "from quirk.config import AppConfig; c = AppConfig(); assert c.connectors.enable_email is False, c.connectors.enable_email; import tomllib; d = tomllib.load(open('pyproject.toml','rb')); assert 'motion' in d['project']['optional-dependencies'], list(d['project']['optional-dependencies']); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' quirk/config.py | grep -c "enable_email:\s*bool\s*=\s*False"` returns 1.
    - `grep -c "^motion\s*=" pyproject.toml` returns 1 (TOML key declared at column 0).
    - `python3 -c "import tomllib; d = tomllib.load(open('pyproject.toml','rb')); assert 'motion' in d['project']['optional-dependencies']"` exits 0.
    - `python3 -c "from quirk.config import AppConfig; assert AppConfig().connectors.enable_email is False"` exits 0.
    - `grep -c "cfg\\.scanners\\.email" quirk/config.py` returns 0 (anti-pattern absent).
    - `python3 -m compileall quirk/config.py` exits 0.
  </acceptance_criteria>
  <done>cfg.connectors.enable_email exists default False; pyproject.toml declares [motion] group (empty list acceptable for Phase 32); STRUCT-02 + STRUCT-03 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration code → SQLite | `ALTER TABLE` runs raw SQL; column name comes from internal `_EMAIL_COLUMNS` constant |
| config layer → user config.yaml | `enable_email` may be set from user-provided YAML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03 | T (Tampering) | quirk/db.py:_ensure_email_columns SQL injection via column name | mitigate | Reuse existing `_SAFE_COL_RE` allowlist regex in db.py — same defense as `_ensure_identity_columns`; raises ValueError on mismatch |
| T-32-04 | I (Info disclosure) | email_scan_json column may store cert subjects/SANs (PII-adjacent) | accept | Same trust profile as existing `kerberos_scan_json` / `dnssec_scan_json`; column is internal to the scan database file |
| T-32-05 | D (DoS) | Repeated init_db() invocations | mitigate | Inspector-first idempotency — column existence check skips ALTER on second call |
</threat_model>

<verification>
- `python3 -m pytest tests/test_email_scanner.py::test_email_scan_json_column_exists -x -q` passes (uses Plan 01 test stub).
- `python3 -m compileall quirk/models.py quirk/db.py quirk/config.py` exits 0.
- `python3 -c "import tomllib; d = tomllib.load(open('pyproject.toml','rb')); assert 'motion' in d['project']['optional-dependencies']"` exits 0.
- `python3 -c "from quirk.config import AppConfig; assert AppConfig().connectors.enable_email is False"` exits 0.
</verification>

<success_criteria>
- email_scan_json column present in fresh DB after init_db().
- _ensure_email_columns is idempotent.
- cfg.connectors.enable_email exists with default False.
- pyproject.toml [motion] group declared (STRUCT-02 satisfied; group body may be empty for Phase 32).
- pyproject.toml diff is the STRUCT-03 deliverable, captured in SUMMARY.
</success_criteria>

<output>
After completion, create `.planning/phases/32-email-scanner/32-02-SUMMARY.md` containing the literal pyproject.toml diff (STRUCT-03 artifact), a 1-line note on idempotency proof, and the column inspector dump.
</output>
