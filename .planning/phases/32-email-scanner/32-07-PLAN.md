---
phase: 32-email-scanner
plan: 07
type: execute
wave: 5
depends_on: ["32-01", "32-02", "32-03", "32-04", "32-05", "32-06"]
files_modified:
  - docs/UAT-SERIES.md
  - docs/phases/phase-32-email-scanner.md
autonomous: false
requirements: [STRUCT-01, STRUCT-02, STRUCT-03, EMAIL-00, EMAIL-01, EMAIL-02, EMAIL-03, EMAIL-04, EMAIL-05, EMAIL-06, EMAIL-07, EMAIL-08, EMAIL-09, EMAIL-10, EMAIL-11, EMAIL-12]
must_haves:
  truths:
    - "docs/UAT-SERIES.md contains a new test case section for Phase 32 email scanning, covering each of the 6 ROADMAP success criteria"
    - "docs/UAT-SERIES.md `**Last Updated:**` header is bumped to today's date"
    - "Obsidian vault `20_Dev-Work/QUIRK/Phases/Phase-32-Email-Scanner.md` is created with status: complete frontmatter and per-plan What-Was-Built sections"
    - "Obsidian `20_Dev-Work/QUIRK/UAT-Series.md` is overwritten with current docs/UAT-SERIES.md contents"
    - "docs/UAT-SERIES.md is committed via gsd-tools commit per CLAUDE.md mandatory step 4"
  artifacts:
    - path: "docs/UAT-SERIES.md"
      provides: "Updated UAT series with Phase 32 test cases + bumped date"
      contains: "Phase 32"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-32-Email-Scanner.md"
      provides: "Obsidian phase note documenting Phase 32 completion"
      contains: "status: complete"
  key_links:
    - from: "docs/UAT-SERIES.md"
      to: "Obsidian UAT-Series.md"
      via: "filesystem copy through /tmp staging file"
      pattern: "cp /tmp/uat_vault.md"
    - from: ".planning/phases/32-email-scanner/32-*-SUMMARY.md"
      to: "Obsidian Phase-32 note"
      via: "manual aggregation into What-Was-Built sections"
      pattern: "What Was Built"
tags: [email-scanner, docs, obsidian, uat, phase-completion, quirk]
---

<objective>
Honor the project-mandated phase completion ritual (CLAUDE.md "Mandatory Phase Completion Steps"): create the Obsidian phase note, update `docs/UAT-SERIES.md`, sync UAT-SERIES to the vault, and commit the doc updates. This plan is REQUIRED — the project's saved feedback memory states every phase must include explicit documentation update + Obsidian sync tasks.

Purpose: Without this plan, Phase 32 is technically functional but does not meet the project's deliverable contract. The Obsidian hub is a key client-facing artifact.

Output: 2 file deltas in repo + 2 vault note writes + 1 git commit. Includes a checkpoint for the user to confirm Obsidian state.

This plan is NOT autonomous — it requires file writes to a path outside the repo (`/Users/digs/vaults/Digs/...`) and a final user confirmation that the vault rendered correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@docs/UAT-SERIES.md
@.planning/phases/32-email-scanner/32-CONTEXT.md
@.planning/phases/32-email-scanner/32-01-SUMMARY.md
@.planning/phases/32-email-scanner/32-02-SUMMARY.md
@.planning/phases/32-email-scanner/32-03-SUMMARY.md
@.planning/phases/32-email-scanner/32-04-SUMMARY.md
@.planning/phases/32-email-scanner/32-05-SUMMARY.md
@.planning/phases/32-email-scanner/32-06-SUMMARY.md

<interfaces>
<!-- CLAUDE.md mandatory steps (verbatim): -->

Step 1: Write Obsidian phase note via filesystem (NOT obsidian CLI content=, file too large for shell):
  Path: /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-32-Email-Scanner.md
  Format: frontmatter (status: complete, type: phase, source, updated) +
          Goal + Requirements Covered + Success Criteria +
          What Was Built (one subsection per plan, sourced from SUMMARY.md files) +
          [[Roadmap]] link

Step 2: Update docs/UAT-SERIES.md
  - Bump `**Last Updated:**` date
  - Add Phase 32 test cases (one per success criterion)
  - Update version strings if needed (Phase 32 is mid-milestone v4.4.0; version bump is Phase 37)

Step 3: Sync UAT-SERIES.md to Obsidian via /tmp staging:
  printf "---\nproject: QU.I.R.K.\ntype: reference\nstatus: active\nsource: docs/UAT-SERIES.md\nupdated: <YYYY-MM-DD>\n---\n\n" > /tmp/uat_vault.md
  cat docs/UAT-SERIES.md >> /tmp/uat_vault.md
  cp /tmp/uat_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"

Step 4: Commit docs/UAT-SERIES.md via gsd-tools:
  node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-32): update UAT-SERIES.md" --files docs/UAT-SERIES.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/UAT-SERIES.md with Phase 32 test cases + bumped date</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (FULL FILE — read existing format, locate `**Last Updated:**` header, identify the most recent phase test series for naming convention reference)
    - .planning/ROADMAP.md lines 487–498 (Phase 32 goal + 6 success criteria — these become the test cases)
  </read_first>
  <action>
**1.** Bump the `**Last Updated:**` header at the top of docs/UAT-SERIES.md to today's date (use the date provided in `<env>` / system reminder context — for this conversation that's 2026-04-27; use the actual current date when this plan executes).

**2.** Add a new Phase 32 test series. Mirror the format and naming convention of the most recent phase series in the file (e.g., if the previous series is "UAT-31-NN", use "UAT-32-NN"). Add one test case per ROADMAP success criterion:

- **UAT-32-01:** Successful email scan against running lab returns CryptoEndpoint rows for all 7 ports with TLS version + cipher + cert metadata; `email_scan_json` populated in DB. (Maps to Success Criterion #1, EMAIL-00..06.)
- **UAT-32-02:** Port-25 STARTTLS endpoint emits MEDIUM `STARTTLS downgrade risk on SMTP` finding regardless of cipher; weak RSA cipher emits HIGH. (Maps to #2, EMAIL-08/09.)
- **UAT-32-03:** Scanning an unreachable port-25 host (cloud VM with egress block, or simulated via iptables drop) does NOT crash the scan; logs `CONNECTION_REFUSED` at DEBUG; remaining ports complete normally. (Maps to #3, EMAIL-01.)
- **UAT-32-04:** With sslyze uninstalled (`pip uninstall sslyze`), the scanner falls through to stdlib (`smtplib`/`imaplib`/`poplib`) and still returns TLS version + cipher + cert from the SSLSocket. (Maps to #4, EMAIL-07.)
- **UAT-32-05:** `docker compose --profile email up` boots the lab; running scan against `localhost` produces ≥1 HIGH weak-cipher and ≥1 MEDIUM STARTTLS-downgrade finding; `labs/email/expected_results.md` matches actual scan output. (Maps to #5, EMAIL-11/12.)
- **UAT-32-06:** Inspect any email endpoint's `service_detail` field — confirm format is `"<protocol_label>:<port>"` (e.g., `"SMTP-STARTTLS:587"`, `"IMAPS:993"`). (Maps to #6, EMAIL-10.)

For each test case, follow the existing UAT-SERIES.md format (test ID, title, preconditions, steps, pass criteria). Read the most recent phase series and copy its structure verbatim — do not invent a new template.

**3.** If a Phase 32 series already exists from a prior partial completion, replace it (do not append duplicates).

This task addresses requirements: every phase requirement (STRUCT-01..03, EMAIL-00..12) — UAT-SERIES is the verification surface for all of them.
  </action>
  <verify>
    <automated>grep -c "UAT-32-0[1-6]\|Phase 32" docs/UAT-SERIES.md && grep -c "Last Updated.*$(date +%Y-%m-%d)\|Last Updated.*2026-04" docs/UAT-SERIES.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "UAT-32-01\|UAT-32-02\|UAT-32-03\|UAT-32-04\|UAT-32-05\|UAT-32-06" docs/UAT-SERIES.md` returns ≥6 (all 6 test IDs present).
    - The `**Last Updated:**` header has today's date (or the date this plan executes).
    - `grep -ci "phase 32\|email scanner" docs/UAT-SERIES.md` returns ≥3.
    - File is valid Markdown (no broken header hierarchy — `markdownlint docs/UAT-SERIES.md` would pass, or simpler check: every `## ` heading is preceded by a blank line).
  </acceptance_criteria>
  <done>UAT-SERIES.md has Phase 32 test series and bumped date; format matches surrounding phase series.</done>
</task>

<task type="auto">
  <name>Task 2: Write Obsidian phase note + sync UAT-SERIES.md to vault</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-32-Email-Scanner.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md</files>
  <read_first>
    - All 6 SUMMARY files: .planning/phases/32-email-scanner/32-{01,02,03,04,05,06}-SUMMARY.md
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/ — list any existing Phase note (e.g., Phase-31-...) for the template pattern (e.g., `ls /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/`)
    - Read the most recent existing phase note in full to confirm template
    - .planning/ROADMAP.md lines 487–498 for Goal + Success Criteria
  </read_first>
  <action>
**1.** Use the Write tool to create `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-32-Email-Scanner.md` (do NOT use `obsidian` CLI `content=` — file is too large for shell expansion per CLAUDE.md). Format:

```markdown
---
project: QU.I.R.K.
type: phase
status: complete
source: .planning/phases/32-email-scanner/
updated: <YYYY-MM-DD>
---

# Phase 32 — Email Scanner

## Goal

QU.I.R.K. can audit TLS posture on all seven standard email protocol ports — detecting weak ciphers, legacy TLS versions, expired certificates, and STARTTLS downgrade risk — stored in `email_scan_json` and producing `CryptoEndpoint` rows with correct `ep.protocol` labels.

## Requirements Covered

STRUCT-01, STRUCT-02, STRUCT-03, EMAIL-00, EMAIL-01, EMAIL-02, EMAIL-03, EMAIL-04, EMAIL-05, EMAIL-06, EMAIL-07, EMAIL-08, EMAIL-09, EMAIL-10, EMAIL-11, EMAIL-12

## Success Criteria

1. Scanning a mail server on any of the 7 default email ports returns TLS version, negotiated cipher suite, cert subject/issuer/expiry, and key algorithm.
2. Port 25 STARTTLS endpoints emit a static MEDIUM `starttls-downgrade-risk` finding regardless of cipher strength; weak RSA key-exchange ciphers emit HIGH.
3. `CONNECTION_REFUSED` on port 25 does not crash the scan.
4. When sslyze fails, the stdlib fallback negotiates STARTTLS and extracts TLS version + cipher + cert.
5. `docker compose --profile email up` starts the lab; scanning produces the documented findings; `labs/email/expected_results.md` matches.
6. `ep.service_detail` follows `"SMTP-STARTTLS:587"`, `"SMTPS:465"`, `"IMAPS:993"`, `"POP3S:995"` convention.

## What Was Built

### Plan 01 — Test Scaffolding
<from 32-01-SUMMARY.md: test count, mock pattern decisions>

### Plan 02 — Schema + Config Foundation
<from 32-02-SUMMARY.md: pyproject.toml diff (verbatim block), models.py / db.py / config.py deltas>

### Plan 03 — Email Scanner Module
<from 32-03-SUMMARY.md: module symbols, line count, test pass count>

### Plan 04 — Risk Engine + Run Scan Wiring
<from 32-04-SUMMARY.md: insertion lines, profile gating verification>

### Plan 05 — Postfix + Dovecot Chaos Lab
<from 32-05-SUMMARY.md: file list, openssl version, captured cipher>

### Plan 06 — Expected Results Manifest
<from 32-06-SUMMARY.md: total finding counts, per-port observations>

## Links

- [[Roadmap]]
- [[Requirements]]
- [[_QUIRK-Hub]]
- Planning artifacts: `.planning/phases/32-email-scanner/`
- Lab: `labs/email/`
- Module: `quirk/scanner/email_scanner.py`
```

Replace each `<from XX-SUMMARY.md: ...>` placeholder with the actual content of the corresponding summary file (or a 1-paragraph distillation if the SUMMARY is long).

**2.** Sync `docs/UAT-SERIES.md` to the vault per CLAUDE.md Step 3:

```bash
TODAY=$(date +%Y-%m-%d)
printf '%s\n' \
  '---' \
  'project: QU.I.R.K.' \
  'type: reference' \
  'status: active' \
  'source: docs/UAT-SERIES.md' \
  "updated: $TODAY" \
  '---' \
  '' > /tmp/uat_vault.md
cat docs/UAT-SERIES.md >> /tmp/uat_vault.md
cp /tmp/uat_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
```

(Use Bash tool — this is the one place a shell command is the right choice; the file write itself is via `cp` not heredoc.)

**3.** Commit `docs/UAT-SERIES.md` via gsd-tools (CLAUDE.md Step 4):

```bash
node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-32): update UAT-SERIES.md" --files docs/UAT-SERIES.md
```

This task addresses: project-mandated phase completion ritual (covers all Phase 32 requirements indirectly through the UAT/docs surface).
  </action>
  <verify>
    <automated>test -f /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-32-Email-Scanner.md && grep -c "status: complete" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-32-Email-Scanner.md && test -f /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md && grep -c "Phase 32\|UAT-32" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md && git log --oneline -1 docs/UAT-SERIES.md | grep -c "phase-32"</automated>
  </verify>
  <acceptance_criteria>
    - Phase-32 obsidian note exists at the documented path with `status: complete` frontmatter.
    - Note contains all 6 plan headings: `### Plan 01`..`### Plan 06`.
    - UAT-Series.md vault note contains "Phase 32" or "UAT-32" entries.
    - Vault UAT-Series.md frontmatter has `source: docs/UAT-SERIES.md`.
    - `git log --oneline -1 docs/UAT-SERIES.md` shows a commit referencing phase-32.
    - No `<from XX-SUMMARY.md>` placeholder remains in the Obsidian note.
  </acceptance_criteria>
  <done>Obsidian phase note and UAT-Series synced; UAT-SERIES.md committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm Obsidian renders correctly</name>
  <what-built>Phase 32 Obsidian note + UAT-Series sync + UAT-SERIES commit, per CLAUDE.md mandatory phase completion steps.</what-built>
  <how-to-verify>
1. In Obsidian (Digs vault), open `20_Dev-Work/QUIRK/Phases/Phase-32-Email-Scanner.md`. Confirm:
   - Frontmatter shows `status: complete` and today's `updated:` date.
   - All 6 "What Was Built" subsections are populated (no placeholder text).
   - The `[[Roadmap]]` and `[[_QUIRK-Hub]]` wikilinks resolve.
2. Open `20_Dev-Work/QUIRK/UAT-Series.md`. Confirm Phase 32 section appears with all 6 UAT-32-NN cases.
3. (Optional) Open `_QUIRK-Hub.md` and confirm Phase 32 appears in the phases list. If not, the hub may need a follow-up update — note in checkpoint resume; this can be a backlog item.
4. Verify the UAT-SERIES commit landed: `git log --oneline -3 docs/UAT-SERIES.md` shows a fresh commit.

If any artifact is missing or rendered incorrectly: type "blocked: <description>" and re-run Task 2.
  </how-to-verify>
  <resume-signal>Type "approved" when both vault notes render correctly and the commit is in place. Phase 32 is then complete.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo writes → Obsidian vault filesystem | Phase note written to a path outside the repo (`/Users/digs/vaults/...`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-19 | T (Tampering) | accidental overwrite of unrelated vault notes via wrong path | mitigate | Hard-coded vault path includes Phase-32-* slug; do not loop or glob; verify with `test -f` before write |
| T-32-20 | I (Info disclosure) | vault notes may sync to cloud (iCloud/Obsidian Sync); no scan secrets included | accept | Phase notes contain code paths and decisions, no credentials; same trust profile as repo |
</threat_model>

<verification>
- The verify-snippet in Task 2 passes.
- Task 3 checkpoint approved.
</verification>

<success_criteria>
- docs/UAT-SERIES.md has Phase 32 test series and bumped date — committed.
- Obsidian Phase-32 note exists with status: complete and full What-Was-Built sections.
- Obsidian UAT-Series.md note overwritten with current docs/UAT-SERIES.md content + frontmatter.
- User confirms Obsidian renders correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/32-email-scanner/32-07-SUMMARY.md` documenting: vault paths written, the UAT-SERIES commit hash, any hub updates deferred to backlog.
</output>
