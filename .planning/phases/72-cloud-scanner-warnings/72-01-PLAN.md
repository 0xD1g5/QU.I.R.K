---
phase: 72-cloud-scanner-warnings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/scanner/aws_connector.py
  - tests/test_aws_connector.py
  - .planning/audit-2026-05-08/AUDIT-TASKS.md
autonomous: true
requirements: [CLOUD-01]
must_haves:
  truths:
    - "_scan_acm skips entries with empty or whitespace-only ARN instead of calling describe_certificate"
    - "_scan_kms skips KMS keys whose KeyMetadata.KeyState is in {Disabled, PendingDeletion, PendingImport, Unavailable}"
    - "_scan_s3_encryption uses as_completed + per-future .exception() so _build_endpoint failures are logged, not silently swallowed"
    - "_scan_eks_encryption iterates the entire enc_cfg list and emits one finding per provider entry"
    - "ThreadPoolExecutor is imported at module scope in aws_connector.py (covers WR-19 portion mapped here)"
    - "Audit rows scanners-cloud/WR-01, WR-02, WR-13, WR-14, WR-19 are flipped to [x] closed under Phase 72"
  artifacts:
    - path: "quirk/scanner/aws_connector.py"
      provides: "ACM empty-ARN guard, KMS disabled-state skip, S3 as_completed exception propagation, EKS multi-entry iteration, module-level ThreadPoolExecutor import"
      contains: "as_completed"
    - path: "tests/test_aws_connector.py"
      provides: "RED-then-GREEN tests for D-07/D-08/D-09/D-10/D-11"
      contains: "def test_"
  key_links:
    - from: "quirk/scanner/aws_connector.py::_scan_acm"
      to: "describe_certificate call"
      via: "if not arn or not arn.strip(): continue"
      pattern: "not arn or not arn\\.strip"
    - from: "quirk/scanner/aws_connector.py::_scan_kms"
      to: "KeyState skip set"
      via: "KeyMetadata.KeyState in {Disabled, PendingDeletion, PendingImport, Unavailable}"
      pattern: "PendingDeletion"
    - from: "quirk/scanner/aws_connector.py::_scan_s3_encryption"
      to: "as_completed loop"
      via: "executor.submit + as_completed + future.exception()"
      pattern: "as_completed"
    - from: "quirk/scanner/aws_connector.py module top"
      to: "ThreadPoolExecutor import"
      via: "from concurrent.futures import ThreadPoolExecutor, as_completed"
      pattern: "from concurrent\\.futures import"
---

<objective>
Close CLOUD-01 by hardening five sites in `quirk/scanner/aws_connector.py`:

1. **WR-01 (D-07)** — `_scan_acm` (lines 53-66): guard `describe_certificate(CertificateArn=arn)` with `if not arn or not arn.strip(): logger.warning(...); continue`. Empty / whitespace ARNs come from malformed listing responses and must degrade gracefully.
2. **WR-02 (D-08)** — `_scan_kms` (lines 315-346): after `describe_key`, check `KeyMetadata.KeyState`. Skip the key (continue) when state ∈ `{"Disabled", "PendingDeletion", "PendingImport", "Unavailable"}`. INFO-level log.
3. **WR-13 (D-09)** — `_scan_s3_encryption` (line 303-306): replace `executor.map(_build_endpoint, buckets)` with `as_completed` + per-future `.exception()`, mirroring `quirk/scanner/email_scanner.py:536-552` (Phase 64 idiom). **Per RESEARCH C-1:** the audit cite says `_classify` but current code uses `_build_endpoint` — apply the migration to the actual current call site name.
4. **WR-14 (D-10)** — `_scan_eks_encryption` (lines 165-169): iterate the entire `enc_cfg` list, emit one finding per provider entry; the D-04 dedup keeps them distinct via the new sort key.
5. **WR-19 (D-11)** — module top: move `from concurrent.futures import ThreadPoolExecutor` (and `as_completed` after D-09) from any function body to module scope alongside other imports.

Then flip audit ledger rows WR-01, WR-02, WR-13, WR-14, WR-19 to `Phase 72 | [x] closed` with per-row evidence.

Purpose: AWS connector emits correct findings on malformed cloud responses and never silently swallows classifier exceptions.
Output: Patched `aws_connector.py`, new `tests/test_aws_connector.py`, five audit rows flipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/72-cloud-scanner-warnings/72-CONTEXT.md
@.planning/phases/72-cloud-scanner-warnings/72-RESEARCH.md
@.planning/audit-2026-05-08/AUDIT-TASKS.md

<interfaces>
Phase 64 as_completed idiom (canonical source — quirk/scanner/email_scanner.py:536-552):
```
from concurrent.futures import ThreadPoolExecutor, as_completed

with ThreadPoolExecutor(max_workers=N) as ex:
    futures = {ex.submit(_build_endpoint, item): item for item in items}
    for f in as_completed(futures):
        try:
            ep = f.result()
        except Exception as e:
            logger.v(f"S3 endpoint task crashed: {e}")
            continue
        if ep is not None:
            results.append(ep)
```

KMS skip set (D-08):
`{"Disabled", "PendingDeletion", "PendingImport", "Unavailable"}` — fixed module-scope frozenset.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AWS connector fixes — empty-ARN guard, KMS state skip, EKS multi-entry, module-level executor import (WR-01/02/14/19)</name>
  <files>quirk/scanner/aws_connector.py</files>
  <read_first>
    - quirk/scanner/aws_connector.py (entire file — 346 lines)
    - .planning/phases/72-cloud-scanner-warnings/72-CONTEXT.md (D-07, D-08, D-10, D-11)
  </read_first>
  <action>
    **Part A — D-11 (WR-19): module-level ThreadPoolExecutor import.**
    Locate the `from concurrent.futures import ThreadPoolExecutor` line at aws_connector.py:226-227 (inside a function body). Move it to the top-of-file imports block. Combine with `as_completed` so the resulting top-level import is `from concurrent.futures import ThreadPoolExecutor, as_completed` (`as_completed` is consumed by Task 2 for D-09; importing it now keeps the diff atomic and avoids a second import edit).

    **Part B — D-07 (WR-01): `_scan_acm` empty-ARN guard.**
    In `_scan_acm` (lines 53-66), before `describe_certificate(CertificateArn=arn)`, insert:
    ```
    if not arn or not arn.strip():
        if logger:
            logger.warning("ACM cert with empty ARN — skipping")
        continue
    ```
    Use the existing logger variable name in scope (grep the surrounding function — likely just `logger`). No raise. Mirrors Phase 71 D-11 fail-soft pattern.

    **Part C — D-08 (WR-02): `_scan_kms` disabled-state skip.**
    At module scope, add the constant:
    ```
    _KMS_SKIP_STATES = frozenset({"Disabled", "PendingDeletion", "PendingImport", "Unavailable"})
    ```
    In `_scan_kms` (lines 315-346), immediately after the `describe_key` response is unpacked and `KeyMetadata` is read, insert:
    ```
    state = key_meta.get("KeyState")
    if state in _KMS_SKIP_STATES:
        if logger:
            logger.info("KMS key %s skipped (state=%s)", key_id, state)
        continue
    ```
    Use the actual variable name for the key-id in scope (likely `key_id` or `KeyId`).

    **Part D — D-10 (WR-14): `_scan_eks_encryption` multi-entry read.**
    Find the current `enc_cfg[0].get("provider")` site at lines 165-169. Replace with a `for cfg in enc_cfg:` loop that emits one finding per provider entry. Each emitted finding must include the provider type in a field that the D-04 dedup key (`severity_rank`, `title`, `host`, `port`) keeps distinct — easiest is to include the provider name in the `title` or `service_detail` so two providers on the same cluster produce two stable findings, not one. Researcher: read the surrounding emit code; if `title` is the natural disambiguator, embed `provider` there. Do not change unrelated emit logic.

    PEP 8. Minimal diff. No incidental cleanup per D-25.
  </action>
  <verify>
    <automated>python -m compileall quirk/scanner/aws_connector.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^from concurrent\\.futures import ThreadPoolExecutor" quirk/scanner/aws_connector.py` returns at least one hit AND occurs in the top-of-file imports (line < 50)
    - `grep -cE "ThreadPoolExecutor" quirk/scanner/aws_connector.py` reflects single source (no function-body import remains; manual review required since grep counts both shapes)
    - `grep -nE "not arn or not arn\\.strip" quirk/scanner/aws_connector.py` returns a hit inside `_scan_acm` body
    - `grep -nE "_KMS_SKIP_STATES" quirk/scanner/aws_connector.py` returns at least two hits (definition + use)
    - `grep -nE "PendingDeletion" quirk/scanner/aws_connector.py` returns at least one hit
    - `grep -nE "enc_cfg\\[0\\]" quirk/scanner/aws_connector.py` returns zero hits (index-0 read replaced by iteration)
    - `grep -nE "for .* in enc_cfg" quirk/scanner/aws_connector.py` returns at least one hit
    - `python -m compileall quirk/scanner/aws_connector.py` exits 0
  </acceptance_criteria>
  <done>
    aws_connector.py has the four edits; file compiles; no incidental refactor introduced.
  </done>
</task>

<task type="auto">
  <name>Task 2: AWS S3 executor.map → as_completed migration (WR-13)</name>
  <files>quirk/scanner/aws_connector.py</files>
  <read_first>
    - quirk/scanner/aws_connector.py lines 290-315 (post-Task 1 — confirm `_build_endpoint` site at line 303)
    - quirk/scanner/email_scanner.py lines 530-560 (Phase 64 as_completed idiom — exact reference)
    - .planning/phases/72-cloud-scanner-warnings/72-CONTEXT.md (D-09)
    - .planning/phases/72-cloud-scanner-warnings/72-RESEARCH.md (C-1 — function name discrepancy)
  </read_first>
  <action>
    Per RESEARCH C-1, the audit row cites `executor.map(_classify, ...)` but the actual current code at `aws_connector.py:303-306` is `executor.map(_build_endpoint, buckets)`. Apply D-09 to the **current call site name** (`_build_endpoint`); the locked behavior change is correct regardless of the inner function identifier.

    Replace the existing `executor.map(_build_endpoint, buckets)` consumption pattern with:
    ```
    futures = {ex.submit(_build_endpoint, bucket): bucket for bucket in buckets}
    for f in as_completed(futures):
        bucket = futures[f]
        try:
            ep = f.result()
        except Exception as e:
            if logger:
                logger.warning("S3 endpoint task crashed for bucket %s: %s", bucket, e)
            continue
        if ep is not None:
            results.append(ep)
    ```

    `as_completed` import is already at module top from Task 1. Preserve the surrounding `with ThreadPoolExecutor(max_workers=...) as ex:` context manager — only the body changes from `for ep in ex.map(...)` to the `submit` + `as_completed` shape. Use the actual variable names already in scope (e.g., the existing `ex` or `executor`, `buckets`, `results`).

    Per D-25: do NOT change `_build_endpoint` itself; do NOT change `max_workers`; do NOT touch the surrounding pagination loop. Only the consumption pattern changes.

    PEP 8. Minimal diff.
  </action>
  <verify>
    <automated>python -m compileall quirk/scanner/aws_connector.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "as_completed\\(" quirk/scanner/aws_connector.py` returns at least one hit inside `_scan_s3_encryption`
    - `grep -nE "executor\\.map\\(_build_endpoint" quirk/scanner/aws_connector.py` returns zero hits
    - `grep -nE "\\.submit\\(_build_endpoint" quirk/scanner/aws_connector.py` returns at least one hit
    - `grep -nE "f\\.result\\(\\)" quirk/scanner/aws_connector.py` returns at least one hit
    - `python -m compileall quirk/scanner/aws_connector.py` exits 0
  </acceptance_criteria>
  <done>
    `_scan_s3_encryption` uses `as_completed` + per-future `.exception()`/`.result()` pattern; failures are logged, not silently dropped.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests + flip WR-01/02/13/14/19 audit rows</name>
  <files>tests/test_aws_connector.py, .planning/audit-2026-05-08/AUDIT-TASKS.md</files>
  <read_first>
    - quirk/scanner/aws_connector.py (post-Tasks 1-2)
    - tests/ — skim a neighbor connector test for fixture/mocking style (e.g., tests/test_azure_blob.py or tests/test_k8s_connector.py)
    - .planning/audit-2026-05-08/AUDIT-TASKS.md lines 91, 92, 103, 104, 109 (WR-01/02/13/14/19 rows)
  </read_first>
  <behavior>
    - WR-01: _scan_acm with an ACM list response containing an empty-string ARN does not call describe_certificate and emits a logger.warning
    - WR-02: _scan_kms with a describe_key response whose KeyState="Disabled" (or "PendingDeletion") skips the key (no finding emitted, INFO log emitted)
    - WR-13: _scan_s3_encryption where _build_endpoint raises for one bucket continues processing other buckets and logs a warning
    - WR-14: _scan_eks_encryption with a cluster having two providers in enc_cfg emits two findings (one per provider entry), not one
    - WR-19: module-scope grep confirms ThreadPoolExecutor import is at top of file
  </behavior>
  <action>
    Create `tests/test_aws_connector.py`. Use `unittest.mock` to stub boto3 client responses (no AWS credentials required at test time). Reuse the existing project fixture style (look at `tests/test_k8s_connector.py` for `MagicMock` + `patch` precedent).

    **WR-01 tests:**
    - `test_scan_acm_skips_empty_arn` — patch the ACM client so `list_certificates` returns one entry with `CertificateArn=""`. Assert `describe_certificate` is NOT called. Use `caplog` (pytest builtin) to assert the warning text contains "empty ARN".
    - `test_scan_acm_skips_whitespace_arn` — same shape with `CertificateArn="   "`.

    **WR-02 tests:**
    - `test_scan_kms_skips_disabled_key` — patch `describe_key` to return `{"KeyMetadata": {"KeyId": "k1", "KeyState": "Disabled"}}`. Assert no finding emitted for `k1`.
    - `test_scan_kms_skips_pending_deletion` — same with `KeyState="PendingDeletion"`.
    - `test_scan_kms_emits_for_enabled` — `KeyState="Enabled"` produces a finding (positive case to prevent over-eager skipping).

    **WR-13 tests:**
    - `test_scan_s3_propagates_classifier_exception` — patch `_build_endpoint` to raise `RuntimeError("boom")` for one bucket and return a valid endpoint for another. Assert (a) the second bucket's endpoint is in results, (b) the exception is logged at WARNING level via `caplog`, (c) no `RuntimeError` is raised at the caller.

    **WR-14 tests:**
    - `test_scan_eks_emits_per_provider_entry` — patch the EKS describe response so `enc_cfg = [{"provider": "kms-1"}, {"provider": "kms-2"}]`. Assert exactly 2 findings emitted, distinguishable by provider in their identity columns.

    **WR-19 test:**
    - `test_threadpool_executor_imported_at_module_scope` — `import quirk.scanner.aws_connector as m; assert "ThreadPoolExecutor" in m.__dict__` AND `inspect.getsource(m).splitlines()[:50]` contains the `from concurrent.futures import ThreadPoolExecutor` line.

    Then update `.planning/audit-2026-05-08/AUDIT-TASKS.md`:
    - Line 91 (WR-01): `| — | [ ] open |` → `| Phase 72 | [x] closed |`
    - Line 92 (WR-02): same flip
    - Line 103 (WR-13): same flip
    - Line 104 (WR-14): same flip
    - Line 109 (WR-19): same flip

    Five rows, one line per row, single docs commit at end. No other rows touched.
  </behavior>
  <verify>
    <automated>pytest tests/test_aws_connector.py -x &amp;&amp; grep -cE "scanners-cloud/WR-(01|02|13|14|19).*Phase 72.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_aws_connector.py exists with ≥ 8 test functions; all pass
    - `grep -cE "scanners-cloud/WR-(01|02|13|14|19).*Phase 72.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 5
    - `git diff --stat .planning/audit-2026-05-08/AUDIT-TASKS.md` shows only the five WR row lines modified
  </acceptance_criteria>
  <done>
    Test module green; AUDIT-TASKS.md shows WR-01/02/13/14/19 closed under Phase 72.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AWS API response → connector | Malformed AWS list responses (empty ARN, anomalous KeyState, multi-entry enc_cfg) may break connector iteration |
| ThreadPoolExecutor task → caller | `executor.map` silently propagates exceptions only on consumption; failures vanish if results are iterated lazily |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | Denial of Service (crash) | _scan_acm describe_certificate(CertificateArn="") | mitigate | Guard with `not arn or not arn.strip()` before API call (D-07); degrade gracefully like Phase 71 D-11 |
| T-72-02 | Information Disclosure (false positive findings) | _scan_kms reports disabled keys as scannable | mitigate | Skip keys in {Disabled, PendingDeletion, PendingImport, Unavailable} (D-08); these cannot encrypt new data |
| T-72-03 | Tampering (silent failure) | _scan_s3_encryption executor.map | mitigate | Replace with as_completed + per-future .exception() (D-09); failures logged at WARNING |
| T-72-04 | Information Disclosure (incomplete data) | _scan_eks_encryption enc_cfg[0] index read | mitigate | Iterate full enc_cfg list (D-10); emit one finding per provider |
</threat_model>

<verification>
- `python -m compileall quirk/scanner/aws_connector.py` clean
- `pytest tests/test_aws_connector.py -x` passes
- AUDIT-TASKS.md WR-01/02/13/14/19 rows show `Phase 72 | [x] closed`
- No collateral changes to aws_connector.py beyond the five locked sites (per D-25)
</verification>

<success_criteria>
- `_scan_acm` empty-ARN guard active
- `_scan_kms` skips Disabled/PendingDeletion/PendingImport/Unavailable
- `_scan_s3_encryption` uses as_completed
- `_scan_eks_encryption` iterates full enc_cfg
- ThreadPoolExecutor imported at module scope
- 8+ tests pass
- WR-01, WR-02, WR-13, WR-14, WR-19 closed
</success_criteria>

<output>
Create `.planning/phases/72-cloud-scanner-warnings/72-01-SUMMARY.md` documenting: five locked sites edited, file diffs summarized, audit rows flipped (WR-01/02/13/14/19), test count, and rationale linking to D-07/D-08/D-09/D-10/D-11. Note the C-1 adjudication (audit cite said `_classify`, current code is `_build_endpoint`).
</output>
</content>
</invoke>