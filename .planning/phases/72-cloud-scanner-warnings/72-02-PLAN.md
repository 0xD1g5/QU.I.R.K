---
phase: 72-cloud-scanner-warnings
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/scanner/azure_connector.py
  - quirk/scanner/k8s_connector.py
  - tests/test_azure_keyvault.py
  - tests/test_k8s_connector.py
  - .planning/audit-2026-05-08/AUDIT-TASKS.md
autonomous: true
requirements: [CLOUD-02]
must_haves:
  truths:
    - "Azure _scan_keyvault_keys populates key_size for RSA (via key.n.bit_length()), EC (via curve→size map), and OCT (via properties.key_size)"
    - "K8s _emit_inaccessible_finding strips colons from cluster_name before embedding it in the finding identity tuple"
    - "K8s _enumerate_secret_types Counter filters out None-typed secrets via `if t is not None` (not `or 'Opaque'` coercion)"
    - "K8s dat_scan_json on the unencrypted code path is a fresh dict that does NOT contain a key_name key"
    - "Audit rows scanners-cloud/WR-03, WR-06, WR-17, WR-20 are flipped to [x] closed under Phase 72"
  artifacts:
    - path: "quirk/scanner/azure_connector.py"
      provides: "key_size populated for RSA/EC/OCT KeyVault keys"
      contains: "bit_length"
    - path: "quirk/scanner/k8s_connector.py"
      provides: "cluster_name colon strip, Counter None exclusion, dat_scan_json fresh-dict in unencrypted branch"
      contains: "replace(\":\", \"\")"
    - path: "tests/test_azure_keyvault.py"
      provides: "key_size assertions for RSA/EC/OCT"
      contains: "def test_"
  key_links:
    - from: "quirk/scanner/azure_connector.py::_scan_keyvault_keys"
      to: "key_size assignment"
      via: "RSA: key.n.bit_length(); EC: curve-name map; OCT: properties.key_size"
      pattern: "bit_length"
    - from: "quirk/scanner/k8s_connector.py::_emit_inaccessible_finding"
      to: "finding identity tuple"
      via: "cluster_name.replace(':', '')"
      pattern: "replace\\(\":\", \"\"\\)"
    - from: "quirk/scanner/k8s_connector.py::_enumerate_secret_types"
      to: "Counter input"
      via: "filter t is not None"
      pattern: "is not None"
---

<objective>
Close CLOUD-02 by fixing four data-correctness defects in the Azure KeyVault and K8s connectors:

1. **WR-03 (D-12)** — `quirk/scanner/azure_connector.py::_scan_keyvault_keys` (lines 50-56): `key_size` is always None because the code reads `properties.key_size` regardless of key type. Populate per-type:
   - RSA: `key.n.bit_length()`
   - EC: curve-name → standard size map (`P-256` → 256, `P-384` → 384, `P-521` → 521, `secp256k1` → 256)
   - OCT: `properties.key_size` if present
   - Unknown: `None`, log at DEBUG
2. **WR-06 (D-13)** — `quirk/scanner/k8s_connector.py::_emit_inaccessible_finding` (lines 344-352): `cluster_name.replace(":", "")` before embedding in the finding's identity tuple. Colons break CSV/CBOM output ordering and dedup.
3. **WR-17 (D-14)** — `quirk/scanner/k8s_connector.py::_enumerate_secret_types` (lines 286-288): per RESEARCH Pitfall 4, current code uses `Counter((s.type or "Opaque") for s in secrets.items)` which masks None secrets as "Opaque". Replace with `Counter(t for t in secret_types if t is not None)` plus a DEBUG log of the skipped-None count.
4. **WR-20 (D-15)** — `quirk/scanner/k8s_connector.py:136-156`: the unencrypted code path currently reuses the encrypted branch's `dat_scan_json` dict, causing a stale `key_name` to leak. Restructure so each branch builds a fresh dict; the unencrypted branch MUST NOT include the `key_name` key.

Then flip audit ledger rows WR-03, WR-06, WR-17, WR-20 to `Phase 72 | [x] closed`.

Purpose: Azure key inventory has correct key sizes; K8s findings have stable identity columns and no cross-branch dict pollution.
Output: Patched `azure_connector.py` and `k8s_connector.py`, new/extended test modules, four audit rows flipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/72-cloud-scanner-warnings/72-CONTEXT.md
@.planning/phases/72-cloud-scanner-warnings/72-RESEARCH.md
@.planning/audit-2026-05-08/AUDIT-TASKS.md

<interfaces>
EC curve → key size mapping (D-12):
```
_EC_CURVE_SIZES = {
    "P-256": 256,
    "P-384": 384,
    "P-521": 521,
    "secp256k1": 256,
}
```

Azure key types (azure-keyvault-keys SDK):
- `KeyType.rsa`, `KeyType.rsa_hsm` → use `key.n.bit_length()`
- `KeyType.ec`, `KeyType.ec_hsm` → curve name from `key.crv`
- `KeyType.oct`, `KeyType.oct_hsm` → `properties.key_size`

Phase 71 D-11 fail-soft pattern: log + skip, don't raise.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Azure KeyVault key_size population (WR-03 / D-12)</name>
  <files>quirk/scanner/azure_connector.py</files>
  <read_first>
    - quirk/scanner/azure_connector.py lines 1-100 (imports + `_scan_keyvault_keys` body)
    - .planning/phases/72-cloud-scanner-warnings/72-CONTEXT.md (D-12)
    - grep `key_size` across quirk/scanner/ to confirm there is no pre-existing helper to reuse
  </read_first>
  <action>
    In `_scan_keyvault_keys` (lines 50-56), replace the unconditional `key_size = properties.key_size` line with a per-key-type derivation block:

    1. Add a module-scope constant:
       ```
       _AZURE_EC_CURVE_SIZES = {"P-256": 256, "P-384": 384, "P-521": 521, "secp256k1": 256}
       ```

    2. In `_scan_keyvault_keys`, replace the existing `key_size = ...` site with:
       ```
       key_size = None
       kt = str(getattr(properties, "key_type", "") or "").lower()
       if "rsa" in kt:
           try:
               key_size = key.n.bit_length()
           except AttributeError:
               key_size = getattr(properties, "key_size", None)
       elif "ec" in kt:
           crv = getattr(key, "crv", None) or getattr(properties, "curve", None)
           key_size = _AZURE_EC_CURVE_SIZES.get(str(crv), None)
       elif "oct" in kt:
           key_size = getattr(properties, "key_size", None)
       else:
           if logger:
               logger.debug("Azure KeyVault key type %r — key_size left as None", kt)
       ```

    Researcher: verify the actual variable names in scope (the SDK object names may be `key_bundle.key` vs `properties` — read the surrounding lines carefully). Use the existing names verbatim. If the SDK returns a `JsonWebKey`-shaped object instead of a dataclass, the `.n` / `.crv` attribute names hold per the azure-keyvault-keys SDK docs.

    Do NOT introduce a new helper module; D-25 forbids incidental refactor. PEP 8. Minimal diff.
  </action>
  <verify>
    <automated>python -m compileall quirk/scanner/azure_connector.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "_AZURE_EC_CURVE_SIZES" quirk/scanner/azure_connector.py` returns at least two hits (definition + use)
    - `grep -nE "bit_length" quirk/scanner/azure_connector.py` returns at least one hit inside `_scan_keyvault_keys`
    - `grep -nE "P-256" quirk/scanner/azure_connector.py` returns at least one hit
    - `python -m compileall quirk/scanner/azure_connector.py` exits 0
  </acceptance_criteria>
  <done>
    azure_connector.py populates key_size per-type; file compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: K8s connector — cluster_name colon-strip, Counter None filter, dat_scan_json fresh-dict (WR-06/17/20)</name>
  <files>quirk/scanner/k8s_connector.py</files>
  <read_first>
    - quirk/scanner/k8s_connector.py lines 130-160 (dat_scan_json branches — D-15)
    - quirk/scanner/k8s_connector.py lines 283-295 (_enumerate_secret_types — D-14)
    - quirk/scanner/k8s_connector.py lines 340-355 (_emit_inaccessible_finding — D-13)
    - .planning/phases/72-cloud-scanner-warnings/72-CONTEXT.md (D-13, D-14, D-15)
    - .planning/phases/72-cloud-scanner-warnings/72-RESEARCH.md (Pitfall 4)
  </read_first>
  <action>
    **Part A — D-13 (WR-06): cluster_name colon-strip.**
    In `_emit_inaccessible_finding` (lines 344-352), locate the assignment that builds the finding identity tuple (or the host/title field). Apply `cluster_name.replace(":", "")` once at the entry of the function:
    ```
    cluster_name = (cluster_name or "").replace(":", "")
    ```
    Single site only. Do NOT propagate the colon strip to other emit functions — D-25 forbids incidental cleanup, and the audit row is scoped to this one function.

    **Part B — D-14 (WR-17): Counter None filter.**
    In `_enumerate_secret_types` (lines 286-288), per RESEARCH Pitfall 4 the current code is `Counter((s.type or "Opaque") for s in secrets.items)`. Replace with:
    ```
    secret_types = [s.type for s in secrets.items]
    skipped_nones = sum(1 for t in secret_types if t is None)
    if skipped_nones and logger:
        logger.debug("K8s _enumerate_secret_types: skipped %d None-typed secrets", skipped_nones)
    return Counter(t for t in secret_types if t is not None)
    ```
    This makes None-typed secrets explicit (DEBUG log) rather than silently coercing to "Opaque" (which made them indistinguishable from true Opaque secrets). Test fixtures in `tests/test_k8s_connector.py` may need updating — Task 3 covers test changes.

    **Part C — D-15 (WR-20): dat_scan_json fresh-dict per branch.**
    Lines 136-156 currently have a nested `try/except` where the unencrypted code path inherits `dat_scan_json` (or `key_name` inside it) from the encrypted branch. Restructure:
    ```
    if encrypted:
        dat_scan_json = {
            "encrypted": True,
            "key_name": <derived from db_enc>,
            # ... other encrypted-branch keys ...
        }
    else:
        dat_scan_json = {
            "encrypted": False,
            # NOTE: no key_name key — the unencrypted path must NOT include it (Phase 72 D-15)
            # ... other unencrypted-branch keys ...
        }
    ```
    Each branch constructs a fresh dict; neither branch reuses the other branch's dict variable. If both branches share some keys, that's fine — just write them explicitly in each branch (minor duplication is acceptable per D-25's "minimal diff" lens vs introducing a helper).

    PEP 8. Minimal diff. No incidental refactor.
  </action>
  <verify>
    <automated>python -m compileall quirk/scanner/k8s_connector.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "cluster_name.*replace\\(\":\", \"\"\\)" quirk/scanner/k8s_connector.py` returns at least one hit
    - `grep -nE "if t is not None" quirk/scanner/k8s_connector.py` returns at least one hit inside `_enumerate_secret_types`
    - `grep -cE "or \"Opaque\"" quirk/scanner/k8s_connector.py` returns 0 inside `_enumerate_secret_types` (the coercion was removed; if a different function still uses "or Opaque", that's outside scope)
    - `grep -nE "no key_name" quirk/scanner/k8s_connector.py` OR `grep -nE "Phase 72 D-15" quirk/scanner/k8s_connector.py` returns at least one hit (inline comment marking the unencrypted branch contract)
    - `python -m compileall quirk/scanner/k8s_connector.py` exits 0
  </acceptance_criteria>
  <done>
    k8s_connector.py has the three locked fixes; file compiles.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests + flip WR-03/06/17/20 audit rows</name>
  <files>tests/test_azure_keyvault.py, tests/test_k8s_connector.py, .planning/audit-2026-05-08/AUDIT-TASKS.md</files>
  <read_first>
    - quirk/scanner/azure_connector.py and quirk/scanner/k8s_connector.py (post-Tasks 1-2)
    - tests/test_azure_blob.py (skim for Azure mock fixture style)
    - tests/test_k8s_connector.py (existing tests — may need updates for D-14 counter change)
    - .planning/audit-2026-05-08/AUDIT-TASKS.md lines 93, 96, 107, 110 (WR-03/06/17/20)
  </read_first>
  <behavior>
    - WR-03: _scan_keyvault_keys returns RSA 2048-bit keys with key_size=2048, EC P-256 keys with key_size=256, OCT 256-bit keys with key_size=256
    - WR-06: _emit_inaccessible_finding builds an identity tuple where the cluster_name field contains no `:`
    - WR-17: _enumerate_secret_types receives [None, "Opaque", "kubernetes.io/tls", None] and returns Counter({"Opaque": 1, "kubernetes.io/tls": 1}) — Nones excluded
    - WR-20: dat_scan_json from the unencrypted code path has `encrypted=False` and `"key_name" not in dat_scan_json`
  </behavior>
  <action>
    **Create tests/test_azure_keyvault.py:**
    - `test_keyvault_rsa_key_size_populated` — construct a fake key object with `key.n.bit_length()` returning 2048 and `properties.key_type="RSA"`. Assert the emitted finding's `key_size == 2048`.
    - `test_keyvault_ec_p256_key_size` — fake key with `crv="P-256"`, `key_type="EC"`. Assert key_size=256.
    - `test_keyvault_ec_p384_key_size` — assert 384.
    - `test_keyvault_ec_p521_key_size` — assert 521.
    - `test_keyvault_oct_key_size` — fake key with `key_type="oct"`, `properties.key_size=256`. Assert key_size=256.
    - `test_keyvault_unknown_key_type_leaves_none` — `key_type="foo"`; assert key_size is None.

    Use `unittest.mock.MagicMock` to build the fake KeyBundle/properties objects. Look at how `tests/test_azure_blob.py` builds Azure mocks for the project-precedent fixture style.

    **Update tests/test_k8s_connector.py — add cases:**
    - `test_emit_inaccessible_finding_strips_colon_from_cluster_name` — call `_emit_inaccessible_finding(cluster_name="my:cluster:dev", ...)` and assert the emitted finding's cluster identity field equals `"myclusterdev"` (or whatever the identity tuple's relevant index is — read the function to confirm).
    - `test_enumerate_secret_types_excludes_none` — pass a `secrets` mock with items having types `[None, "Opaque", "kubernetes.io/tls", None]`. Assert the returned Counter is `{"Opaque": 1, "kubernetes.io/tls": 1}` (length 2, no None key).
    - `test_dat_scan_json_unencrypted_omits_key_name` — drive the unencrypted code path (configure the mock so `encrypted` is False); assert `"key_name" not in dat_scan_json`.
    - `test_dat_scan_json_encrypted_includes_key_name` — drive the encrypted path; assert `"key_name" in dat_scan_json` (positive case — confirms we didn't over-strip).

    Existing k8s tests may need fixture updates because the "or Opaque" coercion is gone. Researcher: run `pytest tests/test_k8s_connector.py -x` after Task 2 lands; for any pre-existing test that fails because it depended on None→Opaque coercion, update the test fixture to no longer pass None types.

    **Flip audit ledger rows:**
    - Line 93 (WR-03): `| — | [ ] open |` → `| Phase 72 | [x] closed |`
    - Line 96 (WR-06): same
    - Line 107 (WR-17): same
    - Line 110 (WR-20): same

    Four rows, single docs commit.
  </behavior>
  <verify>
    <automated>pytest tests/test_azure_keyvault.py tests/test_k8s_connector.py -x &amp;&amp; grep -cE "scanners-cloud/WR-(03|06|17|20).*Phase 72.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_azure_keyvault.py exists with ≥ 6 test functions; all pass
    - tests/test_k8s_connector.py has ≥ 4 new test functions added (covering D-13/D-14/D-15); all pass
    - `grep -cE "scanners-cloud/WR-(03|06|17|20).*Phase 72.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 4
    - `git diff --stat .planning/audit-2026-05-08/AUDIT-TASKS.md` shows only the four WR row lines modified
  </acceptance_criteria>
  <done>
    Tests green; AUDIT-TASKS.md shows WR-03/06/17/20 closed under Phase 72.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Azure KeyVault SDK response → connector | Per-key-type fields (`.n`, `.crv`, `properties.key_size`) shape differs per key type; uniform reads silently drop data |
| K8s API response → finding emit | Cluster names containing `:` corrupt CSV/CBOM downstream; None-typed secrets collide with "Opaque" coercion |
| Encrypted branch dict → unencrypted branch dict | Shared mutable dict leaks stale `key_name` across code paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-05 | Information Disclosure (silent data loss) | Azure _scan_keyvault_keys key_size always None | mitigate | Per-key-type derivation: RSA bit_length, EC curve map, OCT properties.key_size (D-12) |
| T-72-06 | Tampering (output corruption) | K8s _emit_inaccessible_finding colon-bearing cluster_name | mitigate | Strip colons before embedding in finding identity (D-13) |
| T-72-07 | Information Disclosure (count masking) | K8s _enumerate_secret_types None → Opaque coercion | mitigate | Filter None explicitly; DEBUG log skipped count (D-14) |
| T-72-08 | Tampering (cross-branch dict pollution) | K8s dat_scan_json shared between encrypted/unencrypted paths | mitigate | Build fresh dict per branch; unencrypted branch never includes key_name (D-15) |
</threat_model>

<verification>
- `python -m compileall quirk/scanner/azure_connector.py quirk/scanner/k8s_connector.py` clean
- `pytest tests/test_azure_keyvault.py tests/test_k8s_connector.py -x` passes
- AUDIT-TASKS.md WR-03/06/17/20 rows show `Phase 72 | [x] closed`
- No collateral changes beyond locked D-12/13/14/15 sites (D-25 compliance)
</verification>

<success_criteria>
- Azure KeyVault key_size populated for RSA/EC/OCT
- K8s cluster_name colon-stripped in `_emit_inaccessible_finding`
- K8s `_enumerate_secret_types` Counter excludes None
- K8s unencrypted `dat_scan_json` omits `key_name`
- 10+ new/updated tests pass
- WR-03, WR-06, WR-17, WR-20 closed
</success_criteria>

<output>
Create `.planning/phases/72-cloud-scanner-warnings/72-02-SUMMARY.md` documenting: four locked sites edited (D-12/13/14/15), file diffs summarized, audit rows flipped, test counts, and Pitfall-4 note (Counter coercion → explicit filter).
</output>
</content>
</invoke>