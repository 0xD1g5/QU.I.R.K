---
phase: 72-cloud-scanner-warnings
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/engine/cache.py
  - quirk/engine/profiles.py
  - tests/test_cache.py
  - .planning/audit-2026-05-08/AUDIT-TASKS.md
autonomous: true
requirements: [CLOUD-04]
must_haves:
  truths:
    - "cache._read_json wraps json.load in try/except (json.JSONDecodeError, UnicodeDecodeError); on corruption logs at WARNING and returns None (file left on disk for forensics)"
    - "cache.scope_hash includes dataclasses.asdict(cfg.connectors) (minus the _user_set_fields sidecar) so toggling enable_email/enable_broker invalidates the cache"
    - "quirk/engine/profiles.py passes py_compile, has the verified-intact tail (153 lines per research), and ends with an explicit `# eof` marker comment"
    - "Audit rows scanners-cloud/WR-15, WR-16, WR-21 are flipped to [x] closed under Phase 72"
  artifacts:
    - path: "quirk/engine/cache.py"
      provides: "Malformed-JSON safe _read_json; connector-aware scope_hash"
      contains: "JSONDecodeError"
    - path: "quirk/engine/profiles.py"
      provides: "EOF marker comment confirming integrity"
      contains: "# eof"
    - path: "tests/test_cache.py"
      provides: "Malformed-JSON test + cache-invalidation-on-connector-flag test"
      contains: "def test_"
  key_links:
    - from: "quirk/engine/cache.py::_read_json"
      to: "try/except around json.load"
      via: "JSONDecodeError + UnicodeDecodeError catch"
      pattern: "JSONDecodeError"
    - from: "quirk/engine/cache.py::scope_hash"
      to: "connectors dict"
      via: "dataclasses.asdict(cfg.connectors) with _user_set_fields popped"
      pattern: "dataclasses\\.asdict"
    - from: "quirk/engine/profiles.py last line"
      to: "EOF marker"
      via: "# eof comment"
      pattern: "# eof"
---

<objective>
Close CLOUD-04 by hardening cache I/O, extending the cache scope hash, and verifying profiles.py file integrity:

1. **WR-15 (D-18)** — `quirk/engine/cache.py::_read_json` (lines 27-29, 56-63): wrap `json.loads(...)` in `try: ... except (json.JSONDecodeError, UnicodeDecodeError) as e: logger.warning(...); return None`. Corrupt cache files return None (cache miss path runs); file is left on disk for forensics (do NOT delete).
2. **WR-16 (D-19)** — `quirk/engine/cache.py::scope_hash` (lines 32-47): include `connectors` in the hash input via `dataclasses.asdict(cfg.connectors)`. Pop the `_user_set_fields` sidecar (added by D-02 in PLAN 05) before serialization since `frozenset` is not JSON-serializable. Toggling `enable_email`/`enable_broker` now invalidates the cache.
3. **WR-21 (D-06)** — `quirk/engine/profiles.py` EOF integrity verification: per RESEARCH Pitfall 5, the file is 153 lines, `python -m py_compile` exits 0, the tail block is correctly closed by indentation — the file IS intact. Add an explicit `# eof` marker as the final line. Flip WR-21 with evidence `"file intact, py_compile passed, no truncation detected, # eof marker added"`.

Then flip audit ledger rows WR-15, WR-16, WR-21 to `Phase 72 | [x] closed`.

**Coordination note:** D-19 references `cfg.connectors._user_set_fields`, which is created by D-02 in PLAN 05. PLAN 04 must defensively pop the sidecar by key name regardless of whether PLAN 05 has landed first — `dict.pop('_user_set_fields', None)` is a no-op if the field doesn't exist yet. This decouples the wave.

Purpose: Cache survives malformed JSON without crashing; cache invalidates on connector flag changes; profiles.py file integrity has a self-documenting marker.
Output: Patched `cache.py`, patched `profiles.py` (one-line append), extended `tests/test_cache.py`, three audit rows flipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/72-cloud-scanner-warnings/72-CONTEXT.md
@.planning/phases/72-cloud-scanner-warnings/72-RESEARCH.md
@.planning/audit-2026-05-08/AUDIT-TASKS.md

<interfaces>
D-18 reference shape (research-verified):
```
def _read_json(path: str) -> Any:
    try:
        with open(path, "r", encoding="utf-8") as f:
            return json.load(f)
    except (json.JSONDecodeError, UnicodeDecodeError) as e:
        logger.warning("Cache file %s corrupt — ignoring: %s", path, e)
        return None
```
Then load_cache at ~line 56 needs `obj = _read_json(path); if obj is None: return None` (or empty {} if downstream needs a dict).

D-19 reference shape (with sidecar pop):
```
import dataclasses

def scope_hash(cfg, ...):
    connectors_dict = dataclasses.asdict(cfg.connectors)
    connectors_dict.pop("_user_set_fields", None)  # Phase 72: drop non-JSON-serializable sidecar
    parts = {
        # ... existing keys ...
        "connectors": connectors_dict,
    }
    raw = json.dumps(parts, sort_keys=True).encode("utf-8")
    return hashlib.sha256(raw).hexdigest()[:16]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: cache._read_json malformed-JSON guard + scope_hash includes connectors (WR-15 / WR-16)</name>
  <files>quirk/engine/cache.py</files>
  <read_first>
    - quirk/engine/cache.py (entire file — 91 lines)
    - .planning/phases/72-cloud-scanner-warnings/72-CONTEXT.md (D-18, D-19)
    - .planning/phases/72-cloud-scanner-warnings/72-RESEARCH.md (D-19 example with sidecar pop)
  </read_first>
  <action>
    **Part A — D-18 (WR-15): _read_json malformed-JSON handling.**

    In `_read_json` (lines 27-29 per research; the function body uses `with open(...) as f: return json.load(f)` shape), wrap the `json.load` in:
    ```
    try:
        with open(path, "r", encoding="utf-8") as f:
            return json.load(f)
    except (json.JSONDecodeError, UnicodeDecodeError) as e:
        logger.warning("Cache file %s corrupt — ignoring: %s", path, e)
        return None
    ```
    Use the existing logger (or `logging.getLogger(__name__)` if no module-level logger is present — researcher: grep the file for `logger`/`logging` to find the project convention).

    **Important — downstream None handling:** At the `load_cache` consumer (~line 56-63), check whether the existing code can handle a `None` return from `_read_json`. If downstream expects a dict and would `obj.get(...)`, guard with:
    ```
    obj = _read_json(path)
    if obj is None:
        return None  # or {} — researcher picks based on existing return-type contract
    ```
    Per D-18: "Researcher to check whether downstream code handles `None` cleanly; if not, return `{}` instead." Choose whichever maintains the existing call-site contract; document the choice in the inline comment.

    Do NOT delete the corrupt file (per D-18 forensics rationale).

    **Part B — D-19 (WR-16): scope_hash includes connectors.**

    In `scope_hash` (lines 32-47), per RESEARCH `D-19` example, add `connectors` to the hash input:
    ```
    import dataclasses  # if not already imported at module top — researcher confirms

    def scope_hash(cfg, discovery_mode, nmap_extra_args="", ports=None):
        connectors_dict = dataclasses.asdict(cfg.connectors)
        connectors_dict.pop("_user_set_fields", None)  # Phase 72 D-19: drop non-JSON-serializable sidecar (added by D-02)
        parts = {
            "discovery_mode": discovery_mode,
            # ... existing keys: fqdns / cidrs / include_ips / exclude_ips / ports / include_sni / nmap_extra_args ...
            "connectors": connectors_dict,  # Phase 72 D-19: invalidate cache on connector flag changes
        }
        raw = json.dumps(parts, sort_keys=True).encode("utf-8")
        return hashlib.sha256(raw).hexdigest()[:16]
    ```

    Researcher: read the actual current `scope_hash` body and preserve all existing keys verbatim. ONLY add the `connectors` key + the pop guard + the import if missing. Do NOT change the hash-input ordering of other keys (sort_keys=True normalizes anyway, but minimal diff).

    The `_user_set_fields` pop is defensive against the future presence of that sidecar (added by PLAN 05 D-02). It is a no-op if D-02 hasn't landed yet.

    Per D-25: do NOT touch cache eviction or TTL logic. Only `_read_json` and `scope_hash`.

    PEP 8. Minimal diff.
  </action>
  <verify>
    <automated>python -m compileall quirk/engine/cache.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "JSONDecodeError" quirk/engine/cache.py` returns at least one hit inside `_read_json`
    - `grep -nE "UnicodeDecodeError" quirk/engine/cache.py` returns at least one hit
    - `grep -nE "corrupt — ignoring" quirk/engine/cache.py` returns at least one hit
    - `grep -nE "dataclasses\\.asdict\\(cfg\\.connectors\\)" quirk/engine/cache.py` returns at least one hit inside `scope_hash`
    - `grep -nE "pop\\(.*_user_set_fields" quirk/engine/cache.py` returns at least one hit
    - `python -m compileall quirk/engine/cache.py` exits 0
  </acceptance_criteria>
  <done>
    cache.py handles malformed JSON, includes connectors in scope_hash, defensively pops the D-02 sidecar.
  </done>
</task>

<task type="auto">
  <name>Task 2: profiles.py EOF integrity verification + marker (WR-21 / D-06)</name>
  <files>quirk/engine/profiles.py</files>
  <read_first>
    - quirk/engine/profiles.py (entire file — 153 lines per research)
    - .planning/phases/72-cloud-scanner-warnings/72-CONTEXT.md (D-06)
    - .planning/phases/72-cloud-scanner-warnings/72-RESEARCH.md (Pitfall 5)
  </read_first>
  <action>
    Per D-06, execute the verification sequence:

    1. `python -m py_compile quirk/engine/profiles.py` — must pass. (Research confirms it does.)
    2. `git log --oneline -5 -- quirk/engine/profiles.py` — inspect last 5 commits affecting the file.
    3. `wc -l quirk/engine/profiles.py` — must return 153 (per research) or close (off-by-one from a missing trailing newline is acceptable).
    4. Visual diff of the last 30 lines vs. the last known-good commit (e.g., Phase 71 tag `HEAD~N` where N covers the gap to Phase 71 final commit). Use `git show HEAD~N:quirk/engine/profiles.py | tail -30` and compare with `tail -30 quirk/engine/profiles.py`.

    **If steps 1-4 pass (research-expected path — Pitfall 5 confirmed):**
    Append a single line to `quirk/engine/profiles.py`:
    ```
    # eof
    ```
    This is the final line of the file. Audit row WR-21 flips with evidence: `"file intact, py_compile passed, last 30 lines match git history, # eof marker added"`.

    **If step 1 fails OR step 4 shows truncation (unexpected path):**
    Use `git show <last-green-commit>:quirk/engine/profiles.py` to reconstruct the missing tail. Find the last-green commit via `git bisect` or successive `git log -- quirk/engine/profiles.py`. Replace the truncated portion. Append `# eof`. Audit row WR-21 flips with evidence `"tail reconstructed from <commit_sha>, # eof marker added"`.

    Per D-25: do NOT modify any other lines in profiles.py. The locked decisions for profiles.py mutation guards (D-02, D-03) belong to PLAN 05, NOT this plan.

    PEP 8 — comment-only addition.
  </action>
  <verify>
    <automated>python -m compileall quirk/engine/profiles.py &amp;&amp; tail -1 quirk/engine/profiles.py | grep -q "^# eof$"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m compileall quirk/engine/profiles.py` exits 0
    - `tail -1 quirk/engine/profiles.py` returns the line `# eof`
    - `wc -l quirk/engine/profiles.py` returns 154 (153 + 1 marker line) — or 153 if the previous file lacked a trailing newline; either is acceptable as long as `# eof` is the literal final non-empty line
    - `git diff quirk/engine/profiles.py` shows only the single `# eof` line added (Pitfall-5 verification path); if reconstruction was needed, the diff is larger and the SUMMARY documents the source commit
  </acceptance_criteria>
  <done>
    profiles.py file integrity verified per D-06 sequence; `# eof` marker present.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests + flip WR-15/16/21 audit rows</name>
  <files>tests/test_cache.py, .planning/audit-2026-05-08/AUDIT-TASKS.md</files>
  <read_first>
    - quirk/engine/cache.py (post-Task 1)
    - quirk/engine/profiles.py (post-Task 2)
    - tests/test_cache.py (existing — extend, do not replace)
    - .planning/audit-2026-05-08/AUDIT-TASKS.md lines 105, 106, 111 (WR-15/16/21)
  </read_first>
  <behavior>
    - WR-15: _read_json("/path/to/corrupt.json") where the file contains `{not valid json}` returns None (no exception); logger.warning fired
    - WR-15: load_cache on a cache directory with one corrupt JSON file does not raise — corrupt file is skipped, valid files load normally
    - WR-16: scope_hash(cfg_a) != scope_hash(cfg_b) when cfg_a.connectors.enable_email is True and cfg_b.connectors.enable_email is False (all other fields identical)
    - WR-16: scope_hash works when cfg.connectors has the _user_set_fields sidecar populated (no TypeError from frozenset serialization)
    - WR-21: tail -1 of quirk/engine/profiles.py is the literal line `# eof`; py_compile succeeds
  </behavior>
  <action>
    Extend `tests/test_cache.py` with new tests (do NOT replace existing tests):

    **WR-15 tests:**
    - `test_read_json_returns_none_on_malformed_json` — write `{not json` to a tempfile; call `_read_json(path)`; assert result is None; assert `caplog.text` contains `"corrupt"`.
    - `test_read_json_returns_none_on_unicode_error` — write `b"\xff\xfe invalid utf-8"` (raw bytes) to a tempfile; call `_read_json(path)`; assert result is None.
    - `test_load_cache_skips_corrupt_file` — set up a cache directory with one valid JSON file and one corrupt one; call `load_cache(...)`; assert the valid entry is returned and no exception.

    **WR-16 tests:**
    - `test_scope_hash_changes_when_enable_email_flips` — build two `ScanConfig` objects identical except `connectors.enable_email = True` vs `False`. Assert `scope_hash(cfg_a, ...) != scope_hash(cfg_b, ...)`.
    - `test_scope_hash_changes_when_enable_broker_flips` — same with `enable_broker`.
    - `test_scope_hash_stable_for_identical_cfg` — same cfg → same hash (sanity).
    - `test_scope_hash_handles_user_set_fields_sidecar` — set `cfg.connectors._user_set_fields = frozenset({"enable_email"})` (simulate PLAN-05 D-02 having landed); assert `scope_hash(...)` does NOT raise `TypeError: frozenset not JSON serializable`.

    **WR-21 tests:**
    - `test_profiles_py_has_eof_marker` — read the file, assert the last non-empty line is `# eof`.
    - `test_profiles_py_compiles` — `import py_compile; py_compile.compile("quirk/engine/profiles.py", doraise=True)` — no exception.

    Researcher: look at existing `tests/test_cache.py` fixtures (tmp_path, etc.) and reuse the patterns. ScanConfig construction may already have a helper in the existing tests — reuse it.

    **Flip audit ledger rows:**
    - Line 105 (WR-15): `| — | [ ] open |` → `| Phase 72 | [x] closed |`
    - Line 106 (WR-16): same
    - Line 111 (WR-21): same — evidence per D-06 verification result

    Three rows, single docs commit.
  </behavior>
  <verify>
    <automated>pytest tests/test_cache.py -x &amp;&amp; grep -cE "scanners-cloud/WR-(15|16|21).*Phase 72.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_cache.py has ≥ 8 new test functions added; all existing + new tests pass
    - `grep -cE "scanners-cloud/WR-(15|16|21).*Phase 72.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 3
    - `git diff --stat .planning/audit-2026-05-08/AUDIT-TASKS.md` shows only the three WR row lines modified
  </acceptance_criteria>
  <done>
    Tests green; AUDIT-TASKS.md shows WR-15/16/21 closed under Phase 72.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cache file on disk → engine | Corrupt JSON (truncated write, file-system glitch, manual edit) crashes the cache loader |
| Connector flag toggle → cache | Stale cache returned when operator changes connector enable flags without restarting / cache invalidation |
| profiles.py source on disk → import | A truncated source file silently misbehaves; an explicit EOF marker makes truncation detectable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-12 | Denial of Service (crash on bad input) | cache._read_json json.load on corrupt file | mitigate | try/except (JSONDecodeError, UnicodeDecodeError); return None and log WARNING (D-18) |
| T-72-13 | Tampering (stale cache) | scope_hash omits connector flags | mitigate | Include dataclasses.asdict(cfg.connectors) in hash input (D-19) |
| T-72-14 | Tampering (silent file truncation) | profiles.py tail integrity | mitigate | Explicit `# eof` marker; py_compile + git-log verification per D-06 |
</threat_model>

<verification>
- `python -m compileall quirk/engine/cache.py quirk/engine/profiles.py` clean
- `pytest tests/test_cache.py -x` passes
- AUDIT-TASKS.md WR-15/16/21 rows show `Phase 72 | [x] closed`
- profiles.py tail is `# eof` (Pitfall-5 verification path expected; reconstruction path documented in SUMMARY if invoked)
- No collateral changes beyond D-06/D-18/D-19 sites (D-25 compliance)
</verification>

<success_criteria>
- cache._read_json safe under corrupt JSON
- cache.scope_hash invalidates on connector flag changes
- profiles.py verified-intact + EOF marker
- 8+ new tests pass
- WR-15, WR-16, WR-21 closed
</success_criteria>

<output>
Create `.planning/phases/72-cloud-scanner-warnings/72-04-SUMMARY.md` documenting: cache.py diff (try/except + scope_hash extension), profiles.py D-06 verification result (Pitfall-5 confirmation OR reconstruction commit_sha), audit rows flipped with evidence text, test counts, and rationale linking to D-06/D-18/D-19.
</output>
</content>
</invoke>