---
phase: 999.84-chaos-lab-macos-mount-compat
plan: 04
type: execute
wave: 2
depends_on: ["999.84-01", "999.84-02", "999.84-03"]
files_modified:
  - docs/UAT-SERIES.md
autonomous: false
requirements: [BACK-91]
must_haves:
  truths:
    - "End-to-end UAT on macOS Docker Desktop: `./lab.sh down && ./lab.sh all && sleep 60 && docker compose -p chaoslab ps -a` shows zero containers in `Restarting`, `Exited (1)`, `Exited (2)`, `Exited (22)`, or `unhealthy` state. Seeds in `Exited (0)` remain expected — per ROADMAP success criterion 4 + CONTEXT cross-cutting locked UAT criterion"
    - "Re-run idempotency confirmed: a SECOND `./lab.sh down && ./lab.sh all` (with named volumes preserved, especially `chaoslab_gitea_data`) produces the same clean result with zero Exited(22) from gitea-seed — this is the regression scenario CONTEXT locked"
    - "Three bug-fix sites individually green in the global UAT: chaoslab-ldaps-1 `Up`, chaoslab-rabbitmq-broker-1 `Up (healthy)`, chaoslab-gitea-seed-1 `Exited (0)`"
    - "If Plan 01 took the bitnami/openldap fallback, the README row 34 + oracle ## Profile: ldaps deltas it landed in that commit are confirmed visually in this UAT (no follow-up oracle drift)"
    - "`docs/UAT-SERIES.md` gains a Phase 999.84 entry recording the three bug fixes + cross-platform notes; `**Last Updated:**` bumped to 2026-05-15; single commit `docs(phase-999.84): update UAT-SERIES.md`"
    - "Obsidian vault sync per CLAUDE.md Mandatory Phase Completion Steps: Phase note `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-999.84-Chaos-Lab-macOS-Host-Mount-Compat.md` and `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` written via filesystem (NOT via obsidian CLI content= — files too large per CLAUDE.md)"
  artifacts:
    - path: "docs/UAT-SERIES.md"
      provides: "Updated with Phase 999.84 entry + bumped Last Updated date"
      contains: "999.84"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-999.84-Chaos-Lab-macOS-Host-Mount-Compat.md"
      provides: "Obsidian phase note with frontmatter status: complete, full What Was Built section sourced from plan SUMMARYs"
      contains: "status: complete"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
      provides: "Obsidian-synced copy of docs/UAT-SERIES.md with project frontmatter prepended"
      contains: "999.84"
  key_links:
    - from: "Plans 01-03 commits"
      to: "global UAT (Task 1 of this plan)"
      via: "all three fixes verified together in one ./lab.sh all cycle on macOS"
      pattern: "lab.sh"
    - from: "Plan 01 commit (if option-a fallback)"
      to: "README row 34 + expected_results_v4.md ## Profile: ldaps"
      via: "verified in global UAT — no separate edits in this plan"
      pattern: "ldaps"
    - from: "Plans 01-03 SUMMARYs"
      to: "Obsidian Phase note What Was Built section"
      via: "this plan reads each SUMMARY and writes the consolidated phase note"
      pattern: "Phase-999.84"
---

<objective>
Cross-cutting close-out for Phase 999.84. Plans 01-03 each made surgical per-service fixes (ldaps env or image swap; rabbitmq-broker env; gitea-seed idempotency). This plan:

1. Runs the global end-to-end UAT — `./lab.sh down && ./lab.sh all` — on macOS Docker Desktop, twice in a row (first run + re-run with named volumes preserved), to verify all three fixes work in concert AND the gitea-seed idempotency holds.
2. Spot-checks any README / oracle deltas that Plan 01's fallback (option (a), if taken) landed.
3. Updates `docs/UAT-SERIES.md` with a Phase 999.84 entry per CLAUDE.md Mandatory Phase Completion Step 2.
4. Syncs `UAT-SERIES.md` to the Obsidian vault per CLAUDE.md Mandatory Phase Completion Step 3 (filesystem write, NOT CLI content= arg — file is too large).
5. Creates the Obsidian Phase note per CLAUDE.md Mandatory Phase Completion Step 1 (filesystem write).
6. Commits `docs/UAT-SERIES.md` per CLAUDE.md Mandatory Phase Completion Step 4.

This plan is `autonomous: false` because the global UAT is a human-verified checkpoint — visual confirmation of `docker compose ps` across 60+ containers (twice, with a volume-preservation check between) is faster and more reliable as a checkpoint than as a scripted assertion across the whole compose tree.

Structural template: this plan mirrors `999.83-05-PLAN.md` (the sibling phase wrap plan).

Purpose: Close out the phase with one commit + the end-to-end UAT that proves all three macOS bugs are fixed and remain fixed on re-runs.
Output: UAT pass; one commit on `docs/UAT-SERIES.md`; two vault files written outside git per CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/999.84-chaos-lab-macos-mount-compat/CONTEXT.md
@.planning/phases/999.84-chaos-lab-macos-mount-compat/999.84-01-SUMMARY.md
@.planning/phases/999.84-chaos-lab-macos-mount-compat/999.84-02-SUMMARY.md
@.planning/phases/999.84-chaos-lab-macos-mount-compat/999.84-03-SUMMARY.md

<interfaces>
UAT invocation (CONTEXT locked):
- `./lab.sh down && ./lab.sh all`
- Pass criterion: after 60s settle, `docker compose -p chaoslab ps -a` shows zero containers in `Restarting`, `Exited (1)`, `Exited (2)`, `Exited (22)`, or `unhealthy` state
- Seed containers in `Exited (0)` are EXPECTED and correct
- Kerberos auto-skip on macOS continues to apply (LAB_INCLUDE_KERBEROS not set)
- `lab.sh down` correctly sweeps profile-tagged services via the `--profile "*" --remove-orphans` logic (commit 357344a, Phase 41-06) — no workaround needed; the project memory entry that said otherwise was stale and corrected in Phase 999.83

Three bug-fix spot-checks (from Plans 01-03 individual UATs, repeated here in the global UAT context):
- ldaps: `docker ps --filter "name=chaoslab-ldaps-1" --format '{{.Status}}'` → `Up`
- rabbitmq-broker: `docker ps --filter "name=chaoslab-rabbitmq-broker-1" --format '{{.Status}}'` → `Up.*healthy`
- gitea-seed: `docker ps --filter "name=chaoslab-gitea-seed-1" -a --format '{{.Status}}'` → `Exited (0)` (on BOTH first run and re-run)

docs/UAT-SERIES.md target series: the chaos-lab series (likely UAT-1 — confirm by reading the file's series headings during read_first). Phase 999.83 added its own entry to this series; Phase 999.84 appends after it.

Obsidian vault paths (CLAUDE.md mandates filesystem writes for both, not CLI content= — files too large):
- Phase note: `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-999.84-Chaos-Lab-macOS-Host-Mount-Compat.md`
- UAT-SERIES sync: `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md`

Obsidian sync recipe (from CLAUDE.md Step 3):
- `printf "---\nproject: QU.I.R.K.\ntype: reference\nstatus: active\nsource: docs/UAT-SERIES.md\nupdated: 2026-05-15\n---\n\n" > /tmp/uat_vault.md`
- `cat docs/UAT-SERIES.md >> /tmp/uat_vault.md`
- `cp /tmp/uat_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"`

Phase note frontmatter (from CLAUDE.md Step 1 + reference Phases/ template):
- `project: QU.I.R.K.`
- `type: phase`
- `status: complete`
- `source: .planning/phases/999.84-chaos-lab-macos-mount-compat/`
- `updated: 2026-05-15`

Phase note body sections (per CLAUDE.md Step 1 template):
- Goal (from ROADMAP Phase 999.84 entry)
- Requirements Covered (BACK-91)
- Success Criteria (the 6 from ROADMAP)
- What Was Built — one subsection per Plan (01 / 02 / 03), sourced from each SUMMARY.md
- `[[Roadmap]]` link
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Global end-to-end UAT on macOS — first run + re-run with volumes preserved</name>
  <what-built>Plans 01-03 fixes (ldaps macOS-host compat, rabbitmq-broker erlang cookie env, gitea-seed idempotency) are all committed. This UAT confirms the lab comes up clean on macOS AND stays clean on a re-run (the gitea_data persistence scenario that CONTEXT locked).</what-built>
  <how-to-verify>
    From `/Volumes/Digs-1TB/Development/quantum-apps/QUIRK/quantum-chaos-enterprise-lab/` on macOS Docker Desktop.

    **First run (clean slate):**
    1. `./lab.sh down`
    2. Optional clean of gitea data only if you want the absolute fresh-install state: `docker volume rm chaoslab_gitea_data 2>/dev/null || true`. NOT REQUIRED — first run works either way.
    3. `./lab.sh all`
    4. Wait 60 seconds for healthchecks + seed containers.
    5. `docker compose -p chaoslab ps -a`. Confirm:
       - Zero containers in `Restarting`
       - Zero in `Exited (1)`
       - Zero in `Exited (2)`
       - Zero in `Exited (22)` (gitea-seed regression marker)
       - Zero in `(unhealthy)`
       - Long-running services show `Up (healthy)` or `Up`
       - Seed containers (gitea-seed, minio-seed, vault-30-seed, registry-seed, etc.) show `Exited (0)` — correct
       - Kerberos services NOT present (auto-skipped on macOS) — correct
    6. Three bug-fix spot-checks:
       - `docker ps --filter "name=chaoslab-ldaps-1" --format '{{.Status}}'` → starts with `Up`
       - `docker ps --filter "name=chaoslab-rabbitmq-broker-1" --format '{{.Status}}'` → matches `Up.*healthy`
       - `docker ps --filter "name=chaoslab-gitea-seed-1" -a --format '{{.Status}}'` → `Exited (0)`
    7. Smoke ldaps protocol: `openssl s_client -connect localhost:636 -showcerts < /dev/null 2>&1 | grep -c "BEGIN CERTIFICATE"` ≥ 1
    8. Smoke rabbitmq AMQP: `(echo >/dev/tcp/localhost/25672) 2>&1 && echo AMQP_OK` returns AMQP_OK (or use `nc -zv localhost 25672` if available)

    **Re-run with volumes preserved (THE CRITICAL CONTEXT-LOCKED CHECK):**
    9. `./lab.sh down` (this is `compose down --remove-orphans` — does NOT remove named volumes)
    10. Verify gitea volume persisted: `docker volume ls | grep -c chaoslab_gitea_data` returns 1
    11. `./lab.sh all`
    12. Wait 60s.
    13. `docker compose -p chaoslab ps -a` — same zero-failure criteria as step 5. Especially:
        - `chaoslab-gitea-seed-1` shows `Exited (0)` (the bug 3 regression scenario, now fixed)
    14. `docker logs chaoslab-gitea-seed-1 2>&1 | grep -ci "Repo already exists, skipping"` returns 3 (proves the idempotency path actually executed, not just that the script happened to succeed)
    15. `docker logs chaoslab-ldaps-1 2>&1 | grep -ci "Read-only file system\|chown.*denied"` returns 0
    16. `docker logs chaoslab-rabbitmq-broker-1 2>&1 | grep -ci "eacces\|.erlang.cookie"` returns 0
    17. Smoke ldaps + AMQP again (repeat steps 7-8) — both still pass
    18. `./lab.sh down`

    **Plan 01 fallback delta spot-check (only if Plan 01 took the bitnami/openldap fallback):**
    19. `grep -c "bitnami/openldap" quantum-chaos-enterprise-lab/README.md` returns ≥ 1 (Plan 01 edit landed)
    20. `grep -c "bitnami/openldap" quantum-chaos-enterprise-lab/expected_results_v4.md` returns ≥ 1 (Plan 01 oracle delta landed)
    21. No further README / oracle edits are made in this plan — Plan 01 owned that work per CLAUDE.md chaos-lab rule.

    **Failure handling:** if any step fails, capture `docker compose -p chaoslab ps -a` and `docker logs <failing-service> 2>&1 | tail -100` for the failing service(s). Do NOT silently continue to Task 2 — pause and diagnose.
  </how-to-verify>
  <resume-signal>Type "approved" once both runs (steps 1-17) pass and any Plan 01 fallback deltas (steps 19-20, if applicable) are confirmed; or paste failing logs.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update docs/UAT-SERIES.md + sync to Obsidian + create Phase note + commit</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (full file — identify the chaos-lab series to append to; the Phase 999.83 entry added recently is a good locator)
    - .planning/phases/999.84-chaos-lab-macos-mount-compat/999.84-01-SUMMARY.md (bug 1 outcome — which option taken)
    - .planning/phases/999.84-chaos-lab-macos-mount-compat/999.84-02-SUMMARY.md (bug 2 confirmation)
    - .planning/phases/999.84-chaos-lab-macos-mount-compat/999.84-03-SUMMARY.md (bug 3 idempotency)
    - .planning/ROADMAP.md lines 1462-1474 (Phase 999.84 entry — Goal + 6 success criteria, source for Phase note)
    - CLAUDE.md "Mandatory Phase Completion Steps" + "Obsidian Vault Integration" sections
    - one existing Phase note at `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/` (mirror its frontmatter style — recommend reading Phase-999.83-Chaos-Lab-Service-Config-Drift.md as the closest precedent)
  </read_first>
  <action>
    **Step A — `docs/UAT-SERIES.md` update:**

    - Append a Phase 999.84 entry to the relevant chaos-lab UAT series (likely UAT-1; the file should already contain the Phase 999.83 entry from the sibling phase — append after it).
    - Entry content (short subsection): heading `Phase 999.84 — Chaos Lab macOS Host-Mount Compat`; bullet list of the three bug fixes, each one line:
      - Bug 1 — ldaps on macOS: describe which option was taken (KEEP_EXISTING_CONFIG env on osixia, OR image swap to bitnami/openldap:2.6) per 999.84-01-SUMMARY
      - Bug 2 — rabbitmq-broker: RABBITMQ_ERLANG_COOKIE env (cross-platform)
      - Bug 3 — gitea-seed idempotency: repo_exists existence-check (option b) + put_file `|| true` (option a) mixed strategy
    - One closing line noting the UAT pattern that proves the fix: `./lab.sh down && ./lab.sh all` run twice in a row with named volumes preserved produces zero Exited(22) from gitea-seed on the second run.
    - Update `**Last Updated:**` line at the top of the file to `2026-05-15`.

    **Step B — Sync UAT-SERIES.md to Obsidian (filesystem write per CLAUDE.md — file too large for CLI content= arg):**

    Run these three commands in order:
    - `printf "---\nproject: QU.I.R.K.\ntype: reference\nstatus: active\nsource: docs/UAT-SERIES.md\nupdated: 2026-05-15\n---\n\n" > /tmp/uat_vault.md`
    - `cat docs/UAT-SERIES.md >> /tmp/uat_vault.md`
    - `cp /tmp/uat_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"`

    **Step C — Create Obsidian Phase note (filesystem write, NOT obsidian CLI content=):**

    Path: `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-999.84-Chaos-Lab-macOS-Host-Mount-Compat.md`

    Frontmatter (exact keys, no extras):
    - `project: QU.I.R.K.`
    - `type: phase`
    - `status: complete`
    - `source: .planning/phases/999.84-chaos-lab-macos-mount-compat/`
    - `updated: 2026-05-15`

    Body sections (mirror Phase-999.83 note template):
    1. `## Goal` — paste the Goal paragraph from ROADMAP.md Phase 999.84 entry (lines 1463-1464)
    2. `## Requirements Covered` — single-row table or bullet: `BACK-91` with one-line description
    3. `## Success Criteria` — the 6 from ROADMAP, each as a checkbox item marked `[x]`
    4. `## What Was Built` — three subsections (`### Plan 01 — ldaps macOS-host compat`, `### Plan 02 — rabbitmq-broker erlang cookie`, `### Plan 03 — gitea-seed idempotency`). Each subsection: source the body from the corresponding 999.84-NN-SUMMARY.md file (paste the relevant outcome paragraphs verbatim; do not paraphrase).
    5. `## See Also` — `[[Roadmap]]` link

    Write with the Write tool, not via `cat << EOF` heredoc (CLAUDE.md prohibits heredoc-style file creation).

    **Step D — Single commit for the UAT-SERIES.md update (use project's commit tooling per CLAUDE.md Step 4):**

    `node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-999.84): update UAT-SERIES.md" --files docs/UAT-SERIES.md`

    The Obsidian vault files (Phase note + UAT-Series sync) are NOT committed to git — they live outside the repo per CLAUDE.md.

    Do NOT modify CLAUDE.md, ROADMAP.md, or any other project files in this task — ROADMAP/STATE updates happen via the orchestrator's `update_roadmap` step after this plan completes.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && (grep -c "999.84" docs/UAT-SERIES.md) && (grep -c "Last Updated.*2026-05-15" docs/UAT-SERIES.md) && test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-999.84-Chaos-Lab-macOS-Host-Mount-Compat.md" && (grep -c "status: complete" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-999.84-Chaos-Lab-macOS-Host-Mount-Compat.md") && test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" && echo VAULT_OK</automated>
  </verify>
  <done>
    docs/UAT-SERIES.md updated + committed with `docs(phase-999.84):` prefix; vault Phase note + UAT-Series sync written; CLAUDE.md Mandatory Phase Completion Steps 1-4 satisfied.
  </done>
</task>

</tasks>

<verification>
- Global UAT on macOS passes on BOTH first run AND re-run (with chaoslab_gitea_data preserved between)
- ldaps Up, rabbitmq-broker Up(healthy), gitea-seed Exited(0) both times
- Zero Restarting / Exited(1) / Exited(2) / Exited(22) / unhealthy containers in either run
- docs/UAT-SERIES.md updated + committed
- Obsidian Phase-999.84 note + UAT-Series sync written to vault filesystem
</verification>

<success_criteria>
- ROADMAP success criteria 1-4 all visually confirmed in the global UAT
- ROADMAP success criterion 5 (no Linux regression) — implicit from cross-platform fix choices in Plans 01-03; called out in each plan's must_haves
- ROADMAP success criterion 6 (CLAUDE.md chaos-lab rule) — satisfied by Plan 01's commit if fallback taken; spot-checked here
- CLAUDE.md Mandatory Phase Completion Steps 1-4 satisfied (Phase note, UAT-SERIES update, UAT-Series sync, UAT-SERIES commit)
</success_criteria>

<output>
Create `.planning/phases/999.84-chaos-lab-macos-mount-compat/999.84-04-SUMMARY.md` documenting: paste of `docker compose -p chaoslab ps -a` summary line from both first run and re-run, the three bug-fix spot-check outputs, UAT-SERIES.md commit hash, Obsidian vault file paths, and confirmation that Plan 01's chosen branch (option c or option a) ended up working in the global context.
</output>
