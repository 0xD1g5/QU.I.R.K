---
phase: 106-architecture-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture-distributed.md
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-03, ARCH-04]

must_haves:
  truths:
    - "docs/architecture-distributed.md exists and fully specifies the sensor→console wire payload schema including payload_id, pushed_at, received_at, schema_version, sensor_version"
    - "The doc locks the additive data-model: sensor_id + segment nullable on CryptoEndpoint, with (sensor_id, host, port) uniqueness key distinguishing same RFC1918 IPs across segments"
    - "The doc records the three committed PM decisions: Option A unified scoring, one-time-use enrollment tokens, Windows v5.4 floor-vs-ceiling scope"
    - "The doc explicitly enumerates every forbidden addition: Celery, Redis, MQTT/RabbitMQ, PostgreSQL, JWT per-sensor tokens, mTLS/PKI infra, tenant_id, sbommerge, CycloneDX CLI merge, pywin32 Service"
    - "The doc contains exactly two Mermaid diagrams (topology + sensor→console push sequence) that render in GitHub/Obsidian"
  artifacts:
    - path: "docs/architecture-distributed.md"
      provides: "Canonical v5.4 distributed-scanner architecture contract; 10 locked sections"
      contains: "## 3. Wire Contract"
      min_lines: 200
  key_links:
    - from: "docs/architecture-distributed.md §3 Wire Contract"
      to: "Phase 107 sensor_pushes table + Phase 108/109 wire format"
      via: "payload_id / pushed_at / received_at / schema_version / sensor_version field spec"
      pattern: "payload_id"
    - from: "docs/architecture-distributed.md §6 Enrollment & auth"
      to: "Phase 109 ingest route auth"
      via: "inherited require_auth at APIRouter dependencies + HMAC-SHA256 X-Sensor-Signature"
      pattern: "X-Sensor-Signature"
---

<objective>
Author the single canonical v5.4 distributed-scanner architecture document at
`docs/architecture-distributed.md`. This is the no-code gating anchor: every downstream
phase (107–112) cites this doc as its contract. The document locks the wire payload schema,
additive data-model keying, the three committed PM decisions, and the forbidden-additions
list, exactly as decided in 106-CONTEXT.md (D-01..D-15).

Purpose: Lock every decision that would be expensive to change mid-milestone BEFORE any code ships.
Output: `docs/architecture-distributed.md` with the 10 locked sections and two Mermaid diagrams.

THIS PHASE SHIPS ZERO RUNTIME CODE. No schema migrations, no CLI changes, no Python files.
The entire deliverable is one Markdown document.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-architecture-documentation/106-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/REQUIREMENTS.md

# CONTEXT.md is the PRIMARY SOURCE OF TRUTH. Where CONTEXT.md D-NN decisions conflict
# with anything in RESEARCH.md, CONTEXT.md wins.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author core architecture sections (1–7) — overview, topology, wire contract, push sequence, data-model keying, enrollment/auth, merge pipeline</name>
  <files>docs/architecture-distributed.md</files>
  <read_first>
    - .planning/phases/106-architecture-documentation/106-CONTEXT.md (ALL decisions D-01..D-15; §D-12 the exact 10-section outline; §specifics for X-Sensor-Signature, file-per-payload spool, console_utc echo)
    - .planning/research/ARCHITECTURE.md (component map, subcommand-dispatch pattern)
    - .planning/research/PITFALLS.md (same-IP collision, auth bypass, partial-merge, replay/DoS, backward-compat pitfalls the doc must address)
    - quirk/models.py (confirm `class CryptoEndpoint` at L9, `class IntegrationDelivery` at L245 before citing)
    - quirk/intelligence/scoring.py (confirm `def compute_readiness_score` ~L119 and the six-subscore `/ 1.5` rollup ~L290 before citing Option A)
    - quirk/cbom/builder.py (confirm `def build_cbom` ~L445 + Pass-1 `algo_registry` ~L461 before citing component-identity-must-include-sensor_id)
  </read_first>
  <action>
    Create `docs/architecture-distributed.md`. Begin with frontmatter-style title and a one-line
    statement that this is the canonical v5.4 contract and ships no code. Author sections 1–7 of the
    locked outline in CONTEXT.md D-12 (verbatim heading numbering and names). Content per section:

    - `## 1. Overview & design invariants` — sensor/console split; single-tenant only; additive
      schema only (NULL sensor_id = implicit local, D-03); OS-agnostic wire contract; reuse v5.3
      security primitives; same package + new `quirk sensor` / `quirk console` mode flags (no package
      split — cite the subcommand-dispatch pattern in run_scan.py ~L381-514, intercept like serve/schedule/token).
    - `## 2. Topology diagram (Mermaid)` — ONE ```mermaid block showing N segment-local sensors →
      HTTPS push → console FastAPI ingest → SQLite → merge → unified CBOM/score. Must render on GitHub.
    - `## 3. Wire Contract` — the payload schema. MUST name every field: `payload_id` (UUID, unique
      per push), `pushed_at`, `received_at`, `schema_version`, `sensor_version`, plus the endpoint
      findings payload. Present as a field table AND a JSON example (Claude's discretion permits both;
      do both). State transport: HTTPS + zstandard level-3 + httpx (reuse existing), and the
      `X-Sensor-Signature: hmac-sha256=<hex>` application-layer integrity header (per-sensor
      token-derived; NO mTLS — D-04). State the air-gap carve-out (D-15): `quirk sensor export-results`
      writes the IDENTICAL payload (same payload_id/schema_version/pushed_at/HMAC/zstd) to a file;
      `quirk console import-results` runs it through the SAME ingest+dedup path.
    - `## 4. Push sequence diagram (Mermaid)` — ONE ```mermaid sequenceDiagram: sensor scan →
      sign → push → console auth check → body-size/replay/dedup gates → 200/4xx → audit row. Show the
      409 (dup payload_id), 413 (>10MB), and replay-window-reject branches.
    - `## 5. Data-model keying` — `(sensor_id, host, port)` uniqueness key (D-03). `sensor_id`
      (nullable String, indexed) and `segment` (nullable String) added to CryptoEndpoint via the
      existing `_ensure_columns` / `_ADDITIVE_MIGRATIONS` additive-migration path (no Alembic).
      NULL sensor_id = implicit local sensor (backward-compatible). CBOM Pass-1 component identity
      hash MUST include sensor_id so the same RFC1918 host:port in two segments yields two components.
    - `## 6. Enrollment & auth model` — one-time-use tokens (D-02): `secrets.token_urlsafe(32)`,
      SHA-256 hash stored in `sensor_tokens`, raw token never persisted, consumed at enrollment
      (mirrors token_cmd.py pattern ~L100). Enrollment manifest field set (D-13): sensor_id (UUID PK),
      segment, engagement (nullable), enrolled_at, last_push_at (nullable), expected_cadence_minutes,
      sensor_version (nullable). Auth: ingest route inherits `Depends(require_auth)` at the
      `APIRouter(dependencies=[...])` level (anti-bypass rule — cite auth.py require_auth ~L34,
      hmac.compare_digest ~L54/L61). State the replay policy: ±15-min window on `pushed_at` vs
      `received_at` for HTTPS push (D-08), with `console_utc` echoed in rejection responses;
      transport-conditional carve-out (D-15): air-gap import SKIPS the time-window check but KEEPS
      payload_id dedup. Body-size limit 10 MB → 413 (D-09). payload_id dedup → 409 (D-07), accepted
      payload_ids retained indefinitely / no TTL in v5.4 (D-10). Version-skew (D-11): Pydantic ingest
      uses `extra='ignore'`, version mismatch never blocks ingest — surfaces a non-blocking skew warning.
    - `## 7. Merge pipeline` — manual `quirk sensor merge` in v5.4 (D-06); `merge_scan()` built as a
      standalone callable so a v5.5 auto-trigger needs no refactor (record the seam only, no poller).
      Option A scoring (D-01): re-run the UNION of all sensor findings through the unchanged engine
      chain `build_evidence_summary` → `compute_readiness_score` → `build_cbom`. NEVER average
      pre-scored sub-results (mathematically wrong — ratio penalties use full-population denominators);
      `per_segment_scores` added for context only. Coverage/staleness (D-14): default
      `expected_cadence_minutes`=1440; sensor overdue when `now > last_push_at + 2× cadence`; merge
      emits `coverage_warning` listing overdue sensor_ids (null when all current); partial coverage is
      scored but ALWAYS flagged, never silently merged as complete; results stale after 30 days; all
      thresholds operator-overridable. Sensor-local scanned_at timestamps preserved (not rewritten).

    Do NOT paste implementation code. Describe target contracts in prose + field tables + the two
    required Mermaid blocks. Cite seams by file + symbol (confirmed from read_first), not by blind line number.
  </action>
  <acceptance_criteria>
    - `docs/architecture-distributed.md` exists
    - File contains each heading: `## 1. Overview`, `## 2. Topology`, `## 3. Wire Contract`, `## 4. Push`, `## 5. Data-model`, `## 6. Enrollment`, `## 7. Merge` (grep each)
    - `grep -c '^\`\`\`mermaid' docs/architecture-distributed.md` returns at least 2
    - File contains all of: `payload_id`, `pushed_at`, `received_at`, `schema_version`, `sensor_version` (grep each present)
    - File contains all of: `sensor_id`, `segment`, `(sensor_id, host, port)` (grep each)
    - File contains all of: `X-Sensor-Signature`, `hmac-sha256`, `console_utc`, `409`, `413`, `±15` (or `15-min`/`15 min`), `10 MB` (or `10MB`)
    - File contains `extra='ignore'` and `coverage_warning` and `per_segment_scores`
    - File contains `token_urlsafe` and `one-time` (or `one-time-use`) and `sensor_tokens`
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/architecture-distributed.md && grep -q '## 3. Wire Contract' docs/architecture-distributed.md && [ "$(grep -c '^```mermaid' docs/architecture-distributed.md)" -ge 2 ] && for t in payload_id pushed_at received_at schema_version sensor_version X-Sensor-Signature console_utc coverage_warning "extra='ignore'" token_urlsafe; do grep -q "$t" docs/architecture-distributed.md || { echo "MISSING $t"; exit 1; }; done && echo OK</automated>
  </verify>
  <done>Sections 1–7 authored with both Mermaid diagrams; all wire-contract, data-model, enrollment/auth, and merge fields present and grep-verified.</done>
</task>

<task type="auto">
  <name>Task 2: Author decision-lock sections (8–10) — committed PM decisions, forbidden additions, Windows scope</name>
  <files>docs/architecture-distributed.md</files>
  <read_first>
    - .planning/phases/106-architecture-documentation/106-CONTEXT.md (D-01, D-02, D-05 for PM decisions; D-04 for the full forbidden list; D-05 for Windows floor/ceiling)
    - quirk/cli/scheduler_cmd.py (confirm POSIX-isms before citing: relative `Path("output/scheduled")` ~L136; `signal.SIGTERM` handler ~L259 — these are the floor's POSIX-ism audit targets)
    - .planning/REQUIREMENTS.md (ARCH-03 wording for committed decisions; ARCH-04 wording for forbidden list)
  </read_first>
  <action>
    Append sections 8–10 to `docs/architecture-distributed.md` (verbatim heading numbering/names from D-12):

    - `## 8. Committed PM decisions` — the three ARCH-03 decisions, each with rationale:
      (1) Option A unified scoring (D-01) — union through the unchanged engine, not Option B
      weighted-average or Option C weakest-link; (2) one-time-use enrollment tokens (D-02) — not
      time-windowed; (3) Windows v5.4 scope = floor-in / ceiling→v5.5 (D-05). Cross-reference §7 and §6.

    - `## 9. Forbidden additions` — a concrete violation reference table/list enumerating EVERY item
      from D-04: Celery, Redis, MQTT/RabbitMQ, PostgreSQL, JWT per-sensor tokens, mTLS/PKI infra,
      `tenant_id`, sbommerge, CycloneDX CLI merge, pywin32 Service. For each: one line on what is used
      instead (e.g. tenant_id → single-tenant only; Celery/Redis → no async queue, manual merge;
      mTLS → HMAC-SHA256 app-layer signature; JWT per-sensor → SHA-256-hashed one-time tokens;
      Postgres → SQLite + additive migrations; sbommerge/CycloneDX CLI merge → re-run build_cbom over
      the union; pywin32 Service → Scheduled Task in v5.5 ceiling). State that any downstream phase
      introducing one of these is a documented architecture violation.

    - `## 10. Windows scope (floor / ceiling)` — D-05 in full. FLOOR (v5.4): OS-agnostic wire
      contract; `pip install` on Python 3.11+ with no POSIX dependencies; POSIX-ism audit naming the
      two scheduler_cmd.py targets (relative `output/scheduled` path → `cfg.output_root / "scheduled"`;
      SIGTERM handler → platform-conditional); `platformdirs` for data dirs; `windows-latest` CI smoke
      job as a HARD gate (NOT continue-on-error). CEILING (→ v5.5): full PyInstaller frozen EXE +
      Windows Scheduled Task registration + signed packaging. pywin32 Windows Service is OUT entirely.
  </action>
  <acceptance_criteria>
    - File contains headings: `## 8. Committed PM decisions`, `## 9. Forbidden additions`, `## 10. Windows scope`
    - §9 contains ALL of: `Celery`, `Redis`, `MQTT`, `RabbitMQ`, `PostgreSQL` (or `Postgres`), `JWT`, `mTLS`, `tenant_id`, `sbommerge`, `pywin32`
    - §10 contains all of: `floor` (or `Floor`), `ceiling` (or `Ceiling`), `PyInstaller`, `windows-latest`, `platformdirs`, `Scheduled Task`
    - §8 contains all of: `Option A`, `one-time` (or `one-time-use`)
  </acceptance_criteria>
  <verify>
    <automated>grep -q '## 9. Forbidden additions' docs/architecture-distributed.md && for t in Celery Redis MQTT RabbitMQ JWT mTLS tenant_id sbommerge pywin32 PyInstaller windows-latest platformdirs "Option A"; do grep -q "$t" docs/architecture-distributed.md || { echo "MISSING $t"; exit 1; }; done && (grep -q PostgreSQL docs/architecture-distributed.md || grep -q Postgres docs/architecture-distributed.md) && echo OK</automated>
  </verify>
  <done>Sections 8–10 appended; all three PM decisions recorded, full forbidden list enumerated, Windows floor/ceiling locked. All ten sections now present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new this phase) | This phase ships ZERO runtime code — no new runtime attack surface is introduced. |

## STRIDE Threat Register

This is a documentation-only phase. No HIGH runtime threats exist because no code runs. The
inverse is the security-relevant point: **this document is where security-critical contracts get
LOCKED for downstream phases**. The plan ensures the doc commits the mitigations below so phases
108–110 implement them. Failure mode for this phase = a missing/ambiguous contract, not an exploit.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-01 | Spoofing | sensor→console push integrity | mitigate (in doc) | Doc §3/§6 commits HMAC-SHA256 `X-Sensor-Signature` application-layer signature (per-sensor token-derived); no mTLS in v5.4 |
| T-106-02 | Elevation | ingest route auth bypass | mitigate (in doc) | Doc §6 commits inherited `Depends(require_auth)` at `APIRouter(dependencies=[...])` level (anti-bypass rule) |
| T-106-03 | Tampering/Replay | replayed or stale push | mitigate (in doc) | Doc §6 commits ±15-min replay window (HTTPS-only, transport-conditional per D-15) + `payload_id` dedup → 409 |
| T-106-04 | Denial of Service | oversized push body | mitigate (in doc) | Doc §6 commits 10 MB body-size limit → HTTP 413 (FastAPI has no default) |
| T-106-05 | Info Disclosure | exception text in audit/responses | mitigate (in doc) | Doc commits `safe_str()` coverage on push exception stringification (downstream Phase 109) |
| T-106-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase (doc-only). Dep additions (`platformdirs`, `tenacity`) are deferred to phases 108+ and gated there. |
</threat_model>

<verification>
- All ten section headings (§1–§10) present, in order.
- Exactly the two required Mermaid blocks present and well-formed (open with ```mermaid, close with ```).
- All five wire-contract fields, the data-model keying triple, the full forbidden list, and the three PM decisions grep-verified.
- No new Python files created (`git status` shows only docs/architecture-distributed.md).
</verification>

<success_criteria>
ARCH-01: wire payload schema (incl. payload_id/pushed_at/received_at/schema_version/sensor_version),
service roles, enrollment/auth model, and merge pipeline fully specified.
ARCH-02: additive data-model locked — sensor_id + segment nullable on CryptoEndpoint, (sensor_id, host, port) key.
ARCH-03: three committed PM decisions recorded (Option A, one-time tokens, Windows floor/ceiling).
ARCH-04: forbidden additions explicitly enumerated as a concrete violation reference.
</success_criteria>

<output>
Create `.planning/phases/106-architecture-documentation/106-01-SUMMARY.md` when done
</output>
