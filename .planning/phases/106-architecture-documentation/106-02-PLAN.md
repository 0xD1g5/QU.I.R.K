---
phase: 106-architecture-documentation
plan: 02
type: execute
wave: 2
depends_on: ["106-01"]
files_modified:
  - docs/architecture-distributed.md
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-03, ARCH-04]

must_haves:
  truths:
    - "Every code seam the doc cites (file + symbol) matches the actual current code in the repo — no stale line references that point to the wrong symbol"
    - "Every ARCH-0N requirement maps to a concrete section of the document"
    - "The document is synced to the Obsidian vault per project convention"
  artifacts:
    - path: "docs/architecture-distributed.md"
      provides: "Verified canonical contract with accurate seam citations"
      contains: "compute_readiness_score"
  key_links:
    - from: "docs/architecture-distributed.md seam citations"
      to: "quirk/models.py, quirk/db.py, quirk/intelligence/scoring.py, quirk/cbom/builder.py, quirk/dashboard/api/middleware/auth.py, quirk/cli/token_cmd.py, quirk/cli/scheduler_cmd.py"
      via: "file + symbol references"
      pattern: "compute_readiness_score"
---

<objective>
Verify the document authored in 106-01 against the four ROADMAP success criteria and against the
ACTUAL current code seams (so cited symbols are not stale), then sync the finished document to the
Obsidian vault per project convention. Still zero runtime code.

Purpose: Guarantee the canonical contract is internally complete and that every code citation
downstream phases will rely on actually resolves to the right symbol today.
Output: A verified `docs/architecture-distributed.md` (citations corrected if drifted) + Obsidian sync.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-architecture-documentation/106-CONTEXT.md
@.planning/phases/106-architecture-documentation/106-01-SUMMARY.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
<!-- Confirmed seam locations as of planning (executor must re-confirm symbols, correct doc if drifted): -->
quirk/models.py            : class CryptoEndpoint (L9), class IntegrationDelivery (L245)
quirk/db.py                : _ensure_columns (L127), _ADDITIVE_MIGRATIONS (L172)
quirk/intelligence/scoring.py : compute_readiness_score (L119), six-subscore "/ 1.5" rollup (L290)
quirk/cbom/builder.py      : build_cbom (L445), Pass-1 algo_registry (L461)
quirk/dashboard/api/middleware/auth.py : require_auth (L34), hmac.compare_digest (L54, L61)
quirk/cli/token_cmd.py     : secrets.token_urlsafe(32) (L100)
quirk/cli/scheduler_cmd.py : _dispatch_schedule (L110), relative output/scheduled path (L136), SIGTERM handler (L259)
run_scan.py                : subcommand dispatch (~L381-514)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify seam citations against current code + requirement→section coverage; correct any drift</name>
  <files>docs/architecture-distributed.md</files>
  <read_first>
    - docs/architecture-distributed.md (the authored doc from 106-01)
    - quirk/models.py (confirm CryptoEndpoint / IntegrationDelivery symbols at the cited locations)
    - quirk/db.py (confirm _ensure_columns + _ADDITIVE_MIGRATIONS)
    - quirk/intelligence/scoring.py (confirm compute_readiness_score + the /1.5 rollup)
    - quirk/cbom/builder.py (confirm build_cbom + Pass-1 algo_registry)
    - quirk/dashboard/api/middleware/auth.py (confirm require_auth + hmac.compare_digest)
    - quirk/cli/token_cmd.py (confirm token_urlsafe enrollment pattern)
    - quirk/cli/scheduler_cmd.py (confirm the two POSIX-ism targets)
    - .planning/REQUIREMENTS.md (ARCH-01..04 exact wording)
  </read_first>
  <action>
    For each seam the doc cites: open the named file, confirm the cited symbol still exists. If the doc
    cites a line number that no longer points at the named symbol, correct the doc to the current
    location (prefer citing by symbol name + approximate line, so future drift is tolerant). Do NOT
    invent new content — only fix citations that drifted from the authored doc.

    Then verify requirement→section coverage: confirm each of ARCH-01..04 maps to a concrete section
    (ARCH-01 → §1/§3/§6/§7; ARCH-02 → §5; ARCH-03 → §8; ARCH-04 → §9, with §10 covering the Windows
    half of ARCH-03). If any requirement is unaddressed, add the missing content (pulling the decision
    text from CONTEXT.md). Add a short `## Requirement Coverage` mapping table at the end of the doc
    listing ARCH-01..04 → section(s) so the requirement linkage is explicit and grep-checkable.

    Confirm the two Mermaid blocks are well-formed (balanced ```mermaid / ``` fences) and that the
    forbidden list in §9 is complete per D-04.
  </action>
  <acceptance_criteria>
    - Doc cites all of these symbols and each grep-resolves in its named source file: `compute_readiness_score`, `_ensure_columns`, `require_auth`, `token_urlsafe`, `build_cbom` (verify the symbol exists in the corresponding quirk/ file)
    - Doc contains a `## Requirement Coverage` section listing all four: `ARCH-01`, `ARCH-02`, `ARCH-03`, `ARCH-04`
    - `grep -c '^```mermaid' docs/architecture-distributed.md` equals the count of `^```$` closing fences for those blocks (fences balanced)
    - §9 forbidden list still contains all of: Celery, Redis, MQTT, RabbitMQ, JWT, mTLS, tenant_id, sbommerge, pywin32, and PostgreSQL/Postgres
  </acceptance_criteria>
  <verify>
    <automated>for s in compute_readiness_score:quirk/intelligence/scoring.py _ensure_columns:quirk/db.py require_auth:quirk/dashboard/api/middleware/auth.py token_urlsafe:quirk/cli/token_cmd.py build_cbom:quirk/cbom/builder.py; do sym=${s%%:*}; f=${s#*:}; grep -q "$sym" docs/architecture-distributed.md && grep -q "$sym" "$f" || { echo "SEAM FAIL $sym"; exit 1; }; done && for r in ARCH-01 ARCH-02 ARCH-03 ARCH-04; do grep -q "$r" docs/architecture-distributed.md || { echo "MISSING $r"; exit 1; }; done && grep -q '## Requirement Coverage' docs/architecture-distributed.md && echo OK</automated>
  </verify>
  <done>Every cited seam symbol resolves in its source file; ARCH-01..04 each map to a section via the Requirement Coverage table; Mermaid fences balanced; forbidden list complete.</done>
</task>

<task type="auto">
  <name>Task 2: Sync the verified document to the Obsidian vault</name>
  <files>docs/architecture-distributed.md</files>
  <read_first>
    - CLAUDE.md (Obsidian Vault Integration — vault="Digs", QUIRK root 20_Dev-Work/QUIRK, frontmatter standards, guide-sync workflow; note files too large for `content=` shell expansion → write to vault filesystem directly)
    - docs/architecture-distributed.md (the verified doc to sync)
  </read_first>
  <action>
    Sync the verified `docs/architecture-distributed.md` into the Digs vault as a Reference note.
    Per CLAUDE.md, the doc is large — write directly to the vault filesystem rather than passing
    `content=` to the obsidian CLI. Prepend the required QUIRK frontmatter block
    (`project: QU.I.R.K.`, `type: reference`, `status: active`, `source: docs/architecture-distributed.md`,
    `updated: <today ISO>`) then the full doc body. Target:
    `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Reference/Distributed-Architecture.md`.
    Preserve all content and the Mermaid blocks as-is (Obsidian renders ```mermaid natively).
  </action>
  <acceptance_criteria>
    - `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Reference/Distributed-Architecture.md` exists
    - The vault note begins with a frontmatter block containing `project: QU.I.R.K.`, `type: reference`, and `source: docs/architecture-distributed.md`
    - The vault note contains `## 3. Wire Contract` and at least 2 `^```mermaid` blocks (full body synced)
  </acceptance_criteria>
  <verify>
    <automated>VN="/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Reference/Distributed-Architecture.md"; test -f "$VN" && grep -q 'type: reference' "$VN" && grep -q 'source: docs/architecture-distributed.md' "$VN" && grep -q '## 3. Wire Contract' "$VN" && [ "$(grep -c '^```mermaid' "$VN")" -ge 2 ] && echo OK</automated>
  </verify>
  <done>Verified doc synced to the QUIRK Reference folder in the Digs vault with correct frontmatter and full body including Mermaid diagrams.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new this phase) | Documentation-only; no runtime attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-V01 | Tampering | stale seam citation misleads a downstream executor | mitigate | Task 1 re-confirms every cited symbol against current code and corrects drift before phases 107+ rely on it |
| T-106-SC | Tampering | npm/pip/cargo installs | n/a | No package installs (doc-only phase). |
</threat_model>

<verification>
- All cited seam symbols resolve in their named source files.
- ARCH-01..04 each map to a doc section via the Requirement Coverage table.
- Obsidian Reference note exists with correct frontmatter and full body.
</verification>

<success_criteria>
The canonical architecture document is internally complete, its code citations are accurate against
the current repo, all four ARCH requirements are demonstrably covered, and the document is available
in the Obsidian vault for reference.
</success_criteria>

<output>
Create `.planning/phases/106-architecture-documentation/106-02-SUMMARY.md` when done
</output>
