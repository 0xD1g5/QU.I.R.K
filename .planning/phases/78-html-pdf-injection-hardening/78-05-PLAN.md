---
phase: 78-html-pdf-injection-hardening
plan: 05
type: execute
wave: 4
depends_on: [78-02, 78-03, 78-04]
files_modified:
  - tests/test_safe_filter_audit.py
  - tests/test_report_injection_hardening.py
  - docs/UAT-SERIES.md
autonomous: true
requirements:
  - HARDEN-05
  - HARDEN-03
must_haves:
  truths:
    - "tests/test_safe_filter_audit.py exits non-zero when a Jinja `| safe` filter has no upstream `| sanitize` in its filter chain"
    - "tests/test_safe_filter_audit.py exits zero against the current report.html.j2 (zero violations after Plan 02)"
    - "Positive self-test confirms gate catches synthetic `{{ x | safe }}` (no upstream sanitize)"
    - "Negative self-test confirms gate does NOT flag `{{ x | sanitize | safe }}` (paired correctly)"
    - "Forward-guard test fails if any markdown→HTML library (`markdown`, `markdown-it-py`, `mistune`) appears in pyproject.toml dependencies without paired sanitize wiring (D-78-R1)"
    - "Regression test asserts `<script>alert(1)</script>` in a synthetic certificate CN renders as `&lt;script&gt;` (or stripped to empty) in both HTML output AND PDF output"
    - "docs/UAT-SERIES.md updated to reflect HARDEN-* test cases"
    - "Obsidian phase note exists at the vault path"
  artifacts:
    - path: "tests/test_safe_filter_audit.py"
      provides: "AST CI gate for | safe pairing + forward-guard for markdown→HTML deps"
      contains: "def test_safe_filter_paired_with_sanitize"
    - path: "tests/test_report_injection_hardening.py"
      provides: "End-to-end regression: script payload in CN renders escaped in HTML + PDF"
      contains: "def test_script_payload_in_cert_cn_is_escaped"
    - path: "docs/UAT-SERIES.md"
      provides: "Updated UAT pass criteria for HARDEN-* test cases"
      contains: "HARDEN"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-78-HTML-PDF-Injection-Hardening.md"
      provides: "Obsidian phase note"
      contains: "Phase 78"
  key_links:
    - from: "tests/test_safe_filter_audit.py"
      to: "quirk/reports/templates/*.j2"
      via: "jinja2.Environment.parse + nodes.Filter walk"
      pattern: "nodes.Filter"
    - from: "tests/test_report_injection_hardening.py"
      to: "write_reports (HTML + PDF paths)"
      via: "synthetic adversarial endpoint + finding"
      pattern: "write_reports"
---

<objective>
Land the Phase 78 CI gate, regression test, forward-guard test (D-78-R1
markdown→HTML deferred sink), update UAT-SERIES.md + sync to Obsidian, and
create the Obsidian phase note. This is the final plan — it gates future
drift and closes the phase.

Per D-78-R1 (RESEARCH delta): there is NO markdown→HTML conversion path in
`html_renderer.py` today. CONTEXT.md's "post-clean markdown→HTML" decision
is re-cast as a CI assertion that prohibits adding such a library without
paired sanitize wiring — NOT as active sanitization code.

Output: AST gate test, end-to-end regression test, UAT-SERIES.md update +
Obsidian sync + commit, Obsidian phase note, atomic commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-html-pdf-injection-hardening/78-CONTEXT.md
@.planning/phases/78-html-pdf-injection-hardening/78-RESEARCH.md
@.planning/phases/78-html-pdf-injection-hardening/78-PATTERNS.md
@.planning/phases/78-html-pdf-injection-hardening/78-01-SUMMARY.md
@.planning/phases/78-html-pdf-injection-hardening/78-02-SUMMARY.md
@.planning/phases/78-html-pdf-injection-hardening/78-03-SUMMARY.md
@.planning/phases/78-html-pdf-injection-hardening/78-04-SUMMARY.md
@tests/test_scan_error_gate.py
@tests/test_report_sanitization.py
@tests/test_pdf_render_hardening.py
@docs/UAT-SERIES.md
@CLAUDE.md

<interfaces>
<!-- AST gate model: tests/test_scan_error_gate.py (Phase 59 LEAK-03) — copy   -->
<!-- structure verbatim; rename safe_str → sanitize_scanner_text taxonomy.    -->

<!-- Jinja AST walker (RESEARCH §AST CI Gate sketch):                          -->
```python
import jinja2
from jinja2 import nodes
def _has_upstream_sanitize(filter_node: nodes.Filter) -> bool:
    cur = filter_node.node
    while isinstance(cur, nodes.Filter):
        if cur.name == "sanitize":
            return True
        cur = cur.node
    return False
# Walk every .j2 in quirk/reports/templates/; for each nodes.Filter where
# name=="safe", assert _has_upstream_sanitize is True. Violations fail.
```

<!-- Regression test analog (RESEARCH §Regression Test Design + PATTERNS §7):  -->
<!-- Use SimpleNamespace + write_reports + read back report-*.html + PDF.      -->
<!-- Mock pattern available at tests/test_pdf_render_hardening.py:15-56.       -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AST CI gate tests/test_safe_filter_audit.py</name>
  <files>tests/test_safe_filter_audit.py</files>
  <behavior>
    - Module imports stdlib `ast`, `pathlib`, plus `jinja2` and `jinja2.nodes`; module docstring cites Phase 78 / HARDEN-05 + D-78-R1
    - `test_safe_filter_paired_with_sanitize`: walks every `.j2` file under `quirk/reports/templates/`, parses via `jinja2.Environment().parse(source)`, finds every `nodes.Filter` where `.name == "safe"`, asserts each has an upstream `sanitize` filter via `_has_upstream_sanitize`. Exits zero against the current template (Plan 02 left zero `| safe` usages); will fail loudly when a future PR adds an unpaired `| safe`
    - `test_gate_catches_synthetic_bypass`: parse `"{{ scanner_string | safe }}"` (no sanitize upstream) — assert exactly one violation detected. This is the positive self-test from Phase 59 model
    - `test_gate_does_not_flag_safe_patterns`: parse `"{{ scanner_string | sanitize | safe }}"` — assert zero violations. Negative self-test
    - `test_filter_lineno_populated`: parse a known fragment and assert `nodes.Filter.lineno > 0` (per RESEARCH R-5; if `lineno == 0`, gate falls back to template-path-only reporting — encode that fallback rather than failing)
    - `test_no_markdown_to_html_lib_in_deps` (forward guard for D-78-R1): parse `pyproject.toml` via `tomllib`, walk `[project] dependencies` AND every `[project.optional-dependencies]` extra, assert NO entry begins with `markdown`, `markdown-it-py`, `markdown_it_py`, `mistune`, `commonmark`. If any such lib lands later, the gate fails until a paired sanitize-after-conversion test is added. Comment in the test explains: "Phase 78 / D-78-R1: no markdown→HTML conversion exists today; this gate prevents silent introduction of one without paired sanitize wiring"
    - `test_no_bleach_in_deps` (HARDEN-06 belt-and-suspenders, redundant with Plan 01 but kept here as the AST-gate file is the natural home for cross-cutting CI gates): same TOML walk, assert no `bleach` entry
    - Optional secondary check: Python-side AST walker for `jinja2.Markup(...)` / `markupsafe.Markup(...)` calls in `quirk/reports/*.py` whose argument is NOT a call to `sanitize_scanner_text` — flag as violation. Mirror `tests/test_scan_error_gate.py:39-49` `_is_sanitize_call` predicate. Current codebase has zero `Markup` usages; this is a forward guard
  </behavior>
  <action>
    Create `tests/test_safe_filter_audit.py`. Copy the structural skeleton from `tests/test_scan_error_gate.py` verbatim per PATTERNS §6:
    1. Module docstring (Phase 78 / HARDEN-05 + reference D-78-R1).
    2. `from __future__ import annotations`; imports `ast`, `pathlib`, `tomllib`, `jinja2`, `jinja2.nodes`, `pytest`.
    3. Constants: `PROJECT_ROOT = pathlib.Path(__file__).resolve().parent.parent`; `TEMPLATE_DIRS = [PROJECT_ROOT / "quirk" / "reports" / "templates"]`; `REPORT_DIRS = [PROJECT_ROOT / "quirk" / "reports"]`; `MARKDOWN_LIB_PREFIXES = ("markdown", "mistune", "commonmark")`.
    4. `_has_upstream_sanitize(filter_node)` helper per RESEARCH sketch.
    5. The six test functions enumerated in `<behavior>`. Use `tree.find_all(nodes.Filter)` (Jinja canonical walker).
    6. For the pyproject scan, parse the file once at module-import time (cache in a module-level constant) so multiple tests can share. Use `tomllib.loads(open(pyproject_path,'rb').read().decode())`.
    7. For the optional Python-side `Markup` walker, parametrize over `sorted(REPORT_DIRS[0].rglob("*.py"))` per PATTERNS §6 (import-presence gate pattern from test_credential_leakage.py:87-91). The current sweep should return zero violations — keep the gate green.
    Self-test discipline per Phase 59: include BOTH positive (catches synthetic bypass) AND negative (does not flag correct usage) self-tests so CI fails if the detector silently breaks.
  </action>
  <verify>
    <automated>pytest tests/test_safe_filter_audit.py -x -q</automated>
  </verify>
  <done>All six AST gate tests pass; gate is green against current codebase; self-tests prove gate detects synthetic bypass and does not false-positive on correct usage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create end-to-end regression test tests/test_report_injection_hardening.py</name>
  <files>tests/test_report_injection_hardening.py</files>
  <behavior>
    - `test_script_payload_in_cert_cn_is_escaped_in_html`: synthetic endpoint with `cipher_suite="<script>alert(1)</script>"` (or `cert_pubkey_alg`) + finding dict with `<script>` in `host`, `title`, `description`. Call `write_reports(cfg, endpoints, findings)`. Read back the generated `report-*.html`. Assert `"<script>alert(1)</script>"` does NOT appear (raw); assert `"&lt;script&gt;"` DOES appear OR the substring `"script"` is absent entirely (strip-or-escape both acceptable per CONTEXT regression criterion verbatim)
    - `test_script_payload_in_cert_cn_is_escaped_in_pdf`: same fixture, read back the generated `report-*.pdf` via `pypdf.PdfReader(...).pages[0].extract_text()`, assert no raw `<script>` substring. `pytest.importorskip("playwright.sync_api")` and `pytest.importorskip("pypdf")` at module top
    - `test_javascript_url_in_finding_recommendation_stripped`: finding with `recommendation="See javascript:alert(1) for details"` — assert rendered HTML does NOT contain `"javascript:"` (URL stripped by sanitize_scanner_text per Plan 01)
    - `test_db_stored_raw_payload_preserved`: per CONTEXT.md "raw scanner data stays in the DB" invariant + RESEARCH §Cluster C. If write_reports writes to a SQLite DB as part of its flow, query the DB and assert the stored `description` value is the RAW `<script>alert(1)</script>` text. If write_reports does NOT persist (writes only files), document the deferred check and skip the test with a `pytest.skip` + comment citing Cluster C. This proves sanitization is render-boundary only, not write-time
    - Tests skip cleanly if Playwright/pypdf unavailable
    - Fixture style mirrors `tests/test_report_sanitization.py` `_make_cfg` + `_findings_fixture` helpers (PATTERNS §7)
  </behavior>
  <action>
    Create `tests/test_report_injection_hardening.py` modeled on `tests/test_report_sanitization.py` per PATTERNS §7. Module structure:
    1. Module docstring: "Phase 78 / HARDEN-03 regression: scanner-controlled HTML payloads in certificate CN must be rendered as escaped plain text in HTML + PDF." (matches RESEARCH §Regression Test Design verbatim).
    2. `pytest.importorskip` for `playwright.sync_api` and `pypdf` at module level — but only inside the PDF test functions, NOT module-level, so the HTML tests run even without Playwright.
    3. Reuse the `_make_cfg(tmp_path)` and `_findings_fixture()` patterns from `tests/test_report_sanitization.py` and `tests/test_reports_writer.py`. Construct adversarial fixtures with the exact payload `<script>alert(1)</script>` in `cipher_suite`, `host`, `title`, `description` per RESEARCH §Regression Test Design.
    4. Call `write_reports(cfg, endpoints, findings)` (the entry point used by `test_reports_writer.py`).
    5. Locate the rendered HTML via `next(tmp_path.glob("report-*.html"))` and the PDF via `next(tmp_path.glob("report-*.pdf"))` — handle StopIteration as pytest.skip if PDF path absent (Playwright not installed).
    6. Assertions per `<behavior>`. Use the exact strings from CONTEXT.md success criterion #1: input `<script>alert(1)</script>` → output contains `&lt;script&gt;alert(1)&lt;/script&gt;` OR contains no `script` substring at all.
    7. For the DB-invariant test, first inspect the codebase to determine whether `write_reports` writes to a DB. If yes (e.g., via `quirk/engine/findings_evaluator.py` or a session table), assert the raw stored payload is preserved. If no, `pytest.skip("DB-invariant deferred: write_reports does not persist; raw-data preservation will be re-validated when DB-write path lands.")` with a comment citing Cluster C.
  </action>
  <verify>
    <automated>pytest tests/test_report_injection_hardening.py -x -q</automated>
  </verify>
  <done>End-to-end regression test passes; script-in-CN renders escaped in HTML; script-in-CN renders escaped in PDF (or test skips cleanly when Playwright/pypdf unavailable); javascript: URL stripped; DB raw-data invariant either asserted or documented as deferred.</done>
</task>

<task type="auto">
  <name>Task 3: Run full Phase 78 test slice + atomic commit (CI gate + regression)</name>
  <files>tests/test_safe_filter_audit.py, tests/test_report_injection_hardening.py</files>
  <action>
    Run `python -m compileall quirk/` and the full Phase 78 test slice + the existing reports suite as a regression sweep:
    `pytest tests/test_sanitize_scanner_text.py tests/test_md_cell_escape.py tests/test_safe_filter_audit.py tests/test_report_injection_hardening.py tests/test_pdf_metadata_constants.py tests/test_reports_writer.py tests/test_report_sanitization.py tests/test_pdf_render_hardening.py tests/test_scan_error_gate.py -x -q`
    Then run the full quick suite to catch any cross-cutting regression: `pytest -m 'not slow' -x -q`. If anything fails, fix before commit.
    Atomically commit:
    `node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "test(78): add AST CI gate for | safe pairing + end-to-end XSS regression + markdown→HTML forward guard (HARDEN-05, HARDEN-03 regression)" --files tests/test_safe_filter_audit.py tests/test_report_injection_hardening.py`
  </action>
  <verify>
    <automated>python -m compileall quirk/ &amp;&amp; pytest -m 'not slow' -x -q &amp;&amp; git log -1 --pretty=%s | grep -q "test(78): add AST CI gate"</automated>
  </verify>
  <done>Full quick suite green; atomic commit landed; Phase 78 production work complete.</done>
</task>

<task type="auto">
  <name>Task 4: Update docs/UAT-SERIES.md for Phase 78 HARDEN-* test cases</name>
  <files>docs/UAT-SERIES.md</files>
  <action>
    Read `docs/UAT-SERIES.md` to determine the relevant series (per CLAUDE.md "Update docs/UAT-SERIES.md"). HARDEN findings affect report-rendering test cases, so the affected series is the one covering report HTML/PDF output (likely UAT-2-* or similar — read the doc to confirm).
    1. Update the `**Last Updated:**` date at the top to today's ISO date (2026-05-16).
    2. In the relevant series, add new test cases for:
       - **HARDEN-01 (md_cell parity):** open a generated `executive-summary-*.md` and `technical-findings-*.md`, confirm no scanner-controlled cell contains a raw `|` that breaks the table.
       - **HARDEN-02 (Jinja autoescape + sanitize):** view the source of `report-*.html`, confirm any scanner-controlled field with HTML metachars renders as escaped entities.
       - **HARDEN-03 (script-in-CN escaped):** run a scan against a chaos-lab profile (or any fixture) and confirm `<script>alert(1)</script>` placeholder in synthetic test data renders as `&lt;script&gt;` in both `.html` and `.pdf` outputs.
       - **HARDEN-04 (PDF metadata constants):** open the generated `report-*.pdf` in Preview / Adobe Reader / `pdfinfo`, confirm Title is `"QU.I.R.K. Cryptographic Readiness Report"` and Author is `"QU.I.R.K. Scanner"`.
       - **HARDEN-05 (CI gate):** run `pytest tests/test_safe_filter_audit.py`, confirm it exits zero; manually add a `{{ x | safe }}` to a copy of the template and confirm the gate flags it.
       - **HARDEN-06 (nh3 dep):** `pip show nh3` confirms version ≥0.2.17; `pip show bleach` returns "not found".
    3. If any pre-existing UAT case (e.g., a UAT-1-02 version string check) referenced the previous dynamic `<title>` containing `org_name`, update it to expect the constant title.
    4. Keep all additions concise — UAT cases are pass/fail checklists, not narratives.
  </action>
  <verify>
    <automated>grep -q "HARDEN-01" docs/UAT-SERIES.md &amp;&amp; grep -q "HARDEN-04" docs/UAT-SERIES.md &amp;&amp; grep -q "2026-05-16" docs/UAT-SERIES.md</automated>
  </verify>
  <done>docs/UAT-SERIES.md has Phase 78 HARDEN-01..06 test cases; Last Updated reflects today's date.</done>
</task>

<task type="auto">
  <name>Task 5: Sync UAT-SERIES.md to Obsidian vault + commit</name>
  <files>docs/UAT-SERIES.md</files>
  <action>
    Per CLAUDE.md "Mandatory Phase Completion Steps §3 Sync UAT-SERIES.md to Obsidian" (file is too large for CLI `content=` parameter — write directly to filesystem):
    ```bash
    printf "---\nproject: QU.I.R.K.\ntype: reference\nstatus: active\nsource: docs/UAT-SERIES.md\nupdated: 2026-05-16\n---\n\n" > /tmp/uat_vault.md
    cat docs/UAT-SERIES.md >> /tmp/uat_vault.md
    cp /tmp/uat_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
    ```
    Then commit per CLAUDE.md step 4:
    `node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-78): update UAT-SERIES.md" --files docs/UAT-SERIES.md`
  </action>
  <verify>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" &amp;&amp; grep -q "HARDEN-01" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" &amp;&amp; git log -1 --pretty=%s | grep -q "docs(phase-78): update UAT-SERIES.md"</automated>
  </verify>
  <done>Vault UAT-Series.md mirrors docs/UAT-SERIES.md with frontmatter prepended; commit landed.</done>
</task>

<task type="auto">
  <name>Task 6: Create Obsidian phase note (write directly to vault filesystem)</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-78-HTML-PDF-Injection-Hardening.md</files>
  <action>
    Per CLAUDE.md "Mandatory Phase Completion Steps §1 Create Obsidian Phase Note" — write directly to the vault filesystem (do NOT use the obsidian CLI `content=` parameter because phase notes are too large for shell expansion). Use the Write tool.
    Template structure (mirror existing notes at `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/`):
    1. Frontmatter:
       ```
       ---
       project: QU.I.R.K.
       type: phase
       status: complete
       source: .planning/phases/78-html-pdf-injection-hardening/
       updated: 2026-05-16
       ---
       ```
    2. `# Phase 78 — HTML/PDF Injection Hardening`
    3. `## Goal` — from ROADMAP.md Phase 78 section (1 paragraph).
    4. `## Requirements Covered` — bulleted list: HARDEN-01, HARDEN-02, HARDEN-03, HARDEN-04, HARDEN-05, HARDEN-06 (one line each with brief description from REQUIREMENTS.md).
    5. `## Success Criteria` — from ROADMAP.md Phase 78 section (5 criteria).
    6. `## What Was Built` — one subsection per plan, each sourced from the matching SUMMARY.md file:
       - `### Plan 01 — Sanitize chokepoint + nh3 dependency` (from 78-01-SUMMARY.md)
       - `### Plan 02 — Jinja sanitize filter + template metadata` (from 78-02-SUMMARY.md)
       - `### Plan 03 — md_cell rollout to executive.py + writer.py` (from 78-03-SUMMARY.md)
       - `### Plan 04 — Playwright PDF context hardening` (from 78-04-SUMMARY.md)
       - `### Plan 05 — AST CI gate + regression test + docs sync` (from 78-05-SUMMARY.md, which this very plan will write — populate from this plan's task results)
    7. `## Three Deltas vs CONTEXT.md` — document D-78-R1 (no markdown→HTML target — re-cast as forward guard), D-78-R2 (PDF metadata via HTML head, not page.pdf() kwargs), D-78-R3 (Playwright launch site is html_renderer.py not writer.py). One sentence each.
    8. `[[Roadmap]]` link at the bottom.
    Use the Write tool with the full content. Do NOT use Bash heredoc per the "no heredoc writes" rule.
  </action>
  <verify>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-78-HTML-PDF-Injection-Hardening.md" &amp;&amp; grep -q "status: complete" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-78-HTML-PDF-Injection-Hardening.md" &amp;&amp; grep -q "HARDEN-06" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-78-HTML-PDF-Injection-Hardening.md" &amp;&amp; grep -q "D-78-R" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-78-HTML-PDF-Injection-Hardening.md"</automated>
  </verify>
  <done>Obsidian phase note exists at the vault path; status=complete; all six HARDEN requirements documented; three RESEARCH deltas captured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future template edits → CI | New `| safe` usages must pair with `| sanitize` upstream or CI fails |
| future pyproject changes → CI | Adding a markdown→HTML library (markdown, mistune, etc.) must be paired with sanitize wiring or CI fails |
| adversarial scanner output → consultant deliverable (HTML + PDF) | End-to-end regression test asserts hostile payloads never reach unescaped output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-19 | Tampering | Future PR adds `{{ x | safe }}` without `| sanitize` upstream | mitigate | AST gate `test_safe_filter_paired_with_sanitize` walks every `.j2` and fails CI |
| T-78-20 | Tampering | Future PR adds `import markdown` (markdown→HTML) without paired sanitize wiring | mitigate | Forward-guard test `test_no_markdown_to_html_lib_in_deps` walks pyproject.toml and fails CI |
| T-78-21 | Tampering | Future PR adds `Markup(scanner_text)` bypass in Python | mitigate | Optional Python-side `Markup`-without-sanitize walker (defense in depth) |
| T-78-22 | Repudiation | Gate silently stops detecting (regex broken, AST walker no longer matches) | mitigate | Phase 59-style positive + negative self-tests (`test_gate_catches_synthetic_bypass`, `test_gate_does_not_flag_safe_patterns`) catch detector-drift |
| T-78-23 | Information Disclosure | Adversarial CN bypasses HTML escape and renders raw `<script>` | mitigate | End-to-end regression asserts escape/strip in BOTH HTML and PDF outputs |
</threat_model>

<verification>
- `python -m compileall quirk/` exits 0
- `pytest tests/test_safe_filter_audit.py -x -q` passes (six tests including self-tests)
- `pytest tests/test_report_injection_hardening.py -x -q` passes (or cleanly skips PDF tests when Playwright/pypdf unavailable)
- `pytest -m 'not slow' -x -q` passes (full quick suite green)
- docs/UAT-SERIES.md updated and synced to vault
- Obsidian phase note exists at the vault path with `status: complete`
- Atomic commits landed for (a) tests, (b) docs/UAT-SERIES.md
</verification>

<success_criteria>
1. AST CI gate exits non-zero on unpaired `| safe` (positive + negative self-tests confirm detector works)
2. Forward-guard test fails if markdown→HTML library is added to deps without paired sanitize wiring (closes D-78-R1)
3. End-to-end regression: `<script>` in CN renders as `&lt;script&gt;` (or stripped) in BOTH HTML and PDF outputs — closes ROADMAP Phase 78 success criterion #1
4. docs/UAT-SERIES.md updated with HARDEN-01..06 manual test cases + synced to Obsidian + committed
5. Obsidian phase note created with status=complete, all six requirements covered, three RESEARCH deltas documented
</success_criteria>

<output>
After completion, create `.planning/phases/78-html-pdf-injection-hardening/78-05-SUMMARY.md` with:
- AST gate test count + pass status (six tests)
- Regression test count + pass status (four tests)
- Full quick suite pass status
- UAT-SERIES.md changes (which series, which test cases added)
- Obsidian phase note path + frontmatter snapshot
- Requirements closed: HARDEN-05 (full), HARDEN-03 (regression-verified)
- Phase 78 closure note: all six HARDEN-* requirements + ROADMAP success criterion #1 satisfied; phase ready for `/gsd-verify-work`.
</output>
