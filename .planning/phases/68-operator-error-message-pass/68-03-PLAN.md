---
phase: 68
plan: 03
type: execute
wave: 3
depends_on: [68-02]
files_modified:
  - run_scan.py
  - quirk/cli/doctor_cmd.py
  - quirk/cli/schedule_cmd.py
  - quirk/util/optional_extra.py
  - quirk/scanner/kerberos_scanner.py
  - tests/test_scan_robustness.py
autonomous: true
requirements: [UX-01, UX-02]
must_haves:
  truths:
    - "run_scan.py inline error emissions (missing extra, no db path, cannot open db) use format_error()"
    - "doctor_cmd.py _check_* failures wrap format_error() in Rich [red][x] markup"
    - "schedule_cmd.py error exits use format_error() for SCHED-001..004"
    - "optional_extra.py _emit_missing_extra_advisory uses format_error('INSTALL-001')"
    - "kerberos_scanner.py legacy multi-line advisory replaced with format_error('INSTALL-001')"
    - "tests/test_scan_robustness.py asserts format_error/INSTALL-001 instead of '[advisory] scanner='"
  artifacts:
    - path: "run_scan.py"
      provides: "Inline first-run errors emit QRK format"
      contains: "from quirk.errors import format_error"
    - path: "quirk/cli/doctor_cmd.py"
      provides: "Each _check_* failure emits QRK-INSTALL-NNN format wrapped in Rich markup"
      contains: "format_error"
    - path: "quirk/cli/schedule_cmd.py"
      provides: "Schedule CLI error exits emit QRK-SCHED-NNN format"
      contains: "format_error"
  key_links:
    - from: "run_scan.py inline error paths"
      to: "quirk/errors.py:format_error"
      via: "direct import and call at stderr emit sites"
      pattern: "print.*format_error"
    - from: "quirk/cli/doctor_cmd.py _check_* returns"
      to: "quirk/errors.py:format_error"
      via: "f-string wrapping format_error inside [red][x] ... [/red]"
      pattern: "format_error"
---

<objective>
Migrate all CLI-side operator error paths to use `format_error()` from `quirk/errors.py`. Covers: `run_scan.py` inline stderr emissions (missing extra advisory, db path errors), `quirk/cli/doctor_cmd.py` `_check_*` functions, `quirk/cli/schedule_cmd.py` validation exits, `quirk/util/optional_extra.py` missing-extra advisory, and `quirk/scanner/kerberos_scanner.py` legacy multi-line advisory. Updates `tests/test_scan_robustness.py` in the same wave (source-inspection assertion will otherwise break).

Runs in **Wave 3 parallel with Plan 04** (Plan 04 owns dashboard files; this plan owns CLI files — zero file overlap). Both depend on Plan 02.

Purpose: Closes the CLI half of UX-01 + the install-day inline detection half of UX-02 (smoke tests land in Plan 05).
Output: Every CLI error path emits `[QRK-X-NNN] cause. Fix: hint.`; existing CLI test suite remains green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-operator-error-message-pass/68-CONTEXT.md
@.planning/phases/68-operator-error-message-pass/68-RESEARCH.md
@.planning/phases/68-operator-error-message-pass/68-PATTERNS.md
@.planning/phases/68-operator-error-message-pass/68-01-SUMMARY.md
@.planning/phases/68-operator-error-message-pass/68-02-SUMMARY.md
@quirk/errors.py
@run_scan.py
@quirk/cli/doctor_cmd.py
@quirk/cli/schedule_cmd.py
@quirk/util/optional_extra.py
@quirk/scanner/kerberos_scanner.py
@tests/test_scan_robustness.py

<interfaces>
Public API: `from quirk.errors import format_error`. Returns plain string.

Replacement table (RESEARCH.md Category A audit):

| File:Line                  | Current emission                                                    | New emission                                       |
|----------------------------|---------------------------------------------------------------------|----------------------------------------------------|
| run_scan.py:~154           | freeform `[advisory] scanner=... extra=... not installed ...`       | `print(format_error("INSTALL-001"), file=sys.stderr)` |
| run_scan.py:~246           | `print("No database path available. ...", file=sys.stderr)`        | `print(format_error("INSTALL-003"), file=sys.stderr)` |
| run_scan.py:~252           | `print(f"[error] cannot open database: {exc}", file=sys.stderr)`   | `print(format_error("INSTALL-003"), file=sys.stderr)` |
| doctor_cmd.py:31           | `[red][x] Python ... < ...[/red]`                                   | `f"[red][x] {format_error('INSTALL-005')}[/red]"`  |
| doctor_cmd.py:37           | `[red][x] {name} not found in PATH[/red]`                           | per-binary: INSTALL-006 (nmap), INSTALL-007 (syft), INSTALL-008 (semgrep) |
| doctor_cmd.py:57           | `[red][x] {N} compliance entries stale[/red]`                       | `f"[red][x] {format_error('INSTALL-009')}[/red]"`  |
| doctor_cmd.py:81           | `[red][x] cannot open {db_path}: {exc}[/red]`                       | `f"[red][x] {format_error('INSTALL-003')}[/red]"`  |
| doctor_cmd.py:100          | `[red][x] {config_path} malformed: {exc}[/red]`                     | `f"[red][x] {format_error('INSTALL-010')}[/red]"`  |
| schedule_cmd.py:43-44      | Invalid schedule name + sys.exit(2)                                 | print format_error('SCHED-001') + sys.exit(2)      |
| schedule_cmd.py:47-49      | Invalid cron + sys.exit(2)                                          | print format_error('SCHED-002') + sys.exit(2)      |
| schedule_cmd.py:69-71      | Schedule already exists + sys.exit(2)                               | print format_error('SCHED-003') + sys.exit(2)      |
| schedule_cmd.py:117-119    | Schedule not found (enable/disable) + sys.exit(2)                   | print format_error('SCHED-004') + sys.exit(2)      |
| schedule_cmd.py:133-136    | Schedule not found (remove) + sys.exit(2)                           | print format_error('SCHED-004') + sys.exit(2)      |
| optional_extra.py:~228     | freeform stderr advisory inside _emit_missing_extra_advisory        | `print(format_error("INSTALL-001"), file=sys.stderr)` |
| kerberos_scanner.py:248-256| multi-line `[QUIRK] Kerberos scanning requires...`                  | `print(format_error("INSTALL-001"), file=sys.stderr)` |

NEVER interpolate `exc` or dynamic values into format_error output (static registry strings only).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate run_scan.py + optional_extra.py + kerberos_scanner.py inline emits</name>
  <files>run_scan.py, quirk/util/optional_extra.py, quirk/scanner/kerberos_scanner.py</files>
  <read_first>
    - run_scan.py (lines 70-165 and 240-260 — the exact emission sites)
    - quirk/util/optional_extra.py (lines 220-240 — _emit_missing_extra_advisory)
    - quirk/scanner/kerberos_scanner.py (lines 240-260 — legacy advisory)
    - quirk/errors.py (Plan 01 — format_error API)
  </read_first>
  <behavior>
    - Calling run_scan with a not-installed scanner extra prints exactly one stderr line starting with `[QRK-INSTALL-001]`
    - When the configured db path is unreadable, stderr line starts with `[QRK-INSTALL-003]`
    - When no db path is configured, stderr line starts with `[QRK-INSTALL-003]`
    - Exception text from caught OSError/sqlite errors is NOT in the operator-facing stderr line
    - Kerberos scanner missing-impacket path prints exactly one line starting with `[QRK-INSTALL-001]`
  </behavior>
  <action>
**Part A — run_scan.py:**

1. Add to the project-local import group (NOT inside any function):
   `from quirk.errors import format_error`

2. At the missing-extra advisory call site (around line 154), replace the entire `print(f"[advisory] scanner=... extra=... not installed ...", file=sys.stderr)` block with exactly:
   `print(format_error("INSTALL-001"), file=sys.stderr)`
   Remove helper variables used only to build the freeform advisory. Keep surrounding code that populates `scan_error_category="missing_extra"` unchanged.

3. At line ~246, replace `print("No database path available. Use --db-path or --config.", file=sys.stderr)` with:
   `print(format_error("INSTALL-003"), file=sys.stderr)`

4. At line ~252, replace `print(f"[error] cannot open database: {exc}", file=sys.stderr)` with:
   `print(format_error("INSTALL-003"), file=sys.stderr)`
   The `exc` variable may still go to a logger but must NOT be interpolated into the operator-facing print.

5. DO NOT touch line ~688 (`[warn] scan checkpoint ... is {N}h old`) — informational, not an error.
6. DO NOT alter the `errors` subcommand intercept added by Plan 02.

**Part B — quirk/util/optional_extra.py:**

1. Add to the project-local import group: `from quirk.errors import format_error`
2. In `_emit_missing_extra_advisory` (around line 220-240), replace the `print(..., file=sys.stderr)` advisory body with:
   `print(format_error("INSTALL-001"), file=sys.stderr)`
   Keep the function signature unchanged.

**Part C — quirk/scanner/kerberos_scanner.py:**

1. Around lines 248-256, replace the multi-line `print("[QUIRK] Kerberos scanning requires...", file=sys.stderr)` advisory with:
   ```
   from quirk.errors import format_error
   print(format_error("INSTALL-001"), file=sys.stderr)
   ```
   (Local import OK; top-of-file import preferred if no circular-import risk.)
2. Preserve surrounding control flow (early `return`/`raise` after the advisory).
  </action>
  <verify>
    <automated>python -m compileall run_scan.py quirk/util/optional_extra.py quirk/scanner/kerberos_scanner.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from quirk.errors import format_error' run_scan.py` returns at least 1
    - `grep -c 'from quirk.errors import format_error' quirk/util/optional_extra.py` returns at least 1
    - `grep -c 'format_error' quirk/scanner/kerberos_scanner.py` returns at least 1
    - `grep -v '^#' run_scan.py | grep -c 'advisory] scanner='` returns 0
    - `grep -v '^#' run_scan.py | grep -c 'cannot open database:'` returns 0
    - `grep -v '^#' quirk/scanner/kerberos_scanner.py | grep -c 'QUIRK] Kerberos scanning requires'` returns 0
    - `python -m compileall run_scan.py quirk/util/optional_extra.py quirk/scanner/kerberos_scanner.py` succeeds
  </acceptance_criteria>
  <done>All three files emit QRK codes via format_error; no exception text leaks into operator-facing stderr.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate quirk/cli/doctor_cmd.py and quirk/cli/schedule_cmd.py error returns</name>
  <files>quirk/cli/doctor_cmd.py, quirk/cli/schedule_cmd.py</files>
  <read_first>
    - quirk/cli/doctor_cmd.py (every _check_* function returning (False, "...") — lines 25-105)
    - quirk/cli/schedule_cmd.py (every sys.exit(2) call site — lines 40-140)
    - quirk/errors.py
  </read_first>
  <behavior>
    - `quirk doctor` failures display `[QRK-INSTALL-NNN]` codes inside the existing Rich `[red][x] ... [/red]` row markup
    - Doctor table layout is unchanged
    - `quirk schedule create --name 'bad name!'` exits 2 with stderr containing `[QRK-SCHED-001]`
    - `quirk schedule create` with invalid cron exits 2 with `[QRK-SCHED-002]`
    - Duplicate name exits 2 with `[QRK-SCHED-003]`
    - enable/disable/remove for missing name exits 2 with `[QRK-SCHED-004]`
  </behavior>
  <action>
**Part A — quirk/cli/doctor_cmd.py:**

1. Add to imports: `from quirk.errors import format_error`

2. Update each `_check_*` failure return:
   - `_check_python` (line ~31): `return False, f"[red][x] {format_error('INSTALL-005')}[/red]"`
   - `_check_binary` (line ~37) — map by name. Define at module scope below imports:
     ```
     _BINARY_TO_CODE = {"nmap": "INSTALL-006", "syft": "INSTALL-007", "semgrep": "INSTALL-008"}
     ```
     Then in the failure branch:
     ```
     code = _BINARY_TO_CODE.get(name, "INSTALL-006")
     return False, f"[red][x] {format_error(code)}[/red]"
     ```
   - `_check_compliance` (line ~57): `return False, f"[red][x] {format_error('INSTALL-009')}[/red]"`
   - `_check_db` (line ~81): `return False, f"[red][x] {format_error('INSTALL-003')}[/red]"`
   - `_check_config` (line ~100): `return False, f"[red][x] {format_error('INSTALL-010')}[/red]"`

3. DO NOT alter success-path return strings (`[green][✓] ...`). DO NOT change the table assembly or final `sys.exit()` in `run_doctor`.

Note: the original markup may use unicode glyphs (`[✗]`/`[✓]`); preserve whatever is currently there — only the message body inside the brackets changes.

**Part B — quirk/cli/schedule_cmd.py:**

1. Add to imports: `from quirk.errors import format_error`

2. Replace each error emission, keeping `sys.exit(2)` intact:
   - Line ~43-44 (invalid name): `console.print(f"[red]{format_error('SCHED-001')}[/red]")` then `sys.exit(2)`
   - Line ~47-49 (invalid cron): `console.print(f"[red]{format_error('SCHED-002')}[/red]")` then `sys.exit(2)`
   - Line ~69-71 (duplicate): `console.print(f"[red]{format_error('SCHED-003')}[/red]")` then `sys.exit(2)`
   - Line ~117-119 (enable/disable not found): `console.print(f"[red]{format_error('SCHED-004')}[/red]")` then `sys.exit(2)`
   - Line ~133-136 (remove not found): `console.print(f"[red]{format_error('SCHED-004')}[/red]")` then `sys.exit(2)`

3. Do NOT interpolate the offending name/cron into format_error output.
  </action>
  <verify>
    <automated>python -m compileall quirk/cli/doctor_cmd.py quirk/cli/schedule_cmd.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from quirk.errors import format_error' quirk/cli/doctor_cmd.py` returns 1
    - `grep -c 'from quirk.errors import format_error' quirk/cli/schedule_cmd.py` returns 1
    - `grep -c 'format_error' quirk/cli/doctor_cmd.py` returns at least 5
    - `grep -c 'format_error' quirk/cli/schedule_cmd.py` returns at least 5
    - `grep -c '_BINARY_TO_CODE' quirk/cli/doctor_cmd.py` returns at least 2 (def + lookup)
    - `grep -c 'INSTALL-005' quirk/cli/doctor_cmd.py` returns at least 1
    - `grep -c 'INSTALL-006' quirk/cli/doctor_cmd.py` returns at least 1
    - `grep -c 'SCHED-001' quirk/cli/schedule_cmd.py` returns at least 1
    - `grep -c 'SCHED-004' quirk/cli/schedule_cmd.py` returns at least 2 (enable/disable + remove)
    - `python -m compileall quirk/cli/doctor_cmd.py quirk/cli/schedule_cmd.py` succeeds
  </acceptance_criteria>
  <done>doctor_cmd and schedule_cmd emit canonical QRK codes wrapped in existing Rich markup; exit codes unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update tests/test_scan_robustness.py source-inspection assertion</name>
  <files>tests/test_scan_robustness.py</files>
  <read_first>
    - tests/test_scan_robustness.py (line 26 — the `inspect.getsource(run_scan)` assertion)
    - run_scan.py (post-Task-1 — must contain format_error import + INSTALL-001 reference)
  </read_first>
  <behavior>
    - test_scan_robustness still passes after run_scan.py migration
    - Assertion verifies run_scan.py source contains `format_error` AND `INSTALL-001`
    - Old assertion checking for `[advisory] scanner=` is removed
  </behavior>
  <action>
1. Open `tests/test_scan_robustness.py` and locate line ~26 with `assert "[advisory] scanner=" in src`.
2. Replace that assertion with two assertions on the same `src` variable:
   ```
   assert "format_error" in src, "run_scan.py must import/call format_error"
   assert "INSTALL-001" in src, "run_scan.py must reference QRK-INSTALL-001 for missing-extra advisory"
   ```
3. Do not alter any other line in the test file. Do not change imports unless the existing imports are insufficient for the new assertions (they are — only strings).
  </action>
  <verify>
    <automated>python -m pytest tests/test_scan_robustness.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'advisory] scanner=' tests/test_scan_robustness.py` returns 0
    - `grep -c 'format_error' tests/test_scan_robustness.py` returns at least 1
    - `grep -c 'INSTALL-001' tests/test_scan_robustness.py` returns at least 1
    - `python -m pytest tests/test_scan_robustness.py -x -q` exits 0
  </acceptance_criteria>
  <done>test_scan_robustness green against the migrated run_scan.py.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_scan_robustness.py -x -q` exits 0
- `python -m pytest tests/test_errors.py tests/test_errors_cmd.py -x -q` still exits 0 (no regression from earlier waves)
- `python run_scan.py errors --domain INSTALL` still works and shows INSTALL-005..010 entries
- Manual visual: `python run_scan.py doctor` shows `[QRK-INSTALL-NNN]` strings in any failing rows
</verification>

<success_criteria>
- All CLI error paths (run_scan inline, doctor, schedule, optional_extra, kerberos_scanner) emit QRK format
- test_scan_robustness updated and green
- No exception text leaks into operator-facing stderr
- File ownership disjoint from Plan 04 (CLI vs dashboard) — Wave 3 parallel
</success_criteria>

<output>
Create `.planning/phases/68-operator-error-message-pass/68-03-SUMMARY.md` listing: every file modified, the exact line ranges touched, the QRK codes emitted at each site, and confirmation that no `exc` or dynamic value is interpolated into operator-facing output.
</output>
