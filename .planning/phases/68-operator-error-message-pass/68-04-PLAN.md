---
phase: 68
plan: 04
type: execute
wave: 3
depends_on: [68-02]
files_modified:
  - quirk/dashboard/server.py
  - quirk/dashboard/api/middleware/auth.py
  - quirk/dashboard/api/middleware/csrf.py
  - quirk/dashboard/api/middleware/rate_limit.py
  - quirk/dashboard/api/routes/scan.py
  - quirk/dashboard/api/routes/jobs.py
  - quirk/dashboard/api/routes/qramm.py
  - quirk/dashboard/api/routes/schedules.py
  - quirk/dashboard/api/routes/pdf.py
  - tests/test_api_auth.py
  - tests/test_jobs_api.py
  - tests/test_qramm_router.py
  - tests/test_schedules_api.py
autonomous: true
requirements: [UX-01, UX-02]
must_haves:
  truths:
    - "Every HTTPException raised in dashboard middleware/routes uses format_error() for its detail field"
    - "Rate-limit Response body uses json.dumps({detail: format_error('DASHBOARD-003')}) instead of a hardcoded string"
    - "QRAMM_MULTIPLIER_OUT_OF_RANGE dict-detail is converted to a plain string via format_error('DASHBOARD-010')"
    - "server.py wraps uvicorn.run in try/except OSError to format_error('INSTALL-004') for port conflicts"
    - "server.py uvicorn ImportError emits format_error('INSTALL-002')"
    - "Existing API tests assert presence of QRK-DASHBOARD-NNN / QRK-SCHED-NNN codes instead of legacy detail strings"
  artifacts:
    - path: "quirk/dashboard/server.py"
      provides: "Port-conflict handler + INSTALL-002 ImportError emission"
      contains: "INSTALL-004"
    - path: "quirk/dashboard/api/middleware/rate_limit.py"
      provides: "DASHBOARD-003 in JSON-encoded Response body"
      contains: "json.dumps"
    - path: "quirk/dashboard/api/routes/qramm.py"
      provides: "DASHBOARD-009/010/011 — including dict to string conversion for multiplier range"
      contains: "DASHBOARD-010"
  key_links:
    - from: "all dashboard HTTPException raise sites"
      to: "quirk/errors.py:format_error"
      via: "detail=format_error('DASHBOARD-NNN')"
      pattern: "detail=format_error"
    - from: "quirk/dashboard/server.py uvicorn.run"
      to: "quirk/errors.py:format_error('INSTALL-004')"
      via: "try/except OSError checking 'address already in use'"
      pattern: "address already in use"
---

<objective>
Migrate every dashboard error surface to use `format_error()`. Covers middleware (auth/csrf/rate_limit), route handlers (scan/jobs/qramm/schedules/pdf), and the previously-missing port-conflict handler in `server.py`. Converts the one dict-detail HTTPException (QRAMM_MULTIPLIER_OUT_OF_RANGE) to plain string wire format. Updates affected API tests in the same wave.

Runs in **Wave 3 parallel with Plan 03** — file ownership fully disjoint.

Purpose: Closes the dashboard half of UX-01 + the port-conflict half of UX-02.
Output: All dashboard HTTPException details, rate-limit Response body, and server.py error paths emit `[QRK-X-NNN]` format; affected API tests updated and green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-operator-error-message-pass/68-CONTEXT.md
@.planning/phases/68-operator-error-message-pass/68-RESEARCH.md
@.planning/phases/68-operator-error-message-pass/68-PATTERNS.md
@.planning/phases/68-operator-error-message-pass/68-01-SUMMARY.md
@.planning/phases/68-operator-error-message-pass/68-02-SUMMARY.md
@quirk/errors.py
@quirk/dashboard/server.py
@quirk/dashboard/api/middleware/auth.py
@quirk/dashboard/api/middleware/csrf.py
@quirk/dashboard/api/middleware/rate_limit.py
@quirk/dashboard/api/routes/scan.py
@quirk/dashboard/api/routes/jobs.py
@quirk/dashboard/api/routes/qramm.py
@quirk/dashboard/api/routes/schedules.py
@quirk/dashboard/api/routes/pdf.py

<interfaces>
Public API: `from quirk.errors import format_error`. Returns plain string (never dict).

Replacement table (RESEARCH.md Category B + Pitfall #3 + Pattern 6):

| File:Line                         | Current detail                                                              | New detail                              |
|-----------------------------------|-----------------------------------------------------------------------------|-----------------------------------------|
| middleware/auth.py:46, 48         | `"Authentication required"`                                                 | `format_error("DASHBOARD-001")`         |
| middleware/csrf.py:23-27          | `f"Missing CSRF header: {CSRF_HEADER}"`                                     | `format_error("DASHBOARD-002")`         |
| middleware/rate_limit.py:57-62    | `Response(content='{"detail":"Rate limit exceeded"}', ...)`                 | `Response(content=json.dumps({"detail": format_error("DASHBOARD-003")}).encode(), ...)` |
| routes/scan.py:912                | `f"Invalid scan_id format: {scan_id!r}"`                                    | `format_error("DASHBOARD-004")`         |
| routes/scan.py:922                | `f"No scan found with scan_id={scan_id!r}"`                                 | `format_error("DASHBOARD-005")`         |
| routes/scan.py:931                | `"No scan results found. ..."`                                              | `format_error("DASHBOARD-006")`         |
| routes/scan.py:945                | `"No endpoints found for latest scan."`                                     | `format_error("DASHBOARD-006")`         |
| routes/scan.py:1124               | `"Cannot compare a scan to itself."`                                        | `format_error("DASHBOARD-007")`         |
| routes/scan.py:1129               | `"Invalid scan_id format."`                                                 | `format_error("DASHBOARD-004")`         |
| routes/scan.py:1134, 1136         | `f"No scan found: {a!r}"` / `f"No scan found: {b!r}"`                       | `format_error("DASHBOARD-005")`         |
| routes/jobs.py:137                | `"Job not found"`                                                           | `format_error("DASHBOARD-008")`         |
| routes/qramm.py:194               | `"Session not found"`                                                       | `format_error("DASHBOARD-009")`         |
| routes/qramm.py:345-352           | dict with error_code/message/valid_range                                    | `format_error("DASHBOARD-010")` (plain string) |
| routes/qramm.py:362-365           | `"Cannot score a session with no answered questions"`                       | `format_error("DASHBOARD-011")`         |
| routes/schedules.py:101           | `"Schedule not found"`                                                      | `format_error("SCHED-004")`             |
| routes/schedules.py:136-139       | `f"Invalid cron expression: ..."`                                           | `format_error("SCHED-002")`             |
| routes/schedules.py:157-160       | `f"Schedule '{name}' already exists"`                                       | `format_error("SCHED-003")`             |
| routes/pdf.py:40-46               | `"Playwright not installed. Run: pip install playwright ..."`               | `format_error("DASHBOARD-012")` (in existing Response shape) |
| server.py:20-24                   | `"ERROR: uvicorn not installed. ..."`                                       | `format_error("INSTALL-002")`           |
| server.py: around uvicorn.run()   | (no handling)                                                                | try/except OSError checking `address already in use` then format_error("INSTALL-004") + sys.exit(1) |

Test updates (RESEARCH.md "Existing Tests That Will Break"):

| Test file:Line                   | Old assertion                                          | New assertion                                              |
|----------------------------------|--------------------------------------------------------|------------------------------------------------------------|
| tests/test_api_auth.py:136       | `== "Authentication required"`                         | `"QRK-DASHBOARD-001" in data["detail"]`                    |
| tests/test_api_auth.py:197       | `== "Missing CSRF header: X-Quirk-Request"`            | `"QRK-DASHBOARD-002" in data["detail"]`                    |
| tests/test_jobs_api.py:198       | `"Job not found" in data["detail"]`                    | `"QRK-DASHBOARD-008" in data["detail"]`                    |
| tests/test_qramm_router.py:106   | `== "Session not found"`                               | `"QRK-DASHBOARD-009" in data["detail"]`                    |
| tests/test_schedules_api.py:113  | `"dup-test" in data["detail"]`                         | `"QRK-SCHED-003" in data["detail"]`                        |

NEVER interpolate scan_id, schedule name, exc, or other dynamic values into format_error output.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate middleware (auth, csrf, rate_limit) + server.py port-conflict handler</name>
  <files>quirk/dashboard/api/middleware/auth.py, quirk/dashboard/api/middleware/csrf.py, quirk/dashboard/api/middleware/rate_limit.py, quirk/dashboard/server.py</files>
  <read_first>
    - quirk/dashboard/api/middleware/auth.py (lines 1-50 — imports and 401 raise sites)
    - quirk/dashboard/api/middleware/csrf.py (lines 1-30 — imports and CSRF_HEADER constant and 403 raise)
    - quirk/dashboard/api/middleware/rate_limit.py (lines 1-65 — imports and Response build)
    - quirk/dashboard/server.py (entire file — uvicorn import guard and serve function)
    - quirk/errors.py
    - .planning/phases/68-operator-error-message-pass/68-PATTERNS.md (Pattern 5: Rate Limit; Pattern 6: Port Conflict)
  </read_first>
  <behavior>
    - GET /api/health WITHOUT auth returns 401 with detail starting with `[QRK-DASHBOARD-001]`
    - POST /api/scan WITHOUT X-Quirk-Request header returns 403 with detail starting with `[QRK-DASHBOARD-002]`
    - Rate-limit exceeded response body parses as JSON with `detail` starting with `[QRK-DASHBOARD-003]`
    - server.py serve() catches OSError "Address already in use" and emits `[QRK-INSTALL-004]` to stderr then sys.exit(1)
    - Missing uvicorn ImportError path emits `[QRK-INSTALL-002]` to stderr and sys.exit(1)
  </behavior>
  <action>
**Part A — auth.py:**
1. Add to imports: `from quirk.errors import format_error`
2. Replace both `raise HTTPException(status_code=401, detail="Authentication required")` raises with:
   `raise HTTPException(status_code=401, detail=format_error("DASHBOARD-001"))`

**Part B — csrf.py:**
1. Add to imports: `from quirk.errors import format_error`
2. Replace the existing raise (lines ~23-27) with:
   `raise HTTPException(status_code=403, detail=format_error("DASHBOARD-002"))`
   Keep `CSRF_HEADER` constant defined and used elsewhere — only the detail value changes.

**Part C — rate_limit.py:**
1. Add to imports: `import json` and `from quirk.errors import format_error`
2. Replace the Response construction (lines ~57-62), replacing only the `content=` value with:
   `content=json.dumps({"detail": format_error("DASHBOARD-003")}).encode()`
   Keep status_code=429, media_type, and Retry-After header exactly as-is.
3. Continue returning `Response` (not raising HTTPException) — `BaseHTTPMiddleware` cannot raise HTTPException.

**Part D — server.py:**
1. Add to top-of-file imports (after `import sys`): `from quirk.errors import format_error`
   If the existing uvicorn ImportError guard ordering would break with top-level import, do the import inline at call sites; prefer top-level.
2. Update the uvicorn ImportError guard (lines ~17-24) — replace literal `"ERROR: uvicorn not installed..."` with:
   `print(format_error("INSTALL-002"), file=sys.stderr)`
3. Wrap the `uvicorn.run(...)` call inside `serve()` with try/except:
   ```
   try:
       uvicorn.run("quirk.dashboard.api.app:app", host=host, port=port, log_level="info")
   except OSError as exc:
       if "address already in use" in str(exc).lower():
           print(format_error("INSTALL-004"), file=sys.stderr)
           sys.exit(1)
       raise
   ```
   Preserve the exact uvicorn.run kwargs already present — only add the try/except wrapper.
  </action>
  <verify>
    <automated>python -m compileall quirk/dashboard/api/middleware/auth.py quirk/dashboard/api/middleware/csrf.py quirk/dashboard/api/middleware/rate_limit.py quirk/dashboard/server.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from quirk.errors import format_error' quirk/dashboard/api/middleware/auth.py` returns 1
    - `grep -c 'from quirk.errors import format_error' quirk/dashboard/api/middleware/csrf.py` returns 1
    - `grep -c 'from quirk.errors import format_error' quirk/dashboard/api/middleware/rate_limit.py` returns 1
    - `grep -c 'from quirk.errors import format_error' quirk/dashboard/server.py` returns 1
    - `grep -c 'DASHBOARD-001' quirk/dashboard/api/middleware/auth.py` returns at least 1
    - `grep -c 'DASHBOARD-002' quirk/dashboard/api/middleware/csrf.py` returns 1
    - `grep -c 'DASHBOARD-003' quirk/dashboard/api/middleware/rate_limit.py` returns 1
    - `grep -c '^import json' quirk/dashboard/api/middleware/rate_limit.py` returns 1
    - `grep -c 'json.dumps' quirk/dashboard/api/middleware/rate_limit.py` returns at least 1
    - `grep -c 'INSTALL-002' quirk/dashboard/server.py` returns 1
    - `grep -c 'INSTALL-004' quirk/dashboard/server.py` returns 1
    - `grep -c 'address already in use' quirk/dashboard/server.py` returns 1
    - `grep -v '^#' quirk/dashboard/api/middleware/auth.py | grep -c 'Authentication required'` returns 0
    - `grep -v '^#' quirk/dashboard/api/middleware/csrf.py | grep -c 'Missing CSRF header'` returns 0
    - `grep -v '^#' quirk/dashboard/api/middleware/rate_limit.py | grep -c 'Rate limit exceeded'` returns 0
    - `python -m compileall quirk/dashboard/api/middleware/auth.py quirk/dashboard/api/middleware/csrf.py quirk/dashboard/api/middleware/rate_limit.py quirk/dashboard/server.py` succeeds
  </acceptance_criteria>
  <done>All four files emit canonical QRK codes; rate-limiter still returns a Response.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate routes (scan, jobs, qramm, schedules, pdf) — includes dict to string conversion for QRAMM multiplier</name>
  <files>quirk/dashboard/api/routes/scan.py, quirk/dashboard/api/routes/jobs.py, quirk/dashboard/api/routes/qramm.py, quirk/dashboard/api/routes/schedules.py, quirk/dashboard/api/routes/pdf.py</files>
  <read_first>
    - quirk/dashboard/api/routes/scan.py (lines 900-945, 1120-1140 — all HTTPException raises)
    - quirk/dashboard/api/routes/jobs.py (lines 130-140)
    - quirk/dashboard/api/routes/qramm.py (lines 190-200, 340-370 — dict-detail conversion site)
    - quirk/dashboard/api/routes/schedules.py (lines 95-165)
    - quirk/dashboard/api/routes/pdf.py (lines 35-50 — 503 Response for missing Playwright)
    - quirk/errors.py
  </read_first>
  <behavior>
    - GET /api/scan/{bogus-id} returns 400 with detail starting `[QRK-DASHBOARD-004]`
    - GET /api/scan/{unknown-uuid} returns 404 with detail starting `[QRK-DASHBOARD-005]`
    - GET /api/jobs/{missing} returns 404 with detail starting `[QRK-DASHBOARD-008]`
    - GET /api/qramm/sessions/{missing} returns 404 with detail starting `[QRK-DASHBOARD-009]`
    - POST QRAMM score with profile_multiplier=2.0 returns 400 with detail as a STRING starting `[QRK-DASHBOARD-010]` (not a dict)
    - POST /api/schedules with duplicate name returns 409 with detail starting `[QRK-SCHED-003]`
    - GET /api/report.pdf without Playwright returns 503 with body containing `[QRK-DASHBOARD-012]`
  </behavior>
  <action>
For each file below, add `from quirk.errors import format_error` to the imports (alongside the existing fastapi import) and replace `detail=` arguments per the table in the interfaces block.

**scan.py:**
- line ~912: `detail=format_error("DASHBOARD-004")`
- line ~922: `detail=format_error("DASHBOARD-005")`
- line ~931: `detail=format_error("DASHBOARD-006")`
- line ~945: `detail=format_error("DASHBOARD-006")`
- line ~1124: `detail=format_error("DASHBOARD-007")`
- line ~1129: `detail=format_error("DASHBOARD-004")`
- lines ~1134 and ~1136: `detail=format_error("DASHBOARD-005")` for both

**jobs.py:**
- line ~137: `detail=format_error("DASHBOARD-008")`

**qramm.py:**
- line ~194: `detail=format_error("DASHBOARD-009")`
- lines ~345-352 — REPLACE THE ENTIRE DICT BODY. Change from:
  ```
  raise HTTPException(status_code=400, detail={"error_code": "QRAMM_MULTIPLIER_OUT_OF_RANGE", "message": "...", "valid_range": [0.8, 1.5]})
  ```
  to:
  ```
  raise HTTPException(status_code=400, detail=format_error("DASHBOARD-010"))
  ```
  Then update the schema docstring around line ~340: replace any reference to the string `QRAMM_MULTIPLIER_OUT_OF_RANGE` with `QRK-DASHBOARD-010`.
- lines ~362-365: `detail=format_error("DASHBOARD-011")`

**schedules.py:**
- line ~101: `detail=format_error("SCHED-004")`
- lines ~136-139: `detail=format_error("SCHED-002")`
- lines ~157-160: `detail=format_error("SCHED-003")`

**pdf.py:**
- lines ~40-46. Read the current shape first:
  - If it raises HTTPException(503, detail="Playwright not installed..."): change detail to `format_error("DASHBOARD-012")`
  - If it returns Response with JSON body: use `content=json.dumps({"detail": format_error("DASHBOARD-012")}).encode()` (same pattern as rate_limit.py — add `import json` and `from quirk.errors import format_error` to imports)
  - Preserve status_code=503 and media_type.

DO NOT interpolate dynamic values (scan_id, name, multiplier value, etc.) into format_error output. Log via logger if needed.
  </action>
  <verify>
    <automated>python -m compileall quirk/dashboard/api/routes/scan.py quirk/dashboard/api/routes/jobs.py quirk/dashboard/api/routes/qramm.py quirk/dashboard/api/routes/schedules.py quirk/dashboard/api/routes/pdf.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from quirk.errors import format_error' quirk/dashboard/api/routes/scan.py` returns 1
    - `grep -c 'from quirk.errors import format_error' quirk/dashboard/api/routes/jobs.py` returns 1
    - `grep -c 'from quirk.errors import format_error' quirk/dashboard/api/routes/qramm.py` returns 1
    - `grep -c 'from quirk.errors import format_error' quirk/dashboard/api/routes/schedules.py` returns 1
    - `grep -c 'from quirk.errors import format_error' quirk/dashboard/api/routes/pdf.py` returns 1
    - `grep -c 'DASHBOARD-004' quirk/dashboard/api/routes/scan.py` returns at least 2
    - `grep -c 'DASHBOARD-005' quirk/dashboard/api/routes/scan.py` returns at least 3
    - `grep -c 'DASHBOARD-006' quirk/dashboard/api/routes/scan.py` returns at least 2
    - `grep -c 'DASHBOARD-007' quirk/dashboard/api/routes/scan.py` returns 1
    - `grep -c 'DASHBOARD-008' quirk/dashboard/api/routes/jobs.py` returns 1
    - `grep -c 'DASHBOARD-009' quirk/dashboard/api/routes/qramm.py` returns 1
    - `grep -c 'DASHBOARD-010' quirk/dashboard/api/routes/qramm.py` returns at least 1
    - `grep -c 'DASHBOARD-011' quirk/dashboard/api/routes/qramm.py` returns 1
    - `grep -c 'SCHED-002' quirk/dashboard/api/routes/schedules.py` returns at least 1
    - `grep -c 'SCHED-003' quirk/dashboard/api/routes/schedules.py` returns at least 1
    - `grep -c 'SCHED-004' quirk/dashboard/api/routes/schedules.py` returns at least 1
    - `grep -c 'DASHBOARD-012' quirk/dashboard/api/routes/pdf.py` returns 1
    - `grep -v '^#' quirk/dashboard/api/routes/qramm.py | grep -c 'QRAMM_MULTIPLIER_OUT_OF_RANGE'` returns 0
    - `grep -v '^#' quirk/dashboard/api/routes/jobs.py | grep -c '"Job not found"'` returns 0
    - `python -m compileall quirk/dashboard/api/routes/scan.py quirk/dashboard/api/routes/jobs.py quirk/dashboard/api/routes/qramm.py quirk/dashboard/api/routes/schedules.py quirk/dashboard/api/routes/pdf.py` succeeds
  </acceptance_criteria>
  <done>All five route files import format_error and emit canonical QRK codes; QRAMM dict-detail eliminated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update affected API tests to assert QRK codes</name>
  <files>tests/test_api_auth.py, tests/test_jobs_api.py, tests/test_qramm_router.py, tests/test_schedules_api.py</files>
  <read_first>
    - tests/test_api_auth.py (lines 130-140, 195-200 — the two assertions)
    - tests/test_jobs_api.py (line 198 area)
    - tests/test_qramm_router.py (line 106 area)
    - tests/test_schedules_api.py (line 113 area)
  </read_first>
  <behavior>
    - test_api_auth.py 401 assertion checks for "QRK-DASHBOARD-001" substring in detail
    - test_api_auth.py 403 assertion checks for "QRK-DASHBOARD-002" substring
    - test_jobs_api.py 404 assertion checks for "QRK-DASHBOARD-008" substring
    - test_qramm_router.py 404 assertion checks for "QRK-DASHBOARD-009" substring
    - test_schedules_api.py duplicate-name assertion checks for "QRK-SCHED-003" substring (not the schedule name)
    - All four test files exit 0 under pytest
  </behavior>
  <action>
1. `tests/test_api_auth.py` line ~136: change `assert data["detail"] == "Authentication required"` to `assert "QRK-DASHBOARD-001" in data["detail"]`.
2. `tests/test_api_auth.py` line ~197: change `assert data["detail"] == "Missing CSRF header: X-Quirk-Request"` to `assert "QRK-DASHBOARD-002" in data["detail"]`.
3. `tests/test_jobs_api.py` line ~198: change `assert "Job not found" in data["detail"]` to `assert "QRK-DASHBOARD-008" in data["detail"]`.
4. `tests/test_qramm_router.py` line ~106: change `assert data["detail"] == "Session not found"` to `assert "QRK-DASHBOARD-009" in data["detail"]`.
5. `tests/test_schedules_api.py` line ~113: change `assert "dup-test" in data["detail"]` to `assert "QRK-SCHED-003" in data["detail"]`.

If `test_api_auth.py` line ~433 references `QUIRK_SERVE_PORT is out of allowed range` (RESEARCH.md flagged this), determine if pdf.py was migrated in Task 2 — if so, update that assertion to check for the new code (`QRK-DASHBOARD-012` or whatever code pdf.py now emits). Otherwise leave untouched.

Do NOT alter any other lines or test logic. Do NOT change imports.
  </action>
  <verify>
    <automated>python -m pytest tests/test_api_auth.py tests/test_jobs_api.py tests/test_qramm_router.py tests/test_schedules_api.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'QRK-DASHBOARD-001' tests/test_api_auth.py` returns at least 1
    - `grep -c 'QRK-DASHBOARD-002' tests/test_api_auth.py` returns at least 1
    - `grep -c 'QRK-DASHBOARD-008' tests/test_jobs_api.py` returns at least 1
    - `grep -c 'QRK-DASHBOARD-009' tests/test_qramm_router.py` returns at least 1
    - `grep -c 'QRK-SCHED-003' tests/test_schedules_api.py` returns at least 1
    - `grep -c 'Authentication required' tests/test_api_auth.py` returns 0 (legacy assertion gone)
    - `python -m pytest tests/test_api_auth.py tests/test_jobs_api.py tests/test_qramm_router.py tests/test_schedules_api.py -x -q` exits 0
  </acceptance_criteria>
  <done>All four affected API test files updated and green against migrated dashboard code.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_api_auth.py tests/test_jobs_api.py tests/test_qramm_router.py tests/test_schedules_api.py -x -q` exits 0
- `python -m pytest tests/ -x -q -m "not slow"` exits 0 (no regression in unrelated tests)
- `python -m compileall quirk/dashboard` succeeds
- Manual visual: with the dashboard running, hitting an unauthenticated endpoint returns JSON `{"detail": "[QRK-DASHBOARD-001] ..."}`
</verification>

<success_criteria>
- All dashboard error surfaces emit QRK format
- Rate-limit Response uses json.dumps to embed format_error
- QRAMM dict-detail eliminated; replaced with plain string
- server.py wraps uvicorn.run in OSError handler for INSTALL-004
- Affected API tests updated and green
- File ownership disjoint from Plan 03 (dashboard vs CLI) — Wave 3 parallel
</success_criteria>

<output>
Create `.planning/phases/68-operator-error-message-pass/68-04-SUMMARY.md` listing: every file modified, exact line ranges touched, all QRK codes emitted per file, confirmation that QRAMM dict-detail was eliminated, and the test files updated.
</output>
