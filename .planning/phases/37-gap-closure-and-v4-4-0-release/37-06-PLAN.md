---
phase: 37
plan: 06
type: execute
wave: 6
depends_on: [37-05]
files_modified:
  - docs/UAT-SERIES.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-37-Gap-Closure-and-v4-4-0-Release.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md
autonomous: false
requirements:
  - STRUCT-01
  - STRUCT-02
  - STRUCT-03
  - INFRA-01
  - INFRA-02
  - INFRA-03
must_haves:
  truths:
    - "Obsidian phase note exists at /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-37-Gap-Closure-and-v4-4-0-Release.md (CLAUDE.md mandatory step 1)"
    - "docs/UAT-SERIES.md updated with v4.4.0 version strings and any new test cases (CLAUDE.md mandatory step 2)"
    - "UAT-SERIES.md synced to Obsidian vault at 20_Dev-Work/QUIRK/UAT-Series.md (CLAUDE.md mandatory step 3)"
    - "All phase 37 artifacts committed to git with descriptive messages (CLAUDE.md mandatory step 4)"
    - "Full test suite green: pytest -x -q exits 0 with 504+ tests passing"
  artifacts:
    - path: "docs/UAT-SERIES.md"
      provides: "Updated UAT series for v4.4.0"
      contains: "4.4.0"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-37-Gap-Closure-and-v4-4-0-Release.md"
      provides: "Obsidian phase note for Phase 37"
      contains: "status: complete"
  key_links:
    - from: "docs/UAT-SERIES.md"
      to: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
      via: "vault sync"
      pattern: "4.4.0"
---

<objective>
Execute the four mandatory CLAUDE.md phase-completion steps for Phase 37: (1) create the
Obsidian phase note, (2) update docs/UAT-SERIES.md, (3) sync UAT-SERIES.md to the vault,
(4) commit all phase 37 artifacts. Per D-10/D-11, no git tag and no /gsd-complete-milestone
invocation in this plan.

Purpose: Honor the CLAUDE.md non-negotiable phase close-out ritual; close the phase 37
audit trail; leave the repo in a clean, committed state for the milestone-close step the
user will trigger separately.
Output: Obsidian phase note + updated UAT-SERIES (filesystem + vault) + final commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/37-gap-closure-and-v4-4-0-release/37-CONTEXT.md
@docs/UAT-SERIES.md

<interfaces>
CLAUDE.md mandatory phase completion steps (verbatim, must be followed in order):

1. Create Obsidian phase note via direct filesystem write (NOT obsidian CLI content= — too
   large for shell expansion):
   `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-37-Gap-Closure-and-v4-4-0-Release.md`
   Format: frontmatter (status: complete, type: phase, source, updated) + Goal + Requirements
   Covered + Success Criteria + What Was Built (one subsection per plan, sourced from
   SUMMARY.md files) + [[Roadmap]] link.

2. Update `docs/UAT-SERIES.md` to reflect:
   - Version bump from 4.3.0 → 4.4.0 in any version-pinned pass criteria
   - Any new test cases added by v4.4 phases (32-37)
   - `**Last Updated:**` date at the top

3. Sync UAT-SERIES.md to Obsidian via direct vault filesystem write (file too large for CLI
   content= parameter):
   ```bash
   printf "---\nproject: QU.I.R.K.\ntype: reference\nstatus: active\nsource: docs/UAT-SERIES.md\nupdated: <YYYY-MM-DD>\n---\n\n" > /tmp/uat_vault.md
   cat docs/UAT-SERIES.md >> /tmp/uat_vault.md
   cp /tmp/uat_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
   ```

4. Commit `docs/UAT-SERIES.md`:
   ```bash
   node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-37): update UAT-SERIES.md" --files docs/UAT-SERIES.md
   ```
   (Plus a final phase-37 commit covering all other artifacts produced by plans 37-01..37-05.)

Out of scope per D-10 / D-11:
- Git tag `v4.4.0` (defer to user-triggered milestone close)
- `/gsd-complete-milestone` invocation (separate user-driven step)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify full test suite is green before close-out</name>
  <files>(read-only — gate)</files>
  <read_first>
    - .planning/phases/37-gap-closure-and-v4-4-0-release/37-CONTEXT.md (success criterion #5: 504+ tests, 0 failures)
    - All 5 prior plan SUMMARYs in the phase (37-01-SUMMARY.md .. 37-05-SUMMARY.md)
  </read_first>
  <action>
    Run `pytest -x -q 2>&1 | tail -10` and capture output.

    Expected: passing line of the form `XXX passed in YY.YYs` where XXX >= 504.

    If the suite has any failures or errors, STOP. Do NOT proceed to Task 2. Surface the
    failure to the user as a gap that must be closed before phase 37 can sign off. Per success
    criterion #5: "Full test suite passes (504+ tests, 0 failures); no open CRITICAL or HIGH
    unresolved issues."

    Also run `python -m compileall quirk/ run_scan.py` to confirm everything compiles.
  </action>
  <verify>
    <automated>pytest -x -q 2>&1 | tail -5 | grep -E '[0-9]+ passed' && python -m compileall quirk/ run_scan.py 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -x -q` exits 0
    - The pass count line shows >= 504 tests passed
    - `python -m compileall quirk/ run_scan.py` exits 0 (no SyntaxError in any module)
  </acceptance_criteria>
  <done>Full suite green; package compiles; safe to proceed to close-out.</done>
</task>

<task type="auto">
  <name>Task 2: Update docs/UAT-SERIES.md for v4.4.0</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (full file — current Last Updated date and version-pinned criteria)
    - CHANGELOG.md (created by Plan 37-05 — the canonical v4.4.0 narrative)
    - CLAUDE.md (Mandatory Phase Completion Steps section — exact wording for what UAT-SERIES updates entail)
  </read_first>
  <action>
    Edit `docs/UAT-SERIES.md`:

    1. Update the `**Last Updated:**` line at the top of the document to today's date in
       the format already used by the existing line (read first; preserve the format).

    2. Find every occurrence of a version-pinned pass criterion referencing `4.3.0` and update
       to `4.4.0`. Use grep first to locate them:
       ```bash
       grep -n '4\.3\.0\|4\.2\.0' docs/UAT-SERIES.md
       ```
       For each match, decide: is this a pass-criterion ("expected: 4.3.0") that should bump
       to 4.4.0, or is it a historical reference (e.g. "v4.3 milestone") that must be
       preserved? Bump the pass criteria; preserve the history.

    3. Add new test cases for v4.4 features that landed in phases 32-36 if the corresponding
       UAT series is missing v4.4 cases. Per CLAUDE.md, "If new features added: add new test
       cases to the relevant series." Specifically check:
       - Email scanner UAT cases (Phase 32)
       - Broker scanner UAT cases (Phase 33)
       - Motion intelligence UAT cases (Phase 34)
       - Motion CBOM UAT cases (Phase 35)
       - Dashboard motion tab UAT cases (Phase 36)

       Each phase already produced its own UAT.md during phase execution; the role here is
       to roll up references into the central UAT-SERIES catalog if not already present. Do
       NOT duplicate the per-phase UAT.md content; reference it.

    4. Do NOT remove existing series. Do NOT renumber existing series.
  </action>
  <verify>
    <automated>grep -c '4.4.0' docs/UAT-SERIES.md && grep -c 'Last Updated' docs/UAT-SERIES.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '4.4.0' docs/UAT-SERIES.md` returns at least 1 (was 0 before this task)
    - `grep '4\.3\.0' docs/UAT-SERIES.md | grep -v 'v4\.3 milestone\|v4\.3 Data\|v4\.3-ROADMAP' | wc -l` returns 0 (only historical references retained, all pass-criteria bumped)
    - The `**Last Updated:**` line exists and shows today's date
    - File still validates as markdown (no broken table rows, no truncation)
  </acceptance_criteria>
  <done>UAT-SERIES.md reflects v4.4.0 reality.</done>
</task>

<task type="auto">
  <name>Task 3: Create Obsidian phase note via direct filesystem write</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-37-Gap-Closure-and-v4-4-0-Release.md</files>
  <read_first>
    - CLAUDE.md (Mandatory Phase Completion Steps — step 1, exact format requirements)
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/ (existing phase notes — read at least one as a template, e.g. Phase-36-*.md if present, or Phase-31-*.md)
    - .planning/phases/37-gap-closure-and-v4-4-0-release/37-01-SUMMARY.md
    - .planning/phases/37-gap-closure-and-v4-4-0-release/37-02-SUMMARY.md
    - .planning/phases/37-gap-closure-and-v4-4-0-release/37-03-SUMMARY.md
    - .planning/phases/37-gap-closure-and-v4-4-0-release/37-04-SUMMARY.md
    - .planning/phases/37-gap-closure-and-v4-4-0-release/37-05-SUMMARY.md
    - .planning/ROADMAP.md §"Phase 37" (goal, success criteria, requirements list)
  </read_first>
  <action>
    Create the Obsidian phase note at the EXACT path:
    `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-37-Gap-Closure-and-v4-4-0-Release.md`

    Use the Write tool (do NOT use the obsidian CLI with `content=` — file may be too large for
    shell expansion per CLAUDE.md guidance).

    File structure (per CLAUDE.md step 1):

    ```markdown
    ---
    project: QU.I.R.K.
    type: phase
    status: complete
    source: .planning/phases/37-gap-closure-and-v4-4-0-release/
    updated: 2026-04-XX
    ---

    # Phase 37: Gap Closure and v4.4.0 Release

    ## Goal

    All v4.4 requirements are verifiably satisfied, Nyquist VALIDATION.md files accurately
    reflect phase completion, and the version string is bumped to 4.4.0 across all output
    artifacts.

    ## Requirements Covered

    - **STRUCT-01** — All new scanners accept session_start parameter (locked by INFRA-03 tests)
    - **STRUCT-02** — All [motion] extras declared in pyproject.toml at plan time
    - **STRUCT-03** — pyproject.toml diff included as Plan 37-02 deliverable
    - **INFRA-01** — Version bumped to 4.4.0 across all surfaces
    - **INFRA-02** — [motion] meta-extra declared with all direct dependencies
    - **INFRA-03** — Nyquist VALIDATION coverage for all 6 scanner entry points × 3 scenarios

    ## Success Criteria

    1. ✅ quirk/__init__.py and pyproject.toml declare version 4.4.0; CBOM metadata + report
       headers all return 4.4.0
    2. ✅ pyproject.toml [motion] extras group is declared with all direct dependencies
    3. ✅ All six scanner entry points have Nyquist VALIDATION.md coverage for happy /
       refused / plaintext-only
    4. ✅ All 6 phase VALIDATION.md files for v4.4 read nyquist_compliant: true and
       wave_0_complete: true
    5. ✅ Full test suite passes (504+ tests, 0 failures)

    ## What Was Built

    ### Plan 37-01: Version Bump (INFRA-01)

    [Source from 37-01-SUMMARY.md]

    ### Plan 37-02: [motion] Meta-Extra Topology (INFRA-02)

    [Source from 37-02-SUMMARY.md]

    ### Plan 37-03: INFRA-03 Nyquist Coverage Tests

    [Source from 37-03-SUMMARY.md]

    ### Plan 37-04: VALIDATION.md Backfill

    [Source from 37-04-SUMMARY.md]

    ### Plan 37-05: Release Artifacts (CHANGELOG + Release Notes)

    [Source from 37-05-SUMMARY.md]

    ### Plan 37-06: Phase Closure (this plan)

    Obsidian phase note + UAT-SERIES.md update + vault sync + final commit. Per D-10/D-11,
    no git tag and no /gsd-complete-milestone in this phase.

    ## Deferred to Milestone Close

    - Git tag `v4.4.0` (D-10)
    - `/gsd-complete-milestone` invocation (D-11)

    ## See Also

    - [[Roadmap]]
    - [[Phase-36-Dashboard-Motion-Tab]]
    - `CHANGELOG.md` (4.4.0 entry)
    - `docs/release-notes/4.4.0.md`

    ---

    *Phase 37 completed 2026-04-XX. v4.4 milestone close pending user-triggered next step.*
    ```

    Replace `2026-04-XX` with today's actual date. Replace `[Source from 37-NN-SUMMARY.md]`
    placeholders with content sourced from each plan's SUMMARY.md (per CLAUDE.md step 1:
    "What Was Built (one subsection per plan, sourced from SUMMARY.md files)").

    If the parent directory does not exist, create it first:
    `mkdir -p /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/`
  </action>
  <verify>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-37-Gap-Closure-and-v4-4-0-Release.md" && grep -c 'status: complete' "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-37-Gap-Closure-and-v4-4-0-Release.md" && grep -c '\[\[Roadmap\]\]' "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-37-Gap-Closure-and-v4-4-0-Release.md"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the exact path `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-37-Gap-Closure-and-v4-4-0-Release.md`
    - Frontmatter contains `status: complete`, `type: phase`, `project: QU.I.R.K.`, `source:`, `updated:`
    - File contains all required sections: Goal, Requirements Covered, Success Criteria, What Was Built, See Also
    - File contains a [[Roadmap]] wikilink
    - All 6 plan subsections are present in "What Was Built"
    - No `[Source from ...]` placeholder strings remain — `grep -c '\[Source from' "/Users/digs/.../Phase-37-*.md"` returns 0
    - Date placeholder `2026-04-XX` replaced with real date
  </acceptance_criteria>
  <done>Obsidian phase note in place per CLAUDE.md mandatory step 1.</done>
</task>

<task type="auto">
  <name>Task 4: Sync UAT-SERIES.md to Obsidian vault via direct filesystem write</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md</files>
  <read_first>
    - docs/UAT-SERIES.md (just updated by Task 2)
    - CLAUDE.md (Mandatory Phase Completion Steps — step 3 verbatim command)
  </read_first>
  <action>
    Run the EXACT three-command sequence from CLAUDE.md step 3 (substituting today's date):

    ```bash
    printf "---\nproject: QU.I.R.K.\ntype: reference\nstatus: active\nsource: docs/UAT-SERIES.md\nupdated: 2026-04-28\n---\n\n" > /tmp/uat_vault.md
    cat docs/UAT-SERIES.md >> /tmp/uat_vault.md
    cp /tmp/uat_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
    ```

    Replace `2026-04-28` with today's actual date.

    Do NOT use the obsidian CLI's `content=` parameter — the file is too large for shell
    expansion (CLAUDE.md explicitly warns against this).

    The vault target path is `UAT-Series.md` (note the hyphen — different from the repo
    filename `UAT-SERIES.md`).
  </action>
  <verify>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" && head -10 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" | grep -c 'project: QU.I.R.K.' && [ "$(wc -l < /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md)" -gt "$(wc -l < docs/UAT-SERIES.md)" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` exists
    - First line of the file is `---` (frontmatter open)
    - Frontmatter contains `project: QU.I.R.K.`, `type: reference`, `status: active`, `source: docs/UAT-SERIES.md`, `updated: <today>`
    - Vault file line count > docs/UAT-SERIES.md line count (because of the prepended frontmatter — proves the cat-append worked)
  </acceptance_criteria>
  <done>UAT-SERIES.md synced to Obsidian vault per CLAUDE.md mandatory step 3.</done>
</task>

<task type="auto">
  <name>Task 5: Commit all phase 37 artifacts</name>
  <files>(git operations only)</files>
  <read_first>
    - CLAUDE.md (Mandatory Phase Completion Steps — step 4 verbatim command)
    - .planning/phases/37-gap-closure-and-v4-4-0-release/ (all SUMMARY.md files)
  </read_first>
  <action>
    Two commits, in order:

    1. Commit the UAT-SERIES update (per CLAUDE.md step 4 verbatim):
       ```bash
       node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-37): update UAT-SERIES.md" --files docs/UAT-SERIES.md
       ```

    2. Commit all other phase-37 artifacts (everything created by plans 37-01..37-06 that
       was not already committed during execution):
       ```bash
       node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "feat(37): v4.4.0 gap closure and release — INFRA-01/02/03, VALIDATION backfill, CHANGELOG, release notes" --files \
         quirk/__init__.py \
         pyproject.toml \
         quirk/cbom/builder.py \
         quirk/reports/writer.py \
         quirk/config.py \
         tests/test_version.py \
         tests/test_infra03_nyquist_coverage.py \
         CHANGELOG.md \
         docs/release-notes/4.4.0.md \
         .planning/phases/32-email-scanner/32-VALIDATION.md \
         .planning/phases/33-broker-scanner/33-VALIDATION.md \
         .planning/phases/34-motion-intelligence/34-VALIDATION.md \
         .planning/phases/35-cbom-integration/35-VALIDATION.md \
         .planning/phases/36-dashboard-motion-tab/36-VALIDATION.md \
         .planning/phases/37-gap-closure-and-v4-4-0-release/
       ```

    If individual plan execution already committed some files (e.g. plan 37-01 may have
    committed quirk/__init__.py at the time of its task), the commit at step 2 will simply
    not include those files (no error). The goal is that everything in the diff is committed
    by the end of this task.

    Do NOT create a git tag (D-10). Do NOT push. Do NOT invoke `/gsd-complete-milestone` (D-11).

    After both commits, run `git status` to confirm a clean working tree.
  </action>
  <verify>
    <automated>git status --porcelain | wc -l && git log --oneline -3</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` outputs 0 lines (clean working tree — every phase-37 artifact committed)
    - `git log --oneline -2` shows the two commits with subjects starting with `docs(phase-37):` and `feat(37):`
    - `git tag --list 'v4.4.0'` returns nothing (no tag created — D-10 honored)
    - Working branch is still QUIRK-v4 (no rebase, no force push)
  </acceptance_criteria>
  <done>All phase 37 artifacts committed; working tree clean; no tag, no milestone-close trigger.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Final phase 37 sign-off</name>
  <what-built>
    All four CLAUDE.md mandatory close-out steps complete:
    1. Obsidian phase note created
    2. docs/UAT-SERIES.md updated
    3. UAT-SERIES.md synced to vault
    4. All artifacts committed

    Per D-10/D-11, the v4.4.0 git tag and `/gsd-complete-milestone` invocation are deferred to
    a separate user-triggered step.
  </what-built>
  <how-to-verify>
    1. `git log --oneline -5` — confirm the two phase-37 commits at the top, subjects readable
    2. `git status` — confirm clean working tree
    3. `pytest -x -q 2>&1 | tail -3` — confirm 504+ tests, 0 failures
    4. `cat /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-37-Gap-Closure-and-v4-4-0-Release.md | head -20` — confirm frontmatter and Goal section render
    5. `grep -E '^(nyquist_compliant|wave_0_complete):' .planning/phases/3{2,3,4,5,6,7}-*/[0-9]*-VALIDATION.md` — confirm all six v4.4 phase VALIDATION.md files read true
    6. `grep '"4.4.0"' quirk/__init__.py pyproject.toml quirk/cbom/builder.py quirk/reports/writer.py quirk/config.py` — confirm all five version surfaces at 4.4.0
    7. `pytest tests/test_version.py tests/test_infra03_nyquist_coverage.py -q` — confirm INFRA-01 + INFRA-03 lock tests green

    If all seven checks pass, type "approved" and Phase 37 is complete. The next step is
    user-triggered: create the `v4.4.0` git tag and run `/gsd-complete-milestone v4.4`.
  </how-to-verify>
  <resume-signal>
    Type "approved" to close Phase 37, or describe which check failed.
  </resume-signal>
</task>

</tasks>

<verification>
- All four CLAUDE.md mandatory steps complete (Obsidian note, UAT update, vault sync, commit)
- `git status` clean
- `pytest -x -q` green
- All 6 v4.4 phase VALIDATION.md files read nyquist_compliant: true and wave_0_complete: true
- All 5 version surfaces declare 4.4.0
- No git tag created (D-10); no /gsd-complete-milestone triggered (D-11)
</verification>

<success_criteria>
Phase 37 closed per CLAUDE.md ritual; all v4.4 milestone requirements verifiably satisfied;
repo state ready for the user-triggered milestone-close step.
</success_criteria>

<output>
After completion, create `.planning/phases/37-gap-closure-and-v4-4-0-release/37-06-SUMMARY.md`
including the test-suite output from Task 1 and the final git log from Task 5.
</output>
