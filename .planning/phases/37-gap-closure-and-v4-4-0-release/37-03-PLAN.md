---
phase: 37
plan: 03
type: execute
wave: 3
depends_on: [37-02]
files_modified:
  - tests/test_infra03_nyquist_coverage.py
autonomous: true
requirements:
  - INFRA-03
  - STRUCT-01
must_haves:
  truths:
    - "tests/test_infra03_nyquist_coverage.py exists and contains 18 explicit test functions"
    - "Each of the 6 scanner entry points has exactly 3 test cases: happy / refused / plaintext-only"
    - "All 18 tests pass against current main (mocked sslyze + socket where needed)"
    - "Each test imports its scanner entry point directly (not via re-export shim)"
  artifacts:
    - path: "tests/test_infra03_nyquist_coverage.py"
      provides: "INFRA-03 single-surface audit artifact (D-03)"
      contains: "test_scan_email_targets_happy"
      min_lines: 200
  key_links:
    - from: "tests/test_infra03_nyquist_coverage.py"
      to: "quirk/scanner/email_scanner.py"
      via: "imports scan_email_targets directly"
      pattern: "from quirk.scanner.email_scanner import scan_email_targets"
    - from: "tests/test_infra03_nyquist_coverage.py"
      to: "quirk/scanner/broker_scanner.py"
      via: "imports scan_kafka_targets / scan_rabbitmq_targets / scan_redis_targets directly"
      pattern: "from quirk.scanner.broker_scanner import"
---

<objective>
Create a single dedicated test module `tests/test_infra03_nyquist_coverage.py` containing
18 explicit test functions: 6 scanner entry points × 3 scenarios (happy / refused / plaintext-only),
per D-03. This is the single auditable surface for INFRA-03; existing scenario tests in
`test_email_scanner.py` and `test_broker_scanner.py` remain untouched.

Purpose: Satisfy INFRA-03 — every motion scanner entry point has Nyquist coverage for happy
path, graceful degradation, and plaintext-only detection.
Output: One new test module with 18 passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/37-gap-closure-and-v4-4-0-release/37-CONTEXT.md
@.planning/phases/32-email-scanner/32-VALIDATION.md
@.planning/phases/33-broker-scanner/33-VALIDATION.md

<interfaces>
The 6 scanner entry points (per D-03 in 37-CONTEXT.md):
1. `quirk.scanner.email_scanner.scan_email_targets`
2. `quirk.scanner.broker_scanner.scan_kafka_targets`
3. `quirk.scanner.broker_scanner.scan_rabbitmq_targets`
4. `quirk.scanner.broker_scanner.scan_redis_targets`
5. The Azure Service Bus probe path inside `quirk.scanner.broker_scanner` (RABBIT-04) — likely
   reachable via `scan_rabbitmq_targets` with an Azure-flavored target, or a dedicated helper
   like `_probe_azure_servicebus`. Read broker_scanner.py to determine the actual public surface.
6. The AWS SQS probe path inside `quirk.scanner.broker_scanner` (RABBIT-05) — same: identify
   whether it is exposed via `scan_rabbitmq_targets` or a separate helper.

For (5) and (6): if they are NOT public top-level functions, the test still exercises them
via the highest-level public callable that triggers the code path, with a target argument
that forces the Azure / SQS branch (e.g. `target="myns.servicebus.windows.net"` for Azure,
`target="sqs.us-east-1.amazonaws.com"` for SQS). Read broker_scanner.py to confirm the
dispatch logic before writing the tests.

Scenarios (per INFRA-03 wording in REQUIREMENTS.md line 136):
- **happy**: TLS handshake succeeds, scanner returns ≥1 CryptoEndpoint with cert + cipher suite
- **refused**: socket.create_connection (or sslyze) raises ConnectionRefusedError; scanner
  returns gracefully (empty list or partial result with no exception bubble)
- **plaintext-only**: TLS port refused, plaintext port responds; scanner emits the
  appropriate plaintext finding (KAFKA-02 `kafka-plaintext-listener`, RABBIT-02
  `amqp-plaintext-listener`, REDIS-02 `redis-plaintext-no-auth`, EMAIL: SMTP/IMAP/POP3
  without STARTTLS produces a finding from EMAIL-08/09 path)

STRUCT-01 implication: every scanner entry point accepts a `session_start: datetime`
parameter. Tests must pass a fixed `datetime(2026, 1, 1, tzinfo=timezone.utc)` to lock
out per-scanner datetime drift.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Identify the exact entry-point signature for Azure Service Bus and SQS probes</name>
  <files>(read-only — produces signature notes)</files>
  <read_first>
    - quirk/scanner/broker_scanner.py (full file)
    - .planning/phases/33-broker-scanner/ (any SUMMARY.md and PLAN.md files that describe how RABBIT-04 / RABBIT-05 are dispatched)
  </read_first>
  <action>
    Run these commands and capture output:
    ```bash
    grep -n '^def \|^    def ' quirk/scanner/broker_scanner.py
    grep -n 'servicebus\|sqs\.' quirk/scanner/broker_scanner.py
    grep -n 'AMQPS/Azure-ServiceBus\|HTTPS/AWS-SQS' quirk/scanner/broker_scanner.py
    ```

    Determine for the Azure Service Bus and AWS SQS probes:
    - Is there a public function (e.g. `scan_servicebus_targets`, `scan_sqs_targets`) — name it
    - OR are they branches inside `scan_rabbitmq_targets` / `scan_kafka_targets` triggered by hostname pattern?

    Record the answer. Task 2 uses it to choose how to call the probe in tests 13-15 (Azure)
    and tests 16-18 (SQS).

    No file modification.
  </action>
  <verify>
    <automated>grep -c '^def scan_' quirk/scanner/broker_scanner.py</automated>
  </verify>
  <acceptance_criteria>
    - Output of `grep '^def scan_' quirk/scanner/broker_scanner.py` recorded for Task 2
    - The Azure / SQS dispatch mechanism is identified and documented in implementer notes
    - No source file modified
  </acceptance_criteria>
  <done>Caller pattern for Azure Service Bus and AWS SQS probes is identified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write 18 INFRA-03 Nyquist coverage tests in a single module</name>
  <files>tests/test_infra03_nyquist_coverage.py</files>
  <read_first>
    - tests/test_email_scanner.py (skim for fixture patterns — sslyze mocking, target dict shape, session_start usage)
    - tests/test_broker_scanner.py (skim for fixture patterns — mock_sslyze, _detect_kafka_plaintext patching, target dict shape)
    - quirk/scanner/email_scanner.py (signature of scan_email_targets — confirm parameters: targets, session_start, profile, logger)
    - quirk/scanner/broker_scanner.py (signatures of scan_kafka_targets, scan_rabbitmq_targets, scan_redis_targets + Azure/SQS dispatch confirmed in Task 1)
    - quirk/models.py (CryptoEndpoint shape so happy-path tests can assert ≥1 endpoint returned)
    - .planning/phases/37-gap-closure-and-v4-4-0-release/37-CONTEXT.md (D-03 verbatim — 18 explicit functions, single auditable artifact)
  </read_first>
  <behavior>
    Module contains 18 test functions named exactly:

    Email (3 tests):
    - test_scan_email_targets_happy
    - test_scan_email_targets_refused
    - test_scan_email_targets_plaintext_only

    Kafka (3 tests):
    - test_scan_kafka_targets_happy
    - test_scan_kafka_targets_refused
    - test_scan_kafka_targets_plaintext_only

    RabbitMQ (3 tests):
    - test_scan_rabbitmq_targets_happy
    - test_scan_rabbitmq_targets_refused
    - test_scan_rabbitmq_targets_plaintext_only

    Redis (3 tests):
    - test_scan_redis_targets_happy
    - test_scan_redis_targets_refused
    - test_scan_redis_targets_plaintext_only

    Azure Service Bus (3 tests):
    - test_azure_servicebus_probe_happy
    - test_azure_servicebus_probe_refused
    - test_azure_servicebus_probe_plaintext_only  (Azure SB has no plaintext analog at 5671, so this test asserts the documented absence: probe with non-existent host returns empty / no AMQPS endpoint without raising — explicit comment in test body explaining why "plaintext-only" is degenerate for Azure SB)

    AWS SQS (3 tests):
    - test_aws_sqs_probe_happy
    - test_aws_sqs_probe_refused
    - test_aws_sqs_probe_plaintext_only  (AWS SQS HTTPS-only on 443; "plaintext-only" probes 80 and asserts the scanner emits no false-positive finding, explicit comment explaining the degenerate scenario)

    Per-test contract:
    - **happy**: mock sslyze (or socket.create_connection where appropriate) to return a successful handshake; assert scanner returns at least one CryptoEndpoint with non-None tls_version
    - **refused**: monkeypatch the network call to raise ConnectionRefusedError; assert scanner returns without raising and returns either [] or a list with zero TLS-bearing endpoints
    - **plaintext-only**: mock the TLS path to refuse and the plaintext-detect helper (`_detect_kafka_plaintext` for Kafka, raw socket connect for Redis/RabbitMQ, sslyze refusal + smtplib succeed for email) to succeed; assert the scanner emits the expected plaintext finding via the returned CryptoEndpoint or its findings field
  </behavior>
  <action>
    Create `tests/test_infra03_nyquist_coverage.py` with the 18 functions named above.

    Implementation requirements (concrete):

    1. Module docstring at top: `"""INFRA-03 Nyquist coverage — 6 scanner entry points × 3 scenarios = 18 tests. See REQUIREMENTS.md INFRA-03 and 37-CONTEXT.md D-03. This is the single auditable artifact for INFRA-03."""`

    2. Imports:
       ```python
       from datetime import datetime, timezone
       from unittest.mock import MagicMock, patch
       import pytest
       from quirk.scanner.email_scanner import scan_email_targets
       from quirk.scanner.broker_scanner import (
           scan_kafka_targets,
           scan_rabbitmq_targets,
           scan_redis_targets,
       )
       ```
       Plus whichever Azure/SQS callable Task 1 identified.

    3. Module-level constant `SESSION_START = datetime(2026, 1, 1, tzinfo=timezone.utc)` — every test passes this as `session_start=` (STRUCT-01 lock).

    4. Use the same mocking patterns already proven in tests/test_email_scanner.py and
       tests/test_broker_scanner.py — do NOT invent new fixture infrastructure. If the
       existing patterns require pytest fixtures from conftest.py, import them or
       monkeypatch inline.

    5. Each test must include an inline comment listing the requirement IDs it covers,
       e.g. `# INFRA-03 / EMAIL-01 / EMAIL-08 — port-25 plaintext-only path`.

    6. The plaintext-only tests for email must use port 25 (where STARTTLS-downgrade-risk
       finding is the canonical plaintext-only signal per EMAIL-08).

    7. Tests must be deterministic — no live network calls. Any test that would hit a real
       socket must be patched with `unittest.mock.patch` or monkeypatch.

    8. The 18-function count is non-negotiable. No parametrization — D-03 explicitly says
       "18 explicit test functions, one per (entry_point, scenario) pair".

    Reference the existing scanner test modules for cipher-suite mock shapes, sslyze
    ServerScanResult mock shapes, and the `_detect_kafka_plaintext` patch pattern. Do not
    duplicate fixture definitions — import from conftest.py if shared fixtures exist.
  </action>
  <verify>
    <automated>pytest tests/test_infra03_nyquist_coverage.py -v --tb=short 2>&1 | tail -40 && grep -c '^def test_' tests/test_infra03_nyquist_coverage.py</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_infra03_nyquist_coverage.py` exists
    - `grep -c '^def test_' tests/test_infra03_nyquist_coverage.py` returns exactly 18
    - All 18 test names appear: `for n in test_scan_email_targets_happy test_scan_email_targets_refused test_scan_email_targets_plaintext_only test_scan_kafka_targets_happy test_scan_kafka_targets_refused test_scan_kafka_targets_plaintext_only test_scan_rabbitmq_targets_happy test_scan_rabbitmq_targets_refused test_scan_rabbitmq_targets_plaintext_only test_scan_redis_targets_happy test_scan_redis_targets_refused test_scan_redis_targets_plaintext_only test_azure_servicebus_probe_happy test_azure_servicebus_probe_refused test_azure_servicebus_probe_plaintext_only test_aws_sqs_probe_happy test_aws_sqs_probe_refused test_aws_sqs_probe_plaintext_only; do grep -q "^def $n" tests/test_infra03_nyquist_coverage.py || exit 1; done` exits 0
    - `pytest tests/test_infra03_nyquist_coverage.py -x -q` exits 0 (all 18 pass)
    - `grep -c 'session_start=SESSION_START' tests/test_infra03_nyquist_coverage.py` returns at least 18 (one per test, STRUCT-01 lock)
    - No test imports from a non-`quirk.scanner.*` module for the entry point under test (D-03: "imports the scanner entry point directly")
    - File contains the literal docstring "INFRA-03 Nyquist coverage" in the module docstring
  </acceptance_criteria>
  <done>18 deterministic INFRA-03 tests pass; single auditable artifact for the requirement is in place.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_infra03_nyquist_coverage.py -v -q` exits 0
- Full suite (`pytest -x -q`) shows no NEW failures
- `grep -c '^def test_' tests/test_infra03_nyquist_coverage.py` returns exactly 18
- File line count > 200 (sanity check that tests have real bodies, not stubs)
</verification>

<success_criteria>
INFRA-03 satisfied: every motion scanner entry point has happy / refused / plaintext-only
coverage in a single auditable module that downstream VALIDATION matrices (Plan 37-04) can
cite by test-function name.
</success_criteria>

<output>
After completion, create `.planning/phases/37-gap-closure-and-v4-4-0-release/37-03-SUMMARY.md`
including the 18 test names and the entry-point dispatch decision from Task 1.
</output>
