---
phase: 37
plan: 02
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - pyproject.toml
autonomous: true
requirements:
  - INFRA-02
  - STRUCT-02
  - STRUCT-03
must_haves:
  truths:
    - "pyproject.toml [project.optional-dependencies] motion is a meta-extra referencing quirk[email], quirk[broker], quirk[kafka] (D-01)"
    - "[email] sub-extra exists and is empty or only declares stdlib-superset deps (email_scanner.py uses only stdlib + cryptography + sslyze, all already in core)"
    - "[broker] sub-extra exists and pins redis>=5.0 (matches REDIS-03 redis-py guard in broker_scanner.py); no pika required because RABBIT-02 uses stdlib socket per Task 1 inventory"
    - "[kafka] sub-extra continues to pin kafka-python>=2.0"
    - "pip install -e .[motion] resolves to a superset of [email]+[broker]+[kafka] with no errors"
  artifacts:
    - path: "pyproject.toml"
      provides: "[motion] meta-extra and [email]/[broker] sub-extras"
      contains: "motion ="
  key_links:
    - from: "pyproject.toml"
      to: "quirk/scanner/email_scanner.py"
      via: "[email] extras must cover non-stdlib imports"
      pattern: 'email\s*=\s*\['
    - from: "pyproject.toml"
      to: "quirk/scanner/broker_scanner.py"
      via: "[broker] extras must cover non-stdlib imports"
      pattern: 'broker\s*=\s*\['
---

<objective>
Restructure `[project.optional-dependencies]` in pyproject.toml so `[motion]` is a meta-extra
that pulls in flat sub-extras `[email]`, `[broker]`, `[kafka]` (per D-01). Inventory the actual
non-stdlib imports in `quirk/scanner/email_scanner.py` and `quirk/scanner/broker_scanner.py`
(per D-02) and pin matching distributions in the relevant sub-extras.

Purpose: Satisfy INFRA-02 — `pip install quirk[motion]` is the single happy path that yields a
working email + broker scanner; the asymmetry between `motion = []` and `kafka = [...]` is
eliminated.
Output: pyproject.toml with corrected extras topology.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/37-gap-closure-and-v4-4-0-release/37-CONTEXT.md

<interfaces>
Current pyproject.toml [project.optional-dependencies] (verified 2026-04-28):

```toml
motion = []
kafka = ["kafka-python>=2.0"]
redis = ["redis>=5.0"]
```

There is no `[email]` extra and no `[broker]` extra today. INFRA-02 asks for them.

Email scanner imports (verified 2026-04-28 via grep on quirk/scanner/email_scanner.py):
- stdlib: json, ssl, socket, smtplib, imaplib, poplib, concurrent.futures, datetime, typing
- already in core deps: cryptography (x509, hazmat.backends)
- conditional: sslyze (already in core via tls_scanner; not a motion dep)
- internal: quirk.models, quirk.logging_util, quirk.scanner.tls_scanner

→ email_scanner.py has NO new direct external deps beyond core. [email] should be present
  but EMPTY (placeholder so the meta-extra resolves) OR omitted from the meta-extra.

Broker scanner imports (verified 2026-04-28 via grep on quirk/scanner/broker_scanner.py):
- stdlib: base64, json, socket, ssl, urllib.error, urllib.request, concurrent.futures, datetime, typing
- already in core deps: cryptography
- conditional optional: kafka.admin (covered by existing [kafka])
- conditional optional: redis (covered by existing [redis])
- NO pika import currently — RabbitMQ probing uses raw socket + AMQP header bytes (per RABBIT-02 implementation)
- NO confluent_kafka

→ broker_scanner.py's optional deps are [kafka] and [redis]. There is currently NO
  imported pika — the CONTEXT.md note "(likely: ... plus pika)" was a research-time
  guess, not ground truth. Use actual imports per D-02.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit scanner imports and document inventory</name>
  <files>(read-only — produces audit notes appended to plan summary)</files>
  <read_first>
    - quirk/scanner/email_scanner.py (full file — confirm imports list at top + any inline `import` inside try/except)
    - quirk/scanner/broker_scanner.py (full file — confirm imports list at top + any inline `import` inside try/except)
    - .planning/phases/37-gap-closure-and-v4-4-0-release/37-CONTEXT.md (D-02 wording: "Inventory by actual scanner imports")
  </read_first>
  <action>
    Run these exact commands and capture output verbatim into the plan SUMMARY.md (will be created at end):

    ```bash
    grep -n '^import \|^from ' quirk/scanner/email_scanner.py
    grep -n '^import \|^from ' quirk/scanner/broker_scanner.py
    grep -n -E 'try:|import |from ' quirk/scanner/email_scanner.py | head -40
    grep -n -E 'try:|import |from ' quirk/scanner/broker_scanner.py | head -60
    ```

    For each non-stdlib, non-already-in-core import, record:
    - Module name (e.g., `kafka.admin`, `redis`)
    - Distribution name on PyPI (e.g., `kafka-python`, `redis`)
    - Which extra it belongs in: `[email]`, `[broker]`, or `[kafka]`

    DO NOT modify any file in this task. The audit's only output is the inventory list, which
    Task 2 consumes. If an import is found that does NOT have a corresponding extras entry
    after Task 2, that is an INFRA-02 violation and the plan must be revised before commit.

    Expected inventory (verify against actual greps — if grep output diverges, ADJUST Task 2):
    - email_scanner.py non-stdlib non-core: NONE (sslyze is in core via tls_scanner; cryptography is in core)
    - broker_scanner.py non-stdlib non-core: kafka.admin (→ kafka-python, [kafka]) ; redis (→ redis, [redis])

    No pika, no confluent_kafka, no aiokafka. If a future scanner adds them, that future
    phase declares them per STRUCT-02.
  </action>
  <verify>
    <automated>grep -c 'pika\|confluent_kafka\|aiokafka' quirk/scanner/email_scanner.py quirk/scanner/broker_scanner.py | grep -v ':0$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The grep above outputs `0` (no unexpected non-inventoried distributions imported anywhere)
    - Audit text recorded in implementer notes for use by Task 2
    - No source file modified by this task
  </acceptance_criteria>
  <done>Import inventory enumerated and matched against extras structure proposed in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure pyproject.toml extras to meta-topology</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (full file — current extras block lines 33-60)
    - Output of Task 1 (import inventory)
    - .planning/phases/37-gap-closure-and-v4-4-0-release/37-CONTEXT.md (D-01 verbatim spec)
  </read_first>
  <action>
    Replace the existing `[project.optional-dependencies]` block such that the motion-related
    extras read EXACTLY as follows (preserve all other extras: dashboard, identity, cloud, db):

    ```toml
    motion = ["quirk[email]", "quirk[broker]", "quirk[kafka]"]
    email = []
    broker = ["redis>=5.0"]
    kafka = ["kafka-python>=2.0"]
    redis = ["redis>=5.0"]
    ```

    Concrete edits, in order:

    1. Replace the line `motion = []` with `motion = ["quirk[email]", "quirk[broker]", "quirk[kafka]"]`
    2. Add a new line `email = []` immediately after the new `motion = ` line (placeholder per D-02 — email_scanner has no external deps beyond core)
    3. Add a new line `broker = ["redis>=5.0"]` immediately after the new `email = []` line (covers REDIS-03 redis-py guard inside broker_scanner.py)
    4. Leave existing `kafka = ["kafka-python>=2.0"]` and `redis = ["redis>=5.0"]` untouched (they are still independently installable per D-01)

    Rationale (D-02): broker_scanner.py imports `redis as redis_lib` for REDIS-03 enrichment.
    That makes `redis` a [broker] dep. `kafka.admin` is for KAFKA-04 enrichment, still
    installable via the standalone `[kafka]` extra. RabbitMQ probing uses only stdlib socket,
    so `[broker]` does not need `pika` (the CONTEXT.md note was a guess, not ground truth — see
    Task 1 inventory).

    Do not reorder the surrounding extras (dashboard, identity, cloud, db). Do not change any
    version pin in core dependencies.
  </action>
  <verify>
    <automated>python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); od=d['project']['optional-dependencies']; assert od['motion']==['quirk[email]','quirk[broker]','quirk[kafka]'], od['motion']; assert od['email']==[], od['email']; assert 'redis>=5.0' in od['broker']; assert 'kafka-python>=2.0' in od['kafka']; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '^motion = \["quirk\[email\]", "quirk\[broker\]", "quirk\[kafka\]"\]' pyproject.toml` returns one match
    - `grep '^email = \[\]' pyproject.toml` returns one match
    - `grep '^broker = ' pyproject.toml` returns one match and includes `redis>=5.0`
    - `grep '^kafka = \["kafka-python>=2.0"\]' pyproject.toml` returns one match (unchanged)
    - `grep -c '^motion = \[\]' pyproject.toml` returns 0 (old empty meta-extra is gone)
    - `python -c "import tomllib; tomllib.load(open('pyproject.toml','rb'))"` exits 0 (TOML still parses)
    - `pip install -e .[motion] --dry-run` (or `pip install -e .[motion]` if dry-run unsupported) does not error on extras resolution
  </acceptance_criteria>
  <done>pyproject.toml [motion] is a meta-extra; sub-extras match actual scanner imports per D-02.</done>
</task>

</tasks>

<verification>
- `python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); print(d['project']['optional-dependencies']['motion'])"` prints `['quirk[email]', 'quirk[broker]', 'quirk[kafka]']`
- `pip install -e ".[motion]" 2>&1 | grep -i error` returns nothing
- `grep -c '^motion =' pyproject.toml` returns 1 (no duplicate)
- Full test suite still collects (`pytest --collect-only -q` exits 0)
</verification>

<success_criteria>
INFRA-02 satisfied: `[motion]` is a meta-extra; sub-extras `[email]`, `[broker]`, `[kafka]` are
declared; every non-stdlib non-core import in email_scanner.py and broker_scanner.py is covered
by exactly one sub-extra; `pip install quirk[motion]` is the single happy path.
</success_criteria>

<output>
After completion, create `.planning/phases/37-gap-closure-and-v4-4-0-release/37-02-SUMMARY.md`
including the import-inventory output from Task 1.
</output>
