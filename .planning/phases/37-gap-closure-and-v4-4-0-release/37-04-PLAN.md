---
phase: 37
plan: 04
type: execute
wave: 4
depends_on: [37-03]
files_modified:
  - .planning/phases/32-email-scanner/32-VALIDATION.md
  - .planning/phases/33-broker-scanner/33-VALIDATION.md
  - .planning/phases/34-motion-intelligence/34-VALIDATION.md
  - .planning/phases/35-cbom-integration/35-VALIDATION.md
  - .planning/phases/36-dashboard-motion-tab/36-VALIDATION.md
  - .planning/phases/37-gap-closure-and-v4-4-0-release/37-VALIDATION.md
autonomous: false
requirements:
  - INFRA-03
must_haves:
  truths:
    - "Phase 32 VALIDATION.md contains a 'Nyquist Scenarios — INFRA-03' subsection citing 3 test names from test_infra03_nyquist_coverage.py for scan_email_targets (D-04)"
    - "Phase 33 VALIDATION.md contains a 'Nyquist Scenarios — INFRA-03' subsection citing 12 test names from test_infra03_nyquist_coverage.py for the four broker entry points (D-04)"
    - "Phase 34 VALIDATION.md re-run by gsd-plan-checker; nyquist_compliant flag reflects ground truth (D-05)"
    - "Phase 35 VALIDATION.md created by gsd-plan-checker (file did not exist) (D-05)"
    - "Phase 36 VALIDATION.md wave_0_complete: true after verifying 36-VERIFICATION.md is approved + 36-UAT.md is signed off + pytest -x -q is green on main (D-06)"
    - "Phase 37 VALIDATION.md exists with frontmatter declaring nyquist_compliant: true and wave_0_complete: true"
  artifacts:
    - path: ".planning/phases/32-email-scanner/32-VALIDATION.md"
      provides: "Nyquist scenarios matrix for email scanner"
      contains: "Nyquist Scenarios"
    - path: ".planning/phases/33-broker-scanner/33-VALIDATION.md"
      provides: "Nyquist scenarios matrix for broker scanners"
      contains: "Nyquist Scenarios"
    - path: ".planning/phases/35-cbom-integration/35-VALIDATION.md"
      provides: "Plan-checker-generated VALIDATION for phase 35 (was missing)"
      contains: "phase: 35"
    - path: ".planning/phases/37-gap-closure-and-v4-4-0-release/37-VALIDATION.md"
      provides: "This phase's own validation contract"
      contains: "phase: 37"
  key_links:
    - from: ".planning/phases/32-email-scanner/32-VALIDATION.md"
      to: "tests/test_infra03_nyquist_coverage.py"
      via: "scenario rows reference test function names"
      pattern: "test_scan_email_targets_(happy|refused|plaintext_only)"
    - from: ".planning/phases/33-broker-scanner/33-VALIDATION.md"
      to: "tests/test_infra03_nyquist_coverage.py"
      via: "scenario rows reference test function names"
      pattern: "test_scan_(kafka|rabbitmq|redis)_targets_(happy|refused|plaintext_only)"
---

<objective>
Backfill VALIDATION.md across phases 32-37 to accurately reflect ground truth (D-04, D-05, D-06):
- Phases 32 + 33: add an inline "Nyquist Scenarios — INFRA-03" matrix per D-04 referencing
  the 18 tests created by Plan 37-03.
- Phases 34 + 35: re-run gsd-plan-checker per D-05; whatever it produces is authoritative.
- Phase 36: flip wave_0_complete to true per D-06 ONLY after verifying the gating artifacts.
- Phase 37: create this phase's own VALIDATION.md.

Purpose: Satisfy success criterion #4 — all 6 v4.4 phase VALIDATION.md files read
nyquist_compliant: true and wave_0_complete: true with an honest audit trail.
Output: 6 VALIDATION.md files updated/created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/37-gap-closure-and-v4-4-0-release/37-CONTEXT.md
@.planning/phases/32-email-scanner/32-VALIDATION.md
@.planning/phases/33-broker-scanner/33-VALIDATION.md
@.planning/phases/34-motion-intelligence/34-VALIDATION.md
@.planning/phases/36-dashboard-motion-tab/36-VALIDATION.md

<interfaces>
The 18 test names available for citation (from Plan 37-03 output):

Email scanner (Phase 32):
- test_scan_email_targets_happy
- test_scan_email_targets_refused
- test_scan_email_targets_plaintext_only

Broker scanners (Phase 33):
- test_scan_kafka_targets_{happy,refused,plaintext_only}
- test_scan_rabbitmq_targets_{happy,refused,plaintext_only}
- test_scan_redis_targets_{happy,refused,plaintext_only}
- test_azure_servicebus_probe_{happy,refused,plaintext_only}
- test_aws_sqs_probe_{happy,refused,plaintext_only}

VALIDATION.md frontmatter convention (from existing files):
```yaml
---
phase: NN
slug: phase-slug
status: approved
nyquist_compliant: true
wave_0_complete: true
created: YYYY-MM-DD
updated: YYYY-MM-DD
---
```

gsd-plan-checker invocation (per D-05):
```bash
gsd-sdk query plan-checker.run --phase 34
```
(or whatever the actual current invocation is — confirm against CLAUDE.md / get-shit-done docs
before running. If the SDK exposes it, use that; if not, fall back to the slash command
`/gsd-check-plan 34`.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Nyquist Scenarios matrix to phase 32 and phase 33 VALIDATION.md</name>
  <files>.planning/phases/32-email-scanner/32-VALIDATION.md, .planning/phases/33-broker-scanner/33-VALIDATION.md</files>
  <read_first>
    - .planning/phases/32-email-scanner/32-VALIDATION.md (full file — current frontmatter shows nyquist_compliant: true, wave_0_complete: true)
    - .planning/phases/33-broker-scanner/33-VALIDATION.md (full file)
    - tests/test_infra03_nyquist_coverage.py (created by Plan 37-03 — confirm test names match this plan's frontmatter)
    - .planning/phases/37-gap-closure-and-v4-4-0-release/37-CONTEXT.md (D-04 verbatim)
  </read_first>
  <action>
    For 32-VALIDATION.md: append a new top-level section before the "Validation Sign-Off" section:

    ```markdown
    ---

    ## Nyquist Scenarios — INFRA-03

    Per CONTEXT.md D-04: each phase that owns a scanner inlines a Nyquist coverage matrix
    pointing at the dedicated INFRA-03 audit module `tests/test_infra03_nyquist_coverage.py`.

    | Entry Point | Scenario | Test Name | Status |
    |-------------|----------|-----------|--------|
    | scan_email_targets | happy (TLS found) | test_scan_email_targets_happy | ✅ |
    | scan_email_targets | refused (graceful degradation) | test_scan_email_targets_refused | ✅ |
    | scan_email_targets | plaintext-only | test_scan_email_targets_plaintext_only | ✅ |

    *Verification: `pytest tests/test_infra03_nyquist_coverage.py -k scan_email_targets -v`*

    ---
    ```

    For 33-VALIDATION.md: append a similar section with these rows:

    | Entry Point | Scenario | Test Name | Status |
    |-------------|----------|-----------|--------|
    | scan_kafka_targets | happy | test_scan_kafka_targets_happy | ✅ |
    | scan_kafka_targets | refused | test_scan_kafka_targets_refused | ✅ |
    | scan_kafka_targets | plaintext-only | test_scan_kafka_targets_plaintext_only | ✅ |
    | scan_rabbitmq_targets | happy | test_scan_rabbitmq_targets_happy | ✅ |
    | scan_rabbitmq_targets | refused | test_scan_rabbitmq_targets_refused | ✅ |
    | scan_rabbitmq_targets | plaintext-only | test_scan_rabbitmq_targets_plaintext_only | ✅ |
    | scan_redis_targets | happy | test_scan_redis_targets_happy | ✅ |
    | scan_redis_targets | refused | test_scan_redis_targets_refused | ✅ |
    | scan_redis_targets | plaintext-only | test_scan_redis_targets_plaintext_only | ✅ |
    | Azure Service Bus probe | happy | test_azure_servicebus_probe_happy | ✅ |
    | Azure Service Bus probe | refused | test_azure_servicebus_probe_refused | ✅ |
    | Azure Service Bus probe | plaintext-only (degenerate) | test_azure_servicebus_probe_plaintext_only | ✅ |
    | AWS SQS probe | happy | test_aws_sqs_probe_happy | ✅ |
    | AWS SQS probe | refused | test_aws_sqs_probe_refused | ✅ |
    | AWS SQS probe | plaintext-only (degenerate) | test_aws_sqs_probe_plaintext_only | ✅ |

    *Verification: `pytest tests/test_infra03_nyquist_coverage.py -k 'kafka or rabbitmq or redis or servicebus or sqs' -v`*

    Insert this block above the "Validation Sign-Off" header in 33-VALIDATION.md.

    Also bump the frontmatter `updated:` date in both files to today's date (2026-04-28 or
    later — use the actual current date when executing).

    DO NOT modify any other content. DO NOT change `nyquist_compliant` or `wave_0_complete`
    flags (they were already true). DO NOT renumber sections.
  </action>
  <verify>
    <automated>grep -c "Nyquist Scenarios — INFRA-03" .planning/phases/32-email-scanner/32-VALIDATION.md .planning/phases/33-broker-scanner/33-VALIDATION.md && grep -c "test_scan_email_targets_happy" .planning/phases/32-email-scanner/32-VALIDATION.md && grep -c "test_aws_sqs_probe_plaintext_only" .planning/phases/33-broker-scanner/33-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Nyquist Scenarios — INFRA-03" .planning/phases/32-email-scanner/32-VALIDATION.md` returns 1
    - `grep -c "Nyquist Scenarios — INFRA-03" .planning/phases/33-broker-scanner/33-VALIDATION.md` returns 1
    - 32-VALIDATION.md contains the 3 email test names exactly
    - 33-VALIDATION.md contains all 15 broker test names exactly (5 entry points × 3 scenarios)
    - Both files retain `nyquist_compliant: true` and `wave_0_complete: true` in frontmatter
    - The "Validation Sign-Off" section still exists in both files (matrix was inserted ABOVE, not OVER)
  </acceptance_criteria>
  <done>Phases 32 and 33 carry inline Nyquist scenario matrices per D-04.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run plan-checker for phases 34 and 35; create authoritative VALIDATION.md</name>
  <files>.planning/phases/34-motion-intelligence/34-VALIDATION.md, .planning/phases/35-cbom-integration/35-VALIDATION.md</files>
  <read_first>
    - .planning/phases/34-motion-intelligence/34-VALIDATION.md (current — nyquist_compliant: false)
    - .planning/phases/34-motion-intelligence/ (all PLAN.md and SUMMARY.md files for what 34 actually built)
    - .planning/phases/35-cbom-integration/ (entire directory — confirm no VALIDATION.md exists today)
    - .planning/phases/35-cbom-integration/ (all PLAN.md and SUMMARY.md files)
    - .planning/phases/37-gap-closure-and-v4-4-0-release/37-CONTEXT.md (D-05 verbatim — no rubber-stamping; if checker fails, fix the underlying gap)
  </read_first>
  <action>
    For each of phases 34 and 35:

    1. Invoke gsd-plan-checker against the phase. Confirm the actual current invocation
       form first by running:
       ```bash
       gsd-sdk query --help 2>&1 | grep -i 'plan.checker\|check.plan'
       ```
       Use whichever subcommand the SDK exposes. If none exists, fall back to invoking the
       Claude slash command `/gsd-check-plan 34` (and `/gsd-check-plan 35`) per the
       get-shit-done workflow conventions in `~/.claude/get-shit-done/`.

    2. The plan-checker writes / overwrites the phase's VALIDATION.md. Per D-05, that output
       is authoritative — DO NOT hand-edit the result.

    3. If the plan-checker reports a real gap (failing test, missing artifact, contract
       drift), STOP. Surface the gap to the user as a checkpoint before proceeding. Do NOT
       flip `nyquist_compliant: true` to mask a real failure (D-05 explicit prohibition).

    4. For phase 35 specifically: confirm the file did not exist before this task and exists
       after. If the plan-checker refuses to create one because no PLAN.md exists for phase 35,
       surface the gap — phase 35 SUMMARYs imply CBOM-01..04 shipped but the VALIDATION trail
       is missing.

    5. After successful checker runs:
       - 34-VALIDATION.md should now read `nyquist_compliant: true` (or surface a documented gap)
       - 35-VALIDATION.md exists with frontmatter `phase: 35`, status, nyquist_compliant, wave_0_complete
  </action>
  <verify>
    <automated>test -f .planning/phases/34-motion-intelligence/34-VALIDATION.md && test -f .planning/phases/35-cbom-integration/35-VALIDATION.md && grep -E '^(nyquist_compliant|wave_0_complete):' .planning/phases/34-motion-intelligence/34-VALIDATION.md .planning/phases/35-cbom-integration/35-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/35-cbom-integration/35-VALIDATION.md` exists (was missing per CONTEXT.md)
    - `grep '^nyquist_compliant: true' .planning/phases/34-motion-intelligence/34-VALIDATION.md` returns one match (or a documented gap is surfaced as a checkpoint and the user has approved deferral)
    - `grep '^nyquist_compliant: true' .planning/phases/35-cbom-integration/35-VALIDATION.md` returns one match (or documented gap)
    - Both files have `phase:`, `status:`, `nyquist_compliant:`, `wave_0_complete:` keys present in frontmatter
    - No flag was hand-flipped from false to true; if a flag changed, the plan-checker run produced the change
  </acceptance_criteria>
  <done>Phases 34 and 35 carry honest plan-checker-generated VALIDATION.md files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify phase 36 gating artifacts before flipping wave_0_complete</name>
  <what-built>
    Phase 36 VALIDATION.md currently reads `wave_0_complete: false`. Per D-06, this flag is
    gated on:
    1. `36-VERIFICATION.md` is status: approved
    2. `36-UAT.md` is signed off
    3. `pytest -x -q` is green on current main
  </what-built>
  <how-to-verify>
    Before checking the box, run these commands and confirm each returns the expected result:

    1. `grep -E '^(status|approval):' .planning/phases/36-dashboard-motion-tab/36-VERIFICATION.md`
       → expected: `status: approved` (or equivalent approval marker)

    2. `grep -iE 'sign.?off|approved|complete' .planning/phases/36-dashboard-motion-tab/36-UAT.md | head -5`
       → expected: at least one sign-off / approval marker

    3. `pytest -x -q 2>&1 | tail -3`
       → expected: 0 failures, 0 errors (504+ tests pass per success criterion #5)

    If any of the three checks fails, STOP. Do NOT flip wave_0_complete. Surface the failing
    artifact to the user as a separate gap-closure ticket. (Per D-06: "Do not re-run the
    manual UAT cases" — if 36-UAT.md is unsigned, that's a blocker for this phase, not a
    re-test trigger here.)
  </how-to-verify>
  <resume-signal>
    Type "approved" if all three gating checks pass, or describe which artifact failed.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Flip phase 36 wave_0_complete after gating verified</name>
  <files>.planning/phases/36-dashboard-motion-tab/36-VALIDATION.md</files>
  <read_first>
    - .planning/phases/36-dashboard-motion-tab/36-VALIDATION.md (current frontmatter — wave_0_complete: false)
    - Output of Task 3 (gating checks)
  </read_first>
  <action>
    In `.planning/phases/36-dashboard-motion-tab/36-VALIDATION.md` frontmatter only:
    Replace the line `wave_0_complete: false` with `wave_0_complete: true`.

    Update the `updated:` field to today's date.

    Append a single line comment immediately after the frontmatter `---` close:
    `<!-- wave_0_complete flipped 2026-04-28 by Phase 37 (D-06): VERIFICATION.md approved + UAT.md signed off + pytest -x -q green on main. -->`

    Do NOT modify any other content in the file.
  </action>
  <verify>
    <automated>grep '^wave_0_complete: true' .planning/phases/36-dashboard-motion-tab/36-VALIDATION.md && grep '^wave_0_complete: false' .planning/phases/36-dashboard-motion-tab/36-VALIDATION.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep '^wave_0_complete: true' .planning/phases/36-dashboard-motion-tab/36-VALIDATION.md` returns one match
    - `grep '^wave_0_complete: false' .planning/phases/36-dashboard-motion-tab/36-VALIDATION.md` returns zero matches
    - The audit-trail HTML comment is present immediately after the frontmatter close
    - No other section of the file has been modified (diff is two lines + one comment)
  </acceptance_criteria>
  <done>Phase 36 VALIDATION.md reads wave_0_complete: true with an audit-trail comment.</done>
</task>

<task type="auto">
  <name>Task 5: Create phase 37's own VALIDATION.md</name>
  <files>.planning/phases/37-gap-closure-and-v4-4-0-release/37-VALIDATION.md</files>
  <read_first>
    - .planning/phases/22-v42-gap-closure/22-VALIDATION.md (if it exists — analog gap-closure VALIDATION.md as template)
    - .planning/phases/32-email-scanner/32-VALIDATION.md (template structure with the new Nyquist matrix)
    - All 6 plans created in this phase (37-01..37-06) — frontmatter for must_haves and verify commands
  </read_first>
  <action>
    Create `.planning/phases/37-gap-closure-and-v4-4-0-release/37-VALIDATION.md` with:

    Frontmatter:
    ```yaml
    ---
    phase: 37
    slug: gap-closure-and-v4-4-0-release
    status: approved
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-04-28
    updated: 2026-04-28
    ---
    ```

    Sections (mirror the structure of 32-VALIDATION.md):

    1. **Test Infrastructure** table (Framework: pytest 8.x; Quick run: `pytest tests/test_version.py tests/test_infra03_nyquist_coverage.py -x -q`; Full suite: `pytest -x -q`; Estimated runtime: ~15s quick / ~2 min full)

    2. **Sampling Rate** (after every task commit: quick run; after every plan wave: full suite)

    3. **Per-Task Verification Map** — one row per task across all six plans 37-01..37-06,
       referencing the `<verify>` commands from each plan's task. Columns:
       Task ID | Plan | Wave | Requirements | Test Type | Automated Command | Status

    4. **Wave 0 Requirements** — list any pre-existing files this phase depends on:
       - tests/test_version.py (created by Plan 37-01)
       - tests/test_infra03_nyquist_coverage.py (created by Plan 37-03)

    5. **Manual-Only Verifications** — single row: Task 3 of Plan 37-04 (gating verification
       for phase 36 wave_0_complete flip; reason: artifact inspection of files outside the
       test harness)

    6. **Validation Sign-Off** — checklist matching the structure in 32-VALIDATION.md
  </action>
  <verify>
    <automated>test -f .planning/phases/37-gap-closure-and-v4-4-0-release/37-VALIDATION.md && grep -E '^(phase|nyquist_compliant|wave_0_complete):' .planning/phases/37-gap-closure-and-v4-4-0-release/37-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/37-gap-closure-and-v4-4-0-release/37-VALIDATION.md` exists
    - `grep '^phase: 37' .planning/phases/37-gap-closure-and-v4-4-0-release/37-VALIDATION.md` returns one match
    - `grep '^nyquist_compliant: true' .planning/phases/37-gap-closure-and-v4-4-0-release/37-VALIDATION.md` returns one match
    - `grep '^wave_0_complete: true' .planning/phases/37-gap-closure-and-v4-4-0-release/37-VALIDATION.md` returns one match
    - File contains all six required section headers ("Test Infrastructure", "Sampling Rate", "Per-Task Verification Map", "Wave 0 Requirements", "Manual-Only Verifications", "Validation Sign-Off")
    - Per-Task Verification Map has at least one row per plan (6+ rows)
  </acceptance_criteria>
  <done>Phase 37 has its own approved VALIDATION.md.</done>
</task>

</tasks>

<verification>
- All 6 v4.4 phases (32, 33, 34, 35, 36, 37) have VALIDATION.md files
- Each reads `nyquist_compliant: true` and `wave_0_complete: true` (or has a documented gap surfaced through D-05 honesty rule)
- Phases 32 and 33 contain the inline Nyquist scenarios matrix citing test_infra03_nyquist_coverage.py
- Phase 36's wave_0_complete flip carries an audit-trail comment
</verification>

<success_criteria>
Phase goal success criterion #4 satisfied: all 6 v4.4 phase VALIDATION.md files read
nyquist_compliant: true and wave_0_complete: true with an honest, plan-checker-generated or
gating-verified audit trail.
</success_criteria>

<output>
After completion, create `.planning/phases/37-gap-closure-and-v4-4-0-release/37-04-SUMMARY.md`
including the plan-checker invocation outputs from Task 2 and the gating-check outputs from Task 3.
</output>
