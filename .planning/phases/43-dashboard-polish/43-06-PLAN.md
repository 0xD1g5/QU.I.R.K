---
phase: 43-dashboard-polish
plan: 06
type: execute
wave: 5
depends_on: []
files_modified:
  - quirk/dashboard/api/routes/pdf.py
  - src/dashboard/src/pages/print.tsx
autonomous: true
gap_closure: true
requirements: [DASH-06]

must_haves:
  truths:
    - "PDF export button returns a non-empty PDF file containing rendered scan data, not a blank/loading-state page"
  artifacts:
    - path: quirk/dashboard/api/routes/pdf.py
      provides: "wait_for_selector on data-ready sentinel before calling page.pdf()"
      contains: "data-ready"
    - path: src/dashboard/src/pages/print.tsx
      provides: "useEffect sets document.body.setAttribute('data-ready', 'true') after data is non-null"
      contains: "data-ready"
  key_links:
    - from: "print.tsx useEffect"
      to: "document.body dataset"
      via: "setAttribute after data non-null"
      pattern: "data-ready"
    - from: "pdf.py wait_for_selector"
      to: "body[data-ready=\"true\"]"
      via: "Playwright selector wait"
      pattern: "wait_for_selector"
---

<objective>
Close the UAT Gap 2 blank-PDF defect. Playwright's `networkidle` fires after the JS bundle loads but before React hydrates and `useScanData` fetches `/api/scan/latest`. Adding a `data-ready` sentinel on `<body>` (set by print.tsx once data is non-null) and waiting for that sentinel in pdf.py ensures Playwright captures the fully-rendered state.

Purpose: Make the Executive Summary PDF export button produce a real, data-populated PDF.
Output: print.tsx sets sentinel after data loads; pdf.py waits for sentinel before calling page.pdf().
</objective>

<execution_context>
@/Users/digs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/digs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-dashboard-polish/43-UAT.md

<interfaces>
<!-- From quirk/dashboard/api/routes/pdf.py lines 48-61 -->
<!--
    with sync_playwright() as p:
        browser = p.chromium.launch(headless=True)
        context = browser.new_context()
        page = context.new_page()

        page.goto(print_url, wait_until="networkidle", timeout=30_000)
        page.wait_for_load_state("networkidle")   ← redundant second wait

        pdf_bytes = page.pdf(...)

Fix: after the existing goto/networkidle, add:
    page.wait_for_selector('body[data-ready="true"]', timeout=15_000)
Then remove the redundant wait_for_load_state("networkidle") call.
-->

<!-- From src/dashboard/src/pages/print.tsx lines 148-165 -->
<!--
export function PrintPage() {
  const { data, loading, error } = useScanData()

  if (loading) { return <div>Loading scan data...</div> }
  if (error)   { return <div style={{color:"red"}}>{error}</div> }
  if (!data) return null

  const { meta, score, ... } = data
  return ( ... )     ← full render, data guaranteed non-null here

Fix: add a useEffect that sets the sentinel once data is available:
  import { useEffect } from "react"   (already imported via createElement; check import line 1)
  ...
  useEffect(() => {
    if (data) {
      document.body.setAttribute('data-ready', 'true')
    }
    return () => {
      document.body.removeAttribute('data-ready')
    }
  }, [data])

Place this useEffect BEFORE the early-return guards so it runs even if data arrives
after the component mounts. Actually: place it AFTER the guard checks but before the
destructure — wait, useEffect must not be conditional. Place it unconditionally at
the top of PrintPage(), checking `data` inside the effect body (as shown above).
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data-ready sentinel to print.tsx</name>
  <files>src/dashboard/src/pages/print.tsx</files>
  <action>
In `PrintPage()` (line 148), add a `useEffect` that sets a `data-ready` attribute on `<body>` once scan data is available. This is the DOM signal pdf.py will wait for.

1. Confirm `useEffect` is imported. Line 1 imports `createElement` from `"react"`. Add `useEffect` to that import:
   ```ts
   import { createElement, useEffect } from "react"
   ```

2. Inside `PrintPage()`, add the effect BEFORE any early-return guard (hooks must not be conditional):
   ```ts
   useEffect(() => {
     if (data) {
       document.body.setAttribute('data-ready', 'true')
     }
     return () => {
       document.body.removeAttribute('data-ready')
     }
   }, [data])
   ```

   Place this block immediately after the `const { data, loading, error } = useScanData()` line and before `if (loading)`.

3. No other changes to print.tsx. Do not modify any render logic, CSS, or sub-components.

The sentinel is set only when `data` is truthy (scan data successfully fetched), so Playwright's wait_for_selector will block until actual content is rendered.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK/src/dashboard && grep -n "data-ready" src/pages/print.tsx</automated>
  </verify>
  <done>
`grep` returns lines showing both `setAttribute('data-ready', 'true')` and `removeAttribute('data-ready')`. TypeScript build (`npm run build`) exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wait for data-ready sentinel in pdf.py before capturing PDF</name>
  <files>quirk/dashboard/api/routes/pdf.py</files>
  <action>
After `page.goto(print_url, wait_until="networkidle", timeout=30_000)`, add a `wait_for_selector` call that blocks until print.tsx has set the sentinel:

1. Remove the redundant `page.wait_for_load_state("networkidle")` line (line 55) — it is superseded by the new sentinel wait.

2. Add immediately after the `page.goto(...)` line:
   ```python
   page.wait_for_selector('body[data-ready="true"]', timeout=15_000)
   ```

The resulting block should read:
```python
page.goto(print_url, wait_until="networkidle", timeout=30_000)
page.wait_for_selector('body[data-ready="true"]', timeout=15_000)

pdf_bytes = page.pdf(
    format="A4",
    print_background=True,
    margin={"top": "16mm", "bottom": "16mm", "left": "12mm", "right": "12mm"},
)
```

No other changes to pdf.py. Do not touch error handling, port resolution, or response construction.

The `timeout=15_000` (15 seconds) covers realistic API response time on localhost. If the dashboard API is not running or returns no scan data, the selector will time out and Playwright will raise, which the existing `except Exception` handler at the bottom of the route will catch and return as a 500.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && grep -n "data-ready\|wait_for_selector" quirk/dashboard/api/routes/pdf.py</automated>
  </verify>
  <done>
`grep` returns the `wait_for_selector('body[data-ready="true"]', ...)` line. The `wait_for_load_state("networkidle")` line is absent. `python -m compileall quirk/dashboard/api/routes/pdf.py` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pdf.py → Playwright browser | Playwright navigates internal localhost URL only; no external input in the URL |
| print.tsx → document.body | Sentinel attribute written to DOM; contains only the static string "true" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-06-01 | Information Disclosure | pdf.py generates PDF from internal /print route | accept | URL is always 127.0.0.1:{QUIRK_SERVE_PORT}; not user-controlled; existing behaviour unchanged |
| T-43-06-02 | Denial of Service | wait_for_selector timeout | mitigate | timeout=15_000 ms caps the wait; Playwright exception propagates to existing except handler returning HTTP 500 |
| T-43-06-03 | Tampering | data-ready attribute on body | accept | Static string literal "true"; no user content; cleanup on unmount prevents stale sentinel |
</threat_model>

<verification>
1. `grep -n "data-ready" src/dashboard/src/pages/print.tsx` — shows setAttribute and removeAttribute lines
2. `grep -n "wait_for_selector\|data-ready" quirk/dashboard/api/routes/pdf.py` — shows sentinel wait
3. `grep "wait_for_load_state" quirk/dashboard/api/routes/pdf.py` — returns nothing (line removed)
4. `cd src/dashboard && npm run build` exits 0
5. `python -m compileall quirk/dashboard/api/routes/pdf.py` exits 0
</verification>

<success_criteria>
- pdf.py no longer calls page.pdf() immediately after networkidle; it waits for `body[data-ready="true"]`
- print.tsx sets the sentinel via useEffect once scan data is non-null
- PDF export produces a populated document, not a blank/loading-state page
- TypeScript and Python syntax checks both pass
</success_criteria>

<output>
After completion, create `.planning/phases/43-dashboard-polish/43-06-SUMMARY.md`
</output>
