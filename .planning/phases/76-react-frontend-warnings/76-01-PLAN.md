---
phase: 76-react-frontend-warnings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dashboard/src/pages/executive.tsx
  - src/dashboard/src/pages/print.tsx
  - src/dashboard/src/pages/qramm-profile.tsx
  - src/dashboard/src/hooks/__tests__/useScanList.test.ts
  - src/dashboard/src/pages/__tests__/executive-error-coercion.test.tsx
  - src/dashboard/src/pages/__tests__/print-pdf-cleanup.test.tsx
  - src/dashboard/src/pages/__tests__/qramm-profile-submit-error.test.tsx
  - .planning/audit-2026-05-08/AUDIT-TASKS.md
autonomous: true
requirements: [REACT-01]
must_haves:
  truths:
    - "`useScanList.ts` (already shipped) exposes `{sessions, loading, error}` with 401/403/429/non-OK branches AND its primary consumer `pages/scan-history.tsx` (line 54) renders a destructive-text error card on `error` per D-01 (WR-02) — verified by Wave 0 audit; no code edit, audit-flip-only with evidence"
    - "`pages/executive.tsx` PDF error path coerces `body.detail` defensively: `const detail = (body && typeof body === 'object' && typeof body.detail === 'string') ? body.detail : String(body ?? 'Unknown error')` — never throws on raw-string error bodies per D-02 (WR-06)"
    - "`pages/print.tsx` second useEffect (lines 346-350) only sets `data-ready` when `data && !loading && !qrammLoading && !qrammError`; on QRAMM error a visible alert ('QRAMM data unavailable — Q section omitted') renders and the PDF sentinel stays unset per D-03 (WR-07); the BR-05 cleanup effect at lines 332-335 is NOT touched"
    - "`pages/qramm-profile.tsx::handleSubmit` catch block extracts API error message via the D-02 coercion pattern and writes it to `submitError` — operator sees the real reason (e.g., 'Organization Name required') instead of the generic 'Could not start assessment' string per D-04 (WR-08)"
    - "`cd src/dashboard && npm run build` exits 0 after edits"
    - "Audit rows react-frontend/WR-02, WR-06, WR-07, WR-08 flipped to `Phase 76 | [x] closed`"
  artifacts:
    - path: "src/dashboard/src/pages/executive.tsx"
      provides: "Defensive body.detail coercion (D-02) for PDF error rendering"
      contains: "typeof body.detail === 'string'"
    - path: "src/dashboard/src/pages/print.tsx"
      provides: "QRAMM-error-aware data-ready sentinel (D-03)"
      contains: "qrammError"
    - path: "src/dashboard/src/pages/qramm-profile.tsx"
      provides: "API-message-bearing submitError (D-04)"
      contains: "typeof body.detail === 'string'"
    - path: "src/dashboard/src/hooks/__tests__/useScanList.test.ts"
      provides: "RED-then-GREEN evidence that hook surfaces error AND consumer renders banner (D-01 audit-flip evidence for WR-02)"
      contains: "def test_ OR it('"
    - path: "src/dashboard/src/pages/__tests__/executive-error-coercion.test.tsx"
      provides: "RED-then-GREEN coverage of D-02 coercion"
      contains: "Unknown error"
    - path: "src/dashboard/src/pages/__tests__/print-pdf-cleanup.test.tsx"
      provides: "RED-then-GREEN coverage of D-03 sentinel guard"
      contains: "data-ready"
    - path: "src/dashboard/src/pages/__tests__/qramm-profile-submit-error.test.tsx"
      provides: "RED-then-GREEN coverage of D-04 submitError API-message surfacing"
      contains: "submitError"
  key_links:
    - from: "pages/executive.tsx PDF error path"
      to: "defensive body.detail coercion"
      via: "typeof body === 'object' && typeof body.detail === 'string' guard"
      pattern: "typeof body\\.detail === 'string'"
    - from: "pages/print.tsx second useEffect"
      to: "qrammError-gated data-ready"
      via: "if (data && !loading && !qrammLoading && !qrammError) document.body.setAttribute('data-ready', 'true')"
      pattern: "!qrammError"
    - from: "pages/qramm-profile.tsx handleSubmit catch"
      to: "submitError state"
      via: "setSubmitError((body && typeof body === 'object' && typeof body.detail === 'string') ? body.detail : ...)"
      pattern: "setSubmitError"
    - from: "hooks/useScanList.ts"
      to: "pages/scan-history.tsx error card"
      via: "const { sessions, loading, error } = useScanList(); if (error) return <Card>...</Card>"
      pattern: "if \\(error\\)"
---

<objective>
Close REACT-01 (WR-02, WR-06, WR-07, WR-08) — API error surfacing across executive, print, qramm-profile pages, plus the audit-flip-only path for WR-02 (which is already correctly wired per RESEARCH C-1).

1. **WR-02 (D-01)** — RESEARCH C-1 confirms `useScanList.ts` already exposes `{sessions, loading, error}` with full 401/403/429/non-OK branches AND `pages/scan-history.tsx:54` already renders an error card. This is an audit-flip-only row: Wave 0 audits the hook + consumer, the test module proves the contract, no production-code change is required.

2. **WR-06 (D-02)** — `pages/executive.tsx:118` reads `body.detail` after `.catch(() => ({}))` — `.detail` resolves to `undefined` on raw-string error bodies and could throw if `body` is unexpectedly stringified by a future API change. Replace with defensive coercion: `const detail = (body && typeof body === 'object' && typeof body.detail === 'string') ? body.detail : String(body ?? 'Unknown error')`.

3. **WR-07 (D-03)** — `pages/print.tsx:346-350` second useEffect currently sets `data-ready` whenever `data && !loading && !qrammLoading`. Tighten to `data && !loading && !qrammLoading && !qrammError`; render a visible alert "QRAMM data unavailable — Q section omitted" when `qrammError` is truthy. RESEARCH Pitfall 3: the BR-05 cleanup effect at lines 332-335 is already correct from Phase 62 and MUST NOT be touched.

4. **WR-08 (D-04)** — `pages/qramm-profile.tsx:186-188` `submitError` currently hardcodes a generic string. Replace with the D-02 coercion applied to the API response body so the user sees the real reason (e.g., "Organization Name required").

Then `cd src/dashboard && npm run build` (exit 0 required) and flip audit rows WR-02, WR-06, WR-07, WR-08 to `Phase 76 | [x] closed`.

Purpose: Operators see actionable API error messages on every dashboard surface (scan list, executive PDF, print sentinel, QRAMM submit) — no silent swallows, no `undefined` reads on raw-string bodies, no PDF capture of an incomplete page.
Output: 3 source edits, 4 new test modules, 4 audit rows flipped, 1 successful `npm run build`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/76-react-frontend-warnings/76-CONTEXT.md
@.planning/phases/76-react-frontend-warnings/76-RESEARCH.md
@.planning/audit-2026-05-08/AUDIT-TASKS.md

<interfaces>
Existing surfaces (verified at HEAD cf2417a):

src/dashboard/src/hooks/useScanList.ts (lines 1-55):
- Already exports `useScanList(): { sessions, loading, error }` with 401/403/429/non-OK branches at lines 21-39. NO code edit needed.

src/dashboard/src/pages/scan-history.tsx (lines 23, 54-62):
- Already destructures `{ sessions, loading, error }` and renders a destructive-text Card on `error`. NO code edit needed.

src/dashboard/src/pages/executive.tsx (lines 99-126):
- WR-05 site at line 115 (handled in 76-02-PLAN, NOT here)
- WR-06 site at line 118 reads `body.detail` after `.catch(() => ({}))`

src/dashboard/src/pages/print.tsx (lines 332-335, 346-350):
- BR-05 cleanup effect at 332-335 — DO NOT TOUCH (Phase 62, RESEARCH Pitfall 3)
- WR-07 site: second useEffect at 346-350 with guard `data && !loading && !qrammLoading`
- Hook surface: `pages/print.tsx` consumes `useQRAMMPrintData()` which already exports `error` (alias `qrammError` in this file)

src/dashboard/src/pages/qramm-profile.tsx (lines 186-188):
- WR-08 site: `submitError` setter in handleSubmit catch hardcodes generic string

Target coercion (D-02, reused by D-04):
```
const detail = (body && typeof body === 'object' && typeof body.detail === 'string')
    ? body.detail
    : String(body ?? 'Unknown error')
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing tests for D-01..D-04 (4 new test modules)</name>
  <files>src/dashboard/src/hooks/__tests__/useScanList.test.ts, src/dashboard/src/pages/__tests__/executive-error-coercion.test.tsx, src/dashboard/src/pages/__tests__/print-pdf-cleanup.test.tsx, src/dashboard/src/pages/__tests__/qramm-profile-submit-error.test.tsx</files>
  <read_first>
    - src/dashboard/src/hooks/useScanList.ts (full file — confirm `error` is already exported)
    - src/dashboard/src/pages/scan-history.tsx (lines 1-80 — confirm consumer renders error)
    - src/dashboard/src/pages/executive.tsx (lines 90-130 — WR-06 site)
    - src/dashboard/src/pages/print.tsx (lines 320-360 — BR-05 cleanup AND WR-07 site)
    - src/dashboard/src/pages/qramm-profile.tsx (lines 170-200 — WR-08 site)
    - src/dashboard/src/hooks/__tests__/useScanData.test.tsx (Vitest idiom reference)
    - .planning/phases/76-react-frontend-warnings/76-CONTEXT.md (D-01..D-04)
    - .planning/phases/76-react-frontend-warnings/76-RESEARCH.md (C-1, Pitfall 3)
  </read_first>
  <behavior>
    - D-01 (WR-02) — `useScanList()` hook returns object with `error: string | null` key; given a mocked `fetch` that returns `{ ok: false, status: 500, statusText: 'Internal Server Error' }`, hook resolves `error` to a non-empty string AND `sessions` to `[]`
    - D-01 — Render `<ScanHistoryPage />` against a hook stub returning `{ sessions: [], loading: false, error: 'boom' }`; assert the DOM contains the destructive-text error card (RESEARCH C-1 evidence path)
    - D-02 (WR-06) — Given a JSON error body `{detail: 'rate limited'}`, executive PDF error path renders `'rate limited'`
    - D-02 — Given a raw-string body `'oops'` (or `null`), executive PDF error path renders `'oops'` (or `'Unknown error'`) without throwing `Cannot read properties of undefined`
    - D-02 — Given a body where `body.detail` is a number (`{detail: 500}`), the coercion falls back to `String(body)` rather than rendering the number as detail
    - D-03 (WR-07) — Render `<PrintPage />` with `qrammError: 'fetch failed'`; assert `document.body.getAttribute('data-ready')` is null AND the visible "QRAMM data unavailable" alert is present
    - D-03 — Render `<PrintPage />` with `qrammError: null, qrammLoading: false, loading: false, data: <fixture>`; assert `data-ready === 'true'` (happy path still works)
    - D-03 — Confirm the BR-05 cleanup effect still removes `data-ready` on unmount (regression guard for Phase 62)
    - D-04 (WR-08) — Mock fetch to reject with body `{detail: 'Organization Name required'}`; `<QrammProfilePage />` `submitError` renders `'Organization Name required'`, NOT the generic 'Could not start assessment' string
    - D-04 — Mock fetch to reject with raw-string body `'server exploded'`; `submitError` renders `'server exploded'` (D-02 coercion reused)
  </behavior>
  <action>
    Create four Vitest modules. Use `@testing-library/react` for `<PrintPage />`, `<QrammProfilePage />`, and the consumer-banner test; use `renderHook` from `@testing-library/react` for `useScanList`. Mock global `fetch` via `vi.stubGlobal('fetch', vi.fn())`.

    **`src/dashboard/src/hooks/__tests__/useScanList.test.ts`** — two-part test module:
    - Part A (hook): mock fetch to return `{ ok: false, status: 500, statusText: 'Internal Server Error', json: async () => ({}) }`; `renderHook(() => useScanList())`; `await waitFor` until `result.current.loading === false`; assert `result.current.error` is non-empty string and `result.current.sessions.length === 0`.
    - Part B (consumer audit-flip evidence): import `<ScanHistoryPage />`. Stub `useScanList` via `vi.mock('@/hooks/useScanList', () => ({ useScanList: () => ({ sessions: [], loading: false, error: 'boom' }) }))`. `render(<ScanHistoryPage />)` (wrap in `<MemoryRouter>` if needed). Assert the destructive-text error card is in the DOM (`screen.getByText(/Could not load scan history/i)`).
    - Total >= 4 `it()` blocks.

    **`src/dashboard/src/pages/__tests__/executive-error-coercion.test.tsx`** — parametrized table over body inputs: `({detail: 'rate limited'}, 'rate limited')`, `('oops', 'oops')`, `(null, 'Unknown error')`, `(undefined, 'Unknown error')`, `({detail: 500}, '[object Object]' OR fallback)`. Either (a) extract the coercion to a tiny helper inside `executive.tsx` exported for tests OR (b) render `<ExecutivePage />` with a mocked fetch returning the body and assert DOM text. Prefer (a) for unit purity — create test that imports a future-exported `coerceErrorDetail(body)` from `executive.tsx`. Total >= 4 `it()` blocks.

    **`src/dashboard/src/pages/__tests__/print-pdf-cleanup.test.tsx`** — three `it()` blocks:
    - `it('does not set data-ready when qrammError is present')` — stub `useQRAMMPrintData` to return `{ data: null, loading: false, error: 'fetch failed' }` (or the field names the real hook uses — read_first verifies); render `<PrintPage />`; assert `document.body.getAttribute('data-ready')` is null; assert the visible "QRAMM data unavailable" alert is in the DOM.
    - `it('sets data-ready when QRAMM loaded successfully')` — stub hooks with `error: null` and a data fixture; assert `data-ready === 'true'`.
    - `it('BR-05: cleans up data-ready on unmount')` — set the attribute pre-render, unmount, assert attribute removed (Phase 62 regression guard).

    **`src/dashboard/src/pages/__tests__/qramm-profile-submit-error.test.tsx`** — two `it()` blocks parametrized over JSON body and raw-string body; fire the Submit form button via `userEvent.click`; await `screen.findByText(/Organization Name required|server exploded/)`. Mock the POST fetch to reject with the parametrized body.

    All four RED tests MUST fail because:
    - Part A test passes (hook already correct) — keep it as a regression guard. Part B test passes IFF the consumer renders the error card (RESEARCH C-1 says yes); if it fails, the consumer needs the banner too. EITHER outcome is acceptable for Task 1 — log the result.
    - `executive-error-coercion.test.tsx` — fails because `coerceErrorDetail` does not yet exist as an exported helper (D-02 not yet landed).
    - `print-pdf-cleanup.test.tsx` — fails because the second useEffect at lines 346-350 does not yet check `qrammError`.
    - `qramm-profile-submit-error.test.tsx` — fails because `submitError` is still the hardcoded generic string.

    Run `cd src/dashboard && npm test -- useScanList executive-error-coercion print-pdf-cleanup qramm-profile-submit-error 2>&1 | tail -40` and confirm failure on the latter three (with logged status for useScanList).

    Commit message: `test(76-01): add failing tests for REACT-01 (D-01 evidence, D-02 coercion, D-03 sentinel, D-04 submitError)`.
  </action>
  <verify>
    <automated>cd src/dashboard && npm test -- useScanList executive-error-coercion print-pdf-cleanup qramm-profile-submit-error 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - All 4 test files exist
    - `grep -cE "it\\(" src/dashboard/src/hooks/__tests__/useScanList.test.ts` >= 4
    - `grep -cE "it\\(" src/dashboard/src/pages/__tests__/executive-error-coercion.test.tsx` >= 4
    - `grep -cE "it\\(" src/dashboard/src/pages/__tests__/print-pdf-cleanup.test.tsx` >= 3
    - `grep -cE "it\\(" src/dashboard/src/pages/__tests__/qramm-profile-submit-error.test.tsx` >= 2
    - `grep -cE "Unknown error" src/dashboard/src/pages/__tests__/executive-error-coercion.test.tsx` >= 1
    - `grep -cE "qrammError" src/dashboard/src/pages/__tests__/print-pdf-cleanup.test.tsx` >= 1
    - `grep -cE "Organization Name required" src/dashboard/src/pages/__tests__/qramm-profile-submit-error.test.tsx` >= 1
    - `cd src/dashboard && npm test -- executive-error-coercion print-pdf-cleanup qramm-profile-submit-error` exits NON-ZERO (RED state)
  </acceptance_criteria>
  <done>
    Four failing test modules committed; pytest-equivalent (vitest) exits non-zero with assertion errors tied to D-02..D-04. D-01 test status logged in commit body.
  </done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement D-02, D-03, D-04 in executive/print/qramm-profile</name>
  <files>src/dashboard/src/pages/executive.tsx, src/dashboard/src/pages/print.tsx, src/dashboard/src/pages/qramm-profile.tsx</files>
  <read_first>
    - src/dashboard/src/pages/executive.tsx (lines 90-130 — WR-06 site at 118)
    - src/dashboard/src/pages/print.tsx (lines 320-360 — BR-05 cleanup at 332-335 MUST NOT change; WR-07 site at 346-350)
    - src/dashboard/src/pages/qramm-profile.tsx (lines 170-200 — WR-08 site at 186-188)
    - src/dashboard/src/hooks/useQRAMMPrintData.ts (confirm field name — `error` vs `qrammError`)
    - src/dashboard/src/pages/__tests__/executive-error-coercion.test.tsx (Task 1 — confirm expected `coerceErrorDetail` export shape)
    - src/dashboard/src/pages/__tests__/print-pdf-cleanup.test.tsx
    - src/dashboard/src/pages/__tests__/qramm-profile-submit-error.test.tsx
  </read_first>
  <action>
    Apply three surgical edits per `<interfaces>` target shapes.

    **executive.tsx (D-02, WR-06):**
    1. Add a module-private helper near the top of the file:
       ```
       export function coerceErrorDetail(body: unknown): string {
           if (body && typeof body === 'object' && typeof (body as {detail?: unknown}).detail === 'string') {
               return (body as {detail: string}).detail
           }
           return String(body ?? 'Unknown error')
       }
       ```
       (Exported so Task 1 tests can import it.)
    2. At line 118 (or wherever the error-rendering site lives), replace the existing `body.detail` reference with `coerceErrorDetail(body)`. Do NOT touch the WR-05 setTimeout block (handled in 76-02-PLAN).

    **print.tsx (D-03, WR-07):**
    3. Locate the second useEffect at lines 346-350. Replace its body with:
       ```
       useEffect(() => {
           if (data && !loading && !qrammLoading && !qrammError) {
               document.body.setAttribute('data-ready', 'true')
           }
       }, [data, loading, qrammLoading, qrammError])
       ```
       (Use the actual field name confirmed by read_first for the QRAMM error variable.)
    4. Add a visible alert just above the print body that renders when `qrammError` is truthy:
       ```
       {qrammError && (
           <div role="alert" className="text-destructive text-sm">
               QRAMM data unavailable — Q section omitted
           </div>
       )}
       ```
       Place it inside the existing layout — do NOT introduce a new container or restructure (D-12).
    5. **DO NOT TOUCH** the BR-05 cleanup effect at lines 332-335 (Phase 62 — RESEARCH Pitfall 3). Verify by grep after the edit.

    **qramm-profile.tsx (D-04, WR-08):**
    6. In `handleSubmit`'s catch block (lines 186-188), parse the response body and apply the D-02 coercion. Either (a) `import { coerceErrorDetail } from './executive'` (cross-page import — less clean) OR (b) inline the same coercion expression locally (preferred to keep page files independent). Choose (b):
       ```
       const body = await response.json().catch(() => null)
       const detail = (body && typeof body === 'object' && typeof (body as {detail?: unknown}).detail === 'string')
           ? (body as {detail: string}).detail
           : String(body ?? 'Could not start assessment')
       setSubmitError(detail)
       ```
       Note: fallback string stays user-friendly even when API returns nothing — preserves the existing UX baseline.

    Per D-12: no component-structure changes, no new shadcn primitives, no Recharts touches, no Phase-62 hook touches.

    Run `cd src/dashboard && npm test -- useScanList executive-error-coercion print-pdf-cleanup qramm-profile-submit-error` — all green.

    Commit message: `feat(76-01): implement REACT-01 fixes (D-02 coercion, D-03 sentinel guard, D-04 submitError)`.
  </action>
  <verify>
    <automated>cd src/dashboard && npm test -- useScanList executive-error-coercion print-pdf-cleanup qramm-profile-submit-error 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "typeof body\\.detail === 'string'|typeof \\(body as \\{detail\\?: unknown\\}\\)\\.detail === 'string'" src/dashboard/src/pages/executive.tsx` >= 1
    - `grep -cE "export function coerceErrorDetail" src/dashboard/src/pages/executive.tsx` == 1
    - `grep -cE "!qrammError" src/dashboard/src/pages/print.tsx` >= 1
    - `grep -cE "QRAMM data unavailable" src/dashboard/src/pages/print.tsx` >= 1
    - `grep -nE "document\\.body\\.removeAttribute\\('data-ready'\\)" src/dashboard/src/pages/print.tsx` returns at least 2 matches (BR-05 cleanup at ~332-335 still present)
    - `grep -cE "setSubmitError" src/dashboard/src/pages/qramm-profile.tsx` >= 1
    - `grep -cE "typeof.*\\.detail.*=== 'string'" src/dashboard/src/pages/qramm-profile.tsx` >= 1
    - `cd src/dashboard && npm test -- executive-error-coercion print-pdf-cleanup qramm-profile-submit-error` exits 0 (GREEN)
    - `cd src/dashboard && npm test -- useScanList` exits 0
  </acceptance_criteria>
  <done>
    All four Task 1 test modules pass; BR-05 cleanup unchanged; no new shadcn/Tailwind tokens introduced.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build gate + flip WR-02, WR-06, WR-07, WR-08 audit rows</name>
  <files>.planning/audit-2026-05-08/AUDIT-TASKS.md</files>
  <read_first>
    - .planning/audit-2026-05-08/AUDIT-TASKS.md lines 222-233 (react-frontend WR rows)
  </read_first>
  <action>
    Step 1 — Build gate. Run `cd src/dashboard && npm run build` and confirm exit code 0. If non-zero, do NOT flip audit rows; fix the build first.

    Step 2 — Update `.planning/audit-2026-05-08/AUDIT-TASKS.md`, flipping these four rows from `| — | [ ] open |` to `| Phase 76 | [x] closed |`:
    - Line 222 (WR-02): useScanList silently swallows non-OK responses
    - Line 226 (WR-06): executive reads body.detail without coercion check
    - Line 227 (WR-07): print data-ready set even when QRAMM has errored
    - Line 228 (WR-08): qramm-profile submitError swallows actual error message

    No other rows touched. Per-row evidence summaries land in the Plan SUMMARY, not the audit row body. Per-row evidence for WR-02 explicitly states: "hook + consumer already correct (verified by Wave 0 audit + useScanList.test.ts); audit-flip-only" (per user instruction).

    Single docs commit.

    Commit message: `docs(76-01): close react-frontend WR-02/WR-06/WR-07/WR-08 in audit ledger`.
  </action>
  <verify>
    <automated>cd src/dashboard && npm run build 2>&1 | tail -5; cd ../.. && grep -cE "react-frontend/WR-(02|06|07|08).*Phase 76.*\[x\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/dashboard && npm run build` exits 0
    - `grep -cE "react-frontend/WR-(02|06|07|08).*Phase 76.*\\[x\\] closed" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 4
    - `grep -cE "react-frontend/WR-(04|05|09|10|11|12|13).*\\[ \\] open" .planning/audit-2026-05-08/AUDIT-TASKS.md` == 7 (remaining 7 react-frontend rows still open — closed by 76-02/76-03)
  </acceptance_criteria>
  <done>
    `npm run build` exits 0; AUDIT-TASKS.md shows WR-02, WR-06, WR-07, WR-08 closed under Phase 76; remaining 7 react-frontend rows still open pending 76-02/76-03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend API → executive PDF error path | Untrusted body content (could be JSON or raw string) crosses into error rendering |
| Backend API → qramm-profile submit | Same boundary: error body coerced into user-visible string |
| QRAMM data hook → print sentinel | Hook error state crosses into DOM attribute that an external PDF renderer observes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01 | Information Disclosure | print page exposing partial QRAMM data to PDF renderer via premature `data-ready` | mitigate | D-03 sentinel guard — only set `data-ready` when `!qrammError`; render visible alert instead per D-03 |
| T-76-02 | Tampering / DoS | executive PDF error path crashing on raw-string error body (`Cannot read properties of undefined`) | mitigate | D-02 defensive coercion via `coerceErrorDetail(body)` exported helper |
| T-76-03 | Repudiation | qramm-profile generic `submitError` hiding API-side validation failures (operator cannot tell what was rejected) | mitigate | D-04 surface API `detail` field via same D-02 coercion |
| T-76-04 | Information Disclosure | error body containing PII echoed to user via D-02 coercion | accept | API contract guarantees `detail` is operator-facing; CONTEXT scope is dashboard bug fixes only — no new sanitization layer |
</threat_model>

<verification>
- `cd src/dashboard && npm test -- useScanList executive-error-coercion print-pdf-cleanup qramm-profile-submit-error` passes
- `cd src/dashboard && npm run build` exits 0
- BR-05 cleanup effect at print.tsx:332-335 unchanged (grep confirms 2 matches for `removeAttribute('data-ready')`)
- AUDIT-TASKS.md WR-02, WR-06, WR-07, WR-08 rows show `Phase 76 | [x] closed`
- D-12 do-not-touch list honored (no component-structure changes, no new Tailwind tokens, no Recharts touches, no Phase-62 hook touches)
</verification>

<success_criteria>
- `coerceErrorDetail` exported helper exists in `executive.tsx` and is consumed at the PDF error site
- `print.tsx` second useEffect gates `data-ready` on `!qrammError`; alert renders on error
- `qramm-profile.tsx::handleSubmit` catch surfaces API detail message via inline D-02 coercion
- 4 new Vitest modules with full RED→GREEN history
- `npm run build` exits 0
- WR-02, WR-06, WR-07, WR-08 closed in AUDIT-TASKS.md
</success_criteria>

<output>
Create `.planning/phases/76-react-frontend-warnings/76-01-SUMMARY.md` documenting: C-1 confirmed (useScanList hook + scan-history consumer already correct — audit-flip-only with test evidence), D-02 coerceErrorDetail helper landed in executive.tsx, D-03 sentinel guard added without touching BR-05 cleanup, D-04 submitError coercion inlined in qramm-profile.tsx, four audit rows flipped (WR-02, WR-06, WR-07, WR-08), test count, RED→GREEN commit cadence, `npm run build` confirmed green.
</output>
