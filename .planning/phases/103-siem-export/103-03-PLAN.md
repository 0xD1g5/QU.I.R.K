---
phase: 103-siem-export
plan: 03
type: execute
wave: 2
depends_on: ["103-01", "103-02"]
files_modified:
  - quirk/siem/dispatcher.py
  - quirk/cli/export_cmd.py
  - run_scan.py
  - quirk/cli/scheduler_cmd.py
  - tests/test_siem_dispatcher.py
  - tests/test_siem_export_cmd.py
autonomous: true
requirements: [SIEM-01, SIEM-02]

must_haves:
  truths:
    - "`quirk export --siem` locates the latest findings-*.json (or --input PATH) and pushes one CEF event per finding"
    - "Each export batch writes one integration_deliveries row (destination='siem', finding_hash=None)"
    - "An unreachable/misconfigured endpoint yields a clear error + WARNING + a failed audit row, and never aborts/corrupts the scan record"
    - "The after-scan hook fires only when export_after_scan=true and no-ops otherwise"
    - "run_scan.py intercepts `export` before scan argparse; scheduler_cmd calls the hook after the notification hook"
  artifacts:
    - path: "quirk/siem/dispatcher.py"
      provides: "export_findings + export_after_scan_hook"
      contains: "def export_after_scan_hook"
    - path: "quirk/cli/export_cmd.py"
      provides: "run_export argparse entrypoint + findings discovery"
      contains: "def run_export"
    - path: "tests/test_siem_dispatcher.py"
      provides: "dispatch, audit row, failure isolation, hook fire/no-op tests"
    - path: "tests/test_siem_export_cmd.py"
      provides: "CLI argparse + interception + findings discovery tests"
  key_links:
    - from: "run_scan.py"
      to: "quirk.cli.export_cmd.run_export"
      via: "argv[1]=='export' interception block"
      pattern: "run_export"
    - from: "quirk/cli/scheduler_cmd.py"
      to: "quirk.siem.dispatcher.export_after_scan_hook"
      via: "deferred import + try/except after notification hook"
      pattern: "export_after_scan_hook"
    - from: "quirk/siem/dispatcher.py"
      to: "IntegrationDelivery"
      via: "single-commit audit row, destination='siem'"
      pattern: "destination=.siem."
---

<objective>
Wire the layer built in Wave 1 into a usable feature: the dispatcher (`export_findings`, `export_after_scan_hook`), the CLI entrypoint (`run_export`), the run_scan.py interception block, and the scheduler after-scan hook.

Purpose: SIEM-01 (CLI + after-scan invocation, failure isolation) and SIEM-02 (one event per finding, after-scan path). This is the integration seam where the scan record must stay uncorrupted on SIEM failure.
Output: dispatcher.py, export_cmd.py, two modified files (run_scan.py, scheduler_cmd.py), two test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-siem-export/103-CONTEXT.md
@.planning/phases/103-siem-export/103-RESEARCH.md
@.planning/phases/103-siem-export/103-PATTERNS.md
@.planning/phases/103-siem-export/103-01-SUMMARY.md
@.planning/phases/103-siem-export/103-02-SUMMARY.md

<interfaces>
Consumed from Plan 01 (quirk/siem/formatter.py):
  to_cef_finding(finding: dict) -> dict
  build_cef_event(finding: dict, version: str) -> str
Consumed from Plan 02 (quirk/siem/config.py, transport.py):
  load_siem_config(path=None) -> SiemCfg | None
  send_syslog_raw(cef_msg, host, port, protocol="udp", timeout=5) -> None

IntegrationDelivery columns (quirk/models.py L255-260):
  scan_id (str, NOT NULL), finding_hash (str|None), destination (str), status ("ok"|"failed"),
  attempted_at (DateTime, NOT NULL), error_summary (Text|None, always safe_str)

scheduler_cmd seam: run.scan_output_path is set to str(output_dir) at L167; hook goes after the
Phase 101 notification try/except (after L184, before `return run`).

run_scan.py seam: add interception block after the `token` block (after L494).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing dispatcher + CLI-wiring tests (Wave 0)</name>
  <files>tests/test_siem_dispatcher.py, tests/test_siem_export_cmd.py</files>
  <read_first>
    - tests/test_notify_dispatcher.py (DB setup _make_db / init_db helpers L30-64; NOTIFY-07 isolation test pattern)
    - .planning/phases/103-siem-export/103-PATTERNS.md (test_siem_dispatcher.py ~L476-490; test_export_cmd_wiring.py ~L494-501)
    - tests/conftest.py (QUIRK_DB_PATH fixture)
  </read_first>
  <behavior>
    test_siem_dispatcher.py:
    - test_export_one_event_per_finding: 3 findings, monkeypatch send_syslog_raw to record calls -> 3 calls; build_cef_event used
    - test_audit_row_written: after export, exactly one IntegrationDelivery row with destination="siem", finding_hash=None, status="ok"
    - test_unreachable_endpoint: monkeypatch send_syslog_raw to raise OSError -> export_findings returns 0, writes a row with status="failed" and a safe_str error_summary, does NOT raise
    - test_after_scan_hook_fires: SiemCfg.export_after_scan=True (via QUIRK_CONFIG_PATH) + a run with scan_output_path containing a findings-*.json -> send_syslog_raw called
    - test_after_scan_hook_noop: export_after_scan=False -> send_syslog_raw never called, no row written
    - test_hook_never_raises_on_bad_config: load_siem_config returns None -> hook is a clean no-op
    test_siem_export_cmd.py:
    - test_no_flag_exits_1: run_export([]) -> SystemExit code 1
    - test_input_path_used: run_export(["--siem","--input",path]) reads that file
    - test_find_latest_findings: two findings-*.json with different mtimes -> newer chosen
    - test_run_scan_intercepts_export: simulate argv ["export","--siem"] reaches run_export (monkeypatch the import target)
    - test_missing_findings_clear_error: no findings file -> clear error message, non-zero exit, no traceback
  </behavior>
  <action>
    Create both test files. Use init_db on a tmp DB (test_notify_dispatcher helper pattern) for dispatcher tests. Monkeypatch quirk.siem.transport.send_syslog_raw (and/or the dispatcher's imported reference) to record/raise. For the hook tests, write a real findings-*.json into a tmp dir and point run.scan_output_path at it via a lightweight stub object. Tests MUST fail now.
  </action>
  <verify>
    <automated>python -m pytest tests/test_siem_dispatcher.py tests/test_siem_export_cmd.py -q 2>&1 | grep -E "error|ModuleNotFoundError|ImportError|failed"</automated>
  </verify>
  <done>Both files exist and fail because dispatcher.py / export_cmd.py are absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dispatcher.py and export_cmd.py (GREEN)</name>
  <files>quirk/siem/dispatcher.py, quirk/cli/export_cmd.py</files>
  <read_first>
    - quirk/notify/dispatcher.py (per-channel try/except + audit row L203-220; single-commit WR-01 L262-269)
    - quirk/cli/analyze_token_cmd.py (run_X argparse + safe_str error handling L176-238)
    - .planning/phases/103-siem-export/103-RESEARCH.md (Dispatcher pattern ~L570-616; export_cmd argparse ~L423-437; _find_latest_findings ~L478)
    - .planning/phases/103-siem-export/103-PATTERNS.md (Critical Gotcha 5 & 7; QUIRK_CONFIG_PATH discipline)
  </read_first>
  <action>
    dispatcher.py (`from __future__ import annotations`): import logging, datetime/timezone, IntegrationDelivery, safe_str, and the Plan-01/02 functions. Module logger.
    - `export_findings(findings: list, cfg: SiemCfg, db, scan_id: str) -> int`: loop findings; for each call build_cef_event(to_cef_finding handled internally) then send_syslog_raw(cef, cfg.host, cfg.port, cfg.protocol, cfg.timeout_seconds) inside try/except, counting successes and collecting safe_str errors; logger.warning on each failure. After the loop build ONE IntegrationDelivery row per batch: scan_id, finding_hash=None, destination="siem", status "ok" if no errors else "failed", attempted_at=datetime.now(timezone.utc).replace(tzinfo=None), error_summary=safe_str("; ".join(errors)) or None. db.add + db.commit inside its own try/except. Never raise. Return success count.
    - `export_after_scan_hook(run, schedule, db) -> None`: cfg = load_siem_config() with NO args; if cfg is None or not cfg.export_after_scan return; locate latest findings-*.json under run.scan_output_path (reuse _find_latest_findings or inline glob); if none, log warning and return; json.load; derive scan_id from run.scan_output_path stamp or run.scan_id; call export_findings. Whole body wrapped so it never raises into the scheduler.
    export_cmd.py (`from __future__ import annotations`): import argparse, sys, json, pathlib, safe_str. `run_export(argv)`: argparse prog "quirk export" with --siem (store_true), --input, --output-dir (default "output"); if not args.siem print_help + sys.exit(1); `_find_latest_findings(output_dir)` via Path(...).glob("findings-*.json") max by st_mtime; resolve input path (args.input or latest); if missing -> clear "ERROR: no findings file found" + sys.exit(2); load json findings; load_siem_config(); if None -> clear error + exit(2); open a DB session (mirror analyze_token_cmd db acquisition) and call export_findings; print success count. Wrap flow in try/except using safe_str on output.
  </action>
  <verify>
    <automated>python -m pytest tests/test_siem_dispatcher.py tests/test_siem_export_cmd.py -q && python -m compileall quirk/siem/dispatcher.py quirk/cli/export_cmd.py -q</automated>
  </verify>
  <done>All Task-1 dispatcher + CLI tests pass; one batch audit row; failure isolated; hook fires/no-ops per config; compileall clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire run_scan.py interception + scheduler after-scan hook</name>
  <files>run_scan.py, quirk/cli/scheduler_cmd.py</files>
  <read_first>
    - run_scan.py (analyze-token + token interception blocks L484-494 — exact analog)
    - quirk/cli/scheduler_cmd.py (Phase 101 notification hook L171-184 — exact analog; run.scan_output_path set at L167)
    - .planning/phases/103-siem-export/103-PATTERNS.md (run_scan.py block ~L332-339; scheduler block ~L367-383)
  </read_first>
  <action>
    run_scan.py: after the `token` interception block (after L494), add a 4-line block: comment "# --- export subcommand: intercept before scan argparse (Phase 103 SIEM-01/02) ---", `if len(_sys.argv) > 1 and _sys.argv[1] == "export":` deferred `from quirk.cli.export_cmd import run_export`, call `run_export(_sys.argv[2:])`, `return`. No other run_scan.py changes.
    scheduler_cmd.py: immediately after the Phase 101 notification try/except (after L184, before `return run`), add the SIEM hook block mirroring it: comment referencing Phase 103 SIEM-01, deferred `from quirk.siem.dispatcher import export_after_scan_hook`, call `export_after_scan_hook(run=run, schedule=schedule, db=db)`, except Exception (noqa BLE001) logging via safe_str with message "SIEM export error (scan record unaffected): %s". Keep diff minimal (PEP 8).
  </action>
  <verify>
    <automated>python -m compileall run_scan.py quirk/cli/scheduler_cmd.py -q && grep -q "run_export" run_scan.py && grep -q "export_after_scan_hook" quirk/cli/scheduler_cmd.py && python -m pytest tests/test_siem_export_cmd.py -q</automated>
  </verify>
  <done>run_scan.py routes `export` to run_export; scheduler_cmd invokes export_after_scan_hook after the notification hook inside isolating try/except; compileall clean; interception test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| findings-*.json file -> dispatcher | On-disk scan output read and pushed to an external SIEM |
| SIEM delivery failure -> scan record | A delivery failure must not propagate into the committed scan run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-07 | Denial of Service | export_after_scan_hook / scheduler hook | mitigate | Deferred-import + full try/except in scheduler_cmd and inside the hook body; scan run row already committed. Tested by test_unreachable_endpoint, test_hook_never_raises_on_bad_config |
| T-103-08 | Information Disclosure | export_findings / run_export errors | mitigate | All exception output routed through safe_str (no raw exc / credentials in logs or CLI). error_summary always safe_str |
| T-103-09 | Repudiation | IntegrationDelivery audit row | mitigate | One per-batch row (destination="siem", finding_hash=None) records ok/failed + attempted_at for every export attempt. Tested by test_audit_row_written |
| T-103-SC | Tampering | dependency installs | accept | No npm/pip/cargo installs in this phase — all stdlib + existing pyyaml (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `python -m pytest tests/test_siem_dispatcher.py tests/test_siem_export_cmd.py -q` green
- `python -m pytest tests/test_siem_*.py tests/test_siem_export_cmd.py -q` (full wave) green
- `python -m compileall run_scan.py quirk/cli/scheduler_cmd.py quirk/siem -q`
</verification>

<success_criteria>
- `quirk export --siem` pushes one CEF event per finding from the latest/--input findings file
- After-scan hook fires only when export_after_scan=true; never aborts a scan
- Unreachable endpoint -> clear error + WARNING + failed audit row, scan record intact
</success_criteria>

<output>
Create `.planning/phases/103-siem-export/103-03-SUMMARY.md` when done.
</output>
