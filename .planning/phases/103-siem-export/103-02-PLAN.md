---
phase: 103-siem-export
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/siem/config.py
  - quirk/siem/transport.py
  - tests/test_siem_config.py
  - tests/test_siem_transport.py
autonomous: true
requirements: [SIEM-01]

must_haves:
  truths:
    - "[siem] YAML block loads from QUIRK_CONFIG_PATH into a SiemCfg dataclass"
    - "A SQLite DB path or binary/malformed file passed to load_siem_config returns None (never raises)"
    - "send_syslog_raw delivers a CEF event over UDP and over TCP to a syslog receiver"
    - "send_syslog_raw validates host non-empty and 1<=port<=65535 only; it does NOT block internal/loopback targets"
    - "An unreachable endpoint raises OSError for the caller to isolate (never aborts)"
  artifacts:
    - path: "quirk/siem/config.py"
      provides: "SiemCfg dataclass + load_siem_config (QUIRK_CONFIG_PATH discipline)"
      contains: "def load_siem_config"
    - path: "quirk/siem/transport.py"
      provides: "send_syslog_raw UDP/TCP via stdlib socket"
      contains: "def send_syslog_raw"
    - path: "tests/test_siem_config.py"
      provides: "loader + DB-path trap tests"
    - path: "tests/test_siem_transport.py"
      provides: "socketserver UDP/TCP capture + format validation tests"
  key_links:
    - from: "quirk/siem/config.py::load_siem_config"
      to: "QUIRK_CONFIG_PATH env var"
      via: "effective_path = path or os.environ.get('QUIRK_CONFIG_PATH')"
      pattern: "QUIRK_CONFIG_PATH"
---

<objective>
Build the config loader (`SiemCfg` + `load_siem_config`) and the stdlib socket transport (`send_syslog_raw`, UDP+TCP). Both are independent of the formatter (Plan 01) and of each other's tests, so they ship in the same Wave-1 plan.

Purpose: SIEM-01 transport correctness (UDP/TCP framing, timeout, the QUIRK_CONFIG_PATH-not-DB-path trap) and the no-internal-block validation rule.
Output: `quirk/siem/config.py`, `quirk/siem/transport.py`, two test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-siem-export/103-CONTEXT.md
@.planning/phases/103-siem-export/103-RESEARCH.md
@.planning/phases/103-siem-export/103-PATTERNS.md

<interfaces>
quirk/siem/config.py exports:
  @dataclass SiemCfg(host: str, port: int=514, protocol: str="udp",
                     export_after_scan: bool=False, timeout_seconds: int=5)
  load_siem_config(path: str | None = None) -> SiemCfg | None

quirk/siem/transport.py exports:
  send_syslog_raw(cef_msg: str, host: str, port: int,
                  protocol: str="udp", timeout: int=5) -> None  # raises OSError/ValueError on failure
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing config + transport tests (Wave 0)</name>
  <files>tests/test_siem_config.py, tests/test_siem_transport.py</files>
  <read_first>
    - tests/test_notify_config.py (analog: test_no_env_no_path_returns_none, test_binary_file_returns_none, test_binary_env_path_returns_none)
    - .planning/phases/103-siem-export/103-PATTERNS.md (test_siem_config.py ~L389-414; test_siem_transport.py socketserver pattern ~L444-472)
    - .planning/phases/103-siem-export/103-RESEARCH.md (Code Examples: socketserver UDP capture ~L618-647)
  </read_first>
  <behavior>
    test_siem_config.py:
    - test_loads_siem_block: writes a YAML file with a [siem] block, sets QUIRK_CONFIG_PATH -> SiemCfg with host/port/protocol/export_after_scan/timeout_seconds
    - test_no_env_no_path_returns_none: env unset, no path -> None
    - test_missing_siem_block_returns_none: valid YAML without a siem key -> None
    - test_db_path_returns_none: write b"SQLite format 3\x00..." file, load_siem_config(path=that) -> None (Pitfall 2 trap)
    - test_binary_env_path_returns_none: same SQLite bytes via QUIRK_CONFIG_PATH -> None
    test_siem_transport.py:
    - test_send_cef_udp_delivers: socketserver.UDPServer on 127.0.0.1:0, send_syslog_raw -> captured datagram contains b"CEF:0" and the <PRI> prefix b"<12>"
    - test_send_cef_tcp_delivers: socketserver.TCPServer equivalent
    - test_unreachable_raises: send to a closed/refused port (or 0.0.0.0 invalid) raises OSError
    - test_rejects_empty_host: host="" raises ValueError
    - test_rejects_bad_port: port=0 and port=70000 raise ValueError
    - test_does_not_block_loopback: 127.0.0.1 with a live receiver succeeds (asserts no internal-IP block)
  </behavior>
  <action>
    Create both test files importing from quirk.siem.config and quirk.siem.transport. Use socketserver.UDPServer/TCPServer with port 0 (OS-assigned) and a daemon thread calling handle_request; join with timeout. For unreachable, bind+immediately-close a socket to grab a refused port, or use a TCP connect to a closed port. Tests MUST fail now (modules absent).
  </action>
  <verify>
    <automated>python -m pytest tests/test_siem_config.py tests/test_siem_transport.py -q 2>&1 | grep -E "error|ModuleNotFoundError|failed"</automated>
  </verify>
  <done>Both files exist and fail because quirk.siem.config / quirk.siem.transport are absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement config.py and transport.py (GREEN)</name>
  <files>quirk/siem/config.py, quirk/siem/transport.py</files>
  <read_first>
    - quirk/notify/config.py (load_notifications_config verbatim copy target, lines 1-184; module docstring CRITICAL CONSTRAINT block lines 6-15)
    - quirk/notify/channels/webhook.py (send_webhook contract: raises on failure, caller wraps)
    - .planning/phases/103-siem-export/103-RESEARCH.md (Pattern 3 SiemCfg ~L291-328; Pattern 2 send_syslog_raw ~L267-289)
    - .planning/phases/103-siem-export/103-PATTERNS.md (Critical Gotcha 4: do NOT call validate_external_url)
  </read_first>
  <action>
    config.py: `from __future__ import annotations`. Copy quirk/notify/config.py::load_notifications_config STRUCTURE verbatim, replacing "notifications"->"siem" and the NotifyCfg parse with a flat SiemCfg dataclass (host required; port=514, protocol="udp" lowercased, export_after_scan=False, timeout_seconds=5). PRESERVE the module docstring's CRITICAL CONSTRAINT warning that QUIRK_CONFIG_PATH is a YAML path, NOT the scheduler SQLite DB path, reworded for SIEM. `effective_path = path or os.environ.get("QUIRK_CONFIG_PATH")`; return None if missing/not a file; wrap parse in try/except returning None on any exception (binary/malformed). The scheduler calls it with NO args.

    transport.py: `from __future__ import annotations`; import logging, socket; module logger. Implement send_syslog_raw(cef_msg, host, port, protocol="udp", timeout=5): FIRST validate host (non-empty str) and port (1<=int<=65535) raising ValueError otherwise — do NOT call validate_external_url (syslog collectors are internal per D-02). Compute pri=(1*8)+4 (LOG_USER, LOG_WARNING) => "<12>"; payload = f"<{pri}>{cef_msg}".encode("utf-8"); socktype SOCK_STREAM if protocol=="tcp" else SOCK_DGRAM; open socket via `with`, settimeout(timeout); TCP connect+sendall, UDP sendto((host,port)). Let OSError propagate to the caller.
  </action>
  <verify>
    <automated>python -m pytest tests/test_siem_config.py tests/test_siem_transport.py -q && python -m compileall quirk/siem/config.py quirk/siem/transport.py -q</automated>
  </verify>
  <done>All Task-1 tests pass; loader returns None for DB/binary paths; transport delivers UDP and TCP with <12> prefix and rejects malformed host/port; loopback is NOT blocked; compileall clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QUIRK_CONFIG_PATH file -> SiemCfg | Config file may be the wrong file (SQLite DB) or malformed |
| QUIRK process -> syslog collector | CEF bytes sent over plaintext UDP/TCP to an internal target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-04 | Denial of Service | load_siem_config | mitigate | DB-path/binary/malformed file returns None (never raises) so a misconfigured config can never abort a scheduled scan. Tested by test_db_path_returns_none, test_binary_env_path_returns_none |
| T-103-05 | Tampering | send_syslog_raw host/port validation | mitigate | Reject empty host and out-of-range port (ValueError); validate format only — intentionally NOT validate_external_url, since syslog collectors are internal (D-02). Tested by test_rejects_empty_host, test_rejects_bad_port, test_does_not_block_loopback |
| T-103-06 | Information Disclosure | syslog plaintext transport | accept | TLS-wrapped syslog (RFC 5425) deferred per CONTEXT deferred-ideas; internal-network plaintext is the accepted baseline for this phase |
</threat_model>

<verification>
- `python -m pytest tests/test_siem_config.py tests/test_siem_transport.py -q` green
- `python -m compileall quirk/siem -q`
</verification>

<success_criteria>
- SiemCfg loads only via QUIRK_CONFIG_PATH; DB/binary path -> None
- send_syslog_raw delivers over UDP and TCP, validates host/port format, never blocks loopback
- Unreachable endpoint raises OSError for the dispatcher to isolate
</success_criteria>

<output>
Create `.planning/phases/103-siem-export/103-02-SUMMARY.md` when done.
</output>
