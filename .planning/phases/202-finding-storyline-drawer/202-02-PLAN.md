---
phase: 202-finding-storyline-drawer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dashboard/src/types/api.ts
  - src/dashboard/src/hooks/useFindingStoryline.ts
  - src/dashboard/src/hooks/__tests__/useFindingStoryline.test.tsx
autonomous: true
requirements: [STORY-01, STORY-02]

must_haves:
  truths:
    - "`FindingStoryline` exists in TS with all ten fields required-and-nullable (`T | null`), never optional (`?:`), so a missing field cannot become a silent third state alongside null (D-01 honest-absence contract)"
    - "`FindingItem.id` is `number | null` rather than `id?: number`, because it is the fetch key and the undefined-vs-null ambiguity sits exactly where the A6 disabled-trigger decision is made (D-06)"
    - "`useFindingStoryline` fetches `GET /api/findings/{id}/storyline?title=<encoded>` — the `(endpoint id, title)` pair, because `FindingItem.id` is `CryptoEndpoint.id` and is shared across every finding derived from that endpoint (D-06)"
    - "The hook distinguishes a failed fetch from an absent value: a fetch failure sets `error`, never a null-filled payload, so the drawer can render S8 rather than an absence string (D-07, UI-SPEC Loading & Error Contract)"
    - "Closing the drawer mid-fetch performs no state write against an unmounted component, and a re-open re-issues the fetch rather than serving the prior finding's payload (UI-SPEC Loading & Error Contract)"
    - "The hook exposes a `retry` that re-issues the same request without remounting, backing the `Retry storyline` button (UI-SPEC S8)"
  artifacts:
    - path: "src/dashboard/src/types/api.ts"
      provides: "FindingStoryline interface and the FindingItem.id nullability correction"
      contains: "FindingStoryline"
    - path: "src/dashboard/src/hooks/useFindingStoryline.ts"
      provides: "lazy per-finding storyline fetch hook with retry and cancellation"
      exports: ["useFindingStoryline"]
    - path: "src/dashboard/src/hooks/__tests__/useFindingStoryline.test.tsx"
      provides: "cancellation, error-vs-absence, retry, and re-open coverage"
  key_links:
    - from: "src/dashboard/src/hooks/useFindingStoryline.ts"
      to: "src/dashboard/src/lib/api.ts"
      via: "fetchApi with a template-literal URL whose literal prefix is /api/findings"
      pattern: "fetchApi\\(\\s*`/api/findings"
---

<objective>
Land the TypeScript contract and the fetch hook for the storyline drawer.

Purpose: the UI-SPEC's Type Contract is binding and fixes both the shape rule (required-and-nullable,
never optional) and the `FindingItem.id` correction. Landing it first, in its own plan, means the
backend (202-03/202-05) and the components (202-04/202-06) are built against one written contract
instead of discovering each other's field names. It also isolates the blast radius of making
`FindingItem.id` required — every existing `FindingItem` literal in the test suite must gain an `id`.

Output: the `FindingStoryline` interface, the corrected `FindingItem.id`, `useFindingStoryline.ts`,
and its vitest file.

Hard constraints:
- **The ten field names below are LOCKED for the whole phase.** They are transcribed verbatim from
  the UI-SPEC's Type Contract. Field naming was Claude's discretion *before* the spec; the spec
  spent it. Do not rename, reorder, or add fields — 202-03 and 202-05 mirror this exact shape in
  Pydantic, and 202-04/202-06 consume it.
- **No `?:` anywhere in `FindingStoryline`.** 201-UI-E2 is the precedent: `?:` admits `undefined`
  alongside `null` and the type system stops enforcing honest absence.
- All null checks use `!= null`, never truthiness — `if (theme_score_lift)` treats a legitimate `0`
  as absence, the inverse of this codebase's contract (`roadmap.tsx:307`, `print.tsx:204` are the
  convention).
- `.tsx`/`.ts` edits REQUIRE `npm run lint && npm run test` from `src/dashboard/`. This plan touches
  **no rendered component**, so no `npm run build` and no statics commit is required — and you must
  NOT commit `quirk/dashboard/static/` from this plan (202-06 owns the rebuilt statics, and a stray
  static diff here makes that plan's diff unreadable).
- Plain `git add` + `git commit`; never the gsd `commit --files` wrapper.
- Do NOT invoke any mutating GSD verb, and do not touch `.planning/STATE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/202-finding-storyline-drawer/202-CONTEXT.md
@.planning/phases/202-finding-storyline-drawer/202-UI-SPEC.md

<interfaces>
<!-- LOCKED for the phase. Verbatim from 202-UI-SPEC.md "Type Contract". -->
```ts
export interface FindingStoryline {
  finding_id: number
  narrative: string | null
  quantum_impact: string | null
  remediation_guidance: string | null
  theme_slug: string | null
  theme_title: string | null
  theme_score_lift: number | null
  theme_finding_count: number | null
  theme_closed_count: number | null
  finding_position: number | null
}
```

Current state to correct (`src/dashboard/src/types/api.ts:45-59`):
```ts
export interface FindingItem {
  id?: number        // -> becomes `id: number | null`
  host: string
  port: number
  severity: string
  title: string
  ...
}
```

Fetch URL shape (required by 202-07's `fixture-coverage.test.ts` extractor — see below):
`fetchApi(\`/api/findings/${id}/storyline?title=${encodeURIComponent(title)}\`)`

The existing fetch idiom to mirror: `src/dashboard/src/hooks/useHardwareDrift.ts` — `cancelled`
flag, synchronous pre-await state resets, per-status error copy, `finally { setLoading(false) }`.
Note its URL is a hoisted `const url = "..."`, extracted by `extractQuotedConstUrl`; ours is a
template literal, extracted by `extractTemplateLiteralPrefix`, whose regex is
`/fetchApi\(\s*`([^$`]+)\$\{/` (`tests/a11y/fixture-coverage.test.ts:56-63`). The `fetchApi(` call
must therefore take the template literal **directly as its argument**, with `/api/findings/` as the
literal text before the first `${` — if it is hoisted into a variable first, 202-07's extractor
cannot see it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FindingStoryline interface and the FindingItem.id nullability correction</name>
  <files>src/dashboard/src/types/api.ts</files>
  <read_first>
    - `src/dashboard/src/types/api.ts:30-75` (the `FindingItem` declaration and the house comment style for nullability decisions — e.g. the `184.1-06 / SC-3 / D-12` comment just above it)
    - `src/dashboard/src/pages/roadmap.tsx` around :300-315 (the `!= null` guard convention)
  </read_first>
  <action>
Add `FindingStoryline` to `src/dashboard/src/types/api.ts` exactly as given in `<interfaces>`, with a
comment citing `202-UI-SPEC.md` Type Contract and 201-UI-E2, stating that every field is
required-and-nullable on purpose and that the Pydantic mirror must always serialise each field
(never `exclude_none`) so the TS type stays honest.

Change `FindingItem.id` from `id?: number` to `id: number | null`, with a comment stating: it is
`CryptoEndpoint.id`, it is NOT unique per finding (multiple findings per endpoint share it — D-06),
it is the storyline fetch key, and the `undefined`-vs-`null` distinction sits exactly where absence
case A6's disabled-trigger decision is made.

Then fix the fallout: every `FindingItem` literal in the repo that omits `id` now fails
`tsc`. Find them with a repo-wide search rather than guessing, add `id` to each (use `null` where the
fixture is deliberately id-less so A6 stays exercisable, a number otherwise), and record the full
list of touched files in the SUMMARY. If any production (non-test) site constructs a `FindingItem`
without an id, flag it — that is a real A6 source, not a fixture gap. (One is known to exist:
`_derive_identity_findings`' results are appended server-side with no `id=`, so identity findings
arrive with `id: null`.)
  </action>
  <verify>
    <automated>cd src/dashboard && npx tsc --noEmit && grep -c "FindingStoryline" src/types/api.ts && ! grep -A 14 "export interface FindingStoryline" src/types/api.ts | grep -q "?:" && echo TYPES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `FindingStoryline` has exactly the ten locked fields, in the locked order, with zero `?:` occurrences
    - `FindingItem.id` reads `id: number | null` with the explanatory comment
    - `npx tsc --noEmit` exits 0 from `src/dashboard/`
    - The SUMMARY lists every file whose `FindingItem` literals were amended, and flags any production construction site lacking an id
    - `git status --short quirk/dashboard/static` is empty — no statics committed from this plan
  </acceptance_criteria>
  <done>Both sides of the API boundary have one written, honest shape to build against.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useFindingStoryline hook with retry, cancellation, and error-vs-absence separation</name>
  <files>src/dashboard/src/hooks/useFindingStoryline.ts, src/dashboard/src/hooks/__tests__/useFindingStoryline.test.tsx</files>
  <read_first>
    - `src/dashboard/src/hooks/useHardwareDrift.ts` in full (the idiom to mirror exactly)
    - `src/dashboard/src/hooks/__tests__/useScanData.test.tsx` (the hook-test idiom: how `fetchApi` is mocked, how async state is awaited)
    - `src/dashboard/src/lib/api.ts:54+` (`fetchApi`'s signature and what it does about auth headers)
    - `src/dashboard/tests/a11y/fixture-coverage.test.ts:45-63` (the two URL extractors — confirm the template-literal one will match the call you write)
  </read_first>
  <behavior>
    - Given `{ id: 7, title: "Certificate expired" }` and a 200 response, the hook ends with `data` set, `loading` false, `error` null.
    - The URL requested is `/api/findings/7/storyline?title=Certificate%20expired` — the title is URL-encoded, and a title containing `&`, `?`, `#`, or a non-ASCII character round-trips intact.
    - Given a non-ok response, `data` stays null and `error` is a non-empty string. `data` is NEVER a null-filled `FindingStoryline` on failure — absence and failure are different facts.
    - Given `{ id: null }` or no finding selected, the hook issues no request at all (A6's trigger is disabled, so this is a guard, not a fetch).
    - Unmounting mid-flight produces no state update (no React "update on unmounted component" warning; the `cancelled` flag short-circuits every setter).
    - Changing the selected finding re-issues the fetch and clears the previous `data` synchronously before awaiting, so the drawer can never show the prior finding's narrative under the new finding's title.
    - `retry()` re-issues the same request, sets `loading` true again, and leaves `error` cleared on success.
  </behavior>
  <action>
Write `src/dashboard/src/hooks/useFindingStoryline.ts`:

```
export function useFindingStoryline(
  finding: { id: number | null; title: string } | null,
): { data: FindingStoryline | null; loading: boolean; error: string | null; retry: () => void }
```

Mirror `useHardwareDrift`'s structure: `cancelled` flag, synchronous `setData(null)` /
`setLoading(true)` / `setError(null)` before the await, `finally { if (!cancelled) setLoading(false) }`.
Differences required here:

- Guard: if `finding == null || finding.id == null`, set `loading` false, leave `data`/`error` null,
  and issue no request.
- The `fetchApi` call takes the template literal directly (see `<interfaces>`), so 202-07's
  extractor can read `/api/findings/` out of the source.
- `retry` is a `useCallback` bumping a nonce in the effect's dependency list. It must NOT remount or
  close the drawer.
- Error copy: one fixed operator-facing string for every failure cause —
  `Could not load the storyline for this finding.` — per the UI-SPEC's Copywriting Contract. Do NOT
  branch per status code, do NOT include a status code, URL, or retry count in the string. (This
  deliberately diverges from `useHardwareDrift`'s per-status copy: the UI-SPEC locks one string here,
  and the component renders the `Retry storyline` affordance instead of telling the operator which
  HTTP code occurred.) A 401 is still worth distinguishing **in a comment** explaining why it is not:
  the whole dashboard is auth-gated and the app's auth layer already handles the unauthenticated
  case globally.
- Effect deps: `[finding?.id, finding?.title, nonce]` — keyed on BOTH id and title, because id alone
  does not identify a finding (D-06).

Then write `src/dashboard/src/hooks/__tests__/useFindingStoryline.test.tsx` covering every bullet in
the behavior block. Write the tests first and watch them fail against an empty hook, then implement.
Record the RED output in the SUMMARY.
  </action>
  <verify>
    <automated>cd src/dashboard && npm run test -- useFindingStoryline && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E 'fetchApi\(\s*`/api/findings/' src/dashboard/src/hooks/useFindingStoryline.ts` matches — the literal prefix is visible to `extractTemplateLiteralPrefix`'s regex. Confirm by running that extractor's regex against the file and pasting the captured prefix into the SUMMARY
    - Every behavior bullet has a named test; the RED transcript is in the SUMMARY
    - The failure path leaves `data` null — assert this explicitly, not just that `error` is set
    - `retry()` is covered, including that it does not clear `data` on a second failure in a way that would blank an already-rendered panel
    - `npm run test` and `npm run lint` green from `src/dashboard/`
    - No `quirk/dashboard/static/` files staged
  </acceptance_criteria>
  <done>The drawer can fetch lazily, retry in place, and tell a failed fetch apart from an honest absence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> dashboard API | The hook sends a finding title as a query parameter. Untrusted only in the sense that it originates from rendered data; it crosses into a server-side lookup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-202-04 | Tampering | Unencoded title in the query string producing a malformed or truncated request (a title containing `&`/`#` silently changing which finding is looked up) | mitigate | `encodeURIComponent(title)` at the single call site, asserted by a test using a title containing `&`, `?`, `#`, and a non-ASCII character |
| T-202-05 | Information disclosure | HTTP status, URL, or exception text reaching the operator-visible error string | mitigate | One fixed error string in the hook; no status-code branching; asserted by test |
| T-202-06 | Repudiation | A failed fetch rendering as honest absence, telling the operator "no narrative exists" when the request simply failed | mitigate | `data` stays null on failure and `error` is set; asserted explicitly; the drawer's S8 branch (202-06) keys on `error` |
| T-202-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; zero new dependencies phase-wide (D-03 forbids a drawer library) |
</threat_model>

<verification>
- `cd src/dashboard && npx tsc --noEmit` exits 0
- `cd src/dashboard && npm run test && npm run lint` green
- `FindingStoryline` contains zero `?:`; `FindingItem.id` is `number | null`
- `git status --short quirk/dashboard/static` is empty
</verification>

<success_criteria>
- One written, locked API shape exists for both sides of this phase to build against
- The hook's URL is discoverable by the a11y fixture-coverage extractor 202-07 depends on
- Failure and absence are representationally distinct in the hook's return value
</success_criteria>

<output>
Create `.planning/phases/202-finding-storyline-drawer/202-02-SUMMARY.md` when done. Include the RED
transcript, the captured URL prefix from the extractor regex, and the full list of files whose
`FindingItem` literals were amended.
</output>
