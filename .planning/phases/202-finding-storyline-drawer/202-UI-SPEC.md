---
phase: 202
slug: finding-storyline-drawer
status: draft
shadcn_initialized: false
preset: none (no `components.json` in this repo — shadcn/ui components are hand-vendored under `src/dashboard/src/components/ui/`; treat as the shadcn gate's N-branch, same as 201)
created: 2026-09-12
---

# Phase 202 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

> **Scope discipline (per 202-CONTEXT.md D-01..D-05 and the ROADMAP's 4 success criteria):** this is
> an EXTENSION of the **already-existing** finding detail `Sheet` in
> `src/dashboard/src/pages/findings.tsx` (lines 218-254) — not a new drawer, not a redesign of the
> findings table. This spec locks new surface area only: the per-row storyline trigger, the
> narrative section, the theme-framed score-lift attribution block, the drawer's loading / error /
> honest-absence states, the focus-and-keyboard contract, and their copywriting. The findings
> table's existing columns, filters, pagination, severity badge colors, and the page-level
> skeleton/empty states are unchanged and out of scope.

> **Pre-existing surface this phase must correct as a side effect (both are gate-relevant, not
> cosmetic):** the Sheet today sets width via inline `style={{ width: 480 }}`, which overrides
> `sheetVariants`' responsive `w-3/4 sm:max-w-sm` and overflows viewports narrower than 480px
> (see Responsive Contract); and the Sheet renders no `SheetDescription`, which surfaces a Radix
> dev console warning the moment the a11y harness opens it (see Focus & Keyboard Contract, F8).

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (hand-vendored shadcn/ui; no `components.json` project-wide — inherited from 201's finding, re-verified 2026-09-12) |
| Preset | not applicable — existing custom "Obsidian Pro" token set in `src/dashboard/src/index.css` (`--ds-*` custom properties) layered under standard shadcn CSS variables (`--background`, `--card`, `--border`, `--muted-foreground`, `--accent`, `--destructive`) via `tailwind.config.ts` |
| Component library | Radix primitives via hand-vendored shadcn/ui. **Reused, none added (D-03):** `Sheet` / `SheetContent` / `SheetHeader` / `SheetTitle` / `SheetDescription` / `SheetClose` (`@radix-ui/react-dialog` ^1.1.15), `Badge`, `Button`, `Skeleton`, `Table` |
| Icon library | lucide-react ^0.474.0 (`X` already used by `sheet.tsx`; this phase may use `Info` for the attribution disclaimer and `ScrollText` or `BookOpen` for the row trigger — no other new icons) |
| Font | UI text: system default via Tailwind base. Monospace (`var(--ds-font-mono)`) is **not** used in this drawer — no numeric value here is a code/ID surface |

No shadcn init prompt was run (non-interactive agent; `components.json` is absent project-wide and
this phase introduces no registry block). Registry safety gate: not applicable — see Registry Safety.

---

## Spacing Scale

Declared values — **strictly multiples of 4**:

| Token | Value | Tailwind step | Usage in this phase |
|-------|-------|---------------|---------------------|
| xs | 4px | `gap-1` / `p-1` / `mb-1` | Icon-to-text gap in the attribution disclaimer; label-to-value gap |
| sm | 8px | `gap-2` / `space-y-2` | Badge-to-host-string gap (existing, unchanged); attribution block internal line spacing |
| md | 16px | `gap-4` / `p-4` / `mt-4` | Attribution sub-panel padding; gap between drawer sections |
| lg | 24px | `p-6` | `SheetContent`'s existing shell padding (`sheetVariants` already sets `p-6`) — reused, not re-declared |
| xl | 32px | `gap-8` | N/A this phase |
| 2xl | 48px | — | N/A this phase |
| 3xl | 64px | — | N/A this phase |

**Exceptions: none.**

**Explicitly forbidden in this phase's new code** (this is the 201-UI-S1 regression this spec
exists to prevent — and note 201's *fix* for `ml-1.5` was `gap-1.5`, which is also 6px and also
off-grid, so the fix did not actually reach the declared scale):

- Any `.5` Tailwind spacing step: `gap-1.5`, `ml-1.5`, `mt-0.5`, `space-y-2.5`, `p-1.5`, etc. (6px / 2px / 10px — all off the 4px grid).
- Any arbitrary bracket spacing value: `mt-[6px]`, `gap-[10px]`, `p-[14px]`.
- Any inline `style` spacing (`marginLeft: 6`, `padding: 10`).

The pre-existing `gap-1.5` at `roadmap.tsx:294` and `mt-1.5` at `roadmap.tsx:339` are out of this
phase's scope and are **not** to be corrected here — but they must not be copied into
`findings.tsx` or any new 202 component as precedent. Where 202 needs a tight inline gap, use
`gap-1` (4px).

The only non-spacing pixel literals this phase may introduce are the drawer's width breakpoint
(`480px`) and max content width — both 4-multiples, both declared in the Responsive Contract.

---

## Typography

| Role | Size | Weight | Line Height | Tailwind / style |
|------|------|--------|-------------|------------------|
| Body | 14px | 400 (regular) | 1.5 | `text-sm leading-normal` (drawer body default, already set by `findings.tsx:225`) |
| Label | 12px | 600 (semibold) | 1.2 | `text-xs font-semibold` (section eyebrow labels, badges) |
| Heading | 16px | 600 (semibold) | 1.2 | `text-base font-semibold` (drawer title — `findings.tsx:223` already overrides `SheetTitle`'s default `text-lg` to `text-base`; keep that override) |
| Display | 20px | 600 (semibold) | 1.2 | `style={{ fontSize: 20, fontWeight: 600 }}` — **exactly one use in this drawer**: the theme-lift `+N` glyph (see Color) |

Source: identical to the 201 contract (approved 6/6, audited 4/4 on typography) and to the scale
already in use in `findings.tsx` (`h1` 20/600 at line 125, section eyebrow labels `text-xs
font-semibold text-muted-foreground uppercase` at lines 234/240/246, body `text-sm` at line 225).

Only two weights exist project-wide (400 body, 600 headings/labels/emphasis numbers). **This phase
introduces no new size and no new weight.** The narrative paragraph body text uses Body; the
attribution disclaimer uses 12px/400 (`text-xs` with no `font-semibold`) — 12px/400 is the
existing muted-caption treatment already used at `roadmap.tsx:353` and `findings.tsx:115`, and is a
weight/size *pairing* of already-declared values, not a fifth role.

Section eyebrow labels in the drawer reuse the existing verbatim pattern from `findings.tsx:234`:
`className="text-xs font-semibold text-muted-foreground uppercase mb-1"` (Label role, 4px bottom
gap = xs token). New sections must match it exactly rather than inventing a heading treatment.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `hsl(var(--background))` / `var(--ds-bg-page)` | Page background; `SheetContent`'s own `bg-background` (already set by `sheetVariants`) |
| Secondary (30%) | `hsl(var(--card))` / `var(--ds-bg-surface)` with `hsl(var(--border))` | The attribution sub-panel shell (`rounded-lg border border-border bg-card p-4`); the drawer overlay scrim is the primitive's existing `bg-black/80` |
| Accent (10%) | `var(--ds-ok)` (`#3fa876` dark / `#2c8a5d` light) | **Reserved exclusively for ONE element: the theme score-lift `+N` glyph inside the attribution block's lift sentence.** Nothing else new in this phase uses it |
| Destructive | `hsl(var(--destructive))` | **Not used.** The drawer is read-only — no destructive action exists in this phase |

**Accent reserved for:** the single `+N` numeral in the attribution block's lift sentence, rendered
at Display (20px/600) in `var(--ds-ok)`. That is the complete list. Specifically NOT accented: the
theme title, the `pts when …` condition clause, the position line, the closed count, the narrative
text, the row trigger button, any badge, any section label.

Rationale (ties to D-01): the lift number is the one value in this drawer an operator will
mis-attribute. Making it the *only* accented glyph means the eye that lands on it lands inside the
sentence that qualifies it, and there is no second accented number anywhere to invite comparison,
subtraction, or summation.

**Error/absence tone:** honest-absence and error copy render in `text-muted-foreground`
(`var(--ds-text-muted)`) — never `--ds-critical`, never `--ds-high`. An absent value is not a
failure state and must not be tinted as one. A failed *fetch* (see Copywriting Contract) is also
muted, not destructive-tinted: the project's convention is a calm honest statement plus a recovery
path, and an alarm color would imply the finding itself is worse than it is.

**No hardcoded hex, rgb, or `hsl(...)` literals** in this phase's new code. All color goes through
`--ds-*` custom properties or shadcn `hsl(var(--token))` utility classes. The pre-existing
`SEVERITY_STYLES` map (`findings.tsx:25-31`) and the inline `quantum_risk` color map
(`findings.tsx:89-93`) both use hardcoded bracket `hsl(...)` values — they are pre-existing,
unchanged, out of scope, and **must not be extended or copied** by this phase.

**Canvas hazard:** not applicable — this drawer renders no canvas. No `var(--…)` string is passed
to Cytoscape, Recharts, or any 2D context (the Phase 195 failure mode cannot recur here because no
new chart or canvas component is introduced; a chart in this drawer is explicitly out of scope).

---

## Copywriting Contract

All strings below are **exact and greppable**. An implementer must not paraphrase, re-case, or
re-punctuate them; a reviewer should be able to `grep -F` each one.

| Element | Copy |
|---------|------|
| Primary CTA (per-row trigger) | `Storyline` — button label in the new rightmost table column, whose header is also `Storyline`. Accessible name: `aria-label={`Open storyline for ${title} at ${host}:${port}`}` |
| Drawer narrative section label | `STORYLINE` (Label role, uppercase via the existing `uppercase` class on sentence-case source text `Storyline` — match `findings.tsx:234`'s pattern exactly) |
| Attribution section label | `SCORE-LIFT ATTRIBUTION` |
| Attribution theme line | `Remediation theme: {theme_title}` |
| Attribution lift sentence | `+{formatScoreNumber(theme_score_lift)} pts when all {theme_finding_count} findings in this theme are resolved` — the `+{N}` portion is the accented Display glyph; the rest of the sentence is Body/muted and **must be in the same continuous text flow as the number** (see Attribution Block Contract) |
| Attribution position line | `This finding is {finding_position} of {theme_finding_count} in the theme · {theme_closed_count} of {theme_finding_count} verified closed` |
| Attribution disclaimer (**MANDATORY whenever a lift number renders**) | `This is the theme's total lift, not this finding's individual contribution. Resolving this finding alone does not yield the full amount.` |
| Honest absence — finding is not mapped to a theme (`slug_for_title()` returned `None`) | `Not mapped to a remediation theme — no score-lift attribution exists for this finding.` No number, no theme line, no position line, no disclaimer |
| Honest absence — theme known but `theme_score_lift` is `null` | Theme line still renders. Then: `Score lift could not be modelled for this theme in this scan.` No number, no disclaimer (there is no number to qualify) |
| Honest absence — constituent counts unavailable (`theme_finding_count` is `null` or `0`) | Omit the position line entirely. Never render `1 of 0`, `1 of null`, or `0 of 0`. If `theme_finding_count` is null/0 the lift sentence also cannot state its condition, so the whole lift sentence is suppressed and the theme-lift-unmodellable copy above is used instead |
| Honest absence — no catalog narrative for this finding (D-05) | `No catalog narrative exists for this finding type yet — the Description and Remediation fields above are the available guidance.` |
| Honest absence — finding has no stable id (`FindingItem.id` is `null`) | The row's `Storyline` trigger renders **disabled** with `aria-label={`Storyline unavailable for ${title} at ${host}:${port}`}` and `title="Storyline unavailable — this finding has no stable identifier in this scan."`. The drawer is never opened in a state where it cannot fetch |
| Loading state | `Loading storyline…` as `sr-only` text inside a `role="status"` wrapper, alongside the visual skeleton (see Loading & Error Contract). Use the single U+2026 character `…`, not three ASCII periods — `findings.skeleton.tsx:6`'s `Loading...` is pre-existing and is not the precedent for new copy |
| Error state (fetch failed, any cause) | `Could not load the storyline for this finding.` followed by a `Retry` button (`Button variant="outline" size="sm"`, label exactly `Retry`). One fixed string for all failure causes — never `str(exc)`, never a status code, never a URL, per the fixed-detail precedent in 202-CONTEXT's Established Patterns |
| Empty state (page level) | N/A — unchanged. Reuses `findings.tsx:119`'s existing `EmptyStateCard` message verbatim. With zero findings there are no rows, so no trigger and no drawer |
| Destructive confirmation | None — no destructive action in this phase's scope |

**Forbidden copy** (each would re-introduce the defect D-01 rejects, or a fabricated value):

- Any phrasing that attributes the number to the finding: `This finding is worth +4 pts`, `Score lift: +4 pts`, `Your score improves by 4 points`, `+4 pts available here`.
- Any per-finding share, however worded: `≈ +0.5 pts`, `your share: +0.5 pts`, `+4 pts / 8 findings`.
- `N/A`, `Unknown`, `—`, `0`, `0 pts`, `+0 pts`, or `null` as the rendered value of an absent lift. `—` is a value-styled glyph in this codebase (`findings.tsx:88` uses it for an absent `quantum_risk` *cell*, where a table cell must occupy space); the drawer is a prose surface with no column to hold open, so absence is a **sentence**, not a dash.
- A bare number with the condition in a separate element, tooltip, footnote, or `title` attribute.

---

## Attribution Block Contract (D-01 — the load-bearing section)

(Non-template section. D-01's honest theme framing is this phase's hardest design problem; it is
specified structurally here so it is checkable, not left to prose.)

**Placement.** A bordered sub-panel (`rounded-lg border border-border bg-card p-4 space-y-2`) in the
drawer body, rendered **last** — after Description, Remediation, and Quantum Context — and
**separated from the drawer's own severity/host badge row by at least md (16px)**. It must NOT be a
badge in the header badge row (`findings.tsx:226-231`). Reason: a chip sitting beside the severity
badge and the `host:port` string reads as a property *of this finding*, which is exactly the
mis-attribution D-01 forbids. Enclosing the number in its own titled, bordered region whose first
line names a *theme* is the visual mechanism that makes the scope of the number unambiguous.

**Required vertical order (top to bottom), no reordering:**

1. Section label: `SCORE-LIFT ATTRIBUTION` (Label, 12/600, `text-muted-foreground uppercase`, `mb-1`).
2. Theme line: `Remediation theme: {theme_title}` (Body 14/400; the theme title in `font-semibold`, the `Remediation theme:` prefix in `text-muted-foreground`).
3. Lift sentence: one `<p>` containing `+{N}` as a `<span>` at Display/accent, immediately followed — **inside the same paragraph** — by ` pts when all {theme_finding_count} findings in this theme are resolved` at Body/`text-muted-foreground`.
4. Position line: `This finding is {finding_position} of {theme_finding_count} in the theme · {theme_closed_count} of {theme_finding_count} verified closed` (Body 14/400, `text-muted-foreground`).
5. Disclaimer: `Info` icon (`h-3 w-3`, `text-muted-foreground`, `aria-hidden="true"`) + the mandatory disclaimer string, in a `flex items-start gap-1` row at 12px/400 `text-muted-foreground`.

**Invariant (single-node condition).** The number and its condition clause must live in one
paragraph element, in one continuous inline flow. No `<br>`, no grid cell, no sibling `<div>`
between them. A reviewer can check this mechanically: the `textContent` of the element containing
`+{N}` must also contain the substring `when all`. This is the one rule that survives every
downstream change — responsive reflow, copy-paste into a ticket, a screen reader reading the
paragraph as a unit, or a future card-layout refactor — and it is why the number is not allowed to
be a standalone stat.

**Invariant (disclaimer co-presence).** If and only if a lift number renders, the disclaimer
renders. Two assertions: (a) no DOM state exists where `+` + digits appears in the attribution
panel without the disclaimer string also present; (b) the disclaimer is not rendered in any of the
honest-absence states (there is nothing to disclaim).

**Forbidden visuals in this block** (each fabricates or implies a per-finding number):

- Division of any kind. `theme_score_lift / theme_finding_count` must not appear in the source, in any form, including as an intermediate variable, a comment-documented "approximate share", or a tooltip. Per D-01, a reviewer finding a division here treats it as a **blocker**, not a warning.
- A progress bar, segmented bar, donut, or any geometry whose segments map to constituent findings — it visually asserts that each finding is 1/N of the lift.
- A gauge, arrow, delta sparkline, or before/after pair.
- A table of constituent findings with a per-row points column.
- The position fraction (`1 of 8`) styled at Display size or in accent color — it is a *count*, and making it numerically prominent invites reading `1/8 × 4 pts`.
- `theme_score_lift` rendered anywhere else in the drawer (header, badge row, title) — exactly one occurrence per drawer.

---

## Number Formatting Contract

**Rule:** every score/lift numeric the drawer renders goes through
`formatScoreNumber()` imported from `@/lib/utils` — whole numbers render with zero decimals (`4`),
fractional values with exactly one decimal (`4.2`). The implementer **must not** re-derive this:
no `.toFixed(…)`, no `Math.round`, no `Number.prototype.toLocaleString`, no `String(n)`, and no
raw template interpolation (`+{theme_score_lift} pts`) at any call site.

This is not a style preference. Phase 201 shipped raw interpolation on this exact numeric family
(201-UI-C2/E1), which was invisible only because the backend happened to emit pre-rounded integers
(`quirk/intelligence/score_lift.py:164` does `int(delta)`); the remediation was to centralise
formatting in `formatScoreNumber()` so `roadmap.tsx` and `print.tsx` could not diverge. A third
surface re-deriving the rule re-opens that divergence.

**Applies to:** `theme_score_lift` (the only score-family numeric in this drawer).

**Does NOT apply to:** `finding_position`, `theme_finding_count`, `theme_closed_count`. These are
integer cardinal counts by construction (`item_progress()` returns `len(rows)` and a `sum(...)` of
booleans) and render raw. Do not wrap counts in `formatScoreNumber()` — doing so would imply they
can be fractional, which they cannot.

**Required regression coverage:** a test that feeds a fractional `theme_score_lift` (e.g. `4.27`)
through the drawer and asserts the rendered string is exactly `+4.3 pts when all 8 findings in this
theme are resolved` — the branch 201 left untested in both directions (201-UI-E5).

---

## Loading & Error Contract

The fetch is lazy (D-02): the drawer opens before data arrives. **The drawer must never be blank and
must never show a fabricated value while loading.**

**Immediately on open (zero-latency, from the row data already in hand):** the `SheetTitle`
(finding title), the severity badge, the `host:port` line, and the existing `description` /
`remediation` / `quantum_risk` sections all render from `FindingItem` with no skeleton — they are
already loaded. Only the narrative and attribution sections are pending.

**Loading treatment:** a `role="status"` wrapper containing `<span className="sr-only">Loading
storyline…</span>` plus visual `Skeleton` bars (reuse `@/components/ui/skeleton`, matching
`findings.skeleton.tsx`'s idiom):

- Narrative section: 3 bars — `h-4 w-full`, `h-4 w-full`, `h-4 w-3/4` (an `h-4` = 16px bar matches Body's 14px/1.5 ≈ 21px line box closely enough and is the existing `Skeleton` row height family; `space-y-2` between bars).
- Attribution section: the bordered sub-panel shell renders with its real `SCORE-LIFT ATTRIBUTION` label, containing one `h-4 w-40` bar and one `h-4 w-full` bar.

Rendering the real section labels over skeleton bars (rather than skeletonising the labels too)
keeps the drawer's structure stable across the load, so nothing reflows when data lands — and the
skeleton can never be mistaken for a rendered zero.

**Error treatment:** on fetch failure, replace both pending sections with the fixed error string
plus a `Retry` button (exact copy in the Copywriting Contract). Requirements:

- `role="status"` on the error container (it is an informational state, not a page-level alert — `role="alert"` would interrupt a screen reader mid-drawer and over-escalates a non-destructive read failure).
- Muted text, not destructive tint (see Color).
- `Retry` re-issues the same `GET /api/findings/{id}/storyline` without closing the drawer and without losing focus position; focus stays on the `Retry` button while it is re-fetching (`disabled` + `aria-busy="true"` during the in-flight retry so the button is not clicked twice).
- **Never** a silent empty panel, and **never** an error that falls through to the honest-absence copy — a failed fetch and a genuinely absent catalog entry are different facts and must read differently. (This is a deliberate divergence from 201's posture, where a null was correctly indistinguishable from a computation failure because the firewall required it; here the client knows the request failed, so collapsing the two would discard information the client actually has.)
- No retry count, no backoff text, no stack trace, no URL, no HTTP status in the user-visible string.

**Close-while-loading:** closing the drawer mid-fetch must not leave a pending state write against
an unmounted component, and re-opening the same row re-issues the fetch rather than showing a
stale prior finding's narrative. Observable contract: the drawer never displays narrative belonging
to a different finding than the one named in its own `SheetTitle`.

---

## Focus & Keyboard Contract

D-04's a11y capture asserts these; each is named so it is individually testable.

**F1 — Trigger.** A new rightmost table column with header `Storyline`, whose cell contains
`<Button variant="ghost" size="sm">` labeled `Storyline`, with `aria-haspopup="dialog"` and
`aria-label={`Open storyline for ${title} at ${host}:${port}`}`. This is required because the
current open affordance is `onClick` on a `<TableRow>` (`findings.tsx:190-193`) — a non-focusable,
non-keyboard-operable element, so today the drawer is unreachable by keyboard at all. The button is
the contract's trigger.

**F2 — Row click preserved.** The existing row `onClick` stays as a redundant pointer affordance.
Because the Sheet is state-controlled (no `SheetTrigger`), Radix's FocusScope restores focus to
whatever was focused before open — which for a mouse click on the row is `<body>`, not the row. So
a row-click open must programmatically focus that row's `Storyline` button before setting open
state (or capture its ref and restore explicitly in `onCloseAutoFocus`). Either implementation is
acceptable; the assertion in F6 is what must hold.

**F3 — Focus placement on open.** Focus moves inside the `SheetContent`. Initial focus lands on the
Sheet's built-in Close (X) button (`sheet.tsx:65`, the first tabbable descendant) — this is the
accepted default; do **not** add `autoFocus` to a narrative paragraph or to the `Retry` button.
Assertion: after open, `document.activeElement` is inside the `SheetContent` and has accessible
name `Close`.

**F4 — Focus trap.** Tab and Shift+Tab cycle only within the `SheetContent`; no focus reaches the
findings table, filters, or pagination while the drawer is open. Assertion: from the last tabbable
element in the drawer, Tab returns to the first tabbable element in the drawer.

**F5 — Esc closes.** Escape closes the drawer. Assertion: after `Escape`, the `SheetContent` is
removed from the DOM (Radix unmounts on close) and F6 holds.

**F6 — Focus returns to the triggering row.** On close by **any** means — Escape, the Close button,
or an overlay click — focus returns to the exact `Storyline` button of the row that opened the
drawer. Assertion: `document.activeElement.getAttribute('aria-label')` equals `Open storyline for
{that row's title} at {host}:{port}`. Must hold for a keyboard-initiated open and a mouse-initiated
open (F2), and for all three close paths.

**F7 — Overlay click closes** (Radix default, not to be disabled), with F6 holding.

**F8 — Accessible name and description are both wired.** `SheetContent` must render a
`SheetDescription` (already exported from `sheet.tsx:115`, currently unused by `findings.tsx`).
Content: the finding's `host:port` and protocol as the drawer's accessible description. Two reasons,
both concrete:

1. `@radix-ui/react-dialog` ^1.1.15 emits a dev console warning when a `Dialog.Content` has no description. The a11y harness (`run-a11y.mjs`) fails on **any** console output not matched by `tests/console-allowlist.json`, which today contains exactly one entry (a recharts `defaultProps` warning). Opening the drawer during the capture would therefore fail the gate on a console message. **Do not add an allowlist entry to suppress it** — render the description, which is the correct fix and the a11y win the warning exists to prompt.
2. The severity/host line already exists in the drawer body; wiring it (or an equivalent) as the dialog description costs no new visual surface.

**F9 — No new focusable element outside the trap.** The attribution block and narrative are static
text; no new tooltip-only, hover-only, or `title`-attribute-only information is introduced (a
hover-only disclosure would be unreachable by keyboard and would violate the Copywriting
Contract's ban on putting the lift condition in a tooltip).

**F10 — Harness wiring (D-04).** The capture extends the existing harness structurally: `routes.json`
gains a findings-drawer entry, or the harness gains an optional per-route interaction step, opening
the drawer before running `AxePuppeteer` and producing its own variant-aware baseline file
(`baseline-{slug}-{variant}.json`) per the harness's existing D-15/D-16 convention. No new runner,
no new tooling, `@axe-core/puppeteer` stays pinned at `4.11.3` (`pinned-deps.test.ts` guards this).
Axe violations in the opened-drawer state are baselined at exact integer counts with no tolerance
band, per the harness's D-06 posture.

---

## Type Contract

Every nullable numeric and nullable string crossing the API boundary for this feature is declared
**required-and-nullable** (`field: T | null`), never optional (`field?: T`). 201-UI-E2 raised exactly
this: `?:` admits `undefined` in addition to `null`, so the type system stops being able to enforce
the honest-absence contract and a missing field silently becomes a third state.

**New TS interface in `src/dashboard/src/types/api.ts`** (field names are Claude's discretion per
D-01's discretion grant; the *shape rule* is not):

```ts
export interface FindingStoryline {
  finding_id: number
  narrative: string | null
  quantum_impact: string | null
  remediation_guidance: string | null
  theme_slug: string | null
  theme_title: string | null
  theme_score_lift: number | null
  theme_finding_count: number | null
  theme_closed_count: number | null
  finding_position: number | null
}
```

**No `?:` anywhere in this interface.** Any field the backend may omit must be emitted as explicit
`null` by the Pydantic response model so the TS type is honest. The Pydantic mirror uses
`Optional[...] = None` with the field always serialised (not `exclude_none`).

**Required change to the existing type:** `FindingItem.id` is currently `id?: number`
(`types/api.ts:46`). It becomes `id: number | null`. It is the fetch key for the entire feature, so
an `undefined`-vs-`null` ambiguity here is the difference between "no id" and "field absent" at the
exact point the trigger's disabled-state decision is made (see the no-stable-id honest-absence
row). Existing test fixtures that construct `FindingItem` literals must add `id`. This is a
deliberate, in-scope correction of the 201-UI-E2 class, not scope creep.

**Guard style:** all null checks use `!= null` (catches both `null` and `undefined` during the
transition), matching the existing convention at `roadmap.tsx:307` and `print.tsx:204`. Do not use
truthiness (`if (theme_score_lift)`) on any numeric — it would treat a legitimate `0` as absence,
which is the inverse of this codebase's contract.

---

## Responsive Contract

**Drawer width.** Replace the inline `style={{ width: 480 }}` at `findings.tsx:219` with utility
classes on `SheetContent`: `className="w-full sm:w-[480px] sm:max-w-[480px]"`. The inline style
currently overrides `sheetVariants`' `w-3/4 sm:max-w-sm`, so on a 375px-wide viewport the drawer is
480px and overflows horizontally. With the classes above: below the `sm` breakpoint (640px) the
drawer is full-width; at and above `sm` it is a fixed 480px right-side panel. `480` is a multiple of
4 and is the only width literal this phase introduces.

**Vertical scrolling.** `SheetContent` becomes `flex flex-col` with:

- `SheetHeader` (title + description + the Close button) fixed at the top, not scrolling.
- The body region `flex-1 overflow-y-auto` with `min-h-0`, holding the badge row, Description,
  Remediation, Quantum Context, Storyline, and the attribution panel.

Long narrative scrolls **inside that body region only**. The page body must not scroll while the
drawer is open (Radix already applies `overflow: hidden` to `document.body` via the Dialog
primitive — do not defeat it), and the findings table behind the overlay must not scroll.

**Horizontal overflow.** Zero horizontal scrolling at any viewport width. Long single tokens
(hostnames, URLs in narrative text) use `break-words`; the attribution panel's sentences wrap
normally. **No table, no `<pre>`, no fixed-width grid inside the drawer** — the position data is a
sentence (`This finding is 1 of 8 in the theme · …`), not a table, specifically so there is no wide
content to horizontally scroll and no per-row points column to invite the forbidden per-finding
reading.

**Narrow-viewport wrapping.** The attribution lift sentence must wrap gracefully with the accented
`+N` glyph and its condition clause on different visual lines — which is fine, and is why the
single-node invariant is specified as a DOM-structure rule (one paragraph) rather than a visual
one-line rule. Verify at 375px that the `+N` and `when all N findings…` remain in the same
paragraph.

**Touch targets.** The `Storyline` trigger at `size="sm"` (`h-8` = 32px in the vendored `Button`)
is below the 44px comfortable touch minimum. The dashboard is a desktop operator surface (the
findings table's 7 existing columns, filter row, and pagination already assume a wide pointer-driven
viewport) and `size="sm"` matches the existing pagination buttons at `findings.tsx:211-212`, so
consistency wins here. **Assumption — see Assumptions.**

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none new — reuses already-vendored `Sheet`, `Badge`, `Button`, `Skeleton`, `Table` (D-03 forbids adding `vaul` or any other drawer library) | not required |
| third-party | none | not applicable — no third-party registry declared, so the vetting gate has nothing to vet |

No `npx shadcn view` run was needed: zero new blocks, zero third-party registries (verified
2026-09-12 — no `components.json` exists project-wide, so there is no registry configuration at all).

---

## Assumptions (non-interactive session — what would change each)

| # | Assumption | Basis | What would change it |
|---|------------|-------|---------------------|
| A1 | The storyline extends the **existing** finding-detail Sheet rather than adding a second drawer | `findings.tsx:218-254` already renders a detail Sheet opened by row click; D-03 says reuse the Sheet; two drawers on one table would be a worse surface | Operator preferring a dedicated storyline drawer separate from the detail panel — would duplicate the focus contract and need its own a11y baseline |
| A2 | A dedicated focusable `Storyline` column/button is added, rather than making the whole `TableRow` keyboard-operable | success criterion 4 requires a real focus contract, and `<TableRow onClick>` has no keyboard path; a `role="button" tabIndex=0` row fights the table's own semantics and makes the 7 cells one giant target | Operator preferring a keyboard-operable row (then F1/F6's assertions re-target the row element, and the trigger column is dropped) |
| A3 | `Storyline` button at `size="sm"` (32px) is acceptable despite being under 44px | matches existing pagination buttons (`findings.tsx:211-212`); desktop operator surface | A mobile/tablet target being declared in scope — then `size="default"` (36px) plus a 44px touch-area pseudo-element, or a larger row affordance |
| A4 | `finding_position` is derivable server-side (a stable index of this finding within its theme's constituent fingerprint rows) | D-01 explicitly requires `"1 of 8"`, and `item_progress()` queries the constituent rows that make the index expressible | If no stable ordering exists, the position line is suppressed (use the counts-unavailable absence row) rather than inventing an order — do **not** fall back to an arbitrary index |
| A5 | A failed fetch reads differently from honest absence | the client knows the request failed; collapsing them discards real information. Diverges deliberately from 201's firewall posture, where the client could not know | Operator preferring total indistinguishability — then the error state reuses the honest-absence copy and the `Retry` button is dropped |
| A6 | The drawer renders at most one narrative block from the catalogs; if the catalogs yield several fields (impact vs. remediation guidance), each gets its own Label-headed section in the existing `findings.tsx` section idiom | D-05 sources narrative from two catalogs (`ALGO_IMPACT_MAP`, `REMEDIATION_CATALOG`), so up to two text bodies are plausible | Catalogs yielding a different field count — section count follows, the contract (Label + Body paragraph, md gaps) does not change |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
