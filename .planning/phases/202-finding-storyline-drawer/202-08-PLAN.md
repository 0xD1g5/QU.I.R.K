---
phase: 202-finding-storyline-drawer
plan: 08
type: execute
wave: 5
depends_on: ["202-01", "202-02", "202-03", "202-04", "202-05", "202-06", "202-07"]
files_modified:
  - docs/report-interpretation.md
  - docs/operators-guide.md
  - docs/UAT-SERIES.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Report-Interpretation.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Operators-Guide.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-202-Finding-Storyline-Drawer.md
  - .planning/phases/202-finding-storyline-drawer/202-VALIDATION.md
  - .planning/todos/pending
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [STORY-01, STORY-02]

must_haves:
  truths:
    - "An operator reading docs/report-interpretation.md learns what the drawer's theme-framed attribution means: that the number is the THEME's total lift conditioned on resolving all its constituent findings, that it is NOT this finding's individual contribution, and that no per-finding share exists or is computable (D-01)"
    - "The docs state that most findings will show no catalog narrative, and that this is faithful consistency with the deliverable rather than a gap — the report has no narrative for those classes either (D-07)"
    - "The docs state that a finding belonging to more than one remediation theme displays only the specific theme and never the severity catch-all, and that the drawer deliberately does not hint at the other theme (D-08)"
    - "docs/UAT-SERIES.md carries a Series 202 whose every case has exactly one checked Result box, keeping the zero-undispositioned gate green (UATREC-04)"
    - "The Obsidian vault counterparts of every touched doc are re-synced in the same phase (LIVE-03) and a Phase 202 note exists with status complete"
    - "202-VALIDATION.md is closed green (nyquist_compliant: true, wave_0_complete: true, zero non-green rows) BEFORE or in the same commit as the .planning/ROADMAP.md change, so the pre-commit hook does not block"
    - "The `FindingItem.id` non-uniqueness trap and the D-08 one-theme simplification are filed as backlog todos rather than left in a SUMMARY nobody greps (D-06, D-08)"
    - "Every checkbox change is a hand text edit with no mutating GSD verb invoked, and the ROADMAP phase heading is left for the orchestrator because ARTIFACT-01 gates it on 202-VERIFICATION.md"
  artifacts:
    - path: "docs/report-interpretation.md"
      provides: "storyline drawer interpretation: theme framing, honest absence, one-theme rule"
      contains: "not this finding's individual contribution"
    - path: "docs/UAT-SERIES.md"
      provides: "Series 202 covering the trigger, narrative sourcing, absence, attribution, and a11y"
      contains: "Series 202"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-202-Finding-Storyline-Drawer.md"
      provides: "vault phase note with per-plan What Was Built"
  key_links:
    - from: "docs/report-interpretation.md"
      to: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Report-Interpretation.md"
      via: "vault re-sync of the same content below standard frontmatter"
      pattern: "Report-Interpretation"
    - from: ".planning/phases/202-finding-storyline-drawer/202-VALIDATION.md"
      to: ".planning/ROADMAP.md"
      via: "validation closed green no later than the roadmap commit"
      pattern: "nyquist_compliant"
---

<objective>
Discharge CLAUDE.md's Per-Phase Documentation Checklist and Mandatory Phase Completion Steps for
Phase 202, file the two backlog items this phase's decisions generated, and close the phase's
planning artifacts by hand.

Purpose: this phase triggers the "new dashboard tab / UI feature" checklist row (and, latently, the
"new API endpoint" row). The drawer's whole reason for existing is to explain a number honestly, so
it is not complete until an operator can learn from the docs what the number means, what an absence
means, and why only one theme shows. Two of this phase's decisions also need to outlive it:
`FindingItem.id`'s non-uniqueness is a latent trap for ANY future per-finding feature, and D-08's
single-theme display is a deliberate simplification a future phase may need to revisit.

Note on scope: `gsd-ui-review` and `gsd-code-review` are the ORCHESTRATOR's job (autonomous workflow
runs them after execution routing, before phase close, because `202-UI-SPEC.md` exists). This plan
does not run them.

Output: two updated docs, UAT Series 202, four vault writes, two filed todos, a closed
202-VALIDATION.md, and the hand-flipped checkboxes this plan is permitted to flip.

Hard constraints:
- **Do NOT invoke `phase.complete`, `milestone.complete`, `requirements mark-complete`, or any
  mutating `state.*` / `roadmap.*` GSD verb, via `gsd-sdk` OR `gsd-tools.cjs`.** They write
  syntactically valid WRONG values on this machine — `phase.complete` marked a phase complete at 5/7
  plans with an impossible `completed_plans: 142`, and both known corruption signatures read CLEAN on
  it. Every checkbox is a hand text edit. **Do not touch `.planning/STATE.md` at all** — it is
  orchestrator-owned and md5-tracked.
- **`requirements mark-complete` is specifically banned** even though STORY-01/STORY-02 are
  single-phase: the verb has no per-phase granularity and has falsely over-flipped multi-phase
  requirements twice (168-08, 169-02).
- **Do NOT flip the `### Phase 202` heading or the phase-list checkbox in `.planning/ROADMAP.md`.**
  `scripts/verify_phase_gates.py` ARTIFACT-01 blocks that without a `202-VERIFICATION.md`, which the
  orchestrator's verifier produces after all plans finish. See Task 3 for exactly which boxes this
  plan may and may not flip.
- **Close `202-VALIDATION.md` green BEFORE (or in the same commit as) any `.planning/ROADMAP.md`
  change** — the pre-commit hook blocks ROADMAP commits over a non-green validation file (recurred on
  phases 157/158/159).
- **Do NOT write a verification command that greps `202-VALIDATION.md` for the pending glyph.** A row
  containing that glyph in its own command text can never reach zero. Verify by frontmatter key plus a
  count of green rows.
- **Never check a UAT Result box just to satisfy the gate.** `GAP — no substitute coverage` is a
  valid, passing, honest disposition; a fabricated pytest citation is not.
- **No version bump this phase** — leave the `**Version:**` line in `docs/UAT-SERIES.md` alone.
- Use plain `git add` + `git commit`; never the gsd `commit --files` wrapper. A NEW file under
  gitignored `.planning/` needs `git add -f`; an ALREADY-TRACKED one stages fine with plain `git add`
  despite printing an "ignored paths" hint — do not read that hint as failure, check
  `git status --short`. `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` are tracked.
- `.venv/bin/python`, never bare `python`.
- Large vault files must be written DIRECTLY to the vault filesystem, never via the obsidian CLI
  `content=` parameter (shell expansion limits). Always `vault="Digs"` for any CLI call.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/202-finding-storyline-drawer/202-CONTEXT.md
@.planning/phases/202-finding-storyline-drawer/202-UI-SPEC.md
@.planning/phases/202-finding-storyline-drawer/202-01-SUMMARY.md
@.planning/phases/202-finding-storyline-drawer/202-02-SUMMARY.md
@.planning/phases/202-finding-storyline-drawer/202-03-SUMMARY.md
@.planning/phases/202-finding-storyline-drawer/202-04-SUMMARY.md
@.planning/phases/202-finding-storyline-drawer/202-05-SUMMARY.md
@.planning/phases/202-finding-storyline-drawer/202-06-SUMMARY.md
@.planning/phases/202-finding-storyline-drawer/202-07-SUMMARY.md

<interfaces>
<!-- Source -> vault mapping. Vault name is always "Digs". Write large files directly to the vault
     filesystem; never via the obsidian CLI `content=` parameter. -->
docs/report-interpretation.md -> /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Report-Interpretation.md
docs/operators-guide.md       -> /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Operators-Guide.md
docs/UAT-SERIES.md            -> /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md
phase note                    -> /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-202-Finding-Storyline-Drawer.md

Vault frontmatter keys: project, type, status, source, updated.

<!-- UAT gate grammar (all three burned Phase 188) -->
- `**Result:**` must be ONE physical line with exactly one checked box (PASS / FAIL / SKIP)
- DEFERRED pytest citations need FULL class-scoped node IDs and must be `--collect-only` resolvable
- vitest citations from multi-test files cannot pass the zero-skip execution leg -> use GAP honestly

Gate commands:
  .venv/bin/python -m pytest -q tests/test_uat_zero_undispositioned_gate.py \
      tests/test_uat_disposition_integrity.py tests/test_error_codes_freshness.py

<!-- Locked copy to document verbatim (from 202-UI-SPEC.md) -->
  +{N} pts when all {N} findings in this theme are resolved
  This is the theme's total lift, not this finding's individual contribution. Resolving this finding
  alone does not yield the full amount.
  No catalog narrative exists for this finding type yet — the Description and Remediation fields
  above are the available guidance.
  Not mapped to a remediation theme — no score-lift attribution exists for this finding.

The last series currently in docs/UAT-SERIES.md is `## Series 201` (around line 27226); append after it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the storyline drawer — theme framing, honest absence, and the one-theme rule</name>
  <files>docs/report-interpretation.md, docs/operators-guide.md</files>
  <read_first>
    - `docs/report-interpretation.md` (the score-lift subsection Phase 201 added — the new passage is its dashboard-drawer sibling and must match the house voice and not contradict it)
    - `docs/operators-guide.md` (where the dashboard surfaces and the findings table are described today)
    - all seven 202-NN-SUMMARY.md files — **document what SHIPPED, not what was planned.** In particular: 202-01's reachability census (which finding classes can reach a specific theme, which only the catch-all, which none), 202-05's catch-all-only interpretation, and whether `finding_position` shipped as always-null
  </read_first>
  <action>
`docs/report-interpretation.md`: add a storyline-drawer subsection adjacent to the existing score-lift
passage, covering, in the document's existing voice:

- What the drawer is and where it opens from (the findings table's `Storyline` column, in place, with
  no navigation).
- **The theme framing, which is the passage that matters.** The number shown is the owning remediation
  THEME's total lift, conditioned on resolving every constituent finding in that theme — quote the
  lift sentence and the disclaimer verbatim. State explicitly that it is NOT this finding's individual
  contribution, that resolving this finding alone does not yield the full amount, and that **no
  per-finding share is shown because none exists**: lifts are not linear in constituent count, so
  dividing the theme's lift by its findings would fabricate a number the scorer never computed and the
  shares would not sum back to the theme total. Operator language, not code.
- What the closure sentence means (`N of N findings in this theme are verified closed`) and that it is
  the same closure state the roadmap surface reports.
- **Why most findings show no narrative, framed as consistency rather than as a gap.** The narrative
  catalogs are keyed by cryptographic-algorithm keyword, so RSA/ECDSA-key findings and named-weak-cipher
  findings get narrative while the highest-volume classes (plaintext HTTP, legacy TLS,
  expired/expiring certificates, self-signed, untrusted CA) do not. The client report has no narrative
  for those classes either — the drawer showing an honest absence IS the deliverable and the dashboard
  telling the same story for the same finding. Quote the absence string verbatim. Name the classes as
  202-01's census actually found them, not as this plan guesses.
- **The one-theme rule.** A finding can constitute more than one theme at once; the drawer shows the
  specific title-based theme and never the `high-impact-findings` severity catch-all, and it
  deliberately does not mention the other theme — two conditioned lift numbers side by side would make
  the non-additivity point substantially harder to convey. If 202-05's SUMMARY records that a
  catch-all-only finding DOES display the catch-all theme, document that too, in one sentence, because
  an operator who reads the one-theme rule and then sees `Triage high-impact findings` needs it to
  make sense.
- What a `Not mapped to a remediation theme` finding means (quote it): no persisted remediation theme
  claims this finding in this scan, so no lift can be attributed.
- That the drawer is an interactive dashboard affordance and is NOT part of the PDF/DOCX/HTML report —
  state this so a reader does not look for it in a deliverable.

`docs/operators-guide.md`: a short operator-facing passage — how to open it (including that the
`Storyline` button is keyboard-operable and that Escape closes it and returns focus to the row), the
three-sentence version of the theme framing, that a blank narrative is normal, that a disabled
`Storyline` button means the finding has no stable identifier in this scan, and a pointer to
`docs/report-interpretation.md` for the full explanation. Cite Phase 202 / STORY-01 / STORY-02 the way
neighbouring passages cite their phases.

**`docs/api-reference.md` row:** this phase adds a new API endpoint
(`GET /api/findings/{id}/storyline`), which the checklist maps to `docs/api-reference.md`. That file
does not exist. Do not create it as a one-endpoint stub — record the deferral explicitly in the
SUMMARY, naming the endpoint, its required `title` query parameter, and its response fields, so
whoever creates that file has the entry ready.

Do not invent behaviour. Every documented claim must match what the plan SUMMARYs record as shipped;
list each claim beside its source SUMMARY in this plan's SUMMARY.
  </action>
  <verify>
    <automated>grep -cF "not this finding's individual contribution" docs/report-interpretation.md && grep -cF "No catalog narrative exists for this finding type yet" docs/report-interpretation.md && grep -c "high-impact-findings" docs/report-interpretation.md && grep -ci "storyline" docs/operators-guide.md && ! grep -n "Version:" docs/report-interpretation.md | grep -q "5\.2[0-9]" ; echo DOCS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/report-interpretation.md` contains: the verbatim lift sentence and disclaimer, the explicit no-per-finding-share-exists explanation with the non-linearity reason, the closure-sentence meaning, the honest-absence-as-consistency passage with the finding classes named from 202-01's census, the one-theme rule plus the catch-all-only case if it shipped, the A1 meaning, and the not-in-the-report statement
    - `docs/operators-guide.md` contains the brief version including the keyboard path, the disabled-trigger meaning, and the pointer
    - Every claim is traceable to a plan SUMMARY; the traceability list is in this plan's SUMMARY
    - The `docs/api-reference.md` deferral is recorded with the endpoint's full signature
    - No version string is changed in either file
  </acceptance_criteria>
  <done>An operator can learn what the number means, why it is not theirs to divide, why most findings show no story, and why only one theme appears.</done>
</task>

<task type="auto">
  <name>Task 2: UAT Series 202 with honest dispositions</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - `docs/UAT-SERIES.md` Series 201 block (copy its case structure verbatim: ID / Title / Maps to / What to test / Steps / Pass Criteria / Falsifiability / Result / Date / Tester / Notes; also the document header block you must update)
    - `tests/test_uat_zero_undispositioned_gate.py` (module docstring — what the gate parses and why GAP is a passing disposition)
    - `tests/test_uat_disposition_integrity.py` (module docstring — DEFERRED citation conventions, the substitute-proof execution leg, and why vitest citations cannot pass it)
    - all seven 202-NN-SUMMARY.md files (the evidence you are dispositioning against), especially 202-06's named jsdom limitations and 202-07's operator approval
  </read_first>
  <action>
Append `## Series 202: Finding Storyline Drawer (Phase 202 — v5.23)` after Series 201, with an intro
paragraph naming STORY-01 and STORY-02.

One case per verifiable behaviour, following the existing template exactly:

- `UAT-202-01` — the findings table has a `Storyline` column whose button opens a drawer in place,
  without leaving the findings view. Maps to STORY-01.
- `UAT-202-02` — the trigger is keyboard-reachable and keyboard-operable; Escape closes the drawer and
  focus returns to the triggering row's button. Maps to STORY-01 / success criterion 4.
- `UAT-202-03` — a finding whose description carries an algorithm keyword shows narrative text
  byte-identical to the report catalog's, proving no fourth generator. Maps to STORY-01.
- `UAT-202-04` — a plaintext-HTTP finding shows the honest-absence string, not an empty panel and not
  invented text, and the attribution block is still fully populated. Maps to STORY-01 / D-07.
- `UAT-202-05` — requesting a storyline for one of two findings sharing a `CryptoEndpoint.id` returns
  that finding's storyline, not the other's. Maps to STORY-01 / D-06.
- `UAT-202-06` — the attribution block shows the theme name, the conditioned lift sentence, the
  closure sentence, and the disclaimer; no per-finding share appears anywhere. Maps to STORY-02.
- `UAT-202-07` — the drawer's lift number equals the roadmap surface's number for the same theme and
  the same scan. Maps to STORY-02.
- `UAT-202-08` — a finding in two themes shows the specific theme and never `Triage high-impact
  findings`. Maps to STORY-02 / D-08.
- `UAT-202-09` — a theme with no modelable lift shows no number and says why; nothing renders as `0`,
  `N/A`, or a dash. Maps to STORY-02.
- `UAT-202-10` — a scan whose DB predates the remediation fingerprint table yields honest absence and
  a working drawer, not an error. Maps to STORY-02.
- `UAT-202-11` — a finding with no stable identifier renders a disabled trigger with its explanatory
  tooltip, and the drawer does not open. Maps to STORY-01.
- `UAT-202-12` — the opened drawer passes the a11y baseline capture, and the `empty`/`loading` variants
  skip the interaction explicitly. Maps to success criterion 4.

Disposition each case honestly against the SUMMARY evidence. Cases with automated backend coverage get
SKIP with `DEFERRED — covered by <full class-scoped pytest node ID>` citing nodes in
`tests/test_dashboard_finding_storyline.py` or `tests/test_finding_title_bridge.py`, plus the
originating SUMMARY. **Remember that vitest citations cannot pass the integrity gate's execution
leg** — so the cases whose only automated coverage is vitest (the trigger, the focus contract, the
attribution rendering, the disclaimer co-presence) cannot be DEFERRED to a vitest node. For each of
those, either record PASS citing 202-07's verbatim operator approval where that approval actually
covers the behaviour, or record `GAP — no substitute coverage` honestly. 202-06's SUMMARY names any
focus assertion jsdom could not make — those specifically must not be dispositioned as covered.

Every `**Result:**` line must be ONE physical line with exactly one box checked. Add `**Date:**` and
`**Tester:**` lines in the existing style. Update the document header's `**Last Updated:**` block to
today's date with a one-sentence description of the Series 202 addition, following the header's
running-log style. Do NOT change the `**Version:**` line.

Then run the UAT gates. If the integrity gate rejects a citation, fix the citation or downgrade to an
honest GAP — never add an allowlist entry and never check a box to make a gate pass.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_uat_zero_undispositioned_gate.py tests/test_uat_disposition_integrity.py tests/test_error_codes_freshness.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Series 202' docs/UAT-SERIES.md` returns 1 and `grep -c '^### UAT-202-' docs/UAT-SERIES.md` returns 12
    - Every `**Result:**` line in the Series 202 block has exactly one `[x]` on one physical line
    - The three gate tests exit 0
    - Every DEFERRED citation is confirmed collectible via `.venv/bin/python -m pytest --collect-only -q <node-id>`, with the transcripts in the SUMMARY
    - No case whose only automated coverage is vitest is DEFERRED to a vitest node; each is PASS-with-operator-evidence or an honest GAP
    - The header's `**Last Updated:**` shows today's date; `**Version:**` is unchanged
    - `git status --porcelain tests/` shows no modified gate file — no allowlist or gate-code change
  </acceptance_criteria>
  <done>Series 202 exists, every case is honestly dispositioned, and all three gates are green.</done>
</task>

<task type="auto">
  <name>Task 3: Vault sync, phase note, backlog todos, validation close-out, and the permitted checkbox flips</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Report-Interpretation.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Operators-Guide.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-202-Finding-Storyline-Drawer.md, .planning/phases/202-finding-storyline-drawer/202-VALIDATION.md, .planning/todos/pending, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `CLAUDE.md` sections "Obsidian vault sync (LIVE-03)", "Mandatory Phase Completion Steps", "Chaos Lab Maintenance", and the `state.*` verb-integrity section's clause (f) standing gotchas
    - `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-201-Score-Lift-Roadmap-Re-frame.md` (the immediately-preceding phase note — copy its structure and frontmatter exactly)
    - `.planning/phases/201-score-lift-roadmap-re-frame/201-VALIDATION.md` (the structure you are authoring or closing for 202 — including its frontmatter keys and its Per-Task Verification Map and Sign-Off sections)
    - `.planning/phases/201-score-lift-roadmap-re-frame/201-08-SUMMARY.md` (the close-out conventions this task repeats, including the `tail -n +9` vault-diff offset rationale)
    - `.planning/todos/pending/` (the existing todo file format — match it; do not invent a new shape)
    - `scripts/verify_phase_gates.py` (ARTIFACT-01 specifically — confirm for yourself which artifact it requires before which checkbox, rather than trusting this plan's summary of it)
  </read_first>
  <action>
**Vault sync.** Write the three touched docs to the vault by writing DIRECTLY to the vault filesystem —
never via the obsidian CLI `content=` parameter. Prepend the standard frontmatter block (project
`QU.I.R.K.`, type `guide` or `reference`, status `active`, the repo-relative `source` path, `updated` =
today) above the source content, preserving that content byte-for-byte below the frontmatter.

**Phase note.** Write
`/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-202-Finding-Storyline-Drawer.md` with
frontmatter (`status: complete`, `type: phase`, `source`, `updated`) and sections: Goal, Requirements
Covered (STORY-01, STORY-02), Success Criteria (the four from the ROADMAP), What Was Built (one
subsection per plan 202-01 through 202-08, sourced from the SUMMARYs), and a `[[Roadmap]]` link. Three
claims are this phase's load-bearing ones and must appear explicitly: the title-bridge gate that
regenerates its site set from source, the division trip-wire's RED evidence, and the a11y capture's
three closed blindness mechanisms. Match the Phase 201 note's structure.

**Chaos lab.** This phase adds no Compose profile and no scanner signal. Confirm that explicitly
rather than assuming — grep the `expected_results_*.md` oracle files for any roadmap, theme, score, or
finding-title expectation the title bridge or the new endpoint could perturb, and record the finding
verbatim in the SUMMARY as "chaos lab checked, no oracle impact" or the contrary finding with the
offending file and line.

**Backlog todos.** File two pending todos in `.planning/todos/pending/`, matching the existing file
format:
1. `FindingItem.id` is not a unique finding identifier (it is `CryptoEndpoint.id`, shared across every
   finding derived from that endpoint). D-06 deliberately did NOT give findings a unique id because
   the blast radius is wider than Phase 202 needed. This is a latent trap for ANY future per-finding
   feature, not just this one. Cite `202-CONTEXT.md` D-06 and the `scan.py` emission sites.
2. The drawer displays ONE theme and does not disclose that a finding also belongs to another
   (D-08's deliberate simplification, recorded so a future phase can revisit it). Include the live
   figure from 202-05's evidence (how many fingerprints span 2+ themes in the scanned DB) and the
   documented trigger to revisit: an overlap that is NOT the severity catch-all. If 202-05's SUMMARY
   raised the catch-all-only interpretation as an open question, file that as a third todo with the
   affected finding classes named.

**Validation close-out — before touching `.planning/ROADMAP.md`.** If
`.planning/phases/202-finding-storyline-drawer/202-VALIDATION.md` does not exist, author it following
201-VALIDATION.md's structure (frontmatter, Test Infrastructure, Sampling Rate, Per-Task Verification
Map, Manual-Only rows, Sign-Off). Then fill or confirm one row per task across all eight plans,
confirm each row's Automated Command actually passed during this phase (re-run it if the evidence is
not already in a plan SUMMARY), then set that row's Status cell to the green check glyph and its File
Exists cell away from the Wave-0 marker. Set `nyquist_compliant: true` and `wave_0_complete: true`,
flip the Sign-Off checkboxes and `**Approval:**`, and record the Manual-Only rows — the drawer's visual
appearance and the a11y ledger review — as discharged by 202-07's approved operator walkthrough, citing
it, or as outstanding for human UAT. **If any row's command did NOT pass, mark that row red and
STOP**; do not proceed to the ROADMAP change over a red validation row.

Make these edits as plain text edits to the specific cells. Do NOT write a verification command that
greps this file for the pending glyph — a row containing that glyph in its own command text can never
reach zero. Verify by frontmatter key and by a count of green rows. If you must write that glyph into a
command at all, write it as a `⬜` escape.

**Checkbox flips — read this boundary carefully.** First check whether
`.planning/phases/202-finding-storyline-drawer/202-VERIFICATION.md` exists (the orchestrator's verifier
produces it after all plans finish; it may not exist yet when this plan runs).

Permitted unconditionally:
- `.planning/ROADMAP.md`: the Phase 202 **plan-list** checkboxes (`202-01-PLAN.md` … `202-08-PLAN.md`),
  the `**Plans**:` count, and the Progress table row (`202. Finding Storyline Drawer | 8/8 | …`).

NOT permitted by this plan, under any condition:
- The `### Phase 202` heading's or phase-list entry's `[ ]` -> `[x]`. ARTIFACT-01 gates it on
  `202-VERIFICATION.md`, and the orchestrator flips it after the verifier runs. Leave it and say so in
  the SUMMARY.

Conditional:
- `.planning/REQUIREMENTS.md` STORY-01 / STORY-02 checkboxes and their Traceability rows: flip them ONLY
  if `202-VERIFICATION.md` exists and records the phase as verified. If it does not exist yet, leave
  both unflipped and state plainly in the SUMMARY that they remain for the orchestrator's post-verification
  step — claiming a requirement complete before its verification exists is the `phase.complete`
  premature-completion failure reproduced by hand.

All flips are hand text edits. No `phase.complete`, no `milestone.complete`, no
`requirements mark-complete`, no mutating `state.*` / `roadmap.*` verb, from either install. Do not
touch `.planning/STATE.md`.

Commit with plain `git add` + `git commit`, with the `202-VALIDATION.md` change landing no later than
the `.planning/ROADMAP.md` change. New files under `.planning/` need `git add -f`; tracked ones stage
with plain `git add` despite the "ignored paths" hint — verify with `git status --short`, not by
reading that hint as failure.
  </action>
  <verify>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-202-Finding-Storyline-Drawer.md" && diff <(tail -n +9 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md") docs/UAT-SERIES.md > /dev/null && diff <(tail -n +9 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Report-Interpretation.md") docs/report-interpretation.md > /dev/null && diff <(tail -n +9 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Operators-Guide.md") docs/operators-guide.md > /dev/null && grep -c "nyquist_compliant: true" .planning/phases/202-finding-storyline-drawer/202-VALIDATION.md && ls .planning/todos/pending | wc -l && echo CLOSEOUT_OK</automated>
  </verify>
  <acceptance_criteria>
    - Each vault file's body below its frontmatter is byte-identical to its source. Verify with `diff <(tail -n +9 "<vault file>") <source>` — `+9`, not `+7`: the standard frontmatter block is 7 lines plus a blank separator, so the body starts at line 9. If you deviate from that block, recompute the offset with `awk '/^---$/{n++; next} n>=2' <vault file>` instead of hardcoding
    - All three vault doc files carry the five standard frontmatter keys with `updated` = today and `source` = the repo-relative path
    - `Phase-202-Finding-Storyline-Drawer.md` exists with `status: complete`, one "What Was Built" subsection per plan 202-01..202-08, names both STORY IDs, states the three load-bearing claims, and contains a `[[Roadmap]]` wikilink
    - Two (or three) backlog todos exist in `.planning/todos/pending/` in the existing file format, with the live multi-theme figure quoted from 202-05's evidence
    - The SUMMARY records the chaos-lab grep verbatim, command and output
    - `202-VALIDATION.md` frontmatter reads `nyquist_compliant: true` and `wave_0_complete: true`; its Per-Task Verification Map has one row per task across all eight plans with zero non-green Status cells (count green glyphs against the row count; do not grep for the pending glyph); the Manual-Only rows are explicitly dispositioned
    - The `202-VALIDATION.md` edit is committed BEFORE or with the `.planning/ROADMAP.md` change
    - `.planning/ROADMAP.md`'s Phase 202 plan list is checked and the Progress row reads 8/8; the `### Phase 202` heading / phase-list checkbox is **still unflipped**, with that stated in the SUMMARY
    - `.planning/REQUIREMENTS.md` is flipped only if `202-VERIFICATION.md` exists; either way the SUMMARY states which branch was taken and why
    - `git log --oneline -8` shows no commit authored by a `phase.complete` / `milestone.complete` / `requirements mark-complete` invocation; `git status --short .planning/STATE.md` is empty
    - Full suites run once: `.venv/bin/python -m pytest -q -m ""` failing-node SET compared against the empty pre-phase baseline (compare SETS, never raw counts — chaos-lab collection moves with Docker health), and `cd src/dashboard && npm run build && npm run lint && npm run test` green
  </acceptance_criteria>
  <done>Docs and vault are in sync, the phase note exists, the two decisions that outlive this phase are filed, 202-VALIDATION.md is closed green, and only the checkboxes this plan is permitted to flip are flipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation, UAT corpus, backlog ledger, vault files, and planning artifacts only. No code, no inputs, no endpoints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-202-34 | Repudiation | UAT corpus integrity — a checked box that does not reflect real coverage, or a vitest citation that cannot pass the integrity gate's execution leg | mitigate | Every DEFERRED citation resolves via `pytest --collect-only`; vitest-only behaviours take PASS-with-operator-evidence or an honest GAP; no allowlist or gate-code edit permitted; 202-06's named jsdom limitations explicitly may not be dispositioned as covered |
| T-202-35 | Tampering | `.planning/` corruption by mutating GSD state verbs (TOOL-01/04/05 defect class and the `phase.complete` semantic class) | mitigate | No mutating verb invoked from either install; all checkboxes hand-edited; `.planning/STATE.md` untouched and asserted clean by `git status` |
| T-202-36 | Repudiation | Docs presenting the theme lift as this finding's value, or an absent narrative as a product gap — misleading a client-facing reading | mitigate | Acceptance criteria require the verbatim disclaimer, the explicit no-per-finding-share-exists explanation with its non-linearity reason, and the absence-as-consistency framing with the classes named from 202-01's census |
| T-202-37 | Repudiation | Marking STORY-01/STORY-02 or the phase complete before `202-VERIFICATION.md` exists — the `phase.complete` premature-completion failure reproduced by hand | mitigate | The phase heading is never flipped by this plan; REQUIREMENTS flips are conditional on the verification artifact existing, with the branch taken stated in the SUMMARY |
| T-202-38 | Information disclosure | Vault sync copying content outside the QUIRK doc set | accept | Only the three named `docs/` files and one generated phase note are written, to the four paths in `<interfaces>` |
| T-202-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this plan |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q tests/test_uat_zero_undispositioned_gate.py tests/test_uat_disposition_integrity.py tests/test_error_codes_freshness.py` exits 0
- All three vault doc bodies match their sources byte-for-byte; the Phase 202 note exists with `status: complete`
- `202-VALIDATION.md` reads `nyquist_compliant: true` / `wave_0_complete: true` with zero non-green rows, committed no later than the ROADMAP change
- Two or three backlog todos exist in `.planning/todos/pending/`
- `.venv/bin/python -m pytest -q -m ""` failing-node SET matches the empty pre-phase baseline; `npm run build && npm run lint && npm run test` green
- No mutating GSD state verb was invoked; `.planning/STATE.md` untouched; the ROADMAP phase heading unflipped
</verification>

<success_criteria>
- The Per-Phase Documentation Checklist rows triggered by this phase (new dashboard UI feature; new API endpoint, deferred with the entry drafted) are actioned and synced to the vault
- UAT Series 202 exists with 12 honestly-dispositioned cases; all three gates green
- The `FindingItem.id` trap and the one-theme simplification are filed where a future phase will find them
- `202-VALIDATION.md` is closed green first; only the permitted checkboxes are flipped; no mutating verb was used
</success_criteria>

<output>
Create `.planning/phases/202-finding-storyline-drawer/202-08-SUMMARY.md` when done. Include the
claim-to-SUMMARY traceability list, the `docs/api-reference.md` deferral with the endpoint signature,
the chaos-lab grep output verbatim, the UAT gate results and every `--collect-only` transcript, the
list of vault files written, the filed todo filenames, which REQUIREMENTS branch was taken and why,
the explicit statement that the ROADMAP phase heading was left unflipped for the orchestrator, and the
full-suite failing-node SET comparison.
</output>
