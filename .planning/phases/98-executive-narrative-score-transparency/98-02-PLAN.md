---
phase: 98-executive-narrative-score-transparency
plan: 02
type: execute
wave: 2
depends_on: [98-01]
files_modified:
  - quirk/reports/writer.py
  - quirk/reports/executive.py
  - quirk/reports/html_renderer.py
  - quirk/reports/templates/report.html.j2
  - tests/test_exec_narrative_ordering.py
  - tests/test_congruence_guard.py
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-03, EXEC-04, TRANS-01, TRANS-02, TRANS-03]
nyquist_compliant: true

must_haves:
  truths:
    - "write_reports() builds one ExecContent and passes it to both build_exec_markdown() and render_html_report() before any file I/O; the congruence guard fires here so no contradictory report is ever written"
    - "CLI markdown opens with a Readiness Assessment narrative prose block BEFORE any finding table, and includes a Priority Business Risks section and effort/impact-labelled roadmap items"
    - "HTML report renders a .narrative-block before the score card, a .risks-list, the rollup-formula prose, and .priority-label spans on roadmap items"
    - "Score Decomposition + rollup are sourced from exec_content.subscores in HTML (D-07 extend, not rebuild)"
    - "Finding-derived text in new markdown cells is wrapped with md_cell; finding-derived text in new template content pipes through | sanitize"
  artifacts:
    - path: "quirk/reports/writer.py"
      provides: "build_exec_content() seam + guard before renderers; exec_content passed to both"
      contains: "build_exec_content"
    - path: "quirk/reports/executive.py"
      provides: "build_exec_markdown(..., exec_content=...) consuming the shared model; narrative moved before findings"
      contains: "exec_content"
    - path: "quirk/reports/html_renderer.py"
      provides: "render_html_report(..., exec_content=...) routing narrative/risks/roadmap/subscores from shared model"
      contains: "exec_content"
    - path: "quirk/reports/templates/report.html.j2"
      provides: ".narrative-block, .risks-list, .rollup-formula, .priority-label additions"
      contains: "narrative-block"
    - path: "tests/test_exec_narrative_ordering.py"
      provides: "Ordering + presence tests for CLI and HTML"
  key_links:
    - from: "quirk/reports/writer.py:write_reports"
      to: "build_exec_content"
      via: "called after score_raw/roadmap_raw computed, BEFORE the compat wrapper"
      pattern: "build_exec_content\\("
    - from: "quirk/reports/html_renderer.py"
      to: "exec_content.subscores"
      via: "template subscores context var"
      pattern: "exec_content"
    - from: "report.html.j2"
      to: "exec_content fields"
      via: "narrative-block / risks-list / priority-label rendering"
      pattern: "narrative-block|risks-list|priority-label"
---

<objective>
Wire the shared `ExecContent` model (from 98-01) into the orchestration seam and both render surfaces so all three surfaces (CLI markdown, HTML, PDF-via-HTML) carry identical executive content. Insert the congruence guard at build time, move the CLI narrative before finding tables, and add the HTML narrative/risks/rollup/priority elements per the UI-SPEC.

Purpose: Deliver EXEC-01/02/03/04 and TRANS-02/03 on the user-facing surfaces; satisfy D-03 (single source) and D-07 (extend score decomposition, don't rebuild).
Output: Modified writer/executive/html_renderer/template + ordering tests + the guard integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-executive-narrative-score-transparency/98-CONTEXT.md
@.planning/phases/98-executive-narrative-score-transparency/98-RESEARCH.md
@.planning/phases/98-executive-narrative-score-transparency/98-PATTERNS.md
@.planning/phases/98-executive-narrative-score-transparency/98-UI-SPEC.md
@.planning/phases/98-executive-narrative-score-transparency/98-01-SUMMARY.md
@quirk/reports/content_model.py

<interfaces>
<!-- From quirk/reports/content_model.py (98-01): ExecContent fields = narrative_lead, narrative_drivers, top_risks (list[RiskItem: risk_label, impact_sentence, severity]), roadmap_items (list[RoadmapItem: phase, title, why, owner_placeholder, timeframe, effort, impact, priority_score]), score_total, score_band, subscores, raw_sum, sev_counts. build_exec_content(score_raw, findings, roadmap_items). ReportCongruenceError(ValueError).
     CRITICAL key-shape (Pitfall 1): writer.py computes score_raw = compute_readiness_score(...) with key "score", THEN wraps it as score={"total": score_raw["score"], ...} at writer.py:~167. build_exec_content MUST be called with score_raw (canonical) BEFORE that compat wrapper.
     Existing signatures: build_exec_markdown(cfg, endpoints, findings) -> str (executive.py:111). render_html_report(path, cfg, endpoints, findings, score, conf, roadmap_items) -> None (html_renderer.py:145). Add exec_content keyword to both.
     HTML escaping: env.filters["sanitize"]=sanitize_scanner_text (html_renderer.py:158-162); autoescape active for .j2. md_cell from quirk/reports/_md_escape.py for GFM cells.
     CLI section reorder (RESEARCH "Required order after Phase 98"): narrative block at position 2 (after ## Executive Summary, before ## Quantum Readiness Score); Priority Business Risks after score decomposition; remove the old ## Interpretation section (Pitfall 6 — subsume into narrative_drivers).
     HTML insertion points (PATTERNS): .narrative-block before <h2>Quantum Readiness Score</h2> (j2:148); .risks-list after Score Decomposition block, before Findings Breakdown (j2:173); .priority-label inside .roadmap-item (j2:211-223); CSS classes from UI-SPEC HTML/CSS Additions Contract go in the existing <style> block. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Writer seam + congruence guard + CLI markdown narrative/risks/roadmap</name>
  <files>quirk/reports/writer.py, quirk/reports/executive.py, tests/test_congruence_guard.py</files>
  <read_first>
    - quirk/reports/writer.py (write_reports seam ~132-291, compat wrapper ~166-177, _roadmap_markdown ~113-129)
    - quirk/reports/executive.py (build_exec_markdown ~111-308, section ordering, _build_interpretation ~17-104, md_cell usage)
    - quirk/reports/content_model.py (ExecContent API + build_exec_content)
    - .planning/phases/98-executive-narrative-score-transparency/98-RESEARCH.md (Pitfalls 1,2,4,6; "Required order after Phase 98"; writer Integration Seam)
    - .planning/phases/98-executive-narrative-score-transparency/98-PATTERNS.md (executive.py + writer.py sections)
    - .planning/phases/98-executive-narrative-score-transparency/98-UI-SPEC.md (Copywriting Contract: narrative leads, risk items, congruence error)
  </read_first>
  <action>
    In `writer.py:write_reports`, after `score_raw = compute_readiness_score(...)` and `roadmap_raw = build_phased_roadmap(...)` but BEFORE the `score = {"total": score_raw["score"], ...}` compat wrapper, call `exec_content = build_exec_content(score_raw=score_raw, findings=findings, roadmap_items=roadmap_raw.get("items", []))` (import from quirk.reports.content_model with a `# D-03/D-06` tag). Do NOT wrap it in try/except — let `ReportCongruenceError` propagate to the CLI caller (Pitfall 2: guard must fire before any file is written; ensure no report file is written prior to this call). Build/return exec_md by calling `build_exec_markdown(cfg, endpoints, findings, exec_content=exec_content)` and pass `exec_content=exec_content` into the `render_html_report(...)` call (html wiring lands in Task 2, but add the kwarg now). In `executive.py`, add the `exec_content` keyword to `build_exec_markdown` (default None, backward-compat), add `from quirk.reports.content_model import ExecContent`, insert a `## Readiness Assessment` narrative prose block (from `exec_content.narrative_lead` + `narrative_drivers`, "Key factors:" intro per UI-SPEC) immediately after the `## Executive Summary` header and BEFORE `## Quantum Readiness Score` (EXEC-01), add a `## Priority Business Risks` bullet section from `exec_content.top_risks` after the Score Decomposition block (EXEC-02), append effort/impact labels to each roadmap bullet from `exec_content.roadmap_items` (EXEC-03), and remove the old `## Interpretation` section (Pitfall 6). Wrap any finding-derived text in new cells/bullets with `md_cell` (HARDEN-01); leave static narrative-lead/rollup prose unwrapped. Add the integration test `test_guard_blocks_report_generation` to `tests/test_congruence_guard.py` asserting write_reports raises ReportCongruenceError and writes no report file when the guard fires.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m compileall quirk/reports/writer.py quirk/reports/executive.py && python -m pytest tests/test_congruence_guard.py tests/test_executive_score_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m compileall quirk/reports/writer.py quirk/reports/executive.py` exits 0.
    - `grep -n "build_exec_content(" quirk/reports/writer.py` matches; the call appears at a line number lower than the compat-wrapper line (`grep -n 'total.*score_raw\\[' quirk/reports/writer.py`).
    - `pytest tests/test_congruence_guard.py::test_guard_blocks_report_generation -x` exits 0.
    - `pytest tests/test_executive_score_guard.py -x` still green (regression gate).
    - `grep -c "## Interpretation" quirk/reports/executive.py` returns 0 (old section removed).
    - New finding-derived markdown cells are md_cell-wrapped (`grep -n "md_cell" quirk/reports/executive.py` shows wrapping in the new risks/roadmap blocks).
  </acceptance_criteria>
  <done>write_reports builds one ExecContent and fires the guard before I/O; CLI markdown leads with the narrative, includes Priority Business Risks, and labels roadmap items with effort/impact; old Interpretation section gone; guard integration test green.</done>
</task>

<task type="auto">
  <name>Task 2: HTML renderer + template narrative/risks/rollup/priority + ordering tests</name>
  <files>quirk/reports/html_renderer.py, quirk/reports/templates/report.html.j2, tests/test_exec_narrative_ordering.py</files>
  <read_first>
    - quirk/reports/html_renderer.py (render_html_report ~145-211, template context ~189-208, sanitize filter ~158-162, _score_band ~20-29)
    - quirk/reports/templates/report.html.j2 (style block 9-126, score decomposition 155-171, Findings Breakdown ~173, roadmap items 210-223)
    - quirk/reports/content_model.py (ExecContent API)
    - .planning/phases/98-executive-narrative-score-transparency/98-UI-SPEC.md (HTML/CSS Additions Contract, Copywriting Contract, Interaction Contract insertion points)
    - .planning/phases/98-executive-narrative-score-transparency/98-PATTERNS.md (html_renderer.py + report.html.j2 sections)
    - tests/test_html_report.py (render->file->read->assert analog, SimpleNamespace cfg factory)
    - .planning/phases/98-executive-narrative-score-transparency/98-VALIDATION.md (exact pytest node IDs)
  </read_first>
  <action>
    In `html_renderer.py`, add `exec_content: "ExecContent | None" = None` keyword to `render_html_report`. In the `template.render(...)` context: source `subscores=exec_content.subscores if exec_content else score.get("subscores", {})` (D-07), and add `narrative_lead`, `narrative_drivers`, `top_risks`, and roadmap items carrying effort/impact (reuse existing roadmap_now/next/later split but from `exec_content.roadmap_items` so labels are present). Sanitize finding-derived strings via `sanitize_scanner_text` before placing them in context where needed (Pitfall 5); keep static prose unsanitized. In `report.html.j2`, add the four CSS rule blocks from the UI-SPEC HTML/CSS Additions Contract into the existing `<style>` block (no new colors/sizes/weights). Insert a `<div class="narrative-block">` (heading "Readiness Assessment") before `<h2>Quantum Readiness Score</h2>` (EXEC-01); add the `.rollup-formula` block (label "How this score was computed" + the fixed prose) below the Score Decomposition table (TRANS-02); add a `Priority Business Risks` `<ul class="risks-list">` after the decomposition block and before Findings Breakdown, each `<li>` with `.risk-label` + `.risk-impact` spans (EXEC-02); add a `<span class="priority-label">` inside each `.roadmap-item` showing effort/impact labels (EXEC-03). Pipe scanner-derived template values through `| sanitize`; leave the fixed narrative-lead/rollup/effort-impact label strings unsanitized. Create `tests/test_exec_narrative_ordering.py` covering the VALIDATION.md node IDs: test_narrative_before_findings_cli, test_narrative_before_table_html, test_risks_list_in_html, test_priority_labels_in_html_roadmap, test_rollup_formula_in_html (use `content.index(...) < content.index("<table")` ordering pattern from the analog).
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m compileall quirk/reports/html_renderer.py && python -m pytest tests/test_exec_narrative_ordering.py tests/test_html_report.py tests/test_score_transparency.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m compileall quirk/reports/html_renderer.py` exits 0.
    - `grep -c "narrative-block" quirk/reports/templates/report.html.j2` >= 1; `grep -c "risks-list" ...` >= 1; `grep -c "rollup-formula" ...` >= 1; `grep -c "priority-label" ...` >= 1.
    - `pytest tests/test_exec_narrative_ordering.py::test_narrative_before_findings_cli -x` exits 0.
    - `pytest tests/test_exec_narrative_ordering.py::test_narrative_before_table_html -x` exits 0.
    - `pytest tests/test_exec_narrative_ordering.py::test_risks_list_in_html -x` exits 0.
    - `pytest tests/test_exec_narrative_ordering.py::test_priority_labels_in_html_roadmap -x` exits 0.
    - `pytest tests/test_exec_narrative_ordering.py::test_rollup_formula_in_html -x` exits 0.
    - Regression gates green: `pytest tests/test_html_report.py tests/test_score_transparency.py -x`.
  </acceptance_criteria>
  <done>HTML report renders narrative before the score card, a risks-list, the rollup formula prose, and effort/impact priority labels, all sourced from exec_content; score decomposition unchanged but routed through exec_content.subscores; ordering tests + regression gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanner findings -> CLI markdown cells | Finding-derived text rendered into GFM table cells / bullets |
| scanner findings -> HTML template | Finding-derived text rendered into narrative/risks/roadmap HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-03 | Tampering | new markdown cells in executive.py (risk labels, roadmap text) | mitigate | Wrap all finding-derived cell/bullet text with md_cell() from _md_escape.py (HARDEN-01); static narrative-lead/rollup strings left unwrapped. |
| T-98-04 | Tampering | new template content in report.html.j2 (narrative drivers, risk sentences, roadmap titles) | mitigate | Pipe scanner-derived strings through the `| sanitize` Jinja filter (autoescape already active for .j2); fixed copy strings unsanitized. |
| T-98-05 | Information Disclosure | contradictory report emitted (GOOD over CRITICAL) | mitigate | _check_congruence fires in write_reports before any file I/O; ReportCongruenceError aborts the build (TRANS-03 / D-06). |
| T-98-06 | Tampering | Playwright/pypdf import surface | accept | No change to PDF path; both remain lazy-imported (RESEARCH); no new top-level import introduced. |
| T-98-SC | Tampering | npm/pip/cargo installs | accept | Phase 98 adds zero new packages; no install tasks. |
</threat_model>

<verification>
- `python -m compileall quirk/reports/writer.py quirk/reports/executive.py quirk/reports/html_renderer.py` clean.
- Quick suite: `python -m pytest tests/test_exec_content_model.py tests/test_congruence_guard.py tests/test_exec_narrative_ordering.py -x -q` green.
- Regression gates: `python -m pytest tests/test_html_report.py tests/test_score_transparency.py tests/test_executive_score_guard.py -x -q` green.
</verification>

<success_criteria>
- One ExecContent built per report; guard fires before file I/O.
- CLI narrative leads before findings; Priority Business Risks + effort/impact roadmap present.
- HTML narrative-block + risks-list + rollup-formula + priority-label present, sourced from exec_content.
- Score decomposition routed through exec_content.subscores (D-07).
- HARDEN-01 (md_cell) and `| sanitize` applied to new finding-derived content.
- All VALIDATION.md ordering/presence node IDs green; regression gates green.
</success_criteria>

<output>
Create `.planning/phases/98-executive-narrative-score-transparency/98-02-SUMMARY.md` when done.
</output>
