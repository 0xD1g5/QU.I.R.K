---
phase: 110-cross-sensor-merge-scoring
plan: 03
type: execute
wave: 3
depends_on: [110-02]
files_modified:
  - quirk/cli/sensor_cmd.py
  - tests/test_merge_cli.py
  - docs/UAT-SERIES.md
autonomous: true
requirements: [MERGE-05]
must_haves:
  truths:
    - "A consultant runs `quirk sensor merge` and the console prints the merged scan_id, score+rating, and a coverage_warning summary."
    - "The CLI is a thin wrapper over merge_scan() (D-06 v5.5 auto-trigger seam) — no merge logic duplicated in the CLI."
    - "docs/UAT-SERIES.md documents the new merge command and is synced to the Obsidian vault."
  artifacts:
    - path: "quirk/cli/sensor_cmd.py"
      provides: "`merge` subparser + dispatch + _cmd_merge thin wrapper"
      contains: "_cmd_merge"
    - path: "tests/test_merge_cli.py"
      provides: "CLI dispatch test asserting scan_id/score/warning output"
      contains: "def test_"
    - path: "docs/UAT-SERIES.md"
      provides: "merge command UAT coverage + Last Updated bump"
  key_links:
    - from: "quirk/cli/sensor_cmd.py:_cmd_merge"
      to: "quirk.merge.scan.merge_scan"
      via: "lazy import + get_session + print result"
      pattern: "merge_scan\\("
---

<objective>
Expose the merge as the operator-facing `quirk sensor merge` CLI command — a thin wrapper over
merge_scan() — and document it. The command prints the merged scan_id, score+rating, and a
coverage_warning summary, then exits 0.

Purpose: Completes the CLI half of MERGE-05 (operator-invoked merge, D-06 manual-trigger; the
standalone merge_scan() callable remains the v5.5 auto-trigger seam). Also fulfills the per-phase
docs-update + Obsidian-sync + UAT-SERIES requirements.
Output: the `merge` subcommand wired into sensor_cmd.py, a CLI dispatch test, and updated/synced
docs/UAT-SERIES.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-cross-sensor-merge-scoring/110-CONTEXT.md
@.planning/phases/110-cross-sensor-merge-scoring/110-PATTERNS.md
@.planning/phases/110-cross-sensor-merge-scoring/110-02-SUMMARY.md

<interfaces>
<!-- merge_scan return dict (from Plan 02) -->
merge_scan(db, *, now=None, stale_days=30, profile="balanced", weights=None) -> dict
  # keys: scan_id, score, rating, subscores, drivers, coverage_warning (None | {missing_sensors, reason}), endpoint_count

<!-- sensor_cmd.py existing patterns (PATTERNS.md lines ~215-274) -->
run_sensor(argv): sub.add_parser(...) registration + args.action dispatch in a try block
existing subcommands: enroll, push, export-results — _cmd_enroll/_cmd_push/_cmd_export_results
get_session(db_path) ctx manager (quirk/db.py); _default_db_path() (quirk/dashboard/api/deps.py); init_db (quirk/db.py)
sys already imported at module level (~L37) — do NOT re-import
Convention: lazy imports inside handler; sys.exit(0) on success
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire `quirk sensor merge` subcommand + CLI test</name>
  <files>quirk/cli/sensor_cmd.py, tests/test_merge_cli.py</files>
  <read_first>
    - quirk/cli/sensor_cmd.py subparser registration (~L84-117), dispatch block (~L119-129), _cmd_push lazy-import + sys.exit pattern (~L454-564)
    - tests/test_sensor_cmd.py and tests/test_console_cmd.py (CLI dispatch test style, monkeypatch/QUIRK_DB_PATH)
    - .planning/phases/110-cross-sensor-merge-scoring/110-PATTERNS.md (sensor_cmd.py section, lines ~215-274)
  </read_first>
  <behavior>
    - test_merge_cli_dispatch (MERGE-05): invoking run_sensor(["merge"]) (with QUIRK_DB_PATH pointing at a seeded DB or merge_scan monkeypatched to a known result dict) prints a line containing the merged scan_id, a line containing the score and rating, and exits 0.
    - test_merge_cli_coverage_warning: when merge_scan returns a non-null coverage_warning, the CLI prints a WARNING line with the reason and each missing sensor_id.
  </behavior>
  <action>In quirk/cli/sensor_cmd.py register a `merge` subparser after the export-results parser: merge_p = sub.add_parser("merge", help="Merge all sensor data and produce unified CBOM + score") with --db (default None) and --stale-days (type=int, default=30, dest="stale_days"). Add an elif args.action == "merge": _cmd_merge(args) branch inside the existing dispatch try block. Implement _cmd_merge(args) following the _cmd_push pattern: lazy-import merge_scan from quirk.merge.scan, _default_db_path from quirk.dashboard.api.deps, init_db + get_session from quirk.db; resolve db_path = args.db or _default_db_path(); init_db(db_path); open get_session(db_path) as db and call merge_scan(db, stale_days=args.stale_days); print "Merged scan_id: {scan_id}", "Score: {score} ({rating})", and if result coverage_warning is non-null print a WARNING line with the reason followed by one "  - {sid}" line per missing sensor; sys.exit(0). Keep ALL merge logic in merge_scan — the CLI duplicates none of it (thin wrapper, D-06 seam). Author tests/test_merge_cli.py per the behaviors (monkeypatch merge_scan or seed the DB). PEP 8, minimal diff.</action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m compileall quirk/cli/sensor_cmd.py >/dev/null && python -m pytest tests/test_merge_cli.py -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep for _cmd_merge, the "merge" add_parser, and the "merge" dispatch branch in quirk/cli/sensor_cmd.py all match.
    - `grep -c "merge_scan" quirk/cli/sensor_cmd.py` is >= 1 (lazy import + call); no union/scoring/coverage logic inlined in the CLI.
    - tests/test_merge_cli.py passes (dispatch prints scan_id + score + rating, exits 0; coverage_warning path prints WARNING + missing sensors).
  </acceptance_criteria>
  <done>`quirk sensor merge` dispatches to merge_scan, prints scan_id/score/warning, exits 0; CLI test green.</done>
</task>

<task type="auto">
  <name>Task 2: Update docs/UAT-SERIES.md and sync to Obsidian</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (header, Last Updated line, the sensor/distributed test series if present)
    - ./CLAUDE.md (Mandatory Phase Completion Steps section 2 and 3 — UAT update + vault sync commands)
  </read_first>
  <action>Add UAT coverage for `quirk sensor merge` to the relevant series in docs/UAT-SERIES.md: a test case that runs `quirk sensor merge` against a console with enrolled sensor data and asserts the printed merged scan_id, score+rating, and (with an overdue sensor) the coverage_warning summary; note that source per-endpoint scanned_at is preserved and that two-segment same-IP yields two CBOM components. Bump the Last Updated date to today. Then sync to the Obsidian vault per CLAUDE.md section 3 using the printf+cat+cp pattern (write to /tmp then cp to "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md") — file is too large for CLI content=.</action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && grep -qi "sensor merge" docs/UAT-SERIES.md && grep -q "Last Updated" docs/UAT-SERIES.md && test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/UAT-SERIES.md contains a `quirk sensor merge` test case and an updated Last Updated date.
    - The Obsidian vault file /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md exists with the synced content (frontmatter + UAT body).
  </acceptance_criteria>
  <done>UAT-SERIES.md documents the merge command and is synced to the Digs vault.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → merge_scan | console-local CLI invocation; no new network surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-07 | Information Disclosure | CLI printing coverage_warning sensor_ids | accept | sensor_ids are operator-generated UUIDs already in the console DB; printed to the local operator only |
| T-110-08 | Tampering (logic drift) | merge logic duplicated in CLI | mitigate | CLI is a thin wrapper; all merge logic stays in merge_scan (grep gate: no inlined union/scoring) |
| T-110-SC | Tampering | npm/pip installs | mitigate | No new packages; no install task |
</threat_model>

<verification>
- `python -m pytest tests/test_merge_cli.py -q` green.
- `python -m compileall quirk/cli/sensor_cmd.py` exits 0.
- docs/UAT-SERIES.md mentions `sensor merge`; vault UAT-Series.md exists.
</verification>

<success_criteria>
`quirk sensor merge` runs merge_scan and prints scan_id/score/coverage_warning, exits 0;
UAT-SERIES.md updated and synced to Obsidian.
</success_criteria>

<output>
Create `.planning/phases/110-cross-sensor-merge-scoring/110-03-SUMMARY.md` when done.
</output>
