---
phase: 110-cross-sensor-merge-scoring
plan: 02
type: execute
wave: 2
depends_on: [110-01]
files_modified:
  - quirk/merge/__init__.py
  - quirk/merge/scan.py
  - quirk/models.py
  - quirk/db.py
  - tests/test_merge_scan.py
autonomous: true
requirements: [MERGE-01, MERGE-02, MERGE-04, MERGE-05]
must_haves:
  truths:
    - "merge_scan() assembles the union of latest-push-per-sensor rows plus NULL-sensor local rows."
    - "The merged score is computed by one compute_readiness_score() call over the full union (Option A), never an average of per-segment scores."
    - "An overdue or never-pushed enrolled sensor produces a non-null coverage_warning {missing_sensors, reason}; all-current produces null."
    - "Per-endpoint sensor-local scanned_at on the SOURCE rows is not rewritten by the merge."
    - "The merge result is persisted as a merge_runs row carrying scan_id, score, and coverage_warning_json."
  artifacts:
    - path: "quirk/merge/scan.py"
      provides: "merge_scan(db, ...) standalone callable: union → evidence → score → CBOM → coverage_warning → persist"
      exports: ["merge_scan"]
      min_lines: 60
    - path: "quirk/models.py"
      provides: "MergeRun ORM model (plain Column style, no relationship())"
      contains: "class MergeRun"
    - path: "quirk/db.py"
      provides: "_ensure_merge_runs_table called from init_db (checkfirst=True)"
      contains: "_ensure_merge_runs_table"
    - path: "tests/test_merge_scan.py"
      provides: "Option A, coverage_warning, scanned_at-preserved unit tests"
      contains: "test_option_a_score_not_averaged"
  key_links:
    - from: "quirk/merge/scan.py:merge_scan"
      to: "build_evidence_summary → compute_readiness_score → build_cbom"
      via: "single call over union endpoints (findings=None)"
      pattern: "compute_readiness_score\\("
    - from: "quirk/merge/scan.py:merge_scan"
      to: "Sensor.last_push_at + expected_cadence_minutes"
      via: "overdue = now > last_push_at + 2*cadence"
      pattern: "last_push_at"
    - from: "quirk/db.py:init_db"
      to: "merge_runs table"
      via: "_ensure_merge_runs_table(engine)"
      pattern: "_ensure_merge_runs_table"
---

<objective>
Build the standalone merge_scan() callable: assemble the union of all sensor endpoints, score it
once through the unmodified engines (Option A), compute the coverage_warning from sensor push
recency, and persist the merged result as a merge_runs row — all without rewriting the source
endpoints' sensor-local scanned_at.

Purpose: Satisfies MERGE-01 (canonical engine chain over the union, not forked), MERGE-02 (Option A
union scoring, never averaged), MERGE-04 (coverage_warning for overdue/offline sensors), and the
persistence half of MERGE-05 (merged result with a new scan_id; source scanned_at preserved).
Output: quirk/merge/scan.py (merge_scan), a MergeRun ORM model + idempotent table creation, and
unit tests proving Option A, coverage warning, and scanned_at preservation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-cross-sensor-merge-scoring/110-CONTEXT.md
@.planning/phases/110-cross-sensor-merge-scoring/110-RESEARCH.md
@.planning/phases/110-cross-sensor-merge-scoring/110-PATTERNS.md
@.planning/phases/110-cross-sensor-merge-scoring/110-VALIDATION.md
@.planning/phases/110-cross-sensor-merge-scoring/110-01-SUMMARY.md

<interfaces>
<!-- Engine signatures (VERIFIED in RESEARCH §Code Examples) — call, do NOT modify -->
build_evidence_summary(endpoints, findings=None, *, expiring_days=30, reference_utc=None) -> Dict   # evidence.py
compute_readiness_score(evidence, *, profile=None, weights=None) -> Dict   # scoring.py; keys: score, rating, subscores, drivers
build_cbom(endpoints: list[CryptoEndpoint]) -> Bom   # builder.py (sensor-aware after Plan 01)

<!-- Models (quirk/models.py) -->
CryptoEndpoint: sensor_id (nullable), segment, scanned_at, all scan fields
Sensor: sensor_id, last_push_at, expected_cadence_minutes   # use last_push_at for overdue (Pitfall 5), NOT scanned_at

<!-- Reuse patterns -->
SESSION_BRACKET = timedelta(minutes=5)   # quirk/dashboard/api/routes/scan.py L84 — redefine locally to avoid dashboard import
get_session(db_path) context manager     # quirk/db.py L426-452 (commit/rollback/close)
_ensure_integration_deliveries_table(engine) + init_db call site  # quirk/db.py L365-373, ~L410 — analog for _ensure_merge_runs_table
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MergeRun model + idempotent table creation</name>
  <files>quirk/models.py, quirk/db.py</files>
  <read_first>
    - quirk/models.py Sensor class (~L269-288) and SensorPush class (~L335) for plain-Column style + placement
    - quirk/db.py _ensure_integration_deliveries_table (~L365-373) and init_db call site (~L410)
    - .planning/phases/110-cross-sensor-merge-scoring/110-PATTERNS.md (models.py + New Table Registration sections, lines ~278-316, ~431-440)
  </read_first>
  <action>Add a MergeRun(Base) class after SensorPush in quirk/models.py with __tablename__="merge_runs" and plain Columns: id (Integer PK autoincrement), scan_id (String(64), nullable=False, index=True — ISO merge timestamp), merged_at (DateTime, nullable=False), endpoint_count (Integer, nullable=False, default=0), sensor_count (Integer, nullable=False, default=0), score (Integer, nullable=True), coverage_warning_json (Text, nullable=True). NO relationship() declarations (project plain-Column rule, D-01 idiom). In quirk/db.py add `_ensure_merge_runs_table(engine)` mirroring _ensure_integration_deliveries_table (Base.metadata.create_all(engine, checkfirst=True) — new table, not ALTER), and call it inside init_db() right after the existing _ensure_integration_deliveries_table(engine) call. Per MERGE-05 / Phase 107 D-01 additive-model idiom.</action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m compileall quirk/models.py quirk/db.py >/dev/null && python -c "import os,tempfile; os.environ['QUIRK_DB_PATH']=tempfile.mktemp(suffix='.db'); from quirk.db import init_db; e=init_db(os.environ['QUIRK_DB_PATH']); from sqlalchemy import inspect; assert 'merge_runs' in inspect(e).get_table_names(), inspect(e).get_table_names(); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class MergeRun" quirk/models.py` matches; the class has no `relationship(` call.
    - `grep -n "_ensure_merge_runs_table" quirk/db.py` shows the def and a call inside init_db.
    - init_db on a fresh DB creates the merge_runs table (inspector assertion passes).
  </acceptance_criteria>
  <done>MergeRun model exists in plain-Column style and merge_runs is created idempotently by init_db.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement merge_scan() — union, Option A, coverage_warning, persist</name>
  <files>quirk/merge/__init__.py, quirk/merge/scan.py, tests/test_merge_scan.py</files>
  <read_first>
    - quirk/dashboard/api/routes/scan.py union/MAX-scanned_at pattern (~L84, ~L991-1008)
    - tests/test_dar_k8s_scoring.py (build_evidence_summary + compute_readiness_score call chain, factory style)
    - tests/test_cbom_builder.py _tls_endpoint factory (~L25-38)
    - .planning/phases/110-cross-sensor-merge-scoring/110-RESEARCH.md (§Union Query Pattern, §Coverage Warning Computation, §merge_scan() Signature, Pitfalls 1/2/4/5)
    - .planning/phases/110-cross-sensor-merge-scoring/110-PATTERNS.md (quirk/merge/scan.py section, lines ~36-156)
  </read_first>
  <behavior>
    - test_merge_pipeline_uses_existing_engines (MERGE-01): merge_scan over a union calls build_evidence_summary→compute_readiness_score→build_cbom; result dict has score, rating, subscores, scan_id, endpoint_count.
    - test_option_a_score_not_averaged (MERGE-02): for two segments (sensor s1 host 10.0.1.1, sensor s2 host 10.0.2.1), result["score"] == compute_readiness_score(build_evidence_summary(union))["score"] — equals scoring-the-union, NOT the mean of per-segment scores. Assert it differs from round((score_a+score_b)/2) when those differ.
    - test_coverage_warning_overdue_sensor (MERGE-04): a Sensor with last_push_at older than now-2×cadence (or last_push_at=None) → coverage_warning is a dict with that sensor_id in missing_sensors and a non-empty reason.
    - test_coverage_warning_null_when_current (MERGE-04): all sensors pushed within 2×cadence → coverage_warning is None.
    - test_scanned_at_preserved (MERGE-05): after merge_scan, re-querying the SOURCE CryptoEndpoint rows shows their original sensor-local scanned_at unchanged (the merge did not mutate them).
    - test_empty_union_guarded (Pitfall 4): no enrolled sensors and no local rows → result has a coverage_warning noting no data (does not silently return score 100 as "complete").
  </behavior>
  <action>Create quirk/merge/__init__.py (empty package marker). Create quirk/merge/scan.py with `merge_scan(db, *, now=None, stale_days=30, profile="balanced", weights=None) -> dict`. Steps: (1) Union assembly — subquery of func.max(CryptoEndpoint.scanned_at) grouped by non-null sensor_id, join back for latest-push rows; plus NULL-sensor local rows within a locally-defined SESSION_BRACKET=timedelta(minutes=5) window of the latest local scanned_at. (2) coverage_warning via a helper reading Sensor.last_push_at + expected_cadence_minutes: overdue when last_push_at is None OR now > last_push_at + 2×timedelta(minutes=cadence); return None when none overdue else {"missing_sensors":[ids], "reason": <str>}. Use Sensor.last_push_at, NEVER CryptoEndpoint.scanned_at (Pitfall 5). (3) Option A: call build_evidence_summary(union, findings=None) then compute_readiness_score(evidence, profile=profile, weights=weights) EXACTLY ONCE over the full union — never average per-segment results (Pitfall 2 / D-01). (4) build_cbom(union) for the Bom. (5) Guard empty union (Pitfall 4): return a result with a coverage_warning noting no data rather than a clean 100. (6) Persist: scan_id = merge execution time via datetime isoformat(sep=" "); add a MergeRun row (scan_id, merged_at, endpoint_count, sensor_count, score, coverage_warning_json=json.dumps(cw) or None) and commit. Do NOT mutate or rewrite the source CryptoEndpoint.scanned_at values (Pitfall 1 / MERGE-05) — read the union rows in place; persist the merged RESULT only as the merge_runs row. now defaults to datetime.utcnow() and is injectable for tests. Return dict per the signature (scan_id, score, rating, subscores, drivers, coverage_warning, endpoint_count). Author tests/test_merge_scan.py covering the behaviors above using the QUIRK_DB_PATH conftest fixture / in-memory CryptoEndpoint + Sensor rows; inject now= for deterministic overdue tests. PEP 8, minimal.</action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m compileall quirk/merge >/dev/null && python -m pytest tests/test_merge_scan.py -q 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "compute_readiness_score" quirk/merge/scan.py` ≥ 1 and there is NO division by len(segments)/len(sensors) in the score path (`grep -nE "/ *len\(" quirk/merge/scan.py` returns nothing in the scoring path).
    - `grep -n "last_push_at" quirk/merge/scan.py` present; coverage path does NOT key on CryptoEndpoint.scanned_at.
    - merge_scan does NOT assign to `.scanned_at` on any CryptoEndpoint (`grep -nE "\.scanned_at *=" quirk/merge/scan.py` returns nothing).
    - All tests in tests/test_merge_scan.py pass, including test_option_a_score_not_averaged, test_coverage_warning_overdue_sensor, test_coverage_warning_null_when_current, test_scanned_at_preserved, test_empty_union_guarded.
  </acceptance_criteria>
  <done>merge_scan() produces an Option-A union score, a correct coverage_warning, preserves source scanned_at, persists a merge_runs row, and all unit tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingested sensor rows → union/score | already-validated CryptoEndpoint rows aggregated locally; no new network surface |
| coverage computation → operator | sensor recency derived from Sensor.last_push_at written by Phase 109 ingest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-03 | Tampering (false posture) | empty union → score 100 | mitigate | Guard zero-endpoint union → coverage_warning "no data", never a clean complete score (Pitfall 4) |
| T-110-04 | Information Disclosure | coverage_warning.reason | accept | sensor_ids are operator-generated UUIDs already in the console DB; reason is a hardcoded literal, no raw exception text |
| T-110-05 | Tampering (wrong union) | latest-per-sensor query | mitigate | func.max(scanned_at) per sensor_id + NULL-sensor SESSION_BRACKET window; Option A test proves union scoring |
| T-110-06 | Repudiation | scanned_at rewrite | mitigate | Source scanned_at never mutated; merge result persisted as separate merge_runs row (grep gate + test_scanned_at_preserved) |
| T-110-SC | Tampering | npm/pip installs | mitigate | No new packages; no install task |
</threat_model>

<verification>
- `python -m pytest tests/test_merge_scan.py -q` green.
- `python -m compileall quirk/merge quirk/models.py quirk/db.py` exits 0.
- grep gates: no `/ len(` in score path; no `.scanned_at =` in merge_scan; `last_push_at` used for coverage.
</verification>

<success_criteria>
merge_scan() is a standalone callable producing Option-A union score + sensor-aware CBOM +
coverage_warning, persisting a merge_runs row, with source scanned_at preserved — all proven by
tests/test_merge_scan.py.
</success_criteria>

<output>
Create `.planning/phases/110-cross-sensor-merge-scoring/110-02-SUMMARY.md` when done.
</output>
