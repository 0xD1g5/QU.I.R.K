---
phase: 110-cross-sensor-merge-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/cbom/builder.py
  - tests/test_cbom_builder.py
autonomous: true
requirements: [MERGE-03]
must_haves:
  truths:
    - "Two sensors reporting the same RFC1918 host:port in different segments yield two distinct CBOM components (cert + protocol)."
    - "A NULL sensor_id endpoint produces byte-identical bom_refs to the pre-110 format (backward-compat)."
    - "The TLS surrogate index lookup key stays in sync with the cert bom_ref derivation."
  artifacts:
    - path: "quirk/cbom/builder.py"
      provides: "_sensor_prefix(ep) helper threaded through 4 bom_ref derivation sites"
      contains: "_sensor_prefix"
    - path: "tests/test_cbom_builder.py"
      provides: "MERGE-03 two-segment regression + NULL backward-compat tests"
      contains: "test_two_sensors_same_ip_two_components"
  key_links:
    - from: "quirk/cbom/builder.py:_sensor_prefix"
      to: "cert bom_ref (L697), surrogate lookup (L758), SSH proto (L844), TLS proto (L897)"
      via: "f-string prefix insertion"
      pattern: "_sensor_prefix\\(ep\\)"
---

<objective>
Make CBOM component identity sensor-aware so two sensors reporting the same RFC1918 host:port
in different segments produce two distinct components instead of collapsing into one.

Purpose: Satisfies D-03 / MERGE-03 — the foundational identity-key change the merge pipeline
(Plan 02) depends on. This is a surgical change to the existing engine, NOT a fork (MERGE-01
forbids forking the engine, not threading a discriminator into the identity key).
Output: A `_sensor_prefix(ep)` helper threaded through the four host:port-keyed bom_ref sites
in builder.py, plus regression tests proving two-segment separation and NULL backward-compat.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-cross-sensor-merge-scoring/110-CONTEXT.md
@.planning/phases/110-cross-sensor-merge-scoring/110-RESEARCH.md
@.planning/phases/110-cross-sensor-merge-scoring/110-PATTERNS.md
@.planning/phases/110-cross-sensor-merge-scoring/110-VALIDATION.md

<interfaces>
<!-- From quirk/cbom/builder.py — the four host:port-keyed bom_ref derivation sites (VERIFIED in RESEARCH/PATTERNS) -->
build_cbom(endpoints: list[CryptoEndpoint]) -> Bom   # builder.py L445 — do NOT change signature/logic

Current derivations (verbatim, to be modified):
  L697  cert_bom_ref = f"crypto/certificate/{ep.host}:{ep.port}"
  L758  bom_ref_val  = f"crypto/certificate/{ep.host}:{ep.port}"   # TLS surrogate index lookup — MUST match L697
  L844  proto_bom_ref = f"crypto/protocol/ssh/{ep.host}:{ep.port}"
  L897  proto_bom_ref = f"crypto/protocol/tls/{ep.host}:{ep.port}"

DO NOT change: L797 bom_ref_val = f"crypto/certificate/codesign/{ep.host}:{ep.port}"
  (CODE_SIGNING fallback — no cross-segment code-sign scenario in v5.4; changing it risks regression)

CryptoEndpoint has nullable sensor_id (Phase 107 / D-08). NULL = implicit local sensor.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MERGE-03 regression tests (RED)</name>
  <files>tests/test_cbom_builder.py</files>
  <read_first>
    - tests/test_cbom_builder.py (existing _tls_endpoint factory ~L25-38, _certificate_components helper ~L131-141, existing cert tests)
    - .planning/phases/110-cross-sensor-merge-scoring/110-PATTERNS.md (MERGE-03 test pattern, lines ~377-405)
    - .planning/phases/110-cross-sensor-merge-scoring/110-RESEARCH.md (§MERGE-03 regression test structure, §Test Conventions)
  </read_first>
  <behavior>
    - Test test_two_sensors_same_ip_two_components: two _tls_endpoint rows with host="10.0.0.5", port=443, one sensor_id="sensor-a" segment="prod-east", the other sensor_id="sensor-b" segment="prod-west" → build_cbom([ep_a, ep_b]) → cert bom_refs include both "crypto/certificate/sensor-a:10.0.0.5:443" and "crypto/certificate/sensor-b:10.0.0.5:443" and there are exactly 2 certificate components (not collapsed).
    - Test test_null_sensor_id_backward_compat: one _tls_endpoint with sensor_id=None, host="10.0.0.5", port=443 → cert bom_refs == ["crypto/certificate/10.0.0.5:443"] (byte-identical to pre-110 format).
    - Add an analogous TLS-protocol assertion: two-sensor case yields two "crypto/protocol/tls/sensor-*:10.0.0.5:443" refs; NULL case yields "crypto/protocol/tls/10.0.0.5:443".
  </behavior>
  <action>Add the two test functions plus the TLS-protocol assertion to tests/test_cbom_builder.py, reusing the existing _tls_endpoint factory (extend overrides with sensor_id / segment). Filter components by CryptoAssetType.CERTIFICATE for cert refs and by protocol asset type for the TLS-protocol refs, reading bom_ref via getattr(c.bom_ref, "value", None). These tests MUST FAIL before Task 2 (current builder collapses on host:port). Per MERGE-03.</action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m pytest tests/test_cbom_builder.py -k "two_sensors_same_ip or null_sensor_id_backward_compat" -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test_two_sensors_same_ip_two_components and test_null_sensor_id_backward_compat exist in tests/test_cbom_builder.py.
    - Before Task 2: both new tests FAIL (collapsed components / wrong ref count) — RED confirmed.
    - No fenced implementation in PLAN; tests authored against the factory + assertions above.
  </acceptance_criteria>
  <done>Both regression tests exist and fail against the unmodified builder, demonstrating the collision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread _sensor_prefix through the 4 bom_ref sites (GREEN)</name>
  <files>quirk/cbom/builder.py</files>
  <read_first>
    - quirk/cbom/builder.py around L426-438 (placement near _emit_coverage_note), L690-700, L750-762, L840-848, L893-900
    - .planning/phases/110-cross-sensor-merge-scoring/110-PATTERNS.md (builder.py section, lines ~158-211 — verbatim before/after for all 4 sites)
    - .planning/phases/110-cross-sensor-merge-scoring/110-RESEARCH.md (§CRITICAL MERGE-01-vs-D-03 Resolution, Pitfall 3)
  </read_first>
  <action>Add a module-level helper `_sensor_prefix(ep) -> str` near _emit_coverage_note (~L438): return `f"{sid}:"` when `getattr(ep, "sensor_id", None)` is truthy, else `""`. Thread it into exactly four derivations: cert_bom_ref at ~L697, the TLS surrogate index lookup `bom_ref_val` at ~L758 (MUST use the identical formula as L697 or cross-source dedup desyncs — Pitfall 3), the SSH proto_bom_ref at ~L844, and the TLS proto_bom_ref at ~L897 — each becoming `crypto/.../{_sensor_prefix(ep)}{ep.host}:{ep.port}`. Do NOT modify the CODE_SIGNING codesign/ fallback at ~L797. Do NOT alter any other logic, field, or signature (MERGE-01 — engine not forked). Keep diff minimal (PEP 8).</action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python -m compileall quirk/cbom/builder.py >/dev/null && python -m pytest tests/test_cbom_builder.py -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_sensor_prefix" quirk/cbom/builder.py` shows the helper definition plus exactly 4 call sites (cert L697, surrogate L758, SSH L844, TLS L897).
    - `grep -n "crypto/certificate/codesign" quirk/cbom/builder.py` shows the codesign fallback UNCHANGED (no _sensor_prefix).
    - The two Task-1 tests now PASS; the full tests/test_cbom_builder.py suite is green (no regression to existing single-host tests = byte-stable backward-compat).
    - `python -m compileall quirk/cbom/builder.py` exits 0.
  </acceptance_criteria>
  <done>All four host:port bom_refs include the sensor prefix when sensor_id is set, NULL stays byte-identical, full test_cbom_builder.py green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingested CryptoEndpoint rows → CBOM identity key | sensor_id (operator-generated UUID4, validated at Phase 108 enrollment via _UUID_RE) flows into an internal bom_ref string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-01 | Spoofing/Collapse | builder.py bom_ref derivation | mitigate | Include sensor_id in identity key so two-segment same-IP cannot collapse to one component (MERGE-03 regression test enforces) |
| T-110-02 | Injection | _sensor_prefix using sensor_id in bom_ref | accept | sensor_id is a UUID4 validated at enrollment; bom_ref is internal CBOM JSON, never rendered to HTML/SQL — no injection sink |
| T-110-SC | Tampering | npm/pip installs | mitigate | No new packages in this plan; no install task |
</threat_model>

<verification>
- `python -m pytest tests/test_cbom_builder.py -q` fully green.
- `grep -n "_sensor_prefix" quirk/cbom/builder.py` = 1 def + 4 call sites; codesign fallback untouched.
- `python -m compileall quirk/cbom/builder.py` exits 0.
</verification>

<success_criteria>
Two-segment same-IP → two distinct CBOM components (cert + TLS protocol); NULL sensor_id output
byte-identical to pre-110; full builder test suite green; no engine logic forked.
</success_criteria>

<output>
Create `.planning/phases/110-cross-sensor-merge-scoring/110-01-SUMMARY.md` when done.
</output>
