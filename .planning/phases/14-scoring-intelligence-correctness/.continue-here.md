---
phase: 14-scoring-intelligence-correctness
task: complete
total_tasks: 4
status: phase_complete
last_updated: 2026-04-07T00:19:03.131Z
---

<current_state>
Phase 14 is fully complete and verified. All 4 tasks committed. STATE and ROADMAP advanced to Phase 15 (code-hygiene). Clean working tree — no uncommitted changes.
</current_state>

<completed_work>

- Task 1: RED test scaffold (SCORE-01–04) — Done [6b90692]
  - SCORE-01 (profile multipliers): regression guard, already GREEN
  - SCORE-02 (dead validate param): 2 RED tests confirm require_delta_if_baseline still present
  - SCORE-03 (migration advisor matching): regression guard, already GREEN
  - SCORE-04 (dashboard profile wiring): 2 RED tests confirm get_latest_scan missing profile= and calibration fields
- Task 2: Remove dead require_delta_if_baseline from validate_run + main() argparse — Done [a5e160c]
- Task 3: Wire scan-time profile into dashboard compute_readiness_score via calibration.profile in intelligence JSON — Done [5a57eb1]
- Task 4: Phase 14 verification + docs committed — Done [aa329ab]
  - Verification: 4/4 must-haves passed
  - 223 tests passing (215 original + 8 new SCORE tests)
  - PROJECT.md evolved: Scoring Correctness marked validated, test count updated

</completed_work>

<remaining_work>

Nothing — phase complete.

</remaining_work>

<decisions_made>

- SCORE-01/SCORE-03 written as regression guards confirming already-correct behavior
- SCORE-02 fix: validate_run(output_dir: Path) — dead require_delta_if_baseline removed from both signature and argparse
- SCORE-04 fix: dashboard reads calibration.profile (not assessment.profile) from latest intelligence JSON on disk; passes profile=stored_profile to compute_readiness_score
- TDD wave split (RED scaffold in Wave 1, GREEN fixes in Wave 2) prevented tests being retrofitted after implementation

</decisions_made>

<blockers>

None.

</blockers>

<context>

Phase 14 was the third of four v4.1 Foundation Polish phases. All four backlog items resolved: BACK-43 (calibration profile applied), BACK-44 (validate.py fixed), BACK-46 (migration_advisor matching), BACK-60 (dashboard profile wiring). The SCORE-04 fix is architecturally significant — the dashboard was silently ignoring the user's chosen scan profile, making scores non-reproducible between CLI and dashboard views.

Next phase: Phase 15 — code-hygiene. Scope: remove legacy connector stub directory (BACK-37), cfg.scan mutation guard (BACK-45), delete orphaned scorecard.py (BACK-61), update 9 stale + 2 missing Nyquist VALIDATION.md files (BACK-62).

</context>

<next_action>
Start with: /clear then /gsd:plan-phase 15
</next_action>
