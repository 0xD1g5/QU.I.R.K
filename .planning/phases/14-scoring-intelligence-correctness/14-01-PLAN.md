---
phase: 14-scoring-intelligence-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_scoring_correctness.py
autonomous: true
requirements:
  - SCORE-01
  - SCORE-02
  - SCORE-03
  - SCORE-04

must_haves:
  truths:
    - "A test asserts strict profile scores lower than lenient on penalized evidence"
    - "A test asserts validate_run has no require_delta_if_baseline parameter"
    - "A test asserts risk_engine finding titles produce migration recommendations"
    - "A test asserts dashboard compute_readiness_score receives a profile kwarg"
  artifacts:
    - path: "tests/test_scoring_correctness.py"
      provides: "RED test scaffold for SCORE-01 through SCORE-04"
      min_lines: 80
      contains: "ScoringCorrectnessTests"
  key_links:
    - from: "tests/test_scoring_correctness.py"
      to: "quirk/intelligence/scoring.py"
      via: "import compute_readiness_score"
      pattern: "from quirk.intelligence.scoring import compute_readiness_score"
    - from: "tests/test_scoring_correctness.py"
      to: "quirk/validate.py"
      via: "import validate_run"
      pattern: "from quirk.validate import validate_run"
    - from: "tests/test_scoring_correctness.py"
      to: "quirk/assessment/migration_advisor.py"
      via: "import recommend_migration_paths"
      pattern: "from quirk.assessment.migration_advisor import recommend_migration_paths"
---

<objective>
Write RED test scaffold for all four SCORE requirements (SCORE-01 through SCORE-04).

Purpose: Per D-01, Plan 1 creates failing tests that prove each bug exists (or becomes a regression guard if already correct). These tests define the contract that Plan 02 must satisfy.

Output: `tests/test_scoring_correctness.py` with 4+ test methods covering all SCORE requirements. Tests for SCORE-01 and SCORE-03 may pass immediately (per Research Pitfall 1) -- this is expected and they become permanent regression guards.
</objective>

<execution_context>
@/Users/digs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/digs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-scoring-intelligence-correctness/14-CONTEXT.md
@.planning/phases/14-scoring-intelligence-correctness/14-RESEARCH.md

<interfaces>
<!-- Key signatures the executor needs -->

From quirk/intelligence/scoring.py:
```python
PROFILE_MULTIPLIERS: Dict[str, Dict[str, float]] = {
    "strict": {"agility_": 1.4, "identity_": 1.4},
    "balanced": {"agility_": 1.0, "identity_": 1.0},
    "lenient": {"agility_": 0.7, "identity_": 0.7},
}

def compute_readiness_score(
    evidence: Mapping[str, Any],
    *,
    profile: str | None = None,
    weights: Mapping[str, float] | None = None,
) -> Dict[str, Any]:
```

From quirk/validate.py:
```python
def validate_run(output_dir: Path, require_delta_if_baseline: bool = True) -> ValidationResult:
    # body never references require_delta_if_baseline
```

From quirk/assessment/migration_advisor.py:
```python
def recommend_migration_paths(findings: List[Dict]) -> List[Dict]:
```

From quirk/dashboard/api/routes/scan.py (line 329-330):
```python
score_raw = compute_readiness_score(evidence)  # MISSING profile kwarg
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write RED test scaffold for SCORE-01 through SCORE-04</name>
  <files>tests/test_scoring_correctness.py</files>
  <read_first>
    - quirk/intelligence/scoring.py (lines 22-95 -- PROFILE_MULTIPLIERS and compute_readiness_score signature/body)
    - quirk/validate.py (lines 105-145 -- validate_run signature and main() with --no-require-delta)
    - quirk/assessment/migration_advisor.py (full file -- pattern matching logic)
    - quirk/engine/risk_engine.py (lines 31-40, 188-197 -- finding title strings)
    - quirk/dashboard/api/routes/scan.py (lines 320-340 -- missing profile kwarg in compute_readiness_score call)
    - tests/test_intelligence_scoring.py (existing scoring tests -- check for overlap before adding)
    - tests/test_validate.py (existing validate tests -- check for overlap)
  </read_first>
  <action>
Create `tests/test_scoring_correctness.py` with four test classes covering all SCORE requirements.

**File header:**
```python
"""Phase 14 — Scoring & Intelligence Correctness RED scaffold.

Tests assert the correct end-state for SCORE-01 through SCORE-04.
SCORE-01 and SCORE-03 may pass immediately (regression guards per D-02).
SCORE-02 and SCORE-04 must FAIL before Plan 02 fixes land.
"""
import inspect
import unittest
from unittest import mock
```

**Class 1 — ScoringCorrectnessTests (SCORE-01, per D-02/D-03):**
- `test_strict_scores_lower_than_lenient_on_penalized_evidence`: Build evidence dict with agility/identity issues: `{"totals": {"endpoints": 10, "findings": 5}, "cert_key_type_counts": {"RSA": 8, "ECDSA": 0}, "certificate_observations": {"expired_count": 3, "expiring_count": 2, "self_signed_count": 2}, "finding_severity_counts": {"CRITICAL": 1, "HIGH": 2, "MEDIUM": 1, "LOW": 1, "INFO": 0}}`. Call `compute_readiness_score(evidence, profile="strict")` and `compute_readiness_score(evidence, profile="lenient")`. Assert `strict_score["score"] < lenient_score["score"]` with descriptive message: "strict should score LOWER than lenient when evidence has identity/agility issues". (Research probe confirms strict=59, lenient=76 -- this test may pass immediately.)

**Class 2 — ValidateCorrectnessTests (SCORE-02, per D-04/D-05):**
- `test_validate_run_no_delta_param`: Import `validate_run` from `quirk.validate`, get `inspect.signature(validate_run).parameters`, assert `"require_delta_if_baseline" not in params`. This test MUST FAIL before Plan 02 removes the parameter.
- `test_validate_main_no_delta_arg`: Import `main` from `quirk.validate`, read source with `inspect.getsource(main)`, assert `"no-require-delta" not in source` and `"no_require_delta" not in source`. This test MUST FAIL before Plan 02 removes the argparse argument.

**Class 3 — MigrationAdvisorTests (SCORE-03, per D-06):**
- `test_legacy_tls_finding_produces_migration_rec`: Build findings list with `[{"title": "Legacy TLS versions allowed (TLS 1.0/1.1)", "severity": "LOW", "host": "10.0.0.1", "port": 443, "recommendation": "Disable TLS 1.0/1.1"}]`. Call `recommend_migration_paths(findings)`. Assert `len(recs) == 1`. Assert `recs[0]["path"]` contains "Hygiene" or "Modernization". (Research confirms this passes -- regression guard.)
- `test_plaintext_http_finding_produces_migration_rec`: Build findings with `[{"title": "Plaintext HTTP service detected", "severity": "HIGH", "host": "10.0.0.1", "port": 80, "recommendation": "Enable TLS"}]`. Call `recommend_migration_paths(findings)`. Assert `len(recs) == 1`.
- `test_info_findings_filtered_out`: Build findings with `[{"title": "SSH quantum planning advisory", "severity": "INFO", "host": "10.0.0.1", "port": 22, "recommendation": "Plan"}]`. Call `recommend_migration_paths(findings)`. Assert `len(recs) == 0` (INFO severity filtered at line 21).

**Class 4 — DashboardProfileTests (SCORE-04, per D-07/D-08):**
- `test_dashboard_score_call_uses_profile_kwarg`: Use `inspect.getsource` on the `get_latest_scan` function (import from `quirk.dashboard.api.routes.scan`). Assert the source contains `profile=` as an argument to `compute_readiness_score`. This test MUST FAIL before Plan 02 adds the kwarg.
- `test_dashboard_reads_calibration_profile_not_assessment`: Use `inspect.getsource` on `get_latest_scan`. Assert the source contains the string `"calibration"` (reading from `calibration.profile`). Assert the source does NOT contain `"assessment"` followed by `"profile"` on the same line for score purposes. This test MUST FAIL before Plan 02. CRITICAL: per Research Pitfall 2, the field is `calibration.profile`, NOT `assessment.profile` (CONTEXT.md D-07 has an error).

**Runner block at bottom:**
```python
if __name__ == "__main__":
    unittest.main()
```
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && python3 -m pytest tests/test_scoring_correctness.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_scoring_correctness.py exists and contains `class ScoringCorrectnessTests`
    - tests/test_scoring_correctness.py contains `class ValidateCorrectnessTests`
    - tests/test_scoring_correctness.py contains `class MigrationAdvisorTests`
    - tests/test_scoring_correctness.py contains `class DashboardProfileTests`
    - tests/test_scoring_correctness.py contains `from quirk.intelligence.scoring import compute_readiness_score`
    - tests/test_scoring_correctness.py contains `from quirk.validate import validate_run`
    - tests/test_scoring_correctness.py contains `from quirk.assessment.migration_advisor import recommend_migration_paths`
    - tests/test_scoring_correctness.py contains `test_strict_scores_lower_than_lenient`
    - tests/test_scoring_correctness.py contains `test_validate_run_no_delta_param`
    - tests/test_scoring_correctness.py contains `test_dashboard_score_call_uses_profile_kwarg`
    - tests/test_scoring_correctness.py contains `calibration` (correct field path per Research Pitfall 2)
    - SCORE-02 tests (ValidateCorrectnessTests) FAIL when run -- `require_delta_if_baseline` still in signature
    - SCORE-04 tests (DashboardProfileTests) FAIL when run -- profile kwarg not yet in dashboard code
    - SCORE-01 test may PASS (regression guard per D-02 and Research Pitfall 1 -- strict=59, lenient=76)
    - SCORE-03 tests may PASS (regression guard per D-06 and Research live probe)
    - Existing 215 tests remain GREEN: `python3 -m pytest tests/ -v` shows no regressions
  </acceptance_criteria>
  <done>
    tests/test_scoring_correctness.py exists with 7+ test methods across 4 classes. SCORE-02 and SCORE-04 tests fail (RED). SCORE-01 and SCORE-03 tests pass or fail depending on current code state. No regressions in existing test suite.
  </done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/test_scoring_correctness.py -v` runs all new tests
- `python3 -m pytest tests/ -v` shows no regressions (215 existing tests still pass)
- SCORE-02 tests fail (validate_run still has require_delta_if_baseline param)
- SCORE-04 tests fail (dashboard still calls compute_readiness_score without profile)
</verification>

<success_criteria>
- tests/test_scoring_correctness.py exists with test coverage for all 4 SCORE requirements
- RED state confirmed for SCORE-02 and SCORE-04
- Zero regressions in existing test suite
- Test file follows project PEP 8 conventions
</success_criteria>

<output>
After completion, create `.planning/phases/14-scoring-intelligence-correctness/14-01-SUMMARY.md`
</output>
