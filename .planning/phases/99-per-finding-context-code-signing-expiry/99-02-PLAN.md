---
phase: 99-per-finding-context-code-signing-expiry
plan: 02
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - quirk/engine/findings_evaluator.py
  - quirk/scanner/codesign_scanner.py
  - run_scan.py
  - tests/test_codesign_expiry_classification.py
  - tests/test_codesign_findings_evaluator.py
  - tests/test_risk_engine.py
autonomous: true
requirements: [CTX-01, CTX-02, CTX-03]
must_haves:
  truths:
    - "Every finding from _build_finding carries a quantum_risk field (catalog or fallback)"
    - "Catalog-matched findings use REMEDIATION_CATALOG remediation and DROP the NIST_IR_8547_DEPRECATION boilerplate"
    - "An expired code-signing cert classifies HIGH; a cert within 90 days of not_after classifies MEDIUM"
    - "A SAFE-crypto-but-expired cert emits a finding (no longer silently dropped)"
    - "Expiry classification fires on BOTH the LDAP path and the TLS-EKU path"
    - "evaluate_codesign_endpoints emits finding dicts and is wired into run_scan.py findings"
  artifacts:
    - path: "quirk/engine/findings_evaluator.py"
      provides: "_build_finding quantum_risk injection + conditional NIST + evaluate_codesign_endpoints()"
      contains: "def evaluate_codesign_endpoints"
    - path: "quirk/scanner/codesign_scanner.py"
      provides: "_classify_codesign_severity expiry branch + TLS pseudo_parsed expiry fields"
      contains: "approaching-expiry"
    - path: "run_scan.py"
      provides: "evaluate_codesign_endpoints import + call appended to findings"
      contains: "evaluate_codesign_endpoints"
  key_links:
    - from: "run_scan.py"
      to: "evaluate_codesign_endpoints"
      via: "import at L37 + call after broker findings block (~L2124)"
      pattern: "evaluate_codesign_endpoints\\(codesign_endpoints\\)"
    - from: "quirk/engine/findings_evaluator.py:_build_finding"
      to: "quirk.reports.content_model"
      via: "import ALGO_IMPACT_MAP/REMEDIATION_CATALOG/_classify_finding"
      pattern: "from quirk.reports.content_model import"
---

<objective>
Make every finding carry context and specific remediation, and surface code-signing cert expiry
as a first-class finding. Three coordinated edits: (1) `_build_finding` injects `quantum_risk`
and applies catalog remediation with conditional NIST boilerplate (D-02/D-04/D-05); (2)
`_classify_codesign_severity` gains an independent expiry branch and the TLS path gets its
missing expiry fields (D-07/D-08/D-09); (3) a new `evaluate_codesign_endpoints()` turns
CODE_SIGNING endpoints into findings, wired into `run_scan.py` (CTX-03 crux — these endpoints
currently produce ZERO findings).

Purpose: CTX-01/CTX-02/CTX-03 acceptance. Depends on Plan 01's catalogs existing.
Output: enriched evaluator + scanner + run_scan wiring + three test files (two new, one updated).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-per-finding-context-code-signing-expiry/99-CONTEXT.md
@.planning/phases/99-per-finding-context-code-signing-expiry/99-RESEARCH.md
@.planning/phases/99-per-finding-context-code-signing-expiry/99-PATTERNS.md
@.planning/phases/99-per-finding-context-code-signing-expiry/99-UI-SPEC.md

<interfaces>
<!-- From Plan 01 (quirk/reports/content_model.py) — use directly, no exploration -->
ALGO_IMPACT_MAP: Dict[str, tuple[str, str, str]]   # index [2] = quantum_risk_sentence
REMEDIATION_CATALOG: Dict[str, str]                 # same key set as ALGO_IMPACT_MAP
FALLBACK_QUANTUM_RISK: str                          # default when no crypto-class match
_classify_finding(finding: dict) -> str | None      # matches via title/description/category/check_id keywords
# CODESIGN_EXPIRY / CODESIGN_APPROACHING_EXPIRY are keys in both maps and in _ALGO_KEYWORDS
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enrich _build_finding with quantum_risk + catalog remediation + conditional NIST</name>
  <files>quirk/engine/findings_evaluator.py, tests/test_risk_engine.py</files>
  <read_first>
    - quirk/engine/findings_evaluator.py:_build_finding (lines ~65-102), NIST_IR_8547_DEPRECATION constant, _dedupe_findings key tuple (~L303-312), module imports (~L10)
    - quirk/reports/content_model.py (Plan 01 output: ALGO_IMPACT_MAP, REMEDIATION_CATALOG, FALLBACK_QUANTUM_RISK, _classify_finding)
    - tests/test_risk_engine.py:TestBuildFinding::test_quantum_vulnerable_appends_deprecation_phrase (~line 87)
    - .planning/phases/99-per-finding-context-code-signing-expiry/99-RESEARCH.md §Pattern 3 + §Common Pitfalls 5
  </read_first>
  <behavior>
    - _build_finding(... title with "RSA") returns dict where finding["quantum_risk"] == ALGO_IMPACT_MAP["RSA"][2]
    - finding with no crypto-class match → finding["quantum_risk"] == FALLBACK_QUANTUM_RISK
    - catalog-matched finding → finding["recommendation"] == REMEDIATION_CATALOG[crypto_class] AND does NOT end with NIST_IR_8547_DEPRECATION
    - quantum_vulnerable=True finding with NO catalog match → recommendation still ends with NIST_IR_8547_DEPRECATION
    - _build_finding accepts check_id="CODESIGN_EXPIRY" so _classify_finding matches via check_id
    - quantum_risk is NOT part of the _dedupe_findings key tuple (key stays (host, port, title, recommendation))
  </behavior>
  <action>
    Add `from quirk.reports.content_model import ALGO_IMPACT_MAP, REMEDIATION_CATALOG,
    FALLBACK_QUANTUM_RISK, _classify_finding` at module top (safe import direction — content_model
    imports nothing from findings_evaluator). Add optional keyword param `check_id: str = ""` to
    `_build_finding`; store it in the returned dict so `_classify_finding` can match codesign
    findings.

    After building the base finding dict, call `crypto_class = _classify_finding(finding)`. Then
    per D-02: set `finding["quantum_risk"] = ALGO_IMPACT_MAP[crypto_class][2]` when crypto_class is
    in ALGO_IMPACT_MAP, else `FALLBACK_QUANTUM_RISK`. Per D-04: when crypto_class is in
    REMEDIATION_CATALOG, replace the recommendation with `REMEDIATION_CATALOG[crypto_class]`. Per
    D-05: append `NIST_IR_8547_DEPRECATION` ONLY when `quantum_vulnerable and crypto_class not in
    REMEDIATION_CATALOG`. Do NOT add quantum_risk to the `_dedupe_findings` key tuple — leave it
    `(host, port, title, recommendation)`.

    Update `tests/test_risk_engine.py::test_quantum_vulnerable_appends_deprecation_phrase`: its
    fixture must be a finding with NO catalog match (so the boilerplate is still appended), or split
    into two assertions (catalog-hit drops boilerplate, catalog-miss retains it). Keep diffs minimal.
  </action>
  <verify>
    <automated>python -m compileall quirk/engine/findings_evaluator.py && python -m pytest tests/test_risk_engine.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_risk_engine.py -x -q` exits 0
    - `grep -n 'def _dedupe_findings' -A 12 quirk/engine/findings_evaluator.py` shows the dedup key WITHOUT quantum_risk
    - `python -c "from quirk.engine.findings_evaluator import _build_finding; f=_build_finding(severity='HIGH',host='h',port=1,title='RSA-2048 weak',description='d',recommendation='r',quantum_vulnerable=True); print('quantum_risk' in f and 'NIST IR 8547' not in f['recommendation'])"` prints True
  </acceptance_criteria>
  <done>_build_finding attaches quantum_risk (catalog or fallback), applies catalog remediation, conditions NIST boilerplate on catalog miss, accepts check_id; dedup key unchanged; risk-engine tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add codesign expiry classification (both paths) + evaluate_codesign_endpoints + run_scan wiring</name>
  <files>quirk/scanner/codesign_scanner.py, quirk/engine/findings_evaluator.py, run_scan.py, tests/test_codesign_expiry_classification.py, tests/test_codesign_findings_evaluator.py</files>
  <read_first>
    - quirk/scanner/codesign_scanner.py:_classify_codesign_severity (~L145-174), scan_codesign_from_tls_endpoints pseudo_parsed (~L436-441), scan_codesign_from_ldap early-continue (~L356-358), CryptoEndpoint fields (cert_not_after L386/L473)
    - quirk/engine/findings_evaluator.py:evaluate_email_endpoints (~L754-841), evaluate_endpoints TLS cert-expiry datetime block (~L542-579)
    - run_scan.py import L37, broker findings wiring (~L2120-2124), codesign_endpoints assembly (~L1617, ~L1870)
    - .planning/phases/99-per-finding-context-code-signing-expiry/99-UI-SPEC.md §Code-Signing Expiry Findings (verbatim titles/descriptions/recommendations + {subject}/{not_after_date}/{days_remaining} placeholders)
    - .planning/phases/99-per-finding-context-code-signing-expiry/99-RESEARCH.md §Pattern 4/5 + §Common Pitfalls 2/3/4
  </read_first>
  <behavior>
    - _classify_codesign_severity with parsed["expired"]=True → ("HIGH", reasons containing "expired")
    - _classify_codesign_severity with not_after_dt within 90 days and not expired → ("MEDIUM", reasons containing "approaching-expiry")
    - SAFE-crypto-but-expired cert → severity is "HIGH" (not None) — finding NOT dropped
    - expiry stacks with weak crypto: reasons contains both "expired" and a weak-crypto reason; severity HIGH
    - scan_codesign_from_tls_endpoints pseudo_parsed includes "not_after_dt" and "expired" keys
    - evaluate_codesign_endpoints returns a list of dicts (not endpoints); expired branch uses check_id="CODESIGN_EXPIRY", approaching branch check_id="CODESIGN_APPROACHING_EXPIRY"
    - run_scan.py appends codesign_findings to the findings list
  </behavior>
  <action>
    In `codesign_scanner.py:_classify_codesign_severity` (D-07/D-08): after the existing
    weak-crypto checks and BEFORE the `if reasons:` return, add an independent expiry block. Read
    `parsed.get("expired")` (bool) and `parsed.get("not_after_dt")` (datetime). If expired →
    append "expired"; elif not_after_dt present and `0 <= days_remaining <= 90` (tzinfo-normalize
    to UTC before subtracting, mirroring findings_evaluator L544-546) → append "approaching-expiry".
    Change the return so any "expired"/weak-crypto reason → HIGH, else if reasons → MEDIUM, else
    (None, reasons). Ensure datetime/timezone are imported.

    In `scan_codesign_from_tls_endpoints` (D-09, Pitfall 2): extend `pseudo_parsed` to set
    `not_after_dt = getattr(ep, "cert_not_after", None)` and compute `expired` from it
    (tzinfo-normalized comparison), defaulting expired=False when not_after_dt is None.

    In `findings_evaluator.py` add `evaluate_codesign_endpoints(endpoints) -> List[Dict[str, Any]]`
    mirroring `evaluate_email_endpoints`. For each endpoint read host/port/severity/cert_subject/
    cert_not_after via getattr, parse `reasons` from `smime_scan_json` (json.loads, guarded). Three
    branches: "expired" in reasons → HIGH `_build_finding(check_id="CODESIGN_EXPIRY", ...)`;
    "approaching-expiry" in reasons → MEDIUM `_build_finding(check_id="CODESIGN_APPROACHING_EXPIRY",
    ...)`; else weak-crypto → HIGH `_build_finding(quantum_vulnerable=True, ...)`. Titles,
    descriptions, recommendations are the VERBATIM locked strings from 99-UI-SPEC.md §Code-Signing
    Expiry Findings with {subject}/{not_after_date}/{days_remaining} interpolated. For the
    weak-crypto branch (no locked title — research Open Question 2), use
    "Code-signing certificate uses weak algorithm: {subject}" with the reasons enumerated in the
    description and the catalog-driven recommendation.

    In `run_scan.py` (Pitfall 3): add `evaluate_codesign_endpoints` to the import at L37; after the
    broker findings block add `codesign_findings = evaluate_codesign_endpoints(codesign_endpoints)`
    then `if codesign_findings: findings = (findings or []) + codesign_findings`. `codesign_endpoints`
    is already assembled — do not re-collect.

    Create `tests/test_codesign_expiry_classification.py` (analog tests/test_codesign_scanner.py)
    and `tests/test_codesign_findings_evaluator.py` (analog tests/test_risk_engine.py) covering the
    behaviors above, including a TLS-path test asserting pseudo_parsed expiry fields and a
    stacking test.
  </action>
  <verify>
    <automated>python -m compileall quirk/scanner/codesign_scanner.py quirk/engine/findings_evaluator.py run_scan.py && python -m pytest tests/test_codesign_expiry_classification.py tests/test_codesign_findings_evaluator.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_codesign_expiry_classification.py tests/test_codesign_findings_evaluator.py -x -q` exits 0
    - `grep -c 'evaluate_codesign_endpoints' run_scan.py` >= 2 (import + call)
    - `grep -v '^#' quirk/scanner/codesign_scanner.py | grep -c 'approaching-expiry'` >= 1
    - `python -c "import json; from types import SimpleNamespace; from quirk.engine.findings_evaluator import evaluate_codesign_endpoints; from datetime import datetime,timezone,timedelta; e=SimpleNamespace(host='h',port=636,severity='HIGH',cert_subject='CN=x',cert_not_after=datetime.now(timezone.utc)-timedelta(days=1),smime_scan_json=json.dumps({'reasons':['expired']})); f=evaluate_codesign_endpoints([e]); assert isinstance(f,list) and isinstance(f[0],dict) and 'quantum_risk' in f[0]"` exits 0
  </acceptance_criteria>
  <done>Expiry classified on both codesign paths (expired=HIGH, ≤90d=MEDIUM, stacking, SAFE-expired not dropped); evaluate_codesign_endpoints emits enriched dict findings; run_scan wires them in; both new test files pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanned host cert → finding dict | cert_subject / cert_not_after are scanner-derived (untrusted host data) and flow into finding title/description |
| smime_scan_json → reasons list | scanner-serialized JSON parsed back; malformed JSON must not crash the evaluator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-03 | Tampering | cert_subject interpolated into finding title/description | mitigate | Field is scanner-derived; rendering plans (Plan 03) apply `| sanitize` (HTML) and `md_cell` (markdown); evaluator stores raw, renderers escape — consistent with existing email/broker finding handling |
| T-99-04 | Denial of Service | json.loads(smime_scan_json) on malformed input | mitigate | Wrap parse in try/except defaulting reasons=[] (mirrors existing evaluator guards); a bad blob yields no codesign finding rather than an exception |
| T-99-05 | Tampering | naive/aware datetime comparison for expiry | mitigate | Explicit tzinfo normalization to UTC before subtraction (mirror findings_evaluator.py L544-546) so expiry verdict cannot be flipped by tz ambiguity |
| T-99-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (enrichment only — RESEARCH confirms zero new deps); slopcheck N/A |
</threat_model>

<verification>
- `python -m compileall quirk/scanner/codesign_scanner.py quirk/engine/findings_evaluator.py run_scan.py` clean
- `python -m pytest tests/test_codesign_expiry_classification.py tests/test_codesign_findings_evaluator.py tests/test_risk_engine.py -x -q` green
- `python -m pytest tests/ -m "not slow" -q` green (wave-merge gate)
- run_scan.py imports and calls evaluate_codesign_endpoints
</verification>

<success_criteria>
Every finding leaves _build_finding with a quantum_risk field and weakness-specific (or
fallback) remediation; the NIST boilerplate appears only on catalog misses; code-signing cert
expiry produces HIGH/MEDIUM findings on both LDAP and TLS paths; evaluate_codesign_endpoints is
wired into run_scan.py so CODE_SIGNING endpoints finally reach the report.
</success_criteria>

<output>
Create `.planning/phases/99-per-finding-context-code-signing-expiry/99-02-SUMMARY.md` when done.
</output>
