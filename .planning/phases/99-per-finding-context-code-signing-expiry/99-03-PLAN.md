---
phase: 99-per-finding-context-code-signing-expiry
plan: 03
type: execute
wave: 3
depends_on: ["99-02"]
files_modified:
  - quirk/reports/technical.py
  - quirk/reports/templates/report.html.j2
  - tests/test_quantum_risk_render_parity.py
  - quantum-chaos-enterprise-lab/expected_results_v4.md
  - docs/UAT-SERIES.md
autonomous: true
requirements: [CTX-01, CTX-03]
must_haves:
  truths:
    - "CLI markdown findings table has a Quantum Risk column between Description and Recommendation"
    - "HTML All Findings table renders quantum_risk (sanitized); Top Findings cell shows a .quantum-risk-block"
    - "PDF inherits the HTML quantum_risk rendering (no separate work)"
    - "A finding with no quantum_risk renders the locked fallback string"
    - "expected_results_v4 ldaps section documents the new codesign expiry finding path"
  artifacts:
    - path: "quirk/reports/technical.py"
      provides: "Quantum Risk markdown column + fallback"
      contains: "Quantum Risk"
    - path: "quirk/reports/templates/report.html.j2"
      provides: "Quantum Risk column (All Findings) + .quantum-risk-block (Top Findings) + CSS classes"
      contains: "quantum-risk-block"
    - path: "tests/test_quantum_risk_render_parity.py"
      provides: "render-parity gate across markdown + HTML surfaces"
  key_links:
    - from: "quirk/reports/templates/report.html.j2"
      to: "quantum_risk field"
      via: "Jinja2 | sanitize filter"
      pattern: "quantum_risk[^|]*\\| sanitize"
    - from: "quirk/reports/technical.py"
      to: "quantum_risk field"
      via: "md_cell escaped column cell"
      pattern: "quantum_risk"
---

<objective>
Render the new `quantum_risk` field across all three report surfaces (D-03 parity): add a
Quantum Risk column to the CLI markdown findings table, add the column to the HTML "All
Findings" table plus a `.quantum-risk-block` inside the "Top Findings" Description cell (PDF
inherits HTML automatically), prove parity with a test, and update the chaos-lab expected-results
oracle for the new codesign expiry detection path. Close with the mandatory docs/UAT-SERIES sync.

Purpose: CTX-01 render parity + CTX-03 oracle update. Depends on Plan 02 producing findings that
carry quantum_risk.
Output: updated renderers + render-parity test + expected-results oracle + UAT-SERIES update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-per-finding-context-code-signing-expiry/99-CONTEXT.md
@.planning/phases/99-per-finding-context-code-signing-expiry/99-PATTERNS.md
@.planning/phases/99-per-finding-context-code-signing-expiry/99-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Quantum Risk to CLI markdown + HTML template (All Findings column + Top Findings block + CSS)</name>
  <files>quirk/reports/technical.py, quirk/reports/templates/report.html.j2</files>
  <read_first>
    - quirk/reports/technical.py:build_tech_markdown findings table (~L110-125)
    - quirk/reports/templates/report.html.j2: All Findings table (~L335-354), Top Findings table (~L276-291), embedded <style> block (~L9-126), existing `| sanitize` usage (~L283-286, L343-347)
    - .planning/phases/99-per-finding-context-code-signing-expiry/99-UI-SPEC.md §HTML/CSS Additions Contract (exact CSS) + §Copywriting Contract default fallback + §Interaction Contract + §CLI Markdown new column header
    - .planning/phases/99-per-finding-context-code-signing-expiry/99-PATTERNS.md §technical.py + §report.html.j2 sections
  </read_first>
  <behavior>
    - build_tech_markdown header row contains "Quantum Risk" between "Description" and "Recommendation"; separator row has matching column count
    - a finding with quantum_risk set renders that text in the markdown Quantum Risk cell (md_cell-escaped, truncated to 120)
    - a finding with no quantum_risk renders the locked fallback string in the markdown cell
    - rendered HTML All Findings table contains a <th>Quantum Risk</th> and the quantum_risk value
    - rendered HTML Top Findings Description cell contains a div with class "quantum-risk-block"
    - .quantum-risk-block and .quantum-risk-label CSS rules exist in the <style> block with the UI-SPEC values
  </behavior>
  <action>
    In `technical.py`: extend the pipe-table header and separator to 7 columns, inserting
    "Quantum Risk" between Description and Recommendation. In the row loop read
    `qr = (f.get("quantum_risk") or FALLBACK_QR)[:120]` (define FALLBACK_QR module-const from the
    UI-SPEC default-fallback string) and emit it via `md_cell(qr)`.

    In `report.html.j2`: in the "All Findings" table add a `<th>Quantum Risk</th>` and a matching
    `<td>` rendering `<span class="quantum-risk-label">Quantum Risk:</span>
    {{ f.get('quantum_risk', fallback)[:200] | sanitize }}` (executor may place the column before
    or after Recommendation per UI-SPEC discretion). In the "Top Findings" table, INSIDE the
    existing Description `<td>`, add a `.quantum-risk-block` div containing the
    `.quantum-risk-label` span + sanitized `quantum_risk[:120]`, gated on `f.get('quantum_risk')`.
    Add the `.quantum-risk-block` and `.quantum-risk-label` CSS rules verbatim from 99-UI-SPEC.md
    §HTML/CSS Additions Contract into the existing `<style>` block (no new color/size tokens; all
    reference existing CSS variables). The quantum_risk field MUST pass through `| sanitize`
    (scanner-derived text). PDF requires no work — Playwright re-renders the HTML.

    Keep diffs minimal; no React/dashboard changes (UI-SPEC: no dashboard surface).
  </action>
  <verify>
    <automated>python -m compileall quirk/reports/technical.py && python -c "import io; src=open('quirk/reports/templates/report.html.j2').read(); assert 'quantum-risk-block' in src and 'quantum-risk-label' in src and 'quantum_risk' in src and 'sanitize' in src"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' quirk/reports/technical.py | grep -c 'Quantum Risk'` >= 1
    - `grep -Ec 'quantum_risk[^|]*\| sanitize' quirk/reports/templates/report.html.j2` >= 1
    - `grep -c 'quantum-risk-block' quirk/reports/templates/report.html.j2` >= 2 (CSS rule + usage)
    - `python -m compileall quirk/reports/technical.py` exits 0
  </acceptance_criteria>
  <done>CLI markdown has a Quantum Risk column with fallback; HTML All Findings has the column and Top Findings has the sanitized .quantum-risk-block; CSS rules added; PDF inherits via HTML.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render-parity test + expected-results oracle update + UAT-SERIES sync</name>
  <files>tests/test_quantum_risk_render_parity.py, quantum-chaos-enterprise-lab/expected_results_v4.md, docs/UAT-SERIES.md</files>
  <read_first>
    - tests/test_score_render_parity.py (render-parity gate pattern, imports ~L1-19)
    - tests/test_html_renderer_coverage_gaps.py / tests/test_html_renderer_roadmap_section.py (render_html_report minimal-stub call style)
    - quirk/reports/technical.py:build_tech_markdown signature; quirk/reports/html_renderer.py:render_html_report signature
    - quantum-chaos-enterprise-lab/expected_results_v4.md §ldaps / CODE-SIGN section (current weak-algorithm finding entry)
    - docs/UAT-SERIES.md (relevant findings/report series + header Last Updated)
  </read_first>
  <behavior>
    - test_markdown_has_quantum_risk_column: build_tech_markdown output contains "Quantum Risk" header
    - test_html_all_findings_has_quantum_risk: render_html_report output contains the finding's quantum_risk text and a Quantum Risk header
    - test_html_top_findings_risk_block: rendered HTML contains class "quantum-risk-block"
    - test_render_fallback_when_missing: a finding without quantum_risk renders the fallback string in markdown
  </behavior>
  <action>
    Create `tests/test_quantum_risk_render_parity.py` (analog tests/test_score_render_parity.py)
    asserting the four behaviors above against `build_tech_markdown` and `render_html_report` with
    minimal findings stubs (one finding carrying quantum_risk, one without).

    Update `quantum-chaos-enterprise-lab/expected_results_v4.md` §ldaps/CODE-SIGN section per
    CLAUDE.md detection-change rule: document that code-signing certificate expiry now surfaces as
    a finding (expired → HIGH, ≤90 days → MEDIUM) on both LDAP and TLS-EKU paths, and note that the
    lab's 100-year-validity cert exercises only the SAFE/weak-algo path while expiry is covered by
    unit mocks. No lab.sh / profile change (content-only — no new profile/port/service).

    Mandatory project closeout (per CLAUDE.md + memory): update `docs/UAT-SERIES.md` — add/extend
    the relevant series to cover the new Quantum Risk column and code-signing expiry finding,
    bump the `**Last Updated:**` date, then sync to the Obsidian vault and commit per the CLAUDE.md
    "Mandatory Phase Completion Steps" recipe (UAT-SERIES sync + commit). The Obsidian phase note is
    handled at phase-completion, not here.
  </action>
  <verify>
    <automated>python -m pytest tests/test_quantum_risk_render_parity.py -x -q && python -m pytest tests/ -m "not slow" -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_quantum_risk_render_parity.py -x -q` exits 0
    - `python -m pytest tests/ -m "not slow" -q` exits 0 (full phase gate)
    - `grep -ic 'expiry' quantum-chaos-enterprise-lab/expected_results_v4.md` >= 1
    - `docs/UAT-SERIES.md` `**Last Updated:**` reflects today and mentions Quantum Risk / code-signing expiry
  </acceptance_criteria>
  <done>Render-parity test green across markdown + HTML; expected_results_v4 documents the expiry detection path; UAT-SERIES updated, synced, committed; full non-slow suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| finding dict → HTML/markdown render | scanner-derived quantum_risk / cert_subject cross into rendered report consumed by a human in a browser/terminal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-06 | Tampering / Spoofing | quantum_risk + finding fields rendered in report.html.j2 | mitigate | Render every scanner-derived cell through the `| sanitize` Jinja2 filter (env.filters["sanitize"] = sanitize_scanner_text), matching the existing template discipline; XSS via injected finding text is blocked at render |
| T-99-07 | Tampering | quantum_risk rendered in CLI markdown pipe-table | mitigate | Pass quantum_risk through `md_cell()` which escapes pipe/markdown specials, matching existing Description/Recommendation cell handling |
| T-99-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (render + docs only — RESEARCH confirms zero new deps); slopcheck N/A |
</threat_model>

<verification>
- `python -m compileall quirk/reports/technical.py` clean
- `python -m pytest tests/test_quantum_risk_render_parity.py -x -q` green
- `python -m pytest tests/ -m "not slow" -q` green (phase gate)
- report.html.j2 renders quantum_risk through `| sanitize`; markdown through `md_cell`
- expected_results_v4.md ldaps section documents the expiry path; UAT-SERIES synced + committed
</verification>

<success_criteria>
All three render surfaces show the per-finding quantum-risk context (markdown column, HTML All
Findings column + Top Findings block, PDF via HTML), missing values fall back to the locked
string, the chaos-lab oracle documents the new codesign expiry detection path, and UAT-SERIES is
updated/synced/committed.
</success_criteria>

<output>
Create `.planning/phases/99-per-finding-context-code-signing-expiry/99-03-SUMMARY.md` when done.
</output>
