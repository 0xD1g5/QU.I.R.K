---
phase: 89-chaos-lab-profiles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quantum-chaos-enterprise-lab/docker-compose.yml
  - quantum-chaos-enterprise-lab/README.md
  - quantum-chaos-enterprise-lab/expected_results_v4.md
  - labs/postgres-tls/Makefile
  - labs/postgres-tls/postgresql.conf
  - labs/postgres-tls/pg_hba.conf
  - labs/postgres-tls/README.md
  - labs/redis-tls/Makefile
  - labs/redis-tls/redis.conf
  - labs/redis-tls/README.md
  - labs/kafka-tls/Makefile
  - labs/kafka-tls/server.properties
  - labs/kafka-tls/README.md
  - tests/test_phase89_lab_expected_results.py
autonomous: true
requirements: [LAB-01, LAB-02, LAB-04]
must_haves:
  truths:
    - "docker compose --profile postgres-tls up starts a TLS PostgreSQL container with weak RSA-KX ciphers (D-02)"
    - "docker compose --profile redis-tls up starts a standalone TLS Redis container on 39380 with 3DES/RSA ciphers (D-02)"
    - "docker compose --profile kafka-tls up starts apache/kafka:3.9.0 with TLS listener 39093 + plaintext healthcheck 39092 (D-02)"
    - "Each new profile auto-appears in ./lab.sh profiles output (no ALL_PROFILES edit per RESEARCH lab.sh L58-70)"
    - "expected_results_v4.md and README profile table list all three new profiles with ports + expected weak-TLS findings"
  artifacts:
    - path: "labs/postgres-tls/postgresql.conf"
      provides: "Weak ssl_ciphers = AES128-SHA:AES256-SHA, TLS1.2-only (D-02)"
      contains: "ssl_ciphers"
    - path: "labs/redis-tls/redis.conf"
      provides: "Weak tls-ciphers DES-CBC3-SHA:AES128-SHA:AES256-SHA mirroring labs/broker (D-02)"
      contains: "tls-ciphers"
    - path: "labs/kafka-tls/server.properties"
      provides: "Weak ssl.cipher.suites RSA-KX, PEM keystore, apache/kafka 3.9.0 (D-02)"
      contains: "ssl.cipher.suites"
    - path: "quantum-chaos-enterprise-lab/expected_results_v4.md"
      provides: "## Profile sections for postgres-tls, redis-tls, kafka-tls"
      contains: "## Profile: postgres-tls"
  key_links:
    - from: "quantum-chaos-enterprise-lab/docker-compose.yml"
      to: "labs/postgres-tls/postgresql.conf"
      via: "volume bind-mount"
      pattern: "labs/postgres-tls"
    - from: "tests/test_chaos_lab_image_pinning.py"
      to: "quantum-chaos-enterprise-lab/docker-compose.yml"
      via: "CHAOS-05 version-tag pin gate"
      pattern: "image:"
---

<objective>
Add three new official-image weak-TLS chaos-lab profiles — postgres-tls (LAB-01),
redis-tls (LAB-02), kafka-tls (LAB-04) — each with a single intentionally weak/legacy
TLS variant (D-02) so the existing scanners emit real findings. Every profile satisfies
the CLAUDE.md four-file lab-sync obligation (docker-compose.yml + lab.sh auto-derive +
README + expected_results_v4.md) in the same change.

Purpose: Close LAB-01/02/04 — exercise scanner detection against new protocols.
Output: 3 labs/<name>/ support dirs, 3 compose service blocks, README + oracle entries,
a Wave 0 doc-completeness test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-chaos-lab-profiles/89-CONTEXT.md
@.planning/phases/89-chaos-lab-profiles/89-RESEARCH.md

<interfaces>
<!-- CHAOS-05 gate: tests/test_chaos_lab_image_pinning.py asserts every compose `image:`
     has a version tag (not :latest / bare). Tag-level pin satisfies it (RESEARCH L130). -->
<!-- lab.sh _derive_all_profiles() (lab.sh L58-70) reads profiles directly from
     docker-compose.yml via yq/grep — NO hardcoded ALL_PROFILES list to edit. -->
<!-- Verified analogs to mirror:
     labs/broker/redis/redis.conf  — weak tls-ciphers DES-CBC3-SHA:AES128-SHA:AES256-SHA, TLSv1.2
     labs/broker/kafka/server.properties — ssl.cipher.suites RSA-KX, ssl.keystore.type=PEM
     labs/broker/Makefile — RSA-2048 self-signed cert-gen (openssl req -x509 -newkey rsa:2048) -->
<!-- Ports (RESEARCH L186-194, clear of all existing allocations):
     postgres-tls 39432:5432 | redis-tls 39380:6380 + 39379:6379 | kafka-tls 39093:9093 + 39092:9092 -->
<!-- Image pins (RESEARCH L132-137; fetch fresh digests at exec time, tag-pin satisfies gate):
     postgres:16.6 | redis:7.4.1-alpine | apache/kafka:3.9.0 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0 (Wave 0): Doc-completeness scaffold test</name>
  <files>tests/test_phase89_lab_expected_results.py</files>
  <read_first>
    - quantum-chaos-enterprise-lab/expected_results_v4.md (existing "## Profile: <name>" section format, e.g. L448 broker, L471 tls-cert-defects)
    - quantum-chaos-enterprise-lab/README.md (Profile Summary table rows L25-43)
    - tests/test_chaos_lab_image_pinning.py (CHAOS-05 parse pattern to mirror)
  </read_first>
  <action>Create a non-slow pytest module that parses expected_results_v4.md and asserts a "## Profile: postgres-tls", "## Profile: redis-tls", and "## Profile: kafka-tls" section each exist AND each section body mentions its host port (39432 / 39380 / 39093 respectively). Also assert README.md Profile Summary table contains a row for each of the three profile names. Use `grep`-style line scanning, not full markdown parsing. Filter comment/prose noise: read raw lines, match the literal section headers. This test is RED until Tasks 1-3 add the sections.</action>
  <verify>
    <automated>QUIRK_DB_PATH=./quirk.db python -m pytest tests/test_phase89_lab_expected_results.py -q</automated>
  </verify>
  <done>Test file exists, imports cleanly, and FAILS (RED) because the three profile sections are not yet present. compileall passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: postgres-tls profile (LAB-01)</name>
  <files>labs/postgres-tls/Makefile, labs/postgres-tls/postgresql.conf, labs/postgres-tls/pg_hba.conf, labs/postgres-tls/README.md, quantum-chaos-enterprise-lab/docker-compose.yml, quantum-chaos-enterprise-lab/README.md, quantum-chaos-enterprise-lab/expected_results_v4.md</files>
  <read_first>
    - labs/broker/Makefile (RSA-2048 cert-gen pattern; note chmod 640 for postgres uid 999 per RESEARCH Pitfall 1)
    - quantum-chaos-enterprise-lab/docker-compose.yml around the existing postgres-plain block (L151) and an existing profiles:[...] service for the block shape
    - quantum-chaos-enterprise-lab/expected_results_v4.md "## Profile: database" (L370) for STARTTLS-postgres finding wording
  </read_first>
  <behavior>
    - With the profile up, sslyze ProtocolWithOpportunisticTlsEnum.POSTGRES on 39432 negotiates and enumerates ciphers.
    - Scanner emits a weak-cipher (RSA key exchange, no PFS) HIGH finding and an RSA-2048 cert MEDIUM finding.
  </behavior>
  <action>Create labs/postgres-tls/ with: a Makefile `certs` target mirroring labs/broker/Makefile (openssl req -x509 -newkey rsa:2048 ... -subj "/CN=postgres-tls.chaos.local", chmod 644 *.crt, chmod 640 *.key so postgres uid 999 can group-read per RESEARCH Pitfall 1); postgresql.conf with ssl=on, ssl_min/max_protocol_version='TLSv1.2', ssl_ciphers='AES128-SHA:AES256-SHA' (RSA key exchange, no forward secrecy — quantum-vulnerable, D-02); pg_hba.conf with `hostssl all all 0.0.0.0/0 trust`; a README.md noting the intentional weak posture. Add a `postgres-tls` service to docker-compose.yml using image postgres:16.6 (tag-pinned per CHAOS-05), profiles:["postgres-tls"], ports "39432:5432", bind-mounts for the conf + pg_hba + cert/key (mount key where postgres uid 999 can read it per Pitfall 1), command running with config_file=, and a pg_isready healthcheck. Add a README Profile Summary row (postgres-tls | postgres-tls | 39432 | link | v5.0 Phase 89) and a "## Profile: postgres-tls" section to expected_results_v4.md listing port 39432, the postgres-tls service, and the expected HIGH weak-cipher + MEDIUM RSA-2048 findings. NO modern duplicate (D-02).</action>
  <verify>
    <automated>cd quantum-chaos-enterprise-lab && docker compose config -q && python -m pytest ../tests/test_chaos_lab_image_pinning.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config -q` validates (compose well-formed).
    - CHAOS-05 image-pin gate passes (postgres:16.6 tag present).
    - `./lab.sh profiles` lists `postgres-tls`.
    - LIVE (Docker-gated, slow): `PROFILE_ARGS="--profile postgres-tls" ./lab.sh up` brings up a healthy container; `QUIRK_DB_PATH=./quirk.db` scan of localhost:39432 emits the weak-cipher HIGH finding asserted in expected_results_v4.md.
    - expected_results_v4.md "## Profile: postgres-tls" section present (advances Task 0 toward GREEN).
  </acceptance_criteria>
  <done>postgres-tls profile up, scanner-verified weak findings, four-file lab-sync complete (compose + lab.sh auto-derive + README + expected_results).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: redis-tls standalone profile (LAB-02)</name>
  <files>labs/redis-tls/Makefile, labs/redis-tls/redis.conf, labs/redis-tls/README.md, quantum-chaos-enterprise-lab/docker-compose.yml, quantum-chaos-enterprise-lab/README.md, quantum-chaos-enterprise-lab/expected_results_v4.md</files>
  <read_first>
    - labs/broker/redis/redis.conf (verified weak config to copy: tls-ciphers DES-CBC3-SHA:AES128-SHA:AES256-SHA, tls-protocols "TLSv1.2")
    - labs/broker/Makefile (cert-gen)
    - quantum-chaos-enterprise-lab/expected_results_v4.md "## Profile: broker" (L448) for the redis-broker finding wording to mirror
  </read_first>
  <behavior>
    - broker_scanner.py scan_redis_targets() connects to 39380 via direct-socket TLS and classifies the cipher.
    - Scanner emits weak-cipher (3DES, RSA key exchange) HIGH finding; plaintext port 39379 flagged HIGH.
  </behavior>
  <action>Create a STANDALONE redis-tls profile (NOT inside the broker profile per RESEARCH Pitfall 6). labs/redis-tls/redis.conf copies labs/broker/redis/redis.conf exactly (port 6379, tls-port 6380, tls-ciphers "DES-CBC3-SHA:AES128-SHA:AES256-SHA", tls-protocols "TLSv1.2", tls-auth-clients no), pointing at /etc/redis/certs/redis-tls.{crt,key}. labs/redis-tls/Makefile generates the RSA-2048 cert (CN=redis-tls.chaos.local). Add a redis-tls service to docker-compose.yml: image redis:7.4.1-alpine (tag-pinned), profiles:["redis-tls"], ports "39380:6380" and "39379:6379", conf + certs bind-mounts, redis-cli ping healthcheck. The existing broker profile is UNCHANGED. Add a README Profile Summary row and a "## Profile: redis-tls" expected_results_v4.md section listing ports 39380/39379 and the expected HIGH weak-cipher + HIGH plaintext-port findings. NO modern duplicate (D-02).</action>
  <verify>
    <automated>cd quantum-chaos-enterprise-lab && docker compose config -q && python -m pytest ../tests/test_chaos_lab_image_pinning.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config -q` validates; CHAOS-05 gate passes (redis:7.4.1-alpine tag).
    - `./lab.sh profiles` lists `redis-tls`; broker profile services unchanged.
    - LIVE (Docker-gated): `PROFILE_ARGS="--profile redis-tls" ./lab.sh up`; scan of 39380 via broker_scanner Redis-TLS probe emits the weak-cipher HIGH finding asserted in expected_results_v4.md.
    - "## Profile: redis-tls" section present.
  </acceptance_criteria>
  <done>redis-tls standalone profile up, broker_scanner.py probe-verified, four-file lab-sync complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: kafka-tls profile (LAB-04)</name>
  <files>labs/kafka-tls/Makefile, labs/kafka-tls/server.properties, labs/kafka-tls/README.md, quantum-chaos-enterprise-lab/docker-compose.yml, quantum-chaos-enterprise-lab/README.md, quantum-chaos-enterprise-lab/expected_results_v4.md</files>
  <read_first>
    - labs/broker/kafka/server.properties (verified weak config: ssl.cipher.suites RSA-KX, ssl.keystore.type=PEM, ssl.enabled.protocols=TLSv1.2)
    - labs/broker/certs/ (inspect whether the existing PEM keystore is combined cert+key or separate — RESEARCH Pitfall 2 / Open Q3; mirror that exact format)
    - labs/broker/Makefile (cert-gen)
  </read_first>
  <behavior>
    - broker_scanner.py scan_kafka_targets() detects the plaintext listener on 39092 (HIGH) and classifies the TLS listener on 39093 (weak cipher HIGH, RSA-2048 cert MEDIUM).
  </behavior>
  <action>Create a NEW standalone kafka-tls profile using image apache/kafka:3.9.0 (tag-pinned; upgrade from 3.7.0 in broker — broker UNCHANGED). labs/kafka-tls/server.properties mirrors labs/broker/kafka/server.properties weak knobs: ssl.enabled.protocols=TLSv1.2, ssl.cipher.suites=TLS_RSA_WITH_AES_128_CBC_SHA,TLS_RSA_WITH_AES_256_CBC_SHA, ssl.keystore.type=PEM, ssl.client.auth=none; KRaft single-node listeners PLAINTEXT://:9092,SSL://:9093,CONTROLLER://:9094 with advertised SSL://localhost:39093 + PLAINTEXT://localhost:39092. Match the broker profile's verified PEM keystore file format (combined vs separate — confirmed in read_first). labs/kafka-tls/Makefile generates the RSA-2048 cert (CN=kafka-tls.chaos.local). Add the docker-compose service: profiles:["kafka-tls"], ports "39093:9093" + "39092:9092", server.properties + certs bind-mounts, healthcheck on the PLAINTEXT port 9092 (NOT SSL — avoids truststore, per RESEARCH L335). Add a README row and a "## Profile: kafka-tls" expected_results_v4.md section listing 39092/39093 and expected plaintext-listener HIGH + weak-cipher HIGH + RSA-2048 MEDIUM findings. NO modern duplicate (D-02).</action>
  <verify>
    <automated>cd quantum-chaos-enterprise-lab && docker compose config -q && python -m pytest ../tests/test_chaos_lab_image_pinning.py ../tests/test_phase89_lab_expected_results.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config -q` validates; CHAOS-05 gate passes (apache/kafka:3.9.0 tag).
    - `./lab.sh profiles` lists `kafka-tls`; broker profile (kafka-broker 3.7.0) unchanged.
    - LIVE (Docker-gated): `PROFILE_ARGS="--profile kafka-tls" ./lab.sh up`; `docker logs kafka-tls` shows clean KRaft + SSL listener start (verify PEM keystore loads — Pitfall 2); scan emits the plaintext + weak-cipher findings asserted in expected_results_v4.md.
    - Task 0 doc-completeness test now PASSES GREEN (all three sections present).
  </acceptance_criteria>
  <done>kafka-tls profile up, broker_scanner.py probe-verified, four-file lab-sync complete, Task 0 test GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanner host → lab container TLS port | Scanner probes intentionally-weak TLS endpoints (39432/39380/39379/39092/39093) on the lab network |
| Docker image registry → lab host | Pulled images become running containers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01 | Information Disclosure | Weak-TLS lab services exposed beyond the lab | accept | Lab-only fixtures by design (D-02); ports bind to host 39xxx for a local Docker host, not a public interface. Weak crypto IS the test fixture. |
| T-89-02 | Information Disclosure | expected_results_v4.md / README content | mitigate | Oracle entries describe findings + self-signed CN=*.chaos.local certs only; no real secrets or private keys committed (certs are gitignored, generated by Makefile). |
| T-89-SC | Tampering | docker pull of postgres/redis/apache-kafka images | mitigate | Official Docker library images only (RESEARCH Package Legitimacy Audit, all Approved); CHAOS-05 version-tag pin gate (tests/test_chaos_lab_image_pinning.py) blocks :latest/bare tags. No [ASSUMED]/[SUS] packages → no human checkpoint required. |
</threat_model>

<verification>
- `cd quantum-chaos-enterprise-lab && docker compose config -q` validates.
- `python -m pytest tests/test_chaos_lab_image_pinning.py tests/test_phase89_lab_expected_results.py -q` green.
- `./lab.sh profiles` lists postgres-tls, redis-tls, kafka-tls.
- Live (Docker-gated): each profile brought up + scanner emits the asserted weak-TLS findings.
- No new failures vs the 39-failure baseline.
</verification>

<success_criteria>
- LAB-01, LAB-02, LAB-04 profiles up and scanner-verified with weak findings (D-02).
- Four-file lab-sync complete for all three (compose + lab.sh auto-derive + README + expected_results_v4.md).
- broker profile unchanged; no modern duplicates.
- CHAOS-05 gate + doc-completeness test green.
</success_criteria>

<output>
Create `.planning/phases/89-chaos-lab-profiles/89-01-SUMMARY.md` when done.
</output>
