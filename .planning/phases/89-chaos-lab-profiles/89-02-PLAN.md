---
phase: 89-chaos-lab-profiles
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - config.yaml
  - quirk/scanner/dnssec_scanner.py
  - tests/test_phase89_lab_config_identity.py
  - docs/UAT-SERIES.md
autonomous: false
requirements: [LAB-06]
must_haves:
  truths:
    - "config.yaml enables Kerberos/SAML/DNSSEC connectors with live identity-lab targets (D-04)"
    - "All three identity evidence counters (identity_weak_etype_count, saml_weak_signing_count, dnssec_weak_algo_count) flow non-zero into the identity subscore against the live identity profiles (D-04)"
    - "DNSSEC scanner can target the lab resolver on port 15353 (or a surfaced blocker if no override exists)"
  artifacts:
    - path: "config.yaml"
      provides: "enable_kerberos/enable_saml/enable_dnssec + targets for the identity lab (D-04)"
      contains: "enable_kerberos"
    - path: "tests/test_phase89_lab_config_identity.py"
      provides: "Asserts config.yaml identity connector keys present"
      contains: "enable_dnssec"
  key_links:
    - from: "config.yaml"
      to: "quirk/intelligence/evidence.py"
      via: "connector targets → evidence counters (evidence.py L165/L171/L180)"
      pattern: "enable_(kerberos|saml|dnssec)"
    - from: "quirk/intelligence/evidence.py"
      to: "quirk/intelligence/scoring.py"
      via: "counters → identity subscore weights (scoring.py L159-161, L196-198)"
      pattern: "(identity_weak_etype|saml_weak|dnssec_weak)_count"
---

<objective>
Close LAB-06: confirm the BACK-78 identity-evidence wiring end-to-end. Add Kerberos/SAML/DNSSEC
targets to the EXISTING config.yaml scan config (D-04 — not a new file) and prove all three
evidence counters flow non-zero into the identity subscore against the live identity profiles.
The detection code is already wired (RESEARCH: evidence.py L87-89/L165/L171-183, scoring.py
L31-33/L159-161/L196-198) — this is verification + config, NOT new detection code.

Purpose: Confirm the identity evidence gap is closed.
Output: config.yaml identity connector keys, a config-presence test, a UAT assertion, and (if
needed) a DNSSEC resolver-port override.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-chaos-lab-profiles/89-CONTEXT.md
@.planning/phases/89-chaos-lab-profiles/89-RESEARCH.md

<interfaces>
<!-- Identity evidence chain already wired (RESEARCH L496-512, VERIFIED):
     evidence.py::build_evidence_summary() emits identity_weak_etype_count (L165),
     saml_weak_signing_count (L171/L173), dnssec_weak_algo_count (L180/L183), ratios L397-399.
     scoring.py::compute_readiness_score() reads them L159-161, weights 10.0/8.0/8.0 L196-198. -->
<!-- Identity-lab containers (RESEARCH L519-522): kerberos profile samba-dc 88/389;
     saml profile simplesamlphp 8080; dnssec profile bind9-dnssec 15353/udp+tcp. -->
<!-- config_template.yaml L62-70 shows the exact connector key shape to copy:
     enable_kerberos/kerberos_targets, enable_saml/saml_targets, enable_dnssec/dnssec_targets. -->
<!-- KNOWN GAP (RESEARCH Pitfall 5 / Open Q2, confirmed during planning):
     dnssec_scanner.py hardcodes port=53 (L243,293,306,319,333) and uses dns.resolver.resolve
     for NS lookup (L68,73) — NO resolver-port override exists. bind9-dnssec listens on 15353.
     Task 1 must add a resolver override OR surface this as a blocker (NOT a silent skip). -->
<!-- macOS caveat (RESEARCH Pitfall 4): kerberos profile port 88 collides with system KDC;
     lab.sh auto-skips on Darwin unless LAB_INCLUDE_KERBEROS=1. UAT may need Linux/CI or KDC stop. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Identity config + DNSSEC resolver override</name>
  <files>config.yaml, quirk/scanner/dnssec_scanner.py, tests/test_phase89_lab_config_identity.py</files>
  <read_first>
    - config.yaml (connectors: block at L30 — currently only enable_aws/enable_azure)
    - quirk/config_template.yaml L62-70 (exact identity connector key shape)
    - quirk/scanner/dnssec_scanner.py L60-90 + L240-345 (NS resolution + per-RR queries hardcode port=53; scan_dnssec_targets signature L345)
  </read_first>
  <action>Add a `connectors:` extension to config.yaml (D-04, EXISTING file) with enable_kerberos: true + kerberos_targets ["127.0.0.1"] (samba-dc 88/389), enable_saml: true + saml_targets ["http://localhost:8080/simplesaml/saml2/idp/metadata.php"], enable_dnssec: true + dnssec_targets ["weak.example.com","unsigned.example.com"], mirroring config_template.yaml L62-70. For DNSSEC: since dnssec_scanner.py hardcodes port=53 with no override (confirmed gap), add a minimal resolver-override capability — accept a `dnssec_resolver` config value (host:port, default unset → system resolver) threaded into scan_dnssec_targets and the dns.resolver/dns.query calls so the lab's bind9 on 127.0.0.1:15353 is reachable. Keep the diff minimal (PEP 8). Add a dnssec_resolver: "127.0.0.1:15353" key to config.yaml. If the override proves infeasible without broader scanner refactor, STOP and surface as a blocker per D-04/D-03 ethos (no silent fallback). Create tests/test_phase89_lab_config_identity.py asserting config.yaml contains enable_kerberos/enable_saml/enable_dnssec set true and the three *_targets keys present (non-slow).</action>
  <verify>
    <automated>QUIRK_DB_PATH=./quirk.db python -m pytest tests/test_phase89_lab_config_identity.py -q && python -m compileall quirk/scanner/dnssec_scanner.py</automated>
  </verify>
  <acceptance_criteria>
    - config.yaml has all three enable_* connectors true + targets + dnssec_resolver:15353.
    - test_phase89_lab_config_identity.py PASSES GREEN.
    - dnssec_scanner.py compiles; resolver override accepts a host:port and routes queries there (unit-checkable: pass dnssec_resolver and confirm the resolver/query target reflects 15353).
    - If resolver override infeasible → blocker surfaced in SUMMARY, not silently skipped.
  </acceptance_criteria>
  <done>config.yaml identity connectors enabled + DNSSEC resolver reaches port 15353; config-presence test green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>config.yaml now enables Kerberos/SAML/DNSSEC connectors against the live identity, saml, and dnssec lab profiles, with a DNSSEC resolver override pointing at the lab's bind9 on 127.0.0.1:15353. The evidence→scoring chain is already wired (BACK-78); this confirms it end-to-end (LAB-06 / D-04).</what-built>
  <how-to-verify>
    1. Bring up the identity-lab profiles (Docker required). On macOS, kerberos needs `LAB_INCLUDE_KERBEROS=1` and the system KDC stopped (RESEARCH Pitfall 4) — or run on Linux/CI:
       `cd quantum-chaos-enterprise-lab && PROFILE_ARGS="--profile saml --profile dnssec --profile kerberos" ./lab.sh up`
    2. Run a scan with the identity config:
       `QUIRK_DB_PATH=./quirk.db quirk scan --config config.yaml` (or the project's scan entrypoint)
    3. Inspect the evidence counters in the output:
       `cat output/intelligence-*.json | python3 -m json.tool | grep -E "identity_weak_etype|saml_weak|dnssec_weak"`
    4. Expected: all three counters NON-ZERO and the identity subscore reflects them. If any is zero, the scan did not reach that profile's containers — check the profile is running and config targets/ports match (esp. DNSSEC 15353, kerberos port-88 collision).
  </how-to-verify>
  <resume-signal>Type "approved" once all three counters are non-zero and flowing into the identity subscore, or describe which counter is zero.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanner → identity-lab services | Scanner queries Kerberos KDC / SAML metadata / DNSSEC zones on the lab network |
| config.yaml → scanner connectors | Operator-controlled scan config selects targets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-04 | Information Disclosure | config.yaml targets (localhost identity lab) | accept | All targets are localhost lab fixtures (127.0.0.1, localhost:8080, *.example.com lab zones); no real production hosts or secrets. |
| T-89-05 | Tampering | DNSSEC resolver override pointing at 127.0.0.1:15353 | mitigate | Override is opt-in via config key, defaults unset (system resolver); restricted to a host:port value consumed only by the dnssec scanner — no broad code-path change. |
</threat_model>

<verification>
- `python -m pytest tests/test_phase89_lab_config_identity.py -q` green.
- `python -m compileall quirk/scanner/dnssec_scanner.py` clean.
- Human-verify checkpoint: three identity counters non-zero against live profiles, flowing into the identity subscore.
- No new failures vs the 39-failure baseline.
</verification>

<success_criteria>
- LAB-06: identity evidence confirmed flowing end-to-end (D-04).
- config.yaml identity connectors enabled (existing file, not a new config).
- DNSSEC resolver reaches the lab's port 15353 (or blocker surfaced).
</success_criteria>

<output>
Create `.planning/phases/89-chaos-lab-profiles/89-02-SUMMARY.md` when done.
</output>
