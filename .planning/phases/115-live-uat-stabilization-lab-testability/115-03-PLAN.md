---
phase: 115-live-uat-stabilization-lab-testability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - quantum-chaos-enterprise-lab/docker-compose.distributed.yml
  - quantum-chaos-enterprise-lab/sensor-config-b.yaml
  - quantum-chaos-enterprise-lab/lab.sh
  - quantum-chaos-enterprise-lab/scripts/distributed-e2e.sh
  - quantum-chaos-enterprise-lab/expected_results_distributed.md
  - quantum-chaos-enterprise-lab/README.md
  - docs/UAT-SERIES.md
autonomous: true
requirements: [LAB-01]
must_haves:
  truths:
    - "A weak-TLS target (tls-weak-b) exists on segment-b at 10.20.0.20, reachable only by sensor-b"
    - "sensor-b scans both crypto.internal (modern) and 10.20.0.20 (weak TLS); sensor-a cannot reach 10.20.0.20"
    - "lab.sh, expected_results_distributed.md, and chaos-lab README are updated in this same change (no-drift)"
  artifacts:
    - path: "quantum-chaos-enterprise-lab/docker-compose.distributed.yml"
      provides: "tls-weak-b service on segment-b"
      contains: "tls-weak-b"
    - path: "quantum-chaos-enterprise-lab/sensor-config-b.yaml"
      provides: "sensor-b target list including the weak-TLS IP"
      contains: "10.20.0.20"
    - path: "quantum-chaos-enterprise-lab/expected_results_distributed.md"
      provides: "weak-TLS oracle rows + per-segment filter validation"
      contains: "tls-weak-b"
  key_links:
    - from: "sensor-b (segment-b)"
      to: "tls-weak-b (10.20.0.20:443)"
      via: "sensor-config-b.yaml include_ips + segment-b network membership"
      pattern: "10.20.0.20"
    - from: "docker-compose.distributed.yml"
      to: "nginx/legacy/nginx.conf"
      via: "volume mount reusing the tls-legacy pattern"
      pattern: "nginx/legacy/nginx.conf"
---

<objective>
Add a weak-TLS target to segment-b of the distributed chaos lab so the Phase 111
per-segment filter (the previously-blocked Test 7) is exercisable end-to-end (LAB-01).
Apply the CLAUDE.md no-drift rule: docker-compose + lab.sh + e2e script + oracle + README
update in the same change. Also finalize the per-phase docs/UAT-SERIES + Obsidian sync.

Purpose: `lab.sh distributed e2e` exercises a real weak-crypto, segment-isolated target.
Output: tls-weak-b service, sensor-config-b.yaml, updated lab docs and oracle, UAT-SERIES sync.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-live-uat-stabilization-lab-testability/115-CONTEXT.md
@.planning/phases/115-live-uat-stabilization-lab-testability/115-RESEARCH.md
@.planning/phases/115-live-uat-stabilization-lab-testability/115-PATTERNS.md

<interfaces>
docker-compose.distributed.yml: segment-a 10.10.0.0/24, segment-b 10.20.0.0/24. Existing
  tls-target-b (L54-65): nginx:1.28.0, nginx/modern/nginx.conf, expose 443, ipv4 10.20.0.10,
  alias crypto.internal. sensor-b joins segment-b + console-net, mounts sensor-config.yaml.
docker-compose.yml tls-legacy (L18-24): nginx:1.28.0, nginx/legacy/nginx.conf, certs.
  nginx/legacy/nginx.conf already exists (TLS 1.0/1.1 + HIGH:MEDIUM ciphers).
lab.sh distributed arm (L216-267): delegates to docker compose -f docker-compose.distributed.yml;
  no per-service profile enumeration — ALL_PROFILES change NOT required.
scripts/distributed-e2e.sh: sensor-b enroll + push at ~L115-121.
CHAOS-05: all services pinned to nginx:1.28.0 (no :latest).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tls-weak-b service + sensor-config-b.yaml (LAB-01)</name>
  <files>quantum-chaos-enterprise-lab/docker-compose.distributed.yml, quantum-chaos-enterprise-lab/sensor-config-b.yaml</files>
  <read_first>
    - quantum-chaos-enterprise-lab/docker-compose.distributed.yml (tls-target-b L54-65; sensor-b service + its sensor-config.yaml mount)
    - quantum-chaos-enterprise-lab/docker-compose.yml (tls-legacy L18-24)
    - quantum-chaos-enterprise-lab/nginx/legacy/nginx.conf (reused as-is)
    - quantum-chaos-enterprise-lab/sensor-config.yaml (schema to copy)
    - 115-PATTERNS.md "docker-compose.distributed.yml (LAB-01)" + "sensor-config-b.yaml" sections
    - 115-RESEARCH.md "LAB-01: Weak-TLS Distributed Target" + Pitfall 5 + open-Q3
  </read_first>
  <action>
    Add a `tls-weak-b` service to docker-compose.distributed.yml: image nginx:1.28.0
    (CHAOS-05 pinned), volumes mounting ./nginx/legacy/nginx.conf and ./certs (reuse the
    tls-legacy pattern, D-10 — no new image/cert), expose 443 (no host-port binding), on
    the segment-b network with ipv4_address 10.20.0.20 and NO crypto.internal alias
    (distinct from the modern target). Create sensor-config-b.yaml by copying
    sensor-config.yaml and adding 10.20.0.20 to include_ips (open-Q3 — separate config,
    not dual targets in the shared file). Change the sensor-b service volume mount to
    mount sensor-config-b.yaml at /quirk/sensor-config.yaml so only sensor-b scans the
    weak target — preserving segment-a isolation (sensor-a cannot reach 10.20.0.20).
  </action>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('quantum-chaos-enterprise-lab/docker-compose.distributed.yml')); assert 'tls-weak-b' in d['services']; assert d['services']['tls-weak-b']['image']=='nginx:1.28.0'; assert d['services']['tls-weak-b']['networks']['segment-b']['ipv4_address']=='10.20.0.20'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - tls-weak-b present, nginx:1.28.0, mounts nginx/legacy/nginx.conf, ipv4 10.20.0.20 on segment-b, no crypto.internal alias.
    - sensor-config-b.yaml exists with 10.20.0.20 in include_ips.
    - sensor-b service mounts sensor-config-b.yaml (not the shared sensor-config.yaml).
    - both compose + config files parse as valid YAML.
  </acceptance_criteria>
  <done>Weak-TLS target on segment-b, scanned only by sensor-b; segment isolation preserved.</done>
</task>

<task type="auto">
  <name>Task 2: No-drift updates — lab.sh, e2e script, oracle, README (LAB-01 / D-11)</name>
  <files>quantum-chaos-enterprise-lab/lab.sh, quantum-chaos-enterprise-lab/scripts/distributed-e2e.sh, quantum-chaos-enterprise-lab/expected_results_distributed.md, quantum-chaos-enterprise-lab/README.md</files>
  <read_first>
    - quantum-chaos-enterprise-lab/lab.sh (distributed arm L216-267)
    - quantum-chaos-enterprise-lab/scripts/distributed-e2e.sh (sensor-b push ~L115-121)
    - quantum-chaos-enterprise-lab/expected_results_distributed.md (Services table L49-56; E2E outcome L88-99)
    - quantum-chaos-enterprise-lab/README.md (distributed-lab section)
    - 115-PATTERNS.md "lab.sh" + "expected_results_distributed.md" sections
    - CLAUDE.md "Chaos Lab Maintenance" no-drift rule
  </read_first>
  <action>
    Verify the lab.sh distributed arm cleanly handles the new service (generic compose
    ps/logs — RESEARCH confirms no ALL_PROFILES change is required; if any distributed
    service list/comment exists in lab.sh, add tls-weak-b for accuracy). In
    distributed-e2e.sh ensure sensor-b's push exercises the weak target via the
    now-mounted sensor-config-b.yaml and keep `lab.sh distributed e2e` green; add or
    adjust the Test 7 per-segment assertion so it verifies sensor-b merged output contains
    a 10.20.0.20 finding and sensor-a output does not. Add a tls-weak-b row to the
    expected_results_distributed.md Services table and a "LAB-01: Per-Segment Weak-TLS
    Filter Validation" oracle section (weak-TLS findings: TLS 1.0/1.1, legacy cipher,
    elevated quantum_risk; sensor-a sees zero 10.20.0.20 rows, sensor-b sees them with
    non-null scanned_at). Update the chaos-lab README distributed section to list
    tls-weak-b (port/segment/expected findings).
  </action>
  <verify>
    <automated>bash -n quantum-chaos-enterprise-lab/lab.sh && bash -n quantum-chaos-enterprise-lab/scripts/distributed-e2e.sh && grep -q "tls-weak-b" quantum-chaos-enterprise-lab/expected_results_distributed.md && grep -q "tls-weak-b" quantum-chaos-enterprise-lab/README.md</automated>
  </verify>
  <acceptance_criteria>
    - bash -n passes on lab.sh and distributed-e2e.sh.
    - expected_results_distributed.md has a tls-weak-b Services-table row + the per-segment validation section.
    - README distributed section lists tls-weak-b.
    - Test 7 assertion checks sensor-b sees 10.20.0.20 and sensor-a does not.
  </acceptance_criteria>
  <done>lab.sh + e2e script + oracle + README updated in this same change (no-drift rule satisfied).</done>
</task>

<task type="auto">
  <name>Task 3: Update docs/UAT-SERIES.md + sync to Obsidian</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (distributed-lab / live-UAT series header + relevant test cases)
    - CLAUDE.md "Mandatory Phase Completion Steps" §2/§3/§4 (UAT-SERIES update + Obsidian sync + commit)
  </read_first>
  <action>
    Update docs/UAT-SERIES.md to reflect Phase 115 changes: lab re-runnable without
    `docker compose down -v` (STAB-01); scheduled scans exit 0 (STAB-03); no phantom
    email/broker rows in merged output (STAB-04); cmvp_cache.json packaged (STAB-02); and
    a new test case for the segment-b weak-TLS target + per-segment filter (LAB-01, the
    previously-blocked Test 7). Bump the `**Last Updated:**` date. Then sync to Obsidian
    by writing the file (with frontmatter) directly to the vault filesystem at
    /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md using the printf+cat pattern
    in CLAUDE.md §3 (file too large for the obsidian CLI content= parameter).
  </action>
  <verify>
    <automated>grep -q "tls-weak-b\|weak-TLS\|Phase 115\|STAB-01" docs/UAT-SERIES.md && test -f /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/UAT-SERIES.md references the Phase 115 stabilization changes + the weak-TLS test case, with an updated date.
    - The vault copy at 20_Dev-Work/QUIRK/UAT-Series.md exists and includes frontmatter.
  </acceptance_criteria>
  <done>UAT-SERIES.md updated for Phase 115 and synced to the Obsidian vault.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| segment-a ↔ segment-b | Docker network isolation; sensor-a must not reach segment-b hosts |
| sensor-b → weak-TLS target | intentionally insecure target, scanned only from within segment-b |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-09 | Information Disclosure | tls-weak-b (intentionally weak TLS) | accept | Lab-only target; isolated on segment-b; no host-port binding (expose 443 only); not reachable from the host or segment-a |
| T-115-10 | Spoofing/Tampering | segment isolation | mitigate | tls-weak-b joins only segment-b; sensor-config-b.yaml mounted to sensor-b only; sensor-a cannot route to 10.20.0.20 — Test 7 asserts the per-segment boundary holds |
| T-115-11 | Tampering | image supply chain | mitigate | nginx:1.28.0 pinned (CHAOS-05); reuses existing in-repo nginx/legacy/nginx.conf + certs — no new image or external fetch |
</threat_model>

<verification>
- `python -c "import yaml; yaml.safe_load(open('quantum-chaos-enterprise-lab/docker-compose.distributed.yml'))"` parses.
- `bash -n` clean on lab.sh + distributed-e2e.sh.
- Oracle + README + UAT-SERIES all mention tls-weak-b / weak-TLS (no-drift verified).
</verification>

<success_criteria>
- LAB-01: weak-TLS target on segment-b, scanned only by sensor-b; segment isolation preserved.
- No-drift: docker-compose + lab.sh + e2e + oracle + README + UAT-SERIES updated together.
- Test 7 per-segment filter exercisable end-to-end.
</success_criteria>

<output>
Create `.planning/phases/115-live-uat-stabilization-lab-testability/115-03-SUMMARY.md` when done
</output>
