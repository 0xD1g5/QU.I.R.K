---
phase: 115-live-uat-stabilization-lab-testability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - quirk/compliance/cmvp.py
  - quirk/cli/scheduler_cmd.py
  - tests/test_scheduler_posix_fixes.py
autonomous: true
requirements: [STAB-02, STAB-03]
must_haves:
  truths:
    - "cmvp_cache.json loads via importlib.resources and is declared as wheel package-data — no 'CMVP cache unavailable' warning on merge"
    - "Scheduler subprocess cmd passes neither --target nor --output to run_scan; scheduled scans exit 0"
    - "A schedule with no config file fails fast (run marked failed) rather than launching a targetless run_scan"
  artifacts:
    - path: "pyproject.toml"
      provides: "compliance/*.json package-data declaration"
      contains: "compliance"
    - path: "quirk/compliance/cmvp.py"
      provides: "importlib.resources read path for cmvp_cache.json"
      contains: "importlib"
    - path: "quirk/cli/scheduler_cmd.py"
      provides: "run_scan cmd list without --target/--output + config-None fail-fast guard"
    - path: "tests/test_scheduler_posix_fixes.py"
      provides: "STAB-03 static regression guard"
  key_links:
    - from: "cmvp._load_cache"
      to: "quirk.compliance/cmvp_cache.json"
      via: "importlib.resources.files().joinpath().read_text()"
      pattern: "importlib.resources"
    - from: "scheduler_cmd subprocess"
      to: "run_scan --config"
      via: "cmd list with --config + --profile only"
      pattern: "run_scan"
---

<objective>
Ship `cmvp_cache.json` inside the installed wheel and load it wheel-safely (STAB-02), and
stop the scheduler from passing unsupported `--target`/`--output` to `run_scan` (STAB-03).
Both are disjoint from Plan 01's files (no sensor_cmd/console_cmd overlap).

Purpose: No "CMVP cache unavailable" warning on merge in a wheel install; scheduled scans
exit 0.
Output: pyproject package-data + importlib.resources load path; scheduler cmd fix + guard +
regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-live-uat-stabilization-lab-testability/115-CONTEXT.md
@.planning/phases/115-live-uat-stabilization-lab-testability/115-RESEARCH.md
@.planning/phases/115-live-uat-stabilization-lab-testability/115-PATTERNS.md

<interfaces>
cmvp.py: _CACHE_PATH = Path(__file__).parent / "cmvp_cache.json" (L50); _load_cache reads
  _CACHE_PATH.read_text() (~L75-80); refresh_cache writes via _atomic_write_json(_CACHE_PATH).
pyproject.toml L127-128: [tool.setuptools.package-data] quirk = ["reports/templates/*.j2",
  "config_template.yaml", "dashboard/static/**/*"].
scheduler_cmd.py L153-167: cmd = [sys.executable, "-m", "run_scan"]; appends --config (if
  scan_config_path), then --target/schedule.target, --profile, --output/str(output_dir).
run_scan.py argparser (~L589): accepts --config/--profile/etc but NOT --target/--output.
tests/test_scheduler_posix_fixes.py: SCHEDULER_SRC constant, _strip_comments(), module-level
  static-analysis test functions (no class).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship cmvp_cache.json in wheel + wheel-safe load (STAB-02)</name>
  <files>pyproject.toml, quirk/compliance/cmvp.py</files>
  <read_first>
    - pyproject.toml (L127-128 [tool.setuptools.package-data] quirk list)
    - quirk/compliance/cmvp.py (_CACHE_PATH L50, _load_cache ~L75-80, refresh_cache write path)
    - 115-PATTERNS.md "cmvp.py — _load_cache (STAB-02)" + "pyproject.toml — package-data" sections
    - 115-RESEARCH.md "STAB-02: CMVP Packaging Fix" + Pitfall 2
  </read_first>
  <action>
    In pyproject.toml add `"compliance/*.json"` to the `quirk` package-data list (D-08).
    In cmvp.py migrate the READ path in _load_cache from `_CACHE_PATH.read_text()` to
    `importlib.resources.files("quirk.compliance").joinpath("cmvp_cache.json").read_text(encoding="utf-8")`,
    then json.loads the text. Keep the existing schema/assertion validation unchanged.
    Keep `_CACHE_PATH` and the refresh_cache write path (`_atomic_write_json(_CACHE_PATH, ...)`)
    AS-IS — refresh is a developer-only tool run from a source checkout (Pitfall 2). Do not
    touch the staleness/last_verified logic.
  </action>
  <verify>
    <automated>python -m pytest tests/ -k cmvp -x -q && python -c "from quirk.compliance import cmvp; cmvp._load_cache(force_reload=True)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "compliance" pyproject.toml` shows the new package-data entry.
    - `grep -n "importlib" quirk/compliance/cmvp.py` matches in the load path.
    - _load_cache(force_reload=True) succeeds; existing cmvp tests pass.
    - refresh_cache still writes via _CACHE_PATH (unchanged).
  </acceptance_criteria>
  <done>cmvp_cache.json is declared package-data and loaded via importlib.resources; no warning on a wheel-install merge.</done>
</task>

<task type="auto">
  <name>Task 2: Drop --target/--output from scheduler + fail-fast guard + regression test (STAB-03)</name>
  <files>quirk/cli/scheduler_cmd.py, tests/test_scheduler_posix_fixes.py</files>
  <read_first>
    - quirk/cli/scheduler_cmd.py (cmd construction L153-167; dispatch function around it)
    - run_scan.py argparser ~L589 (confirms no --target/--output)
    - 115-PATTERNS.md "scheduler_cmd.py (STAB-03)" + "test_scheduler_posix_fixes.py" sections
    - 115-RESEARCH.md "STAB-03: Scheduler Arg Fix" + Pitfall 4 + open-Q1
    - tests/test_scheduler_posix_fixes.py (SCHEDULER_SRC, _strip_comments, existing test fns)
  </read_first>
  <action>
    In scheduler_cmd's run_scan cmd builder remove both the `--target`/`schedule.target`
    pair and the `--output`/`str(output_dir)` pair (D-06). Keep `--config` (when
    scan_config_path is set) and `--profile`. Add a fail-fast guard (open-Q1): if
    scan_config_path is None, set the run status to "failed", commit, log an error naming
    the schedule, and return — do NOT launch run_scan with no target. Do NOT widen
    run_scan.py's arg surface. Leave a NOTE comment explaining target+output come from
    --config (cfg target + cfg.output.directory, SENSOR-05 anchoring).
    Add module-level test_scheduler_cmd_drops_target_and_output to test_scheduler_posix_fixes.py
    copying the existing _strip_comments static-analysis style: assert '"--target"' not in
    stripped src and '"--output"' not in stripped src.
  </action>
  <verify>
    <automated>python -m pytest tests/test_scheduler_posix_fixes.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - In stripped scheduler_cmd.py source, the string literals "--target" and "--output" are absent.
    - A config-None dispatch marks the run failed and returns (fail-fast guard present).
    - test_scheduler_cmd_drops_target_and_output added as a module-level function and passes.
    - Existing test_scheduler_posix_fixes.py tests still pass.
  </acceptance_criteria>
  <done>Scheduler passes neither --target nor --output; config-less schedules fail fast; regression locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduler → run_scan subprocess | scheduler builds an argv list for an external process |
| wheel package → compliance data file | runtime reads a packaged JSON resource |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-06 | Denial of Service | scheduler dispatch with no config | mitigate | Fail-fast guard marks run failed instead of launching a targetless/hung run_scan (open-Q1) |
| T-115-07 | Tampering | run_scan argv surface | mitigate | Scheduler conforms to run_scan's existing arg surface; run_scan.py not widened (D-06) — no new injectable flags |
| T-115-08 | Information Disclosure | cmvp packaging change | accept | Packaging/load-path change only; no crypto logic or secret material touched (ASVS V6 N/A per RESEARCH) |
</threat_model>

<verification>
- `python -m pytest tests/test_scheduler_posix_fixes.py tests/ -k cmvp -q` green.
- `python -m compileall quirk/compliance/cmvp.py quirk/cli/scheduler_cmd.py` clean.
</verification>

<success_criteria>
- STAB-02: cmvp_cache.json packaged + loaded via importlib.resources.
- STAB-03: scheduler cmd free of --target/--output; fail-fast on missing config; regression test green.
</success_criteria>

<output>
Create `.planning/phases/115-live-uat-stabilization-lab-testability/115-02-SUMMARY.md` when done
</output>
