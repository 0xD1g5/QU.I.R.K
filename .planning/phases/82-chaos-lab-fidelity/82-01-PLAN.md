---
phase: 82-chaos-lab-fidelity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quantum-chaos-enterprise-lab/docker-compose.yml  # OWNED LINE RANGES: 1-1000 (all NON-broker, NON-source service blocks) — image-tag pin sweep + ldaps verification. DO NOT touch lines 670-720 (gitea-seed — owned by 82-03) or 1062-1102 (rabbitmq-broker block — owned by 82-02). All other services in scope.
  - quantum-chaos-enterprise-lab/expected_results_v4.md   # ldaps oracle section
  - quantum-chaos-enterprise-lab/README.md                 # Profile Summary ldaps row + pinning policy note
autonomous: true
requirements: [CHAOS-01, CHAOS-05]
must_haves:
  truths:
    - "`./lab.sh up --profile ldaps` brings ldaps up cleanly on macOS Docker Desktop with no chown/Read-only-file-system errors"
    - "`./lab.sh down && ./lab.sh up --profile ldaps` round-trips cleanly (idempotent across volume lifecycle)"
    - "`ldapsearch -x -H ldaps://localhost:636 -b dc=chaos,dc=local` returns a successful TLS bind"
    - "Every `image:` field in docker-compose.yml has an explicit, non-`:latest`, non-bare-name tag"
    - "expected_results_v4.md and README.md document the ldaps profile with its new bitnamilegacy/openldap base image"
  artifacts:
    - path: "quantum-chaos-enterprise-lab/docker-compose.yml"
      provides: "All service blocks (except broker and gitea-seed) carry explicit pinned image tags"
      contains: "bitnamilegacy/openldap:2.6.10-debian-12-r4"
    - path: "quantum-chaos-enterprise-lab/expected_results_v4.md"
      provides: "ldaps profile oracle section reflecting the bitnamilegacy/openldap migration"
    - path: "quantum-chaos-enterprise-lab/README.md"
      provides: "Profile Summary row for ldaps with current image + 38636/636 port note"
  key_links:
    - from: "docker-compose.yml ldaps service block (lines 741-758)"
      to: "./certs (read-only bind-mount at /opt/bitnami/openldap/certs)"
      via: "volumes mapping"
      pattern: "bitnamilegacy/openldap"
    - from: "lab.sh _derive_all_profiles()"
      to: "ldaps profile token"
      via: "runtime compose-file parse"
      pattern: "profiles:\\s*\\[\"ldaps\"\\]"
---

<objective>
Resolve DEF-999.83-A (`ldaps` macOS chown failure) and execute the broader image-pin
audit required by CHAOS-05. The ldaps service block has already been migrated to
`bitnamilegacy/openldap:2.6.10-debian-12-r4` upstream (Phase 79 follow-on, commit on
2026-05-15) — this plan verifies that migration is correct on macOS Docker Desktop,
finalizes the oracle + README parity, and sweeps every remaining unpinned image
across the chaos-lab compose file.

Purpose: Close two of the six Phase 82 success criteria (#1 ldaps cleanliness, #5
no-`:latest` policy across docker-compose.yml) and unblock the CI gate that Plan
82-04 will install.

Output: Updated `docker-compose.yml` (image tags pinned across all services except
the two blocks owned by 82-02 and 82-03), updated `expected_results_v4.md` ldaps
oracle, updated `README.md` Profile Summary table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/82-chaos-lab-fidelity/82-CONTEXT.md
@quantum-chaos-enterprise-lab/docker-compose.yml
@quantum-chaos-enterprise-lab/lab.sh
@quantum-chaos-enterprise-lab/expected_results_v4.md
@quantum-chaos-enterprise-lab/README.md
@quantum-chaos-enterprise-lab/smime/

<file-collision-strategy>
This plan runs in Wave 1 in parallel with 82-02 (rabbitmq-broker) and 82-03
(gitea-seed). All three plans modify the same file: `docker-compose.yml`.

YOUR OWNED LINE RANGES in docker-compose.yml:
  - Lines 1-669 (everything before gitea-seed)
  - Lines 721-1043 (everything after gitea-seed, before broker)
  - Lines 1103-1148 (everything after broker block, before `volumes:`)

DO NOT MODIFY:
  - Lines 670-720 (gitea-seed block) — owned by 82-03
  - Lines 1062-1102 (rabbitmq-broker block + neighbours kafka-broker/redis-broker
    are READ-ONLY for you; if they need pin updates, leave them — 82-02 owns
    them and is doing its own image-pin pass within its block)

PROCEDURE:
  1. Read current docker-compose.yml at task start (line numbers may have
     shifted slightly if another wave-1 plan committed first).
  2. Identify your service blocks BY NAME, not by absolute line number.
  3. Make ONLY image-tag changes within your owned blocks.
  4. Stage with: `git add quantum-chaos-enterprise-lab/docker-compose.yml
     quantum-chaos-enterprise-lab/expected_results_v4.md
     quantum-chaos-enterprise-lab/README.md`
  5. If `git commit` fails due to merge conflict on docker-compose.yml: re-pull,
     re-apply ONLY your block changes, re-commit. DO NOT touch sibling blocks.
  6. NEVER use `git add -A` or `git add .`.
</file-collision-strategy>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify ldaps profile + finalize oracle/README parity</name>
  <files>
    quantum-chaos-enterprise-lab/docker-compose.yml,
    quantum-chaos-enterprise-lab/expected_results_v4.md,
    quantum-chaos-enterprise-lab/README.md
  </files>
  <action>
    Verify the ldaps service block at docker-compose.yml lines 741-758 matches the
    bitnamilegacy/openldap parity contract used by smime (lines 769-782) and adcs
    (lines 831-856):
      - image: bitnamilegacy/openldap:2.6.10-debian-12-r4 (confirm exact tag)
      - LDAP_TLS_* env vars point to /opt/bitnami/openldap/certs/{modern.crt,modern.key,ca.crt}
      - volumes mount ./certs at /opt/bitnami/openldap/certs:ro
      - host port mapping 636:636 (note: CONTEXT mentions host port 38636 but
        the current block uses 636; the success criterion in ROADMAP #1 says
        `ldapsearch against localhost:636` so KEEP 636. The "preserve host port
        38636" line in the user prompt is superseded by the live state of the
        file. Document this in SUMMARY.)
    If the block is already correct, leave it untouched — DO NOT churn the diff.

    Then verify docker-compose runtime:
      cd quantum-chaos-enterprise-lab
      ./lab.sh down 2>/dev/null || true
      ./lab.sh up --profile ldaps
      docker ps --filter 'name=chaoslab-ldaps' --format '{{.Names}} {{.Status}}'
      ldapsearch -x -H ldaps://localhost:636 -b 'dc=chaos,dc=local' -LLL -s base 2>&1 | head -20
      ./lab.sh down
      ./lab.sh up --profile ldaps   # second cycle — idempotency check
      docker ps --filter 'name=chaoslab-ldaps' --format '{{.Names}} {{.Status}}'

    If Docker daemon is unreachable on this host, skip the live test and document
    "Docker not available on planner host — runtime verification deferred to
    `tests/test_chaos_lab_idempotency.py` in Plan 82-04" in SUMMARY.

    Update `expected_results_v4.md` ldaps section to reflect the bitnamilegacy
    image base, the LDAPS port 636, and the expected `ldapsearch` happy-path
    response. Use the smime/adcs sections as the formatting template.

    Update `README.md` Profile Summary table row for `ldaps`:
      - Image column: bitnamilegacy/openldap:2.6.10-debian-12-r4
      - Ports column: 636
      - Expected scanner findings: TLS LDAPS handshake success on 636 (the
        existing language stays; only the image-base column changes)
  </action>
  <verify>
    <automated>
      python3 -c "
import re, yaml
with open('quantum-chaos-enterprise-lab/docker-compose.yml') as f:
    data = yaml.safe_load(f)
ldaps = data['services']['ldaps']
assert ldaps['image'] == 'bitnamilegacy/openldap:2.6.10-debian-12-r4', f'ldaps image mismatch: {ldaps[\"image\"]}'
assert 'ldaps' in ldaps['profiles'], 'ldaps profile token missing'
print('ldaps service block parity: OK')
"
      grep -q 'bitnamilegacy/openldap' quantum-chaos-enterprise-lab/expected_results_v4.md || echo "WARN: expected_results_v4.md not updated"
      grep -q 'bitnamilegacy/openldap' quantum-chaos-enterprise-lab/README.md || echo "WARN: README.md not updated"
    </automated>
  </verify>
  <done>
    ldaps service block uses bitnamilegacy/openldap:2.6.10-debian-12-r4; ldapsearch
    succeeds against localhost:636 (or live test skipped + documented if Docker
    unavailable); expected_results_v4.md ldaps section and README.md Profile
    Summary row both reference the bitnamilegacy image.
  </done>
</task>

<task type="auto">
  <name>Task 2: Image-tag pin sweep — every service except broker + gitea-seed</name>
  <files>
    quantum-chaos-enterprise-lab/docker-compose.yml
  </files>
  <action>
    Sweep every service block in YOUR OWNED LINE RANGES (see file-collision-strategy
    above). For each `image:` field without an explicit pinned tag — i.e. `:latest`,
    no colon at all (bare image name), or a major-only tag that is widely understood
    to be a moving alias — replace with a specific, currently-supported, non-EOL
    pinned tag.

    Services that need pin work (as of read of compose file at planner-time —
    re-verify at executor-time, the file may have shifted):
      - tls-modern, tls-legacy, tls-expired, tls-selfsigned, tls-mtls-required,
        http-on-8444, legacy-http (httpd:2.4 — fine if confirmed current),
        ssh-alt (lscr.io/linuxserver/openssh-server — no tag),
        unknown-port (alpine — bare),
        tls-slow-proxy (haproxy:latest — MUST replace),
        unknown-port-2 (alpine — bare),
        postgres-plain (postgres:16 — promote to specific minor),
        redis-plain (redis:7-alpine — promote to specific minor),
        rabbitmq-mgmt (rabbitmq:3-management — promote to specific minor),
        all phaseA nginx blocks (nginx:stable — promote to specific stable patch),
        localstack/localstack:3, mcr.microsoft.com/azure-storage/azurite (no tag),
        id-postgres (postgres:16), keycloak (already pinned), step-ca (no tag),
        openldap (osixia/openldap:1.5.0 — pinned already, but flag as EOL —
        leave-as-is unless you have a known-good alternative; document choice
        in SUMMARY),
        phpldapadmin (osixia/phpldapadmin:0.9.0 — same EOL caveat),
        whoami (already pinned), registry (registry:2 — promote),
        registry-seed (docker:24-dind — promote),
        gitea (gitea/gitea:1.21 — promote to current patch),
        gitea-init (gitea/gitea:1.21 — promote in lockstep),
        vault-30, vault-30-seed (already pinned),
        simplesamlphp (kenchan0130/simplesamlphp — no tag),
        postgres-ssl-off (postgres:15 — promote),
        mysql-ssl-off (mysql:8.0 — promote),
        minio (already pinned),
        minio-seed (minio/mc:latest — MUST replace),
        postfix-email, dovecot-email (build context — N/A),
        kafka-broker (apache/kafka:3.7.0 — already pinned, leave),
        redis-broker (redis:7-alpine — promote — wait, redis-broker is at lines
        1084-1101, which is OUTSIDE rabbitmq-broker's 1062-1082 range. YOU OWN
        redis-broker. Promote it),
        tls-cert-* nginx:stable blocks (promote).

    For each promotion: pick the most recent NON-EOL pinned tag from upstream
    (Docker Hub, GHCR, etc.). Document each tag choice in SUMMARY.md as a small
    table: `service | old image | new image | rationale`.

    Do NOT introduce SHA digests (CONTEXT D-Area-2 — version tags only, no SHA).

    For builds (postfix-email, dovecot-email, ssh-weak, bind9-dnssec, samba-dc,
    jwt-*, gitea-seed): SKIP. They use `build:` not `image:` and are out of scope
    for the pin sweep (they pin via the `FROM` directive inside their Dockerfile —
    those will be audited in Plan 82-04 by the test, not here).

    For osixia/openldap:1.5.0 and osixia/phpldapadmin:0.9.0 specifically: these are
    pinned but the upstream is unmaintained. Leave AS-IS for this plan (out of
    scope to migrate phpldapadmin or the identity-profile openldap to bitnamilegacy
    — that would touch identity-profile fixtures + tests, which is out of phase).
    Document the decision in SUMMARY.

    PRESERVE every other field in each service block (environment, volumes, ports,
    healthcheck, command, depends_on). Diff should be IMAGE-TAG-ONLY edits.
  </action>
  <verify>
    <automated>
      python3 -c "
import yaml, re
with open('quantum-chaos-enterprise-lab/docker-compose.yml') as f:
    data = yaml.safe_load(f)
fails = []
for name, svc in data['services'].items():
    img = svc.get('image')
    if img is None:
        continue   # build-only services
    # Reject :latest and bare-name (no colon)
    if img.endswith(':latest'):
        fails.append(f'{name}: uses :latest ({img})')
    if ':' not in img:
        fails.append(f'{name}: bare image name, no tag ({img})')
if fails:
    print('PIN-SWEEP FAILURES:')
    for f in fails:
        print(' -', f)
    raise SystemExit(1)
print('All image: fields carry a non-:latest, non-bare tag.')
"
    </automated>
  </verify>
  <done>
    Every service block in YOUR OWNED LINE RANGES with an `image:` field has a
    specific pinned tag that is neither `:latest` nor bare; broker (lines
    1062-1082) and gitea-seed (lines 670-720) blocks left untouched for parallel
    plans to own; SUMMARY.md documents every tag promotion in a service|old|new
    table.
  </done>
</task>

<task type="auto">
  <name>Task 3: Atomic commit with explicit file paths</name>
  <files>
    quantum-chaos-enterprise-lab/docker-compose.yml,
    quantum-chaos-enterprise-lab/expected_results_v4.md,
    quantum-chaos-enterprise-lab/README.md
  </files>
  <action>
    Stage ONLY the three files this plan owns. NEVER use `git add -A` or `git add .`.

    git status --short quantum-chaos-enterprise-lab/
    git add quantum-chaos-enterprise-lab/docker-compose.yml \
            quantum-chaos-enterprise-lab/expected_results_v4.md \
            quantum-chaos-enterprise-lab/README.md
    git diff --cached --stat

    If the staged diff for docker-compose.yml shows changes inside the broker
    block (lines ~1062-1082) or the gitea-seed block (lines ~670-720), unstage
    and restore those ranges (you've drifted into a sibling plan's territory):
      git restore --staged quantum-chaos-enterprise-lab/docker-compose.yml
      # then re-edit to remove drift, re-add

    Commit with the GSD tools helper:
      node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit \
        "fix(82-01): ldaps verification + image-tag pin sweep (CHAOS-01, CHAOS-05)" \
        --files quantum-chaos-enterprise-lab/docker-compose.yml \
                quantum-chaos-enterprise-lab/expected_results_v4.md \
                quantum-chaos-enterprise-lab/README.md

    If commit fails due to wave-1 race (sibling plan committed first):
      git pull --rebase   # ours-clean rebase if no overlap
      # If conflict on docker-compose.yml: keep BOTH sides' service-block edits
      # (they should be disjoint by block). Resolve manually, re-commit.
  </action>
  <verify>
    <automated>
      git log --oneline -1 | grep -q '82-01' || (echo "Commit message missing 82-01 tag"; exit 1)
      git show --stat HEAD | grep -E 'docker-compose\.yml|expected_results_v4\.md|README\.md' | wc -l | awk '{exit ($1 < 3)}'
    </automated>
  </verify>
  <done>
    Atomic commit landed with subject `fix(82-01): ldaps verification + image-tag
    pin sweep (CHAOS-01, CHAOS-05)`; only the three owned files appear in the
    commit's stat; no `-A` or `.` in git history.
  </done>
</task>

</tasks>

<verification>
  - docker-compose.yml `ldaps` service uses bitnamilegacy/openldap:2.6.10-debian-12-r4
  - All `image:` fields in compose file are pinned (no `:latest`, no bare names),
    excluding broker + gitea-seed blocks owned by parallel plans
  - expected_results_v4.md and README.md reflect the ldaps state
  - Commit landed cleanly with explicit `git add <paths>`, not `-A`
</verification>

<success_criteria>
  - CHAOS-01: DEF-999.83-A ldaps macOS bring-up clean (verified live OR deferred
    to 82-04 idempotency test with documented rationale)
  - CHAOS-05: Image pin policy satisfied across the YOUR-OWNED portion of the
    compose file; remaining pin work (broker block, gitea-seed block) handled
    by 82-02 + 82-03; CI gate test scaffolded by 82-04
</success_criteria>

<output>
After completion, create `.planning/phases/82-chaos-lab-fidelity/82-01-SUMMARY.md`
including:
  - ldaps verification result (live test outcome OR deferred-to-82-04 note)
  - Pin-sweep audit table: service | old image | new image | rationale
  - Any EOL-upstream services left as-is (osixia/openldap, osixia/phpldapadmin)
    with justification
  - Files modified, commit SHA
</output>
