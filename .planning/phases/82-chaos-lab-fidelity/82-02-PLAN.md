---
phase: 82-chaos-lab-fidelity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - quantum-chaos-enterprise-lab/docker-compose.yml  # OWNED LINE RANGE: 1062-1102 (rabbitmq-broker service block ONLY; kafka-broker 1044-1060 and redis-broker 1084-1101 are READ-ONLY here — 82-01 owns the broader pin sweep). Image-tag for rabbitmq-broker itself is OWNED.
  - quantum-chaos-enterprise-lab/expected_results_v4.md   # broker profile section (rabbitmq idempotency note)
autonomous: true
requirements: [CHAOS-02]
must_haves:
  truths:
    - "RABBITMQ_ERLANG_COOKIE is set as a deterministic env var on the rabbitmq-broker service"
    - "No bind-mount of .erlang.cookie exists (would override the env var across down/up cycles)"
    - "`./lab.sh up --profile broker && ./lab.sh down && ./lab.sh up --profile broker` produces chaoslab-rabbitmq-broker-1 in Up (healthy) on the second run with no Erlang cookie mismatch"
    - "rabbitmq-broker image is pinned to a specific non-:latest, non-EOL tag"
  artifacts:
    - path: "quantum-chaos-enterprise-lab/docker-compose.yml"
      provides: "rabbitmq-broker service block with RABBITMQ_ERLANG_COOKIE env var and no cookie bind-mount"
      contains: "RABBITMQ_ERLANG_COOKIE"
    - path: "quantum-chaos-enterprise-lab/expected_results_v4.md"
      provides: "broker profile oracle reflecting cookie-determinism contract"
  key_links:
    - from: "docker-compose.yml rabbitmq-broker service (lines 1062-1082)"
      to: "RabbitMQ Erlang clustering subsystem"
      via: "RABBITMQ_ERLANG_COOKIE env var"
      pattern: "RABBITMQ_ERLANG_COOKIE"
---

<objective>
Resolve DEF-999.83-B (rabbitmq Erlang cookie reset across `lab.sh down/up` cycles).
The compose file already sets `RABBITMQ_ERLANG_COOKIE=lab-erlang-cookie-do-not-use-in-prod`
on the rabbitmq-broker service (line 1071 at planner-time read). This plan VERIFIES
the env var is in place, confirms NO bind-mount of `.erlang.cookie` exists that
would clobber it, runs the down/up regression cycle, and pins the rabbitmq image
to a specific tag.

Purpose: Close ROADMAP Phase 82 success criterion #2 — rabbitmq-broker survives
`lab.sh down && lab.sh up` cycles without cookie-mismatch failures.

Output: Verified rabbitmq-broker service block; idempotency regression evidence
captured in SUMMARY; expected_results_v4.md broker section updated to document
the cookie-determinism contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/82-chaos-lab-fidelity/82-CONTEXT.md
@quantum-chaos-enterprise-lab/docker-compose.yml
@quantum-chaos-enterprise-lab/lab.sh
@quantum-chaos-enterprise-lab/expected_results_v4.md

<file-collision-strategy>
This plan runs in Wave 1 in parallel with 82-01 (ldaps + global pin sweep) and
82-03 (gitea-seed idempotency). All three plans modify the same file:
`docker-compose.yml`.

YOUR OWNED LINE RANGE in docker-compose.yml:
  - Lines 1062-1082 (the `rabbitmq-broker:` service block, from the service
    key down to its last YAML field, inclusive of healthcheck)

DO NOT MODIFY any other service block. Specifically:
  - kafka-broker (lines 1044-1060) — read-only for you; 82-01 owns its pin
  - redis-broker (lines 1084-1101) — read-only for you; 82-01 owns its pin
  - All other services — owned by 82-01

PROCEDURE:
  1. Read current docker-compose.yml at task start.
  2. Locate the `rabbitmq-broker:` service BY NAME, not absolute line number.
  3. Make ONLY changes within the rabbitmq-broker block.
  4. Stage with: `git add quantum-chaos-enterprise-lab/docker-compose.yml
     quantum-chaos-enterprise-lab/expected_results_v4.md`
  5. If commit fails due to merge conflict on docker-compose.yml: re-pull,
     re-apply ONLY your block changes, re-commit.
  6. NEVER use `git add -A` or `git add .`.
</file-collision-strategy>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Erlang cookie env var + confirm no cookie bind-mount</name>
  <files>
    quantum-chaos-enterprise-lab/docker-compose.yml
  </files>
  <action>
    Read the `rabbitmq-broker:` service block (currently lines 1062-1082).

    Required state (assert all are present):
      - `image:` is a specific pinned tag, NOT `rabbitmq:3-management` (a moving
        major-minor alias) and NOT `:latest`. Promote to a specific patch tag
        such as `rabbitmq:3.12.14-management` or the current 3.12.x management
        patch (executor selects current non-EOL tag and documents the choice
        in SUMMARY).
      - `environment:` contains `RABBITMQ_ERLANG_COOKIE=<deterministic value>`
        (the current value `lab-erlang-cookie-do-not-use-in-prod` is acceptable;
        leave as-is if present).
      - `volumes:` contains NO entry that mounts a host path or named volume
        onto `/var/lib/rabbitmq/.erlang.cookie` or `/.erlang.cookie` or any path
        ending in `.erlang.cookie`. If such a bind-mount exists, REMOVE it
        (it would defeat the env var and cause the DEF-999.83-B reset).
      - Optional but recommended: add a non-secret inline comment above the
        env var explaining it is a deterministic `[lab-only]` cookie (per
        CONTEXT D-Area-1).

    If the block is already in the correct state, leave it untouched (no diff
    churn). The only required edit is the image tag promotion.

    Then run the regression:
      cd quantum-chaos-enterprise-lab
      ./lab.sh up --profile broker
      docker ps --filter 'name=chaoslab-rabbitmq-broker' --format '{{.Names}} {{.Status}}'
      ./lab.sh down
      ./lab.sh up --profile broker
      sleep 30   # rabbitmq health-check warmup
      docker ps --filter 'name=chaoslab-rabbitmq-broker' --format '{{.Names}} {{.Status}}'
      docker logs chaoslab-rabbitmq-broker-1 2>&1 | grep -iE 'cookie|erlang' | tail -20

    Expected: second run shows `Up (healthy)` and logs contain NO
    "Cookie file ... must be accessible by owner only" or "Connection attempt
    from disallowed node" cookie-mismatch lines.

    If Docker daemon is unreachable on the executor host, skip the live test
    and document "Docker not available — runtime verification deferred to
    `tests/test_chaos_lab_idempotency.py` in Plan 82-04" in SUMMARY.
  </action>
  <verify>
    <automated>
      python3 -c "
import yaml, re
with open('quantum-chaos-enterprise-lab/docker-compose.yml') as f:
    data = yaml.safe_load(f)
svc = data['services']['rabbitmq-broker']
img = svc['image']
assert img != 'rabbitmq:3-management', f'rabbitmq-broker still on moving alias: {img}'
assert not img.endswith(':latest'), f'rabbitmq-broker on :latest: {img}'
assert ':' in img, f'rabbitmq-broker bare image: {img}'
env = svc.get('environment', [])
# environment may be a list ['K=V', ...] or a dict {K: V}
if isinstance(env, list):
    cookie = [e for e in env if str(e).startswith('RABBITMQ_ERLANG_COOKIE')]
else:
    cookie = [k for k in env if k == 'RABBITMQ_ERLANG_COOKIE']
assert cookie, 'RABBITMQ_ERLANG_COOKIE env var not set on rabbitmq-broker'
vols = svc.get('volumes', [])
bad = [v for v in vols if '.erlang.cookie' in str(v)]
assert not bad, f'forbidden cookie bind-mount present: {bad}'
print(f'rabbitmq-broker OK — image={img}, cookie env set, no .erlang.cookie bind-mount')
"
    </automated>
  </verify>
  <done>
    rabbitmq-broker service uses a specific pinned tag (not `rabbitmq:3-management`,
    not `:latest`); RABBITMQ_ERLANG_COOKIE env var is set; no `.erlang.cookie`
    bind-mount; second-cycle bring-up reaches Up (healthy) with clean Erlang
    logs (OR live verification deferred to 82-04 with explicit note).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update broker oracle + atomic commit</name>
  <files>
    quantum-chaos-enterprise-lab/expected_results_v4.md,
    quantum-chaos-enterprise-lab/docker-compose.yml
  </files>
  <action>
    Update `expected_results_v4.md` broker profile section to document:
      - rabbitmq-broker is idempotent across `lab.sh down/up` cycles
      - Erlang cookie is set deterministically via env var (lab-only;
        not a secret)
      - Expected scanner finding language (existing finding text stays;
        only the idempotency note is added)
    If the broker section already states this, leave it untouched.

    Stage and commit:
      git status --short quantum-chaos-enterprise-lab/
      git add quantum-chaos-enterprise-lab/docker-compose.yml \
              quantum-chaos-enterprise-lab/expected_results_v4.md
      git diff --cached --stat

      # Sanity check: confirm no drift into sibling-plan blocks
      git diff --cached quantum-chaos-enterprise-lab/docker-compose.yml | \
          grep -E '^@@' | head -5
      # The @@ hunk header(s) should reference lines ~1062-1082 ONLY.
      # If hunks appear in other ranges, you've drifted — restore + redo.

      node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit \
        "fix(82-02): rabbitmq erlang cookie determinism (CHAOS-02)" \
        --files quantum-chaos-enterprise-lab/docker-compose.yml \
                quantum-chaos-enterprise-lab/expected_results_v4.md

    Handle wave-1 race: if commit fails due to a sibling plan landing first,
    `git pull --rebase`, resolve any docker-compose.yml conflict by KEEPING
    BOTH SIDES' disjoint block edits, re-commit.
  </action>
  <verify>
    <automated>
      git log --oneline -1 | grep -q '82-02' || (echo "82-02 commit missing"; exit 1)
      git show --stat HEAD | grep -E 'docker-compose\.yml|expected_results_v4\.md' | wc -l | awk '{exit ($1 < 2)}'
    </automated>
  </verify>
  <done>
    Atomic commit landed with subject `fix(82-02): rabbitmq erlang cookie
    determinism (CHAOS-02)`; only the two owned files in the commit's stat;
    no `-A` in git history.
  </done>
</task>

</tasks>

<verification>
  - rabbitmq-broker image pinned (no `rabbitmq:3-management`, no `:latest`)
  - RABBITMQ_ERLANG_COOKIE env var set on rabbitmq-broker
  - No bind-mount of any `.erlang.cookie` path
  - Live regression cycle succeeds (or 82-04 deferral documented)
  - expected_results_v4.md broker section reflects cookie-determinism
  - Commit landed with explicit file paths
</verification>

<success_criteria>
  - CHAOS-02: DEF-999.83-B closed — rabbitmq-broker survives down/up cycles
</success_criteria>

<output>
After completion, create `.planning/phases/82-chaos-lab-fidelity/82-02-SUMMARY.md`
including:
  - Rabbitmq image tag chosen (old → new)
  - Cookie env var state (was-set-already vs. added)
  - Live regression cycle output (or deferral note)
  - Files modified, commit SHA
</output>
