---
phase: 101-notification-fan-out-security-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quirk/util/safe_exc.py
  - quirk/models.py
  - quirk/db.py
  - pyproject.toml
  - tests/test_notify_safe_str_secrets.py
  - tests/test_integration_deliveries_schema.py
  - tests/test_install_all_includes_notify.py
autonomous: true
requirements: [ISEC-02, NOTIFY-07]
must_haves:
  truths:
    - "safe_str() scrubs Slack xoxb- tokens, hooks.slack.com webhook URLs, and SMTP connection strings from any error text"
    - "init_db creates an integration_deliveries table with columns scan_id, finding_hash, destination, status, attempted_at, error_summary"
    - "pip resolving quirk-scanner[all] includes slack-sdk (notify joins all, like docx)"
  artifacts:
    - path: "quirk/models.py"
      provides: "IntegrationDelivery SQLAlchemy model"
      contains: "class IntegrationDelivery"
    - path: "quirk/db.py"
      provides: "_ensure_integration_deliveries_table called from init_db"
      contains: "_ensure_integration_deliveries_table"
    - path: "quirk/util/safe_exc.py"
      provides: "3 new sensitive patterns (Slack token, Slack webhook URL, SMTP conn string)"
      contains: "xox"
  key_links:
    - from: "quirk/db.py::init_db"
      to: "quirk/db.py::_ensure_integration_deliveries_table"
      via: "init_db chain call"
      pattern: "_ensure_integration_deliveries_table\\(engine\\)"
    - from: "pyproject.toml [all]"
      to: "quirk-scanner[notify]"
      via: "extra inclusion"
      pattern: "quirk-scanner\\[notify\\]"
---

<objective>
Establish the shared, dependency-free foundation for the v5.3 integration surface: the secret-scrubbing patterns, the `integration_deliveries` audit table, and the `[notify]` optional extra. These primitives are consumed by every later plan in this phase and by Phases 103/104/105.

Purpose: Ship the ANCHOR primitives (ISEC-02 secret scrubbing, NOTIFY-07 delivery audit log) plus the packaging surface so channels and the dispatcher can build against them with no further schema or packaging work.
Output: Extended `safe_exc.py`, new `IntegrationDelivery` model + idempotent migration, `[notify]` extra in `pyproject.toml` (joins `[all]`), and three guard tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-notification-fan-out-security-foundation/101-RESEARCH.md
@.planning/phases/101-notification-fan-out-security-foundation/101-PATTERNS.md

<interfaces>
From quirk/util/safe_exc.py:
- `_SENSITIVE_PATTERNS: Final[tuple[re.Pattern[str], ...]]` (line 21) — append 3 regexes inside this tuple.
- `safe_str(exc: BaseException) -> str` (line 43) — already iterates _SENSITIVE_PATTERNS; no change to the function body.

From quirk/models.py:
- `from sqlalchemy import Column, Integer, String, Boolean, DateTime, Text, Float, ForeignKey` (line 4) — all needed types already imported.
- `class ScheduledRun(Base)` (line 183) — analog for the new model's column shape. `scan_id = Column(String(64), nullable=True)` at line 199.

From quirk/db.py:
- `init_db(db_path) -> Engine` (line 358) — runs `Base.metadata.create_all(engine, checkfirst=True)` at L374, then a chain of `_ensure_*` calls ending with `_ensure_scan_checkpoints_table(engine)` at L391, then `return engine` at L392.
- `_ensure_scheduled_tables(engine)` (line 330) — exact analog: body is a single `Base.metadata.create_all(engine, checkfirst=True)`.
- `get_engine(db_path) -> Engine` (line 69).

From pyproject.toml:
- `[project.optional-dependencies]` at line 34; `docx = ["python-docx>=1.1.0"]` block at line 84; `all = [...]` block at line 90 with `"quirk-scanner[docx]"` at line 98.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend safe_str with integration secret patterns (ISEC-02)</name>
  <read_first>quirk/util/safe_exc.py (lines 21-60, the _SENSITIVE_PATTERNS tuple + safe_str loop); 101-PATTERNS.md "safe_exc.py — extend _SENSITIVE_PATTERNS" section</read_first>
  <files>quirk/util/safe_exc.py, tests/test_notify_safe_str_secrets.py</files>
  <behavior>
    - safe_str(Exception("token xoxb-1234567890-abcdEFGH")) returns a string NOT containing "xoxb-1234567890-abcdEFGH"
    - safe_str(Exception("posted to https://hooks.slack.com/services/T00/B00/XXXXXXXX")) returns a string NOT containing the services path
    - safe_str(Exception("smtp://user:hunter2@smtp.example.com")) returns a string NOT containing "hunter2"
    - safe_str(Exception("ordinary connection refused")) returns the original message unchanged (no over-redaction)
  </behavior>
  <action>Append three compiled regexes to the existing `_SENSITIVE_PATTERNS` tuple in safe_exc.py per 101-PATTERNS.md: a Slack token pattern `xox[bpoa]-[0-9A-Za-z\-]{10,}`, a Slack webhook URL pattern `hooks\.slack\.com/services/[A-Za-z0-9/]+`, and an SMTP connection-string-with-credentials pattern `smtp[s]?://[^:@\s]+:[^@\s]+@`. Do NOT modify the `safe_str` function body — it already iterates the tuple. Write the test file first (RED), asserting each shape is redacted and that an ordinary message is left intact.</action>
  <verify>
    <automated>python -m pytest tests/test_notify_safe_str_secrets.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -v '^#' quirk/util/safe_exc.py | grep -c 'xox\[bpoa\]'` returns 1.
    - Behavior assertion: all tests in tests/test_notify_safe_str_secrets.py pass; the "ordinary message" test confirms no over-redaction.
  </acceptance_criteria>
  <done>safe_str redacts all three new integration-secret shapes and leaves non-secret text intact; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add IntegrationDelivery model + idempotent migration (NOTIFY-07)</name>
  <read_first>quirk/models.py (lines 183-199, ScheduledRun); quirk/db.py (lines 330-337 _ensure_scheduled_tables, lines 358-392 init_db chain); 101-PATTERNS.md "IntegrationDelivery model" + "_ensure_integration_deliveries_table" sections</read_first>
  <files>quirk/models.py, quirk/db.py, tests/test_integration_deliveries_schema.py</files>
  <behavior>
    - After init_db(db_path), SQLAlchemy inspector reports a table named "integration_deliveries"
    - Its column name set is a superset of {id, scan_id, finding_hash, destination, status, attempted_at, error_summary}
    - scan_id is indexed; finding_hash and error_summary are nullable; destination and status are non-null
  </behavior>
  <action>Add `class IntegrationDelivery(Base)` to quirk/models.py with `__tablename__ = "integration_deliveries"` and columns exactly per 101-PATTERNS.md: id (Integer PK autoincrement), scan_id (String(64), nullable=False, index=True), finding_hash (String(64), nullable=True), destination (String(64), nullable=False), status (String(16), nullable=False), attempted_at (DateTime, nullable=False), error_summary (Text, nullable=True). Use only already-imported sqlalchemy types. Add `_ensure_integration_deliveries_table(engine)` to quirk/db.py whose body is `Base.metadata.create_all(engine, checkfirst=True)` (exact analog of `_ensure_scheduled_tables`), and call it in the init_db chain immediately after `_ensure_scan_checkpoints_table(engine)` at L391, before `return engine`. Document the table as a shared primitive for Phases 103/104/105. Write the schema test first (RED) using the `sa_inspect` pattern from 101-PATTERNS.md.</action>
  <verify>
    <automated>python -m pytest tests/test_integration_deliveries_schema.py -x -q && python -m compileall quirk/models.py quirk/db.py</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'class IntegrationDelivery' quirk/models.py` returns 1; `grep -c '_ensure_integration_deliveries_table(engine)' quirk/db.py` returns 2 (definition + init_db call).
    - Behavior assertion: schema test confirms table exists with all 7 columns.
  </acceptance_criteria>
  <done>init_db creates integration_deliveries idempotently; column set matches the agreed schema; compileall clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add [notify] extra to pyproject.toml + [all] inclusion + CI guard (packaging)</name>
  <read_first>pyproject.toml (lines 34-100, optional-dependencies through [all]; note the docx precedent at L84/L98 and the impacket exclusion comment); tests/test_install_all_excludes_impacket.py (CI-guard dry-run structure); 101-RESEARCH.md Pitfall 6 + Open Question 1</read_first>
  <files>pyproject.toml, tests/test_install_all_includes_notify.py</files>
  <action>Add a `notify = ["slack-sdk>=3.33.0"]` extra to `[project.optional-dependencies]` (insert near the docx block) with a comment noting email uses stdlib smtplib and webhook uses stdlib urllib (no further deps). Add `"quirk-scanner[notify]"` to the `all = [...]` list alongside `quirk-scanner[docx]` (Claude's-discretion decision D: notify JOINS all, mirroring docx per research Pitfall 6 — slack-sdk has no cryptography downgrade chain). Create tests/test_install_all_includes_notify.py modeled on test_install_all_excludes_impacket.py but as an INCLUSION guard: run a pip dry-run resolve of `.[all]` and assert `slack-sdk` IS present in the resolved set. During execution, run the pip dry-run for `.[all,notify]` to confirm no httpx/aiohttp version conflict with the core `httpx>=0.28.0` dep (research Open Question 1) and record the result in the SUMMARY; if a conflict appears, EXCLUDE notify from [all] instead and flip the guard to an exclusion assertion.</action>
  <verify>
    <automated>python -m pytest tests/test_install_all_includes_notify.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'slack-sdk>=3.33.0' pyproject.toml` returns 1; `grep -c 'quirk-scanner\[notify\]' pyproject.toml` returns at least 1 (in [all], assuming no conflict).
    - Behavior assertion: the CI-guard test passes (asserts slack-sdk resolves under [all]).
    - SUMMARY records the pip dry-run conflict-check outcome.
  </acceptance_criteria>
  <done>[notify] extra exists, joins [all] (or is excluded with the guard flipped if a conflict is found), and a CI guard locks the invariant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exception text → log / integration_deliveries.error_summary | Untrusted error strings may carry secrets injected from operator config/env at runtime |
| pip resolver → installed environment | A new extra widens the transitive dependency surface for minimal installs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-01 | Information Disclosure | safe_str / error_summary column | mitigate | Add Slack token, Slack webhook URL, and SMTP conn-string patterns to _SENSITIVE_PATTERNS; downstream plans always write `safe_str(exc)` to error_summary (ISEC-02) |
| T-101-02 | Tampering | pyproject [all] dependency chain | mitigate | CI guard test asserts the resolved [all] set; pip dry-run verifies no httpx/aiohttp downgrade vs core httpx>=0.28.0 before committing notify into [all] |
| T-101-SC | Tampering | slack-sdk install | mitigate | slack-sdk rated [OK] by slopcheck (101-RESEARCH Package Legitimacy Audit, official Slack SDK, >1M/week); no [ASSUMED]/[SUS] packages → no blocking checkpoint required |
</threat_model>

<verification>
- `python -m pytest tests/test_notify_safe_str_secrets.py tests/test_integration_deliveries_schema.py tests/test_install_all_includes_notify.py -x -q` all green.
- `python -m compileall quirk/` clean.
</verification>

<success_criteria>
- safe_str scrubs the three new integration-secret shapes without over-redacting ordinary text.
- init_db idempotently creates integration_deliveries with the agreed 7-column schema.
- [notify] extra exists and its [all] membership is locked by a CI guard, with the dependency-conflict check recorded.
</success_criteria>

<output>
Create `.planning/phases/101-notification-fan-out-security-foundation/101-01-SUMMARY.md` when done.
</output>
