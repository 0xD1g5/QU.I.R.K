---
phase: 30
plan: 03
type: execute
wave: 3
depends_on: [30-01, 30-02]
files_modified:
  - quirk/intelligence/evidence.py
  - quirk/intelligence/scoring.py
  - quirk/cbom/builder.py
  - quantum-chaos-enterprise-lab/storage/vault-seed.sh
  - labs/storage/expected_results.md
  - docs/UAT-SERIES.md
autonomous: true
requirements: [VAULT-01, VAULT-02, VAULT-03]
tags: [vault, evidence, scoring, cbom, chaos-lab, docs, uat, obsidian]
must_haves:
  truths:
    - "_PROTOCOL_KEYS in evidence.py includes 'VAULT' (appended after 'AZURE_BLOB')"
    - "build_evidence_summary returns dar_vault_weak_count and dar_vault_weak_ratio"
    - "dar_vault_weak_count increments only when ep.protocol=='VAULT' and ep.severity=='HIGH' (not MEDIUM, not None)"
    - "SCORE_WEIGHTS contains dar_vault_weak_ratio: 8.0"
    - "compute_readiness_score includes a 'Vault weak crypto posture' driver in dar_impacts"
    - "NUM_SUBSCORES (or its equivalent — 5 subscores total) is unchanged — Plan 03 must NOT add a 6th subscore (Pitfall 7)"
    - "CBOM builder Pass 2 certificate skip-list includes 'VAULT'"
    - "CBOM builder Pass 3 protocol skip-list includes 'VAULT'"
    - "CBOM builder Pass 1 does NOT skip 'VAULT' — transit endpoints must produce algorithm components"
    - "vault-seed.sh extension enables PKI mount with RSA-2048 root CA (HIGH finding path)"
    - "vault-seed.sh extension enables userpass auth method (MEDIUM finding path)"
    - "labs/storage/expected_results.md documents Vault scanner expected output"
    - "docs/UAT-SERIES.md updated with Phase 30 UAT cases"
    - "All Plan 01 RED tests in tests/test_dar_vault_scoring.py turn GREEN"
  artifacts:
    - path: "quirk/intelligence/evidence.py"
      provides: "VAULT in _PROTOCOL_KEYS + dar_vault_weak_count counter + ratio in return dict"
      contains: "dar_vault_weak_count"
    - path: "quirk/intelligence/scoring.py"
      provides: "dar_vault_weak_ratio: 8.0 weight + Vault impact in dar_impacts list"
      contains: "dar_vault_weak_ratio"
    - path: "quirk/cbom/builder.py"
      provides: "VAULT added to Pass 2 + Pass 3 skip tuples (NOT Pass 1)"
      contains: "VAULT"
    - path: "quantum-chaos-enterprise-lab/storage/vault-seed.sh"
      provides: "PKI mount + userpass auth method for chaos lab RED finding paths"
      contains: "vault secrets enable pki"
    - path: "labs/storage/expected_results.md"
      provides: "Vault scanner section documenting expected scan output"
      min_lines: 30
    - path: "docs/UAT-SERIES.md"
      provides: "Phase 30 UAT test cases"
      contains: "Phase 30"
  key_links:
    - from: "quirk/intelligence/evidence.py"
      to: "build_evidence_summary return dict"
      via: "dar_vault_weak_count and dar_vault_weak_ratio keys present"
      pattern: "dar_vault_weak_count"
    - from: "quirk/intelligence/scoring.py SCORE_WEIGHTS"
      to: "dar_impacts list"
      via: "Vault impact tuple appended"
      pattern: "Vault weak crypto posture"
    - from: "quirk/cbom/builder.py Pass 2"
      to: "VAULT protocol skip"
      via: "if ep.protocol in (..., 'AZURE_BLOB', 'VAULT'): continue"
      pattern: "AZURE_BLOB.*VAULT"
    - from: "quirk/cbom/builder.py Pass 3"
      to: "VAULT protocol skip"
      via: "elif ep.protocol in (..., 'AZURE_BLOB', 'VAULT'): continue"
      pattern: "AZURE_BLOB.*VAULT"
    - from: "vault-seed.sh PKI section"
      to: "scan output HIGH finding"
      via: "key_type=rsa key_bits=2048 produces severity=HIGH on scan"
      pattern: "key_bits=2048"
---

<objective>
Complete Phase 30 by extending the intelligence and CBOM pipelines to handle the new
`protocol="VAULT"` endpoints from Plan 02, extending the existing chaos lab `vault-seed.sh` to
seed PKI and userpass scenarios for live RED-finding paths, and updating user-facing
documentation. After this plan:

1. The evidence summary returns `dar_vault_weak_count` and `dar_vault_weak_ratio`
2. The readiness score includes a "Vault weak crypto posture" impact in `dar_impacts` (no new subscore added — preserves NUM_SUBSCORES=5)
3. The CBOM builder skips `VAULT` in Pass 2 + Pass 3 (but NOT Pass 1, per research — transit endpoints must produce algorithm components for the PQC positive findings)
4. The `vault-seed.sh` chaos seed (used by the existing `storage` Docker Compose profile at port 20009) seeds a PKI mount with an RSA-2048 root CA and enables `userpass` auth — producing the live HIGH/MEDIUM findings expected by `labs/storage/expected_results.md`
5. `labs/storage/expected_results.md` and `docs/UAT-SERIES.md` document the Phase 30 expected output and UAT cases
6. All Plan 01 RED tests in `tests/test_dar_vault_scoring.py` turn GREEN

Purpose: Land the intelligence + CBOM + chaos lab + docs work that the user-facing experience
(scan output, score, CBOM, chaos lab walkthrough, UAT) depends on. Per CLAUDE.md mandatory
phase completion steps, this plan also delivers the docs/UAT-SERIES.md update — the Obsidian
sync (vault file write + property:set) is performed by the orchestrator's
`update_project_md`/`update_roadmap` post-execution steps, not by tasks here.

Output: 6 modified files completing Phase 30. After this plan, all Plan 01 tests are GREEN
and the `storage` Docker Compose profile produces the live findings table documented in
`labs/storage/expected_results.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-hashicorp-vault-connector/30-RESEARCH.md
@.planning/phases/30-hashicorp-vault-connector/30-PATTERNS.md
@.planning/phases/30-hashicorp-vault-connector/30-01-SUMMARY.md
@.planning/phases/30-hashicorp-vault-connector/30-02-SUMMARY.md
@CLAUDE.md

# Reference implementations to extend
@quirk/intelligence/evidence.py
@quirk/intelligence/scoring.py
@quirk/cbom/builder.py
@quantum-chaos-enterprise-lab/storage/vault-seed.sh
@labs/storage/expected_results.md
@docs/UAT-SERIES.md
@tests/test_dar_vault_scoring.py

<interfaces>
<!-- Required modifications the executor must produce. -->

evidence.py changes:
- _PROTOCOL_KEYS tuple — append "VAULT" after "AZURE_BLOB"
- After dar_storage_aws_managed_count initialization (~line 84), add: `dar_vault_weak_count = 0`
- After `elif proto == "AZURE_BLOB":` block, add new elif:
  ```python
  elif proto == "VAULT":
      sev = str(getattr(ep, "severity", "") or "")
      if sev == "HIGH":
          dar_vault_weak_count += 1
  ```
- After `dar_storage_aws_managed_ratio` in the return dict, add:
  ```python
  "dar_vault_weak_count": dar_vault_weak_count,
  "dar_vault_weak_ratio": round(dar_vault_weak_count / total_endpoints, 4) if total_endpoints else 0.0,
  ```

scoring.py changes:
- After `"dar_storage_aws_managed_ratio": 4.0,` in SCORE_WEIGHTS, add:
  ```python
  "dar_vault_weak_ratio": 8.0,    # Phase 30 — Vault crypto policy gap
  ```
- After `dar_storage_aws_managed = max(0, _as_int(evidence.get("dar_storage_aws_managed_count", 0)))` (~line 134), add:
  ```python
  dar_vault_weak = max(0, _as_int(evidence.get("dar_vault_weak_count", 0)))
  ```
- Append to existing `dar_impacts` list (do NOT create a new subscore block):
  ```python
  ("Vault weak crypto posture", -_ratio(dar_vault_weak, denom) * w["dar_vault_weak_ratio"]),
  ```

builder.py changes:
- Pass 2 — add "VAULT" to certificate skip tuple
- Pass 3 — add "VAULT" to protocol skip tuple
- Pass 1 — DO NOT add "VAULT". Transit endpoints carry cert_pubkey_alg and must produce algorithm components for PQC positive findings.

vault-seed.sh extension — add BEFORE the final `vault secrets list` line. Exact additions:
1. PKI section: enable pki, tune max-lease-ttl, generate RSA-2048 root CA with common_name="quirk-test-ca"
2. userpass section: enable userpass, create test user "testuser" with password "testpass" and policies=default

labs/storage/expected_results.md — append a Vault Scanner section with:
- Lab setup command: `docker compose --profile storage up -d`
- Scanner config snippet (enable_vault: true, vault_addr, vault_token)
- Expected scan output table with rows for transit keys (4 informational), PKI HIGH, token HIGH, userpass MEDIUM
- Expected Evidence/Scoring Impact: dar_vault_weak_count = 2 (PKI HIGH + token HIGH), dar_vault_weak_ratio
- Expected CBOM Output: PQC algorithm components present if PQC keys seeded; VAULT protocol does not produce certificate or protocol components

docs/UAT-SERIES.md — add Phase 30 UAT cases per CLAUDE.md mandatory step #2. The UAT-SERIES.md
relevant series for Vault is the cloud/storage series (the actual series number must be looked
up from the existing file's structure). Add at minimum:
- UAT case: enable Vault scanner, run against chaos lab, verify HIGH count
- UAT case: verify dat_scan_json populated for all VAULT rows in SQLite output
- UAT case: verify CBOM does not contain spurious VAULT certificate or protocol components
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend evidence.py + scoring.py + builder.py (turn dar_vault_scoring tests GREEN)</name>
  <files>quirk/intelligence/evidence.py, quirk/intelligence/scoring.py, quirk/cbom/builder.py</files>
  <read_first>
    - quirk/intelligence/evidence.py (read entire file — _PROTOCOL_KEYS at line 9; dar_db_* counter init at lines 78-79; dar_storage_* counter init at lines 81-84; elif proto blocks at lines 150-183; return dict at lines 235-242)
    - quirk/intelligence/scoring.py (read entire file — SCORE_WEIGHTS at lines 5-27; dar_ extraction at lines 131-134; dar_impacts list at lines 172-177)
    - quirk/cbom/builder.py (lines 400-420 Pass 1 — confirm DAR skip clause does NOT include VAULT; lines 436-438 Pass 2 — current certificate skip tuple ending in "S3", "AZURE_BLOB"; lines 517-519 Pass 3 — current protocol skip tuple ending in "S3", "AZURE_BLOB")
    - tests/test_dar_vault_scoring.py (the contract this task must satisfy — every test must pass after this task)
    - .planning/phases/30-hashicorp-vault-connector/30-RESEARCH.md (Code Examples section: Evidence Counter Pattern + scoring.py Impact Addition + Pitfall 6 + Pitfall 7)
    - .planning/phases/30-hashicorp-vault-connector/30-PATTERNS.md (evidence.py + scoring.py + cbom/builder.py pattern sections)
  </read_first>
  <action>
    Make three coordinated edits — one per file. All edits are ADDITIVE (no deletions).

    EDIT 1 — quirk/intelligence/evidence.py (4 sub-edits in this file):

    1a. Locate `_PROTOCOL_KEYS = (...)` at the top of the file (around line 9). Append `"VAULT"` after `"AZURE_BLOB"`:
    ```python
    _PROTOCOL_KEYS = ("TLS", "HTTP", "SSH", "UNKNOWN", "KERBEROS", "SAML", "DNSSEC",
                      "POSTGRESQL", "MYSQL", "RDS", "S3", "AZURE_BLOB", "VAULT")
    ```

    1b. Locate the dar_storage_* counter initializers (after `dar_storage_aws_managed_count = 0`, around line 84). Immediately after that line, add:
    ```python
        # Vault DAR counter (Phase 30)
        dar_vault_weak_count = 0   # transit keys with weak alg + PKI HIGH + HIGH auth methods
    ```
    Indent to match the surrounding counters (likely 4 spaces inside `build_evidence_summary` function body).

    1c. Locate the existing `elif proto == "AZURE_BLOB":` block (around lines 178-183). Immediately AFTER the closing of that elif block, add:
    ```python
            elif proto == "VAULT":
                sev = str(getattr(ep, "severity", "") or "")
                if sev == "HIGH":
                    dar_vault_weak_count += 1
    ```
    Match the indentation of the `elif proto == "AZURE_BLOB":` block exactly (likely 8 spaces).

    1d. Locate the return dict, specifically the lines containing `"dar_storage_aws_managed_ratio"` (around line 242). Immediately AFTER the `dar_storage_aws_managed_ratio` entry, add two new entries:
    ```python
            "dar_vault_weak_count": dar_vault_weak_count,
            "dar_vault_weak_ratio": round(dar_vault_weak_count / total_endpoints, 4) if total_endpoints else 0.0,
    ```
    Match the indentation of the surrounding dict entries.

    EDIT 2 — quirk/intelligence/scoring.py (3 sub-edits in this file):

    2a. Locate the SCORE_WEIGHTS dict (lines 5-27). Find the `"dar_storage_aws_managed_ratio": 4.0,` line. Immediately AFTER it, add:
    ```python
        "dar_vault_weak_ratio": 8.0,    # Phase 30 — Vault crypto policy gap
    ```
    Indent to match other SCORE_WEIGHTS entries (likely 4 spaces).

    2b. Locate the dar_storage_aws_managed extraction (around line 134, line `dar_storage_aws_managed = max(0, _as_int(evidence.get("dar_storage_aws_managed_count", 0)))`). Immediately AFTER it, add:
    ```python
        dar_vault_weak = max(0, _as_int(evidence.get("dar_vault_weak_count", 0)))
    ```
    Indent to match.

    2c. Locate the `dar_impacts: List[Tuple[str, float]] = [` list (around line 172-177). Append a new tuple as the LAST entry inside the list (before the closing `]`):
    ```python
            ("Vault weak crypto posture", -_ratio(dar_vault_weak, denom) * w["dar_vault_weak_ratio"]),
    ```
    Match the indentation of the existing tuples in the list.

    DO NOT modify NUM_SUBSCORES (Pitfall 7). DO NOT create a new subscore block. The Vault
    impact joins the existing dar_impacts list — the dar subscore cap stays at 25, just reaches
    the floor faster when Vault findings are present.

    EDIT 3 — quirk/cbom/builder.py (2 sub-edits in this file):

    3a. Locate the Pass 2 certificate skip tuple (around lines 436-438). It currently reads:
    ```python
    if ep.protocol in ("SSH", "CONTAINER", "SOURCE", "KERBEROS", "SAML", "DNSSEC",
                       "GCP", "CLOUD_SQL", "POSTGRESQL", "MYSQL", "RDS",
                       "S3", "AZURE_BLOB"):
        continue
    ```
    Append `"VAULT"` to the tuple (preserve existing line wrapping):
    ```python
    if ep.protocol in ("SSH", "CONTAINER", "SOURCE", "KERBEROS", "SAML", "DNSSEC",
                       "GCP", "CLOUD_SQL", "POSTGRESQL", "MYSQL", "RDS",
                       "S3", "AZURE_BLOB", "VAULT"):
        continue
    ```

    3b. Locate the Pass 3 protocol skip tuple (around lines 517-519). It currently reads:
    ```python
    elif ep.protocol in ("JWT", "CONTAINER", "SOURCE", "AWS", "AZURE", "GCP", "CLOUD_SQL",
                         "DNSSEC", "SAML", "KERBEROS", "POSTGRESQL", "MYSQL", "RDS",
                         "S3", "AZURE_BLOB"):
        # These are not TLS/SSH network protocols — no ProtocolProperties component.
        continue
    ```
    Append `"VAULT"` to the tuple:
    ```python
    elif ep.protocol in ("JWT", "CONTAINER", "SOURCE", "AWS", "AZURE", "GCP", "CLOUD_SQL",
                         "DNSSEC", "SAML", "KERBEROS", "POSTGRESQL", "MYSQL", "RDS",
                         "S3", "AZURE_BLOB", "VAULT"):
        # These are not TLS/SSH network protocols — no ProtocolProperties component.
        continue
    ```

    DO NOT add VAULT to the Pass 1 DAR skip clause (around line 410). Transit endpoints carry
    cert_pubkey_alg and MUST fall through to the generic `_register_algorithm` path so PQC
    algorithms (ml-dsa, slh-dsa) produce positive quantum-safe components in the CBOM.

    Verify after all edits:
    - `python -m compileall quirk/intelligence/evidence.py quirk/intelligence/scoring.py quirk/cbom/builder.py` exits 0
    - `python -m pytest tests/test_dar_vault_scoring.py -x -q` shows zero failures (RED → GREEN)
    - `python -m pytest tests/ -x -q --ignore=tests/test_chaos_storage.py 2>&1 | tail -3` shows no regressions (existing dar_storage / dar_db tests still pass)
  </action>
  <acceptance_criteria>
    - `grep -c '"VAULT"' quirk/intelligence/evidence.py` returns at least 2 (in _PROTOCOL_KEYS, in elif proto)
    - `grep -c 'dar_vault_weak_count' quirk/intelligence/evidence.py` returns at least 4 (init, increment, return-key, assignment)
    - `grep -c 'dar_vault_weak_ratio' quirk/intelligence/evidence.py` returns at least 1 (return dict key)
    - `grep -c 'elif proto == "VAULT":' quirk/intelligence/evidence.py` returns 1
    - `grep -c '"dar_vault_weak_ratio": 8.0' quirk/intelligence/scoring.py` returns 1
    - `grep -c 'dar_vault_weak = max' quirk/intelligence/scoring.py` returns 1
    - `grep -c 'Vault weak crypto posture' quirk/intelligence/scoring.py` returns 1
    - `grep -c '"VAULT"' quirk/cbom/builder.py` returns at least 2 (Pass 2 + Pass 3)
    - `grep -A2 'Pass 1' quirk/cbom/builder.py | grep -c '"VAULT"'` returns 0 (Pass 1 must NOT skip VAULT)
    - `python -m compileall quirk/intelligence/evidence.py quirk/intelligence/scoring.py quirk/cbom/builder.py 2>&1 | grep -v 'Compiling'` shows no errors
    - `python -c "from quirk.intelligence.evidence import _PROTOCOL_KEYS; assert 'VAULT' in _PROTOCOL_KEYS; print('OK')"` exits 0 and prints `OK`
    - `python -c "from quirk.intelligence.scoring import SCORE_WEIGHTS; assert SCORE_WEIGHTS['dar_vault_weak_ratio'] == 8.0; print('OK')"` exits 0 and prints `OK`
    - `python -m pytest tests/test_dar_vault_scoring.py -x -q 2>&1 | tail -3` shows zero failures
    - `python -m pytest tests/test_intelligence_evidence.py tests/test_intelligence_scoring.py tests/test_cbom_builder.py -x -q 2>&1 | tail -3` shows no regressions
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_dar_vault_scoring.py tests/test_intelligence_evidence.py tests/test_intelligence_scoring.py -x -q</automated>
  </verify>
  <done>
    evidence.py has VAULT in _PROTOCOL_KEYS + dar_vault_weak_count counter increment + ratio in
    return dict; scoring.py has dar_vault_weak_ratio: 8.0 + extraction + Vault impact in
    dar_impacts; builder.py Pass 2 + Pass 3 skip tuples include VAULT (Pass 1 unchanged). All
    Plan 01 RED tests in tests/test_dar_vault_scoring.py turn GREEN. NUM_SUBSCORES still 5.
    Existing tests for dar_storage / dar_db / cbom builder do not regress.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend vault-seed.sh + labs/storage/expected_results.md (chaos lab seed + docs)</name>
  <files>quantum-chaos-enterprise-lab/storage/vault-seed.sh, labs/storage/expected_results.md</files>
  <read_first>
    - quantum-chaos-enterprise-lab/storage/vault-seed.sh (the existing 33-line file — extend, do not replace)
    - quantum-chaos-enterprise-lab/docker-compose.yml lines 651-720 (verify Vault container at port 20009 in storage profile + vault-seed entrypoint already mounts the seed script)
    - labs/storage/expected_results.md (read entire file — Phase 28 section is the analog template; Phase 30 section appended at end)
    - .planning/phases/30-hashicorp-vault-connector/30-RESEARCH.md (Runtime State Inventory section + Open Questions 2 & 3)
    - .planning/phases/30-hashicorp-vault-connector/30-PATTERNS.md (vault-seed.sh + labs/storage/expected_results.md pattern sections)
  </read_first>
  <action>
    Make two file edits.

    EDIT 1 — quantum-chaos-enterprise-lab/storage/vault-seed.sh: Insert the PKI and userpass
    sections BEFORE the final `vault secrets list` line (the last line of the existing file).
    Follow the same `command 2>/dev/null || echo "already done"` idempotency pattern used on
    line 15 for transit. Exact text to insert:

    ```sh
    echo "=== Enabling PKI secrets engine (Phase 30 — VAULT-02 RED finding) ==="
    vault secrets enable pki 2>/dev/null || echo "PKI already enabled"
    vault secrets tune -max-lease-ttl=87600h pki

    echo "=== Generating PKI root CA (RSA-2048, intentionally weak for HIGH finding) ==="
    vault write pki/root/generate/internal \
      common_name="quirk-test-ca" \
      key_type=rsa \
      key_bits=2048 \
      ttl=87600h 2>/dev/null || echo "PKI root CA already generated"

    echo "=== Enabling userpass auth method (Phase 30 — VAULT-03 MEDIUM finding) ==="
    vault auth enable userpass 2>/dev/null || echo "Userpass already enabled"

    echo "=== Creating test userpass user ==="
    vault write auth/userpass/users/testuser \
      password=testpass \
      policies=default 2>/dev/null || echo "Test user already created"
    ```

    Insert this block AFTER the existing `vault kv put secret/crypto-lab ...` block (line 30)
    and BEFORE the existing `echo "=== Vault seed complete ==="` line (line 32). The final
    `vault secrets list` (line 33) stays at the end.

    EDIT 2 — labs/storage/expected_results.md: Append a new section at the end of the file.
    The existing file documents Phase 28 (Object Storage / MinIO). Phase 30 adds a Vault
    section. Use this exact content:

    ```markdown


    ## Phase 30 — Vault Connector

    ### Lab Setup

    The Vault chaos target is part of the same `storage` Docker Compose profile as the MinIO
    target (Phase 28). Vault listens on host port `20009` and is automatically seeded by
    `vault-seed.sh` (extended in Phase 30).

    ```bash
    cd quantum-chaos-enterprise-lab
    docker compose --profile storage up -d
    # Wait ~10s for vault-seed to complete (transit + PKI + userpass)
    docker compose logs vault-seed | tail -5
    ```

    Verify the seed completed:

    ```bash
    docker compose exec vault vault secrets list
    # Expect: transit/, pki/, kv/, sys/, identity/, cubbyhole/
    docker compose exec vault vault auth list
    # Expect: token/, userpass/
    ```

    ### Scanner Configuration

    Add to your QU.I.R.K. config.yaml:

    ```yaml
    connectors:
      enable_vault: true
      vault_addr: "http://localhost:20009"
      vault_token: "root"
      vault_transit_mount: "transit"
    ```

    Or via environment:

    ```bash
    export VAULT_ADDR="http://localhost:20009"
    export VAULT_TOKEN="root"
    ```

    ### Expected Scan Output

    | host                                                   | service_detail   | severity                   |
    |--------------------------------------------------------|------------------|----------------------------|
    | `http://localhost:20009/transit/keys/rsa-2048`         | `transit/rsa-2048` | (none — informational)   |
    | `http://localhost:20009/transit/keys/rsa-1024`         | `transit/rsa-1024` | (none — informational)   |
    | `http://localhost:20009/transit/keys/aes256`           | `transit/aes256`   | (none — informational)   |
    | `http://localhost:20009/transit/keys/ecdsa-p256`       | `transit/ecdsa-p256` | (none — informational) |
    | `http://localhost:20009/pki`                           | `PKI/pki`          | `HIGH` (RSA-2048 root CA)  |
    | `http://localhost:20009/auth/token/`                   | `auth/token/`      | `HIGH` (root token enabled) |
    | `http://localhost:20009/auth/userpass/`                | `auth/userpass/`   | `MEDIUM` (userpass enabled) |

    Total VAULT endpoints: 7 (4 transit informational + 1 PKI HIGH + 1 token HIGH + 1 userpass MEDIUM).

    ### Expected Evidence/Scoring Impact

    | Counter / Field                  | Value | Notes                                              |
    |----------------------------------|-------|----------------------------------------------------|
    | `dar_vault_weak_count`           | 2     | PKI HIGH + token HIGH (MEDIUM does not increment)  |
    | `dar_vault_weak_ratio`           | 2 / total_endpoints | Floats; depends on overall scan size |
    | Score subscore `data_at_rest`    | Reduced | Vault impact joins existing dar_impacts list   |
    | NUM_SUBSCORES                    | 5     | Unchanged — Vault adds an impact, not a subscore   |

    ### Expected CBOM Output

    - PQC components: none in this seed (Vault OSS 1.15 does not yet support ml-dsa/slh-dsa
      transit key types). When Vault Enterprise or future OSS adds PQC support, ml-dsa-* and
      slh-dsa-* transit keys will produce positive quantum-safe algorithm components.
    - Standard transit-key components: `RSA` (2048, 1024), `AES` (256), `ECDSA` (256) — produced
      via Pass 1's generic `_register_algorithm` path (Pass 1 does NOT skip VAULT).
    - VAULT endpoints produce NO Certificate components (Pass 2 skip).
    - VAULT endpoints produce NO ProtocolProperties components (Pass 3 skip).

    ### dat_scan_json Persistence

    Each VAULT row in the SQLite `crypto_endpoints` table has `dat_scan_json` populated with:
    - Transit key rows: `{"mount_point", "key_name", "key_type", "exportable", "latest_version"}`
    - PKI rows: `{"mount_point", "sig_alg", "key_size", "finding"}`
    - Auth method rows: `{"auth_path", "auth_type", "remediation"}`

    ### Teardown

    ```bash
    docker compose --profile storage down -v
    ```

    ### Limitations

    - Vault dev mode is ephemeral — transit/PKI/userpass state is lost on container restart.
      The seed script re-runs idempotently on the next `docker compose up`.
    - LDAP auth method is NOT seeded (would require an external LDAP server). The `token` auth
      method is always present in dev mode and provides the always-on HIGH finding path for
      VAULT-03 testing.
    - The PKI root CA generated by the seed uses RSA-2048 specifically to trigger the HIGH
      finding. Production deployments should use RSA-4096 or ECDSA-P384.
    ```

    Verify after edits:
    - `python -m py_compile` is not applicable to .sh / .md files; verify shell syntax instead:
      `sh -n quantum-chaos-enterprise-lab/storage/vault-seed.sh` exits 0
    - `wc -l quantum-chaos-enterprise-lab/storage/vault-seed.sh` shows the file grew (≥ 47 lines from original 33)
    - `wc -l labs/storage/expected_results.md` shows growth (≥ +50 lines)
  </action>
  <acceptance_criteria>
    - `grep -c 'vault secrets enable pki' quantum-chaos-enterprise-lab/storage/vault-seed.sh` returns 1
    - `grep -c 'vault write pki/root/generate/internal' quantum-chaos-enterprise-lab/storage/vault-seed.sh` returns 1
    - `grep -c 'key_bits=2048' quantum-chaos-enterprise-lab/storage/vault-seed.sh` returns 1
    - `grep -c 'common_name="quirk-test-ca"' quantum-chaos-enterprise-lab/storage/vault-seed.sh` returns 1
    - `grep -c 'vault auth enable userpass' quantum-chaos-enterprise-lab/storage/vault-seed.sh` returns 1
    - `grep -c 'auth/userpass/users/testuser' quantum-chaos-enterprise-lab/storage/vault-seed.sh` returns 1
    - `grep -c 'Phase 30' quantum-chaos-enterprise-lab/storage/vault-seed.sh` returns at least 2
    - `sh -n quantum-chaos-enterprise-lab/storage/vault-seed.sh` exits 0
    - `tail -1 quantum-chaos-enterprise-lab/storage/vault-seed.sh | grep -c 'vault secrets list'` returns 1 (final line preserved)
    - `grep -c 'Phase 30 — Vault Connector' labs/storage/expected_results.md` returns 1
    - `grep -c 'dar_vault_weak_count' labs/storage/expected_results.md` returns at least 1
    - `grep -c 'http://localhost:20009/transit/keys' labs/storage/expected_results.md` returns at least 1
    - `grep -c 'PKI/pki' labs/storage/expected_results.md` returns at least 1
    - `grep -c 'auth/token/' labs/storage/expected_results.md` returns at least 1
    - `grep -c 'auth/userpass/' labs/storage/expected_results.md` returns at least 1
    - `grep -c 'NUM_SUBSCORES' labs/storage/expected_results.md` returns at least 1
  </acceptance_criteria>
  <verify>
    <automated>sh -n quantum-chaos-enterprise-lab/storage/vault-seed.sh && grep -c 'Phase 30 — Vault Connector' labs/storage/expected_results.md</automated>
  </verify>
  <done>
    vault-seed.sh extended with PKI mount (RSA-2048 root CA generating HIGH finding) and
    userpass auth method (MEDIUM finding); final `vault secrets list` line preserved as last
    line. labs/storage/expected_results.md has a complete Phase 30 — Vault Connector section
    documenting lab setup, scanner config, expected scan output table (7 rows), evidence
    counter values, CBOM expectations, and limitations.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update docs/UAT-SERIES.md with Phase 30 UAT cases (CLAUDE.md mandatory step)</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (read entire file structure — find the relevant series for cloud/storage scanners; identify the Phase 28 entries as the analog template; locate the **Last Updated:** date at top)
    - CLAUDE.md (Mandatory Phase Completion Steps section #2 and #4 — UAT-SERIES update + commit)
    - .planning/phases/30-hashicorp-vault-connector/30-RESEARCH.md (Validation Architecture — Phase Requirements → Test Map for the test cases to surface in UAT)
  </read_first>
  <action>
    Update docs/UAT-SERIES.md per CLAUDE.md mandatory step #2.

    Sub-step A — Update the `**Last Updated:**` date at the top of the file to today's date
    (2026-04-26). If the date is already correct, no change needed.

    Sub-step B — Locate the cloud/storage UAT series (whichever section currently contains the
    Phase 28 entries). The series number must be discovered from the existing file structure;
    research's Validation Architecture section describes Phase 30 tests but does not assign a
    series. Choose the same series that contains Phase 28's S3/Blob entries — Vault is the
    natural extension because it shares the `storage` Docker Compose profile.

    Add Phase 30 UAT cases at the end of that series, following the same format as the
    surrounding Phase 28 cases. The minimum set of cases to add (assign sequential numbers
    extending the series):

    1. **Vault scanner enabled, chaos lab scan**
       - Setup: `docker compose --profile storage up -d` (chaos lab Vault running on port 20009 + seeded with PKI + userpass)
       - Config: `enable_vault: true`, `vault_addr: "http://localhost:20009"`, `vault_token: "root"`
       - Run: `python -m quirk scan --config config.yaml`
       - Pass criteria: scan output includes at least 7 VAULT-protocol endpoints (4 transit + 1 PKI + 1 token + 1 userpass); PKI row has severity=HIGH; auth/token/ row has severity=HIGH; auth/userpass/ row has severity=MEDIUM

    2. **Vault PQC positive finding (when seeded)**
       - Setup: same as case 1 (Vault OSS does not yet support ml-dsa, but document the expected behavior for Enterprise / future OSS)
       - Pass criteria: if `transit/keys/<pq-key>` of type ml-dsa-87 is present, the CBOM contains a positive quantum-safe algorithm component named `ml-dsa-87` with `nist_level >= 1`

    3. **Vault dat_scan_json populated in SQLite**
       - Setup: same as case 1
       - Verification: query SQLite — `SELECT host, service_detail, dat_scan_json FROM crypto_endpoints WHERE protocol = 'VAULT';`
       - Pass criteria: every row has dat_scan_json populated with valid JSON; transit rows contain `key_type`; PKI rows contain `mount_point`; auth rows contain `auth_type`

    4. **Vault scanner disabled when hvac not installed**
       - Setup: ensure hvac is NOT in active venv (or simulate by mocking HVAC_AVAILABLE=False)
       - Config: `enable_vault: true`
       - Pass criteria: scan completes without crash; verbose log contains "hvac not installed — Vault scanning skipped"; zero VAULT-protocol endpoints in output

    5. **Vault scanner produces scan_error on missing token**
       - Setup: `enable_vault: true`, `vault_addr: "http://localhost:20009"`, no `vault_token`, no `VAULT_TOKEN` env var
       - Pass criteria: scan output contains exactly one VAULT endpoint with `scan_error` containing "vault-no-token"; no other VAULT endpoints

    6. **Vault scoring impact on data_at_rest subscore**
       - Setup: same as case 1 (chaos lab live)
       - Verification: compare `data_at_rest` subscore before vs after enabling Vault scanner
       - Pass criteria: `data_at_rest` subscore is lower with Vault enabled (due to dar_vault_weak_count=2 finding); NUM_SUBSCORES still 5

    7. **CBOM does not contain spurious VAULT certificate or protocol components**
       - Setup: same as case 1; export CBOM
       - Pass criteria: CBOM has no `cryptographicAsset` with type `certificate` whose source is a VAULT endpoint; no `cryptographicAsset` of type `protocol` for VAULT endpoints; transit-key algorithm components ARE present (Pass 1 not skipped)

    Use the exact case-formatting style of the existing UAT-SERIES.md entries (numbered list,
    bold subheadings, etc.). If the existing file uses Setup / Steps / Pass criteria layout,
    follow that. If the file uses a more compact format, mirror the Phase 28 entries.

    Sub-step C — Verify the file is still valid Markdown and contains the expected content.

    Note: per CLAUDE.md mandatory steps #1 (Obsidian phase note) and #3 (sync UAT-SERIES.md to
    Obsidian) are performed by the orchestrator's `update_project_md` post-execution step, NOT
    by this task. Likewise, the commit per step #4 is performed by the standard /gsd workflow's
    final commit, not by this task.
  </action>
  <acceptance_criteria>
    - `grep -c 'Phase 30' docs/UAT-SERIES.md` returns at least 3 (one per major UAT case grouping)
    - `grep -c 'enable_vault' docs/UAT-SERIES.md` returns at least 1
    - `grep -c 'vault_addr' docs/UAT-SERIES.md` returns at least 1
    - `grep -c 'VAULT' docs/UAT-SERIES.md` returns at least 5
    - `grep -c 'dar_vault_weak_count' docs/UAT-SERIES.md` returns at least 1
    - `grep -c 'PKI' docs/UAT-SERIES.md` returns at least 1
    - `grep -c 'userpass' docs/UAT-SERIES.md` returns at least 1
    - `grep -c 'hvac' docs/UAT-SERIES.md` returns at least 1
    - `grep -c '2026-04-26' docs/UAT-SERIES.md` returns at least 1 (Last Updated date)
    - `python -c "import sys; content = open('docs/UAT-SERIES.md').read(); assert 'ml-dsa' in content or 'PQC' in content; print('OK')"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'Phase 30' docs/UAT-SERIES.md && grep -c 'enable_vault' docs/UAT-SERIES.md</automated>
  </verify>
  <done>
    docs/UAT-SERIES.md updated with at least 7 Phase 30 UAT cases covering: chaos lab scan,
    PQC positive findings, dat_scan_json persistence, hvac unavailable graceful degradation,
    missing token scan_error, scoring impact, and CBOM no-spurious-components invariant.
    Last Updated date set to 2026-04-26. Format matches surrounding Phase 28 entries.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 02 scanner output → evidence/scoring/CBOM pipeline | Endpoints from scan_vault_targets cross into the intelligence layer; the layer must handle severity=HIGH/MEDIUM/None correctly without crashing on missing fields. |
| vault-seed.sh → Vault dev container | Seed script runs as root in container; root CA private key generated by Vault stays in container memory (dev mode only — ephemeral). |
| docs/UAT-SERIES.md → public-facing docs | UAT documentation is shipped with the project; must not contain real production tokens or addresses. Use only `localhost:20009` and `root` (chaos lab convention). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-12 | Tampering | quirk/intelligence/evidence.py | mitigate | All edits are additive; no existing counter or _PROTOCOL_KEYS entry is modified. The `elif proto == "VAULT"` block uses `getattr(ep, "severity", "") or ""` to safely handle endpoints with missing severity attribute. Existing dar_storage / dar_db tests confirm no regressions. |
| T-30-13 | Tampering | quirk/intelligence/scoring.py | mitigate | Vault impact appended to existing dar_impacts list — does NOT add a new subscore (Pitfall 7). NUM_SUBSCORES stays 5. The dar subscore cap is per-subscore so adding more impacts cannot cause score overflow. |
| T-30-14 | Information Disclosure | vault-seed.sh PKI root CA | accept | The RSA-2048 root CA private key is intentionally weak and only generated in the dev-mode chaos lab container. It is never persisted, never exported, and never used outside the local Docker network. The HIGH finding is the entire point. |
| T-30-15 | Information Disclosure | docs/UAT-SERIES.md | mitigate | Use only `localhost:20009` and the well-known `root` chaos lab token in UAT examples — these are standard chaos-lab conventions, never production secrets. |
| T-30-16 | Denial of Service | vault-seed.sh idempotency | mitigate | Each new section uses `2>/dev/null || echo "already done"` pattern (matching the existing transit section). Re-running the seed on container restart does not crash. |
</threat_model>

<verification>
After all tasks:
- `python -m compileall quirk/intelligence/evidence.py quirk/intelligence/scoring.py quirk/cbom/builder.py 2>&1 | grep -v 'Compiling'` shows no errors
- `python -m pytest tests/test_dar_vault_scoring.py -x -q` shows zero failures (Plan 01 RED tests now GREEN)
- `python -m pytest tests/test_vault_connector.py -x -q` still shows zero failures (Plan 02 GREEN preserved)
- `python -m pytest tests/ -x -q --ignore=tests/test_chaos_storage.py 2>&1 | tail -5` shows full unit-test suite passing
- `sh -n quantum-chaos-enterprise-lab/storage/vault-seed.sh` exits 0 (shell script syntactically valid)
- `tail -1 quantum-chaos-enterprise-lab/storage/vault-seed.sh` shows `vault secrets list` (final-line invariant preserved)
- `grep -c 'Phase 30' labs/storage/expected_results.md` returns at least 1
- `grep -c 'Phase 30' docs/UAT-SERIES.md` returns at least 3
- (Optional, manual) `docker compose --profile storage up -d` followed by `docker compose logs vault-seed | tail -10` shows the new PKI + userpass sections completed successfully
</verification>

<success_criteria>
- evidence.py has VAULT in _PROTOCOL_KEYS + dar_vault_weak_count counter (HIGH-only) + dar_vault_weak_ratio in return dict
- scoring.py has dar_vault_weak_ratio: 8.0 + dar_vault_weak extraction + Vault impact in dar_impacts list (no new subscore)
- cbom/builder.py Pass 2 + Pass 3 skip tuples include VAULT (Pass 1 unchanged so transit endpoints register algorithm components)
- vault-seed.sh extended with PKI mount (RSA-2048 root CA) + userpass auth method (testuser/testpass); idempotency preserved; final `vault secrets list` line preserved
- labs/storage/expected_results.md has a complete Phase 30 — Vault Connector section
- docs/UAT-SERIES.md updated with at least 7 Phase 30 UAT cases + current date
- All 9+ Plan 01 RED tests in tests/test_dar_vault_scoring.py turn GREEN
- All Plan 02 GREEN tests remain GREEN (no regressions)
- NUM_SUBSCORES still 5
</success_criteria>

<output>
After completion, create `.planning/phases/30-hashicorp-vault-connector/30-03-SUMMARY.md` documenting:
- Files modified (6 total)
- Counter / weight / impact values added
- Chaos lab seed extension content
- Documentation additions (expected_results.md + UAT-SERIES.md)
- Test status: tests/test_dar_vault_scoring.py turns from RED → GREEN; tests/test_vault_connector.py still GREEN
- The mandatory CLAUDE.md post-phase steps that the orchestrator must perform after this plan completes:
  1. Create Obsidian phase note at /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-30-HashiCorp-Vault-Connector.md
  2. Sync docs/UAT-SERIES.md to /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md
  3. Commit docs/UAT-SERIES.md with message "docs(phase-30): update UAT-SERIES.md"
</output>
