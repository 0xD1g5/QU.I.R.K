---
phase: 56.1-close-qramm-06-07-wire-ci-staleness-gate-workflow
plan: 01
subsystem: infra
tags: [github-actions, ci, staleness, qramm, compliance, pytest]

requires:
  - phase: 51-qramm-core-infrastructure
    provides: tests/test_qramm_staleness.py (QRAMM-06 hard gate, QRAMM-07 CLI smoke)
  - phase: 55-qramm-compliance-mapping-view
    provides: tests/test_compliance_freshness.py (COMPLY-08 hard gate)
provides:
  - GitHub Actions workflow that runs the QRAMM staleness gate, compliance freshness gate, and CLI smoke tests on every PR, every push to main, and weekly cron
affects: [phase-56.1-plan-02, future-qramm-phases, future-compliance-phases]

tech-stack:
  added: []
  patterns:
    - "Pytest staleness gate wired to CI via dedicated workflow (separate from path-filtered dashboard-quality.yml)"
    - "Cron + PR + push triggers ensure gate fires on both human change and time passage"

key-files:
  created:
    - .github/workflows/python-staleness.yml
  modified: []

key-decisions:
  - "D-01: Workflow display name is exactly 'Python Staleness Gate'"
  - "D-02: Single job, single cron entry '0 9 * * 1' (Mondays 09:00 UTC); no per-trigger duplication"
  - "D-03: No paths: filter — staleness depends on time passage, not file changes"
  - "D-04: Production CI does NOT set QUIRK_CI_STALENESS_OVERRIDE_DATE (real today() drives the gate)"

patterns-established:
  - "Staleness as a CI-enforced concern: time-driven failures via weekly cron complement PR/push triggers"
  - "Editable install via pip install -e . (pyproject.toml is sole entrypoint; no requirements.txt or [dev] extras)"

requirements-completed: []

duration: 2min
completed: 2026-05-08
---

# Phase 56.1 Plan 01: Wire CI Staleness Gate Workflow Summary

**New GitHub Actions workflow `python-staleness.yml` runs QRAMM staleness, compliance freshness, and CLI smoke tests on PR / push-to-main / weekly cron — closing the wiring gap left by the path-filtered dashboard-quality.yml.**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-05-08
- **Completed:** 2026-05-08
- **Tasks:** 1
- **Files modified:** 1 (created)

## Accomplishments
- Added `.github/workflows/python-staleness.yml` with three triggers (pull_request, push to main, weekly cron Mondays 09:00 UTC)
- Workflow runs the exact pytest invocation: `pytest tests/test_qramm_staleness.py tests/test_compliance_freshness.py -v`
- All four locked decisions D-01..D-04 encoded; verify smoke (yaml.safe_load + structural assertions) passes

## Task Commits

1. **Task 1: Create python-staleness.yml workflow** - `b87aede` (feat)

## Files Created/Modified
- `.github/workflows/python-staleness.yml` — CI staleness gate workflow (28 lines)

## Decisions Made
None beyond the four pre-locked decisions (D-01..D-04) embodied in the file:
- **D-01** Workflow `name: Python Staleness Gate`
- **D-02** Single job `staleness`; single cron `'0 9 * * 1'`
- **D-03** No `paths:` filter on `pull_request` or `push`
- **D-04** No `QUIRK_CI_STALENESS_OVERRIDE_DATE` env (production uses real today())

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered
None. The pre-write security hook flagged GitHub Actions injection patterns; reviewed and confirmed N/A (no untrusted `github.event.*` inputs in any `run:` block).

## User Setup Required
None — workflow runs automatically on the PR that introduces it (per D-07 natural verification).

## Next Phase Readiness
- Plan 02 (CLAUDE.md "Staleness Review Cadence" doc + REQUIREMENTS.md QRAMM-06/07 marking) is unblocked
- The workflow itself will be exercised on the PR that lands plan 02 (or this commit on push to main)

---
*Phase: 56.1-close-qramm-06-07-wire-ci-staleness-gate-workflow*
*Completed: 2026-05-08*
