---
phase: 56.1
plan: 02
subsystem: docs/governance
tags: [staleness, qramm, comply-08, requirements, roadmap, claude-md]
requires: [56.1-01]
provides: [staleness-cadence-doc, qramm-requirement-closure, roadmap-finalization]
affects: [CLAUDE.md, quirk/qramm/model_meta.py, .planning/REQUIREMENTS.md, .planning/ROADMAP.md]
tech_stack_added: []
patterns_used: [docs-cadence-pillar]
key_files_created: []
key_files_modified:
  - CLAUDE.md
  - quirk/qramm/model_meta.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
decisions:
  - Inserted "Staleness Review Cadence" section between "Code Standards" and "Chaos Lab Maintenance" (per D-05)
  - Added COMPLY-08 row to REQUIREMENTS.md traceability table to capture CI protection (table previously did not list it)
metrics:
  duration: ~5 minutes
  completed: 2026-05-08
---

# Phase 56.1 Plan 02: CLAUDE.md cadence + REQUIREMENTS.md / ROADMAP.md closure + model_meta pointer Summary

Closed the documentation half of Phase 56.1: added a top-level Staleness Review
Cadence operational rule to CLAUDE.md, pointed `model_meta.py` at it, marked
QRAMM-05/06/07 complete in REQUIREMENTS.md (with COMPLY-08 cross-noted as
CI-protected), and finalized ROADMAP.md's Phase 56.1 entry with a real Goal and
the actual three-plan list.

## Files Modified

### 1. CLAUDE.md

Inserted a new `## Staleness Review Cadence` section between `## Code Standards`
and `## Chaos Lab Maintenance`. Section text:

```
## Staleness Review Cadence

Two project data files carry a `last_verified` date and are gated by CI to
prevent silent staleness:

- `quirk/qramm/model_meta.py` — QRAMM model (90-day cadence,
  `STALENESS_THRESHOLD_DAYS = 90`)
- `quirk/compliance/__init__.py` — Compliance mappings (365-day cadence)

When CI fails with a "STALE" assertion:

1. Re-verify the catalog against its `source_url`.
2. Bump `last_verified` to today's ISO date in the relevant file.
3. Commit with message `chore: re-verify <model> catalog (YYYY-MM-DD)`.

Enforcement workflow: `.github/workflows/python-staleness.yml` (runs on every
PR, every push to `main`, and weekly Monday 09:00 UTC cron).
```

Commit: `d84fd15`

### 2. quirk/qramm/model_meta.py

Added a single pointer comment after the existing `# Per QRAMM-06` line:

```python
# Per QRAMM-06 — quarterly re-verification cadence for an active open-source model.
# See CLAUDE.md "Staleness Review Cadence" for the bump procedure.
STALENESS_THRESHOLD_DAYS: int = 90
```

`STALENESS_THRESHOLD_DAYS = 90`, `QRAMM_MODEL.{last_verified, qramm_version, source_url}`
all unchanged.

Commit: `e0026a7`

### 3. .planning/REQUIREMENTS.md

- Toggled QRAMM-05, QRAMM-06, QRAMM-07 checkboxes from `[ ]` to `[x]`.
- Updated the traceability table:
  - `QRAMM-05` → `Complete (Phase 55)`
  - `QRAMM-06` → `Complete (Phase 55 + 56.1)`
  - `QRAMM-07` → `Complete (Phase 55 + 56.1)`
- Added a COMPLY-08 row (was missing from the v4.7 table) noting:
  `Complete — CI-protected by python-staleness.yml (Phase 56.1)`.

Commit: `9796f13`

### 4. .planning/ROADMAP.md

Replaced the placeholder Phase 56.1 Goal/Plans block with finalized content:

- **Goal:** Wire the QRAMM staleness gate, the compliance freshness gate, and
  the QRAMM CLI smoke tests into a new GitHub Actions workflow so that
  QRAMM-06's 90-day pytest gate is actually enforced on every PR, every push to
  `main`, and weekly via cron — and document the cadence in `CLAUDE.md`.
- **Requirements:** QRAMM-06, QRAMM-07, COMPLY-08
- **Plans:** 3 plans (56.1-01, 56.1-02, 56.1-03)

`(INSERTED)` marker preserved on heading.

Commit: `81376eb`

## Verification

All four `<verify><automated>` blocks passed:

| Task | Verify command | Result |
|------|----------------|--------|
| 1 | grep cadence section + file paths + workflow path | OK |
| 2 | grep pointer + python import constants intact | OK |
| 3 | grep QRAMM-06/07 [x] + Phase 55 + 56.1 + COMPLY note | OK |
| 4 | grep Phase 56.1 + 3 plan filenames + no "to be planned" | OK |

## Deviations from Plan

### Auto-added Items

**1. [Rule 2 - Critical functionality] Added COMPLY-08 row to traceability table**

- **Found during:** Task 3
- **Issue:** REQUIREMENTS.md traceability table did not contain a COMPLY-08
  row, but the plan's verify block required `grep -q 'CI-protected by python-staleness.yml'`
  AND a must-have truth states "REQUIREMENTS.md COMPLY-08 row notes
  'CI-protected by python-staleness.yml (Phase 56.1)'".
- **Fix:** Added a new row at the end of the traceability table:
  `| COMPLY-08 | Phase 49 | Complete — CI-protected by python-staleness.yml (Phase 56.1) |`
- **Files modified:** `.planning/REQUIREMENTS.md`
- **Commit:** 9796f13

No other deviations. No code logic changed.

## Self-Check: PASSED

- FOUND: CLAUDE.md (Staleness Review Cadence section)
- FOUND: quirk/qramm/model_meta.py (Staleness Review Cadence pointer)
- FOUND: .planning/REQUIREMENTS.md (QRAMM-06/07 [x] + COMPLY-08 row)
- FOUND: .planning/ROADMAP.md (finalized Phase 56.1 entry)
- FOUND commits: d84fd15, e0026a7, 9796f13, 81376eb
