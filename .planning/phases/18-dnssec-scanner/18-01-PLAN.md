---
phase: 18-dnssec-scanner
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_dnssec_scanner.py
  - quirk/scanner/dnssec_scanner.py
autonomous: true
requirements: [DNSSEC-01, DNSSEC-02, DNSSEC-03, DNSSEC-04, DNSSEC-05, DNSSEC-06, DNSSEC-07]

must_haves:
  truths:
    - "RED test file exists with failing tests covering all 7 DNSSEC requirements"
    - "A stub dnssec_scanner.py exists so test imports resolve"
    - "Each test exercises the specific behavior described in its requirement"
  artifacts:
    - path: "tests/test_dnssec_scanner.py"
      provides: "RED test scaffold for DNSSEC-01 through DNSSEC-07"
      min_lines: 150
    - path: "quirk/scanner/dnssec_scanner.py"
      provides: "Stub module with DNSPYTHON_AVAILABLE guard and function signatures"
      contains: "DNSPYTHON_AVAILABLE"
  key_links:
    - from: "tests/test_dnssec_scanner.py"
      to: "quirk/scanner/dnssec_scanner.py"
      via: "import scan_dnssec_targets"
      pattern: "from quirk\\.scanner\\.dnssec_scanner import"
---

<objective>
Create TDD RED test scaffold for the DNSSEC scanner — failing tests covering all 7 requirements (DNSSEC-01 through DNSSEC-07) plus a minimal stub module so imports resolve.

Purpose: Tests define the contract before implementation. Plan 02 turns these tests GREEN.
Output: `tests/test_dnssec_scanner.py` (RED), `quirk/scanner/dnssec_scanner.py` (stub)
</objective>

<execution_context>
@/Users/digs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/digs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-dnssec-scanner/18-CONTEXT.md
@.planning/phases/18-dnssec-scanner/18-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From quirk/models.py (CryptoEndpoint fields used by DNSSEC):
- host: str
- port: int (always 53 for DNSSEC)
- protocol: str ("DNSSEC")
- cert_pubkey_alg: str (algorithm name e.g. "RSASHA1", "NONE", "NSEC", "DS-MISMATCH")
- cert_pubkey_size: int | None (RSA key size or None)
- dnssec_scan_json: str | None (JSON blob)
- service_detail: str (e.g. "dnskey:tag=12345:role=KSK", "unsigned-zone", "nsec-exposure", "ds-chain-broken")
- scanned_at: datetime

From quirk/scanner/jwt_scanner.py (pattern to follow):
```python
def scan_jwt_targets(targets: list, timeout: int = 30, logger=None) -> List[CryptoEndpoint]:
```

Target function signature (per D-15):
```python
def scan_dnssec_targets(targets: list, timeout: int = 10, logger=None) -> list[CryptoEndpoint]:
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stub dnssec_scanner.py with import guard and function signatures</name>
  <files>quirk/scanner/dnssec_scanner.py</files>
  <read_first>
    - quirk/scanner/jwt_scanner.py (import guard pattern to replicate)
    - quirk/models.py (CryptoEndpoint import path)
  </read_first>
  <action>
Create `quirk/scanner/dnssec_scanner.py` with:

1. Module docstring: `"""DNSSEC scanner module — queries DNSKEY/DS records and classifies algorithms per RFC 8624/9905."""`

2. Import guard (per Pitfall 2 from RESEARCH.md):
```python
try:
    import dns.message
    import dns.query
    import dns.rdatatype
    import dns.resolver
    import dns.dnssec
    import dns.flags
    DNSPYTHON_AVAILABLE = True
except ImportError:
    DNSPYTHON_AVAILABLE = False
```

3. `DNSSEC_ALG_MAP` constant dict (per D-04):
```python
DNSSEC_ALG_MAP: dict[int, tuple[str, str]] = {
    1:  ("RSAMD5",              "CRITICAL"),
    3:  ("DSA",                 "CRITICAL"),
    5:  ("RSASHA1",             "CRITICAL"),
    6:  ("DSA-NSEC3-SHA1",      "CRITICAL"),
    7:  ("RSASHA1-NSEC3-SHA1",  "CRITICAL"),
    8:  ("RSASHA256",           "HIGH"),
    10: ("RSASHA512",           "HIGH"),
    12: ("ECC-GOST",            "CRITICAL"),
    13: ("ECDSAP256SHA256",     "SAFE"),
    14: ("ECDSAP384SHA384",     "SAFE"),
    15: ("ED25519",             "SAFE"),
    16: ("ED448",               "SAFE"),
}
```

4. Stub functions that raise `NotImplementedError`:
- `def scan_dnssec_targets(targets: list, timeout: int = 10, logger=None) -> list:` — returns `[]` if not `DNSPYTHON_AVAILABLE`, else raises `NotImplementedError("Plan 02 implements")`
- `def _resolve_ns(domain: str, timeout: int) -> list[str]:` — raises `NotImplementedError`
- `def _query_rrset(domain: str, rdtype, ns_ip: str, timeout: int):` — raises `NotImplementedError`
- `def _parse_dnskeys(dnskey_rrset) -> list[dict]:` — raises `NotImplementedError`
- `def _parse_ds_records(ds_rrset) -> list[dict]:` — raises `NotImplementedError`
- `def _check_chain(dnskeys: list[dict], ds_records: list[dict]) -> bool:` — raises `NotImplementedError`
- `def _detect_nsec_type(domain: str, ns_ip: str, timeout: int) -> str | None:` — raises `NotImplementedError`
- `def _scan_domain(domain: str, timeout: int, logger) -> list:` — raises `NotImplementedError`

5. Import `from quirk.models import CryptoEndpoint` at top (below guard, inside try block or unconditionally since models is always available).
  </action>
  <verify>
    <automated>python -c "from quirk.scanner.dnssec_scanner import scan_dnssec_targets, DNSPYTHON_AVAILABLE, DNSSEC_ALG_MAP; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - quirk/scanner/dnssec_scanner.py contains `DNSPYTHON_AVAILABLE`
    - quirk/scanner/dnssec_scanner.py contains `DNSSEC_ALG_MAP`
    - quirk/scanner/dnssec_scanner.py contains `def scan_dnssec_targets(`
    - quirk/scanner/dnssec_scanner.py contains `def _resolve_ns(`
    - quirk/scanner/dnssec_scanner.py contains `def _parse_dnskeys(`
    - quirk/scanner/dnssec_scanner.py contains `def _check_chain(`
    - quirk/scanner/dnssec_scanner.py contains `def _detect_nsec_type(`
    - quirk/scanner/dnssec_scanner.py contains `NotImplementedError`
    - Python import succeeds: `from quirk.scanner.dnssec_scanner import scan_dnssec_targets`
  </acceptance_criteria>
  <done>Stub module importable; all function signatures present; DNSSEC_ALG_MAP constant defined</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RED test scaffold for DNSSEC-01 through DNSSEC-07</name>
  <files>tests/test_dnssec_scanner.py</files>
  <read_first>
    - tests/test_jwt_scanner.py (test pattern: mock-based, CryptoEndpoint assertion style)
    - quirk/scanner/dnssec_scanner.py (the stub just created — verify imports work)
    - .planning/phases/18-dnssec-scanner/18-CONTEXT.md (D-01 through D-18 decisions)
    - .planning/phases/18-dnssec-scanner/18-RESEARCH.md (mocking strategy, patterns 1-6)
  </read_first>
  <behavior>
    DNSSEC-01 tests:
    - test_dnskey_query_do_bit: Mock dns.query.udp_with_fallback, call _scan_domain("weak.chaos.local"), verify mock was called with want_dnssec=True and RD flag cleared
    - test_authoritative_ns_resolution: Mock dns.resolver.resolve for NS and A records, call _resolve_ns("example.com"), verify NS IPs returned

    DNSSEC-02 tests:
    - test_algorithm_classification_critical: For alg 5 (RSASHA1) and alg 7 (RSASHA1-NSEC3-SHA1), verify DNSSEC_ALG_MAP returns ("RSASHA1", "CRITICAL") and ("RSASHA1-NSEC3-SHA1", "CRITICAL")
    - test_algorithm_classification_high: For alg 8 (RSASHA256), verify DNSSEC_ALG_MAP returns ("RSASHA256", "HIGH")
    - test_algorithm_classification_safe: For alg 13 (ECDSAP256SHA256), verify DNSSEC_ALG_MAP returns ("ECDSAP256SHA256", "SAFE")

    DNSSEC-03 tests:
    - test_unsigned_zone: Mock DNS response with no DNSKEY rrset, verify scan_dnssec_targets returns CryptoEndpoint with cert_pubkey_alg="NONE", service_detail="unsigned-zone"

    DNSSEC-04 tests:
    - test_cryptoendpoint_protocol: Verify all returned CryptoEndpoints have protocol="DNSSEC"
    - test_dnssec_scan_json_populated: Verify dnssec_scan_json is non-None JSON string containing "domain" key

    DNSSEC-05 tests:
    - test_nsec_detection: Mock NXDOMAIN response with NSEC rrset in authority, verify CryptoEndpoint with service_detail="nsec-exposure" is returned
    - test_nsec3_no_exposure: Mock NXDOMAIN response with NSEC3 rrset in authority, verify NO nsec-exposure CryptoEndpoint

    DNSSEC-06 tests:
    - test_ds_chain_broken: Mock DNSKEY with tag=12345 and DS with key_tag=99999, verify CryptoEndpoint with service_detail="ds-chain-broken"
    - test_ds_chain_valid: Mock matching key tags, verify NO ds-chain-broken CryptoEndpoint

    DNSSEC-07 tests:
    - test_chaos_lab_integration: pytest.mark.integration, skip unless QUIRK_INTEGRATION_TESTS env var, test scan_dnssec_targets against localhost:15353 for all 4 chaos lab zones
  </behavior>
  <action>
Create `tests/test_dnssec_scanner.py` with:

1. Module docstring: `"""Tests for DNSSEC scanner (DNSSEC-01 through DNSSEC-07). Tests mock dns.query/dns.resolver to avoid network calls. Scanner module: quirk/scanner/dnssec_scanner.py"""`

2. Imports:
```python
import json
import os
import pytest
from unittest.mock import patch, MagicMock, PropertyMock
from quirk.scanner.dnssec_scanner import (
    scan_dnssec_targets,
    DNSSEC_ALG_MAP,
    DNSPYTHON_AVAILABLE,
)
```

3. Helper to build a mock DNSKEY rdata object:
```python
def _mock_dnskey(algorithm: int, flags: int = 257, key_bytes: bytes = b'\x03\x01\x00\x01' + b'\x00' * 256):
    """Create a mock DNSKEY rdata with given algorithm, flags, key material."""
    rdata = MagicMock()
    rdata.algorithm = algorithm
    rdata.flags = flags
    rdata.key = key_bytes
    rdata.protocol = 3
    return rdata
```

4. Helper to build a mock DS rdata:
```python
def _mock_ds(key_tag: int, algorithm: int = 13, digest_type: int = 2):
    rdata = MagicMock()
    rdata.key_tag = key_tag
    rdata.algorithm = algorithm
    rdata.digest_type = digest_type
    return rdata
```

5. Tests (all should FAIL against the stub — they call functions that raise NotImplementedError):

**DNSSEC-01:**
- `test_dnskey_query_do_bit()`: Patch `quirk.scanner.dnssec_scanner.dns.query.udp_with_fallback` and `quirk.scanner.dnssec_scanner.dns.resolver.resolve`. Set up mock NS resolution returning `["10.0.0.1"]`. Set up mock DNSKEY response with one ECDSAP256SHA256 (alg 13, flags 257) DNSKEY. Call `scan_dnssec_targets(["example.com"])`. Assert `udp_with_fallback` was called. Get the `request` arg from the call. Assert `request` was created with `want_dnssec=True` (the DO bit). This test will fail because `scan_dnssec_targets` raises NotImplementedError.

- `test_authoritative_ns_resolution()`: Patch `quirk.scanner.dnssec_scanner.dns.resolver.resolve` to return mock NS records then A records. Import and call `_resolve_ns("example.com", timeout=5)`. Assert result is a list of IP strings. Will fail (NotImplementedError).

**DNSSEC-02:**
- `test_algorithm_classification_critical()`: Assert `DNSSEC_ALG_MAP[5] == ("RSASHA1", "CRITICAL")` and `DNSSEC_ALG_MAP[7] == ("RSASHA1-NSEC3-SHA1", "CRITICAL")`. This test PASSES against stub (ALG_MAP is defined). Include it for completeness.

- `test_algorithm_classification_high()`: Assert `DNSSEC_ALG_MAP[8] == ("RSASHA256", "HIGH")`.

- `test_algorithm_classification_safe()`: Assert `DNSSEC_ALG_MAP[13] == ("ECDSAP256SHA256", "SAFE")`.

- `test_rsasha1_produces_critical_finding()`: Mock full scan returning a DNSKEY with alg=5. Call `scan_dnssec_targets(["weak.example.com"])`. Assert returned CryptoEndpoint has `cert_pubkey_alg == "RSASHA1"`. Will fail (NotImplementedError).

**DNSSEC-03:**
- `test_unsigned_zone()`: Mock DNS response where DNSKEY query returns empty answer (no DNSKEY rrset, or NXDOMAIN rcode). Call `scan_dnssec_targets(["unsigned.example.com"])`. Assert exactly one CryptoEndpoint returned with `cert_pubkey_alg == "NONE"` and `service_detail == "unsigned-zone"` and `protocol == "DNSSEC"`. Will fail (NotImplementedError).

**DNSSEC-04:**
- `test_cryptoendpoint_protocol_dnssec()`: Mock scan of a signed domain with one DNSKEY (alg 13). Call `scan_dnssec_targets(["signed.example.com"])`. Assert all returned endpoints have `protocol == "DNSSEC"` and `port == 53`. Will fail (NotImplementedError).

- `test_dnssec_scan_json_populated()`: Same mock, assert `ep.dnssec_scan_json is not None` and `json.loads(ep.dnssec_scan_json)` contains key `"domain"` with value `"signed.example.com"`. Will fail (NotImplementedError).

**DNSSEC-05:**
- `test_nsec_detection_exposure()`: Mock NXDOMAIN response authority section containing an NSEC rrset (rdtype=47). Call scan. Assert a CryptoEndpoint with `service_detail == "nsec-exposure"` and `cert_pubkey_alg == "NSEC"`. Will fail (NotImplementedError).

- `test_nsec3_no_exposure()`: Mock NXDOMAIN response authority section containing NSEC3 rrset (rdtype=50) but no NSEC. Call scan. Assert NO CryptoEndpoint with `service_detail == "nsec-exposure"`. Will fail (NotImplementedError).

**DNSSEC-06:**
- `test_ds_chain_broken()`: Mock DNSKEY response with one key (tag computed to 12345 via mock of dns.dnssec.key_id). Mock DS response with key_tag=99999. Call scan. Assert CryptoEndpoint with `service_detail == "ds-chain-broken"`. Will fail (NotImplementedError).

- `test_ds_chain_valid()`: Mock DNSKEY with tag 12345 and DS with key_tag 12345. Call scan. Assert NO `ds-chain-broken` endpoint. Will fail (NotImplementedError).

**DNSSEC-07:**
- `test_chaos_lab_integration()`: Mark with `@pytest.mark.integration` and `@pytest.mark.skipif(not os.environ.get("QUIRK_INTEGRATION_TESTS"), reason="Set QUIRK_INTEGRATION_TESTS=1")`. Call `scan_dnssec_targets(["weak.chaos.local", "safe.chaos.local", "broken.chaos.local", "unsigned.chaos.local"])` (no mocks). Assert:
  - At least one CryptoEndpoint with `cert_pubkey_alg == "RSASHA1"` (from weak zone)
  - At least one with `cert_pubkey_alg == "ECDSAP256SHA256"` (from safe zone)
  - At least one with `service_detail == "ds-chain-broken"` (from broken zone)
  - At least one with `service_detail == "unsigned-zone"` (from unsigned zone)

6. For the mocked tests, the common mock setup pattern is:
- Patch `quirk.scanner.dnssec_scanner.dns.resolver.resolve` — return mock NS then A answers
- Patch `quirk.scanner.dnssec_scanner.dns.query.udp_with_fallback` — return mock DNS response
- Patch `quirk.scanner.dnssec_scanner.dns.dnssec.key_id` — return a specific key_tag int
- Build mock response objects where `response.answer` is a list of mock rrsets, each with `.rdtype` and iterable rdata
  </action>
  <verify>
    <automated>pytest tests/test_dnssec_scanner.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_dnssec_scanner.py contains `test_dnskey_query_do_bit`
    - tests/test_dnssec_scanner.py contains `test_algorithm_classification_critical`
    - tests/test_dnssec_scanner.py contains `test_unsigned_zone`
    - tests/test_dnssec_scanner.py contains `test_cryptoendpoint_protocol_dnssec`
    - tests/test_dnssec_scanner.py contains `test_nsec_detection_exposure`
    - tests/test_dnssec_scanner.py contains `test_ds_chain_broken`
    - tests/test_dnssec_scanner.py contains `test_chaos_lab_integration`
    - tests/test_dnssec_scanner.py contains `from quirk.scanner.dnssec_scanner import`
    - tests/test_dnssec_scanner.py contains `@pytest.mark.integration`
    - At least 3 DNSSEC_ALG_MAP assertion tests PASS (map is already defined in stub)
    - Remaining tests FAIL with NotImplementedError or AssertionError (RED state)
    - `pytest tests/test_dnssec_scanner.py --co -q` lists at least 14 tests collected
  </acceptance_criteria>
  <done>RED scaffold committed: 14+ tests, 3+ passing (ALG_MAP tests), remainder failing against stub. All 7 DNSSEC requirements have at least one test.</done>
</task>

</tasks>

<verification>
- `python -c "from quirk.scanner.dnssec_scanner import scan_dnssec_targets, DNSPYTHON_AVAILABLE, DNSSEC_ALG_MAP"` exits 0
- `pytest tests/test_dnssec_scanner.py --co -q` shows at least 14 tests collected
- `pytest tests/test_dnssec_scanner.py -x -q -k "algorithm_classification"` passes (ALG_MAP tests)
- `pytest tests/test_dnssec_scanner.py -q` shows mix of PASSED and FAILED (RED state)
- `pytest tests/ -q --ignore=tests/test_dnssec_scanner.py` still passes (no regressions)
</verification>

<success_criteria>
- Stub module importable with DNSPYTHON_AVAILABLE guard and DNSSEC_ALG_MAP constant
- 14+ tests covering all 7 DNSSEC requirements (at least 2 per req)
- ALG_MAP tests pass; functional tests fail against stub (RED state)
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/18-dnssec-scanner/18-01-SUMMARY.md`
</output>
