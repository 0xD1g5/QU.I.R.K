---
phase: 07-polish-and-packaging
plan: 03
type: execute
wave: 1
depends_on:
  - 07-01
files_modified:
  - quirk/reports/html_renderer.py
  - quirk/reports/templates/report.html.j2
  - quirk/reports/writer.py
autonomous: true
requirements:
  - BRAND-03

must_haves:
  truths:
    - "Running a scan produces a report-{stamp}.html alongside the existing Markdown reports"
    - "The HTML file is self-contained: no CDN links, no external font references, no external script src — opens offline"
    - "The HTML report contains the QU.I.R.K. wordmark in the header with electric-blue (#3b9dff) styling"
    - "The HTML report contains both an Executive Summary section and a Technical Appendix section"
    - "Score band color coding: EXCELLENT/GOOD = green, MODERATE = amber, FAIR/POOR = red"
    - "PDF report generated by Playwright rendering the HTML file (not the dashboard URL)"
    - "If Playwright is unavailable the HTML report is still written without error"
  artifacts:
    - path: "quirk/reports/html_renderer.py"
      provides: "render_html_report(path, cfg, endpoints, findings, score, conf, roadmap_items) function"
      exports: ["render_html_report"]
    - path: "quirk/reports/templates/report.html.j2"
      provides: "Jinja2 template — self-contained HTML with embedded CSS"
    - path: "quirk/reports/writer.py"
      provides: "HTML and PDF generation steps inserted after Markdown writes"
  key_links:
    - from: "quirk/reports/writer.py"
      to: "quirk/reports/html_renderer.py"
      via: "from quirk.reports.html_renderer import render_html_report, render_pdf_report"
    - from: "quirk/reports/html_renderer.py"
      to: "quirk/reports/templates/report.html.j2"
      via: "PackageLoader('quirk', 'reports/templates') in Jinja2 Environment"
---

<objective>
Build the standalone HTML report renderer (Jinja2) and wire PDF generation into write_reports().

Purpose: A scan run now produces a professional single-file HTML report and PDF — the primary consulting deliverable (D-08 through D-12). Satisfies BRAND-03.
Output: quirk/reports/html_renderer.py, quirk/reports/templates/report.html.j2, writer.py updated.
</objective>

<execution_context>
@/Users/digs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/digs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-polish-and-packaging/07-CONTEXT.md
@.planning/phases/07-polish-and-packaging/07-01-SUMMARY.md
</context>

<interfaces>
<!-- Key signatures from writer.py that html_renderer.py must match -->

From quirk/reports/writer.py — write_reports() call site context:
```python
def write_reports(cfg, endpoints, findings, run_stats=None):
    # ...
    score = {"total": score_raw["score"], "subscores": ..., "drivers": [...]}
    conf = {"confidence": conf_raw["confidence_score"], "confidence_factors": {...}}
    roadmap_items = roadmap_raw.get("items", [])  # list of dicts with timeframe, title, why, dependencies keys
    # HTML renderer inserts AFTER roadmap_path write (step 3), BEFORE run_stats write (step 4)
```

From quirk/config — cfg shape (relevant fields):
```python
cfg.assessment.name          # str: organization name
cfg.assessment.report_owner  # str
cfg.assessment.data_classification  # str
cfg.assessment.timezone      # str
cfg.output.directory         # str: output path
```

From quirk/models — CryptoEndpoint relevant columns:
```python
ep.host          # str
ep.port          # int
ep.protocol      # str: tls/ssh/jwt/aws/azure/container/source
ep.cipher_suite  # str or None
ep.tls_version   # str or None
ep.cert_pubkey_alg  # str or None
ep.cert_subject     # str or None
ep.cert_not_after   # datetime or None
```

Findings dict shape:
```python
{
    "title": str,
    "severity": str,  # CRITICAL/HIGH/MEDIUM/LOW/INFO
    "description": str,
    "recommendation": str,
    "host": str,
    "port": int,
}
```

Score band thresholds (from scoring.py _rating()):
EXCELLENT >= 85, GOOD >= 70, MODERATE >= 55, FAIR >= 35, POOR < 35
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create quirk/reports/html_renderer.py and report.html.j2 template</name>
  <files>quirk/reports/html_renderer.py, quirk/reports/templates/report.html.j2</files>

  <read_first>
    - quirk/reports/writer.py — write_reports() full function: understand score/conf/roadmap_items shape already computed, and the stamp variable for filename
    - quirk/reports/executive.py — build_exec_markdown() signature to understand what data is already available vs what html_renderer gets directly
    - quirk/reports/technical.py — build_tech_markdown() signature
    - tests/test_html_report.py — four test stubs: test_report_contains_wordmark, test_html_is_self_contained, test_html_report_sections, test_pdf_graceful_degradation
    - .planning/phases/07-polish-and-packaging/07-RESEARCH.md — Pattern 2 (Jinja2 renderer code) and the CSS color values (--bg: #0a0a0f, --accent: #3b9dff)
  </read_first>

  <behavior>
    - test_html_report.py::test_report_contains_wordmark — render_html_report() call produces HTML containing "QU.I.R.K."
    - test_html_report.py::test_html_is_self_contained — no external http(s):// in link/script elements
    - test_html_report.py::test_html_report_sections — HTML contains "Executive Summary" and "Technical Appendix"
  </behavior>

  <action>
    1. Create directory `quirk/reports/templates/` and add an empty `__init__.py` to `quirk/reports/` if it does not already exist (it likely does).

    2. Create `quirk/reports/html_renderer.py`:

       ```python
       """Jinja2-based standalone HTML report renderer for QU.I.R.K. (Phase 7, D-08 to D-12)."""
       import os
       from datetime import datetime, timezone
       from typing import Any, Dict, List, Optional

       from jinja2 import Environment, PackageLoader, select_autoescape


       def _score_band(total: int) -> str:
           if total >= 85:
               return "EXCELLENT"
           if total >= 70:
               return "GOOD"
           if total >= 55:
               return "MODERATE"
           if total >= 35:
               return "FAIR"
           return "POOR"


       def _score_color(band: str) -> str:
           return {
               "EXCELLENT": "#4caf50",
               "GOOD": "#66bb6a",
               "MODERATE": "#f9a825",
               "FAIR": "#f57c00",
               "POOR": "#e53935",
           }.get(band, "#aaaaaa")


       def _severity_color(severity: str) -> str:
           return {
               "CRITICAL": "#e53935",
               "HIGH": "#f57c00",
               "MEDIUM": "#f9a825",
               "LOW": "#5c9cff",
               "INFO": "#888888",
           }.get(str(severity).upper(), "#888888")


       def render_html_report(
           path: str,
           cfg: Any,
           endpoints: List[Any],
           findings: List[Dict[str, Any]],
           score: Dict[str, Any],
           conf: Dict[str, Any],
           roadmap_items: List[Dict[str, Any]],
       ) -> None:
           """Render a self-contained HTML report to *path*.

           All CSS is inlined. No CDN references. Works offline (D-08).
           """
           env = Environment(
               loader=PackageLoader("quirk", "reports/templates"),
               autoescape=select_autoescape(["html", "j2"]),
           )
           template = env.get_template("report.html.j2")

           total_score = score.get("total", 0)
           band = _score_band(total_score)

           # Severity counts
           sev_counts: Dict[str, int] = {}
           for f in (findings or []):
               s = str(f.get("severity", "INFO")).upper()
               sev_counts[s] = sev_counts.get(s, 0) + 1

           # Roadmap sections
           def roadmap_section(tf: str) -> List[Dict]:
               return [r for r in (roadmap_items or []) if r.get("timeframe") == tf or r.get("phase") == tf]

           html = template.render(
               org_name=getattr(getattr(cfg, "assessment", None), "name", "Unknown"),
               report_owner=getattr(getattr(cfg, "assessment", None), "report_owner", ""),
               data_classification=getattr(getattr(cfg, "assessment", None), "data_classification", "CONFIDENTIAL"),
               generated_at=datetime.now(timezone.utc).strftime("%Y-%m-%d %H:%M UTC"),
               total_score=total_score,
               score_band=band,
               score_color=_score_color(band),
               confidence=conf.get("confidence", 0),
               sev_counts=sev_counts,
               drivers=score.get("drivers", []),
               findings=findings or [],
               endpoints=endpoints or [],
               roadmap_now=roadmap_section("NOW"),
               roadmap_next=roadmap_section("NEXT"),
               roadmap_later=roadmap_section("LATER"),
               severity_color=_severity_color,
           )
           os.makedirs(os.path.dirname(path) if os.path.dirname(path) else ".", exist_ok=True)
           with open(path, "w", encoding="utf-8") as f:
               f.write(html)


       def render_pdf_report(html_path: str, pdf_path: str) -> bool:
           """Render html_path to pdf_path using Playwright headless Chromium.

           Returns True on success, False if Playwright is unavailable (graceful degradation, D-11).
           """
           try:
               from playwright.sync_api import sync_playwright
           except ImportError:
               return False
           try:
               with sync_playwright() as p:
                   browser = p.chromium.launch()
                   page = browser.new_page()
                   page.goto(f"file://{os.path.abspath(html_path)}")
                   page.pdf(
                       path=pdf_path,
                       format="A4",
                       margin={"top": "15mm", "bottom": "15mm", "left": "12mm", "right": "12mm"},
                       print_background=True,
                   )
                   browser.close()
               return True
           except Exception:
               return False
       ```

    3. Create `quirk/reports/templates/report.html.j2` — a complete, self-contained Jinja2 template. The template must:
       - Have all CSS embedded in a `<style>` block — NO `<link rel="stylesheet" href="...">`, NO CDN URLs in link/script
       - Use CSS variables: `--bg: #0a0a0f`, `--surface: #12121a`, `--accent: #3b9dff`, `--critical: #e53935`, `--high: #f57c00`, `--medium: #f9a825`, `--low: #5c9cff`
       - Render the `QU.I.R.K.` wordmark in the report header in electric-blue
       - Contain an `<section id="executive-summary">` with heading "Executive Summary"
       - Contain a `<section id="technical-appendix">` with heading "Technical Appendix"
       - Score gauge: a `<div>` showing the numeric score, band label, and colored indicator
       - Findings table: iterate `findings` list, each row showing severity (colored), title, host, description
       - Endpoints table in Technical Appendix: iterate `endpoints`, show host, port, protocol, cipher_suite, tls_version
       - Roadmap section: three subsections NOW / NEXT / LATER iterating `roadmap_now`, `roadmap_next`, `roadmap_later`
       - Page title: `QU.I.R.K. — {{ org_name }} Quantum Readiness Report`

       Minimal structure (expand with full CSS and content):

       ```html
       <!DOCTYPE html>
       <html lang="en">
       <head>
         <meta charset="UTF-8">
         <meta name="viewport" content="width=device-width, initial-scale=1.0">
         <title>QU.I.R.K. — {{ org_name }} Quantum Readiness Report</title>
         <style>
           /* === QU.I.R.K. Report Stylesheet — all CSS embedded, no external refs === */
           :root {
             --bg: #0a0a0f;
             --surface: #12121a;
             --surface2: #1a1a26;
             --border: #2a2a3a;
             --accent: #3b9dff;
             --text: #e8e8f0;
             --text-muted: #8888aa;
             --critical: #e53935;
             --high: #f57c00;
             --medium: #f9a825;
             --low: #5c9cff;
             --info: #666688;
             --green: #4caf50;
             --amber: #f9a825;
             --red: #e53935;
           }
           *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
           body {
             background: var(--bg);
             color: var(--text);
             font-family: 'Courier New', Courier, monospace;
             font-size: 14px;
             line-height: 1.6;
             padding: 0;
           }
           a { color: var(--accent); text-decoration: none; }
           .report-header {
             background: var(--surface);
             border-bottom: 2px solid var(--accent);
             padding: 24px 40px;
             display: flex;
             align-items: baseline;
             gap: 20px;
           }
           .wordmark {
             font-size: 28px;
             font-weight: 900;
             letter-spacing: 0.12em;
             color: var(--accent);
             font-family: 'Courier New', Courier, monospace;
           }
           .report-meta {
             color: var(--text-muted);
             font-size: 12px;
           }
           .report-body { max-width: 1100px; margin: 0 auto; padding: 40px; }
           section { margin-bottom: 56px; }
           h1 { font-size: 22px; color: var(--accent); border-bottom: 1px solid var(--border); padding-bottom: 8px; margin-bottom: 24px; }
           h2 { font-size: 17px; color: var(--text); margin: 24px 0 12px; }
           h3 { font-size: 14px; color: var(--text-muted); margin: 16px 0 8px; }
           .score-card {
             display: inline-flex;
             flex-direction: column;
             align-items: center;
             background: var(--surface);
             border: 2px solid {{ score_color }};
             border-radius: 8px;
             padding: 24px 40px;
             margin-bottom: 24px;
           }
           .score-value { font-size: 56px; font-weight: 900; color: {{ score_color }}; line-height: 1; }
           .score-band { font-size: 16px; color: {{ score_color }}; margin-top: 4px; letter-spacing: 0.1em; }
           .score-label { font-size: 11px; color: var(--text-muted); margin-top: 4px; }
           .sev-grid { display: flex; gap: 16px; flex-wrap: wrap; margin: 16px 0; }
           .sev-badge {
             padding: 8px 16px;
             border-radius: 4px;
             font-size: 13px;
             font-weight: bold;
             min-width: 80px;
             text-align: center;
           }
           .sev-CRITICAL { background: var(--critical); color: #fff; }
           .sev-HIGH { background: var(--high); color: #fff; }
           .sev-MEDIUM { background: var(--medium); color: #111; }
           .sev-LOW { background: var(--low); color: #fff; }
           .sev-INFO { background: var(--info); color: #ddd; }
           table { width: 100%; border-collapse: collapse; margin: 16px 0; font-size: 13px; }
           th {
             background: var(--surface2);
             color: var(--accent);
             text-align: left;
             padding: 8px 12px;
             font-weight: 600;
             border-bottom: 1px solid var(--border);
           }
           td { padding: 8px 12px; border-bottom: 1px solid var(--border); vertical-align: top; }
           tr:last-child td { border-bottom: none; }
           tr:hover td { background: var(--surface2); }
           .sev-cell { font-weight: bold; font-size: 11px; padding: 2px 6px; border-radius: 3px; }
           .roadmap-phase { margin: 16px 0; }
           .roadmap-phase h3 { color: var(--accent); text-transform: uppercase; font-size: 13px; letter-spacing: 0.1em; }
           .roadmap-item {
             background: var(--surface);
             border-left: 3px solid var(--accent);
             padding: 10px 16px;
             margin: 8px 0;
             border-radius: 0 4px 4px 0;
           }
           .roadmap-item strong { color: var(--text); }
           .roadmap-item span { color: var(--text-muted); font-size: 12px; }
           .drivers-list { list-style: none; }
           .drivers-list li::before { content: "▸ "; color: var(--accent); }
           .drivers-list li { padding: 3px 0; color: var(--text-muted); font-size: 13px; }
           .meta-table td:first-child { color: var(--text-muted); width: 180px; }
           .meta-table td:last-child { color: var(--text); }
           .footer {
             background: var(--surface);
             border-top: 1px solid var(--border);
             padding: 16px 40px;
             text-align: center;
             color: var(--text-muted);
             font-size: 11px;
           }
         </style>
       </head>
       <body>

       <header class="report-header">
         <span class="wordmark">QU.I.R.K.</span>
         <span class="report-meta">Quantum Readiness Report &nbsp;|&nbsp; {{ org_name }} &nbsp;|&nbsp; {{ generated_at }}</span>
       </header>

       <div class="report-body">

         <!-- ===== EXECUTIVE SUMMARY ===== -->
         <section id="executive-summary">
           <h1>Executive Summary</h1>

           <table class="meta-table" style="width:auto;margin-bottom:24px;">
             <tr><td>Organization</td><td>{{ org_name }}</td></tr>
             <tr><td>Report owner</td><td>{{ report_owner }}</td></tr>
             <tr><td>Data classification</td><td>{{ data_classification }}</td></tr>
             <tr><td>Generated</td><td>{{ generated_at }}</td></tr>
           </table>

           <h2>Quantum Readiness Score</h2>
           <div class="score-card">
             <div class="score-value">{{ total_score }}</div>
             <div class="score-band">{{ score_band }}</div>
             <div class="score-label">/ 100 &nbsp;|&nbsp; Confidence: {{ confidence }}/100</div>
           </div>

           <h2>Findings Breakdown</h2>
           <div class="sev-grid">
             {% for sev in ['CRITICAL','HIGH','MEDIUM','LOW','INFO'] %}
               {% if sev_counts.get(sev, 0) > 0 %}
               <div class="sev-badge sev-{{ sev }}">{{ sev }}: {{ sev_counts.get(sev, 0) }}</div>
               {% endif %}
             {% endfor %}
             {% if not sev_counts %}
             <span style="color:var(--text-muted)">No findings</span>
             {% endif %}
           </div>

           {% if drivers %}
           <h2>Score Drivers</h2>
           <ul class="drivers-list">
             {% for d in drivers %}<li>{{ d }}</li>{% endfor %}
           </ul>
           {% endif %}

           {% if findings %}
           <h2>Top Findings</h2>
           <table>
             <thead><tr><th>Severity</th><th>Title</th><th>Host</th><th>Description</th></tr></thead>
             <tbody>
             {% for f in findings[:10] %}
             <tr>
               <td><span class="sev-cell sev-{{ f.get('severity','INFO')|upper }}">{{ f.get('severity','INFO')|upper }}</span></td>
               <td>{{ f.get('title','') }}</td>
               <td>{{ f.get('host','') }}{% if f.get('port') %}:{{ f.get('port') }}{% endif %}</td>
               <td>{{ f.get('description','')[:120] }}{% if f.get('description','')|length > 120 %}…{% endif %}</td>
             </tr>
             {% endfor %}
             </tbody>
           </table>
           {% endif %}

           <h2>Transition Roadmap</h2>
           {% for phase_label, items in [('NOW', roadmap_now), ('NEXT', roadmap_next), ('LATER', roadmap_later)] %}
           {% if items %}
           <div class="roadmap-phase">
             <h3>{{ phase_label }}</h3>
             {% for item in items %}
             <div class="roadmap-item">
               <strong>{{ item.get('title','') }}</strong><br>
               <span>{{ item.get('why','') }}</span>
             </div>
             {% endfor %}
           </div>
           {% endif %}
           {% endfor %}
         </section>

         <!-- ===== TECHNICAL APPENDIX ===== -->
         <section id="technical-appendix">
           <h1>Technical Appendix</h1>

           <h2>All Findings</h2>
           {% if findings %}
           <table>
             <thead><tr><th>Severity</th><th>Title</th><th>Host</th><th>Port</th><th>Recommendation</th></tr></thead>
             <tbody>
             {% for f in findings %}
             <tr>
               <td><span class="sev-cell sev-{{ f.get('severity','INFO')|upper }}">{{ f.get('severity','INFO')|upper }}</span></td>
               <td>{{ f.get('title','') }}</td>
               <td>{{ f.get('host','') }}</td>
               <td>{{ f.get('port','') }}</td>
               <td>{{ f.get('recommendation','')[:160] }}</td>
             </tr>
             {% endfor %}
             </tbody>
           </table>
           {% else %}
           <p style="color:var(--text-muted)">No findings recorded for this scan.</p>
           {% endif %}

           <h2>Endpoint Inventory</h2>
           {% if endpoints %}
           <table>
             <thead><tr><th>Host</th><th>Port</th><th>Protocol</th><th>TLS Version</th><th>Cipher Suite</th><th>Cert Key</th></tr></thead>
             <tbody>
             {% for ep in endpoints %}
             <tr>
               <td>{{ ep.host }}</td>
               <td>{{ ep.port }}</td>
               <td>{{ ep.protocol or '' }}</td>
               <td>{{ ep.tls_version or '' }}</td>
               <td>{{ ep.cipher_suite or '' }}</td>
               <td>{{ ep.cert_pubkey_alg or '' }}</td>
             </tr>
             {% endfor %}
             </tbody>
           </table>
           {% else %}
           <p style="color:var(--text-muted)">No endpoints recorded for this scan.</p>
           {% endif %}
         </section>

       </div><!-- /report-body -->

       <footer class="footer">
         Generated by <strong>QU.I.R.K.</strong> Quantum Infrastructure Readiness Kit &nbsp;|&nbsp; {{ generated_at }}
       </footer>

       </body>
       </html>
       ```
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_html_report.py::test_report_contains_wordmark tests/test_html_report.py::test_html_is_self_contained tests/test_html_report.py::test_html_report_sections -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `quirk/reports/html_renderer.py` exists and contains `def render_html_report(`
    - `quirk/reports/html_renderer.py` contains `def render_pdf_report(`
    - `quirk/reports/html_renderer.py` contains `from jinja2 import Environment, PackageLoader`
    - `quirk/reports/templates/report.html.j2` exists
    - `quirk/reports/templates/report.html.j2` contains `class="wordmark"` and the string `QU.I.R.K.`
    - `quirk/reports/templates/report.html.j2` contains `id="executive-summary"` and `id="technical-appendix"`
    - `quirk/reports/templates/report.html.j2` contains `--accent: #3b9dff`
    - `quirk/reports/templates/report.html.j2` does NOT contain any `http://` or `https://` URL in a `<link` or `<script` tag
    - `pytest tests/test_html_report.py::test_report_contains_wordmark -x -q` exits 0
    - `pytest tests/test_html_report.py::test_html_is_self_contained -x -q` exits 0
    - `pytest tests/test_html_report.py::test_html_report_sections -x -q` exits 0
  </acceptance_criteria>

  <done>render_html_report() produces a self-contained, QU.I.R.K.-branded HTML file. Three of four test_html_report.py stubs are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire HTML and PDF generation into write_reports()</name>
  <files>quirk/reports/writer.py</files>

  <read_first>
    - quirk/reports/writer.py — full file, identify the insertion point: after roadmap_path write (step 3) and before the run_stats block (step 4); the stamp variable is already in scope; html_path and pdf_path naming convention must follow `report-{stamp}.html` and `report-{stamp}.pdf`
    - quirk/reports/html_renderer.py — render_html_report() and render_pdf_report() signatures just created
    - tests/test_html_report.py::test_pdf_graceful_degradation — asserts writer reloads without error when playwright is None in sys.modules
    - tests/test_packaging.py::test_package_data_templates — asserts quirk/reports/templates/ directory exists
  </read_first>

  <behavior>
    - test_html_report.py::test_pdf_graceful_degradation — writer.py reloads cleanly when playwright is unavailable
    - test_packaging.py::test_package_data_templates — templates dir exists and report.html.j2 present
  </behavior>

  <action>
    Edit `quirk/reports/writer.py`:

    1. Add the import near the top (after existing imports, before PLATFORM_VERSION constant):
       ```python
       from quirk.reports.html_renderer import render_html_report, render_pdf_report
       ```

    2. In `write_reports()`, insert the HTML + PDF steps immediately after the `roadmap_path` write block (after `f.write(_roadmap_markdown(roadmap_items))`) and before the `# 4) Ensure reporting timing` comment:

       ```python
       # 3b) Standalone HTML report (D-08) + PDF via Playwright (D-11)
       html_path = os.path.join(outdir, f"report-{stamp}.html")
       render_html_report(
           path=html_path,
           cfg=cfg,
           endpoints=endpoints,
           findings=findings,
           score=score,
           conf=conf,
           roadmap_items=roadmap_items,
       )

       pdf_path = os.path.join(outdir, f"report-{stamp}.pdf")
       pdf_ok = render_pdf_report(html_path=html_path, pdf_path=pdf_path)
       if not pdf_ok:
           pdf_path = None  # Playwright unavailable — HTML report still written
       ```

    3. In the output files list at the end of write_reports() (the `for p in [...]` block), add `html_path` and `pdf_path` to the list:
       The new list in the rich summary task (from Plan 02) already collects output_files. If that list is already updated, ensure `html_path` and `pdf_path` are included alongside the other paths.

    4. Also add `quirk/reports/templates` to setuptools package data in `pyproject.toml` so the template is included when the package is installed. Add this section to pyproject.toml:
       ```toml
       [tool.setuptools.package-data]
       quirk = ["reports/templates/*.j2"]
       ```
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_html_report.py tests/test_packaging.py::test_package_data_templates -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `quirk/reports/writer.py` contains `from quirk.reports.html_renderer import render_html_report, render_pdf_report`
    - `quirk/reports/writer.py` contains `render_html_report(` inside `write_reports()`
    - `quirk/reports/writer.py` contains `render_pdf_report(`
    - `quirk/reports/writer.py` contains `html_path = os.path.join(outdir, f"report-{stamp}.html")`
    - `pyproject.toml` contains `[tool.setuptools.package-data]`
    - `pyproject.toml` contains `"reports/templates/*.j2"`
    - `pytest tests/test_html_report.py -x -q` exits 0 (all four stubs GREEN)
    - `pytest tests/test_packaging.py::test_package_data_templates -x -q` exits 0
  </acceptance_criteria>

  <done>write_reports() produces report-{stamp}.html and report-{stamp}.pdf (when Playwright available). All test_html_report.py stubs GREEN. Template packaging declared in pyproject.toml.</done>
</task>

</tasks>

<verification>
`.venv/bin/python -m pytest tests/test_html_report.py tests/test_packaging.py::test_package_data_templates -q`

All five tests pass. Full suite: `.venv/bin/python -m pytest tests/ -q` — no regressions.
</verification>

<success_criteria>
- quirk/reports/html_renderer.py and templates/report.html.j2 exist
- HTML report is self-contained (no CDN), contains wordmark, executive summary, and technical appendix
- write_reports() wired to produce HTML + PDF alongside Markdown reports
- pyproject.toml has package-data entry for j2 templates
- All test_html_report.py tests GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-and-packaging/07-03-SUMMARY.md`
</output>
