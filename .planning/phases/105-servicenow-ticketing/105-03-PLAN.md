---
phase: 105-servicenow-ticketing
plan: 03
type: execute
wave: 3
depends_on: ["105-01", "105-02"]
files_modified:
  - docs/configuration.md
  - docs/sample-config.yaml
  - docs/UAT-SERIES.md
autonomous: true
requirements: [TICKET-02]
must_haves:
  truths:
    - "docs/configuration.md documents the ServiceNow ticketing backend: [ticketing].servicenow block, --backend servicenow, env-var-name credential model, https-only"
    - "docs/sample-config.yaml has a ticketing.servicenow example block"
    - "docs/UAT-SERIES.md has a Series 105 section covering ServiceNow ticketing and is synced to the Obsidian vault and committed"
    - "An Obsidian Phase-105 note exists with status complete"
  artifacts:
    - path: "docs/configuration.md"
      provides: "ServiceNow Ticketing documentation section"
      contains: "servicenow"
    - path: "docs/sample-config.yaml"
      provides: "ticketing.servicenow example block"
      contains: "servicenow"
    - path: "docs/UAT-SERIES.md"
      provides: "Series 105 ServiceNow ticketing UAT cases + updated header date"
      contains: "Series 105"
  key_links:
    - from: "docs/UAT-SERIES.md"
      to: "Obsidian vault UAT-Series.md"
      via: "printf-prepend sync + gsd-tools commit"
      pattern: "Series 105"
---

<objective>
Document the ServiceNow ticketing backend and close out milestone v5.3's final phase: configuration docs, sample config, UAT-SERIES Series 105, Obsidian phase note, and the mandatory UAT-SERIES sync + commit per CLAUDE.md.

Purpose: TICKET-02 is not shippable until a user knows how to configure and run it; v5.3 milestone close requires the standing CLAUDE.md bookkeeping steps.
Output: docs/configuration.md + docs/sample-config.yaml (extended), docs/UAT-SERIES.md (Series 105 + synced + committed), Obsidian Phase-105 note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/105-servicenow-ticketing/105-RESEARCH.md
@.planning/phases/105-servicenow-ticketing/105-01-SUMMARY.md
@.planning/phases/105-servicenow-ticketing/105-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document ServiceNow ticketing in configuration.md + sample-config.yaml</name>
  <files>docs/configuration.md, docs/sample-config.yaml</files>
  <read_first>
    - docs/configuration.md — the existing "Jira Ticketing" section (the analog to mirror for structure: config block, env-var credential model, CLI usage, security notes)
    - docs/sample-config.yaml — the existing `ticketing.jira` block (mirror shape for the servicenow sub-block)
    - 105-RESEARCH.md "Config YAML shape for users" (lines 554-568), Pitfall 4 (https-only), and the env-var credential model
  </read_first>
  <action>
    In docs/configuration.md, add a "ServiceNow Ticketing" subsection adjacent to the Jira Ticketing section. Document: the [ticketing].servicenow config block (instance_url — MUST be https://, user_env, password_env, optional table defaulting to incident, optional allow_internal); that user_env/password_env hold env-var NAMES not credential values (e.g. QUIRK_SNOW_USER / QUIRK_SNOW_PASSWORD), resolved at runtime, never written to SQLite/JSON/logs; the CLI command `quirk ticket create --backend servicenow` (default backend is jira); the dedup behavior (correlation_id carries the SHA256 fingerprint, second scan appends a work_notes journal entry instead of duplicating the incident); the [tickets] extra requirement; and the SSRF/https-only security guardrails. In docs/sample-config.yaml, add a `servicenow` sub-block under `ticketing:` mirroring the example in RESEARCH (instance_url, user_env, password_env). Keep diffs minimal; do not alter the jira sections.
  </action>
  <acceptance_criteria>
    - source: docs/configuration.md contains a ServiceNow ticketing section naming instance_url, user_env, password_env, --backend servicenow, https-only, and correlation_id/work_notes dedup
    - source: docs/sample-config.yaml contains a ticketing.servicenow block with instance_url/user_env/password_env
    - test-command: `grep -qi servicenow docs/configuration.md && grep -qi servicenow docs/sample-config.yaml`
  </acceptance_criteria>
  <verify>
    <automated>grep -qi 'servicenow' docs/configuration.md && grep -qi 'backend servicenow' docs/configuration.md && grep -qi 'servicenow' docs/sample-config.yaml && echo OK</automated>
  </verify>
  <done>configuration.md and sample-config.yaml document the ServiceNow backend (https-only, env-var-name creds, --backend servicenow, dedup); jira sections unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add UAT Series 105, sync to Obsidian, write Phase-105 note, commit UAT-SERIES</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md — the "## Series 104: Jira Ticketing" section (~line 12126) as the exact template for a new Series 105 section; and the header `**Last Updated:**` line (~line 4)
    - CLAUDE.md "Mandatory Phase Completion Steps" (Obsidian phase note path + format; UAT-SERIES sync printf-prepend pattern; step-4 gsd-tools commit)
    - 105-VALIDATION.md "Manual-Only Verifications" — the live ServiceNow human-UAT instructions
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/ existing Phase-NN note as the Obsidian note template
  </read_first>
  <action>
    Add "## Series 105: ServiceNow Ticketing (Phase 105 — v5.3)" to docs/UAT-SERIES.md mirroring Series 104. Coverage line: TICKET-02. Include an automated-gates case (run `python -m pytest tests/test_ticketing_servicenow.py -x -q`, `python -m pytest tests/test_ticket_cmd.py -x -q`, and confirm `git diff --quiet quirk/ticketing/base.py quirk/ticketing/jira.py` for the TICKET-04 zero-change invariant) and a human-UAT case for live ServiceNow incident creation + dedup (configure [ticketing].servicenow, run `quirk ticket create --backend servicenow`, confirm one incident per finding with QRAMM evidence + correlation_id, re-run and confirm work_notes appended not duplicated — sourced from 105-VALIDATION.md Manual-Only Verifications). Update the `**Last Updated:**` header line to 2026-05-25 noting Phase 105 COMPLETE (ServiceNow Ticketing TICKET-02: ServiceNowChannel subclass via stdlib urllib Table API, ServiceNowTicketingCfg + _parse_servicenow_cfg https-only, --backend flag, zero changes to base.py/jira.py). Then perform the CLAUDE.md mandatory steps: (a) write the Obsidian Phase-105 note directly to /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-105-ServiceNow-Ticketing.md (frontmatter status: complete, type: phase, source, updated; Goal; Requirements Covered TICKET-02; Success Criteria; What Was Built per plan from the SUMMARYs; [[Roadmap]] link) — use the vault filesystem Write, NOT obsidian CLI content=; (b) sync UAT-SERIES.md to the vault via the printf-prepend pattern to /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md; (c) commit docs/UAT-SERIES.md via `node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-105): update UAT-SERIES.md" --files docs/UAT-SERIES.md`.
  </action>
  <acceptance_criteria>
    - source: docs/UAT-SERIES.md contains "## Series 105: ServiceNow Ticketing" with TICKET-02 coverage and an automated-gates case referencing tests/test_ticketing_servicenow.py
    - source: the UAT-SERIES.md header `**Last Updated:**` line references Phase 105
    - behavior: Obsidian note /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-105-ServiceNow-Ticketing.md exists with status: complete; vault UAT-Series.md updated; docs/UAT-SERIES.md committed via gsd-tools
    - test-command: `grep -q "Series 105" docs/UAT-SERIES.md && test -f /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-105-ServiceNow-Ticketing.md`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Series 105" docs/UAT-SERIES.md && grep -qi "Phase 105" docs/UAT-SERIES.md && test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-105-ServiceNow-Ticketing.md" && echo OK</automated>
  </verify>
  <done>Series 105 added; UAT-SERIES header notes Phase 105; Obsidian Phase-105 note (status complete) written; vault UAT-Series synced; docs/UAT-SERIES.md committed via gsd-tools.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → user config | Documentation prescribes env-var-name credential model + https-only; no secrets in docs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-10 | Information Disclosure | sample-config.yaml example creds | mitigate | sample uses env-var NAMES (QUIRK_SNOW_USER/PASSWORD), never literal credentials; mirrors jira block convention |
| T-105-11 | Tampering | docs prescribe insecure http:// | mitigate | configuration.md explicitly documents https-only requirement and rejection-at-parse behavior |
| T-105-SC | Tampering | npm/pip/cargo installs | accept | docs-only plan; no installs |
</threat_model>

<verification>
- `grep -qi servicenow docs/configuration.md docs/sample-config.yaml` — both match
- `grep -q "Series 105" docs/UAT-SERIES.md` — match
- `test -f /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-105-ServiceNow-Ticketing.md` — exists
- `git log --oneline -1 -- docs/UAT-SERIES.md` — shows the docs(phase-105) commit
</verification>

<success_criteria>
- ServiceNow backend documented in configuration.md + sample-config.yaml (https-only, env-var-name creds, --backend servicenow, dedup behavior)
- UAT Series 105 added with automated-gates + live human-UAT cases
- Obsidian Phase-105 note written with status complete; UAT-Series synced to vault
- docs/UAT-SERIES.md committed via gsd-tools (CLAUDE.md step 4)
</success_criteria>

<output>
Create `.planning/phases/105-servicenow-ticketing/105-03-SUMMARY.md` when done
</output>
