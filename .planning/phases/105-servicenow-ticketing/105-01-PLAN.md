---
phase: 105-servicenow-ticketing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_ticketing_servicenow.py
  - quirk/ticketing/config.py
  - quirk/ticketing/servicenow.py
autonomous: true
requirements: [TICKET-02]
must_haves:
  truths:
    - "A first-scan finding becomes one ServiceNow incident via POST /api/now/table/incident carrying QRAMM evidence and correlation_id=fingerprint"
    - "A second scan of the same finding appends a work_notes journal entry (PATCH) instead of creating a duplicate incident"
    - "ServiceNow Basic-auth credentials resolve from env-var names only and never appear in SQLite, scan JSON, or logs"
    - "An http:// instance_url is rejected at config parse time (returns None)"
    - "A loopback/internal instance_url is blocked by validate_external_url at construction"
    - "quirk/ticketing/base.py and quirk/ticketing/jira.py are byte-for-byte unchanged"
  artifacts:
    - path: "quirk/ticketing/servicenow.py"
      provides: "ServiceNowChannel(TicketingChannel) implementing only the 3 abstract methods + _NoRedirectHandler"
      contains: "class ServiceNowChannel(TicketingChannel)"
    - path: "quirk/ticketing/config.py"
      provides: "ServiceNowTicketingCfg dataclass + _parse_servicenow_cfg + servicenow field on TicketingCfg"
      contains: "class ServiceNowTicketingCfg"
    - path: "tests/test_ticketing_servicenow.py"
      provides: "Wave 0 mocked-urllib tests for TICKET-02 behaviors + config validation"
      contains: "def test_create_incident"
  key_links:
    - from: "quirk/ticketing/servicenow.py"
      to: "quirk/ticketing/base.py::TicketingChannel"
      via: "subclass inheriting compute_fingerprint/build_ticket_evidence/dispatch_finding"
      pattern: "class ServiceNowChannel\\(TicketingChannel\\)"
    - from: "quirk/ticketing/servicenow.py"
      to: "ServiceNow Table API"
      via: "urllib build_opener(_NoRedirectHandler).open(req, timeout=10)"
      pattern: "build_opener\\(_NoRedirectHandler\\)"
---

<objective>
Add the ServiceNow ticketing backend (TICKET-02) as a pure additive subclass of the Phase 104 TicketingChannel ABC, plus its config dataclass and parser, plus the Wave 0 test scaffold that defines the expected behaviors before implementation.

Purpose: A security team using ServiceNow can auto-create one incident per finding via the SAME abstraction and SHA256 dedup proven by Jira in Phase 104 — proving TICKET-04's "one abstraction, not two code paths" holds with a second backend.
Output: quirk/ticketing/servicenow.py (new), quirk/ticketing/config.py (extended), tests/test_ticketing_servicenow.py (new Wave 0 scaffold).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-servicenow-ticketing/105-CONTEXT.md
@.planning/phases/105-servicenow-ticketing/105-RESEARCH.md
@.planning/phases/105-servicenow-ticketing/105-PATTERNS.md

<interfaces>
<!-- The ABC the executor MUST subclass. Read-only — do NOT modify base.py. -->
From quirk/ticketing/base.py (Phase 104 — DO NOT MODIFY):
- class TicketingChannel(ABC): destination: str = "unknown"
- @abstractmethod find_by_fingerprint(self, fp: str) -> Optional[str]   # issue key/sys_id or None
- @abstractmethod create_issue_from_finding(self, finding: dict, fp: str, evidence: str) -> str   # returns key/sys_id
- @abstractmethod add_rediscovery_comment(self, issue_key: str, fp: str) -> None
- @staticmethod compute_fingerprint(finding: dict) -> str   # SHA256(f"{host}:{port}::{title}") hex — INHERIT, NEVER override
- @staticmethod build_ticket_evidence(finding: dict) -> str   # whitelisted-field evidence — INHERIT
- dispatch_finding(self, finding, db, scan_id) -> None   # owns dedup+audit; never raises; writes IntegrationDelivery(destination=self.destination)

From quirk/util/url_allowlist.py:
- validate_external_url(url, allow_internal=False) -> result with .ok (bool) and .reason (str)

From quirk/util/safe_exc.py:
- safe_str(exc) -> str   # already scrubs `Authorization: Basic <b64>` and 40+ char base64 blobs

From quirk/notify/channels/webhook.py (Phase 103 — analog, copy _NoRedirectHandler verbatim):
- class _NoRedirectHandler(urllib.request.HTTPRedirectHandler): redirect_request(...) raises HTTPError "Redirect blocked (SSRF guard)"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0 (Wave 0): Write failing mocked-urllib tests for ServiceNowChannel + config</name>
  <files>tests/test_ticketing_servicenow.py</files>
  <read_first>
    - tests/test_ticketing_jira.py — file structure, _make_cfg/_sample_finding helpers, monkeypatch.setenv creds, dispatch_finding + DB audit-row assertions, config-validation test shape
    - tests/test_notify_webhook.py — _FakeOpener / _FakeHTTPResponse and the `patch("...urllib.request.build_opener", return_value=_FakeOpener(...))` mocking pattern
    - 105-PATTERNS.md "tests/test_ticketing_servicenow.py" section (lines 347-499) — exact helper + test bodies to mirror
    - 105-RESEARCH.md "Phase Requirements to Test Map" (lines 640-651) and "Test Pattern — mocked urllib" (lines 652-680)
  </read_first>
  <action>
    Create tests/test_ticketing_servicenow.py mirroring tests/test_ticketing_jira.py structure with urllib mocking from tests/test_notify_webhook.py. Add _FakeHTTPResponse (with status, read() returning json-encoded body, __enter__/__exit__) and _FakeOpener(callback) helpers. Add _make_cfg() building a ServiceNowTicketingCfg (instance_url=https://myco.service-now.com, user_env=QUIRK_SNOW_USER, password_env=QUIRK_SNOW_PASSWORD, table=incident, allow_internal=False) and _sample_finding() with title/severity/host/port/description/recommendation.
    Write these tests (initially failing — ServiceNowChannel and config symbols do not yet exist):
    - test_create_incident: patch build_opener with a _FakeOpener returning {"result":{"sys_id":"a"*32,"number":"INC0000042"}} status 201; assert create_issue_from_finding returns the sys_id, the captured Request.method == "POST", json.loads(req.data)["correlation_id"] == compute_fingerprint(finding), and ["short_description"] == finding title.
    - test_dedup_then_work_notes: use a real DB via tmp_path + init_db + get_session (mirror jira test lines 146-173). First dispatch_finding: GET returns {"result":[]} then POST. Second dispatch_finding: GET returns {"result":[{"sys_id":<id>}]} then PATCH to /incident/<id> with body work_notes; assert POST happened exactly once across both runs and PATCH happened once on the second, and the captured PATCH Request.method == "PATCH".
    - test_correlation_id_is_fingerprint: assert the POST body correlation_id equals base TicketingChannel.compute_fingerprint(finding) (NOT a servicenow-local hash).
    - test_http_instance_url_rejected: _parse_servicenow_cfg({"instance_url":"http://myco.service-now.com","user_env":"U","password_env":"P"}) is None.
    - test_missing_instance_url: _parse_servicenow_cfg({"user_env":"U","password_env":"P"}) is None.
    - test_missing_env_fields_rejected: _parse_servicenow_cfg with empty user_env is None.
    - test_https_valid_config: _parse_servicenow_cfg with valid https + env names returns a ServiceNowTicketingCfg with table defaulting to "incident".
    - test_ssrf_guard: ServiceNowChannel(_make_cfg(instance_url="https://127.0.0.1/")) raises ValueError matching "SSRF".
    - test_credentials_not_in_logs: monkeypatch setenv creds; make the _FakeOpener raise urllib.error.HTTPError whose message embeds a planted 40+ char Basic-auth-like string; run dispatch_finding against a tmp_path DB; query the IntegrationDelivery row and assert the planted credential substring is ABSENT from error_summary (safe_str enforcement) and status == "failed".
    Imports from quirk.ticketing.config: ServiceNowTicketingCfg, _parse_servicenow_cfg. Imports ServiceNowChannel inside test bodies (lazy, matching jira test). Run command must reference the targeted file. Do NOT import or touch base.py/jira.py beyond the inherited compute_fingerprint call via the channel instance.
  </action>
  <acceptance_criteria>
    - source: tests/test_ticketing_servicenow.py exists and defines test_create_incident, test_dedup_then_work_notes, test_correlation_id_is_fingerprint, test_http_instance_url_rejected, test_missing_instance_url, test_missing_env_fields_rejected, test_https_valid_config, test_ssrf_guard, test_credentials_not_in_logs
    - behavior: the file imports ServiceNowTicketingCfg and _parse_servicenow_cfg from quirk.ticketing.config and ServiceNowChannel from quirk.ticketing.servicenow (lazy in-body)
    - test-command: `python -m pytest tests/test_ticketing_servicenow.py -q` collects all 9 tests (they FAIL/ERROR at this task because implementation is absent — RED state expected)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_ticketing_servicenow.py --collect-only -q 2>&1 | grep -c "test_" </automated>
  </verify>
  <done>Test file collects 9 named tests; tests reference correlation_id==compute_fingerprint, http-reject, SSRF, and credential-scrub behaviors. Tests are RED (implementation not yet present).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Add ServiceNowTicketingCfg dataclass + _parse_servicenow_cfg to config.py</name>
  <files>quirk/ticketing/config.py</files>
  <read_first>
    - quirk/ticketing/config.py — existing JiraTicketingCfg (line 25), TicketingCfg (line 44), _parse_ticketing_cfg (line 79), _parse_jira_cfg (line 85) as the exact mirror template
    - 105-PATTERNS.md "quirk/ticketing/config.py — MODIFY" section (lines 172-259) — dataclass shape, container extension, parser body
    - 105-RESEARCH.md Pitfall 4 (lines 422-426) — https-only reject-at-parse rationale
  </read_first>
  <behavior>
    - _parse_servicenow_cfg({}) and missing instance_url → None
    - http:// instance_url → None (cleartext Basic-auth guard)
    - missing/empty user_env or password_env → None
    - valid https + env names → ServiceNowTicketingCfg with table defaulting to "incident", allow_internal defaulting to False
  </behavior>
  <action>
    Add @dataclass ServiceNowTicketingCfg with fields: instance_url: str, user_env: str, password_env: str, table: str = "incident", allow_internal: bool = False (env-var NAMES only, never values — per TICKET-02 criterion 3). Add a `servicenow: Optional[ServiceNowTicketingCfg] = None` field to the existing TicketingCfg dataclass. Extend _parse_ticketing_cfg to read raw.get("servicenow") or {} and pass servicenow=_parse_servicenow_cfg(servicenow_raw) into TicketingCfg. Add _parse_servicenow_cfg(raw: dict) -> Optional[ServiceNowTicketingCfg] mirroring _parse_jira_cfg: return None if not raw; return None if no instance_url; return None if not str(instance_url).startswith("https://") (reject http:// at parse — Pitfall 4, creds transit in Authorization header); read user_env/password_env as str, return None if either empty; otherwise construct ServiceNowTicketingCfg with table=str(raw.get("table","incident")) and allow_internal=bool(raw.get("allow_internal", False)). PEP 8; minimal diff — do not alter _parse_jira_cfg or JiraTicketingCfg.
  </action>
  <acceptance_criteria>
    - source: quirk/ticketing/config.py defines ServiceNowTicketingCfg, _parse_servicenow_cfg, and TicketingCfg has a servicenow field; _parse_ticketing_cfg wires servicenow
    - behavior: the config-validation tests in test_ticketing_servicenow.py pass (http reject, missing fields, valid https)
    - test-command: `python -m pytest tests/test_ticketing_servicenow.py -k "instance_url or env_fields or valid_config" -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_ticketing_servicenow.py -k "instance_url or env_fields or valid_config" -q</automated>
  </verify>
  <done>Config validation tests green; ServiceNowTicketingCfg and _parse_servicenow_cfg present; JiraTicketingCfg/_parse_jira_cfg untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ServiceNowChannel (3 abstract methods, urllib Table API)</name>
  <files>quirk/ticketing/servicenow.py</files>
  <read_first>
    - quirk/ticketing/jira.py — subclass shape: destination class var, __init__ (SSRF guard + env-resolved creds), method signatures; DO NOT modify, only mirror
    - quirk/notify/channels/webhook.py — _NoRedirectHandler class (copy verbatim) + build_opener + opener.open(req, timeout=N) + json body encoding
    - quirk/ticketing/base.py interfaces block above — the 3 abstract signatures and inherited compute_fingerprint (NEVER override)
    - 105-PATTERNS.md "quirk/ticketing/servicenow.py" section (lines 20-168) — imports, _NoRedirectHandler, __init__, GET/POST/PATCH bodies
    - 105-RESEARCH.md Pitfalls 1,2,4,6 + Code Examples (lines 451-552)
  </read_first>
  <behavior>
    - find_by_fingerprint(fp): GET /api/now/table/{table}?sysparm_query=correlation_id=<fp>&sysparm_limit=1&sysparm_fields=sys_id → result[0]["sys_id"] or None
    - create_issue_from_finding(finding, fp, evidence): POST .../incident with short_description/description/correlation_id → returns result["sys_id"] (NOT number); accepts 200 and 201
    - add_rediscovery_comment(sys_id, fp): PATCH .../incident/{sys_id} with {"work_notes": ...}; accepts 200/201
    - __init__: SSRF-guard raises ValueError on internal URL; builds Authorization: Basic header from env-resolved creds, stored only on self._auth_header
  </behavior>
  <action>
    Create quirk/ticketing/servicenow.py. Imports: from __future__ import annotations; base64, json, logging, os, urllib.error, urllib.request; Optional from typing; urlencode from urllib.parse; TicketingChannel from quirk.ticketing.base; safe_str from quirk.util.safe_exc; validate_external_url from quirk.util.url_allowlist. Copy _NoRedirectHandler VERBATIM from webhook.py (raises HTTPError "Redirect blocked (SSRF guard)" on any redirect — Pitfall 6). Define class ServiceNowChannel(TicketingChannel) with destination = "servicenow". __init__(self, cfg): call validate_external_url(cfg.instance_url, allow_internal=cfg.allow_internal); if not result.ok raise ValueError(f"SSRF blocked ({result.reason}) for ServiceNow URL"); store self._cfg = cfg; resolve user=os.environ.get(cfg.user_env,"") and password=os.environ.get(cfg.password_env,""); creds=base64.b64encode(f"{user}:{password}".encode("utf-8")).decode("ascii"); self._auth_header=f"Basic {creds}" (NEVER logged — relies on safe_str if it leaks). Implement the three methods using urllib.request.Request with method= "GET"/"POST"/"PATCH", headers Accept/Content-Type/Authorization=self._auth_header, opener=build_opener(_NoRedirectHandler), opener.open(req, timeout=10). find_by_fingerprint: build query via urlencode({"sysparm_query": f"correlation_id={fp}","sysparm_limit":"1","sysparm_fields":"sys_id"}); return result[0]["sys_id"] if non-empty else None. create_issue_from_finding: POST to f"{instance_url}/api/now/table/{table}" body {short_description: str(finding.get("title","QUIRK Finding"))[:255], description: evidence, correlation_id: fp}; return data["result"]["sys_id"] (Pitfall 2 — sys_id NOT number). add_rediscovery_comment(issue_key, fp): PATCH to f"{instance_url}/api/now/table/{table}/{issue_key}" body {work_notes: f"Rediscovery: QUIRK re-detected this finding on a subsequent scan.\nFingerprint: {fp}"}; accept resp.status in (200,201) else RuntimeError (Pitfall 1 — PATCH not POST/PUT). In all three, catch urllib.error.HTTPError → raise RuntimeError(f"ServiceNow <VERB> failed: HTTP {exc.code}") from exc and urllib.error.URLError → raise RuntimeError("ServiceNow <VERB> failed: connection error") from exc. Do NOT define compute_fingerprint (inherited — TICKET-03 cross-backend identity). PEP 8.
  </action>
  <acceptance_criteria>
    - source: quirk/ticketing/servicenow.py defines class ServiceNowChannel(TicketingChannel) with destination = "servicenow" and exactly the 3 abstract methods plus __init__; NO `def compute_fingerprint` anywhere in the file
    - source: every opener uses build_opener(_NoRedirectHandler); create returns data["result"]["sys_id"]; add_rediscovery_comment uses method="PATCH"
    - behavior: all tests in test_ticketing_servicenow.py pass (create returns sys_id, dedup→work_notes, correlation_id==fingerprint, SSRF guard, creds scrubbed)
    - test-command: `python -m pytest tests/test_ticketing_servicenow.py -q` all green
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_ticketing_servicenow.py -q && test -z "$(grep -n 'def compute_fingerprint' quirk/ticketing/servicenow.py)" && git diff --quiet quirk/ticketing/base.py quirk/ticketing/jira.py && echo "BASE_JIRA_UNCHANGED"</automated>
  </verify>
  <done>All 9 ServiceNow tests green; no compute_fingerprint override; base.py and jira.py show no git diff (TICKET-04 zero-change constraint proven).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QUIRK CLI → ServiceNow instance | Outbound HTTPS with Basic-auth creds; user-supplied instance_url crosses untrusted-network boundary |
| OS env → ServiceNowChannel | Credential values enter the process only via os.environ at __init__ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-01 | Information Disclosure | servicenow.py self._auth_header in exceptions/logs | mitigate | safe_str(exc) in base.dispatch_finding (already covers Authorization: Basic + 40+ char base64); never log the header; test_credentials_not_in_logs asserts absence in error_summary |
| T-105-02 | Information Disclosure | cleartext credential transit | mitigate | _parse_servicenow_cfg rejects non-https instance_url at parse (returns None); test_http_instance_url_rejected |
| T-105-03 | Elevation of Privilege | SSRF via instance_url | mitigate | validate_external_url(instance_url) at __init__; test_ssrf_guard |
| T-105-04 | Elevation of Privilege | redirect-based SSRF bypass | mitigate | _NoRedirectHandler copied verbatim from webhook.py raises HTTPError on any 3xx |
| T-105-05 | Tampering | duplicate-incident storm if dedup fails | mitigate | correlation_id search-before-create via inherited dispatch_finding + integration_deliveries idempotency; test_dedup_then_work_notes |
| T-105-06 | Tampering | sysparm_query injection via fingerprint | accept | compute_fingerprint output is 64-char hex [0-9a-f] only; urlencode used as defense-in-depth |
| T-105-SC | Tampering | npm/pip/cargo installs | accept | zero new pip deps (stdlib urllib per v5.3-D-06); no install task; RESEARCH Package Legitimacy Audit confirms none required |
</threat_model>

<verification>
- `python -m pytest tests/test_ticketing_servicenow.py -q` — all green
- `git diff --quiet quirk/ticketing/base.py quirk/ticketing/jira.py` — exits 0 (zero changes, TICKET-04)
- `grep -n 'def compute_fingerprint' quirk/ticketing/servicenow.py` — no output
- `python -m compileall quirk/ticketing/servicenow.py quirk/ticketing/config.py` — succeeds
</verification>

<success_criteria>
- ServiceNowChannel creates an incident (POST → sys_id) carrying QRAMM evidence + correlation_id on first scan
- Second scan dedups (GET) and appends work_notes (PATCH) — no duplicate incident
- Credentials resolve from env-var names only; never in SQLite/JSON/logs (safe_str verified)
- http:// instance_url rejected at parse; internal instance_url blocked at __init__
- base.py and jira.py unchanged; compute_fingerprint inherited (cross-backend identity)
</success_criteria>

<output>
Create `.planning/phases/105-servicenow-ticketing/105-01-SUMMARY.md` when done
</output>
