---
phase: 111-console-dashboard-awareness
plan: 02
type: execute
wave: 2
depends_on: ["111-01"]
files_modified:
  - src/dashboard/src/types/api.ts
  - src/dashboard/src/hooks/useSensorRegistry.ts
  - src/dashboard/src/hooks/useMergeLatest.ts
  - src/dashboard/src/pages/sensors.tsx
  - src/dashboard/src/components/sidebar.tsx
  - src/dashboard/src/App.tsx
  - src/dashboard/src/pages/findings.tsx
  - src/dashboard/src/pages/cbom.tsx
  - src/dashboard/src/pages/executive.tsx
  - src/dashboard/src/pages/__tests__/sensors-loading.test.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]
must_haves:
  truths:
    - "A top-level Sensors page (/sensors) renders the registry table with text+color status badges"
    - "Findings and CBOM views have a shared-pattern 'All segments' default Select that filters rows client-side by segment"
    - "The Executive page renders per-segment ScoreGauges (maxValue=100) alongside the org-wide gauge when merge data is present"
    - "A non-dismissible amber coverage_warning banner (role=alert) renders above the gauges when coverage_warning is non-null"
    - "TypeScript types in types/api.ts mirror the Pydantic schema fields added in Plan 01"
    - "npm run build succeeds so the statics reflect the .tsx changes"
  artifacts:
    - path: "src/dashboard/src/pages/sensors.tsx"
      provides: "Sensors registry page"
      min_lines: 40
    - path: "src/dashboard/src/hooks/useSensorRegistry.ts"
      provides: "registry fetch hook"
    - path: "src/dashboard/src/hooks/useMergeLatest.ts"
      provides: "merge/latest fetch hook"
  key_links:
    - from: "src/dashboard/src/types/api.ts"
      to: "quirk/dashboard/api/schemas.py"
      via: "mirrored sensor_id/segment fields + new interfaces"
      pattern: "sensor_id\\?: string"
    - from: "src/dashboard/src/pages/executive.tsx"
      to: "/api/merge/latest"
      via: "useMergeLatest hook"
      pattern: "useMergeLatest"
    - from: "src/dashboard/src/pages/sensors.tsx"
      to: "/api/sensor/registry"
      via: "useSensorRegistry hook"
      pattern: "useSensorRegistry"
---

<objective>
Build the Phase 111 frontend: mirror the new backend schema fields into TypeScript, add two data hooks, a Sensors page + nav item, shared segment filters on Findings and CBOM, per-segment gauges and the coverage-warning banner on the Executive page — then `npm run build` and add vitest coverage. Implement the APPROVED 111-UI-SPEC.md exactly; do not re-decide visuals.

Purpose: Surface the distributed scanner's sensor/segment/coverage awareness in the console dashboard, consuming the Plan 01 endpoints.
Output: New page + hooks, modified nav/routes/findings/cbom/executive, a successful production build, and a Sensors-page vitest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/111-console-dashboard-awareness/111-CONTEXT.md
@.planning/phases/111-console-dashboard-awareness/111-UI-SPEC.md
@.planning/phases/111-console-dashboard-awareness/111-RESEARCH.md
@.planning/phases/111-console-dashboard-awareness/111-PATTERNS.md
@.planning/phases/111-console-dashboard-awareness/111-01-SUMMARY.md

<critical>
- `.tsx` edits are INVISIBLE until `cd src/dashboard && npm run build` runs (FastAPI serves pre-built statics). A dedicated build task is mandatory (Task 3).
- Presence/component tests assert field presence + content-model, NOT visual appearance. Visual fidelity is gated on the UI-SPEC + human UAT, not asserted here.
- Recharts children must stay statically mounted (toggle via opacity); the per-segment ScoreGauges are SVG-based (not Recharts) so this applies only to any Recharts touch in executive.tsx — do not convert existing `<Bar>`/`<Cell>` to conditional mounts.
- types/api.ts MUST mirror the Plan 01 Pydantic fields exactly (Trap T3 / Pitfall 8) — otherwise finding.segment is silently `undefined`.
</critical>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mirror TS types + add useSensorRegistry / useMergeLatest hooks</name>
  <files>src/dashboard/src/types/api.ts, src/dashboard/src/hooks/useSensorRegistry.ts, src/dashboard/src/hooks/useMergeLatest.ts</files>
  <read_first>
    - src/dashboard/src/types/api.ts (FindingItem ~L23-34, CbomComponent ~L47-53 — exact existing field style)
    - src/dashboard/src/hooks/useScanData.ts (full file — the cancellation pattern: `let cancelled=false`, clear state before fetch, `if(!cancelled)` guards, cleanup return)
    - src/dashboard/src/lib/api.ts (fetchApi signature + HARDEN-API-01 rule — must use fetchApi, never raw fetch)
    - .planning/phases/111-console-dashboard-awareness/111-PATTERNS.md (types/api.ts section + hooks section)
    - .planning/phases/111-console-dashboard-awareness/111-01-SUMMARY.md (confirm the exact Pydantic field names shipped in Plan 01)
  </read_first>
  <action>
    In types/api.ts: add optional `sensor_id?: string` and `segment?: string` to BOTH `FindingItem` and `CbomComponent` interfaces (Trap T3 — mirror Plan 01 exactly). Append new interfaces: `SensorRegistryItem` (sensor_id: string; segment: string; sensor_version: string | null; last_push_at: string | null; status: "current" | "stale" | "unknown"), `SensorRegistryResponse` (sensors: SensorRegistryItem[]), `MergeLatestData` (scan_id: string | null; merged_at: string | null; score: number | null; endpoint_count: number; sensor_count: number; coverage_warning: { missing_sensors: string[]; reason: string } | null; per_segment_scores: Record<string, number>), `MergeLatestResponse` (merge: MergeLatestData | null).
    Create `useSensorRegistry.ts` and `useMergeLatest.ts` following the useScanData cancellation pattern exactly: clear state synchronously, `fetchApi("/api/sensor/registry")` / `fetchApi("/api/merge/latest")`, guard every setState with `if (!cancelled)`, return `{ cancelled = true }` cleanup, empty dependency array (fetch once on mount). useSensorRegistry exposes `{ sensors, loading, error }` (default `json.sensors ?? []`); useMergeLatest exposes `{ merge, loading, error }` (default `json.merge ?? null`). Use `fetchApi`, never raw `fetch`.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK/src/dashboard && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "sensor_id?: string" src/dashboard/src/types/api.ts` and the field appears under both FindingItem and CbomComponent.
    - `grep -q "interface MergeLatestData" src/dashboard/src/types/api.ts` and `interface SensorRegistryItem`.
    - Both hook files call `fetchApi(` and contain `cancelled` guard logic; neither calls raw `fetch(`.
    - `npx tsc --noEmit` passes (types compile against the new interfaces).
  </acceptance_criteria>
  <done>TS types mirror Plan 01 schemas; two cancellation-safe hooks created using fetchApi; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Sensors page + nav/route wiring + segment Select on Findings & CBOM</name>
  <files>src/dashboard/src/pages/sensors.tsx, src/dashboard/src/components/sidebar.tsx, src/dashboard/src/App.tsx, src/dashboard/src/pages/findings.tsx, src/dashboard/src/pages/cbom.tsx</files>
  <read_first>
    - src/dashboard/src/pages/data-at-rest.tsx (page structure: Card+Table, TableHead scope="col" text-xs font-semibold, hover:bg-accent/5, loading skeleton, EmptyStateCard)
    - src/dashboard/src/components/sidebar.tsx (lucide import block ~L4-20, NAV_ITEMS ~L29-42, nav link map with min-h-[44px] + aria-label)
    - src/dashboard/src/App.tsx (import + Route registration pattern)
    - src/dashboard/src/pages/findings.tsx (severityFilter/protocolFilter Select pattern + filtered findings useMemo)
    - src/dashboard/src/pages/cbom.tsx (qsFilter Select pattern, SelectTrigger className="w-40 h-8 text-sm", data.cbom_components)
    - .planning/phases/111-console-dashboard-awareness/111-UI-SPEC.md (Sensors page §1, segment filter §2, status badge spec, copywriting contract, accessibility checklist)
    - .planning/phases/111-console-dashboard-awareness/111-PATTERNS.md (sensors.tsx, sidebar.tsx, findings.tsx, cbom.tsx sections + Traps T1)
  </read_first>
  <action>
    Create `src/dashboard/src/pages/sensors.tsx` mirroring data-at-rest.tsx: `SensorsPage()` using `useSensorRegistry()`; loading skeleton (role="status", aria-label="Loading sensors", sr-only text, h-7 heading + 5× h-10 rows); error → muted `<p>`; empty → `EmptyStateCard` with the exact enroll-command copy from the UI-SPEC; populated → h1 "Sensors" + Card/CardContent p-0 + Table with columns Sensor ID (font-mono text-xs) / Segment / Version (— if null) / Last Seen (relative time or "Never") / Status. Add a `SensorStatusBadge` helper using the exact UI-SPEC badge classes per status (current = quantum-safe green; stale = #d4893a amber; unknown = secondary) — ALWAYS render the text label ("Current"/"Stale"/"Unknown") + aria-label, never color-only. Add a small relative-time helper (Intl.RelativeTimeFormat or inline math — no date library).
    In sidebar.tsx: add `Radio` to the lucide-react named import block (Trap T1 — omission causes "Radio is not defined"); insert `{ path: "/sensors", label: "Sensors", Icon: Radio }` into NAV_ITEMS after Scan History (`/scans`) and before Schedules. No change to the nav-link render map (it already applies min-h-[44px] + aria-label).
    In App.tsx: import `{ SensorsPage }` and add `<Route path="/sensors" element={<SensorsPage />} />`.
    In findings.tsx: add `segmentFilter` state (default "all"); derive `distinctSegments` via useMemo from `data.findings` (`.map(f => f.segment).filter(Boolean)`, sorted, deduped); add the segment `<Select>` per UI-SPEC §2 (placeholder "All segments", aria-label "Filter by segment", className "w-40 h-8 text-sm") after the protocol filter; add `if (segmentFilter !== "all") filtered = filtered.filter(f => f.segment === segmentFilter)` to the findings useMemo. Apply the identical pattern in cbom.tsx using `data.cbom_components` and `c.segment` after the qsFilter Select.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK/src/dashboard && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Radio" src/dashboard/src/components/sidebar.tsx` in BOTH the import block and NAV_ITEMS (path "/sensors").
    - `grep -q 'path="/sensors"' src/dashboard/src/App.tsx` and `SensorsPage` imported.
    - sensors.tsx renders all three status badge variants with a visible text label (grep for "Current"/"Stale"/"Unknown" string literals) — not color-only.
    - findings.tsx and cbom.tsx each contain `segmentFilter` state, a `distinctSegments` useMemo, the "All segments" Select with aria-label "Filter by segment", and the `=== segmentFilter` filter predicate.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Sensors page + nav + route live; Radio imported (no runtime undefined); shared "All segments" filter on both Findings and CBOM; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Executive per-segment gauges + coverage banner, npm build, Sensors vitest</name>
  <files>src/dashboard/src/pages/executive.tsx, src/dashboard/src/pages/__tests__/sensors-loading.test.tsx</files>
  <read_first>
    - src/dashboard/src/pages/executive.tsx (lucide import ~L9, RegressionAlertChip placement ~L227, gauge flex-wrap layout ~L232-256, the severity Recharts Bar/Cell below)
    - src/dashboard/src/components/gauges/ScoreGauge.tsx (full prop interface: score, label, size, maxValue, isOverall)
    - src/dashboard/src/hooks/useMergeLatest.ts (created in Task 1)
    - .planning/phases/111-console-dashboard-awareness/111-UI-SPEC.md (per-segment gauges §3 maxValue=100 + label truncation; coverage banner §4 markup/copy/role=alert/non-dismissible)
    - .planning/phases/111-console-dashboard-awareness/111-PATTERNS.md (executive.tsx section + Traps T2; tests section)
    - src/dashboard/src/components/EmptyStateCard.tsx (for vitest mocking context if needed)
  </read_first>
  <action>
    In executive.tsx: add `AlertTriangle` to the lucide-react import (Trap T2 — it is NOT currently imported here). Call `const { merge } = useMergeLatest()` at the top of ExecutivePage. Insert the coverage banner immediately before `<RegressionAlertChip />` (inside the space-y-8 block), rendered only when `merge?.coverage_warning` is truthy: the exact UI-SPEC §4 markup — `role="alert"`, `aria-live="polite"`, AlertTriangle icon (#d4893a, aria-hidden), heading "Incomplete sensor coverage", factual body with missing-sensor count + optional "Missing: <list>", amber `var(--ds-high-dim)`/`var(--ds-high-bdr)` styling, NO dismiss button (non-dismissible). Insert per-segment gauges inside the existing `flex flex-wrap justify-around gap-8` div, after the subscore gauges: `{merge?.per_segment_scores && Object.entries(...).map(...)}` rendering `<ScoreGauge key={seg} score={segScore} label={truncated} size={120} maxValue={100} />` with the UI-SPEC truncation rule (>16 chars → slice(0,15)+'…'). Do NOT touch the existing Recharts `<Bar>`/`<Cell>` — keep them statically mounted.
    Create `src/dashboard/src/pages/__tests__/sensors-loading.test.tsx` (vitest) mocking `useSensorRegistry`: assert loading skeleton renders (role="status"), empty state renders the enroll-command copy, and a populated list renders one row per sensor with its status text label present.
    Run the production build (see verify) — REQUIRED so the served statics reflect every .tsx change in this plan.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK/src/dashboard && npm run build && npx vitest run src/pages/__tests__/sensors-loading.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "AlertTriangle" src/dashboard/src/pages/executive.tsx` in the lucide import.
    - executive.tsx contains the coverage banner with `role="alert"` and NO dismiss/close button, gated on `merge?.coverage_warning`.
    - per-segment ScoreGauge uses `maxValue={100}` and the >16-char truncation; existing Recharts children remain unconditionally mounted (no new conditional mount of `<Bar>`/`<Cell>`).
    - `npm run build` exits 0 (statics rebuilt — mandatory project rule).
    - sensors-loading vitest passes loading/empty/populated cases.
  </acceptance_criteria>
  <done>Coverage banner + per-segment gauges live on Executive; production build succeeds; Sensors-page vitest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser fetch → /api/* | Client requests must carry auth/CSRF headers via fetchApi |
| merged score → consultant interpretation | The coverage banner is a UX-integrity control: an incomplete merged score must be visually unmistakable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-06 | Spoofing/Tampering | new data hooks | mitigate | useSensorRegistry/useMergeLatest call `fetchApi` (injects X-API-Key + X-Quirk-Request CSRF) — never raw fetch (HARDEN-API-01); enforced by Task 1 acceptance grep |
| T-111-07 | Repudiation/Integrity | coverage_warning banner | mitigate | Banner renders whenever `merge.coverage_warning` is non-null and is non-dismissible — a merged score on incomplete coverage cannot be silently presented as complete; factual non-alarming copy per UI-SPEC |
| T-111-08 | Information Disclosure | error display on Sensors page | accept | Errors shown as muted text from the hook's `error` string (HTTP status / generic message); backend already avoids leaking exception internals (Plan 01 T-111-05) |
| T-111-SC | Tampering | npm installs | accept | No new npm packages (UI-SPEC: all shadcn primitives + lucide already installed) — no install surface |
</threat_model>

<verification>
- `cd src/dashboard && npx tsc --noEmit` clean
- `cd src/dashboard && npm run build` exits 0
- `npx vitest run src/pages/__tests__/sensors-loading.test.tsx` green
- Manual/human-UAT (deferred, not asserted here): open the built dashboard, confirm Sensors table/badges, segment filters, per-segment gauges, and amber coverage banner against 111-UI-SPEC.md
</verification>

<success_criteria>
- Sensors page + nav live; segment filters on Findings & CBOM; per-segment gauges + coverage banner on Executive
- TS types mirror Plan 01; hooks use fetchApi with cancellation
- npm run build succeeds; Sensors vitest green
</success_criteria>

<output>
Create `.planning/phases/111-console-dashboard-awareness/111-02-SUMMARY.md` when done
</output>
