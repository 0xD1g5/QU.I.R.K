---
phase: 999.83-chaos-lab-service-config-drift
plan: 03
subsystem: chaos-lab
tags: [chaos-lab, docker-compose, deprecation, BACK-90]
status: complete
requirements: [BACK-90]
key-files:
  deleted:
    - quantum-chaos-enterprise-lab/storage/vault-seed.sh
    - quantum-chaos-enterprise-lab/storage/kms-seed.sh
    - quantum-chaos-enterprise-lab/storage/postgres-init.sql
  modified:
    - quantum-chaos-enterprise-lab/docker-compose.yml
---

# Phase 999.83 Plan 03: Deprecated `storage` Profile Removal Summary

Removed the deprecated v4.1 `storage` profile entirely (5 services + 3 seed scripts + 1 named volume) per CONTEXT D-3 recommended option, eliminating BACK-90 bug 3 (vault-seed `rsa-1024` rejection) by retiring the dead code path rather than patching it.

## Bug 3 — Chosen Option (c): Removal

CONTEXT D-3 recommended option (c) — remove the deprecated profile entirely. Rationale: README row 33 already marked the profile deprecated; the v4.3 DAR work already split its three concerns into `database` (postgres-pgcrypto coverage), `storage-s3` (minio/SSE), and `vault` (vault-30 / image 1.17). Patching the `rsa-1024` line would have extended the lifetime of dead code with no functional benefit. Simpler lab = less drift.

## Task 1 — QUIRK Python sanity grep (proof of zero blast radius)

| Grep target | Pattern | Result |
|---|---|---|
| `quirk/` | `20007|20009|20010` | 0 matches |
| `quirk/` | `localstack-kms|postgres-pgcrypto` | 0 matches |
| `quirk/` + `docs/` | `vault.*1\.15` | 0 active-code matches |
| `docs/` | `profile.*storage[^-]` (excluding storage-s3) | 0 matches |

Zero QUIRK Python changes required. No doc-side references to hand to Plan 05 beyond what CONTEXT already identified (README row 33 + oracle profile-storage section, already in Plan 05's scope).

## Task 2 — Compose deletions

**5 service blocks removed** (all carried `profiles: ["storage"]`):

| Service | Image | Port | Reason removed |
|---|---|---|---|
| `localstack-kms` | localstack/localstack:3 | 20007 | Config-introspection-only, never scanner target |
| `localstack-kms-seed` | amazon/aws-cli:latest | — | Orphaned with localstack-kms |
| `vault` (legacy) | hashicorp/vault:1.15 | 20009 | Superseded by `vault-30` (image 1.17, port 28200) |
| `vault-seed` | hashicorp/vault:1.15 | — | Source of BACK-90 bug 3 (`rsa-1024` rejection); orphan with `vault` |
| `postgres-pgcrypto` | postgres:16 | 20010 | Coverage moved to `database` profile's postgres-ssl-off |

Also removed:
- Section-header comment block `# PHASE 4 — STORAGE PROFILE (profile: storage)` (was lines 682-687)
- Named volume declaration `pgcrypto_data:` (was line 1117) — used only by deleted `postgres-pgcrypto`

**3 orphan seed scripts removed via `git rm`**:
- `quantum-chaos-enterprise-lab/storage/vault-seed.sh`
- `quantum-chaos-enterprise-lab/storage/kms-seed.sh`
- `quantum-chaos-enterprise-lab/storage/postgres-init.sql`

**Retained** (verified by `ls`): `quantum-chaos-enterprise-lab/storage/minio-seed.sh` — still referenced by `minio-seed` in `storage-s3` profile.

**Critical preservations** (verified via `profiles:` line inspection before deletion):
- `vault-30` (image hashicorp/vault:1.17, port 28200, `profiles: ["vault"]`) — UNTOUCHED
- `vault-30-seed` (`profiles: ["vault"]`) — UNTOUCHED

## Post-deletion verification

| Check | Expected | Actual |
|---|---|---|
| `docker compose config > /dev/null` exit code | 0 | 0 (`CONFIG_OK`) |
| `grep -cE 'profiles: \["storage"\]' docker-compose.yml` | 0 | 0 |
| `grep -cE 'profiles: \["storage-s3"\]' docker-compose.yml` | >= 2 | 2 |
| `grep -cE 'profiles: \["vault"\]' docker-compose.yml` | >= 2 | 2 |
| `grep -c "hashicorp/vault:1.17"` | >= 1 | 3 |
| `grep -c "hashicorp/vault:1.15"` | 0 | 0 |
| `test ! -f storage/vault-seed.sh` | pass | pass |
| `test ! -f storage/kms-seed.sh` | pass | pass |
| `test ! -f storage/postgres-init.sql` | pass | pass |
| `test -f storage/minio-seed.sh` | pass | pass |

`yq` not installed locally; grep-equivalent profile count proves storage profile is fully gone while storage-s3 / vault remain intact. `lab.sh ALL_PROFILES` auto-derives via yq/grep on `profiles:` tokens (lines 60-69) and will reflect the removal without manual edit.

## Doc cleanup deferred to Plan 05

Per plan, the following are owned by Plan 05's cross-cutting docs commit (NOT this commit):
- `quantum-chaos-enterprise-lab/README.md` row 33 (deprecated `storage` profile entry)
- `quantum-chaos-enterprise-lab/expected_results_v4.md` `## Profile: storage` section
- Image-pin policy note (cross-cutting from CONTEXT)

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check: PASSED
- File `quantum-chaos-enterprise-lab/docker-compose.yml`: present, parses, 0 storage-profile matches
- Files `storage/{vault-seed.sh,kms-seed.sh,postgres-init.sql}`: removed (verified via `ls`)
- File `storage/minio-seed.sh`: retained
- Commit will be created next with the listed deletions.
