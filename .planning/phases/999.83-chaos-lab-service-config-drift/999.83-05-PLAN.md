---
phase: 999.83-chaos-lab-service-config-drift
plan: 05
type: execute
wave: 2
depends_on: ["999.83-01", "999.83-02", "999.83-03", "999.83-04"]
files_modified:
  - quantum-chaos-enterprise-lab/README.md
  - quantum-chaos-enterprise-lab/expected_results_v4.md
  - docs/UAT-SERIES.md
autonomous: false
requirements: [BACK-90]
must_haves:
  truths:
    - "README.md profile-table row 33 (deprecated `storage` profile) is deleted entirely; adjacent rows 32 (`source`) and 34 (`ssh-weak`) become consecutive and unchanged"
    - "README.md gains a new short subsection titled `### Image Pin Policy` immediately after the Profile Summary table, codifying the rule that every service image carries an explicit non-floating version tag, with the four affected services in this phase named as examples"
    - "expected_results_v4.md `## Profile: storage` section (lines ~409-429) is deleted in full; preceding `## Profile: vault` (line 389) and following `## Profile: email` (line 431) sections remain intact"
    - "expected_results_v4.md row 365 (mysql-ssl-off), row 381 (minio encrypted-bucket), and `## Profile: source` section are unchanged (Plans 01/02/04 preserve scanner outputs)"
    - "End-to-end UAT verifies `./lab.sh down && ./lab.sh all` on macOS produces zero `Exited (1/2)` and zero `unhealthy` containers after a 60s settle window (CONTEXT cross-cutting locked UAT criterion + ROADMAP success criterion 6); kerberos auto-skip remains active on macOS"
    - "docs/UAT-SERIES.md gains a Phase 999.83 entry recording the four fixes + image-pin policy, with `**Last Updated:**` date bumped to 2026-05-15, and synced to Obsidian per CLAUDE.md Mandatory Phase Completion Steps"
  artifacts:
    - path: "quantum-chaos-enterprise-lab/README.md"
      provides: "Profile table with deprecated `storage` row removed + new Image Pin Policy subsection"
      contains: "Image Pin Policy"
    - path: "quantum-chaos-enterprise-lab/expected_results_v4.md"
      provides: "## Profile: storage section deleted; other profile sections intact"
      contains: "## Profile: storage-s3"
    - path: "docs/UAT-SERIES.md"
      provides: "Updated with Phase 999.83 entry"
      contains: "999.83"
  key_links:
    - from: "Plan 03 docker-compose.yml deletion"
      to: "README.md row 33 + oracle ## Profile: storage section"
      via: "all three deleted in this single cross-cutting commit"
      pattern: "^## Profile: storage$"
    - from: "Plans 01/02/04 image pins"
      to: "README.md Image Pin Policy subsection"
      via: "policy codifies the rule applied across the four plans"
      pattern: "Image Pin Policy"
---

<objective>
Cross-cutting cleanup for Phase 999.83. Plans 01-04 each made surgical service-level fixes; this plan handles the doc work that spans all four and runs the global UAT:

1. Delete README.md row 33 (deprecated `storage` profile — removed in Plan 03)
2. Delete the `## Profile: storage` section in `expected_results_v4.md` (Plan 03 follow-up)
3. Add a new `### Image Pin Policy` subsection to README.md codifying CONTEXT's "no floating tags" cross-cutting decision
4. Run the end-to-end UAT — `./lab.sh down && ./lab.sh all` on macOS — and verify zero `Exited (1/2)` / zero `unhealthy` after 60s settle (CONTEXT locked UAT criterion + ROADMAP success criterion 6)
5. Update `docs/UAT-SERIES.md` with a Phase 999.83 entry and sync to Obsidian per CLAUDE.md project rules

This plan is `autonomous: false` because the global UAT (step 4) is a human-verified checkpoint — visual confirmation of `docker compose ps` across 60+ containers is faster and more reliable than a scripted assertion.

Purpose: Close out the phase with one atomic docs commit and the end-to-end UAT that proves all four bugs are fixed in concert.
Output: README + oracle edits in one commit; UAT pass; UAT-SERIES.md update + Obsidian sync in a second commit per CLAUDE.md's split between `docs(chaos-lab):` and `docs(phase-999.83):` styles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/999.83-chaos-lab-service-config-drift/CONTEXT.md
@.planning/phases/999.83-chaos-lab-service-config-drift/999.83-01-SUMMARY.md
@.planning/phases/999.83-chaos-lab-service-config-drift/999.83-02-SUMMARY.md
@.planning/phases/999.83-chaos-lab-service-config-drift/999.83-03-SUMMARY.md
@.planning/phases/999.83-chaos-lab-service-config-drift/999.83-04-SUMMARY.md

<interfaces>
quantum-chaos-enterprise-lab/README.md:
- Lines 20-21: `## Profile Summary` heading
- Lines 23-24: table header + separator
- Line 33: deprecated `storage` row (DELETE this line)
- Rows 32 (`source`), 34 (`ssh-weak`), 39 (`database`), 40 (`storage-s3`), 41 (`vault`): unchanged
- After the Profile Summary table, before `## Documentation` (currently ~line 46): INSERT new `### Image Pin Policy` subsection
- Image pin examples to cite in the new subsection (read from 999.83-01..04 SUMMARYs):
  - gitea/gitea:1.21 (unchanged by Plan 01)
  - minio/minio:RELEASE.YYYY-MM-DDTHH-MM-SSZ (Plan 02 — read exact tag from 999.83-02-SUMMARY.md)
  - mysql:8.0 (Plan 04 — newly pinned)
  - hashicorp/vault:1.17 (active, on `vault` profile) and hashicorp/vault:1.15 deleted via Plan 03

quantum-chaos-enterprise-lab/expected_results_v4.md:
- Line 409: `## Profile: storage` heading — start of section to DELETE
- Through line ~429 (the `---` separator before `## Profile: email`) — end of section to DELETE
- Lines 371-388 (`## Profile: storage-s3`), 389-407 (`## Profile: vault`), 431+ (`## Profile: email`): unchanged

docs/UAT-SERIES.md — per CLAUDE.md "Mandatory Phase Completion Steps":
- Append a Phase 999.83 entry under the relevant series (likely the UAT-1 chaos-lab series; identify the right series during read_first)
- Bump `**Last Updated:**` date at top of file to 2026-05-15

Obsidian vault sync paths (per CLAUDE.md, write directly to filesystem — files too large for CLI content= arg):
- Phase note: /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-999.83-Chaos-Lab-Service-Config-Drift.md
- UAT-SERIES sync: /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md (use the printf+cat+cp pattern from CLAUDE.md)

UAT invocation (CONTEXT locked):
- `./lab.sh down && ./lab.sh all`
- Pass criterion: after 60s settle, `docker compose ps` shows zero containers in `Exited (1)`, `Exited (2)`, or `unhealthy` state
- Seed containers in `Exited (0)` are EXPECTED and correct
- Kerberos auto-skip on macOS continues to apply (LAB_INCLUDE_KERBEROS not set)
- `lab.sh down` already sweeps profile-tagged services correctly — fix landed 2026-04-29 in commit `357344a` (Phase 41-06). Both `down` and `reset` arms invoke `compose --profile "*" down --remove-orphans`. No workaround needed; the project memory that suggested otherwise was stale and has been corrected.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.md row 33 deletion + Image Pin Policy subsection + oracle ## Profile: storage deletion</name>
  <files>quantum-chaos-enterprise-lab/README.md, quantum-chaos-enterprise-lab/expected_results_v4.md</files>
  <read_first>
    - quantum-chaos-enterprise-lab/README.md lines 1-50 (profile table + surrounding context)
    - quantum-chaos-enterprise-lab/expected_results_v4.md lines 400-435 (the ## Profile: vault → storage → email region)
    - .planning/phases/999.83-chaos-lab-service-config-drift/999.83-02-SUMMARY.md (read exact minio RELEASE tag chosen in Plan 02 — this is the value to cite in the policy subsection)
    - .planning/phases/999.83-chaos-lab-service-config-drift/999.83-04-SUMMARY.md (confirm `mysql:8.0` is the chosen pin)
  </read_first>
  <action>
    Edit 1 — README.md row 33 deletion: Delete the entire line currently reading `| storage | localstack-kms, vault (1.15), postgres-pgcrypto | 20007, 20009, 20010 | [Expected Findings](expected_results_v4.md#profile-storage) | v4.1; **deprecated** — see \`database\` / \`storage-s3\` / \`vault\` |`. Remove the line entirely (no placeholder blank row) so the markdown table reflows cleanly. Adjacent rows 32 (source) and 34 (ssh-weak) remain untouched and become consecutive.

    Edit 2 — README.md new `### Image Pin Policy` subsection: Insert immediately AFTER the Profile Summary table and BEFORE the `## Documentation` heading (currently ~line 46). The subsection content (write as plain markdown — do NOT enclose in a fenced code block; the prose should be readable as a normal docs section):

    Heading: `### Image Pin Policy`

    First paragraph (one sentence): all services in docker-compose.yml carry an explicit version tag because floating tags (`:latest`, `:8`, `:3`) are silently advanced by Docker Hub, which is exactly what broke four services at once and was discovered under Phase 999.83 / BACK-90.

    Then a three-bullet pin rule:
    - Major-only tags (e.g. `mysql:8`, `nginx:1`, `postgres:15`) are NOT acceptable — Docker Hub re-points them across breaking minor releases.
    - Minor or dated pins ARE acceptable — e.g. `mysql:8.0`, `gitea/gitea:1.21`, `hashicorp/vault:1.17`, `minio/minio:RELEASE.YYYY-MM-DDTHH-MM-SSZ`.
    - When upgrading a pin, treat it as a chaos-lab maintenance change per CLAUDE.md: update lab.sh / README.md / expected_results_v4.md together in the same commit if behavior changes.

    Closing one-line note: list the exact pins this phase enforced — gitea/gitea:1.21 (unchanged), minio/minio:<EXACT-TAG-FROM-PLAN-02-SUMMARY> (newly pinned from `:latest`), mysql:8.0 (newly pinned from `:8`), and legacy hashicorp/vault:1.15 deleted along with the deprecated `storage` profile.

    Replace `<EXACT-TAG-FROM-PLAN-02-SUMMARY>` with the literal RELEASE.YYYY-MM-DDTHH-MM-SSZ tag recorded in 999.83-02-SUMMARY.md.

    Edit 3 — expected_results_v4.md `## Profile: storage` section deletion: Delete from the line `## Profile: storage` (line ~409) through and including the trailing `---` separator (~line 429), so that `## Profile: vault` (line 389-407) is immediately followed by `## Profile: email` (currently line 431) with a single `---` separator and one blank line between them, matching the spacing of other section boundaries in the file.

    No other content edits. Specifically: row 365 (mysql-ssl-off), row 381 (minio encrypted-bucket), and `## Profile: source` stay exactly as they were before this phase — Plans 01/02/04 preserve their scanner outputs.

    Single atomic commit with message: `docs(chaos-lab): drop deprecated storage profile docs + add image-pin policy (BACK-90)`. Body lists the three edits (row 33 deleted, Image Pin Policy added, oracle section deleted).

    Minimal diff outside the three targeted edits.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && grep -c "Image Pin Policy" quantum-chaos-enterprise-lab/README.md && grep -c "^## Profile: storage$" quantum-chaos-enterprise-lab/expected_results_v4.md && grep -c "vault (1.15)" quantum-chaos-enterprise-lab/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Image Pin Policy" quantum-chaos-enterprise-lab/README.md` == 1
    - `grep -c "vault (1.15)" quantum-chaos-enterprise-lab/README.md` == 0 (row 33 deleted)
    - `grep -c '| storage |' quantum-chaos-enterprise-lab/README.md` == 0 (no profile-table row for `storage`)
    - `grep -c '| storage-s3 |' quantum-chaos-enterprise-lab/README.md` == 1 (storage-s3 row intact)
    - `grep -c "^## Profile: storage$" quantum-chaos-enterprise-lab/expected_results_v4.md` == 0 (section deleted)
    - `grep -c "^## Profile: storage-s3$" quantum-chaos-enterprise-lab/expected_results_v4.md` == 1 (kept)
    - `grep -c "^## Profile: vault$" quantum-chaos-enterprise-lab/expected_results_v4.md` == 1 (kept)
    - `grep -c "^## Profile: email$" quantum-chaos-enterprise-lab/expected_results_v4.md` == 1 (kept)
    - `grep -c "mysql:8.0" quantum-chaos-enterprise-lab/README.md` >= 1 (cited in policy subsection)
    - `grep -cE "minio/minio:RELEASE\\." quantum-chaos-enterprise-lab/README.md` >= 1 (cited in policy subsection)
  </acceptance_criteria>
  <done>
    Both docs files reflect the four bug fixes; deprecated profile references gone; image-pin policy codified.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end UAT — `./lab.sh down && ./lab.sh all` produces zero unhealthy/Exited(1/2)</name>
  <what-built>All four BACK-90 fixes (Plans 01-04) plus the cross-cutting docs cleanup (Task 1 above) are committed. This UAT confirms the lab as a whole comes up clean after a from-scratch start.</what-built>
  <how-to-verify>
    From `/Volumes/Digs-1TB/Development/quantum-apps/QUIRK/quantum-chaos-enterprise-lab/`:

    1. Pre-clean: `./lab.sh down` (sweeps all profile-tagged services via the `--profile "*" --remove-orphans` logic landed in commit `357344a`).

    2. `./lab.sh all`

    3. Wait 60 seconds for healthchecks + seed containers to settle.

    4. `docker compose ps`. Confirm:
       - Zero containers in `Exited (1)` state
       - Zero containers in `Exited (2)` state
       - Zero containers in `(unhealthy)` state
       - Long-running services show `Up (healthy)` or `Up`
       - Seed containers (gitea-seed, minio-seed, vault-30-seed, registry-seed, etc.) show `Exited (0)` — this is correct
       - Kerberos service is NOT present (auto-skipped on macOS) — also correct
       - Legacy `vault` (image 1.15), `vault-seed`, `localstack-kms`, `localstack-kms-seed`, `postgres-pgcrypto` are NOT present (Plan 03 removed them)

    5. Spot-check the four bug fix sites:
       - `docker ps --filter "name=chaoslab-gitea-1" --format '{{.Status}}'` shows `Up.*healthy`
       - `docker ps --filter "name=chaoslab-gitea-seed-1" -a --format '{{.Status}}'` shows `Exited (0)`
       - `docker ps --filter "name=chaoslab-minio-seed-1" -a --format '{{.Status}}'` shows `Exited (0)`
       - `docker ps --filter "name=chaoslab-mysql-ssl-off-1" --format '{{.Status}}'` shows `Up` (no Exited)
       - `docker ps --filter "name=chaoslab-vault-30-1" --format '{{.Status}}'` shows `Up.*healthy` (vault-30 = active `vault` profile, unaffected by Plan 03 deletion)

    6. Smoke scan (required, not optional — gates ROADMAP criterion 4): `quirk scan localhost:23306` reports `MySQL/ssl-off` HIGH, preserving oracle row 365.

    7. `./lab.sh down`

    If all checks pass, the phase is functionally complete and we move to Task 3 (UAT-SERIES + Obsidian sync). If anything fails, capture `docker compose ps` output + `docker logs <failing-service>` and pause for diagnosis.
  </how-to-verify>
  <resume-signal>Type "approved" once all checks pass; paste failing logs or `docker compose ps` output otherwise.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update docs/UAT-SERIES.md, sync UAT-Series + Phase note to Obsidian vault, commit</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (full file — identify the chaos-lab series to append to)
    - .planning/phases/999.83-chaos-lab-service-config-drift/999.83-01-SUMMARY.md through 999.83-04-SUMMARY.md (collect fix summaries for the phase note)
    - CLAUDE.md "Mandatory Phase Completion Steps" + "Obsidian Vault Integration" sections (canonical procedure)
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/ — list one existing Phase-NN-Name.md to mirror frontmatter style
  </read_first>
  <action>
    Step A — docs/UAT-SERIES.md update:
    - Append a Phase 999.83 entry to the relevant chaos-lab UAT series (likely UAT-1; identify the right one during read_first).
    - Entry content (one short subsection): heading "Phase 999.83 — Chaos Lab Service Config Drift"; bullet list of the four bug fixes (gitea root-user, minio KMS, vault legacy storage removal, mysql 8.0 pin) with the same one-line summaries used in the SUMMARY files; one line on the new Image Pin Policy in README.
    - Update `**Last Updated:**` line at the top of the file to `2026-05-15`.

    Step B — Sync UAT-SERIES.md to Obsidian per CLAUDE.md (file too large for CLI content= arg — use printf+cat+cp filesystem pattern):
    - `printf "---\nproject: QU.I.R.K.\ntype: reference\nstatus: active\nsource: docs/UAT-SERIES.md\nupdated: 2026-05-15\n---\n\n" > /tmp/uat_vault.md`
    - `cat docs/UAT-SERIES.md >> /tmp/uat_vault.md`
    - `cp /tmp/uat_vault.md /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md`

    Step C — Create Obsidian Phase note per CLAUDE.md (filesystem write, not CLI content=):
    - Path: `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-999.83-Chaos-Lab-Service-Config-Drift.md`
    - Frontmatter: `project: QU.I.R.K.`, `type: phase`, `status: complete`, `source: .planning/phases/999.83-chaos-lab-service-config-drift/`, `updated: 2026-05-15`
    - Body sections per CLAUDE.md template: Goal (from ROADMAP), Requirements Covered (BACK-90), Success Criteria (the 6 from ROADMAP), What Was Built (one subsection per Plan 01-05, sourced from each SUMMARY.md), `[[Roadmap]]` link.

    Step D — Single commit for the UAT-SERIES.md update via the project's commit tooling:
    - `node "/Users/digs/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(phase-999.83): update UAT-SERIES.md" --files docs/UAT-SERIES.md`

    The Obsidian vault files (Phase note + UAT-Series sync) are NOT committed to git — they live outside the repo per CLAUDE.md.

    Do not modify CLAUDE.md or other project files in this task.
  </action>
  <verify>
    <automated>cd /Volumes/Digs-1TB/Development/quantum-apps/QUIRK && grep -c "999.83" docs/UAT-SERIES.md && test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-999.83-Chaos-Lab-Service-Config-Drift.md" && echo VAULT_NOTE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "999.83" docs/UAT-SERIES.md` >= 1
    - `grep -c "Last Updated.*2026-05-15" docs/UAT-SERIES.md` >= 1
    - `test -f /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-999.83-Chaos-Lab-Service-Config-Drift.md` (exists)
    - `test -f /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md` (exists with updated content)
    - `grep -c "status: complete" /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-999.83-Chaos-Lab-Service-Config-Drift.md` == 1
    - `git log -1 --format=%s docs/UAT-SERIES.md` matches `docs(phase-999.83): update UAT-SERIES.md`
  </acceptance_criteria>
  <done>
    UAT-SERIES.md updated and committed; vault notes (Phase + UAT-Series) written to filesystem; CLAUDE.md mandatory phase completion steps satisfied.
  </done>
</task>

</tasks>

<verification>
- README.md row 33 deleted; Image Pin Policy subsection added; storage-s3 / database / source / vault rows intact
- expected_results_v4.md ## Profile: storage section deleted; storage-s3 / vault / source / database / email sections intact
- `./lab.sh down && ./lab.sh all` produces zero Exited(1/2) and zero unhealthy after 60s
- docs/UAT-SERIES.md updated and committed
- Obsidian vault: Phase-999.83 note + UAT-Series sync written
</verification>

<success_criteria>
- ROADMAP success criterion 5 satisfied (oracle entries for source / storage-s3 / database unchanged; storage section deleted with rationale captured in commit message + this plan)
- ROADMAP success criterion 6 satisfied (lab.sh down && lab.sh all clean on macOS)
- CLAUDE.md "Chaos Lab Maintenance" rule satisfied (compose + README + oracle changed together across plans 01-05)
- CLAUDE.md "Mandatory Phase Completion Steps" 1-4 satisfied (Obsidian Phase note, UAT-SERIES update, Obsidian UAT-Series sync, UAT-SERIES commit)
</success_criteria>

<output>
Create `.planning/phases/999.83-chaos-lab-service-config-drift/999.83-05-SUMMARY.md` documenting: README + oracle edits applied, UAT pass evidence (paste the `docker compose ps` summary line), UAT-SERIES.md commit hash, Obsidian vault note paths, and a brief note on the `lab.sh down` workaround used (so a future phase can pick up the follow-up cleanly).
</output>
