---
phase: 06-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/configuration.md
autonomous: true
requirements:
  - DOC-03
must_haves:
  truths:
    - "Every top-level key in config.yaml is documented with its type, default value, and description"
    - "The CLI flag reference covers all quirk scan flags and all quirk serve flags"
    - "Scan profile differences (quick/standard/deep) are explained"
    - "Required vs optional fields are clearly distinguished"
  artifacts:
    - path: "docs/configuration.md"
      provides: "Complete config.yaml and CLI flag reference"
      contains: "assessment"
  key_links:
    - from: "docs/configuration.md"
      to: "config.yaml"
      via: "documented key names match exactly"
      pattern: "tls_enum_mode"
---

<objective>
Write the complete configuration reference for config.yaml and all CLI flags.

Purpose: A power user or consultant who needs to tune a scan — change concurrency, target a CIDR, enable a connector, or use a scan profile — must be able to answer every question from this document without reading source code.

Output:
- docs/configuration.md — complete reference for all config.yaml keys, scan profiles, and CLI flags
</objective>

<execution_context>
@/Users/digs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/digs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-documentation/06-CONTEXT.md
@.planning/phases/06-documentation/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/configuration.md</name>
  <files>docs/configuration.md</files>
  <read_first>
    - config.yaml (canonical config file — all top-level keys and defaults)
    - quirk/config.py (ConnectorsCfg dataclass — connector fields and types)
    - run_scan.py (argparse definitions — all CLI flags with defaults)
    - .planning/phases/06-documentation/06-RESEARCH.md (Complete CLI Flag Reference and Complete config.yaml Key Reference sections — use these verbatim)
  </read_first>
  <action>
Create docs/configuration.md. Use the verified data from RESEARCH.md verbatim — do not re-derive values by reading source files during writing.

**Document structure:**

### 1. Overview section
Brief intro: config.yaml lives in the working directory. Run `quirk --config /path/to/config.yaml` to use a non-default location. The file has 6 top-level blocks: `assessment`, `scan`, `targets`, `connectors`, `output`, `intelligence`.

### 2. Assessment Block (required)

| Key | Type | Example | Required | Description |
|-----|------|---------|----------|-------------|
| `name` | string | `"Quantum Crypto Readiness - ACME Corp"` | Yes | Assessment name — appears in all report headers |
| `data_classification` | string | `"confidential"` | Yes | One of: `public`, `internal`, `confidential`, `regulated` |
| `report_owner` | string | `"ACME Corp"` | Yes | Client name as it appears in the report |
| `timezone` | string | `"America/New_York"` | Yes | IANA timezone for report timestamps (e.g. `"Europe/London"`, `"UTC"`) |

### 3. Scan Block

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| `timeout_seconds` | int | `5` | Global connection timeout in seconds |
| `concurrency` | int | `200` | Maximum parallel workers (global cap) |
| `ports_tls` | list[int] | `[443,8443,9443,10443,11443,12443,8444,8000,2222,5555]` | Ports probed for TLS/HTTP/SSH |
| `include_sni` | bool | `true` | Send SNI extension in TLS handshakes |
| `tls_enum_mode` | string | `"fast"` | TLS enumeration depth: `off`, `fast`, `deep` |
| `fingerprint_timeout_seconds` | int | `2` | Per-target fingerprint timeout |
| `fingerprint_concurrency` | int | `200` | Fingerprint phase worker count |
| `tls_timeout_seconds` | int | `5` | TLS scan phase connection timeout |
| `tls_concurrency` | int | `150` | TLS scan phase worker count |
| `ssh_timeout_seconds` | int | `5` | SSH scan phase connection timeout |
| `ssh_concurrency` | int | `100` | SSH scan phase worker count |

Include a note: "For large scans (1000+ hosts), reduce `concurrency` to 50 and use `--safe-mode` to prevent connection exhaustion."

### 4. Targets Block

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| `fqdns` | list[string] | `[]` | Fully qualified domain names to include in the scan |
| `cidrs` | list[string] | `[127.0.0.1]` | CIDR ranges or single IP addresses |
| `include_ips` | list[string] | `[]` | Additional IPs to append to CIDR results |
| `exclude_ips` | list[string] | `[]` | IPs to exclude from scanning |

Example showing typical client config:
```yaml
targets:
  fqdns:
    - api.acme.com
    - auth.acme.com
  cidrs:
    - 10.0.0.0/24
    - 192.168.1.0/28
  exclude_ips:
    - 10.0.0.1   # router — no services
```

### 5. Connectors Block

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| `enable_aws` | bool | `false` | Enable AWS cloud connector (ACM, KMS, CloudFront, ELBv2) |
| `enable_azure` | bool | `false` | Enable Azure cloud connector (Key Vault, App Gateway) |
| `enable_windows_adcs` | bool | `false` | Windows AD CS connector (stub — not implemented in v3.9) |
| `enable_jwt` | bool | `false` | Enable JWT/REST API scanner |
| `enable_container` | bool | `false` | Enable container/binary crypto scanner |
| `enable_source` | bool | `false` | Enable source code scanner |
| `aws_region` | string | `"us-east-1"` | AWS region for cloud connector |
| `aws_profile` | string | `null` | AWS named profile; `null` uses the default credential chain |
| `azure_subscription_id` | string | `null` | Azure subscription UUID |
| `azure_keyvault_urls` | list[string] | `[]` | Key Vault base URLs (e.g. `https://myvault.vault.azure.net`) |
| `jwt_targets` | list[string] | `[]` | REST endpoint URLs for JWT scanner |
| `container_targets` | list[string] | `[]` | Docker image refs for container scanner |
| `source_targets` | list[string] | `[]` | Git repo paths or URLs for source scanner |

Add a note: "See [Connector Guides](connectors/) for per-connector credential setup and least-privilege templates."

### 6. Output Block

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| `directory` | string | `"output"` | Directory for reports, CBOM files, and logs |
| `db_path` | string | `"output/quirk.db"` | SQLite database path for scan results |

### 7. Intelligence Block

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| `intelligence_version` | string | `"3.9.0"` | Intelligence layer version tag (informational) |
| `profile` | string | `"balanced"` | Score calibration profile: `lenient`, `balanced`, `strict` |
| `calibration_overrides` | dict | `{}` | Per-weight scoring overrides for advanced users |

Add a note explaining the three profiles:
- `lenient` — reduces penalty weights; suitable for immature environments where score shock would be counterproductive
- `balanced` — default; production-calibrated weights from RESEARCH.md
- `strict` — increases penalty weights; suitable for high-compliance environments (FedRAMP, CNSA 2.0)

### 8. Scan Profiles (--profile flag)

Explain what quick/standard/deep changes:

| Profile | Timeout | TLS enum mode | Use case |
|---------|---------|---------------|----------|
| `quick` | 2s | `off` | Discovery pass — find live hosts, no deep enum |
| `standard` | 5s | `fast` | Default — balances speed and depth |
| `deep` | 10s | `deep` | Full enumeration — slow, most thorough |

### 9. CLI Flag Reference

Two sections:

**`quirk` (scan) flags:**

| Flag | Default | Description |
|------|---------|-------------|
| `--config PATH` | (interactive) | Path to config.yaml; skips interactive prompts when provided |
| `--profile` | `standard` | Scan profile: `quick`, `standard`, `deep` |
| `--score-profile` | `balanced` | Scoring calibration: `lenient`, `balanced`, `strict` |
| `--verbose` | `false` | Verbose output during scan |
| `--progress` | `false` | Show tqdm progress bars |
| `--discovery` | `builtin` | Discovery mode: `builtin` or `nmap` |
| `--nmap-path` | `nmap` | Path to nmap executable |
| `--nmap-timeout` | `1800` | Nmap discovery timeout in seconds |
| `--nmap-extra-args` | `""` | Extra nmap arguments (pass as quoted string) |
| `--safe-mode` | `false` | Reduce concurrency and increase timeouts for fragile networks |
| `--rate-limit` | `0.0` | Targets per second rate limit (0 = disabled) |
| `--cache` | `false` | Enable discovery/fingerprint result cache |
| `--cache-ttl-hours` | `24` | Cache time-to-live in hours |
| `--resume` | `false` | Reuse cache if valid (skip re-discovery) |
| `--force-discovery` | `false` | Ignore existing discovery cache and re-run |

**`quirk serve` flags:**

| Flag | Default | Description |
|------|---------|-------------|
| `--port` | `8512` | Port to serve the dashboard on |
| `--host` | `127.0.0.1` | Host address to bind |
| `--no-open` | `false` | Suppress auto-opening of browser on startup |

### 10. Minimal Valid Config

Provide a copy-pasteable minimal config that works for a first scan:

```yaml
assessment:
  name: "Quantum Crypto Readiness - CLIENT NAME"
  data_classification: "confidential"
  report_owner: "CLIENT NAME"
  timezone: "America/New_York"

targets:
  cidrs: [127.0.0.1]
```

All other keys use their defaults.
  </action>
  <verify>
    <automated>test -f docs/configuration.md &amp;&amp; grep -q "tls_enum_mode" docs/configuration.md &amp;&amp; grep -q "quirk serve" docs/configuration.md &amp;&amp; grep -q "calibration_overrides" docs/configuration.md &amp;&amp; echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - docs/configuration.md exists
    - docs/configuration.md contains "assessment" as a section heading
    - docs/configuration.md contains "tls_enum_mode" (scan block key)
    - docs/configuration.md contains "calibration_overrides" (intelligence block key)
    - docs/configuration.md contains "quirk serve" (serve flags section)
    - docs/configuration.md contains "--safe-mode" (CLI flag)
    - docs/configuration.md contains "quick" and "standard" and "deep" (scan profiles)
    - docs/configuration.md contains "lenient" and "balanced" and "strict" (score profiles)
    - docs/configuration.md contains "connectors/" link to connector guides
  </acceptance_criteria>
  <done>docs/configuration.md is a complete reference. Every key in config.yaml is documented. All CLI flags for quirk and quirk serve are listed. Scan profiles and score profiles are explained. A minimal valid config snippet is included.</done>
</task>

</tasks>

<verification>
- `test -f docs/configuration.md` exits 0
- `grep -c "tls_enum_mode\|calibration_overrides\|quirk serve\|--safe-mode" docs/configuration.md` returns 4
- All 6 config.yaml top-level blocks (assessment, scan, targets, connectors, output, intelligence) appear as sections
</verification>

<success_criteria>
A consultant who wants to configure a real client scan can answer every question — port ranges, concurrency tuning, connector activation, scoring profile selection — from docs/configuration.md without reading config.yaml or run_scan.py source.
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation/06-02-SUMMARY.md` per the summary template.
</output>
