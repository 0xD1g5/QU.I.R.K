---
phase: 06-documentation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/chaos-lab.md
  - quantum-chaos-enterprise-lab/README.md
autonomous: true
requirements:
  - DOC-07
must_haves:
  truths:
    - "An operator can start any lab profile by copying the command from the guide"
    - "All 10 profiles (core + phaseA + cloud + identity + pki + jwt + registry + source + storage + ssh-weak + ldaps) are documented"
    - "The port matrix is included inline so operators know what findings to expect per port"
    - "quantum-chaos-enterprise-lab/README.md links to docs/chaos-lab.md as the authoritative reference"
  artifacts:
    - path: "docs/chaos-lab.md"
      provides: "Complete chaos lab operator guide — all profiles, port matrix, expected findings"
      contains: "20001"
    - path: "quantum-chaos-enterprise-lab/README.md"
      provides: "Updated lab README linking to docs/chaos-lab.md"
      contains: "docs/chaos-lab.md"
  key_links:
    - from: "quantum-chaos-enterprise-lab/README.md"
      to: "docs/chaos-lab.md"
      via: "markdown link"
      pattern: "docs/chaos-lab.md"
    - from: "docs/chaos-lab.md"
      to: "quantum-chaos-enterprise-lab/docker-compose.yml"
      via: "documented port numbers match docker-compose.yml service definitions"
      pattern: "20001"
---

<objective>
Write the chaos lab operator guide and update the lab README to point to it.

Purpose: The chaos lab is a critical validation tool for QU.I.R.K. — operators use it to verify scanner behavior and train new team members. With 10 profiles across two waves of development (pre-Phase 4 and Phase 4), the current CHAOS_LAB_BUILD_AND_OPERATIONS file is incomplete. The new guide must cover every profile with copy-pasteable commands and an inline port matrix.

Output:
- docs/chaos-lab.md — authoritative operator guide covering all 10 profiles + port matrix
- quantum-chaos-enterprise-lab/README.md — updated to link to docs/chaos-lab.md (per D-15)
</objective>

<execution_context>
@/Users/digs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/digs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-documentation/06-CONTEXT.md
@.planning/phases/06-documentation/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/chaos-lab.md</name>
  <files>docs/chaos-lab.md</files>
  <read_first>
    - quantum-chaos-enterprise-lab/docker-compose.yml (all service definitions, profiles, exact port mappings)
    - quantum-chaos-enterprise-lab/expected_results_v3.md (per-port expected findings oracle)
    - quantum-chaos-enterprise-lab/CHAOS_LAB_BUILD_AND_OPERATIONS_text_only.md (historical context for narrative details)
    - .planning/phases/06-documentation/06-RESEARCH.md (Chaos Lab Complete Port Matrix section — use verbatim, DO NOT re-derive ports from docker-compose.yml)
    - .planning/phases/06-documentation/06-CONTEXT.md (decisions D-14, D-15, D-16)
  </read_first>
  <action>
Write docs/chaos-lab.md. Use the verified port matrix from RESEARCH.md verbatim — it has been extracted and cross-checked against docker-compose.yml. Do not re-derive port numbers during writing.

Key data points to use exactly as documented in RESEARCH.md:
- Vault port is 20009 (NOT 20008 — this is a known pitfall documented in RESEARCH.md)
- postgres-pgcrypto port is 20010
- ssh-weak port is 20022 (ubuntu:18.04 OpenSSH 7.6p1)
- ldaps port is 636 (standard LDAPS)
- registry uses port 20005
- source (Gitea) uses port 20006

**Document structure:**

### 1. Overview

Brief intro explaining the chaos lab: a Docker Compose environment that simulates a realistic enterprise with deliberate cryptographic vulnerabilities. Purpose: verify QU.I.R.K. scanner behavior, reproduce known findings, train new team members, and validate scanner changes without targeting production infrastructure.

**Prerequisites:**
- Docker Desktop (macOS/Windows) or Docker Engine + Docker Compose plugin (Linux)
- Docker socket accessible at `/var/run/docker.sock`
- `./lab.sh` script in the lab directory (wraps docker compose with standard project name)

### 2. Quick Start

```bash
cd quantum-chaos-enterprise-lab

# Start core services (always-on baseline)
./lab.sh up

# Start a specific profile
PROFILE_ARGS="--profile jwt" ./lab.sh up

# Start all Phase 4 profiles
PROFILE_ARGS="--profile jwt --profile registry --profile source --profile storage --profile ssh-weak --profile ldaps" ./lab.sh up

# Stop everything
./lab.sh down
```

### 3. Profile Reference

One subsection per profile. Each subsection has: start command, port matrix, expected findings, teardown.

#### 3.1 Core Profile (always-on)

No `--profile` flag required. Started by default with `./lab.sh up`.

| Port | Service | Expected Protocol | Finding Tag |
|------|---------|------------------|------------|
| 443 | tls-modern | TLS | MODERN_TLS |
| 8443 | tls-legacy | TLS | LEGACY_TLS |
| 9443 | tls-expired | TLS | CERT_EXPIRED_OR_EXPIRING |
| 10443 | tls-selfsigned | TLS | CERT_SELFSIGNED |
| 11443 | tls-mtls-required | TLS | MTLS_REQUIRED |
| 12443 | tls-slow-proxy | TLS | TLS_SLOW_PROXY |
| 8444 | http-on-8444 | HTTP | HTTP_ON_TLS_LIKE_PORT |
| 8000 | legacy-http | HTTP | PLAINTEXT_HTTP |
| 2222 | ssh-alt | SSH | SSH_BANNER |
| 5555 | unknown-port | UNKNOWN | UNKNOWN_OPEN_PORT |

**Start:** `./lab.sh up`

Run QU.I.R.K. against the core lab:
```yaml
# config-lab-core.yaml
assessment:
  name: "Chaos Lab - Core"
  data_classification: "internal"
  report_owner: "Lab"
  timezone: "UTC"
targets:
  cidrs: [127.0.0.1]
```
```bash
quirk --config config-lab-core.yaml
```



#### 3.2 phaseA Profile

| Port | Service | Expected Protocol | Finding Tag |
|------|---------|------------------|------------|
| 15001 | tls-altport | TLS | TLS_ON_ODD_PORT |
| 18000 | http-redirect | HTTP | HTTP_REDIRECT_302 |
| 5556 | unknown-port-2 | UNKNOWN | UNKNOWN_OPEN_PORT_2 |
| 15432 | postgres-plain | UNKNOWN | DB_PLAINTEXT_POSTGRES |
| 16379 | redis-plain | UNKNOWN | DB_PLAINTEXT_REDIS |
| 15672 | rabbitmq-mgmt | HTTP | RABBITMQ_MGMT_HTTP |
| 13443 | tls-missing-intermediate | TLS | CERT_CHAIN_INCOMPLETE |
| 14443 | tls-rsa1024 | TLS | CERT_RSA_1024 |
| 15443 | tls-sha1 | TLS | CERT_SHA1_SIG |
| 24443 | ingress-sni | TLS | INGRESS_SNI |

**Start:** `PROFILE_ARGS="--profile phaseA" ./lab.sh up`



#### 3.3 cloud Profile

| Port | Service | Expected Protocol | Finding Tag |
|------|---------|------------------|------------|
| 24566 | localstack-tls | TLS | CLOUD_AWS_LOCALSTACK_TLS |
| 21000 | azurite-blob-tls | TLS | CLOUD_AZURITE_BLOB_TLS |
| 21001 | azurite-queue-tls | TLS | CLOUD_AZURITE_QUEUE_TLS |
| 21002 | azurite-table-tls | TLS | CLOUD_AZURITE_TABLE_TLS |

**Start:** `PROFILE_ARGS="--profile cloud" ./lab.sh up`



#### 3.4 identity Profile

| Port | Service | Expected Protocol | Finding Tag |
|------|---------|------------------|------------|
| 15449 | keycloak-tls | TLS | IDP_TLS |
| 19000 | step-ca | TLS | PRIVATE_CA_TLS |
| 13890 | openldap | UNKNOWN | LDAP_TCP |
| 18082 | phpldapadmin | HTTP | LDAP_ADMIN_HTTP |
| 16443 | mtls-gateway | TLS | MTLS_REQUIRED |

**Start:** `PROFILE_ARGS="--profile identity" ./lab.sh up`



#### 3.5 pki Profile

| Port | Service | Expected Protocol | Finding Tag |
|------|---------|------------------|------------|
| 17443 | mtls-stepca-gateway | TLS | MTLS_STEPCA |

**Start:** `PROFILE_ARGS="--profile pki" ./lab.sh up`

> **Note:** The pki profile depends on the identity profile (`step-ca` and `whoami` services). Start identity first, or start both together.



#### 3.6 jwt Profile (Phase 4 — LAB-01)

| Port | Service | Algorithm | Expected Finding | Key Size |
|------|---------|-----------|-----------------|----------|
| 20001 | jwt-rs256 | RS256 (RSA) | quantum-vulnerable | 2048-bit |
| 20002 | jwt-hs256 | HS256 (HMAC-SHA256) | WEAK_KEY_SIZE | 128-bit (16-byte key) |
| 20003 | jwt-rsa1024 | RS256 (RSA) | WEAK_KEY_SIZE + quantum-vulnerable | 1024-bit |
| 20004 | jwt-algnone | none | CRITICAL_NO_SIGNATURE | 0 bits |

**Start:** `PROFILE_ARGS="--profile jwt" ./lab.sh up`

config.yaml to scan JWT targets:
```yaml
connectors:
  enable_jwt: true
  jwt_targets:
    - "http://localhost:20001/token"
    - "http://localhost:20002/token"
    - "http://localhost:20003/token"
    - "http://localhost:20004/token"
```



#### 3.7 registry Profile (Phase 4 — LAB-02)

| Port | Service | Content | Expected Finding |
|------|---------|---------|-----------------|
| 20005 | Docker Registry v2 | image-old-libssl (openssl 1.0.2n) | OUTDATED_CRYPTO_LIB |
| 20005 | Docker Registry v2 | image-old-pycrypto (cryptography 2.9.2) | OUTDATED_CRYPTO_LIB |
| 20005 | Docker Registry v2 | image-mixed (both old packages) | OUTDATED_CRYPTO_LIB |

**Start:** `PROFILE_ARGS="--profile registry" ./lab.sh up`

> **Note:** The `registry-seed` container seeds test images on startup. Docker socket must be accessible from within the container (`/var/run/docker.sock` mount). Allow 30–60 seconds for seeding to complete.

config.yaml to scan registry images:
```yaml
connectors:
  enable_container: true
  container_targets:
    - "localhost:20005/image-old-libssl:latest"
    - "localhost:20005/image-old-pycrypto:latest"
    - "localhost:20005/image-mixed:latest"
```



#### 3.8 source Profile (Phase 4 — LAB-03)

| Port | Service | Seeded Repos | Expected Finding |
|------|---------|-------------|-----------------|
| 20006 | Gitea | crypto-antipatterns-python | Weak algorithm (hashlib.md5), hardcoded key, weak random |
| 20006 | Gitea | crypto-antipatterns-go | Weak algorithm (md5.Sum), deprecated TLS config |
| 20006 | Gitea | crypto-antipatterns-java | RC4 usage, static IV, deprecated cipher |

**Start:** `PROFILE_ARGS="--profile source" ./lab.sh up`

Gitea admin credentials: username `admin`, password `admin123`

config.yaml to scan Gitea repos:
```yaml
connectors:
  enable_source: true
  source_targets:
    - "http://admin:admin123@localhost:20006/admin/crypto-antipatterns-python"
    - "http://admin:admin123@localhost:20006/admin/crypto-antipatterns-go"
    - "http://admin:admin123@localhost:20006/admin/crypto-antipatterns-java"
```



#### 3.9 storage Profile (Phase 4 — LAB-04)

| Port | Service | Resource | Expected Finding |
|------|---------|---------|-----------------|
| 20007 | LocalStack KMS | SYMMETRIC_DEFAULT key | AES_256 quantum-vulnerable |
| 20007 | LocalStack KMS | RSA_2048 key | RSA_2048 quantum-vulnerable |
| 20007 | LocalStack KMS | ECC_NIST_P256 key | ECC_P256 quantum-vulnerable |
| 20009 | HashiCorp Vault | transit/keys/rsa-2048 | RSA_2048 quantum-vulnerable |
| 20009 | HashiCorp Vault | transit/keys/rsa-1024 | RSA_1024 weak + quantum-vulnerable |
| 20009 | HashiCorp Vault | transit/keys/aes256 | AES_256 quantum-vulnerable |
| 20010 | postgres-pgcrypto | encrypted_demo table | pgp_sym_encrypt weak passphrase |

> **Note:** Vault port is **20009** (maps to internal port 8200). LocalStack KMS is on port **20007** (independent instance from the cloud profile LocalStack on 24566).

Credentials:
- Vault root token: `root`
- LocalStack: access key `test`, secret key `test`
- PostgreSQL: default `postgres` superuser

**Start:** `PROFILE_ARGS="--profile storage" ./lab.sh up`



#### 3.10 ssh-weak Profile (Phase 4 — LAB-05)

| Port | Service | Expected Protocol | Finding Tag |
|------|---------|------------------|------------|
| 20022 | OpenSSH 7.6p1 (ubuntu:18.04) | SSH | SSH_WEAK_ALGORITHMS |

This service intentionally runs an older OpenSSH version (7.6p1 from Ubuntu 18.04) that supports legacy KEX algorithms (`diffie-hellman-group1-sha1`, `diffie-hellman-group14-sha1`), weak host key types, and deprecated MAC algorithms.

**Start:** `PROFILE_ARGS="--profile ssh-weak" ./lab.sh up`

Add port 20022 to your scan config:
```yaml
scan:
  ports_tls: [443, 8443, 9443, 10443, 11443, 12443, 8444, 8000, 2222, 5555, 20022]
```



#### 3.11 ldaps Profile (Phase 4 — LAB-06)

| Port | Service | Expected Protocol | Finding Tag |
|------|---------|------------------|------------|
| 636 | OpenLDAP over TLS (osixia/openldap:1.5.0) | TLS | LDAPS_TLS |

Standard LDAPS port (636). QU.I.R.K. scans this using sslyze — add port 636 to `ports_tls` to include it in a scan.

**Start:** `PROFILE_ARGS="--profile ldaps" ./lab.sh up`

Add port 636 to your scan config:
```yaml
scan:
  ports_tls: [443, 8443, 9443, 10443, 11443, 12443, 8444, 8000, 2222, 5555, 636]
```



### 4. Starting Multiple Profiles

```bash
PROFILE_ARGS="--profile jwt --profile registry --profile source --profile storage --profile ssh-weak --profile ldaps" ./lab.sh up
```

All Phase 4 profiles can run simultaneously. Note port overlap: the storage profile uses a dedicated LocalStack instance on port 20007 — this is separate from the cloud profile LocalStack on port 24566.

### 5. Complete Port Reference

Quick-reference table for all lab ports:

| Port | Service | Profile | Expected Finding |
|------|---------|---------|-----------------|
| 443 | tls-modern | core | MODERN_TLS |
| 636 | ldaps | ldaps | LDAPS_TLS |
| 2222 | ssh-alt | core | SSH_BANNER |
| 5555 | unknown-port | core | UNKNOWN_OPEN_PORT |
| 5556 | unknown-port-2 | phaseA | UNKNOWN_OPEN_PORT_2 |
| 8000 | legacy-http | core | PLAINTEXT_HTTP |
| 8443 | tls-legacy | core | LEGACY_TLS |
| 8444 | http-on-8444 | core | HTTP_ON_TLS_LIKE_PORT |
| 9443 | tls-expired | core | CERT_EXPIRED_OR_EXPIRING |
| 10443 | tls-selfsigned | core | CERT_SELFSIGNED |
| 11443 | tls-mtls-required | core | MTLS_REQUIRED |
| 12443 | tls-slow-proxy | core | TLS_SLOW_PROXY |
| 13443 | tls-missing-intermediate | phaseA | CERT_CHAIN_INCOMPLETE |
| 13890 | openldap | identity | LDAP_TCP |
| 14443 | tls-rsa1024 | phaseA | CERT_RSA_1024 |
| 15001 | tls-altport | phaseA | TLS_ON_ODD_PORT |
| 15443 | tls-sha1 | phaseA | CERT_SHA1_SIG |
| 15449 | keycloak-tls | identity | IDP_TLS |
| 15432 | postgres-plain | phaseA | DB_PLAINTEXT_POSTGRES |
| 15672 | rabbitmq-mgmt | phaseA | RABBITMQ_MGMT_HTTP |
| 16379 | redis-plain | phaseA | DB_PLAINTEXT_REDIS |
| 16443 | mtls-gateway | identity | MTLS_REQUIRED |
| 17443 | mtls-stepca-gateway | pki | MTLS_STEPCA |
| 18000 | http-redirect | phaseA | HTTP_REDIRECT_302 |
| 18082 | phpldapadmin | identity | LDAP_ADMIN_HTTP |
| 19000 | step-ca | identity | PRIVATE_CA_TLS |
| 20001 | jwt-rs256 | jwt | JWT quantum-vulnerable |
| 20002 | jwt-hs256 | jwt | JWT WEAK_KEY_SIZE |
| 20003 | jwt-rsa1024 | jwt | JWT WEAK_KEY_SIZE |
| 20004 | jwt-algnone | jwt | JWT CRITICAL_NO_SIGNATURE |
| 20005 | registry | registry | OUTDATED_CRYPTO_LIB |
| 20006 | gitea | source | WEAK_ALGORITHM/HARDCODED_KEY |
| 20007 | localstack-kms | storage | AWS KMS crypto inventory |
| 20009 | vault | storage | HashiCorp Vault crypto inventory |
| 20010 | postgres-pgcrypto | storage | pgcrypto weak passphrase |
| 20022 | ssh-weak | ssh-weak | SSH_WEAK_ALGORITHMS |
| 21000 | azurite-blob | cloud | CLOUD_AZURITE_BLOB_TLS |
| 21001 | azurite-queue | cloud | CLOUD_AZURITE_QUEUE_TLS |
| 21002 | azurite-table | cloud | CLOUD_AZURITE_TABLE_TLS |
| 24443 | ingress-sni | phaseA | INGRESS_SNI |
| 24566 | localstack-cloud | cloud | CLOUD_AWS_LOCALSTACK_TLS |

### 6. Troubleshooting

**Services not starting:**
```bash
./lab.sh logs [service-name]   # Check service logs
./lab.sh ps                    # Check container status
```

**Port conflicts:**
If ports conflict with local services, check `docker-compose.yml` host port mappings. All lab ports are in non-standard ranges (20000+) to minimize conflicts with typical development environments.

**registry-seed failures:**
The seed container needs Docker socket access. Confirm `/var/run/docker.sock` is mounted and your user has Docker group membership.

**Gitea seed timing:**
The source profile `gitea-seed` waits for Gitea to be healthy before seeding. Allow 30–60 seconds on first start. If repos are empty, check `gitea-seed` container logs.

### 7. Historical Reference

The existing `CHAOS_LAB_BUILD_AND_OPERATIONS_text_only.md` in the lab directory is retained as a historical artifact documenting the original lab build decisions. This guide (`docs/chaos-lab.md`) is the authoritative operator reference going forward.
  </action>
  <verify>
    <automated>test -f docs/chaos-lab.md &amp;&amp; grep -q "20001" docs/chaos-lab.md &amp;&amp; grep -q "20009" docs/chaos-lab.md &amp;&amp; grep -q "ldaps" docs/chaos-lab.md &amp;&amp; grep -q "ssh-weak" docs/chaos-lab.md &amp;&amp; echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - docs/chaos-lab.md exists
    - docs/chaos-lab.md contains "20001" (jwt-rs256 port)
    - docs/chaos-lab.md contains "20009" (Vault port — NOT 20008)
    - docs/chaos-lab.md contains "20010" (postgres-pgcrypto port)
    - docs/chaos-lab.md contains "20022" (ssh-weak port)
    - docs/chaos-lab.md contains "636" (ldaps port)
    - docs/chaos-lab.md contains "ldaps" as a profile name
    - docs/chaos-lab.md contains "ssh-weak" as a profile name
    - docs/chaos-lab.md contains "jwt" and "registry" and "source" and "storage" as profile names
    - docs/chaos-lab.md contains all 10 profile names: core, phaseA, cloud, identity, pki, jwt, registry, source, storage, ssh-weak/ldaps
    - docs/chaos-lab.md does NOT contain "20008" as the Vault port (common pitfall per RESEARCH.md)
  </acceptance_criteria>
  <done>docs/chaos-lab.md documents all 10 profiles with copy-pasteable start commands, port matrices, expected findings, and config.yaml snippets for connector-based profiles. The complete port reference table lists all lab ports in a single summary. Vault port is correctly documented as 20009.</done>
</task>

<task type="auto">
  <name>Task 2: Update quantum-chaos-enterprise-lab/README.md</name>
  <files>quantum-chaos-enterprise-lab/README.md</files>
  <read_first>
    - quantum-chaos-enterprise-lab/README.md (current content to preserve what's useful and update the Documentation link)
    - .planning/phases/06-documentation/06-CONTEXT.md (decision D-15)
  </read_first>
  <action>
Update quantum-chaos-enterprise-lab/README.md to point to docs/chaos-lab.md as the authoritative reference per D-15.

Read the current README first to understand its structure. Keep any useful content (Quick Start section, lab.sh commands) but update the Documentation section to link to docs/chaos-lab.md.

Changes to make:
1. Update the "Documentation" section (currently links to `CHAOS_LAB_BUILD_AND_OPERATIONS.md`) to instead link to `../docs/chaos-lab.md`
2. Add a note explaining the historical artifact:
   ```markdown
   ## Documentation

   The complete operator guide is at **[docs/chaos-lab.md](../docs/chaos-lab.md)** — covers all profiles including Phase 4 additions (jwt, registry, source, storage, ssh-weak, ldaps).

   > **Historical artifact:** `CHAOS_LAB_BUILD_AND_OPERATIONS_text_only.md` in this directory is retained for reference but is no longer updated. The docs/chaos-lab.md guide is the authoritative reference.
   ```
3. Keep the existing Quick Start section and any lab.sh script documentation that is present
4. Do NOT remove the historical artifact file itself — only update the README to redirect to the new guide
  </action>
  <verify>
    <automated>grep -q "docs/chaos-lab.md" quantum-chaos-enterprise-lab/README.md &amp;&amp; echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - quantum-chaos-enterprise-lab/README.md contains "docs/chaos-lab.md" (link to new authoritative guide)
    - quantum-chaos-enterprise-lab/README.md contains "historical" or "Historical" (acknowledging the old file)
    - quantum-chaos-enterprise-lab/CHAOS_LAB_BUILD_AND_OPERATIONS_text_only.md still exists (not deleted)
  </acceptance_criteria>
  <done>quantum-chaos-enterprise-lab/README.md links to docs/chaos-lab.md as the authoritative operator guide. The historical CHAOS_LAB_BUILD_AND_OPERATIONS_text_only.md is retained untouched.</done>
</task>

</tasks>

<verification>
- `test -f docs/chaos-lab.md` exits 0
- `grep -q "20001" docs/chaos-lab.md` exits 0
- `grep -q "20009" docs/chaos-lab.md` exits 0 (Vault — correct port)
- `grep -v "20008" docs/chaos-lab.md | grep -c "Vault" > 0` (Vault section doesn't use wrong port)
- `grep -q "docs/chaos-lab.md" quantum-chaos-enterprise-lab/README.md` exits 0
- `test -f quantum-chaos-enterprise-lab/CHAOS_LAB_BUILD_AND_OPERATIONS_text_only.md` exits 0 (historical artifact preserved)
</verification>

<success_criteria>
An operator can open docs/chaos-lab.md and immediately find the start command for any of the 10 lab profiles. The port matrix is complete and accurate. The quantum-chaos-enterprise-lab/README.md points to docs/chaos-lab.md so consultants who navigate to the lab directory find the current guide.
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation/06-06-SUMMARY.md` per the summary template.
</output>
