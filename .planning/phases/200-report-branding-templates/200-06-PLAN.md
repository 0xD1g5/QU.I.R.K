---
phase: 200-report-branding-templates
plan: 06
type: execute
wave: 3
depends_on: ["200-01", "200-02", "200-03", "200-04", "200-05"]
files_modified:
  - .planning/backlog/999.105-customizable-reporting-engine/TIER2-GO-NO-GO.md
  - .planning/HORIZON.md
autonomous: true
requirements: [RPT-05]

must_haves:
  truths:
    - "A written go/no-go document exists for 999.105 Tier 2 (section-composition profiles) with an explicit verdict and honest sizing (D: decision doc only, no implementation)"
    - "The document explains concretely how the zero-CRITICAL congruence guard would have to be redesigned under section composition, naming writer.py:307, writer.py:927, and the actual raising guard in content_model.py"
    - "The document addresses all nine presence-based parity test files by name and sketches the profile x renderer x section matrix redesign they would need"
    - "The verdict, key constraints, and sizing also appear in the TRACKED HORIZON.md 999.105 row — because .planning/backlog/ is untracked and would otherwise lose the decision"
    - "No congruence-guard, parity-test, or renderer source file is modified by this plan"
  artifacts:
    - path: ".planning/backlog/999.105-customizable-reporting-engine/TIER2-GO-NO-GO.md"
      provides: "Long-form Tier 2 go/no-go decision with evidence, constraints, and sizing"
      min_lines: 80
    - path: ".planning/HORIZON.md"
      provides: "999.105 ledger row carrying the verdict, constraints, and sizing in tracked form"
      contains: "999.105"
  key_links:
    - from: ".planning/HORIZON.md"
      to: ".planning/backlog/999.105-customizable-reporting-engine/TIER2-GO-NO-GO.md"
      via: "row text citing the doc AND restating its substance (the doc is untracked)"
      pattern: "TIER2-GO-NO-GO"
---

<objective>
Produce the evidence-based go/no-go for 999.105 Tier 2 — section-composition report profiles —
and land its substance in the tracked ledger.

Purpose: RPT-05. Tier 2 was deferred because naive section composition breaks two load-bearing
invariants at once: the congruence guard (a CRITICAL finding with a non-capped band must be
blocked, which assumes the severity counts the guard sees are the counts every renderer shows)
and nine presence-based parity test files (which assert sections and fields are PRESENT across
renderers — a profile that omits a section flips them red wholesale). This phase just built the
Tier 1 branding/template/profile machinery, so the sizing evidence is fresh and first-hand.

Output: `TIER2-GO-NO-GO.md` plus a HORIZON.md 999.105 row update.

Hard constraints:
- DECISION DOCUMENT ONLY. Do not implement section composition, do not add a `sections:` config
  key, do not modify `writer.py`, `content_model.py`, `html_renderer.py`, `docx_renderer.py`, or
  any parity test file. Verify this with `git status --porcelain quirk/ tests/` at the end.
- `.planning/backlog/` is UNTRACKED (verified: `git ls-files .planning/backlog/` is empty). The
  HORIZON.md row must therefore CARRY the verdict, key constraints, and sizing in its own text —
  a row that merely links the doc loses the decision the moment the working copy changes machine.
- `writer.py:307` and `writer.py:927` are severity-counting/bucketing sites, NOT the raising
  guard. The raising guard is `content_model.py::_check_congruence` (:626) with public wrapper
  `assert_congruent` (:650), invoked from `build_exec_content` (:869-876). The document must state
  this correctly — the ROADMAP's shorthand cites the two writer.py lines, and repeating that
  shorthand as if those lines raise would put a wrong premise into the tracked ledger.
- Sizing is in context-cost and plan-count terms, not human time units.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/HORIZON.md
@.planning/phases/200-report-branding-templates/200-CONTEXT.md
@.planning/phases/200-report-branding-templates/200-RESEARCH.md
@.planning/phases/200-report-branding-templates/200-01-SUMMARY.md
@.planning/phases/200-report-branding-templates/200-03-SUMMARY.md
@.planning/phases/200-report-branding-templates/200-05-SUMMARY.md
@.planning/backlog/999.105-customizable-reporting-engine/IDEA.md

<interfaces>
<!-- Congruence evidence set (READ ONLY this plan) -->
quirk/reports/content_model.py:626   _check_congruence(score_band, sev_counts)   <- the raising guard
quirk/reports/content_model.py:650   assert_congruent(band, findings)            <- public wrapper
quirk/reports/content_model.py:869-876  invoked inside build_exec_content, BEFORE any file I/O
quirk/reports/writer.py:293-315      categorize_waves() — CRITICAL -> NOW bucketing (:307)
quirk/reports/writer.py:~927         crit_count feeding the Rich console summary table

<!-- The nine presence-based parity files the doc must address by name -->
tests/test_cross_surface_parity.py            tests/test_report_render_parity.py
tests/test_key_reuse_render_parity.py         tests/test_quantum_risk_render_parity.py
tests/test_report_coverage_parity.py          tests/test_score_render_parity.py
tests/test_score_parity.py                    tests/test_finding_engine_parity.py
tests/test_cli_dashboard_discovery_parity.py

<!-- Tracking status: .planning/backlog/ is untracked; .planning/HORIZON.md IS tracked -->
<!-- The 999.105 row currently reads: "Customizable reporting engine — ... three-tier shape and -->
<!-- feasibility in .planning/backlog/999.105-customizable-reporting-engine/IDEA.md" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gather first-hand Tier 2 constraint evidence</name>
  <files>.planning/backlog/999.105-customizable-reporting-engine/TIER2-GO-NO-GO.md</files>
  <read_first>
    - quirk/reports/content_model.py lines 600-680 and 860-890 (`_check_congruence`, `assert_congruent`, and the call site inside `build_exec_content`)
    - quirk/reports/writer.py lines 290-320 and 900-955 (the CRITICAL bucketing and the console `crit_count`)
    - tests/test_cross_surface_parity.py and tests/test_report_render_parity.py (module docstrings + a representative assertion from each — what "presence-based" concretely means)
    - .planning/backlog/999.105-customizable-reporting-engine/IDEA.md (the three-tier shape and the prior feasibility note this doc supersedes for Tier 2)
  </read_first>
  <action>
Collect the evidence the verdict will rest on, and write it into
`.planning/backlog/999.105-customizable-reporting-engine/TIER2-GO-NO-GO.md` as the doc's evidence
sections (the verdict itself is Task 2).

Congruence anatomy section: state precisely where the guard raises
(`content_model.py::_check_congruence`, wrapper `assert_congruent`, call site in
`build_exec_content` before any file I/O) and what the two ROADMAP-cited `writer.py` lines actually
are (severity bucketing and a console count). State the invariant in one sentence: the severity
counts the guard validates are assumed to be the counts every renderer displays. Then state exactly
how section composition breaks it — a profile that drops the findings table while the band stays
capped produces a report whose visible content no longer justifies its own band, and the guard
cannot see that because it runs on `ExecContent`, upstream of any renderer.

Parity-suite section: enumerate all nine files by name with, for each, the count of test functions
(`grep -c "^def test_"`) and a one-line statement of what it asserts the presence of. Then state
the shared failure mode: each asserts presence unconditionally, so any omitted section flips them
red regardless of whether the omission was intentional.

Tier 1 evidence section: record what this phase actually cost and learned, sourced from the
200-01/200-03/200-05 SUMMARYs — the sandbox swap's real blast radius, whether the packaged template
needed changes, how many renderer touch points branding required, and whether the additive-field
approach perturbed the parity suite at all. This is the honest sizing input: Tier 1 was additive
and did not disturb parity; Tier 2 is subtractive and does.

Every quantitative claim in this section must come from a command you ran, quoted with its output.
No estimates presented as measurements.
  </action>
  <verify>
    <automated>test -f .planning/backlog/999.105-customizable-reporting-engine/TIER2-GO-NO-GO.md && for f in cross_surface_parity report_render_parity key_reuse_render_parity quantum_risk_render_parity report_coverage_parity score_render_parity score_parity finding_engine_parity cli_dashboard_discovery_parity; do grep -q "test_$f.py" .planning/backlog/999.105-customizable-reporting-engine/TIER2-GO-NO-GO.md || echo "MISSING:$f"; done; echo EVIDENCE_CHECK_DONE</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `EVIDENCE_CHECK_DONE` with zero `MISSING:` lines — all nine parity files named
    - The doc names `content_model.py::_check_congruence` as the raising guard and explicitly corrects the `writer.py:307/:927` shorthand rather than repeating it
    - Each of the nine parity files carries a test-function count sourced from an executed `grep -c "^def test_"`, with the command quoted
    - The Tier 1 evidence section cites the three SUMMARY files by name and states whether the parity suite was perturbed by this phase (it should not have been)
    - `git status --porcelain quirk/ tests/` is empty — evidence gathering read source, it did not change it
  </acceptance_criteria>
  <done>The constraint evidence is written down, first-hand and measured, ready for a verdict to rest on.</done>
</task>

<task type="auto">
  <name>Task 2: Write the verdict and sizing, and land the substance in HORIZON.md</name>
  <files>.planning/backlog/999.105-customizable-reporting-engine/TIER2-GO-NO-GO.md, .planning/HORIZON.md</files>
  <read_first>
    - .planning/HORIZON.md lines 20-40 (the Open-Item Ledger table shape and the 999.105 row, plus how neighbouring rows carry closure substance inline rather than by reference — see the 999.104 row's style)
    - .planning/backlog/999.105-customizable-reporting-engine/TIER2-GO-NO-GO.md (the evidence sections from Task 1)
    - .planning/ROADMAP.md §"Current Milestone: v5.23" (the sequencing commitment: v5.24 is UAT Coverage Drain — the verdict must not silently claim a slot that is already committed)
  </read_first>
  <action>
Complete `TIER2-GO-NO-GO.md` with, in this order: a `**Verdict:**` line (GO, NO-GO, or
GO-WITH-CONDITIONS) as the first content after the title; a rationale paragraph tied to the
evidence sections; a "Required redesign" section proposing the concrete shape a GO would demand —
at minimum how the congruence guard becomes composition-aware (e.g. the guard receiving the
rendered section set, or a hard rule that certain sections are non-omittable when the band is
capped) and how the presence-based parity assertions become a profile x renderer x section matrix
rather than nine files of unconditional presence checks; a "Sizing" section in plan-count and
context-cost terms with the riskiest unknown named; and an "If NO-GO / conditions" section naming
what evidence would change the verdict later.

The verdict must be argued, not asserted: if the required redesign touches all nine parity files
plus the guard, say so and let the sizing reflect it. A GO that understates the parity cost is the
failure mode this document exists to prevent.

Then update the `999.105` row in `.planning/HORIZON.md`. Because `.planning/backlog/` is untracked,
the row must carry the substance itself in the style neighbouring closed rows use: the Tier 1
CLOSED status (shipped this phase — branding, sandboxed template override, path guard, profiles,
citing Phase 200 and the RPT IDs), the Tier 2 verdict verbatim, the two named constraints
(composition-aware congruence guard; nine presence-based parity files needing a matrix redesign),
the sizing, and a pointer to `TIER2-GO-NO-GO.md` as the long-form LOCAL copy. Do not delete the
row's existing history — extend it the way the 999.104 row extends.

Take a pre-image of `.planning/HORIZON.md` before editing and diff afterward to confirm only the
999.105 row changed. Do not invoke any GSD `state.*`, `phase.complete`, or `roadmap.*` verb —
this is a hand text edit.
  </action>
  <verify>
    <automated>grep -n "^\*\*Verdict:\*\* \(GO\|NO-GO\|GO-WITH-CONDITIONS\)" .planning/backlog/999.105-customizable-reporting-engine/TIER2-GO-NO-GO.md && grep -c "TIER2-GO-NO-GO" .planning/HORIZON.md && git diff --stat .planning/HORIZON.md</automated>
  </verify>
  <acceptance_criteria>
    - `TIER2-GO-NO-GO.md` has a `**Verdict:**` line matching GO / NO-GO / GO-WITH-CONDITIONS as its first content line after the title
    - It contains a "Required redesign" section addressing BOTH the congruence guard and the parity matrix, and a "Sizing" section expressed in plan counts and context cost (no human time units)
    - `.planning/HORIZON.md`'s 999.105 row contains: the verdict string, the words "congruence" and "parity", a sizing figure, the Phase 200 / RPT-01..RPT-05 citation, and the `TIER2-GO-NO-GO.md` filename
    - `git diff .planning/HORIZON.md` shows exactly one changed row and no other line altered
    - `git status --porcelain quirk/ tests/` is empty — no implementation, no test change
    - No GSD mutating verb was invoked (`git log --oneline -3` shows only hand-authored commits)
  </acceptance_criteria>
  <done>The Tier 2 decision exists, is argued from measured evidence, and survives in the tracked ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Planning documents only. No code, no inputs, no endpoints, no dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200-20 | Repudiation | The Tier 2 decision living only in an UNTRACKED `.planning/backlog/` file and being lost | mitigate | The tracked `HORIZON.md` 999.105 row carries the verdict, both named constraints, and the sizing in its own text, not merely a link |
| T-200-21 | Repudiation | A wrong premise entering the tracked ledger — treating `writer.py:307/:927` as the raising guard | mitigate | The doc states the real raising site (`content_model.py::_check_congruence`) and explicitly corrects the shorthand; the acceptance criteria check for it |
| T-200-22 | Tampering | Scope creep — a "spike" that starts implementing section composition | mitigate | `git status --porcelain quirk/ tests/` asserted empty in both tasks; RPT-05 is decision-doc-only by requirement |
| T-200-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this plan |
</threat_model>

<verification>
- `TIER2-GO-NO-GO.md` exists with a verdict line, evidence sections, redesign section, and sizing
- All nine parity test files are named in the doc with measured test counts
- `.planning/HORIZON.md`'s 999.105 row carries the verdict substance and cites the doc
- `git status --porcelain quirk/ tests/` is empty
- No GSD mutating verb invoked
</verification>

<success_criteria>
- ROADMAP criterion 5 is satisfied: a written go/no-go exists for 999.105 Tier 2 explicitly
  addressing the congruence guard and the presence-based parity suite redesign
- The decision is durable — it lives in tracked form, not only in an untracked backlog file
- Zero implementation was performed
</success_criteria>

<output>
Create `.planning/phases/200-report-branding-templates/200-06-SUMMARY.md` when done. Include the
verdict, the measured parity-file counts, and the HORIZON.md diff.
</output>
