---
phase: 200-report-branding-templates
plan: 07
type: execute
wave: 4
depends_on: ["200-01", "200-02", "200-03", "200-04", "200-05", "200-06"]
files_modified:
  - docs/configuration.md
  - docs/operators-guide.md
  - docs/getting-started.md
  - docs/report-interpretation.md
  - docs/UAT-SERIES.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Configuration.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Operators-Guide.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Getting-Started.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Report-Interpretation.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md
  - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-200-Report-Branding-Templates.md
  - .planning/phases/200-report-branding-templates/200-VALIDATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [RPT-01, RPT-02, RPT-03, RPT-04, RPT-05]

must_haves:
  truths:
    - "An operator reading docs/configuration.md learns the full report: section — every branding field, template_dir, profile — and that assessment.logo_path is a deprecated fallback"
    - "An operator reading docs/operators-guide.md and docs/getting-started.md learns the `quirk report profile save|list` commands and the --report-profile flag, and how they differ from --profile and --score-profile"
    - "The operator-facing template-override docs state the security posture honestly: templates render sandboxed, and removing `| sanitize` from an override weakens scanner-data XSS hardening for that operator's own report"
    - "docs/UAT-SERIES.md carries a Series 200 whose every case has exactly one checked Result box, keeping the zero-undispositioned gate green (UATREC-04)"
    - "The Obsidian vault counterparts of every touched doc are re-synced in the same phase (LIVE-03) and a Phase 200 note exists with status complete"
    - "200-VALIDATION.md is closed green (nyquist_compliant: true, wave_0_complete: true, zero pending rows) BEFORE the ROADMAP.md checkbox commit, so the repo's pre-commit hook does not block the phase close"
  artifacts:
    - path: "docs/configuration.md"
      provides: "report: section reference incl. deprecation note"
      contains: "report.branding"
    - path: "docs/UAT-SERIES.md"
      provides: "Series 200 covering branding, template override/SSTI, path guard, profiles"
      contains: "Series 200"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-200-Report-Branding-Templates.md"
      provides: "Vault phase note with per-plan What Was Built"
  key_links:
    - from: "docs/UAT-SERIES.md"
      to: "tests/test_report_template_sandbox.py"
      via: "DEFERRED disposition citing full class-scoped pytest node IDs"
      pattern: "test_report_template_sandbox"
    - from: "docs/configuration.md"
      to: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Configuration.md"
      via: "vault re-sync of the same content"
      pattern: "Configuration"
---

<objective>
Discharge CLAUDE.md's Per-Phase Documentation Checklist and Mandatory Phase Completion Steps for
Phase 200, and close the phase's planning artifacts by hand.

Purpose: this phase triggers three checklist rows at once — new config options, a new CLI command,
and new report surfaces — plus the mandatory UAT/Obsidian/phase-note steps. The phase is not
complete until an operator can learn all of it from the docs and the vault mirrors it.

Output: four updated docs, UAT Series 200, five vault writes, a closed 200-VALIDATION.md, and
hand-flipped ROADMAP/REQUIREMENTS checkboxes.

Hard constraints:
- Do NOT invoke `phase.complete`, `milestone.complete`, `requirements mark-complete`, or any
  mutating `state.*`/`roadmap.*` GSD verb. They write well-formed WRONG values on this machine
  (`phase.complete` marked a phase complete at 5/7 plans with an impossible `completed_plans`).
  Checkboxes are hand-edited text edits.
- Use plain `git add` + `git commit`; `gsd-tools`/`gsd-sdk` `commit --files` misreports failure on
  this repo's gitignored `.planning/` paths.
- Never check a UAT Result box just to satisfy the gate. `GAP — no substitute coverage` is a valid,
  passing, honest disposition; a fabricated pytest citation is not.
- `docs/error-codes.md` was already regenerated in plan 200-02. Do NOT hand-edit it here; just
  confirm its gate is still green.
- Close `200-VALIDATION.md` BEFORE (or in the same commit as) the `.planning/ROADMAP.md` change —
  the pre-commit hook blocks ROADMAP commits over a non-green validation file (recurred on phases
  157/158/159).
- Do not write a verification command that greps `200-VALIDATION.md` for the pending glyph: a row
  containing that glyph in its own command text can never reach zero. Verify by frontmatter key
  and a count of green rows.
- No version bump this phase — leave the `**Version:**` line in `docs/UAT-SERIES.md` alone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/200-report-branding-templates/200-CONTEXT.md
@.planning/phases/200-report-branding-templates/200-01-SUMMARY.md
@.planning/phases/200-report-branding-templates/200-02-SUMMARY.md
@.planning/phases/200-report-branding-templates/200-03-SUMMARY.md
@.planning/phases/200-report-branding-templates/200-04-SUMMARY.md
@.planning/phases/200-report-branding-templates/200-05-SUMMARY.md
@.planning/phases/200-report-branding-templates/200-06-SUMMARY.md
@.planning/phases/200-report-branding-templates/200-SSTI-GATE.md

<interfaces>
<!-- Source -> vault mapping (vault name is always "Digs"). Large files MUST be written directly -->
<!-- to the vault filesystem, never via the obsidian CLI `content=` parameter. -->
docs/configuration.md         -> /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Configuration.md
docs/operators-guide.md       -> /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Operators-Guide.md
docs/getting-started.md       -> /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Getting-Started.md
docs/report-interpretation.md -> /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Report-Interpretation.md
docs/UAT-SERIES.md            -> /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md
phase note                    -> /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-200-Report-Branding-Templates.md

Vault frontmatter keys: project, type, status, source, updated.

<!-- UAT gate grammar (all three burned Phase 188 — see 188-05-SUMMARY.md Deviations) -->
- `**Result:**` must be ONE physical line with exactly one checked box (PASS / FAIL / SKIP)
- DEFERRED pytest citations need FULL class-scoped node IDs and must be `--collect-only` resolvable
- vitest citations from multi-test files cannot pass the zero-skip execution leg -> use GAP honestly

Gate commands:
  .venv/bin/python -m pytest -q tests/test_uat_zero_undispositioned_gate.py \
      tests/test_uat_disposition_integrity.py tests/test_error_codes_freshness.py
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the report: config section, the profile CLI, and the branding surfaces</name>
  <files>docs/configuration.md, docs/operators-guide.md, docs/getting-started.md, docs/report-interpretation.md</files>
  <read_first>
    - docs/configuration.md (the section-per-config-block structure and how the `assessment` block is documented today, including `logo_path`)
    - docs/operators-guide.md (the existing CLI command reference section and the config-options section — find where `--profile`/`--score-profile` are documented)
    - docs/getting-started.md (the command list a new operator reads first)
    - docs/report-interpretation.md (where report surfaces/sections are described — the branding/cover note belongs beside them)
    - .planning/phases/200-report-branding-templates/200-02-SUMMARY.md and 200-05-SUMMARY.md (the shipped config keys, error codes, CLI syntax, and profile directory semantics — document what shipped, not what was planned)
  </read_first>
  <action>
`docs/configuration.md`: add a `report:` section documenting every key with type, default, and
effect — `branding.logo_path`, `branding.client_name`, `branding.engagement_name`,
`branding.prepared_by`, `branding.cover_date`, `branding.confidentiality_line`, `template_dir`,
`profile`. State that every field is optional and an unset field renders exactly as before. Add the
deprecation note beside the existing `assessment.logo_path` entry: `report.branding.logo_path` is
preferred, `assessment.logo_path` is still honored as a fallback and is not being removed. Document
which surfaces render which fields (HTML/PDF and DOCX render the full set including the logo; CLI
markdown and console carry the identity text only, no logo). Document the path rules: no `..`
segments, and what `QRK-CONFIG-003` / `QRK-CONFIG-004` mean when an operator hits them.

`docs/operators-guide.md`: document the `quirk report profile save <name> --config <file>` and
`quirk report profile list` commands, the `--report-profile <name>` scan flag, the
`~/.quirk/report_profiles/` location and its `QUIRK_PROFILES_DIR` override, and the precedence
rule in one unambiguous sentence — a value set explicitly in the engagement config always beats the
profile's value. Explicitly distinguish the three similarly-named flags (`--profile` = scan
profile, `--score-profile` = scoring calibration, `--report-profile` = report branding/templates).
Also document `report.template_dir`: the operator's directory is searched first with the packaged
template as fallback, only `.j2` templates are used, and templates render inside a sandboxed Jinja2
environment. State the honest security posture: the sandbox protects the HOST, autoescape protects
against scan-data XSS, and an override that drops `| sanitize` weakens scanner-data hardening for
that operator's own report.

`docs/getting-started.md`: add the `quirk report profile` command to the command list in the
existing style, with a one-line branding quickstart.

`docs/report-interpretation.md`: add a short note describing what branding appears where on the
deliverable (cover logo + identity block, running header/footer identity, CLI identity lines) so a
reader of a branded report knows which parts are operator-supplied labels rather than scan results.

Cite `Phase 200 / RPT-01..RPT-04` in each passage's provenance the way neighbouring passages cite
their phases. Do not invent behaviour — every documented claim must match what the SUMMARYs record
as shipped.
  </action>
  <verify>
    <automated>grep -c "report.branding" docs/configuration.md && grep -c "report-profile" docs/operators-guide.md && grep -c "quirk report profile" docs/getting-started.md && grep -c "sandbox" docs/operators-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/configuration.md` documents all eight `report:` keys (verify each key name appears) plus the `assessment.logo_path` deprecation note and both error codes
    - `docs/operators-guide.md` contains all three flag names (`--profile`, `--score-profile`, `--report-profile`) in one distinguishing passage, the two `quirk report profile` commands, `QUIRK_PROFILES_DIR`, and the precedence sentence
    - `docs/operators-guide.md` states the `| sanitize` removal consequence for override templates explicitly
    - `docs/getting-started.md` lists `quirk report profile` among the commands
    - `docs/report-interpretation.md` describes branding placement across HTML/PDF, DOCX, and CLI
    - Every documented CLI invocation is executed once to confirm it is accurate, and the transcripts are recorded in the SUMMARY
  </acceptance_criteria>
  <done>An operator can learn the whole phase's surface from the docs, with the security posture stated honestly.</done>
</task>

<task type="auto">
  <name>Task 2: Add UAT Series 200 and keep the disposition gates green</name>
  <files>docs/UAT-SERIES.md</files>
  <read_first>
    - docs/UAT-SERIES.md (the most recent series block — copy its case structure verbatim: ID / Title / Maps to / What to test / Steps / Pass Criteria / Falsifiability / Result / Date / Tester / Notes; also read the document header block you must update)
    - tests/test_uat_zero_undispositioned_gate.py (module docstring — what the gate parses and why GAP is a passing disposition)
    - tests/test_uat_disposition_integrity.py (module docstring — DEFERRED citation conventions and the substitute-proof execution leg)
    - all six 200-NN-SUMMARY.md files and 200-SSTI-GATE.md (the evidence you are dispositioning against)
  </read_first>
  <action>
Append a `## Series 200: Report Branding & Templates (Phase 200 — v5.23)` section after the last
existing series, with an intro paragraph naming RPT-01 through RPT-05.

Write one case per verifiable behavior, following the existing case template exactly:
- `UAT-200-01` — a config with the full `report.branding` set produces a branded HTML/PDF report:
  logo on the cover, identity block on cover and header/footer. Maps to RPT-01.
- `UAT-200-02` — the same config produces a branded DOCX with the same fields. Maps to RPT-01.
- `UAT-200-03` — the CLI executive summary, scorecard, and console summary carry the identity text
  and no logo. Maps to RPT-01.
- `UAT-200-04` — `assessment.logo_path` alone still brands the report; `report.branding.logo_path`
  wins when both are set. Maps to RPT-01.
- `UAT-200-05` — a `report.template_dir` containing a modified `report.html.j2` overrides the
  packaged template; removing the file falls back to the packaged one. Maps to RPT-02.
- `UAT-200-06` — an override template containing SSTI payloads cannot reach host internals
  (SecurityError or marker-free output). Maps to RPT-02.
- `UAT-200-07` — a `..`-containing `template_dir` or `logo_path` is rejected at load with a
  `QRK-CONFIG-003` message naming the field and value. Maps to RPT-03.
- `UAT-200-08` — no report path field is reachable from any dashboard surface. Maps to RPT-03.
- `UAT-200-09` — `quirk report profile save` then `list` then a scan with `--report-profile`
  reproduces the branding without re-entering values. Maps to RPT-04.
- `UAT-200-10` — an explicitly-set engagement-config branding value survives profile application.
  Maps to RPT-04.
- `UAT-200-11` — the Tier 2 go/no-go document exists and addresses the congruence guard and the
  parity suite; the HORIZON 999.105 row carries the verdict. Maps to RPT-05.

Disposition each case honestly against the evidence in the plan SUMMARYs. Cases with automated
backend coverage get SKIP with `DEFERRED — covered by <full class-scoped pytest node ID>` citing a
node in `tests/test_report_branding.py`, `tests/test_report_branding_cli.py`,
`tests/test_report_template_sandbox.py`, `tests/test_report_path_guard.py`, or
`tests/test_report_profiles.py`, plus the originating SUMMARY. Visual-fidelity cases (logo
placement, cover layout — UAT-200-01/02 in their appearance aspect) have no automated substitute
because this repo's render tests assert presence, not appearance: record `GAP — no substitute
coverage` and note that a live operator walkthrough is the honest path. RPT-05's case is a document
review — disposition it against the actual file.

Every `**Result:**` line must be ONE physical line with exactly one box checked. Add `**Date:**`
and `**Tester:**` lines in the existing style. Update the document header's `**Last Updated:**`
block to today's date with a one-sentence description of the Series 200 addition, following the
header's running-log style. Do NOT change the `**Version:**` line.

Then run the UAT gates. If the integrity gate rejects a citation, fix the citation or downgrade to
an honest GAP — never add an allowlist entry and never check a box to make a gate pass.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_uat_zero_undispositioned_gate.py tests/test_uat_disposition_integrity.py tests/test_error_codes_freshness.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Series 200' docs/UAT-SERIES.md` returns 1
    - `grep -c '^### UAT-200-' docs/UAT-SERIES.md` returns 11
    - Every `**Result:**` line in the Series 200 block has exactly one `[x]` on one physical line
    - The three gate tests exit 0
    - Every DEFERRED citation resolves: each cited node is confirmed collectible via `.venv/bin/python -m pytest --collect-only -q <node-id>`, with the transcript in the SUMMARY
    - The header's `**Last Updated:**` shows today's date; `**Version:**` is unchanged
    - `git status --porcelain tests/` shows no modified gate file — no allowlist or gate-code change
  </acceptance_criteria>
  <done>Series 200 exists, every case is honestly dispositioned, and all three gates are green.</done>
</task>

<task type="auto">
  <name>Task 3: Vault sync, phase note, chaos-lab check, validation close-out, and checkbox flips</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Configuration.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Operators-Guide.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Getting-Started.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Report-Interpretation.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-200-Report-Branding-Templates.md, .planning/phases/200-report-branding-templates/200-VALIDATION.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - CLAUDE.md (sections "Obsidian vault sync (LIVE-03)", "Mandatory Phase Completion Steps", "Chaos Lab Maintenance")
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-199-Wave-A-Correctness-Drain.md (the immediately-preceding phase note — copy its structure and frontmatter exactly)
    - .planning/phases/200-report-branding-templates/200-VALIDATION.md (the Per-Task Verification Map you must close row by row)
    - all six 200-NN-SUMMARY.md files (source material for the "What Was Built" subsections)
    - .planning/phases/199-wave-a-correctness-drain/199-05-SUMMARY.md (the close-out conventions this task repeats)
  </read_first>
  <action>
Sync the five touched docs to the vault by writing directly to the vault filesystem — never via the
obsidian CLI `content=` parameter. Prepend the standard frontmatter block (project `QU.I.R.K.`,
type `guide` or `reference`, status `active`, the repo-relative `source` path, `updated` = today)
to the file body, preserving the source content byte-for-byte below the frontmatter. Do not
summarize or rewrite guide content. Targets: `docs/configuration.md`, `docs/operators-guide.md`,
`docs/getting-started.md`, `docs/report-interpretation.md` → the four `Guides/` paths;
`docs/UAT-SERIES.md` → `UAT-Series.md`.

Write the phase note at
`/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-200-Report-Branding-Templates.md` with
frontmatter (`status: complete`, `type: phase`, `source`, `updated`) and sections: Goal,
Requirements Covered (RPT-01..RPT-05), Success Criteria (the five from the ROADMAP), What Was Built
(one subsection per plan 200-01 through 200-07, sourced from the SUMMARYs — include the SSTI
GO/NO-GO verdict explicitly, because the containment gate is the phase's load-bearing claim), and a
`[[Roadmap]]` link. Match the Phase 199 note's structure.

Chaos lab: this phase adds no Compose profile and no scanner signal. Confirm that explicitly rather
than assuming — grep the `expected_results_*.md` oracle files for any report-branding or
template-related expectation that a `report:` config block could perturb, and record the finding
verbatim in the SUMMARY as "chaos lab checked, no oracle impact" (or the contrary finding with the
offending file and line).

Before touching `.planning/ROADMAP.md`, close out
`.planning/phases/200-report-branding-templates/200-VALIDATION.md`. Its Per-Task Verification Map
was filled at planning time with one row per task. For each row, confirm the row's Automated
Command actually passed during this phase (re-run it if the evidence is not already in a plan
SUMMARY), then edit that row's `Status` cell to the green check glyph and its `File Exists` cell
away from the Wave-0 marker where the file now exists. Set `nyquist_compliant: true` and
`wave_0_complete: true` in the frontmatter, flip the six `Validation Sign-Off` checkboxes and
`**Approval:**`, and record the Manual-Only row (branded HTML/DOCX visual appearance) as
outstanding-for-human-UAT rather than silently green. If any row's command did NOT pass, mark that
row red and STOP — do not flip the ROADMAP checkbox over a red validation row.

Make these edits as plain text edits to the specific cells. Do NOT write a verification command
that greps this file for the pending glyph — a row containing that glyph in its own command text
can never reach zero. Verify by frontmatter key and by a count of green rows.

Then hand-flip the Phase 200 checkbox in `.planning/ROADMAP.md` and the RPT-01..RPT-05 checkboxes
plus their status-table rows in `.planning/REQUIREMENTS.md`. Text edits only — no
`phase.complete`, no `milestone.complete`, no `requirements mark-complete`, no mutating `state.*`
verb. If you touch `.planning/STATE.md` at all, take a pre-image first and diff it afterward
against the two known corruption signatures (a garbled bold-field code span; a dropped frontmatter
key).

Commit with plain `git add` + `git commit`, with the `200-VALIDATION.md` change landing no later
than the `.planning/ROADMAP.md` change.
  </action>
  <verify>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-200-Report-Branding-Templates.md" && diff <(tail -n +9 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md") docs/UAT-SERIES.md > /dev/null && diff <(tail -n +9 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Guides/Configuration.md") docs/configuration.md > /dev/null && grep -c "nyquist_compliant: true" .planning/phases/200-report-branding-templates/200-VALIDATION.md && echo VAULT_AND_VALIDATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - Each vault file's body below its frontmatter is byte-identical to its source. Verify with `diff <(tail -n +9 "<vault file>") <source>` — `+9`, not `+7`: CLAUDE.md's `printf` frontmatter is 7 lines plus a blank separator, so the body starts at line 9. If you deviate from that exact printf block, recompute the offset with `awk '/^---$/{n++; next} n>=2' <vault file>` instead of hardcoding a number
    - All five vault doc files carry the five standard frontmatter keys with `updated` = today and `source` = the repo-relative path
    - `Phase-200-Report-Branding-Templates.md` exists with `status: complete`, one "What Was Built" subsection per plan (200-01 .. 200-07), names all five RPT IDs, states the SSTI GO/NO-GO verdict, and contains a `[[Roadmap]]` wikilink
    - The SUMMARY records the chaos-lab grep verbatim, command and output
    - `200-VALIDATION.md` frontmatter reads `nyquist_compliant: true` and `wave_0_complete: true`; its Per-Task Verification Map has zero non-green Status cells (count green glyphs against the row count; do not grep for the pending glyph) and the Manual-Only row is recorded as outstanding human UAT
    - The `200-VALIDATION.md` edit is committed BEFORE or with the `.planning/ROADMAP.md` change
    - `.planning/ROADMAP.md` Phase 200 checkbox is `[x]`; `.planning/REQUIREMENTS.md` shows RPT-01..RPT-05 checked with their status-table rows updated to Phase 200 / Complete
    - `git log --oneline -8` shows no commit authored by a `phase.complete` / `milestone.complete` / `requirements mark-complete` invocation; `git diff` on `.planning/STATE.md` (if any) shows no garbled bold-field code span and no dropped frontmatter key
    - Full backend suite run once: `.venv/bin/python -m pytest -q -m ""` failing-node SET compared against the empty pre-phase baseline (compare SETS, never raw counts — chaos-lab collection moves with Docker health)
  </acceptance_criteria>
  <done>Docs, UAT series, and vault are in sync; the phase note exists; the chaos-lab check is recorded; 200-VALIDATION.md is closed green; requirement and phase checkboxes are hand-flipped safely, in that order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation, UAT corpus, and vault files only. No code, no inputs, no endpoints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200-23 | Repudiation | UAT corpus integrity — a checked box that does not reflect real coverage | mitigate | Every DEFERRED citation must resolve via `pytest --collect-only`; visual-fidelity cases take an honest `GAP` rather than a manufactured citation; no allowlist or gate-code edit permitted |
| T-200-24 | Tampering | `.planning/STATE.md` / ROADMAP corruption by mutating GSD state verbs (TOOL-01/04/05 defect class and the `phase.complete` semantic class) | mitigate | No mutating verb is invoked; checkboxes are hand-edited; any STATE.md touch is pre-imaged and signature-diffed |
| T-200-25 | Information disclosure | Docs understating the override-template security posture, leading an operator to strip `| sanitize` unknowingly | mitigate | `docs/operators-guide.md` states the sandbox/autoescape/sanitize division of responsibility explicitly, as an acceptance criterion |
| T-200-26 | Information disclosure | Vault sync copying content outside the QUIRK doc set | accept | Only the five named `docs/` files and one generated phase note are written, to the six paths enumerated in `<interfaces>` |
| T-200-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this plan |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q tests/test_uat_zero_undispositioned_gate.py tests/test_uat_disposition_integrity.py tests/test_error_codes_freshness.py` exits 0
- All five vault doc bodies match their sources byte-for-byte; the Phase 200 note exists with `status: complete`
- `200-VALIDATION.md` reads `nyquist_compliant: true` / `wave_0_complete: true` with zero non-green rows, committed no later than the ROADMAP change
- `.venv/bin/python -m pytest -q -m ""` failing-node SET matches the empty pre-phase baseline
- No mutating GSD state verb was invoked
</verification>

<success_criteria>
- Per-Phase Documentation Checklist rows triggered by this phase (new config option, new CLI
  command, new report surface) are actioned across the four docs and synced to the vault
- UAT Series 200 exists with 11 honestly-dispositioned cases; all three gates green
- Vault carries current Configuration, Operators-Guide, Getting-Started, Report-Interpretation,
  UAT-Series, and a Phase 200 note
- Chaos-lab no-impact finding recorded with evidence, not assumed
- `200-VALIDATION.md` closed green first, then ROADMAP and REQUIREMENTS checkboxes hand-flipped,
  all without touching a mutating GSD verb
</success_criteria>

<output>
Create `.planning/phases/200-report-branding-templates/200-07-SUMMARY.md` when done. Include the
chaos-lab grep output verbatim, the UAT gate results, the `--collect-only` transcripts for every
DEFERRED citation, the list of vault files written, and the full-suite failing-node SET comparison.
</output>
