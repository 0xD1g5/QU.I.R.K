---
phase: 41
plan: 07
type: execute
wave: 5
depends_on: [41-04, 41-05, 41-06]
files_modified:
  - docs/UAT-SERIES.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [CI-01, CI-02, CI-03, ROBUST-01, ROBUST-02, ROBUST-03, ROBUST-04]

must_haves:
  truths:
    - "docs/UAT-SERIES.md updated with Phase 41 entries (UAT-41-01 stderr advisory, UAT-41-02 upper-bound formula doc, UAT-41-03 lab.sh profile sweep, UAT-41-04 60s budget)"
    - "docs/UAT-SERIES.md mirrored to /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
    - "Phase 41 Obsidian phase note created at /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-41-CI-Stability-Scanner-Robustness.md with status: complete"
    - "ROADMAP.md Phase 41 entry checkbox marked [x]"
    - "STATE.md updated to reflect Phase 41 completion"
  artifacts:
    - path: "docs/UAT-SERIES.md"
      provides: "Phase 41 UAT entries"
      contains: "UAT-41-01"
    - path: "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-41-CI-Stability-Scanner-Robustness.md"
      provides: "Obsidian phase completion note"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 41 marked complete"
  key_links:
    - from: "docs/UAT-SERIES.md"
      to: "20_Dev-Work/QUIRK/UAT-Series.md (vault mirror)"
      via: "filesystem copy with frontmatter"
      pattern: "UAT-41"
---

<objective>
Execute the Mandatory Phase Completion Steps from CLAUDE.md: UAT-SERIES update + sync, Obsidian
phase note, roadmap mark complete.

Purpose: Per CLAUDE.md and the user's auto-memory feedback ([feedback_uat_series_update],
[feedback_obsidian_phase_notes], [feedback_docs_obsidian_per_phase]), every phase must close
with these housekeeping steps before declaring done.

Output: Phase 41 marked complete across roadmap, state, UAT series, and Obsidian vault.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@docs/UAT-SERIES.md
@.planning/phases/41-01-SUMMARY.md
@.planning/phases/41-02-SUMMARY.md
@.planning/phases/41-03-SUMMARY.md
@.planning/phases/41-04-SUMMARY.md
@.planning/phases/41-05-SUMMARY.md
@.planning/phases/41-06-SUMMARY.md
@.planning/phases/41-ci-stability-scanner-robustness/41-VALIDATION.md

<interfaces>
Manual UAT entries to add (from VALIDATION.md §"Manual-Only Verifications"):
- UAT-41-01: stderr advisory format on missing [motion] extra (ROBUST-01 / D-12)
- UAT-41-02: docs/configuration.md upper-bound formula present (ROBUST-02 / D-10)
- UAT-41-03: lab.sh profile-tagged service sweep on down + reset (D-18)
- UAT-41-04: default `pytest -m 'not slow'` finishes <60s on a developer machine (CI-03 / D-16)

Obsidian phase note path:
/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-41-CI-Stability-Scanner-Robustness.md

Vault frontmatter required:
project: QU.I.R.K.
type: phase
status: complete
source: .planning/phases/41-ci-stability-scanner-robustness/
updated: 2026-04-29 (or completion date)

CLAUDE.md mandate (Mandatory Phase Completion Steps):
1. Create Obsidian phase note (write directly to vault filesystem, not via CLI content=)
2. Update docs/UAT-SERIES.md (add Phase 41 entries, bump Last Updated)
3. Sync UAT-SERIES.md to Obsidian (printf frontmatter + cat + cp)
4. Commit docs/UAT-SERIES.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/UAT-SERIES.md with Phase 41 entries + sync to Obsidian</name>
  <files>docs/UAT-SERIES.md, /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md</files>
  <read_first>
    - docs/UAT-SERIES.md (full file — find existing series ordering and format)
    - .planning/phases/41-ci-stability-scanner-robustness/41-VALIDATION.md §"Manual-Only Verifications"
    - CLAUDE.md "Mandatory Phase Completion Steps"
  </read_first>
  <action>
    1. Open docs/UAT-SERIES.md and locate the **Last Updated** date at the top — bump to today's date (2026-04-29 or current).

    2. Add a new "Phase 41 — CI Stability & Scanner Robustness" series section. For each UAT,
    follow the existing UAT entry style in the file. Entries:

    **UAT-41-01: stderr advisory on missing [motion] extra**
    - Pre: in a venv without `quirk[motion]` installed (or `pip uninstall -y kafka-python pika redis`)
    - Step: `quirk scan --target localhost --enable-broker`
    - Pass: stderr contains exactly `[advisory] scanner=broker_scanner extra=motion not installed — run `pip install quirk[motion]` to enable`; CLI exits 0; scan_errors[] in JSON output includes one `category=missing_extra` entry

    **UAT-41-02: configuration docs include upper-bound formula**
    - Step: `grep -q "scan_upper_bound" docs/configuration.md && grep -q "safety_margin" docs/configuration.md`
    - Pass: both grep commands exit 0

    **UAT-41-03: lab.sh profile-tagged service sweep**
    - Pre: `./quantum-chaos-enterprise-lab/lab.sh up` (any profile-tagged services running)
    - Step: `./quantum-chaos-enterprise-lab/lab.sh down`
    - Pass: `docker ps -a --filter label=com.docker.compose.project=quirk-lab` returns zero rows
    - Repeat for `./lab.sh reset` to validate the reset arm

    **UAT-41-04: default pytest run finishes <60s**
    - Pre: clean repo, dependencies installed
    - Step: `time pytest -m 'not slow' tests/`
    - Pass: real time reported by `time` is under 60s

    3. Save docs/UAT-SERIES.md.

    4. **Sync to Obsidian vault filesystem** (per CLAUDE.md — file too large for CLI content=):
    ```bash
    printf "---\nproject: QU.I.R.K.\ntype: reference\nstatus: active\nsource: docs/UAT-SERIES.md\nupdated: 2026-04-29\n---\n\n" > /tmp/uat_vault.md
    cat docs/UAT-SERIES.md >> /tmp/uat_vault.md
    cp /tmp/uat_vault.md "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"
    ```
    Use the actual completion date in the `updated:` field.
  </action>
  <verify>
    <automated>grep -q "UAT-41-01" docs/UAT-SERIES.md &amp;&amp; grep -q "UAT-41-02" docs/UAT-SERIES.md &amp;&amp; grep -q "UAT-41-03" docs/UAT-SERIES.md &amp;&amp; grep -q "UAT-41-04" docs/UAT-SERIES.md &amp;&amp; test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" &amp;&amp; grep -q "UAT-41-01" "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "UAT-41-0[1-4]" docs/UAT-SERIES.md` returns at least 4
    - `grep -q "scan_error_category=missing_extra" docs/UAT-SERIES.md || grep -q "category=missing_extra" docs/UAT-SERIES.md` exit 0
    - `grep -q "compose --profile" docs/UAT-SERIES.md` exit 0
    - Vault file exists: `test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md"` exit 0
    - Vault file has frontmatter: `head -5 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/UAT-Series.md" | grep -q "project: QU.I.R.K."` exit 0
  </acceptance_criteria>
  <done>UAT-SERIES.md has 4 new Phase 41 entries; vault mirror up to date with frontmatter.</done>
</task>

<task type="auto">
  <name>Task 2: Create Obsidian phase note + mark roadmap complete + update STATE.md</name>
  <files>/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-41-CI-Stability-Scanner-Robustness.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - /Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/ (ls — confirm naming convention used by prior phases)
    - One existing phase note in vault (e.g. Phase-40-Chaos-Lab-Parity.md) for template reference
    - .planning/ROADMAP.md lines 750-825 (Phase 41 entry)
    - .planning/STATE.md (full file)
    - All Phase 41 SUMMARY.md files (41-01..41-06)
    - CLAUDE.md "Vault Folder Structure" + "Frontmatter Standards" + "Phase notes" sections
  </read_first>
  <action>
    1. **Write Phase 41 Obsidian note** directly to vault filesystem:
    `/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-41-CI-Stability-Scanner-Robustness.md`

    Structure (per CLAUDE.md template):
    ```markdown
    ---
    project: QU.I.R.K.
    type: phase
    status: complete
    source: .planning/phases/41-ci-stability-scanner-robustness/
    updated: 2026-04-29
    ---

    # Phase 41 — CI Stability & Scanner Robustness

    ## Goal
    The CI test suite runs green with zero skipped-for-code-reasons tests and completes
    deterministically in under 60 seconds; all scanners degrade gracefully under missing
    extras, slow targets, and unexpected exceptions with a consistent, documented
    timeout/retry policy.

    ## Requirements Covered
    - CI-01, CI-02, CI-03
    - ROBUST-01, ROBUST-02, ROBUST-03, ROBUST-04

    ## Success Criteria (from ROADMAP)
    1. pytest runs with zero skip/xfail markers on code-reason tests; SAML scan-window test green
    2. Missing [motion] → advisory + scan completes
    3. Per-scanner timeout respected; documented upper-bound
    4. Unexpected exception → scan_errors[] entry; scan continues
    5. Single-source-of-truth timeout/retry policy
    6. Default pytest run <60s

    ## What Was Built
    ### Plan 01 — Wave 0 test infrastructure
    [Source: 41-01-SUMMARY.md] pytest config, skip registry + AST-walk meta-gate, scan_error_category column, ROBUST-01..03 + TimeoutsCfg test stubs.

    ### Plan 02 — TimeoutsCfg / RetryCfg
    [Source: 41-02-SUMMARY.md] Nested dataclasses on ScanCfg, deprecation-alias properties for legacy flat fields, backward-compat config loader.

    ### Plan 03 — D-08 mutation removal + run_scan.py:743 bug fix
    [Source: 41-03-SUMMARY.md] BACK-45 cfg.scan mutation pattern dissolved; cfg.scan.profile AttributeError bug fixed; all timeout reads route through cfg.scan.timeouts.<name>_seconds.

    ### Plan 04 — D-14 BaseException wrapper + D-12 advisory + D-15 trends filter
    [Source: 41-04-SUMMARY.md] _wrapped_phase helper around every scanner phase; missing-extra stderr advisories; trends.py excludes missing_extra from regression counts.

    ### Plan 05 — Stale skip deletion + slow-test marking
    [Source: 41-05-SUMMARY.md] 13 stale code-reason skips deleted; 9 slow-test candidates marked; meta-gate green.

    ### Plan 06 — Documentation + lab.sh fix
    [Source: 41-06-SUMMARY.md] docs/configuration.md updated; docs/timeout-retry-audit.md created (ROBUST-04); lab.sh down + reset arms sweep profile-tagged services.

    ## Links
    - [[Roadmap]]
    - [[UAT-Series]]
    ```

    Replace the per-plan stub bullets with the actual SUMMARY content (1-2 lines each, sourced
    from each plan's SUMMARY.md). Use the actual completion date.

    2. **Mark Phase 41 complete in ROADMAP.md** — line ~753:
    Change `- [ ] **Phase 41:** ...` to `- [x] **Phase 41:** ...`. Do NOT modify other phase entries.

    3. **Update STATE.md**:
       - `stopped_at:` → "Phase 41 complete"
       - `last_activity:` → completion date
       - Add to `### Decisions` a Phase 41 entry summarizing key decisions:
         - "[41-01]: TimeoutsCfg + RetryCfg sub-tables on ScanCfg with deprecation-alias properties on four legacy fields (D-06/07)"
         - "[41-03]: BACK-45 cfg.scan mutation pattern dissolved by passing explicit kwargs; cfg.scan.profile bug at run_scan.py:743 fixed (uses scan_profile local var)"
         - "[41-04]: _wrapped_phase helper around every scanner phase implements D-14; trends.py respects scan_error_category to exclude missing_extra from regression counts (D-15)"
         - "[41-06]: lab.sh down + reset arms both sweep profile-tagged services (D-18 + extension per RESEARCH OQ-4)"
       - Update Phase Overview table — Phase 41 row: change Depends column or add a Status column entry indicating complete (follow existing STATE.md conventions).
       - If progress.completed_phases counter is tracked, increment it.

    4. After all writes, run `python -m compileall .planning/ docs/ -q` (will skip non-py files; just sanity check no syntax breakage anywhere).
  </action>
  <verify>
    <automated>test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-41-CI-Stability-Scanner-Robustness.md" &amp;&amp; head -10 "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-41-CI-Stability-Scanner-Robustness.md" | grep -q "status: complete" &amp;&amp; grep -q "\- \[x\] \*\*Phase 41:" .planning/ROADMAP.md &amp;&amp; grep -q "Phase 41 complete" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - Vault phase note exists: `test -f "/Users/digs/vaults/Digs/20_Dev-Work/QUIRK/Phases/Phase-41-CI-Stability-Scanner-Robustness.md"` exit 0
    - Vault note frontmatter has `status: complete`: `head -10 "/Users/digs/.../Phase-41-...md" | grep -q "status: complete"` exit 0
    - Vault note references each plan: `grep -c "Plan 0" "/Users/digs/.../Phase-41-...md"` returns at least 6
    - ROADMAP.md Phase 41 marked: `grep -E "^- \[x\] \*\*Phase 41:" .planning/ROADMAP.md` exit 0
    - STATE.md updated: `grep -q "Phase 41" .planning/STATE.md && grep -q "\[41-" .planning/STATE.md` exit 0
    - At least 3 [41-XX] decision entries in STATE.md: `grep -c "\[41-" .planning/STATE.md` returns at least 3
  </acceptance_criteria>
  <done>Phase 41 closed across all four artifacts: vault phase note, ROADMAP checkbox, STATE.md decisions log, UAT-SERIES sync.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of phase closure</name>
  <action>Pause for human verification of phase closure (manual UAT and visual sanity-check on vault note + roadmap + STATE).</action>
  <what-built>
    Phase 41 closure complete:
    - docs/UAT-SERIES.md has 4 new UAT-41-XX entries
    - Vault UAT-Series.md mirror updated
    - Vault Phase-41 note created with status: complete
    - ROADMAP.md Phase 41 marked [x]
    - STATE.md updated with Phase 41 decisions
  </what-built>
  <how-to-verify>
    1. Open the Obsidian vault — confirm `20_Dev-Work/QUIRK/Phases/Phase-41-CI-Stability-Scanner-Robustness.md` renders cleanly with status complete and all 6 plan summaries.
    2. Run `pytest -m 'not slow'` from repo root — confirm exits 0 and wall-clock under 60s.
    3. Run `pytest tests/test_skip_registry.py -x` — confirm green (meta-gate passes).
    4. Optional: bring up chaos lab and exercise UAT-41-03 (`./lab.sh up && ./lab.sh down && docker ps -a` — zero quirk-lab profile rows).
    5. Optional: in a venv without [motion], invoke `quirk scan --enable-broker` and confirm UAT-41-01 stderr advisory line.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any closure gap to address.</resume-signal>
</task>

</tasks>

<verification>
- All Mandatory Phase Completion Steps from CLAUDE.md executed
- Vault phase note + UAT mirror present with correct frontmatter
- ROADMAP.md and STATE.md reflect Phase 41 complete
- Human verification recorded
</verification>

<success_criteria>
- docs/UAT-SERIES.md updated and synced to vault
- Vault Phase-41 note created with status: complete
- ROADMAP.md Phase 41 checkbox [x]
- STATE.md decisions section logs Phase 41 entries
- Human verifier approves closure
</success_criteria>

<output>
After completion, create `.planning/phases/41-ci-stability-scanner-robustness/41-07-SUMMARY.md`.
</output>
