---
phase: 41
plan: 05
type: execute
wave: 4
depends_on: [41-01, 41-04]
files_modified:
  - tests/conftest.py
  - tests/test_cloud_connectors.py
  - tests/test_email_scanner.py
  - tests/test_broker_db_schema.py
  - tests/test_version.py
  - tests/test_cli_init.py
  - tests/test_cli_version.py
  - tests/test_chaos_storage.py
  - tests/test_dnssec_scanner.py
  - tests/test_saml_scanner.py
  - tests/test_kerberos_scanner.py
  - tests/test_cbom_motion_golden.py
autonomous: true
requirements: [CI-01, CI-02, CI-03]

must_haves:
  truths:
    - "All stale code-reason skips deleted (D-04 set: tests/conftest.py:111, 9× test_cloud_connectors.py @skipif(not _HAS_GCP_MODULE), tests/test_broker_db_schema.py:70, 16× test_email_scanner.py _skip_scanner)"
    - "tests/test_version.py defensive skips converted to pytest.fail (RESEARCH lines 91-92)"
    - "All identified slow-test candidates marked @pytest.mark.slow (RESEARCH lines 221-229)"
    - "tests/test_skip_registry.py meta-gate from Plan 01 passes — no unregistered skips remain"
    - "Default `pytest` invocation finishes in <60s on a developer machine (D-16)"
  artifacts:
    - path: "tests/conftest.py"
      provides: "dashboard_client fixture without stale skip; replaced with pytest.fail or moved to importorskip at module level"
    - path: "tests/test_cloud_connectors.py"
      provides: "Tests no longer skipped by _HAS_GCP_MODULE guard"
  key_links:
    - from: "tests/test_skip_registry.py meta-gate"
      to: "ALLOWED_SKIPS registry membership"
      via: "AST walk over tests/*.py"
      pattern: "ALLOWED_SKIPS"
---

<objective>
Delete every stale code-reason skip identified in RESEARCH, convert defensive skips to assertions,
mark slow tests, and turn the Plan 01 skip-registry meta-gate green.

Purpose: CI-01 cannot be claimed until the meta-gate from Plan 01 passes. The deletion targets
expanded from D-04's initial 3 to 13 entries during research; this plan owns the full set.

Output: Default `pytest` runs <60s with zero stale skip markers; meta-gate green; CI-01..03 met.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-ci-stability-scanner-robustness/41-CONTEXT.md
@.planning/phases/41-ci-stability-scanner-robustness/41-RESEARCH.md
@.planning/phases/41-ci-stability-scanner-robustness/41-PATTERNS.md
@.planning/phases/41-01-SUMMARY.md

<interfaces>
Deletion targets (RESEARCH §"Skip-Marker Triage Table"):
- tests/conftest.py:111 — `pytest.skip("quirk.dashboard not yet implemented")` — DELETE (dashboard exists)
- tests/test_cloud_connectors.py — 9 lines (~154, 162, 179, 232, 256, 280, 303, 327, 360) `@pytest.mark.skipif(not _HAS_GCP_MODULE, reason="gcp_connector.py not yet created")` — DELETE
- tests/test_broker_db_schema.py:70 — `pytest.skip("Column already present...")` — DELETE entire skip line (or convert to assertion if test still meaningful)
- tests/test_email_scanner.py:81-84 — `_skip_scanner = pytest.mark.skipif(not _EMAIL_SCANNER_AVAILABLE, ...)` + 16 `@_skip_scanner` decorator usages — DELETE the helper and all 16 uses

Convert-to-fail targets:
- tests/test_version.py:32 — `pytest.skip(f"CLI --version not invokable: {exc}")` → `pytest.fail(f"CLI --version not invokable: {exc}")`
- tests/test_version.py:34 — `pytest.skip("CLI --version returned non-zero")` → `pytest.fail("CLI --version returned non-zero")`

Slow-marker targets (RESEARCH §"Slow-Test Candidates"):
- tests/test_version.py — `test_cli_version_subprocess`
- tests/test_cli_init.py — all 3 tests
- tests/test_cli_version.py — `test_cli_version`
- tests/test_chaos_storage.py — both `test_minio_*` (already skipif env-gated; ALSO add @slow)
- tests/test_dnssec_scanner.py — `test_chaos_lab_integration` (already gated; ALSO add @slow)
- tests/test_saml_scanner.py — `test_chaos_lab_integration` (already gated; ALSO add @slow)
- tests/test_kerberos_scanner.py — `test_samba_dc_integration` (already gated; ALSO add @slow)
- tests/test_cbom_motion_golden.py — `test_generate_fixtures` (already gated; ALSO add @slow)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete stale code-reason skips</name>
  <files>tests/conftest.py, tests/test_cloud_connectors.py, tests/test_email_scanner.py, tests/test_broker_db_schema.py, tests/test_version.py</files>
  <read_first>
    - tests/conftest.py lines 75-115 (dashboard_client fixture)
    - tests/test_cloud_connectors.py lines 1-50, then around lines 154, 162, 179, 232, 256, 280, 303, 327, 360
    - tests/test_email_scanner.py lines 1-100 (find _skip_scanner definition + early imports)
    - tests/test_broker_db_schema.py lines 60-90 (around line 70)
    - tests/test_version.py lines 25-40 (lines 32, 34 skip conversions)
    - .planning/phases/41-ci-stability-scanner-robustness/41-RESEARCH.md §"Skip-Marker Triage Table"
  </read_first>
  <action>
    1. **tests/conftest.py:111** — In the dashboard_client fixture's `except ImportError:` block, replace `pytest.skip("quirk.dashboard not yet implemented")` with `pytest.fail("quirk.dashboard import failed unexpectedly: " + repr(exc))`. Add `as exc` to the `except ImportError` clause if not already there. Keep the rest of the fixture unchanged.

    2. **tests/test_cloud_connectors.py** — Delete every `@pytest.mark.skipif(not _HAS_GCP_MODULE, reason="gcp_connector.py not yet created")` decorator (9 occurrences). The `_HAS_GCP_MODULE` constant itself can stay (or be removed if unreferenced after deletion — verify via grep). Run `python -m compileall tests/test_cloud_connectors.py -q` clean.

    3. **tests/test_email_scanner.py** — Delete the `_skip_scanner` definition (lines ~81-84) AND every `@_skip_scanner` decorator usage (16 occurrences). Also delete any `_EMAIL_SCANNER_AVAILABLE = ...` related guard if it becomes unreferenced. Run compileall.

    4. **tests/test_broker_db_schema.py:70** — Delete the entire `pytest.skip("Column already present...")` line. Inspect the surrounding test function — if removing the skip leaves a test that simply re-asserts what `test_init_db_twice_no_error` already covers, delete the entire test function instead. Per RESEARCH "Idempotency is covered by sibling test as noted." Choose the cleaner option and document the choice in the SUMMARY.

    5. **tests/test_version.py:32 and :34** — Convert both `pytest.skip(...)` calls to `pytest.fail(...)` with the same message. These are defensive skips on environment failure that should hard-fail rather than silently pass.

    6. Run `python -m compileall tests/ -q` clean. Run `pytest --collect-only tests/test_cloud_connectors.py tests/test_email_scanner.py tests/test_broker_db_schema.py tests/test_version.py tests/conftest.py 2>&1 | tail -5` — collection errors must be zero.
  </action>
  <verify>
    <automated>python -m compileall tests/conftest.py tests/test_cloud_connectors.py tests/test_email_scanner.py tests/test_broker_db_schema.py tests/test_version.py -q &amp;&amp; ! grep -q 'pytest.skip("quirk.dashboard' tests/conftest.py &amp;&amp; ! grep -q "_HAS_GCP_MODULE.*gcp_connector.py not yet" tests/test_cloud_connectors.py &amp;&amp; ! grep -q "_skip_scanner" tests/test_email_scanner.py &amp;&amp; ! grep -q 'pytest.skip("Column already present' tests/test_broker_db_schema.py &amp;&amp; ! grep -q 'pytest.skip(f"CLI --version not invokable' tests/test_version.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_skip_scanner" tests/test_email_scanner.py` returns 0
    - `grep -c "@pytest.mark.skipif(not _HAS_GCP_MODULE" tests/test_cloud_connectors.py` returns 0
    - `grep -c 'pytest.skip("quirk.dashboard not yet' tests/conftest.py` returns 0
    - `grep -c "pytest.skip" tests/test_version.py` returns 0
    - `grep -c "pytest.fail" tests/test_version.py` returns at least 2
    - `python -m compileall tests/ -q` exits 0
    - `pytest --collect-only tests/test_cloud_connectors.py tests/test_email_scanner.py tests/test_broker_db_schema.py tests/test_version.py tests/conftest.py` exits 0
  </acceptance_criteria>
  <done>All 13 stale code-reason skips deleted; defensive skips in test_version.py converted to pytest.fail; collection clean.</done>
</task>

<task type="auto">
  <name>Task 2: Mark slow tests + verify meta-gate green + 60s budget</name>
  <files>tests/test_version.py, tests/test_cli_init.py, tests/test_cli_version.py, tests/test_chaos_storage.py, tests/test_dnssec_scanner.py, tests/test_saml_scanner.py, tests/test_kerberos_scanner.py, tests/test_cbom_motion_golden.py</files>
  <read_first>
    - tests/test_version.py (find test_cli_version_subprocess)
    - tests/test_cli_init.py (find all 3 tests)
    - tests/test_cli_version.py (find test_cli_version)
    - tests/test_chaos_storage.py lines 35-75 (test_minio_* tests with existing skipif)
    - tests/test_dnssec_scanner.py around line 475 (test_chaos_lab_integration)
    - tests/test_saml_scanner.py around line 366
    - tests/test_kerberos_scanner.py around line 360
    - tests/test_cbom_motion_golden.py around line 189
    - .planning/phases/41-ci-stability-scanner-robustness/41-RESEARCH.md §"Slow-Test Candidates"
  </read_first>
  <action>
    1. Add `@pytest.mark.slow` decorator to each test listed in the Slow-Test Candidates table:
       - tests/test_version.py::test_cli_version_subprocess
       - tests/test_cli_init.py — every test_* function (all 3)
       - tests/test_cli_version.py::test_cli_version
       - tests/test_chaos_storage.py::test_minio_unencrypted_bucket_produces_high_finding
       - tests/test_chaos_storage.py::test_minio_encrypted_bucket_no_finding
       - tests/test_dnssec_scanner.py::test_chaos_lab_integration
       - tests/test_saml_scanner.py::test_chaos_lab_integration
       - tests/test_kerberos_scanner.py::test_samba_dc_integration
       - tests/test_cbom_motion_golden.py::test_generate_fixtures

    Place `@pytest.mark.slow` ABOVE any existing `@pytest.mark.skipif(...)` decorator (decorators
    apply bottom-up; ordering does not affect behavior but readability prefers slow first).

    Ensure `import pytest` is present at the top of each file (most already have it).

    2. Run the Plan 01 skip-registry meta-gate to verify it now passes:
       `pytest tests/test_skip_registry.py -x`
       This should be GREEN. If it still reports unregistered skips, the registry in
       tests/skip_registry.py needs the corresponding line numbers updated (line numbers may
       have shifted due to deletions in Task 1). Update ALLOWED_SKIPS line numbers to match
       current file state.

    3. Run a quick-suite timing check (best-effort — acceptance criterion is structural; 60s budget is verified by UAT-41-04):
       `time pytest -m 'not slow' tests/ 2>&1 | tail -5`
       If significantly over 60s, identify the offending test (look for >2s "slowest" pytest
       output) and add @pytest.mark.slow. Document any added beyond the canonical list in the SUMMARY.
  </action>
  <verify>
    <automated>python -m compileall tests/ -q &amp;&amp; grep -q "@pytest.mark.slow" tests/test_version.py &amp;&amp; grep -q "@pytest.mark.slow" tests/test_cli_init.py &amp;&amp; grep -q "@pytest.mark.slow" tests/test_cli_version.py &amp;&amp; grep -q "@pytest.mark.slow" tests/test_chaos_storage.py &amp;&amp; grep -q "@pytest.mark.slow" tests/test_dnssec_scanner.py &amp;&amp; grep -q "@pytest.mark.slow" tests/test_saml_scanner.py &amp;&amp; grep -q "@pytest.mark.slow" tests/test_kerberos_scanner.py &amp;&amp; grep -q "@pytest.mark.slow" tests/test_cbom_motion_golden.py &amp;&amp; pytest tests/test_skip_registry.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "@pytest.mark.slow" tests/test_version.py tests/test_cli_init.py tests/test_cli_version.py tests/test_chaos_storage.py tests/test_dnssec_scanner.py tests/test_saml_scanner.py tests/test_kerberos_scanner.py tests/test_cbom_motion_golden.py | wc -l` returns 8
    - `pytest tests/test_skip_registry.py -x` exits 0
    - `pytest -m 'not slow' --collect-only -q tests/ 2>&1 | tail -1` reports the deselected count is at least 9 (one per slow-marked test)
    - `python -m compileall tests/ -q` exits 0
  </acceptance_criteria>
  <done>All slow-test candidates marked; Plan 01 meta-gate green; default pytest run scopes correctly to non-slow tests.</done>
</task>

</tasks>

<verification>
- `pytest -m 'not slow' -x` exits 0 (full default suite green)
- `pytest tests/test_skip_registry.py -x` exits 0 (meta-gate green)
- Wall-clock for `pytest -m 'not slow' tests/` is under 60s (UAT-verified)
</verification>

<success_criteria>
- All 13 stale code-reason skips deleted (D-04 expanded set)
- Defensive skips in test_version.py converted to pytest.fail
- 9 slow-test candidates marked @pytest.mark.slow
- Plan 01 skip-registry meta-gate passes
- Default pytest run scopes to non-slow tests; <60s budget achievable
- CI-01, CI-02, CI-03 met
</success_criteria>

<output>
After completion, create `.planning/phases/41-ci-stability-scanner-robustness/41-05-SUMMARY.md`.
</output>
