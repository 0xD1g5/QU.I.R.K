---
context: default
phase: none — milestone boundary (v5.20 initialized, Phase 187 not started)
task: 0
total_tasks: 0
status: clean_pause
last_updated: 2026-09-07T19:26:50.830Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: `phase.complete` is UNSAFE — the verb marks phases/milestones complete without
      checking their plans are done (reproduced live at 5/7 plans with impossible
      `completed_plans: 142`). All phase/milestone closes on this machine are hand-written under
      the **pre-image + signature-diff protocol** (CLAUDE.md §GSD state verbs, clause h).
- [ ] CONSTRAINT: `phases.clear` is DESTRUCTIVE in this repo — v5.17–v5.19 phase dirs were never
      archived by complete-milestone; running the verb at this boundary would have deleted the
      only copies of 15 phases' SUMMARYs/VERIFICATIONs. Archive by `mv` to
      `milestones/vX-phases/` instead (done 2026-09-07), and check `git status` afterward: some
      phase files ARE git-tracked and moves must be recorded as renames (`git add -f` new path).
- [ ] CONSTRAINT: any mutating `gsd-sdk`/`gsd-tools.cjs` state verb requires a pre-image of
      `.planning/STATE.md` first and a full diff against the two named corruption signatures
      after. `state.milestone-switch` ran clean this session but deleted the entire old
      `## Current Position` narrative — one live watch item survived only because the diff was
      actually read (rescued to HORIZON.md).

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| phase.complete semantic class | Writes well-formed WRONG values; both corruption signatures read clean on it | blocking | Hand-write closes; grep for unchecked plan boxes on "completed" phases |
| phases.clear on unarchived dirs | Would delete only copies of phase artifacts | blocking | Archive by mv + record git renames; verify Destructive Archive Gate output on commit |
| Hand-written ledger completeness | The initial Open-Item Ledger missed 4 v5.16 carried-forward rows the same day it was written; a PM spot-question caught them | advisory | TRIAGE-09 (Phase 189) builds the derived CI gate; until it ships, re-check ledger completeness by hand at every boundary |
| gsd commit wrapper false failure | `gsd-sdk/gsd-tools commit --files` aborts on gitignored `.planning/` despite staging succeeding | advisory | Use plain `git commit <tracked-paths>` (add `-f` only for deliberate new tracking) |

<current_state>
Milestone v5.20 "Release & Correctness Drain" fully initialized 2026-09-07 and committed
(7129cfa, 9f57e3de, 55a78651, 53bf8ec1, b5c491cc + rename-recording commit). Nothing mid-flight;
this is a clean boundary pause. `.planning/phases/` is empty; Phase 187 has no directory yet.
STATE.md frontmatter: milestone v5.20, status planning, 0/4 phases.
</current_state>

<completed_work>

This session (the /gsd-new-milestone boundary):
- Milestone-boundary doc review: version drift PASS (all at 5.18.0 awaiting v5.19 tag), coverage
  gaps PASS, 3 stale Obsidian notes re-synced (_QUIRK-Hub.md rebuilt, vault Roadmap/Requirements)
- Backlog reconciliation audit: 92 BACK-* IDs classified with per-item evidence
  (`.planning/reports/backlog-reconciliation-2026-09-07.md`)
- HORIZON.md promoted to canonical **Open-Item Ledger** (+ same-day completeness patch)
- PROJECT.md Current Milestone + Active sections reconciled (were stale at v5.16)
- STATE.md reset via `state.milestone-switch` (protocol-verified clean; watch item rescued)
- REQUIREMENTS.md: 10 reqs (REL-01, SCORE-06/07, TRIAGE-03..09)
- ROADMAP.md: 4 phases 187–190, user-approved
- v5.17–v5.19 phase dirs archived to `milestones/vX-phases/`; 9 tracked files recorded as renames
- Memory updated: `feedback_pm_milestone_planning.md` (ledger canonicalization note)
</completed_work>

<remaining_work>

- Phase 187: Release v5.19 (REL-01) — gating wave, everything else waits on it
- Phase 188: Scoring Integrity (SCORE-06, SCORE-07)
- Phase 189: Config Correctness Drain (TRIAGE-03/04/05/08/09 — includes the derived backlog gate)
- Phase 190: Scanner Port & Protocol Drain (TRIAGE-06/07)
- Phases 189/190 are independent of each other; parallelizable after 187
</remaining_work>

<decisions_made>

- HORIZON.md Open-Item Ledger = single canonical open-item source (BACK-89 precedent justified it)
- v5.20 scoped small by user-as-PM: release + scoring core + all correctness riders; UX set /
  phantom certs / 999.96 / Exposure Map deferred (visible in ledger, not lost)
- TRIAGE-09 derived gate pulled INTO v5.20 (initially deferred; user's items-not-forgotten concern
  plus the same-day ledger miss made the mechanical gate non-optional)
- Research skipped: all items pre-researched with file:line evidence; SCORE-06 formula design
  happens at Phase 188 discuss/plan
- SCORE-06 is NOT a drive-by: formula change moves historical numbers, needs recorded
  migration/communication decision
</decisions_made>

<blockers>
- None. Clean pause.
</blockers>

## Required Reading (in order)
1. `CLAUDE.md` §GSD `state.*` Verb Integrity (esp. clause h) — the toolchain constraints above
2. `.planning/ROADMAP.md` "## Current Milestone: v5.20" — phases, success criteria, structure rationale
3. `.planning/REQUIREMENTS.md` — the 10 requirement texts (each cites its backlog/todo source)
4. `.planning/HORIZON.md` "## Open-Item Ledger" — what was deliberately deferred and why
5. `.planning/reports/backlog-reconciliation-2026-09-07.md` — evidence base for TRIAGE-09's gate

## Critical Anti-Patterns (do NOT repeat these)
- [LEDGER-BY-HAND]: hand-derived open-item lists rot same-day → TRIAGE-09 builds the run-time
  derived gate; until then, re-derive rather than trust
- [PHASE-CLOSE-VIA-VERB]: `phase.complete`/`milestone.complete` write wrong values cleanly →
  hand-edit closes, pre-image + signature-diff every mutating state verb

## Infrastructure State
- No services running; chaos lab down; no background tasks
- `config-lab-core.yaml` untracked at repo root = repro artifact for 999.93/TRIAGE-03 — keep it
- `output-narrow/`, `output-widened/` untracked at repo root — pre-existing, purpose unknown, left alone
- Local editable install: was healthy as of v5.19 close; REL-01 requires `pip install -e . --no-deps` after the version bump
- `gsd-sdk` resolves to npx hash `4db0de1f85c3165e` (patched install; a GSD update changes the hash and silently drops the TOOL-05 patches — re-verify before trusting state verbs)

## Pre-Execution Critique Required
- Not applicable — pausing at milestone boundary, no design-vs-execution gap open.

<context>
The session was a full /gsd-new-milestone boundary run with two extra PM-directed workstreams:
the backlog↔HORIZON reconciliation (user's explicit ask — "ensure items marked months ago are not
forgotten") and the resulting ledger canonicalization. The milestone theme is deliberately
correctness-leaning right after an ops cycle; v5.21 should lean capability (recorded in
PROJECT.md Key context). Everything the user cares about tracking-wise now lives in HORIZON's
ledger; its honesty gate ships in Phase 189.
</context>

<next_action>
Start with: `/gsd-discuss-phase 187` (or `/gsd-plan-phase 187` to skip discussion, or
`/gsd-autonomous` to run the whole milestone). Phase 187 = cut and publish v5.19.0: bump
pyproject.toml + README + CHANGELOG.md + UAT-1-02/header, `pip install -e . --no-deps`, tag
`v5.19.0`, verify release workflow green incl. Windows asset.
</next_action>
