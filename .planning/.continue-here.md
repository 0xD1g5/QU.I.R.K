---
context: research
phase: demo-prep (Phase 206 PAUSED — see 999.114)
task: 8
total_tasks: 11
status: in_progress
last_updated: 2026-09-14T02:00:59.876Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each was discovered through actual failure in the session being paused.

- [ ] **CONSTRAINT: a measurement you wrote can report its own blind spot as a confident zero.** This
      happened **three times in one session** — two bad extraction patterns and a drifted shell
      working directory — and each produced a confident, wrong conclusion that read as good news.
      **Structural mitigation required:** every counting/grepping check must include a
      **known-positive control** — a value you can predict independently and that MUST be non-zero.
      If the control is zero, the measurement is broken, not the subject. Do not accept a `0` from
      your own extractor as evidence of anything.

**Do not proceed until the box above is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Extractor reports blind spot as a confident zero | 3 occurrences: an `awk` range that matched no `files_modified` block returned `0` for all 13 plans (reading as "the fence holds"); a slug grep returned empty because slugs were bullets not filenames; a drifted `cwd` made real test files look missing | **blocking** | Include a known-positive control in every count. `206-13` MUST have doc-artifact hits; the files I just created MUST exist. A control that fails means the instrument failed. |
| Green tests proving nothing | ~3,200 lines of scoring tests were green the entire time the readiness score was blind to a 31-host vulnerable estate. `test_score_always_bounded_1000_iterations` is passed perfectly by a function returning the constant `91` | **blocking** | Red-proof every new gate/property against the known-broken state BEFORE trusting it. A property suite that passes on code you know is broken is testing nothing. |
| Mutating GSD `state.*` verbs corrupt STATE.md | `state.begin-phase` dropped the `last_activity` frontmatter key outright, fabricated a `stopped_at` value out of truncated body prose, and orphaned a continuation line under `## Current Position`. Logged as HIT #11 | advisory | Pre-image `.planning/STATE.md`, run the verb, `diff` the FULL file, hand-correct. Never trust the verb's `{"updated": [...]}` return — it omitted all three defects. |
| Planning from a derived artifact instead of the source | `206-08` narrowed `UAT-7-16` from 5 Pass Criteria bullets to 3 because it followed `docs/uat-coverage-gaps.md`'s lossy one-line summary instead of the case's own block in `docs/UAT-SERIES.md` | advisory | Always read the primary artifact. Derived summaries lose detail silently. |
| Fencing off a site as "already correct" without checking | The orchestrator told the `999.113` executor that `agility_high_impact_ratio` "was always correct — DO NOT TOUCH IT". It had the same defect (dividing by 398 findings, 330 of them INFO). Caught only by doing the arithmetic | advisory | Verify "this one is fine" claims with the same rigour as "this one is broken" claims. |

<current_state>
On `main`, clean working tree, **38 commits ahead of `origin/main` — nothing has been pushed.**

Phase 206 (Dashboard UI Coverage Drain) is PAUSED at 5 of 13 plans, deliberately, held by a
mechanical gate. Current work is demo readiness for a client presentation on **2026-09-18**.

`999.113` (readiness-score denominator defect) is CLOSED. `999.115` (usable-range compression) is
filed at P1 as its successor. The next task is the **scoring property suite P1-P6** — the operator's
explicit resume instruction.
</current_state>

<completed_work>

- Phase 206 paused with a 4-layer guarantee, one mechanical — `6bda9dfa`
- chaos-lab branch merged (MinIO -> quay.io fix + 31-host multihost estate) — `37a8c496`
- phase-206 branch merged so the resume gate actually runs in CI — `5d91e1e6`
- `999.113` denominator fix + red-proof pair + CHANGELOG — `0b0ed1c7`, `a49c7dd6`/`9fadfaa2`, `e0aec9e3`
- Unregistered skip marker removed from the resume gate — `c5a2dbe0`
- `999.115` filed at P1, `999.113` closed in the ledger — `546804bc`
- CI on `main`: 2 failures -> 1 (the remaining one is a diagnosed false positive)
</completed_work>

<remaining_work>

1. **Scoring property suite P1-P6 — START HERE.** See `<next_action>`.
2. **Calibration ladder** — named reference estates. Operator set the bottom rung
   (multihost estate must score **below 40**); middle rungs still need the operator's judgement.
3. **`/print` sidebar defect** — `App.tsx:60` renders `<Sidebar />` unconditionally and `/print` is a
   `<Route>` inside that shell (`App.tsx:76`); no `print:hidden` / `@media print` anywhere in `src/`;
   `print.tsx`'s `PRINT_CSS` has no sidebar-hiding rule. The PDF export uses this same route.
4. **Scoring-derivation summary** for the Friday discussion — publish as an Artifact. Default plan is
   layered (client-safe narrative, then an appendix with arithmetic/weights/known-limits).
</remaining_work>

<decisions_made>

- **Pause Phase 206** rather than finish it — internal test coverage, zero demo-visible value.
- **Guarantee the resume with a TEST, not a note.** `BACK-A11Y-01` was invisible ~3 months after a
  v5.0 archive; `BACK-89` drifted P2->P1 invisible for 3.5 months; v5.24 slipped three boundaries.
  In all three the work WAS written down — writing it down is what failed.
- **`999.113` D1**: every ratio divides by the population its own numerator is drawn from. Not a new
  principle — `agility_high_impact_ratio` already did it and `_endpoints_assessed`'s docstring
  already instructed it; 34 sites were inconsistent with the one site that already said so.
- **`999.113` D5**: do not tune to a target. Held — the executor reported 87 against a <40 target and
  changed no weights.
- **Choose the next scoring model BY MEASUREMENT, not argument.** Attempt #1 (Phase 188 SCORE-06)
  failed because nothing could tell whether a change had helped.
</decisions_made>

<blockers>
- **`main` CI, 1 red:** `test_back_star_ci_enforced_leg`. DIAGNOSED FALSE POSITIVE — `BACK-51` is
  properly closed at `HORIZON.md:49`; the gate keys on `(ID, title)` and matches a narrative prose
  line in the ARCHIVED `v5.23-ROADMAP.md` that says "BACK-51 closed by recorded decision". Filed for
  v5.25. Deliberately not fixed — the honest fix is at the gate and was out of demo scope.
- **Score calibration gap:** 87 against a <40 target. Tracked as `999.115` at P1, not a bug to patch.
</blockers>

## Required Reading (in order)

1. `.planning/HORIZON.md` Open-Item Ledger, rows **999.115 / 999.113 / 999.114** — the single source
   of truth for open items. `999.115` carries the measurement that decides the model question.
2. `.planning/decisions/999.113-denominator-semantics.md` — D1-D5 + D1(d). The shape any successor
   decision doc should follow.
3. `.planning/STATE.md` § "Phase 206 PAUSE RECORD" — what's done, what's left, the doc-fence invariant.
4. `tests/test_paused_phase_resume_gate.py` — the mechanism holding Phase 206. Do not weaken it.
5. `quirk/intelligence/scoring.py` — `_ratio`, `_apply_weighted_impacts` (25.0 start, clamp `[0,25]`),
   `SCORE_WEIGHTS`.

## Critical Anti-Patterns (do NOT repeat these)

- **[ANTI-PATTERN] Trusting your own zero** → include a known-positive control in every count.
- **[ANTI-PATTERN] Shipping a gate you haven't seen fail** → red-proof it against the broken state first.
- **[ANTI-PATTERN] Tuning a score to hit a target number** → forbidden by `999.113` D5, and it is
  exactly what makes a score indefensible in a client room.

## Infrastructure State

- **git**: on `main`, clean tree, **38 commits ahead of `origin/main`**. Nothing pushed.
- **Branches left in place** (do NOT `git clean` or delete): `phase-206-dashboard-ui-coverage`
  (merged, but its gitignored phase artifacts live only on disk), `fix-chaos-lab-minio-registry` (merged).
- **vitest baseline**: 59 files, 414 passed, 2 skipped.
- **pytest**: 5050 passed, 6 failed — all 6 verified pre-existing against baseline `6cfcfcc3`.
- **Docker / chaos lab**: not running. The `multihost` profile is now on `main`; a live re-scan would
  supply the real DAR/motion weakness counts the `999.113` reconstruction lacked.

<context>
The strategic frame that matters more than any individual task:

**The existing scoring test suite asserts mechanism, bounds, and rendering — never MEANING.** That is
why attempt #1 at scoring failed, and why `999.95` -> `999.113` -> `999.115` are three instances of
one defect class, each found by accident rather than by a test.

The property suite is the missing test class. It is also the *instrument* that makes every later
model decision measurable instead of arguable. **Do not begin by picking a model shape.** Build the
instrument, then run the candidates through it.

Measured range, for grounding: PERFECT estate -> **100**; MAXIMALLY BROKEN (all ratios = 1.0) ->
**19**; the real 31-host purpose-built vulnerable estate -> **87**. The floor is reachable in
principle; reaching it requires an estate that cannot exist. The model spends ~80% of its scale on
impossible infrastructure.
</context>

<next_action>
Start with: create `tests/test_score_properties.py` encoding P1-P6, and **red-proof it against
current `main` first** — P1, P2, P4 and P6 must demonstrably FAIL today, before any further fix. A
property suite that passes on known-broken code is testing nothing, which is precisely the state the
existing ~3,200 lines are in.

  P1  Monotonicity — adding a real weakness never RAISES the score
  P2  Scan-config invariance — identical infrastructure, wider port scan -> identical score
      (already pinned by tests/test_score_denominator_999_113.py; fold in or cross-reference)
  P3  Severity coherence — an estate with CRITICALs scores below one with only MEDIUMs
  P4  Dynamic range — the multihost reference estate scores BELOW 40  <- will fail at 87, and that
      failing test IS the deliverable: it converts "87 reads as not bad" into a standing numeric
      statement of exactly how far off calibration the model is
  P5  Explanation fidelity — the drivers shown to the client ARE the largest penalties
  P6  Sensitivity — a material posture change produces a material score change (not 91 -> 90)

Then the calibration ladder, which needs the operator's bands for the middle rungs.
</next_action>
