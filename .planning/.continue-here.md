---
context: default
phase: none — milestone boundary (v5.21 initialized, no phase started)
task: n/a
total_tasks: n/a
status: milestone_initialized
last_updated: 2026-09-08T20:04:57.934Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| `phase.complete` / `milestone.complete` trusted to close work | SEMANTIC toolchain defect class (prior sessions, reproduced live): writes well-formed but WRONG values without checking plans are done | blocking | Every phase/milestone close is hand-written under the pre-image + signature-diff protocol (CLAUDE.md TOOL sections; ROADMAP.md standing constraints) |
| Mutating `gsd-sdk`/`gsd-tools` state verb run bare | Both installs patched, but npx cache dirs are content-addressed — a GSD version bump silently discards patches | blocking | Pre-image `.planning/STATE.md`, run verb, scripted diff against both corruption signatures + value semantics. Verified clean this session for `state.milestone-switch` |
| Merging `origin/UX-Updates` | Branch diverged 2026-02-18; a merge is +4,327/−143,607 across 817 files — deletes ~7 months of main | blocking | VERDICT-01 (Phase 194) is cherry-pick-only, commit `f05e7dc7`; written into REQUIREMENTS.md and ROADMAP.md |
| `gsd commit` wrapper on `.planning/` files | Wrapper falsely reports failure on this repo's gitignored-but-grandfathered-tracked planning files | advisory | Use plain `git add -f <tracked planning files> && git commit` (worked 4x this session) |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Milestone **v5.21 Dashboard Parity & Exposure Capability** is fully initialized and committed as of
2026-09-08. Nothing is mid-flight: PROJECT.md, STATE.md (status: planning), REQUIREMENTS.md
(13 reqs), and ROADMAP.md (Phases 191–195, 13/13 mapped) are all written, PM-approved, and
committed (`b7033647`, `18fe73b4`, `2c4a8fc2`, plus a todo-tag commit). Research lives untracked at
`.planning/research/` (STACK/FEATURES/ARCHITECTURE/PITFALLS/SUMMARY). Obsidian vault is fully
synced (hub callouts, Roadmap, Requirements, phase notes 187–190). No phase directory exists yet.
</current_state>

<completed_work>

- Milestone-boundary doc review: version drift PASS (5.19.0 everywhere; v5.20 deliberately untagged),
  coverage gaps PASS, 2 stale Obsidian items found → both fixed by session end
- Backlog item **999.104** (CLI config ↔ dashboard parity) filed with Feasibility & Effort
  (`.planning/backlog/999.104-cli-config-dashboard-parity/IDEA.md`) + HORIZON ledger row
- PROJECT.md Current Milestone + Active sections rewritten for v5.21; HORIZON rationale-log entry added
- STATE.md switched via `gsd-sdk query state.milestone-switch` — verified clean (pre-image + signature diff)
- 4-researcher + synthesizer research pass → `.planning/research/SUMMARY.md` (confidence HIGH)
- REQUIREMENTS.md: PARITY-01..04, OBS-01..02, SPKI-01..02, VERDICT-01, DASH-09, MAP-01..03
- ROADMAP.md: Phase 191 SPKI → 192 Config Visibility+Skip Obs → 193 Connector/Credential Parity →
  194 Advanced Fields+Verdict+Phantom-Cert → 195 Exposure Map (spike-gated)
- Todo `dashboard-cert-view-phantom-tls-rows` tagged `resolves_phase: 194`
</completed_work>

<remaining_work>

- Phase 191 (SPKI Fingerprint Persistence): discuss → plan → execute; then 192–195 per ROADMAP.md
- Optional pending decision: add proposed "Worktree Isolation" section to CLAUDE.md (see blockers)
</remaining_work>

<decisions_made>

- v5.21 = capability cycle (cadence owed after v5.19/v5.20 ops+correctness); "big but worth it" per PM
- 999.104 scoped to tiers 1–3; tier 4 (server-side config.yaml editing) OUT — own threat model needed
- 999.96 split: feature half = 999.104 tier 2 (Phase 193); observability half its own reqs (Phase 192)
- Version v5.21, not v6.0
- Exposure Map: verified-edges-only, spike (MAP-01) gates rendering inside Phase 195; score firewall
  test (MAP-03) written in the same phase
</decisions_made>

<blockers>
- None blocking. One pending human decision (non-blocking): approve the proposed CLAUDE.md
  "Worktree Isolation" note (use_worktrees=false rationale + .planning//CLAUDE.md invisibility in
  worktrees + absolute-paths rule). Recommended this session; not yet approved.
</blockers>

## Required Reading (in order)
1. `.planning/ROADMAP.md` — v5.21 phase structure + standing-constraints block
2. `.planning/REQUIREMENTS.md` — the 13 requirements with research guardrails baked into wording
3. `.planning/research/SUMMARY.md` — phase-by-phase rationale, pitfalls, build order; Phases 193/195
   flagged as needing deeper research at plan time
4. `CLAUDE.md` — GSD `state.*` verb integrity sections (TOOL-01..05) before touching STATE.md

## Critical Anti-Patterns (do NOT repeat these)
- See table above — the four rows are the operative set for this boundary.

## Infrastructure State
- No services running started by this session. Docker/chaos-lab state not touched.
- Untracked repo-root leftovers from v5.20 live work (pre-existing, deliberately left):
  `config-lab-*.yaml` (config-lab-core.yaml is the 999.93 repro artifact per HORIZON),
  `output-190/`, `output-narrow/`, `output-uat188/`, `output-widened/`.
- Research files under `.planning/research/` are untracked by design (public-repo planning exclusion).

<context>
This session ran the full /gsd-new-milestone workflow end to end at the v5.20 → v5.21 boundary.
The PM's own ask (CLI→dashboard config parity) was filed as 999.104 and turned out to subsume
999.96's feature half. Research was strongly convergent; the roadmapper followed its 5-phase spine
unchanged. Phase 191 is rated "standard patterns" (in-repo precedent:
HardwareDevice.ssh_host_key_fingerprint), so discuss can be light — the one thing its plan MUST do
is thread the new column through PushEnvelope + merge/scan.py projection with a round-trip
sensor-push test (v5.8 B-01 recurrence guard), which is already written into SPKI-01's wording.
</context>

<next_action>
Start with: `/clear` then `/gsd-discuss-phase 191` (or `/gsd-plan-phase 191` directly — research
says standard patterns; or `/gsd-autonomous` to run the milestone).
</next_action>
