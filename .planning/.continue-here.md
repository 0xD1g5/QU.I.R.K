---
context: default
phase: 86-scoring-correctness-hotfix
milestone: v4.10.1
task: null
total_tasks: 3
status: milestone_complete
last_updated: 2026-05-22T12:52:56Z
---

<current_state>
**Clean stopping point — no in-progress work.**

v4.10.1 (Scoring Correctness Hotfix) is fully shipped:
- Phase 86 marked complete (commit `7abc1c5`); 3/3 plans done; VERIFICATION.md PASSED 5/5
- HUMAN-UAT closed PASS with gauge color match confirmed post-hard-refresh (commit `bce265b`)
- Release tag/changelog landed (`94ac361`, `e55f88b`)

Only uncommitted change: `.planning/STATE.md` — a pure counter normalization (`total_phases` recomputed from carry-over `60` → `1` to match v4.10.1's actual scope; `percent` flipped `3` → `100`).

Side note: during this session also ran `/gsd-update` from `1.41.2` → `1.42.3`. **Restart Claude Code on resume** to pick up new skill definitions (notably the new `gsd-surface` skill).
</current_state>

<completed_work>

Completed in this session:
- Phase 86 closeout fully landed (no new code — verification and bookkeeping only)
- GSD toolchain updated: 1.41.2 → 1.42.3 (global `~/.claude` install, clean reinstall)

Phase 86 plans (already complete before this session):
- 86-01 — Backend scoring math fix (SCORE-FIX-01..03) — done
- 86-02 — Frontend gauge fix (GAUGE-01..03) — done
- 86-03 — Release engineering + HUMAN-UAT (RELEASE-01..02) — done, commit `bce265b`
</completed_work>

<remaining_work>

**Nothing within Phase 86 / v4.10.1.** The milestone is shippable.

Decision branch for next session:

1. **Close out v4.10.1 cleanly** — commit `STATE.md` counter normalization, then `/gsd-complete-milestone v4.10.1` to archive it.
2. **Open v5.0** — `/gsd-new-milestone v5.0`. Pre-locked Phase 01 scope (already captured in `.planning/REQUIREMENTS.md` as Future Requirements):
   - `EVIDENCE-TALLY-01` — 3 subscores (Hygiene, Modern TLS, Data at Rest) report `25` despite HIGH/CRITICAL findings. Audit `quirk/intelligence/evidence.py::build_evidence_summary` for missing penalty-counter increments.
   - `RENDER-CLI-01` — Audit `quirk/reports/executive.py` and `quirk/reports/html_renderer.py` for same backend-scale vs render-scale mismatch as the dashboard. Apply equivalent normalization.
   - `RENDER-PDF-01` — Same audit for the Playwright-rendered PDF report.
</remaining_work>

<decisions_made>

- **Phase 86 was a single-phase vertical MVP slice** — backend math + frontend gauge + release engineering shipped together because splitting would yield half-fixes that contradict each other (backend without frontend → different wrong number; frontend without backend → meaningless).
- **Three render-side bugs deferred to v5.0 Phase 01** rather than absorbed into the hotfix. Same bug class is likely present in CLI/HTML/PDF reports and evidence summarizer, but a full-stack scoring correctness sweep is the right shape — mixing into a hotfix risks shipping new bugs.
- **`v4.10.0` release tag intentionally NOT pushed** to remote (local-only). PyPI distribution name is `quirk-scanner` (PEP 541 rejection of `qu-i-r-k` and `quirk` — see memory `feedback_pypi_pep541_dry_run`).
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — confirms v4.10.1 status: completed (100%)
2. `.planning/phases/86-scoring-correctness-hotfix/VERIFICATION.md` — verifier PASSED 5/5
3. `.planning/phases/86-scoring-correctness-hotfix/HUMAN-UAT.md` — UAT closure record
4. `.planning/REQUIREMENTS.md` — see **Future Requirements (deferred to v5.0 — Stabilization)** for pre-locked Phase 01 scope
5. `.planning/ROADMAP.md` — milestone progression context
6. MEMORY: `feedback_pypi_pep541_dry_run`, `feedback_local_import_shadow_trap`, `feedback_optional_extra_import_trap` — landmines surfaced during v4.10 UAT that v5.0 work should respect

## Critical Anti-Patterns (do NOT repeat)
- **PEP 541 dry-run is mandatory for PyPI name changes** — `pip index versions` is insufficient; must use Pending Publisher dry-run on PyPI. (Caught at v4.10 UAT-85-11.)
- **Local-import shadow trap** — adding `from X import Y, NAME` inside a function branch where `NAME` is module-scope makes `NAME` function-local for the whole function (Python compile-time rule). AST gate at `tests/test_run_scan_init_db_scope.py` prevents recurrence.
- **Optional-extra import trap** — never `import foo` unconditionally at top level if `foo` is only declared in `[extras]`. Use lazy-import in the using function. (Caught v4.10 post-ship: `pypdf` in `html_renderer.py` masked staleness-gate redness.)

## Infrastructure State
- Git branch: `main`, clean except `.planning/STATE.md` (counter normalization, safe to commit)
- GSD toolchain: `1.42.3` (just updated; restart required)
- Local tag `v4.10.0` exists; **not** pushed to remote
- PyPI distribution name: `quirk-scanner` (NOT `quirk` or `qu-i-r-k` — both squatted/rejected)
- No running dev servers, watchers, or chaos lab profiles to babysit

<context>
Phase 86 was bookkeeping completion of a 3-plan hotfix that was already executed and verified. The session ended at a natural seam between milestones. The next-actions branch is binary and low-stakes: either formally close v4.10.1 or open v5.0 (which will absorb the three pre-locked render-side bugs). Either path can be picked up cold without re-deriving any prior context.

The session also performed an out-of-band GSD update — orthogonal to the work state but requires a Claude Code restart before the next skill invocation (so `/gsd-new-milestone` or `/gsd-complete-milestone` lands on the fresh skill definitions).
</context>

<next_action>
Start by:

1. **Restart Claude Code** (GSD 1.42.3 needs a fresh skill loader).
2. Then run **one** of:
   - `git add .planning/STATE.md && git commit -m "chore(state): normalize v4.10.1 progress counters"` followed by `/gsd-complete-milestone v4.10.1` to archive the shipped milestone, OR
   - `/gsd-new-milestone v5.0` to open Stabilization with EVIDENCE-TALLY-01 / RENDER-CLI-01 / RENDER-PDF-01 already pre-locked as Phase 01 scope (defined in `.planning/REQUIREMENTS.md` → Future Requirements section).

Recommended: close v4.10.1 first, then open v5.0 — cleaner state transitions.
</next_action>
