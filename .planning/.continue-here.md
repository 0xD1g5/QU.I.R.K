---
context: default
phase: demo-prep (Phase 206 PAUSED at 5/13; v5.24 phases 203/204/205 COMPLETE)
task: 5
total_tasks: 7
status: paused
last_updated: 2026-09-14T16:05:00.000Z
---

# STATUS — tasks 1 and 2 are DONE; read this before re-doing them

**Commit `344f7b30` (2026-09-14) closed tasks 1 and 2.** A resuming session must NOT re-run them as
if unstarted:

- **Task 1 (real multihost scan) — DONE.** Measured 2026-09-14 under scoring v3: 400 findings,
  5 CRITICAL / 14 HIGH / 33 MEDIUM / 16 LOW / 332 INFO, readiness **15/100 POOR**. The full scan
  command is **19 seconds** wall clock, reproducible across three consecutive runs.
- **Task 2 (oracle rebuild) — DONE.** All 31 hosts, generated by
  `quantum-chaos-enterprise-lab/scripts/gen_multihost_oracle_table.py`. `README.md` and
  `docs/chaos-lab.md` corrected in the same commit.
- **Task 3 — PARTIAL.** The measured R6 evidence exists and the ladder ceiling holds against it
  (real 15 vs fixture 18 vs ceiling 30). The rung was NOT added; plan is in
  `.planning/todos/pending/r5-ladder-fixture-is-not-the-measurement-it-claims.md`.
- **Task 4 — PARTIAL.** The operator's Friday walkthrough is now CONFIRMED and recorded in
  `HANDOFF.json` under `demo_walkthrough_confirmed` (7 segments, 30-45 min). The visual PDF
  eyeball remains user-led.

**Next:** the segment 5->6 number-disagreement decision (see the new P1 below), then the hygiene
tail, tasks 5-7.

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each was discovered through actual failure, not predicted.

- [x] **CONSTRAINT: do not hand-write an expected finding for a host you have not measured.**
      **DISCHARGED 2026-09-14 by measurement, not by inference.** The oracle's per-host table now
      covers all 31 hosts and every row is generated from `findings-*.json` by a committed script,
      so the constraint is now structurally enforced rather than remembered. It earned its keep:
      two of the previous ten hand-written rows had the severity COUNT right and the MEANING wrong
      (10.80.0.50's two HIGHs are plaintext-HTTP on the MinIO ports, not the unencrypted-bucket
      finding the oracle claimed; 10.80.0.31's MEDIUM is a blocked TLS handshake, not a
      cleartext-broker finding). **Regenerate, never hand-edit**, after any re-measurement.

- [ ] **CONSTRAINT: verify `SCORING_VERSION` INSIDE the prober before quoting any score.**
      `lab.sh up` passes no `--build`, so a stale `chaoslab-mh-prober` image is reused silently. On
      2026-09-14 that made a scan report **91/100** from the superseded scoring v2 while `main` was
      on v3 — the same evidence scores **15/100**. The run exits 0 and prints a confident number
      either way; `Platform version` is NOT a staleness signal (pyproject has not bumped since the
      PyPI release). **Structural mitigation:**
      `docker exec chaoslab-mh-prober-1 python -c "from quirk.intelligence.scoring import SCORING_VERSION; print(SCORING_VERSION)"`
      — and rebuild with `-p chaoslab` or compose tags a different image and the rebuild does
      nothing. Filed at `.planning/todos/pending/lab-sh-up-silently-reuses-stale-prober-image.md`.

- [ ] **CONSTRAINT: the CLI and the dashboard disagree on the SAME scan — do not show both.**
      Report 15/100 with 5 CRITICAL; dashboard 19/100 with 7 CRITICAL, on one clean run. Root
      cause: the SAML IdP publishes ONE certificate under `use=signing` AND `use=encryption`, so
      two `crypto_endpoints` rows exist and `scan.py:480-498` emits one CRITICAL per ROW. One weak
      1024-bit key becomes two CRITICALs, and CRITICAL count drives the score cap. Separately, the
      CLI omits the finding entirely despite scoring it. **A third, independent issue:**
      `SESSION_BRACKET = 5 min` merges scans taken under five minutes apart (34 certs, 14
      CRITICAL) — at a 19-second scan this is easy to trigger by accident. **Scan once.** Filed at
      `.planning/todos/pending/cli-dashboard-score-divergence-same-scan.md`.

- [ ] **CONSTRAINT: `pytest -k "scor"` misses a THIRD of the scoring surface.** 36 test files
      import `quirk.intelligence.scoring`; **12 have no "scor" in the path**. Two genuine
      regressions sat red in `test_pqc_agility_bonus.py` through an entire pause *while that
      documented pre-commit check was being followed*. **Structural mitigation:** select from the
      import graph, never the filename —
      `.venv/bin/python -m pytest -q -p no:randomly $(grep -rln "quirk.intelligence.scoring" tests/)`

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Trusting a prior handoff's classification of a failing test | The previous handoff listed 11 tests as "pinned values D's curve moved". One — `test_identity_surface::test_weak_kerberos_lowers_score` — was not stale: it had caught a live P1 violation (3 weak-Kerberos findings moved an estate 77 -> 84). | **blocking** | Before "repairing" any red test, reproduce it and ask whether the MODEL is wrong. A handoff's triage is a hypothesis, not a finding. |
| Reading an exit code instead of the result | `gh pr checks --watch` exited 0 while Linux Full Suite had FAILED; I reported green on that basis and had to correct it. | **blocking** | Read the actual check table / failing-node list. Never infer CI state from a wrapper's exit status. |
| Comparing failing COUNTS instead of SETS | Counts move with Docker health and with skip collection; only the node SET is meaningful. | advisory | Capture the failing SET and `comm` it against the base branch's own CI run — not against a local macOS baseline. |
| Prose inserted inside a parsed comment block | `_parse_lab_exceptions()` stops at the first non-entry line after its header. Putting an explanation under the header silently truncated the list and un-declared even the entry that had been correct. | advisory | Keep commentary ABOVE the header. A FORMAT WARNING now sits at that site. |
| The workflow's own placeholder grep false-positives | `grep -l "To be filled\|placeholder\|TBD"` matches prose ASSERTING the absence of placeholders. Five phase docs matched; all were clean. | advisory | Open each hit before reporting a false completion. |

<current_state>
On **`main`** at **`a4eb5a57`**, clean tree, **everything pushed and merged**. Nothing in flight
except one CI run.

Both PRs merged today:
- **#17** `a5ffc9f0` — scoring v3 (999.115) + two `/print` deliverable fixes, 24 commits
- **#18** `a4eb5a57` — the 3 multihost chaos-lab docs/config gates that were red on `main`

**Scoring v3 is live.** `SCORING_VERSION = "3.0"`. Calibration ladder **5/5** against blind-set
operator targets: R1 100 EXCELLENT / R2 78 GOOD / R3 46 FAIR / R4 24 POOR / R5 (the 31-host estate)
**87 -> 18**. Every capped score discloses what capped it, e.g.
*"5 open CRITICAL findings — score limited to 18 (computed 71)."*

**`main` CI went from 5 failing nodes to an expected 1.** Python Staleness Gate and Dashboard
Quality both PASSED on `a4eb5a57`; **Python CI was still `in_progress` when this was written** —
re-check it first thing.

v5.24 phase status: **203, 204, 205 COMPLETE**. **206 PAUSED at 5 of 13** (plans 01-03, 05-06 done;
04 and 07-13 remain). 207 is **operator-led MANUAL by standing decision — an autonomous runner must
not enter it**. 208 not started.
</current_state>

<completed_work>

- **999.115 tasks 19-22** — repaired 11 red scoring tests, bumped `SCORING_VERSION` 2.0 -> 3.0,
  compared the full-suite failing SET against the captured baseline (identical, zero regressions),
  filed P2b.
- **Found and fixed a real model defect** the prior handoff had mislabelled a stale pin — `c47aa6c1`.
- **Promoted 7 properties** xfail -> green (P1, P4, P7a, P7b, ladder R2/R3/R4); kept P2b open and
  strengthened it so it can no longer pass vacuously.
- **`/print` fixed twice** — `93e5afb1` removed the dashboard chrome from every exported client PDF;
  `609c1bbe` made unassessed subscores render `—` instead of empty space. Both verified LIVE against
  a running dashboard via headless Chromium, not only in jsdom.
- **Friday derivation summary** — `.planning/999.115-scoring-derivation-summary.md`, layered
  client-safe narrative (Part 1) then presenter notes (Parts 2-4).
- **`main` pushed** — `origin/main` had diverged (PR #16 merged remotely while this work was local).
  Merged rather than force-pushed; proved content-neutral (`git diff b2050898 HEAD` empty) first.
- **PR #18** closed the three multihost gates, including a genuine bug: both lab files instructed
  `quirk scan --config …` and **there is no `scan` subcommand** — a recurrence of RVW-021.
</completed_work>

<remaining_work>

**Tasks 1-4 are ONE scan, not four runs.** The operator asked for them in this order.

**Task 1 — run a real multihost scan end to end.**
```bash
cd quantum-chaos-enterprise-lab && PROFILE_ARGS="--profile multihost" ./lab.sh up
docker exec chaoslab-mh-prober-1 \
  quirk --config /scan-config.yaml --allow-internal-targets --allow-cleartext-broker-probe
# then, on the host:
quirk serve --db-path quirk-output/quirk.db
```
`--config` is required (no `scan` subcommand, no `--targets`); `--allow-internal-targets` is
required because 10.80.0.0/24 is RFC1918. **Capture the full per-host output** — tasks 2, 3 and 4
all consume it.

**Task 2 — rebuild the oracle's multihost table** in
`quantum-chaos-enterprise-lab/expected_results_v4.md` from the measured output. It currently
documents 10 hosts plus the prober; the scan config targets **31**. Its header also still says
*"TEN INDEPENDENT HOSTS"*, which is stale. See blocking constraint 1.

**Task 3 — add a sixth, NON-SYNTHETIC ladder rung** to `tests/test_score_properties.py` from that
real scan. Four of the five current rungs are constructed; this is the cheapest way to start
retiring that caveat *before* the number is shown to a client. **999.113 D5 still governs — never
tune to a target.**

**Task 4 — rehearse the demo path** and eyeball the exported `/print` PDF. This is the same run as
task 1; confirm it, do not re-scan. Ask the operator whether their Friday walkthrough differs.

**Hygiene (tasks 5-7), all small:**
- **5.** Retire 2 stale pending todos into `.planning/todos/completed/`:
  `readiness-score-denominator-is-probe-count-not-assessable-endpoints.md` (999.113, CLOSED at
  `HORIZON.md:24`) and `score-drivers-leak-from-domains-excluded-from-the-headline.md` (P5c, fixed
  by `1b1e114d` — verified: `category_table` carries drivers on `main` today). 19 pending -> 17.
- **6.** `STATE.md` frontmatter says `completed_phases: 2`; ROADMAP shows **3** (`[x]` on 203/204/205).
  Known `phase.complete` counter defect — hand-correct, do not run the verb.
- **7.** Decide on the BACK-51 gate false positive — the **last red on `main`**. See `<blockers>`.
</remaining_work>

<decisions_made>

- **A test the prior handoff called a stale pin was right, and the model was wrong.** The
  compression guard read `total_score > _ceiling`, applying compression only ABOVE the ceiling. A
  piecewise linear map is not order-preserving: computed 84 emitted 84, computed 85 emitted **71**,
  and nothing in [85,100] ever emitted more than 84 — every well-run estate scored below a mediocre
  one. Fixed by compressing across the whole range. **The ladder moved by zero points**, because all
  five rungs already computed above their own ceilings — exactly why a five-rung ladder could not
  see it and a property test could.
- **Register P2b's xfail rather than delete the marker** — every other `skip_registry` entry records
  an environment gap; P2b is a known open defect kept numeric. The gate checks both directions, so
  the entry cannot outlive the defect.
- **Do NOT promote P2b's `[20]` parameter** despite it XPASSing — it passed by a half-to-even
  rounding coincidence while the computed score still rose.
- **Fix `/print` structurally, not with CSS** — and keep the early return INSIDE
  `status === "authenticated"`, or an authenticated view silently becomes anonymous.
- **Do not resume Phase 206 before the demo** — internal test coverage, zero demo-visible value, and
  the pause reason has not expired.
- **One real scan is the highest-value next action** because it pays three times: oracle rows,
  ladder rung, demo rehearsal.
</decisions_made>

<blockers>
- **`main` CI, 1 red (expected):** `test_backlog_reconciliation_gate.py::test_back_star_ci_enforced_leg`.
  **Diagnosed FALSE POSITIVE** — BACK-51 *is* closed at `HORIZON.md:49`; the gate keys on
  `(ID, title)` and matches a narrative prose line in the **archived** `v5.23-ROADMAP.md`. Filed at
  `.planning/todos/pending/backlog-gate-false-positive-on-archived-roadmap-prose.md`. Task 7 decides:
  fix now (the only thing between `main` and a fully green suite) or leave it filed for v5.25.
- **P2b OPEN by design** — not demo-blocking. Do not promote its xfail.
- **Python CI on `a4eb5a57` was still running** when this was written. Verify on resume.
</blockers>

## Required Reading (in order)

1. `.planning/999.115-scoring-derivation-summary.md` — what the score now means and where it is
   weakest. Part 3 ("Known limits") is the honest list; read it before task 3.
2. `quantum-chaos-enterprise-lab/expected_results_v4.md` § `## Profile: multihost` — the oracle you
   are extending, including its own warning not to claim findings the hosts do not emit.
3. `docs/chaos-lab.md` § `3.32 multihost Profile` — written yesterday; run commands and the
   `/32`-not-`/24` rationale.
4. `tests/test_score_properties.py` header — the `xfail(strict=True)` convention and the
   known-positive control the ladder depends on.
5. `.planning/todos/pending/p2b-healthy-endpoints-dilute-the-readiness-score.md` — the one open
   scoring defect, with a spike-first plan.

## Critical Anti-Patterns (do NOT repeat these)

- **[ANTI-PATTERN] Writing an oracle row you did not measure** → every row traceable to captured
  scan output, or it does not go in.
- **[ANTI-PATTERN] `-k "scor"` as the pre-commit scoring check** → select from the import graph.
- **[ANTI-PATTERN] Trusting a handoff's triage of a red test** → reproduce, then ask whether the
  model is wrong.
- **[ANTI-PATTERN] Inferring CI state from a wrapper's exit code** → read the check table.
- **[ANTI-PATTERN] Tuning to a target** → 999.113 D5. Fitting to blind operator-set bands is the
  ladder's intended use; adjusting weights because a number demos well is not.

## Infrastructure State

- **git**: `main` @ `a4eb5a57`, clean, `origin/main` identical. Tag `pre-999.115-scoring-model`
  marks the pre-scoring-v3 state. Branches `scoring-999.115-model-fix` and
  `fix-chaos-lab-docs-drift` deliberately **left undeleted** — with the tag they keep the backout
  path intact through the demo. Delete after 2026-09-18.
- **Docker**: running. The chaos lab was NOT up when this was written.
  **Gotcha:** a batch run of many `test_chaos_lab_idempotency` profiles produced 13 failures that
  were a resource/collision artifact, not a real regression — the same cases pass in isolation.
  Bring up only the `multihost` profile for task 1.
- **Dashboard**: no server running (one was started on :8519 for `/print` verification, then stopped).
- **Scratch**: `/Users/digs/.claude/jobs/26f4a6a4/tmp/` holds the captured baseline sets, CI failing
  sets, and the `/print` before/after PNG + PDF. Throwaway.

<context>
The frame that matters: 999.95 -> 999.113 -> 999.115 were three instances of one defect class, each
found by accident rather than by a test, because ~3,200 lines of scoring tests asserted mechanism,
bounds and rendering but never MEANING. That instrument now exists, and scoring v3 was chosen by
measurement against it rather than by argument.

What remains is the honest tail of that work. The ladder's constants are a **first fit** on four
synthetic rungs and one real estate; the model's *shape* is well evidenced and its *numbers* are not
yet. Task 3 is the first real datum. That is why a measured scan outranks resuming Phase 206 with
four days to the demo — it is the difference between a number you can defend and a number you merely
computed.

The demo is **2026-09-18**. Scoring v3 moves every score the product has ever emitted, downward and
substantially; the operator chose that with eyes open, and the three-layer backout exists for
exactly that reason.
</context>

<next_action>
First: re-check Python CI on `a4eb5a57` (`gh run list --branch main --limit 8`) and confirm the
failing set is the single BACK-51 false positive.

Then task 1 — bring up ONLY the `multihost` profile and run the real scan from `mh-prober`,
capturing the full per-host output. Tasks 2, 3 and 4 all consume that one run. Do not hand-write an
expected finding for any host the scan did not produce.
</next_action>
