# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg
.eggs/

# Virtual environments
.venv/
.venv2/
venv/
env/
ENV/

# SQLite databases
*.db
*.sqlite
*.sqlite3

# Output and data directories
output/
data/
tmp/
recordings/
quirk-output/

# Phase 196 HOUSE-01 — ad-hoc scan output dirs from lab/UAT runs; reproducible, evidence archived under .planning/milestones/
/output-*/
# Phase 196 HOUSE-01 — ad-hoc lab scan configs live in quantum-chaos-enterprise-lab/scan-configs/, never repo root
/config-lab-*.yaml

# Local scratch / build artifacts
.clone/
.vite/

# Claude / GSD tooling
.claude/

# macOS
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db

# pytest cache
.pytest_cache/
.cache/

# Coverage
.coverage
htmlcov/
coverage.xml

# Editor
.idea/
.vscode/
*.swp
*.swo
*~

# Environment variables
.env
.env.*

# Phase 120 / PUBREPO-PLANNING-EXCL — internal planning artifacts excluded from public repo.
# History rewrite to remove prior tracked copies happens in Plan 120-04.
.planning/

# Phase 120 / PUBREPO-DEV-ONLY — dev-only artifacts excluded from public repo.
# History stripped in Plan 120-04's second filter-repo pass.
# Visual Studio / VS Code IDE auto-generated state
.vs/
.vscode/extensions.json
# Local scan output cache (working dir for ./run_scan.py)
output/
# UAT runner output blob
uat-auto-results.json
# Historical / internal-only top-level memos
RESTRUCTURE-AUDIT-2026-03-31.md
SECURITY_TEAM_MEMO.md
# Design exploration (preserved in pre-rewrite mirror clone)
design/
# AI / Claude instruction files (kept on disk for local dev; not for public repo)
CLAUDE.md
AGENTS.md

# Local browser-automation artifacts (Claude Code Playwright MCP)
.playwright-mcp/
